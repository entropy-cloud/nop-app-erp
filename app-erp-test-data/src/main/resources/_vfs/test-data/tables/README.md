# 本目录存放跨测试类共享的主数据夹具（CSV）。
#
# 命名约定：每个文件对应一张主数据表，文件名 = 表名.csv（如 erp_md_material.csv）。
# 格式对齐平台 _cases/input/tables/*.csv 约定（nop-entropy/docs-for-ai/02-core-guides/testing.md）。
# 加载顺序由同级 load-order.txt 指定。
#
# 注意：本目录当前为骨架占位，不含具体 CSV。首批夹具随各域 BizModel 测试深化时补充。
