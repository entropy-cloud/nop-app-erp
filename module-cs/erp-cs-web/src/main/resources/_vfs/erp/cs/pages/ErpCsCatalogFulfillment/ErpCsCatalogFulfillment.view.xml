<?xml version="1.0" encoding="UTF-8" ?>
<view x:extends="_gen/_ErpCsCatalogFulfillment.view.xml" x:schema="/nop/schema/xui/xview.xdef"
      xmlns:x="/nop/schema/xdsl.xdef">

    <grids>
                <grid id="list">
            <cols x:override="bounded-merge">
                <col id="id" mandatory="true" ui:number="true" sortable="true"/>
                <col id="code" mandatory="true" sortable="true"/>
                <col id="orgId" label="业务组织"/>
                <col id="catalogItemId" label="目录项"/>
                <col id="sequence" ui:number="true" sortable="true"/>
                <col id="actionType" sortable="true"/>
                <col id="assignToRole" sortable="true"/>
                <col id="estimatedDuration" ui:number="true" sortable="true"/>
                <col id="isMandatory" sortable="true"/>
            </cols>
        </grid>
        <grid id="pick-list"/>
    </grids>

    <forms>
        <form id="view" size="lg">
            <layout x:override="replace">
=========>baseInfo[基本信息]======
 code[编码] orgId[业务组织]
 catalogItemId[目录项] sequence[执行顺序]
=========>action[执行动作]======
 actionType[动作类型] actionConfig[动作配置]
 assignToRole[执行角色] estimatedDuration[预估时长(分钟)]
 isMandatory[是否必须执行]
========^audit[审计信息]=========
 createdBy[创建人] createTime[创建时间]
 updatedBy[修改人] updateTime[修改时间]
            </layout>
        </form>
        <form id="edit" size="lg">
            <layout x:override="replace">
=========>baseInfo[基本信息]======
 code[编码] orgId[业务组织]
 catalogItemId[目录项] sequence[执行顺序]
=========>action[执行动作]======
 actionType[动作类型] actionConfig[动作配置]
 assignToRole[执行角色] estimatedDuration[预估时长(分钟)]
 isMandatory[是否必须执行]
========^audit[审计信息]=========
 createdBy[创建人] createTime[创建时间]
 updatedBy[修改人] updateTime[修改时间]
            </layout>
        </form>
        <form id="add" x:prototype="edit"/>
        <form id="query" editMode="query" title="查询条件">
            <layout>
 code[编码] orgId[业务组织]
 catalogItemId[目录项] actionType[动作类型]
 isMandatory[是否必须执行]
            </layout>
            <cells>
                <cell id="code" filterOp="like"/>
                <cell id="orgId" filterOp="eq"/>
                <cell id="catalogItemId" filterOp="eq"/>
                <cell id="actionType" filterOp="eq"/>
                <cell id="isMandatory" filterOp="eq"/>
            </cells>
        </form>
    </forms>

    <pages>
        <crud name="main"/>
        <picker name="picker"/>
    </pages>
</view>