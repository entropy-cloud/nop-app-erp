<?xml version="1.0" encoding="UTF-8" ?>
<view x:extends="_gen/_ErpCsSlaPolicy.view.xml" x:schema="/nop/schema/xui/xview.xdef" xmlns:x="/nop/schema/xdsl.xdef">

    <grids>
                <grid id="list">
            <cols x:override="bounded-merge">
                <col id="id" mandatory="true" ui:number="true" sortable="true"/>
                <col id="code" mandatory="true" sortable="true"/>
                <col id="name" sortable="true"/>
                <col id="ticketTypeId" label="工单类型"/>
                <col id="minPriority" sortable="true"/>
                <col id="teamId" label="客服团队"/>
                <col id="resolveHours" ui:number="true" sortable="true"/>
                <col id="resolveDays" ui:number="true" sortable="true"/>
                <col id="isWorkingDays" sortable="true"/>
                <col id="escalationUserId" sortable="true"/>
            </cols>
        </grid>
        <grid id="pick-list"/>
    </grids>

    <forms>
        <form id="view" size="lg">
            <layout x:override="replace">
=========>baseInfo[基本信息]======
 code[编码] name[名称]
 ticketTypeId[适用工单类型] minPriority[最低触发优先级]
=========>policy[SLA时限策略]======
 resolveHours[解决时限(小时)] resolveDays[解决时限(天)]
 isWorkingDays[仅计算工作日]
=========>escalate[升级配置]======
 escalationUserId[升级通知人] teamId[适用团队]
========^audit[审计信息]=========
 description[说明]
 createdBy[创建人] createTime[创建时间]
 updatedBy[修改人] updateTime[修改时间]
            </layout>
        </form>
        <form id="edit" size="lg">
            <layout x:override="replace">
=========>baseInfo[基本信息]======
 code[编码] name[名称]
 ticketTypeId[适用工单类型] minPriority[最低触发优先级]
=========>policy[SLA时限策略]======
 resolveHours[解决时限(小时)] resolveDays[解决时限(天)]
 isWorkingDays[仅计算工作日]
=========>escalate[升级配置]======
 escalationUserId[升级通知人] teamId[适用团队]
========^audit[审计信息]=========
 description[说明](2)
 createdBy[创建人] createTime[创建时间]
 updatedBy[修改人] updateTime[修改时间]
            </layout>
        </form>
        <form id="add" x:prototype="edit"/>
        <form id="query" editMode="query" title="查询条件">
            <layout>
 code[编码] name[名称]
 ticketTypeId[适用工单类型] teamId[适用团队]
 minPriority[最低触发优先级]
            </layout>
            <cells>
                <cell id="code" filterOp="like"/>
                <cell id="name" filterOp="like"/>
                <cell id="ticketTypeId" filterOp="eq"/>
                <cell id="teamId" filterOp="eq"/>
                <cell id="minPriority" filterOp="eq"/>
            </cells>
        </form>
    </forms>

    <pages>
        <crud name="main"/>
        <picker name="picker"/>
    </pages>
</view>