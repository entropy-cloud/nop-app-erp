<?xml version="1.0" encoding="UTF-8" ?>
<view x:extends="_gen/_ErpMdPartner.view.xml" x:schema="/nop/schema/xui/xview.xdef" xmlns:x="/nop/schema/xdsl.xdef">

    <grids>
        <grid id="list"/>
        <grid id="pick-list">
            <cols x:override="bounded-merge">
                <col id="id" mandatory="true" ui:number="true" sortable="true"/>
                <col id="code" mandatory="true" sortable="true"/>
                <col id="name" mandatory="true" sortable="true"/>
                <col id="partnerType" sortable="true"/>
                <col id="taxNo" sortable="true"/>
                <col id="creditLimit" ui:number="true" sortable="true"/>
                <col id="status" sortable="true">
                    <gen-control>
                        <c:script><![CDATA[
                            const valueProp = dispMeta?.id || 'status';
                            const labelProp = propMeta?.['graphql:labelProp'] || (valueProp + '_label');
                            return {
                                type: "tpl",
                                tpl: '<span class="label label-${' + valueProp
                                    + " == 'ACTIVE' ? 'success' : 'default'}\">"
                                    + "${" + labelProp + "}</span>"
                            };
                        ]]></c:script>
                    </gen-control>
                </col>
            </cols>
        </grid>
    </grids>

    <forms>
        <form id="view">
            <layout x:override="replace">
=========>baseInfo[基本信息]======
 code[编码] name[名称]
 partnerType[类型] status[状态]
 customerGroup[客户组]
=========>contact[联系方式]======
 contactPerson[联系人] phone[电话]
 email[邮箱] address[地址]
==========^credit[信用与余额]=========
 taxNo[税号] creditLimit[信用额度]
 creditPeriodDays[信用期(天)] receivableBalance[应收余额]
 payableBalance[应付余额]
==========^audit[审计信息]=========
 remark[备注]
 createdBy[创建人] createTime[创建时间]
 updatedBy[修改人] updateTime[修改时间]
            </layout>
        </form>
        <form id="edit">
            <layout x:override="replace">
=========>baseInfo[基本信息]======
 code[编码] name[名称]
 partnerType[类型] status[状态]
 customerGroup[客户组]
=========>contact[联系方式]======
 contactPerson[联系人] phone[电话]
 email[邮箱] address[地址]
 taxNo[税号] creditLimit[信用额度]
 creditPeriodDays[信用期(天)] receivableBalance[应收余额]
 payableBalance[应付余额]
 remark[备注](2)
            </layout>
        </form>
        <form id="add"/>
        <form id="query" editMode="query" title="查询条件">
            <layout>
 code[编码] name[名称]
 status[状态] partnerType[类型]
            </layout>
            <cells>
                <cell id="code" filterOp="like"/>
                <cell id="name" filterOp="like"/>
                <cell id="status" filterOp="eq"/>
                <cell id="partnerType" filterOp="eq"/>
            </cells>
        </form>
        <form id="pick-query" editMode="query" title="查询条件">
            <layout>
 code[编码] name[名称]
 partnerType[类型] status[状态]
            </layout>
            <cells>
                <cell id="code" filterOp="like"/>
                <cell id="name" filterOp="like"/>
                <cell id="partnerType" filterOp="eq"/>
                <cell id="status" filterOp="eq"/>
            </cells>
        </form>
    </forms>

    <pages>
        <crud name="main"/>
        <picker name="picker" filterForm="pick-query"/>
    </pages>
</view>