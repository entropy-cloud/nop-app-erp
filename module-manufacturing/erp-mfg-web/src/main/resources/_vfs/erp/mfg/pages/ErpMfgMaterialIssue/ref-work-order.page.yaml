# fixedProps用于指定关联属性，表示这是一个关联子表的crud页面。在本页面中workOrderId字段的值固定为传入的值。
# 父页面（ErpMfgWorkOrder 列表行）通过 row-action drawer 打开本页，data.workOrderId 注入到子表上下文。
x:gen-extends: |
  <web:GenPage view="ErpMfgMaterialIssue.view.xml" page="main" fixedProps="workOrderId" xpl:lib="/nop/web/xlib/web.xlib" />
