<?xml version="1.0" encoding="UTF-8" ?>
<view x:extends="_gen/_ErpMfgBom.view.xml" x:schema="/nop/schema/xui/xview.xdef" xmlns:x="/nop/schema/xdsl.xdef">

    <grids>
        <grid id="list">
            <cols x:override="bounded-merge">
                <col id="id" mandatory="true" ui:number="true" sortable="true"/>
                <col id="code" mandatory="true" sortable="true"/>
                <col id="productId" label="产品"/>
                <col id="bomType" mandatory="true" sortable="true"/>
                <col id="versionLabel" sortable="true"/>
                <col id="qty" ui:number="true" sortable="true"/>
                <col id="isActive" sortable="true"/>
                <col id="isDefault" sortable="true"/>
            </cols>
        </grid>
        <grid id="pick-list"/>
    </grids>

    <forms>
        <form id="view">
            <layout x:override="replace">
==========>baseInfo[基本信息]======
 code[BOM编码] productId[产品]
 bomType[BOM类型] versionLabel[版本号]
 qty[BOM数量] isActive[是否有效]
 isDefault[是否默认] consumption[消耗控制]
 useMultiLevelBom[展开多层BOM] inspectionRequired[需要质检]
==========^audit[审计信息]=========
 remark[备注]
 createdBy[创建人] createTime[创建时间]
 updatedBy[修改人] updateTime[修改时间]
            </layout>
        </form>
        <form id="edit">
            <layout x:override="replace">
==========>baseInfo[基本信息]======
 code[BOM编码] productId[产品]
 bomType[BOM类型] versionLabel[版本号]
 qty[BOM数量] isActive[是否有效]
 isDefault[是否默认] consumption[消耗控制]
 useMultiLevelBom[展开多层BOM] inspectionRequired[需要质检]
 remark[备注](2)
            </layout>
        </form>
        <form id="add"/>
        <form id="query" editMode="query" title="查询条件">
            <layout>
 code[BOM编码] productId[产品]
 isActive[是否有效]
            </layout>
            <cells>
                <cell id="code" filterOp="like"/>
            </cells>
        </form>
    </forms>

    <pages>
        <crud name="main"/>
        <picker name="picker"/>
    </pages>
</view>