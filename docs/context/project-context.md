# Project Context

## Purpose

Keep this file as the shortest low-churn context an AI agent needs before doing useful work.

This file is not an execution dashboard. Do not mirror active plans, current blockers, current backlog rows, or dated audit/log status here. Discover those from their owning files when needed.

## Project Identity

- Project name: nop-app-erp
- Product type: enterprise resource planning (ERP) application skeleton built on the Nop Platform
- Primary users: ERP system operators/admins and developers learning Nop Platform from a realistic business domain app
- Documentation freshness: `fresh` (bootstrap docs just initialized; ORM model not yet designed)

**Freshness gating:**

- If freshness is `stale` or `unknown`, agents may research, audit, and draft alignment docs, but must not implement product behavior until the baseline is re-established or a human confirms intended behavior.
- If freshness is `partially stale`, agents may implement only slices whose requirement, owner doc, codebase-map route, and touched code area have been verified fresh; otherwise treat the slice as `plan-first` or `research-only`.
- AI may not mark stale docs fresh without human confirmation or human-approved owner-doc evidence.

## Current Technical Baseline

- Frontend stack: Baidu AMIS (JSON-driven UI in `.view.xml` files) — available after codegen
- Backend stack: Java 17+, Quarkus, Nop Platform (nop-entropy 2.0.0-SNAPSHOT) — available after codegen
- Database/model source: XML model in `model/app-erp.orm.xml`; `model/app-erp.api.xml` will be generated during/after codegen

## Current Project Stage

`nop-app-erp` is in the **bootstrap stage**. Only the AGE documentation structure and an empty ORM model skeleton exist. The Maven multi-module project (api/codegen/dao/service/web/app/delta/meta) and Java sources will be generated by `nop-cli` after the ORM model is designed. See "Current Project Stage" in `AGENTS.md`.

Until the ORM model and generated modules exist:

- do not invent Java module paths, package names, or view paths as if they already exist
- design and discussion work should focus on `model/app-erp.orm.xml` and `docs/design/`, `docs/architecture/`, `docs/requirements/`
- the verification commands below are not yet executable; they become valid only after the first codegen pass

## Verification Commands

> **Bootstrap-stage note:** commands below target the future generated multi-module project. They are NOT executable yet because `mvn` modules and Java sources do not exist. They become valid only after the first `nop-cli` codegen pass completes. Do not report verification success until the modules exist and these commands are run against them.

| Purpose                   | Command                                                                                                              |
| ------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| Install dependencies      | `mvn dependency:resolve -DskipTests` (requires nop-entropy parent built first; available after codegen)             |
| Run app locally           | `mvn clean package -DskipTests && java -Dfile.encoding=UTF8 -Dquarkus.profile=dev -jar app-erp-app/target/app-erp-app-1.0-SNAPSHOT-runner.jar` |
| Typecheck / compile check | `mvn compile -DskipTests`                                                                                            |
| Build                     | `mvn clean package -DskipTests`                                                                                      |
| Lint / static check       | `none`                                                                                                               |
| Unit tests                | `mvn test`                                                                                                           |
| E2E / integration tests   | `none`                                                                                                               |
| Generate project (once)   | `java -jar nop-cli.jar gen -t=/nop/templates/orm model/app-erp.orm.xlsx` (run from project root after ORM design)    |

## Optional Layers Currently In Use

Mark only the optional layers this project actually maintains.

- [ ] `docs/discussions/`
- [ ] `docs/audits/`
- [ ] `docs/testing/`
- [ ] `docs/skills/`
- [ ] `docs/analysis/`
- [ ] `docs/retrospectives/`
- [ ] `docs/lessons/`

## AI Block Conditions

AI MUST stop and wait for human input before proceeding when:

- verification commands are all placeholders and cannot be inferred from the project
- any change touches data-deletion, accounting/finance, or other ERP protected areas with no existing test coverage and no owner doc describing expected behavior
- any change modifies XML models (`model/*.orm.xml`, `model/*.api.xml`) without explicit human approval — these drive code generation
- no requirement or owner doc describes the intended behavior of the change — do not implement into a vacuum (whether a requirement/owner doc exists is checked against `docs/requirements/` and `docs/design/`, not a field here)

These are project-specific hard stops in addition to `AGENTS.md`, `docs/context/ai-autonomy-policy.md`, source-of-truth conflict rules, and required plan/closure audit rules.

For ambiguity that does not affect user-visible behavior, contracts, protected areas, or closure evidence, resolve by writing assumptions into the relevant doc and proceed according to the autonomy policy. Mark uncertain assumptions explicitly so humans can review later.

## Notes For AI Agents

- If this file is empty or stale, ask for or create a context update before large implementation work.
- **Current work in progress**: inspect unfinished plans in `docs/plans/`, not this file.
- AI autonomy defaults to `implement`; it is gated by freshness (above) and Protected Areas (`ai-autonomy-policy.md`). No per-slice autonomy value is maintained here — autonomy labels live on backlog/roadmap work items, not in this file.
- AI may correct factual context from live repo evidence, but must not mark stale docs fresh or downgrade protected areas without human confirmation.
- Do not report verification success while commands still contain `<fill real command>` placeholders or target non-existent modules.
- Building requires `nop-entropy` parent POM to be available in local Maven repository first.
- The current focus is ORM model design in `model/app-erp.orm.xml`; do not invent Java module/package/view paths until codegen runs.
