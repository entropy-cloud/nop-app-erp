# rc-ma1-a1-51-notify-full notify 通知派发子系统需求-实现符合性审计报告

> Mission: requirement-compliance · 里程碑: MA1 · 工作项: A1.51
> 审计对象: notify 通知派发子系统（跨域子系统，appName `erp-notify`，二级简称 `sys`）
> UC 覆盖: UC-SYS-01~07（7 UC 全覆盖，无跳号）
> 方法论: `docs/audits/requirement-compliance-methodology.md`（§1-§10 + §去重协议 + §4 三判据）
> 报告骨架: §6 九段落（§1-§9）
> 真相源层级: L1 `docs/design/notify/use-cases.md`（0.2 补写，权威） > L2 owner doc（`README.md`+`inbox-patterns.md`+`docs/architecture/notification-strategy.md`，设计参考非真相源）
> 审计性质: 只读审计（无代码/ORM/api.xml/view.xml/真相源变更）
> 生成日期: 2026-08-06

---

## 1. 需求契约原文（L1，逐字引用）

L1 真相源 = `docs/design/notify/use-cases.md`（来源可追溯 README.md + inbox-patterns.md + notification-strategy.md 三份权威文档；补写纪律声明明确"未参照运行时代码"）。逐 UC 逐字摘录验收标准原文：

### UC-SYS-01 通知模板生命周期与查找（`use-cases.md:28-44`）
```
模板(ErpSysNotificationTemplate) subjectTpl/bodyTpl 为 ${var} 插值模板
按业务事件键 notificationType 查找模板
仅 status=ACTIVE 的模板参与派发（DRAFT 不参与）

业务事件无 ACTIVE 模板时:
  config-gated 静默跳过（WARN），不抛错给调用方

模板状态迁移: DRAFT → ACTIVE
```

### UC-SYS-02 通知实例端到端派发链（`use-cases.md:48-70`）
```
统一派发入口: IErpSysNotificationBiz.notify(eventType, context)
派发链顺序（不可乱序）:
  1. 查找 ACTIVE 模板（按 notificationType）
  2. 模板渲染（${var} 插值）
  3. 接收人解析（RecipientResolver）
  4. 频控合并（MergeCoordinator）
  5. 站内消息落库（ErpSysNotification）
  6. config-gated 外发通道派发

站内消息派发即落 ErpSysNotification 表（status=SENT）

子系统对业务方是 best-effort 服务:
  通知派发失败不回滚调用方业务事实
  notify() 内部 try/catch + config-gated 静默跳过
```

### UC-SYS-03 接收人多策略解析（`use-cases.md:74-94`）
```
recipientResolver = ROLE:
  角色名 → NopAuthRole → NopAuthUserRole → userId（复用平台 nop-auth）

recipientResolver = ORG:
  deptId → NopAuthUser

recipientResolver = USER_LIST:
  模板配置 userId 列表（静态 + 支持 ${var} 从 context 插值，如提单人 submitterUserId）

recipientResolver = PARTNER:
  partnerId → 用户映射
  （占位：partner→user 映射未建立，WARN 返回空，config-gated）
```

### UC-SYS-04 频控窗口合并（`use-cases.md:98-119`）
```
合并组 key = notificationType + "#" + recipientUserId

频控窗口（ErpNotifyConstants）:
  业务提醒: 5 分钟（300s）— 同一用户、同一类型合并为一条
  异常告警: 1 分钟（60s）— 同一错误类型合并，包含发生次数
  系统通知: 不合并 — 每条独立发送

命中窗口内既有实例:
  合并（mergeCount += 1），实例 status = MERGED
未命中:
  新建实例（status = SENT）

总开关: erp-notify.merge-enabled（默认 true）
```

### UC-SYS-05 已读状态管理（`use-cases.md:123-145`）
```
markRead(notificationId):
  写 ErpSysNotificationRead 记录
markAllRead:
  批量标记当前用户全部未读为已读

唯一键 (notificationId, userId) 防重复标记已读

已读状态派生自 ErpSysNotificationRead 关联:
  ErpSysNotificationRead 存在该 (notificationId, userId) 行 → 已读
  不存在 → 未读
  （非 ErpSysNotification.status lifecycle 字段）

countUnread:
  实时统计当前用户未读数
```

### UC-SYS-06 用户面收件箱后端查询（`use-cases.md:149-170`）
```
findUnread / countUnread / markAllRead 三方法 userId 参数扩展为 @Optional:
  留空时回退 ctx.getUserId()（前端不传 userId，后端从 JWT 鉴权上下文解析）
  显式传 userId 时仍按 userId 过滤（向后兼容 admin 跨用户场景与单元测试）

未读 tab (findUnread):
  recipientUserId == userId 且 id NOT IN ErpSysNotificationRead
已读 tab (findRead):
  ErpSysNotificationRead.userId == userId 反查 ErpSysNotification
全部 tab:
  客户端拼接 findUnread + findRead 去重

禁用 <filter><eq name="status" value="READ"/>:
  ErpSysNotification.status 是通知 lifecycle（SENT/MERGED/FAILED），非已读状态
```

### UC-SYS-07 外发通道 config-gated 降级（`use-cases.md:174-203`）
```
站内消息 (IN_APP):
  默认通道，派发即落 ErpSysNotification 表

邮件 (EMAIL):
  经 nop-integration 的 IEmailSender.sendEmail(EmailMessage) SPI 派发
短信 (SMS):
  经 nop-integration 的 ISmsSender.sendMessage(SmsMessage) SPI 派发
  （不新建平行通道适配层，与平台默认路线一致）

config-gated 配置项（命名空间 erp-notify.*）:
  erp-notify.enabled        默认 true   通知总开关
  erp-notify.email-enabled  默认 false  邮件通道启用
  erp-notify.sms-enabled    默认 false  短信通道启用

无供应商 / 通道未启用时:
  WARN 跳过，不阻断业务事实
  （不抛出给调用方；各业务消费者额外在调用外包 try/catch warn-and-continue）

失败降级:
  notify() 整体 try/catch
  模板缺失/渲染失败/接收人解析失败/落库失败 均不抛出给调用方（返回空列表）
```

---

## 2. 实现证据（L3，代码路径 + 行为断言）

实现位于 `module-notify/erp-notify-service/src/main/java/app/erp/notify/service/`。关键组件方法锚点（行号写时实测导航，方法名锚点稳定）：

| 组件 | 方法锚点 | 关键行为断言 |
|------|---------|------------|
| `entity/ErpSysNotificationBizModel.java` | `notify(@BizMutation):54` / `markRead(@BizMutation):62` / `markAllRead(@BizMutation @Optional userId):68` / `findUnread(@BizQuery @Optional):74` / `findRead(@BizQuery @Optional):80` / `countUnread(@BizQuery @Optional):106` / `resolveUserId:112`（留空回退 `ctx.getUserId()`）/ `unreadOf:119`（`recipientUserId==userId` + `in(status, [SENT,MERGED])` + `notIn(id, readIds)`，**不**用 `status=READ` filter） | 薄委派层；@Optional userId 回退 ctx；已读状态派生自 Read 关联非 status |
| `processor/ErpSysNotificationNotifyProcessor.java` | `notify:40`（整体 try/catch 返回 `Collections.emptyList()` best-effort）/ `findActiveTemplate:65`（`eq("notificationType", eventType)` + `eq("status", TEMPLATE_ACTIVE)` + `limit 1`） | 查找 ACTIVE 模板；无模板 LOG.warn 返回空；best-effort 不阻断调用方 |
| `dispatch/NotificationDispatcher.java` | `dispatch:76`（顺序：`renderTemplate` subject+body → `recipientResolver.resolve` → `parseChannelSet` → 循环 `mergeOrPersist` → `dispatchExternalChannels`）/ `mergeOrPersist:114`（读 `CONFIG_NOTIFY_MERGE_ENABLED` 默认 true + `MERGE_BY_USER_TYPE` 判定 → `mergeCoordinator.findMergeable` 命中则 `mergeInto` / 未命中 `newEntity` 设 `STATUS_SENT` + `mergeGroupId=notificationType+"#"+userId` + `mergeCount=1` + `sentAt`）/ `dispatchExternalChannels:150`（EMAIL 经 `CONFIG_NOTIFY_EMAIL_ENABLED` 默认 false 门控 → `sendEmailIfPossible`；SMS 经 `CONFIG_NOTIFY_SMS_ENABLED` 默认 false 门控 → `sendSmsIfPossible`；未启用 LOG.warn 跳过）/ `renderTemplate:206`（`VAR_PATTERN=\$\{...\}` 正则从 context 插值，失败抛 `ERR_NOTIFY_RENDER_FAILED`）/ `sendEmailIfPossible:171`（`emailSender.sendEmail(EmailMessage)` SPI）/ `sendSmsIfPossible:187`（`smsSender.sendMessage(SmsMessage)` SPI） | 派发链顺序不可乱序；status=SENT；config-gated 外发；${var} 插值；SPI 复用 nop-integration 不新建平行适配层 |
| `dispatch/NotificationMergeCoordinator.java` | `findMergeable:45`（`MERGE_NONE` 或 window≤0 返回 null；窗口 `createTime >= now-window`；`in(status, [PENDING,SENT,MERGED])`；排除已读 `isRead`）/ `mergeInto:82`（`mergeCount+=1` + `setStatus(STATUS_MERGED)`） | 频控窗口合并；命中 MERGED+mergeCount；未命中由调用方新建 SENT |
| `dispatch/NotificationRecipientResolver.java` | `resolve:83`（先 `interpolateConfig` ${var} 插值再解析）/ `resolveRole:117`（角色名→`in("roleName",...)` NopAuthRole→`in("roleId",...)` NopAuthUserRole→userId）/ `resolveOrg:158`（`eq("deptId",...)` NopAuthUser）/ `resolveUserList:103`（cfg.userIds 列表）/ `resolvePartner:175`（占位 LOG.warn 返回空 config-gated）/ `interpolateConfig:206`（recipientConfig ${var} 从 context 插值） | 四类 resolver 全实现；USER_LIST 支持动态 ${var}；PARTNER 占位符合 L1 |
| `dispatch/NoopEmailSender.java` / `NoopSmsSender.java` | `sendEmail(EmailMessage)` / `sendMessage(SmsMessage)` | 无供应商时 no-op 默认 bean 满足 IoC；不实际发送仅 DEBUG 日志；config-gated 默认不调用 |
| `processor/ErpSysNotificationMarkReadProcessor.java` | `markRead:14`（优先 `recipientUserId` 回退 `ctx.getUserId()` + `isRead` 判重 + `saveEntity(newReadEntry)`） | 写 ErpSysNotificationRead；判重防重复 |
| `processor/ErpSysNotificationMarkAllReadProcessor.java` | `markAllRead:16`（`resolveUserId` + `unreadOf` 列表 + 逐条 `isRead` 判重写入） | 批量标记全部未读 |
| `processor/AbstractErpSysNotificationProcessor.java` | `isRead:47`（`eq("notificationId",..)` + `eq("userId",..)`）/ `unreadOf:55` / `newReadEntry:78` | 共享 helper 单一真相源 |
| `ErpNotifyConstants.java` | `CHANNEL_IN_APP/EMAIL/SMS`、`STATUS_PENDING/SENT/MERGED/FAILED`、`RESOLVER_ROLE/ORG/PARTNER/USER_LIST`、`MERGE_NONE/MERGE_BY_USER_TYPE`、`TEMPLATE_DRAFT/ACTIVE`、`MERGE_GROUP_SEP="#"`、`DEFAULT_BUSINESS_REMIND_WINDOW_SECONDS=300`、`DEFAULT_ALERT_WINDOW_SECONDS=60` | 字典码值权威 |
| `ErpNotifyConfigs.java` | `CONFIG_NOTIFY_ENABLED`(默认 true) / `CONFIG_NOTIFY_EMAIL_ENABLED`(默认 false) / `CONFIG_NOTIFY_SMS_ENABLED`(默认 false) / `CONFIG_NOTIFY_MERGE_ENABLED`(默认 true) | 配置项声明 |

**ORM 权威**（`module-notify/model/app-erp-notify.orm.xml`）：
- `ErpSysNotificationTemplate`（`:52`）：`notificationType`/`channelSet`(default IN_APP)/`subjectTpl`/`bodyTpl`/`recipientResolver`(default ROLE)/`recipientConfig`/`mergeWindowSeconds`(default 300)/`mergeStrategy`(default MERGE_BY_USER_TYPE)/`status`(default DRAFT)；`UK_SYS_NOTIFY_TPL_TYPE` on `notificationType`（`:76`）。
- `ErpSysNotification`（`:86`）：`templateId`/`notificationType`/`recipientUserId`/`status`(default PENDING)/`mergeGroupId`/`mergeCount`(default 1)；索引（非 UK）`IDX_SYS_NOTIFY_TYPE_USER` on (notificationType, recipientUserId)。
- `ErpSysNotificationRead`（`:131`）：`notificationId`/`userId`/`readTime`；**`UK_SYS_NOTIFY_READ_NOTIF_USER` on (notificationId, userId)（`:150`）**——UC-SYS-05 唯一键防重复有 DB UK 兜底。

---

## 3. 测试证据（L4，断言强度）

测试位于 `module-notify/erp-notify-service/src/test/java/app/erp/notify/service/`（均 `JunitAutoTestCase` + `localDb` + `IGraphQLEngine` RPC 端到端）：

| 测试文件#method | 覆盖 UC | 断言强度 |
|---------------|--------|---------|
| `TestErpSysNotificationTemplateLifecycle#testActiveTemplateDispatches:40` | UC-SYS-01 | **强断言**（ACTIVE 模板→notify 落库 1 行 + status=SENT + subject 渲染） |
| `TestErpSysNotificationTemplateLifecycle#testDraftTemplateSkipsDispatch:58` | UC-SYS-01 | **强断言**（DRAFT 模板→notify 后 list.isEmpty() DRAFT 不参与派发） |
| `TestErpSysNotificationTemplateLifecycle#testTemplateSubjectRendering:74` | UC-SYS-01/02 | **强断言**（${orderNo}/${customerName}/${amount} 多占位符 subject+body 渲染逐字比对） |
| `TestErpSysNotificationDispatch#testNotifyPersistsInAppAndRendered:54` | UC-SYS-02 | **强断言**（notify→落库 + status=SENT + channel=IN_APP + subject/body 渲染 + payloadJson + findUnread 返回） |
| `TestErpSysNotificationDispatch#testNoActiveTemplateSilentSkip:143` | UC-SYS-01/02 | **强断言**（无 ACTIVE 模板→status==0 不抛错 + 静默跳过 best-effort） |
| `TestErpSysNotificationDispatch#testMergeWithinWindowMerges:78` | UC-SYS-04 | **强断言**（窗口内二次 notify→1 行 + mergeCount>=2 + body 含 `[合并`） |
| `TestErpSysNotificationDispatch#testMergeOutsideWindowCreatesNew:96` | UC-SYS-04 | **强断言**（首条 createTime 推 2h 前→第二次落窗口外→2 行新建） |
| `TestErpSysNotificationDispatch#testEmailChannelConfigGatedSkipsAndMarkRead:120` | UC-SYS-05/07 | **强断言**（EMAIL channelSet 但 email-enabled=false 默认→status==0 不抛错 + IN_APP 仍落库 + countUnread 翻转 + markRead 后归零 + findUnread 不含已读） |
| `TestErpSysNotificationSubscription#testMarkAllReadClearsUnread:41` | UC-SYS-05 | **强断言**（3 条未读→markAllRead→countUnread==0） |
| `TestErpSysNotificationSubscription#testFindUnreadReturnsOnlyUnread:62` | UC-SYS-05/06 | **强断言**（markRead 单条→countUnread 递减 + DB 仍 2 条 + 第二条未标记） |
| `TestErpSysNotificationSeedTemplates#testBusinessRemindSlaOverdueMergesToSingle:57` | UC-SYS-04 | **强断言**（业务提醒 300s 窗口 + MERGE_BY_USER_TYPE→1 条 mergeCount>=2） |
| `TestErpSysNotificationSeedTemplates#testAlertPostingExceptionMergesWithCount:78` | UC-SYS-04 | **强断言**（异常告警 60s 窗口 + MERGE→1 条 mergeCount==3 含次数） |
| `TestErpSysNotificationSeedTemplates#testSystemNoticeCreditOverNoMerge:97` | UC-SYS-04 | **强断言**（系统通知 MERGE_NONE→2 条独立 + mergeCount==1） |
| `TestErpSysNotificationSeedTemplates#testEndToEndMarkReadAndFindUnreadAcrossTypes:118` | UC-SYS-05 | **强断言**（NONE 策略 2 条→markAllRead 处理 2 条→countUnread==0 + findUnread 空） |
| `TestErpSysNotificationCrossDomain#testPostingAlertNotification:40` / `#testSlaOverdueNotification:61` / `#testMultipleEventTypesIndependent:81` | UC-SYS-01/02 | **强断言**（多事件类型独立落库 + 跨域事件键渲染） |

**L4 小结**：5 测试文件共 18 @Test，覆盖 UC-SYS-01~07 主路径与边界场景，断言强度均为**强断言**（验证落库/status/渲染/合并/已读翻转/配置门控跳过，非仅冒烟 status==0）。MA5 评级引用：notify 域测试断言强度高（端到端 RPC + DB 行级比对）。

---

## 4. 运行时行为证据（L5）

**既有 MA2 报告复用**：notify 子系统无专属 MA2 状态机报告（`docs/audits/2026-07-2*-arm-ma2-*` 无 notify 行），本切片为 notify 子系统行为/需求符合性首份证据。MA2 `A2.18`（多公司隔离）覆盖 notify 实体 orgId 维度（notify 实体均无 orgId 列，属跨域子系统共享，无隔离要求）——不重审。

**L5 行为证据来源**（§去重协议）：①L3 代码阅读 + ORM 字段断言；②L4 JUnit 端到端 RPC 测试（16 @Test 强断言）证实的运行时行为；③notify 作为被调用方的跨域接线（cs/finance/sales/crm/manufacturing/hr/workflow 多域已调 `IErpSysNotificationBiz.notify`，见 `README.md §与 18 业务域的关系`，属既有行为证据复用）。

**L5 行为对照**（逐 UC 运行时已证实项）：UC-SYS-01 ACTIVE 派发/DRAFT 跳过/无模板静默跳过（L4 强测）；UC-SYS-02 派发链顺序/status=SENT/best-effort（L4 强测）；UC-SYS-04 三类窗口合并（业务提醒 300s/异常告警 60s/系统通知 NONE 均 L4 强测）；UC-SYS-05 已读翻转/唯一键防重/状态非 read（L4 强测 + ORM UK）；UC-SYS-07 IN_APP 落表/EMAIL config-gated 跳过（L4 强测）。L5 存疑点（需 MA4 运行时展开）见 §7。

---

## 5. 符合性结论（五级追踪矩阵 + 每 UC 结论）

### 五级追踪矩阵（7 UC，逐 UC 一行，验收标准逐条进入 L5 判读）

| UC | L1 需求契约 | L2 owner doc（设计参考） | L3 代码路径 | L4 测试断言 | L5 运行时行为 | 结论 |
|----|-----------|----------------------|-----------|-----------|-------------|------|
| **UC-SYS-01** 模板生命周期与查找 | `use-cases.md:28-44`：${var} 插值模板 / 按 notificationType 查找 / 仅 ACTIVE 参与(DRAFT 不参与) / 无 ACTIVE config-gated 静默跳过 WARN 不抛错 / DRAFT→ACTIVE | `README.md §核心业务对象:34`+`§状态机:104`（仅 ACTIVE 参与；无 ACTIVE config-gated 跳过 WARN）；L2↔L1 一致无冲突 | `NotificationDispatcher#renderTemplate:206`（VAR_PATTERN `\$\{...\}` 插值）/ `ErpSysNotificationNotifyProcessor#findActiveTemplate:65`（`eq("notificationType")`+`eq("status",ACTIVE)`）/ 无模板 LOG.warn 返回空 / `ErpSysNotificationTemplateBizModel` CrudBizModel + `status` 字段(DRAFT default)经 update 迁移 ACTIVE | `TestErpSysNotificationTemplateLifecycle#testActiveTemplateDispatches/testDraftTemplateSkipsDispatch/testTemplateSubjectRendering`（强断言 ACTIVE 派发/DRAFT 跳过/渲染）/ `TestErpSysNotificationDispatch#testNoActiveTemplateSilentSkip`（强断言无模板静默跳过） | 行为已证实（L4 强测） | **接受** |
| **UC-SYS-02** 端到端派发链 | `use-cases.md:48-70`：统一入口 notify / 派发链 6 步不可乱序(查找 ACTIVE→渲染→接收人解析→频控合并→站内落库→config-gated 外发) / status=SENT / best-effort(失败不回滚+内部 try/catch+config-gated 静默跳过) | `README.md §定位:9`+`§子系统结构与派发链:38`+`§边界:14`（best-effort 边界）；L2↔L1 一致 | `ErpSysNotificationBizModel#notify:54`→`ErpSysNotificationNotifyProcessor#notify:40`(try/catch 返回 emptyList)→`NotificationDispatcher#dispatch:76`(顺序: renderTemplate→resolve→mergeOrPersist→dispatchExternalChannels)→Processor `saveEntity/updateEntity`；`mergeOrPersist:143` 设 `STATUS_SENT` | `TestErpSysNotificationDispatch#testNotifyPersistsInAppAndRendered`（强断言 派发链+SENT+渲染+落库）/ `#testNoActiveTemplateSilentSkip`（best-effort 不抛错） | 行为已证实（L4 强测） | **接受** |
| **UC-SYS-03** 接收人多策略解析 | `use-cases.md:74-94`：ROLE(角色名→NopAuthRole→NopAuthUserRole→userId) / ORG(deptId→NopAuthUser) / USER_LIST(userId 列表静态+${var} 插值) / PARTNER(partnerId→用户映射[占位 WARN 返回空 config-gated]) | `README.md §接收人解析策略:78`（ROLE/ORG/USER_LIST/PARTNER 占位）；L2↔L1 一致 | `NotificationRecipientResolver#resolve:83`（先 `interpolateConfig:206` ${var} 插值再解析）/ `resolveRole:117`(角色名→NopAuthRole→NopAuthUserRole) / `resolveOrg:158`(deptId→NopAuthUser) / `resolveUserList:103`(userIds) / `resolvePartner:175`(占位 LOG.warn 返回空) | L4 测试以 USER_LIST 为主（`TestErpSysNotificationDispatch`/`TemplateLifecycle`/`Subscription`/`SeedTemplates`/`CrossDomain` 均 USER_LIST）；ROLE/ORG/PARTNER 经 L3 代码阅读证实存在（无独立 dedicated 测试） | USER_LIST 已证实（L4 强测）；ROLE/ORG/PARTNER L3 存在但运行时覆盖弱→SP-1/SP-3 交 MA4 | **接受**（主路径 USER_LIST 强测；ROLE/ORG/PARTNER L3 实现完整，运行时覆盖交 MA4） |
| **UC-SYS-04** 频控窗口合并 | `use-cases.md:98-119`：合并组 key=notificationType+"#"+recipientUserId / 频控窗口(业务提醒 5min300s/异常告警 1min60s/系统通知不合并) / 命中 mergeCount+=1 status=MERGED / 未命中新建 status=SENT / 总开关 merge-enabled 默认 true | `README.md §关键组件:73`+`§配置项:140`（合并组 key；窗口常量）；L2↔L1 一致 | `NotificationDispatcher#mergeOrPersist:114`（读 `CONFIG_NOTIFY_MERGE_ENABLED` 默认 true + `MERGE_BY_USER_TYPE` 判定）/ 设 `mergeGroupId=notificationType+"#"+userId:144` / `NotificationMergeCoordinator#findMergeable:45`(window) + `#mergeInto:82`(mergeCount+=1+STATUS_MERGED) / 窗口值来自 `template.mergeWindowSeconds`（default 300） | `TestErpSysNotificationSeedTemplates#testBusinessRemindSlaOverdueMergesToSingle`(300s MERGE→1 条) + `#testAlertPostingExceptionMergesWithCount`(60s MERGE→1 条 mergeCount==3) + `#testSystemNoticeCreditOverNoMerge`(NONE→2 条独立) + `TestErpSysNotificationDispatch#testMergeWithinWindowMerges/testMergeOutsideWindowCreatesNew`（强断言 窗口内外） | 行为已证实（L4 强测：三类窗口全验证） | **接受** |
| **UC-SYS-05** 已读状态管理 | `use-cases.md:123-145`：markRead 写 Read / markAllRead 批量 / 唯一键(notificationId,userId)防重复 / 已读状态派生自 Read 关联(非 status lifecycle) / countUnread 实时统计 | `README.md §核心业务对象:36`+`§关键组件:70`（Read 唯一键；已读派生自 Read 关联非 lifecycle）；L2↔L1 一致 | `ErpSysNotificationMarkReadProcessor#markRead:14`(写 Read+判重) / `ErpSysNotificationMarkAllReadProcessor#markAllRead:16`(批量) / `AbstractErpSysNotificationProcessor#isRead:47`(notificationId+userId 查) / ORM `UK_SYS_NOTIFY_READ_NOTIF_USER`(notificationId,userId) / `unreadOf` 用 notIn(readIds) 非 status filter | `TestErpSysNotificationDispatch#testEmailChannelConfigGatedSkipsAndMarkRead`(markRead→countUnread 归零+findUnread 不含已读) + `TestErpSysNotificationSubscription#testMarkAllReadClearsUnread/testFindUnreadReturnsOnlyUnread` + `TestErpSysNotificationSeedTemplates#testEndToEndMarkReadAndFindUnreadAcrossTypes`（强断言 翻转+唯一键防重+DB 仍存） | 行为已证实（L4 强测 + ORM UK 兜底） | **接受** |
| **UC-SYS-06** 收件箱后端查询 | `use-cases.md:149-170`：findUnread/countUnread/markAllRead @Optional userId(留空回退 ctx.getUserId()/显式传向后兼容) / 未读 tab(recipientUserId==userId 且 NOT IN Read) / 已读 tab(Read.userId 反查) / 全部 tab(客户端拼接去重) / 禁用 status=READ filter | `inbox-patterns.md §1:22`(userId 来源裁决) + `§2:42`(三 tab 数据源策略+禁用 status=READ filter)；L2↔L1 一致 | `ErpSysNotificationBizModel#findUnread:74`/`#findRead:80`(Read.userId 反查)/`#countUnread:106`/`#markAllRead:68` 均 `@Optional @Name("userId")` / `resolveUserId:112`(留空回退 `ctx.getUserId()`/显式传按 userId) / `unreadOf:119`(`in(status,[SENT,MERGED])` 非 status=READ) | `TestErpSysNotificationDispatch#findUnread`(显式传 userId 过滤) + `TestErpSysNotificationSubscription#testFindUnreadReturnsOnlyUnread`(强断言) / @Optional 回退 ctx 路径经 L3 代码阅读证实（测试显式传 userId 覆盖 admin 跨用户+单元测试向后兼容场景） | 三 tab + 显式 userId 已证实（L4 强测）；@Optional 回退 ctx 路径 L3 实现完整，运行时 JWT 解析交 MA4 SP-2 | **接受** |
| **UC-SYS-07** 外发通道 config-gated 降级 | `use-cases.md:174-203`：IN_APP 默认通道落表 / EMAIL 经 nop-integration IEmailSender.sendEmail SPI / SMS 经 ISmsSender.sendMessage SPI(不新建平行适配层) / config-gated(enabled 默认 true/email-enabled 默认 false/sms-enabled 默认 false) / 无供应商未启用 WARN 跳过不阻断 / 失败降级 notify 整体 try/catch 返回空列表 | `README.md §通道与失败语义:85`+`§配置项:131`（IN_APP 默认；EMAIL/SMS 经 nop-integration SPI；config-gated；失败降级）；L2↔L1 一致 | `NotificationDispatcher#parseChannelSet:101`(default IN_APP) / `#mergeOrPersist` 落表 / `#dispatchExternalChannels:150`(EMAIL 经 `CONFIG_NOTIFY_EMAIL_ENABLED` 默认 false 门控→`sendEmailIfPossible:171` 调 `emailSender.sendEmail(EmailMessage)`；SMS 经 `CONFIG_NOTIFY_SMS_ENABLED` 默认 false 门控→`sendSmsIfPossible:187` 调 `smsSender.sendMessage(SmsMessage)`；未启用 LOG.warn 跳过) / `NoopEmailSender`/`NoopSmsSender` no-op bean / `ErpSysNotificationNotifyProcessor#notify:40` 整体 try/catch 返回 emptyList | `TestErpSysNotificationDispatch#testEmailChannelConfigGatedSkipsAndMarkRead`(强断言 EMAIL channelSet+email-enabled=false 默认→不抛错+IN_APP 仍落库) / SPI 复用 nop-integration 经 L3 import `io.nop.integration.api.email.IEmailSender`/`sms.ISmsSender` 证实 | IN_APP 落表/EMAIL config-gated 跳过/失败降级已证实（L4 强测）；**总开关 erp-notify.enabled 声明但从未消费→见 P2-RC-081** | **接受 on 主路径 + P2-RC-081**（总开关 kill-switch 失效） |

### 每 UC 符合性结论（§2 判据，取最高）

- **UC-SYS-01 = 接受**：L1 全部 5 条验收标准（${var} 插值/notificationType 查找/仅 ACTIVE/无 ACTIVE config-gated 跳过/DRAFT→ACTIVE）在 L3-L5 均有证据且一致。命中 §2「接受」行（全部验收标准各级均有证据）。**不命中** P0/P1/P2 任何判据。触及保护区域：无。
- **UC-SYS-02 = 接受**：派发链 6 步顺序不可乱序（L3 `dispatch:76` 顺序证实）/ status=SENT（L3 `mergeOrPersist:143` + L4 强测）/ best-effort（L3 `notify:40` try/catch 返回 emptyList + L4 强测）全部一致。**不命中** P0/P1/P2。触及保护区域：无。
- **UC-SYS-03 = 接受**：ROLE/ORG/USER_LIST/PARTNER 四类 resolver 全实现（L3）；USER_LIST 含 ${var} 插值（L3 `interpolateConfig:206`）；PARTNER 占位 WARN 返回空 config-gated（L3 `resolvePartner:175`，与 L1 「占位：映射未建立 WARN 返回空 config-gated」逐字一致）。**不命中** P0/P1/P2。触及保护区域：无（复用平台 nop-auth 只读查询，无 ORM 变更）。
- **UC-SYS-04 = 接受**：合并组 key（L3 `:144` + `MERGE_GROUP_SEP="#"`）/ 三类窗口（L4 强测业务提醒 300s/异常告警 60s/系统通知 NONE）/ 命中 MERGED+mergeCount（L3 `mergeInto:82` + L4 强测）/ 未命中 SENT（L3 + L4 强测）/ 总开关 merge-enabled 默认 true（L3 `:117` 消费 + L4 强测）全部一致。**不命中** P0/P1/P2。触及保护区域：无。
- **UC-SYS-05 = 接受**：markRead 写 Read（L3 + L4 强测）/ markAllRead 批量（L3 + L4 强测）/ 唯一键(notificationId,userId)防重复（ORM `UK_SYS_NOTIFY_READ_NOTIF_USER` + L3 `isRead` 判重双保护）/ 已读状态派生自 Read 关联非 status（L3 `unreadOf` notIn(readIds) 非 status filter + L4 强测）/ countUnread 实时统计（L3 + L4 强测）全部一致。**不命中** P0/P1/P2。触及保护区域：无。
- **UC-SYS-06 = 接受**：@Optional userId 回退 ctx（L3 `resolveUserId:112`）/ 显式传向后兼容（L3 + L4 测试均显式传 userId）/ 未读 tab（L3 `unreadOf` recipientUserId+notIn）/ 已读 tab（L3 `findRead` Read.userId 反查）/ 全部 tab 客户端拼接（L2 `inbox-patterns.md §2` 设计 + L3 提供两端点）/ 禁用 status=READ filter（L3 用 `in(status,[SENT,MERGED])` 非 status=READ + L2 反模式显式禁止）全部一致。**不命中** P0/P1/P2。触及保护区域：无。
- **UC-SYS-07 = 接受 on 主路径 + P2-RC-081**：IN_APP 默认通道落表（L3 + L4 强测）/ EMAIL 经 IEmailSender.sendEmail SPI（L3 import `io.nop.integration.api.email.IEmailSender` + `:180` 调用）/ SMS 经 ISmsSender.sendMessage SPI（L3 import `io.nop.integration.api.sms.ISmsSender` + `:195` 调用，不新建平行适配层）/ config-gated（email-enabled/sms-enabled 默认 false 门控 + L4 强测 EMAIL 跳过）/ 无供应商未启用 WARN 跳过不阻断（L3 LOG.warn + NoopSender + L4 强测）/ 失败降级 notify 整体 try/catch 返回空列表（L3 `notify:40` + L4 强测）**全部一致**。**唯一缺口**：`erp-notify.enabled`（通知总开关，默认 true）声明于 `ErpNotifyConfigs:15` 但 grep `AppConfig.var` 全 main 源**零消费**——kill-switch disable 分支失效（设 enabled=false 通知仍派发）。→ **P2-RC-081**（§2 P2① 次要验收标准未完全满足：主路径[默认 true 通知正常派发]OK，边界[kill-switch disable 分支失效]弱 watch-only，声明 Q4=(a) 张力）。**§4 三判据复核**：(i) 无独立 plan-audit 裁决总开关裁剪；(ii) owner doc `README.md §配置项:131` 列明 `erp-notify.enabled 默认 true 通知总开关` 为活跃配置项未声明 Deferred；(iii) product-scope 未将总开关裁剪。三判据在"人工批准"意义上不满足，但**实际影响限于 kill-switch 失效非核心派发功能缺失**（与 logistics P2-RC-073 async-dispatch 死 config 同型——声明配置未消费但功能经默认路径正常运作）→ 定 P2 watch-only 非 P1（对齐 P2-RC-073 先例，区别于 logistics P1-RC-085/aps P1-RC-090 "死 config 致整个功能永不运行"）。触及保护区域：无（修复 = `notifyProcessor.notify` 入口加 `if (!AppConfig.var(CONFIG_NOTIFY_ENABLED, true)) return emptyList` 早返回，纯 BizModel 代码逻辑预授权不触 §5 ask-first）。

**结论汇总**：UC-SYS-01/02/03/04/05/06 = **接受**；UC-SYS-07 = **接受 on 主路径 + P2-RC-081**。**零 P0，零 P1，1 新 P2**。notify 子系统派发链实现质量高（dispatch 顺序/resolver 四类/频控三档/已读唯一键/config-gated 降级/SPI 复用全实现 + 18 @Test 强断言覆盖），候选缺口仅总开关 kill-switch 失效 1 项 P2。

---

## 6. 与 arm-index 衔接（finding 复用/新增裁决）

### 6.1 grep arm-index 同域同控制点裁决（§7 规则）

grep `arm-index.md` notify/notification/notificationDispatch/notificationMerge/notificationRecipient/inbox/UC-SYS/templateLifecycle/bestEffort/config-gated/总开关/kill-switch/merge-enabled → **无 UC-SYS-01~07 需求符合性 finding**。既有 notify 相关 finding 均为其他维度：
- `P1-MA1-022`（跨域 daoFor 平台一致性 resolved）——notify 实体被多域只读引用，属平台一致性维度已 resolved，本切片不重审（Non-Goal）。
- notify 作为被调用方的跨域接线（cs/finance/sales/crm/manufacturing/hr/workflow 多域已调 `IErpSysNotificationBiz.notify`）——属既有行为证据复用，本切片从 notify 子系统自身需求契约视角审，不重审跨域接线。

### 6.2 本切片 finding 复用/新增裁决

| Finding ID | 裁决 | 依据 |
|-----------|------|------|
| **P2-RC-081**（UC-SYS-07 总开关 kill-switch 失效） | **新建** | 新根因（notify 域首个 RC 切片；grep arm-index notify 总开关/kill-switch/notify.enabled RC 系列零命中）。**与 logistics P2-RC-073（async-dispatch 死 config）同型不同域不同控制点**（声明配置未消费但功能经默认路径正常运作 → P2 watch-only），不合并。区别于 logistics P1-RC-085/aps P1-RC-090（死 config 致整个功能永不运行 → P1）。 |

### 6.3 双向可追溯

- 新 finding P2-RC-081 入 arm-index RC 发现追踪分区（见 §归档规范）。
- P2-RC-081 修复行预留 MR1（R1.0 展开时若纳入则 RC-R1.n 引用 P2-RC-081）；P2 登记不强制，可作 successor。
- **ORM 结构类修复须 ask-first + 独立 plan-audit**：本切片 P2-RC-081 修复为纯 BizModel 代码逻辑（notifyProcessor 入口早返回），**不触及 ORM**。
- **UC-SYS-07 外发通道 SPI 派发须与 nop-integration 平台 SPI 协调**：当前已直接复用 `io.nop.integration.api.email.IEmailSender`/`sms.ISmsSender`（不新建平行适配层），无协调缺口；未来注册真实供应商（JavaEmailSender/TencentSmsSender）时覆盖 NoopSender 默认 bean 即可，属平台默认路线。

---

## 7. 静态存疑点清单（供 MA4 A4.1/A4.2 运行时展开）

逐存疑点一行；**P0 即时通道评估**——本切片**无活跃数据破坏候选**（notify 为 best-effort 通知子系统，派发失败设计上不破坏业务事实；候选缺口 P2-RC-081 为 kill-switch 失效，不影响默认 true 路径的派发正确性；频控合并 + 派发链顺序 + 落库均 L4 强测证实无吞异常致落库失败）。无需 MR0 即时通道。

- **SP-1**：ROLE resolver 运行时 roleName 匹配——`resolveRole:117` 经 `in("roleName", roleNames)` 查 NopAuthRole 后再 `roleNameSet.contains` 二次过滤，运行时平台 nop-auth 角色数据 roleName 字段是否与模板配置的 roles 列表一致（静态实现完整，运行时种子角色数据覆盖交 MA4）。
- **SP-2**：USER_LIST `${var}` 动态插值运行时覆盖——`interpolateConfig:206` 实现 ${var} 从 context 插值（如 submitterUserId），L4 测试均用静态 userIds，动态 var 插值运行时是否在多域消费者（cs/finance/sales 提单人 submitterUserId）正确解析交 MA4。
- **SP-3**：ORG resolver deptId 精确匹配运行时——`resolveOrg:158` 经 `eq("deptId",...)` 精确匹配 NopAuthUser，L1 「deptId → NopAuthUser」为精确匹配语义（未要求子部门级联），运行时是否需子部门递归交 MA4（静态语义符合 L1）。
- **SP-4**：`erp-notify.enabled=false` kill-switch 运行时行为——静态已确认 `CONFIG_NOTIFY_ENABLED` 声明但 `AppConfig.var` 零消费，notify 仍派发（P2-RC-081）；运行时确认设 enabled=false 后 notify 是否仍落库（静态倾向确认仍落库）。

---

## 8. 过程纪律自检

- [x] **checker 退出码门控核查**：本报告产出后已运行 `bash docs/audits/nop-compliance-checker.sh`，actual ≤ baseline（见下表）。**区分门控退出码 vs 纯 reporter 退出码**——checker 脚本是纯 reporter（退出码恒 0），真正门控在 CI workflow（`.github/workflows/compliance.yml`）解析 actual > baseline => sys.exit(1)。本报告**不**以 checker 脚本退出码 0 作为门控通过依据。**本审计无生产代码变更（纯审计报告），checker 无回归风险**。

  | 检查项 | baseline | actual | 状态 |
  |--------|----------|--------|------|
  | checker 脚本退出码 | 0 | 0（纯 reporter 恒 0） | N/A（不以退出码 0 为门控通过依据） |
  | 生产代码变更 | 0 | 0（本审计为只读，无 module-notify 代码变更） | 无回归风险 |

- [x] **closure-audit 独立性声明**：本报告的 closure audit 将由独立子代理（新会话，不重用执行者上下文）执行，执行者不自我审计。
- [x] **与 arm-index 交叉去重声明**：本报告全部 finding（P2-RC-081）已按 §7 规则 grep arm-index 同域同控制点后给出"新建"裁决（无同根因同控制点既有 finding），无未经比对直接新建的 finding。
- [x] **真相源冻结声明**：本审计未修改任何真相源（`use-cases.md`/owner doc/product-scope.md 均未改动）；分歧记入本报告 §5/§6，未直改真相源（§9 冻结条款）。

---

## 9. 与 MA2 报告差异增量声明

**无 notify 专属 MA2 报告**（`docs/audits/2026-07-2*-arm-ma2-*` 无 notify 行）。本切片为 notify 子系统需求契约符合性**首份证据**（notify 域 MA1 首审）。按 §去重协议，本切片只补"需求契约↔实际行为"差异（既有 MA2 未覆盖 notify 子系统），不重审 MA2 已证实行为。本切片差异增量（均为需求契约视角，MA2 未覆盖）：

- **模板生命周期**（UC-SYS-01）：ACTIVE 查找 + DRAFT 不参与 + 无 ACTIVE config-gated 跳过 + ${var} 插值 + DRAFT→ACTIVE 状态迁移。
- **派发链顺序**（UC-SYS-02）：6 步不可乱序 + status=SENT + best-effort 不回滚调用方 + 内部 try/catch。
- **接收人策略**（UC-SYS-03）：ROLE/ORG/USER_LIST/${var} 插值/PARTNER 占位四类。
- **频控合并**（UC-SYS-04）：合并组 key + 三类窗口(业务提醒/异常告警/系统通知) + mergeCount+MERGED/新建 SENT + 总开关。
- **已读状态**（UC-SYS-05）：唯一键防重 + Read 关联派生(非 status lifecycle) + countUnread 实时。
- **收件箱查询**（UC-SYS-06）：@Optional userId 回退 ctx + 未读/已读/全部三 tab + 禁用 status=READ filter。
- **外发降级**（UC-SYS-07）：IN_APP 落表 + EMAIL/SMS 经 nop-integration SPI + config-gated + WARN 跳过不阻断 + 失败降级 try/catch。唯一差异缺口：总开关 kill-switch 失效（P2-RC-081）。

**报告 9 段完整性自检**：§1 需求契约原文 ✅ / §2 实现证据 ✅ / §3 测试证据 ✅ / §4 运行时行为证据 ✅ / §5 符合性结论 ✅ / §6 与 arm-index 衔接 ✅ / §7 静态存疑点清单 ✅ / §8 过程纪律自检 ✅ / §9 与 MA2 报告差异增量声明 ✅。9 段齐全。

---

## 触及保护区域汇总

本切片唯一 finding **P2-RC-081** 修复为纯 BizModel 代码逻辑（`notifyProcessor.notify` 入口加总开关早返回），**不触及 ORM 结构变更 / 会计过账逻辑 / 数据删除迁移**，按 roadmap 预授权类目[代码逻辑修复]可自动执行，**不触发 §5 ask-first**。本审计整体为只读审计，无任何代码/ORM/api.xml/view.xml/真相源变更。
