# rc-ma4-a4-2-163-169-b2b-runtime b2b 域 Registry 派发/异步队列/归档/编码映射/重试/激活门槛/证书停用隐式机制运行时确认

> Plan: `docs/plans/2026-08-08-0135-3-rc-ma4-a4-2-163-169-b2b-runtime.md`
> Mission: requirement-compliance（MA4 运行时行为验证 切片 A4.2.163-A4.2.169）
> Work Item: A4.2.163 / A4.2.164 / A4.2.165 / A4.2.166 / A4.2.167 / A4.2.168 / A4.2.169（A1.47 §7 SP-1..SP-7 七项静态存疑点运行时确认）
> 来源: `docs/backlog/requirement-compliance-roadmap.md` A4.2.163-A4.2.169
> Audit Status: planned
> 审计性质: **只读审计**（零生产代码/ORM/api.xml/view.xml/config 默认值/真相源变更；无测试侧补充）

## 0. 与既有 MA1/A1.x 报告差异增量声明（§9）

本报告**只补运行时行为证据**（methodology §去重协议），不重审 A1.47 已裁决的需求符合性结论与 finding 分级：

- **A1.47**（`docs/audits/2026-08-05-1400-2-rc-ma1-a1-47-b2b-edi-full.md`）：b2b 域全域 8 UC 五级追踪（MA1 首审）+ §7 SP-1..SP-7 静态存疑点 + §6 finding（8 新 P1-RC-080 + P2-RC-062~068 + 4 reuse[P1-MA2-073/P1-MA2-088/P1-MA1-022/P1-MA3-046]）。本报告复用其 L3 静态判定 + L4 测试断言 + §6 finding 编号，只补 **SP-1..SP-7（A4.2.163-169）七项存疑点的运行时证据**。
- **A2.14**（`docs/audits/2026-07-28-1020-arm-ma2-ext-domains-state-machine.md`）：覆盖 b2b **EdiDoc 信封状态机 + ASN webhook 幂等**两个控制点（产出 P1-MA2-073 resolved R1.23 via deferral + P1-MA2-088 resolved R1.28），**不含** UC-B2B-001 isActive 派发门槛 / UC-B2B-002 needsWebService 异步派发 / UC-B2B-004 出站映射 / UC-B2B-007 伙伴上线状态机+门槛 / UC-B2B-008 MFT 证书停用 需求契约维度（A1.47 §9 已声明只补需求视角差异）。本报告**不重审状态机迁移维度**。
- **P1-MA2-073**（b2b EDI 出站自动化缺失，resolved R1.23 via **deferral**）：本报告维持其 reuse 注记（A4.2.164/A4.2.167 运行时确认无异步队列/无自动重试），**不重开 deferral 裁决**。
- **P1-MA2-088**（webhook 幂等 TOCTOU，resolved R1.28）：本报告**不重审**（A1.47 §9 已声明复用其行为证据，A4.2.165 仅引用其幂等/确定性 code 行为作为 webhook 主路径佐证）。

本切片**只补**的运行时差异：(i) A4.2.163 isActive 派发门槛两处代码点（Registry findOutboundProviders/findInboundProviders + findFormatByCode）零 isActive 过滤确认；(ii) A4.2.164 needsWebService 调用点 census + module-b2b 调度/队列 census；(iii) A4.2.165 parseToAsn 零 archive 调用 + archive 守卫 RECEIVED-only + 重跑 testArchiveAfterAcknowledged 实证；(iv) A4.2.166 resolveOutbound 调用点 census + generatePayload 内部原值实证；(v) A4.2.167 调度/重试 census + markError 恒 ERROR 分级 + 重跑 testErrorAndRetryCycle 实证；(vi) A4.2.168 activate 零状态守卫 + 推进 mutation/pass-rate/认证清单 census；(vii) A4.2.169 findExpiringCertificates 仅查询 + 证书停用调度 census。

---

## 1. 存疑点清单与判据（A1.47 §7 七项）

| # | 工作项 | 来源存疑点 | 静态判定 | 运行时判据 |
|---|--------|-----------|---------|-----------|
| 1 | A4.2.163 | A1.47 §7 SP-1（UC-B2B-001 业务规则）isActive=false 格式运行时是否仍被 Registry 派发 | `findFormatByCode:91-97` 仅判 null 不查 isActive | 确认 `ErpB2bEdiDocCreateOutboundProcessor#findFormatByCode:91-97` + `ErpB2bEdiRegistry#findOutboundProviders:59-68`/`#findInboundProviders:71-80` file:line 无 isActive 过滤 |
| 2 | A4.2.164 | A1.47 §7 SP-2（UC-B2B-002 基本流 ⑤）needsWebService=true Provider 是否经隐式机制走异步队列 | `createOutbound:39-71` 同步直发 + needsWebService 分支零调用 | 确认 `createOutbound:39-71` file:line 同步链 + needsWebService 调用点 census + module-b2b scheduler/queue/cron census |
| 3 | A4.2.165 | A1.47 §7 SP-3（UC-B2B-003 step9）入站 webhook EdiDoc 是否经隐式 hook 自动 archive | `parseToAsn:87-171` 零 archive 调用 | 确认 `parseToAsn:87-171` file:line 零 archive + archive 调用点 census + `archive():160-161` 守卫 + 重跑 `TestErpB2bEdiPosting#testArchiveAfterAcknowledged:106-123` |
| 4 | A4.2.166 | A1.47 §7 SP-4（UC-B2B-004 业务规则 双向使用）出站 UBL Invoice 物料编码是否经 codeMappingResolver 映射 | `generatePayload:51-84` 注入未调 resolveOutbound | 确认 `UblInvoiceEdiProvider#generatePayload:51-84` file:line 零 resolveOutbound 调用 + resolveOutbound 调用点 census + 对照入站 `parseToAsn:156` resolveInbound |
| 5 | A4.2.167 | A1.47 §7 SP-5（UC-B2B-006 自动重试策略）ERROR+WARN blocking_level 是否存在隐式自动重试调度 | grep autoRetry/@Scheduled/Quartz/cron 零命中 | 确认 module-b2b 调度 census + markError 恒 ERROR 分级 + 重跑 `TestErpB2bEdiPosting#testErrorAndRetryCycle:76-103` |
| 6 | A4.2.168 | A1.47 §7 SP-6（UC-B2B-007 基本流程 ③④⑤）activate 是否可从 TESTING/CERTIFIED 直接跳 PRODUCTION 绕过门槛 | `activate:22-27` 任意态→PRODUCTION 零状态守卫 | 确认 `ErpB2bPartnerProfileBizModel#activate:22-27` file:line 零守卫 + 推进 mutation/pass-rate/认证清单 census + goLiveDate writer census |
| 7 | A4.2.169 | A1.47 §7 SP-7（UC-B2B-008 异常 证书过期）证书过期是否经 scheduler 自动停用 MftConfig | `findExpiringCertificates:39-50` 仅查询 | 确认 `ErpB2bMftCertificateBizModel#findExpiringCertificates:39-50` file:line 仅查询 + 停用调度 census |

---

## 2. 运行时证据采集（L3 `file:line` + L4 强断言，2026-08-08 live code 实测）

### 2-1 A4.2.163 isActive=false 格式派发确认 — 确认仍派发（维持 P2-RC-062）

**L3 代码路径**（live code 实测）：

- `module-b2b/erp-b2b-service/src/main/java/app/erp/b2b/service/processor/ErpB2bEdiDocCreateOutboundProcessor.java#findFormatByCode:91-97` — QueryBean 仅 `eq("code", code)`（:93）+ orderField("code")（:95），`findFirstByQuery`（:96）**仅判 null 不查 `isActive`** → inactive 格式记录仍被选中参与派发。
- `ErpB2bEdiRegistry#findOutboundProviders:59-68` / `#findInboundProviders:71-80` — 遍历 providerMap（Provider bean 按 getCode 建图，非 ErpB2bEdiFormat 记录），仅经 `getApplicability` 过滤（:62-63/:74-75），**零 `ErpB2bEdiFormat.isActive` 查询/过滤**。
- `createOutbound:39-71` 派发链（:40 `findOutboundProviders` → :47 `findFormatByCode` → :53 `checkDuplicate` → :55 `generatePayload` → :57-66 建 TO_SEND 信封）——**全链无 isActive 门槛**。

**裁决**：确认 **isActive=false 格式仍参与派发**（Registry 建图不经 ErpB2bEdiFormat 记录、findFormatByCode 不查 isActive，两条路径均无过滤）。**与 A1.47 §6 衔接维持 P2-RC-062（登记不强制，修复归 MR1——findFormatByCode/findOutboundProviders/findInboundProviders 增 isActive=true 过滤，纯 BizModel 代码逻辑预授权不触 §5 ask-first）**。

### 2-2 A4.2.164 needsWebService 异步队列确认 — 确认无异步派发（reuse P1-MA2-073 维持，修复归 MR1）

**L3 代码路径**（live code 实测）：

- `ErpB2bEdiDocCreateOutboundProcessor#createOutbound:39-71` — **同步直发**：Registry 查 Provider → findFormatByCode → checkDuplicate → `provider.generatePayload`（:55）→ 建 TO_SEND 信封 + writeLog，返回。**全程零 `needsWebService()` 调用、零异步队列/cron 派发分支**。
- **needsWebService 调用点 census**：grep `needsWebService` 跨 module-b2b 命中仅 3 类——SPI 声明 `IErpB2bEdiProvider:46`、实现 `UblInvoiceEdiProvider#needsWebService:93-95`（return false）+ `UblDespatchAdviceEdiProvider#needsWebService:142-144`（return false）、ORM/API bean 字段定义（ErpB2bEdiFormat.needsWebService propId 7，仅存储）。**零生产调用方**。
- **调度/队列 census**：grep `autoRetry|@Scheduled|Quartz|ScheduledExecutor|ScheduledTask|@Job|@Scheduler` 跨 module-b2b **零命中**（无 cron/无 Job bean/无异步发送队列）。

**裁决**：确认 **needsWebService 异步/同步派发分支零实现**——createOutbound 同步直发 + needsWebService 零调用方 + 调度/队列 census 零命中。**与 A1.47 §6 衔接维持 reuse P1-MA2-073（resolved R1.23 via deferral 注记，修复归 MR1 与 deferral 裁决协同；不重开）**。

### 2-3 A4.2.165 入站 EdiDoc 自动 archive 确认 — 确认无自动 archive（维持 P2-RC-064）

**L3 代码路径**（live code 实测）：

- `ErpB2bAsnHandleInboundWebhookProcessor#parseToAsn:87-171` — 成功路径：`createInbound(RECEIVED)`（:105-107）+ 建 ErpB2bAsn（:117-144，确定性 code ASN-WEBHOOK-{eventId} 为 P1-MA2-088 R1.28 修复证据）+ 建 AsnLine + `resolveInbound`（:156-160）+ `writeEdiLog`（:167）——**末尾零 `archive()` 调用**，EdiDoc 留 RECEIVED。
- **archive 调用点 census**：grep `archive` 跨 module-b2b/erp-b2b-service/src/main 仅 2 处——`ErpB2bAsnMatchPurchaseOrderProcessor:92`（**独立 PO 匹配流**：ASN MATCHED 后手动/触发调用 archive，非 webhook 入站隐式 hook，范围主张与计划草案审查一致）+ `ErpB2bEdiDocBizModel#archive:157-168`（mutation 本体）。
- `ErpB2bEdiDocBizModel#archive:157-168` 守卫 :160-161：`if (!EDI_DOC_STATE_RECEIVED.equals(state)) throw illegalTransition` ——**仅 RECEIVED→ARCHIVED 合法迁移**，其余态（含 ACKNOWLEDGED/ERROR/TO_SEND/SENT/CANCELLED）抛 ERR_B2B_EDI_DOC_ILLEGAL_TRANSITION。

**L4 测试证据**（2026-08-08 重跑全绿）：

- **重跑** `TestErpB2bEdiPosting#testArchiveAfterAcknowledged:106-123` — PASS：seed TO_SEND → markSent → markAcknowledged（:114-115）→ `assertThrows(NopException.class, ... archive(...))`（:119）→ 状态断言 ACKNOWLEDGED 不变（:121-122）——**ACKNOWLEDGED→archive 抛非法迁移 NopException，状态不变**，与 archive 守卫仅 RECEIVED 合法逐字命中。

**裁决**：确认 **入站 EdiDoc 无自动 archive**——parseToAsn 成功路径零 archive 调用（EdiDoc 留 RECEIVED）+ archive 守卫仅 RECEIVED→ARCHIVED 合法（ACKNOWLEDGED→archive 重跑实证抛异常状态不变）+ 全 module-b2b main 无 webhook 隐式 hook。**与 A1.47 §6 衔接维持 P2-RC-064（登记不强制，修复归 MR1——parseToAsn 成功路径末尾调 ediDocBiz.archive，纯 BizModel 代码逻辑预授权）**。

### 2-4 A4.2.166 出站 UBL Invoice 编码映射确认 — 确认无出站映射（维持 P2-RC-065）

**L3 代码路径**（live code 实测）：

- `UblInvoiceEdiProvider` 注入 `@Inject CodeMappingResolver codeMappingResolver`（:32-35）但 `#generatePayload:51-84` **零调用 `resolveOutbound`**——构建 UBL Invoice XML 骨架：查 ErpSalInvoice（:53-63，仅 `eq("code", relatedBillCode)` 定位单据）+ 头字段 + LegalMonetaryTotal 0.00（:79）+ 关闭标签（:82）。报文不含映射后物料编码，出站侧用内部原值。
- **resolveOutbound 调用点 census**：grep `resolveOutbound` 跨 module-b2b **仅 1 命中 = 定义本身** `CodeMappingResolver#resolveOutbound:42-52`（partnerId+mappingType+internalCode→externalCode，未找到返回原值+WARN）——**零生产调用方**。
- **对照入站方向**：`ErpB2bAsnHandleInboundWebhookProcessor#parseToAsn:156` 调 `codeMappingResolver.resolveInbound(partnerId, MATERIAL, supplierPartNo)`（外部→内部，结果写 line.remark :160）——入站映射已落地，出站未消费。

**裁决**：确认 **出站 UBL Invoice 物料编码不经过 codeMappingResolver 映射**（generatePayload 注入 codeMappingResolver 但零调用 + resolveOutbound 全域零调用方 + 报文用内部原值）。**与 A1.47 §6 衔接维持 P2-RC-065（登记不强制，修复归 MR1——generatePayload 构建前调 resolveOutbound，纯 BizModel 代码逻辑预授权）**。

### 2-5 A4.2.167 ERROR+WARN 自动重试调度确认 — 确认无自动重试（reuse P1-MA2-073 维持，修复归 MR1）

**L3 代码路径**（live code 实测）：

- **调度 census**：grep `autoRetry|@Scheduled|Quartz|ScheduledExecutor|ScheduledTask|@Job|@Scheduler` 跨 module-b2b **零命中**——无自动重试调度/无 cron/无 Job bean。
- `ErpB2bEdiDocBizModel#markError:97-108` — **恒设 `blockingLevel=BLOCKING_LEVEL_ERROR`**（:102），不支持 WARN 分级（L1 UC-B2B-006 自动重试策略表「ERROR 且 blocking_level=WARN → 系统每 30 分钟自动重试最多 3 次」的 WARN 触发前提运行时不可达）。
- `#retry:112-126` — **仅手工**：ERROR→TO_SEND 守卫（:115-117）+ retryCount++（:119）+ `setError(null)`（:120，P2-RC-067 审计轨迹边界）+ blockingLevel=INFO（:121）+ writeLog 新 EdiLog（:123-124）。零自动触发方。

**L4 测试证据**（2026-08-08 重跑全绿）：

- **重跑** `TestErpB2bEdiPosting#testErrorAndRetryCycle:76-103` — PASS：markError→ERROR + error 字段 + blockingLevel=ERROR（:88-91）；retry→TO_SEND + retryCount≥1 + blockingLevel=INFO（:93-97）；重发 SENT（:99-100）；log≥4（:102）——手动重试循环运行时实证，无任何自动调度参与。

**裁决**：确认 **无自动重试调度**——调度 census 零命中 + markError 恒 ERROR 无 WARN 分级（L1 自动重试策略表 WARN 分支运行时不可达）+ 重试仅手工 `retry:112-126`（重跑实证）。**与 A1.47 §6 衔接维持 reuse P1-MA2-073（resolved R1.23 via deferral 注记，修复归 MR1 与 deferral 裁决协同；不重开）**。

### 2-6 A4.2.168 activate 门槛绕过确认 — 确认可绕过（维持 P1-RC-080 P1，Q4 强制实现，修复归 MR1）

**L3 代码路径**（live code 实测）：

- `ErpB2bPartnerProfileBizModel#activate:22-27` — `requireEntity` + `profile.setStatus("PRODUCTION")`（:24）+ `updateEntity`（:25）——**任意态→PRODUCTION 零状态守卫**（不校验当前 status，REGISTERED/TESTING/CERTIFIED/SUSPENDED 均可直接跳 PRODUCTION），**activate 未设 goLiveDate**。
- `#suspend:31-36`（→SUSPENDED）+ `#deactivate:40-45`（→TERMINATED，**未设 archivedAt**）——均零状态守卫。
- **推进 mutation census**：grep `goLiveDate|passRate|pass_rate|advanceTo|promoteTo|setStatus("TESTING")|setStatus("CERTIFIED")|checklist` 跨 module-b2b/erp-b2b-service/src/main **零业务命中**——TESTING/CERTIFIED 推进 mutation 零实现。
- `ErpB2bTestExchangeBizModel`（17 行 CRUD 桩，仅 setEntityName）——**通过率≥90% 聚合零实现**。
- `ErpB2bCertificationChecklistBizModel`（17 行 CRUD 桩，仅 setEntityName）——**认证清单全必检项门槛零实现**。
- ORM 字段就绪无 writer（A1.47 §2/§6 证据）：`ErpB2bPartnerProfile` 已有 `goLiveDate`(propId 19)/`archivedAt`(propId 20)/`status`(dict `erp-b2b/partner-status` 6 值)。

**裁决**：确认 **TESTING/CERTIFIED 可直接跳 PRODUCTION 绕过门槛**——activate 零状态守卫 + 推进 mutation/pass-rate/认证清单零实现 + goLiveDate 零 writer。**与 A1.47 §6 衔接维持 P1-RC-080 P1（§2 P1①功能完全缺失 + §2 P1③状态迁移非法未守卫；Q4 强制实现，修复归 MR1 R1.0 展开器——状态守卫+推进 mutation+goLiveDate/archivedAt 写入属纯 BizModel 代码逻辑预授权不触 §5 ask-first）**。

### 2-7 A4.2.169 证书过期自动停用确认 — 确认无自动停用（维持 P2-RC-068）

**L3 代码路径**（live code 实测）：

- `ErpB2bMftCertificateBizModel#findExpiringCertificates:39-50` — `@BizQuery` 仅查询：`eq("isActive", true)`（:46）+ `le("expiresAt", threshold)`（:47）+ `setLimit(200)`（:48）+ `findAllByQuery`（:49）——**零停用写入**（无 active=false 更新、无 ErpB2bMftConfig 联动）。
- **停用调度 census**：grep `cron|scheduler|deactivat|Job` 跨 module-b2b/erp-b2b-service/src/main **仅 1 命中 = `ErpB2bPartnerProfileBizModel#deactivate:40`**（伙伴档案 TERMINATED mutation，与证书/MFT 无关）——**零 scheduler/零 Job/零证书停用机制**。

**裁决**：确认 **证书过期无自动停用**——findExpiringCertificates 仅查询能力 + 停用调度 census 零命中。**与 A1.47 §6 衔接维持 P2-RC-068（登记不强制，修复归 MR1——证书过期 scheduler 自动停用 MftConfig.active=false 属代码逻辑预授权）**。

---

## 3. 测试证据汇总（L4，断言强度）

| 工作项 | 测试 | 断言强度 | 覆盖存疑点 | 运行结果（2026-08-08） |
|--------|------|---------|-----------|----------------------|
| A4.2.163 | `ErpB2bEdiRegistry#findOutboundProviders:59-68`/`#findInboundProviders:71-80` + `ErpB2bEdiDocCreateOutboundProcessor#findFormatByCode:91-97` 只读 census | 只读 census | SP-1（isActive 不参与派发过滤） | ✅（census） |
| A4.2.164 | needsWebService 调用点 census（3 类命中均定义/字段非调用）+ module-b2b 调度/队列 census 零命中 | 只读 census | SP-2（无异步派发） | ✅（census） |
| A4.2.165 | **重跑** `TestErpB2bEdiPosting#testArchiveAfterAcknowledged:106-123` + archive 调用点 census + `archive():160-161` 守卫 file:line | 强（assertThrows + 状态不变断言 :119/:121-122） | SP-3（无自动 archive + RECEIVED-only 守卫） | PASS ✅ |
| A4.2.166 | resolveOutbound 调用点 census（仅定义 1 命中）+ `generatePayload:51-84` 零调用 file:line | 只读 census | SP-4（无出站映射） | ✅（census） |
| A4.2.167 | **重跑** `TestErpB2bEdiPosting#testErrorAndRetryCycle:76-103` + markError 恒 ERROR 分级 + 调度 census | 强（ERROR→retry→TO_SEND 全链断言 :88-102） | SP-5（无自动重试） | PASS ✅ |
| A4.2.168 | `activate:22-27` file:line + 推进 mutation/pass-rate/认证清单 census + 双 CRUD 桩（各 17 行） | 只读 census | SP-6（可绕过门槛） | ✅（census） |
| A4.2.169 | `findExpiringCertificates:39-50` file:line + 停用调度 census（仅 deactivate 伙伴档案 1 命中非相关） | 只读 census | SP-7（无证书停用） | ✅（census） |

`mvn test -pl module-b2b/erp-b2b-service` → **Tests run: 33, Failures: 0, Errors: 0, Skipped: 0 — BUILD SUCCESS**（含 TestErpB2bEdiPosting=3 / TestErpB2bAsnInbound=7 / TestErpB2bAsnInventoryIntegration=6 / TestErpB2bMftTransport=5 / TestErpB2bAsnCrudSmoke=5 / TestErpB2bEdiEnvelope=7 全绿）。

---

## 4. 运行时行为证据（L5）

- **复用 A1.47 §4/§5 + A2.14**：EdiDoc 信封状态机迁移守卫（TO_SEND→SENT→ACKNOWLEDGED + ERROR→retry→TO_SEND + RECEIVED→ARCHIVED + cancel 守卫）经 `TestErpB2bEdiPosting` 强测证实（A2.14 已证，本报告不重审状态机维度）；ASN webhook HMAC 验签 + 幂等（P1-MA2-088 R1.28 修复证据：确定性 ASN-WEBHOOK-{eventId} code + UK 兜底）经 `TestErpB2bAsnInbound` 强测证实。
- **本切片补的运行时差异**（经 live code 实测 + L4 重跑 + census）：
  - **A4.2.163** inactive 格式仍参与派发（Registry 建图不经 ErpB2bEdiFormat 记录 + findFormatByCode 不查 isActive 双路径实证）。
  - **A4.2.164** createOutbound 同步直发 + needsWebService 零调用方 + module-b2b 调度/队列 census 零命中。
  - **A4.2.165** parseToAsn 零 archive 调用（EdiDoc 留 RECEIVED）+ archive 守卫仅 RECEIVED 合法 + ACKNOWLEDGED→archive 重跑实证抛异常状态不变；PO 匹配流 archive（ErpB2bAsnMatchPurchaseOrderProcessor:92）为独立流程非 webhook 隐式 hook。
  - **A4.2.166** resolveOutbound 全域零调用方 + generatePayload 报文用内部原值（对照入站 parseToAsn:156 resolveInbound 已落地）。
  - **A4.2.167** 调度 census 零命中 + markError 恒 ERROR 无 WARN 分级 + 重试仅手工 retry（重跑实证手动循环）。
  - **A4.2.168** activate 任意态→PRODUCTION 零守卫 + 推进 mutation/pass-rate/认证清单零实现 + goLiveDate 零 writer。
  - **A4.2.169** findExpiringCertificates 仅查询 + 停用调度 census 零命中（仅伙伴档案 deactivate mutation 非相关命中）。

---

## 5. 符合性结论（七项存疑点裁决）

### 5.1 七项裁决矩阵

| 工作项 | 来源存疑点 | §2 判据命中分支 | 运行时裁决 | finding 衔接 |
|--------|-----------|----------------|-----------|-------------|
| **A4.2.163** | A1.47 §7 SP-1 isActive=false 派发 | 确认仍派发 | **确认仍派发**（Registry 无 isActive 过滤 + findFormatByCode 仅判 null） | 维持 P2-RC-062（arm-index :267） |
| **A4.2.164** | A1.47 §7 SP-2 needsWebService 异步队列 | 确认无异步派发 | **确认无异步派发**（同步直发 + 零调用方 + 调度 census 零命中） | 维持 reuse P1-MA2-073（arm-index :558） |
| **A4.2.165** | A1.47 §7 SP-3 入站自动 archive | 确认无自动 archive | **确认无自动 archive**（parseToAsn 零调用 + RECEIVED-only 守卫 + 重跑实证） | 维持 P2-RC-064（arm-index :269） |
| **A4.2.166** | A1.47 §7 SP-4 出站编码映射 | 确认无出站映射 | **确认无出站映射**（resolveOutbound 零调用方 + 报文内部原值） | 维持 P2-RC-065（arm-index :270） |
| **A4.2.167** | A1.47 §7 SP-5 自动重试调度 | 确认无自动重试 | **确认无自动重试**（调度零命中 + 恒 ERROR 无 WARN + 仅手工 retry） | 维持 reuse P1-MA2-073（arm-index :558） |
| **A4.2.168** | A1.47 §7 SP-6 activate 门槛绕过 | 确认可绕过 | **确认可绕过**（零状态守卫 + 门槛零实现 + goLiveDate 零 writer） | 维持 P1-RC-080（arm-index :266） |
| **A4.2.169** | A1.47 §7 SP-7 证书过期停用 | 确认无自动停用 | **确认无自动停用**（仅查询 + 停用调度 census 零命中） | 维持 P2-RC-068（arm-index :273） |

### 5.2 裁决分支汇总

- **七项全部确认隐式机制缺失**（A4.2.163-169）——与 A1.47 §7 静态判定**无一翻转**，运行时未发现任何 grep 遗漏的隐式机制（isActive 过滤/异步队列/自动 archive/出站映射/自动重试/状态门槛守卫/证书停用调度）。
- **七项全部按 A1.47 §6 衔接维持既有 finding 分级**（P1-RC-080 + P2-RC-062/064/065/068 + reuse P1-MA2-073），无未经比对新建。P2-RC-063/066/067 无对应 §7 SP 行（A1.47 §7 仅 SP-1..SP-7），不在本切片范围（A1.47 §6 已裁决，本报告不重复）。
- **零升级触发 MR0**（运行时未发现活跃数据破坏——七项均为功能缺失/missing-automation 类，A1.47 §7 P0 评估一致：b2b 子系统 config-gated OFF 默认 + MFT transport Mock-only Deferred 控制暴露面）。
- **零测试侧补充**（纯只读审计，无代码变更）。

---

## 6. 与 arm-index 衔接（维持既有分级裁决）

### 6.1 比对表

| 本切片存疑点 | 比对 arm-index | 裁决 | 差异依据 |
|-------------|---------------|------|---------|
| A4.2.163 isActive 派发 | `P2-RC-062`（:267，A1.47 新建） | **维持 P2（登记不强制，修复归 MR1）** | 同根因同控制点：Registry/Processor 不查 format.isActive。本报告运行时确认双路径均无 isActive 过滤 |
| A4.2.164/167 异步派发 + 自动重试 | `P1-MA2-073`（:558，reuse 注记） | **维持 reuse resolved-via-deferral 注记（修复归 MR1 与 deferral 裁决协同，不重开）** | 同根因同控制点：出站自动化缺失。本报告运行时确认 needsWebService 零调用方 + 调度 census 零命中 + markError 恒 ERROR + 仅手工 retry |
| A4.2.165 入站自动 archive | `P2-RC-064`（:269，A1.47 新建） | **维持 P2（登记不强制，修复归 MR1）** | 同根因同控制点：parseToAsn 后留 RECEIVED。本报告运行时确认零 archive 调用 + RECEIVED-only 守卫 + ACKNOWLEDGED→archive 重跑实证 |
| A4.2.166 出站映射 | `P2-RC-065`（:270，A1.47 新建） | **维持 P2（登记不强制，修复归 MR1）** | 同根因同控制点：generatePayload 未调 resolveOutbound。本报告运行时确认 resolveOutbound 全域零调用方 + 报文内部原值 |
| A4.2.168 activate 门槛 | `P1-RC-080`（:266，A1.47 新建） | **维持 P1（Q4 强制实现，修复归 MR1）** | 同根因同控制点：伙伴上线状态机+门槛全缺。本报告运行时确认 activate 零守卫 + 推进/pass-rate/认证清单零实现 |
| A4.2.169 证书停用 | `P2-RC-068`（:273，A1.47 新建） | **维持 P2（登记不强制，修复归 MR1）** | 同根因同控制点：MFT 边界。本报告运行时确认 findExpiringCertificates 仅查询 + 停用调度 census 零命中 |
| （声明）A4.2.163-169 其他 finding | `P2-RC-063`/`P2-RC-066`/`P2-RC-067`（:268/:271/:272） | **不在本切片存疑点范围** | P2-RC-063（非终态幂等返回）/ P2-RC-066（webhook REST 路径）/ P2-RC-067（retry 清 error）对应 A1.47 §7 未列入 SP-1..SP-7 的静态判定维度（A1.47 §6 已裁决），本计划 Non-Goals 不覆盖，仅声明维持 |
| （声明）P1-MA2-088 | resolved R1.28（:573） | **不重审** | A1.47 §9 已声明复用其行为证据；本报告仅引用 webhook 幂等/确定性 code 作为 A4.2.165 主路径佐证 |

### 6.2 新 finding 清单

- **无**（零新 finding；七项全部经 grep arm-index 同域同控制点比对后给出「维持既有分级」裁决，符合 A1.47 §6「禁止未经比对新建」要求 + 计划 Phase 2 决策项）。

### 6.3 复用 finding 交叉引用注记（追加 RC A4.2.163-169 运行时确认，见 arm-index 对应段落）

- **P1-RC-080**（:266，UC-B2B-007）：追加 RC A4.2.168 运行时确认注记——`ErpB2bPartnerProfileBizModel#activate:22-27` 任意态→PRODUCTION 零状态守卫（requireEntity + setStatus("PRODUCTION") + updateEntity，未设 goLiveDate）+ `#suspend:31-36`/`#deactivate:40-45` 零守卫（deactivate 未设 archivedAt）+ 推进 mutation census 零命中（grep goLiveDate/passRate/advanceTo/promoteTo/setStatus("TESTING")/setStatus("CERTIFIED")/checklist 零业务命中）+ `ErpB2bTestExchangeBizModel`/`ErpB2bCertificationChecklistBizModel` 各 17 行 CRUD 桩（通过率≥90% + 认证清单全必检项门槛零实现）。维持 P1（Q4 强制实现，修复归 MR1；纯 BizModel 代码逻辑预授权不触 ask-first，ORM goLiveDate/archivedAt/status 字段已就绪）。
- **P2-RC-062**（:267，UC-B2B-001）：追加 RC A4.2.163 运行时确认注记——`ErpB2bEdiRegistry#findOutboundProviders:59-68`/`#findInboundProviders:71-80` 遍历 providerMap 仅经 getApplicability 过滤零 ErpB2bEdiFormat.isActive 查询 + `ErpB2bEdiDocCreateOutboundProcessor#findFormatByCode:91-97` 仅 eq("code") 判 null 不查 isActive → inactive 格式仍参与派发运行时确认成立。维持 P2（登记不强制，修复归 MR1）。
- **P2-RC-064**（:269，UC-B2B-003）：追加 RC A4.2.165 运行时确认注记——`ErpB2bAsnHandleInboundWebhookProcessor#parseToAsn:87-171` 成功路径零 archive 调用（EdiDoc 留 RECEIVED）+ archive 调用点 census 仅 `ErpB2bAsnMatchPurchaseOrderProcessor:92`（独立 PO 匹配流非 webhook 隐式 hook）+ `ErpB2bEdiDocBizModel#archive:157-168` 守卫 :160-161 仅 RECEIVED→ARCHIVED 合法 + 重跑 `TestErpB2bEdiPosting#testArchiveAfterAcknowledged:106-123` PASS（ACKNOWLEDGED→archive 抛 NopException :119 + 状态不变 :121-122）。维持 P2（登记不强制，修复归 MR1）。
- **P2-RC-065**（:270，UC-B2B-004）：追加 RC A4.2.166 运行时确认注记——`UblInvoiceEdiProvider` 注入 codeMappingResolver（:34-35）但 `#generatePayload:51-84` 零 resolveOutbound 调用 + grep resolveOutbound 跨 module-b2b 仅定义 1 命中（`CodeMappingResolver:42-52`）零生产调用方 + 报文用内部原值（对照入站 `parseToAsn:156` resolveInbound 已落地）。维持 P2（登记不强制，修复归 MR1）。
- **P2-RC-068**（:273，UC-B2B-008）：追加 RC A4.2.169 运行时确认注记——`ErpB2bMftCertificateBizModel#findExpiringCertificates:39-50` @BizQuery 仅查询（isActive=true + expiresAt≤threshold + limit 200）零停用写入 + 停用调度 census 零命中（grep cron/scheduler/deactivat/Job 仅 `ErpB2bPartnerProfileBizModel#deactivate:40` 伙伴档案 mutation 非相关）——证书过期自动停用缺失运行时确认成立。维持 P2（登记不强制，修复归 MR1）。
- **P1-MA2-073**（:558，UC-B2B-002/006）：追加 RC A4.2.164/167 运行时确认注记——`ErpB2bEdiDocCreateOutboundProcessor#createOutbound:39-71` 同步直发零 needsWebService 分支 + needsWebService 调用点 census 仅定义/实现（`UblInvoiceEdiProvider:93-95`/`UblDespatchAdviceEdiProvider:142-144` 均 return false）/ORM 字段，零生产调用方 + module-b2b 调度/队列 census 零命中（autoRetry/@Scheduled/Quartz/ScheduledExecutor/ScheduledTask/@Job/@Scheduler 零命中）+ `markError:97-108` 恒 blockingLevel=ERROR 无 WARN 分级（:102）+ 重试仅手工 `retry:112-126`（重跑 `testErrorAndRetryCycle:76-103` PASS）——无异步派发/无自动重试运行时确认成立。维持 resolved-via-deferral 注记（修复归 MR1 与 deferral 裁决协同，不重开）。

---

## 7. 过程纪律自检

- [x] **checker 退出码门控核查**：本报告为纯只读运行时确认（**零生产代码/ORM/api.xml/view.xml/config 默认值/真相源变更，零测试侧补充**），checker 复跑 **actual == baseline，零漂移**（R1a=0/R1b=0/R1c=0/R1d=14/R2a=34/R2b=229/R2c=1382/R2d=34/R3=5/R4=0/R5=0/R6=2/R7=0/R8=0/R10=6/R11=0/R12a=69/R12b=66/R12c=40，EXIT=0；与 `docs/audits/compliance-baseline.md` §BASELINE 机器可读块逐项一致）。区分门控退出码 vs 纯 reporter 退出码——checker 脚本是纯 reporter（退出码恒 0），真正门控在 CI workflow 解析 actual > baseline。
- [x] **closure-audit 独立性声明**：本报告的 closure audit 将由独立子代理（新会话，不重用执行者上下文）执行，执行者不自我审计。
- [x] **与 arm-index 交叉去重声明**：本报告全部 7 项运行时裁决已按 §去重协议 grep arm-index 同域同控制点后给出「维持既有分级」结论（P1-RC-080 + P2-RC-062/064/065/068 + reuse P1-MA2-073），**无未经比对直接新建的 finding**。
- [x] **只读审计纪律声明**：零生产代码/ORM/api.xml/view.xml/真相源变更 + 零测试侧补充；非目标项（P1-RC-080 / P2-RC-062~068 修复 / P1-MA2-073 重新实现）归 MR1 R1.0 展开器（P1-RC-080 Q4 强制实现；P2 登记不强制；MR1 R1.0 不自动启动，待另行人工裁决）。
- [x] **测试运行验证**：`mvn test -pl module-b2b/erp-b2b-service` → **Tests run: 33, Failures: 0, Errors: 0, Skipped: 0 — BUILD SUCCESS**（2026-08-08 实测；TestErpB2bEdiPosting=3 / TestErpB2bAsnInbound=7 / TestErpB2bAsnInventoryIntegration=6 / TestErpB2bMftTransport=5 / TestErpB2bAsnCrudSmoke=5 / TestErpB2bEdiEnvelope=7 全绿）。

---

## 8. 报告 9 段完整性自检

| # | 段落 | 状态 |
|---|------|------|
| 1 | 存疑点清单与判据（A1.47 §7 七项 + 判据） | ✅ §1 |
| 2 | 运行时证据采集（L3 file:line + L4 强断言，七项逐项） | ✅ §2 |
| 3 | 测试证据汇总（L4 Test*.java + 断言强度） | ✅ §3 |
| 4 | 运行时行为证据（L5 复用 A1.47 §4 + 本切片差异） | ✅ §4 |
| 5 | 符合性结论（七项裁决矩阵 + §2 判据命中分支） | ✅ §5 |
| 6 | 与 arm-index 衔接（维持既有分级裁决 + 交叉引用注记 + 排除声明） | ✅ §6 |
| 7 | 过程纪律自检（checker actual==baseline + 独立性 + 交叉去重 + 只读纪律） | ✅ §7 |
| 8 | 报告 9 段完整性自检 | ✅ §8 |
| 9 | 与既有 MA1/A1.x 报告差异增量声明 | ✅ §0 |

**9 段齐全**——本报告可定稿。

---

## 整体裁决

**PASS（七项存疑点全数收口，七项全部确认隐式机制缺失，零新 finding / 不触发 MR0）**：

- **A4.2.163-169 七项运行时确认全部证实 A1.47 §7 静态判定**——运行时未发现任何 grep 遗漏的隐式机制：inactive 格式仍被 Registry 派发（双路径零 isActive 过滤）、needsWebService 异步队列零实现（零调用方 + 调度 census 零命中）、入站 EdiDoc 留 RECEIVED 无自动 archive（RECEIVED-only 守卫 + ACKNOWLEDGED→archive 重跑实证抛异常状态不变）、出站报文用内部原值（resolveOutbound 全域零调用方）、无自动重试（调度零命中 + markError 恒 ERROR + 仅手工 retry 重跑实证）、activate 任意态→PRODUCTION 可绕过（零守卫 + 门槛零实现）、证书过期无自动停用（仅查询能力）。
- **七项全部衔接维持既有 finding 分级**：P1-RC-080（A4.2.168，Q4 强制实现）/ P2-RC-062（A4.2.163）/ P2-RC-064（A4.2.165）/ P2-RC-065（A4.2.166）/ P2-RC-068（A4.2.169）登记不强制 + reuse P1-MA2-073（A4.2.164/167，resolved-via-deferral 注记，修复归 MR1 协同）——Q4 强制实现义务（P1-RC-080）归 MR1 R1.0 展开器，无未经比对新建 finding。
- **零升级触发 MR0**——运行时未发现活跃数据破坏（七项均为功能缺失/missing-automation 类，A1.47 §7 P0 评估一致）。
- **A1.47 §7 七项静态判定无一翻转**。**本审计不实施生产代码修复、无测试侧补充**（结果表面 = 本报告 + arm-index 交叉引用注记 + roadmap/log 同步）。
