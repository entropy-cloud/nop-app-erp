# A4.2.113-A4.2.118 + A4.2.120-A4.2.123 projects-F1/F2/F3 成本归集/预算/结算/多币种/调度/质保金/转固 运行时行为确认报告

> Report Status: done
> 里程碑：MA4（requirement-compliance mission 运行时展开器，Work Items A4.2.113-A4.2.118 + A4.2.120-A4.2.123）
> 域/功能切片：projects / 立项与成本归集（A1.34）/ 预算与 DAG（A1.35）/ 结算与看板（A1.36）
> 来源：plan `docs/plans/2026-08-07-2359-3-rc-ma4-a4-2-113-123-projects-f1-f2-f3-runtime.md`
> Source Audits: `docs/audits/2026-08-05-2200-2-rc-ma1-a1-34-projects-f1-startup-cost-collection.md`（A1.34 §7 SP-1..SP-5）/ `docs/audits/2026-08-05-2200-3-rc-ma1-a1-35-projects-f2-budget-dag.md`（A1.35 §7 SP-1..SP-4）/ `docs/audits/2026-08-05-2330-1-rc-ma1-a1-36-projects-f3-settlement-dashboard.md`（A1.36 §7 SP-1..SP-4）
> 审计类型：只读运行时确认（零生产代码/ORM/api.xml/view.xml/config 默认值/真相源变更）
> Skill：`docs/skills/multi-dimensional-audit-prompt.md`
> 产出时间：2026-08-07

> 结论速览：十项存疑点经 live code 实测全数收口——A1.34/A1.35/A1.36 静态判定**无一翻转**。**0 新 finding / 不触发 MR0 / 不归 MR1（本审计）**。维持既有分级：4 项 P1 维持（A4.2.113→P1-RC-050+P1-RC-051 / A4.2.115→P1-RC-049 / A4.2.121→P1-RC-053 / A4.2.122→P1-RC-052，修复义务归 MR1 R1.0 展开器）+ 2 项 P2 维持（A4.2.114→P2-RC-048 / A4.2.120→P2-RC-050）+ 2 项多币种投影维持（A4.2.116→P1-MA1-010 / A4.2.120→P1-MA1-010 协同）+ 3 项边界/契约主路径闭合（A4.2.117 resumeProject 迁移实现存在 + 归集恢复行为正确[与 P1-RC-050 协同] / A4.2.118 STRICT 抛错事务回滚主路径闭合 / A4.2.123 转固 data map 字段约定完整主路径闭合）。**A4.2.119 MR1 successor 阻塞排除**（P1-RC-049 未落地致运行时探查结构不可达，保留 todo 待 MR1 回队，镜像 A4.2.3/A4.2.79 先例）。**A1.36 SP-5 dashboard orgId 排除**（已由 A4.1.25/A4.2.10 闭合）。

---

## 0. 业财过账保护区域探针纪律前置声明

projects 域工时/结算过账经 finance 引擎间接产生凭证（`TimesheetPostingDispatcher` → `IErpFinVoucherBiz.post REQUIRES_NEW` + `ProjectSettlementPostingDispatcher` → finance 过账引擎）。本审计为**只读运行时确认**——不改 ExpenseCostAggregator / PostingDispatcher / ProjectSettlementAcctDocProvider / ORM / batch.xml / 任何过账逻辑，不触及业财过账保护区域写入侧。

**READ-ONLY 探针标记（业财过账行为探针）**：
- **A4.2.113**（ExpenseCostAggregator 状态/超预算守卫缺口）——只读确认 `refreshExpenseCost:60-64` 守卫缺失，不改 Aggregator（修复归 MR1 触 ExpenseCostAggregator 业财过账路径须 ask-first）。
- **A4.2.116**（多币种 exchangeRate=ONE 工时过账折算失真）——只读确认 `TimesheetPostingDispatcher.buildEvent:130` 硬编码，不改 PostingDispatcher（修复归 MR1 触过账 buildEvent 须 ask-first）。
- **A4.2.120**（多币种 ProjectPnl exchangeRate=ONE 金额偏差）——只读确认 `ProjectPnlCalculator:105` 硬编码，不改 Calculator（PnL 非总账凭证，修复归 MR1 纯 BizModel 预授权）。
- **A4.2.122**（质保金凭证分录缺失）——只读确认 `ProjectSettlementAcctDocProvider` 无质保金分录，不改 Provider（修复归 MR1 触 ProjectSettlementAcctDocProvider/VoucherFact 须 ask-first）。

**A4.2.119 MR1-successor 阻塞排除**：A4.2.119（A1.35 SP-4 采购路径物料归集实现后 budgetChecker.check 接入）内容描述明确「P1-RC-049 successor 落地后」——P1-RC-049（物料归集）属 MR1 未实现 finding，运行时探查结构上不可达（与 A4.2.3/A4.2.79 MR1 阻塞先例同型），保留 `todo` 待 MR1 回队，本报告不覆盖。

**A1.36 SP-5 dashboard orgId 排除**：A1.36 SP-5（看板 getProjectGrossMargin Σprofit/Σrevenue 聚合口径 + orgId/行级权限）已由 **A4.1.25 + A4.2.10** 闭合（运行时确认经 11 DashboardBizModel 全集 census 复核 + 新建 P2-RC-086 watch-only，详见 `docs/audits/2026-08-06-1926-rc-ma4-dashboard-orgid-rowlevel-permission-leak.md`）。本计划不重复覆盖。

---

## 1. 逐项运行时证据 + §2 判据裁决

### A4.2.113（A1.34 SP-1 + A1.35 SP-1 + A1.35 SP-3 合并）ExpenseCostAggregator ON_HOLD/超预算违规归集累积确认 — **维持 P1-RC-050 + P1-RC-051**

**探查目标**：确认 `ExpenseCostAggregator.refreshExpenseCost` 仅查 project==null 不校验状态[ON_HOLD/COMPLETED/CANCELLED] 也不调 budgetChecker；确认 closeProject 前触发是否归集 ON_HOLD/超预算期间单据；确认 STRICT 模式不抛错（budgetChecker 未被调用）。**触及业财过账行为探针——只读确认，不改 ExpenseCostAggregator。**

**Live code 实测**（`module-projects/erp-prj-service/src/main/java/app/erp/prj/service/cost/ExpenseCostAggregator.java`）：
- `refreshExpenseCost:56-126` 主入口。
- `loadProject:60-64`：**仅 project==null 抛 `ERR_PROJECT_NOT_REFERENCEABLE`，从不校验项目 status**（ON_HOLD/COMPLETED/CANCELLED 项目报销行仍被归集）——**P1-RC-050 状态守卫缺口确认活跃**。
- 全方法体 `:66-126`（`findApprovedClaims` + `findLinesForProject` + 幂等 `existsLine:171-177` + `saveExpenseLine:179-195` + `setCostCategory(COST_CATEGORY_EXPENSE):189`）**无任何 `budgetChecker.check(...)` 调用**——**P1-RC-051 报销路径预算检查缺口确认活跃**。
- `setExchangeRate(BigDecimal.ONE):112` 单币种路径（与 A4.2.116 多币种投影协同）。
- closeProject 触发链：`ErpPrjProjectCloseProjectProcessor` 经 `ErpPrjProjectRefreshActualCostProcessor` → `ExpenseCostAggregator.refreshExpenseCost`（closeProject 前 config-gated `erp-prj.expense-aggregation-enabled` 默认 true）。
- `ErpPrjProjectBizModel.requireReferenceable:62-73` 仅 OPEN 通过实现正确，**但生产代码零调用方**（grep 全 module-projects/inventory/purchase/sales 生产代码 `requireReferenceable` 仅 IErpPrjProjectBiz 接口声明 `:14,27,36` + BizModel 实现 `:35,64` + 测试 `:253`，零跨域消费方）。

**裁决**：**维持 P1-RC-050 + P1-RC-051 P1**（§2 P1① 功能实质偏离验收标准——状态守卫 + 预算守卫双缺口同站点同控制点，UC-PRJ-09 AC-①「拒绝新费用归集」+ UC-PRJ-04 AC-①「报销审核预算检查」均未满足；违规归集行经幂等去重 `existsLine` 累积使 actualCost 虚增 + PnL 失真，GL 借贷平衡不受影响——不触发 MR0）。**修复归 MR1 触 ExpenseCostAggregator 业财过账路径须 ask-first**（roadmap §横切关注点 #5）。运行时确认闭合维持分级。

---

### A4.2.114（A1.34 SP-2）requireReferenceable 是否被任何 delta/未来定制消费确认 — **维持 P2-RC-048**

**探查目标**：确认全 module-projects/inventory/purchase/sales 生产代码 grep requireReferenceable 零调用方；确认 xbiz 是否暴露为 @BizMutation + AMIS view.xml 是否含 mutation 按钮（前端可达但跨域消费方仍缺失）。

**Live code 实测**：
- grep 全 module-projects/inventory/purchase/sales 生产代码 `requireReferenceable`：**唯一引用 = `IErpPrjProjectBiz.java:14,27,36`（接口声明）+ `ErpPrjProjectBizModel.java:35,64`（实现）+ 测试 `TestErpPrjBudgetAndCollection.java:253`**。跨域 inventory/purchase/sales 生产代码**零命中**。
- `ErpPrjProjectBizModel.requireReferenceable:62-73` 标注 `@BizMutation`（`:63`）——**xbiz 经 codegen 从 @BizMutation 自动暴露为 GraphQL mutation**，故**前端经 GraphQL 原则上可达**（xbiz 为 build 时生成非静态文件，@BizMutation 注解即 xbiz 暴露契约）。
- grep `module-projects/erp-prj-web` `requireReferenceable`：**零命中**——**AMIS view.xml 无 mutation 按钮消费**。
- 工时路径内联自有 `ErpPrjTimesheetSubmitProcessor.validateProjectReferenceable:65-78`（非调 requireReferenceable Facade）；费用路径完全不校验状态（P1-RC-050）。

**裁决**：**维持 P2-RC-048 P2**（§2 P2① 死 flag 部署启用决策——@BizMutation 暴露故 API 经 GraphQL 前端可达，但零 AMIS 按钮 + 零跨域生产消费方，主路径[工时内联校验]OK 跨域/费用门控弱，watch-only）。**修复归 MR1 纯 BizModel/xbiz 预授权不触 ask-first，登记不强制**。

---

### A4.2.115（A1.34 SP-3）物料归集经 inventory 配置触发是否部分可达确认 — **维持 P1-RC-049**

**探查目标**：确认 inventory 模块零引用 ErpPrjCostCollection + `InvPostingDispatcher.resolveBusinessType` 无 PROJECT_COST 分支；确认 config-gated 路径（`erp-prj.material-collection-enabled` / `erp-inv.project-collection`）是否存在但默认关闭。

**Live code 实测**：
- grep `module-inventory` `PROJECT_COST|ErpPrjCostCollection|material-collection-enabled|project-collection`：**零命中**——inventory 模块对 projects 物料归集**完全无引用**。
- `InvPostingDispatcher.resolveBusinessType:152-179`（`module-inventory/erp-inv-service/.../posting/InvPostingDispatcher.java`）：仅 emit `PURCHASE_INPUT`（`:168-170` INCOMING）/ `SALES_OUTPUT`（`:171-173` OUTGOING）/ `MANUFACTURING_RECEIPT`（`:175-177` MANUFACTURING），采购退货/销售退货/维护领料/制造领料 `:158-163` 返回 null——**全文零 PROJECT_COST 分支**，从不写项目成本。
- config-gated 路径普查：grep `module-projects` `material-collection-enabled|material.collection|project.collection|erp-prj.material`：**零命中**——`ErpPrjConstants.java` config 键全集（budget-control-mode / default-labor-cost-rate / default-payroll-subject-id / expense-aggregation-enabled / pnl-calc-cron / pnl-auto-calc-enabled / settlement-require-approval / task-dependency-max-depth / task-strict-predecessor-check / strict-project-task-completion-check / strict-project-start-precheck）**无物料归集相关 config key**。`ErpInvConstants` 亦无 project-collection config。**config-gated 路径不存在（非"存在但默认关闭"）**——比审计假设更强：结构上完全不可达。

**裁决**：**维持 P1-RC-049 P1**（§2 P1① 功能完全缺失——物料归集结构上不可达，inventory 模块零引用 + InvPostingDispatcher 无 PROJECT_COST 分支 + 零 config-gate key，Q4 强制实现）。**修复归 MR1 纯 BizModel/可能 ORM 预授权**（跨域 inventory→projects 写契约须 ask-first）。运行时确认闭合维持分级。

---

### A4.2.116（A1.34 SP-4）多币种 exchangeRate=ONE 在多币种项目工时过账折算失真确认 — **维持 P1-MA1-010 投影**

**探查目标**：确认 `TimesheetPostingDispatcher.buildEvent` exchangeRate=ONE 硬编码 + `ErpPrjCostCollection.exchangeRate` 经 `ExpenseCostAggregator:112 setExchangeRate(ONE)` 单币种路径；确认多币种项目工时过账凭证 amountFunctional 折算失真（GL 借贷平衡不破坏）。**触及业财过账行为探针——只读确认。**

**Live code 实测**：
- `TimesheetPostingDispatcher.buildEvent:130`（`module-projects/erp-prj-service/.../posting/TimesheetPostingDispatcher.java`）：**`event.setExchangeRate(BigDecimal.ONE)` 硬编码**——不解析 timesheet.currencyId 与项目本位币（或法人本位账套）的汇率。
- `:125` `event.setBusinessType(ErpFinBusinessType.PROJECT_COST_COLLECTION)` + `:129` `event.setCurrencyId(timesheet.getCurrencyId())`——currencyId 传入但 exchangeRate 固定 ONE。
- `:128` `event.setAcctSchemaId(resolveAcctSchemaId(timesheet.getOrgId()))`——经 `AcctSchemaResolver.resolvePrimarySchemaId` 按法人解析本位账套，但汇率仍 ONE。
- `ExpenseCostAggregator:112` `newHead.setExchangeRate(BigDecimal.ONE)`——费用归集头同样单币种路径。
- `ProjectPnlCalculator:105` `pnl.setExchangeRate(BigDecimal.ONE)`——PnL 快照同型（与 A4.2.120 协同）。

**裁决**：**维持 P1-MA1-010 投影**（多币种折算失真——USD 项目 + CNY 本位账套场景下工时过账凭证 amountFunctional 按 exchangeRate=ONE 折算，与 P1-MA1-010 多币种四件套同根因[ORM 元数据层 propId + 业务逻辑层汇率逻辑]；GL 借贷平衡不破坏仅折算失真，不触发 MR0）。**修复归 MR1 与 P1-MA1-010 多币种四件套协同，触过账 buildEvent 须 ask-first**。运行时确认闭合维持投影分级。

---

### A4.2.117（A1.34 SP-5）ON_HOLD→OPEN resumeProject 迁移 + resume 后归集恢复确认 — **主路径行为正确 + watch-only residual（零测试覆盖）**

**探查目标**：确认 `resumeProject` 迁移 setStatus(OPEN) ✅；确认 resume 后工时/费用归集是否正确恢复（与 SP-1 同根因——若 ON_HOLD 期间归集未暂停[见 A4.2.113]，resume 后行为差异不显著）；确认零测试覆盖缺口。

**Live code 实测**：
- `ErpPrjProjectBizModel.resumeProject:112-114`（`module-projects/erp-prj-service/.../entity/ErpPrjProjectBizModel.java`）：Facade 单行委托 → `resumeProjectProcessor.resumeProject(projectId, context)`。
- `ErpPrjProjectResumeProjectProcessor.resumeProject:23-26`（`.../processor/ErpPrjProjectResumeProjectProcessor.java`）：`transition(projectId, PROJECT_STATUS_ON_HOLD, PROJECT_STATUS_OPEN, context)`——**ON_HOLD→OPEN 守卫齐全**（`:31-35` 非 ON_HOLD 抛 `ERR_PROJECT_NOT_CLOSABLE`）+ `setStatus(OPEN):36` + `updateEntity:37`。**迁移实现存在 ✅**。
- resume 后归集恢复行为：与 A4.2.113 协同——ON_HOLD 期间工时路径 `ErpPrjTimesheetSubmitProcessor.validateProjectReferenceable:65-78` 拒绝非 OPEN（ON_HOLD 拒绝），resume 后恢复 OPEN 则工时归集恢复；费用路径 `ExpenseCostAggregator` 不校验状态（P1-RC-050），故 ON_HOLD 期间与 resume 后费用归集行为差异不显著（均归集）。
- 零测试覆盖：grep `module-projects/erp-prj-service/src/test` `resumeProject|ON_HOLD.*OPEN`：**零命中**——ON_HOLD→OPEN resume 迁移零行为测试。

**裁决**：**主路径行为正确闭合**（resumeProject ON_HOLD→OPEN 迁移实现存在 + 守卫齐全；归集恢复行为正确——与 A4.2.113 维持 P1-RC-050 协同：状态守卫修复[MR1]后 resume 语义完整）。**登记 watch-only residual**（零测试覆盖归 MR1 测试补充——纯测试补强预授权不触 ask-first）。

---

### A4.2.118（A1.35 SP-2）STRICT 模式下工时提交超预算实际拒绝运行时行为确认 — **主路径行为正确闭合**

**探查目标**：确认 `BudgetChecker.check` STRICT 模式抛 `ERR_BUDGET_EXCEEDED` + `ErpPrjTimesheetSubmitProcessor.submit` `runBudgetCheckHook` 在 setStatus SUBMITTED 之后、`timesheetDao().updateEntity` 之前触发；确认 STRICT 抛错触发 @BizMutation 事务回滚使 timesheet 保持 UNSUBMITTED。

**Live code 实测**：
- `BudgetChecker.check:44-69`（`module-projects/erp-prj-service/.../cost/BudgetChecker.java`）：`:56 used = sumUsedAmount(projectId)` + `:57 projected = used.add(addAmount)` + `:58 if (projected.compareTo(total) > 0)` + `:59 ErpPrjConfigs.budgetControlStrict()` 返回 true → `:60-64` 抛 `NopException(ERR_BUDGET_EXCEEDED)` 带 4 param（`ARG_PROJECT_ID` / `ARG_BUDGET_TOTAL` / `ARG_BUDGET_USED` / `ARG_AMOUNT`）。
- `ErpPrjConfigs.budgetControlStrict():42-44`：`BUDGET_MODE_STRICT.equals(budgetControlMode())`；`DEFAULT_BUDGET_CONTROL_MODE = BUDGET_MODE_WARNING`（默认 WARNING，STRICT 需显式配置）。
- `ErpPrjTimesheetSubmitProcessor.submit:34-61`（`.../processor/ErpPrjTimesheetSubmitProcessor.java`）：`:57 setStatus(SUBMITTED)` → `:58 runBudgetCheckHook(timesheet, costAmount)` → `:59 timesheetDao().updateEntity(timesheet)`。**runBudgetCheckHook 在 setStatus SUBMITTED 之后、updateEntity 持久化之前触发**。
- `runBudgetCheckHook:103-105` → `budgetChecker.check(timesheet.getProjectId(), costAmount)`。
- submit 经 `@BizMutation`（BizModel 层）自动包装事务——STRICT 抛 `ERR_BUDGET_EXCEEDED` 触发事务回滚，timesheet 状态变更不持久化（保持 UNSUBMITTED）。
- L4 强测：`TestErpPrjBudgetAndCollection#testStrictModeRejectsOverBudget:95-119` 断言 `ERR_BUDGET_EXCEEDED` 抛出 + errorCode 精确比对（`:115`）。

**裁决**：**主路径行为正确闭合**（STRICT 抛错回滚 timesheet 状态不迁移——事务边界完整回滚，异常为 ERR_BUDGET_EXCEEDED 带 4 param；L4 强测覆盖）。无 finding 新建。

---

### A4.2.120（A1.36 SP-1）多币种项目 exchangeRate=ONE 在 ProjectPnl 金额偏差确认 — **维持 P2-RC-050 + P1-MA1-010 协同**

**探查目标**：确认跨币种 Billing/CostCollection 经 amountFunctional（已折算到本位币）聚合 + PnL exchangeRate=ONE 快照；确认多币种项目 PnL revenueAmount/cost 因 source 币种混合产生精度偏差（GL 借贷平衡不破坏）。**触及业财过账行为探针——只读确认。**

**Live code 实测**：
- `ProjectPnlCalculator.refreshPnl:57-132`（`module-projects/erp-prj-service/.../pnl/ProjectPnlCalculator.java`）：`:105 pnl.setExchangeRate(BigDecimal.ONE)` 硬编码。
- `sumRevenue:144-161`：读 `ErpPrjBilling.amountFunctional`（`:158`）——Billing 头 amountFunctional 已由 finance 过账折算到本位币。
- `sumCostByCategory:163-194`：读 `ErpPrjCostCollectionLine.amount`（`:179`）——归集行 amount 经 `ExpenseCostAggregator:112 setExchangeRate(ONE)` + `:114 setAmountFunctional(addedTotal)` 单币种路径。
- Javadoc `:47-48` 显式「多币种基线：聚合使用 amountFunctional（本位币）与行 amount，PnL 快照 exchangeRate=1。跨币种 rollup 精度归 successor」。
- 多币种项目（不同 currencyId Billing + CostCollection）经 amountFunctional 聚合时，source 币种混合 + exchangeRate=ONE 快照致 PnL revenueAmount/cost 与单币种基线产生精度偏差。

**裁决**：**维持 P2-RC-050 P2**（§2 P2① 多币种 PnL 精度偏差边界弱——主路径[单币种项目]exchangeRate=ONE 正确，多币种项目精度偏差非借贷平衡破坏[PnL 非总账凭证]；声明 Q4=(a) 张力但实际影响限于精度）。**与 P1-MA1-010 协同**（多币种四件套同根因）。**修复归 MR1 纯 BizModel[ProjectPnl 折算]预授权不触 ask-first，登记不强制**。运行时确认闭合维持分级。

---

### A4.2.121（A1.36 SP-2）pnl-auto-calc-enabled=true 时 batch 手动触发调度路径确认 — **维持 P1-RC-053**

**探查目标**：确认 config key `erp-prj.pnl-auto-calc-enabled` 默认 false + 无 nop-job 消费 → batch task `pnl-calc.batch.xml` 自动 cron 路径不可达；确认 config=true + 无 nop-job 消费时 batch 是否经其他路径（手动 API）可达 + 记录无自动 cron 触发。

**Live code 实测**：
- `ErpPrjConstants.java:20` `CONFIG_PNL_CALC_CRON = "erp-prj.pnl-calc-cron"` + `:22` `CONFIG_PNL_AUTO_CALC_ENABLED = "erp-prj.pnl-auto-calc-enabled"`。
- `ErpPrjConfigs.java:16` `DEFAULT_PNL_AUTO_CALC_ENABLED = false` + `:70-74 pnlAutoCalcEnabled()` 读 config 默认 false + `:82-84 pnlCalcCron()` 读 config 默认空串。
- grep `module-projects` `nop-job|IJobInvoker|JobBean|@Scheduled|IScheduler`：**零命中**——**无 nop-job/cron/scheduler Job bean 消费 `erp-prj-pnl-calc`**。
- `IErpPrjProjectPnlBiz.java:20` Javadoc「汇总计算可手工触发或经 nop-job（erp-prj-pnl-calc）周期触发」——**期望非实现**（nop-job 消费方零）。
- batch task `pnl-calc.batch.xml`（`module-projects/erp-prj-service/src/main/resources/_vfs/nop/batch-task/prj/pnl-calc.batch.xml`）：`taskName="prj.pnl-calc"` + `loader` 过滤 `status IN ['DRAFT','OPEN','ON_HOLD']` + `processor` 调 `IErpPrjProjectPnlBiz.refreshPnl(item.id, null, null, ...)`——**仅手动可触发**（无 cron 注册 + 无 Job bean 自动调度）。
- 模块元数据 `module-projects/erp-prj-meta/.../_module-meta.json:29` configKey `erp-prj.pnl-auto-calc-enabled` 声明存在——config 键存在但默认 false + 无消费方。

**裁决**：**维持 P1-RC-053 P1**（§2 P1①/P1② PnL 自动计算调度接线缺失——config key 默认 false + 零 nop-job/cron/Job bean 消费，自动 cron 路径不可达；规则评估仅经手动 @BizMutation `ErpPrjProjectPnlBizModel.refreshPnl` 可达；Q4 强制实现[L1「定时任务(nop-job)触发」未满足]）。**修复归 MR1 纯调度接线[batch.xml + nop-job wiring]预授权不触 ask-first**（roadmap §横切关注点 #5）。运行时确认闭合维持分级。

---

### A4.2.122（A1.36 SP-3）质保金 schema 字段在 delta/未来定制消费运行时行为确认 — **维持 P1-RC-052**

**探查目标**：确认 retentionAmount/retentionDueDate ORM 列存在但零 service writer + `ProjectSettlementAcctDocProvider` 无质保金分录；确认经 delta 填充 retentionAmount 后 Provider 是否感知（质保金凭证是否需新增 businessType）。

**Live code 实测**：
- ORM `app-erp-projects.orm.xml:803-804`（`module-projects/model/`）：`retentionAmount`（propId 16，domain="amount"，DECIMAL 20,4）+ `retentionDueDate`（propId 17，DATE）**schema 存在**——生成 `_ErpPrjProjectSettlement.java` getter/setter。
- grep `module-projects/erp-prj-service/src/main` `[Rr]etention`：**零命中**——**零 service writer**（createSettlement Processor 不填 retentionAmount/retentionDueDate，无留存工作流，无到期返还 mutation）。
- `ProjectSettlementAcctDocProvider.createFacts:56-102`（`.../posting/ProjectSettlementAcctDocProvider.java`）：仅 CLOSE 分支（`:67-76` Dr 固定资产 1601 / Cr 在建工程 1603）+ FINAL/INTERIM 分支（`:77-100` Dr 项目成本 5101 + Dr/Cr 本年利润 4103 / Cr 项目收入 6001）——**全文零质保金分录**（无 retentionAmount 读取，无 AR/资金面辅助账分录）。
- `readDecimal/readString/readLong/readBoolean:117-151` 仅读 settlementType/finalRevenue/finalCost/projectId/transferToAsset——**不读 retentionAmount/retentionDueDate**。经 delta 填充 retentionAmount 后 Provider **不感知**（无读取分支）。

**裁决**：**维持 P1-RC-052 P1**（§2 P1① 功能完全缺失——质保金零 writer + 无凭证分录，UC-PRJ-07 AC-④⑤「质保金留存 + 到期返还」未满足；Q4 强制实现；ORM 列已存在不触结构变更）。**修复归 MR1 触 ProjectSettlementAcctDocProvider/VoucherFact 须 ask-first**（质保金凭证若新增 businessType 经 Provider 注册属既有范式，但触 VoucherFact 须 ask-first）。运行时确认闭合维持分级。

---

### A4.2.123（A1.36 SP-4）CLOSE 转固 IErpAstAssetBiz.save data map 契约鲁棒性确认 — **主路径行为正确闭合**

**探查目标**：确认 `ErpPrjProjectSettlementProcessor` 经 `assetBiz.save(data map)` 建 assets 卡片（非专用转固 API）；确认 data map 字段约定[code/name/orgId/acquisitionDate/originalValue/currentValue/residualValue/status]的契约鲁棒性 + 来源项目回链 + 资产域侧字段语义。

**Live code 实测**：
- `ErpPrjProjectSettlementApproveProcessor.approve:26-38`（`.../processor/ErpPrjProjectSettlementApproveProcessor.java`）：`:30-33` `if (SETTLEMENT_TYPE_CLOSE && transferToAsset==true && assetCardId==null) → processor.createAndActivateAsset(settlement, context)`——**CLOSE 转固触发链确认**。
- `ErpPrjProjectSettlementProcessor.createAndActivateAsset:174-194`（`.../processor/ErpPrjProjectSettlementProcessor.java`）：
  - data map 字段约定完整：`code`（`:177` "AST-PRJ-{projectId}-{millis}"）+ `name`（`:178` "{project.name}-转固"）+ `orgId`（`:179` settlement.orgId）+ `acquisitionDate`（`:180` settlement.businessDate）+ `originalValue`（`:181` settlement.finalCost）+ `currentValue`（`:182` settlement.finalCost）+ `residualValue`（`:183` ZERO）+ `status`（`:184` "IN_SERVICE"）。
  - `:186 assetBiz.save(data, context)` 经 `IErpAstAssetBiz` Facade（跨域写合规）。
  - `:188 settlement.setAssetCardId(asset.getId())`——**来源项目回链经 settlement.assetCardId 写回**。
  - `:190-192` 失败抛 `ERR_SETTLEMENT_CAPITALIZATION_FAILED`。
- 来源项目回链：`ErpPrjProjectSettlement.assetCardId` 指向 assets 卡片；资产卡片经 data map 写入（非专用转固 API，但字段约定完整覆盖资产域核心字段[code/name/orgId/acquisitionDate/originalValue/currentValue/residualValue/status]）。
- 红冲回退：`rollbackAssetIfNeeded:196-206`（asset status 回 DRAFT）+ `ErpPrjProjectSettlementReverseSettlementProcessor`（冲销链）。
- L4 强测：`TestErpPrjProjectSettlement`（4 @Test：CLOSE 转固 + 红冲凭证 + 资产卡片）+ E2E `projects-settlement-posting`（凭证行级 Dr1601/Cr1603 强值断言）。

**裁决**：**主路径行为正确闭合**（data map 字段约定完整 + 资产卡片字段完整性 + 来源项目回链经 assetCardId + 跨域经 IErpAstAssetBiz Facade 合规 + L4 强测 + E2E 强值断言）。**登记 watch-only residual**（非专用转固 API 契约脆弱——data map 字段约定靠调用方保证非类型安全，successor 可引入专用转固 API；非活跃数据破坏，watch-only）。

---

## 2. 裁决汇总表

| 存疑点 | 关联 finding | 静态判定 | 运行时裁决 | §2 判据 | 修复归属 |
|--------|-------------|---------|-----------|---------|---------|
| A4.2.113 ExpenseCostAggregator 状态/超预算守卫缺口 | P1-RC-050 + P1-RC-051 | P1 缺陷（同站点双缺口） | **维持 P1**（守卫缺失活跃，违规归集累积，GL 不破坏） | §2 P1① | MR1（触 ExpenseCostAggregator ask-first） |
| A4.2.114 requireReferenceable 死 flag | P2-RC-048 | P2 死 flag | **维持 P2**（@BizMutation 暴露但零 AMIS 按钮 + 零跨域消费方，watch-only） | §2 P2① | MR1（纯 BizModel/xbiz 预授权） |
| A4.2.115 物料归集 PROJECT_COST 不可达 | P1-RC-049 | P1 完全缺失 | **维持 P1**（inventory 零引用 + 无 PROJECT_COST 分支 + 零 config-gate key，结构不可达） | §2 P1① | MR1（跨域契约 + 可能 ORM ask-first） |
| A4.2.116 多币种 exchangeRate=ONE 工时过账 | P1-MA1-010 | P1 投影 | **维持 P1-MA1-010 投影**（buildEvent:130 硬编码 ONE，折算失真 GL 不破坏） | §2 P2①（投影） | MR1（触过账 buildEvent ask-first） |
| A4.2.117 ON_HOLD→OPEN resume 迁移 | — | 边界 | **主路径行为正确闭合**（迁移实现存在 + 守卫齐全）+ watch-only residual（零测试覆盖） | 接受 + watch-only | MR1 测试补充（纯测试预授权） |
| A4.2.118 STRICT 超预算拒绝事务回滚 | — | 边界 | **主路径行为正确闭合**（STRICT 抛错 + 事务回滚 timesheet 不迁移 + 4 param + L4 强测） | 接受 | 无 |
| A4.2.120 多币种 ProjectPnl 偏差 | P2-RC-050 + P1-MA1-010 | P2 精度偏差 | **维持 P2**（exchangeRate=ONE 快照，多币种精度偏差，PnL 非总账 GL 不破坏） | §2 P2① + Q4 张力 | MR1（纯 BizModel[ProjectPnl 折算]预授权） |
| A4.2.121 PnL 调度接线缺失 | P1-RC-053 | P1 调度缺失 | **维持 P1**（config 默认 false + 零 nop-job/Job bean 消费，自动 cron 不可达） | §2 P1①/P1② | MR1（纯调度接线预授权不触 ask-first） |
| A4.2.122 质保金零 writer + 无凭证分录 | P1-RC-052 | P1 完全缺失 | **维持 P1**（ORM 列存在 + 零 service writer + Provider 无质保金分录，经 delta 填充不感知） | §2 P1① | MR1（触 Provider/VoucherFact ask-first） |
| A4.2.123 转固 data map 契约 | — | 边界 | **主路径行为正确闭合**（data map 字段约定完整 + assetCardId 回链 + L4/E2E 强测）+ watch-only residual（非专用 API 契约脆弱） | 接受 + watch-only | successor（专用转固 API） |

---

## 3. 与既有 finding 衔接（arm-index 维持注记）

| Finding | 运行时裁决 | arm-index 衔接 |
|---------|-----------|---------------|
| **P1-RC-049**（物料归集缺失） | 维持 P1（A4.2.115） | arm-index RC 发现追踪分区追加 RC A4.2.115 运行时确认交叉引用注记（inventory 零引用 + InvPostingDispatcher 无 PROJECT_COST 分支 + 零 config-gate key，结构不可达，维持 P1 Q4 强制实现，修复归 MR1 跨域契约 + 可能 ORM ask-first） |
| **P1-RC-050 + P1-RC-051**（ExpenseCostAggregator 状态/超预算守卫双缺口同站点） | 维持 P1（A4.2.113） | arm-index RC 发现追踪分区追加 RC A4.2.113 运行时确认交叉引用注记（refreshExpenseCost:60-64 守卫缺失活跃 + 无 budgetChecker.check 调用，维持 P1，修复归 MR1 触 ExpenseCostAggregator ask-first） |
| **P1-RC-052**（质保金零 writer + 无凭证分录） | 维持 P1（A4.2.122） | arm-index RC 发现追踪分区追加 RC A4.2.122 运行时确认交叉引用注记（ORM 列存在 + 零 service writer + Provider 无质保金分录 + 经 delta 填充不感知，维持 P1，修复归 MR1 触 Provider/VoucherFact ask-first） |
| **P1-RC-053**（PnL 调度接线缺失） | 维持 P1（A4.2.121） | arm-index RC 发现追踪分区追加 RC A4.2.121 运行时确认交叉引用注记（config 默认 false + 零 nop-job/Job bean 消费，自动 cron 不可达，维持 P1，修复归 MR1 纯调度接线预授权不触 ask-first） |
| **P2-RC-048**（requireReferenceable 死 flag） | 维持 P2（A4.2.114） | arm-index RC 发现追踪分区追加 RC A4.2.114 运行时确认交叉引用注记（@BizMutation 暴露 + 零 AMIS 按钮 + 零跨域消费方，维持 P2 watch-only，修复归 MR1 纯 BizModel/xbiz 预授权） |
| **P2-RC-049**（余量公式承诺项） | 不在本计划范围（A4.2.119 MR1-successor 阻塞） | 维持 todo MR1（与 P1-RC-049 物料归集 successor 协同） |
| **P2-RC-050**（多币种 ProjectPnl 精度偏差） | 维持 P2（A4.2.120） | arm-index RC 发现追踪分区追加 RC A4.2.120 运行时确认交叉引用注记（exchangeRate=ONE 快照 + 多币种精度偏差 + PnL 非总账 GL 不破坏，维持 P2 + Q4 张力，修复归 MR1 纯 BizModel[ProjectPnl 折算]预授权） |
| **P1-MA1-010**（多币种四件套 propId） | 投影维持（A4.2.116 + A4.2.120 协同） | 维持 todo MR1（多币种维度，本审计仅投影确认不重审） |
| **P1-MA2-067/068/069/070**（projects 状态机 resolved） | 维持 resolved（不重审，§去重协议） | 维持 resolved 无回退 |
| **P1-MA2-084**（finance 控制引擎 resolved） | 维持 resolved（不重审） | 维持 resolved |

**reuse 不新建**：A4.2.117（resume 迁移）+ A4.2.118（STRICT 抛错事务回滚）+ A4.2.123（转固 data map 契约）无对应既有 finding，主路径闭合/watch-only residual 非新 finding（实现边界观察）。

---

## 4. 多维度裁决自检（multi-dimensional-audit-prompt）

| 维度 | 自检结论 |
|------|---------|
| 需求正确性 | 十项运行时确认均对齐 L1 use-case 验收标准；P1 缺陷项（A4.2.113/115/121/122）功能实质偏离/完全缺失维持 P1 Q4 强制实现；P2 项（A4.2.114/120）主路径 OK 边界弱维持 P2；多币种投影（A4.2.116）折算失真 GL 不破坏；边界项（A4.2.117/118/123）主路径行为正确闭合 |
| 异常路径/失败模式 | A4.2.113 守卫缺失致违规归集累积（非数据丢失，幂等去重可手工清理）/ A4.2.118 STRICT 抛错事务回滚正确（timesheet 不迁移）/ A4.2.122 质保金零 writer 无异常路径（功能缺失非异常吞没） |
| 并发/事务边界 | A4.2.118 @BizMutation 事务回滚完整（submit 经事务包装，runBudgetCheckHook 在 updateEntity 前抛错回滚）/ A4.2.113 幂等 existsLine 去重（无并发破坏活跃数据） |
| 业财过账完整性 | GL 借贷平衡不破坏（A4.2.113 违规归集仅 PnL 失真 / A4.2.116/120 多币种仅折算失真 / A4.2.122 质保金缺失不影响既有凭证）——**不触发 MR0** |
| 验证充分性 | A4.2.118 经 L4 强测 + 事务回滚语义确认 / A4.2.123 经 L4 + E2E 强值断言 / P1/P2 缺陷项经 live code 实测确认活跃 / 多币种投影经 buildEvent:130 + Calculator:105 硬编码实测 |
| 保护区域纪律 | A4.2.113/116/120/122 业财过账探针 READ-ONLY 声明到位，MR1 fix 路径 ask-first 门控（ExpenseCostAggregator / buildEvent / ProjectSettlementAcctDocProvider/VoucherFact）；A4.2.121 调度接线预授权不触 ask-first；A4.2.115 跨域契约 ask-first |
| §去重协议 | 0 新 finding，全部维持既有分级（P1-RC-049/050/051/052/053 + P2-RC-048/050 + P1-MA1-010 投影 + P1-MA2-067/068/069/070/084 维持，无未经比对直接新建的 finding） |

---

## 5. 过程纪律自检

- [x] **checker 退出码门控核查**：本报告为只读运行时确认，**零生产代码/ORM/api.xml/view.xml/config 默认值/真相源变更**（仅新增本报告 + 更新 arm-index + roadmap + plan + log）。checker 无回归风险（actual=baseline，无 .java/.xml/.yaml 生产文件变更）。
- [x] **closure-audit 独立性声明**：本报告的 closure audit 将由独立子代理（新会话，不重用执行者上下文）执行，执行者不自我审计。
- [x] **与 arm-index 交叉去重声明**：本报告全部裁决（P1-RC-049/050/051/052/053 维持 P1 + P2-RC-048/050 维持 P2 + P1-MA1-010 投影维持 + P1-MA2-067/068/069/070/084 维持 resolved）已 grep arm-index 同域同控制点后给出维持裁决（§3），无未经比对直接新建的 finding。A4.2.117/118/123 主路径闭合/watch-only 为实现边界观察非 arm-index finding。
- [x] **A4.2.119 MR1-successor 阻塞排除声明**：A4.2.119（采购路径物料归集实现后 budgetChecker.check 接入）内容明确「P1-RC-049 successor 落地后」，P1-RC-049 属 MR1 未实现 finding，运行时探查结构上不可达（与 A4.2.3/A4.2.79 MR1 阻塞先例同型），保留 `todo` 待 MR1 P1-RC-049 落地后回队，本报告不覆盖（见 Deferred But Adjudicated）。
- [x] **A1.36 SP-5 dashboard orgId 排除声明**：A1.36 SP-5（看板 orgId/行级权限）已由 A4.1.25 + A4.2.10 闭合（运行时确认经 11 DashboardBizModel 全集 census 复核 + P2-RC-086 watch-only，详见 `docs/audits/2026-08-06-1926-rc-ma4-dashboard-orgid-rowlevel-permission-leak.md`），本计划不重复覆盖。
- [x] **业财保护区域探针纪律声明**：A4.2.113（ExpenseCostAggregator）/ A4.2.116（TimesheetPostingDispatcher buildEvent）/ A4.2.120（ProjectPnlCalculator）/ A4.2.122（ProjectSettlementAcctDocProvider）触及业财过账行为探针——**READ-ONLY 只读确认**，不改 ExpenseCostAggregator/PostingDispatcher/Calculator/Provider/ORM。MR1 fix 路径 ask-first 门控。

---

## 6. 段落完整性自检

- [x] §0 业财过账保护区域探针纪律前置声明（READ-ONLY 标记 + A4.2.119 排除 + A1.36 SP-5 排除）
- [x] §1 逐项运行时证据 + §2 判据裁决（A4.2.113-A4.2.118 + A4.2.120-A4.2.123 各 file:line 证据 + 裁决）
- [x] §2 裁决汇总表（10 项 + 关联 finding + 静态判定 + 运行时裁决 + §2 判据 + 修复归属）
- [x] §3 与既有 finding 衔接（arm-index 维持注记 + reuse 不新建）
- [x] §4 多维度裁决自检（multi-dimensional-audit-prompt 7 维度）
- [x] §5 过程纪律自检（checker 门控 + 独立性 + 交叉去重 + A4.2.119 排除 + A1.36 SP-5 排除 + 业财探针纪律）
- [x] §6 段落完整性自检

**真相源冻结条款遵守声明**：本审计未修改任何真相源（`product-scope.md` / `use-cases.md` / `cost-collection.md` / `state-machine.md` / `task-dag.md` / `profitability.md` / `dashboards.md` 的需求契约段落）。发现的 doc 分歧（cost-collection.md §3.3 三时机表 vs ExpenseCostAggregator 无 budgetChecker 调用 / profitability.md:82 nop-job vs 实现未接线 / profitability.md:57-58 质保金设计意图 vs 零 writer）记入本报告 §1，不直改真相源（§9 冻结条款）。
