# rc-ma4-a4-2-174-177-logistics-runtime logistics 域重复发运/轮询/容量/运费空金额运行时确认

> Plan: `docs/plans/2026-08-07-1410-2-rc-ma4-a4-2-174-177-logistics-runtime.md`
> Mission: requirement-compliance（MA4 运行时行为验证 切片 A4.2.174-A4.2.177）
> Work Item: A4.2.174 / A4.2.175 / A4.2.176 / A4.2.177（A1.49 §7 SP-1..SP-4 四项静态存疑点运行时确认）
> 来源: `docs/backlog/requirement-compliance-roadmap.md` A4.2.174-A4.2.177
> Audit Status: closed
> 审计性质: **只读审计**（零生产代码/ORM/api.xml/view.xml/config 默认值/真相源变更；**新增 1 个 L4 探针测试**——plan A4.2.174 项显式要求"重复发运创建探针"）

## 0. 与既有 MA1/A1.x 报告差异增量声明（§9）

本报告**只补运行时行为证据**（methodology §去重协议），不重审 A1.49 已裁决的需求符合性结论与 finding 分级：

- **A1.49**（`docs/audits/2026-08-06-2243-1-rc-ma1-a1-49-logistics-full.md`）：logistics 域全域 7 UC 五级追踪（MA1 首审）+ §7 SP-1..SP-4 静态存疑点 + §6 finding（5 新 P1[P1-RC-083~087] + 6 新 P2[P2-RC-073~078] + 3 reuse[P1-MA2-078/079/080] + 引用 P1-MA2-092/P2-MA2-072）。本报告复用其 L3 静态判定 + L4 测试断言 + §6 finding 编号，只补 **SP-1..SP-4（A4.2.174-177）四项存疑点的运行时证据**。
- **P1-RC-084**（UC-LOG-01 DRAFT 24h 升级通知）/ **P1-RC-087**（UC-LOG-06 通知 sales）：**不在本切片范围**（A1.49 §7 仅 SP-1..SP-4，无对应 SP 行），本报告仅声明维持（修复归 MR1 R1.0 展开器，087 跨域 sales 契约 ask-first），不重审其缺失性。
- **P2-RC-073/074/076/077/078**（async-dispatch 死 config / 追踪异常 3 天 / 加密 stub / 连通性测试 / signatureImage-POD）：无 §7 SP 行（A1.49 §7 仅 SP-1..SP-4），不在本切片范围（A1.49 §6 已裁决），仅声明维持。
- **P1-MA2-078/079**（审批门控 / 部分签收，resolved R1.25 via deferral）与 **P1-MA2-080**（网关重试+过账失败告警，resolved R1.16 via implementation）：本报告引作 UC-LOG-02/03/04/06 行为证据与衔接注记，**不重审**（§去重协议）。

本切片**只补**的运行时差异：(i) A4.2.174 双 shipment 同 relatedBillCode 经 BizModel save 路径均可创建且均 DELIVERED → 各产生 1 张 FREIGHT 凭证（**新增 L4 探针** `testDuplicateShipmentSameRelatedBillPostsTwoVouchers` 实证重复过账）；(ii) A4.2.175 tracking-poll-cron 死 config 确认 + scanForPolling 仅 manual @BizMutation + 无调度接线（grep census + job conf 普查）；(iii) A4.2.176 `ErpLogDeliveryBooking` 实体 ORM 不存在 + 预约创建入口不存在 → 当前运行时无超卖风险（实体缺失=功能缺失非数据破坏）；(iv) A4.2.177 path-1 AUTO + freightAmount=null → `buildFreightPostingEvent` 写 null → `LogisticsFreightProvider.readDecimal` null→ZERO → 0 金额 POSTED 凭证生成（**只读探针**，读实现不写代码；对照 path-2 显式 null/≤0 守卫与既有测试）。

---

## 1. 存疑点清单与判据（A1.49 §7 四项）

| # | 工作项 | 来源存疑点 | 静态判定 | 运行时判据 |
|---|--------|-----------|---------|-----------|
| 1 | A4.2.174 | A1.49 §7 SP-1（UC-LOG-01）重复发运防护缺失（P1-RC-083）下同一出库单 2 条发运单均 DELIVERED 是否产生 2 张 FREIGHT 凭证致重复过账 | `defaultPrepareSave:68-81` 仅校验 trackingNo+carrierId 重复 + ORM（orm.xml:223-226）无 (relatedBillType,relatedBillCode) UK + grep 零重复防护逻辑 | 探针实证：双 shipment 同 relatedBillCode 均可创建并 DELIVERED → 凭证数断言（是否各 1 张 FREIGHT 凭证）；裁决三选一：确认重复过账 / 确认被防护 / 确认不产生凭证 |
| 2 | A4.2.175 | A1.49 §7 SP-2（UC-LOG-03）轮询调度未接线（P1-RC-085）下无 webhook 承运商 + manual 不调 scanForPolling → DISPATCHED 运单是否滞留（状态不推进→运费不过账） | config key `erp-log.tracking-poll-cron`（默认 `0 0 */4 * * ?`）存在于 ErpLogConfigs:15 但 grep `IScheduler\|QuartzJob\|job.yaml\|batch.xml` module-logistics 零命中，scanForPolling 仅 manual @BizMutation | 确认 config key 存在但零 scheduler 消费（grep census 复核）+ 状态推进仅 webhook/scanForPolling 两入口 → DISPATCHED 滞留确认（不实际等待 4h） |
| 3 | A4.2.176 | A1.49 §7 SP-3（UC-LOG-07）容量预约逻辑缺失（P1-RC-086）下是否多预约超卖 | `ErpLogDeliveryWindowBizModel` 17 行空壳 CRUD + `ErpLogDeliveryBooking` 预约实体 ORM 不存在（delivery-window.md:61 标"预留"） | 确认预约创建入口不存在 → 当前运行时无超卖风险（实体缺失=功能缺失非数据破坏）；物化实体时须同时落地容量守卫（修复义务归 MR1） |
| 4 | A4.2.177 | A1.49 §7 SP-4（UC-LOG-04）path-1 AUTO + freightAmount=null 时 buildFreightPostingEvent 写 null amount → voucherBiz.post 是否生成 0/null 金额凭证 | path-1 AUTO 无 null 守卫（MANUAL 模式可人工补录）；`ErpLogFreightPosting` 强测覆盖 MANUAL 补录路径对照 | 只读探针：确认 `buildFreightPostingEvent` 在 freightAmount=null + AUTO 模式下行为（写入 null amount 或默认值）+ `voucherBiz.post` 对 null/0 金额的响应（Provider readDecimal / posting engine 零守卫确认） |

---

## 2. 运行时证据采集（L3 `file:line` + L4 强断言，2026-08-07 live code 实测）

### 2-1 A4.2.174 重复发运→重复运费过账确认 — 确认重复过账可能（维持 P1-RC-083 P1）

**L3 代码路径**（live code 实测）：

- `ErpLogShipmentBizModel#defaultPrepareSave:60-82` — 应用层前置校验**仅** trackingNo+carrierId 维度（`:68-81` 双过滤 eq trackingNo + eq carrierId + ne id + findCount>0 抛 `ERR_LOG_SHIPMENT_TRACKING_NO_DUPLICATE`）；**无 relatedBillType+relatedBillCode 维度校验**。
- ORM `app-erp-logistics.orm.xml:223-226` — unique-keys 仅 `UK_LOG_SHIPMENT_CODE_ORG`（code,orgId）+ `UK_LOG_SHIPMENT_TRACKING_CARRIER`（trackingNo,carrierId,delVersion），**无 (relatedBillType,relatedBillCode) UK**；relatedBillCode 为普通列（orm.xml:175 propId=7 tagSet=var）。
- grep census：`relatedBillType\|relatedBillCode` erp-log-service/src/main 仅命中常量定义（ErpLogConstants:32 注释）+ 事件 POJO（ShipmentDeliveredEvent 字段）+ 分流读侧（onDelivered:80-85），**零重复校验逻辑**。

**L4 探针测试**（2026-08-07 新增 + 运行 PASS）：

- **新增** `TestErpLogFreightPosting#testDuplicateShipmentSameRelatedBillPostsTwoVouchers` — seed 金融前置（期间/账套/科目/组织/币种）+ carrier → **经 BizModel save 路径**（`ErpLogShipment__save` GraphQL，触发 defaultPrepareSave）创建 2 条发运单：同 relatedBillType=SALES_DELIVERY + relatedBillCode=REL-BILL-DUP-001，异 code/trackingNo → **两条 save 均 status=0**（无 relatedBill 维度拒绝实证）→ 双 webhook DELIVERED（均 status=DELIVERED + freightSettlementStatus=SETTLED）→ `findBillLinks` 断言：FRT-DUP-1 恰 1 张 FREIGHT 凭证 + FRT-DUP-2 恰 1 张 FREIGHT 凭证 + **两 voucherId 不同**（2 张独立凭证，重复过账实证）。

**裁决**：**确认重复过账可能**（三选一命中"确认重复过账"）——同一出库单可创建多条非 CANCELLED 发运单且均 DELIVERED 后各产生 1 张 FREIGHT 凭证（探针实证 2 张独立凭证）。**与 A1.49 §6 衔接维持 P1-RC-083 P1**（Q4 强制实现；修复 = `defaultPrepareSave` 增 relatedBillType+relatedBillCode 非 CANCELLED 重复查询守卫 **或** 加 (relatedBillType,relatedBillCode,delVersion) DB UK；**触及 ORM 结构变更[DB UK]须 ask-first + 独立 plan-audit §5 ORM 类**，BizModel 校验逻辑预授权——归 MR1 R1.0 展开器）。

### 2-2 A4.2.175 轮询缺失运行时影响确认 — 确认滞留成立（维持 P1-RC-085 P1）

**L3 代码路径**（live code 实测）：

- `ErpLogConfigs:15` — `CONFIG_TRACKING_POLLING_CRON = "erp-log.tracking-poll-cron"` + `:32` `DEFAULT_TRACKING_POLLING_CRON = "0 0 */4 * * ?"`（config key 存在）。
- **scheduler 消费 census**：grep `IScheduler\|QuartzJob\|job.yaml\|batch.xml\|IJobInvoker\|@Scheduled\|nop-batch\|cron` module-logistics 全部 main/app 资源 → **唯一命中 = ErpLogConfigs.java 自身常量**；module-logistics 无任何 `.job.yaml`/`scheduler.yaml` 文件；app-erp-all job conf（19 个 job.yaml + scheduler.yaml）**零 erp-log tracking-poll job**（ls 普查无 log/shipment 相关）。
- `CONFIG_TRACKING_POLLING_CRON` 消费 census：全仓 grep 仅 ErpLogConfigs 定义 + `IErpLogShipmentBiz` javadoc:44 注释引用——**零生产代码消费**（死 config）。
- `scanForPolling`：`ErpLogShipmentBizModel:126` `@BizMutation`（manual 入口）→ `ErpLogShipmentScanForPollingProcessor#scanForPolling:19` → `gatewayDispatcher.scanForPolling` —— 仅 manual 触发。
- **状态推进入口 census**：`advanceTracking` 仅两个调用点——webhook Processor（`ErpLogShipmentHandleTrackingWebhookProcessor`）+ polling 路径（`GatewayDispatcher.scanForPolling` → `advanceTrackingViaPolling`）。**无第三条 DELIVERED 推进路径**。

**运行时判据确认**（不实际等待 4h）：无 webhook 回调（承运商不主动回调）+ manual 不调 scanForPolling → DISPATCHED 运单**无任何自动机制推进**至 DELIVERED → 运费过账不触发（freightSettlementStatus 保持 PENDING）。状态滞留成立——定时轮询缺位使"自动追踪更新"验收标准（L1 use-cases.md:39）运行时不可达，仅 webhook 驱动（依赖承运商回调）或人工 scanForPolling。

**裁决**：**确认轮询缺失成立（DISPATCHED 滞留）**。**与 A1.49 §6 衔接维持 P1-RC-085 P1**（Q4 强制实现；修复 = nop-batch job.yaml 注册 tracking-poll job + 消费 config key 门控 + 批量调 scanForPolling；纯调度接线预授权不触 ask-first——归 MR1 R1.0 展开器）。

### 2-3 A4.2.176 容量超卖运行时影响确认 — 确认当前无超卖风险（维持 P1-RC-086 P1）

**L3 代码路径**（live code 实测）：

- `ErpLogDeliveryWindowBizModel` — **17 行空壳 CrudBizModel**（仅构造器 setEntityName，零自定义 @BizMutation/@BizQuery/@BizAction），实测逐行核验。
- `ErpLogDeliveryBooking` 实体 — grep `DeliveryBooking` 跨 module-logistics **零命中**（ORM `app-erp-logistics.orm.xml` 无该实体；DAO 层无 ErpLogDeliveryBooking 类；`delivery-window.md:61` 标"预留"）。
- `ErpLogDeliveryWindow` ORM 实体存在（orm.xml:359-370，table `erp_log_delivery_window`）：maxCapacity（propId=7 defaultValue=10）+ currentBooked（propId=8 defaultValue=0）**列存在但零业务逻辑消费**（无容量检查/currentBooked±1/预约实体）。
- grep `maxCapacity\|currentBooked\|priorityScore\|missedFee\|MISSED\|capacity` erp-log-service/src/main 零业务命中（仅 ORM 列）。

**运行时判据确认**：预约（Booking）创建入口**不存在**——无 ErpLogDeliveryBooking 实体 ⇒ 无预约 mutation/BizModel ⇒ 发运单无法关联预约 ⇒ **当前运行时无超卖风险**（无预约可超卖；实体缺失=功能缺失，非数据破坏）。`maxCapacity=10/currentBooked=0` 列是死数据载体，无守卫无计数。

**裁决**：**确认无超卖风险但功能缺失成立**。**与 A1.49 §6 衔接维持 P1-RC-086 P1**（Q4 强制实现；修复 = 物化 `ErpLogDeliveryBooking` 实体 + 容量预约 BizModel/Processor 逻辑[currentBooked±1 守卫 + MISSED 爽约费 + priorityScore + 窗口过期 + 幂等 UK]，**触及 ORM 结构变更[新增实体]须 ask-first + 独立 plan-audit §5 ORM 类**——归 MR1 R1.0 展开器）。

### 2-4 A4.2.177 path-1 AUTO + freightAmount=null 凭证金额确认 — 确认 0 金额凭证生成（**会计只读探针**）

**L3 代码路径**（live code 实测，**只读探针——零代码变更**）：

- `AbstractErpLogShipmentDeliveredProcessor#buildFreightPostingEvent:181-200` — `:192` `billData.put(BILL_DATA_FREIGHT_AMOUNT, shipment.getFreightAmount())`：**写入运单原值**（未设置时 = null），无 null 守卫无默认值。
- path-1 门控 `onDelivered:88-96` — AUTO（默认）模式直接 `voucherBiz.post(event, context)`（`:94-96`）；**path-1 AUTO 无 freightAmount null/≤0 前置守卫**（对照 path-2 `handlePurchaseReceiptDelivered:134-139` 显式 `freightAmount == null || compareTo(ZERO)<=0 → mark SETTLED 跳过` 守卫——path-2 有守卫、path-1 无）。
- `LogisticsFreightProvider#readDecimal:82-91` — `:87-88` `value == null → return BigDecimal.ZERO`：**null → 0**（非 null 金额、非拒绝）。
- `ErpFinPostingProcessor` — `:726/:824` `fact.getAmount() == null ? BigDecimal.ZERO : fact.getAmount()`（null → 0）；`:737` `totalDebit.compareTo(totalCredit) != 0` 借贷平衡检查（0==0 通过，**无 0 金额拒绝**）；`persistVoucher:787-869` 创建 docStatus=POSTED 凭证 + 0 金额行（debit/credit 0.0000）；FactsValidator 链 = `ErpFinAcctDocRegistry.getValidators()` 全仓 grep `implements IErpFinn` **零实现**（验证器链空，无金额守卫）。

**既有测试对照**（重跑全绿，2026-08-07）：

- `TestErpLogFreightPosting#testSalesFreightPostedAndSettled` — AUTO（默认）+ freightAmount=150 → 凭证 + SETTLED（主路径对照）；
- `TestErpLogPath2LandedCost#testPath2SkipWhenFreightAmountNull:115` — path-2 null/≤0 显式跳过（**守卫对照**——path-2 有、path-1 AUTO 无）；
- `onDelivered:90-93` MANUAL 分支 — 仅 LOG + return 不调 post（MANUAL 模式补录路径只读确认：人工补运费后经重入触发）。

**运行时判据确认**（只读推导，决定性）：freightAmount=null + path-1 AUTO → `buildFreightPostingEvent` 写 null → `readDecimal` null→ZERO → facts 金额=0 → posting engine 借贷平衡 0==0 通过 + 验证器链空 → **0 金额 POSTED 凭证生成**（非 null 金额凭证、无显式拒绝）。**AUTO 模式无 null 守卫确认**。

**裁决**：**确认 0 金额凭证生成行为**（null → ZERO → 0 金额凭证，无守卫拒绝）。**与 A1.49 §5/§6 衔接维持 UC-LOG-04 接受 on 主路径 + P2-RC-075 维持 P2（watch-only 登记不强制）**；0 金额凭证生成登记为**残余风险**（污染凭证数据质量、影响后续对账，但无 GL 失衡——借贷均 0 平衡，非活跃数据破坏，不触发 MR0）；修复（path-1 AUTO 增 null/≤0 → SETTLED 跳过守卫，对齐 path-2 语义）**触及会计过账逻辑须 ask-first + 独立 plan-audit §5 会计类——归 MR1 R1.0 展开器**。**本探针零代码变更，未触碰任何过账逻辑**（保护区域遵守声明）。

---

## 3. 测试证据汇总（L4，断言强度）

| 工作项 | 测试 | 断言强度 | 覆盖存疑点 | 运行结果（2026-08-07） |
|--------|------|---------|-----------|----------------------|
| A4.2.174 | **新增** `TestErpLogFreightPosting#testDuplicateShipmentSameRelatedBillPostsTwoVouchers`（BizModel save 路径双创建成功 + 双 DELIVERED + 凭证数断言 1+1 + 两 voucherId 独立） | **强**（GraphQL 全链 + 行级状态断言 + 凭证数精确断言） | SP-1（重复发运可创建 + 重复过账证实） | PASS ✅ |
| A4.2.174 佐证 | **重跑** `TestErpLogShipmentPostingEnd#testSalesDeliveryFullLifecycleWithFreightVoucher`（主生命周期）+ `TestErpLogFreightPosting#testSalesFreightPostedAndSettled`（单发运过账基线） | **强** | SP-1 主路径对照 | PASS ✅ |
| A4.2.175 | **重跑** `TestErpLogCarrierGatewayIntegration#testPollingAdvancesMultipleShipments`（scanForPolling 推进多运单——manual 触发证实）+ `TestErpLogShipmentGateway` 全 6 用例（webhook 驱动路径证实） | **强** | SP-2（推进仅 manual/webhook 两入口，无自动调度） | PASS ✅ |
| A4.2.176 | 无测试（实体缺失=零断言面；grep census + orm.xml 检查为证据） | — | SP-3（预约实体不存在 → 无超卖风险） | census ✅ |
| A4.2.177 | **重跑** `TestErpLogFreightPosting#testSalesFreightPostedAndSettled`（AUTO 主路径对照）+ `TestErpLogPath2LandedCost#testPath2SkipWhenFreightAmountNull:115`（path-2 null 守卫对照）+ MANUAL 分支只读确认 | **强**（对照链） | SP-4（AUTO 无 null 守卫 vs path-2 有守卫） | PASS ✅ |

`mvn test -pl module-logistics/erp-log-service` → **Tests run: 27, Failures: 0, Errors: 0, Skipped: 0 — BUILD SUCCESS**（2026-08-07 实测；TestErpLogFreightPosting=4[含 1 新增探针] / TestErpLogShipmentPostingEnd=2 / TestErpLogShipmentGateway=6 / TestErpLogCarrierGatewayIntegration=2 / TestErpLogPath2LandedCost=3 / TestErpLogShipmentCrudSmoke=5 / TestErpLogShipmentTrackingNoUk=2 / TestErpLogCarrierConfigCredentialMasking=1 / TestLogPostingFaultInjection=1 等，27 合计全绿）。

---

## 4. 运行时行为证据（L5）

- **复用 A1.49 §4/§5 + MA2 A2.15**：发运单 6 态状态机迁移守卫（advise/completeShipment/cancelShipment/advanceTracking 全 src 守卫 + 幂等）+ 网关重试 maxRetries=3 指数退避 + deadLetter 死信保留 ADVISED + DELIVERED→`IErpFinVoucherBiz.post` Facade + path-2 config-gated + trackingNo UK（P1-MA2-092 resolved R1.28）作既有证据（本报告不重审状态机/网关维度）。
- **本切片补的运行时差异**（经 live code 实测 + L4 新增/重跑 + census）：
  - **A4.2.174**：`defaultPrepareSave:68-81` 无 relatedBill 维度校验 + ORM 无 (relatedBillType,relatedBillCode) UK → **探针实证**：同出库单 2 条发运单 BizModel save 均成功 + 均 DELIVERED + **各 1 张 FREIGHT 凭证（2 张独立 voucherId）——重复过账运行时证实**。
  - **A4.2.175**：`erp-log.tracking-poll-cron` 死 config（零 scheduler 消费 census + app-erp-all job conf 零 erp-log job）+ `advanceTracking` 仅 webhook/scanForPolling 两入口 → **无 webhook + manual 不调 scanForPolling ⇒ DISPATCHED 滞留成立**（运费不过账）。
  - **A4.2.176**：`ErpLogDeliveryWindowBizModel` 17 行空壳 + `ErpLogDeliveryBooking` ORM 不存在（grep 零命中 + delivery-window.md:61 预留）→ **预约入口不存在 ⇒ 当前运行时无超卖风险**（实体缺失=功能缺失非数据破坏）。
  - **A4.2.177**：`buildFreightPostingEvent:192` 写 null → `LogisticsFreightProvider.readDecimal:87-88` null→ZERO → `ErpFinPostingProcessor` 借贷 0==0 通过 + 验证器链空 → **0 金额 POSTED 凭证生成**（path-1 AUTO 无 null 守卫；对照 path-2 :134-139 有守卫）。

---

## 5. 符合性结论（四项存疑点裁决）

### 5.1 四项裁决矩阵

| 工作项 | 来源存疑点 | §2 判据命中分支 | 运行时裁决 | finding 衔接 |
|--------|-----------|----------------|-----------|-------------|
| **A4.2.174** | A1.49 §7 SP-1 重复发运→重复运费过账 | 三选一命中**确认重复过账** | **确认重复过账可能**（探针实证：同出库单双发运单均 DELIVERED → 2 张独立 FREIGHT 凭证） | **维持 P1-RC-083 P1**（arm-index :280；Q4 强制实现归 MR1，DB UK 触 ORM ask-first） |
| **A4.2.175** | A1.49 §7 SP-2 轮询缺失运行时影响 | 确认滞留成立 | **确认 DISPATCHED 滞留**（cron 死 config census + 推进仅 webhook/scanForPolling 两入口） | **维持 P1-RC-085 P1**（arm-index :282；Q4 强制实现归 MR1，纯调度接线预授权） |
| **A4.2.176** | A1.49 §7 SP-3 容量超卖运行时影响 | 确认无超卖风险 | **确认当前无超卖风险**（预约实体/入口不存在；功能缺失非数据破坏） | **维持 P1-RC-086 P1**（arm-index :283；Q4 强制实现归 MR1，物化实体触 ORM ask-first） |
| **A4.2.177** | A1.49 §7 SP-4 path-1 AUTO + freightAmount=null | 确认 0 金额凭证生成 | **确认 0 金额 POSTED 凭证生成**（null→ZERO→借贷 0==0 平衡→无拒绝；AUTO 无 null 守卫） | **维持 UC-LOG-04 接受 on 主路径 + P2-RC-075 P2**（arm-index :287；0 金额凭证登记残余风险归 MR1 会计 ask-first） |

### 5.2 裁决分支汇总

- **四项运行时确认与 A1.49 §7 静态判定无一翻转**：A4.2.174 静态判定即"重复发运防护完全缺失"（`defaultPrepareSave` 无 relatedBill 维度 + 无 DB UK），运行时**证实双 DELIVERED 各产生 1 张 FREIGHT 凭证**——静态判定的证实细化，非行为翻转；A4.2.175/176/177 与静态判定逐字一致。
- **四项全部按 A1.49 §6 衔接维持既有 finding 分级**（P1-RC-083/085/086 维持 P1 + UC-LOG-04 接受 on 主路径/P2-RC-075 维持 P2），无未经比对新建 finding。P1-RC-084/087 与 P2-RC-073~078 无 §7 SP 行（A1.49 §7 仅 SP-1..SP-4），不在本切片范围，仅声明维持。
- **零升级触发 MR0**（运行时未发现活跃数据破坏：重复过账需双创建+双送达特定条件非默认活跃，且每 shipment 独立过账非并发 UK 破坏；0 金额凭证借贷均 0 平衡无 GL 失衡；容量缺失因预约实体不存在无超卖——与 A1.49 §7 P0 评估一致）。
- **测试侧补充 1 个探针**（plan A4.2.174 项显式要求"重复发运创建探针"——探针为只读审计的证据手段，非生产代码变更）；SP-4 按 plan 不新增测试（只读探针）。

---

## 6. 与 arm-index 衔接（维持既有分级裁决 + 交叉引用注记）

### 6.1 比对表

| 本切片存疑点 | 比对 arm-index | 裁决 | 差异依据 |
|-------------|---------------|------|---------|
| A4.2.174 重复发运→重复过账 | `P1-RC-083`（:280，A1.49 新建） | **维持 P1（运行时证实重复过账，Q4 强制实现归 MR1）** | 同根因同控制点：relatedBill 维度重复防护缺失。本报告运行时证实同出库单双发运单均 DELIVERED → 2 张独立 FREIGHT 凭证（探针实证） |
| A4.2.175 轮询缺失 | `P1-RC-085`（:282，A1.49 新建） | **维持 P1（运行时证实 DISPATCHED 滞留，Q4 强制实现归 MR1）** | 同根因同控制点：tracking-poll-cron 死 config。本报告 census 复核零 scheduler 消费 + 推进仅 webhook/scanForPolling 两入口 |
| A4.2.176 容量超卖 | `P1-RC-086`（:283，A1.49 新建） | **维持 P1（运行时证实无超卖风险但功能缺失，Q4 强制实现归 MR1）** | 同根因同控制点：预约实体/逻辑缺失。本报告确认 ErpLogDeliveryBooking 不存在 → 预约入口不存在 → 当前无超卖风险 |
| A4.2.177 path-1 null 金额 | `P2-RC-075`（:287，A1.49 新建）+ UC-LOG-04 接受 on 主路径 | **维持 P2 + 登记残余风险（0 金额凭证生成，归 MR1 会计 ask-first）** | 同根因同控制点：过账机制漂移/金额守卫。本报告只读确认 AUTO 无 null 守卫 → 0 金额凭证生成行为 |
| （声明）P1-RC-084 | `P1-RC-084`（:281） | **不在本切片存疑点范围** | 无 §7 SP 行（A1.49 §7 仅 SP-1..SP-4），A1.49 §6 已裁决，仅声明维持 |
| （声明）P1-RC-087 | `P1-RC-087`（:284） | **不在本切片存疑点范围，维持 P1（Q4 强制实现归 MR1，跨域 sales ask-first）** | UC-LOG-06 通知 sales 无 §7 SP 行，本计划 Non-Goals 显式排除，仅声明维持不重审 |
| （声明）P2-RC-073/074/076/077/078 | `P2-RC-073/074/076/077/078` | **不在本切片存疑点范围，维持 P2（登记不强制）** | 无 §7 SP 行，A1.49 §6 已裁决，仅声明维持 |
| （声明）P1-MA2-078/079/080 | resolved（R1.25 via deferral / R1.16 via implementation） | **维持 resolved 注记（不重审）** | §去重协议：reuse 项仅引作行为证据/同控制点交叉引用，不重开裁决 |

### 6.2 新 finding 清单

- **无**（零新 finding；四项全部经 grep arm-index 同域同控制点比对后给出「维持既有分级」裁决，符合 §去重协议「禁止未经比对新建」要求 + 计划 Phase 2 决策项）。

### 6.3 复用 finding 交叉引用注记（追加 RC A4.2.174-177 运行时确认，见 arm-index 对应段落）

- **P1-RC-083**（:280，UC-LOG-01）：追加 RC A4.2.174 运行时确认注记——`ErpLogShipmentBizModel#defaultPrepareSave:68-81` 仅 trackingNo+carrierId 维度 + ORM（orm.xml:223-226）无 (relatedBillType,relatedBillCode) UK → **新增探针** `TestErpLogFreightPosting#testDuplicateShipmentSameRelatedBillPostsTwoVouchers` PASS（BizModel save 路径同出库单双创建成功 + 双 DELIVERED + 各 1 张 FREIGHT 凭证 + 两 voucherId 独立）——**重复运费过账运行时证实**。维持 P1（Q4 强制实现；修复 = defaultPrepareSave 增 relatedBill 维度守卫 或 DB UK，后者触 ORM ask-first，归 MR1）。
- **P1-RC-085**（:282，UC-LOG-03）：追加 RC A4.2.175 运行时确认注记——`erp-log.tracking-poll-cron`（ErpLogConfigs:15/32）零生产代码消费（census：module-logistics 无 job.yaml/scheduler.yaml + app-erp-all job conf 零 erp-log job + `CONFIG_TRACKING_POLLING_CRON` 仅 javadoc 引用）+ `scanForPolling` 仅 manual @BizMutation（ErpLogShipmentBizModel:126）+ `advanceTracking` 仅 webhook/scanForPolling 两入口 → **无 webhook + manual 不调用 ⇒ DISPATCHED 滞留成立**。维持 P1（Q4 强制实现；修复 = nop-batch job.yaml 注册 tracking-poll job，纯调度接线预授权，归 MR1）。
- **P1-RC-086**（:283，UC-LOG-07）：追加 RC A4.2.176 运行时确认注记——`ErpLogDeliveryWindowBizModel` 17 行空壳 + `ErpLogDeliveryBooking` ORM 不存在（grep 零命中 + delivery-window.md:61 预留）→ **预约创建入口不存在 ⇒ 当前运行时无超卖风险**（maxCapacity=10/currentBooked=0 列死数据载体）。维持 P1（Q4 强制实现；修复 = 物化 ErpLogDeliveryBooking + 容量守卫，触 ORM ask-first，归 MR1）。
- **P2-RC-075**（:287，UC-LOG-04）：追加 RC A4.2.177 运行时确认注记——`buildFreightPostingEvent:192` 写 null freightAmount → `LogisticsFreightProvider.readDecimal:87-88` null→ZERO → `ErpFinPostingProcessor` 借贷 0==0 平衡（:737）+ 验证器链空（全仓零 `implements IErpFinn`）→ **0 金额 POSTED 凭证生成**（path-1 AUTO 无 null 守卫；对照 path-2 :134-139 显式守卫）。**只读探针零代码变更**。维持 P2 + 登记残余风险（修复 = path-1 AUTO 增 null/≤0 → SETTLED 跳过守卫，**触会计过账逻辑须 ask-first + 独立 plan-audit §5 会计类**，归 MR1）。

---

## 7. 过程纪律自检

- [x] **checker 退出码门控核查**：本报告为只读运行时确认（**零生产代码/ORM/api.xml/view.xml/config 默认值/真相源变更**；仅测试侧新增 1 个探针测试——checker 扫全生产代码不含测试），checker 复跑 **actual == baseline，零漂移**（R1a=0/R1b=0/R1c=0/R1d=14/R2a=34/R2b=229/R2c=1382/R2d=34/R3=5/R4=0/R5=0/R6=2/R7=0/R8=0/R10=6/R11=0/R12a=69/R12b=66/R12c=40；与 `docs/audits/compliance-baseline.md` §BASELINE 机器可读块逐项一致）。区分门控退出码 vs 纯 reporter 退出码——checker 脚本是纯 reporter（退出码恒 0），真正门控在 CI workflow 解析 actual > baseline。
- [x] **closure-audit 独立性声明**：本报告的 closure audit 将由独立子代理（新会话，不重用执行者上下文）执行，执行者不自我审计。
- [x] **与 arm-index 交叉去重声明**：本报告全部 4 项运行时裁决已按 §去重协议 grep arm-index 同域同控制点后给出「维持既有分级」结论（P1-RC-083/085/086 维持 P1 + P2-RC-075 维持 P2 + 声明项[P1-RC-084/087/P2-RC-073~078/P1-MA2-078/079/080]维持），**无未经比对直接新建的 finding**。
- [x] **只读审计纪律声明**：零生产代码/ORM/api.xml/view.xml/真相源变更 + 测试侧补充 1 个探针（plan 显式要求的证据手段）；非目标项（P1-RC-083/085/086/084/087 强制实现 / P2-RC-073~078 登记）归 MR1 R1.0 展开器（P1 Q4 强制实现；P2 登记不强制；MR1 R1.0 不自动启动，待另行人工裁决）。
- [x] **会计保护区域声明（SP-4）**：A4.2.177 探针为**只读**——仅读取 `buildFreightPostingEvent`/`LogisticsFreightProvider.readDecimal`/`ErpFinPostingProcessor` 既有实现，**零过账逻辑变更**；0 金额凭证残余风险修复（若实施）须 ask-first + 独立 plan-audit §5 会计类，归 MR1。
- [x] **真相源冻结声明**：本审计期间未修改任何真相源（use-cases.md / owner doc / product-scope.md）；分歧记入报告不直改。
- [x] **测试运行验证**：`mvn test -pl module-logistics/erp-log-service` → **Tests run: 27, Failures: 0, Errors: 0, Skipped: 0 — BUILD SUCCESS**（2026-08-07 实测；含 1 新增探针 testDuplicateShipmentSameRelatedBillPostsTwoVouchers 全绿）。

---

## 8. 报告 9 段完整性自检

| # | 段落 | 状态 |
|---|------|------|
| 1 | 存疑点清单与判据（A1.49 §7 四项 + 判据） | ✅ §1 |
| 2 | 运行时证据采集（L3 file:line + L4 强断言，四项逐项） | ✅ §2 |
| 3 | 测试证据汇总（L4 Test*.java + 断言强度） | ✅ §3 |
| 4 | 运行时行为证据（L5 复用 A1.49 §4 + 本切片差异） | ✅ §4 |
| 5 | 符合性结论（四项裁决矩阵 + §2 判据命中分支） | ✅ §5 |
| 6 | 与 arm-index 衔接（维持既有分级裁决 + 交叉引用注记 + 排除声明） | ✅ §6 |
| 7 | 过程纪律自检（checker actual==baseline + 独立性 + 交叉去重 + 只读纪律 + 会计保护区域 + 冻结声明） | ✅ §7 |
| 8 | 报告 9 段完整性自检 | ✅ §8 |
| 9 | 与既有 MA1/A1.x 报告差异增量声明 | ✅ §0 |

**9 段齐全**——本报告可定稿。

---

## 整体裁决

**PASS（四项存疑点全数收口，四项全部确认 A1.49 §7 静态判定，零新 finding / 不触发 MR0）**：

- **A4.2.174**：**确认重复过账可能**——同一出库单（relatedBillType+relatedBillCode 相同）经 BizModel save 路径（defaultPrepareSave 仅 trackingNo+carrierId 维度 + ORM 无该维度 UK）可创建多条发运单，均 DELIVERED 后**各产生 1 张 FREIGHT 凭证（2 张独立 voucherId）**（新增探针实证）。**P1-RC-083 运行时证实**，维持 P1（Q4 强制实现归 MR1；DB UK 方案触 ORM ask-first）。
- **A4.2.175**：**确认 DISPATCHED 滞留**——`erp-log.tracking-poll-cron` 死 config（零 scheduler 消费 census + app-erp-all job conf 零 erp-log job）+ 推进仅 webhook/scanForPolling 两入口；无 webhook 承运商 + manual 不调用 ⇒ 状态不推进 → 运费不过账。维持 P1-RC-085 P1（归 MR1，纯调度接线预授权）。
- **A4.2.176**：**确认当前无超卖风险**——`ErpLogDeliveryBooking` 实体 ORM 不存在 + `ErpLogDeliveryWindowBizModel` 17 行空壳 ⇒ 预约入口不存在（实体缺失=功能缺失非数据破坏）。维持 P1-RC-086 P1（归 MR1，物化实体触 ORM ask-first）。
- **A4.2.177**：**确认 0 金额凭证生成**——path-1 AUTO 无 null 守卫，null freightAmount → readDecimal null→ZERO → 借贷 0==0 平衡 + 验证器链空 → 0 金额 POSTED 凭证（**只读探针，零代码变更**）。维持 UC-LOG-04 接受 on 主路径 + P2-RC-075 P2 + 登记残余风险（修复触会计过账须 ask-first，归 MR1）。
- **四项全部衔接维持既有 finding 分级，零新 finding**（P1-RC-083/085/086 维持 P1 + P2-RC-075 维持 P2 + P1-RC-084/087/P2-RC-073~078/P1-MA2-078/079/080 声明维持）；**零升级触发 MR0**（重复过账需特定条件非默认活跃 + 0 金额凭证借贷平衡无 GL 失衡 + 无预约无超卖）；**测试侧补充 1 个探针**（plan 要求的证据手段）；**A1.49 §7 四项静态判定无一翻转**。本审计不实施生产代码修复，结果表面 = 本报告 + 1 探针测试 + arm-index 交叉引用注记 + roadmap/log 同步。
