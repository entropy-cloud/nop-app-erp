# rc-ma4-a4-2-155-162-contract-runtime 合同域总金额校验/版本/行复制/到期/失效/审批/折扣/OCR 隐式机制运行时确认

> Plan: `docs/plans/2026-08-08-0135-2-rc-ma4-a4-2-155-162-contract-runtime.md`
> Mission: requirement-compliance（MA4 运行时行为验证 切片 A4.2.155-A4.2.162）
> Work Item: A4.2.155 / A4.2.156 / A4.2.157 / A4.2.158 / A4.2.159 / A4.2.160 / A4.2.161 / A4.2.162（A1.45-46 §7 SP-1..SP-8 八项静态存疑点运行时确认）
> 来源: `docs/backlog/requirement-compliance-roadmap.md` A4.2.155-A4.2.162
> Audit Status: closed
> 审计性质: **只读审计 + 一处测试侧补充**（A4.2.159 TERMINATED 输入断言，限 erp-ct-service 测试类目，不触生产代码）

## 0. 与既有 MA1/A1.x 报告差异增量声明（§9）

本报告**只补运行时行为证据**（methodology §去重协议），不重审 A1.45-46 已裁决的需求符合性结论与 finding 分级：

- **A1.45-46**（`docs/audits/2026-08-05-1400-1-rc-ma1-a1-45-46-contract-lifecycle-billing-rebate.md`）：contract 域全域 10 UC 五级追踪（MA1 首审）+ §7 SP-1..SP-8 静态存疑点 + §6 finding（8 新 P1-RC-072~079 + 1 reuse P1-MA2-071）。本报告复用其 L3 静态判定 + L4 测试断言 + §6 finding 编号，只补 **SP-1..SP-8（A4.2.155-162）八项存疑点的运行时证据**。
- **A2.14**（`docs/audits/2026-07-28-1020-arm-ma2-ext-domains-state-machine.md`）：覆盖 contract 合同/InvoicePlan 状态机迁移 + EquipmentStatusLinker，**不含创建校验/审批工作流/计费/返利/仓库/到期自动化需求契约维度**（A1.45-46 已声明复用其证据），本报告**不重审状态机迁移维度**。
- **P1-MA2-072**（NEGOTIATION→TERMINATED，resolved R1.22 via **implementation**）：本报告**不重开**（A1.45-46 §9 已确认 terminate 守卫扩展接受 NEGOTIATION）。
- **P1-MA2-071**（EXPIRED Job+续期，resolved R1.22 via **deferral**）：本报告维持其 reuse 注记（A4.2.158 运行时确认无自动到期 job），**不重开 deferral 裁决**。

本切片**只补**的运行时差异：(i) A4.2.155 GraphQL save 金额不一致合同实际拒绝行为（L4 既有测试 createContract 未设 totalAmount 运行时实证）；(ii) A4.2.156 save 后版本自动创建机制（无 submit/negotiate mutation + 测试手工 seed 版本实证）；(iii) A4.2.157 amend 前端行复制编排 census；(iv) A4.2.158 全局 job 注册点 census（`app-erp-all/_vfs/nop/job/conf/` 19 job.yaml + scheduler.yaml 权威点）；(v) A4.2.159 TERMINATED 输入断言（**新增测试**，SUSPENDED 拒绝测试重跑佐证）；(vi) A4.2.160 全局审批引擎 census（xbiz `<actions/>` 零接线 + 全仓零消费者）；(vii) A4.2.161 purchase/sales 折扣调用 census + 订单行折扣字段对照；(viii) A4.2.162 OCR 全仓 census + ocrText/fullTextSearch 零 writer 实证。

---

## 1. 存疑点清单与判据（A1.45-46 §7 八项）

| # | 工作项 | 来源存疑点 | 静态判定 | 运行时判据 |
|---|--------|-----------|---------|-----------|
| 1 | A4.2.155 | A1.45-46 §7 SP-1（UC-CT-01 ①）运行时合同创建是否经前端/XMeta 层隐式校验 totalAmount=∑行金额 | 无跨字段校验（`defaultPrepareSave:57` 仅 businessDate + XMeta mandatory 单字段） | 确认 `ErpCtContractBizModel#defaultPrepareSave:57` file:line + XMeta（erp-ct-meta）跨字段校验表达式 grep + 前端 AMIS 金额一致性校验 grep + GraphQL save 金额不一致合同实际拒绝行为（L4 实证） |
| 2 | A4.2.156 | A1.45-46 §7 SP-2（UC-CT-01 ④）运行时合同到 NEGOTIATION 是否经隐式机制创建 v1 版本 | 无 submit/negotiate mutation + 测试手工 seed 版本 | 确认无 `submit`/`negotiate` mutation + GraphQL save 路径（`_ErpCtContract` mutation）无版本创建钩子 + 测试手工 seed 版本 file:line 证据 |
| 3 | A4.2.157 | A1.45-46 §7 SP-3（UC-CT-02 ②）运行时 amend 后变更单是否经前端编排半自动复制原合同行 | `amend:35` 仅版本翻转零行复制 | 确认 `ErpCtContractAmendProcessor#amend:35` file:line + AMIS 变更单编辑表单行复制交互 grep census |
| 4 | A4.2.158 | A1.45-46 §7 SP-4（UC-CT-05）运行时是否有其他全局 job 扫描合同到期 | 跨 module-contract 零 Job | 确认 job 注册权威点 census（`app-erp-all/_vfs/nop/job/conf/`）+ scheduler.yaml + module-contract Job bean grep + 仅手工 `expire:118` |
| 5 | A4.2.159 | A1.45-46 §7 SP-5（UC-CT-06）运行时 terminate 后 InvoicePlan 是否经合同头 TERMINATED 状态隐式失效 | triggerInvoice ACTIVE 守卫拒绝 → 隐式失效无显式截停标记 | 确认 `triggerInvoice:35` 非 ACTIVE 态双守卫（SUSPENDED 专属 :46-49 + 通用非 ACTIVE :50-54）+ 重跑 `testTriggerInvoiceRejectedForSuspendedContract:93` + **补充 TERMINATED 输入断言** |
| 6 | A4.2.160 | A1.45-46 §7 SP-6（UC-CT-07）运行时是否有其他全局审批引擎驱动合同审批 | ApprovalMatrix/Record 仅 CRUD 桩 | 确认 `ErpCtApprovalMatrixBizModel`/`ErpCtApprovalRecordBizModel` 17 行桩 + xbiz/审批流 config 零接线 + 合同提交（save/activate）无审批触发 + 全仓零消费者 |
| 7 | A4.2.161 | A1.45-46 §7 SP-7（UC-CT-08）运行时 purchase/sales 订单行是否经隐式机制应用合同折扣 | 零 resolveDiscount 调用 | 确认 grep census 跨 module-purchase/sales 零调用 + 订单行无 contract 引用折扣字段（ORM 字段对照） |
| 8 | A4.2.162 | A1.45-46 §7 SP-8（UC-CT-10）运行时是否有外部 OCR 服务/异步任务处理 ErpCtDocument | 零 OCR 业务逻辑 | 确认 grep 零 OCR/异步任务 + ocrText/fullTextSearch 零 writer + 前端文档上传无 OCR 钩子 + 全仓 OCR 服务 census |

---

## 2. 运行时证据采集（L3 `file:line` + L4 强断言）

### 2-1 A4.2.155 合同创建 totalAmount=∑行金额 跨字段校验确认 — 确认无跨字段校验（维持 P1-RC-072）

**L3 代码路径**（live code 实测）：

- `module-contract/erp-ct-service/.../entity/ErpCtContractBizModel.java#defaultPrepareSave:57-63` — 仅 `super.defaultPrepareSave` + businessDate 兜底（`if (entity.getBusinessDate() == null) setBusinessDate(today)`），**零 totalAmount=∑行金额 校验 + 零 startDate<endDate 校验**。`activate`（:67）与 `ErpCtContractActivateProcessor#activate:33` 仅校验 type/direction 组合 + 版本 FINALIZED，无金额校验。
- **XMeta census**：`_ErpCtContract.xmeta`（erp-ct-meta）全部 `mandatory="true"` 均为**单字段**（code:28/contractName:36/contractType:40/contractDirection:44/partnerId:48/startDate:60/endDate:64/status:72 等），grep `validator|expression|validate` 跨 `ErpCtContract.xmeta`/`_ErpCtContract.xmeta` **零命中**——无跨字段校验表达式（totalAmount 非 mandatory，纯录入字段）。
- **前端 AMIS census**：`ErpCtContract.view.xml` + `_gen/_ErpCtContract.view.xml` — totalAmount 仅 `ui:number` 列（:15）+ 表单字段（finance 分组 :71/:105），grep `validator|validate|金额一致` 跨 erp-ct-web `ErpCtContract*` view **零命中**——无前端金额一致性校验。

**L4 运行时实证**（既有测试重跑，2026-08-08 全绿）：

- `TestErpCtContractPosting#createContract:192-208` — data map 含 code/contractName/contractType/contractDirection/partnerId/currencyId/startDate/endDate/status=NEGOTIATION，**未设 totalAmount**（而合同行 amount=1000 存在，Σ行金额=1000）→ `ErpCtContract__save` **status=0 成功**（`:205` assertEquals(0, resp.getStatus())）→ **GraphQL save 不拒绝金额不一致（totalAmount=null ≠ Σ行金额）合同，运行时实证**。

**裁决**：确认**无跨字段校验**——后端 defaultPrepareSave 零金额校验 + XMeta 单字段 mandatory + 前端零校验，save 实际不拒绝金额不一致合同。**与 A1.45-46 §6 衔接维持 P1-RC-072（Q4 强制实现，修复归 MR1）**。

### 2-2 A4.2.156 NEGOTIATION v1 版本自动创建确认 — 确认无自动 v1（维持 P1-RC-072）

**L3 代码路径**（live code 实测）：

- `ErpCtContractBizModel` mutation 全集：activate:67 / suspend:73 / resume:85 / terminate:97 / expire:118 / amend:130——**无 `submit`/`negotiate` @BizMutation**（grep `submit|negotiate` 跨 erp-ct-service/src/main 仅 javadoc 状态机注释命中，零方法声明）。
- GraphQL save 路径 `ErpCtContract__save` → `defaultPrepareSave:57-63` **零版本创建钩子**；`ErpCtContractVersionBizModel`（89 行）仅 CRUD（无 createVersion 业务方法，grep `createVersion|newEntity` 仅通用 CRUD helper）。

**L4 测试手工 seed 实证**（file:line 证据）：

- `TestErpCtContractTerminate#createVersion:183-192` — 经 `ErpCtContractVersion__save` mutation 手工 seed 版本（contractId/versionNo/isCurrent/status）。
- `TestErpCtContractPosting#createVersion:225-235` — 同样手工 seed；`setupActiveContract:126` **先手工 createVersion(contractId, 1, true, "FINALIZED") 再 activate**——activate 守卫要求 FINALIZED 版本，若 save 自动创建 v1 则无需手工 seed，测试结构本身证实 **v1 非自动创建**（否则 activate 前无需手工 seed）。

**裁决**：确认**无自动 v1**——无 submit/negotiate mutation + save 路径零版本钩子 + 测试须手工 seed 版本方能 activate。**与 A1.45-46 §6 衔接维持 P1-RC-072（修复归 MR1）**。

### 2-3 A4.2.157 amend 后行复制前端编排确认 — 确认无行复制（维持 P1-RC-073）

**L3 代码路径**（live code 实测）：

- `ErpCtContractAmendProcessor#amend:35-66` — 仅版本翻转编排：`:42-52` 遍历版本旧 isCurrent=true→false + updateEntity；`:54-60` `newEntity()` 新建版本头（contractId/versionNo=max+1/versionDate/isCurrent=true/status=DRAFT）；`:63-64` 合同头→DRAFT。**零 ErpCtContractLine 复制操作**。

**前端 AMIS census**（live code 实测）：

- `ErpCtContract.view.xml` rowActions（:153-205）：view/update(drawer)/activate/suspend/resume/terminate/view-line/delete——**无 amend 按钮**（amend 仅后端 mutation，前端未暴露行级按钮）；edit 表单 lines 子表 `grid="sub-grid-edit"`（:117，`ErpCtContractLine.view.xml` 手工行编辑）——**零行复制交互**。
- grep `复制行|copyLine|cloneLine|duplicate` 跨 `module-contract/erp-ct-web` **零命中**——前端无任何行复制编排。

**裁决**：确认**无行复制**——amend Processor 仅版本翻转 + 前端 edit 表单纯手工行编辑，变更单行须手工重录。**与 A1.45-46 §6 衔接维持 P1-RC-073（修复归 MR1）**。

### 2-4 A4.2.158 全局合同到期 job 确认 — 确认无自动 job（reuse P1-MA2-071 维持，修复归 MR1）

**job 注册权威点 census**（live code 实测）：

- `app-erp-all/src/main/resources/_vfs/nop/job/conf/` — **19 个 job.yaml + scheduler.yaml 全量清单**：erp-ast-depreciation / erp-crm-event-reminder / erp-crm-forecast-recalc / erp-crm-funnel-aggregation / erp-crm-lead-scoring-recalc / erp-crm-sequence-overdue / erp-cs-csat-reminder / erp-cs-entitlement-expiry / erp-cs-sla-scan / erp-fin-ar-ap-auto-recon / erp-fin-cash-forecast-refresh / erp-fin-deferred-posting-sweep / erp-hr-contract-expiry / erp-mfg-crp-run / erp-mfg-jobcard-auto-generate / erp-mnt-due-visit-generation / erp-prj-pnl-calc / erp-qa-spc-capability / erp-qa-spc-sampling / scheduler.yaml——**零 `erp-ct-*` job**；到期类 job 仅 `erp-hr-contract-expiry.job.yaml`（扫描 hr_contract，非 erp_ct_contract）+ `erp-cs-entitlement-expiry.job.yaml`（CS 权益）——**均非合同域到期扫描**。
- `scheduler.yaml` — 仅 `enabled: true`，零 per-job 注册。
- module-contract 零 Job bean：grep `class.*Job|@Cron|@Scheduled|cron` 跨 module-contract 仅命中 `ErpCtConfigs.java:50-58`（**e-signature 签名状态轮询 cron 配置项**，javadoc 显式「注册归部署 Non-Goal」）——非合同到期 job。
- 全仓反向 census：grep `ErpCtContract|erp_ct_contract` 跨 module-contract 外（Java/XML）**零业务命中**（仅 app-erp-all 资源索引/build 产物 + tools 脚本）。

**仅手工路径**：`ErpCtContractBizModel#expire:118-126`（ACTIVE 守卫 + setStatus(EXPIRED) + updateEntity），无任何自动调度。

**裁决**：确认**无自动到期扫描**——job 注册权威点 19 job.yaml + scheduler.yaml 零 contract 到期 job + scheduler.yaml 零注册 + module-contract 零 Job bean，仅手工 `expire:118` 可达。**与 A1.45-46 §6 衔接维持 reuse P1-MA2-071（resolved-via-deferral 注记，MR1 重新实现 ErpCtContractExpiryJob + 续期 + 分级通知义务声明，不重开 deferral 裁决）**。

### 2-5 A4.2.159 TERMINATED 隐式失效 InvoicePlan 确认 — 确认隐式失效（无显式截停标记，行为经状态守卫达成；维持 P1-RC-076 分级）

**L3 代码路径**（live code 实测）：

- `ErpCtInvoicePlanTriggerInvoiceProcessor#triggerInvoice:35-54` — **非 ACTIVE 态双守卫**：
  - `:46-49` **SUSPENDED 专属守卫**：`status==SUSPENDED` → 抛 `ERR_CT_CONTRACT_SUSPENDED`（携带 ARG_CONTRACT_CODE）。
  - `:50-54` **通用非 ACTIVE 守卫**：`status != ACTIVE` → 抛 `ERR_CT_CONTRACT_NOT_ACTIVE`（携带 ARG_CONTRACT_CODE + ARG_CURRENT_STATUS）。
- TERMINATED 合同路径：`plan.getContractLine().getContract()` status=TERMINATED → **命中 `:50-54` 通用守卫抛 ERR_CT_CONTRACT_NOT_ACTIVE**——未开票 InvoicePlan（isInvoiced=false 通过 `:38-41` 已开票守卫后）在 TERMINATED 合同上触发被拒。
- `ErpCtContractBizModel#terminate:107-110` 注释自承：「InvoicePlan 无独立状态列，合同头 TERMINATED 后未开票计划经合同头**隐式失效**（triggerInvoice 校验合同 ACTIVE 即拒绝，isInvoiced=false 永不可再触发）」——**无显式截停标记**，行为经状态守卫达成。
- `ErpCtErrors.java:40-44` — `ERR_CT_CONTRACT_NOT_ACTIVE` + `ERR_CT_CONTRACT_SUSPENDED` ErrorCode 存在。

**L4 测试证据**（2026-08-08 重跑全绿）：

- **重跑** `TestErpCtContractPosting#testTriggerInvoiceRejectedForSuspendedContract:93-105` — PASS：seed ACTIVE 合同 → suspend → saveInvoicePlan → triggerInvoice → `assertNotEquals(0, bad.getStatus())`（:104）——SUSPENDED 专属守卫运行时实证。
- **新增 TERMINATED 输入断言**（本计划测试侧补充，`TestErpCtContractPosting#testTriggerInvoiceRejectedForTerminatedContract`）：seed ACTIVE 合同 → `ErpCtContract__terminate` → saveInvoicePlan（isInvoiced=false）→ triggerInvoice → **assertNotEquals(0) PASS**——TERMINATED 合同上未开票 InvoicePlan 触发实际被拒（经 `:50-54` 通用非 ACTIVE 守卫），运行时实证。
- `TestErpCtContractTerminate#testTerminateFromActiveSucceeds:48` + `#testTerminateFromNegotiationSucceeds:60`（既有，PASS）——terminate 状态翻转主路径佐证。

**裁决**：确认**隐式失效**——TERMINATED 合同上未开票 InvoicePlan 触发实际被拒（ERR_CT_CONTRACT_NOT_ACTIVE 经通用非 ACTIVE 守卫），无显式截停标记，行为经状态守卫达成。**与 A1.45-46 §6 衔接维持 P1-RC-076（法务门控/截停/TODO/归档缺失分级不变，修复归 MR1）**。

### 2-6 A4.2.160 全局审批引擎确认 — 确认无审批工作流（维持 P1-RC-076~077 分级）

**L3 代码路径**（live code 实测）：

- `ErpCtApprovalMatrixBizModel`（17 行 CRUD 桩，仅 setEntityName）+ `ErpCtApprovalRecordBizModel`（17 行 CRUD 桩）——零业务方法。
- **xbiz census**：`ErpCtApprovalMatrix.xbiz` / `ErpCtApprovalRecord.xbiz` / `ErpCtContract.xbiz`（erp-ct-service/src/main/resources/_vfs/erp/ct/model/）——均 `<actions/>` **零自定义 action**（纯生成 CRUD，`_*.xbiz` 仅 DefaultBizGenExtends/PostExtends）；grep `approv|workflow|wf` 跨 erp-ct-service 命中均为 APPROVE_STATUS 常量/注释，**零审批流接线**。
- 合同提交无审批触发：save → `defaultPrepareSave:57-63` 零审批逻辑；activate → `ErpCtContractActivateProcessor#activate:33` 仅 NEGOTIATION 守卫 + type/direction + 版本 FINALIZED 校验，**零审批引擎调用**。

**全仓反向 census**：grep `ErpCtApprovalMatrix|ErpCtApprovalRecord` 跨 module-contract 外（Java）**零业务命中**（仅 tools 脚本 + app-erp-all 资源索引）——**无全局审批引擎驱动合同审批**。

**裁决**：确认**无审批工作流**——ApprovalMatrix/Record 仅 CRUD 桩 + xbiz 零接线 + save/activate 零审批触发 + 全仓零消费者。**与 A1.45-46 §6 衔接维持 P1-RC-077（审批引擎全缺）+ P1-RC-076（法务门控缺）分级不变（修复归 MR1）**。

### 2-7 A4.2.161 purchase/sales 订单行折扣应用确认 — 确认无自动折扣应用（维持 P1-RC-078 分级）

**grep census 复核**（live code 实测）：

- grep `resolveDiscount|IErpCtVolumeDiscountBiz|contractDiscount` 跨 `module-purchase`+`module-sales`（Java/XML 全量）**零命中**——采购/销售订单行**无调用合同折扣 API 站点**（`ErpCtVolumeDiscountBizModel#resolveDiscount:44` API 存在但仅 contract 域内部可达）。
- **订单行折扣字段对照**（ORM 真值）：
  - purchase：`app-erp-purchase.orm.xml:554-555` `ErpPurOrder` discountRate（整单折扣率）/discountAmount（折扣金额）——**手工录入字段**，无 contract 引用折扣关联。
  - sales：`app-erp-sales.orm.xml:184-185` `ErpSalOrderLine` discountRate/discountAmount（行折扣率/行折扣金额）+ `:295/346` `ErpSalOrder.contractId` 引用 **`ErpSalContract`（sales 域自有合同实体，非 ErpCtContract）**——销售订单关联的是 sales 域合同实体，与 contract 域 `ErpCtVolumeDiscount` 折扣带**无关联字段**。
- 全仓反向 census：grep `ErpCtVolumeDiscount` 跨 module-contract 外 **零业务命中**（仅 docs 引用）——无隐式消费方。

**裁决**：确认**无自动折扣应用**——purchase/sales 零 resolveDiscount 调用 + 订单行折扣字段纯手工（sales 订单 contractId 指向 sales 域 ErpSalContract 而非 contract 域折扣实体）。**与 A1.45-46 §6 衔接维持 P1-RC-078（跨域契约行为不一致，修复归 MR1，purchase/sales 跨域协调 ask-first）**。

### 2-8 A4.2.162 外部 OCR 服务确认 — 确认无 OCR 处理（维持 P1-RC-079 分级）

**grep census 复核**（live code 实测）：

- grep `ocr|fullText|tesseract|extractText` 跨 module-contract 命中文件均为**非业务逻辑**：
  - `ErpCtSignatureProviderRegistry.java:18` + `app-service.beans.xml:17` — 假阳性（"Registry" 含 ocr 子串，e-signature 注册表 javadoc）。
  - `app-erp-contract.orm.xml` / `_app.orm.xml:1161-1165` / deploy SQL / `ErpCtDocumentInputBean`/`OutputBean` / i18n / view.xml / `_ErpCtDocument.xmeta` / `_templates` — 均为**实体/API 字段定义**（OCR_TEXT/FULL_TEXT_SEARCH 列定义）。
- `ErpCtDocumentBizModel`（17 行 CRUD 桩，仅 setEntityName）——零 OCR 编排。
- **ocrText/fullTextSearch/ocrStatus 零 writer**：grep `setOcrText|setFullTextSearch|setOcrStatus` 跨全仓（除 _gen + API bean）**零业务命中**——setter 仅存在于生成的 `ErpCtDocumentInputBean.java:164/178/192` + `ErpCtDocumentOutputBean.java:165/179/193`（DTO 字段定义，非业务逻辑）。
- **全仓 OCR 服务 census**：grep `ocrService|ocr_service|tesseract|paddleocr` 跨 `app-erp-all`+`module-*`（除 contract）**零命中**——无外部 OCR 服务/异步任务。
- **前端**：`ErpCtDocument.view.xml` — ocrText/ocrStatus/fullTextSearch 仅作**显示列/手工可编辑字段**（:18-20 列 + :48-50 OCR 分组表单），attachmentFileId 上传无 OCR 钩子（grep ocr 跨 view 零命中）——文档上传后无任何机制填充。

**裁决**：确认**无 OCR 处理**——零 OCR 业务逻辑 + ocrText/fullTextSearch 零 writer（仅 DTO 字段定义）+ 全仓零 OCR 服务 + 前端上传零钩子，文档上传后字段永不填充。**与 A1.45-46 §6 衔接维持 P1-RC-079（OCR/全文/保留/Legal Hold 全缺 + legalHold ORM 缺，修复归 MR1；legalHold ORM 结构 + purgeDate→delete 数据删除须 ask-first + 独立 plan-audit）**。

---

## 3. 测试证据汇总（L4，断言强度 + 本计划测试侧补充）

| 工作项 | 测试 | 断言强度 | 覆盖存疑点 | 运行结果（2026-08-08） |
|--------|------|---------|-----------|----------------------|
| A4.2.155 | `TestErpCtContractPosting#createContract:192-208`（重跑，未设 totalAmount 的 save 成功） | 强（save 成功断言 status=0，实证金额不一致不拒绝） | SP-1（无跨字段校验运行时实证） | Tests run: 44 内含，0 failure ✅ |
| A4.2.156 | `TestErpCtContractPosting#createVersion:225` + `TestErpCtContractTerminate#createVersion:183`（手工 seed 版本，activate 前必须） | 强（手工 seed 结构证实 v1 非自动） | SP-2（无自动 v1） | 同上 ✅ |
| A4.2.157 | 前端 AMIS census（`ErpCtContract.view.xml` edit 表单 sub-grid-edit + rowActions 无 amend 按钮）+ grep 复制行零命中 | 只读 census | SP-3（无行复制前端编排） | ✅（census） |
| A4.2.158 | job 注册权威点 census（`app-erp-all/_vfs/nop/job/conf/` 19 job.yaml + scheduler.yaml 零 erp-ct-*）+ 全仓反向 grep | 只读 census | SP-4（无自动到期 job） | ✅（census） |
| A4.2.159 | **重跑** `TestErpCtContractPosting#testTriggerInvoiceRejectedForSuspendedContract:93-105`（SUSPENDED 拒绝）+ **新增** `#testTriggerInvoiceRejectedForTerminatedContract`（TERMINATED 拒绝） | 强（assertNotEquals(0) 双状态实证） | SP-5（TERMINATED 隐式失效） | Tests run: 44（含新增），0 failure ✅ |
| A4.2.160 | xbiz census（ApprovalMatrix/Record/Contract 三 xbiz 均 `<actions/>`）+ 全仓零消费者 grep | 只读 census | SP-6（无审批引擎） | ✅（census） |
| A4.2.161 | grep census（module-purchase/sales 零 resolveDiscount）+ ORM 字段对照（pur/sal 订单行折扣字段 + sales contractId→ErpSalContract） | 只读 census | SP-7（无自动折扣应用） | ✅（census） |
| A4.2.162 | 全仓 OCR census + ocrText/fullTextSearch 零 writer grep + `ErpCtDocument.view.xml` 上传钩子 census | 只读 census | SP-8（无 OCR 处理） | ✅（census） |

---

## 4. 运行时行为证据（L5）

- **复用 A1.45-46 §4/§5**：UC-CT-01/02/03/05/06/07/08/10 主路径静态判定 + A2.14 状态机迁移守卫（本报告不重审状态机维度）。本报告只补八项存疑点的运行时差异。
- **本切片补的运行时差异**（经 live code 实测 + L4 强断言 + 一处测试侧补充）：
  - **A4.2.155** GraphQL save 实证不拒绝金额不一致合同（`createContract` 未设 totalAmount 仍 save 成功）+ XMeta/前端零跨字段校验。
  - **A4.2.156** save 路径零版本钩子 + 测试手工 seed 版本证实 v1 非自动（activate 前须手工 seed FINALIZED 版本）。
  - **A4.2.157** amend Processor 仅版本翻转 + 前端 edit 表单纯手工行编辑（sub-grid-edit）零复制交互 + grep 复制行零命中。
  - **A4.2.158** job 注册权威点 19 job.yaml + scheduler.yaml 零 erp-ct-* + 零注册 + module-contract 零 Job bean（签名轮询 cron 为部署 Non-Goal 配置项非到期 job）。
  - **A4.2.159** TERMINATED 输入断言新增实证：TERMINATED 合同上未开票 InvoicePlan 触发实际被拒（ERR_CT_CONTRACT_NOT_ACTIVE 通用守卫 :50-54）+ SUSPENDED 拒绝测试重跑佐证（:46-49 专属守卫）。
  - **A4.2.160** 三 xbiz `<actions/>` 零接线 + save/activate 零审批触发 + 全仓零 ApprovalMatrix/Record 消费者。
  - **A4.2.161** purchase/sales 零 resolveDiscount 调用 + 订单行折扣字段纯手工 + sales contractId 指向 ErpSalContract（非 ErpCt 折扣实体）。
  - **A4.2.162** 全仓零 OCR 服务 + ocrText/fullTextSearch 零业务 writer + 文档上传零钩子（字段永不填充）。

---

## 5. 符合性结论（八项存疑点裁决）

### 5.1 八项裁决矩阵

| 工作项 | 来源存疑点 | §2 判据命中分支 | 运行时裁决 | finding 衔接 |
|--------|-----------|----------------|-----------|-------------|
| **A4.2.155** | A1.45-46 §7 SP-1 创建 totalAmount=∑行 隐式校验 | 确认无跨字段校验 | **确认无跨字段校验**（defaultPrepareSave 零金额校验 + XMeta 单字段 + 前端零校验 + save 实证不拒绝） | 维持 P1-RC-072（arm-index :258） |
| **A4.2.156** | A1.45-46 §7 SP-2 隐式创建 v1 版本 | 确认无自动 v1 | **确认无自动 v1**（无 submit/negotiate + save 零钩子 + 测试手工 seed 实证） | 维持 P1-RC-072（arm-index :258） |
| **A4.2.157** | A1.45-46 §7 SP-3 amend 前端行复制 | 确认无行复制 | **确认无行复制**（amend 仅版本翻转 + 前端零复制交互） | 维持 P1-RC-073（arm-index :259） |
| **A4.2.158** | A1.45-46 §7 SP-4 全局到期 job | 确认无自动 job | **确认无自动 job**（job 权威点 19 yaml + scheduler.yaml 零 erp-ct-* + 仅手工 expire:118） | 维持 reuse P1-MA2-071（arm-index :554） |
| **A4.2.159** | A1.45-46 §7 SP-5 TERMINATED 隐式失效 | 确认隐式失效（无显式截停标记） | **确认隐式失效**（TERMINATED 输入断言实证 ERR_CT_CONTRACT_NOT_ACTIVE，行为经状态守卫达成） | 维持 P1-RC-076（arm-index :262） |
| **A4.2.160** | A1.45-46 §7 SP-6 全局审批引擎 | 确认无审批工作流 | **确认无审批工作流**（CRUD 桩 + xbiz 零接线 + 零触发 + 零消费者） | 维持 P1-RC-077（arm-index :263） |
| **A4.2.161** | A1.45-46 §7 SP-7 订单侧折扣应用 | 确认无自动应用 | **确认无自动折扣应用**（purchase/sales 零调用 + 订单行字段纯手工） | 维持 P1-RC-078（arm-index :264） |
| **A4.2.162** | A1.45-46 §7 SP-8 外部 OCR 服务 | 确认无 OCR 处理 | **确认无 OCR 处理**（零业务逻辑 + 零 writer + 零服务 + 零钩子） | 维持 P1-RC-079（arm-index :265） |

### 5.2 裁决分支汇总

- **八项全部确认隐式机制缺失**（A4.2.155-162）——与 A1.45-46 §7 静态判定**无一翻转**，运行时未发现任何 grep 遗漏的隐式机制（前端编排/XMeta 跨字段/job 注册/审批引擎/折扣调用/OCR 服务）。
- **八项全部按 A1.45-46 §6 衔接维持既有 finding 分级**（P1-RC-072/073/076/077/078/079 + P1-RC-074/075 不涉及本切片存疑点维度除外声明见 §0 + reuse P1-MA2-071），无未经比对新建。
- **零升级触发 MR0**（运行时未发现活跃数据破坏——八项均为功能缺失/missing-automation 类，A1.45-46 §7 P0 评估一致）。
- **一处测试侧补充**（A4.2.159 TERMINATED 输入断言，限 erp-ct-service 测试类目，不触生产代码）。

---

## 6. 与 arm-index 衔接（维持既有分级裁决）

### 6.1 比对表

| 本切片存疑点 | 比对 arm-index | 裁决 | 差异依据 |
|-------------|---------------|------|---------|
| A4.2.155/156 创建校验 + v1 版本 | `P1-RC-072`（:258，A1.45-46 新建）| **维持 P1（Q4 强制实现，修复归 MR1）** | 同根因同控制点：创建校验 + submit/v1 + 预算缺失。本报告运行时确认无跨字段校验 + 无自动 v1 |
| A4.2.157 amend 行复制 | `P1-RC-073`（:259，A1.45-46 新建）| **维持 P1（Q4 强制实现，修复归 MR1）** | 同根因同控制点：行复制/驳回恢复缺失。本报告运行时确认 amend 仅版本翻转 + 前端零复制交互 |
| A4.2.158 到期 job | `P1-MA2-071`（:554，reuse 注记）| **维持 reuse resolved-via-deferral 注记（MR1 重新实现义务声明，不重开 deferral 裁决）** | 同根因同控制点：到期自动化缺失。本报告运行时确认 job 注册权威点 20 yaml 零 erp-ct-* |
| A4.2.159 TERMINATED 隐式失效 | `P1-RC-076`（:262，A1.45-46 新建）| **维持 P1（Q4 强制实现，修复归 MR1）** | 同根因同控制点：终止编排缺失。本报告运行时确认隐式失效（新增 TERMINATED 输入断言实证），无显式截停标记分级不变 |
| A4.2.160 审批引擎 | `P1-RC-077`（:263，A1.45-46 新建）| **维持 P1（Q4 强制实现，修复归 MR1）** | 同根因同控制点：审批工作流全缺。本报告运行时确认三 xbiz `<actions/>` 零接线 + 零触发 + 零消费者 |
| A4.2.161 折扣订单侧 | `P1-RC-078`（:264，A1.45-46 新建）| **维持 P1（Q4 强制实现，修复归 MR1）** | 同根因同控制点：折扣跨域消费缺失。本报告运行时确认 purchase/sales 零调用 + 订单行字段纯手工 |
| A4.2.162 OCR | `P1-RC-079`（:265，A1.45-46 新建）| **维持 P1（Q4 强制实现，修复归 MR1）** | 同根因同控制点：文档仓库引擎全缺。本报告运行时确认零 writer + 零服务 + 零钩子 |
| （声明）A4.2.155-162 其他 finding | `P1-RC-074`/`P1-RC-075`（:260/:261）| **不在本切片存疑点范围** | P1-RC-074（invoiceTerm 生成/已开票锁）/ P1-RC-075（消耗计费引擎）对应 A1.45-46 §7 未列入 SP-1..SP-8 的静态判定维度，本计划 Non-Goals 不覆盖，仅声明维持 |
| （声明）P1-MA2-072 | resolved-via-implementation | **不重审** | A1.45-46 §9 已确认 terminate 守卫扩展，本报告仅引用 terminate 行为作为 A4.2.159 探针输入 |

### 6.2 新 finding 清单

- **无**（零新 finding；八项全部经 grep arm-index 同域同控制点比对后给出「维持既有分级」裁决，符合 A1.45-46 §6「禁止未经比对新建」要求 + 计划 Phase 2 决策项）。

### 6.3 复用 finding 交叉引用注记（追加 RC A4.2.155-162 运行时确认）

- **P1-RC-072**（:258，UC-CT-01）：追加 RC A4.2.155/156 运行时确认注记——`ErpCtContractBizModel#defaultPrepareSave:57-63` 仅 businessDate 兜底 + `_ErpCtContract.xmeta` mandatory 全单字段（validator/expression 零命中）+ `ErpCtContract.view.xml` totalAmount 零 validator；GraphQL save 实证不拒绝金额不一致合同（`TestErpCtContractPosting#createContract:192-208` 未设 totalAmount 仍 save 成功）；无 submit/negotiate mutation + `TestErpCtContractPosting#createVersion:225`/`TestErpCtContractTerminate#createVersion:183` 手工 seed 版本（activate 前必须）证实 v1 非自动。维持 P1（修复归 MR1，纯 BizModel 预授权）。
- **P1-RC-073**（:259，UC-CT-02）：追加 RC A4.2.157 运行时确认注记——`ErpCtContractAmendProcessor#amend:35-66` 仅版本翻转（旧 isCurrent=false + 新建版本头 :54-60）零 ErpCtContractLine 复制 + `ErpCtContract.view.xml` rowActions 无 amend 按钮 + edit 表单 lines 子表 sub-grid-edit 纯手工编辑 + grep 复制行跨 erp-ct-web 零命中。维持 P1（修复归 MR1，纯 BizModel/Processor 预授权）。
- **P1-MA2-071**（:554，UC-CT-05）：追加 RC A4.2.158 运行时确认注记——job 注册权威点 `app-erp-all/_vfs/nop/job/conf/` 19 job.yaml + scheduler.yaml **零 `erp-ct-*` job**（到期类仅 erp-hr-contract-expiry[hr_contract]+erp-cs-entitlement-expiry[CS 权益]，均非 erp_ct_contract）+ scheduler.yaml 仅 enabled:true 零注册 + module-contract 零 Job bean（`ErpCtConfigs.java:50-58` 签名轮询 cron 系部署 Non-Goal 配置项）+ 全仓零 ErpCtContract 业务消费者——无自动到期扫描运行时确认成立，仅手工 `expire:118` 可达。维持 resolved-via-deferral 注记（MR1 重新实现 ErpCtContractExpiryJob + 续期 + 分级通知义务声明，不重开 deferral 裁决）。
- **P1-RC-076**（:262，UC-CT-06）：追加 RC A4.2.159 运行时确认注记——`ErpCtInvoicePlanTriggerInvoiceProcessor#triggerInvoice:35-54` 双守卫（SUSPENDED 专属 :46-49 ERR_CT_CONTRACT_SUSPENDED + 通用非 ACTIVE :50-54 ERR_CT_CONTRACT_NOT_ACTIVE）；**新增 `TestErpCtContractPosting#testTriggerInvoiceRejectedForTerminatedContract`**（seed ACTIVE→terminate→saveInvoicePlan→triggerInvoice→assertNotEquals(0) PASS）+ 重跑 `testTriggerInvoiceRejectedForSuspendedContract:93-105` PASS——TERMINATED 合同上未开票 InvoicePlan 触发实际被拒，隐式失效成立（terminate:107-110 注释自承，无显式截停标记）。维持 P1（修复归 MR1）。
- **P1-RC-077**（:263，UC-CT-07）：追加 RC A4.2.160 运行时确认注记——`ErpCtApprovalMatrixBizModel`/`ErpCtApprovalRecordBizModel` 17 行 CRUD 桩 + 三 xbiz（ApprovalMatrix/ApprovalRecord/Contract）均 `<actions/>` 零自定义 action + save/activate 零审批触发 + 全仓零 ErpCtApprovalMatrix/Record 业务消费者。维持 P1（修复归 MR1，纯 BizModel 预授权）。
- **P1-RC-078**（:264，UC-CT-08）：追加 RC A4.2.161 运行时确认注记——grep `resolveDiscount|IErpCtVolumeDiscountBiz` 跨 module-purchase+module-sales **零命中** + `ErpPurOrder` discountRate/discountAmount（:554-555）+ `ErpSalOrderLine` 行折扣（:406-407）纯手工字段 + `ErpSalOrder.contractId`（:295）指向 **ErpSalContract（sales 域自有实体）** 非 ErpCt 折扣实体——订单引用合同行时无折扣应用运行时确认成立。维持 P1（修复归 MR1，purchase/sales 跨域协调 ask-first）。
- **P1-RC-079**（:265，UC-CT-10）：追加 RC A4.2.162 运行时确认注记——grep `ocr|fullText|tesseract` 跨 module-contract 命中均非业务逻辑（"Registry" 子串假阳性 + 实体/DTO/DDL 字段定义）+ `ErpCtDocumentBizModel` 17 行 CRUD 桩 + ocrText/fullTextSearch/ocrStatus **零业务 writer**（setter 仅 API Bean DTO 定义）+ 全仓零 OCR 服务 + `ErpCtDocument.view.xml` 上传零钩子（ocr 字段仅显示/手工编辑）——文档上传后字段永不填充运行时确认成立。维持 P1（修复归 MR1；legalHold ORM 结构 + purgeDate→delete 数据删除须 ask-first + 独立 plan-audit）。

---

## 7. 过程纪律自检

- [x] **checker 退出码门控核查**：本报告为只读运行时确认 + 一处测试侧补充（A4.2.159 TERMINATED 输入断言，限 erp-ct-service 测试类目，**不触生产代码/ORM/api.xml/view.xml/config 默认值/真相源**），checker 复跑 **actual == baseline，零漂移**（R1a=0/R1b=0/R1c=0/R1d=14/R2a=34/R2b=229/R2c=1382/R2d=34/R3=5/R4=0/R5=0/R6=2/R7=0/R8=0/R10=6/R11=0/R12a=69/R12b=66/R12c=40，EXIT=0；测试补充不触生产代码，预期无漂移）。区分门控退出码 vs 纯 reporter 退出码——checker 脚本是纯 reporter（退出码恒 0），真正门控在 CI workflow 解析 actual > baseline。
- [x] **closure-audit 独立性声明**：本报告的 closure audit 将由独立子代理（新会话，不重用执行者上下文）执行，执行者不自我审计。
- [x] **与 arm-index 交叉去重声明**：本报告全部 8 项运行时裁决已按 §去重协议 grep arm-index 同域同控制点后给出「维持既有分级」结论（P1-RC-072/073/076/077/078/079 + reuse P1-MA2-071），**无未经比对直接新建的 finding**。
- [x] **测试侧补充范围声明**：本计划唯一代码变更 = 一处测试侧补充（A4.2.159 `TestErpCtContractPosting#testTriggerInvoiceRejectedForTerminatedContract` + 对应空 autotest.yaml case 目录），限 erp-ct-service 测试类目，不触生产代码。
- [x] **测试运行验证**：`mvn test -pl module-contract/erp-ct-service` → **Tests run: 44, Failures: 0, Errors: 0, Skipped: 0 — BUILD SUCCESS**（2026-08-08 实测，含新增 TERMINATED 断言；TestErpCtContractPosting=4 含新增 / TestErpCtContractTerminate / TestErpCtESignature=19 / TestErpCtContractRebate / TestErpCtRebateSettlementEnd / TestErpCtContractCrudSmoke 全绿）。
- [x] **只读审计纪律声明**：零生产代码/ORM/api.xml/view.xml/真相源变更（唯一代码变更 = 测试侧补充）；非目标项（P1-RC-072~079 修复 / P1-MA2-071 重新实现）归 MR1 R1.0 展开器（Q4 强制实现义务不因本计划改变；MR1 R1.0 不自动启动，待另行人工裁决）。

---

## 8. 报告 9 段完整性自检

| # | 段落 | 状态 |
|---|------|------|
| 1 | 存疑点清单与判据（A1.45-46 §7 八项 + 判据） | ✅ §1 |
| 2 | 运行时证据采集（L3 file:line + L4 强断言，八项逐项） | ✅ §2 |
| 3 | 测试证据汇总（L4 Test*.java + 断言强度 + 本计划测试侧补充） | ✅ §3 |
| 4 | 运行时行为证据（L5 复用 A1.45-46 §4 + 本切片差异） | ✅ §4 |
| 5 | 符合性结论（八项裁决矩阵 + §2 判据命中分支） | ✅ §5 |
| 6 | 与 arm-index 衔接（维持既有分级裁决 + 交叉引用注记 + 排除声明） | ✅ §6 |
| 7 | 过程纪律自检（checker actual==baseline + 独立性 + 交叉去重 + 测试范围声明 + 只读纪律） | ✅ §7 |
| 8 | 报告 9 段完整性自检 | ✅ §8 |
| 9 | 与既有 MA1/A1.x 报告差异增量声明 | ✅ §0 |

**9 段齐全**——本报告可定稿。

---

## 整体裁决

**PASS（八项存疑点全数收口，八项全部确认隐式机制缺失，零新 finding / 不触发 MR0）**：

- **A4.2.155-162 八项运行时确认全部证实 A1.45-46 §7 静态判定**——运行时未发现任何 grep 遗漏的隐式机制：无前端/XMeta 跨字段金额校验（save 实证不拒绝金额不一致合同）、无自动 v1 版本（测试手工 seed 实证）、无 amend 行复制前端编排、无全局到期 job（job 注册权威点 20 yaml 零 erp-ct-*）、TERMINATED 隐式失效经状态守卫达成（新增 TERMINATED 输入断言实证）、无审批工作流（xbiz 零接线 + 零消费者）、purchase/sales 零折扣调用（订单行字段纯手工 + sales contractId 指向 ErpSalContract）、无 OCR 处理（零 writer + 零服务 + 零钩子）。
- **八项全部衔接维持既有 finding 分级**：P1-RC-072（A4.2.155/156）/ P1-RC-073（A4.2.157）/ P1-MA2-071 reuse resolved-via-deferral（A4.2.158，MR1 重新实现义务声明）/ P1-RC-076（A4.2.159）/ P1-RC-077（A4.2.160）/ P1-RC-078（A4.2.161）/ P1-RC-079（A4.2.162）——Q4 强制实现义务归 MR1 R1.0 展开器，无未经比对新建 finding。
- **零升级触发 MR0**——运行时未发现活跃数据破坏（八项均为功能缺失/missing-automation/流程编排缺失类，A1.45-46 §7 P0 评估一致）。
- **A1.45-46 §7 八项静态判定无一翻转**。**本审计不实施生产代码修复**（唯一代码变更 = 一处测试侧补充：A4.2.159 TERMINATED 输入断言，限 erp-ct-service 测试类目；结果表面 = 本报告 + arm-index 交叉引用注记 + roadmap/log 同步）。
