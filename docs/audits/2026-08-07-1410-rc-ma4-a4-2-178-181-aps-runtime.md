# rc-ma4-a4-2-178-181-aps-runtime aps 域 WorkOrder 事件/路由/派工/sales 集成缺口运行时确认

> Plan: `docs/plans/2026-08-07-1410-3-rc-ma4-a4-2-178-181-aps-runtime.md`
> Mission: requirement-compliance（MA4 运行时行为验证 切片 A4.2.178-A4.2.181）
> Work Item: A4.2.178 / A4.2.179 / A4.2.180 / A4.2.181（A1.50 §7 SP-1..SP-4 四项静态存疑点运行时确认）
> 来源: `docs/backlog/requirement-compliance-roadmap.md` A4.2.178-A4.2.181
> Audit Status: closed
> 审计性质: **只读审计**（零生产代码/ORM/api.xml/view.xml/config 默认值/真相源变更；无新增测试——四项判据均经静态决定性事实 + 既有 L4 测试重跑佐证，plan 未要求新增探针）

## 0. 与既有 MA1/A1.x 报告差异增量声明（§9）

本报告**只补运行时行为证据**（methodology §去重协议），不重审 A1.50 已裁决的需求符合性结论与 finding 分级：

- **A1.50**（`docs/audits/2026-08-06-2243-2-rc-ma1-a1-50-aps-full.md`）：aps 域 7 UC 五级追踪（MA1 首审）+ §7 SP-1..SP-4 静态存疑点 + §6 finding（3 新 P1[P1-RC-088~090] + 2 新 P2[P2-RC-079~080] + 3 reuse[P0-MA2-019/P1-MA2-077/078]）。本报告复用其 L3 静态判定 + L4 测试断言 + §6 finding 编号，只补 **SP-1..SP-4（A4.2.178-181）四项存疑点的运行时证据**。
- **reuse P0-MA2-019 / P1-MA2-077 / P1-MA2-078**（产能预留 UK / OperationOrder 状态守卫 / cancel IN_PROGRESS 审批门控）：按 A1.50 §去重协议与 §7 P0 评估，**不重审**（resolved/行为证据引用）。
- 本切片不实施的修复义务（P1-RC-088/089/090 Q4 强制实现 + P2-RC-079/080 登记不强制）全部归 **MR1 R1.0 展开器**，本报告仅维持分级与声明触达区域。

本切片**只补**的运行时差异：(i) A4.2.178 某 WorkOrder 无 OperationOrder 时 `ApsLoadSourceProvider.findScheduledSlots` 返回空 → mfg CRP 该 WorkOrder 负荷盲（静态决定性 + `TestErpApsCrossDomainIntegration` 重跑佐证）；(ii) A4.2.179 引擎恒用 `op.machineId`、主工作中心满载时 `findFreeSlotForward` 返回 null 标 DRAFT 冲突、无备选降级（静态决定性 + 既有排满用例重跑）；(iii) A4.2.180 nop-job/调度零接线 + DispatchRule/DispatchLog 17 行空壳 + 无物料齐套检查 → PLANNED 工序无自动派工路径（grep census 实证）；(iv) A4.2.181 `scheduleBackward` DEADLINE_NOT_REACHABLE 仅记 SchedulingResult 无 sales 通知 + APS 侧 `earliestCompletionDate`/`checkFeasibility` @BizQuery 存在但 module-sales 零调用（grep census 实证）。

---

## 1. 存疑点清单与判据（A1.50 §7 四项）

| # | 工作项 | 来源存疑点 | 静态判定 | 运行时判据 |
|---|--------|-----------|---------|-----------|
| 1 | A4.2.178 | A1.50 §7 SP-1（UC-APS-01）WorkOrder 下达事件→OperationOrder 自动创建缺失（P1-RC-088）下，某 WorkOrder 无 OperationOrder → mfg CRP 负荷来源空 | `ErpApsOperationOrderBizModel#defaultPrepareSave:60` 仅自动 businessDate + grep `WorkOrder\|IErpMfgWorkOrderBiz\|Event` 仅 ApsLoadSourceProvider（aps→mfg 反向导出）无 mfg→aps 事件订阅 | 确认某 WorkOrder 无 OperationOrder 时 `ApsLoadSourceProvider.findScheduledSlots` 按 workOrderId 聚合返回空（CRP 负荷盲）；重跑 `TestErpApsCrossDomainIntegration` 佐证现状；裁决三选一：确认负荷来源空 / 行为翻转（存在隐式事件订阅或批量创建）/ 升级触发 MR0 |
| 2 | A4.2.179 | A1.50 §7 SP-2（UC-APS-06）路由缺失（P1-RC-089）下引擎恒用 `op.machineId` → 主工作中心满载时备选闲置、不降级 | `ErpApsSchedulingEngine` 零引用 ErpApsOpRouting（grep 零命中）+ OperationOrder 无 selectedRoutingId 列 + dict 无 UNSCHEDULABLE | 确认主工作中心满载时 `findFreeSlotForward` 主选失败行为（留 DRAFT + NO_AVAILABLE_SLOT 冲突，不尝试备选/降级）；重跑 `TestErpApsSchedulingEngine` 既有排满用例（同工作中心不重叠断言）；裁决三选一：确认无降级排产 / 行为翻转（存在路由消费或降级）/ 升级触发 MR0 |
| 3 | A4.2.180 | A1.50 §7 SP-3（UC-APS-07）派工缺失（P1-RC-090）下物料齐套永不检查 → 派工后物料短缺不可预见 | nop-job/调度零接线（grep 零命中）+ `ErpApsDispatchRuleBizModel`/`ErpApsDispatchLogBizModel` 17 行空壳 + 无物料齐套检查 | 确认 PLANNED 工序无自动派工路径阻止 IN_PROGRESS（自动派工机制不存在）；grep census（nop-job/job.yaml/batch.xml/物料齐套/JobCard）+ app-erp-all job conf 普查；裁决三选一：确认自动派工缺失 / 行为翻转（存在隐式调度或齐套检查）/ 升级触发 MR0 |
| 4 | A4.2.181 | A1.50 §7 SP-4（UC-APS-03/05）sales 集成缺失（P2-RC-079/080）下销售可能承诺 APS 无法达成的交期 | `scheduleBackward:105` DEADLINE_NOT_REACHABLE 冲突记入 SchedulingResult 但通知 sales 零实现 + APS 侧 `earliestCompletionDate:112`/`checkFeasibility:118` @BizQuery 暴露但 module-sales 零调用 | 确认 sales 承诺交期 < APS earliestCompletionDate 时无拦截/通知（违约风险）；grep census（aps→sales 通知零命中 + module-sales 调 ATP/CTP 零命中）；裁决三选一：确认跨域接线缺失 / 行为翻转（存在隐式通知或 sales 触发）/ 升级触发 MR0 |

---

## 2. 运行时证据采集（L3 `file:line` + L4 重跑，2026-08-07 live code 实测）

### 2-1 A4.2.178 WorkOrder 事件缺失 → CRP 负荷来源空确认 — 确认负荷来源空（维持 P1-RC-088 P1）

**L3 代码路径**（live code 实测）：

- `ErpApsOperationOrderBizModel#defaultPrepareSave:60-66`（实测 259 行文件）——`super.defaultPrepareSave` 之后**仅** businessDate 兜底（`:63-65`），**无 WorkOrder 事件订阅、无工艺路线读取、无批量 DRAFT 创建、无 workcenter 存在性校验**（与方法锚点 A1.50 §2 `:60` 完全一致，零漂移）。
- **事件订阅 census**：grep `GlobalRequestEvent\|subscribeEvent\|@Subscribe\|IErpMfgWorkOrderBiz` module-aps（excl _gen/target）**零命中**（rg_exit=1）；grep `WorkOrder` 命中均为 OperationOrder 自身 workOrderId 字段引用（BizModel:203/210/212/214、Engine:68/110/198/218/232/245/252）+ ApsLoadSourceProvider 导出字段——**无 mfg→aps WorkOrder 下达事件订阅**。
- `ApsLoadSourceProvider.findScheduledSlots:45-69`——按 `in("workOrderId", ids)` + `eq("status", PLANNED)` 过滤查询（`:50-55`），查询结果 `orders.isEmpty()` → `Collections.emptyList()`（`:67-69`）；**某 WorkOrder 无 OperationOrder（或仅 DRAFT）→ 返回空列表 → mfg CRP 该 WorkOrder 负荷盲**。
- mfg 侧消费方（对照）：`CrpLoadCalculator:107/115` `apsSlotsByWo.get(wo.getId())` → 无 slot 时 `woSlots=null` → 该 WorkOrder 无负荷时段聚合（aps→mfg **反向只读导出**方向确认，与 A1.50 §2 一致）。

**L4 重跑佐证**（2026-08-07 实测）：`TestErpApsCrossDomainIntegration` 3/3 绿——`testLoadSourceProviderReturnsScheduledSlots:44`（PLANNED 导出 1 slot）/ `testLoadSourceProviderExcludesNonPlannedOps:61`（**仅 DRAFT 工序 → slots.isEmpty() 实证负荷来源空**）/ `testLoadSourceProviderFiltersByWorkOrderId:73`（workOrderId 过滤）。其中 `:61` 实证"某 WorkOrder 无 PLANNED 工序 → 空"，与"某 WorkOrder 无 OperationOrder → 空"同路径（orders.isEmpty → emptyList）。

**裁决**：**确认负荷来源空可能**（三选一命中"确认负荷来源空"）——OperationOrder 须手工创建（defaultPrepareSave 无自动创建链路）且 findScheduledSlots 对无工序 WorkOrder 返回空，mfg CRP 该 WorkOrder 负荷盲成立。**与 A1.50 §6 衔接维持 P1-RC-088 P1**（Q4 强制实现；修复 = 订阅 manufacturing WorkOrder 下达事件[RELEASED]→读工艺路线工序列表→按 sequence 批量创建 OperationOrder(DRAFT) + workcenter 存在性校验；**跨域 manufacturing WorkOrder 事件契约须 ask-first**，事件订阅 Processor + 批量创建 BizModel 逻辑预授权——归 MR1 R1.0 展开器）。**不触发 MR0**（属计划完整性非数据破坏，产能预留 UK 仍防双占，A1.50 §7 P0 评估维持）。

### 2-2 A4.2.179 路由缺失 → 主工作中心过载确认 — 确认无降级排产（维持 P1-RC-089 P1）

**L3 代码路径**（live code 实测）：

- `ErpApsSchedulingEngine#scheduleForward:77`（前向）/`#scheduleBackward:119`（后向）——`timelines.computeIfAbsent(op.getMachineId(), WorkCenterTimeline::new)`：**恒用 `op.machineId` 绑定工作中心**，无任何路由选择入口。
- **路由消费 census**：grep `ErpApsOpRouting\|OpRouting\|selectedRoutingId\|manualOverride` `scheduling/` + `processor/`（excl _gen）**零命中**（rg_exit=1）；grep `selectedRoutingId\|UNSCHEDULABLE\|OP_STATUS_HOLD` module-aps 全模块（excl _gen/target）仅 view.xml 调色板数组 `warningVals=['ON_HOLD','SUSPENDED',...]`（ErpApsOperationOrder.view.xml:33 / ErpApsSchedule.view.xml:22，前端共享调色板残留，与后端 dict 真值无对应——已知 P2-MA4 族视觉类问题不属本切片）→ **引擎零消费 ErpApsOpRouting 确认**。
- **主选失败行为**：`findFreeSlotForward:81` 返回 null 时 `:82-88`——`setPlannedStartDateT(null)` + `setPlannedEndDateT(null)` + `setStatus(DRAFT)` + `addConflict(op.getId(), "NO_AVAILABLE_SLOT", "工作中心 " + op.getMachineId() + " 展望期内无连续可用时段")`；**无备选工作中心尝试、无降级路径**。`WorkCenterTimeline.findFreeSlotForward:53-73` 语义确认：遍历 busy 区间跳 end 重试，超 horizonEnd 返 null（capacity=1 单工位）。
- **ORM 真相源**：`ErpApsOperationOrder`（app-erp-aps.orm.xml:55-114）**无 selectedRoutingId 列**；`operation-order-status` dict（:8-14）仅 DRAFT/PLANNED/IN_PROGRESS/FINISHED/CANCELLED **5 值无 UNSCHEDULABLE**（2026-08-07 实测与 A1.50 §2 一致）。

**L4 重跑佐证**（2026-08-07 实测）：`TestErpApsSchedulingEngine` 6/6 绿——`testForwardScheduleSequenceMaintenanceCapacity:50` 断言同工作中心不重叠（**主选绑定行为**：工序 C 排 A 完工 10:00 之后而非另寻工作中心）；`testInsertRushOrderRevertsLowerPriorityOnly:100`/`testInsertRushOrderRejectsInProgress:128`/`testScheduleStateMachine:146`/`testCheckFeasibilityDoesNotPersistShadow:163`/`testBackwardScheduleDeadlineReachable:86` 全绿（排满/竞争场景均在主选工作中心内消化或标冲突，无降级断言且代码无降级路径）。

**裁决**：**确认无降级排产**（三选一命中"确认无降级排产"）——主工作中心满载时工序留 DRAFT + NO_AVAILABLE_SLOT 冲突，备选工作中心（即便配置 ErpApsOpRouting 备选）**运行时不会被消费** → 主工作中心过载/备选闲置并存成立。**与 A1.50 §6 衔接维持 P1-RC-089 P1**（Q4 强制实现；修复 = Engine 增路由选择[priority ASC 查 ErpApsOpRouting + 主选 findFreeSlotForward 失败自动尝试备选 + 回写 selectedRoutingId] + OperationOrder 加 selectedRoutingId 列 + dict 加 UNSCHEDULABLE；**触及 ORM 结构变更须 ask-first + 独立 plan-audit §5 ORM 类**，路由选择 Engine 逻辑预授权——归 MR1 R1.0 展开器）。**不触发 MR0**（产能预留 UK 防并发双占，排不下留 DRAFT 不破坏数据，A1.50 §7 P0 评估维持）。

### 2-3 A4.2.180 派工缺失 → 物料齐套永不检查确认 — 确认自动派工缺失（维持 P1-RC-090 P1）

**L3 代码路径**（live code 实测）：

- **nop-job/调度接线 census**：grep `IScheduler\|QuartzJob\|job.yaml\|batch.xml\|IJobInvoker\|@Scheduled\|nop-batch\|IJobExecutor` module-aps（excl _gen/target）**零命中**（rg_exit=1）；`find module-aps -name "*.job.yaml" -o -name "*.scheduler.yaml" -o -name "*.batch.xml"` **零文件**；app-erp-all job conf（job.yaml/scheduler.yaml/batch.xml 资源）**零 aps 引用**（rg 零命中）。
- `ErpApsDispatchRuleBizModel`（17 行）/`ErpApsDispatchLogBizModel`（17 行）——**纯 CrudBizModel 空壳**（仅构造器 setEntityName，零自定义 @BizMutation/@BizQuery/@BizAction；2026-08-07 实测 `wc -l` = 17/17，与方法锚点 A1.50 §2 一致）。
- **物料齐套 census**：grep `物料齐套\|material.*ready\|齐套\|JobCard\|IErpMfgJobCardBiz\|IErpInvStockBiz` module-aps/erp-aps-service/src/main **零业务命中**（唯一 `onHand` 命中 = `ErpApsAtpCtpServiceImpl:111/113` ATP 库存可用性检查，属 UC-APS-05 影子模拟，**非派工齐套检查**）。
- **状态推进入口**：`ErpApsOperationOrderBizModel#start:126-137`——仅手工 @BizMutation（PLANNED→IN_PROGRESS 守卫），**无自动派工路径**；OperationOrder status dict 无 HOLD/ON_HOLD 值（orm.xml:8-14）。
- **库存余额守恒不受影响确认**：库存消耗经 inventory 移动单状态机（`invStockMove` 状态守卫 + 余额更新），APS 侧无库存写路径——自动派工缺失**不破坏**库存余额守恒（A1.50 §7 SP-3 P0 评估维持）。

**运行时判据确认**（grep census 实证，无调度接线故无周期扫描可观察）：PLANNED 工序须手工 `start` 才可 IN_PROGRESS；物料齐套检查**不存在**，派工后物料短缺不可预见（开工后停线风险）。自动派工机制不存在确认成立。

**裁决**：**确认自动派工缺失**（三选一命中"确认自动派工缺失"）——nop-job 调度零接线 + 空壳 BizModel + 零齐套检查 → UC-APS-07 周期扫描/物料齐套/IN_PROGRESS 自动推进/DispatchLog/ON_HOLD 全部运行时不可达。**与 A1.50 §6 衔接维持 P1-RC-090 P1**（Q4 强制实现；修复 = nop-job bean 周期扫描 + 物料齐套[跨域 inventory] + 操作工/工装检查 + 记 DispatchLog + 手动强制/HOLD + ON_HOLD 通知 + JobCard[跨域 mfg]；**operation-order-status dict 加 HOLD/ON_HOLD 值须 ORM ask-first + 独立 plan-audit §5 ORM 类** + **跨域 inventory/manufacturing 协调 ask-first**，调度接线+BizModel 逻辑预授权——归 MR1 R1.0 展开器）。**不触发 MR0**（库存余额守恒不受影响，属运营效率非数据破坏，A1.50 §7 P0 评估维持）。

### 2-4 A4.2.181 sales 集成缺失 → 承诺不可达交期确认 — 确认跨域接线缺失（维持 P2-RC-079/080 P2）

**L3 代码路径**（live code 实测）：

- `ErpApsSchedulingEngine#scheduleBackward:105-155`——DEADLINE_NOT_REACHABLE 分支 `:139-145`：推算开工早于 earliest → `setStatus(DRAFT)` + `setFeasible(false)`（`:141`）+ `addConflict(op.getId(), "DEADLINE_NOT_REACHABLE", ...)`（`:142`）——冲突**仅记入 SchedulingResult**（返回给调用方，无任何外部通知动作）。
- **aps→sales 通知 census**：grep `IErpSal\|sales.*notify\|通知 sales\|通知销售` module-aps/erp-aps-service/src/main **零命中**（rg_exit=1）——P2-RC-079（通知 sales 零实现）运行时确认。
- **APS 侧能力存在性**：`ErpApsOperationOrderBizModel#earliestCompletionDate:112` / `#checkFeasibility:118` @BizQuery 完整暴露 → 委托 `ErpApsAtpCtpServiceImpl#earliestCompletionDate:53` / `#checkFeasibility:62`（影子模拟 + CtpResult.feasible/capacityGapMinutes，`testCheckFeasibilityDoesNotPersistShadow:163` 强测影子不持久化）——**APS 提供方服务完整**（A1.50 UC-APS-05 接受 on APS 侧维持）。
- **sales 侧零调用 census**：grep `atpCtp\|AtpCtp\|earliestCompletionDate\|checkFeasibility\|IErpApsAtpCtp` module-sales（excl _gen/target）**零命中**（rg_exit=1）——sales 域订单/行审核动作**不调用** APS ATP/CTP，P2-RC-080（跨域触发接线缺失）运行时确认。

**L4 重跑佐证**（2026-08-07 实测）：`TestErpApsDemandPlanning` 3/3 绿（`testAtpAvailableWhenStockSufficient`/`testAtpUnavailableWhenStockInsufficient`/`testEarliestCompletionDateReturnsNotNull`）+ `TestErpApsSchedulingEngine#testCheckFeasibilityDoesNotPersistShadow:163` 绿——APS 侧能力全部强测通过，**能力存在性确认**；sales 侧无任何测试调用 APS（零接线即零覆盖）。

**裁决**：**确认跨域接线缺失**（三选一命中"确认跨域接线缺失"）——sales 承诺交期 < APS earliestCompletionDate 时：APS 侧有检测能力（scheduleBackward DEADLINE_NOT_REACHABLE / checkFeasibility 结果）但**无任何机制送达 sales**（后向不可达不通知 + sales 审核不触发 ATP/CTP）→ 销售可能承诺 APS 无法达成的交期（订单交付违约风险）。**与 A1.50 §6 衔接维持 P2-RC-079/080 P2**（登记不强制 watch-only；修复 = scheduleBackward 不可达分支通知 sales[跨域 sales 协调 ask-first] 或经 notify 子系统派发 + sales 域审核动作注入 `IErpApsAtpCtpService`[跨域 sales↔aps ask-first]，BizModel 接线逻辑预授权——归 MR1 R1.0 展开器）。**不触发 MR0**（属客户交付 SLA 风险非数据破坏，A1.50 §7 P0 评估维持）。

---

## 3. 测试证据（L4 重跑，2026-08-07 实测）

`mvn test -pl module-aps/erp-aps-service` → **Tests run: 36, Failures: 0, Errors: 0, Skipped: 0 — BUILD SUCCESS**：

| 测试类 | 用例数 | 结果 | 佐证存疑点 |
|--------|--------|------|-----------|
| `TestErpApsCrossDomainIntegration` | 3 | ✅ 3/3 | A4.2.178（PLANNED 导出/DRAFT 排除[负荷来源空实证]/workOrderId 过滤） |
| `TestErpApsSchedulingEngine` | 6 | ✅ 6/6 | A4.2.179（同工作中心不重叠[主选绑定]）/ A4.2.181（checkFeasibility 影子不持久化） |
| `TestErpApsCapacityReservation` | 3 | ✅ 3/3 | reuse P0-MA2-019 行为证据（并发冲突/预留获取/插单释放） |
| `TestErpApsOperationOrderStateGuards` | 11 | ✅ 11/11 | reuse P1-MA2-077 行为证据（start/complete/cancel 守卫）+ A4.2.180（仅手工 start 路径） |
| `TestErpApsDemandPlanning` | 3 | ✅ 3/3 | A4.2.181（ATP/CTP 能力强测） |
| `TestErpApsScheduleManagement` | 5 | ✅ 5/5 | UC-APS-02 方案状态机（接受维持） |
| `TestErpApsOperationOrderCrudSmoke` | 5 | ✅ 5/5 | UC-APS-01 手工 CRUD 路径（无事件/批量创建断言——断言缺口维持） |

**断言缺口维持**（与 A1.50 §3 一致）：UC-APS-01 自动创建链路零断言；UC-APS-03 后向时间戳/冲突断言弱（`testBackwardScheduleDeadlineReachable:86` 仅 status==0）；UC-APS-06 路由零断言；UC-APS-07 派工零断言——四项缺口即四项 finding 的 L4 侧证据（无测试覆盖即无运行时实现）。

---

## 4. compliance checker 基线确认（§8 过程纪律）

本报告为**只读审计，零生产代码变更**。`bash docs/audits/nop-compliance-checker.sh` 实测（2026-08-07）actual vs `compliance-baseline.md`：

| 规则 | actual（实测） | 基线 | 结论 |
|------|--------------|------|------|
| R1d | 14 | 14 | = ✅ |
| R2a | 34 | 34 | = ✅ |
| R2b | 229 | 229 | = ✅ |
| R2c（全生产代码 daoFor 总量） | 1382 | 1382 | = ✅ |
| R2d | 34 | 34 | = ✅ |
| R8 | 0 | 0 | = ✅ |

**actual == baseline 零漂移**（git status 仅新增/修改 .md 文档文件，无任何生产代码/ORM/view.xml 变更）——checker 退出码门控：本报告不以 checker 脚本退出码 0 作为门控通过依据（checker 为纯 reporter，真正门控在 CI workflow 解析 actual > baseline），以 actual == baseline 零漂移作为只读审计无回归证据。

---

## 5. 与既有 finding 衔接（§6 规则：全部维持，无新 finding）

| Finding ID | A1.50 分级 | 本切片运行时裁决 | 衔接结论 |
|-----------|-----------|-----------------|---------|
| `P1-RC-088`（UC-APS-01 WorkOrder 事件→OperationOrder 自动创建缺失） | P1（Q4 强制实现，MR1） | A4.2.178 **确认负荷来源空**：defaultPrepareSave:60-66 无事件订阅/批量创建 + findScheduledSlots:45-69 无工序返空 + TestErpApsCrossDomainIntegration 重跑佐证 | **维持 P1**（修复归 MR1 跨域 manufacturing ask-first；不触发 MR0） |
| `P1-RC-089`（UC-APS-06 替代路由选择缺失） | P1（Q4 强制实现，MR1） | A4.2.179 **确认无降级排产**：引擎:77/119 恒用 op.machineId + 路由 grep 零命中 + findFreeSlotForward:81 null → DRAFT+NO_AVAILABLE_SLOT 无备选 + ORM 无 selectedRoutingId/dict 无 UNSCHEDULABLE | **维持 P1**（修复归 MR1 触 ORM ask-first；不触发 MR0） |
| `P1-RC-090`（UC-APS-07 自动派工缺失） | P1（Q4 强制实现，MR1） | A4.2.180 **确认自动派工缺失**：nop-job 零接线 + job conf 零 aps + DispatchRule/DispatchLog 17 行空壳 + 齐套 grep 零业务命中 + start 仅手工 | **维持 P1**（修复归 MR1 ORM + 跨域 inventory/manufacturing ask-first；库存余额守恒不受影响，不触发 MR0） |
| `P2-RC-079`（UC-APS-03 通知 sales 缺失 + 后向断言弱） | P2（登记不强制，successor watch-only） | A4.2.181 **确认通知 sales 零实现**：scheduleBackward:139-145 DEADLINE_NOT_REACHABLE 仅记 SchedulingResult + aps 主源 grep 零命中 | **维持 P2**（登记不强制；修复归 MR1 跨域 sales ask-first / notify 子系统派发 + 测试补充预授权） |
| `P2-RC-080`（UC-APS-05 sales ATP/CTP 触发接线缺失） | P2（登记不强制，successor watch-only） | A4.2.181 **确认 module-sales 零调用**：APS 侧 @BizQuery:112/118 完整 + 强测（能力存在）但 sales grep 零命中（接线缺失） | **维持 P2**（登记不强制；修复归 MR1 跨域 sales↔aps ask-first） |
| reuse `P0-MA2-019` / `P1-MA2-077` / `P1-MA2-078` | resolved（不重审） | TestErpApsCapacityReservation 3/3 + TestErpApsOperationOrderStateGuards 11/11 重跑全绿引作行为证据 | **维持注记**（resolved 不重审，§去重协议） |

**零新 finding**：四项存疑点运行时裁决全部与 A1.50 静态判定一致（无一翻转），无未经比对直接新建的 finding；全部经 grep arm-index aps/operationOrder/scheduling/routing/dispatch/atp/ctp/UC-APS 同域同控制点比对维持既有分级。

---

## 6. 多维度裁决汇总

| 工作项 | 存疑点 | 运行时裁决 | Finding 衔接 | 行为翻转 | MR0 |
|--------|--------|-----------|-------------|---------|-----|
| A4.2.178 | SP-1 WorkOrder 事件缺失→CRP 负荷来源空 | 确认负荷来源空 | P1-RC-088 维持 P1 | 否 | 不触发 |
| A4.2.179 | SP-2 路由缺失→主工作中心过载/备选闲置并存 | 确认无降级排产 | P1-RC-089 维持 P1 | 否 | 不触发 |
| A4.2.180 | SP-3 派工缺失→物料齐套永不检查 | 确认自动派工缺失 | P1-RC-090 维持 P1 | 否 | 不触发 |
| A4.2.181 | SP-4 sales 集成缺失→承诺不可达交期 | 确认跨域接线缺失 | P2-RC-079/080 维持 P2 | 否 | 不触发 |

A1.50 §7 四项静态存疑点**全部运行时确认成立、无一翻转**，无需细化裁决/回写 A1.50 报告（A1.50 维持 closed）。

---

## 7. 过程纪律自检

- [x] **checker 退出码门控核查**：已运行 `bash docs/audits/nop-compliance-checker.sh`（2026-08-07）。区分门控退出码 vs 纯 reporter 退出码——checker 脚本是纯 reporter（退出码恒 0），真正门控在 CI workflow（`.github/workflows/compliance.yml`）解析 actual > baseline => sys.exit(1)。本报告**不以** checker 脚本退出码 0 作为门控通过依据。**本报告为只读审计无生产代码变更，checker 无回归风险**；actual 实测（R1d=14/R2a=34/R2b=229/R2c=1382/R2d=34/R8=0）**== baseline 零漂移**（§4）。
- [x] **closure-audit 独立性声明**：本报告的 closure audit 将由独立子代理（新会话，不重用执行者上下文）执行，执行者不自我审计。
- [x] **与 arm-index 交叉去重声明**：本报告全部裁决经 grep arm-index aps/operationOrder/scheduling/forward/backward/rush/atp/ctp/dispatch/routing/constraint/UC-APS 同域同控制点比对后给出"维持"结论（P1-RC-088/089/090 维持 P1 + P2-RC-079/080 维持 P2 + reuse P0-MA2-019/P1-MA2-077/078 维持注记不重审），**0 新 finding**，无未经比对直接新建的 finding。
- [x] **真相源冻结声明（§9）**：本审计期间未修改任何真相源（`docs/design/aps/use-cases.md` / owner doc / `product-scope.md` / `<domain>/model/*.orm.xml` / `.api.xml` / 生产代码 / view.xml）；分歧记入报告不直改。

---

## 报告 9 段完整性自检

- [x] §0 与既有报告差异增量声明（§9 去重协议）✅
- [x] §1 存疑点清单与判据（A1.50 §7 四项 + 运行时判据三选一）✅
- [x] §2 运行时证据采集（四项各含 L3 file:line + grep census + 裁决）✅
- [x] §3 测试证据（L4 重跑 36/36 全绿 + 断言缺口维持）✅
- [x] §4 compliance checker actual == baseline 零漂移 ✅
- [x] §5 与既有 finding 衔接（P1-RC-088/089/090 + P2-RC-079/080 + reuse 三项，全部维持）✅
- [x] §6 多维度裁决汇总（四项确认成立无一翻转）✅
- [x] §7 过程纪律自检（checker 门控 + closure-audit 独立性 + 交叉去重 + 冻结声明）✅
