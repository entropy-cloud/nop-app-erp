# rc-ma4-a4-2-182-185-notify-runtime notify 接收人解析器与 kill-switch 运行时确认

> Plan: `docs/plans/2026-08-07-1533-1-rc-ma4-a4-2-182-185-notify-runtime.md`
> Mission: requirement-compliance（MA4 运行时行为验证 切片 A4.2.182-A4.2.185）
> Work Item: A4.2.182 / A4.2.183 / A4.2.184 / A4.2.185（A1.51 §7 SP-1..SP-4 四项静态存疑点运行时确认）
> 来源: `docs/backlog/requirement-compliance-roadmap.md` A4.2.182-A4.2.185
> Audit Status: closed
> 审计性质: **只读审计 + 测试侧补充**（唯一代码变更 = erp-notify-service 测试类目新增 1 个运行时测试类；零生产代码/ORM/api.xml/view.xml/config 默认值/真相源变更；A4.2.183 跨域消费者 census 为只读探针不改他域代码）

## 0. 与既有 MA1/A1.x 报告差异增量声明（§9）

本报告**只补运行时行为证据**（methodology §去重协议），不重审 A1.51 已裁决的需求符合性结论与 finding 分级：

- **A1.51**（`docs/audits/2026-08-06-2243-3-rc-ma1-a1-51-notify-full.md`）：notify 域 7 UC 五级追踪（MA1 首审）+ §7 SP-1..SP-4 静态存疑点 + §6 finding（1 新 P2[P2-RC-081]）。本报告复用其 L3 静态判定 + L4 测试断言 + §6 finding 编号，只补 **SP-1..SP-4（A4.2.182-185）四项存疑点的运行时证据**。
- **reuse P1-MA1-022**（跨域 daoFor 平台一致性 resolved）：按 A1.51 §去重协议**不重审**。
- 本切片不实施的修复义务（P2-RC-081 修复归 MR1 R1.0 展开器，本报告仅确认运行时行为）按 plan Non-Goals 声明维持。

本切片**只补**的运行时差异：(i) A4.2.182 ROLE resolver 运行时 roleName 匹配（平台 nop-auth 种子角色数据 + GraphQL notify 端到端）——**新增 dedicated 测试**（A1.51 §3 L4 缺口：ROLE 零独立运行时覆盖）；(ii) A4.2.183 USER_LIST `${var}` 动态插值运行时 + 多域消费者 context 键 census——**新增 dedicated 测试**；(iii) A4.2.184 ORG resolver deptId 精确匹配运行时（无子部门递归确认）——**新增 dedicated 测试**；(iv) A4.2.185 `erp-notify.enabled=false` kill-switch 运行时行为——**新增运行时探针测试**。

---

## 1. 存疑点清单与判据（A1.51 §7 四项）

| # | 工作项 | 来源存疑点 | 静态判定 | 运行时判据 |
|---|--------|-----------|---------|-----------|
| 1 | A4.2.182 | A1.51 §7 SP-1（UC-SYS-03 ROLE）ROLE resolver 运行时 roleName 匹配——`resolveRole:117` 经 `in("roleName", roleNames)` 查 NopAuthRole 后再 `roleNameSet.contains` 二次过滤（`:136-141`）→ `in("roleId", ...)` 查 NopAuthUserRole → userId 集合；运行时平台 nop-auth 角色数据 roleName 字段与模板配置 roles 列表一致性 + 种子覆盖 | 静态实现完整；L4 零 dedicated 测试（USER_LIST 独占覆盖，A1.51 §3/§4 证实） | seed NopAuthRole(roleName)+NopAuthUser+NopAuthUserRole（对齐 seedRole 模式）→ 模板 ROLE + roles 配置 → GraphQL notify → 断言 recipientUserId 命中；不匹配角色名 → config-gated 空集（resolveRole:119-144）；裁决三选一：主路径闭合（运行时证实）/ 行为翻转 / 升级触发 MR0 |
| 2 | A4.2.183 | A1.51 §7 SP-2（UC-SYS-03 USER_LIST）`${var}` 动态插值运行时覆盖——`interpolateConfig:206` 对 recipientConfig 中 `${var}` 按 context 插值（CFG_VAR_PATTERN `:62-63`）；L4 测试全静态 userIds，无 dedicated ${var} 插值断言 | 静态实现完整；跨域消费者先例已存在（`TestErpPurPaymentApprovalNotifications.seedResultTemplate:234-235` 已用 `{"userIds":["${submitterUserId}"]}` 经 notify 链路） | 模板 USER_LIST `{"userIds":["${submitterUserId}"]}` + context 含 submitterUserId → notify → 断言插值后接收人 == 动态值；census cs/finance/sales 消费者 context 键传递（submitterUserId/escalationUserId 等，只读）；裁决三选一：主路径闭合 / 行为翻转 / 升级触发 MR0 |
| 3 | A4.2.184 | A1.51 §7 SP-3（UC-SYS-03 ORG）ORG resolver deptId 精确匹配运行时——`resolveOrg:158` 经 `eq("deptId", deptId)` 精确匹配 NopAuthUser；L1 `use-cases.md:83-84`「deptId → NopAuthUser」为精确匹配语义（未要求子部门级联） | 静态实现完整；静态语义符合 L1 | seed NopAuthUser 含 deptId（同/异 deptId 两用户）→ 模板 ORG + deptId 配置 → notify → 断言精确命中（同 deptId 命中、异 deptId 不命中、无子部门递归）；裁决三选一：主路径闭合（精确匹配符合 L1）/ 行为翻转 / 升级触发 MR0 |
| 4 | A4.2.185 | A1.51 §7 SP-4（UC-SYS-07）`erp-notify.enabled=false` kill-switch 运行时行为——`ErpNotifyConfigs.CONFIG_NOTIFY_ENABLED`（`:15-16`）声明但 `AppConfig.var` 零消费（`ErpSysNotificationNotifyProcessor.notify:40-63` 无总开关检查，P2-RC-081） | 静态已确认 kill-switch 失效（设 enabled=false 通知仍派发） | `AppConfig.getConfigProvider().assignConfigValue("erp-notify.enabled", "false")`（对齐 `TestErpCsSlaNotification:103` 模式）→ seed 模板 + notify → 断言通知仍落库（kill-switch 失效运行时证实）→ finally 恢复 true；裁决三选一：确认仍落库（维持 P2-RC-081）/ 行为翻转（kill-switch 生效）/ 升级触发 MR0 |

---

## 2. 运行时证据采集（L3 `file:line` + 新增 L4 测试，2026-08-07 live code 实测）

### 2-1 A4.2.182 ROLE resolver 运行时 roleName 匹配 — 主路径闭合（运行时证实双过滤正确）

**L3 代码路径**（live code 实测，`module-notify/erp-notify-service/src/main/java/app/erp/notify/service/dispatch/NotificationRecipientResolver.java`）：

- `resolve:83`——先 `interpolateConfig:206` 再 `parseConfig:183` 后按 resolver 类型分发；`case RESOLVER_ROLE:96-98`（default 分支）→ `resolveRole(cfg)`。
- `resolveRole:117-156`——`:119-121` 无 roles 或空列表 → 空集；`:128` `in("roleName", roleNames)` 查 NopAuthRole；`:131-133` roles 为空 → LOG.warn + config-gated 空集；`:136-141` **`roleNameSet.contains(r.getRoleName())` 二次过滤**后收集 roleId；`:147` `in("roleId", roleIds)` 查 NopAuthUserRole → `:149-155` 收集 userId。
- 平台 nop-auth 实体只读查询（`NopAuthRole`/`NopAuthUserRole`），与 A1.51 §2 锚点一致（`resolveRole:117`/`:136-141` 零漂移）。

**L4 运行时测试**（本切片新增 `TestErpSysNotificationRecipientResolverRuntime`）：

- `testRoleResolverMatchesRoleName`——seedRole("财务员", "rr-role-user-1")（对齐 `TestErpPurPaymentApprovalNotifications.seedRole:259-286` 模式：NopAuthRole[roleId="rr-role-财务员"+roleName="财务员"] + NopAuthUser[userId+userName+nickName+password+openId+gender+userType+status+tenantId] + NopAuthUserRole[userId+roleId]）→ 模板 ROLE + `{"roles":["财务员"]}` → GraphQL `ErpSysNotification__notify` → **断言 1 行落库 + recipientUserId == "rr-role-user-1" + status=SENT** —— ROLE 链 `in("roleName")` + `roleNameSet.contains` 双过滤在平台 nop-auth 种子数据下运行时正确。
- `testRoleResolverUnknownRoleNameConfigGatedEmpty`——模板 ROLE + `{"roles":["不存在的角色"]}`（该角色名无 NopAuthRole 匹配）→ notify → **断言零落库**（resolveRole:131-133 无匹配角色 → config-gated 空集 → `NotificationDispatcher.dispatch:81-85` recipients.isEmpty → 跳过）。

**裁决**：**主路径闭合**（三选一命中"主路径闭合"）——ROLE resolver 在平台 nop-auth 角色数据（roleName 精确匹配 + roleNameSet 二次过滤）下运行时解析正确，种子角色数据覆盖确认成立；不匹配角色名 config-gated 空集行为确认。

### 2-2 A4.2.183 USER_LIST `${var}` 动态插值运行时 — 主路径闭合（运行时证实插值 + 消费者 census）

**L3 代码路径**（live code 实测，`NotificationRecipientResolver.java`）：

- `interpolateConfig:206-221`——`:207-209` 无 context/无 `${` → 原样返回；`:212-219` `CFG_VAR_PATTERN`（`:62-63` `\$\{([a-zA-Z_][a-zA-Z0-9_]*)\}`）逐个占位符从 context 取值替换；`:216-217` 缺失键 → 空串；→ `parseConfig:183` 解析插值后 JSON → `resolveUserList:103-115` 收集 userIds。
- 派发链：`ErpSysNotificationNotifyProcessor.notify:40` → `NotificationDispatcher.dispatch:76`（`:80` `recipientResolver.resolve(template, context)` 传 context → 插值生效）。

**跨域消费者 context 键 census**（只读探针，2026-08-07 live code 实测）：

| 消费域 | 事件类型 | context 键（key → 值来源） | 调用点 |
|--------|---------|---------------------------|--------|
| purchase（wf） | `wf.pur-payment.result` | `submitterUserId` → `result.createdBy` | `payment-approval/v1.xwf:106-119` |
| sales（wf） | `wf.sal-receipt.result` | `submitterUserId` → `result.createdBy` | `receipt-approval/v1.xwf:103` |
| assets（wf） | `wf.ast-disposal.result` | `submitterUserId` → `result.createdBy` | `asset-disposal-approval/v1.xwf:103` |
| hr（wf） | `wf.hr-salary.result` | `submitterUserId` → `result.createdBy` | `salary-approval/v1.xwf:154` |
| cs | `cs.sla-overdue` | `escalationUserId` → `ticket.getAssignedToId()` + ticketId/ticketCode/customerName | `ErpCsTicketScanOverdueTicketsProcessor:101-104` |
| finance | `fin.posting-exception` | exceptionId/billHeadCode/businessType/postingType/errorCode/errorMessage/failedStage/voucherDate/postingNo/amount | `ErpFinPostingExceptionRecorder:143-155` |
| sales | `sal.credit-over-limit` | customerName/orderNo/orderAmount/creditLimit/overAmount | `CreditLimitChecker:244-249` |

census 结论：`submitterUserId` 键由 4 个审批 wf listener 注入（pur-payment/sal-receipt/ast-disposal/hr-salary，均为 `result.createdBy`），cs 用 `escalationUserId`，全部经 `IErpSysNotificationBiz.notify` 统一入口 → `interpolateConfig` 插值路径运行时可达；既有跨域端到端先例（`TestErpPurPaymentApprovalNotifications` Phase 1 result 通知）已实证该链。

**L4 运行时测试**（本切片新增）：

- `testUserListVarInterpolation`——模板 USER_LIST + `{"userIds":["${submitterUserId}"]}` + context `{"submitterUserId": "rr-dynamic-user-1"}` → notify → **断言 1 行落库 + recipientUserId == "rr-dynamic-user-1"**（非字面 `${submitterUserId}`）——interpolateConfig:206-221 运行时插值正确。

**裁决**：**主路径闭合**（三选一命中"主路径闭合"）——USER_LIST `${var}` 从 context 动态插值运行时正确，多域消费者（wf 族 submitterUserId + cs escalationUserId + fin/sales 事件上下文）context 键传递链路 census 成立，${var} 在多域调用链运行时可达。

### 2-3 A4.2.184 ORG resolver deptId 精确匹配运行时 — 主路径闭合（精确匹配符合 L1，无子部门递归）

**L3 代码路径**（live code 实测，`NotificationRecipientResolver.java`）：

- `resolveOrg:158-173`——`:159-162` 无 deptId 或空白 → 空集；`:164` `eq("deptId", deptId.toString())` 精确匹配 NopAuthUser；`:165-171` 收集 userId。**无子部门递归**（无 deptId → 子部门 deptId 集合展开逻辑，grep `parentDeptId\|children\|recursion\|递归` resolveOrg 周边零命中）。

**L4 运行时测试**（本切片新增）：

- `testOrgResolverDeptIdExactMatch`——seed NopAuthUser 两用户（`rr-org-user-1` deptId="D-100" / `rr-org-user-2` deptId="D-200"）→ 模板 ORG + `{"deptId":"D-100"}` → notify → **断言 D-100 用户精确命中 1 行 + D-200 用户零行** —— 精确匹配语义运行时成立、异 deptId 不误命中、无子部门级联。

**裁决**：**主路径闭合**（三选一命中"主路径闭合"）——L1 `use-cases.md:83-84`「deptId → NopAuthUser」精确匹配语义运行时成立，无需子部门递归（静态语义符合 L1 确认，运行时行为一致）。

### 2-4 A4.2.185 erp-notify.enabled=false kill-switch 运行时 — 确认仍落库（维持 P2-RC-081）

**L3 代码路径**（live code 实测）：

- `ErpNotifyConfigs.java:15-16`——`CONFIG_NOTIFY_ENABLED = "erp-notify.enabled"` + `DEFAULT_NOTIFY_ENABLED = true` 声明。
- `AppConfig.var` 消费 census（`module-notify/erp-notify-service/src/main` 全量）：仅 `NotificationDispatcher:117`（merge-enabled）/`:152`（email-enabled）/`:161`（sms-enabled）3 处，**`CONFIG_NOTIFY_ENABLED` 零消费**（与 A1.51 §5/P2-RC-081 静态判定一致）。
- `ErpSysNotificationNotifyProcessor.notify:40-63`——入口 `:42` 直接 `findActiveTemplate`，**无总开关检查**；`NotificationDispatcher.dispatch:76-99` 亦不读总开关。

**L4 运行时探针**（本切片新增）：

- `testNotifyEnabledFalseStillDispatches`——`AppConfig.getConfigProvider().assignConfigValue("erp-notify.enabled", "false")`（对齐 `TestErpCsSlaNotification:103` 模式）→ seed 模板 USER_LIST → notify → **断言通知仍落库 1 行 + status=SENT** → `finally` 恢复 `"true"` —— kill-switch 失效运行时证实（设 enabled=false 通知仍派发）。

**裁决**：**确认仍落库**（三选一命中"确认仍落库"）——`erp-notify.enabled=false` 运行时通知仍派发落库，`CONFIG_NOTIFY_ENABLED` 零消费运行时证实，**与 A1.51 §6 衔接维持 P2-RC-081**（kill-switch 失效，P2 登记不强制，修复归 MR1 R1.0 展开器：`notifyProcessor.notify` 入口加 `if (!AppConfig.var(CONFIG_NOTIFY_ENABLED, DEFAULT_NOTIFY_ENABLED)) return Collections.emptyList()` 早返回，纯 BizModel 代码逻辑预授权不触 ask-first）。**不触发 MR0**（kill-switch 失效不影响默认 true 路径派发正确性 + notify 为 best-effort 通知子系统不破坏业务事实，A1.51 §7 P0 评估维持）。

---

## 3. 测试证据（L4，2026-08-07 实测）

`mvn test -pl module-notify/erp-notify-service` → **Tests run: 23, Failures: 0, Errors: 0, Skipped: 0 — BUILD SUCCESS**：

| 测试类 | 用例数 | 结果 | 佐证存疑点 |
|--------|--------|------|-----------|
| `TestErpSysNotificationRecipientResolverRuntime`（**本切片新增**） | 5 | ✅ 5/5 | A4.2.182（testRoleResolverMatchesRoleName/testRoleResolverUnknownRoleNameConfigGatedEmpty）+ A4.2.183（testUserListVarInterpolation）+ A4.2.184（testOrgResolverDeptIdExactMatch）+ A4.2.185（testNotifyEnabledFalseStillDispatches） |
| `TestErpSysNotificationDispatch` | 5 | ✅ 5/5 | 既有回归（派发链/合并/已读/EMAIL config-gated/无模板静默跳过） |
| `TestErpSysNotificationTemplateLifecycle` | 3 | ✅ 3/3 | 既有回归（ACTIVE 派发/DRAFT 跳过/渲染） |
| `TestErpSysNotificationSubscription` | 2 | ✅ 2/2 | 既有回归（markAllRead/findUnread） |
| `TestErpSysNotificationSeedTemplates` | 4 | ✅ 4/4 | 既有回归（三类窗口合并/已读跨类型） |
| `TestErpSysNotificationCrossDomain` | 3 | ✅ 3/3 | 既有回归（跨域事件键） |

**L4 缺口闭合**：A1.51 §3/§4 记录的「ROLE/ORG/PARTNER 零 dedicated 运行时覆盖 + ${var} 插值零独立断言」——ROLE（2 测试：命中 + config-gated 空集）、ORG（1 测试：精确匹配/无级联）、${var} 插值（1 测试：动态值命中）已补 dedicated 运行时覆盖（PARTNER 占位行为 A1.51 已裁决接受——L1 逐字「占位 WARN 返回空 config-gated」，静态实现 `resolvePartner:175-181` 与 L1 逐字一致，非本切片范围）。

---

## 4. compliance checker 基线确认（§8 过程纪律）

本切片为**只读审计 + 测试侧补充**（唯一代码变更 = erp-notify-service 测试类目新增测试类，零生产代码/ORM/api.xml/view.xml/config 默认值/真相源变更）。`bash docs/audits/nop-compliance-checker.sh` 实测（2026-08-07）actual vs `compliance-baseline.md`：

| 规则 | actual（实测） | 基线（机器可读块） | 结论 |
|------|--------------|-------------------|------|
| R1d | 14 | 14 | = ✅ |
| R2a | 34 | 34 | = ✅ |
| R2b | 229 | 229 | = ✅ |
| R2c | 1382 | 1382 | = ✅ |
| R2d | 34 | 34 | = ✅ |
| R8 | 0 | 0 | = ✅ |

**actual == baseline 零漂移**（git status 仅新增/修改 .md 文档文件 + 1 个测试源文件，无任何生产代码/ORM/view.xml 变更）——checker 退出码门控：本报告不以 checker 脚本退出码 0 作为门控通过依据（checker 为纯 reporter，真正门控在 CI workflow 解析 actual > baseline），以 actual == baseline 零漂移作为只读审计 + 测试侧补充无回归证据。

---

## 5. 与既有 finding 衔接（§6 规则：全部维持，无新 finding）

| Finding ID | A1.51 分级 | 本切片运行时裁决 | 衔接结论 |
|-----------|-----------|-----------------|---------|
| `P2-RC-081`（UC-SYS-07 总开关 erp-notify.enabled 声明但从未消费，kill-switch 失效） | P2（登记不强制，successor watch-only + 声明 Q4=(a) 张力） | A4.2.185 **确认仍落库**：`assignConfigValue("erp-notify.enabled","false")` → notify 仍落库 1 行 + status=SENT（新增探针实证）+ `AppConfig.var` census 零消费 CONFIG_NOTIFY_ENABLED（`:15-16` 声明仅 3 处消费全为 merge/email/sms） | **维持 P2-RC-081 P2**（kill-switch 失效运行时确认成立；P2 登记不强制，修复归 MR1 R1.0 展开器纯 BizModel 预授权；不触发 MR0） |
| A4.2.182/183/184（A1.51 SP-1/2/3） | 静态接受（运行时覆盖交 MA4） | 主路径闭合（运行时证实 ROLE 双过滤 + ${var} 插值 + ORG 精确匹配） | **无新 finding 不登记**（主路径闭合/接受不登记，符合 plan §Goals「主路径闭合或接受」） |
| reuse `P1-MA1-022`（跨域 daoFor 平台一致性） | resolved（不重审） | 本切片测试侧仅引 NopAuth 实体 seed 模式（对齐 seedRole 先例），非跨域 daoFor 业务编排 | **维持注记**（resolved 不重审，§去重协议） |

**零新 finding**：四项存疑点运行时裁决全部与 A1.51 静态判定一致（无一翻转），无未经比对直接新建的 finding；P2-RC-081 经 grep arm-index notify/notification/总开关/kill-switch/notify.enabled 同域同控制点比对维持既有分级（无同根因同控制点既有 finding 可合并——P2-RC-081 为 notify 域唯一 RC finding，A1.51 §6.2 已裁决新建）。

---

## 6. 多维度裁决汇总

| 工作项 | 存疑点 | 运行时裁决 | Finding 衔接 | 行为翻转 | MR0 |
|--------|--------|-----------|-------------|---------|-----|
| A4.2.182 | SP-1 ROLE resolver 运行时 roleName 匹配 | 主路径闭合（双过滤正确 + config-gated 空集） | 无新 finding | 否 | 不触发 |
| A4.2.183 | SP-2 USER_LIST ${var} 动态插值运行时 | 主路径闭合（插值正确 + 多域消费者 census） | 无新 finding | 否 | 不触发 |
| A4.2.184 | SP-3 ORG resolver deptId 精确匹配运行时 | 主路径闭合（精确匹配符合 L1，无子部门递归） | 无新 finding | 否 | 不触发 |
| A4.2.185 | SP-4 erp-notify.enabled=false kill-switch 运行时 | 确认仍落库 | P2-RC-081 维持 P2 | 否 | 不触发 |

A1.51 §7 四项静态存疑点**全部运行时确认成立、无一翻转**，无需细化裁决/回写 A1.51 报告（A1.51 维持 closed）。预期 MR0 不触发（A1.51 §7 P0 评估维持——本切片无活跃数据破坏候选：kill-switch 失效不影响默认 true 路径派发正确性）。

---

## 7. 过程纪律自检

- [x] **checker 退出码门控核查**：已运行 `bash docs/audits/nop-compliance-checker.sh`（2026-08-07）。区分门控退出码 vs 纯 reporter 退出码——checker 脚本是纯 reporter（退出码恒 0），真正门控在 CI workflow（`.github/workflows/compliance.yml`）解析 actual > baseline => sys.exit(1)。本报告**不以** checker 脚本退出码 0 作为门控通过依据。**本切片为只读审计 + 测试侧补充，生产代码 zero 变更，checker 无回归风险**；actual 实测（R1d=14/R2a=34/R2b=229/R2c=1382/R2d=34/R8=0）**== baseline 机器可读块零漂移**（§4）。
- [x] **closure-audit 独立性声明**：本报告的 closure audit 将由独立子代理（新会话，不重用执行者上下文）执行，执行者不自我审计。
- [x] **与 arm-index 交叉去重声明**：本报告全部裁决经 grep arm-index notify/notification/notificationRecipient/UC-SYS/总开关/kill-switch/notify.enabled 同域同控制点比对后给出"维持"结论（P2-RC-081 维持 P2 + reuse P1-MA1-022 维持注记不重审），**0 新 finding**，无未经比对直接新建的 finding。
- [x] **真相源冻结声明（§9）**：本审计期间未修改任何真相源（`docs/design/notify/use-cases.md` / owner doc / `product-scope.md` / `<domain>/model/*.orm.xml` / `.api.xml` / 生产代码 / view.xml）；分歧记入报告不直改。
- [x] **跨域探针纪律声明**：A4.2.183 多域消费者 census 为**只读探针**（仅 grep 读取 cs/finance/sales/purchase/hr/assets wf listener 与 BizModel 的 context 键传递，不改他域任何代码），测试侧补充全部限 erp-notify-service 测试类目。

---

## 报告 9 段完整性自检

- [x] §0 与既有报告差异增量声明（§9 去重协议）✅
- [x] §1 存疑点清单与判据（A1.51 §7 四项 + 运行时判据三选一）✅
- [x] §2 运行时证据采集（四项各含 L3 file:line + 新增 L4 测试 + census）✅
- [x] §3 测试证据（新增 5 测试 + 既有 18 测试重跑 23/23 全绿 + L4 缺口闭合声明）✅
- [x] §4 compliance checker actual == baseline 零漂移 ✅
- [x] §5 与既有 finding 衔接（P2-RC-081 维持 P2 + 主路径闭合不登记 + reuse P1-MA1-022 维持注记）✅
- [x] §6 多维度裁决汇总（四项确认成立无一翻转）✅
- [x] §7 过程纪律自检（checker 门控 + closure-audit 独立性 + 交叉去重 + 冻结声明 + 跨域探针纪律）✅

## 触及保护区域汇总

本切片唯一代码变更为 erp-notify-service **测试类目**新增（`TestErpSysNotificationRecipientResolverRuntime`，5 @Test），零生产代码/ORM/api.xml/view.xml/真相源变更。测试对平台 nop-auth 实体（NopAuthRole/NopAuthUser/NopAuthUserRole）为**只读种子 + 只读查询断言**（对齐 `TestErpPurPaymentApprovalNotifications.seedRole:259-286` 既有先例），不触发任何保护区域。P2-RC-081 修复义务归 MR1 R1.0 展开器（纯 BizModel 代码逻辑预授权不触 ask-first，plan Non-Goals 声明维持）。
