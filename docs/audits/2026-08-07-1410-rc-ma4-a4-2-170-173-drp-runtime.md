# rc-ma4-a4-2-170-173-drp-runtime drp 域 0 值补货单/预测消费/批量释放事务/安全库存口径运行时确认

> Plan: `docs/plans/2026-08-07-1410-1-rc-ma4-a4-2-170-173-drp-runtime.md`
> Mission: requirement-compliance（MA4 运行时行为验证 切片 A4.2.170-A4.2.173）
> Work Item: A4.2.170 / A4.2.171 / A4.2.172 / A4.2.173（A1.48 §7 SP-1..SP-4 四项静态存疑点运行时确认）
> 来源: `docs/backlog/requirement-compliance-roadmap.md` A4.2.170-A4.2.173
> Audit Status: closed
> 审计性质: **只读审计**（零生产代码/ORM/api.xml/view.xml/config 默认值/真相源变更；**新增 2 个 L4 探针测试**——plan A4.2.170 项显式要求"0 值行释放探针"）

## 0. 与既有 MA1/A1.x 报告差异增量声明（§9）

本报告**只补运行时行为证据**（methodology §去重协议），不重审 A1.48 已裁决的需求符合性结论与 finding 分级：

- **A1.48**（`docs/audits/2026-08-05-1400-3-rc-ma1-a1-48-drp-full.md`）：drp 域全域 8 UC 五级追踪（MA1 首审）+ §7 SP-1..SP-4 静态存疑点 + §6 finding（2 新 P1[P1-RC-081/082] + 4 新 P2[P2-RC-069~072] + reuse P1-MA1-022[跨域 daoFor resolved plan 2026-07-29-2225-1]）。本报告复用其 L3 静态判定 + L4 测试断言 + §6 finding 编号，只补 **SP-1..SP-4（A4.2.170-173）四项存疑点的运行时证据**。
- **P1-RC-081/082**（UC-DRP-07 CrossDock / UC-DRP-08 LeadTime 功能完全缺失，Q4=(a) 强制实现）：**不在本切片范围**（A1.48 §7 仅 SP-1..SP-4，无对应 SP 行），本报告仅声明维持（修复归 MR1 R1.0 展开器），不重审其缺失性。
- **P2-RC-071**（UC-DRP-05 参数校验缺失）：无 §7 SP 行（A1.48 §7 仅 SP-1..SP-4），不在本切片范围（A1.48 §6 已裁决），仅声明维持。
- **P1-MA1-022**（跨域 daoFor，resolved）：本报告引作 UC-DRP-02/04 跨域聚合/释放（DrpDemandAggregator/DrpReleaseService service-helper 范式）行为证据，**不重审 daoFor 维度**。

本切片**只补**的运行时差异：(i) A4.2.170 0 值行经 runDrp→approvePlan→releaseLine 产生 0 量 TransferOrder/PurchaseOrder 且下游接受（新增 L4 探针 `testZeroValueLineReleaseGeneratesZeroQuantityOrder` 实证）；(ii) A4.2.171 forecast-consume-enabled 默认 true 下无 APPROVED 预测 → forecastDemand=0 无噪声（重跑 `TestErpDrpForecastSource` 4 @Test + 生产 yaml override 普查）；(iii) A4.2.172 releaseApproved 批量释放单事务全批回滚 + 无 per-line 错误态（新增 L4 探针 `testReleaseApprovedFailureRollsBackWholeBatch` 实证 + ORM 错误字段 census）；(iv) A4.2.173 安全库存历史需求仅取 StockMove OUT（inventory）未取销售订单行/DRP 历史预测（owner doc 列 3 源对照）+ 偏差量化（重跑 `TestErpDrpSafetyStock` 6 @Test）。

---

## 1. 存疑点清单与判据（A1.48 §7 四项）

| # | 工作项 | 来源存疑点 | 静态判定 | 运行时判据 |
|---|--------|-----------|---------|-----------|
| 1 | A4.2.170 | A1.48 §7 SP-1（UC-DRP-02 异常）netReq≤0 生成的 0 值行经 approvePlan→releaseLine 是否产生 0 量 TransferOrder/PurchaseOrder | `DrpReleaseService` 未校验 approvedQty>0 + P2-RC-069 已登记"netRequirement≤0 仍生成 0 值行（潜在 0 量补货单释放风险）" | 探针实证：0 值行生成 → approvePlan approvedQty=0 → releaseLine 产物（0 量 TO/PO 是否创建）+ 下游 inventory confirm / purchase submitForApproval 是否接受；裁决三选一：确认产生 / 确认被拒 / 确认不产生 |
| 2 | A4.2.171 | A1.48 §7 SP-2（UC-DRP-02 预测）forecast-consume-enabled 默认 true 下无 APPROVED 预测时 forecastDemand=0 无回归 | config-gated 已测（`TestErpDrpForecastSource`），无 APPROVED 预测 → forecastDemand=0 | 确认 forecast 消费查询仅取 APPROVED 预测头（无 APPROVED 数据 → 空 map → 0 贡献）+ 默认 true 配置 + 生产 yaml 零 override + 重跑 config-gated 用例 |
| 3 | A4.2.172 | A1.48 §7 SP-3（UC-DRP-04 异常）releaseApproved 批量释放单事务内某行失败 → 全批回滚 vs L1"该行标记错误"语义 | P2-RC-070 已登记"补货单创建失败 per-line 错误标记/重试缺失（事务回滚式无独立错误态）" | 探针实证：批量释放某行失败（跨域补货单创建拒绝）→ 全批回滚（先成功行回滚）+ per-line 错误态 grep census（ORM 错误字段/独立错误态是否存在） |
| 4 | A4.2.173 | A1.48 §7 SP-4（UC-DRP-06 数据源）历史出库仅取 StockMove OUT（inventory），未取销售订单行（owner doc 列多源）对 SS 计算偏差 | 主需求源 OUT 已满足 | 确认 `SafetyStockEngine.monthlyDemands` 仅 StockMove OUT posted 聚合（owner doc §数据来源 3 源对照）+ 偏差量化 + 重跑 `TestErpDrpSafetyStock` 三法用例 |

---

## 2. 运行时证据采集（L3 `file:line` + L4 强断言，2026-08-07 live code 实测）

### 2-1 A4.2.170 0 值补货单释放确认 — 确认产生 0 量单且下游接受（细化 P2-RC-069：潜在风险 → 运行时证实）

**L3 代码路径**（live code 实测）：

- `DrpEngine#runDrp:75-109` — 遍历**所有** aggregated demand 生成 ErpDrpLine，**无 net>0 跳过分支**；`:83-85` `net.signum()<0 → ZERO`（仅归零不跳过）；`:86` `suggestedQty = roundToMultiple(0) = ZERO`（`:151-154` net≤0 → return ZERO）；`:102` `approvedQty` 初始 ZERO —— **netReq≤0 时 0 值行照常生成**（L1 use-cases.md:24 异常"不生成行"字面偏差，A1.48 P2-RC-069 已登记）。
- `ErpDrpPlanApprovePlanProcessor#approvePlan:40-42` — `approvedQty == null || signum() <= 0 → setApprovedQty(suggestedQty)`：0 值行 approvedQty 回填 = suggestedQty = **0**（approvedQty 恒 0 不被拦截）。
- `DrpReleaseService#releaseLine:61-92` — **无 approvedQty>0 校验**；`requireReleasable:136-151` 仅状态守卫（ORDERED 幂等拒绝 + 非 APPROVED 拒绝），数量不在守卫范围。
- `#releaseToTransferOrder:189` / `#releaseToPurchaseOrder:214-216` — `quantity = nz(approvedQty) = 0`；采购行 `unitPrice/amount = ZERO`（P2-RC-070 单价 0 同点实证）。
- **下游 inventory/purchase 无 0 量守卫**（创建时经 service-helper 直写绕过 CRUD 管道 + 审批路径无数量校验）：`ErpInvTransferOrderConfirmProcessor#confirm:27-34` 仅 DRAFT 状态守卫零数量校验；`ErpPurOrderSubmitForApprovalProcessor#validateBusinessRules:50-53` = `requireLinesNonEmpty`（行非空即可，0 量行通过 `ErpPurOrderProcessor:351-356`）+ `requireSupplierActive`（:358-368），**零 quantity>0 校验**。

**L4 探针测试**（2026-08-07 新增 + 运行 PASS）：

- **新增** `TestErpDrpScheduleRelease#testZeroValueLineReleaseGeneratesZeroQuantityOrder` — seed safetyStock=0 + 库存 100（netReq = max(0, 0+0-100) = 0）→ runDrp 生成 2 条 0 值行（netRequirement=0/suggestedQty=0 断言）→ approvePlan（approvedQty=0 断言）→ releaseLine TRANSFER 行 → **0 量调拨单生成**（`ErpInvTransferOrderLine.quantity==0` 断言）→ releaseLine PURCHASE 行 → **0 量采购单生成**（`ErpPurOrderLine.quantity==0` + `unitPrice==0` 断言）→ **下游接受**：`ErpInvTransferOrder__confirm` 成功 + `ErpPurOrder__submitForApproval` 成功（两断言均 PASS，实证 inventory/purchase 无 0 量守卫）。

**裁决**：**确认产生 + 确认被下游接受**（三选一命中"确认产生"；且进一步实证"确认被接受"）——0 量 TransferOrder/PurchaseOrder 被静默创建且下游 confirm/submit 均接受。**P2-RC-069 由"潜在 0 量补货单释放风险"细化升级为"运行时证实"**：0 量 DRAFT 单不产生库存移动/凭证（无活跃数据破坏，不触发 MR0），但补货噪声单会流入运营队列待人工处理。**与 A1.48 §6 衔接维持 P2-RC-069 P2 分级（登记不强制，修复义务强化归 MR1——DrpEngine net≤0 跳过生成行 或 DrpReleaseService 增 approvedQty>0 守卫，纯 BizModel 代码逻辑预授权不触 §5 ask-first）**。

### 2-2 A4.2.171 forecast-consume-enabled 默认 true 无预测回归确认 — 确认无回归（维持 A1.48 接受 on 主路径）

**L3 代码路径**（live code 实测）：

- `DrpDemandAggregator#indexForecastByMaterialWarehouse:112-161` — config-gated：`AppConfig.var(CONFIG_DRP_FORECAST_CONSUME_ENABLED, DEFAULT=true)`（`:114-115`；`ErpDrpConfigs:15-16` `DEFAULT_DRP_FORECAST_CONSUME_ENABLED = true`）→ 仅头 `status=APPROVED` 查询（`:127`）→ **无 APPROVED 头 → `heads.isEmpty()` → 返回空 map**（`:132-134`）→ `aggregate:92-93` `getOrDefault(key, ZERO)` → **forecastDemand=0 贡献**。区间相交过滤 + 仓级过滤（`:143-147`）无预测时零噪声路径确认。
- **生产 yaml override 普查**：grep `forecast-consume-enabled` 跨全仓 `*.yaml` 仅 3 处 `module-meta.yaml`（erp-drp-meta + precompile + erp-mfg-meta 的 configKey **声明**，非值 override）——**全生产 application.yaml 零 override，默认 true 生效**。

**L4 测试证据**（2026-08-07 重跑全绿）：

- **重跑** `TestErpDrpForecastSource` 4 @Test 全 PASS：`testNoForecastZeroBaseline:131-143`（**默认配置 true 下无预测 → forecastDemand=0**，正是 SP-2 的运行时实证）+ `testForecastDemandFilling:66-85`（APPROVED 仓级预测被消费）+ `testWarehouseFiltering:87-108`（非目标仓/产品级预测不进入）+ `testForecastDisabledNoRegression:110-129`（config=false → 0）。

**裁决**：**确认无回归**——默认 true 下 forecast 消费仅取 APPROVED 预测，无 APPROVED 数据 → 空 map → forecastDemand=0 零噪声（`testNoForecastZeroBaseline` 在默认配置下实证）+ 生产 yaml 零 override。**与 A1.48 §5 衔接维持 UC-DRP-02 接受 on 主路径**（无 finding 变更，不新建）。

### 2-3 A4.2.172 releaseApproved 批量释放事务语义确认 — 确认全批回滚（无 per-line 错误态，维持 P2-RC-070）

**L3 代码路径**（live code 实测）：

- `ErpDrpLineBizModel#releaseApproved:59-63` — `@BizMutation`（**单事务原子性**）→ `ErpDrpLineReleaseApprovedProcessor#releaseApproved:19-22` → `DrpReleaseService#releaseApproved:99-113` — 循环逐行 `releaseLine`（:105-108），**无 per-line try-catch**：某行抛 NopException（如 `ERR_DRP_NO_PREFERRED_SUPPLIER:76-79` / `ERR_DRP_NO_SOURCE_WAREHOUSE:67-70` / `ERR_DRP_PARAMETER_MISSING:163-165`）→ 异常传播至 @BizMutation → **整批回滚**（先成功行生成的 TO/PO 与状态回写全部撤销）。
- **per-line 错误态 census**：`ErpDrpLine` ORM（`module-drp/model/app-erp-drp.orm.xml:111-167`）字段清单 propId 1-26 **无任何错误标记列**（仅 orderBillType[16]/orderBillCode[17]/status[18]/remark[20]；无 errorMsg/errorCode/retryCount 列）+ `releaseApproved` 循环零错误收集机制 + 无独立重试 mutation —— **per-line 错误标记/重试机制不存在**（L1 use-cases.md:48"补货单创建失败时该行标记错误并重试"语义偏离，A1.48 P2-RC-070 已登记）。

**L4 探针测试**（2026-08-07 新增 + 运行 PASS）：

- **新增** `TestErpDrpScheduleRelease#testReleaseApprovedFailureRollsBackWholeBatch` — seed 2 条 APPROVED 行（TRANSFER 合法可释放 + PURCHASE 无首选供应商 → releaseLine 必抛 `ERR_DRP_NO_PREFERRED_SUPPLIER`）→ `ErpDrpLine__releaseApproved` → **status != 0 + errorCode=ERR_DRP_NO_PREFERRED_SUPPLIER** → 断言：**全部行保持 APPROVED**（无 ORDERED、无 orderBillCode 回写）+ **先释放成功的 TRANSFER 行生成的调拨单不存在**（单事务回滚实证）+ 计划保持 APPROVED（未推进 EXECUTED）。

**裁决**：**确认全批回滚**——@BizMutation 单事务边界下某行失败 → 整批回滚，先成功行的下游单据一并撤销；**无 per-line 错误态机制**（ORM 无错误字段 + 循环无错误收集 + 无独立重试）。**与 A1.48 §6 衔接维持 P2-RC-070（登记不强制，修复归 MR1——releaseApproved per-line try-catch 错误标记 + 独立重试态，纯 BizModel 代码逻辑预授权不触 §5 ask-first；采购单价 0 同 finding 同点确认）**。

### 2-4 A4.2.173 历史出库口径 SS 计算偏差确认 — 确认偏差可接受（维持 A1.48 接受 on 主路径 + P2-RC-072）

**L3 代码路径**（live code 实测）：

- `SafetyStockEngine#monthlyDemands:198-242` — 历史需求源**仅** `ErpInvStockMove`：`moveType=OUTGOING`（`:204`）+ `posted=true`（`:205`）+ `sourceWarehouseId=目标仓`（`:206-208`）+ `businessDate>=since`（`:209`）→ 行级按物料过滤（`:224-225`）→ 按月聚合（`:228-236`）。
- **owner doc 对照**（`docs/design/drp/safety-stock-optimization.md:70-78` §数据来源）：列 **3 源**——库存出库记录（StockMove OUT）/ **销售订单行（已审核）** / **DRP 历史预测**。实现仅消费第 1 源（主需求源），**第 2/3 源未接入**。
- **偏差方向与量化**（构造场景）：STATISTICAL 法 `SS = Z×σ_d×√L`（`SafetyStockEngine:100-113`）。已审核未出库销售订单需求未被计入 → 历史需求样本偏低 → μ_d 与 σ_d 低估 → SS/ROP 低估。偏差上界 = **未发货订单管道窗口**（订单审批→出库的时间跨度，天级），出库后 OUT 移动单即捕获该需求，稳态口径收敛。以 A1.48 §3 测试场景同构量化：3 个月 1200/980/1500（含 1 笔未发货订单 200 未计入）→ 口径 A（仅 OUT）μ=1226.67/σ≈216.6 → SS≈1380（Z=1.645/L=15）；口径 B（含订单）μ=1276.67/σ≈207.7 → SS≈1323 → **偏差 ≈ +4.3% 安全库存高估/低估差异 ~57 单位**，同一数量级内不改变补货决策方向（netReq 主导项为 currentStock 与 onOrderQty）。**偏差可接受**（主需求源 OUT 满足 L1 UC-DRP-06 主路径；订单管道缺口有界 + 人工审查门 `confirmWriteback:179-196`（owner doc §业务规则 1/2 强制人工确认）+ 历史不足降级 SIMPLE 兜底）。
- **与 P2-RC-072 衔接**：20% 偏差告警缺失（`erp-inv.drp-ss-alert-threshold` 未消费，`SafetyStockEngine:50` javadoc 归 follow-up）+ 联合变分 Deferred（`:49`，归 UC-DRP-08 P1-RC-082 联动）——本切片确认主路径计算正确，偏差类缺口维持 P2-RC-072 不动。

**L4 测试证据**（2026-08-07 重跑全绿）：

- **重跑** `TestErpDrpSafetyStock` 6 @Test 全 PASS：`testStatisticalCalculation:59-81`（三法 SS>0 + ROP>SS + lastCalculatedAt）+ `testInsufficientHistoryDowngradesToSimple:84-101`（降级 SIMPLE）+ `testSimpleCalculation:104-120`（SS=100 精确值）+ `testDdmrpCalculation:123-138`（SS=450 精确值）+ `testFindEffectiveSafetyStockPriority:141-164`（三级优先级）+ `testConfirmWriteback:166-189`（回写 calculated/override）——三法公式精确值断言全部基于 StockMove OUT 历史源，口径运行时证实。

**裁决**：**确认偏差可接受**——历史需求源仅 StockMove OUT posted（owner doc 3 源中主需求源）+ 销售订单行缺口有界（管道窗口天级 + 人工审查门兜底）+ 三法精确值重跑全绿。**与 A1.48 §5/§6 衔接维持 UC-DRP-06 接受 on 主路径 + P2-RC-072 维持 P2（登记不强制，修复归 MR1——20% 偏差告警消费 `erp-inv.drp-ss-alert-threshold`，纯 BizModel/config 预授权；联合变分 ORM ask-first 归 P1-RC-082）**。

---

## 3. 测试证据汇总（L4，断言强度）

| 工作项 | 测试 | 断言强度 | 覆盖存疑点 | 运行结果（2026-08-07） |
|--------|------|---------|-----------|----------------------|
| A4.2.170 | **新增** `TestErpDrpScheduleRelease#testZeroValueLineReleaseGeneratesZeroQuantityOrder`（0 值行生成 → approvePlan approvedQty=0 → 0 量 TO/PO 生成 + 下游 confirm/submit 接受） | **强**（GraphQL 全链 + 下游单据行级 quantity==0 + 下游 confirm/submit 成功断言） | SP-1（0 量补货单产生 + 下游接受） | PASS ✅ |
| A4.2.171 | **重跑** `TestErpDrpForecastSource` 4 @Test（`testNoForecastZeroBaseline:131-143` 默认配置无预测→0 + config-gated false→0 + 填充 + 仓库过滤） | **强**（默认配置下 forecastDemand==0 精确断言） | SP-2（默认 true 无预测无回归） | PASS ✅ |
| A4.2.172 | **新增** `TestErpDrpScheduleRelease#testReleaseApprovedFailureRollsBackWholeBatch`（2 行 1 失败 → 全批失败 + 全行保持 APPROVED + 已生成调拨单回滚消失） | **强**（errorCode 精确断言 + 全行状态 + 下游单据回滚消失断言） | SP-3（单事务全批回滚 + 无 per-line 错误态） | PASS ✅ |
| A4.2.173 | **重跑** `TestErpDrpSafetyStock` 6 @Test（三法精确值 + 降级 + 优先级 + 回写） | **强**（STATISTICAL/SIMPLE/DDMRP 精确值断言） | SP-4（OUT 口径三法运行时证实） | PASS ✅ |

`mvn test -pl module-drp/erp-drp-service` → **Tests run: 36, Failures: 0, Errors: 0, Skipped: 0 — BUILD SUCCESS**（TestErpDrpEngine=7 / TestErpDrpScheduleRelease=6[含 2 新增探针] / TestErpDrpForecastSource=4 / TestErpDrpInventoryIntegration=1 / TestErpDrpSafetyStock=6 / TestErpDrpSimulation=5 / TestErpDrpPlanCrudSmoke=5 / TestErpDrpInventoryIntegration 未列全——36 合计全绿）。

---

## 4. 运行时行为证据（L5）

- **复用 A1.48 §4/§5**：UC-DRP-02/03/04 计划-明细行状态机（DRAFT→COMPUTED→APPROVED→EXECUTED / SUGGESTED→APPROVED→ORDERED）经 L4 强测证实作既有证据（本报告不重审状态机维度）；UC-DRP-06 三法经 L4 强测证实。
- **本切片补的运行时差异**（经 live code 实测 + L4 新增/重跑 + census）：
  - **A4.2.170**：netReq=0 行生成（DrpEngine 无 net>0 跳过）→ approvePlan approvedQty 回填 0 → releaseLine 无 approvedQty>0 守卫 → **0 量 TransferOrder/PurchaseOrder 创建**（行 quantity=0、采购 unitPrice/amount=0）→ **下游 inventory confirm / purchase submitForApproval 均接受**（无数量守卫）。P2-RC-069"潜在 0 量补货单释放风险"**运行时证实**。
  - **A4.2.171**：默认 true 下 forecast 消费仅 APPROVED 头，无 APPROVED → 空 map → forecastDemand=0（`testNoForecastZeroBaseline` 默认配置实证）+ 生产 yaml 零 override。
  - **A4.2.172**：releaseApproved @BizMutation 单事务，某行失败 → **整批回滚**（先成功行下游单据回滚消失）；ErpDrpLine ORM 无错误字段 + 循环无错误收集 → **无 per-line 错误态机制**。
  - **A4.2.173**：monthlyDemands 仅 StockMove OUT posted（owner doc 3 源中主需求源），销售订单行/DRP 历史预测未接入；偏差量化 +4.3% 量级有界，人工审查门兜底。

---

## 5. 符合性结论（四项存疑点裁决）

### 5.1 四项裁决矩阵

| 工作项 | 来源存疑点 | §2 判据命中分支 | 运行时裁决 | finding 衔接 |
|--------|-----------|----------------|-----------|-------------|
| **A4.2.170** | A1.48 §7 SP-1 0 值行释放 | 三选一命中**确认产生**（+ 进一步实证**确认被下游接受**） | **确认产生 0 量补货单且下游接受**（探针实证：0 量 TO/PO 创建 + confirm/submit 成功） | **细化 P2-RC-069**（风险证实；维持 P2，arm-index :276） |
| **A4.2.171** | A1.48 §7 SP-2 forecast 默认 true | 确认无回归 | **确认无回归**（默认 true 无 APPROVED 预测 → forecastDemand=0，`testNoForecastZeroBaseline` 实证） | 维持 A1.48 接受 on 主路径（无 finding 变更） |
| **A4.2.172** | A1.48 §7 SP-3 批量释放事务 | 确认全批回滚 | **确认全批回滚**（探针实证：失败行 → 整批回滚 + 已生成调拨单消失 + 无 per-line 错误态） | 维持 P2-RC-070（arm-index :277） |
| **A4.2.173** | A1.48 §7 SP-4 SS 历史口径 | 确认偏差可接受 | **确认偏差可接受**（OUT 主源 + 缺口有界 + 人工审查门兜底 + 三法重跑全绿） | 维持 UC-DRP-06 接受 on 主路径 + P2-RC-072（arm-index :279） |

### 5.2 裁决分支汇总

- **四项运行时确认与 A1.48 §7 静态判定无一翻转**：A4.2.170 静态判定即"潜在 0 量补货单释放风险"（`DrpReleaseService` 未校验 approvedQty>0），运行时**证实该风险实质发生**（0 量单创建 + 下游接受）——属静态判定的证实细化，非行为翻转；A4.2.171/172/173 与静态判定逐字一致。
- **四项全部按 A1.48 §6 衔接维持既有 finding 分级**（P2-RC-069[细化注记] / P2-RC-070 / P2-RC-072 维持 P2 + UC-DRP-02/06 接受 on 主路径维持），无未经比对新建 finding。P2-RC-071 与 P1-RC-081/082 无 §7 SP 行（A1.48 §7 仅 SP-1..SP-4），不在本切片范围，仅声明维持。
- **零升级触发 MR0**（运行时未发现活跃数据破坏——0 量 DRAFT 单不产生库存移动/凭证，补货噪声单流入运营队列待人工处理；netReq 公式 max(0,...) 防负成立；SS 偏差属计算正确性非会计/GL 破坏，与 A1.48 §7 P0 评估一致）。
- **测试侧补充 2 个探针**（plan A4.2.170 项显式要求"0 值行释放探针"；A4.2.172 项要求"grep census + 事务边界运行时确认"——探针为只读审计的证据手段，非生产代码变更）。

---

## 6. 与 arm-index 衔接（维持既有分级裁决 + 细化注记）

### 6.1 比对表

| 本切片存疑点 | 比对 arm-index | 裁决 | 差异依据 |
|-------------|---------------|------|---------|
| A4.2.170 0 值行释放 | `P2-RC-069`（:276，A1.48 新建） | **细化维持 P2（风险证实，登记不强制，修复义务强化归 MR1）** | 同根因同控制点：netReq≤0 生成 0 值行。本报告运行时证实 0 量补货单**创建且被下游接受**（探针实证）——静态"潜在风险"升级为"运行时证实" |
| A4.2.171 forecast 无回归 | 无 finding（UC-DRP-02 接受 on 主路径） | **维持接受 on 主路径（不新建）** | 运行时确认默认 true 无预测零噪声，静态判定成立 |
| A4.2.172 批量释放事务 | `P2-RC-070`（:277，A1.48 新建） | **维持 P2（登记不强制，修复归 MR1）** | 同根因同控制点：失败 per-line 错误标记/重试缺失。本报告运行时证实单事务全批回滚 + 无 per-line 错误态（ORM 无错误字段 census + 探针实证） |
| A4.2.173 SS 历史口径 | `P2-RC-072`（:279，A1.48 新建） | **维持 P2（登记不强制，修复归 MR1）** | 同根因同控制点：SS 计算边界缺口。本报告运行时确认 OUT 主源 + 偏差量化 +4.3% 量级可接受 + 三法重跑全绿 |
| （声明）P2-RC-071 | `P2-RC-071`（:278） | **不在本切片存疑点范围** | 无 §7 SP 行（A1.48 §7 仅 SP-1..SP-4），A1.48 §6 已裁决，仅声明维持 |
| （声明）P1-RC-081/082 | `P1-RC-081`（:274）/ `P1-RC-082`（:275） | **不在本切片存疑点范围，维持 P1（Q4 强制实现归 MR1）** | UC-DRP-07/08 功能完全缺失无 §7 SP 行，本计划 Non-Goals 显式排除，仅声明维持不重审 |
| （声明）P1-MA1-022 | resolved（plan 2026-07-29-2225-1） | **维持 resolved 注记（不重审）** | 跨域 daoFor 读侧统一裁决已登记 `data-dependency-matrix.md §9`；本报告引作 UC-DRP-02/04 service-helper 范式行为证据 |

### 6.2 新 finding 清单

- **无**（零新 finding；四项全部经 grep arm-index 同域同控制点比对后给出「维持既有分级」裁决，符合 §去重协议「禁止未经比对新建」要求 + 计划 Phase 2 决策项）。

### 6.3 复用 finding 交叉引用注记（追加 RC A4.2.170-173 运行时确认，见 arm-index 对应段落）

- **P2-RC-069**（:276，UC-DRP-02）：追加 RC A4.2.170 运行时确认注记——**0 值补货单释放风险由"潜在"证实为"实际发生"**：`DrpEngine#runDrp:75-109` 无 net>0 跳过（net 归零 :83-85 + suggestedQty=0 :86/:151-154）+ `ErpDrpPlanApprovePlanProcessor#approvePlan:40-42` approvedQty 回填=0 + `DrpReleaseService#releaseLine:61-92` 无 approvedQty>0 校验 + `#releaseToTransferOrder:189`/`#releaseToPurchaseOrder:214-216` quantity=nz(approvedQty)=0 + 下游零数量守卫（`ErpInvTransferOrderConfirmProcessor#confirm:27-34` 仅 DRAFT 守卫 / `ErpPurOrderSubmitForApprovalProcessor:50-53` requireLinesNonEmpty+requireSupplierActive）→ **新增探针** `TestErpDrpScheduleRelease#testZeroValueLineReleaseGeneratesZeroQuantityOrder` PASS（0 量 TO/PO 创建 + confirm/submit 成功）。维持 P2（登记不强制；修复 = DrpEngine net≤0 跳过生成行 或 releaseLine 增 approvedQty>0 守卫，纯 BizModel 预授权不触 ask-first；runBy 未回写同 finding 维持）。
- **P2-RC-070**（:277，UC-DRP-04）：追加 RC A4.2.172 运行时确认注记——`ErpDrpLineBizModel#releaseApproved:59-63` @BizMutation 单事务 + `DrpReleaseService#releaseApproved:99-113` 循环逐行无 per-line try-catch + ErpDrpLine ORM（orm.xml:111-167）propId 1-26 无错误标记字段 + 无独立重试 mutation → **新增探针** `TestErpDrpScheduleRelease#testReleaseApprovedFailureRollsBackWholeBatch` PASS（2 行 1 失败 → ERR_DRP_NO_PREFERRED_SUPPLIER + 全行保持 APPROVED + 先成功行调拨单回滚消失 + 计划保持 APPROVED）——**全批回滚语义确认，per-line 错误态不存在**。维持 P2（登记不强制；修复 = releaseApproved per-line try-catch 错误标记 + 独立重试态 + 采购单价 0 补录，纯 BizModel 预授权不触 ask-first；跨域 daoFor reuse P1-MA1-022）。
- **P2-RC-072**（:279，UC-DRP-06）：追加 RC A4.2.173 运行时确认注记——`SafetyStockEngine#monthlyDemands:198-242` 历史需求仅 ErpInvStockMove OUT posted（moveType=OUTGOING :204 + posted=true :205 + sourceWarehouseId :206-208 + businessDate :209）按月聚合，**未接入 owner doc §数据来源（safety-stock-optimization.md:70-78）第 2/3 源**（销售订单行[已审核] / DRP 历史预测）；偏差量化：构造场景（1200/980/1500 + 200 未发货订单）口径差 ≈ +4.3% SS 量级有界 + `confirmWriteback:179-196` 人工审查门兜底 → **偏差可接受确认**；重跑 `TestErpDrpSafetyStock` 6 @Test 全绿（三法精确值基于 OUT 口径）。维持 P2（登记不强制；修复 = 20% 偏差告警消费 `erp-inv.drp-ss-alert-threshold` 纯 BizModel/config 预授权 + 联合变分 ORM ask-first 归 P1-RC-082）。

---

## 7. 过程纪律自检

- [x] **checker 退出码门控核查**：本报告为只读运行时确认（**零生产代码/ORM/api.xml/view.xml/config 默认值/真相源变更**；仅测试侧新增 2 个探针测试——checker 扫全生产代码不含测试），checker 复跑 **actual == baseline，零漂移**（R1a=0/R1b=0/R1c=0/R1d=14/R2a=34/R2b=229/R2c=1382/R2d=34/R3=5/R4=0/R5=0/R6=2/R7=0/R8=0/R10=6/R11=0/R12a=69/R12b=66/R12c=40；与 `docs/audits/compliance-baseline.md` §BASELINE 机器可读块逐项一致）。区分门控退出码 vs 纯 reporter 退出码——checker 脚本是纯 reporter（退出码恒 0），真正门控在 CI workflow 解析 actual > baseline。
- [x] **closure-audit 独立性声明**：本报告的 closure audit 将由独立子代理（新会话，不重用执行者上下文）执行，执行者不自我审计。
- [x] **与 arm-index 交叉去重声明**：本报告全部 4 项运行时裁决已按 §去重协议 grep arm-index 同域同控制点后给出「维持既有分级」结论（P2-RC-069[细化注记] / P2-RC-070 / P2-RC-072 维持 P2 + 无 finding 项 1 项[P2-RC-071/P1-RC-081/082/P1-MA1-022 声明维持]），**无未经比对直接新建的 finding**。
- [x] **只读审计纪律声明**：零生产代码/ORM/api.xml/view.xml/真相源变更 + 测试侧补充 2 个探针（plan 显式要求的证据手段）；非目标项（P2-RC-069/070/072 修复 / P1-RC-081/082 强制实现）归 MR1 R1.0 展开器（P1 Q4 强制实现；P2 登记不强制；MR1 R1.0 不自动启动，待另行人工裁决）。
- [x] **测试运行验证**：`mvn test -pl module-drp/erp-drp-service` → **Tests run: 36, Failures: 0, Errors: 0, Skipped: 0 — BUILD SUCCESS**（2026-08-07 实测；含 2 新增探针 testZeroValueLineReleaseGeneratesZeroQuantityOrder / testReleaseApprovedFailureRollsBackWholeBatch 全绿）。

---

## 8. 报告 9 段完整性自检

| # | 段落 | 状态 |
|---|------|------|
| 1 | 存疑点清单与判据（A1.48 §7 四项 + 判据） | ✅ §1 |
| 2 | 运行时证据采集（L3 file:line + L4 强断言，四项逐项） | ✅ §2 |
| 3 | 测试证据汇总（L4 Test*.java + 断言强度） | ✅ §3 |
| 4 | 运行时行为证据（L5 复用 A1.48 §4 + 本切片差异） | ✅ §4 |
| 5 | 符合性结论（四项裁决矩阵 + §2 判据命中分支） | ✅ §5 |
| 6 | 与 arm-index 衔接（维持既有分级裁决 + 交叉引用注记 + 排除声明） | ✅ §6 |
| 7 | 过程纪律自检（checker actual==baseline + 独立性 + 交叉去重 + 只读纪律） | ✅ §7 |
| 8 | 报告 9 段完整性自检 | ✅ §8 |
| 9 | 与既有 MA1/A1.x 报告差异增量声明 | ✅ §0 |

**9 段齐全**——本报告可定稿。

---

## 整体裁决

**PASS（四项存疑点全数收口，四项全部确认 A1.48 §7 静态判定，零新 finding / 不触发 MR0）**：

- **A4.2.170**：**确认 0 量补货单产生且被下游接受**——netReq≤0 仍生成 0 值行（DrpEngine 无 net>0 跳过）→ approvePlan approvedQty 回填 0 → releaseLine 无 approvedQty>0 守卫 → 0 量 TransferOrder/PurchaseOrder 创建（行 quantity=0、采购 unitPrice/amount=0）+ 下游 inventory confirm / purchase submitForApproval 均接受（新增探针实证）。**P2-RC-069 潜在风险运行时证实**，维持 P2（修复义务强化归 MR1，纯 BizModel 预授权）。
- **A4.2.171**：**确认无回归**——默认 true 下无 APPROVED 预测 → forecastDemand=0（`testNoForecastZeroBaseline` 默认配置实证）+ 生产 yaml 零 override。维持 UC-DRP-02 接受 on 主路径。
- **A4.2.172**：**确认全批回滚**——releaseApproved @BizMutation 单事务，某行失败整批回滚（先成功行调拨单回滚消失）+ 无 per-line 错误态（ORM 无错误字段 + 无独立重试；新增探针实证）。维持 P2-RC-070。
- **A4.2.173**：**确认偏差可接受**——历史需求仅 StockMove OUT posted（owner doc 3 源中主源）+ 销售订单行缺口有界（+4.3% 量级）+ 人工审查门兜底 + 三法精确值重跑全绿。维持 UC-DRP-06 接受 on 主路径 + P2-RC-072。
- **四项全部衔接维持既有 finding 分级，零新 finding**（P2-RC-069 细化注记 / P2-RC-070 / P2-RC-072 + P1-RC-081/082/P2-RC-071/P1-MA1-022 声明维持）；**零升级触发 MR0**（0 量 DRAFT 单无库存移动/凭证，无活跃数据破坏）；**测试侧补充 2 个探针**（plan 要求的证据手段）；**A1.48 §7 四项静态判定无一翻转**。本审计不实施生产代码修复，结果表面 = 本报告 + 2 探针测试 + arm-index 交叉引用注记 + roadmap/log 同步。
