# ck-hr-attendance-payroll-r3 — human-resource「考勤、薪酬与排班」hr-2 五维符合性审计报告（ai-check-r3 M1.11）

> 工作项：M1.11（U14 × 五维 × hr-2，冻结清单 §4 映射表第 11 行 / §3.3 U14 行）。
> 执行日期：2026-09-09。审计时点 T0：HEAD `2c1c1ef254599253f02ba7d231d96c6f08b0cc50`（与计划基线行一致）；脏面 = 3 条 untracked（本计划 + 同批姊妹计划 `2026-09-09-0547-2`/`2026-09-09-0547-3`，均为 mission-driver 起草的未执行计划，零生产路径脏面——MI.9 收官审计脏树实跑先例，Phase 1 时点披露在案，以下全部证据引用该时点）。
> 判定依据（冻结）：`docs/audits/check/2026-09-06-1645-ai-check-r3/m0-5-audit-checklists.md` §1/§2（判定标准/机械核查程式/跨轮查重）+ §3.2 共享代码唯一归属 + §3.3 U14 行 + §4 第 11 行 + §6 勘误 E1。执行期仅在本报告与计划勾选注记追加证据，判定标准零改动。
> 范围：考勤 `ErpHrAttendance*` / 工时 `ErpHrTimesheet*`(+Line) / 请假 `ErpHrLeaveRequest*`/`ErpHrLeaveBalance*` / 薪酬 `ErpHrSalary*`（Salary/SalaryItem/SalarySimulation/SalarySimulationItemAdjustment）+ 薪资引擎 `payroll/`（PayrollCalculator/IncomeTaxCalculator/SocialInsuranceCalculator/TaxBracket/TaxBracketParser）/ 工资单 `ErpHrPayrollBankFile*` + `ErpHrSalaryGenerateBankFileProcessor`/`ErpHrSalarySimulationConvertToFormalProcessor` / 税与社保配置 `ErpHrTaxConfig*`/`ErpHrTaxSpecialDeduction*`/`ErpHrSocialInsurance*` / 薪酬过账 `posting/`（SalaryPostingDispatcher/SalaryPostingExecutor/SalaryPostingProvider）/ 排班 `ErpHrShift*`（Shift/ShiftAssignment/ShiftRotationPattern/ShiftSwapRequest）+ `scheduling/ShiftAttendanceCalculator` + `ErpHrLeaveApproverTimeoutJob` + 状态机 Bean 4（LeaveRequest/SalaryApproval/SalaryPayment/Timesheet）+ U14 余下面承接族（competency/development plan/assessment/survey/gap 13 BizModel + 8 processor/calculator），物理面 `module-hr/erp-hr-{dao,service}` 的 `src/main`；owner docs `payroll.md`（593 行）+ `shift-scheduling.md`（360 行）+ `payroll-simulation.md`（261 行）+ `state-machine.md`（307 行，考勤/薪酬语义）+ `README.md`（127 行）。
> 切片边界：hr-1 面（组织/岗位/员工/编制 + 招聘入职衔接消费点）归 M1.10 已收官未触碰；招聘 BizModel 本体范围外项 P3-CK-hr-009/017 维持登记态复核；common 抽象族（`AbstractErpCrudBizModel` 状态锁基类/MaskHelper）归 U20（M1.15）本切片只审调用点；posting 引擎内部归 fin-1（M1.1 已收官），Dispatcher/Executor/Provider 为消费侧；聚合横切面归 U21（M1.16）；notify 派发子系统本体归 U11。
> Skills：`nop-platform-conformance-audit-prompt` + `code-quality-audit-prompt`（DIM-B，roadmap M1.11 行「同上」解析承 M1.10 行）；`code-quality-audit-prompt`（维度⑮断言抽样 + 三态裁决 + Phase 7 汇总）；其余维 Skill: none。
> 零生产代码改动（roadmap 规则 6）：本报告 + 双索引 + 计划勾选注记为唯一产物（收官 `git status` 机械核证见 §4）。

## 1. 五维覆盖矩阵（5/5 完整）

| 维度 | 判定面（hr-2 增量） | 机械核查结果 | verdict |
| --- | --- | --- | --- |
| **DIM-B 后端平台合规** | §1.1 全套机械程式 + 15 维度走查（hr-2 专属焦点：①Model→Delta→Java ②跨实体 I*Biz——薪酬过账消费侧 + shift↔attendance 联动 ⑧状态机 ⑨审批流/作业 ⑮断言抽样） | 反模式族 hr-2 范围全零（`extends RuntimeException`=0 / `@Inject private`=0 / `System.currentTimeMillis`=0 / `@Transactional` 真实共存=0——唯一命中 SalaryPostingExecutor L14-15 为 javadoc「本类不带 @Transactional」说明引注，R6=2 基线条目均为他域站点）；checker 19 规则逐项 = M0.3 快照行零漂移（R1a-c=0/R1d=14/R2a=34/R2b=242/R2c=1542/R2d=38/R3=5/R4=0/R5=0/R6=2/R7=0/R8=0/R10=14/R11=0/R12a=71/R12b=66/R12c=42）；`IDaoProvider|IOrmTemplate` 命中：processor 族（processor-extension-pattern 惯例，r1「验证为正确」在案）+ payroll 引擎/posting 消费侧 3 文件 10 站点无豁免注释（PayrollCalculator 4 同域 / IncomeTaxCalculator 3 同域 / SalaryPostingDispatcher 3 跨域）→ 新立 P3-CK-hr2-029-r3；codegen：`__XGEN_FORCE_OVERRIDE__` 38 文件（src）全为 `erp-hr-meta/_vfs/dict/erp-hr/*.dict.yaml` 生成产物 + `_gen`/`_` 前缀脏面=空（零手改）；聚合完整性（E1 勘误路径）`app.action-auth.xml` x:extends 含 `/erp/hr/auth/erp-hr.action-auth.xml`（L17）。15/15 无跳维：①业务编排 Java + 4 状态机 Bean 均有 owner doc/entity-state-machine-bean.md 契约背书，退化轴显式登记；②跨实体访问 BizModel 全经 `@Inject I*Biz`（LeaveRequest→IErpHrLeaveBalanceBiz+IErpHrShiftBiz、Swap→IErpHrShiftAssignmentBiz、Attendance clockOut→IErpHrShiftAssignmentBiz+IErpHrShiftBiz、Salary→处理器族），薪酬过账消费侧 Executor 注入 `IErpFinVoucherBiz`（跨域写经 I*Biz ✓，REQUIRES_NEW 由 Facade 承接——引擎内部归 fin-1），shift↔attendance 联动（calcAttendance/onLeaveApproved/onLeaveCancelled/clockOut 跨天回退）全 I*Biz 管道；⑧4 Bean（LeaveRequest 4 边单源 cancel/SalaryApproval 6 边含 REJECTED 重提+reverseApprove/SalaryPayment 2 边双终态/Timesheet 4 边含 REJECTED 重提）矩阵与 dict 实仓逐边一致、直抛领域码 + 同码补参范式统一；⑨薪酬审批流 DIRECT+WORKFLOW 双模（ErpHrSalary.xbiz submitForApproval wf 适配 Long→String 平台已知限制 + approve 单一委托点 postAccruals 覆盖直批与 wf 回调）+ job 双层门控（`erp-hr-leave-approver-timeout.job.yaml` cronExpr 外层 + `erp-hr.leave-approver-timeout-cron` 内层空值跳过）+ 单条失败隔离；⑮6 doc × 16 断言点——**2 漂移聚簇 state-machine.md §适用对象五**（Survey「CrudBizModel 桩零状态机 mutation」vs 实仓 publish/close/archive 全状态机 + published-immutable 守卫；DevelopmentPlan「OVERDUE 预留死状态」vs updatePlanItemStatus L190 IN_PROGRESS→OVERDUE writer 已落地）→ 扩样全部 hr-2 owner doc 后新立 P3-CK-hr2-027-r3；③④⑤⑥⑦⑩⑪⑬逐维 pass（NopException+erp.err.hr.* 集中、@Inject 非私有、CoreMetrics/StringHelper/JsonTool、CrudBizModel+@BizQuery/@BizMutation+@BizLoader、无跨模块 orm 实体引用（drp 式 notGenCode 不涉）、PII 薪酬 13 金额字段 + cumulativeData MaskHelper fail-closed 脱敏）；FNPT 注册面 hr-2 ≈42 自定义 mutation 零注册仅 5 项在册 → 新立 P3-CK-hr2-028-r3。blocker=0/major=0/minor=0 之外新发面全 P3 | **finding**（3 新立 P3 + 23 归并 + 2 复用，见 §2） |
| **DIM-F 前端页面与 E2E** | §1.2 全套 + hr-2 面 = 薪酬核算页（复杂手写页清单成员）+ 考勤/请假/排班/工资单页面族 + 薪酬模拟/社保/税务/调研/胜任力页面族 | `npm run validate:flux` step [1/3] `FLUX_PAGE_ERROR_COUNT: 0 pageCount=999 erpPages=855`（与基线一致）；整体 exit 1 余项 = **325 条 ERR 100% `variant="primary"`×dropdown-button 既有 stub 外部漂移族（non-variant=0，successor 在案，非本切片 finding）**；hr 域命中 31 条逐条核对全为同族成员（非 variant = 0）；flux-only：`component="AMIS"` 保留层 = 0（hr + 全工作区双口径）、ORM `ext:web-renderer="flux"` 缺失 = 0（hr orm 42 处声明在位）。hr-2 面逐页走查：①ErpHrSalary（薪酬核算页）`main.page.yaml` = 3 行 codegen stub（`x:gen-extends` GenPage，M0.4 源头链合规）+ 保留层 `ErpHrSalary.view.xml`（174 行，`x:extends="_gen/_ErpHrSalary.view.xml"` + bounded-merge + 审批五动作按钮 `@mutation:ErpHrSalary__submitForApproval/withdrawApproval/approve/reject/reverseApprove` REST 约定）；②dashboard/payroll-approval（M0.4 L133-134 手写 flux 页 MI.8 批 2 i18nEn 44 处在位）+ org-chart 孪生文件面（M1.10 已归并裁决 U21 不重开）；③Attendance/Timesheet/TimesheetLine/LeaveRequest(+team-vacation-calendar flux 手写页 MI.8 登记行 57-58)/LeaveBalance/Shift/ShiftAssignment/ShiftSwapRequest/ShiftRotationPattern/SalarySimulation(+ItemAdjustment)/PayrollBankFile/SocialInsurance{Config,Base}/TaxConfig/TaxSpecialDeduction/Survey 族/Competency 族实体页 = codegen wrapper + 保留层 view.xml delta 面（`x:extends="_gen/…"`）逐页核验；④report/payroll-simulation-comparison（M0.4 行 60，`@query:` REST 约定 + i18nEn）。死状态样式分支查重：hr-2 页面零 `docStatus==ACTIVE` 型 per-dict 死分支（pur-017-r3 族不涉）；各页共享 `dangerVals` 通用危险值分类器含个别对本 dict 不可达值（timesheet-status 无 CANCELLED/survey-status 无 REJECTED）——共享模板常量非 per-dict 分支语义，display-only 无数据影响，登记不立项；数据访问全 REST `@query:`/`@mutation:`/`@rpc:` 零 GraphQL 断言（`graphql:labelProp` 为 view.xml to-one 元数据键读取，codegen 约定非传输）；界面标签 i18n-en 承载在 ORM 模型源（`i18n-en:displayName/label`）。E2E：hr spec 10 个 business-action + smoke + visual snapshot——违规选择器（`data-slot\|data-testid\|\.cxd-`）= 0；`E2E_ENGINE` 缺省 flux 实证（pages/engine.ts `return 'flux'`）；GraphQL 命中均为 spec 头注释登记的 API 驱动型数据层通道（DIRECT `@BizMutation` 全栈可达性驱动，runbook L122 页面断言禁令不适用，M1.10 fin-4 同判例）；PageObject 经 `pages`/`_helper` adapter 层 | **pass** |
| **DIM-S seed 数据** | §1.3 全套 + hr-2 族 seed（考勤/工时/请假/薪酬/排班/税社保 CSV + M1.3 批配置链）+ **薪酬过账 posted 一致性裁决**（M1.10 Phase 4 明示归 hr-2） | `TestErpSeedDataIntegrity` **4/0/0/0 全绿**（363 实体 findAll + to-one 零悬空 + 零孤儿 CSV + scope-pinning 计数；2026-09-09 06:47 实跑 BUILD SUCCESS）；`git status --porcelain _init-data/` 空（只读审计零 seed 变更）；资产清点 **372 CSV + 1 SQL**（= seed-data.md 对账表 M1.5 批次后最新登记行，逐数一致）；deploy `_seed_*.sql` 命中 6 文件 = module-cs×3 方言 + module-notify×3 方言，登记处表 seed-data.md §同步义务均 ✅已聚合（`nop_sys_code_rule.csv` + `erp_sys_notification_template.csv`），hr 无 deploy seed 零同步义务；hr-2 族 seed 18 CSV 在位（attendance/timesheet+line/leave_request+balance/salary+item/simulation+item_adj/payroll_bank_file/shift+assignment+rotation_pattern+swap_request/tax_config+special_deduction/social_insurance_base+config），M1.3 批 +32 CSV 登记（plan `2026-09-01-2255-2`：C17 值中性化契约 salary/tax_config 仅 year=2025、attendance/leave 冻结 2026-05 窗口、social_insurance_config 仅 SHANGHAI）核对一致；**薪酬过账 posted 一致性裁决**：`erp_hr_salary.csv` 无 POSTED 列（RC-R1.89 posted 列经 ORM 默认 false），2 行均 `APPROVE_STATUS=UNSUBMITTED`+`PAYMENT_STATUS=PENDING`（REMARK「演示伪值·待核算提交」）→ **seed 零 posted=true 行 ⟺ 零凭证回链，双射裁决空验一致**（posted=true 当且仅当有凭证回链在 hr seed 面无反例；M1.10 移交裁决面闭合） | **pass** |
| **DIM-T 单元测试** | §1.4 全套 + hr-2 范围（考勤/请假/薪酬/排班/工资单族 BizModel + payroll 引擎 + 薪酬过账链）+ **P1 考勤→薪酬→过账清单行覆盖**（M1.10 明示归 hr-2） | `mvn test -pl module-hr/erp-hr-service` **249/0/0/0 全绿**（= `cjk-baseline.md` §批注账 MI.6 批 2 行锚点 `hr 249` 精确一致，零失败零回归；同批姊妹计划 0547-2/3 面 = crm/cs/ct/b2b/drp + mnt/aps/log/notify/md/common 模块，hr 无姊妹增量待披露）。覆盖对账：hr-2 BizModel 注解动作 ≈70（SalarySimulation 15 / Salary 7 / Attendance 5 / LeaveRequest 5 / Shift+Assignment+Swap 4×3 / Timesheet 3 / Survey 族 6 / DevPlan 3 / Gap 2 / Assessment 2 / Rotation 1 + 14 个零注解桩 BizModel）↔ 测试逐项落点：考勤 clockIn/clockOut/makeUp/getTodayAttendance ↔ `TestErpHrAttendanceEngine`+`TestErpHrAttendanceMakeUp`+`TestErpHrAttendanceCrossDayClockOut`（跨天回退 5 组）、请假 submit/approve/reject/cancel/getBalance ↔ `TestErpHrLeaveEngine`、薪酬引擎 calculateSalary/runPayroll/markPaid/voidSalary/generateBankFile ↔ `TestErpHrPayrollEngine`+`TestErpHrSalaryPostingChain`+`TestErpHrSalaryWorkflowApproval`、模拟族 ↔ `TestErpHrPayrollSimulation`、排班族 ↔ `TestErpHrShiftScheduling`、工时族 ↔ `TestErpHrTimesheetFamily`、调研生命周期 ↔ `TestErpHrSurveyLifecycle`+CrudSmoke、胜任力族 ↔ `TestErpHrCompetencyManagement`；**P1 考勤→薪酬→过账清单行全覆盖**（testing-strategy §关键业务流 P1 行：Attendance→PayrollEngine→SalaryPostingChain→WorkflowApproval 四类在案，280 发放凭证 + 270/290/300 计提链断言在位）；**缺口**：`ErpHrSalaryBizModel.findPayrollSummary` + `queryCumulativeTaxData` 零测试断言 → 新立 P3-CK-hr2-030-r3（读侧聚合查询，写路径/引擎链全覆盖，按业务关键度 P3）；getCompanySummary/getProjectSummary/findAnomalies 零专属断言 → 同控制点（P2-CK-hr2-013 四汇总/异常 @BizQuery）测试缺位并入该 ID（mfg-022-r3/hr-1 findDepartmentTree 前例）；快照纪律：`SnapshotTest.RECORDING` = 0 残留；`delVersion` 7 处命中均为逻辑删除断言/注释（非屏蔽滥用）；`_cases` 零 `*` 通配屏蔽（createTime/updateTime 框架自动屏蔽）；249 全绿 = CHECKING 态等价证明 | **finding**（1 新立 P3 + 2 项证据并入既有 ID，见 §2） |
| **DIM-I i18n（MI 先行时序口径）** | §1.5 全套（仅零回归 + 白名单合规，不产同类 finding） | `--strict` **PASS exit 0**（0 new violations vs 冻结 SNAPSHOT，170 baseline files 单向收紧成立，totals CAT1..4=0/0/209/1318）；`--self-test` **PASS**；hr 探针族（CAT-1 20）维持清零零回归（Phase 1 report mode CAT1..4 = 0/0/0/0 一致）；WHITELIST hr 条目 = **1 条**（<3 条 → 全数核对并记录条目数）：`ErpHrConstants.java` 四要素齐备——文件路径（module-hr/erp-hr-service/…/ErpHrConstants.java，cats[3]）/ 理由（`HR_ROLE_ID="HR 专员"` 为 seed 角色名数据契约，运行期匹配 nop_auth_role.csv seed roleId 与 erp-hr.action-auth.xml `roles="HR 专员"` 字面 + `IUserContext.isUserInRole` 守卫，改英文破坏角色解析行为，C2 窄类豁免同 cs「客服主管」先例）/ owner doc 指针（i18n-compliance.md 判定准绳表 #5 + CAT-3 行 C2②）/ 裁决来源（plan 2026-09-07-1715-1 Phase 2，2026-09-07）——HEAD 实仓复核 `ErpHrConstants` L293 `HR_ROLE_ID = "HR 专员"` 与登记一致零缺陷；`grep -L @Locale` 全量 `*Errors.java` = 0 缺失（`ErpHrErrors` @Locale 在位）；`git status --porcelain 'module-*/erp-*-meta/**'` + `_vfs/i18n/**` = 空（`_` 前缀生成 i18n yaml 零手改） | **pass** |

## 2. Finding 三态裁决（§2 跨轮查重全维强制）

> 查重源：r1 `docs/audits/check/ai-check-index.md` §报告清单 `ck-hr-attendance-payroll.md`（C6.2：0 P0 / 5 P1 / 9 P2 / 12 P3，**本切片主查重源**）§Finding 追踪 26 条逐一比对 + 同域跨切片参考 `ck-hr-org.md`（C6.1，M1.10 r3 已归并裁决 13 open 项：P2-002/003/005 + P3-006..008/010..016）+ r2 只读目录 `docs/audits/check/2026-08-28-2049-ai-check-r2/`（无 hr-2 同型新独立登记）+ §Mission 基线快照（daoFor 读侧 P1-MA1-022、死状态 P1-MA2-039/040、调动休假告警 P2-MA2-050 均已裁决偏离不重复报告；P3-CK-hr2-017 个税累计损坏响亮失败修复体 P1-MA4-018、posted writer P1-MA4-017 已修在位复核）。**本轮新立 4 条**（全 P3，`P{n}-CK-hr2-{027..030}-r3`，承 r1 hr-2 族 001..026 新立自 027 起）；历史 ID 零覆写。行号均为 T0 = HEAD `2c1c1ef25` 实测。

### 2.1 复用（同型已 fixed，HEAD 复核有效）— 2 条

| 原 ID | 修复在位证据（T0 = HEAD `2c1c1ef25`） |
| --- | --- |
| P2-CK-hr2-006（tryPostPayment 无去重守卫叠加凭证 REQUIRES_NEW 先行提交） | F1.2 去重守卫在位：`SalaryPostingDispatcher.tryPostPayment` L171-177 `alreadyPosted(buildBillCode, SALARY_PAYMENT)` 命中跳过 + `alreadyPosted` L198-220（per-businessType 回链反查 POSTED 且未冲销，镜像引擎 `ErpFinPostingProcessor.alreadyPosted` 范式，已冲销凭证不视为命中）；回归测试 `TestErpHrSalaryPostingChain` 守卫短路断言在案（249 全绿内）；跨轮索引该行状态已 `fixed` 与本复核一致 |
| P2-CK-hr2-007（salary/attendance/leave/simulation 全实体通用 CRUD 无守卫，同型 P1-CK-pur-003 族/hr-004 家族 hr2 站点） | F1.3 状态锁基类在位：hr-2 全部实体 BizModel extends `AbstractErpCrudBizModel`（ErpHrSalary/Attendance/LeaveRequest/LeaveBalance/Timesheet/Line/Shift/ShiftAssignment/ShiftRotationPattern/ShiftSwapRequest/SalarySimulation/SimulationItemAdjustment/PayrollBankFile/TaxConfig/TaxSpecialDeduction/SocialInsurance{Config,Base}/Survey 族/Competency 族逐文件核验；posted/APPROVED 拒通用 update/delete，config 默认 ON）；调用点合规，基类本体归 U20（M1.15） |

### 2.2 归并（同型 open 追加证据至原 ID）— 24 条

| 原 ID | r3 复核证据（T0，原 ID 状态不动仍 open） |
| --- | --- |
| P1-CK-hr2-001 | 出勤折算分母恒 22：`PayrollCalculator` L42 `DEFAULT_REQUIRED_WORK_DAYS="22"` + L68/L306 summarizeAttendance 恒回写常量（contract 侧无 requiredDays 来源）；有薪假按缺勤扣薪：L294-297 `!isAbsent` 才计 presentDays，批准休假经 calcAttendance 置 isAbsent=true（ShiftCalcAttendanceProcessor L36-42）→ 年假/带薪假日同样折减基本工资 |
| P1-CK-hr2-002 | 银行文件路径发放不触发 280：`ErpHrSalaryGenerateBankFileProcessor` L34-45 逐条 `markPaidTargetStatus()` 翻 PAID 全程零 `postingDispatcher.tryPostPayment` 调用——与 state-machine.md §四.6「paymentStatus → PAID 触发 SALARY_PAYMENT(280)」断言的漂移面维持（markPaid 手工路径已触发，批量路径缺口在案） |
| P1-CK-hr2-003 | runPayroll 零逐员工失败隔离：`ErpHrSalaryRunPayrollProcessor` L25-32 循环体无 try/catch——任一员工缺合同（`ERR_EMPLOYMENT_CONTRACT_NOT_FOUND`）/社保基数（`ERR_SOCIAL_INSURANCE_BASE_NOT_FOUND`）/税务配置（`ERR_TAX_CONFIG_NOT_FOUND`）即整批 NopException 回滚，UC-HR-04「跳过并告警」分歧原样 |
| P1-CK-hr2-004 | SocialInsuranceCalculator 忽略有效期：`findBase` L87-94 仅 eq(employeeId) 无 effectiveFrom/To 过滤无排序 limit 1；`findConfigs` L96-101 仅 eq(cityCode)——年调后同城同险种多行配置全量参与计扣、基数历史行任取，原样 |
| P1-CK-hr2-005 | getComparison 派生字段恒 0：`readSalaryField` L516-540 switch 仅 8 个输入项，`SALARY_ITEM_CODES` 13 项中 grossSalary/socialInsurance/housingFund/taxAmount/netSalary 5 个派生项命中 `default: return ZERO` → previousAmount/currentAmount/simulatedAmount/baseline 四列对派生行恒 0，原样 |
| P2-CK-hr2-008 | 无余额记录静默放行：`checkLeaveBalance` L140-142 `if (balance == null) return;`——与 `getBalance` L126-129 同数据（entitled+carried−used）得出相反结论（守卫放行 vs 余额显示负数），原样 |
| P2-CK-hr2-009 | 重叠守卫仅同假别：`checkDateOverlap` L159 `eq("leaveType", leave.getLeaveType())`——跨假别同期休假双双 APPROVED 且 onLeaveApproved 按 leaveRequestId 覆盖 assignment 的先批休假取消联动失效面维持（OnLeaveCancelled 现 leaveRequestId 匹配守卫缓解覆盖扩散，见 022 行） |
| P2-CK-hr2-010 | ErpHrSalary 无 (employeeId, year, month) UK（实仓 ORM：全域 unique-key 仅 `UK_HR_EMPLOYEE_CODE_ORG` + `UK_HR_ATTENDANCE_EMP_DATE` 两处，salary 实体块零 unique-key）+ `existsNonVoidSalary` 前置检查 TOCTOU（BizModel L245-265 findCount 竞态窗口），并发核算重复薪酬行风险原样 |
| P2-CK-hr2-011 | findActiveContract 名不副实：`PayrollCalculator` L273-280 eq(employeeId) + limit 1——零 status 过滤、零 ordering，多合同员工（调动旧约 + 续签新约）任取一行作核算基数，原样 |
| P2-CK-hr2-012 | applyBatchAdjustment 四类调整全部写 basicSalary：`ErpHrSalarySimulationApplyBatchAdjustmentProcessor` javadoc L18「按调薪类型解析 + 逐人 basicSalary 调整记录」+ 实现逐人 setBasicSalary——ALLOWANCE（津贴调整）语义错置面原样 |
| P2-CK-hr2-013 | computeAllEmployeeSims 全量重算 N+1：getComparison/getProjectSummary/getCompanySummary/findAnomalies 四个汇总/异常 @BizQuery 每次打开逐员工 5+ 查询全量重算（BizModel L146-153/196-247 消费链）+ runPayroll 同型，原样；**DIM-T 追加证据**：getCompanySummary/getProjectSummary/findAnomalies 零专属测试断言（grep src/test 零命中），同控制点覆盖缺位并入本 ID（mfg-022-r3 前例） |
| P2-CK-hr2-014 | generateRotation 活跃口径仅 SCHEDULED：`ErpHrShiftRotationPatternGenerateRotationProcessor` L101/L181 `eq("status", SCHEDULED)`——PRESENT/ABSENT 日被视为空位可重插；插入路径零 UK flush-catch（L192 flushSession 仅服务 regenerate 删除侧），未翻译原始异常面原样 |
| P3-CK-hr2-015 | 死状态 + 孤儿 dict 原样：shift-assignment-status CANCELLED 零 writer（`ErpHrConstants` L101 唯一声明点，service 全文零 setStatus(CANCELLED) writer）；旧 6 值 `erp-hr/salary-approval-status`（PENDING/REVIEWED/APPROVED_FINANCE/APPROVED_MANAGER/PAID/VOID）ORM L77-84 孤儿声明零列引用（approveStatus 实引 `wf/approve-status` L742）；**扩员 2 站点**：`ErpHrErrors` L138 错误描述「未找到 APPROVED_MANAGER 状态的薪酬记录」+ `SalaryPostingProvider` L31 javadoc「触发：APPROVED_MANAGER 计提」——旧 6 态迁移的代码侧文字残留（同控制点扩员，原 ID 状态不动） |
| P3-CK-hr2-016 | LeaveApproverTimeoutJob：三层 cron 键漂移原样（job.yaml cronExpr 外层 + config `erp-hr.leave-approver-timeout-cron` 内层空值跳过 execute 门控）；顶层失败仅 LOG.error（execute L105-107）无告警通道——M5 批后 job 已注入 `IErpSysNotificationBiz`（转派通知用），但 job 级失败 alert 派发仍缺（通知通道在位使修复成本降低，证据更新） |
| P3-CK-hr2-017 | TaxBracketParser 空/畸形表：`resolveBracket` L229 `brackets.get(0)` 空表裸 IOOBE（parse 对 null/非 List 返回空表 L21-27；F 批 null rangeUpperLimit 防御已补 L231 但空表路径仍在）；坏 rate 值 toBigDecimal L52-54 返 null → L89 multiply NPE；latest 月 cumulativeData null → L154 条件跳过累计静默重置。P1-MA4-018 修复体（损坏 JSON 响亮失败 L184-190）HEAD 复核在位，三处配置数据边界缺口原样 |
| P3-CK-hr2-018 | 三配置点硬编码：加班费率 L40 `DEFAULT_OVERTIME_HOURLY_RATE=50` + 标准工时 L299 `8` + 月工作日 L42 `22`；README L71「个税起征点、月加班工时上限、每日标准工时均为可配置项」+ L89 `erp-hr.max-overtime-hours-per-month | 36`——`ErpHrConfigs` 22 个 accessor 实核零 overtime/requiredWorkDays/standardHours 项（零 accessor 零消费），仅 taxThresholdMonthly 在位 |
| P3-CK-hr2-019 | BizModel 保留 processor 化前 dup 死代码：`ErpHrLeaveRequestBizModel` `checkLeaveBalance` L134 / `checkDateOverlap` L156 / `resolveApproverId` L204 零调用方（生产调用全部走 `AbstractErpHrLeaveRequestProcessor` L97/L119 单源副本）——五 BizModel 双副本面经 M5 重构收窄至 leave 族 3 helper，死副本仍在本切片逐行比对双份零漂移登记 |
| P3-CK-hr2-020 | leave REJECTED 无重提边：`ErpHrLeaveRequestStateMachine` 4 边矩阵（submit 仅 DRAFT 源 L40）vs Timesheet/Salary Bean 均有 REJECTED→SUBMITTED 重提边——处理不对称原样；doc 场景 B「修改后重新提交」未落地维持 |
| P3-CK-hr2-021 | computeWorkHours 无负值守卫（`ErpHrAttendanceBizModel` L162-168 Duration.between 负值直除）+ doMakeUp 手工补卡零未来时间校验（L100-132 直接写目标时间戳），负工时可落库进入加班/出勤聚合面原样 |
| P3-CK-hr2-022 | leave 联动三处漂移复核：①onLeaveCancelled 已增 leaveRequestId 匹配守卫（`ErpHrShiftOnLeaveCancelledProcessor` L24，无条件置 SCHEDULED 面收窄——首站点证据更新）；②approve L30 `setApproverId(resolveApproverId(context))` 仍以恒 null 覆盖写；③resolveApproverId 恒 null（BizModel L204-207 + Abstract 同名副本）原样 |
| P3-CK-hr2-023 | 休假期间边界三处原样：computeDurationDays `Math.max(days,0)`（BizModel L78-83，endDate<startDate 得 0 天可提交）；sumUsedDays `dateBetween("startDate", yearStart, yearEnd)` 按起始日落年（跨年休假全额计入起始年）；sumUnpaidLeaveDays `ge(startDate) le(endDate)` 整单过滤（跨月休假整单漏算无薪扣减） |
| P3-CK-hr2-024 | 排班调换边界原样：submit 零同日期校验（`ErpHrShiftSwapRequestSubmitProcessor` L20-28 target 任意日期 assignment 均可配对，swapDate 仅取 source 侧）+ 零同 assignment 多 PENDING 守卫；approve 零双批准守卫（两笔 PENDING 先后 approve 两次换班均过状态守卫） |
| P3-CK-hr2-025 | 休假余额并发扣减竞态原样：checkLeaveBalance 读派生（findBalance + sumUsedDays）零锁零 REQUIRES_NEW，两笔并发 approve 均过校验超额审批面维持 |
| P3-CK-hr2-026 | convertToFormal 部分冲突静默丢弃：`ErpHrSalarySimulationConvertToFormalProcessor` L47/64-76 conflicts 收集后 convertedCount>0 时零持久化/零日志/零返回（静默丢弃）——doc §4.2「仅转换无冲突的员工」行为一致但冲突可观测性缺失；个税跳档告警有效税率近似仅检升档（BizModel L809 5pp 阈值 + L253 config 门控）原样 |

### 2.3 新立 `-r3` — 4 条（全 P3）

**P3-CK-hr2-027-r3**（DIM-B 维度⑮ owner-doc 漂移簇：state-machine.md §适用对象五未同步）

- **控制点**：`docs/design/human-resource/state-machine.md` §适用对象五（「dict 含值无状态机 writer」Deferred 登记）两行与实仓漂移——①Survey 行「`ErpHrSurveyBizModel` 为 CrudBizModel 桩（18 行，零状态机 mutation），三态（OPEN/CLOSED/ARCHIVED）为预留死状态」vs 实仓 `ErpHrSurveyBizModel`（144 行）已落地完整状态机：publish（DRAFT→OPEN，startDate/endDate/题目非空守卫 L49-68）+ close（OPEN→CLOSED，触发 aggregateResult L73-80）+ archive（CLOSED→ARCHIVED L84-90）+ published-immutable defaultPrepareUpdate 守卫（L100-119，RC-R1.9 P1-MA2-041 + P1-RC-016 修复体）；②DevelopmentPlan 行「计划项 OVERDUE 为预留死状态——本期仅活态有写入」vs 实仓 `updatePlanItemStatus` 守卫 L190 显式允许 IN_PROGRESS→OVERDUE writer（仅缺自动逾期 job，与「无 OVERDUE 自动 job」子句自洽但「死状态」定性已失真）。
- **问题/严重性**：owner doc Deferred 登记未随 RC-R1.9/R1.10 修复批同步，§五 的「死状态预留」清单对 Survey/OVERDUE 已失真——后续审计/开发按 §五 误判状态机实现态（同型先例 mfg2-025-r3 / fin3-018-r3 / ast2-025-r3 owner-doc 未同步族，P3 校准）。
- **三态裁决**：新立。r1 hr-2 族无此形态（015 为 dict 死值登记，非 doc 行失真）；r2 无同型；同族跨单元先例均 P3。级别 **P3**（纯文档面，零运行时影响；同簇两行一次修复）。
- **修复建议**：归 M2.x——§五 Survey 行改记「状态机已落地（publish/close/archive + 不可编辑守卫）」并将该行移出 Deferred 清单；DevelopmentPlan 行改「OVERDUE 手动 writer 已落地，自动逾期 job 仍为 successor」；CANCELLED(devplan)/SUSPENDED 两行复核后保留。

**P3-CK-hr2-028-r3**（DIM-B 维度⑨/⑭ FNPT 注册面）

- **控制点**：hr-2 自定义 mutation ≈42 个零 FNPT 注册——保留层 `erp-hr.action-auth.xml` 全量 grep `FNPT:ErpHr*` 仅命中 5 项（`ErpHrLeaveRequest:approve` L77-80 + `ErpHrSalary:approve/reverseApprove/markPaid/voidSalary` L101-116）；未注册：clockIn/clockOut/makeUpClockIn/makeUpClockOut（Attendance 4）+ submit/approve/reject（Timesheet 3）+ submit/reject/cancel（Leave 3）+ calculateSalary/runPayroll/generateBankFile（Salary 3）+ createSimulation/adjustItem/applyBatchAdjustment/convertToFormal/submitForReview/approve/reject（Simulation 7）+ calcAttendance/onLeaveApproved/onLeaveCancelled（Shift 3）+ assignSingle/assignBatch/copyFromPeriod（Assignment 3）+ generateRotation（1）+ swap submit/approve/reject/cancel（4）+ survey publish/close/archive/submitResponse（4）+ aggregateResult（1）+ generateDevelopmentPlan/updatePlanItemStatus/completePlan（3）+ refreshGapAnalysis/WithLevels（2）+ completeAssessment（1）。
- **问题**：deny-by-default 权限模型下非「HR 专员/薪酬审批人」角色对这些动作无权限载体——同族既定裁决（fin4-023-r3 口径）：菜单页面可达但页面内业务动作按钮权限面不完整（内部不一致：核心敏感动作 markPaid/voidSalary/reverseApprove/approve 已注册证明范式在位，其余 ≈42 个为遗漏而非裁决豁免；xbiz 侧 `<auth permissions>` 与 action-auth FNPT 双轨中仅 xbiz 轨覆盖）。
- **三态裁决**：新立。r1 hr-2 族无此形态；r2 无同型；同族跨单元先例 = P3-CK-drp-018（22 mutation）/ P3-CK-b2b-011（4）/ P3-CK-prj-022-r3 / P3-CK-qa-029-r3 / P3-CK-fin4-023-r3（13）——均 P3 校准。级别 **P3**（主守卫 = 状态机 Bean + F1.3 状态锁基类在位，权限面为纵深防御层；且薪酬敏感五动作已注册）。
- **修复建议**：归 M2.x——按 `ErpHrSalary:markPaid` 范式在保留层 action-auth 补 FNPT 注册（roles=HR 专员/薪酬审批人按 useCases 对齐）；与 drp-018/b2b-011/prj-022-r3/qa-029-r3/fin4-023-r3 同批跨域对齐收口。

**P3-CK-hr2-029-r3**（DIM-B 维度② daoFor 豁免注释族）

- **控制点**：3 文件 10 站点 `IDaoProvider` 注入/`daoFor` 查询零豁免理由注释——`PayrollCalculator` 4 站点（L45 注入 + L274 ErpHrEmploymentContract / L283 ErpHrAttendance / L317 ErpHrLeaveRequest / L130 newEntity 同域读查询）；`IncomeTaxCalculator` 3 站点（L56 注入 + L125 ErpHrSalary / L195 ErpHrTaxConfig / L207 ErpHrTaxSpecialDeduction）；`SalaryPostingDispatcher` 3 跨域站点（L203 ErpFinVoucherBillR / L211 ErpFinVoucher 跨 fin 域 + L366 ErpMdAcctSchema 跨 md 域——类 javadoc 详载 D1/D3/RC-R1.89 设计裁决但无「为何不经 I*Biz」豁免行）。
- **问题**：对照同切片范式不对称——`SocialInsuranceCalculator` L24-25（「配置数据，非业务实体 CRUD 管道；对齐 projects 域 TimesheetPostingDispatcher 读主数据模式」）、`GapAnalysisCalculator` L26（「仅为 newGapEntity 实体工厂；核心 calculate 不依赖 IoC」）、`AbstractErpHrGapAnalysisProcessor`（「Employee 主数据本域直接 dao 即可——仅取外键字段」）均有注释理由，同族 calculator/dispatcher 未随批登记（fin3-017-r3 / fin4-022-r3 / mfg3-019-r3 同族「豁免注释缺失 + 切片内范式不对称」口径）。
- **三态裁决**：新立。r1 hr-2 族无此形态（r1 D 维裁决未覆盖 payroll 引擎注释面）；同族先例均 P3。级别 **P3**（读侧查询，行为合规缺登记；processor 族 daoFor = processor-extension-pattern 惯例 r1「验证为正确」在案不在本条范围）。
- **修复建议**：归 M2.x——3 文件按 SocialInsuranceCalculator 行文补豁免注释（同域只读聚合/跨域回链反查镜像引擎范式）；与 fin3-017/fin4-022/mfg3-019 同批对齐收口。

**P3-CK-hr2-030-r3**（DIM-T 覆盖缺口）

- **控制点**：`ErpHrSalaryBizModel.findPayrollSummary`（薪酬汇总看板聚合，L289-348）与 `queryCumulativeTaxData`（个税累计数据回读，L140-162）零测试断言（grep `module-hr/erp-hr-service/src/test` 零命中；`_cases` 族亦无间接覆盖证据）。
- **问题/严重性**：testing-strategy 覆盖要求表「BizModel 每公开方法 ≥1 测试」缺口；两方法均为读侧聚合查询（无状态翻转、无资金移动），核心写路径与 P1 考勤→薪酬→过账链全覆盖——按业务关键度定级 P3（mfg2-027-r3 findBomTree 同型校准；P2 起跳仅当缺口涉关键业务流）。
- **三态裁决**：新立。r1 hr-2 族无此形态（013 为性能维，本条为覆盖维，控制点不同）；同单元 3 个模拟汇总查询的同类缺口按同控制点并入 P2-CK-hr2-013（见 §2.2），不在此重复立项。
- **修复建议**：归 M2.x——补 findPayrollSummary 聚合口径（orgGroups 分组/total 四项 setScale(2)）与 queryCumulativeTaxData（VOID 过滤 + upToMonth 窗口 + 空 fallback "{}"）两组快照测试。

### 2.4 范围外注记（格集合外，维持登记态）

- **P3-CK-hr-009**（招聘 mutation 入参无边界）+ **P3-CK-hr-017**（hire 初始 ACTIVE vs PROBATION 需求分歧）：招聘 BizModel 本体（6 mutation：moveToScreening/scheduleInterview/makeOffer/hire/reject/close，`ErpHrRecruitmentBizModel`）维持登记态复核——r1 C6.1 已审、本切片格集合外（M1.10 §2.4 同口径移交项承接完成，闭合 U14 归属真空）；hire 衔接消费点 `ErpHrRecruitmentHireProcessor` 归 hr-1 已收官面。
- **DIM-F dangerVals 共享分类器**：各页共享 `dangerVals` 通用危险值数组含个别对本 dict 不可达值（timesheet-status 无 CANCELLED / survey-status 无 REJECTED/CANCELLED）——共享模板常量非 per-dict 死状态分支（P3-CK-pur-017-r3 族形态不成立），display-only 登记不立项。

### 2.5 归属标注（§3.2 共享代码边界）

- **posting 引擎内部归 fin-1（M1.1 已收官）**：hr 薪酬过账 Dispatcher/Executor/Provider 为消费侧——消费侧调用序已审合规（Executor `@Inject IErpFinVoucherBiz` 跨域写经 I*Biz ✓ / REQUIRES_NEW 由 Facade 承接零本地 @Transactional / Provider 实现 fin SPI `IErpFinAcctDocProvider` 四 businessType 科目映射与 payroll.md §9.1 一致）；引擎内部缺陷（凭证引擎/红冲/sweep）一律归 fin-1。
- **common 抽象族归 U20（M1.15）**：`AbstractErpCrudBizModel` 状态锁基类（hr-2 全实体接入，调用点合规）、`MaskHelper` 薪酬脱敏（fail-closed 调用点合规）——基类行为缺陷归 U20 格。
- **聚合横切面归 U21（M1.16）**：action-auth 聚合器/flux 导出门禁本体/`_init-data` 全量装载/`TestErpSeedDataIntegrity` 门禁宿主——本格仅核 hr 注册在位性（L17 x:extends ✓）。
- **notify 派发子系统本体归 U11**：Dispatcher 失败告警（`IErpSysNotificationBiz.notify` hr.salary-posting-failure）与 job 转派通知为消费侧调用，合规。
- **hr-2 专属判定面（§3.3 U14 B 行）双向核证**：①薪酬模拟——recalculateWithOverrides 克隆降级方案 Decision javadoc 如实登记（Explore 结论 + 0831-2 零修改声明）；②排班/考勤语义——跨天班次（isCrossDayShift endTime≤startTime）、迟到= clockIn−startTime、缺勤（requireClockIn 无打卡/leaveRequestId 标记）与 shift-scheduling.md §4.1/4.2 逐条一致（README/state-machine 散布语义经 §五漂移簇登记后收敛）。

## 3. 统计

| 级别 | 本轮新立 | 复用（fixed 复核有效） | 归并（open 追加证据） |
| --- | --- | --- | --- |
| P0 | 0 | 0 | 0 |
| P1 | 0 | 0 | 5（hr2-001..005） |
| P2 | 0 | 2（hr2-006/007） | 7（hr2-008..014） |
| P3 | 4（hr2-027..030-r3） | 0 | 12（hr2-015..026） |
| **合计** | **4** | **2** | **24** |

五格 verdict：DIM-B **finding**（3 新立 P3：027 doc 漂移簇 / 028 FNPT 注册面 / 029 daoFor 豁免注释族）/ DIM-F **pass** / DIM-S **pass**（含 M1.10 移交 posted 一致性裁决闭合）/ DIM-T **finding**（1 新立 P3-030 覆盖缺口 + 2 项证据并入 013）/ DIM-I **pass**。r1 hr-2 族 26 条 ID 状态零覆写（2 fixed 复核有效 + 24 open 追加证据）；同域 hr-org 17 条零触碰（2 范围外项维持登记态复核）。

## 4. 剩余风险声明（横切关注点 13 完整四件套）

- **已查**：hr-2 范围 payroll 引擎 5 文件 + posting 3 文件 + scheduling 1 + 状态机 Bean 4 + hr-2 BizModel 18 + U14 承接族 BizModel 13 + processor 族 ~30 + job 1 + constants/errors/configs 全读走查；机械程式全套实跑（compliance checker 19 规则 = M0.3 快照行零漂移 / 反模式 grep 族全零 / `IDaoProvider|IOrmTemplate` 逐文件注释核对 / codegen 安全 38 dict.yaml 零手改 + `_gen` 零脏面 / 聚合完整性 E1 勘误路径 / `validate:flux` 双数字 0 error·999·855 + 325 ERR 全 variant 族对账含 hr 31 条 / flux-only 双 grep 全零 / `TestErpSeedDataIntegrity` 4/0/0/0 + seed 零变更 + 372 CSV+1 SQL 对账 + deploy 6 文件登记核对 + hr-2 18 CSV + M1.3 批契约 + posted 一致性空验裁决 / hr 回归 249 全绿两轮 + 覆盖对账 ≈70 方法 × 10 测试类 + P1 关键业务流行四类在案 + RECORDING=0 + delVersion 合规 / `--strict` PASS + `--self-test` PASS + 白名单 1 条四要素 HEAD 复核 + `@Locale` 零缺失 + meta 零手改）；15 维度无跳维；⑮ 6 doc × 16 断言点（2 漂移扩样全文走查）；r1 hr-2 26 条逐一现场复核三态裁决 + 同域 hr-org 13 open 项参考 + r2 目录 + §Mission 基线快照；hr-2 面 ~26 页逐页走查 + E2E 10 spec 纪律核对；U14 余下面承接复核（competency/development/assessment/survey 族 + 招聘范围外项）。
- **未深查（边界归属）**：posting 引擎内部（归 fin-1 已收官）；common 抽象族基类本体（归 U20/M1.15）；`erp-hr-web` `_gen` 视图生成细节与全局面契约 drift（codegen 面，M0.4 矩阵判定，全局面归 U21）；ErpHrReportBizModel 渲染细则（报表子系统既有验收，hr-1 面已结构扫描）；测试代码仅作覆盖对账与行为语义消费；`tests/e2e` hr spec 的业务断言正确性（本格仅审纪律面：selector/引擎/通道约定）。
- **残留风险（登记不裁决）**：① 24 条归并 open finding 修复归 M2.x（hr 修复批 M2.6），其中 P1 五条（022 折算分母/银行文件无凭证/整批回滚/社保有效期/对比视图恒 0）在薪酬投产场景影响面最大，建议 M2.x 优先级排序参考；② 4 条新立 P3 与同族跨域站点（028 FNPT 注册面与 drp/b2b/prj/qa/fin4 同批收口；029 daoFor 豁免注释族全仓扫描归 U20/M1.15；027 doc 漂移与 030 覆盖缺口随 hr 修复批处理）；③ `validate:flux` 325 条 variant 漂移为批前在案外部事项（successor 在案），非本切片范围；④ 本格集合覆盖完成后 U14×五维（hr-1 + hr-2）10 格全部落盘——U14 基础格完整性就绪，M1.17 收官校验依赖解除（`ck-hr-org-r3.md` §残留风险 ④ 闭合）。
- **successor 触发条件**：M1.17 收官以覆盖矩阵完整性校验本报告 5/5 格；M2.x 修复以「先写失败测试」承接 §2.2 open 项（P2-CK-hr2-013 同时承接 3 汇总查询测试缺位注记；P3-CK-hr2-015 同时承接 ErpHrErrors:138/SalaryPostingProvider:31 旧口径文字残留扩员）；`erp-hr.deduct-unpaid-leave`/`erp-hr.simulation-auto-convert` 等 config 投产启用时 P1-CK-hr2-001/026 影响面应即升优先级先写失败测试。

## Verification

- Phase 1 双 checker 红线：compliance checker 19 规则 = M0.3 快照行零漂移（R1d=14/R2a=34/R2b=242/R2c=1542/R2d=38/R3=5/R6=2/R10=14/R12a=71/R12b=66/R12c=42，其余 0）；CJK report mode CAT1..4 = 0/0/0/0（CAT5 21158 行豁免仅统计）——与 known-good-baselines 2026-09-08 MI 终态行对账一致。
- Phase 4 seed 门禁：`mvn test -pl app-erp-all -Dtest=TestErpSeedDataIntegrity` → Tests run: 4, Failures: 0, Errors: 0, Skipped: 0，BUILD SUCCESS（2026-09-09 06:47）。
- Phase 5 切片回归：`mvn test -pl module-hr/erp-hr-service` → Tests run: 249, Failures: 0, Errors: 0, Skipped: 0，BUILD SUCCESS（= cjk-baseline §批注账 MI.6 批 2 行锚点 249 精确一致）。
- Phase 6 双 PASS：`node tools/check-hardcoded-cjk.mjs --strict` → PASS exit 0（0 new violations，170 baseline files，totals 0/0/209/1318 单向收紧）；`--self-test` → PASS。
- Phase 7 收尾回归：`mvn test -pl module-hr/erp-hr-service` 复跑 → Tests run: 249, Failures: 0, Errors: 0, Skipped: 0，BUILD SUCCESS（审计只读不变式复证）。
- Phase 7 零改动核证：`git status --porcelain` 过滤 `module-*`/`app-erp-all` 生产路径 = 空（触碰面 = 本报告 + 双索引 + 计划勾选注记 + roadmap 注记 + 当日日志，全部 docs 面）。

## Closure

- 本计划 Phase 1~7 全部执行项与退出标准 `[x]`（证据注记落各 Phase 勾选行）；五维覆盖矩阵 5/5（DIM-B finding / DIM-F pass / DIM-S pass / DIM-T finding / DIM-I pass），无跳维无跳页。
- 双索引同步在案：本轮 `ai-check-r3-index.md` §本目录产物清单追加 M1.11 行；跨轮 `docs/audits/check/ai-check-index.md` §报告清单追加 M1.11 行 + §Finding 追踪追加新立 4 条 `-r3` ID 行（归并 24 条证据落本报告 §2.2，原 ID 状态零覆写）。
- 零生产代码改动核证通过（`git status --porcelain` 过滤生产路径 = 空）；hr service 回归根绿（两轮 249/0/0/0）。
- 独立结束审计：待派发（mission driver CLOSURE_AUDIT 步骤，fresh session 子代理执行；执行者未自我审计）。
