# 待办事项

## 目的

使用此文件列出 AI 可以检查或执行的候选工作。

待办事项不是需求、owner docs 或计划的替代品。它仅有助于选择下一个切片。

## 工作项

| 优先级 | 工作项 | 路线图 | 状态 | AI 自主权 |
|--------|--------|--------|------|-----------|
| P0 | CRUD 全 18 域（Milestone 1-3） | `crud-roadmap.md` | ✅ done | `implement` |
| P1 | 核心业务循环（进销存+财务） | `core-business-roadmap.md` M1 | ✅ done | `plan-first` |
| P2 | 扩展 5 域业务逻辑 | `extended-roadmap.md` M2 | ✅ done | `plan-first` |
| P3 | 新增 8 域业务逻辑 | `extended-roadmap.md` M3 | ✅ done | `plan-first` |
| P4 | 业财一体端到端 | `core-business-roadmap.md` M4 | ✅ done | `plan-first` |
| — | 部署期演示种子数据（`_vfs/_init-data/` CSV + DataInitInitializer，解除空库阻断） | `2026-07-08-1234-1` | ✅ done | `plan-first` |
| — | 18 域 CRUD 列表/表单页面 Playwright E2E 冒烟回归套件（每域 1 代表性主单据头实体，spec 35→53） | `2026-07-08-1234-2` | ✅ done | `plan-first` |
| — | 核心业财端到端业务交易单据部署期种子（P2P+O2C 最小连通集：源单据+已过账财务产物直 seed，23 张交易 CSV；解除 1234-1 Deferred「业务交易单据种子」） | `2026-07-08-1445-1` | ✅ done | `plan-first` |
| — | 核心域（finance/sales/purchase）看板 KPI + 报表渲染数据驱动精确数值浏览器 E2E 断言（`*.value.spec.ts` 6 spec，GraphQL 取值断言 + 期望值表派生；解除 0637-1 + 1234-1 Deferred「数据驱动 KPI 精确数值断言」，spec 53→59） | `2026-07-08-1445-2` | ✅ done | `plan-first` |
| P5 | 7 扩展域 posted/businessDate 标准字段补充（cs/hr/logistics/b2b/contract/drp/aps） | `2026-07-08-0056-1` | ✅ done | `ask-first`（ORM 保护区域，经 mission-driver 显式指令授权） |
| — | 运营域（库存/资产/项目）业务交易单据 + 计算产物部署期种子（最小连通集，解除 1445-1 扩展域种子 Deferred + 为运营域数值断言提供数据基线） | `2026-07-08-2210-1` | ✅ done | `plan-first` |
| — | 运营域（库存/资产/项目）看板 KPI + 报表渲染数据驱动精确数值浏览器 E2E 断言（依赖 2210-1 种子，解除 1445-2 扩展域数值断言 Deferred） | `2026-07-08-2210-2` | ✅ done | `plan-first` |
| — | 制造域业务交易单据部署期种子（最小连通集 4 表：work_order/cost_variance/forecast/forecast_line；解除 2210-1 Deferred「其他扩展域交易种子（manufacturing）」子集；使制造域看板 4 KPI + production-variance/forecast-variance 报表数值非空；crp_load 因 workcenter 配置链依赖归 Deferred） | `2026-07-09-0930-1` | ✅ done | `plan-first` |
| — | 维护+质量域业务交易单据部署期种子（最小连通集 11 表：维护 8 表 equipment_category/equipment/schedule/request/downtime_entry/visit/visit_task/spare_part_usage + 质量 3 表 inspection/non_conformance/action；解除 2210-1 Deferred「其他扩展域交易种子（maintenance/quality）」子集；使维护+质量域看板 getDashboardKpi + 4 报表 + 3 预警数值非空；SPC 三表因 spc_chart.parameterId 配置链依赖归 Deferred） | `2026-07-09-0930-2` | ✅ done | `plan-first` |
| — | 制造/维护/质量域看板 KPI + 报表渲染数据驱动精确数值浏览器 E2E 断言（依赖 0930-1/0930-2 种子，7 `*.value.spec.ts`：3 看板 getDashboardKpi + qa getSpcOutOfControlWarning 确定性 0 + 4 报表 production-variance/forecast-variance/maintenance-history/inspection-summary；解除 2210-2 Deferred「其他扩展域看板/报表数值断言（manufacturing/quality/maintenance 子集）」） | `2026-07-09-0930-3` | ✅ done | `plan-first` |
| — | CRM/客服/人力域业务交易单据部署期种子（最小连通集 12 表 + 2 处加性追加：CRM 5 表 stage/lead/forecast_period/forecast/forecast_line + CS 3 表 ticket_type/ticket/survey + HR 4 表 department/employee/salary_simulation/simulation_item_adj + 跨域追加 md_partner EMPLOYEE 行/fin_ar_ap_item EMPLOYEE_ADVANCE·EXPENSE_CLAIM·OPEN 行；解除 2210-1 Deferred「其他扩展域交易种子（CRM/CS/HR）」子集；使三域纯报表域 5 报表 lead-conversion-funnel/forecast-accuracy/ticket-sla-csat-summary/payroll-simulation-comparison/employee-net-balance 数值非空；CRM/CS/HR 配置表 + GL 凭证归 Deferred） | `2026-07-09-1045-1` | ✅ done | `plan-first` |
| — | CRM/客服/人力域报表渲染数据驱动精确数值浏览器 E2E 断言（纯报表域无看板，5 `*.value.spec.ts`：CRM lead-conversion-funnel/forecast-accuracy + CS ticket-sla-csat + HR payroll-simulation-comparison/employee-net-balance；解除 0930-3 Deferred「其他扩展域看板/报表数值断言（CRM/CS/HR 子集）」；spec 72→77 文件） | `2026-07-09-1045-2` | ✅ done | `plan-first` |
| — | 主数据域看板 KPI + 2 报表渲染数据驱动精确数值浏览器 E2E 断言；并修复 `getDashboardKpi.vendorCount` 字典值漂移缺陷（`ErpMdDashboardBizModel` 常量 VENDOR→SUPPLIER 对齐权威字典 `erp-md/partner-type`，原致 vendorCount 永远返回 0；3 `*.value.spec.ts`：getDashboardKpi 5 字段 + 2 预警空集 + 物料价格清单/往来单位清单报表；解除 1045-2 Deferred「其他扩展域看板/报表数值断言（master-data 子集）」；完成 10 域看板 + 全报表域数值断言覆盖里程碑；spec 77→80 文件） | `2026-07-09-1145-1` | ✅ done | `plan-first` |
| — | 质量域 SPC 三表（spc_chart/spc_sample/spc_capability）最小连通种子 + `getSpcOutOfControlWarning` 数据驱动数值断言（Strategy C 完整参照完整性：spc_chart 1 行 parameterId=0 占位软引用 + spc_sample 1 行 isOutOfControl=true + spc_capability 1 行 INADEQUATE + non_conformance 追加 SPC·OPEN 行；使三计数器 outOfControlChartCount/inadequateCapabilityCount/openSpcNcrCount 由确定性 0 转非空 1/1/1；解除 0930-2 Deferred「SPC 三表 seed」+ 0930-3「确定性 0」状态；关闭质量看板最后一个预警可观测性缺口；spec 文件数不变，断言更新） | `2026-07-09-1145-2` | ✅ done | `plan-first` |
| — | 制造域工作中心配置链（workcenter/workcenter_calendar/workcenter_capacity）+ crp_load 最小连通种子，使 CRP 负荷报表（crp-load-report）由空转非空可观测并叠加数据驱动数值断言（Strategy C 完整参照：workcenter 1 行 WC-001 + 单班 08:00~16:00 ALL_WEEK calendar + efficiencyFactor=1 capacity + loadDate=2026-07-15 loadHours=4 crp_load；capacityHours=8.00 / loadRate=0.50 确定性派生；解除 0930-1 Deferred「crp_load 表 + crp-load 报表 seed」；完成全报表域数值断言覆盖里程碑 crp-load 最后一个缺口；spec 80→81） | `2026-07-09-0628-1` | ✅ done | `plan-first` |
| — | CRUD 读写两面数据驱动 E2E 验证：13 域数据驱动列表断言（`*.list-value.spec.ts` 经 GraphQL findPage 断言 seed 行数 + token）+ master-data CRUD 写路径（`master-data.write.spec.ts` create/update/delete 持久化经 mutation + 序列 warm-up 重试验证）；解除 1234-2 两 Deferred「CRUD 写操作 E2E」+「数据驱动 CRUD 列表断言」；测试数 84→99） | `2026-07-09-0628-2` | ✅ done | `plan-first` |
| — | 部署期序列推进修复 + CRUD 写路径浏览器层 E2E 扩展：新增 `_init-data/zz-sequence-advance.sql`（MERGE 创建 NOP_SYS_SEQUENCE default 行 NEXT_VALUE=100000，消除 create 首次主键碰撞）+ `_helper.ts` `runAmisFormWrite`（AMIS 表单层写）+ warm-up 重试简化为单次容错 + 3 新写 spec（master-data AMIS ErpMdPartner / quality GraphQL ErpQaRiskRegister 含 dict status / maintenance GraphQL ErpMntEquipmentCategory）；解除 0628-2 Deferred「AMIS 表单按钮/dict 下拉写路径」；测试数 99→102） | `2026-07-09-0814-1` | ✅ done | `plan-first` |
| — | 代表域业务动作浏览器层 E2E（inventory StockMove 状态机+过账流水 / CRM Lead docStatus 状态迁移 / CS Ticket 六态状态机 + 非法迁移守卫；经 GraphQL `/graphql` mutation 调自定义 `@BizMutation`，状态翻转经 `__get` 独立断言；3 新 spec + `_helper.ts` 三原语 createViaSave/callMutation/verifyState；解除 0628-2 Deferred「复杂业务动作 E2E（审批/状态机/过账触发等 BizModel 自定义动作）」；测试数 102→108） | `2026-07-09-0814-2` | ✅ done | `plan-first` |
| — | P2P/O2C 核心业财循环跨域编排链浏览器层 E2E：经 GraphQL 驱动全链 `__save`(头+行)→`submitForApproval`→`approve`，每步 `verifyState` 经 `__get` 断言 approveStatus 翻转 + 业财过账产物断言（Receive/Delivery approve→stockMove DONE；Invoice approve→posted=true + voucher bill_r 回链 + AR-AP 辅助账 PAYABLE/RECEIVABLE openAmount=含税总额/OPEN）；2 新 spec + `orchestration/_helper.ts` runP2pChain/runO2cChain + findItems/清理原语；**种子 COA 完备性修复**（erp_md_subject.csv +5 科目 1401/1403/1131/2221/6401 使过账 Provider 科目码可达，persistVoucher 不写 gl_balance 故 finance 看板基线不受影响）；**Payment/Receipt xwf 裁决**（nop 用户 submitForApproval 被 wf 步骤参与者 user:$0 拒绝→归 Deferred）；解除 0814-2 两 Deferred「跨域编排链完整 E2E」+「业财过账凭证数值断言」；测试数 108→110） | `2026-07-09-1249-1` | ✅ done | `plan-first` |
| — | 看板 AMIS 前端渲染层运行时浏览器回归（`tests/e2e/visual/dashboards.visual.spec.ts` 10 域，驱动真实 AMIS 页面→拦截 AMIS 自身 GraphQL 响应→断言 DOM，验证 page→AMIS GraphQL→adaptor→DOM 全路径，区别于数值断言层绕过前端；全 10 域渲染管线结构 + 2 非参数化域 projects/master-data 确定性数值 token；部分解除 0637-1 Deferred「像素级视觉回归」以 DOM 内容/结构断言替代像素 diff；**执行期发现 AMIS `$var` GraphQL 查询模板损坏 P1 产品缺陷**（8 参数化看板 page.yaml 手写 `query($var:Type)` 经 AMIS 模板解析裸 `$var` 被替换为空致 KPI 恒 0/空，冒烟层+数值层均漏检，本层首个捕获），记入 docs/bugs/ + Deferred successor；测试数 110→120） | `2026-07-09-1249-2` | ✅ done | `plan-first` |
| — | 看板/报表 AMIS 前端渲染管线 P1 缺陷修复（1249-2 Deferred successor）：缺陷 A `$var` 模板损坏——34 个 page.yaml 的 `dataType: raw` 手写 GraphQL 查询裸 `$var` 经 amis-core `dataMapping`→`tokenize` 替换为空，以 YAML 安全转义 `${'$'}`（amis-formula `\$` 单趟变体）修复，覆盖 Long/String/Int/BigDecimal 全参数类型（`@query:` 因 `guessType` 推断 Int/Float 无法产出 Long/BigDecimal 被否决）；缺陷 B reportContainer 接线——草案指定「镜像 balance-sheet onEvent」经运行时核实**本身损坏**（`event.data.result` 恒空 + `target:` 被忽略 + `html:""` 静态），改用经运行时验证可用的 service reload 范式（渲染 button reload form 内部 service，adaptor 拍平 HTML→`data.reportHtml`，body `html:"${reportHtml}"`），全 24 报表重构；`dashboards.visual.spec.ts` 8 参数化域升级数值 token 断言 + projects 毛利率 token（10/10 通过）；新增 `reports.visual.spec.ts` 4 张代表性报表（fin×2+crm+hr，4/4 通过）；`dashboards.md` §实现约定 增 AMIS 取数/报表容器范式；两 bug 文档标记已修复 | `2026-07-09-1728-1` | ✅ done | `plan-first` |
| — | 业务动作浏览器层 E2E 由 3 代表域扩展至 6 代表域（0814-2 Deferred「全 18 域全业务动作覆盖」successor）：新增 4 spec（maintenance ErpMntVisit 5 态状态机 schedule/start/complete/cancel + 设备联动副作用 UNDER_MAINTENANCE↔RUNNING；projects ErpPrjTask 4 态 startTask/completeTask/blockTask/unblockTask + 前驱 DAG 门控；quality ErpQaAction CAPA 3 态 startAction/completeAction/verifyAction 最小状态机；quality ErpQaNonConformance NCR 无 CAPA 路径 submitReview/escalateToRecall/cancel + 非法迁移守卫）；复用 0814-2 `_helper.ts` 三原语 createViaSave/callMutation/verifyState 验证范式在多型 DIRECT 状态机（含副作用联动/DAG 门控/过账标志）下可复用性；NCR resolve+postNcr/reverseNcr（需 RESOLVED+CAPA 门控）归 successor；审批门控域（manufacturing WorkOrder/assets Disposal/purchase-sales Return/quality Recall）归 xwf 业务动作 successor；测试数 124→131） | `2026-07-09-2004-1` | ✅ done | `plan-first` |
| — | 业财闭环方向二浏览器层 E2E（财务侧 DIRECT 红字冲销 `ErpFinVoucher__reverse` + 域监听者状态回退，M5.2 后端 1452-2 浏览器层覆盖）：2 新 reverse spec（p2p-reverse AP_INVOICE 红冲 + ErpPurInvoice 回退 / o2c-reverse AR_INVOICE 红冲 + ErpSalInvoice 回退），断言红字凭证生成 + 原凭证 isReversed=true + reversalOfVoucherId 回链 + 域单据 posted=false + approveStatus APPROVED→REJECTED（PurReversalListener/SalReversalListener）；`orchestration/_helper.ts` runP2pReverse/runO2cReverse + cleanupP2pReverse/cleanupO2cReverse；**枚举入参裁决**（businessType 经 Nop GraphQL 暴露为 String scalar，quoted 枚举名接受，reverse 返回标量 Long 无选择集）；**清理范围裁决**（reverseProcess 同 billHeadCode 写红字凭证 voucher_bill_r，既有 cleanupVoucherByBillCode 已覆盖原+红字；AR-AP 红冲为既有行 status→CANCELLED 非新增，无需扩展清理）；明确不解除 1249-1 Deferred「域审批轴 reverseApprove」（不同机制归 approval-pattern successor）；测试数 131→133） | `2026-07-09-2004-2` | ✅ done | `plan-first` |
| — | useWorkflow 审批轴浏览器层可行性探索（xwf，4 实体 Payment/Receipt/Disposal/Salary）——**权威裁决：不可行（NOT FEASIBLE）**：sysUser 兜底根因定位（`WorkflowEngineImpl.newSteps:274-283` fallback sysUser(0) 作 submit step owner）；3 条候选路径经临时探针 spec fresh-DB 实测均阻断（① wf 委托 `NopAuthUserSubstitution` nop→0 存在但 sysUser(0) 无法经浏览器层 `__save` 物化——`NopAuthUser.userId` `tagSet="seq"` 覆盖显式 "0" 为 UUID；② 浏览器层无用户身份注入/伪装 API；③ `.xwf` submit step `<assignment>` 放宽属生产审批契约变更）；后续 wf 步骤经 `WorkflowService__invokeAction` GraphQL 浏览器层可达（actorType=all）但依赖先突破 submit user:$0 阻塞；收敛 1249-1/2004-2/2004-1 同一 xwf Deferred 为单一权威裁决（e2e-runbook 增「useWorkflow 审批轴浏览器层」段）；纯探索+文档，零代码/spec 变更，测试数 133 不变） | `2026-07-09-2330-1` | ✅ done | `plan-first` |
| — | 报表 AMIS 前端渲染层 DOM 断言由 4 代表报表扩展至全 24 报表域（1249-2 Deferred「报表 AMIS 前端渲染层 DOM 断言」successor + 1728-1:182 残留风险 note 承接）：`assertReportRendered` 提取至 `visual/_helper.ts`（与 `assertDashboardRendered` 共存）；新增 20 报表 config（fin 5/mfg 3/ast 2/crm 2/hr 2/mnt 2/prj 2/qa 2/md 2/inv 1/cs 1）覆盖全路径含非平凡域参数报表表单→service 接线；**执行期发现并解决 AMIS 表单两种变量序列化问题**（value-spec 层不可见）：① 空表单字段发送 `""` 而非 null（后端按 `""` 过滤返回空数据）→ `page.route` 拦截器 `""`→null；② DatePicker 序列化日期为 Unix 时间戳（后端无法解析）→ 拦截器 10 位数字→ISO 日期 + `fillDates` 覆盖 `${NOW()}`；token 为 format-independent subject/label（数值 ≥1000 因 DOM 千分位仅在 value-spec 层断言）；解除 1249-2 Deferred + 1728-1:182 note；测试数 133→153） | `2026-07-09-2330-2` | ✅ done | `plan-first` |
| — | 审批门控 DIRECT 业务动作浏览器层 E2E（2004-1/2330-1 Deferred「useApproval DIRECT 子集」successor）：4 新 spec 覆盖 4 实体 DIRECT useApproval 审批轴 submitForApproval→approve→approveStatus 翻转 + reject 守卫 + 审批后域迁移——manufacturing WorkOrder（审批轴+checkAvailability→STOCK_RESERVED→start→IN_PROCESS→close→CLOSED，无子件 BOM 作 start 门控 enabler）、purchase ErpPurReturn（复用 runP2pChain 产 approved Receive 前置 + approve→posted=true 反向出库+PURCHASE_RETURN 过账）、sales ErpSalReturn（复用 runO2cChain 产 approved Delivery 前置 + approve→posted=true 反向入库+SALES_RETURN 过账）、quality Recall（register→submit→approve 双字段翻转 approveStatus=APPROVED+status=APPROVED→locateTargets→notifyCustomers→close，ErpInvBatch 作 locateTargets 批次门控 enabler）；approveStatus/status 翻转均经 verifyState `__get` 独立断言；posted 布尔翻转可观测；测试数 153→164） | `2026-07-10-0335-1` | ✅ done | `plan-first` |
| — | 依赖门控与剩余 DIRECT 业务动作浏览器层 E2E（2004-1 Deferred「NCR resolve 经 CAPA 闭包门控路径」+ 0335-1 Deferred「maintenance Request 子集」successor）：2 新 spec 覆盖两条 DIRECT 业务动作路径——quality NCR resolve CAPA 闭包门控（跨实体依赖门控负路径 CAPA PENDING 时 resolve 抛 ERR_NCR_RESOLVE_CAPA_NOT_COMPLETED + status 不变；CAPA 三步闭包后正路径 resolve 成功 status=RESOLVED；dispositionType=CONCESSION 干净隔离门控——RETURN/SCRAP 触发过账/退货副作用归 successor）、maintenance ErpMntRequest 自定义 status 5 态状态机（ORM use-approval tagSet 但无 approveStatus 列，自定义 status 驱动：正路径 accept→startRepair→complete + 分支 rejectRequest/cancel + 非法守卫；accept 生成响应式 visit 按 code 清理避免污染 periodVisitCount）；status 翻转均经 verifyState `__get` 独立断言；测试数 164→167） | `2026-07-10-0335-2` | ✅ done | `plan-first` |
| — | 业财过账凭证行精确数值浏览器层 E2E 断言（各域细化端到端验证：解除 0814-2/1249-1/0335-1/2004-2 四条 Deferred「业财过账凭证 subjectCode + debitAmount/creditAmount 精确数值断言」）：`orchestration/_helper.ts` 新增 `findVoucherIdByBillCode`(按 postingType NORMAL/REVERSAL 区分原/红字凭证) + `assertVoucherLines`(按 voucherId 查 ErpFinVoucherLine 逐行断言 subjectCode + dcDirection + 借贷金额) 两原语；4 链路 spec 内联断言——p2p-chain AP_INVOICE(1403 Dr50/2221 Dr6.5/2202 Cr56.5)、o2c-chain AR_INVOICE(1131 Dr113/6001 Cr100/2221 Cr13)、p2p/o2c-reverse 红字同向取负(buildReversalDraft dcDirection 不变金额取负，1403 Dr-50 等) + 原凭证行不变、pur-return PURCHASE_RETURN(2202 Dr25/1401 Cr25，头 totalAmount 显式置 25 因 Provider 读头字段非行级聚合)、sal-return SALES_RETURN(1401 Dr50/6401 Cr50，TOTAL_COST=computeTotalCost 行级聚合)；e2e-runbook 增凭证行断言子段 + Provider 期望值表；新断言全内联既有 spec，套件总数 167 不变(167→167，N=0)；纯测试+文档，零生产代码/契约/ORM 模型变更） | `2026-07-10-0704-1` | ✅ done | `plan-first` |
| — | 制造工单完整链路编排浏览器层 E2E（各域细化端到端验证：解除 0335-1 Deferred「WorkOrder 完整制造链编排 E2E」+ 0814-2 Deferred「全 18 域全业务动作覆盖」manufacturing 子集）：1 新 spec（`mfg-chain.spec.ts`）+ `orchestration/_helper.ts` `runMfgChain`/`cleanupMfg` 编排原语——经 GraphQL 驱动 WorkOrder+MaterialIssue+JobCard 三聚合根协作全链（前置备货→BOM+行→WorkOrder+OUTPUT/INPUT 行→审批轴 submit→approve→齐套校验 checkAvailability→STOCK_RESERVED→开工 start→IN_PROCESS→领料出库 MaterialIssue.confirm→OUTGOING 移动+materialCost 回写→报工 JobCard.recordWork→laborCost 回写→完工入库 reportCompletion→MANUFACTURE 入库移动+totalCost/unitCost 重算+COMPLETED）；断言跨聚合根协作产物（OUTGOING/ MANUFACTURE 移动 + materialCost/laborCost/totalCost/unitCost 回写）+ posted=false Non-Goal（完工入库 GL 过账依赖 finance 域制造过账 Provider，待 successor）；**确定性成本裁决**（组件用测试专用新建物料非种子 MAT-003，避免 WEIGHTED_AVERAGE 混合+清理抹除种子余额污染 inventory dashboard totalValue 10450 基线）；**@RequestBean 入参裁决**（JobCardWorkRecord 经 GraphQL 展平为独立标量参数非 record 包装）；e2e-runbook 增「制造链编排层」子段；实测套件 165→166（校准 runbook 预存 167 偏差）；纯测试+文档，零生产代码/契约/ORM 模型变更） | `2026-07-10-0704-2` | ✅ done | `plan-first` |
| — | 业财过账凭证行精确数值断言扩展——库存移动过账 + NCR SCRAP 过账（各域细化端到端验证：解除 0704-1 两项 Deferred「库存移动过账凭证行断言 PURCHASE_INPUT/SALES_OUTPUT」+ 0335-2 一项 Deferred「NCR SCRAP 过账凭证行断言」）：p2p-chain 增 PURCHASE_INPUT(1401 Dr50/2202 Cr50)、o2c-chain 增 SALES_OUTPUT(6401 Dr1200/1401 Cr1200) 内联断言；新建 quality-ncr-scrap-posting.action.spec.ts——NCR resolve AUTO_POST→NcrPostingDispatcher→NcrScrapAcctDocProvider→NCR_SCRAP 凭证行 Dr 6711=120/Cr 1401=120；种子 COA 补 6711 科目行；**avgCost 来源裁决**（MAT_1 种子余额行唯一，resolveStockBalance(limit 1) 确定性返回 avgCost=120，无需前置备货）；**acctSchemaId 配置 gap 修复**（webServer 增 -Derp-qua.ncr-default-acct-schema=1）；执行中修复预存启动阻断（ErpFinBudgetScenario/ErpHrLeaveRequest/ErpHrRecruitment/ErpInvLandedCost view.xml 属性违规+pricing-rule/sales-price-list 页面引用断裂）；套件 166→167；纯测试+种子+webServer 配置，零生产代码/契约/ORM 模型变更 | `2026-07-10-1800-1` | ✅ done | `plan-first` |
| — | 制造域扩展 E2E（WorkOrder 异常分支 + 部分完工 + 生产差异浏览器层，解除 0704-2 三项 Deferred）：2 新 spec——`mfg-work-order-exception.action.spec.ts`（异常分支 3 正路径 stop/resume/close/cancel + 2 非法迁移守卫 + 部分完工 willFinish 门控 6 tests）+ `mfg-variance.spec.ts`（config `erp-mfg.variance-auto-calc-enabled=true` 启用后满量完工触发 ProductionVarianceCalculator.calculateVariances 5 类差异行 + ProductionVarianceDispatcher.dispatchIfApplicable PRODUCTION_VARIANCE 凭证 6 行 Dr 1410/Cr 1411/Dr 1412/Cr 1413/Dr 1415/Cr 1414 精确数值断言 1 test）；webServer JVM args 增 config + 种子 COA 补 1410/1412/1413/1414/1415 五差异科目行；`cleanupMfg` 扩展 ErpMfgCostVariance + PRODUCTION_VARIANCE 凭证清理；`runMfgChain` 增 `MfgChainOptions` 支持测试专用成品物料隔离；套件 167→174；纯测试+种子+webServer 配置，零生产代码/契约/ORM 模型变更 | `2026-07-10-1800-2` | ✅ done | `plan-first` |
| P6 | 销售定价引擎（UC-SAL-11）：销售价格清单（头/行，客户组/物料/阶梯/期间/优先级）+ 促销规则引擎（折扣/满减/买赠/价格覆盖，可配叠加）+ 订单行取价接入价格清单 + 行级折扣字段 + 前端 CRUD 页面替换占位。erp-survey 对标：Odoo Pricelist + ERPNext Pricing Rule 均为核心内置功能，当前仅有基础取价为 gap | `2026-07-10-1100-1` | `in-progress` | `plan-first` |
| P6 | 信用冻结：出库/发票审核环节信用控制（UC-SAL-11 Non-Goal successor）：`CreditLimitChecker` 扩展 `checkCreditHold` 方法，在出库/发票审核时检查客户当前信用状况（point-in-time），复用三级策略，新增配置开关（默认关闭向后兼容）。erp-survey 对标：ERPNext Credit Limit 在 Sales Order + Sales Invoice 双点校验为标准功能 | `2026-07-10-1100-2` | ✅ done | `plan-first` |
| P7 | 到岸成本分摊（Landed Cost）：到岸成本单（头/行费用要素）+ 分摊引擎（按金额/数量/重量比例）+ 审核时更新成本层 + GL 凭证（借存货/贷应付）。解除 5 个 plan Deferred + logistics path-2 阻塞。erp-survey 对标：ERPNext Landed Cost Voucher + Metasfresh 标配 | `2026-07-10-1100-3` | ✅ done | `plan-first` |
| P7 | 预算管理（UC-FIN-11/13）：预算方案 + 明细行 + 控制日志 3 实体 + BUDGET 影子凭证范式（postingType=BUDGET，余额从 VoucherLine 聚合，不动 GlBalance）+ 预算控制 SPI（HARD/WARN/NONE 三级，采购/付款审核时校验）+ 预算对比报表 + 前置修复实际数聚合隔离 BUDGET 凭证。erp-survey 对标：iDempiere GL Budget。设计完备（budget.md 107行）但零实现，use-case 审计 🔶 | `2026-07-10-1100-4` | ✅ done | `plan-first` |
| P7 | 制造完工入库与领料 GL 过账：MANUFACTURING_RECEIPT(完工入库 Dr 存货/Cr WIP) + MANUFACTURING_ISSUE(领料 Dr WIP/Cr 存货)，形成制造业财一体成本流转闭环。解除 0704-2 posted=false Deferred。erp-survey 对标：Odoo mrp.account / ERPNext Stock Entry GL / Metasfresh 均为标配 | `2026-07-10-1100-5` | ✅ done | `plan-first` |
| P7 | 维修备件消耗 GL 过账：MAINTENANCE_ISSUE(Dr 维修费用/Cr 存货)，maintenance 域实物侧备件消耗过账。解除 1018-3 Deferred + 与 assets 域 MAINTENANCE_EXPENSE(470) 防双重扣减兼容。erp-survey 对标：ERPNext/Metasfresh 维修费用化过账标配 | `2026-07-10-1100-6` | ✅ done | `plan-first` |
| P8 | HR 休假/考勤/招聘/合同到期引擎：休假审批状态机+余额实体+排班钩子激活、考勤打卡端点+无薪假扣减、招聘状态机+HIRED→自动创建员工+合同、合同到期扫描Job+通知+续签。erp-survey 对标：Odoo/ERPNext/Axelor/AureusERP 4/4 均为核心内置。原 extended-roadmap Non-Goal 纯优先级排序（无技术约束），CRUD 骨架已就位，本期补引擎 | `2026-07-10-1100-7` | `ready` | `plan-first` |

## 就绪不变量

`ready` 意味着以下所有条件都为真：

- owner doc 路径存在且对此切片已知不过时
- `docs/context/project-context.md` 中的验证命令是真实的
- 无阻塞性未解决问题或明确标记为非阻塞
- 保护区域在 `docs/context/ai-autonomy-policy.md` 中配置

## 状态值

- `ready` - AI 可以根据自主权标签继续
- `in-progress` - 当前正在实施或计划中
- `blocked` - 在阻塞项解决之前无法继续
- `done` - 已完成并验证

## AI 自主权值

- `implement` - 可直接实现（设计文档充分，AI 自行决定实现细节）
- `plan-first` - 需先编写计划（跨域/业财打通等复杂场景）
- `ask-first` - 需人工确认（涉及 ORM 模型保护区域）

## 选择规则

当被要求在未命名任务的情况下继续时，选择优先级最高的 `ready` 项目。

如果表过时，请降级行或在实施前询问。
