# 待办事项

## 目的

使用此文件列出 AI 可以检查或执行的候选工作。

待办事项不是需求、owner docs 或计划的替代品。它仅有助于选择下一个切片。

## 工作项

| 优先级 | 工作项 | 路线图 | 状态 | AI 自主权 |
|--------|--------|--------|------|-----------|
| P0 | CRUD 全 18 域（Milestone 1-3） | `crud-roadmap.md` | ✅ done | `implement` |
| P1 | 核心业务循环（进销存+财务） | `core-business-roadmap.md` M1 | ✅ done | `plan-first` |
| P2 | 扩展 5 域业务逻辑 | `extended-roadmap.md` M2 | ✅ done | `plan-first` |
| P3 | 新增 8 域业务逻辑 | `extended-roadmap.md` M3 | ✅ done | `plan-first` |
| P4 | 业财一体端到端 | `core-business-roadmap.md` M4 | ✅ done | `plan-first` |
| — | 部署期演示种子数据（`_vfs/_init-data/` CSV + DataInitInitializer，解除空库阻断） | `2026-07-08-1234-1` | ✅ done | `plan-first` |
| — | 18 域 CRUD 列表/表单页面 Playwright E2E 冒烟回归套件（每域 1 代表性主单据头实体，spec 35→53） | `2026-07-08-1234-2` | ✅ done | `plan-first` |
| — | 核心业财端到端业务交易单据部署期种子（P2P+O2C 最小连通集：源单据+已过账财务产物直 seed，23 张交易 CSV；解除 1234-1 Deferred「业务交易单据种子」） | `2026-07-08-1445-1` | ✅ done | `plan-first` |
| — | 核心域（finance/sales/purchase）看板 KPI + 报表渲染数据驱动精确数值浏览器 E2E 断言（`*.value.spec.ts` 6 spec，GraphQL 取值断言 + 期望值表派生；解除 0637-1 + 1234-1 Deferred「数据驱动 KPI 精确数值断言」，spec 53→59） | `2026-07-08-1445-2` | ✅ done | `plan-first` |
| P5 | 7 扩展域 posted/businessDate 标准字段补充（cs/hr/logistics/b2b/contract/drp/aps） | `2026-07-08-0056-1` | ✅ done | `ask-first`（ORM 保护区域，经 mission-driver 显式指令授权） |
| — | 运营域（库存/资产/项目）业务交易单据 + 计算产物部署期种子（最小连通集，解除 1445-1 扩展域种子 Deferred + 为运营域数值断言提供数据基线） | `2026-07-08-2210-1` | ✅ done | `plan-first` |
| — | 运营域（库存/资产/项目）看板 KPI + 报表渲染数据驱动精确数值浏览器 E2E 断言（依赖 2210-1 种子，解除 1445-2 扩展域数值断言 Deferred） | `2026-07-08-2210-2` | ✅ done | `plan-first` |
| — | 制造域业务交易单据部署期种子（最小连通集 4 表：work_order/cost_variance/forecast/forecast_line；解除 2210-1 Deferred「其他扩展域交易种子（manufacturing）」子集；使制造域看板 4 KPI + production-variance/forecast-variance 报表数值非空；crp_load 因 workcenter 配置链依赖归 Deferred） | `2026-07-09-0930-1` | ✅ done | `plan-first` |
| — | 维护+质量域业务交易单据部署期种子（最小连通集 11 表：维护 8 表 equipment_category/equipment/schedule/request/downtime_entry/visit/visit_task/spare_part_usage + 质量 3 表 inspection/non_conformance/action；解除 2210-1 Deferred「其他扩展域交易种子（maintenance/quality）」子集；使维护+质量域看板 getDashboardKpi + 4 报表 + 3 预警数值非空；SPC 三表因 spc_chart.parameterId 配置链依赖归 Deferred） | `2026-07-09-0930-2` | ✅ done | `plan-first` |
| — | 制造/维护/质量域看板 KPI + 报表渲染数据驱动精确数值浏览器 E2E 断言（依赖 0930-1/0930-2 种子，7 `*.value.spec.ts`：3 看板 getDashboardKpi + qa getSpcOutOfControlWarning 确定性 0 + 4 报表 production-variance/forecast-variance/maintenance-history/inspection-summary；解除 2210-2 Deferred「其他扩展域看板/报表数值断言（manufacturing/quality/maintenance 子集）」） | `2026-07-09-0930-3` | ✅ done | `plan-first` |
| — | CRM/客服/人力域业务交易单据部署期种子（最小连通集 12 表 + 2 处加性追加：CRM 5 表 stage/lead/forecast_period/forecast/forecast_line + CS 3 表 ticket_type/ticket/survey + HR 4 表 department/employee/salary_simulation/simulation_item_adj + 跨域追加 md_partner EMPLOYEE 行/fin_ar_ap_item EMPLOYEE_ADVANCE·EXPENSE_CLAIM·OPEN 行；解除 2210-1 Deferred「其他扩展域交易种子（CRM/CS/HR）」子集；使三域纯报表域 5 报表 lead-conversion-funnel/forecast-accuracy/ticket-sla-csat-summary/payroll-simulation-comparison/employee-net-balance 数值非空；CRM/CS/HR 配置表 + GL 凭证归 Deferred） | `2026-07-09-1045-1` | ✅ done | `plan-first` |
| — | CRM/客服/人力域报表渲染数据驱动精确数值浏览器 E2E 断言（纯报表域无看板，5 `*.value.spec.ts`：CRM lead-conversion-funnel/forecast-accuracy + CS ticket-sla-csat + HR payroll-simulation-comparison/employee-net-balance；解除 0930-3 Deferred「其他扩展域看板/报表数值断言（CRM/CS/HR 子集）」；spec 72→77 文件） | `2026-07-09-1045-2` | ✅ done | `plan-first` |
| — | 主数据域看板 KPI + 2 报表渲染数据驱动精确数值浏览器 E2E 断言；并修复 `getDashboardKpi.vendorCount` 字典值漂移缺陷（`ErpMdDashboardBizModel` 常量 VENDOR→SUPPLIER 对齐权威字典 `erp-md/partner-type`，原致 vendorCount 永远返回 0；3 `*.value.spec.ts`：getDashboardKpi 5 字段 + 2 预警空集 + 物料价格清单/往来单位清单报表；解除 1045-2 Deferred「其他扩展域看板/报表数值断言（master-data 子集）」；完成 10 域看板 + 全报表域数值断言覆盖里程碑；spec 77→80 文件） | `2026-07-09-1145-1` | ✅ done | `plan-first` |
| — | 质量域 SPC 三表（spc_chart/spc_sample/spc_capability）最小连通种子 + `getSpcOutOfControlWarning` 数据驱动数值断言（Strategy C 完整参照完整性：spc_chart 1 行 parameterId=0 占位软引用 + spc_sample 1 行 isOutOfControl=true + spc_capability 1 行 INADEQUATE + non_conformance 追加 SPC·OPEN 行；使三计数器 outOfControlChartCount/inadequateCapabilityCount/openSpcNcrCount 由确定性 0 转非空 1/1/1；解除 0930-2 Deferred「SPC 三表 seed」+ 0930-3「确定性 0」状态；关闭质量看板最后一个预警可观测性缺口；spec 文件数不变，断言更新） | `2026-07-09-1145-2` | ✅ done | `plan-first` |
| — | 制造域工作中心配置链（workcenter/workcenter_calendar/workcenter_capacity）+ crp_load 最小连通种子，使 CRP 负荷报表（crp-load-report）由空转非空可观测并叠加数据驱动数值断言（Strategy C 完整参照：workcenter 1 行 WC-001 + 单班 08:00~16:00 ALL_WEEK calendar + efficiencyFactor=1 capacity + loadDate=2026-07-15 loadHours=4 crp_load；capacityHours=8.00 / loadRate=0.50 确定性派生；解除 0930-1 Deferred「crp_load 表 + crp-load 报表 seed」；完成全报表域数值断言覆盖里程碑 crp-load 最后一个缺口；spec 80→81） | `2026-07-09-0628-1` | ✅ done | `plan-first` |
| — | CRUD 读写两面数据驱动 E2E 验证：13 域数据驱动列表断言（`*.list-value.spec.ts` 经 GraphQL findPage 断言 seed 行数 + token）+ master-data CRUD 写路径（`master-data.write.spec.ts` create/update/delete 持久化经 mutation + 序列 warm-up 重试验证）；解除 1234-2 两 Deferred「CRUD 写操作 E2E」+「数据驱动 CRUD 列表断言」；测试数 84→99） | `2026-07-09-0628-2` | ✅ done | `plan-first` |
| — | 部署期序列推进修复 + CRUD 写路径浏览器层 E2E 扩展：新增 `_init-data/zz-sequence-advance.sql`（MERGE 创建 NOP_SYS_SEQUENCE default 行 NEXT_VALUE=100000，消除 create 首次主键碰撞）+ `_helper.ts` `runAmisFormWrite`（AMIS 表单层写）+ warm-up 重试简化为单次容错 + 3 新写 spec（master-data AMIS ErpMdPartner / quality GraphQL ErpQaRiskRegister 含 dict status / maintenance GraphQL ErpMntEquipmentCategory）；解除 0628-2 Deferred「AMIS 表单按钮/dict 下拉写路径」；测试数 99→102） | `2026-07-09-0814-1` | ✅ done | `plan-first` |
| — | 代表域业务动作浏览器层 E2E（inventory StockMove 状态机+过账流水 / CRM Lead docStatus 状态迁移 / CS Ticket 六态状态机 + 非法迁移守卫；经 GraphQL `/graphql` mutation 调自定义 `@BizMutation`，状态翻转经 `__get` 独立断言；3 新 spec + `_helper.ts` 三原语 createViaSave/callMutation/verifyState；解除 0628-2 Deferred「复杂业务动作 E2E（审批/状态机/过账触发等 BizModel 自定义动作）」；测试数 102→108） | `2026-07-09-0814-2` | ✅ done | `plan-first` |
| — | P2P/O2C 核心业财循环跨域编排链浏览器层 E2E：经 GraphQL 驱动全链 `__save`(头+行)→`submitForApproval`→`approve`，每步 `verifyState` 经 `__get` 断言 approveStatus 翻转 + 业财过账产物断言（Receive/Delivery approve→stockMove DONE；Invoice approve→posted=true + voucher bill_r 回链 + AR-AP 辅助账 PAYABLE/RECEIVABLE openAmount=含税总额/OPEN）；2 新 spec + `orchestration/_helper.ts` runP2pChain/runO2cChain + findItems/清理原语；**种子 COA 完备性修复**（erp_md_subject.csv +5 科目 1401/1403/1131/2221/6401 使过账 Provider 科目码可达，persistVoucher 不写 gl_balance 故 finance 看板基线不受影响）；**Payment/Receipt xwf 裁决**（nop 用户 submitForApproval 被 wf 步骤参与者 user:$0 拒绝→归 Deferred）；解除 0814-2 两 Deferred「跨域编排链完整 E2E」+「业财过账凭证数值断言」；测试数 108→110） | `2026-07-09-1249-1` | ✅ done | `plan-first` |
| — | 看板 AMIS 前端渲染层运行时浏览器回归（`tests/e2e/visual/dashboards.visual.spec.ts` 10 域，驱动真实 AMIS 页面→拦截 AMIS 自身 GraphQL 响应→断言 DOM，验证 page→AMIS GraphQL→adaptor→DOM 全路径，区别于数值断言层绕过前端；全 10 域渲染管线结构 + 2 非参数化域 projects/master-data 确定性数值 token；部分解除 0637-1 Deferred「像素级视觉回归」以 DOM 内容/结构断言替代像素 diff；**执行期发现 AMIS `$var` GraphQL 查询模板损坏 P1 产品缺陷**（8 参数化看板 page.yaml 手写 `query($var:Type)` 经 AMIS 模板解析裸 `$var` 被替换为空致 KPI 恒 0/空，冒烟层+数值层均漏检，本层首个捕获），记入 docs/bugs/ + Deferred successor；测试数 110→120） | `2026-07-09-1249-2` | ✅ done | `plan-first` |
| — | 看板/报表 AMIS 前端渲染管线 P1 缺陷修复（1249-2 Deferred successor）：缺陷 A `$var` 模板损坏——34 个 page.yaml 的 `dataType: raw` 手写 GraphQL 查询裸 `$var` 经 amis-core `dataMapping`→`tokenize` 替换为空，以 YAML 安全转义 `${'$'}`（amis-formula `\$` 单趟变体）修复，覆盖 Long/String/Int/BigDecimal 全参数类型（`@query:` 因 `guessType` 推断 Int/Float 无法产出 Long/BigDecimal 被否决）；缺陷 B reportContainer 接线——草案指定「镜像 balance-sheet onEvent」经运行时核实**本身损坏**（`event.data.result` 恒空 + `target:` 被忽略 + `html:""` 静态），改用经运行时验证可用的 service reload 范式（渲染 button reload form 内部 service，adaptor 拍平 HTML→`data.reportHtml`，body `html:"${reportHtml}"`），全 24 报表重构；`dashboards.visual.spec.ts` 8 参数化域升级数值 token 断言 + projects 毛利率 token（10/10 通过）；新增 `reports.visual.spec.ts` 4 张代表性报表（fin×2+crm+hr，4/4 通过）；`dashboards.md` §实现约定 增 AMIS 取数/报表容器范式；两 bug 文档标记已修复 | `2026-07-09-1728-1` | ✅ done | `plan-first` |
| — | 业务动作浏览器层 E2E 由 3 代表域扩展至 6 代表域（0814-2 Deferred「全 18 域全业务动作覆盖」successor）：新增 4 spec（maintenance ErpMntVisit 5 态状态机 schedule/start/complete/cancel + 设备联动副作用 UNDER_MAINTENANCE↔RUNNING；projects ErpPrjTask 4 态 startTask/completeTask/blockTask/unblockTask + 前驱 DAG 门控；quality ErpQaAction CAPA 3 态 startAction/completeAction/verifyAction 最小状态机；quality ErpQaNonConformance NCR 无 CAPA 路径 submitReview/escalateToRecall/cancel + 非法迁移守卫）；复用 0814-2 `_helper.ts` 三原语 createViaSave/callMutation/verifyState 验证范式在多型 DIRECT 状态机（含副作用联动/DAG 门控/过账标志）下可复用性；NCR resolve+postNcr/reverseNcr（需 RESOLVED+CAPA 门控）归 successor；审批门控域（manufacturing WorkOrder/assets Disposal/purchase-sales Return/quality Recall）归 xwf 业务动作 successor；测试数 124→131） | `2026-07-09-2004-1` | ✅ done | `plan-first` |
| — | 业财闭环方向二浏览器层 E2E（财务侧 DIRECT 红字冲销 `ErpFinVoucher__reverse` + 域监听者状态回退，M5.2 后端 1452-2 浏览器层覆盖）：2 新 reverse spec（p2p-reverse AP_INVOICE 红冲 + ErpPurInvoice 回退 / o2c-reverse AR_INVOICE 红冲 + ErpSalInvoice 回退），断言红字凭证生成 + 原凭证 isReversed=true + reversalOfVoucherId 回链 + 域单据 posted=false + approveStatus APPROVED→REJECTED（PurReversalListener/SalReversalListener）；`orchestration/_helper.ts` runP2pReverse/runO2cReverse + cleanupP2pReverse/cleanupO2cReverse；**枚举入参裁决**（businessType 经 Nop GraphQL 暴露为 String scalar，quoted 枚举名接受，reverse 返回标量 Long 无选择集）；**清理范围裁决**（reverseProcess 同 billHeadCode 写红字凭证 voucher_bill_r，既有 cleanupVoucherByBillCode 已覆盖原+红字；AR-AP 红冲为既有行 status→CANCELLED 非新增，无需扩展清理）；明确不解除 1249-1 Deferred「域审批轴 reverseApprove」（不同机制归 approval-pattern successor）；测试数 131→133） | `2026-07-09-2004-2` | ✅ done | `plan-first` |
| — | useWorkflow 审批轴浏览器层可行性探索（xwf，4 实体 Payment/Receipt/Disposal/Salary）——**权威裁决：不可行（NOT FEASIBLE）**：sysUser 兜底根因定位（`WorkflowEngineImpl.newSteps:274-283` fallback sysUser(0) 作 submit step owner）；3 条候选路径经临时探针 spec fresh-DB 实测均阻断（① wf 委托 `NopAuthUserSubstitution` nop→0 存在但 sysUser(0) 无法经浏览器层 `__save` 物化——`NopAuthUser.userId` `tagSet="seq"` 覆盖显式 "0" 为 UUID；② 浏览器层无用户身份注入/伪装 API；③ `.xwf` submit step `<assignment>` 放宽属生产审批契约变更）；后续 wf 步骤经 `WorkflowService__invokeAction` GraphQL 浏览器层可达（actorType=all）但依赖先突破 submit user:$0 阻塞；收敛 1249-1/2004-2/2004-1 同一 xwf Deferred 为单一权威裁决（e2e-runbook 增「useWorkflow 审批轴浏览器层」段）；纯探索+文档，零代码/spec 变更，测试数 133 不变） | `2026-07-09-2330-1` | ✅ done | `plan-first` |
| — | 报表 AMIS 前端渲染层 DOM 断言由 4 代表报表扩展至全 24 报表域（1249-2 Deferred「报表 AMIS 前端渲染层 DOM 断言」successor + 1728-1:182 残留风险 note 承接）：`assertReportRendered` 提取至 `visual/_helper.ts`（与 `assertDashboardRendered` 共存）；新增 20 报表 config（fin 5/mfg 3/ast 2/crm 2/hr 2/mnt 2/prj 2/qa 2/md 2/inv 1/cs 1）覆盖全路径含非平凡域参数报表表单→service 接线；**执行期发现并解决 AMIS 表单两种变量序列化问题**（value-spec 层不可见）：① 空表单字段发送 `""` 而非 null（后端按 `""` 过滤返回空数据）→ `page.route` 拦截器 `""`→null；② DatePicker 序列化日期为 Unix 时间戳（后端无法解析）→ 拦截器 10 位数字→ISO 日期 + `fillDates` 覆盖 `${NOW()}`；token 为 format-independent subject/label（数值 ≥1000 因 DOM 千分位仅在 value-spec 层断言）；解除 1249-2 Deferred + 1728-1:182 note；测试数 133→153） | `2026-07-09-2330-2` | ✅ done | `plan-first` |
| — | 审批门控 DIRECT 业务动作浏览器层 E2E（2004-1/2330-1 Deferred「useApproval DIRECT 子集」successor）：4 新 spec 覆盖 4 实体 DIRECT useApproval 审批轴 submitForApproval→approve→approveStatus 翻转 + reject 守卫 + 审批后域迁移——manufacturing WorkOrder（审批轴+checkAvailability→STOCK_RESERVED→start→IN_PROCESS→close→CLOSED，无子件 BOM 作 start 门控 enabler）、purchase ErpPurReturn（复用 runP2pChain 产 approved Receive 前置 + approve→posted=true 反向出库+PURCHASE_RETURN 过账）、sales ErpSalReturn（复用 runO2cChain 产 approved Delivery 前置 + approve→posted=true 反向入库+SALES_RETURN 过账）、quality Recall（register→submit→approve 双字段翻转 approveStatus=APPROVED+status=APPROVED→locateTargets→notifyCustomers→close，ErpInvBatch 作 locateTargets 批次门控 enabler）；approveStatus/status 翻转均经 verifyState `__get` 独立断言；posted 布尔翻转可观测；测试数 153→164） | `2026-07-10-0335-1` | ✅ done | `plan-first` |
| — | 依赖门控与剩余 DIRECT 业务动作浏览器层 E2E（2004-1 Deferred「NCR resolve 经 CAPA 闭包门控路径」+ 0335-1 Deferred「maintenance Request 子集」successor）：2 新 spec 覆盖两条 DIRECT 业务动作路径——quality NCR resolve CAPA 闭包门控（跨实体依赖门控负路径 CAPA PENDING 时 resolve 抛 ERR_NCR_RESOLVE_CAPA_NOT_COMPLETED + status 不变；CAPA 三步闭包后正路径 resolve 成功 status=RESOLVED；dispositionType=CONCESSION 干净隔离门控——RETURN/SCRAP 触发过账/退货副作用归 successor）、maintenance ErpMntRequest 自定义 status 5 态状态机（ORM use-approval tagSet 但无 approveStatus 列，自定义 status 驱动：正路径 accept→startRepair→complete + 分支 rejectRequest/cancel + 非法守卫；accept 生成响应式 visit 按 code 清理避免污染 periodVisitCount）；status 翻转均经 verifyState `__get` 独立断言；测试数 164→167） | `2026-07-10-0335-2` | ✅ done | `plan-first` |
| — | 业财过账凭证行精确数值浏览器层 E2E 断言（各域细化端到端验证：解除 0814-2/1249-1/0335-1/2004-2 四条 Deferred「业财过账凭证 subjectCode + debitAmount/creditAmount 精确数值断言」）：`orchestration/_helper.ts` 新增 `findVoucherIdByBillCode`(按 postingType NORMAL/REVERSAL 区分原/红字凭证) + `assertVoucherLines`(按 voucherId 查 ErpFinVoucherLine 逐行断言 subjectCode + dcDirection + 借贷金额) 两原语；4 链路 spec 内联断言——p2p-chain AP_INVOICE(1403 Dr50/2221 Dr6.5/2202 Cr56.5)、o2c-chain AR_INVOICE(1131 Dr113/6001 Cr100/2221 Cr13)、p2p/o2c-reverse 红字同向取负(buildReversalDraft dcDirection 不变金额取负，1403 Dr-50 等) + 原凭证行不变、pur-return PURCHASE_RETURN(2202 Dr25/1401 Cr25，头 totalAmount 显式置 25 因 Provider 读头字段非行级聚合)、sal-return SALES_RETURN(1401 Dr50/6401 Cr50，TOTAL_COST=computeTotalCost 行级聚合)；e2e-runbook 增凭证行断言子段 + Provider 期望值表；新断言全内联既有 spec，套件总数 167 不变(167→167，N=0)；纯测试+文档，零生产代码/契约/ORM 模型变更） | `2026-07-10-0704-1` | ✅ done | `plan-first` |
| — | 制造工单完整链路编排浏览器层 E2E（各域细化端到端验证：解除 0335-1 Deferred「WorkOrder 完整制造链编排 E2E」+ 0814-2 Deferred「全 18 域全业务动作覆盖」manufacturing 子集）：1 新 spec（`mfg-chain.spec.ts`）+ `orchestration/_helper.ts` `runMfgChain`/`cleanupMfg` 编排原语——经 GraphQL 驱动 WorkOrder+MaterialIssue+JobCard 三聚合根协作全链（前置备货→BOM+行→WorkOrder+OUTPUT/INPUT 行→审批轴 submit→approve→齐套校验 checkAvailability→STOCK_RESERVED→开工 start→IN_PROCESS→领料出库 MaterialIssue.confirm→OUTGOING 移动+materialCost 回写→报工 JobCard.recordWork→laborCost 回写→完工入库 reportCompletion→MANUFACTURE 入库移动+totalCost/unitCost 重算+COMPLETED）；断言跨聚合根协作产物（OUTGOING/ MANUFACTURE 移动 + materialCost/laborCost/totalCost/unitCost 回写）+ posted=false Non-Goal（完工入库 GL 过账依赖 finance 域制造过账 Provider，待 successor）；**确定性成本裁决**（组件用测试专用新建物料非种子 MAT-003，避免 WEIGHTED_AVERAGE 混合+清理抹除种子余额污染 inventory dashboard totalValue 10450 基线）；**@RequestBean 入参裁决**（JobCardWorkRecord 经 GraphQL 展平为独立标量参数非 record 包装）；e2e-runbook 增「制造链编排层」子段；实测套件 165→166（校准 runbook 预存 167 偏差）；纯测试+文档，零生产代码/契约/ORM 模型变更） | `2026-07-10-0704-2` | ✅ done | `plan-first` |
| — | 业财过账凭证行精确数值断言扩展——库存移动过账 + NCR SCRAP 过账（各域细化端到端验证：解除 0704-1 两项 Deferred「库存移动过账凭证行断言 PURCHASE_INPUT/SALES_OUTPUT」+ 0335-2 一项 Deferred「NCR SCRAP 过账凭证行断言」）：p2p-chain 增 PURCHASE_INPUT(1401 Dr50/2202 Cr50)、o2c-chain 增 SALES_OUTPUT(6401 Dr1200/1401 Cr1200) 内联断言；新建 quality-ncr-scrap-posting.action.spec.ts——NCR resolve AUTO_POST→NcrPostingDispatcher→NcrScrapAcctDocProvider→NCR_SCRAP 凭证行 Dr 6711=120/Cr 1401=120；种子 COA 补 6711 科目行；**avgCost 来源裁决**（MAT_1 种子余额行唯一，resolveStockBalance(limit 1) 确定性返回 avgCost=120，无需前置备货）；**acctSchemaId 配置 gap 修复**（webServer 增 -Derp-qua.ncr-default-acct-schema=1）；执行中修复预存启动阻断（ErpFinBudgetScenario/ErpHrLeaveRequest/ErpHrRecruitment/ErpInvLandedCost view.xml 属性违规+pricing-rule/sales-price-list 页面引用断裂）；套件 166→167；纯测试+种子+webServer 配置，零生产代码/契约/ORM 模型变更 | `2026-07-10-1800-1` | ✅ done | `plan-first` |
| — | 制造域扩展 E2E（WorkOrder 异常分支 + 部分完工 + 生产差异浏览器层，解除 0704-2 三项 Deferred）：2 新 spec——`mfg-work-order-exception.action.spec.ts`（异常分支 3 正路径 stop/resume/close/cancel + 2 非法迁移守卫 + 部分完工 willFinish 门控 6 tests）+ `mfg-variance.spec.ts`（config `erp-mfg.variance-auto-calc-enabled=true` 启用后满量完工触发 ProductionVarianceCalculator.calculateVariances 5 类差异行 + ProductionVarianceDispatcher.dispatchIfApplicable PRODUCTION_VARIANCE 凭证 6 行 Dr 1410/Cr 1411/Dr 1412/Cr 1413/Dr 1415/Cr 1414 精确数值断言 1 test）；webServer JVM args 增 config + 种子 COA 补 1410/1412/1413/1414/1415 五差异科目行；`cleanupMfg` 扩展 ErpMfgCostVariance + PRODUCTION_VARIANCE 凭证清理；`runMfgChain` 增 `MfgChainOptions` 支持测试专用成品物料隔离；套件 167→174；纯测试+种子+webServer 配置，零生产代码/契约/ORM 模型变更 | `2026-07-10-1800-2` | ✅ done | `plan-first` |
| — | 制造域 config-gated 浏览器层 E2E（解除 1800-2 两项 Non-Goals + 0704-2 两项 Deferred「质检门控阻断完工 + 批次基因追溯浏览器层」）：2 新 spec——`mfg-genealogy.spec.ts`（`runMfgChain({ withBatchTracking: true })` 创建输入 ErpInvBatch + 备货/领料移动携 batchNo 使余额按批次维度落入 + MaterialIssueLine.batchNo → 满量完工触发 BatchGenealogyWriter 写入 inputLot→outputLot 基因链 + forwardTrace/backwardTrace 返回非空消耗关系 1 test）+ `mfg-inspection-gate.spec.ts`（config `erp-mfg.inspection-gate-enabled=true` + BOM.inspectionRequired=true → reportCompletion 被 Gate 1 拦截抛 ERR_INSPECTION_REQUIRED + docStatus 保持 IN_PROCESS 负路径 + inspectionRequired=false 对照路径 COMPLETED 2 tests）；`runMfgChain` 扩展 `withBatchTracking`/`inspectionRequired` 两可选参数 + `callMutation` 导出 + `cleanupMfg` 扩展基因链产物清理；webServer JVM args 增 `-Derp-mfg.inspection-gate-enabled=true`；套件 174→177；纯测试+config+helper 扩展，零生产代码/契约/ORM 模型变更 | `2026-07-11-0730-1` | ✅ done | `plan-first` |
| — | 质量 NCR 过账生命周期浏览器层 E2E 扩展（解除 1800-1 Deferred「NCR reverseNcr 红字凭证行断言」+ 0335-2 Deferred「NCR RETURN 处置退货编排 successor」RETURN 子集）：2 新 spec——`quality-ncr-reverse-voucher-line.action.spec.ts`（resolve AUTO_POST 产 NCR_SCRAP 正向凭证 → reverseNcr 红冲 → 红字凭证行同向取负断言 Dr 6711=-120/Cr 1401=-120 + 原正向凭证 isReversed=true 凭证级回退标记，区别于 1800-1 仅断言 ncr.posted=false 实体级回退）+ `quality-ncr-return-disposition.action.spec.ts`（resolve RETURN+supplierId 非空 → NcrReturnOrchestrator.orchestrateReturn → IErpPurReturnBiz.save 创建采购退货单 code=PR-FROM-NCR-{ncrId} + ncr.returnCode 登记 + ErpPurReturn supplierId/docStatus/approveStatus 验证；CONCESSION 对照 returnCode=null 无退货副作用）；无 CAPA 简化 setup 对齐 0335-2 无 CAPA 路径；套件 177→180（business-actions 44 全绿含新增 3 test，全 workspace 视觉/报表/crud/看板 AMIS 渲染超时为预存环境问题非回归）；纯测试，零生产代码/契约/ORM 模型变更 | `2026-07-11-0730-2` | ✅ done | `plan-first` |
| — | 剩余 DIRECT 业务动作 E2E 收尾（解除 0335-2 两项显式 Deferred「maintenance Request→Visit 编排」+「finance voucher 手工 post」）：2 新 spec——`mnt-request-visit-orchestration.action.spec.ts`（accept → generateResponsiveVisit 创建响应式 DRAFT Visit 6 字段精确断言 code/equipmentId/visitDate/visitType/status/assignedTo；visitDate 取自同事务 createTime 日期部分规避 JVM/客户端时区跨日；`_helper.ts` 新增 `findFirst` 原语）+ `finance-voucher-post.action.spec.ts`（Explore 探针选定 LANDED_COST(490) 最简 PostingEvent billData=ALLOCATIONS+COST_ELEMENTS → `ErpFinVoucher__post` 原始 mutation Long scalar return → 凭证头 voucherType/postingType/docStatus + 业财回链 ErpFinVoucherBillR + 凭证行 Dr 1401/Cr 2202 + 幂等路径重复 post 返回 null；businessType 枚举 String scalar quoted 对齐 reverse 裁决）；套件 184→186；纯测试+helper 扩展，零生产代码/契约/ORM 模型变更 | `2026-07-11-2329-2` | ✅ done | `plan-first` |
| — | 报表下载产物（XLSX/PDF）运行时浏览器层回归（六处 Deferred 承接：1728-1/2330-2/1249-2/0930-3/1045-2/1145-1 同一「报表下载产物字节级 diff」optimization candidate 触发条件裁定已满足）：1 新 spec `reports.download.spec.ts`（24 报表 × {xlsx,pdf} = 48 用例）+ `reports/_helper.ts` 新增 `assertReportDownload` 原语——**Phase 1 Explore 裁定**（实测 `/graphql` 端点对 WebContentBean 返回 JSON 序列化不产二进制流，选定 `/p/{operationName}` page-query 二进制下载专路，两路径调同一 `__download` @BizQuery 后端逻辑一致）；**文本提取实现**（XLSX 解析 zip central directory 取真实压缩大小 + 提取 worksheet inline strings；PDF 解压 FlateDecode + ToUnicode bfrange/bfchar CMap 解码 + CJK Radical U+2F00–U+2FDF 8 映射归一化使子集字体映射与标准 CJK token 对齐）；断言三维度（非空 + 魔数 PK/%PDF + 弱结构 token）；回归捕获能力经实测证明（错误 reportName/renderType 产 JSON 错误体→魔数断言红）；残留 successor：page.yaml 下载按钮 URL `/graphql`→`/p/` 修复属生产代码变更；e2e-runbook 增「报表下载产物运行时回归层」段 + 套件计数 186→234；纯测试+文档，零生产代码/契约/ORM 模型变更 | `2026-07-12-0204-1` | ✅ done | `plan-first` |
| — | Finance AR/AP 核销单生命周期浏览器层 E2E（承接 1249-1 Deferred「域级 settle 核销」段——xwf 段经 2330-1 裁决不可行，本计划仅承接 DIRECT 可达的 `ErpFinReconciliation` 核销段）：1 新 spec `fin-reconciliation.action.spec.ts`（7 用例）——DIRECT `@BizMutation` 核销生命周期 create→post→reverse（不经 xwf）+ 双面对账 `checkDualSideConsistency @BizQuery` 返回 DualSideDiffReport 结构 + 5 validateLine 守卫负路径（direction/partner/item-not-open/over-amount/date-before-invoice 各 ErrorCode message token）；自包含建 OPEN 对（新建 partner `E2E-RECON-PN-` + 2 行 ErpFinArApItem：AP_INVOICE + PAYMENT 同 partner+direction+金额）+ cleanup 删 partner 使 PartnerBalanceUpdater 写入余额字段消失（finance 看板 KPI 1130/1130 + ar-ap-aging 基线实测无漂移）；**Phase 1 Decision**（lines 顶参经 `input('[i_app_erp_fin_dao_dto_ReconciliationLineInput]', [...])` typed variable，对齐 StockMoveRequest 命名先例；既有 createViaSave/verifyState 充分无需改 `_helper.ts`，setup 原语以本地函数入 spec）；e2e-runbook 业务动作表 +finance 核销行 + 套件计数 234→241；纯测试+文档，零生产代码/契约/ORM 模型变更 | `2026-07-12-0204-2` | ✅ done | `plan-first` |
| — | 报表 AMIS 下载按钮 URL 契约漂移修复（承接 0204-1 successor「page.yaml 下载按钮 URL `/graphql`→`/p/` 修复属生产代码变更」——触发条件已满足）：全 24 报表 page.yaml 的 48 下载按钮实例 `api.url` 由失效的 `/graphql`（该端点对 WebContentBean 返回 JSON 序列化不产二进制流→AMIS 按钮完全不可用）修复为 `/p/{ErpXxxReport__download}` 二进制下载专路 + REST 风格入参 map（移除 raw GraphQL query/variables 包装）；1 新 spec `reports.amis-download.spec.ts`（5 报表抽样 × {xlsx,pdf} = 10 用例，覆盖三类参数形态：参数化/零参/字符串参）+ `reports/_helper.ts` 新增 `assertAmisDownloadButton` 原语（驱动真实 AMIS 按钮 → `waitForEvent('download')` 落盘文件断言非空+魔数+token + `/p/` 请求层 URL 守卫 + `page.route` 归一化 AMIS 日期序列化）；**Phase 1 Decision**（实测裁定 `actionType: download`+`api{url:/p/..,data:{reportName,renderType,data}}` 结构可行，AMIS 发 JSON body 与直调层字节级一致；关键发现 Playwright `response.body()` 对 blob 消费响应返回 0 字节，字节源改取 download 事件落盘文件）；e2e-runbook「下载功能」已知限制段更新 + 新增「报表 AMIS 下载按钮回归层」段 + 套件计数 241→251；生产代码变更仅 24 page.yaml（下载按钮 URL+结构），零后端/契约/ORM 模型变更 | `2026-07-12-0413-1` | ✅ done | `plan-first` |
| — | Finance 银行对账 + 坏账业务动作浏览器层 E2E（解除 0204-2 两项 Deferred「银行对账 `ErpFinBankReconciliation` 浏览器层 E2E」+「坏账 `ErpFinBadDebt` 浏览器层 E2E」，触发条件=推进 finance 域对应业务动作 E2E 时已满足）：2 新 spec——`fin-bank-recon.action.spec.ts`（3 用例：DIRECT `@BizMutation` 银行对账三态状态机 generate→post→reverse，docStatus DRAFT→POSTED→CANCELLED 复用 erp-fin/voucher-status 红冲回退=CANCELLED 非 REVERSED；自包含建 ErpFinFundAccount(BANK,subjectId=1002)+ErpFinBankStatement+Line（1 行 UNMATCHED CREDIT 100 满足平衡等式 statementBalance(1100)−bookBalance(1000)=bankCreditUnrecorded(100)−bankDebitUnrecorded(0)）→ post 产 BANK_RECON_ADJ 调整凭证（Dr 1002/Cr 2240OTHER 各 100，经 ErpFinVoucherBillR billCode=recon.code 反查）+ reverse 红冲 REVERSAL 凭证 + 2 非法迁移守卫）+ `fin-bad-debt.action.spec.ts`（3 用例：writeOff→submit→approve DIRECT 审批轴 UNSUBMITTED→SUBMITTED→APPROVED + approve 触发 executeWriteOff ArApItem OPEN→WRITTEN_OFF + BAD_DEBT_WRITE_OFF 凭证（借Allowance/贷AR 不进 P&L，CloseVoucherWriter 直接持久化）；recover→submit→approve 收回路径 WRITTEN_OFF→OPEN 恢复 + BAD_DEBT_RECOVERY 凭证；runBadDebtProvision 期末计提 BadDebtProvisionResult 结构非空 action ∈ {RESERVE,RELEASE,NONE}）；**科目依赖裁决**（按 1800-1 范式种子 erp_md_subject.csv 补 3 行 1231 坏账准备/6701 信用减值损失/2240OTHER 未达调整 + webServer JVM args 补 3 项 bad-debt-allowance/expense/ar-subject-code，requireSubject config 默认 null 未配置抛 ERR_CLOSE_SUBJECT_NOT_CONFIGURED）；e2e-runbook 业务动作表 +2 finance 行 + webServer JVM 参数段 + 种子 COA 段 + 套件计数 251→257；纯测试+种子+config，零后端/契约/ORM 模型变更 | `2026-07-12-0413-2` | ✅ done | `plan-first` |
| — | Finance 凭证行精确数值断言 + 自动核销浏览器层 E2E（解除 0413-2 两项 Deferred「坏账核销/计提凭证行精确数值断言」+「runAutoReconciliation 三策略浏览器层 E2E」）：Phase 1 叠加凭证行数值断言——`fin-bank-recon` NORMAL Dr 1002/Cr 2240OTHER 各 100 + REVERSAL 同向取负 + `fin-bad-debt` writeOff Dr 1231/Cr 1122 + recovery Dr 1122/Cr 1231 + provision RESERVE Dr 6701/Cr 1231（金额动态计算=requiredProvision−allowanceBalance）；Phase 2 新建 `fin-auto-recon.action.spec.ts` 3 策略用例（FIFO/BY_AMOUNT/BY_RATIO，自包含 partner+OPEN AR 对→runAutoReconciliation→核销单自动创建+posted+双方辅助账 SETTLED）+ webServer JVM arg 追加 `-Derp-fin.auto-reconcile=true`；**后端 flush 修复**（`ErpFinReconciliationBizModel.runAutoReconciliation` 在 create→post 之间缺 `orm().flushSession()`，loadLines SQL 看不到未 flush 的 INSERT→ERR_RECONCILIATION_NOT_FOUND，镜像既有 flushBeforeBalance 范式补一行）；套件 257→260（+3 auto-recon）；playwright webServer JVM arg 增 auto-reconcile | `2026-07-12-1321-2` | ✅ done | `plan-first` |
| — | 日期参数报表在真实用户面下载/渲染失效的生产修复（承接 0413-1 Deferred「日期参数报表空日期/默认日期下载真实可用时」——触发条件已满足）：12 日期参数报表（crp-load/production-variance/forecast-variance/asset-depreciation/asset-disposal/downtime-summary/maintenance-history/project-cost-summary/timesheet-detail/inspection-summary/ncr-capa-summary/ar-ap-aging）经 AMIS `input-date` 发裸 Unix 时间戳/空串 → 后端 strict-parse `DateTimeParseException` → 下载/渲染失效。**Phase 1**：8 报表 BizModel（fin/mfg/ast/mnt/prj/qa 的 `asDate`+`asLong` + inv `asLong` + md `asString`）宽容解析落地（空串→null / 10·13 位时间戳→`LocalDate` / ISO→`LocalDate`）；**Phase 2**：12 报表 page.yaml `input-date` 追加 `valueFormat: "YYYY-MM-DD"`（AMIS hygiene）+ 移除测试层 `page.route` 日期归一化 workaround（`reports/_helper.ts` + `visual/_helper.ts`）；12 日期参数 + 12 非日期参数报表在真实 AMIS 序列化下下载/渲染全绿（82 passed）；e2e-runbook「下载功能」已知限制段移除日期序列化缺陷免责声明；生产代码变更=8 BizModel Java + 12 page.yaml，零 ORM/契约变更 | `2026-07-12-1321-3` | ✅ done | `plan-first` |
| — | contract + drp 域 DIRECT 业务动作浏览器层 E2E（承接 2004-1 Deferred「全 18 域全业务动作覆盖（DIRECT 域剩余）」——触发条件=各域细化端到端验证时已满足；两域均无 useWorkflow/useApproval，纯 DIRECT @BizMutation）：4 新 spec（9 用例）——`ct-contract-lifecycle`（合同 6 动作状态机 activate→suspend→resume→terminate + expire/amend 路径 + DRAFT/TERMINATED→activate 非法守卫，经 `__save` 置 NEGOTIATION 入口）、`ct-contract-version`（finalizeVersion→signVersion 状态翻转 + isCurrent 翻转 + SIGNED→finalize 非法守卫）、`drp-plan-engine`（runDrp DRAFT→COMPUTED+DrpLine 行生成 → resetToDraft 行清理 → 重算 → approvePlan COMPUTED→APPROVED + DRAFT→approve 非法守卫）、`drp-safety-stock`（calculate 计算结果非空+lastCalculatedAt + confirmWriteback override 回写 ErpDrpParameter.safetyStock）；status/字段翻转均经 verifyState `__get` 独立断言；**执行期修复 SafetyStockEngine 除零缺陷**（`monthlyDemands` 无历史出库时返回空列表致 `mean()` 除零 ArithmeticException，对齐既有 `result.isEmpty()→add(ZERO)` 意图返回 [0]，drp-service 30 单测全绿含 TestErpDrpSafetyStock）；自包含 setup（__save 强制 org FK→用种子 org=2；drp plan/calc 用区别 warehouseId 三元组隔离 confirmWriteback）；e2e-runbook 业务动作表 +contract/drp 行 + 套件计数 260→269 | `2026-07-14-0215-2` | ✅ done | `plan-first` |
| — | hr 域 DIRECT 业务动作浏览器层 E2E（承接 2004-1 Deferred「全 18 域全业务动作覆盖（DIRECT 域剩余）」hr 子集——触发条件=各域细化端到端验证时已满足；hr 为 Tier-1 未覆盖域，45 个 @BizMutation 数量最多）：4 新 spec（15 用例）——`hr-payroll`（薪酬引擎 calculateSalary DIRECT 计算触发 gross/net 非空+UNSUBMITTED/PENDING + voidSalary 回退 + markPaid negative-path 守卫 ERR_SALARY_ILLEGAL_STATUS_TRANSITION + generateBankFile negative-path 守卫；自包含建 employee+contract+SocialInsuranceBase+Config(HOUSING_FUND)+TaxConfig(七级累进) 配置链）、`hr-salary-simulation`（薪酬模拟 What-If 生命周期 createSimulation→adjustItem→submitForReview→approve→convertToFormal 正向链+正式 salary 回链 + reject + applyBatchAdjustment FIXED + 非法迁移守卫；**Explore 裁定**：直接 __save 源 ErpHrSalary 作模拟源期间——runPayroll 遍历 ALL active 员工含种子 HR-EMP-001/002 缺合同/社保配置会抛错不可行；applyBatchAdjustment 返回 Map 非实体 GraphQL 不支持字段选择须原始 mutation 无选择集；adjustmentReason VARCHAR(20)+ext:dict 须合法 dict 值 SALARY_CHANGE）、`hr-recruitment`（招聘漏斗 6 动作 OPEN→SCREENING→INTERVIEW→OFFERED→HIRED + hire 自动创建员工+合同联动 findFirst 反查 + reject/close + HIRED→moveToScreening 非法守卫）、`hr-leave-attendance`（休假审批 DRAFT→SUBMITTED→APPROVED→CANCELLED + reject + 日期重叠守卫 ERR_LEAVE_DATE_OVERLAP + clockIn/clockOut 打卡端点+workHours 派生 + 重复 clockIn 守卫 ERR_ALREADY_CLOCKED_IN）；status 翻转均经 verifyState `__get` 独立断言；markPaid/generateBankFile happy-path 归 Deferred（salary 审批轴经 useWorkflow xwf 浏览器层不可达同 2330-1 裁决）；e2e-runbook 业务动作表 +4 hr 行 + 套件计数 269→284；纯测试+文档，零生产代码/契约/ORM 模型变更 | `2026-07-14-0215-3` | ✅ done | `plan-first` |
| — | aps + b2b + logistics 域 DIRECT 业务动作浏览器层 E2E（承接 0215-1 Deferred「aps / b2b / logistics 域 DIRECT 业务动作 E2E」successor——触发条件=按域推进剩余 DIRECT 业务动作浏览器层覆盖时已满足；三域均无 useWorkflow/useApproval，纯 DIRECT @BizMutation 浏览器层可达；webhook/批量扫描动作排除）：3 新 spec（14 用例）——`aps-schedule`（排产方案状态机 DRAFT→PUBLISHED→ARCHIVED + DRAFT→archive 合法路径 + ARCHIVED→publish 非法守卫 ERR_APS_SCHEDULE_ILLEGAL_STATUS）、`aps-operation-order`（排产引擎 scheduleForward/scheduleBackward 自包含 setup 可达 PLANNED 翻转+SchedulingResult.feasible+scheduledOperationIds 非空+plannedStart/EndDateT 写回；**Explore 裁定** ErpApsOperationOrder.workOrderId/machineId 无 FK 约束 + ErpApsSchedulingEngine 纯算法 POJO capacity=1 无工作中心日历/产能配置依赖，自包含 setup 可达不降级）、`b2b-edi-doc`（EDI 信封状态机出站 TO_SEND→markSent→markAcknowledged + markError/retry retryCount++ + cancel + 入站 createInbound→archive + ACKNOWLEDGED→markSent/CANCELLED→retry 非法守卫；**Explore 裁定 (b)** createOutbound happy-path 经 provider.generatePayload 跨域查 ErpSalInvoice 缺失抛 ERR_B2B_EDI_PARSE_FAILED，跨域耦合度高违反单域隔离原则故降级为 watch-only residual，经 __save 预置 TO_SEND 态直接测后续状态迁移；ErpB2bEdiDoc 状态字段为 `state` 非 `status` B1 修复）、`log-shipment`（发运单状态机 advise→completeShipment→cancelShipment 经 MockCarrierGatewayClientFactory(gatewayId=mock) 网关完整可达 DISPATCHED+trackingNo/labelUrl 写回；**Explore 裁定 (a)** MockClient 默认 failureMode=SUCCESS，自包含建 ErpLogCarrier(gatewayId=mock)+ErpLogShipment 即可让 completeDeliveryOrder success，不降级；CANCELLED→advise/DRAFT→completeShipment 非法守卫）；status/state 翻转均经 verifyState `__get` 独立断言；全套件 89→103 business-actions 测试全绿（+14 new）；e2e-runbook 业务动作表 +aps/b2b/logistics 行 + 套件计数 284→298；纯测试+文档，零生产代码/契约/ORM 模型/种子/config 变更 | `2026-07-14-0508-1` | ✅ done | `plan-first` |
| — | 到岸成本分摊 + 维修备件消耗 GL 过账生命周期浏览器层 E2E（收口「posting-landed-but-E2E-missing」缺口类别——S-3 到岸成本引擎 + S-6 备件消耗过账后端 completed 但零浏览器层 E2E；两 Phase 共享同一凭证行数值断言范式）：2 新 spec（2 用例）——`inv-landed-cost.action.spec.ts`（DIRECT `@BizMutation` 到岸成本审核生命周期：自包含建测试专用物料+INCOMING 备货+APPROVED Receive（经 `__save` 直置 approveStatus=APPROVED 非 runP2pChain 避 MAT_1 余额污染）+ LandedCost+Line → `allocate` @BizQuery 分摊预览 `List<Map>` 非空 → `approve` @BizMutation 审核三步编排 分摊→CostAdjust→LANDED_COST(490) 过账 → approveStatus=APPROVED+docStatus=DONE+posted=true + LANDED_COST 凭证行 Dr 1401=50/Cr 2202=50（billHeadCode=landedCost.code 无后缀）+ APPROVED→approve 非法守卫）+ `mnt-spare-part-posting.action.spec.ts`（DIRECT `@BizMutation` 备件消耗确认：自包含建测试专用备件物料+INCOMING 备货+种子设备 id=1+Usage+Line → `confirm(usageId)` → OUTGOING 移动 ERP_MNT_SPARE_PART auto-DONE → docStatus=ACTIVE+posted=true + MAINTENANCE_ISSUE 凭证行 Dr 6602=50/Cr 1403=50（billHeadCode=usage.code+"-MI"，config erp-mnt.spare-part-posting-enabled=true））；webServer JVM arg 追加 `-Derp-mnt.spare-part-posting-enabled=true`；e2e-runbook 业务动作表 +inventory landed-cost/maintenance spare-part 行 + 套件计数 298→300；纯测试+config，零生产代码/契约/ORM 模型变更 | `2026-07-14-0606-2` | ✅ done | `plan-first` |
| — | projects 工时/结算过账生命周期 + 凭证行数值浏览器层 E2E（承接 0215-1 同形 Deferred「全 18 域全业务动作覆盖（DIRECT 域剩余）」——触发条件=按域推进剩余 DIRECT 业务动作浏览器层覆盖时已满足；projects 域工时过账 + 结算过账为 DIRECT `@BizMutation` 浏览器层可达但零 E2E；收口 projects 域过账凭证行断言完全缺失）：2 新 spec（2 用例）——`projects-timesheet-posting.action.spec.ts`（工时 DIRECT 状态机 UNSUBMITTED→submit→SUBMITTED→approve→APPROVED+posted + PROJECT_COST_COLLECTION 凭证行 Dr 5101=800/Cr 2211=800 确定性派生 hours×costRate + cancel reverse 红冲同向取负 Dr 5101=-800/Cr 2211=-800 + 2 非法守卫；自包含 ProjectType(defaultSubjectId=5101 行 id)+Project(OPEN)+Task+Timesheet，costRate 单填驱动 CostRateResolver 优先级 1，orgId 必填驱动 acctSchema 解析）+ `projects-settlement-posting.action.spec.ts`（结算 DIRECT createSettlement(CLOSE)→submit→approve→APPROVED+posted+跨域建资产卡片 IN_SERVICE + PROJECT_SETTLEMENT 凭证行 Dr 1601=1000/Cr 1603=1000 CLOSE 资本化 + reverseSettlement 红冲同向取负 + 资产卡片 status 回退 DRAFT 断言 + 非法守卫；PnL 快照硬前置先 refreshPnl 产 CALCULATED 快照，finalCost 经自包含 CostCollection+Line(MATERIAL,1000)→refreshPnl→totalCost=1000 确定性；spec cleanup 显式清理跨域资产卡片 reverseSettlement 仅回退 status 不删除）；**cleanup 健壮性修复** setup 置 try 内 + finally 全量 null 守卫（首次 setup 失败致孤儿 OPEN 项目污染 dashboard，重构后 fresh-DB 无漂移）；种子 erp_md_subject.csv +3 行 5101/2211/4103 + webServer JVM arg `-Derp-prj.default-payroll-subject-id=2211`（科目编码非行 ID 非对称注释）；e2e-runbook 业务动作表 +projects 2 行 + 套件计数 46→48 spec + JVM arg 段 + 种子 COA 段；套件 2 passed + projects/assets dashboard value 回归无漂移；纯测试+种子+config，零生产 Java/契约/ORM 模型变更 | `2026-07-14-0742-2` | ✅ done | `plan-first` |
| — | assets 过账凭证行精确数值浏览器层 E2E 断言（承接 0215-1 Deferred「assets 凭证行精确数值断言 successor」——触发条件=assets 过账业务动作 E2E 落地后已满足；收口 assets 作为唯一「过账 happy-path E2E 已落地但凭证行级数值断言缺失」的核心域）：在 4 个既有 assets 业务动作 spec 内叠加凭证行精确数值断言（subjectCode + dcDirection + debitAmount/creditAmount），覆盖 5 过账业务类型——DEPRECIATION（Dr 6602=333.3333/Cr 1602=333.3333，经 executed.voucherId 直用，金额=(12000-0)/36 SCALE=4 HALF_UP）/ CAPITALIZATION（Dr 1601=800/Cr 1603=800，金额=CostItem 汇总 500+300）/ ASSET_INVENTORY_ADJUSTMENT-SHORTAGE（Dr 6711=500/Cr 1601=500，金额=asset netBookValue）/ MAINTENANCE_EXPENSE（Dr 6602=300/Cr 1002=300，金额=LABOR+SPARE_PART）/ MAINTENANCE_CAPITALIZATION（Dr 1601=250/Cr 1002=250，金额=SUBCONTRACT）；4 路径均叠加红冲凭证行同向取负断言（dcDirection 不变金额取负，经 `findVoucherIdByBillCode(billCode,'REVERSAL')`）；复用 `findVoucherIdByBillCode` + `assertVoucherLines` 两原语零生产代码变更；e2e-runbook 凭证行断言表 +5 assets 行 + assets spec 描述注记；新断言全内联既有 4 ast spec，套件总数不变；纯测试+文档，零生产代码/契约/ORM 模型变更 | `2026-07-14-0742-1` | ✅ done | `plan-first` |
| — | contract + drp 跨域编排浏览器层 E2E（承接 0215-2 Deferred「跨域编排面」三项——触发条件=contract/drp 细化端到端验证时已满足）：3 新 spec（9 用例）覆盖 0215-2 未覆盖的三项 DIRECT `@BizMutation` 跨域编排——`ct-rebate-accrual`（返利计提引擎 runAccrual PROGRESSIVE 逐张 posted 发票聚合→ErpCtRebateAccrual 行写入 + agreement.totalAccumulatedAmount/estimatedRebateAmount 回写 + DRAFT 非法守卫；**Phase 1 Explore 裁决**：__save 直置 posted=true 不可行 use-approval tagSet 强制 false，复用种子 posted AP 发票 PINV-2026-001 只读前置）、`ct-invoice-plan-trigger`（triggerInvoice INBOUND→AP/OUTBOUND→AR 发票草稿创建 code=`CT-INV-{planId}` + plan isInvoiced/invoiceBillCode/invoiceDate 回写 + 已开票/SUSPENDED 守卫 + triggerDuePlans 批量入口；**执行期后端修复** triggerDuePlans findList 经 meta 算子白名单拒 le(planDate) 改用 daoProvider 直查对齐同模块范式，38 JUnit 全绿）、`drp-release-line`（releaseLine TRANSFER→ErpInvTransferOrder/PURCHASE→ErpPurOrder 双路径下游单据创建 code=`DRP-TO/PO-{lineId}` + 行 status=ORDERED + orderBillType/Code 回写 + 非 APPROVED/缺 source warehouse/缺 supplier 三守卫；__save 直置 APPROVED 绕过引擎精确控制 replenishmentType + 测试专用物料隔离）；e2e-runbook 业务动作表 +3 行 + 套件计数 48→51 spec；business-actions 全套件 124 passed（+9 new）0 回归 + module-contract/erp-ct-service 38 JUnit 全绿；纯测试+文档+1 行后端 bug 修复（triggerDuePlans meta 算子白名单），零契约/ORM/种子/config 变更 | `2026-07-14-0941-1` | ✅ done | `plan-first` |
| — | b2b + logistics + aps 跨域编排浏览器层 E2E（承接 0508-1 Deferred But Adjudicated 四项——触发条件=b2b/logistics/aps 细化端到端验证时已满足）：3 新 spec（6 用例）覆盖 0508-1 未覆盖的四项 DIRECT `@BizMutation` 跨域编排——`b2b-asn-match-receive`（ASN→PO 匹配 matchPurchaseOrder 按 relatedBillCode 跨域读 PO + 逐行 materialId 匹配→MATCHED + createReceiveFromAsn config-gated 跨域建 ErpPurReceive 草稿仅头无行 + 非法守卫 + retryMatch 幂等）、`aps-rush-order`（insertRushOrder 插单区间重排：背景工序 priority=50 scheduleForward→PLANNED + 急单 priority=10 同 machineId 窗口重叠→insertRushOrder 将背景工序回退 DRAFT 重排→plannedStart 被推移区间重排可观测 + SchedulingResult feasible/scheduledOperationIds 断言）、`log-delivered-freight-posting`（handleTrackingWebhook DELIVERED→onDelivered→FREIGHT 过账 path-1 SALES_DELIVERY + freightSettlementStatus PENDING→SETTLED + Dr 6601/Cr 1002 凭证行数值断言 + 幂等守卫第二次 webhook advanceTracking 返回 false 无重复凭证）；webServer JVM arg 追加 `-Derp-b2b.asn-auto-create-receive=true` + `-Derp-log.webhook-signature-required=false`；path-2 到岸成本降级 Deferred But Adjudicated（config-gate 默认关闭 + path-1 已代表验证 + 后端单测覆盖）；e2e-runbook 业务动作表 +3 行 + 套件计数 51→54 spec；business-actions 新 spec 6 passed + 既有 14 spec 回归 0 失败；纯测试+文档+config JVM arg，零生产代码/契约/ORM/种子变更 | `2026-07-14-0941-2` | ✅ done | `plan-first` |
| P6 | 销售定价引擎（UC-SAL-11）：销售价格清单（头/行，客户组/物料/阶梯/期间/优先级）+ 促销规则引擎（折扣/满减/买赠/价格覆盖，可配叠加）+ 订单行取价接入价格清单 + 行级折扣字段 + 前端 CRUD 页面替换占位。erp-survey 对标：Odoo Pricelist + ERPNext Pricing Rule 均为核心内置功能，当前仅有基础取价为 gap | `2026-07-10-1100-1` | ✅ done | `plan-first` |
| P6 | 信用冻结：出库/发票审核环节信用控制（UC-SAL-11 Non-Goal successor）：`CreditLimitChecker` 扩展 `checkCreditHold` 方法，在出库/发票审核时检查客户当前信用状况（point-in-time），复用三级策略，新增配置开关（默认关闭向后兼容）。erp-survey 对标：ERPNext Credit Limit 在 Sales Order + Sales Invoice 双点校验为标准功能 | `2026-07-10-1100-2` | ✅ done | `plan-first` |
| P7 | 到岸成本分摊（Landed Cost）：到岸成本单（头/行费用要素）+ 分摊引擎（按金额/数量/重量比例）+ 审核时更新成本层 + GL 凭证（借存货/贷应付）。解除 5 个 plan Deferred + logistics path-2 阻塞。erp-survey 对标：ERPNext Landed Cost Voucher + Metasfresh 标配 | `2026-07-10-1100-3` | ✅ done | `plan-first` |
| P7 | 预算管理（UC-FIN-11/13）：预算方案 + 明细行 + 控制日志 3 实体 + BUDGET 影子凭证范式（postingType=BUDGET，余额从 VoucherLine 聚合，不动 GlBalance）+ 预算控制 SPI（HARD/WARN/NONE 三级，采购/付款审核时校验）+ 预算对比报表 + 前置修复实际数聚合隔离 BUDGET 凭证。erp-survey 对标：iDempiere GL Budget。设计完备（budget.md 107行）但零实现，use-case 审计 🔶 | `2026-07-10-1100-4` | ✅ done | `plan-first` |
| P7 | 制造完工入库与领料 GL 过账：MANUFACTURING_RECEIPT(完工入库 Dr 存货/Cr WIP) + MANUFACTURING_ISSUE(领料 Dr WIP/Cr 存货)，形成制造业财一体成本流转闭环。解除 0704-2 posted=false Deferred。erp-survey 对标：Odoo mrp.account / ERPNext Stock Entry GL / Metasfresh 均为标配 | `2026-07-10-1100-5` | ✅ done | `plan-first` |
| P7 | 维修备件消耗 GL 过账：MAINTENANCE_ISSUE(Dr 维修费用/Cr 存货)，maintenance 域实物侧备件消耗过账。解除 1018-3 Deferred + 与 assets 域 MAINTENANCE_EXPENSE(470) 防双重扣减兼容。erp-survey 对标：ERPNext/Metasfresh 维修费用化过账标配 | `2026-07-10-1100-6` | ✅ done | `plan-first` |
| P8 | HR 休假/考勤/招聘/合同到期引擎：休假审批状态机+余额实体+排班钩子激活、考勤打卡端点+无薪假扣减、招聘状态机+HIRED→自动创建员工+合同、合同到期扫描Job+通知+续签。erp-survey 对标：Odoo/ERPNext/Axelor/AureusERP 4/4 均为核心内置。原 extended-roadmap Non-Goal 纯优先级排序（无技术约束），CRUD 骨架已就位，本期补引擎 | `2026-07-10-1100-7` | `ready` | `plan-first` |
| P8 | 制造委外引擎 + 成本要素拆分（M-1+M-2 successor，承接 `docs/audits/2026-07-12-1504-competitive-levers-implementation-audit.md` 裁决）：(1) 委外加工单生命周期——`ErpMfgSubcontractOrderBizModel` 当前 15 行 CRUD 空壳，补 委外单→材料发出出库→收回加工品入库→委外费用过账 全链 + MRP 委外释放（`MrpReleaseService:49` 当前明示不支持）；(2) `CostRollupService` 成本要素拆分——材料+人工已实，overhead/subcontract 恒 0，补 制造费用分配率应用 + 委外费归集。模型实体已就位（`ErpMfgSubcontractOrder/Line`、`ErpMfgCostRollup/Line`），`competitive-comparison.md` 杠杆 F 声明"委外"待兑现。erp-survey 对标：Odoo Subcontracting + ERPNext Subcontracting + iDempiere 均为制造标配 | `2026-07-12-1504-1`（顺序计划 `2026-07-13-0455-1` + `2026-07-13-0455-2`） | ✅ done | `plan-first` |
| — | 核销时点动态汇兑损益（M-3 watch-only residual，承接 `docs/audits/2026-07-12-1504-competitive-levers-implementation-audit.md` 裁决）：`ErpFinReconciliationBizModel` 核销时 `fxGainLoss` 硬编码 ZERO；期末重估由 `ExchangeRevaluationService` 承载已部分覆盖。**重开触发条件**：出现多币种核销业务需求（核销时点即期汇率与发票/收付款入账汇率差异需动态计算汇兑损益）。未触发前为残留风险，不立项 | `2026-07-12-1504-1` | watch-only residual | `plan-first` |

## 就绪不变量

`ready` 意味着以下所有条件都为真：

- owner doc 路径存在且对此切片已知不过时
- `docs/context/project-context.md` 中的验证命令是真实的
- 无阻塞性未解决问题或明确标记为非阻塞
- 保护区域在 `docs/context/ai-autonomy-policy.md` 中配置

## 状态值

- `ready` - AI 可以根据自主权标签继续
- `in-progress` - 当前正在实施或计划中
- `blocked` - 在阻塞项解决之前无法继续
- `done` - 已完成并验证

## AI 自主权值

- `implement` - 可直接实现（设计文档充分，AI 自行决定实现细节）
- `plan-first` - 需先编写计划（跨域/业财打通等复杂场景）
- `ask-first` - 需人工确认（涉及 ORM 模型保护区域）

## 选择规则

当被要求在未命名任务的情况下继续时，选择优先级最高的 `ready` 项目。

如果表过时，请降级行或在实施前询问。
