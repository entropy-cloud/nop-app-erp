| Date | Source | Git State | Scope | Commands Passed | Known Failures | Evidence | Notes |
| 2026-08-31 | local | dirty（plan `2026-08-31-1143-1-app-erp-all-default-seed-loading`：`application.yaml` +1 行 `init-database-data: true` + `scripts/start-app.sh` 新文件 + docs 3 处 owner-doc 追加 + seed-data.md/e2e-runbook.md 资产口径勘误 96→97/91→97） | full mvn build + app-erp-all scoped test + compliance checker + fresh-DB 二轮启动 | `mvn clean install -DskipTests`（156 reactor 模块 BUILD SUCCESS，01:58）；`./scripts/start-app.sh start`（fresh-DB 重置，13s ready，97 CSV + 1 SQL 全装载 0 冲突/0 列映射错误）；GraphQL 抽样（Currency=2/Material=4/Partner=5/PurOrder=1/SalOrder=1 与 CSV 行数一致）；`./scripts/start-app.sh restart`（二轮 fresh-DB 13s ready 稳定）；`mvn test -pl app-erp-all -Dtest=TestErpSeedDataIntegrity,TestAuthSeedLoadingProof`（5/0/0 绿——2/0/0 + 3/0/0）；`mvn test -pl app-erp-all -Dtest=TestErpAiIntrospectionDisabledByDefault,TestErpAiIntrospectionEnabled,TestErpFinApDocBatchWiring,TestErpFinApDocumentPipeline`（14/0/0 绿——4 个无声明的 JunitAutoTestCase 子类零影响）；`bash docs/audits/nop-compliance-checker.sh`（exit 0，零生产代码变更零漂移） | `mvn test -pl app-erp-all` **5 errors 预存回归**（与本计划变更无关，stash 回滚 application.yaml 后同样失败实证）：`TestErpC03O2cGoldenPath`/`TestErpC04SalReturnWithCs`（F2.11 `StockMoveBookkeeper.findBalance` 自然键精确匹配回归——seed `erp_inv_stock_balance` SKU_ID 非空致查询 miss，ERR_AVAILABLE_INSUFFICIENT）、`TestErpC08MrpApsRelease`（F2.6 mfg BOM/MRP 修复后快照 NET_REQUIREMENT/PLANNED_QUANTITY 漂移 40→120）、`TestErpC09QaNcrCapaScrap`（F2.12 qa-004 NCR severity 数字 20→NORMAL 字符串，app-erp-all 集成快照未同步重录）、`TestErpC12PrjTimesheetSettlement`（F2.12 prj 修复后快照漂移）。**归属 ai-check F2.x 批收尾义务（app-erp-all 集成测试快照同步），非本计划** | 本仓库工作树（plan `docs/plans/2026-08-31-1143-1-app-erp-all-default-seed-loading.md` Phase 1-3 + 日志 `docs/logs/2026/08-31.md`） | **app-erp-all 默认 seed 装载启用基线**：`application.yaml:18` 新增 `init-database-data: true`（演示/沙盒语义，用户裁决推翻 1234-1「生产默认关」姿态；所有 profile 共享）；`scripts/start-app.sh` 为标准启动入口（fresh-DB 重置 + allow-create-default-user + flux 渲染）；seed 资产 97 CSV + 1 SQL 全部装载 0 冲突 + 引用完整性门禁 5/0/0 全绿 + 4 个无声明的 JunitAutoTestCase 子类零影响（ALL_LAZY 懒 bean 未引用机制核实）；既有 23 个 `@NopTestProperty` 显式声明测试不受默认值变更影响。**5 个 app-erp-all 集成测试预存回归已登记**（08-28 全绿基线后 ai-check F2.6/F2.11/F2.12 引入，未跑 app-erp-all 全量验证致漏网），归 ai-check 批收尾 |

## Rule

Do not mark a command as passed unless it actually ran in the current repository state.

`Commands Passed` must contain only passing commands. Put accepted failures in `Known Failures` with the reason and evidence.

A dirty working-tree baseline must name the changed files in `Notes` or link to a dated log/testing note that does.

`full` means all real verification commands configured in `docs/context/project-context.md`. Commands explicitly marked `none` are excluded and should be noted.

| Date | Source | Git State | Scope | Commands Passed | Known Failures | Evidence | Notes |
| 2026-08-31 | local | dirty（plan `2026-08-31-1426-2-app-erp-all-integration-regression-fix`：3 处产品代码修复——prj `ErpPrjProjectSettlementCreateSettlementProcessor` 守卫收窄为同类型防重（F2.12 prj-004 过宽拦截 CLOSE→→FINAL 两阶段合法链回归）+ qa `SpcSamplingService.findInspectionByCode` I*Biz 化用错 `findPage+DEFAULT_SELECTION`（F2.12 qa-001 同型缺陷）改 `findList(q, null, ctx)` + sales `ReturnRefundOrchestrator.orchestrateRefund` & `ErpSalInvoiceProcessor` I*Biz 化传 `null` ctx（被前面 C03/C04 delivery 失败遮蔽的更深 NPE）改 `new ServiceContextImpl()`；prj `ProjectCostAggregator.rollbackFromTimesheet` I*Biz 化用错 `findPage+DEFAULT_SELECTION` 改 `findList(q, null, ctx)`（F2.12 prj-001 同型缺陷，08-29 跨月后暴露）+ prj `TestErpPrjProjectSettlement.testDuplicateCloseSettlementRejectedAndCrossTypeAllowed` 增 CLOSE 侧拒绝+跨类型放行断言；C03/C04 input json5 补 `skuId: "1"`（F2.11 自然键匹配兼容）+ 五类 force-save 重录 + 掩码恢复（HEAD `*`/`@var:` 取回 + 重录字面保留 + 删旧 bug 行为产物 C04 erp_sal_receipt_line.csv id=100037 负核销行 + erp_sal_receipt.csv UNRECEIVED→RECEIVED 对齐 F2.10 修复后正确语义）） | full mvn build + app-erp-all scoped test + compliance checker + 全 reactor `mvn test` + 五类 CHECKING 二连跑 | `mvn clean clean install -DskipTests`（156 reactor 模块 BUILD SUCCESS，01:43 min）；`mvn test -pl app-erp-all`（**69/0/0/1 全绿**——5 集成测试 100% 通过，含新增 C12 CLOSE 侧断言 + 二连跑稳定）；五类 force-save 重录 + CHECKING **二连跑 5/0/0 全绿**；`bash docs/audits/nop-compliance-checker.sh`（exit 0，零生产代码合规漂移——R2c=1542 与 08-28 plan-0219-2 基线 1537 差 +5 已被 ai-check 批/后续合法吸收）；全 reactor `mvn test`（**3975 tests / 1 failure (hr 预存) / 1 error (drp 预存) / 1 skipped / 671 reports**——hr `TestErpHrDepartmentPositionDeleteGuard#testDeleteEmptyPositionAllowed` 与 drp `TestErpDrpCrossDock#testStagingTimeoutFallbackJob` 经 stash 验证为预存失败（与本计划无关，**新发现**：ai-check 批漏网回归不止原 plan 1143-1 识别的 5 个集成测试，另有 hr/drp 域测试）；`mvn test -pl module-projects/erp-prj-service`（**179/0/0 全绿**——含 NPE 修复 + 新增 testDuplicateCloseSettlementRejectedAndCrossTypeAllowed）；`mvn test -pl module-quality/erp-qa-service`（**184/0/0 全绿**——含 findList 修复） | 2 个预存失败：`TestErpHrDepartmentPositionDeleteGuard#testDeleteEmptyPositionAllowed`（hr 域——`assertNull` 失败，position 删除后 get 返回实体，stash 验证预存）、`TestErpDrpCrossDock#testStagingTimeoutFallbackJob`（drp 域——快照 field-value 失配，stash 验证预存）——均与本计划改动域无关（plan 仅改 prj/qa/sales），属 ai-check F2.x 批（或后续批）漏网回归，归 successor 收尾 | 本仓库工作树（plan `docs/plans/2026-08-31-1426-2-app-erp-all-integration-regression-fix.md` Phase 1-3 + 日志 `docs/logs/2026/08-31.md`） | **app-erp-all 五个集成测试预存回归修复后全绿基线**：plan 1143-1 登记的 5 个 C0X 失败（C03O2c/C04SalReturn/C08MrpAps/C09QaNcr/C12PrjTimesheet）全部清零；C12 守卫收窄与 owner doc（profitability.md §关键流程 2 FINAL/CLOSE 并列语义）自洽保留 F2.12 防 GL 重复确认本意（testDuplicateFinalSettlementRejected 保持绿 + 新增 CLOSE 侧拒绝 + 跨类型放行覆盖）；F2.12 同型 I*Biz 化 `findPage+DEFAULT_SELECTION` 缺陷全仓清剿（prj ProjectCostAggregator + qa SpcSamplingService）+ null ctx 遗漏全仓清剿（sales ReturnRefundOrchestrator/ErpSalInvoiceProcessor），执行期合并发现 3 处同主题缺陷；plan 1143-1 行 Known Failures（5 个 C0X）已修复可清零。**新发现 2 个预存回归（hr/drp 域）归 successor**（非本计划范围） |
