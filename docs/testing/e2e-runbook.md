# Playwright E2E 冒烟回归运行手册

## 概述

本手册指导如何运行 `nop-app-erp` 的 Playwright E2E 冒烟回归套件，覆盖 10 域看板 + 24 域报表页面 + 18 域 CRUD 列表/表单页 + 1 KB 建议定向冒烟 + 29 个数据驱动数值断言 spec + 13 域 CRUD 数据驱动列表断言 spec + 4 域 CRUD 写路径 spec（master-data GraphQL 层 + master-data AMIS 表单层 + quality GraphQL 层 + maintenance GraphQL 层）+ 65 业务动作 spec（inventory StockMove 状态机+过账 / CRM Lead 状态迁移 / CS Ticket 六态状态机 / maintenance Visit 5 态+设备联动副作用 + maintenance Request 自定义 status 5 态 + maintenance Request→Visit 响应式副作用编排（accept 生成 DRAFT Visit 字段断言） / projects Task 4 态+DAG 门控 + projects 工时过账生命周期 submit/approve/cancel + PROJECT_COST_COLLECTION 凭证行 Dr 5101/Cr 2211 + 红冲 / projects 结算过账 CLOSE createSettlement→approve→posted + Dr 1601/Cr 1603 + reverseSettlement 红冲 + 资产卡片回退 / projects 损益结转 FINAL/INTERIM approve→posted + Dr 5101/Cr 6001 + 条件性 4103 本年利润 + reverseSettlement 红冲 + INTERIM 对照 / quality CAPA 3 态 + NCR 无 CAPA 路径 + NCR resolve CAPA 闭包门控跨实体依赖 + NCR SCRAP 过账凭证行 + NCR reverseNcr 红字凭证行断言 + NCR RETURN 处置退货编排 / manufacturing WorkOrder DIRECT 审批轴+域迁移 + manufacturing WorkOrder 异常分支 stop/resume/close/cancel + 部分完工 willFinish 门控 / purchase Return DIRECT 审批轴+posted / sales Return DIRECT 审批轴+posted / quality Recall DIRECT 审批轴+locateTargets/close / finance voucher 手工 post（业财过账工厂+入口，LANDED_COST 最简 PostingEvent） / finance 核销单生命周期（create→post→reverse + 双面对账查询 + 5 validateLine 守卫负路径，自包含 partner 隔离） / finance 银行对账生命周期（generate→post→reverse 三态状态机 + BANK_RECON_ADJ 未达调整凭证 + 红冲，自包含 FundAccount+对账单 setup） / finance 坏账生命周期（writeOff→submit→approve DIRECT 审批轴 + recover 收回 + runBadDebtProvision 期末计提，自包含 partner+OPEN AR 项隔离） / finance 自动核销（runAutoReconciliation FIFO/BY_AMOUNT/BY_RATIO 三策略 config 门控 + 自包含 partner+OPEN AR 对隔离） / finance 预算方案生命周期 submit/approve/reject/cancel + BUDGET 影子凭证行数值断言（正向/红冲同向取负，按 ErpMdSubject.direction 借贷）+ 非法迁移守卫 / finance 预算控制 hook（PO approve HARD 阻断事务回滚无 ControlLog 持久化 + WARN 放行 ControlLog WARNED 持久化，config 门控 budget-check-enabled + budget-purchase-expense-subject-code=6601）+ getBudgetVsActual 对比查询结构断言 / contract 合同 6 动作状态机 + contract 版本 finalize/sign + contract 返利计提引擎 runAccrual（PROGRESSIVE 逐张 posted 发票聚合 + ErpCtRebateAccrual 行写入 + 非法守卫） + contract 返利结算 postSettlement（DRAFT→POSTED + 跨域负额 credit memo 发票 AP/AR + creditMemoBillCode=CT-REBATE-{id} + accruals isSettled 翻转 + 非法态守卫，无 GL 凭证） + contract 发票计划触发跨域编排 triggerInvoice（INBOUND→AP / OUTBOUND→AR 发票草稿）+ triggerDuePlans 批量入口 / drp 净需求引擎 runDrp/resetToDraft/approvePlan + drp 安全库存 calculate/confirmWriteback + drp 行释放跨域编排 releaseLine（TRANSFER→ErpInvTransferOrder / PURCHASE→ErpPurOrder 双路径）+ drp 批量释放 releaseApproved（APPROVED plan 全行 ORDERED + 计划 EXECUTED + 下游单据创建断言 + SUGGESTED-only no-op 守卫） / assets 折旧引擎+CIP 转固链+盘点 8 动作+维修费用化/资本化+减值/重估 VALUE_ADJUSTMENT 3 种调整类型 DIRECT useApproval 审批轴 / hr 薪酬引擎+模拟 What-If+招聘漏斗+休假审批+考勤打卡+排班 4 实体+人才发展 4 实体（评估→差距→发展计划闭环）+员工调动合同联动 / aps 排产方案状态机 publish/archive + aps 排产引擎 scheduleForward/scheduleBackward / b2b EDI 信封状态机出站+入站生命周期+retry / logistics 发运单 advise→completeShipment→cancelShipment + mock 承运商网关 + inventory 到岸成本审核生命周期 approve→LANDED_COST 过账凭证行 Dr 1401/Cr 2202 + maintenance 备件消耗 confirm→MAINTENANCE_ISSUE 过账凭证行 Dr 6602/Cr 1403，经 GraphQL 调自定义 `@BizMutation`）+ 2 跨域编排链 spec（P2P PO→Receive→Invoice / O2C SO→Delivery→Invoice 全链审批+过账产物，经 GraphQL 驱动）+ 4 制造链编排 spec（manufacturing WorkOrder+MaterialIssue+JobCard 三聚合根协作 齐套→领料→报工→完工，经 GraphQL 驱动 + 生产差异计算 config-gated auto-calc 满量完工触发 ErpMfgCostVariance 5 类差异行 + PRODUCTION_VARIANCE 凭证行精确数值断言 + 批次基因追溯 config-gated write-on-completion inputLot→outputLot 基因链 + forwardTrace/backwardTrace 追溯查询 + 质检门控 Gate 1 config+BOM.inspectionRequired 阻断完工负路径+对照路径）+ 2 反向冲销 spec（业财闭环方向二：财务侧 `ErpFinVoucher__reverse` 红字冲销 + 域监听者回退，`p2p-reverse.spec.ts` / `o2c-reverse.spec.ts`）+ 3 剩余 DIRECT 业务动作 spec（库存物理冲销 `ErpInvStockMoveBiz__reverse` DONE→反向移动单+relatedBill 挂链+方向取反 / 域 reverseApprove DIRECT 轴 `ErpPurInvoice__reverseApprove` APPROVED→REJECTED+posted 红冲 / Recall generateReturns 跨域建退货单 批次追溯→ErpSalReturn 创建，`inventory-stock-move-reverse.action.spec.ts` / `p2p-reverse-approve.spec.ts` / `quality-recall-generate-returns.action.spec.ts`）+ 1 看板 AMIS 前端渲染层 spec（10 域，`dashboards.visual.spec.ts`，DOM 结构 + echarts canvas + 2 非参数化域数值 token）+ 1 报表 AMIS 前端渲染层 spec（24 域，`reports.visual.spec.ts`，AMIS service reload 注入 renderHtml 响应），共 235 测试。

测试层级：**冒烟级**（页面 DOM 渲染 + 关键元素存在 + GraphQL `/graphql` 请求返回 200 + 无未捕获 console error）。非像素级视觉回归。

在冒烟级之上，核心域（finance/sales/purchase）+ 运营域（inventory/assets/projects）+ 扩展域（manufacturing/maintenance/quality）+ 扩展域 CRM/客服/人力（纯报表域，无看板）+ 主数据域（master-data，看板 KPI + 预警 + 2 报表）已叠加**数据驱动数值断言层**（`*.value.spec.ts`）：直接经 GraphQL query 取后端聚合原始值，断言匹配确定性期望值。详见下方「数据驱动数值断言层」。

在数值断言层之上，13 域代表性实体（有 seed 行的）叠加了**CRUD 数据驱动列表断言层**（`*.list-value.spec.ts`）：经 GraphQL `ErpXxx__findPage` 查询断言列表返回 seed 行（行数 ≥ seed + 关键字段 token）。master-data ErpMdPartner 额外叠加**CRUD 写路径验证**（GraphQL 层 `master-data.write.spec.ts` + AMIS 表单层 `master-data.write.amis.spec.ts`）：create→get 验证→update→get 验证→delete→get 验证全链，断言写操作持久化。quality `ErpQaRiskRegister` + maintenance `ErpMntEquipmentCategory` 叠加 GraphQL 层写路径（`quality.write.spec.ts` / `maintenance.write.spec.ts`）。详见下方「CRUD 数据驱动列表断言层 + 写路径验证」。

## 前置条件

1. **JDK 17+**（项目使用 Java 25）
2. **Maven 3.9+**
3. **Node.js 18+**（项目使用 Node 25）
4. **Google Chrome**（系统安装，Playwright `channel: 'chrome'`）
   - 若已安装 Playwright bundled chromium，config 会自动检测并优先使用
5. **预构建 uber-jar**：`mvn clean install -DskipTests` 产出 `app-erp-all/target/app-erp-all-1.0-SNAPSHOT-runner.jar`

## 安装依赖

```bash
# 根目录执行一次
npm install
```

## 启动方式

### 方式 A：自动启动（推荐首次）

Playwright `webServer` 自动启动 Quarkus 应用：

```bash
npx playwright test
```

webServer 命令含测试专用 JVM 参数：
- `-Dnop.auth.service-public=true` — 服务端认证旁路（sys 用户上下文）
- `-Dnop.auth.login.allow-create-default-user=true` — 自动创建测试用户 `nop`/`123`
  - `-Dnop.orm.init-database-data=true` — 部署期种子数据初始化（21 张主数据表 + 23 张交易单据表 + 13 张运营域表 + 4 张制造域表 + 11 张维护+质量域表 + 12 张 CRM/CS/HR 域表 + 3 张质量域 SPC 表 + 4 张制造域工作中心配置链+crp_load 表，详见下方「种子库启动」）
  - `-Derp-qua.ncr-default-acct-schema=1` — NCR SCRAP 过账 acctSchemaId（种子 ACCT-FIN-01，plan 2026-07-10-1800-1）
  - `-Derp-fin.bad-debt-allowance-subject-code=1231` / `-Derp-fin.bad-debt-expense-subject-code=6701` / `-Derp-fin.ar-subject-code=1122` — 坏账核销/收回/计提凭证科目编码（requireSubject config 默认 null，未配置抛 ERR_CLOSE_SUBJECT_NOT_CONFIGURED；种子 erp_md_subject.csv 补齐 1231/6701，1122 已在种子，plan 2026-07-12-0413-2）
  - `-Derp-fin.auto-reconcile=true` — 启用自动核销引擎（`ErpFinReconciliationBizModel.runAutoReconciliation` config 门控，默认 false 关闭抛 ERR_AUTO_RECON_DISABLED；plan 2026-07-12-1321-2）
  - `-Derp-fin.budget-check-enabled=true` + `-Derp-fin.budget-purchase-expense-subject-code=6601` — 启用预算控制 hook（`ErpPurOrderProcessor.validateBusinessRulesForApprove → IErpFinBudgetControlBiz.check` config 双门控，默认 false 关闭向后兼容；科目 6601 销售费用经 resolveBudgetSubjectId findByCode 解析，种子 id=8；无匹配 APPROVED 预算方案时返回 PASS，不影响既有 PO 审批流；plan 2026-07-14-0606-1）
  - `-Derp-fin.expense-budget-check-enabled=true` + `-Derp-fin.budget-expense-subject-code=6602` — 启用报销预算控制 hook（`ErpFinExpenseClaimProcessor.runBudgetCheckHook` config 独立门控，默认 false 关闭向后兼容；科目 6602 折旧费用经 resolveBudgetSubjectId findByCode 解析，种子 id=31；无匹配 APPROVED 预算方案时返回 PASS；plan 2026-07-14-1218-2）
  - `-Derp-prj.default-payroll-subject-id=2211` — 工时过账贷方科目编码（应付职工薪酬，`ErpPrjConfigs.defaultPayrollSubjectCode()` 按 code 直填 BILL_DATA_CREDIT_SUBJECT_CODE；config 键名含 `-id` 后缀但值为科目**编码**非种子行 ID，与借方 projectType.defaultSubjectId=数字ID 非对称；未配置抛 ERR_PAYROLL_SUBJECT_NOT_CONFIGURED；种子 erp_md_subject.csv 补齐 2211，plan 2026-07-14-0742-2）
  - `-Derp-mnt.spare-part-posting-enabled=true` — 启用维修备件消耗 GL 过账（MAINTENANCE_ISSUE，config 默认 false 关闭向后兼容；plan 2026-07-14-0606-2）
  - `-Derp-mnt.labor-posting-enabled=true` + `-Derp-mnt.default-labor-hourly-rate=80` — 启用维修工时费用化 GL 过账（MAINTENANCE_LABOR，Dr 6602 折旧费用 / Cr 2211 应付职工薪酬；config 默认 false 关闭向后兼容；rate 单位「元/小时」测试值 80，使 totalMinutes=60 → laborCost=80 确定性派生；plan 2026-07-18-0949-1）
  - `-Derp-b2b.asn-auto-create-receive=true` — 启用 ASN→采购入库自动创建（`ErpB2bAsnBizModel.createReceiveFromAsn` config 门控，默认 false 关闭向后兼容；plan 2026-07-14-0941-2）
  - `-Derp-log.webhook-signature-required=false` — 跳过物流 webhook HMAC-SHA256 验签（`ErpLogShipmentBizModel.handleTrackingWebhook` config 门控，默认 true 开启验签；测试用降低 setup 复杂度；plan 2026-07-14-0941-2）
- `rm -f db/erp.mv.db` — fresh-DB 重置（seed 非幂等，每次启动前清库）
- 页面模型校验保持默认开启（`nop.web.validate-page-model=true`，application.yaml 默认值）——`ErpCsTicket.view.xml`/`ErpHrEmployee.view.xml` layout 缺陷已修复（见 `docs/bugs/`），启动期页面校验安全网已恢复

### 方式 B：复用已运行实例（开发调试推荐）

先手动启动应用：

```bash
lsof -ti :8080 | xargs kill -9 2>/dev/null  # 清理端口
java -Dfile.encoding=UTF8 \
     -Dnop.auth.service-public=true \
     -Dnop.auth.login.allow-create-default-user=true \
     -jar app-erp-all/target/app-erp-all-1.0-SNAPSHOT-runner.jar
```

然后运行测试（跳过 webServer）：

```bash
SKIP_WEBSERVER=1 npx playwright test
```

### 种子库启动（演示 / 数据可见性）

默认 webServer 命令（方式 A）已含 **部署期种子数据初始化**：

- `-Dnop.orm.init-database-data=true` — 激活平台 `DataInitInitializer`（条件 bean，仅此 JVM 属性开启时实例化），从 `_vfs/_init-data/*.csv` 按拓扑序插入 **91 张 CSV**：
  - **21 张核心主数据表**（1234-1）：组织/币种/计量单位/物料/SKU/往来单位/仓库/员工/科目体系/税率/结算方式等（科目体系：8 基础科目 + 1249-1 补齐 5 过账科目 1401/1403/1131/2221/6401 + 1800-1 补齐 1 NCR SCRAP 科目 6711 + 0413-2 补齐 3 finance 业务动作科目 1231 坏账准备/6701 信用减值损失/2240OTHER 未达账项调整 + 0215-1 补齐 5 assets 业务动作科目 1601 固定资产/1602 累计折旧/1603 在建工程/6301 营业外收入/6602 折旧费用 + 0742-2 补齐 3 projects 业务动作科目 5101 项目成本/2211 应付职工薪酬/4103 本年利润 + 1218-1 补齐 3 assets VALUE_ADJUSTMENT 科目 6702 资产减值损失/1604 固定资产减值准备/4002 资本公积 + 1218-2 补齐 2 finance ExpenseClaim/EmployeeAdvance 科目 2241 其他应付款-员工/1221 其他应收款-员工预支，使 Pur/Sal/Inv/QA/Ast/Prj 过账 + finance 银行对账/坏账核销收回计提 Provider/config 科目码经 `findByCode` 可达，解除过账优雅降级）
  - **23 张业务交易单据表**（1445-1）：P2P（PO/Receive/Invoice/Payment 头+行）+ O2C（SO/Delivery/Invoice/Receipt 头+行）+ 已过账财务产物（凭证/凭证行/凭证回链/AR-AP 辅助账/GL 余额/会计期间 OPEN）
  - **13 张运营域表**（2210-1）：库存（stock_move+line/stock_balance/cost_layer）+ 资产（asset_category/asset/depreciation_schedule）+ 项目（project_type/project/cost_collection/timesheet/budget/project_pnl）；三域看板读域表非 GL，posted 统一 false
  - **4 张制造域表**（2026-07-09-0930-1）：work_order（4 行 IN_PROCESS/STOCK_PARTIAL/COMPLETED×2）+ cost_variance + forecast(APPROVED) + forecast_line；看板 4 `@BizQuery` + production-variance/forecast-variance 报表读域表非 GL，posted 统一 false；crp_load 因 workcenter 配置链依赖归 Deferred
  - **11 张维护+质量域表**（2026-07-09-0930-2）：维护域 8 表（equipment_category/equipment/schedule/request/downtime_entry/visit/visit_task/spare_part_usage）+ 质量域 3 表（inspection/non_conformance/action）；看板 `getDashboardKpi` + 3 预警（findEquipmentDowntimeAlert/findMaintenanceOverdueAlert/findCapaOverdueAlert）+ 4 报表（maintenance-history/downtime-summary/inspection-summary/ncr-capa-summary）读域表非 GL，posted 统一 false；SPC 三表曾因 spc_chart.parameterId 配置链依赖归 Deferred，**已于 2026-07-09-1145-2 落地**（见下方 1145-2 行）
  - **12 张 CRM/CS/HR 域表 + 2 处既有 CSV 加性追加**（2026-07-09-1045-1）：CRM 5 表（stage/lead/forecast_period/forecast/forecast_line）+ CS 3 表（ticket_type/ticket/survey）+ HR 4 表（department/employee/salary_simulation/salary_simulation_item_adj）+ `erp_md_partner` 追加 1 行 EMPLOYEE 类型 + `erp_fin_ar_ap_item` 追加 2 行 EMPLOYEE_ADVANCE/EXPENSE_CLAIM·OPEN；三域为纯报表域（无看板 BizModel），5 报表（CRM lead-conversion-funnel/forecast-accuracy、CS ticket-sla-csat-summary、HR payroll-simulation-comparison/employee-net-balance）读域表非 GL，无 posted 列；HR employee-net-balance 经跨域 finance/master-data 加性追加驱动；CRM/CS/HR 配置表 + GL 凭证归 Deferred
  - **3 张质量域 SPC 表 + 1 处既有 CSV 加性追加**（2026-07-09-1145-2）：spc_chart（1 行，parameterId=0 占位软引用）+ spc_sample（1 行 isOutOfControl=true）+ spc_capability（1 行 capabilityLevel=INADEQUATE）+ `erp_qa_non_conformance` 追加 1 行 sourceType=SPC·status=OPEN；质量看板 `getSpcOutOfControlWarning` 三计数器（outOfControlChartCount/inadequateCapabilityCount/openSpcNcrCount）读 SPC 域表非 GL，由确定性 0 转非空可观测（解除 0930-2/0930-3 SPC Deferred）；Strategy C 完整参照完整性（sample/capability.chartId 指向真实 chart 行）；SPC 引擎双层门控默认关，seed 静态结果行不被重算覆盖；SPC 控制图可视化/ErpQaParameter 物化/SPC 引擎重算链归 Deferred
  - **4 张制造域工作中心配置链 + crp_load 表**（2026-07-09-0628-1）：workcenter（1 行 WC-001 主装配线）+ workcenter_calendar（1 行 单班 08:00~16:00 ALL_WEEK IS_ACTIVE=true）+ workcenter_capacity（1 行 efficiencyFactor=1 IS_ACTIVE=true）+ crp_load（1 行 loadDate=2026-07-15 loadHours=4 workOrderId→0930-1 WO-1）；CRP 负荷报表（crp-load-report）经 `CrpLoadCalculator.getLoadReport` 读 crp_load+workcenter+calendar+capacity 非空可观测（capacityHours=8.00 / loadRate=0.50 确定性派生，解除 0930-1 crp_load Deferred）；Strategy C 完整参照完整性（calendar/capacity/crp_load.workcenterId 指向真实 workcenter 行）；CRP 重算链经 nop-job 双层门控默认关，seed 静态 crp_load 行不被重算覆盖；CRP 前端可视化/crp_load 重算链归 Deferred
- `rm -f db/erp.mv.db db/erp.trace.db` — **fresh-DB 重置**。`DataInitInitializer` 非幂等（无存在性检查），持久 H2 文件库重复启动会主键冲突；故每次 webServer 启动前删除 db 文件，确保 seed 在空表上插入。交易种子加入后该行为不变（仅 CSV 增多，webServer JVM 参数与重置逻辑无改动）。
- **序列推进（0814-1）**：`_init-data/zz-sequence-advance.sql` 在所有 CSV 加载后执行（按文件名排序），`MERGE INTO NOP_SYS_SEQUENCE` 创建 default 序列行 `NEXT_VALUE=100000`（> 种子显式 id 上限 8）。因 `SysSequenceGenerator.lazyInit()` 是 delay-method（所有 bean 启动后才运行），`.sql` 执行时表为空，MERGE 创建行后 `addDefaultSequence()` 的 `if(!exists)` 守卫跳过，advanced 值保留。消除 GraphQL/AMIS 表单 create 首次主键碰撞（首 save id≥100000）。详见 `docs/architecture/seed-data.md`「部署期序列推进修复」。

手动启动种子库（方式 B）：

```bash
lsof -ti :8080 | xargs kill -9 2>/dev/null
rm -f db/erp.mv.db db/erp.trace.db   # 必需：非幂等，须 fresh-DB
java -Dfile.encoding=UTF8 \
     -Dnop.auth.service-public=true \
     -Dnop.auth.login.allow-create-default-user=true \
     -Dnop.orm.init-database-data=true \
     -jar app-erp-all/target/app-erp-all-1.0-SNAPSHOT-runner.jar
```

- **生产安全**：生产 `application.yaml` 保持 `init-database-data` 缺省（`false`）；seed 仅经上述 JVM 属性 / webServer 触发。
- **种子范围**：核心主数据（bootstrap）+ P2P/O2C 最小连通交易单据集（含已过账财务产物）+ 运营域（库存/资产/项目）最小连通集 + 制造域最小连通集（work_order/cost_variance/forecast/forecast_line）+ 维护+质量域最小连通集（equipment/schedule/request/downtime_entry/visit/visit_task/spare_part_usage/inspection/non_conformance/action）+ CRM/CS/HR 最小连通集（stage/lead/forecast·line/ticket_type/ticket/survey/department/employee/salary_simulation·item_adj + 员工型 partner 追加 + ar_ap_item EMPLOYEE_ADVANCE/EXPENSE_CLAIM 追加）+ 质量域 SPC 最小连通集（spc_chart/spc_sample/spc_capability + non_conformance SPC 追加）+ 制造域工作中心配置链+crp_load 最小连通集（workcenter/workcenter_calendar/workcenter_capacity/crp_load）。核心域看板/报表/CRUD 列表经 GraphQL 证实数据非空；交易数据已使 finance/sales/purchase 域 KPI 数值非空（采购额/销售额/收入/净利润可观测）；运营域数据已使 inventory/assets/projects 域 KPI 非空（库存总值/资产原值+折旧/项目数+已发生成本+毛利率可观测）；制造域数据已使 manufacturing 域看板 4 KPI + production-variance/forecast-variance 报表 + crp-load 负荷报表非空（在制工单数/完工量/齐套待产/准时率/状态分布/产出趋势/工单延期预警 + 工作中心负荷/产能/负荷率可观测，crp-load 由 0628-1 解除空集）；维护+质量域数据已使 maintenance/quality 域看板 `getDashboardKpi` + 4 报表 + 3 预警非空（设备总数/运行数/待处理请求/本期维护访问/质检数/合格率/不合格数/开放 NCR/停机预警/维护逾期/CAPA 逾期可观测）；质量域 SPC 数据已使 quality 域看板 `getSpcOutOfControlWarning` 三计数器（outOfControlChartCount/inadequateCapabilityCount/openSpcNcrCount）非空（SPC 失控图/能力不足图/待处置 SPC NCR 可观测，解除 0930-2/0930-3 SPC Deferred）；CRM/CS/HR 域数据已使三域 5 报表非空（线索漏斗 leadCount/expectedRevenue、预测准确率 commitAmount/lineCount、工单 SLA/CSAT 命中率与均分、薪酬模拟差异+部门小计、员工净余额可观测）。其余扩展域交易单据未 seed（logistics/b2b/contract/drp/aps，Non-Goal，按域逐批补充）。
- 机制详情（主数据门控/非幂等/列映射/平台 bug 修复 见 `docs/analysis/2026-07-08-1234-1-seed-data-table-column-map.md`；交易单据表清单/列映射/拓扑序/范围裁决 见 `docs/analysis/2026-07-08-1445-1-transaction-seed-table-map.md`；运营域表清单/列映射/拓扑序/范围裁决 见 `docs/analysis/2026-07-08-2210-1-operational-domain-seed-table-map.md`；制造域表清单/列映射/拓扑序/范围裁决 见 `docs/analysis/2026-07-09-0930-1-manufacturing-seed-table-map.md`；维护+质量域表清单/列映射/拓扑序/范围裁决 见 `docs/analysis/2026-07-09-0930-2-maintenance-quality-seed-table-map.md`；CRM/CS/HR 域表清单/列映射/拓扑序/范围裁决 见 `docs/analysis/2026-07-09-1045-1-crm-cs-hr-seed-table-map.md`；制造域工作中心配置链+crp_load 表清单/列映射/拓扑序/范围裁决/期望值派生 见 `docs/analysis/2026-07-09-0628-1-crp-load-seed-table-map.md`）。

## 分层运行

| 级别 | 命令 | 用途 |
| --- | --- | --- |
| 单文件 | `npx playwright test tests/e2e/dashboards/finance.smoke.spec.ts --workers=1` | 调试单个页面 |
| 看板套件 | `npx playwright test tests/e2e/dashboards/ --workers=1` | 看板回归 |
| 报表套件 | `npx playwright test tests/e2e/reports/ --workers=1` | 报表回归（含 48 下载产物用例 + 10 AMIS 下载按钮用例） |
| CRUD 套件 | `npx playwright test tests/e2e/crud/ --workers=1` | 18 域 CRUD 列表/表单回归 + 13 域列表断言 + 写路径 |
| 列表断言套件 | `npx playwright test tests/e2e/crud/*.list-value.spec.ts --workers=1` | 13 域 findPage seed 行断言 |
| 写路径套件 | `npx playwright test tests/e2e/crud/*.write.spec.ts --workers=1` | CRUD 写持久化验证（create/update/delete） |
| 业务动作套件 | `npx playwright test tests/e2e/business-actions/ --workers=1` | 84 spec（17 域）自定义 @BizMutation 经 GraphQL 全栈可达 + 状态机迁移 + 跨实体依赖门控 |
| 跨域编排套件 | `npx playwright test tests/e2e/orchestration/ --workers=1` | P2P/O2C 全链审批 + 业财过账产物（stockMove/voucher/AR-AP）+ 制造链三聚合根协作（齐套→领料→报工→完工）+ 生产差异 + 批次基因追溯 + 质检门控阻断完工 + 财务侧反向冲销（红字凭证 + 域监听者回退） |
| 像素截图基线套件 | `BASE_URL=http://127.0.0.1:8011 SKIP_WEBSERVER=1 npx playwright test tests/e2e/visual/*.snapshot.spec.ts --workers=1` | 10 看板 + 6 代表性报表像素级视觉回归（`toHaveScreenshot`） |
| 全套件 | `npx playwright test --workers=1` | 提交前完整回归 |

全套件运行时间：约 35 分钟（343 测试：冒烟 + 数值断言 + CRUD 列表断言 + 4 写路径 spec（master-data GraphQL + master-data AMIS + quality + maintenance）+ 65 业务动作 spec（inventory StockMove + inventory StockMove reverse 物理冲销 + CRM Lead + CS Ticket + CS CannedResponse 宏匹配 + maintenance Visit + maintenance Request + maintenance Request→Visit 副作用编排 + maintenance 备件消耗 confirm+MAINTENANCE_ISSUE 过账凭证行 + projects Task + projects 工时过账生命周期 submit/approve/cancel+PROJECT_COST_COLLECTION 凭证行 + projects 结算过账 CLOSE createSettlement→approve→posted+Dr 1601/Cr 1603+reverseSettlement 红冲+资产卡片回退 + projects 损益结转 FINAL/INTERIM approve→posted+Dr 5101/Cr 6001+条件性 4103+reverseSettlement 红冲+INTERIM 对照 + quality CAPA/NCR + quality NCR resolve CAPA 门控 + quality NCR SCRAP 过账凭证行 + quality NCR reverseNcr 红字凭证行 + quality NCR RETURN 处置退货编排 + quality Recall generateReturns 跨域建退货单 + manufacturing WorkOrder + manufacturing WorkOrder 异常分支+部分完工 + purchase Return + sales Return + quality Recall + inventory 到岸成本 approve+LANDED_COST 过账凭证行 + finance voucher post + finance 核销单生命周期 create→post→reverse + 双面对账 + finance 银行对账 generate→post→reverse + BANK_RECON_ADJ 未达调整凭证 + finance 坏账 writeOff→submit→approve + recover + runBadDebtProvision + finance 自动核销 runAutoReconciliation FIFO/BY_AMOUNT/BY_RATIO 三策略 config 门控 + finance 预算方案生命周期 submit/approve/reject/cancel + BUDGET 影子凭证行数值断言 + finance 预算控制 hook（PO approve HARD 阻断/WARN 放行 + getBudgetVsActual，config 门控 budget-check-enabled + budget-purchase-expense-subject-code=6601） + contract 合同生命周期 6 动作状态机 activate/suspend/resume/terminate/expire/amend + contract 版本 finalize/sign + contract 返利计提引擎 runAccrual + contract 返利结算 postSettlement（跨域负额 credit memo+计提翻转，无 GL 凭证） + contract 发票计划触发跨域编排 triggerInvoice/triggerDuePlans + drp 净需求引擎 runDrp/resetToDraft/approvePlan + drp 安全库存 calculate/confirmWriteback + drp 行释放跨域编排 releaseLine TRANSFER→调拨单/PURCHASE→采购订单 + drp 批量释放 releaseApproved（全行 ORDERED+plan EXECUTED+SUGGESTED-only no-op 守卫） + assets 折旧引擎 executeDepreciation/reverseDepreciation + assets CIP 转固链 + assets 盘点 8 动作 + assets 维修费用化/资本化 + assets 减值/重估 VALUE_ADJUSTMENT 3 种调整类型 DIRECT useApproval 审批轴 + hr 薪酬引擎 calculateSalary/voidSalary+markPaid/generateBankFile 守卫 + hr 薪酬模拟 What-If 生命周期 + hr 招聘漏斗 6 动作+hire 员工联动 + hr 休假审批+考勤打卡 + hr 排班 assignSingle/assignBatch/copyFromPeriod/calcAttendance + hr 排班 generateRotation 错峰引擎+regenerate=true 重建 + hr 排班 swap submit/approve/reject/cancel 状态机+双向 shiftId 交换 + hr 评估→差距→发展计划闭环 completeAssessment 自动 gap refresh+gapSeverity 多档+refreshGapAnalysisWithLevels+generateDevelopmentPlan+updatePlanItemStatus+completePlan + hr 员工调动 transferEmployee handleContract=YES/NO/AUTO 三分支合同联动 + aps 排产方案状态机 publish/archive + aps 工序排产引擎 scheduleForward/scheduleBackward + b2b EDI 信封状态机出站/入站生命周期+retry + logistics 发运单 advise→completeShipment→cancelShipment+mock 承运商网关）+ 2 跨域编排 spec（P2P + O2C）+ 1 域 reverseApprove DIRECT 轴 spec（P2P Invoice reverseApprove）+ 4 制造链编排 spec（WorkOrder+MaterialIssue+JobCard 三聚合根协作 + 生产差异计算 config-gated auto-calc + 批次基因追溯 config-gated write-on-completion + 质检门控 Gate 1 阻断完工）+ 2 反向冲销 spec（P2P + O2C 财务侧红冲）+ 1 看板前端渲染层 spec（10 域 visual）+ 1 报表前端渲染层 spec（24 域 visual）+ 1 报表下载产物回归层 spec（24 域 × {xlsx,pdf} = 48 用例）+ 1 报表 AMIS 下载按钮回归层 spec（5 域抽样 × {xlsx,pdf} = 10 用例，0413-1），含每测试 UI 登录；`--workers=1`）。

## 数据驱动数值断言层

冒烟套件（`*.smoke.spec.ts`）仅验证渲染存在性。在 1234-1 主数据 + 1445-1 P2P/O2C 交易 + 2210-1 运营域交易 + 0930-1 制造域 + 0930-2 维护+质量域 + 1045-1 CRM/CS/HR 域 + 1145-2 质量域 SPC + 0628-1 制造域工作中心配置链+crp_load 种子基线上，核心域（finance/sales/purchase）+ 运营域（inventory/assets/projects）+ 扩展域（manufacturing/maintenance/quality）+ 扩展域 CRM/客服/人力（纯报表域，无看板）+ 主数据域（master-data，看板 KPI + 2 预警 + 2 报表）叠加了**数据驱动数值断言层**（`*.value.spec.ts`，28 spec：dashboards/{finance,sales,purchase,inventory,assets,projects,manufacturing,maintenance,quality,master-data}.value + reports/fin-{balance-sheet,income-statement,ar-ap-aging}.value + reports/{ast-depreciation,inv-inventory-trace,prj-cost-summary,mfg-production-variance,mfg-forecast-variance,mfg-crp-load,mnt-maintenance-history,qa-inspection-summary}.value + reports/{crm-lead-conversion-funnel,crm-forecast-accuracy,cs-ticket-sla-csat,hr-employee-net-balance,hr-payroll-simulation-comparison}.value + reports/{md-material-price-list,md-partner-list}.value），验证「seed → 聚合 → GraphQL → 断言」端到端数值链。

### 断言范式

- **看板 KPI**：spec 内 `page.request.post('/graphql', { query, variables })`（复用 UI 登录会话 cookie）取后端 `ErpXxxDashboard__getDashboardKpi` 原始返回 Map，与期望值表逐字段 `expect(Number(field)).toBe(expected)` 比对。
  - **日期漂移防护**：销售/采购/库存/维护/质量看板 KPI 默认区间依赖服务端当前日期。spec **显式传 `startDate=2026-07-01` / `endDate=2026-07-31`** 覆盖种子日期区间（制造看板传 `2026-06-01`/`2026-07-31` 覆盖跨 6/7 月 COMPLETED 工单），使断言确定性不依赖运行时日期。财务看板传 `periodId=1` 锁定种子期间；资产看板传 `periodId="2026-07"` 锁定种子折旧期间；项目看板 `getDashboardKpi()`/`getProjectGrossMargin()` 无日期参数，聚合域表全量。质量看板 `getSpcOutOfControlWarning`（无参数）确定性断言非零 1/1/1（SPC 三表已 seed，1145-2）。
- **报表渲染**：`page.request.post` 取 `Erp{Fin,Ast,Inv,Prj,Mfg,Mnt,Qa,Crm,Cs,Hr,Md}Report__renderHtml` 返回 HTML 字符串，断言含期望数值 token（剥离千分位逗号后匹配，规避 AMIS 渲染层 DOM 抖动）。CRM/CS/HR 三域为**纯报表域（无看板）**，故仅有报表渲染断言；可选入参经 `data:{forecastId|ticketType|simulationId:...}` 内联 map 传入（镜像 fin-income-statement `data:{periodId}` 范式）。master-data 两报表（物料价格清单/往来单位清单）零参全量渲染，token 含 materialCode/partnerCode + dict code（materialType/partnerType 经 `orm_propValueByName` 取原始值）+ 数值（NumberFormat `#,##0.00` 剥离千分位）。
- **主数据域看板/预警**：master-data `ErpMdDashboard__getDashboardKpi` 零参全表内存聚合（materialCount/customerCount/vendorCount/inactiveMaterialCount/inactivePartnerCount），无 trend（设计权威 `dashboards.md` 明示「主数据看板无趋势图」）；2 预警（`findMaterialWithoutSkuAlert`/`findSkuWithoutPriceAlert`）断言空集（种子真实态：4 material 全有 SKU、4 SKU 全有 purchasePrice>0）。vendorCount 经 1145-1 修复（常量 `"VENDOR"`→`"SUPPLIER"` 对齐权威字典 `erp-md/partner-type`）后返回真实供应商计数 2。

### 期望值表（确定性派生）

期望值派生自固化 seed CSV 的确定性聚合，落盘于：
- `docs/analysis/2026-07-08-1445-2-kpi-expected-values.md`（核心域 finance/sales/purchase 看板 + finance 报表，每 KPI 标注期望值 + 派生公式 + seed 行依据）
- `docs/analysis/2026-07-08-2210-2-operational-kpi-expected-values.md`（运营域 inventory/assets/projects 看板 + 运营域报表）
- `docs/analysis/2026-07-09-0930-3-mfg-mnt-qa-kpi-expected-values.md`（制造/维护/质量域看板 + 报表）
- `docs/analysis/2026-07-09-1045-2-crm-cs-hr-report-expected-values.md`（CRM/客服/人力域 5 报表，纯报表域无看板）
- `docs/analysis/2026-07-09-1145-1-master-data-expected-values.md`（主数据域看板 KPI + 2 预警 + 2 报表）
- `docs/analysis/2026-07-09-1145-2-quality-spc-seed-table-map.md`（质量域 SPC 三表种子表映射 + Strategy C 裁决 + getSpcOutOfControlWarning 期望值派生；supersede 0930-3 的 SPC=0 + openNcrCount=2 行）
- `docs/analysis/2026-07-09-0628-1-crp-load-seed-table-map.md`（制造域工作中心配置链+crp_load 种子表映射 + capacityHours 口径 + Strategy C 裁决 + crp-load-report 期望值派生；解除 0930-1 crp_load Deferred）

当前基线值：

| 域 / 报表 | 断言字段 | 期望值 |
| --- | --- | --- |
| finance 看板 | revenue / netProfit | 1130 / 1130 |
| sales 看板 | salesAmount / orderCount | 1000 / 1 |
| purchase 看板 | purchaseAmount / orderCount | 850 / 1 |
| inventory 看板 | totalValue / incomingQty / outgoingQty | 10450 / 100 / 0 |
| assets 看板 | originalValue / accumulatedDepreciation / netBookValue / periodDepreciation | 135000 / 6000 / 129000 / 2000 |
| projects 看板 | openProjectCount / totalBudget / incurredCost / executionRate | 1 / 50000 / 30000 / 0.6 |
| projects 毛利率 | totalRevenue / totalGrossProfit / grossMarginPct | 50000 / 20000 / 0.4 |
| manufacturing 看板 | inProcessCount / periodCompletedQty / stockPartialCount / onTimeRate | 1 / 180 / 1 / 0.5 |
| CRP 负荷报表 | workcenterCode + loadDate + loadHours/capacityHours/loadRate token | WC-001 / 2026-07-15 / 4.00 / 8.00 / 0.50 |
| maintenance 看板 | equipmentTotal / runningCount / openRequestCount / periodVisitCount | 3 / 2 / 1 / 1 |
| quality 看板 | inspectionCount / passRate / rejectedCount / openNcrCount | 3 / 0.6666666666666666 / 1 / 3 |
| quality SPC 预警 | outOfControlChartCount / inadequateCapabilityCount / openSpcNcrCount | 1 / 1 / 1 |
| 利润表 | 主营业务收入 token | 1,130.00 |
| 资产负债表 | 银行存款 token | 169.50 |
| AR-AP 账龄 | 全结算态（空明细） | title 渲染 + 合计行存在 |
| 资产折旧明细表 | asset 2 原值/累计折旧/本期折旧/净值 token | 120000.00 / 6000.00 / 2000.00 / 114000.00 |
| 库存追溯链报表 | 移动单号/数量 token（moveId=1） | MV-2026-001 / 100.00 |
| 项目成本汇总表 | 预算/实际成本/执行率 token | 50000.00 / 30000.00 / 60.00% |
| CRM 线索转化漏斗 | stageName + leadCount + expectedRevenue token | 验证 / 报价 / 50000.00 / 80000.00 |
| CRM 销售预测准确率 | commit/weighted/bestCase + lineWeightedRevenue token（forecastId=1） | 50000.00 / 45000.00 / 80000.00 / 63000.00 |
| CS 工单 SLA/CSAT 综合 | ticketTypeName + avgCsat/avgNps token（ticketType=1 投诉桶） | 投诉 / 5.00 / 9.00 |
| HR 员工净余额 | partnerName + advance/expense/netBalance token | 张三员工往来 / 1000.00 / 700.00 / 员工欠公司 |
| HR 薪酬模拟对比 | employeeName + salaryItemCode + original/adjusted + 部门小计 token（simulationId=1） | 赵明 / 钱华 / BASE_SALARY / 10000.00 / 11000.00 / 部门小计 |
| master-data 看板 | materialCount / customerCount / vendorCount / inactiveMaterialCount / inactivePartnerCount | 4 / 2 / 2（修复后）/ 0 / 0 |
| master-data 预警 | findMaterialWithoutSkuAlert / findSkuWithoutPriceAlert 行数 | 0 / 0（种子全有 SKU、全有 purchase 价） |
| 物料价格清单报表 | materialCode + materialType code + 采购/销售/零售价 token | MAT-001 / MAT-002 / FINISHED_PRODUCT / 120.00 / 200.00 / 280.00 / 300.00 / 500.00 / 680.00 |
| 往来单位清单报表 | partnerCode + partnerType code + creditLimit token | CUST-001 / CUST-002 / SUP-001 / SUP-002 / CUSTOMER / SUPPLIER / 500000.00 / 300000.00 |

### seed 漂移同步机制（强制）

**期望值表依赖 seed CSV 内容**。若 seed 变更（行增减 / 金额改动 / 业务日期/period 改动）：
1. 重新派生期望值（手算或 GraphQL 抽样导出）。
2. 同步更新对应分析文档（核心域 `2026-07-08-1445-2-kpi-expected-values.md` / 运营域 `2026-07-08-2210-2-operational-kpi-expected-values.md` / 制造+维护+质量域 `2026-07-09-0930-3-mfg-mnt-qa-kpi-expected-values.md` / CRM+CS+HR 域 `2026-07-09-1045-2-crm-cs-hr-report-expected-values.md`）。
3. 同步更新对应 `*.value.spec.ts` 的 `expected` / `expectedTokens`。

不更新则数值断言会失败（这是设计预期——暴露 seed 漂移，非测试脆弱）。manufacturing/maintenance/quality 域数值断言层已落地（0930-3，期望值表 `docs/analysis/2026-07-09-0930-3-mfg-mnt-qa-kpi-expected-values.md`）；CRM/客服/人力域报表数值断言层已落地（1045-2，期望值表 `docs/analysis/2026-07-09-1045-2-crm-cs-hr-report-expected-values.md`）；主数据域看板/报表数值断言层已落地（1145-1，期望值表 `docs/analysis/2026-07-09-1145-1-master-data-expected-values.md`，含 vendorCount 字典值漂移修复）；制造域 CRP 负荷报表数值断言层已落地（0628-1，期望值表 + capacityHours 口径 `docs/analysis/2026-07-09-0628-1-crp-load-seed-table-map.md`，完成全报表域数值断言覆盖里程碑）；其余扩展域（logistics/b2b/contract/drp/aps，无看板无报表）数值断言归后续批次（触发条件：对应域有可断言看板/报表时）。

## CRUD 数据驱动列表断言层 + 写路径验证

在数值断言层与 CRUD 冒烟层之上，0628-2 叠加了 CRUD 读写两面的数据驱动验证（解除 1234-2 两项 Deferred）：

### 列表断言层（`*.list-value.spec.ts`，13 域）

13 域代表性实体（有 seed 行的）经 GraphQL `ErpXxx__findPage(query:{offset:0,limit:200})` 查询，断言列表返回 seed 行（`items.length >= seed 行数` + JSON body 含关键字段 token）。调 `_helper.ts` 的 `assertCrudListValues({ entityName, route, expectedCount, expectedTokens, fields })` 委派，镜像 dashboards/reports helper 委派范式。

| 域 | 实体 | seed 行数 | token 依据 |
| --- | --- | --- | --- |
| master-data | ErpMdPartner | 5 | CUST-001/SUP-001/CUSTOMER/SUPPLIER |
| inventory | ErpInvStockMove | 1 | MV-2026-001/INCOMING |
| purchase | ErpPurOrder | 1 | PO-2026-001 |
| sales | ErpSalOrder | 1 | SO-2026-001 |
| finance | ErpFinVoucher | 4 | PZ-2026-001/TRANSFER |
| assets | ErpAstAsset | 3 | AST-2026-001/AST-2026-002 |
| projects | ErpPrjProject | 1 | PRJ-2026-001 |
| manufacturing | ErpMfgWorkOrder | 4 | WO-2026-001/COMPLETED/IN_PROCESS |
| quality | ErpQaInspection | 3 | INS-2026-001/ACCEPTED/REJECTED |
| maintenance | ErpMntVisit | 2 | VIS-2026-001/PLANNED |
| crm | ErpCrmLead | 2 | LEAD-2026-001/OPPORTUNITY |
| cs | ErpCsTicket | 2 | TKT-2026-001/HIGH |
| hr | ErpHrEmployee | 2 | HR-EMP-001/HR-EMP-002 |

### 写路径验证（`*.write.spec.ts`，master-data GraphQL + master-data AMIS + quality/maintenance GraphQL）

master-data ErpMdPartner 经 `runCrudWriteCycle`（GraphQL 层）+ `runAmisFormWrite`（AMIS 表单层）完成 create→get 验证→update→get 验证→delete→get 验证（not-found）全链；quality `ErpQaRiskRegister`（含 dict `status`）+ maintenance `ErpMntEquipmentCategory` 经 `runCrudWriteCycle` 补非 master-data 域覆盖。

**写机制说明（关键发现 + 0814-1 修复）**：
- 写持久化经 GraphQL mutation（`page.request.post('/graphql')`，复用 UI 登录会话 cookie）调用平台标准 `ErpXxx__save`/`ErpXxx__update`/`ErpXxx__delete`（同一 `CrudBizModel` 方法，AMIS 表单按钮提交经 `/graphql` 走相同 mutation）。
- **序列碰撞已修复（0814-1）**：平台 `SysSequenceGenerator` 默认序列 `nextValue` 初始化为 1（`addDefaultSequence`），但种子 CSV 显式插入 id=1~8，导致 create 主键冲突。`_init-data/zz-sequence-advance.sql`（0814-1 新增）在种子 CSV 加载后 `MERGE` 创建 default 序列行 `NEXT_VALUE=100000`（> 种子 id 上限 8）；因 `SysSequenceGenerator.lazyInit()` 是 `ioc:delay-method`（所有 bean 启动后才运行），`.sql` 执行时表为空，`MERGE` 创建行后 `addDefaultSequence()` 的 `if(!exists)` 守卫跳过，advanced 值保留。**效果**：首 GraphQL/AMIS save 即返回 id≥100000，无主键碰撞。
- **warm-up 重试已简化（0814-1）**：0628-2 helper 的 `MAX_SEQ_RETRIES=30` warm-up 重试循环（序列碰撞时逐次消费 id 直到越过种子区间）经 0814-1 序列修复后不再需要，简化为**单次容错**（最多 1 次重试，仅防御并发/唯一 code 瞬态冲突）。
- `update` mutation 支持部分字段更新（仅需 id + 待改字段）；`delete` 为逻辑删除（`delVersion`），删除后 `__get` 返回 not-found。
- **状态隔离**：fresh-DB 每次启动重置（webServer 默认 `rm -f db/erp.mv.db`）+ 同运行内唯一 code（`E2E-{entityName}-{ts}`）。
- **AMIS 表单写路径（0814-1 新增）**：`runAmisFormWrite` 经浏览器 UI 点「新增」→填表单（文本 + dict 下拉）→「确认」→列表/GraphQL 验证→行操作「编辑」→验证更新→（delete）。dict 下拉经 DOM evaluate 定位（label/option 多 locale 变体 + dict value code 匹配，规避 zh/en locale 漂移）。**已知限制**：AMIS action-group dropdown 的 Delete action（gated by confirmText）在 Playwright 下不触发其 confirm/API（Edit action 直接开 dialog 正常）→ AMIS spec 的 delete 改用同一 GraphQL `__delete` mutation（UI 按钮调用的同一端点），delete 机制本身由 GraphQL 层写 spec 独立证明。seq-default id 字段在 add 表单被标 mandatory（ORM 仍服务端生成实际 id，表单值仅满足客户端校验）。

## 业务动作浏览器层 E2E（`business-actions/`，17 域 84 spec）

在 CRUD 读写路径之上，0814-2 叠加了自定义 `@BizMutation` 经 GraphQL `/graphql` 的全栈可达性 + 状态机迁移验证（解除 0628-2 Deferred「复杂业务动作 E2E」），覆盖 3 个代表域的非审批状态机/过账动作。2026-07-09-2004-1 将覆盖由 3 域扩展至 6 域：新增 maintenance Visit（5 态+设备联动副作用）、projects Task（4 态+前驱 DAG 门控）、quality CAPA（3 态）+ NCR（无 CAPA 路径），进一步验证三原语 helper 范式在多型状态机（含副作用联动、DAG 门控、过账标志）下的可复用性。2026-07-10-0335-1 将覆盖扩展至 10 域：新增 4 个 DIRECT useApproval 审批轴实体（manufacturing WorkOrder / purchase Return / sales Return / quality Recall），验证审批轴 `submitForApproval`→`approve`→`approveStatus` 翻转 + reject 守卫 + 审批后域特定状态迁移 + Return approve 的 `posted` 布尔翻转（过账触发可观测）。2026-07-10-0335-2 补齐两条 DIRECT 业务动作路径：NCR resolve CAPA 闭包门控（跨实体依赖门控负/正路径）+ maintenance Request 自定义 status 5 态状态机（use-approval tagSet 但无 approveStatus 列，自定义 status 驱动），验证三原语范式在跨实体依赖门控 + 自定义（非平台审批轴）状态机下的可复用性。2026-07-10-1800-1 新增 NCR SCRAP 过账凭证行断言（resolve AUTO_POST → NcrPostingDispatcher → NcrScrapAcctDocProvider → Dr 6711 / Cr 1401），验证凭证行级科目码/金额在 quality 域过账下的正确性。2026-07-12-0204-2 新增 finance 核销单 `ErpFinReconciliation` 生命周期（DIRECT `@BizMutation` create→post→reverse + 双面对账 `@BizQuery` + 5 validateLine 守卫负路径），解除 finance 核销作为核心域 business-actions E2E 最大缺口（核销独立作用于辅助账 ErpFinArApItem，不经 xwf；post/reverse 对辅助账 openAmount/status 回写断言 + 自包含 partner 隔离使往来余额字段随 cleanup 消失）。useWorkflow（xwf）域经 2330-1 权威裁决浏览器层不可行，排除。

| 域 | 实体/动作 | spec | 验证内容 |
| --- | --- | --- | --- |
| inventory | `ErpInvStockMove` generateMove/complete/cancel | `inventory-stock-move.action.spec.ts` | 状态机 DRAFT→CONFIRMED→DONE + 过账型下游产物（不可变流水 `ErpInvStockLedger` 非空）+ cancel 异常路径 + CONFIRMED 态 confirm 拒绝守卫 |
| crm | `ErpCrmLead` qualify/moveStage/cancel | `crm-lead.action.spec.ts` | docStatus NEW→QUALIFIED + 漏斗 stageId 翻转（convLog 留痕归 Deferred）+ cancel |
| cs | `ErpCsTicket` assign/start/resolve/close/cancel | `cs-ticket.action.spec.ts` | 六态状态机 NEW→ASSIGNED→IN_PROGRESS→RESOLVED→CLOSED 全链 + cancel + 非法迁移 ErrorCode 守卫 |
| cs | `ErpCsCannedResponse` suggestForTicket/renderTemplate/applyCannedResponse | `cs-canned-response.action.spec.ts` | 三级宏匹配（精确>类型>全局兜底）+ 系统变量替换（customer_name/ticket_id/agent_name）+ usageCount 递增 + TicketAction NOTE 审计写入（plan 2026-07-11-1234-2） |
| maintenance | `ErpMntVisit` schedule/start/complete/cancel | `maintenance-visit.action.spec.ts` | 5 态状态机 DRAFT→SCHEDULED→IN_PROGRESS→COMPLETED + cancel（SCHEDULED→CANCELLED）+ 设备状态联动副作用（start→UNDER_MAINTENANCE，complete/cancel→RUNNING 恢复种子态）+ COMPLETED→start 非法迁移守卫（2004-1） |
| maintenance | `ErpMntRequest` accept/startRepair/complete/rejectRequest/cancel | `mnt-request.action.spec.ts` | 自定义 status 5 态状态机（ORM use-approval tagSet 但无 approveStatus 列，生命周期由自定义 status 驱动）：正路径 OPEN→ACCEPTED→IN_PROGRESS→COMPLETED(+completedAt) + COMPLETED→accept 非法守卫 + 分支 rejectRequest(→REJECTED)/cancel(→CANCELLED)；accept 生成响应式 visit 按 code 清理避免污染 periodVisitCount（0335-2） |
| maintenance | `ErpMntRequest` accept → 响应式 Visit 副作用编排 | `mnt-request-visit-orchestration.action.spec.ts` | accept 经 `generateResponsiveVisit` 创建响应式 DRAFT Visit 副作用字段精确断言：code 精确匹配 `VST-REQ-{requestId}`（eqFilter 非 like）/ equipmentId 一致 / visitDate == 服务端当日（同事务 createTime 日期部分，时区无关）/ visitType=RESPONSIVE / status=DRAFT / assignedTo 回退 request.requestedBy；visitDate 落看板区间须清理（2329-2） |
| projects | `ErpPrjTask` startTask/completeTask/blockTask/unblockTask | `projects-task.action.spec.ts` | 4 态状态机 TODO→IN_PROGRESS→DONE + blockTask/unblockTask 闭环（reason 必填）+ 前驱 DAG 门控（无前驱任务绕过 STRICT）+ DONE→startTask/blockTask 非法迁移守卫（2004-1） |
| projects | `ErpPrjTimesheet` submit/approve/cancel（工时过账生命周期） | `projects-timesheet-posting.action.spec.ts` | DIRECT `@BizMutation` 工时状态机 UNSUBMITTED→SUBMITTED→APPROVED(posted=true) + PROJECT_COST_COLLECTION 业财过账凭证行精确数值断言（Dr 5101 项目成本 / Cr 2211 应付职工薪酬，amount=hours×costRate=800 确定性，CostRateResolver 优先级 1 单填 costRate）+ cancel reverse 红冲凭证行同向取负（Dr 5101=-800/Cr 2211=-800）+ 2 非法守卫（UNSUBMITTED→approve / APPROVED→submit）；自包含 ProjectType(defaultSubjectId=5101 行 id)+Project(OPEN)+Task(IN_PROGRESS)+Timesheet(costRate/orgId 必填驱动 acctSchema 解析)；config `erp-prj.default-payroll-subject-id=2211`（科目编码非行 ID）（0742-2） |
| projects | `ErpPrjProjectSettlement` createSettlement/submit/approve/reverseSettlement（结算过账 CLOSE） | `projects-settlement-posting.action.spec.ts` | DIRECT `@BizMutation` 结算三轴状态机 createSettlement(CLOSE,UNSUBMITTED)→submit(SUBMITTED)→approve(APPROVED+posted=true+跨域建资产卡片 IN_SERVICE) + PROJECT_SETTLEMENT 业财过账凭证行精确数值断言（CLOSE 资本化 Dr 1601 固定资产 / Cr 1603 在建工程，amount=finalCost=1000 确定性）+ reverseSettlement reverse 红冲凭证行同向取负（Dr 1601=-1000/Cr 1603=-1000）+ 资产卡片 status 回退 DRAFT 断言 + 非法守卫（UNSUBMITTED→approve）；PnL 快照硬前置（先 refreshPnl 产 CALCULATED 快照，createSettlement 缺快照抛 ERR_SETTLEMENT_PNL_SNAPSHOT_MISSING）；自包含 Project+CostCollection+Line(MATERIAL,1000)→refreshPnl→finalCost=1000；spec cleanup 显式清理跨域资产卡片（reverseSettlement 仅回退 status 不删除）（0742-2） |
| quality | `ErpQaAction`（CAPA）startAction/completeAction/verifyAction | `quality-capa.action.spec.ts` | 3 态状态机 PENDING→IN_PROGRESS→COMPLETED + verifyAction 填充验证字段（不改 status）+ COMPLETED→completeAction 守卫（2004-1，最小状态机证明范式可复用性） |
| quality | `ErpQaNonConformance`（NCR）submitReview/escalateToRecall/cancel | `quality-ncr.action.spec.ts` | 无 CAPA 路径状态机 OPEN→IN_REVIEW→cancel(CANCELLED) / escalateToRecall(ESCALATED_TO_RECALL) + 非法迁移守卫；resolve（CAPA 闭包门控）+ postNcr/reverseNcr（需 RESOLVED）归 successor（2004-1） |
| quality | `ErpQaNonConformance`（NCR）resolve（CAPA 闭包门控） | `quality-ncr-resolve-capa-gate.action.spec.ts` | 跨实体依赖门控：负路径（CAPA 仍 PENDING 时 resolve 抛 ERR_NCR_RESOLVE_CAPA_NOT_COMPLETED 对应 message + status 不变 IN_REVIEW）+ CAPA 三步闭包（start→complete→verify）+ 正路径（全 CAPA 闭包后 resolve 成功 status=RESOLVED）；dispositionType=CONCESSION 干净隔离门控（RETURN/SCRAP 触发过账/退货副作用归 successor）（0335-2） |
| manufacturing | `ErpMfgWorkOrder` submitForApproval/approve/reject + checkAvailability/start/close | `mfg-work-order.action.spec.ts` | DIRECT useApproval 审批轴 UNSUBMITTED→SUBMITTED→APPROVED（+ docStatus DRAFT→SUBMITTED→NOT_STARTED）+ reject(REJECTED) 守卫 + 审批后域迁移 checkAvailability(STOCK_RESERVED，无子件 BOM 作 enabler)→start(IN_PROCESS)→close(CLOSED)（0335-1） |
| purchase | `ErpPurReturn` submitForApproval/approve/reject + cancel | `pur-return.action.spec.ts` | DIRECT useApproval 审批轴 + approve→posted=true（反向出库+PURCHASE_RETURN 红字过账）；approve 前置复用 runP2pChain 产 approved Receive + 行 + qty 校验 + reason；reject/cancel 路径 + 非法守卫（0335-1） |
| sales | `ErpSalReturn` submitForApproval/approve/reject + cancel | `sal-return.action.spec.ts` | DIRECT useApproval 审批轴 + approve→posted=true（反向入库+SALES_RETURN 凭证+负 AR 辅助账）；approve 前置复用 runO2cChain 产 approved Delivery + 行 + qty 校验 + reason；reject/cancel 路径 + 非法守卫（0335-1） |
| quality | `ErpQaRecall` register/submitForApproval/approve/reject + locateTargets/notifyCustomers/close/cancel | `qa-recall.action.spec.ts` | DIRECT useApproval 审批轴 + 双字段翻转（approveStatus=APPROVED + status=APPROVED）+ 域状态机 locateTargets(IN_PROGRESS，ErpInvBatch 作批次门控 enabler)→notifyCustomers(notifyCustomer=true)→close(CLOSED) + reject(REJECTED+status=CANCELLED)/cancel 守卫（0335-1） |
| quality | `ErpQaNonConformance`（NCR）resolve SCRAP 过账凭证行 | `quality-ncr-scrap-posting.action.spec.ts` | resolve AUTO_POST → NcrPostingDispatcher.dispatchScrap → NcrScrapAcctDocProvider → NCR_SCRAP 凭证行 Dr 6711=120/Cr 1401=120 精确数值断言（SCRAP_AMOUNT=qty1×seed avgCost120）+ posted=true + reverseNcr 红冲清理（1800-1） |
| manufacturing | `ErpMfgWorkOrder` stop/resume/close/cancel + 部分完工 reportCompletion(willFinish) | `mfg-work-order-exception.action.spec.ts` | 异常分支正路径 A: stop(IN_PROCESS→STOPPED)→resume(→IN_PROCESS)→close(→CLOSED+actualEndDate)；正路径 B: stop→close(从 STOPPED 可达)；正路径 C: cancel(NOT_STARTED→CANCELLED)；非法迁移守卫 IN_PROCESS→cancel 拒绝 + CLOSED→stop/resume 拒绝；部分完工 reportCompletion(completedQty=10<plannedQty=20)→willFinish=false→docStatus 保持 IN_PROCESS+completedQuantity=10→close→CLOSED（非 COMPLETED）（1800-2） |
| quality | `ErpQaNonConformance`（NCR）reverseNcr 红字凭证行 | `quality-ncr-reverse-voucher-line.action.spec.ts` | resolve AUTO_POST 产 NCR_SCRAP 正向凭证 → reverseNcr 红冲 → 红字凭证行同向取负断言（Dr 6711=-120/Cr 1401=-120，buildReversalDraft dcDirection 不变金额取负）+ 原正向凭证 isReversed=true 凭证级回退标记（markOriginalVoucherReversed，O-8 统一行为，区别于 1800-1 仅断言 ncr.posted=false 实体级回退）（0730-2） |
| quality | `ErpQaNonConformance`（NCR）resolve RETURN 处置退货编排 | `quality-ncr-return-disposition.action.spec.ts` | resolve RETURN+supplierId 非空 → NcrReturnOrchestrator.orchestrateReturn → IErpPurReturnBiz.save 创建采购退货单（code=PR-FROM-NCR-{ncrId}，warehouseId/currencyId 据 materialId 查 ErpInvStockBalance）+ ncr.returnCode 登记 + ErpPurReturn supplierId/docStatus=DRAFT/approveStatus=UNSUBMITTED 验证；CONCESSION 对照 returnCode=null 无退货副作用；无 CAPA 简化 setup 对齐 0335-2（0730-2） |
| inventory | `ErpInvStockMove` reverse 物理冲销 | `inventory-stock-move-reverse.action.spec.ts` | `reverse(moveId)` 校验 DONE → 构造反向移动单（inverseMoveType INCOMING↔OUTGOING + source/dest swap + relatedBillType=REVERSAL + relatedBillCode 挂链 + originReturnedMoveId 追溯）→ business-linked auto-DONE；非 DONE 守卫（CONFIRMED → ERR_REVERSE_NOT_DONE）（1234-1） |
| purchase | `ErpPurInvoice` reverseApprove DIRECT 轴审批逆向 | `p2p-reverse-approve.spec.ts` | 域侧 DIRECT 审批逆向（与 p2p-reverse.spec.ts 财务侧 ErpFinVoucher__reverse 互补）：runP2pChain 产 posted Invoice → `ErpPurInvoice__reverseApprove` → approveStatus APPROVED→REJECTED + posted true→false + postedAt/postedBy null + postingDispatcher.reverse 红字凭证生成 + 原 NORMAL 凭证 isReversed=true（1234-1） |
| quality | `ErpQaRecall` generateReturns 跨域建退货单 | `quality-recall-generate-returns.action.spec.ts` | generateReturns 跨域编排：自包含批次追溯 setup（ErpInvBatch + INCOMING 批次备货 + ErpSalDelivery+Line + OUTGOING 业务联动批次出库 → batchTrace 追溯 → RecallTargetLocator 定位）→ register→submit→approve→locateTargets(→IN_PROGRESS)→notifyCustomers→generateReturns → RecallTarget RETURNED + generatedReturnId 非空 + ErpSalReturn 创建（customerId=target.partnerId 匹配 + docStatus=DRAFT）（1234-1） |
| finance | `ErpFinVoucher` post（业财过账工厂+入口） | `finance-voucher-post.action.spec.ts` | `IErpFinVoucherBiz.post(PostingEvent)` 浏览器层全栈可达性：构造最小 LANDED_COST(490) PostingEvent（billData=ALLOCATIONS+COST_ELEMENTS，PostingEventInput 经 GraphQL variable，businessType 枚举以 String scalar 传递）→ 原始 mutation（Long scalar return 无选择集，对齐 reverse 范式）→ 断言凭证头（voucherType=TRANSFER/postingType=NORMAL/docStatus=POSTED/totalDebit=totalCredit=100）+ 业财回链 ErpFinVoucherBillR（billCode/businessType=LANDED_COST）+ 凭证行 Dr 1401=100/Cr 2202=100 + 幂等路径（重复 post 同 billHeadCode 返回 null）；断言凭证级 docStatus=POSTED 而非 posted（凭证无 posted 列语义，2329-2） |
| finance | `ErpFinReconciliation` 核销生命周期（create→post→reverse + 双面对账查询） | `fin-reconciliation.action.spec.ts` | DIRECT `@BizMutation` 核销生命周期（不经 xwf）：自包含建 OPEN 对（新建 partner `E2E-RECON-PN-` + 2 行 ErpFinArApItem：AP_INVOICE + PAYMENT 同 partner+direction+金额）→ `create(direction,partnerId,businessDate,lines:List<ReconciliationLineInput>)`（lines 经 `input('[i_app_erp_fin_dao_dto_ReconciliationLineInput]', [...])` typed variable）断言 DRAFT → `post` 断言 POSTED + 双方辅助账 openAmount→0/status=SETTLED → `reverse` 断言 REVERSED + openAmount 恢复/status=OPEN + `checkDualSideConsistency @BizQuery` 返回 DualSideDiffReport 结构非空 + 5 负路径守卫（direction/partner/item-not-open/over-amount/date-before-invoice 各 ErrorCode message token）；cleanup 删 partner 使 PartnerBalanceUpdater 写入的余额字段消失，finance 看板/ar-ap-aging 基线无漂移（0204-2） |
| finance | `ErpFinBankReconciliation` 银行对账生命周期（generate→post→reverse） | `fin-bank-recon.action.spec.ts` | DIRECT `@BizMutation` 银行对账三态状态机（docStatus DRAFT→POSTED→CANCELLED，复用 `erp-fin/voucher-status`，红冲回退=CANCELLED 非 REVERSED）：自包含建 `ErpFinFundAccount`(BANK, subjectId=1002) + `ErpFinBankStatement`+Line（1 行 UNMATCHED CREDIT 100，满足平衡等式 statementBalance(1100) − bookBalance(1000) = bankCreditUnrecorded(100) − bankDebitUnrecorded(0)）→ `generate(statementId)` 断言 DRAFT → `post(reconciliationId)` 断言 POSTED + unmatched 行存在故产 `BANK_RECON_ADJ` 调整凭证（Dr 1002 / Cr 2240OTHER 各 100，经 ErpFinVoucherBillR billCode=recon.code 反查）+ **凭证行数值断言**（NORMAL Dr 1002=100/Cr 2240OTHER=100，plan 2026-07-12-1321-2）→ `reverse(reconciliationId)` 断言 CANCELLED + REVERSAL 红冲凭证（postingType 区分）+ **红冲凭证行数值断言**（同向取负 Dr 1002=-100/Cr 2240OTHER=-100，plan 2026-07-12-1321-2）+ 2 非法迁移守卫（POSTED→post / DRAFT→reverse 各 ErrorCode token）；科目 2240OTHER 经种子补齐（0413-2） |
| finance | `ErpFinBadDebt` 坏账生命周期（writeOff→submit→approve + recover + runBadDebtProvision） | `fin-bad-debt.action.spec.ts` | DIRECT `@BizMutation` 坏账三路径（审批门控 `erp-fin.bad-debt-write-off-require-approval` 默认 true）：(a) 核销正路径 自包含建 partner `E2E-BADDEBT-PN-` + OPEN RECEIVABLE ErpFinArApItem → `writeOff(arApItemId,reason)` 创建=UNSUBMITTED（不立即变异）→ `submit`→SUBMITTED → `approve`→APPROVED + executeWriteOff（ArApItem OPEN→WRITTEN_OFF + openAmount→0 + BAD_DEBT_WRITE_OFF 凭证 借Allowance(1231)/贷AR(1122)，不进 P&L，经 CloseVoucherWriter 直接持久化）+ **凭证行数值断言**（Dr 1231=100/Cr 1122=100，plan 2026-07-12-1321-2）；(b) 收回路径 先 writeOff→approve 建 WRITTEN_OFF → `recover(arApItemId,reason)`→submit→approve + executeRecovery（ArApItem WRITTEN_OFF→OPEN + openAmount 恢复 + BAD_DEBT_RECOVERY 凭证）+ **凭证行数值断言**（Dr 1122=100/Cr 1231=100，plan 2026-07-12-1321-2）；(c) 期末计提 `runBadDebtProvision(periodId)` 返回 BadDebtProvisionResult（action ∈ {RESERVE,RELEASE,NONE} + 结构非空 + voucherId 与 action 一致性）+ **RESERVE 凭证行数值断言**（Dr 6701/Cr 1231，金额=requiredProvision−allowanceBalance 动态计算，plan 2026-07-12-1321-2）；状态字段 ErpFinBadDebt.approvalStatus（UNSUBMITTED/SUBMITTED/APPROVED/REJECTED）+ ArApItem.status（OPEN/WRITTEN_OFF）；科目 1231/6701 经种子补齐 + 3 webServer JVM 属性（allowance/expense/ar-subject-code，0413-2） |
| finance | `ErpFinReconciliation` 自动核销三策略（runAutoReconciliation） | `fin-auto-recon.action.spec.ts` | DIRECT `@BizMutation` `runAutoReconciliation(direction,partnerId,strategy)` 浏览器层 E2E（config 门控 `erp-fin.auto-reconcile=true`）：自包含建 partner `E2E-AUTORECON-PN-` + OPEN RECEIVABLE AR 对（AR_INVOICE + RECEIPT 同 partner+金额）→ `runAutoReconciliation(direction:"RECEIVABLE",partnerId,strategy)` 断言 reconciliationIds 非空（核销单自动 create+post）+ 双方辅助账 openAmount→0/status=SETTLED + 核销单 docStatus=POSTED；三策略各 1 用例（FIFO 按到期日升序逐笔核销 / BY_AMOUNT 精确金额 1:1 匹配 / BY_RATIO 按余额比例分摊尾差归末行）；cleanup 删核销单+行+辅助账+partner；**后端 flush 修复**（runAutoReconciliation create→post 间补 `orm().flushSession()`，解除 loadLines SQL 看不到未 flush INSERT 的 session 可见性 gap，镜像既有 flushBeforeBalance 范式）；webServer JVM arg 追加 `-Derp-fin.auto-reconcile=true`（plan 2026-07-12-1321-2） |
| finance | `ErpFinBudgetScenario` 预算方案生命周期 + BUDGET 影子凭证（submit/approve/reject/cancel） | `fin-budget-scenario.action.spec.ts` | DIRECT `@BizMutation` 预算方案审批生命周期（自定义状态机 DRAFT→SUBMITTED→APPROVED/CANCELLED + REJECTED，无 useWorkflow/useApproval）：自包含建 scenario(DRAFT, controlLevel=NONE) + 2 预算行（DEBIT 费用 6601 id=8 + CREDIT 收入 5001 id=6 同 periodId=1）→ `submit`→SUBMITTED → `approve`→APPROVED + scenario.voucherId 回写 + **BUDGET 影子凭证行数值断言**（经新增 `findBudgetVoucherIdByCode(code,reversal)` 按 postingType=BUDGET + reversalOfVoucherId IS NULL/IS NOT NULL 区分正向/红冲；正向 Dr 6601=100/Cr 5001=100，借贷方向按 ErpMdSubject.direction）→ `reject`→REJECTED → `cancel`→CANCELLED + **红冲 BUDGET 凭证行同向取负**（Dr 6601=-100/Cr 5001=-100）+ 原正向凭证 isReversed=true；2 非法迁移守卫（DRAFT→approve / APPROVED→submit 各 ErrorCode「不允许此操作」）；初始 approveStatus=UNSUBMITTED（wf/approve-status 字典无 DRAFT，浏览器层 __save 受字典校验约束）（plan 2026-07-14-0606-1） |
| finance | 预算控制 hook（PO approve）+ `getBudgetVsActual` 对比查询 | `fin-budget-control.action.spec.ts` | 预算控制 SPI（`IErpFinBudgetControlBiz.check`）经 `ErpPurOrderProcessor.validateBusinessRulesForApprove` 钩子触发（config 双门控 `erp-fin.budget-check-enabled=true` + `erp-fin.budget-purchase-expense-subject-code=6601`）：(a) **HARD 阻断** 自包含建+approve 预算方案(controlLevel=HARD, budget 100) + PO(totalAmountWithTax=200>余量) → submitForApproval→SUBMITTED → approve 抛 ERR_BUDGET_EXCEEDED「预算超支」+ approveStatus 保持 SUBMITTED + **无 ControlLog 持久化**（writeControlLog(BLOCKED) 与 throw 同处 @BizMutation 事务，NopException 触发回滚，经运行验证）；(b) **WARN 放行** controlLevel=WARN → approve→APPROVED + ControlLog actionResult=WARNED 持久化（approve 提交事务）；(c) **getBudgetVsActual** approve 方案后 `ErpFinBudgetLine__getBudgetVsActual` @BizQuery 返回 BudgetVsActualRow 列表（复杂类型须显式 selection set）+ budgetAmount>=setup/actualAmount 结构断言；webServer JVM arg 追加 `-Derp-fin.budget-check-enabled=true -Derp-fin.budget-purchase-expense-subject-code=6601`（向后兼容：无匹配预算行维度 PASS）（plan 2026-07-14-0606-1） |
| finance | `ErpFinExpenseClaim` 费用报销生命周期 + EXPENSE_CLAIM 凭证行 | `fin-expense-claim.action.spec.ts` | DIRECT useApproval 审批轴三轴状态机（approveStatus UNSUBMITTED→SUBMITTED→APPROVED + posted）+ EXPENSE_CLAIM 业财过账凭证行精确数值断言：自包含建测试 partner(EMPLOYEE)+employee(partnerId 非空，种子员工 partnerId=null 须自包含) + ExpenseClaim(DRAFT/UNSUBMITTED + 1 行 amountWithoutTax=100/taxAmount=13/amountWithTax=113) → submitForApproval→SUBMITTED → approve→APPROVED+posted=true + **OWN_ACCOUNT 凭证行**（Dr 6602=100/Dr 2221=13/Cr 2241=113，billHeadCode=claim.code）+ **COMPANY_ACCOUNT 凭证行**（Cr 1002=113 非 2241，公司直付不挂应付-员工辅助账）+ reverseApprove 红冲（APPROVED→REJECTED+posted=false + REVERSAL 凭证行同向取负 Dr 6602=-100/Dr 2221=-13/Cr 2241=-113 + 原凭证 isReversed=true）+ 2 非法守卫（UNSUBMITTED→approve / APPROVED→submitForApproval 各「不允许执行该操作」）；approve 不改 docStatus（仅 approveStatus+posted）；科目 2241 其他应付款-员工经种子补齐（1218-2）；ExpenseClaimAcctDocProvider 硬编码 6602（非读行 subjectId）；config `erp-fin.expense-budget-check-enabled=true` 启用后无匹配预算 PASS-on-no-match 向后兼容（plan 2026-07-14-1218-2） |
| finance | ExpenseClaim 预算控制 hook（HARD 阻断 + WARN 放行） | `fin-expense-claim-budget.action.spec.ts` | 预算控制 SPI 经 `ErpFinExpenseClaimProcessor.runBudgetCheckHook` 钩子触发（config 独立门控 `erp-fin.expense-budget-check-enabled=true` + `erp-fin.budget-expense-subject-code=6602`，与 PO 的 budget-check-enabled/budget-purchase-expense-subject-code 分离）：(a) **HARD 阻断** 自包含建+approve 预算方案(controlLevel=HARD, budget 6602 id=31 amount=50, periodId=1, costCenterId=null) + ExpenseClaim(amountFunctional=100>余量50) → submitForApproval→SUBMITTED → approve 抛 ERR_BUDGET_EXCEEDED「预算超支」+ approveStatus 保持 SUBMITTED + **无 ControlLog 持久化**（事务回滚）；(b) **WARN 放行** controlLevel=WARN → approve→APPROVED+posted=true + ControlLog actionResult=WARNED 持久化 + sourceBillType=EXPENSE_CLAIM；budget check 金额=claim.getAmountFunctional()（非 amountWithoutTax）；webServer JVM arg 追加 `-Derp-fin.expense-budget-check-enabled=true -Derp-fin.budget-expense-subject-code=6602`（plan 2026-07-14-1218-2） |
| finance | `getBudgetVsActual` 预算对比报表数值断言 | `fin-budget-vs-actual.value.spec.ts` | `ErpFinBudgetLine__getBudgetVsActual` @BizQuery 的 budgetAmount/actualAmount/availableAmount 三值增量断言（初始→actual 增量→红冲回退）：自包含建预算方案(NONE, budget 6602 amount=1000, periodId=1, costCenterId=null)+approve→BUDGET 凭证 Dr 6602=1000 → **初始** budgetAmount=1000/actualAmount=0/availableAmount=1000（subjectName="折旧费用"按种子 ErpMdSubject.name 断言）→ approve ExpenseClaim(amountWithoutTax=200)→NORMAL 凭证 Dr 6602=200 → **actual 增量** actualAmount=200/availableAmount=800 → reverseApprove→原+红冲凭证均 isReversed=true 双双排除 → **红冲回退** actualAmount=0/availableAmount=1000；聚合用 voucherLine.amountFunctional（非 debit/credit）；REVERSAL 凭证 isReversed=true（persistVoucher 对红冲凭证设 true + markOriginalVoucherReversed 对原凭证设 true），getBudgetVsActual 过滤 isReversed=false（plan 2026-07-14-1218-2） |
| finance | `ErpFinEmployeeAdvance` 员工借款生命周期 + 报销抵扣联动 | `fin-employee-advance.action.spec.ts` | DIRECT useApproval 审批轴 + EMPLOYEE_ADVANCE 凭证行 + 报销抵扣 EMPLOYEE_ADVANCE_SETTLE 凭证：(a) **借款审核** 自包含建 partner+employee + EmployeeAdvance(amountFunctional=500) → submit→approve→APPROVED+posted=true + **凭证行**（Dr 1221=500/Cr 1002=500，billHeadCode=advance.code）+ (b) **reverseApprove 红冲** REVERSAL 凭证行同向取负 Dr 1221=-500/Cr 1002=-500 + 原凭证 isReversed=true + (c) **报销抵扣联动** 先 approve EmployeeAdvance(500)→receivable AR/AP item(openAmount=500) → 再 approve ExpenseClaim(200+26+226, OWN_ACCOUNT)→payable AR/AP item(openAmount=226) + AdvanceOffsetOrchestrator.offset 自动抵扣 net=min(226,500)=226 → 断言三张凭证：EMPLOYEE_ADVANCE(Dr 1221=500/Cr 1002=500, billHeadCode=advance.code) + EXPENSE_CLAIM(Dr 6602=200/Dr 2221=26/Cr 2241=226, billHeadCode=claim.code) + EMPLOYEE_ADVANCE_SETTLE(Dr 2241=226/Cr 1221=226, billHeadCode=claim.code) + advance.settledAmount=226/outstandingAmount=274 + claim.settleAdvanceId 回链；同 billCode 多业务类型区分经 ErpFinVoucherBillR.billType 过滤（findVoucherIdByBillCodeAndType）；(d) 非法守卫；科目 1221/2241 经种子补齐（1218-2）（plan 2026-07-14-1218-2） |
| finance | `ErpFinEmployeeAdvance` 员工借款现金还款（EMPLOYEE_ADVANCE_SETTLE 现金还款路径） | `fin-employee-advance-cash-repay.action.spec.ts` | DIRECT `@BizMutation` cashRepay(advanceId, amount) 现金还款全栈可达：自包含建 partner+employee + EmployeeAdvance(500) → submit→approve→posted=true → cashRepay → 断言 advance 字段翻转（settledAmount/outstandingAmount）+ docStatus 保持不变（派生投影）+ **EMPLOYEE_ADVANCE_SETTLE CASH 凭证行数值断言**（Dr 1002 / Cr 1221）；三测试覆盖：(a) **全额还款** cashRepay(500) → settledAmount=500 + outstandingAmount=0（已结清派生投影）+ Dr 1002=500/Cr 1221=500（billHeadCode=`EA-CASH-REPAY-{code}-{millis}`，经 ErpFinVoucherBillR.businessType + JS 层 startsWith 前缀匹配反查）；(b) **部分还款** cashRepay(200) → settledAmount=200 + outstandingAmount=300 + Dr 1002=200/Cr 1221=200；(c) **超额守卫** 先部分还 200（outstanding=300）→ cashRepay(500) → GraphQL errors 含「超过」/「未还余额」语义 token + 字段不变；SETTLE_TYPE=CASH 分派由 `EmployeeAdvanceAcctDocProvider` 按 `billData.SETTLE_TYPE` 路由（缺省/null → OFFSET 既有报销抵扣路径零回归）；docStatus 保持不变对齐 owner doc §还款状态派生（不引入字典 SETTLED 值）（plan 2026-07-18-0718-2） |
| finance | `ErpFinBadDebt` 坏账反审核红冲闭环（reverseApprove） | `fin-bad-debt-reverse-approve.action.spec.ts` | DIRECT `@BizMutation` `reverseApprove(id)` 反审核红冲闭环全栈可达：自包含建 partner+OPEN RECEIVABLE ErpFinArApItem → writeOff→submit→approve 产生 APPROVED+BAD_DEBT_WRITE_OFF 凭证+ArApItem WRITTEN_OFF → `reverseApprove(id)` 断言 APPROVED→REJECTED + 原 NORMAL 凭证 isReversed=true + **红字凭证行同向取负**（Dr 1231=-100/Cr 1122=-100，经 findVoucherIdByBillCode(code,'REVERSAL') 反查）+ ArApItem 回退 OPEN+openAmount=100；两测试覆盖：(a) **核销反向** BAD_DEBT_WRITE_OFF 凭证红冲 + ArApItem WRITTEN_OFF→OPEN 对称回退；(b) **收回反向** writeOff→approve 前置 WRITTEN_OFF → recover→approve（OPEN+BAD_DEBT_RECOVERY 凭证）→ reverseApprove → REJECTED + ArApItem 回退 WRITTEN_OFF + 红字凭证行 Dr 1122=-100/Cr 1231=-100；ErpFinBadDebt 无 useWorkflow tagSet 故 DIRECT 路径调 FinPostingExecutor.reverse（plan 2026-07-18-1745-3） |
| finance | `ErpFinEmployeeAdvance` 员工借款反向现金还款红冲闭环（reverseCashRepay） | `fin-employee-advance-cash-repay-reverse.action.spec.ts` | DIRECT `@BizMutation` `reverseCashRepay(advanceId)` 反向现金还款红冲闭环全栈可达：自包含建 partner+employee + EmployeeAdvance(500) → submit→approve→posted=true → cashRepay(500) → `reverseCashRepay(advanceId)` 断言 advance 字段回退（settled=0/outstanding=500）+ 原 cashRepay NORMAL 凭证 isReversed=true + **红字凭证行同向取负**（Dr 1002=-500/Cr 1221=-500，经 ErpFinVoucherBillR.businessType + JS startsWith 前缀匹配 + postingType=REVERSAL 区分原/红字反查）；两测试覆盖：(a) **正向反向** cashRepay(500) → reverseCashRepay → 字段回退 + 红字凭证行同向取负 + approveStatus 保持 APPROVED；(b) **守卫** 已过账 advance 但无 cashRepay 凭证 → GraphQL errors 含「未找到」/「现金还款」语义 token + 字段不变；兑现 owner doc `expense-claim.md §红冲联动` cashRepay 路径承诺（解除 owner-doc 漂移，plan 2026-07-18-1745-3） |
| finance | `ErpFinBadDebt` 反向坏账准备计提红冲闭环（reverseBadDebtProvision） | `fin-bad-debt-provision-reverse.action.spec.ts` | DIRECT `@BizMutation` `reverseBadDebtProvision(periodId)` 反向指定期间全部 BAD_DEBT_RESERVE/RELEASE 已过账未冲销凭证全栈可达：自包含建 partner+OPEN RECEIVABLE ErpFinArApItem（确保必需 > Allowance 账面 → 触发 RESERVE）→ `runBadDebtProvision(periodId)` 产 1 张 BDR NORMAL 凭证（billCode=`BAD-DEBT-RESERVE-{period.code}`）→ `reverseBadDebtProvision(periodId)` 断言 GraphQL response `reversedReserveCount=1` + `reversedReserveAmount=requiredProvision-allowanceBalance` + 原 NORMAL 凭证 isReversed=true + **红字凭证行同向取负**（Dr 6701=-X / Cr 1231=-X，经 `findVoucherIdByBillCode(billCode,'REVERSAL')` 反查）；两测试覆盖：(a) **正向反向** runBadDebtProvision → reverseBadDebtProvision → 凭证红冲 + 红字凭证行同向取负 + reversedReserveCount=1；(b) **守卫** 无 BDR/BDL 凭证 → GraphQL errors 含「未找到」/「坏账准备」语义 token；服务层经 `ErpFinVoucherBillR` 反查 billCode 完全匹配 + 调 `FinPostingExecutor.reverse`（平台 reverseProcess 内部原子红冲所有累积凭证，覆盖 `CloseVoucherWriter` 无幂等检查的多凭证场景）；兑现 owner doc `bad-debt.md §步骤2b 反向红冲` 承诺（plan 2026-07-18-2251-2） |
| assets | `ErpAstDepreciationSchedule` 折旧引擎（executeDepreciation/reverseDepreciation） | `ast-depreciation.action.spec.ts` | DIRECT `@BizMutation` 折旧引擎生命周期：自包含建 `ErpAstAsset`(IN_SERVICE + STRAIGHT_LINE + 原值/残值/年限) → `executeDepreciation(assetId,period)` 断言 schedule status=EXECUTED + STRAIGHT_LINE 数学 (12000-0)/36=333.3333 + 资产卡片 accumulatedDepreciation/netBookValue 回写 + DEPRECIATION 过账 posted=true + **DEPRECIATION 凭证行数值断言**（Dr 6602=333.3333/Cr 1602=333.3333，经 executed.voucherId 直用）→ `reverseDepreciation` 断言 REVERSED + posted=false + 资产卡片回退 + **红冲凭证行同向取负断言**（Dr 6602=-333.3333/Cr 1602=-333.3333，经 `findVoucherIdByBillCode(assetCode#period,'REVERSAL')`）；非法迁移守卫（无 schedule 时 reverse 拒绝 + 不存在资产拒绝）；期间控制须经 ErpFinAccountingPeriod(code=period,status=OPEN)；科目 6602(折旧费用 Dr)/1602(累计折旧 Cr) 经种子补齐（0215-1）（plan 2026-07-14-0215-1 + 0742-1 凭证行断言） |
| assets | `ErpAstCip` 在建工程转固链（startConstruction→addCostItem→addProgressBilling→transferToAsset→reverseTransfer） | `ast-cip-capitalization.action.spec.ts` | DIRECT `@BizMutation` CIP 转固全链编排：自包含建 `ErpAstCip`(DRAFT) → `startConstruction`(IN_CONSTRUCTION) → `addCostItem`(PURCHASE 500 + OTHER 300 = accumulatedCost 800) → `addProgressBilling`(进度付款 400，不参与转固) → `transferToAsset`(TRANSFERRED + isCompleted + completedAssetId 回链 + 资产原值=成本归集合计 800) + **CAPITALIZATION 凭证行数值断言**（Dr 1601=800/Cr 1603=800，经 `findVoucherIdByBillCode(capCode,'NORMAL')`）→ `reverseTransfer`(IN_CONSTRUCTION + isCompleted=false) + **红冲凭证行同向取负断言**（Dr 1601=-800/Cr 1603=-800，REVERSAL）；资本化单 posted=true（CAPITALIZATION 过账 Dr 1601/Cr 1603，科目经 0215-1 补齐）；cleanup 逐域删除凭证→折旧计划→资产→资本化单→成本行→CIP（plan 2026-07-14-0215-1 + 0742-1 凭证行断言） |
| assets | `ErpAstInventory` 资产盘点 8 动作生命周期 | `ast-inventory-count.action.spec.ts` | DIRECT `@BizMutation` 盘点状态机：自包含建测试资产类别+资产（范围隔离）→ `createInventory`(DRAFT + 范围展开行) → `submitForCount`(COUNTING) → 物理盘点行更新 actualQuantity=0(SHORTAGE) → `reconcile`(RECONCILING + shortageAmount=bookValue) → `processVariance`(盘亏 SCRAPPED) → `approve`(复核) → `post`(POSTED + ASSET_INVENTORY_ADJUSTMENT 过账 Dr 6711/Cr 1601) + **凭证行数值断言**（Dr 6711=500/Cr 1601=500，经 `findVoucherIdByBillCode(invCode,'NORMAL')`）→ `reverse`(RECONCILING + 红冲 + **红冲凭证行同向取负断言** Dr 6711=-500/Cr 1601=-500)；cancel 异常路径 + 非法迁移守卫（CANCELLED→submitForCount 拒绝）；MATCHED-only 零差异路径 Provider 返回空 facts 不产凭证故须实际差异达 POSTED；科目 1601 经种子补齐（0215-1）（plan 2026-07-14-0215-1 + 0742-1 凭证行断言） |
| assets | `ErpAstMaintenance` 维修费用化/资本化双路径 | `ast-maintenance.action.spec.ts` | DIRECT `@BizMutation` 维修双路径：(a) 费用化 EXPENSE：createMaintenance→submit→startWork→completeWork→decideTreatment(EXPENSE)→approve→post(POSTED + MAINTENANCE_EXPENSE 凭证 Dr 6602/Cr 1002) + **凭证行数值断言**（Dr 6602=300/Cr 1002=300，LABOR 200+SPARE_PART 100）→reverse(COMPLETED + **红冲凭证行同向取负** Dr 6602=-300/Cr 1002=-300)；(b) 资本化 CAPITALIZE：同上但 decideTreatment(CAPITALIZE)→post(POSTED + 资产原值增量 + MAINTENANCE_CAPITALIZATION 凭证 Dr 1601/Cr 1002 + 折旧重算) + **凭证行数值断言**（Dr 1601=250/Cr 1002=250，SUBCONTRACT 250）→reverse(资产原值回退 + **红冲凭证行同向取负** Dr 1601=-250/Cr 1002=-250)；cancel 异常路径 + IN_PROGRESS→cancel 非法守卫；费用归集行 ErpAstMaintenanceCost(LABOR/SPARE_PART/SUBCONTRACT) aggregateCost>0 为 post 前置；科目 6602/1601 经种子补齐（0215-1）（plan 2026-07-14-0215-1 + 0742-1 凭证行断言） |
| assets | `ErpAstValueAdjustment` 资产减值/重估 DIRECT useApproval 审批轴 | `ast-value-adjustment.action.spec.ts` | DIRECT useApproval 审批轴三轴状态机（docStatus+approveStatus+posted）+ VALUE_ADJUSTMENT 过账凭证行数值断言：自包含建 `ErpAstAsset`(IN_SERVICE + 原值 12000 + categoryId=1 种子 category 三 subject 字段 null → fallback 默认科目码) + `ErpAstValueAdjustment`(DRAFT/UNSUBMITTED) → 3 种调整类型 approve 链：(a) IMPAIRMENT submitForApproval→approve(APPROVED+posted=true+docStatus=ACTIVE) + **凭证行数值断言**（Dr 6702=3000/Cr 1604=3000，经 `findVoucherIdByBillCode(code,'NORMAL')`）+ 净值减少(12000→9000)；(b) REVALUATION_UP **凭证行**（Dr 1601=5000/Cr 4002=5000）+ 净值增加(12000→17000)；(c) REVALUATION_DOWN **凭证行**（Dr 6702=2000/Cr 1601=2000）+ 净值减少(12000→10000)；reverseApprove(IMPAIRMENT) APPROVED→REJECTED+posted=false+docStatus 保持 ACTIVE + **红冲凭证行同向取负**（Dr 6702=-3000/Cr 1604=-3000）+ 原正向凭证 isReversed=true + 净值回退(9000→12000)；cancel(DRAFT→CANCELLED+approveStatus 不变)+withdrawApproval(SUBMITTED→UNSUBMITTED)+非法守卫(UNSUBMITTED→approve/APPROVED→submitForApproval/CANCELLED→approve 各拒绝)；科目 6702/1604/4002 经种子补齐（1218-1）（plan 2026-07-14-1218-1） |
| contract | `ErpCtContract` 合同生命周期 6 动作状态机 | `ct-contract-lifecycle.action.spec.ts` | DIRECT `@BizMutation` 合同状态机（无 useWorkflow/useApproval）：自包含建 `ErpCtContract`（经 `__save` 直置 `status=NEGOTIATION` 入口——DRAFT→NEGOTIATION 无 @BizMutation + SALES+OUTBOUND 经 validateTypeDirectionCombo 放行）→ `activate`(NEGOTIATION→ACTIVE) → `suspend`(ACTIVE→SUSPENDED) → `resume`(→ACTIVE) → `terminate`(→TERMINATED) 正向链；`expire` 路径(ACTIVE→EXPIRED)；`amend` 路径(ACTIVE→DRAFT + 新建修订版本 ErpCtContractVersion 回链 findPageTotal>0)；非法迁移守卫(DRAFT→activate / TERMINATED→activate 各抛 ERR_CT_ILLEGAL_STATUS_TRANSITION message token「不允许执行该操作」)；status 翻转均经 verifyState `__get` 独立断言（plan 2026-07-14-0215-2） |
| contract | `ErpCtContractVersion` 版本 finalize/sign 状态机 | `ct-contract-version.action.spec.ts` | DIRECT `@BizMutation` 版本状态机：自包含建合同头 + `ErpCtContractVersion`(DRAFT + isCurrent=true) → `finalizeVersion`(DRAFT→FINALIZED) → `signVersion`(FINALIZED→SIGNED + isCurrent 翻转保持 true + 同合同他版本 isCurrent 原子置 false)；非法迁移守卫(SIGNED→finalizeVersion 抛 ERR_CT_ILLEGAL_STATUS_TRANSITION，须 DRAFT)；status/isCurrent 翻转经 verifyState `__get` 独立断言（plan 2026-07-14-0215-2） |
| contract | `ErpCtRebateAgreement` 返利计提引擎（runAccrual） | `ct-rebate-accrual.action.spec.ts` | DIRECT `@BizMutation` 返利计提编排（委派 RebateEngine.accrue）：自包含建 `ErpCtRebateAgreement`(ACTIVE, PROGRESSIVE, partnerId=3=SUP-001) + `ErpCtRebateTier`(fromAmount=0, rebatePercent=10 开放档) → `runAccrual(agreementId, asOfDate)` → 聚合期间 posted=true 发票（PROGRESSIVE 逐张，PURCHASE 读 ErpPurInvoice 按 supplierId）→ 断言 `ErpCtRebateAccrual` 行写入（sourceBillCode=种子 posted AP 发票 PINV-2026-001 + sourceBillType=AP_INVOICE + accruedRebate=96.05 即 960.5×10% delta + billAmountSource=960.5）+ agreement.totalAccumulatedAmount/estimatedRebateAmount 回写断言；非法守卫(DRAFT agreement 抛 ERR_CT_REBATE_AGREEMENT_NOT_ACTIVE token「不可计提」)；**Phase 1 Explore 裁决**：__save 直置 posted=true 不可行（use-approval tagSet 强制 posted=false），复用种子 posted 发票 PINV-2026-001 作只读前置（非 runP2pChain 避 MAT_1 余额/GL voucher 污染）；cleanup 删 accrual+tier+agreement（不删种子发票）（plan 2026-07-14-0941-1） |
| contract | `ErpCtInvoicePlan` 发票计划触发跨域编排（triggerInvoice/triggerDuePlans） | `ct-invoice-plan-trigger.action.spec.ts` | DIRECT `@BizMutation` 发票计划触发生成 AP/AR 发票草稿：自包含建 `ErpCtContract`(ACTIVE, INBOUND or OUTBOUND) + ContractLine + InvoicePlan → `triggerInvoice(planId)` → 断言 AP 发票草稿创建（INBOUND→`ErpPurInvoice` code=`CT-INV-{planId}` + supplierId=contract.partnerId + totalAmount=plan.amount + posted=false）/ AR 发票草稿（OUTBOUND→`ErpSalInvoice` code=`CT-INV-{planId}` + customerId=contract.partnerId）+ plan 回写（isInvoiced=true + invoiceBillCode=`CT-INV-{planId}` + invoiceDate 非空）；非法守卫（已开票 plan 抛 ERR_CT_INVOICE_PLAN_ALREADY_INVOICED token「已生成发票」；SUSPENDED 合同 plan 抛 ERR_CT_CONTRACT_SUSPENDED token「已中止」）；`triggerDuePlans(contractId, asOfDate)` 批量入口（config-gated erp-ct.invoiceplan-auto-trigger 默认 true）返回触发行数>=2 + 逐 plan 回写断言；**执行期后端修复**（triggerDuePlans findList 经 meta 算子白名单拒 le(planDate)，改用 daoProvider 直查绕过，对齐同模块 loadAccruedBillCodes 范式，38 JUnit 全绿无回归）；cleanup 删 AP/AR 发票草稿+行 + Contract（cascade Line+Plan）（plan 2026-07-14-0941-1） |
| drp | `ErpDrpPlan` 净需求计算引擎（runDrp/resetToDraft/approvePlan） | `drp-plan-engine.action.spec.ts` | DIRECT `@BizMutation` DRP 引擎状态机（委派 DrpEngine）：自包含建 `ErpDrpParameter`(materialId=4/warehouseId=1/org=2/safetyStock=100) + `ErpDrpPlan`(DRAFT/org=2) → `runDrp`(DRAFT→COMPUTED + ErpDrpLine 净需求行生成 planId 隔离 findPageTotal>0 + totalReplenishmentQty>0，net=safetyStock+forecast−stock+alloc−onOrder=100) → `resetToDraft`(COMPUTED→DRAFT + SUGGESTED 行清理) → 重算 runDrp → `approvePlan`(COMPUTED→APPROVED + SUGGESTED 行→APPROVED)；非法迁移守卫(DRAFT→approvePlan 抛 ERR_DRP_PLAN_ILLEGAL_TRANSITION，须 COMPUTED，token「不允许此操作」)；orgId=2 __save 强制 org FK 校验 + DrpDemandAggregator 按 orgId 聚合参数行（plan 2026-07-14-0215-2） |
| drp | `ErpInvDrpSafetyStockCalc` 安全库存 calculate/confirmWriteback | `drp-safety-stock.action.spec.ts` | DIRECT `@BizMutation` 安全库存引擎（委派 SafetyStockEngine）：自包含建 `ErpDrpParameter`(safetyStock=0) + `ErpInvDrpSafetyStockCalc`(SIMPLE/PCT95/leadTime=7/override=50) → `calculate`(calculatedSafetyStock/calculatedRop 计算结果非空 + lastCalculatedAt 写回；无 posted 出库历史→SIMPLE meanDaily=0→结果 0 非空) → `confirmWriteback`(overrideSafetyStock=50 回写 ErpDrpParameter.safetyStock 0→50，三元组 (mat4,wh2,org2) 区别 plan-engine 精确命中)；**执行期修复引擎除零缺陷**（SafetyStockEngine.monthlyDemands 无历史返回空列表致 mean() 除零，对齐既有 [0] 意图，drp-service 30 单测全绿）（plan 2026-07-14-0215-2） |
| drp | `ErpDrpLine` 行释放跨域编排（releaseLine） | `drp-release-line.action.spec.ts` | DIRECT `@BizMutation` 行释放编排（委派 DrpReleaseService.releaseLine）：自包含建测试专用物料 + DrpPlan(APPROVED) + `ErpDrpLine`(APPROVED, replenishmentType=TRANSFER\|PURCHASE, 经 __save 直置 APPROVED 绕过引擎精确控制 replenishmentType) + `ErpDrpParameter`(preferredSourceWarehouseId\|preferredSupplierId) → `releaseLine(lineId)` → (a) TRANSFER 路径断言 `ErpInvTransferOrder` 创建（code=`DRP-TO-{lineId}` + fromWarehouseId=preferredSourceWarehouseId + toWarehouseId=line.warehouseId + docStatus=DRAFT）+ 行回写（orderBillType=ERP_INV_TRANSFER_ORDER + orderBillCode + status=ORDERED）；(b) PURCHASE 路径断言 `ErpPurOrder` 创建（code=`DRP-PO-{lineId}` + supplierId=preferredSupplierId + docStatus=DRAFT）+ 行回写（orderBillType=ERP_PUR_ORDER + status=ORDERED）；非法守卫（非 APPROVED 行抛 ERR_DRP_LINE_NOT_SUGGESTED token「仅 APPROVED 行可释放」；TRANSFER 缺 source warehouse 抛 ERR_DRP_NO_SOURCE_WAREHOUSE token「首选调出仓库」；PURCHASE 缺 preferred supplier 抛 ERR_DRP_NO_PREFERRED_SUPPLIER token「首选供应商」）；**Phase 3 Explore 裁决**：__save 直置 APPROVED 绕过 runDrp→approvePlan 引擎链（replenishmentType 须精确控制，引擎内部决定 replenishmentType 不可用）；建测试专用物料隔离（避 drp-plan-engine/safety-stock 参数三元组冲突）；cleanup 删下游单据+行+Parameter+Plan+物料（plan 2026-07-14-0941-1） |
| hr | `ErpHrSalary` 薪酬引擎 calculateSalary/voidSalary + markPaid/generateBankFile 守卫 | `hr-payroll.action.spec.ts` | DIRECT `@BizMutation` 薪酬引擎（calculateSalary 触发 PayrollCalculator 编排：出勤比例→基本工资→社保→公积金→个税累计预扣→实发）：自包含建 employee + ACTIVE EmploymentContract(monthlySalary) + SocialInsuranceBase + SocialInsuranceConfig(HOUSING_FUND) + TaxConfig(七级累进) 配置链 → `calculateSalary(employeeId,year,month)` 断言 ErpHrSalary 创建 + grossSalary/netSalary 非空 + approveStatus=UNSUBMITTED + paymentStatus=PENDING → `voidSalary` 回退 PENDING→VOID；markPaid **negative-path 守卫**（UNSUBMITTED salary→ERR_SALARY_ILLEGAL_STATUS_TRANSITION，硬守卫 approveStatus==APPROVED，salary 审批轴经 useWorkflow xwf 浏览器层不可达同 2330-1）；generateBankFile **negative-path 守卫**（无 APPROVED 行→ERR_NO_APPROVED_SALARY_FOR_BANK_FILE）（plan 2026-07-14-0215-3） |
| hr | `ErpHrSalarySimulation` 薪酬模拟 What-If 生命周期 | `hr-salary-simulation.action.spec.ts` | DIRECT `@BizMutation` 模拟状态机（核算复用 PayrollCalculator.recalculateWithOverrides）：自包含 __save 源 ErpHrSalary(2025-6 PENDING，避免 runPayroll 遍历种子员工缺配置抛错) + TaxConfig(目标年 recalculateDerived 个税所需) → `createSimulation`(DRAFT 冻结源快照) → `adjustItem`(即时应变 basicSalary 10000→12000 gross 重算) → `submitForReview`(IN_REVIEW 须有调整项) → `approve`(APPROVED) → `convertToFormal`(CONVERTED + 正式 salary 回链 findFirst 目标期) ；reject 路径(IN_REVIEW→REJECTED)；`applyBatchAdjustment` FIXED（返回 Map 非实体，原始 mutation 无选择集，affectedCount=1）；非法迁移守卫(DRAFT→approve ERR_HR_SIMULATION_ILLEGAL_TRANSITION + DRAFT→submitForReview 无调整项 ERR_HR_SIMULATION_NO_ADJUSTMENT)（plan 2026-07-14-0215-3） |
| hr | `ErpHrRecruitment` 招聘漏斗 6 动作状态机 + hire 员工联动 | `hr-recruitment.action.spec.ts` | DIRECT `@BizMutation` 招聘状态机：自包含建 `ErpHrRecruitment`(OPEN 入口) → `moveToScreening`(SCREENING) → `scheduleInterview`(INTERVIEW) → `makeOffer`(OFFERED + offerSalary) → `hire`(HIRED + employeeId 回写 + ErpHrEmployee 自动创建 ACTIVE + ErpEmploymentContract 自动创建 ACTIVE 经 findFirst 反查)；reject(OPEN→REJECTED)/close(→CLOSED) 异常路径；非法迁移守卫(HIRED→moveToScreening ERR_RECRUITMENT_ILLEGAL_STATUS_TRANSITION token「不允许执行该操作」)；status 翻转经 verifyState 独立断言（plan 2026-07-14-0215-3） |
| hr | `ErpHrLeaveRequest` 休假审批 + `ErpHrAttendance` 考勤打卡 | `hr-leave-attendance.action.spec.ts` | DIRECT `@BizMutation` 双状态机：(a) 休假审批 自包含建员工 + ErpHrLeaveRequest(DRAFT) → `submit`(SUBMITTED) → `approve`(APPROVED + approvedAt，LeaveBalance null 代码容忍 if(balance==null)return 不阻断) → `cancel`(CANCELLED)；reject 路径(SUBMITTED→REJECTED)；**日期重叠守卫**（同员工同类型重叠区间第二条 submit 抛 ERR_LEAVE_DATE_OVERLAP token「重叠」）；(b) 考勤打卡 自包含建员工 → `clockIn`(当日 ErpHrAttendance 创建 clockIn 非空) → `clockOut`(clockOut+workHours 派生) → 重复 clockIn 守卫(ERR_ALREADY_CLOCKED_IN token「已签到」(employeeId,date) 唯一约束)（plan 2026-07-14-0215-3） |
| aps | `ErpApsSchedule` 排产方案状态机 publish/archive | `aps-schedule.action.spec.ts` | DIRECT `@BizMutation` 排产方案状态机（无 useWorkflow/useApproval）：自包含建 `ErpApsSchedule`（DRAFT 经 `__save`）→ `publish`(DRAFT→PUBLISHED) → `archive`(PUBLISHED→ARCHIVED) 正向链；`archive from DRAFT`(DRAFT→ARCHIVED，archive 允许 DRAFT\|PUBLISHED 合法非守卫路径)；非法迁移守卫(ARCHIVED→publish 抛 ERR_APS_SCHEDULE_ILLEGAL_STATUS token「不允许执行该操作」)；status 翻转经 verifyState `__get` 独立断言（plan 2026-07-14-0508-1） |
| aps | `ErpApsOperationOrder` 工序排产引擎 scheduleForward/scheduleBackward | `aps-operation-order.action.spec.ts` | DIRECT `@BizMutation` 排产引擎（委派 ErpApsSchedulingProcessor→ErpApsSchedulingEngine 纯算法 POJO capacity=1）：自包含建 Schedule(DRAFT,horizon 区间)+OperationOrder(DRAFT,setupTime/runtimePerUnit/qty 派生 duration) → `scheduleForward`(SchedulingResult.feasible=true + scheduledOperationIds 非空 + op DRAFT→PLANNED + plannedStart/EndDateT 写回非空) + `scheduleBackward`(同上，需 latestEndDateT)；**Explore 裁定**：workOrderId/machineId 列无 FK 约束 + 引擎无工作中心日历/产能配置依赖，自包含 setup 可达不降级（plan 2026-07-14-0508-1） |
| b2b | `ErpB2bEdiDoc` EDI 信封状态机（出站+入站生命周期） | `b2b-edi-doc.action.spec.ts` | DIRECT `@BizMutation` EDI 信封状态机（无 useWorkflow/useApproval）：(a) 出站正向链 自包含建 ErpB2bEdiDoc(TO_SEND 经 `__save`，绕过 createOutbound 跨域依赖) → `markSent`(TO_SEND→SENT) → `markAcknowledged`(SENT→ACKNOWLEDGED)；(b) 失败重试 TO_SEND→`markError`(ERROR)→`retry`(TO_SEND+retryCount++)；(c) 取消 TO_SEND→`cancel`(CANCELLED)；(d) 入站 `createInbound`(RECEIVED，formatCode=null 不查 format)→`archive`(ARCHIVED)；非法守卫(ACKNOWLEDGED→markSent / CANCELLED→retry 各抛 ERR_B2B_EDI_DOC_ILLEGAL_TRANSITION token「不允许执行该操作」)；**Explore 裁定 (b)**：createOutbound happy-path 经 provider.generatePayload 跨域查 ErpSalInvoice 缺失抛 ERR_B2B_EDI_PARSE_FAILED，跨域耦合度高故降级为 watch-only residual，经 __save 预置 TO_SEND 态直接测后续状态迁移；状态字段为 `state` 非 `status`（ORM column name="state"）（plan 2026-07-14-0508-1） |
| logistics | `ErpLogShipment` 发运单状态机 advise/completeShipment/cancelShipment | `log-shipment.action.spec.ts` | DIRECT `@BizMutation` 发运单状态机（委派 GatewayDispatcher→承运商网关 SPI）：(a) 正向链 自包含建 ErpLogCarrier(gatewayId="mock")+ErpLogShipment(DRAFT) → `advise`(DRAFT→ADVISED) → `completeShipment`(ADVISED→DISPATCHED + 经 MockCarrierGatewayClientFactory.completeDeliveryOrder 回写 trackingNo/labelUrl)；(b) 取消 ADVISED→`cancelShipment`(CANCELLED)；(c) DISPATCHED→`cancelShipment`(CANCELLED，经 mock client.cancelShipment 无副作用)；非法守卫(CANCELLED→advise / DRAFT→completeShipment 各抛 ERR_LOG_SHIPMENT_ILLEGAL_TRANSITION token「不允许执行该操作」)；**Explore 裁定 (a)**：MockClient 默认 failureMode=SUCCESS，自包含建 Carrier(mock 网关)+Shipment 即可达，不降级（plan 2026-07-14-0508-1） |
| inventory | `ErpInvLandedCost` 到岸成本审核生命周期 + LANDED_COST 过账 | `inv-landed-cost.action.spec.ts` | DIRECT `@BizMutation` 到岸成本审核编排（无 useWorkflow/useApproval）：自包含建测试专用物料 + INCOMING 备货 + APPROVED Receive(经 `__save` 直置 approveStatus=APPROVED 不触发审批工作流，非 runP2pChain 避 MAT_1 余额污染) + LandedCost(DRAFT/UNSUBMITTED) + Line(costElement=FREIGHT) → `allocate(id)` @BizQuery 分摊预览返回 `List<Map>` 非空（allocatedAmount/materialId 字段，状态不改）→ `approve(id)` @BizMutation 审核三步编排（分摊→CostAdjust→LANDED_COST(490) 过账）→ 断言 approveStatus=APPROVED + docStatus=DONE + posted=true + **LANDED_COST 凭证行数值断言**（经 `findVoucherIdByBillCode(landedCost.code, 'NORMAL')` 反查——billHeadCode=landedCost.code 无后缀 LandedCostPostingDispatcher.java:69；Dr 1401=50/Cr 2202=50）+ CostAdjust avgCost 更新断言（MOVING_AVERAGE path 更新 StockBalance.avgCost）+ APPROVED→approve 非法守卫（ERR_LANDED_COST_ALREADY_APPROVED）；cleanup 删凭证+CostAdjust+Ledger+Balance+移动+LandedCost+Receive+物料（plan 2026-07-14-0606-2） |
| maintenance | `ErpMntSparePartUsage` 备件消耗 confirm + MAINTENANCE_ISSUE 过账 | `mnt-spare-part-posting.action.spec.ts` | DIRECT `@BizMutation` 备件消耗确认+过账（config-gated `erp-mnt.spare-part-posting-enabled=true`）：自包含建测试专用备件物料 + INCOMING 备货（避免清理抹除种子余额污染 inventory dashboard）+ Equipment(种子 id=1 RUNNING, confirm 不改变设备状态) + SparePartUsage(DRAFT/UNSUBMITTED) + Line → `confirm(usageId)` → SparePartIssueService.issue（OUTGOING 移动 relatedBillType=ERP_MNT_SPARE_PART business-linked auto-DONE）→ 断言 docStatus=ACTIVE + posted=true + OUTGOING 移动单存在(docStatus=DONE) + **MAINTENANCE_ISSUE 凭证行数值断言**（经 `findVoucherIdByBillCode(usage.code + "-MI", 'NORMAL')` 反查——billHeadCode=usage.code+"-MI" MaintenanceIssuePostingDispatcher.java:93,129；Dr 6602=50 维修费用/Cr 1403=50 存货）；cleanup 删凭证+OUTGOING 移动+Ledger+Balance+INCOMING 备货+Usage+物料；config-gated 关闭路径（posted=true 无凭证）经后端单测覆盖（plan 2026-07-14-0606-2） |
| maintenance | `ErpMntVisit` complete + MAINTENANCE_LABOR 工时费用化过账 | `mnt-labor-posting.action.spec.ts` | DIRECT `@BizMutation` 维修访问完工+工时费用化过账（config-gated `erp-mnt.labor-posting-enabled=true` + `erp-mnt.default-labor-hourly-rate=80`，webServer JVM arg）：自包含建 Equipment(种子 id=1 RUNNING) + Visit(DRAFT + startTime/endTime 预置产生确定性 totalMinutes) → `schedule`(DRAFT→SCHEDULED) → `start`(SCHEDULED→IN_PROGRESS + 设备状态联动 UNDER_MAINTENANCE) → `complete(visitId)` → doComplete（status COMPLETED + totalMinutes=Duration.between(startTime,endTime).toMinutes() + completedAt）+ `MaintenanceLaborPostingDispatcher.postLabor`（config-gated）→ 断言 status=COMPLETED + totalMinutes 经 verifyState `__get` 独立断言（60/30/null 三态）+ **MAINTENANCE_LABOR 凭证行精确数值断言**（经 `findVoucherIdByBillCode(visit.code + "-ML", 'NORMAL')` 反查——billHeadCode=visit.code+"-ML" MaintenanceLaborPostingDispatcher；Dr 6602=80 折旧费用/Cr 2211=80 应付职工薪酬，rate=80 元/小时，60min×80/60=80；30min→40；totalMinutes=null→无凭证守卫）；cleanup 删凭证 + Visit（不删种子设备；posting 仅写 voucher/voucher_line/voucher_bill_r 不写 gl_balance 不污染 finance 看板基线）；config-gated 关闭路径经后端单测 `TestErpMntLaborPosting` 覆盖（plan 2026-07-18-0949-1） |
| b2b | `ErpB2bAsn` ASN→PO 匹配 + 跨域建收（matchPurchaseOrder/retryMatch/createReceiveFromAsn） | `b2b-asn-match-receive.action.spec.ts` | DIRECT `@BizMutation` ASN 跨域编排：自包含建 ErpPurOrder（APPROVED via `__save`）+ ErpPurOrderLine + ASN（RECEIVED, relatedBillCode=PO.code via `__save` 绕过 handleInboundWebhook）+ AsnLine（materialId 匹配 PO line）→ `matchPurchaseOrder(asnId)` 跨域读 PO 按 relatedBillCode 匹配 → 断言 MATCHED → `createReceiveFromAsn(asnId)` config-gated `erp-b2b.asn-auto-create-receive=true` 跨域建 ErpPurReceive 草稿（code=`RCV-FROM-ASN-{asnCode}` + orderId=PO.id + supplierId/warehouseId/currencyId 据 PO 回填 + docStatus=UNSUBMITTED + approveStatus=UNSUBMITTED + receiveStatus=NOT_RECEIVED，**仅头无行**）+ ASN RECEIVED_TO_STOCK；非法守卫（无匹配 PO 保持 RECEIVED + 非 RECEIVED 态 matchPurchaseOrder 抛 ERR_B2B_ASN_ILLEGAL_TRANSITION + retryMatch 幂等）；cleanup 删 ErpPurReceive+AsnLine+ASN+ErpPurOrderLine+ErpPurOrder（plan 2026-07-14-0941-2） |
| aps | `ErpApsOperationOrder` insertRushOrder 插单区间重排 | `aps-rush-order.action.spec.ts` | DIRECT `@BizMutation` 插单编排（委派 ErpApsSchedulingProcessor.insertRushOrder）：自包含建 Schedule（DRAFT）+ 背景工序（priority=50, machineId=100）→ scheduleForward→PLANNED（plannedStart 落 08:00）→ 建急单工序（priority=10, 同 machineId, earliestStartDateT 重叠, latestEndDateT 兜底窗口）→ `insertRushOrder(operationOrderId)` → 断言 SchedulingResult（feasible=true + scheduledOperationIds 含急单 id）+ 急单 `__get` status=PLANNED + plannedStart/End 写回 + 背景工序 `__get` plannedStart 被推移（区间重排可观测——急单优先排定，背景工序被推后）；**Explore 裁决**：feasible=false 不可达（正向排产总是 feasible=true）+ 非 DRAFT 工序无状态守卫（方法内置置 DRAFT）；cleanup 删急单+背景工序+Schedule（plan 2026-07-14-0941-2） |
| logistics | `ErpLogShipment` DELIVERED 运费过账（handleTrackingWebhook → onDelivered → FREIGHT posting） | `log-delivered-freight-posting.action.spec.ts` | DIRECT `@BizMutation` DELIVERED 运费过账编排：自包含建 ErpLogCarrier（mock 网关）+ ErpLogShipment（SALES_DELIVERY, freightSettlementStatus=PENDING, freightAmount=100）→ advise→completeShipment（DISPATCHED + trackingNo=MOCK-{code}）→ 构造 payload JSON `{"trackingNo":"...","eventType":"DELIVERED","signedBy":"E2E"}` → `handleTrackingWebhook(carrierCode, signature, payload)`（config `erp-log.webhook-signature-required=false` 跳过 HMAC 验签）→ 断言 shipment DELIVERED + freightSettlementStatus=SETTLED + **FREIGHT 凭证行数值断言**（经 `findVoucherIdByBillCode(shipment.code, 'NORMAL')` 反查——billHeadCode=shipment.code；Dr 6601=100 销售费用/Cr 1002=100 银行存款，LogisticsFreightProvider PREPAID 默认）+ 幂等守卫（第二次 DELIVERED webhook advanceTracking 返回 false → onDelivered 不调用 → 凭证数不增加）；path-2 到岸成本降级 Deferred But Adjudicated（config-gate 默认关闭 + path-1 已代表验证 + 后端单测覆盖）；cleanup 删 FREIGHT 凭证+Shipment+Carrier（plan 2026-07-14-0941-2） |
| contract | `ErpCtRebateSettlement` 返利结算 postSettlement（DRAFT→POSTED + 跨域负额 credit memo） | `ct-rebate-settlement.action.spec.ts` | DIRECT `@BizMutation` 返利结算编排（无 useWorkflow/useApproval）：自包含建 `ErpCtContract`(currencyId)+ContractLine(materialId)+`ErpCtRebateAgreement`(ACTIVE,PROGRESSIVE,partnerId=3=PURCHASE,contractId 非空驱动 resolveCurrencyId/MaterialId)+Tier(fromAmount=0,rebatePercent=10) → 复用 0941-1 范式 `runAccrual(agreementId,asOfDate)` 聚合种子 posted AP 发票 PINV-2026-001 产 `ErpCtRebateAccrual`(isSettled=false,accruedRebate=96.05=960.5×10%) → 建 `ErpCtRebateSettlement`(DRAFT) → `postSettlement(settlementId)` → 断言 status DRAFT→POSTED + `creditMemoBillCode=CT-REBATE-{id}` + `creditMemoBillType=AP_INVOICE`（__get 独立断言）+ 跨域反查负额 credit memo `ErpPurInvoice`(code=CT-REBATE-{id}, supplierId=partnerId, **totalAmount=-96.05 负额**, posted=false) + accruals `isSettled=true` 翻转 + 非法态守卫（已 POSTED 再 postSettlement 抛 ERR_CT_SETTLEMENT_ILLEGAL_TRANSITION token「不允许」+ 状态保持 POSTED 事务回滚）；**按设计不过 GL 凭证**（postSettlement 改走跨域负额 credit memo 发票 AP/AR 侧，故不断言凭证行——见 Non-Goals）；cleanup 删 credit memo 发票+行+accruals+settlement+tier+agreement+contractLine+contract（不删种子发票）（plan 2026-07-17-1005-2） |
| drp | `ErpDrpLine` 批量释放 releaseApproved（APPROVED plan → 全行 ORDERED + plan EXECUTED） | `drp-release-approved.action.spec.ts` | DIRECT `@BizMutation` 批量释放编排（委派 DrpReleaseService.releaseApproved）：自包含建测试专用物料×2 + `ErpDrpPlan`(APPROVED 经 `__save` 直置绕过引擎) + 2 `ErpDrpParameter`(preferredSourceWarehouseId\|preferredSupplierId 各隔离) + 2 `ErpDrpLine`(APPROVED, TRANSFER/PURCHASE 混合 经 `__save` 直置 APPROVED 绕过 runDrp→approvePlan 引擎链精确控制 replenishmentType，0941-1 范式) → `releaseApproved(planId)` → **mutation 返回 null**（ErpDrpLineBizModel:43）故断言 errors null + 逐行 verifyState 断言 status=ORDERED + orderBillType/orderBillCode（TRANSFER→`DRP-TO-{lineId}`/ERP_INV_TRANSFER_ORDER，PURCHASE→`DRP-PO-{lineId}`/ERP_PUR_ORDER）+ 计划 EXECUTED 翻转（advancePlanToExecutedIfComplete 全部行 ORDERED 后）+ findFirst 下游单据创建（`ErpInvTransferOrder` fromWarehouseId=preferredSourceWarehouseId/toWarehouseId=line.warehouseId + `ErpPurOrder` supplierId=preferredSupplierId）+ 守卫（SUGGESTED-only plan releaseApproved no-op：plan 保持 APPROVED + 行保持 SUGGESTED + 无下游，非 APPROVED 行被过滤跳过 released=0 空返回不抛错）；cleanup 删下游单据+行+Parameter+Plan+物料（plan 2026-07-17-1005-2） |
| projects | `ErpPrjProjectSettlement` FINAL/INTERIM 损益结转 approve（posted + Dr 5101/Cr 6001 + 条件性 4103） | `projects-pnl-settlement.action.spec.ts` | DIRECT `@BizMutation` 损益结转编排（同 0742-2 三轴状态机 createSettlement→submit→approve）：自包含建 `ErpPrjProject`(OPEN)+`ErpPrjBilling`(amountFunctional=10000,docStatus=OPEN)+`ErpPrjCostCollection`(OPEN)+Line(MATERIAL,amount=6000) → `refreshPnl` 产 CALCULATED 快照（revenueAmount=10000/totalCost=6000）→ `createSettlement(settlementType=FINAL\|INTERIM)`（**关键工程化：finalRevenue=10000 ≠ finalCost=6000 使 4103 行确定性出现**，profitLoss=4000>0）→ submit(SUBMITTED)→approve(APPROVED+posted=true, FINAL/INTERIM transferToAsset=false 故不建资产卡片 区别 CLOSE）+ **PROJECT_SETTLEMENT 凭证行精确数值断言**（经 `from '../orchestration/_helper'` 导入 findVoucherIdByBillCode/assertVoucherLines，billHeadCode=settlement.code）：Dr 5101(项目成本)=6000 + 条件性 Dr 4103(本年利润)=4000（profitLoss>0→DEBIT）+ Cr 6001(项目收入)=10000 + reverseSettlement 红冲同向取负（Dr 5101=-6000/Dr 4103=-4000/Cr 6001=-10000）+ INTERIM 对照（凭证结构同 FINAL，ProjectSettlementAcctDocProvider 同分支仅 settlementType 字面不同）+ 非法守卫（UNSUBMITTED→approve）；**docStatus 字典修正**（执行期捕获：ErpPrjBilling/CostCollection.docStatus 绑 erp-prj/project-status 无 APPROVED，改 docStatus=OPEN+approveStatus=APPROVED，同 0742-2）；cleanup 删凭证+结算行+结算单+PnL快照+成本行/头+Billing+Project（plan 2026-07-17-1005-2） |
| finance | `ErpFinNotesReceivable` 应收票据 DIRECT 7 动作生命周期 + 4 业财过账凭证行 | `fin-notes-receivable.action.spec.ts` | DIRECT `@BizMutation` 应收票据状态机（无 useWorkflow/useApproval）：自包含建 `ErpFinNotesReceivable`（notesType=BANK_ACCEPTANCE/currencyId/orgId/partnerId/amountFunctional=1000，经 `__save` 直置 status 入口）→ **receive**(→RECEIVED+posted=true) + 凭证行 Dr 1121=1000/Cr 1122=1000（NOTES_RECEIVABLE_RECEIVED）+ **discount**(RECEIVED→DISCOUNTED+posted=true，dueDate=2026-07-31/discountDate=2026-07-01→remainingDays=30，discountInterest=1000×0.12×30/360=10/netAmount=990) + 凭证行三件套 Dr 1002=990/Dr 6603=10/Cr 1121=1000（NOTES_RECEIVABLE_DISCOUNTED，单币种 6051 fx 行 signum=0 抑制）+ **endorse**(RECEIVED→ENDORSED+posted=true，endorsementFromId=note.id 自引用 GraphQL @Name 非空约束) + 凭证行 Dr 2202=1000/Cr 1121=1000（NOTES_RECEIVABLE_ENDORSED）+ **collect**(RECEIVED→COLLECTION_PENDING 无过账)→**honor**(→HONORED+posted=true) + 凭证行 Dr 1002=1000/Cr 1121=1000（NOTES_RECEIVABLE_COLLECTION）+ **dishonor**(COLLECTION_PENDING→DISHONORED 终态无过账) + **writeOff**(经 receive 前置 posted=true → WRITE_OFF+posted=false + REVERSAL 同向取负 Dr 1121=-1000/Cr 1122=-1000，businessTypeForStatus(RECEIVED)→NOTES_RECEIVABLE_RECEIVED 红冲，findVoucherIdByBillCode(code,'REVERSAL') 按 postingType 区分同 billCode 原/红字凭证) + 守卫（discount from DISCOUNTED / honor from RECEIVED 各抛 ERR_NOTES_RECEIVABLE_ILLEGAL_STATUS_TRANSITION + status 不变）；科目 1121/6603 经种子补齐（本 plan 1430-1）；**执行期发现 latent defect**：ErpFinArApItemGenerator.buildCode 生成辅助账 code "ARI-NOTES_RECEIVABLE-{sourceBillCode}-{uuid8}" 超 voucherCode precision 50（长 sourceBillCode>19 字符时 sqlState=22001），E2E 用紧凑 base36 note.code 绕过，详见 `docs/bugs/2026-07-17-1430-...`（plan 2026-07-17-1430-1） |
| finance | `ErpFinNotesPayable` 应付票据 DIRECT 4 动作生命周期 + 2 业财过账凭证行 | `fin-notes-payable.action.spec.ts` | DIRECT `@BizMutation` 应付票据状态机（无 useWorkflow/useApproval）：自包含建 `ErpFinNotesPayable`（主路径 notesType=COMMERCIAL_ACCEPTANCE 短路授信校验 isBankAcceptance=false → 无需建 CreditFacility，最简 setup；currencyId/orgId/partnerId=3 SUP-001/amountFunctional=1000）→ **issue**(→ISSUED+posted=true) + 凭证行 Dr 2202=1000/Cr 2203=1000（NOTES_PAYABLE_ISSUED，不生成辅助账故无 buildCode 溢出）+ **honor**(ISSUED→HONORED+posted=true) + 凭证行 Dr 2203=1000/Cr 1002=1000（NOTES_PAYABLE_HONORED）+ **dishonor**(ISSUED→DISHONORED 无过账) + **writeOff**(经 issue 前置 posted=true → WRITE_OFF+posted=false + REVERSAL 同向取负 Dr 2202=-1000/Cr 2203=-1000，doWriteOff 红冲 NOTES_PAYABLE_ISSUED) + 守卫（honor from non-ISSUED 抛 ERR + BANK_ACCEPTANCE + CreditFacility totalAmount=100<票面1000 → issue 抛 ERR_CREDIT_FACILITY_INSUFFICIENT 事务回滚 status 不变）；科目 2203 经种子补齐（本 plan 1430-1）；授信强一致校验三短路条件 isBankAcceptance && credit-check-on-issue(默认 true) && creditFacilityId!=null（plan 2026-07-17-1430-1） |
| finance | `ErpFinCashForecast` 现金预测 refreshForecast（三源聚合 + 区间清理 + 幂等重写） | `fin-cash-forecast.action.spec.ts` | DIRECT `@BizMutation` 现金预测聚合编排（无 useWorkflow/useApproval，返回 Integer 标量无选择集，对齐 finance-voucher-post `gql.raw` 长标量返回范式）：自包含建 AR/AP 辅助账项 + 应收/应付票据（`__save` 直置 status 入口）→ `ErpFinCashForecast__refreshForecast(fromDate,toDate)` 原始 mutation 经 GraphQL 调用（`LocalDate` 入参 ISO String quoted）。三测试覆盖：(a) **三源聚合** 区间内 OPEN AR(INFLOW/100) + OPEN AP(OUTFLOW/200) + 非终态应收票据 RECEIVED(INFLOW/300) + ISSUED 应付票据(OUTFLOW/400) + 区间外对照源 → 计数 ≥4 + 逐行 `findItems ErpFinCashForecast` 断言 direction/amount/forecastDate/sourceBillType 与源对齐（AR→INFLOW/AP→OUTFLOW/NR→INFLOW/NP→OUTFLOW）+ 区间外源不出现在结果；(b) **终态过滤** SETTLED AR 不参与预测（0 预测行）；(c) **幂等重写** 同区间两次 refresh → 计数稳定（count1==count2）+ 先清后写不累积；cleanup 按 sourceBillCode 逐码删 ErpFinCashForecast 行 + 删源实体保护共享 DB 基线；测试区间 [2026-08-10,2026-08-15] 晚于 today 避种子 AR/AP OPEN 项（dueDate=08-04/05）干扰（plan 2026-07-17-2256-1） |
| finance | `ErpFinCreditFacility` 银行授信额度占用契约 reserve/release（强一致校验 + 不足守卫） | `fin-credit-facility.action.spec.ts` | DIRECT `@BizMutation` 授信额度占用契约（无 useWorkflow/useApproval，返回实体带选择集）：每测试自包含建独立 facility 隔离（facilityType=BANK_ACCEPTANCE 字典值，total=1000/used=0/available=1000，Processor 无 auto-compute 须 __save 显式三值）→ 三测试覆盖：(a) **reserveCredit(300)** → usedAmount=300/availableAmount=700 同步重算（available=total−used）+ totalAmount 不变；(b) **不足守卫** 先 reserve(300)→available=700 再 reserve(2000>700) → GraphQL errors 含「不足」语义 token（ERR_CREDIT_FACILITY_INSUFFICIENT 中文描述）+ 事务回滚 used/available 不变；(c) **releaseCredit(300)** reserve 后 → usedAmount=0（下限 0）/availableAmount=1000 恢复；状态翻转均经 verifyState __get 独立断言；不断言 version 乐观锁自增/stale 拒绝（并发面归后端单测）（plan 2026-07-17-2256-1） |
| finance | `ErpFinCreditFacility` 授信利息计提 accrueInterest（计息公式 + Dr 6603 / Cr 1002 凭证行精确数值 + 区间级幂等） | `fin-credit-facility-interest.action.spec.ts` | DIRECT `@BizMutation` 授信利息计提（无 useWorkflow/useApproval，返回 Long voucherId 标量无选择集，对齐 fin-cash-forecast refreshForecast `gql.raw` 长标量返回范式）：webServer JVM arg `-Derp-fin.credit-facility-default-interest-rate=0.05`（5% 年化使 mutation 可达；rate=0 时抛 RATE_NOT_CONFIGURED 守卫）；每测试经 `__save` 直置 facility 三值（total=1000/used=300/available=700，orgId=2，对齐 2256-1 直 __save 范式）→ 三测试覆盖：(a) **正路径** accrueInterest(facilityId,"2026-07-01","2026-07-31") → GraphQL 返回 voucherId 非空 + findVoucherIdByBillCode("CFI-INT-{id}-2026-07-01_2026-07-31","NORMAL") 反查 = 返回值 + assertVoucherLines Dr 6603 = 1.2917（300×0.05×31/360 HALF_UP scale=4）/ Cr 1002 = 1.2917 + facility 三值不变；(b) **usedAmount=0 空操作** → data null + errors null（返回 null 标量）+ findFirst 反查无凭证；(c) **非法日期区间守卫** fromDate>toDate → GraphQL errors 含「日期」语义 token（ERR_CREDIT_FACILITY_INTEREST_INVALID_DATE_RANGE i18n 中文描述）+ 无凭证；cleanup 经 cleanupVoucherByBillCode 删凭证 + deleteById 删 facility 保护 finance 看板/报表基线（plan 2026-07-18-0718-1） |
| maintenance × assets | `ErpAstMaintenance`(`maintenanceVisitId`) × `ErpMntSparePartUsage.confirm` 跨域并存防双重扣减 | `mnt-ast-linked-visit-anti-double-deduct.action.spec.ts` | DIRECT `@BizMutation` 跨域并存防双重扣减编排（两域均无 useWorkflow/useApproval）：自包含建测试专用备件物料 + INCOMING 备货（隔离避免污染 inventory dashboard totalValue 基线）+ 种子设备 id=1 + DRAFT `ErpMntVisit`（仅作 maintenanceVisitId 软 FK 持有，linkedVisit 判定只看 != null）+ `ErpMntSparePartUsage`(**visitId 显式指向 visit**，DRAFT/UNSUBMITTED) + Line(qty=10 × unitCost=5 → amount=50) → `confirm(usageId)` → 断言 `MAINTENANCE_ISSUE` 凭证 Dr 6602=50/Cr 1403=50（billHeadCode=usage.code+"-MI"）+ 建 `ErpAstMaintenance`(**maintenanceVisitId=visit.id 触发 linkedVisit=true** + SPARE_PART cost=50 → totalCost=50) → submit→startWork→completeWork→decideTreatment(EXPENSE)→approve→post → 断言 `MAINTENANCE_EXPENSE` 凭证 **Dr 6602=50/Cr 2502=50 维修中转清算（非 Cr 1403 存货）** —— 备件成本已由 mnt MAINTENANCE_ISSUE 贷记 1403 一次，assets 不再贷存货改贷中转清算防双重扣减；测试 2（linkedVisit=false 对照）独立 assets 维修（无 maintenanceVisitId，SPARE_PART cost=80）→ post → 断言 `MAINTENANCE_EXPENSE` 凭证 Dr 6602=80/**Cr 1002=80 银行存款**（独立维修分支差异，与既有 ast-maintenance spec 路径一致）；cleanup 依赖反向：assets 凭证→mnt 凭证→mnt OUTGOING 移动→assets 费用行+维修工单→mnt UsageLine+Usage+Visit→StockLedger/StockBalance 按物料→INCOMING 备货→测试物料；种子 erp_md_subject.csv 补 2502 维修中转清算（plan 2026-07-17-2256-2） |
| hr | `ErpHrShiftAssignment` 单/批量分配 + `ErpHrShift.calcAttendance` 计算入口 | `hr-shift-assignment.action.spec.ts` | DIRECT `@BizMutation` 排班分配 + 考勤计算编排（均无 useWorkflow/useApproval）：自包含建测试专用 employee + ErpHrShift(startTime/endTime/graceLateMinutes) → `assignSingle(employeeId,shiftId,assignmentDate)` 单分配 status=SCHEDULED + 字段回写 + 重复 (employeeId,date) 守卫 **ERR_SHIFT_DUPLICATE_ASSIGNMENT** token「已存在」（一人一天一排班唯一约束）；`assignBatch(employeeIds:List<Long>,shiftId,startDate,endDate)` 返回 `List<ErpHrShiftAssignment>` 经 callMutationOk selection=`id status employeeId shiftId assignmentDate` 断言行数（3 emp × 3 day 笛卡尔积 = 9 行）+ 重复区间跳过既有 → 0 新增行；`copyFromPeriod(sourceStartDate,sourceEndDate,targetStartDate)` 周期复制按 day-offset 复制源区间到目标区间；`calcAttendance(employeeId,assignmentDate)` 数据驱动迟到计算（clockIn=09:30 vs shift.startTime=09:00+graceLate=15 → lateMinutes=30 + assignment.actualStartTime 写回 + status SCHEDULED→PRESENT + isAbsent=false 双落点），准时对照（clockIn=09:05 → lateMinutes=0）；status 翻转 + 字段回写 + List 行数均经 verifyState `__get` 或 GraphQL List 选择集独立断言（plan 2026-07-18-0100-1） |
| hr | `ErpHrShiftRotationPattern` 轮换引擎 generateRotation（首次 + regenerate=true 幂等） | `hr-shift-rotation.action.spec.ts` | DIRECT `@BizMutation` 轮换引擎编排（无 useWorkflow/useApproval）：自包含建测试专用 employee×3 + ErpHrShift×3（codes 对应 patternData）+ `ErpHrShiftRotationPattern`(patternData=JSON 数组 of 班次 code 字符串) → `generateRotation(patternId,groupMemberIds:List<Long>,staggerDays,startDate,endDate,regenerate)` 算法：逐成员 i 按 `memberStart = startDate + staggerDays·i` 错峰起始 → 逐日 sequence[dayIndex % cycleLength] 跳 OFF 跳既有 SCHEDULED → 建 SCHEDULED assignment（确定性样本 3 成员 × staggerDays=1 × cycleLength=3 × 3-day 区间 → 3+2+1=**6 行**）；**regenerate=true** 先 CANCEL 区间内 SCHEDULED 既有（BizModel :71-73 + :177-194 status→CANCELLED）后重建同 count，反查 CANCELLED 行存在 + 当前 SCHEDULED 行数 = 6；**regenerate=false** 静默跳过既有 SCHEDULED（不 CANCEL、不抛守卫）→ 0 新增行；返回 List 行数经 callMutationOk selection + findItems 反查 CANCELLED/SCHEDULED 独立断言（plan 2026-07-18-0100-1） |
| hr | `ErpHrShiftSwapRequest` 排班调换 4 动作状态机 + approve 双向班次交换副作用 | `hr-shift-swap.action.spec.ts` | DIRECT `@BizMutation` 排班调换状态机（无 useWorkflow/useApproval）：自包含建 2 employee × 2 shift × 2 assignment（A 员工 shiftA + B 员工 shiftB 同日）→ `submit(sourceAssignmentId,targetAssignmentId,reason)` **无前置状态守卫、无重复守卫**（每次 submit 经 `code = "SWAP-" + source.id + "-" + nanoTime()` 建新 PENDING 行）→ status=PENDING → `approve(swapRequestId)` PENDING→APPROVED + **核心副作用经 verifyState 双 assignment `__get`**：(a) source.shiftId 翻转=原 target shiftId；(b) target.shiftId 翻转=原 source shiftId；(c) 双 assignment.swapRequestId=swap.id；(d) 双 assignment.replacedByAssignmentId 双向回链 source.replacedBy=target.id / target.replacedBy=source.id；(e) 双 assignment.status=SCHEDULED 重置；`reject` PENDING→REJECTED + 双 assignment 不变；`cancel` PENDING→CANCELLED + 双 assignment 不变；非法迁移守卫（APPROVED→approve / APPROVED→reject 抛 **ERR_SHIFT_SWAP_ILLEGAL_STATUS_TRANSITION** token「不允许」）+ status 不变；spec 显式避测「重复 submit 抛守卫」假设（B1 修订：重复 submit 会建第二个 PENDING 行）（plan 2026-07-18-0100-1） |
| hr | `ErpHrLeaveRequest.approve/cancel` → `ErpHrShiftAssignment` 跨实体钩子联动 + 区间范围准确性 | `hr-leave-shift-linkage.action.spec.ts` | DIRECT `@BizMutation` 跨实体钩子编排（无 useWorkflow/useApproval）：自包含建 employee + shift + 3 SCHEDULED ShiftAssignment（2026-08-10/08-11 区间内 + 2026-08-13 区间外对照）+ ErpHrLeaveRequest(DRAFT, startDate=2026-08-10, endDate=2026-08-12) → `submit`(SUBMITTED) → `approve`(APPROVED) **内部委派 `IErpHrShiftBiz.onLeaveApproved`** → 经 `findFirst` 按 (employeeId, assignmentDate) 反查 3 行断言：(a) 区间内 08-10/08-11 行 4 字段翻转 `isAbsent=true` + `absenceReason=LEAVE` + `leaveRequestId=leave.id`（GraphQL String/Long 类型桥经 `Number()` coercion 比对，同 hr-shift-assignment:147-148 范式）+ `status=ABSENT`；(b) 区间外 08-13 行字段不变（`isAbsent=false` + `absenceReason=null` + `leaveRequestId=null` + `status=SCHEDULED`）；**cancel 正路径还原**：approve 后 → `cancel`(CANCELLED) **内部委派 `IErpHrShiftBiz.onLeaveCancelled`**（仅解除由该 leaveRequestId 标记的行）→ 经 `findFirst` 反查原被标记的 2 行 4 字段还原 `isAbsent=false` + `absenceReason=null` + `leaveRequestId=null` + `status=SCHEDULED`；**区间范围准确性裁决**（Explore item b）：`ErpHrShiftBizModel.findAssignmentsByEmployeeRange :224-233` 仅按 `employeeId + dateBetween(assignmentDate, startDate, endDate)` 过滤，**无 status 过滤**——所有区间内行不论 SCHEDULED/CANCELLED/ABSENT/PRESENT 均被 onLeaveApproved 选中并标记；setup 显式置 3 assignment 全 SCHEDULED 避免噪声，负路径（CANCELLED/ABSENT 在区间内也被翻转）归 Non-Goal successor；4 字段翻转均经 `findFirst` 反查独立断言（plan 2026-07-18-0347-2） |
| hr | `ErpHrEmployeeAssessment`+`ErpHrGapAnalysis`+`ErpHrDevelopmentPlan` 评估→差距→发展计划闭环 + refresh 双入口 + 子项状态机 | `hr-assessment-dev-plan.action.spec.ts` | DIRECT `@BizMutation` 胜任力评估闭环编排（competency-management.md §实现注记 completeAssessment 直传 levels Decision，7 DIRECT 动作，无 useWorkflow/useApproval）：自包含建测试专用 competency×3 + position + roleCompetency×3（同 requiredLevel=5）+ employee(positionId 非空) + assessment(SELF,DRAFT) + detail×3（不同 actualLevel 触发不同 gapSeverity）→ `submitAssessment(assessmentId)` DRAFT→SUBMITTED + 守卫（无 detail 行抛 ERR_ASSESSMENT_NO_DETAILS token「明细」）→ `completeAssessment(assessmentId)` SUBMITTED→COMPLETED + overallScore 写回（mean(4,3,2)=3.00）+ **自动 gap refresh 链**（completeAssessment 内部调 refreshGapAnalysisWithLevels 直传聚合 levels 避免二次查询跨事务可见性问题）→ 经 findFirst 按 employeeId + competencyId 反查 ErpHrGapAnalysis 3 行 + gapSeverity 多档精确数值断言（c1 actual=4 gap=1 MINOR / c2 actual=3 gap=2 MODERATE / c3 actual=2 gap=3 CRITICAL）+ gapValue/requiredLevel/actualLevel 数值断言；**refreshGapAnalysis 双入口对照**：(1) `refreshGapAnalysis(employeeId)` 内部聚合 latest COMPLETED → 同 3 行（幂等覆盖）；(2) `refreshGapAnalysisWithLevels(employeeId, aggregatedLevels:Map<Long,Integer>)` 直传 levels（generic `input('Map', {...})` 范式，键经 String→Long 转换）→ gap 行按新 levels 刷新（actual=5 → 全 NONE）+ 守卫（员工无 position 抛 ERR_GAP_NO_ROLE_REQUIREMENT token「岗位」）；**generateDevelopmentPlan + 子项状态机**：`generateDevelopmentPlan(employeeId)` 仅对 CRITICAL+MODERATE gaps → IN_PROGRESS + N items（MINOR 跳过）+ 经 findFirst 反查 NOT_STARTED 状态 + targetLevel=requiredLevel → `updatePlanItemStatus(planItemId,'IN_PROGRESS')` 状态翻转 + startDate 写回 → `updatePlanItemStatus(planItemId,'ACHIEVED')` + endDate 写回 → `completePlan(planId)` → COMPLETED；**守卫**：completePlan on COMPLETED 抛 ERR_DEV_PLAN_ILLEGAL_STATUS_TRANSITION token「不允许」+ generateDevelopmentPlan 无 actionable gaps 返回 null（data null + errors null）；status/overallScore/gapSeverity/gapValue/planItem status 翻转均经 verifyState `__get` 或 findFirst 反查独立断言；**执行期 latent defect 修复**（`refreshGapAnalysisWithLevels` Map<Long,Integer> GraphQL 反序列化键类型不匹配——JSON object 键为 String，Java `Map<Long,Integer>.getOrDefault(Long,0)` 失败返回 0 全 CRITICAL；新增 `ErpHrGapAnalysisBizModel.normalizeLevelMap`/`toLongKey`/`toIntValue` 三 helper + `ERR_GAP_INVALID_LEVEL_MAP` ErrorCode，仅类型不匹配时抛，hr-service JUnit 全绿）（plan 2026-07-18-0100-2） |
| hr | `ErpHrEmployee.transferEmployee` 员工调动 + 合同联动（handleContract YES/NO/AUTO 三分支） | `hr-transfer.action.spec.ts` | DIRECT `@BizMutation` 员工调动编排（use-cases.md UC-HR-08，1 DIRECT 动作，无 useWorkflow/useApproval）：自包含建 employee(ACTIVE) + 源 EmploymentContract(ACTIVE) + 目标 Department + 目标 Position（属目标 dept）→ **handleContract=YES**：`transferEmployee(employeeId,targetDeptId,targetPositionId,targetSuperiorId=1 种子,effectiveDate,'YES')` → verifyState ErpHrEmployee dept/position 翻转 + 源合同 status=TERMINATED（`ErpHrEmployeeBizModel` 硬编码 `CONTRACT_STATUS_TERMINATED`）+ 经 findFirst 反查新 ACTIVE 合同 status=ACTIVE + signDate=effectiveDate + id≠源合同；**handleContract=NO**：同 setup → transferEmployee(...'NO') → verifyState dept/position 翻转 + 源合同 status 仍 ACTIVE + 计数仍 1（无新合同）；**handleContract=AUTO**（config-gated `erp-hr.transfer-auto-handle-contract=true` 默认值，BizModel.normalizeHandleContract 把 null/空/未知值视作 AUTO）：行为同 YES（源 TERMINATED + 新 ACTIVE）；**守卫**：RESIGNED 员工抛 ERR_EMPLOYEE_NOT_TRANSFERABLE token「不可调动」+ dept 不变；目标 dept + position 不属 dept（sourcePos 假冒 targetPos）抛 ERR_TRANSFER_TARGET_POSITION_NOT_FOUND token「目标职位」；员工字段翻转 + 合同 status 翻转 + 新合同创建均经 verifyState `__get` 或 findFirst/findPageTotal 反查独立断言；**Explore 裁决**：(a) GraphQL schema 标 `targetSuperiorId`/`handleContract` 为非空（即便 Java 签名允许 null）—— `targetSuperiorId=1` 种子 HR-EMP-001 + `handleContract` 显式传字符串字面值；(b) `buildSuccessorCode` 新合同码超 code precision=50 同 1430-1 类 buildCode overflow 缺陷，spec 用紧凑短码 `C${tag}-{ms}-{seq}` 规避，生产代码层修复归 successor（plan 2026-07-18-0100-2） |
| maintenance | `ErpMntVisit` cancel + MAINTENANCE_LABOR 凭证红冲（cancel-reversal） | `mnt-visit-cancel-reversal.action.spec.ts` | DIRECT `@BizMutation` 维修访问 cancel 红冲编排（config-gated `erp-mnt.labor-posting-enabled=true` + `erp-mnt.default-labor-hourly-rate=80`，webServer JVM arg）：自包含建 Equipment(种子 id=1 RUNNING) + Visit(DRAFT + startTime/endTime 预置 60min 产生确定性 totalMinutes) → `schedule`(DRAFT→SCHEDULED) → `start`(SCHEDULED→IN_PROGRESS) → `complete`(→COMPLETED + doComplete 触发 `postLabor` 生成 MAINTENANCE_LABOR NORMAL 凭证 voucherId=A，billHeadCode=visit.code+"-ML") → **状态机纠错**：经 `ErpMntVisit__update` 将 status 改回 IN_PROGRESS（模拟「错误完工」回退，validateNotTerminal 仅 DRAFT/SCHEDULED/IN_PROGRESS 可 cancel）→ `cancel(visitId)` → doCancel 内 `if (isPostingEnabled) try { reverseLabor(visit) } catch LOG.warn`（吞异常保持 cancel 终态）→ verifyState 断言 status=CANCELLED + 经 `findFirst ErpFinVoucher(eqFilter('id',A))` 断言原凭证 isReversed=true（ErpFinPostingProcessor.markOriginalVoucherReversed 公共流程）+ 经 `findVoucherIdByBillCode(visit.code+"-ML",'REVERSAL')` 反查红字凭证 voucherId=B（与原凭证共用 billHeadCode，按 postingType 区分）+ assertVoucherLines Dr 6602=-80 折旧费用/Cr 2211=-80 应付职工薪酬（红字同向取负：dcDirection 不变，金额取负，rate=80 元/小时，60min×80/60=80）；cleanup 删凭证（原+红字共用 billHeadCode，cleanupVoucherByBillCode 全量清理）+ Visit；config-gated 关闭路径经后端单测 `TestErpMntVisitCancelReversal` 覆盖（plan 2026-07-18-1745-1） |
| maintenance | `ErpMntSparePartUsage` reverseConfirm + MAINTENANCE_ISSUE 凭证红冲 + 反向 OUTGOING 移动单 | `mnt-spare-part-usage-reversal.action.spec.ts` | DIRECT `@BizMutation` 备件消耗红冲编排（2 测试，config-gated `erp-mnt.spare-part-posting-enabled=true`）：(a) **正路径** 自包含建测试专用备件物料 + INCOMING 备货 + Equipment(种子 id=1) + SparePartUsage(DRAFT/UNSUBMITTED) + Line(qty=10×unitCost=5=amount=50) → `confirm(usageId)` → posted=true + docStatus=ACTIVE + OUTGOING 移动单(DONE) + MAINTENANCE_ISSUE NORMAL 凭证 voucherId=A（Dr 6602=50/Cr 1403=50）→ `reverseConfirm(usageId)` → validateCanReverse 守卫通过 → 内部 try { reverseIssue(usage)（红冲 MAINTENANCE_ISSUE 凭证）} catch LOG.warn + findIssueMove + try { stockMoveBiz.reverse(originalMove.id)（生成 REVERSAL 反向移动单）} catch LOG.warn + reload usage + doReverseConfirm（posted=false + docStatus=CANCELLED）→ verifyState 双字段翻转独立断言 + 经 findFirst ErpFinVoucher 断言原凭证 isReversed=true + findVoucherIdByBillCode(usage.code+"-MI",'REVERSAL') 反查红字凭证 voucherId=B + assertVoucherLines Dr 6602=-50/Cr 1403=-50（同向取负）+ findFirst ErpInvStockMove 反查 REVERSAL 反向冲销移动单（relatedBillType=REVERSAL + relatedBillCode=原移动单 code）；(b) **未过账守卫** 未 confirm 直接 reverseConfirm → GraphQL errors 含「未过账」/「不可红冲」语义 token（ERR_SPARE_PART_USAGE_NOT_POSTED）+ 状态不变；cleanup 依赖反向：凭证（原+红字）→ REVERSAL 移动+OUTGOING 移动+Ledger+Balance+UsageLine+Usage+物料（plan 2026-07-18-1745-1） |
| inventory | `ErpInvLandedCost` reverseApprove + LANDED_COST 凭证红冲 + 成本层回退 | `inv-landed-cost-reversal.action.spec.ts` | DIRECT `@BizMutation` 到岸成本红冲编排（2 测试）：(a) **正路径** 复用 0606-2 既有到岸成本 setup（建测试专用物料+INCOMING 备货+APPROVED Receive+LandedCost+Line）→ `approve(id)` → posted=true + LANDED_COST NORMAL 凭证 voucherId=A（Dr 1401=50/Cr 2202=50）+ 成本层 avgCost 更新（10→15）→ `reverseApprove(id)` → validateCanReverse 守卫通过 → 内部 try { postingDispatcher.reverse（红冲 LANDED_COST 凭证）} catch LOG.warn + CostAdjustmentService.reverseCostAdjust（反向应用 LANDED_COST_SUPPLEMENT 成本层，avgCost 回退 15→10）+ doReverseApprove（posted=false + approveStatus=REJECTED + docStatus=CANCELLED）→ verifyState 三字段翻转独立断言 + 经 findFirst ErpFinVoucher 断言原凭证 isReversed=true + findVoucherIdByBillCode(landedCost.code,'REVERSAL') 反查红字凭证 voucherId=B（billHeadCode=landedCost.code 无后缀）+ assertVoucherLines Dr 1401=-50/Cr 2202=-50（同向取负）+ findFirst ErpInvStockBalance 断言 avgCost 回退至原始 10；(b) **未过账守卫** 未 approve 直接 reverseApprove → GraphQL errors 含「未过账」语义 token（ERR_LANDED_COST_NOT_POSTED）+ 状态不变；cleanup 依赖反向：凭证（原+红字共用 billHeadCode）→ CostAdjustLine/CostAdjust → Ledger+Balance+INCOMING 备货移动+LandedCost+Receive+物料（plan 2026-07-18-1745-2） |
| manufacturing | `ErpMfgMaterialIssue` reverseConfirm + MANUFACTURING_ISSUE 凭证红冲 + 反向 OUTGOING 移动单 | `mfg-material-issue-reversal.action.spec.ts` | DIRECT `@BizMutation` 领料红冲编排（2 测试）：(a) **正路径** 自包含建测试专用组件物料+成品物料 + INCOMING 备货（qty=20×unitCost=5）+ BOM+Line + WorkOrder+INPUT 行 + MaterialIssue(DRAFT/UNSUBMITTED) + Line(qty=10) → `confirm(issueId)` → posted=true + docStatus=DONE + OUTGOING 移动单(DONE) + MANUFACTURING_ISSUE NORMAL 凭证 voucherId=A（Dr 1411 WIP=50/Cr 1401 存货=50，billHeadCode=issue.code+"-MI"）→ `reverseConfirm(issueId)` → validateCanReverse 守卫通过 → 内部 try { issuePostingDispatcher.reverse（红冲 MANUFACTURING_ISSUE 凭证）} catch LOG.warn + findIssueMove + try { stockMoveBiz.reverse(originalMove.id)（生成 REVERSAL 反向移动单）} catch LOG.warn + reload issue + doReverseConfirm（posted=false + docStatus=CANCELLED）→ verifyState 双字段翻转独立断言 + 经 findFirst ErpFinVoucher 断言原凭证 isReversed=true + findVoucherIdByBillCode(issue.code+"-MI",'REVERSAL') 反查红字凭证 voucherId=B + assertVoucherLines Dr 1411=-50/Cr 1401=-50（同向取负）+ findFirst ErpInvStockMove 反查 REVERSAL 反向冲销移动单（relatedBillType=REVERSAL + relatedBillCode=原移动单 code）；(b) **未过账守卫** 未 confirm 直接 reverseConfirm → GraphQL errors 含「未过账」/「不可红冲」语义 token（ERR_MATERIAL_ISSUE_NOT_POSTED）+ 状态不变；cleanup 依赖反向：凭证（原+红字）→ REVERSAL 移动+OUTGOING 移动+Ledger+Balance+MaterialIssueLine+MaterialIssue+WorkOrderLine+WorkOrder+BOMLine+BOM+物料（plan 2026-07-18-1745-2） |
| manufacturing | `ErpMfgCostVariance` calculateVariances 重算 + PRODUCTION_VARIANCE 凭证红冲闭环 | `mfg-variance-recompute-reversal.action.spec.ts` | DIRECT `@BizMutation` 重算红冲编排（1 测试）：自包含复用 1800-2 既有 `runMfgChain` + variance setup（测试专用成品物料 STANDARD costing + 运行时 FIRMED rollup materialCost=90/laborCost=5/overheadCost=5/unitCost=100，config `erp-mfg.variance-auto-calc-enabled=true` webServer JVM arg）→ runMfgChain 满量完工触发 Call site B 首次差异计算/过账 → 5 类差异行 + NORMAL PRODUCTION_VARIANCE 凭证（billHeadCode=woCode+"-PV"，Dr 1410=100/Cr 1411=100/Dr 1412=50/Cr 1413=50/Dr 1415=50/Cr 1414=50）+ 全行 posted=true → **关键驱动**：再次 `ErpMfgCostVariance__calculateVariances(workOrderId)` → Call site A `reverseIfExists` 红冲原凭证（woCode+"-PV" + PRODUCTION_VARIANCE）→ deleteByWorkOrder 删旧行 → calculateVariances 重算 → dispatchIfApplicable 派发新凭证 → 断言：(1) 原 NORMAL 凭证 isReversed=true（findFirst ErpFinVoucher by id）；(2) REVERSAL 红字凭证存在（findVoucherIdByBillCode(woCode+"-PV",'REVERSAL')）+ assertVoucherLines 行同向取负（Dr 1410=-100/Cr 1411=-100/Dr 1412=-50/Cr 1413=-50/Dr 1415=-50/Cr 1414=-50）；(3) ErpFinVoucherBillR(billCode=woCode+"-PV") 反查过滤 postingType=NORMAL + isReversed=false 仅 1 条（无孤儿凭证一致性不变量）；(4) ErpMfgCostVariance 数据行全 posted=true（dispatchIfApplicable 重新派发成功 markPosted）；cleanup 经 `cleanupMfg` 扩展覆盖 ErpMfgCostVariance + PRODUCTION_VARIANCE 凭证（原+REVERSAL+新 NORMAL 共用 billHeadCode，cleanupVoucherByBillCode 全量清理）+ 本 spec 专属清理 FIRMED rollup/Line + 测试专用物料（plan 2026-07-18-2251-1） |
| finance | `ErpFinNotesReceivable` 多币种票据贴现 + FX 6051 凭证行（signum≠0 触发 + 单币种对照 signum=0 抑制） | `fin-notes-receivable-fx-discount.action.spec.ts` | 多入口 DIRECT `@BizMutation` 多币种票据贴现三层验证（1430-1 Deferred「多币种票据贴现浏览器层 E2E」successor，解除触发条件经实时仓库核实已满足）：(1) **FX 状态机生命周期** 自包含建 USD `ErpFinNotesReceivable`(currencyId=2 + exchangeRate=6.6667 + amountSource=USD 100 + amountFunctional=CNY 666.67) + `ErpFinFundAccount(BANK,currencyId=2)` → `discount` mutation（discountDate=2026-07-01 + discountRate=0.12 + bankId）→ DISCOUNTED + posted=true + 3 行凭证（Dr 1002=660/Dr 6603=6.67/Cr 1121=666.67 全 functional CNY；6051 因 `ErpFinNotesReceivableProcessor.buildDiscount:249` 硬编码 ZERO 抑制）；(2) **FX 6051 触发分支** 经 `ErpFinVoucher__post(event:PostingEventInput)` 直驱 NOTES_RECEIVABLE_DISCOUNTED event（billData.EXCHANGE_GAIN_LOSS=5.00 非零，billHeadCode 唯一幂等键，对齐 `finance-voucher-post.action.spec.ts` 范式）→ 4 行凭证（Dr 1002=655/Dr 6603=6.67/Dr 6051=5.00 signum>0 分支/Cr 1121=666.67 复式平衡 NET_AMOUNT_FX_VOUCHER=655 派生）——**唯一可达 6051 浏览器层路径**（Java builder 缺陷，spec 改用 post 直驱）；(3) **对照 6051 抑制** 同 `ErpFinVoucher__post` 入口 + EXCHANGE_GAIN_LOSS=0 → 3 行凭证（Dr 1002=990/Dr 6603=10/Cr 1121=1000 无 6051），与 (2) 唯一变量 EXCHANGE_GAIN_LOSS 形成 crisp 4 行 vs 3 行对照；汇率方向经 `ExchangeRevaluationService.java:55` 实测确认 functional/source（USD→CNY=6.6667 表示 1 USD=6.6667 CNY）；凭证行翻转经 `verifyState`（`__get`）/`findVoucherIdByBillCode`/`assertVoucherLines` 独立断言；种子 erp_md_subject.csv 补 6051 汇兑损益（id=44，EXPENSE/DEBIT 对齐 ExchangeRevaluationService 既有用法）；**Java builder 缺陷 successor**：`ErpFinNotesReceivableProcessor.buildDiscount:249` 外币 exchangeGainLoss 派生归 Deferred 显式 successor（plan 规则 13/14，不改生产代码即时修）；cleanup 删凭证+note+discount+fundAccount 保护 finance 看板基线（plan 2026-07-19-0120-1） |

### 业务动作调用范式（`_helper.ts`）

- **三个原语**：`createViaSave`（经标准 `__save` 建前置实体，复用 write.spec.ts `${entity}__save_input` 范式）、`callMutation`（经 GraphQL mutation 调自定义 `@BizMutation`，标量入参内联、复杂入参经 `input(type, value)` 包装走 variable + 显式 GraphQL input 类型名）、`verifyState`（经 `__get` 独立断言状态字段，独立于 mutation 返回值权威查库）。
- **关键发现（Nop GraphQL filter 格式）**：`__findPage` 的 `filter` 必须用 Nop FieldTreeBean Map 格式 `{ $type:'eq', name, value }`（`$type` 持有算子，`name`/`value` 持有属性）；plain-map 字段相等（`{moveId:123}`）报 `nop.err.core.filter.op-is-null`。`eqFilter(field,value)` / `andFilter(...leafs)` helper 封装此格式。
- **状态字段核实**：inventory StockMove 状态字段 `docStatus`（DRAFT/CONFIRMED/DONE/CANCELLED）+ `posted`（Boolean，过账标志）；CRM Lead 状态字段为 `docStatus`（非 `leadStatusId` 后者线索子状态 FK）；CS Ticket 状态字段为 `status`（六态）。
- **实现修订（generateMove 自动 confirm）**：经核实 `ErpInvStockMoveProcessor`，`generateMove` 内部经 `doConfirm` 自动推进 DRAFT→CONFIRMED（独立创建无 relatedBillType 停在 CONFIRMED），「confirm」为内部过渡步骤无独立 DRAFT 创建入口。故 StockMove spec 实测状态链为 `generateMove(独立)→CONFIRMED→complete→DONE`。
- **posted 字段语义**：`posted` 反映跨域财务过账（`InvPostingDispatcher`：成功置 true、失败优雅降级 false 不阻塞 DONE 终态）。spec 断言 `typeof posted === 'boolean'` + 同事务不可变流水 `ErpInvStockLedger` 非空（可靠过账产物）；凭证借贷平衡精确数值归 finance 数值断言层 successor。
- **产物清理（强制）**：业务动作创建不可逆下游产物（库存流水/余额、工单审计/调查、线索转化日志）。全栈共享同一 Quarkus+H2 实例（`reuseExistingServer: true`），不清理会污染下游数值断言（dashboard KPI 聚合 stock_balance/ledger、report 聚合 ticket/survey）。每个 spec 在断言完成后经 `deleteByFilter`/`deleteById` 清理自身产物（镜像 write.spec.ts create→delete 范式，但扩展到不可逆下游产物逐域删除）。

## 跨域编排链浏览器层 E2E（`orchestration/`，P2P + O2C）

在业务动作层之上，1249-1 叠加了 P2P（Procure-to-Pay）+ O2C（Order-to-Cash）核心业财循环的跨域编排链浏览器层端到端验证（解除 0814-2 Deferred「跨域编排链完整 E2E」+「业财过账凭证数值断言」）。经 GraphQL `/graphql` 驱动全链 `__save`（头+行）→ `submitForApproval` → `approve`，每步 `verifyState` 经 `__get` 独立断言 approveStatus 翻转，并断言业财过账产物（库存移动 + GL 凭证 + AR-AP 辅助账）。

| 链路 | spec | 验证内容 |
| --- | --- | --- |
| P2P（PO→Receive→Invoice） | `p2p-chain.spec.ts` | PO/Receive/Invoice approveStatus UNSUBMITTED→SUBMITTED→APPROVED + Receive approve 触发 INCOMING 移动（DONE）+ Invoice approve GL 过账（posted=true + voucher bill_r 回链 + AP 辅助账 PAYABLE/openAmount=含税56.5/OPEN） |
| O2C（SO→Delivery→Invoice） | `o2c-chain.spec.ts` | SO/Delivery/Invoice approveStatus 三态翻转 + SO approve 信用控制通过 + Delivery approve 触发 OUTGOING 移动（DONE）+ Invoice approve GL 过账（posted=true + voucher 回链 + AR 辅助账 RECEIVABLE/openAmount=含税113/OPEN） |
| 制造链（WorkOrder+MaterialIssue+JobCard 三聚合根协作） | `mfg-chain.spec.ts` | WorkOrder 审批轴 DRAFT→SUBMITTED→NOT_STARTED + 齐套校验 STOCK_RESERVED + 开工 IN_PROCESS + MaterialIssue.confirm 触发 OUTGOING 移动 + WorkOrder.materialCost 回写 + JobCard.recordWork 回写 WorkOrder.laborCost + reportCompletion 触发 MANUFACTURE 入库移动（DONE）+ WorkOrder COMPLETED + completedQuantity/totalCost/unitCost 重算 + posted=false（完工入库 GL 过账为 Non-Goal，0704-2） |
| 生产差异（config-gated auto-calc 满量完工触发） | `mfg-variance.spec.ts` | config `erp-mfg.variance-auto-calc-enabled=true` 启用后满量完工（willFinish=true）触发 ProductionVarianceCalculator.calculateVariances 5 类差异行（MATERIAL_USAGE/LABOR_EFFICIENCY/LABOR_RATE/OVERHEAD/VOLUME）+ ProductionVarianceDispatcher.dispatchIfApplicable PRODUCTION_VARIANCE 凭证行精确数值断言（Dr 1410=100/Cr 1411=100/Dr 1412=50/Cr 1413=50/Dr 1415=50/Cr 1414=50，标准 vs 实际成本差额派生）+ ErpMfgCostVariance 5 行 posted=true；自包含隔离（测试专用成品物料+运行时 FIRMED rollup，非 MAT-001，1800-2） |
| 批次基因追溯（config-gated write-on-completion + 追溯查询） | `mfg-genealogy.spec.ts` | `runMfgChain({ withBatchTracking: true })` 创建输入 ErpInvBatch + MaterialIssueLine.batchNo + 备货/领料移动携 batchNo（余额按批次维度落入）→ 满量完工触发 BatchGenealogyWriter.writeOnCompletion 写入 inputLot→outputLot 基因链（ErpMfgBatchGenealogy findPage 非空 + inputMaterialId/outputMaterialId 正确）+ forwardTrace(outputLotId)/backwardTrace(inputLotId) 返回非空消耗关系（0730-1） |
| 质检门控阻断完工（Gate 1: config + BOM.inspectionRequired） | `mfg-inspection-gate.spec.ts` | config `erp-mfg.inspection-gate-enabled=true` + `runMfgChain({ inspectionRequired: true })` BOM 设 inspectionRequired=true → 满量完工 reportCompletion 被 ErpMfgWorkOrderProcessor.isInspectionGated 拦截抛 ERR_INSPECTION_REQUIRED（GraphQL errors 含「质检」语义 token）+ docStatus 保持 IN_PROCESS；对照路径 inspectionRequired=false（默认）→ reportCompletion 成功 COMPLETED（门控精确性，0730-1） |
| 委外加工单生命周期（config-gated posting + release） | `mfg-subcontract-chain.spec.ts` | `runSubcontractChain` 编排 DRAFT→SUBMITTED→APPROVED→ISSUED→RECEIVED→COMPLETED 全链 + 三段 GL 凭证（SUBCONTRACT_ISSUE/RECEIPT/FEE billHeadCode `{code}-SI/-SR/-SF`）精确数值断言 + posted=true；config `erp-mfg.subcontract-posting-enabled=true` 启用三段过账。0035-2 扩展（5 测试）：① MRP 释放→自动建 APPROVED 骨架单（config `erp-mfg.subcontract-release-enabled=true`，code=`SUB-MRP-{lineId}`，processingFee=0，计划行 isFirmed + plan FIRMED）；② 多行发料（`lineCount:3` → 3 StockMoveLine + Dr 1408 汇总/Cr 1401 按物料分列）；③ 部分收货（`stopAfterIssue` + receivedQty<lineQty → MANUFACTURE 移动行 quantity=receivedQty）；④ cancel 正路径（DRAFT/SUBMITTED/APPROVED→CANCELLED）+ 守卫（ISSUED/RECEIVED/COMPLETED 拒绝）。1934-1 扩展（2 测试）：⑤ reverseCompletion 红冲正路径——COMPLETED→CANCELLED + posted=false（经 `verifyState` `__get` 独立断言）+ 三段 GL 红字凭证行同向取负（dcDirection 不变金额取负：SI Dr1408=−issueCost/Cr1401=−issueCost 等）+ 原 NORMAL 凭证 isReversed=true + issue OUTGOING 移动反向（REVERSAL 移动单，库存域 `ErpInvStockMoveProcessor.reverse` 生成的反向冲销移动单 relatedBillType=REVERSAL + relatedBillCode=原移动单 code）；⑥ reverseCompletion 守卫——RECEIVED（未 COMPLETED）调用抛 `ERR_SUBCONTRACT_CANNOT_REVERSE`（GraphQL errors 含「不允许红冲」语义 token）+ docStatus 不变（9 测试） |

### 编排链范式（`orchestration/_helper.ts`）

- **链式驱动**：`runP2pChain` / `runO2cChain` 编排多实体链式创建（头 `__save` → 行独立 `__save` FK 引用头 id，行实体 `registerShortName=true` 有独立 GraphQL 端点）+ `submitForApproval` + `approve`（DIRECT 审批模式，Payment/Receipt xwf WORKFLOW 模式归 Deferred）。每步 `verifyState` 经 `__get` 权威断言状态翻转。返回各实体 id + 下游移动单（findFirst by relatedBillType+relatedBillCode）供 spec 断言/清理。
- **过账产物断言原语**：`findItems`（`__findPage` 返回 items，读 stockMove.code / ar_ap.openAmount 等详情）；`findPageTotal`（0814-2 原语，total≥1 存在性）。
- **清理原语**：`cleanupVoucherByBillCode`（经 billCode 关联删凭证行+凭证+回链）+ `cleanupArApByCode`（按 sourceBillCode 删 AR-AP 辅助账）+ `cleanupStockMove`（移动单凭证+流水+移动单行+移动单+余额逐域逻辑删除）。链路 spec 在 `finally` 调 `cleanupP2p`/`cleanupO2c` 清理全部不可逆下游产物，保护共享 DB 数值断言基线（inventory dashboard totalValue / ar-ap-aging 报表）。
- **O2C 备货前置**：WH-RAW/MAT-1 种子无余额，出库会因负库存禁止（`CONFIG_ALLOW_NEGATIVE_STOCK` 默认 false）失败。链路前先 `generateMove` INCOMING 备货（独立移动 → CONFIRMED → `complete` → DONE），WH-RAW/MAT-1 余额无种子行清理时整行删除安全。

### 制造链编排层（WorkOrder + MaterialIssue + JobCard 三聚合根协作，plan 2026-07-10-0704-2）

在 P2P/O2C 业财循环编排之上，0704-2 叠加了制造工单完整链路编排浏览器层 E2E：经 GraphQL `/graphql` 驱动 WorkOrder + MaterialIssue + JobCard 三聚合根协作全链——前置备货（组件物料建库存）→ BOM+行 → WorkOrder+OUTPUT/INPUT 行 → 审批轴（submit→approve）→ 齐套校验（checkAvailability→STOCK_RESERVED）→ 开工（start→IN_PROCESS）→ 领料出库（MaterialIssue→confirm，触发 OUTGOING 移动 + WorkOrder.materialCost 回写）→ 报工（JobCard→startJob→recordWork→回写 WorkOrder.laborCost→submitJob→completeJob）→ 完工入库（reportCompletion，触发 MANUFACTURE 入库移动 + WorkOrder.totalCost/unitCost 重算 + COMPLETED）。

- **链式驱动（`runMfgChain`）**：镜像 `runP2pChain`/`runO2cChain` 范式，多实体链式创建（BOM/BOMLine/WorkOrder/WorkOrderLine/MaterialIssue/MaterialIssueLine/JobCard 均经 `createViaSave` `__save`，行实体 `registerShortName=true` 有独立端点，FK 显式引用头 id）+ 审批轴 + 状态机迁移（每步 `verifyState` `__get` 独立断言 docStatus 翻转）。返回各实体 id + 完工入库移动单供 spec 断言/清理。
- **跨聚合根协作产物断言**：(1) MaterialIssue.confirm 经 `IErpInvStockMoveBiz.generateMove` 生成 OUTGOING 移动（relatedBillType=ERP_MFG_ISSUE，业务联动自动 DONE）+ 出库流水 totalCost 绝对值回写 WorkOrder.materialCost；(2) JobCard.recordWork 经 `@RequestBean JobCardWorkRecord` 写 JobCardTimeLog + laborCost（durationMins/60 × hourlyRate）回写 WorkOrder.laborCost；(3) reportCompletion 经 `generateCompletionMove` 生成 MANUFACTURE 入库移动（relatedBillType=ERP_MFG_WORK_ORDER，读 WorkOrderLine OUTPUT 行 destWarehouseId）+ WorkOrder totalCost（material+labor+overhead+subcontract）/unitCost（total/completed）重算 + docStatus=COMPLETED。
- **确定性成本裁决**：组件物料用**测试专用新建物料**（非种子 MAT-003）。MAT-003 在 WH-RAW 有种子余额（100@8.5，WEIGHTED_AVERAGE），内联备货会与种子混合致 materialCost 非确定性、且清理整行删余额会抹除种子 850 污染 inventory dashboard totalValue（10450→9600 回归）。新建测试物料（MOVING_AVERAGE，无种子余额）→ materialCost 确定性（出库 20 × unitCost 50 = 1000，无混合）+ 清理整行删余额安全（不污染种子库存基线）。
- **`@RequestBean` 入参序列化裁决**：`ErpMfgJobCard__recordWork` 的 `@RequestBean JobCardWorkRecord` 经 Nop GraphQL **展平为独立标量参数**（非 `record` 包装，经后端 `TestErpMfgWorkOrderEndToEnd#recordWorkRequest` 范式核实）——直接经 `callMutationOk` 内联标量参数（jobCardId/operatorId/workDate/durationMins/hourlyRate/...），区别于 `generateMove` 的 `request: input(StockMoveRequest)` 包装。
- **posted=false（Non-Goal）**：完工入库移动（MANUFACTURE）经 `reportCompletion` 生成但 `posted=false`——MANUFACTURING_RECEIPT GL 过账凭证依赖 finance 域制造过账 Provider（尚未构建，见 `2237-1` state-machine doc §实现偏离补注）。成本归集（materialCost/laborCost/totalCost/unitCost 在 WorkOrder 上）已完整，仅 GL 过账缺失，故 spec 断言 `completionMove.posted=false` + `WorkOrder.posted=false`。完工入库 GL 过账（1100-5 已落地）/ 生产差异计算（1800-2 已落地）/ 异常分支+部分完工（1800-2 已落地）/ 批次基因追溯（0730-1 已落地，见 `mfg-genealogy.spec.ts`）/ 质检门控 Gate 1（0730-1 已落地，见 `mfg-inspection-gate.spec.ts`）均已解除 Deferred。
- **清理原语（`cleanupMfg`）**：逐域逻辑删除（入库移动+成品余额 → MaterialIssue(行) → JobCard+TimeLog → 出库移动+组件余额 → WorkOrderLine → WorkOrder → BOM(行) → 备货移动+组件余额 → 测试专用组件物料），保护共享 DB 数值断言基线（inventory dashboard totalValue）。0730-1 扩展：批次基因链产物（ErpMfgBatchGenealogy filter workOrderId + 输出批次 batchNo=`FG-{woCode}` + 输入批次 by id）。

### 制造域 config-gated 浏览器层 E2E（plan 2026-07-11-0730-1）

0704-2/1800-2 制造链编排基线之上，0730-1 叠加了两项 config-gated 功能的浏览器层 E2E：批次基因追溯 + 质检门控阻断完工（Gate 1）。webServer JVM args 追加 `-Derp-mfg.inspection-gate-enabled=true`（`playwright.config.ts:18`），与既有 `variance-auto-calc-enabled=true` 并列；该 config 仅对 `BOM.inspectionRequired=true` 的工单生效，不影响既有 mfg-chain/mfg-variance spec（BOM.inspectionRequired 默认 false）。

- **批次基因追溯（`mfg-genealogy.spec.ts`）**：`runMfgChain({ withBatchTracking: true })` 扩展——创建输入 `ErpInvBatch`（batchNo=`GEN-IN-{ts}`）+ 备货/领料移动携 batchNo（`StockMoveBookkeeper.upsertBalance` 以 batchNo 为余额键维度之一，故备货/领料须共用同一 batchNo 使余额按批次维度落入）+ MaterialIssueLine.batchNo 引用之。满量完工触发 `BatchGenealogyWriter.writeOnCompletion` 写入 `ErpMfgBatchGenealogy`（inputLot→outputLot 消耗行，产出批次 batchNo=`FG-{woCode}` 由 `ensureOutputLot` 自动创建）。断言三层：findPage 非空 + inputMaterialId(组件)/outputMaterialId(成品) 正确 + `forwardTrace(outputLotId)`/`backwardTrace(inputLotId)` 返回非空消耗关系。
- **质检门控 Gate 1（`mfg-inspection-gate.spec.ts`）**：`runMfgChain({ inspectionRequired: true })` 扩展——BOM 经 `__save` 设 `inspectionRequired=true`，链路停在 completeJob（WO=IN_PROCESS，跳过 reportCompletion）。负路径：spec 驱动 `reportCompletion`（满量完工 willFinish=true）被 `ErpMfgWorkOrderProcessor.isInspectionGated` 拦截抛 `ERR_INSPECTION_REQUIRED`（GraphQL errors 含「质检」语义 token）+ docStatus 保持 IN_PROCESS。对照路径：`inspectionRequired=false`（默认）→ reportCompletion 成功 COMPLETED（Gate 1 未命中，门控精确性）。

- **关键发现（业财过账 COA 完备性修复）**：执行发现种子 COA（`erp_md_subject.csv`）与过账 Provider 硬编码科目码不一致——`PurAcctDocProvider`(1403/2221/2202)、`SalAcctDocProvider`(1131/6001/2221)、`InvAcctDocProvider`(1401/6401/2202) 所需 1403/2221/1131/1401/6401 在种子缺失，致 `resolveSubjects` 抛 `ERR_SUBJECT_NOT_FOUND`→过账优雅降级 posted=false。补齐种子 COA（`erp_md_subject.csv` +5 行 1401/1403/1131/2221/6401，`findByCode` 全局按码解析无需 COA 映射）后过账 happy-path 可达。安全性：`persistVoucher` 仅写 voucher/voucher_line/voucher_bill_r（**不写 gl_balance**），finance 看板/资产负债表/利润表读 gl_balance 不受影响，全套件 0 回归。此为种子演示数据完备性修复（非生产代码/契约/模型变更）。
- **Payment/Receipt xwf 浏览器层裁决（Deferred — 已权威裁决不可行）**：Payment/Receipt 为 `useWorkflow=true` xwf WORKFLOW 模式。原型实证：`nop` 浏览器用户调 `submitForApproval`，xwf 返回 `步骤[submit]不允许被用户[<nop uuid>]调用,步骤的参与者限定为[user:$0]`——wf `submit` 步骤参与者限定为 `user:$0`（SYS id=0，后端测试 `setUserId("0")` 规避），`nop` 用户不匹配致 submit 被拒。**经 plan 2026-07-09-2330-1 权威裁决：不可行**（3 条路径均阻断：① wf 委托机制存在但 sysUser(0) 无法经浏览器层 `__save` 物化为真实可引用用户——`NopAuthUser.userId` 列 `tagSet="seq"` 覆盖显式 "0" 为 UUID；② 浏览器层无用户身份注入/伪装 API（`-Dnop.auth.service-public=true` 仅旁路 auth 不改 `ctx.getUserId()`）；③ `.xwf` submit step `<assignment>` 放宽属生产审批契约变更非浏览器层可行路径）。归 Deferred successor（触发条件：nop-entropy 平台支持浏览器层测试用户身份映射 / 委托免 sysUser 物化 / sysUser 种子物化时，详见下方「useWorkflow 审批轴浏览器层」段）。

### 凭证行精确数值断言（plan 2026-07-10-0704-1）

在凭证存在性 + 辅助账 openAmount 断言之上，0704-1 叠加了过账凭证行（`ErpFinVoucherLine`）级 **subjectCode + dcDirection + debitAmount/creditAmount 精确数值**断言，覆盖 P2P/O2C 正向发票过账 + 财务侧反向冲销红字凭证 + Return 过账四条核心业财链路。捕获能力升级：科目码映射错位（如 Provider subjectCode 拼写错误）、不含税/含税金额混用、红冲借贷镜像断裂（方向翻转或金额未取负）均可在凭证行级被发现——此前仅断言凭证存在性 + 辅助账 openAmount，无法捕获此类行级错误。0742-1 将覆盖扩展至 assets 域全部 5 个过账业务类型（DEPRECIATION / CAPITALIZATION / ASSET_INVENTORY_ADJUSTMENT-SHORTAGE / MAINTENANCE_EXPENSE / MAINTENANCE_CAPITALIZATION）的正向 + 红冲凭证行断言，assets 自此不再是「过账 happy-path E2E 已落地但凭证行级数值断言缺失」的核心域。

**可复用原语（`orchestration/_helper.ts`）：**

- `findVoucherIdByBillCode(page, billCode, postingType?)`：经 `ErpFinVoucherBillR`(billCode) 反查 voucherId，可选按 `ErpFinVoucher.postingType`(NORMAL/REVERSAL) 过滤。反向冲销红字凭证与原正常凭证共用同一 billHeadCode（`reverseProcess` 同 code 写 voucher_bill_r），故 postingType 过滤是区分原/红字凭证的唯一手段。
- `assertVoucherLines(page, voucherId, expectedLines[])`：经 `ErpFinVoucherLine__findPage`(voucherId)（实体 `registerShortName=true` 有独立端点）查凭证行，按 subjectCode 唯一匹配，逐行断言 dcDirection + debitAmount + creditAmount。BigDecimal 经 `Number()` 转换后 `toBe` 精确匹配（金额为确定性种子派生，无浮点误差）。

**Provider 结构派生期望值表（期望行由各 AcctDocProvider 的科目码 + 金额口径派生）：**

| 业务类型 | spec | 凭证行期望（subjectCode / dcDirection / 金额） | 金额来源 |
| --- | --- | --- | --- |
| AP_INVOICE | `p2p-chain.spec.ts` | 1403 DEBIT 50 / 2221 DEBIT 6.5 / 2202 CREDIT 56.5 | Dr 1403=TOTAL_AMOUNT(50) + Dr 2221=TOTAL_TAX(6.5) / Cr 2202=TOTAL_WITH_TAX(56.5) |
| AR_INVOICE | `o2c-chain.spec.ts` | 1131 DEBIT 113 / 6001 CREDIT 100 / 2221 CREDIT 13 | Dr 1131=TOTAL_WITH_TAX(113) / Cr 6001=TOTAL_AMOUNT(100) / Cr 2221=TOTAL_TAX(13) |
| 红字（AP_INVOICE 冲销） | `p2p-reverse.spec.ts` | 1403 DEBIT -50 / 2221 DEBIT -6.5 / 2202 CREDIT -56.5 | `buildReversalDraft`：dcDirection 不变、金额取负（原行 50→-50） |
| 红字（AR_INVOICE 冲销） | `o2c-reverse.spec.ts` | 1131 DEBIT -113 / 6001 CREDIT -100 / 2221 CREDIT -13 | 同上：方向不变、金额取负 |
| PURCHASE_RETURN | `pur-return.action.spec.ts` | 2202 DEBIT 25 / 1401 CREDIT 25 | Dr 2202 应付-暂估=TOTAL_AMOUNT / Cr 1401 库存=TOTAL_AMOUNT（退货 qty5×price5=25，**读头 totalAmount 非行级聚合，spec 须显式置头 totalAmount**） |
| SALES_RETURN | `sal-return.action.spec.ts` | 1401 DEBIT 50 / 6401 CREDIT 50 | Dr 1401 库存=TOTAL_COST / Cr 6401 主营业务成本=TOTAL_COST（`computeTotalCost`=Σ 行 qty×price=5×10=50，行级聚合） |
| PURCHASE_INPUT | `p2p-chain.spec.ts` | 1401 DEBIT 50 / 2202 CREDIT 50 | Dr 1401 库存=TOTAL_COST / Cr 2202 应付-暂估=TOTAL_COST（InvAcctDocProvider.PURCHASE_INPUT，TOTAL_COST=ledger.totalCost=qty10×unitCost5=50，plan 2026-07-10-1800-1） |
| SALES_OUTPUT | `o2c-chain.spec.ts` | 6401 DEBIT 1200 / 1401 CREDIT 1200 | Dr 6401 成本=TOTAL_COST / Cr 1401 库存=TOTAL_COST（InvAcctDocProvider.SALES_OUTPUT，TOTAL_COST=qty10×avgCost120=1200，备货20@120 MOVING_AVERAGE，plan 2026-07-10-1800-1） |
| NCR_SCRAP | `quality-ncr-scrap-posting.action.spec.ts` | 6711 DEBIT 120 / 1401 CREDIT 120 | Dr 6711 营业外支出=SCRAP_AMOUNT / Cr 1401 库存=SCRAP_AMOUNT（NcrScrapAcctDocProvider.NCR_SCRAP，SCRAP_AMOUNT=qty1×seed avgCost120=120，MAT_1 种子余额行，plan 2026-07-10-1800-1） |
| 红字（NCR_SCRAP 冲销） | `quality-ncr-reverse-voucher-line.action.spec.ts` | 6711 DEBIT -120 / 1401 CREDIT -120 | `buildReversalDraft`：dcDirection 不变、金额取负（reverseNcr→NcrPostingExecutor.reverse→voucherBiz.reverse；原正向凭证 isReversed=true，markOriginalVoucherReversed O-8 统一行为，plan 2026-07-11-0730-2） |
| PRODUCTION_VARIANCE | `mfg-variance.spec.ts` | 1410 DEBIT 100 / 1411 CREDIT 100 / 1412 DEBIT 50 / 1413 CREDIT 50 / 1415 DEBIT 50 / 1414 CREDIT 50 | 按成本要素汇总净差异方向入账（ProductionVarianceAcctDocProvider）：MATERIAL unfavorable DEBIT Dr 1410/Cr 1411=100（MATERIAL_USAGE +100 + VOLUME 0）+ LABOR unfavorable DEBIT Dr 1412/Cr 1413=50（LABOR_EFFICIENCY -50 + LABOR_RATE +100）+ OVERHEAD favorable CREDIT Dr 1415/Cr 1414=50（OVERHEAD -50）；标准成本 FIRMED rollup（mat90/lab5/ovh5/unit100）vs 实际成本（mat1000/lab100/ovh0）差额派生（plan 2026-07-10-1800-2） |
| DEPRECIATION | `ast-depreciation.action.spec.ts` | 6602 DEBIT 333.3333 / 1602 CREDIT 333.3333（+ 红字同向取负 Dr 6602=-333.3333 / Cr 1602=-333.3333） | Dr 6602 折旧费用 / Cr 1602 累计折旧（DepreciationAcctDocProvider.DEPRECIATION）；金额=DEPRECIATION_AMOUNT=schedule.actualAmount=(originalValue 12000 − residualValue 0) / usefulLifeMonths 36，SCALE=4 HALF_UP=333.3333；正向经 executed.voucherId 直用，红冲经 `findVoucherIdByBillCode(assetCode#period,'REVERSAL')`（billHeadCode=assetCode#period，plan 2026-07-14-0742-1） |
| CAPITALIZATION | `ast-cip-capitalization.action.spec.ts` | 1601 DEBIT 800 / 1603 CREDIT 800（+ 红字同向取负 Dr 1601=-800 / Cr 1603=-800） | Dr 1601 固定资产 / Cr 1603 在建工程（CapitalizationAcctDocProvider.CAPITALIZATION，sourceType=CIP）；金额=ORIGINAL_VALUE=CostItem amountFunctional 汇总（PURCHASE 500 + OTHER 300=800）；billHeadCode=cap.code，经 `findVoucherIdByBillCode(capCode,'NORMAL'/'REVERSAL')`（plan 2026-07-14-0742-1） |
| ASSET_INVENTORY_ADJUSTMENT（SHORTAGE） | `ast-inventory-count.action.spec.ts` | 6711 DEBIT 500 / 1601 CREDIT 500（+ 红字同向取负 Dr 6711=-500 / Cr 1601=-500） | Dr 6711 营业外支出 / Cr 1601 固定资产（AssetInventoryAcctDocProvider，shortage>0 分支）；金额=SHORTAGE_AMOUNT=asset netBookValue（self-contained 测试资产 500）；billHeadCode=inventory.code（plan 2026-07-14-0742-1） |
| MAINTENANCE_EXPENSE | `ast-maintenance.action.spec.ts` | 6602 DEBIT 300 / 1002 CREDIT 300（+ 红字同向取负 Dr 6602=-300 / Cr 1002=-300） | Dr 6602 维修费用 / Cr 1002 银行存款（独立维修 linkedVisit=false，MaintenanceExpenseAcctDocProvider.MAINTENANCE_EXPENSE）；金额=TOTAL_COST=LABOR 200 + SPARE_PART 100=300；billHeadCode=maintenance.code（plan 2026-07-14-0742-1） |
| MAINTENANCE_CAPITALIZATION | `ast-maintenance.action.spec.ts` | 1601 DEBIT 250 / 1002 CREDIT 250（+ 红字同向取负 Dr 1601=-250 / Cr 1002=-250） | Dr 1601 固定资产 / Cr 1002 银行存款（MaintenanceCapitalizationAcctDocProvider.MAINTENANCE_CAPITALIZATION）；金额=CAPITALIZED_AMOUNT=totalCost=SUBCONTRACT 250；billHeadCode=maintenance.code（plan 2026-07-14-0742-1） |
| VALUE_ADJUSTMENT(IMPAIRMENT) | `ast-value-adjustment.action.spec.ts` | 6702 DEBIT 3000 / 1604 CREDIT 3000（+ 红字同向取负 Dr 6702=-3000 / Cr 1604=-3000） | Dr 6702 资产减值损失 / Cr 1604 固定资产减值准备（ValueAdjustmentAcctDocProvider.IMPAIRMENT）；金额=adjustmentAmount=3000；billHeadCode=adjustment.code；category 三 subject 字段 null → fallback 默认值 6702/1604；科目 6702/1604/4002 经种子补齐（1218-1）（plan 2026-07-14-1218-1） |
| VALUE_ADJUSTMENT(REVALUATION_UP) | `ast-value-adjustment.action.spec.ts` | 1601 DEBIT 5000 / 4002 CREDIT 5000 | Dr 1601 固定资产 / Cr 4002 资本公积（ValueAdjustmentAcctDocProvider.REVALUATION_UP，CAPITAL_RESERVE 恒为 4002 硬编码不经 category 解析）；金额=adjustmentAmount=5000；billHeadCode=adjustment.code（plan 2026-07-14-1218-1） |
| VALUE_ADJUSTMENT(REVALUATION_DOWN) | `ast-value-adjustment.action.spec.ts` | 6702 DEBIT 2000 / 1601 CREDIT 2000 | Dr 6702 资产减值损失 / Cr 1601 固定资产（ValueAdjustmentAcctDocProvider.REVALUATION_DOWN）；金额=adjustmentAmount=2000；billHeadCode=adjustment.code（plan 2026-07-14-1218-1） |
| FREIGHT | `log-delivered-freight-posting.action.spec.ts` | 6601 DEBIT 100 / 1002 CREDIT 100 | Dr 6601 销售费用-运费 / Cr 1002 银行存款（LogisticsFreightProvider.FREIGHT，SALES_DELIVERY + freightTerms=PREPAID 默认）；金额=FREIGHT_AMOUNT=shipment.freightAmount=100；billHeadCode=shipment.code，经 `findVoucherIdByBillCode(shipment.code, 'NORMAL')` 反查（plan 2026-07-14-0941-2） |
| EXPENSE_CLAIM (OWN_ACCOUNT) | `fin-expense-claim.action.spec.ts` | 6602 DEBIT 100 / 2221 DEBIT 13 / 2241 CREDIT 113（+ 红字同向取负 Dr 6602=-100/Dr 2221=-13/Cr 2241=-113） | Dr 6602 管理费用(amountWithoutTax) / Dr 2221 进项税(taxAmount) / Cr 2241 应付-员工(amountWithTax)（ExpenseClaimAcctDocProvider.EXPENSE_CLAIM，paymentMode=OWN_ACCOUNT）；科目 6602 硬编码非读行 subjectId；billHeadCode=claim.code；科目 2241 经种子补齐（1218-2）（plan 2026-07-14-1218-2） |
| EXPENSE_CLAIM (COMPANY_ACCOUNT) | `fin-expense-claim.action.spec.ts` | 6602 DEBIT 100 / 2221 DEBIT 13 / 1002 CREDIT 113 | Dr 6602 管理费用 / Dr 2221 进项税 / Cr 1002 银行存款（paymentMode=COMPANY_ACCOUNT 公司直付不挂应付-员工辅助账）；billHeadCode=claim.code（plan 2026-07-14-1218-2） |
| EMPLOYEE_ADVANCE | `fin-employee-advance.action.spec.ts` | 1221 DEBIT 500 / 1002 CREDIT 500（+ 红字同向取负 Dr 1221=-500/Cr 1002=-500） | Dr 1221 其他应收款-员工预支(amountFunctional) / Cr 1002 银行存款(amountFunctional)（EmployeeAdvanceAcctDocProvider.EMPLOYEE_ADVANCE）；billHeadCode=advance.code；科目 1221 经种子补齐（1218-2）（plan 2026-07-14-1218-2） |
| EMPLOYEE_ADVANCE_SETTLE | `fin-employee-advance.action.spec.ts` | 2241 DEBIT 226 / 1221 CREDIT 226 | Dr 2241 应付-员工(net) / Cr 1221 应收-员工预支(net)（EmployeeAdvanceAcctDocProvider.EMPLOYEE_ADVANCE_SETTLE，报销抵扣路径 SETTLE_TYPE=OFFSET/缺省）；net=min(payableOpen=226, receivableOpen=500)=226；billHeadCode=claim.code（非 advance.code，AdvanceOffsetOrchestrator 经 EmployeeAdvancePostingDispatcher.postSettle 置）；同 billCode 多业务类型区分经 ErpFinVoucherBillR.billType（plan 2026-07-14-1218-2） |
| EMPLOYEE_ADVANCE_SETTLE (CASH) | `fin-employee-advance-cash-repay.action.spec.ts` | 1002 DEBIT {amount} / 1221 CREDIT {amount} | Dr 1002 银行存款(amount) / Cr 1221 应收-员工预支(amount)（EmployeeAdvanceAcctDocProvider.EMPLOYEE_ADVANCE_SETTLE，现金还款路径 SETTLE_TYPE=CASH）；billHeadCode=`EA-CASH-REPAY-{advanceCode}-{millis}`（含时间戳避免多次还款碰撞，经 ErpFinVoucherBillR.businessType + JS startsWith 前缀匹配反查）；docStatus 保持不变（派生投影表达已结清，不引入字典 SETTLED）；Provider SETTLE 分支按 billData.SETTLE_TYPE 分派（缺省/null → OFFSET 既有报销抵扣路径零回归）（plan 2026-07-18-0718-2） |
| PROJECT_SETTLEMENT（FINAL/INTERIM，profit>0） | `projects-pnl-settlement.action.spec.ts` | 5101 DEBIT 6000 / 4103 DEBIT 4000 / 6001 CREDIT 10000（+ 红字同向取负 Dr 5101=-6000 / Dr 4103=-4000 / Cr 6001=-10000） | Dr 5101 项目成本=finalCost / 条件性 Dr 4103 本年利润=profitLoss（profitLoss=finalRevenue−finalCost，signum>0→DEBIT \|profitLoss\|，signum<0→CREDIT \|profitLoss\|，signum=0 不发） / Cr 6001 项目收入=finalRevenue（ProjectSettlementAcctDocProvider FINAL/INTERIM 分支，CLOSE 走 Dr 1601/Cr 1603 转固另一分支）；金额确定性工程化：setup Billing amountFunctional=10000 + CostCollectionLine MATERIAL amount=6000 → refreshPnl revenueAmount=10000/totalCost=6000 → profitLoss=4000>0 使 4103 确定性出现；billHeadCode=settlement.code，经 `findVoucherIdByBillCode(settlement.code,'NORMAL'/'REVERSAL')`（plan 2026-07-17-1005-2） |
| NOTES_RECEIVABLE_RECEIVED | `fin-notes-receivable.action.spec.ts` | 1121 DEBIT 1000 / 1122 CREDIT 1000（+ writeOff 红字同向取负 Dr 1121=-1000 / Cr 1122=-1000） | Dr 1121 应收票据=FACE_AMOUNT / Cr 1122 应收账款=FACE_AMOUNT（NotesReceivableAcctDocProvider.NOTES_RECEIVABLE_RECEIVED，cr 行设 partnerId 生成 RECEIVABLE 辅助账抵 AR）；金额=note.amountFunctional=1000；billHeadCode=note.code，经 `findVoucherIdByBillCode(note.code,'NORMAL'/'REVERSAL')`（plan 2026-07-17-1430-1） |
| NOTES_RECEIVABLE_DISCOUNTED（单币种三件套） | `fin-notes-receivable.action.spec.ts` | 1002 DEBIT 990 / 6603 DEBIT 10 / 1121 CREDIT 1000 | Dr 1002 银行存款=netAmount / Dr 6603 财务费用-利息支出=discountInterest / Cr 1121 应收票据=faceAmount（NotesReceivableAcctDocProvider.NOTES_RECEIVABLE_DISCOUNTED 科目分解五件套，单币种 6051 fx 行 signum=0 抑制不发）；discountInterest=face×rate×remainingDays/360=1000×0.12×30/360=10.00（HALF_UP scale 2，须 dueDate>discountDate 使 remainingDays>0）/ netAmount=990；billHeadCode=note.code（plan 2026-07-17-1430-1） |
| NOTES_RECEIVABLE_ENDORSED | `fin-notes-receivable.action.spec.ts` | 2202 DEBIT 1000 / 1121 CREDIT 1000 | Dr 2202 应付账款=FACE_AMOUNT / Cr 1121 应收票据=FACE_AMOUNT（NotesReceivableAcctDocProvider.NOTES_RECEIVABLE_ENDORSED，dr 行设 partnerId 生成 PAYABLE 辅助账抵 AP）；billHeadCode=note.code（plan 2026-07-17-1430-1） |
| NOTES_RECEIVABLE_COLLECTION | `fin-notes-receivable.action.spec.ts` | 1002 DEBIT 1000 / 1121 CREDIT 1000 | Dr 1002 银行存款=FACE_AMOUNT / Cr 1121 应收票据=FACE_AMOUNT（NotesReceivableAcctDocProvider.NOTES_RECEIVABLE_COLLECTION，到期托收承兑）；billHeadCode=note.code（plan 2026-07-17-1430-1） |
| NOTES_PAYABLE_ISSUED | `fin-notes-payable.action.spec.ts` | 2202 DEBIT 1000 / 2203 CREDIT 1000（+ writeOff 红字同向取负 Dr 2202=-1000 / Cr 2203=-1000） | Dr 2202 应付账款=FACE_AMOUNT / Cr 2203 应付票据=FACE_AMOUNT（NotesPayableAcctDocProvider.NOTES_PAYABLE_ISSUED，开出应付票据；不生成 AR/AP 辅助账）；billHeadCode=note.code，经 `findVoucherIdByBillCode(note.code,'NORMAL'/'REVERSAL')`（plan 2026-07-17-1430-1） |
| NOTES_PAYABLE_HONORED | `fin-notes-payable.action.spec.ts` | 2203 DEBIT 1000 / 1002 CREDIT 1000 | Dr 2203 应付票据=FACE_AMOUNT / Cr 1002 银行存款=FACE_AMOUNT（NotesPayableAcctDocProvider.NOTES_PAYABLE_HONORED，到期兑付）；billHeadCode=note.code（plan 2026-07-17-1430-1） |
| MAINTENANCE_EXPENSE（跨域防双重扣减，linkedVisit=true） | `mnt-ast-linked-visit-anti-double-deduct.action.spec.ts` | 6602 DEBIT 50 / 2502 CREDIT 50 | Dr 6602 维修费用 / **Cr 2502 维修中转清算**（备件成本已由 mnt MAINTENANCE_ISSUE 贷记 1403 一次，assets 不再贷存货改贷中转清算防双重扣减）；linkedVisit=maintenanceVisitId!=null=true；金额=totalCost=SPARE_PART cost=50（同 mnt 备件出库 amount）；billHeadCode=maintenance.code（plan 2026-07-17-2256-2） |
| MAINTENANCE_LABOR | `mnt-labor-posting.action.spec.ts` | 6602 DEBIT 80 / 2211 CREDIT 80（部分时长 30min → 6602 DEBIT 40 / 2211 CREDIT 40） | Dr 6602 折旧费用（种子 subjectName 实测为「折旧费用」，断言按 subjectCode 不受影响）/ Cr 2211 应付职工薪酬（MaintenanceLaborAcctDocProvider.MAINTENANCE_LABOR，权责发生制员工薪酬计提）；金额=laborCost=totalMinutes × hourlyRate / 60（HALF_UP scale=4）；rate=config `erp-mnt.default-labor-hourly-rate=80` 元/小时；billHeadCode=visit.code+"-ML"（MaintenanceLaborPostingDispatcher.java:84），经 `findVoucherIdByBillCode(visit.code+"-ML",'NORMAL')` 反查；totalMinutes=null/≤0 守卫不产凭证；config-gated 默认 false 关闭向后兼容（plan 2026-07-18-0949-1） |
| CREDIT_FACILITY_INTEREST | `fin-credit-facility-interest.action.spec.ts` | 6603 DEBIT 1.2917 / 1002 CREDIT 1.2917 | Dr 6603 财务费用-利息支出 / Cr 1002 银行存款（CreditFacilityInterestAcctDocProvider）；amount=usedAmount × rate × days / 360 = 300 × 0.05 × 31 / 360 = 1.2917（HALF_UP scale=4，360 天基准对齐贴现范式）；rate 来源 config `erp-fin.credit-facility-default-interest-rate=0.05`；billHeadCode=`CFI-INT-{facilityId}-{fromDate}_{toDate}` 区间级幂等键（IErpFinVoucherBiz.post 内置 alreadyPosted 守护，同 facility + 同区间二次调用返回 null 无第二张凭证）；partnerId=null（plan 2026-07-18-0718-1） |

**关键裁决：**

- **红字凭证同向取负（非借贷互换）**：`ErpFinPostingProcessor.buildReversalDraft` 保持原凭证行 dcDirection 不变、仅金额取负（如原 2202 CREDIT credit=56.5 → 红字 2202 CREDIT credit=-56.5，**非** 翻转为 DEBIT）。断言据此写同向取负期望。同时断言原正常凭证行金额不变（正数），验证红冲仅新增红字凭证、不修改原凭证行。
- **PURCHASE_RETURN 读头 totalAmount**：`PurReturnPostingDispatcher` 置 `billData.TOTAL_AMOUNT = returnOrder.getTotalAmount()`（头字段，非行级聚合，头 `totalAmount` 列 `defaultValue=0` 无自动 rollup）。故 `pur-return` spec 创建退货头时须显式置 `totalAmount=qty×price`，凭证行方为有意义的非零金额（对齐 Java 集成测试 `TestErpPurReturnPosting` 显式 setTotalAmount 范式）。SALES_RETURN 则读行级 `computeTotalCost`（Σ qty×unitPrice），无需置头金额。
- **套件计数**：0704-1 新断言全部内联至既有 spec（不新增 spec 文件/测试用例）。1800-1 新增 1 个 spec（`quality-ncr-scrap-posting.action.spec.ts`），套件总数 167（166→167，N=1）。0742-1 assets 凭证行断言全部内联至既有 4 个 ast spec（不新增 spec 文件/测试用例，套件总数不变）。1005-2 新增 3 个 spec（`ct-rebate-settlement` / `drp-release-approved` / `projects-pnl-settlement`，各 2 用例=6 测试），并校正预存漂移（业务动作表头声称「17 域 55 spec」，实际 58；本计划后 61），business-actions 全套件 153 passed（18.8m，0 新增失败）。1430-1 新增 2 个 spec（`fin-notes-receivable` 7 用例 / `fin-notes-payable` 5 用例=12 测试，资金/票据 DIRECT 生命周期 + 6 业财过账凭证行精确数值断言 + 种子 COA 补齐 1121/2203/6603），套件总数 61→63，business-actions 抽样回归 0 新增失败。2256-1 新增 2 个 spec（`fin-cash-forecast` 3 用例 / `fin-credit-facility` 3 用例=6 测试，现金预测聚合/幂等 + 授信额度 reserve/release/不足守卫），套件总数 64→66，business-actions 抽样回归 0 新增失败。2256-2 新增 1 个 spec（`mnt-ast-linked-visit-anti-double-deduct` 2 用例，跨域防双重扣减正路径 linkedVisit=true Cr 2502 + 对照 linkedVisit=false Cr 1002 + 种子 COA 补 2502 维修中转清算），套件总数 66→67，maintenance/assets 既有 spec 抽样回归 10 passed 0 新增失败。0100-1 新增 3 个 spec（`hr-shift-assignment` 5 用例 / `hr-shift-rotation` 2 用例 / `hr-shift-swap` 4 用例=11 测试，hr 排班 4 实体 DIRECT 全栈可达：单/批量/周期复制分配 + calcAttendance 迟到/准时双落点 + generateRotation 错峰引擎 + regenerate=true CANCEL/重建幂等 + swap submit/approve/reject/cancel 状态机 + approve 双向班次交换 swapRequestId/replacedByAssignmentId 双向回链 + 重复 submit 不抛守卫 + 非法状态迁移守卫），解除 0215-3 Deferred「排班批量生成 + 调换审批深度编排 E2E」，套件总数 67→70，business-actions 全套件 184 passed（22.3m，0 新增失败）。0100-2 新增 2 个 spec（`hr-assessment-dev-plan` 4 用例 / `hr-transfer` 4 用例=8 测试，hr 人才发展 4 实体 + 调动 8 DIRECT 全栈可达：completeAssessment 自动 gap refresh 链 + gapSeverity 多档 MINOR/MODERATE/CRITICAL + refreshGapAnalysis/refreshGapAnalysisWithLevels 双入口 + generateDevelopmentPlan actionable 过滤/null 返回 + updatePlanItemStatus NOT_STARTED→IN_PROGRESS→ACHIEVED 状态机 + completePlan 终态守卫 + transferEmployee handleContract=YES/NO/AUTO 三分支合同联动 + RESIGNED/position-not-in-target-dept 双守卫；**执行期修复 1 处 latent defect**（refreshGapAnalysisWithLevels Map<Long,Integer> GraphQL 反序列化键类型不匹配，新增 normalizeLevelMap helper + ERR_GAP_INVALID_LEVEL_MAP ErrorCode；**执行期发现 1 处 latent defect 转 successor**（buildSuccessorCode 超 code precision=50 同 1430-1 类，spec 紧凑短码规避，生产代码修复归 successor）），解除 0215-3 Deferred「胜任力评估 / 差距分析 / 发展计划 / 员工调动 E2E」+ Non-Goal「员工调动跨域编排」，套件总数 70→72。0347-2 新增 1 个 spec（`hr-leave-shift-linkage` 2 用例，hr 休假→排班跨实体钩子联动 2 DIRECT 路径：approve 触发 `onLeaveApproved` 标记区间内 ShiftAssignment 4 字段翻转 isAbsent=true/absenceReason=LEAVE/leaveRequestId=leave.id/status=ABSENT + 区间外对照行字段不变 + cancel 触发 `onLeaveCancelled` 还原由该 leave 标记的行 4 字段回退；区间范围准确性裁决：`findAssignmentsByEmployeeRange` 仅按 employeeId + dateBetween 过滤无 status 过滤，setup 显式置全 SCHEDULED 避免噪声），解除 0100-1 Deferred「`ErpHrLeaveRequest.approve` → `ErpHrShiftAssignment` isAbsent 标记联动 E2E」，套件总数 72→73，business-actions 全套件 194 passed（23.5m，0 新增失败）。0718-1 新增 1 个 spec（`fin-credit-facility-interest` 3 用例，finance 授信利息计提 DIRECT `@BizMutation` accrueInterest 全栈可达：正路径凭证行精确数值 Dr 6603=1.2917 / Cr 1002=1.2917 经 assertVoucherLines 独立反查 + findVoucherIdByBillCode 区间级幂等键反查 + facility 三值不变 + usedAmount=0 空操作返回 null + fromDate>toDate 守卫「日期」语义 token；webServer JVM arg 追加 `-Derp-fin.credit-facility-default-interest-rate=0.05` config-gated；后端 `ErpFinCreditFacilityBizModel.accrueInterest` + `CreditFacilityInterestVoucherBuilder` + `CreditFacilityInterestAcctDocProvider` + bean 注册 + config 常量 + 2 ErrorCode + JUnit 6 用例 + 浏览器层 E2E 1 spec 3 用例；解除 2256-1 Deferred「授信额度利息计提 CREDIT_FACILITY_INTEREST」），套件总数 73→74，finance 抽样回归 25 passed 0 新增失败。0718-2 新增 1 个 spec（`fin-employee-advance-cash-repay` 3 用例，finance 员工借款现金还款 DIRECT `@BizMutation` cashRepay 全栈可达：全额还款 settledAmount=500/outstandingAmount=0 + 派生投影 docStatus 不变 + EMPLOYEE_ADVANCE_SETTLE CASH 凭证行 Dr 1002/Cr 1221 精确数值经 assertVoucherLines 独立反查 + 部分还款 settledAmount=200/outstandingAmount=300 + 超额守卫「超过」/「未还余额」语义 token + 字段不变；后端 `ErpFinEmployeeAdvanceBizModel.cashRepay` + `EmployeeAdvancePostingDispatcher.postCashRepay` + `EmployeeAdvanceAcctDocProvider` SETTLE_TYPE=CASH 分派 + 3 ErrorCode + 常量 + JUnit 6 用例 + 浏览器层 E2E 1 spec 3 用例；解除 1218-2 Deferred「员工借款现金还款 EMPLOYEE_ADVANCE_SETTLE 现金还款路径」），套件总数 74→75，finance 抽样回归（fin-employee-advance + fin-expense-claim + fin-credit-facility-interest 11 passed）0 新增失败。1745-1 新增 2 个 spec（`mnt-visit-cancel-reversal` 1 用例 / `mnt-spare-part-usage-reversal` 2 用例=3 测试，maintenance 域过账红冲闭环 DIRECT `@BizMutation` 全栈可达：visit cancel 触发 MAINTENANCE_LABOR 凭证红冲（原 isReversed=true + 红字凭证行同向取负 Dr 6602=-80/Cr 2211=-80，状态机纠错经 __update 改回 IN_PROGRESS 模拟「错误完工」回退）+ reverseConfirm 触发 MAINTENANCE_ISSUE 凭证红冲 + 反向 OUTGOING 移动单（REVERSAL relatedBillType）+ posted=false/docStatus=CANCELLED + 未过账守卫 ERR_SPARE_PART_USAGE_NOT_POSTED；后端 MntPostingExecutor.reverse + 两 dispatcher reverseLabor/reverseIssue + ErpMntVisitBizModel.doCancel 内嵌触发 + ErpMntSparePartUsageBizModel.reverseConfirm 新方法 + IErpInvStockMoveBiz.reverse 跨域调用 + JUnit 5 用例 + 浏览器层 E2E 2 spec 3 用例；解除 0949-1/1100-6 两处 Deferred「cancel 触发已生成 ML/MI 凭证红冲」），套件总数 75→77，maintenance 既有 spec 抽样回归（mnt-labor-posting + mnt-spare-part-posting + mnt-request + mnt-request-visit-orchestration + mnt-ast-linked-visit-anti-double-deduct 9 passed）0 新增失败。1745-2 新增 2 个 spec（`inv-landed-cost-reversal` 2 用例 / `mfg-material-issue-reversal` 2 用例=4 测试，inventory 到岸成本 + manufacturing 领料过账红冲闭环 DIRECT `@BizMutation` 全栈可达：reverseApprove 触发 LANDED_COST 凭证红冲（Dr 1401=-50/Cr 2202=-50）+ CostAdjustmentService.reverseCostAdjust 反向应用 LANDED_COST_SUPPLEMENT 成本层（avgCost 15→10）+ reverseConfirm 触发 MANUFACTURING_ISSUE 凭证红冲（Dr 1411=-50/Cr 1401=-50）+ 反向 OUTGOING 移动单（REVERSAL relatedBillType）+ posted=false/docStatus=CANCELLED + 未过账守卫 ERR_LANDED_COST_NOT_POSTED/ERR_MATERIAL_ISSUE_NOT_POSTED；后端 LandedCostPostingDispatcher.reverse + ManufacturingIssuePostingDispatcher.reverse + ErpInvLandedCostProcessor.reverseApprove 新方法 + ErpMfgMaterialIssueBizModel.reverseConfirm 新方法 + CostAdjustmentService.reverseCostAdjust 反向应用成本层 + IErpInvStockMoveBiz.reverse 跨域调用 + JUnit 4 用例 + 浏览器层 E2E 2 spec 4 用例），套件总数 77→79。1745-3 新增 2 个 spec（`fin-bad-debt-reverse-approve` 2 用例 / `fin-employee-advance-cash-repay-reverse` 2 用例=4 测试，finance 域过账红冲闭环 DIRECT `@BizMutation` 全栈可达：坏账 reverseApprove 触发 BAD_DEBT_WRITE_OFF/RECOVERY 凭证红冲（Dr 1231=-100/Cr 1122=-100 writeOff 路径 / Dr 1122=-100/Cr 1231=-100 recovery 路径）+ ArApItem 状态对称回退（WRITTEN_OFF↔OPEN）+ approvalStatus APPROVED→REJECTED + reverseCashRepay 触发 EMPLOYEE_ADVANCE_SETTLE CASH 凭证红冲（Dr 1002=-500/Cr 1221=-500）+ advance 字段回退（settled-=amount/outstanding+=amount）+ 未过账守卫 ERR_BAD_DEBT_NOT_APPROVED_OR_NOT_POSTED + 无凭证守卫 ERR_EMPLOYEE_ADVANCE_CASH_REPAY_VOUCHER_NOT_FOUND；后端 ErpFinBadDebtProcessor.reverseApprove 新方法 + FinPostingExecutor.reverse 接线 + ErpFinEmployeeAdvanceBizModel.reverseCashRepay 新方法 + ErpFinVoucherBillR 反查定位单笔 cashRepay NORMAL 凭证 + EmployeeAdvancePostingDispatcher.reverseSettle 既有 API 接线 + 2 ErrorCode + JUnit 6 用例 + 浏览器层 E2E 2 spec 4 用例；兑现 owner doc `expense-claim.md §红冲联动` cashRepay 路径承诺（解除 owner-doc 漂移，规则 13 不可降级）），套件总数 79→81。
- **NCR SCRAP avgCost 来源裁决（plan 2026-07-10-1800-1）**：`NcrPostingDispatcher.resolveStockBalance` 按 `materialId`（**非** warehouseId）查 `ErpInvStockBalance`(limit 1)。MAT_1(materialId=1) 种子有唯一余额行（warehouseId=1, avgCost=120, MOVING_AVERAGE），故 `resolveStockBalance(1)` 确定性返回 avgCost=120。SCRAP posting 不扣减物理库存（`NcrPostingDispatcher` 注释：物理出库属 successor），种子余额不受影响。**无需前置备货**——备货会为 materialId=1 新增第二行致 limit 1 返回行不确定（非确定性）。SCRAP_AMOUNT = NCR.quantity(1) × 120 = 120。
- **NCR SCRAP acctSchemaId 配置（plan 2026-07-10-1800-1）**：`NcrPostingDispatcher.resolveAcctSchemaId` 读 config `erp-qua.ncr-default-acct-schema`（默认空→null），致凭证行 `acctSchemaId` 非空约束失败。webServer JVM 属性增 `-Derp-qua.ncr-default-acct-schema=1`（种子 ACCT-FIN-01），解除阻塞。
- **plan 2026-07-18-2251-1 新增 1 spec**：`mfg-variance-recompute-reversal.action.spec.ts`（1 用例）——manufacturing 生产差异重算孤儿凭证修复（`ProductionVarianceDispatcher.reverseIfExists` 红冲闭环）：复用 1800-2 既有 `runMfgChain` + variance setup（测试专用成品物料 + 运行时 FIRMED rollup）→ 首次完工产 NORMAL PRODUCTION_VARIANCE 凭证 → 再次 `ErpMfgCostVariance__calculateVariances` 触发 reverseIfExists 红冲原凭证（isReversed=true）+ 生成 REVERSAL 红字凭证（同向取负）+ 派发新 NORMAL 凭证 + 数据行全 posted=true + ErpFinVoucherBillR 反查仅 1 条 isReversed=false NORMAL 凭证（无孤儿）。后端 JUnit 同型覆盖在 `TestErpMfgVarianceRecomputeReversal`（4 用例：Call site A 单凭证反向 + 关键一致性断言、Call site B 完工自动重算反向、红冲失败容错、首次容错）。表头声称「65 业务动作 spec」实际 82（含本计划新增）。
- **plan 2026-07-18-2251-2 新增 1 spec**：`fin-bad-debt-provision-reverse.action.spec.ts`（2 用例）——finance 坏账准备期末计提反向红冲闭环（`ErpFinBadDebt.reverseBadDebtProvision(periodId)` 红冲闭环）：复用 0413-2 既有坏账 setup（建 partner+OPEN AR 对+period）→ `runBadDebtProvision` mutation（产 1 张 BDR NORMAL 凭证，billCode=`BAD-DEBT-RESERVE-{period.code}`）→ `reverseBadDebtProvision` mutation 断言 GraphQL response `reversedReserveCount=1` + `reversedReserveAmount=requiredProvision-allowanceBalance` + 原 NORMAL 凭证 isReversed=true + **红字凭证行同向取负**（Dr 6701=-X / Cr 1231=-X，经 `findVoucherIdByBillCode(billCode,'REVERSAL')` 反查）+ (b) 守卫：无 BDR/BDL 凭证 → GraphQL errors 含「未找到」/「坏账准备」语义 token。后端 JUnit 同型覆盖在 `TestErpFinBadDebtProvisionReversal`（5 用例：单凭证反向 + 多凭证累积反向 + 未找到守卫 + CLOSED_FINAL 守卫 + 混合 BDR/BDL 反向）。表头声称「65 业务动作 spec」实际 83（含本计划新增）。

### 反向冲销层（业财闭环方向二：财务侧 DIRECT 红字冲销 + 域监听者回退）

在正向编排链之上，2004-2 叠加了业财闭环方向二的浏览器层 E2E：经 `ErpFinVoucher__reverse(billHeadCode, businessType)` 财务侧 DIRECT 红字冲销（M5.2 后端 1452-2，`reverseProcess` + `VoucherReversedEvent` + 域监听者）的浏览器层全栈可达性 + 红字凭证生成 + 原凭证补标 + 域单据回退验证。**机制区分**：本层覆盖财务侧 DIRECT 红冲（财务员直接红冲已过账凭证，无审批/xwf 依赖），区别于域审批轴 `ErpXxx__reverseApprove`（需 APPROVED 前置，归 approval-pattern successor，本层不解除）。

| 链路 | spec | 验证内容 |
| --- | --- | --- |
| P2P 反向（AP_INVOICE 红冲 + 采购发票回退） | `p2p-reverse.spec.ts` | `runP2pReverse` 正向链产 posted AP_INVOICE 凭证 → `ErpFinVoucher__reverse(AP_INVOICE)` → 原正常凭证 NORMAL+isReversed=true + 红字凭证 REVERSAL+isReversed=true+reversalOfVoucherId 回链 + `ErpPurInvoice` posted=false + approveStatus APPROVED→REJECTED（`PurReversalListener.rollbackInvoice`） |
| O2C 反向（AR_INVOICE 红冲 + 销售发票回退） | `o2c-reverse.spec.ts` | `runO2cReverse` 正向链产 posted AR_INVOICE 凭证（含 O2C 备货前置）→ `ErpFinVoucher__reverse(AR_INVOICE)` → 原正常凭证 isReversed=true + 红字凭证生成 + `ErpSalInvoice` posted=false + approveStatus APPROVED→REJECTED（`SalReversalListener.rollbackInvoice`） |

#### 编排链范式复用与反向裁决（`orchestration/_helper.ts`）

- **反向驱动**：`runP2pReverse` / `runO2cReverse` 复用正向 `runP2pChain` / `runO2cChain`（产 posted AP/AR Invoice 凭证）后，附加一步 `ErpFinVoucher__reverse` mutation，返回 reversalVoucherId + 经 voucher_bill_r 反查 originalVoucherId（NORMAL 凭证）。
- **枚举入参序列化裁决（Explore 探针）**：`reverse(billHeadCode, businessType: ErpFinBusinessType)` 的 Java 枚举入参经 Nop GraphQL **暴露为 String scalar**——原型实证：unquoted `AP_INVOICE`（GraphQL enum scalar 写法）被解析器拒绝（`非法的字符` / `nop.err.graphql.parse.unexpected-char`）；quoted `"AP_INVOICE"`（String scalar 写法）接受并返回红字凭证 id。故 helper 直接构造 `mutation{ ErpFinVoucher__reverse(billHeadCode:"...",businessType:"AP_INVOICE") }` 原始查询（`reverse` 返回标量 Long，无选择集，与既有 `callMutation` 总包选择集不兼容）。枚举名经 `.name()` 在事件/回链中以字符串形式存储（`PurReversalListener`/`SalReversalListener` 按 `switch(businessType)` 字符串分派）。
- **清理范围裁决（Explore 探针）**：`reverseProcess` 以**同一 billHeadCode** 写红字凭证的 voucher_bill_r（与原凭证共用 billCode），故既有 `cleanupVoucherByBillCode` 已覆盖原+红字凭证（voucher_line + voucher + voucher_bill_r）清理；AR-AP 红冲为既有行 `status` OPEN→CANCELLED + openAmount 归零（`cancelOnReverse`，非新增行），既有 `cleanupArApByCode`（按 sourceBillCode deleteByFilter）已覆盖——**无需扩展清理原语**（`cleanupP2p`/`cleanupO2c` 已包含这两步，反向层直接复用为 `cleanupP2pReverse`/`cleanupO2cReverse`）。
- **与正向编排层的层间关系**：反向层是正向层的扩展（正向链产前置 posted 凭证 + 反向 mutation + 反向回退断言），复用正向 helper 的全部链式驱动/过账产物断言/清理原语。反向层断言聚焦回退目标态（原凭证 isReversed + 域单据 posted/approveStatus 翻转），由 `VoucherReversedEvent` 同事务派发给各域监听者实现（`posting.md §冲销机制方向二 §实现策略 裁决3` SYNC 默认）。
- **监听者失败隔离残留风险**：`ErpFinReversalListenerRegistry.dispatch` try/catch 隔离各域监听者失败（失败不回滚已过账红字凭证、不阻断其他域监听者，落入 finance 5.1 异常工作台）。spec 断言以 `__findPage`/`__get` 权威查库为准；若监听者异常，域回退可能未发生——本层 happy-path 全绿（监听者无异常），异常路径归 successor。

## useWorkflow 审批轴浏览器层（xwf）— 可行性裁决：不可行

> 本段为 plan `2026-07-09-2330-1` 权威裁决记录，收敛此前散乱于 1249-1 Deferred「Payment/Receipt xwf」+ 2004-2 Deferred「Payment-Receipt xwf 反向 useWorkflow 子集」的同一未解阻塞。

### 范围

4 个 `useWorkflow="true"` 实体（经 ORM 核实），各有 `.xwf` 工作流定义：

- purchase **Payment**（`module-purchase/model/app-erp-purchase.orm.xml:928`）→ `payment-approval/v1.xwf`
- sales **Receipt**（`module-sales/model/app-erp-sales.orm.xml:720`）→ `receipt-approval/v1.xwf`
- assets **Disposal**（`module-assets/model/app-erp-assets.orm.xml:580`）→ `asset-disposal-approval/v1.xwf`
- hr **Salary**（`module-hr/model/app-erp-hr.orm.xml:668`）→ `salary-approval/v1.xwf`

> 区别于 useApproval（approvalStatus 轴，DIRECT `@BizMutation`）：PO/Receive/Invoice、SO/Delivery/Invoice 的 `submitForApproval`→`approve` 浏览器层已覆盖（1249-1，DIRECT 入口，不经 wf 步骤参与者校验）。manufacturing WorkOrder / purchase-sales Return / quality Recall 经核实非 `useWorkflow="true"`，属 useApproval DIRECT 轴（2004-1 曾误标 WorkOrder 为 xwf）。

### sysUser 兜底机制（user:$0 阻塞根因）

useWorkflow 实体 `submitForApproval` 启动 wf 时，`submit` 起始步骤无 `<assignment>`（`.xwf` 设计：由 `ApprovalFlowHelper.start` 自动 complete）。引擎 `WorkflowEngineImpl.newSteps:274-283` fallback：start step 无 assignment + 无 selected actors → `getManagerActor()`（`.xwf` 无 `<auth>` → null）→ **`getSysUser()`**（`WfRuntime.getSysUser:229` → `resolveUser(IWfActor.SYS_USER_ID="0")`）。故 submit step actor/owner=sysUser(id=0)。

`ApprovalFlowHelper.start:20` 随后调 `getLatestStartStep().invokeAction(COMPLETE, ctx)` → `WorkflowStepImpl.invokeAction:228` 先 `allowCallByUser` → `WorkflowEngineImpl.allowCallByUser:1053`：owner=sysUser(0)，`owner.getActorId()("0") != ctx.getUserId()(nop 浏览器用户 uuid)` → 落 `canBeDelegatedBy` → 委托无配置 → 拒绝 submit。

### 三条候选路径评估（均阻断）

1. **wf 委托 API（`NopAuthUserSubstitution` nop→0）——机制存在但实测阻断**：`WfActorAssignSupport.canBeDelegatedBy:71` → `DaoUserDelegateService`（已注册）读 `NopAuthUserSubstitution`。临时探针 spec fresh-DB 实测：建 `NopAuthUserSubstitution(substitutedUserId="0")` 前须物化 sysUser(0) 为真实 `nop_auth_user` 行（FK 校验），但 `NopAuthUser.userId` 列 `tagSet="seq"`（`nop-auth/model/nop-auth.orm.xml:38-39`）→ `__save` 时 seq 生成器**覆盖显式 userId="0"**，实测创建的 SYS 用户 userId=UUID（非 "0"）→ 委托 FK「类型为[用户]，id为[0]的记录不存在」失败。**sysUser(0) 是引擎虚拟用户，浏览器层 `__save` 无法物化为真实可引用行**。
2. **nop 用户身份映射/伪装（浏览器层等效 `setUserId`）——不存在**：nop-entropy 全量 wf/auth 模块无浏览器层（GraphQL `/graphql`）用户身份注入/伪装 API。`-Dnop.auth.service-public=true` 仅旁路 auth 校验，不改 `ctx.getUserId()`。后端 `ContextProvider.getOrCreateContext().setUserId("0")` 是线程局部出口，浏览器层无此注入点。
3. **`.xwf` submit step `<assignment>` 放宽——属生产行为变更**：改 submit step 增 `<actor actorType="all"/>` 可使 `allowCallByUser` 经 `containsUser`（`IWfActor:75` ACTOR_TYPE_ALL→true）放行，但此为**生产审批契约变更**（削弱 sysUser 自动 complete 语义），超出「浏览器层可行性」范围。

### 后续步骤驱动可行性（已确认可行，但依赖 submit 突破）

wf 审批步骤（finance-approval/cc-finance 等 `actorType="all"` 步骤）经 `WorkflowService__invokeAction` GraphQL mutation（`@BizModel("WorkflowService")` + `@BizMutation("invokeAction")`，`WfActionRequestBean` 字段 `wfId`/`stepId`/`actionName` 作直接参数；step action 内含 `doTransition` 自动迁移）浏览器层可达（`allowCallByUser` 对 actorType=all 步骤的任意用户返回 true）。**但该路径依赖先突破 submit 步骤 user:$0 阻塞**——submit 不突破则 wf 不启动，后续步骤驱动无意义。

### successor 触发条件

useWorkflow 审批轴浏览器层覆盖需 nop-entropy 平台工作流引擎支持以下任一：
- 浏览器层测试用户身份映射 / wf 委托免 sysUser(0) 物化（消除 seq PK 阻塞）
- sysUser(0) 平台种子物化为真实 `nop_auth_user` 行（使委托 FK 可达）
- 生产 `.xwf` 审批门控重构（submit step 显式 assignment，作为受控生产变更评审，非纯浏览器层可行路径）

## 看板 AMIS 前端渲染层 E2E（`visual/`，10 域）

在数值断言层之上，1249-2 叠加了看板 AMIS 前端渲染层 DOM 断言（`tests/e2e/visual/dashboards.visual.spec.ts`，1 spec / 10 测试）。区别于数值断言层（`page.request.post('/graphql')` 直调后端绕过 AMIS），本层驱动真实 AMIS 页面 → 拦截 AMIS 自身 GraphQL 响应 → 断言 DOM 渲染，验证「page → AMIS GraphQL 调用 → adaptor → DOM 渲染」全路径。解除 0637-1 Deferred「像素级视觉回归」的部分意图（以 DOM 内容/结构断言替代像素 diff；纯像素 diff 仍 Deferred）。

### 断言范式（`visual/_helper.ts`）

`assertDashboardRendered(cfg)` 编排：`loginAndNavigate` → `page.waitForResponse`（拦截含 `getDashboardKpi` 的 `/graphql` 响应，断言 200）→ 断言 KPI 卡片结构（`.border.rounded.p-3` wrapper 可见且 count≥1）→ 条件化数值 token（`cfg.expectedKpiTokens` 提供时，`expect.poll` 断言 `span.h3` textContent 含 token）→ 条件化 echarts canvas（`cfg.hasChart` 时 `toBeVisible` + boundingBox 非零）→ 条件化预警表格（`cfg.alertTable` 时 `table` 可见）。

### 执行期发现的产品缺陷（首个能捕获的层）

本层首次发现 **AMIS `$var` GraphQL 查询模板损坏缺陷**（`docs/bugs/2026-07-09-1249-dashboard-amis-var-mangling.md`）：8 参数化看板 page.yaml 手写 `query($var:Type){...($var)...}` 经 AMIS 运行时模板解析，裸 `$var` 被替换为空，致查询损坏（实测请求体 `query(:Long){ ...periodId: }`）、KPI 恒 0/空。冒烟层（仅查标签 + GraphQL 200，损坏查询仍返回 200）与数值断言层（直调后端绕过 AMIS）均无法捕获。

据此断言分层：
- **全 10 域**：AMIS GraphQL 管线（200）+ KPI 卡片结构 + echarts canvas（master-data 无 trend 跳过）+ 预警表格——全绿。
- **2 非参数化域**（projects `50000`、master-data `4`，查询无 `$var`，AMIS 路径完整）：额外断言确定性数值 token 渲染进 DOM——全绿。
- **8 参数化域**（finance/sales/purchase/inventory/assets/manufacturing/maintenance/quality）：因 `$var` 损坏缺陷暂仅断言结构，数值 token 断言随修复 successor（见 plan 2026-07-09-1249-2 Deferred）落地。

### 层间区分

| 层 | 验证路径 | 捕获能力 |
| --- | --- | --- |
| 冒烟层 `*.smoke.spec.ts` | AMIS 页面 → GraphQL 200 + 标签关键词 | 页面可达性 + GraphQL 不崩溃（不验数值） |
| 数值断言层 `*.value.spec.ts` | `page.request.post` 直调后端绕过 AMIS | 后端聚合确定性数值（不验前端渲染） |
| 前端渲染层 `*.visual.spec.ts` | AMIS 页面 → AMIS 自身 GraphQL → adaptor → DOM | AMIS 渲染管线完整性 + adaptor/模板回归（首个捕获 `$var` 损坏缺陷） |

## 报表 AMIS 前端渲染层 E2E（`visual/`，24 域）

在报表数值断言层之上，1728-1 + 2330-2 叠加了报表 AMIS 前端渲染层 DOM 断言（`tests/e2e/visual/reports.visual.spec.ts`，1 spec / 24 测试）。区别于数值断言层（`page.request.post('/graphql')` 直调后端绕过 AMIS），本层驱动真实 AMIS 报表页 → 填充参数表单 → 点「渲染报表」→ 拦截 AMIS 发出的 `renderHtml` GraphQL 响应（断言 200）→ 轮询 body 文本断言报表专属 token 注入 DOM。验证「page form → AMIS service reload → renderHtml → adaptor 拍平 reportHtml → `type:html html:"${reportHtml}"` 注入 DOM」全路径。覆盖全 24 报表域：fin 5 / mfg 3 / ast 2 / crm 2 / hr 2 / mnt 2 / prj 2 / qa 2 / md 2 / inv 1 / cs 1。

### 断言范式（`visual/_helper.ts#assertReportRendered`）

`assertReportRendered(cfg)` 编排：`loginAndNavigate` → `page.waitForResponse`（拦截含 `renderHtml` 的 `/graphql` 响应）→ 条件化 `fill`（input-number/input-text 表单字段）→ 条件化 `fillDates`（AMIS input-date 经 label 定位）→ 点「渲染报表」→ 断言 200 → `expect.poll` 轮询 body textContent 含首个 token → 逐 token 断言全量。token 为 format-independent subject/label（title/header/code/dict-label），数值 ≥1000 因 DOM 千分位格式化（如 120000→"120,000.00"）仅在 value-spec 层断言。AMIS 日期序列化（裸时间戳/空串）经后端 `asDate`/`asLong`/`asString` 宽容解析兜底（plan 2026-07-12-1321-3），不再经测试层 `page.route` 归一化。

### AMIS 表单变量规范化拦截器

执行期发现 AMIS 表单的两种变量序列化问题（value-spec 层直调后端不可见）：
1. **空字符串过滤**：AMIS 对空表单字段发送 `""` 而非 null，后端按 `""` 过滤返回空数据。拦截器将 `""` → null（镜像 value-spec 层省略未用过滤参数的行为）。
2. **DatePicker 时间戳**：AMIS DatePicker 将日期序列化为 Unix 时间戳（如 `"1783440000"`），后端 `DateTimeFormatter` 无法解析。拦截器将 10 位纯数字 → ISO 日期字符串。ar-ap-aging 的 `${NOW()}` 默认值亦产此问题，经 `fillDates` 填有效日期覆盖。

### helper 提取说明

`assertReportRendered` 与 `assertDashboardRendered` 共存于 `visual/_helper.ts`，reports.visual.spec.ts 与 dashboards.visual.spec.ts 均从此 import，共享 `loginAndNavigate` + `test` + `expect` 导入结构一致性。

## 像素级截图视觉回归层 E2E（`visual/*.snapshot.spec.ts`，10 看板 + 6 代表性报表）

在前端渲染层 DOM 断言之上，2026-07-17-2010-2 叠加了**像素级截图视觉回归层**（`tests/e2e/visual/dashboards.snapshot.spec.ts` + `tests/e2e/visual/reports.snapshot.spec.ts`，共 1 spec / 16 测试）。区别于 DOM 内容/结构断言（数值渲染进 DOM + echarts canvas 存在 + 表格行），本层 `expect(page).toHaveSnapshot(...)` 比对**整页位图**，捕获 DOM 层的结构盲区：CSS 错位 / 元素重叠 / echarts canvas 尺寸塌缩 / 响应式断点破坏。

### 可行性裁决依据（Phase 1 Explore）

`tests/e2e/visual/_exploration/` 下的 `snapshot-feasibility.exploration.spec.ts`（finance 参数化 + master-data 非参数化 × 4 变体 = 8 张基线）+ `snapshot-feasibility.measure.spec.ts`（5 关键变体 `maxDiffPixels: 0` 最严格口径）经 3 次新鲜浏览器上下文运行：finance-v-a-bare / finance-v-b-font / finance-v-c-mask / master-data-v-a-bare / master-data-v-b-font **全部 0 diff pixels（exact match）**。裁决：macOS + Chrome（`channel: 'chrome'`）+ 系统 PingFang/Apple 系字体栈在同一 OS/浏览器版本下跨次渲染像素级一致；echarts canvas 经 `waitForLoadState('networkidle') + waitForTimeout(1500)` 等待动画末态后亦 pixel-exact。

### 断言范式（`visual/_helper.ts#assertSnapshot`）

`assertSnapshot(page, opts)` 编排：

1. **echarts 动画末态等待**：`page.waitForLoadState('networkidle') + waitForTimeout(1500)`（覆盖默认 1s echarts 动画 + 容余）；`opts.skipEchartsSettle` 跳过（报表页面无 canvas）。
2. **字体固化注入**：`page.addStyleTag` 注入显式字体链 `-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "PingFang SC", "Microsoft YaHei", sans-serif !important`（本期实测系统字体本已稳定，此为防御未来环境漂移）；`opts.skipFontHardening` 跳过。
3. **canonical mask**：`[page.locator('header').first(), page.locator('canvas')]`——header 含用户名/头像动态文本，canvas 为 echarts 绘制目标（防御未来 canvas 绘制时序漂移）。
4. **附加 mask**：`opts.mask` 数组附加页级动态区域（如含 `${NOW()}` 日期戳的报表容器）。
5. **容差**：`maxDiffPixelRatio: 0.01`（1%，Phase 1 选定）——本期实测 0 diff，1% 作为 CI 环境次像素抗锯齿漂移的 belt-and-suspenders，远小于真实布局回归（CSS 错位/重叠/canvas 塌缩 > 1%）；`opts.maxDiffPixelRatio` 覆盖。
6. **threshold**：Playwright 默认 0.2（per-pixel 颜色差异容忍，不变更）。

`playwright.config.ts` 全局默认 `expect.toHaveScreenshot.maxDiffPixelRatio: 0.01`，使未显式传容差的截图断言共享同范式。

### 已知动态区域 mask 清单

| 区域 | mask 来源 | 跨次稳定原因 |
| --- | --- | --- |
| 用户名/头像（`header`） | canonical mask | 用户名随会话变化 |
| echarts canvas | canonical mask | 默认动画时序 + canvas 绘制跨环境漂移风险 |
| 报表 `${NOW()}`/日期戳容器 | opts.mask（按需） | 服务端当前时间戳 |

### 基线更新流程

当 `page.yaml`/`view.xml`/报表 `.xpt.xml` 模板经意变更导致像素合理漂移时：

```bash
# 1. 复跑确认漂移仅来自预期变更（非真实缺陷）
BASE_URL=http://127.0.0.1:8011 SKIP_WEBSERVER=1 \
  npx playwright test tests/e2e/visual/dashboards.snapshot.spec.ts tests/e2e/visual/reports.snapshot.spec.ts \
  --workers=1

# 2. 审视 diff 报告（test-results/*-actual.png vs baseline）确认无回归

# 3. 重录受影响基线
BASE_URL=http://127.0.0.1:8011 SKIP_WEBSERVER=1 \
  npx playwright test tests/e2e/visual/dashboards.snapshot.spec.ts \
  --workers=1 --update-snapshots
```

### 已知限制

- **同 OS/浏览器版本基线**：基线捕获于 macOS + Chrome `channel: 'chrome'`，跨 OS（Linux CI 无头）或浏览器版本变化需重新捕获分支专属基线（Playwright snapshot 文件名含 `-chromium-darwin` 平台后缀自动隔离）。
- **跨浏览器矩阵**（Firefox/WebKit/移动视口）：本计划 Non-Goal，AMIS 主目标为 Chromium，归 successor（触发条件：需支持非 Chromium 浏览器时）。
- **报表下载产物字节级 diff**：本层仅覆盖 AMIS 前端渲染 HTML 注入容器的像素比对，不覆盖 XLSX/PDF 下载产物（由 `reports.download.spec.ts` 二进制有效性回归层覆盖；字节级 diff 仍 open optimization candidate）。
- **canvas 内容真实性**：canvas 经 canonical mask 屏蔽像素比对（防御动画漂移），不直接断言 echarts 图表数据正确性（数值正确性由 `*.value.spec.ts` + DOM 内容层 `*.visual.spec.ts` 共同覆盖）。

## 报表下载产物运行时回归层 E2E（`reports/`，24 域 × 2 产物）

在报表 AMIS 前端渲染层之上，2026-07-12-0204-1 叠加了报表下载产物（XLSX/PDF）的运行时二进制有效性回归（`tests/e2e/reports/reports.download.spec.ts`，1 spec / 48 测试）。本层验证「page → 下载目标 → `Erp{Domain}Report__download` → 二进制产物」全路径的**后端可达性 + 产物有效性**：每产物非空 + 正确魔数（XLSX=`PK\x03\x04` zip 头 / PDF=`%PDF`）+ 弱结构 token（经文本提取断言产物含报表专属 title/label token）。

### 下载路径裁定（Phase 1 Explore 实测）

- **选定路径**：`POST /p/{ErpXxxReport__download}`（page-query 二进制下载专路）。实测 `/graphql` 端点对 WebContentBean 返回 JSON 序列化（`buildJaxrsGraphQLResponse` → `JSON.stringify(res)`，源码 `QuarkusGraphQLWebService.java:62-64` + `GraphQLWebService.buildJaxrsGraphQLResponse`），不产二进制流；`/p/{operationName}` 经 `buildJaxrsPageResponse` → `consumeWebContent` 写字节流 + `Content-Disposition: attachment`（源码 `GraphQLWebService.java:400-443`）。
- **路径等价性**：`/graphql` 与 `/p/` 调同一 `Erp{Domain}Report__download` @BizQuery，后端逻辑（模板解析 + `ALLOWED_RENDER_TYPES` 校验 + `reportEngine.getRenderer` 渲染 + WebContentBean 包装）完全一致，差异仅在 HTTP 序列化层。
- **page.yaml 下载按钮 URL 已修复**（plan 2026-07-12-0413-1）：全 24 page.yaml 的 48 下载按钮 `api.url` 已由 `/graphql` 修复为 `/p/{operationName}` + REST 入参 map，AMIS 按钮现真实可用（见下方「报表 AMIS 下载按钮回归层」）。本层（直调 `/p/`）与 AMIS 按钮层互补：直调层覆盖全 48 用例后端可达性，AMIS 按钮层抽样覆盖 page.yaml 接线 + AMIS download api 兼容性。

### 文本提取实现（`reports/_helper.ts`）

- **XLSX**：Nop 报表引擎输出标准 zip 但条目用 data descriptor（local header `compressedSize=0`），须解析 central directory（`PK\x05\x06` EOCD → `PK\x01\x02` CD 条目）取真实压缩大小。提取 `xl/worksheets/*.xml` 的 `<t>...</t>` 文本（Nop 用 `t="inlineStr"` 内联字符串，非 `xl/sharedStrings.xml`）。
- **PDF**：解压所有 FlateDecode 流 → 解析 `ToUnicode` bfrange/bfchar CMap 建 glyph→Unicode 映射 → 解码 `Tj`/`TJ` 文本操作符 → 归一化 CJK Radical 变体（U+2F00–U+2FDF 范围 8 个映射：⼯→工 / ⼼→心 / ⽃→斗 / ⽐→比 / ⽣→生 / ⽬→目 / ⾦→金 / ⼊→入）使 PDF 子集字体的 radical 映射能与标准 CJK Unified Ideograph token 对齐。24 报表标题 token 经归一化后 PDF 提取全覆盖。

### 断言范式（`reports/_helper.ts#assertReportDownload`）

`assertReportDownload({ domain, reportName, renderType, data, expectedTokens })` 编排：`loginAndNavigate` → `page.request.post('/p/{bizName}__download', { reportName, renderType, data })` → `resp.body()` Buffer → ①长度 > 0 → ②首 4 字节魔数（XLSX `PK\x03\x04` / PDF `%PDF`）→ ③文本提取（XLSX zip 解析 / PDF CMap 解码 + radical 归一化）后断言至少一个 `expectedTokens` 子串存在。失败消息含 reportName + renderType + 失败维度。

## 报表 AMIS 下载按钮回归层 E2E（`reports/`，5 报表抽样 × 2 产物）

在报表下载产物运行时回归层（直调 `/p/`，绕过 AMIS）之上，2026-07-12-0413-1 叠加了**驱动真实 AMIS 下载按钮**的回归（`tests/e2e/reports/reports.amis-download.spec.ts`，1 spec / 10 测试）。区别于直调层（`page.request.post('/p/...')` 证明后端可达性），本层点击 page.yaml 中真实的「下载 XLSX/PDF」按钮，验证「page.yaml `api` 接线（url `/p/{biz}__download` + REST body map）→ AMIS `actionType: download`+`responseType: blob` → 浏览器文件下载」全路径——首个能捕获 page.yaml 下载按钮 URL/结构漂移（如历史 `/graphql` URL）的层。

### 抽样选型（三类参数形态 × {xlsx, pdf}）

非全 24×2=48 用例（直调层已覆盖全 48 后端可达性，AMIS 层抽样足以守卫 page.yaml 接线 + 控制套件时长）：

| 参数形态 | 报表 | 填充 |
| --- | --- | --- |
| 参数化（单数值） | fin income-statement | periodId（page.yaml 默认 1） |
| 参数化（数值 + 日期区间） | mfg crp-load-report | workcenterId=1（日期留空，经 route 归一化 → null → 后端全量） |
| 零参 | crm lead-conversion-funnel | 无表单字段 |
| 字符串参（零参全量渲染） | md partner-list | partnerType 留空 |
| 字符串参（填充） | cs ticket-sla-csat-summary | ticketType=1（投诉桶） |

### 断言范式（`reports/_helper.ts#assertAmisDownloadButton`）

`assertAmisDownloadButton({ domain, reportName, renderType, route, formFields, fillDates, expectedTokens })` 编排：`loginAndNavigate` → 条件化 `fill`（input-number/input-text）/`fillDates`（AMIS input-date 经 label 定位）→ 点「下载 XLSX/PDF」→ ①`page.waitForRequest('/p/{biz}__download')` 断言按钮命中 `/p/` 端点（URL 回退到 `/graphql` 的回归会令此 promise 超时）→ ②`page.waitForEvent('download')` → `download.path()` 落盘文件 `fs.readFileSync` Buffer → ③长度 > 0 → ④魔数 → ⑤文本提取后断言至少一个 `expectedTokens` 子串。AMIS 日期序列化（裸时间戳/空串）经后端 `asDate`/`asLong`/`asString` 宽容解析兜底（plan 2026-07-12-1321-3），不再经测试层 `page.route('**/p/*')` 归一化。

**字节源选择关键发现**：Playwright `response.body()` 对「页面以 blob 消费的响应」返回 **0 字节**（响应流已转移给 Blob），故断言字节源取 download 事件落盘文件（用户真实收到的产物，端到端证明力度等同直调层），而非 `resp.body()`。请求层 `waitForRequest` 保留 `/p/` URL 守卫（捕获 URL 漂移）。

## CRUD 套件（18 域列表/表单冒烟）

`tests/e2e/crud/` 下每域 1 个代表性「主单据头」实体 spec（共 18 spec），调 `_helper.ts` 的 `runCrudListSmoke({ domain, entityRoute, addFormField })` 委派。每 spec 断言：列表页 DOM 渲染（`.cxd-Crud`/`.cxd-Table`）+ add 按钮（`button:has(.fa-plus)`）+ `/graphql` 查询 200 + 无 console error + add 表单打开后字段渲染（`input[name="code"]`）。

| 域 | 实体 | 路由 | 选型理由 |
| --- | --- | --- | --- |
| master-data | `ErpMdPartner`（往来单位） | `/ErpMdPartner-main` | 2328-2 基准沿用 |
| inventory | `ErpInvStockMove`（库存移动单） | `/ErpInvStockMove-main` | 2328-2 基准沿用 |
| purchase | `ErpPurOrder`（采购订单） | `/ErpPurOrder-main` | 改选主单据头（基准 `ErpPurRequisition`） |
| sales | `ErpSalOrder`（销售订单） | `/ErpSalOrder-main` | 改选主单据头（基准 `ErpSalQuotation`） |
| finance | `ErpFinVoucher`（会计凭证） | `/ErpFinVoucher-main` | 改选主单据头（基准 `ErpFinVoucherTemplate`） |
| assets | `ErpAstAsset`（固定资产） | `/ErpAstAsset-main` | 2328-2 基准沿用 |
| projects | `ErpPrjProject`（项目） | `/ErpPrjProject-main` | 2328-2 基准沿用 |
| manufacturing | `ErpMfgWorkOrder`（工单） | `/ErpMfgWorkOrder-main` | 改选主单据头（基准 `ErpMfgRouting`） |
| quality | `ErpQaInspection`（质检单） | `/ErpQaInspection-main` | 改选主单据头（基准 `ErpQaInspectionTemplate`） |
| maintenance | `ErpMntVisit`（维护访问） | `/ErpMntVisit-main` | 2328-2 基准沿用 |
| crm | `ErpCrmLead`（线索/商机） | `/ErpCrmLead-main` | 改选主单据头（基准 `ErpCrmBundlePricing`） |
| cs | `ErpCsTicket`（客服工单） | `/ErpCsTicket-main` | 改选主单据头（基准 `ErpCsTicketType`） |
| hr | `ErpHrEmployee`（员工） | `/ErpHrEmployee-main` | 改选主单据头（基准 `ErpHrSurvey`） |
| aps | `ErpApsOperationOrder`（工序工单） | `/ErpApsOperationOrder-main` | 2328-2 基准沿用 |
| logistics | `ErpLogShipment`（发运单） | `/ErpLogShipment-main` | 2328-2 基准沿用 |
| b2b | `ErpB2bAsn`（提前发货通知） | `/ErpB2bAsn-main` | 2328-2 基准沿用 |
| contract | `ErpCtContract`（合同） | `/ErpCtContract-main` | 2328-2 基准沿用 |
| drp | `ErpDrpPlan`（DRP 计划） | `/ErpDrpPlan-main` | 2328-2 基准沿用 |

「改选主单据头」理由：浏览器 E2E 倾向用户最常打开的列表页（业务单据头）而非 config/模板实体。冒烟级仅断言列表 DOM + add 表单字段可见（不持久化），故主单据头 mandatory 字段不阻断。全 343 实体覆盖 + CRUD 写操作 + 数据驱动断言为 successor（触发条件见 `docs/plans/2026-07-08-1234-2-crud-page-e2e-smoke.md` Deferred）。

## 认证机制

- 每个测试通过 UI 登录（`nop`/`123`）获取会话
- storageState 方案不适用（SPA 路由守卫在 context 初始化时拒绝预注入 token）
- 测试用户由 `allow-create-default-user=true` JVM 属性创建（非生产配置变更）

## 诊断

遵循 `docs/references/playwright-e2e-guide.md` 决策树：

1. **应用未启动？** → 检查端口 8080、runner jar 是否存在
2. **页面空白？** → 检查 console error、SPA 是否加载、hash 路由是否正确
3. **GraphQL 非 200？** → 检查 `/graphql` 端点（非 `/api/GenericApi`）、查询语法
4. **登录失败？** → 确认 `allow-create-default-user=true`、H2 DB 是否已初始化
5. **列表/页面渲染超时（`locator.waitFor: Timeout 15000ms` 等 `.cxd-Crud`）？** → **不要假设时序/环境问题**。几乎总是服务端页面构建错误：查 server 日志的 `errorCode=`（如 `nop.err.xui.grid.col-not-prop` = view.xml grid 列引用了不存在的实体属性）→ `@_loc=[行:列]/path` 定位源文件。诊断法见 `docs/lessons/05-nop-e2e-failure-log-first-diagnosis.md`（日志优先、从后向前）；`node tools/parse-nop-errors.mjs <server.log>` 与 `node tools/check-view-cols.mjs` 可批量定位。历史教训：跨 ≥10 份计划误诊的"17 CRUD smoke 渲染超时环境问题"真因为 view.xml 非法列阻断渲染（plan 0012-1 已全修）。

失败时查看 trace：

```bash
npx playwright show-trace test-results/<test-name>/trace.zip
```

## 已知限制

- **空库冒烟**：~~H2 文件库无业务数据，KPI 卡片渲染 DOM 但数值为 0/空。~~ **已解除**：webServer 默认含 `-Dnop.orm.init-database-data=true`（fresh-DB 重置 + 91 张 CSV 种子：21 主数据 + 23 P2P/O2C 交易单据 + 13 运营域表 + 4 制造域表 + 11 维护+质量域表 + 12 CRM/CS/HR 域表 + 3 质量域 SPC 表 + 4 制造域工作中心配置链+crp_load 表 + 3 处加性追加）。核心域（finance/sales/purchase）+ 运营域（inventory/assets/projects）+ 扩展域（manufacturing/maintenance/quality）看板 KPI 与报表数值经交易数据驱动**非空可观测**（含质量域 SPC 失控预警三计数器非空 + 制造域 CRP 负荷报表非空）；CRM/CS/HR 三域 5 报表经种子数据驱动**非空可观测**（三域无看板）。核心域 + 运营域 + 扩展域（manufacturing/maintenance/quality）+ 扩展域（CRM/CS/HR 纯报表域）+ 主数据域（master-data 看板/2 报表）均已叠加**数据驱动数值断言层**（28 `*.value.spec.ts`，见上方「数据驱动数值断言层」）；其余扩展域（logistics/b2b/contract/drp/aps）无看板无报表未 seed。
- **单浏览器**：仅 chromium（Chrome channel），不支持 Firefox/WebKit/移动视口。
- **冒烟级**：不断言像素级视觉一致性、不验证报表渲染内容正确性、不断言下载产物。
- **页面验证已恢复**：`ErpCsTicket.view.xml`/`ErpHrEmployee.view.xml` layout 缺陷已修复（见 `docs/bugs/`），启动期页面模型校验（`validate-page-model=true`）已恢复全绿，不再使用 `-Dnop.web.validate-page-model=false` 绕过。
- **page.yaml 已修复**：全部 34 page.yaml 已修复 API URL（`/api/GenericApi`→`/graphql`）+ GraphQL Map 字段选择移除。原始 page.yaml 存在系统性 bug（从未运行时测试过）。
- **下载功能**：报表下载 button（XLSX/PDF）的后端 `ErpXxxReport__download` 经 `/p/{operationName}` 端点（page-query 二进制下载专路）已建立**下载产物运行时回归层**（`tests/e2e/reports/reports.download.spec.ts`，24 报表 × {xlsx,pdf} = 48 用例，断言非空 + 魔数 + 弱结构 token，见下方「报表下载产物运行时回归层」）。**page.yaml 下载按钮 URL 已修复**（plan 2026-07-12-0413-1）：全 24 报表 page.yaml 的 48 下载按钮 `api.url` 由失效的 `/graphql`（该端点对 WebContentBean 返回 JSON 序列化而非二进制流）修复为 `/p/{ErpXxxReport__download}` + REST 风格入参 map，AMIS `actionType: download`+`responseType: blob` 现真实触发浏览器文件下载。新增**报表 AMIS 下载按钮回归层**（`tests/e2e/reports/reports.amis-download.spec.ts`，5 报表抽样 × {xlsx,pdf} = 10 用例，驱动真实 AMIS 按钮 → `waitForEvent('download')` 落盘文件断言，见下方「报表 AMIS 下载按钮回归层」）。
  - ~~已知预存限制（非下载 URL 修复引入，归 successor）：日期参数报表（12 报表：crp-load/production-variance/forecast-variance/asset-depreciation/asset-disposal/downtime-summary/maintenance-history/project-cost-summary/timesheet-detail/inspection-summary/ncr-capa-summary/ar-ap-aging）的 AMIS `input-date` 将日期序列化为裸 Unix 时间戳、未填字段序列化为空串，后端 strict-parse 抛 `DateTimeParseException`——renderHtml 同样行为（visual helper 经 `page.route` 测试层归一化）。AMIS 下载回归层的 `assertAmisDownloadButton` 经 `page.route('**/p/*')` 同范式归一化（`""→null` / 10 位时间戳→ISO 日期）使测试可执行；生产用户面对空/默认日期下载仍受此 AMIS 序列化缺陷影响，触发条件：产品要求日期参数报表空/默认日期下载真实可用时（需后端宽容解析或前端 AMIS 日期格式化）。~~ **已解除**（plan 2026-07-12-1321-3）：后端 `asDate`/`asLong`/`asString` helper 宽容解析落地（8 报表 BizModel：fin/mfg/ast/mnt/prj/qa 的 `asDate`+`asLong` + inv 的 `asLong` + md 的 `asString`，空串→null / 10·13 位 Unix 时间戳→`LocalDate` / ISO→`LocalDate`）+ 12 报表 page.yaml 的 `input-date` 追加 `valueFormat: "YYYY-MM-DD"`（AMIS hygiene）+ 测试层日期归一化 workaround 移除（`reports/_helper.ts` + `visual/_helper.ts` 的 `page.route` 拦截器）。12 日期参数报表 + 12 非日期参数报表在**真实 AMIS 序列化**（无 workaround）下下载（XLSX/PDF）+ 渲染（HTML）全绿（82 passed）。

## 文件结构

```
package.json                          # @playwright/test 依赖 + scripts
playwright.config.ts                  # 配置（端口/webServer/testDir/channel）
tests/e2e/
├── auth.ts                           # 登录 helper（performLogin + loginAndNavigate）
├── fixtures.ts                       # test 扩展（console error 检查器）
├── global-setup.ts                   # globalSetup（保留，当前未使用——storageState 不适用）
├── dashboards/
│   ├── _helper.ts                    # runDashboardSmoke + assertDashboardKpiValues 共享函数
│   ├── finance.smoke.spec.ts         # 独立 spec（含 KPI 文本断言）
│   ├── finance.value.spec.ts         # 数值断言层（GraphQL getDashboardKpi 取值）
│   ├── sales.smoke.spec.ts           # 使用 _helper
│   ├── sales.value.spec.ts           # 数值断言层
│   ├── purchase.smoke.spec.ts
│   ├── purchase.value.spec.ts        # 数值断言层
│   ├── inventory.smoke.spec.ts
│   ├── inventory.value.spec.ts       # 数值断言层（运营域）
│   ├── assets.smoke.spec.ts
│   ├── assets.value.spec.ts          # 数值断言层（运营域）
│   ├── projects.smoke.spec.ts
│   ├── projects.value.spec.ts        # 数值断言层（运营域：getDashboardKpi + getProjectGrossMargin）
│   ├── manufacturing.smoke.spec.ts
│   ├── manufacturing.value.spec.ts   # 数值断言层（扩展域）
│   ├── maintenance.smoke.spec.ts
│   ├── maintenance.value.spec.ts     # 数值断言层（扩展域）
│   ├── quality.smoke.spec.ts
│   ├── quality.value.spec.ts         # 数值断言层（扩展域：getDashboardKpi + getSpcOutOfControlWarning）
│   ├── master-data.smoke.spec.ts
│   └── master-data.value.spec.ts     # 数值断言层（主数据域：getDashboardKpi 5 字段 + 2 预警空集）
├── crud/
│   ├── _helper.ts                    # runCrudListSmoke + assertCrudListValues + runCrudWriteCycle 共享函数
│   ├── cs-kb-suggestion.smoke.spec.ts  # KB 建议定向冒烟（suggestForTicket GraphQL 200）
│   ├── master-data.smoke.spec.ts     # ErpMdPartner
│   ├── inventory.smoke.spec.ts       # ErpInvStockMove
│   ├── purchase.smoke.spec.ts        # ErpPurOrder
│   ├── sales.smoke.spec.ts           # ErpSalOrder
│   ├── finance.smoke.spec.ts         # ErpFinVoucher
│   ├── assets.smoke.spec.ts          # ErpAstAsset
│   ├── projects.smoke.spec.ts        # ErpPrjProject
│   ├── manufacturing.smoke.spec.ts   # ErpMfgWorkOrder
│   ├── quality.smoke.spec.ts         # ErpQaInspection
│   ├── maintenance.smoke.spec.ts     # ErpMntVisit
│   ├── crm.smoke.spec.ts             # ErpCrmLead
│   ├── cs.smoke.spec.ts              # ErpCsTicket
│   ├── hr.smoke.spec.ts              # ErpHrEmployee
│   ├── aps.smoke.spec.ts             # ErpApsOperationOrder
│   ├── logistics.smoke.spec.ts       # ErpLogShipment
│   ├── b2b.smoke.spec.ts             # ErpB2bAsn
│   ├── contract.smoke.spec.ts        # ErpCtContract
│   ├── drp.smoke.spec.ts             # ErpDrpPlan
│   ├── *.list-value.spec.ts          # 13 域 CRUD 数据驱动列表断言（findPage seed 行 + token）
│   ├── master-data.write.spec.ts     # master-data CRUD 写路径 GraphQL 层（create/update/delete 持久化）
│   ├── master-data.write.amis.spec.ts # master-data CRUD 写路径 AMIS 表单层（UI 新增/编辑/删除，0814-1）
│   ├── quality.write.spec.ts         # quality ErpQaRiskRegister CRUD 写路径 GraphQL 层（0814-1，含 dict status）
│   └── maintenance.write.spec.ts     # maintenance ErpMntEquipmentCategory CRUD 写路径 GraphQL 层（0814-1）
├── business-actions/                 # 业务动作浏览器层 E2E（0814-2 + 2004-1 + 0335-1 + 0215-3 + 0508-1 + 0941-2 + 1218-1 + 1005-2，自定义 @BizMutation 经 GraphQL，17 域 63 spec）
│   ├── _helper.ts                    # createViaSave/callMutation/verifyState/eqFilter/deleteByFilter 原语
│   ├── inventory-stock-move.action.spec.ts  # StockMove generateMove/complete/cancel 状态机+过账
│   ├── crm-lead.action.spec.ts              # Lead qualify/moveStage/cancel 状态迁移
│   ├── cs-ticket.action.spec.ts             # Ticket 六态状态机 + 非法迁移守卫
│   ├── cs-canned-response.action.spec.ts    # CannedResponse 三级宏匹配 + 变量渲染 + usageCount + 审计
│   ├── maintenance-visit.action.spec.ts     # Visit schedule/start/complete/cancel 5 态 + 设备联动副作用（2004-1）
│   ├── projects-task.action.spec.ts         # Task startTask/completeTask/blockTask/unblockTask 4 态 + DAG 门控（2004-1）
│   ├── quality-capa.action.spec.ts          # CAPA startAction/completeAction/verifyAction 3 态（2004-1）
│   ├── quality-ncr.action.spec.ts           # NCR submitReview/escalateToRecall/cancel 无 CAPA 路径（2004-1）
│   ├── mnt-request-visit-orchestration.action.spec.ts  # Request accept → 响应式 Visit 副作用字段断言（2329-2）
│   ├── finance-voucher-post.action.spec.ts  # ErpFinVoucher post(PostingEvent) 业财过账工厂入口 + 幂等（2329-2）
│   ├── fin-reconciliation.action.spec.ts    # ErpFinReconciliation 核销生命周期 create→post→reverse + 双面对照（0204-2）
│   ├── fin-bank-recon.action.spec.ts        # ErpFinBankReconciliation 银行对账 generate→post→reverse + BANK_RECON_ADJ 凭证（0413-2）
│   ├── fin-bad-debt.action.spec.ts          # ErpFinBadDebt 坏账 writeOff→submit→approve + recover + runBadDebtProvision（0413-2）
│   ├── aps-schedule.action.spec.ts          # ErpApsSchedule 排产方案状态机 publish/archive + ARCHIVED→publish 守卫（0508-1）
│   ├── aps-operation-order.action.spec.ts   # ErpApsOperationOrder scheduleForward/scheduleBackward 引擎 + PLANNED 翻转（0508-1）
│   ├── b2b-edi-doc.action.spec.ts           # ErpB2bEdiDoc EDI 信封状态机 出站/入站生命周期 + retry + 非法守卫（0508-1）
│   └── log-shipment.action.spec.ts          # ErpLogShipment 发运单 advise→completeShipment→cancelShipment + mock 网关 + 守卫（0508-1）
├── orchestration/                       # 跨域编排链浏览器层 E2E（1249-1 正向 + 2004-2 反向）
│   ├── _helper.ts                        # runP2pChain/runO2cChain + runP2pReverse/runO2cReverse + findItems + 清理原语（凭证/AR-AP/移动单产物）
│   ├── p2p-chain.spec.ts                 # P2P PO→Receive→Invoice 全链 + stockMove + GL/AP 过账产物
│   ├── o2c-chain.spec.ts                 # O2C SO→Delivery→Invoice 全链 + 出库移动 + GL/AR 过账产物
│   ├── p2p-reverse.spec.ts              # 业财闭环方向二：AP_INVOICE 财务侧红冲 + 采购发票回退（2004-2）
│   └── o2c-reverse.spec.ts              # 业财闭环方向二：AR_INVOICE 财务侧红冲 + 销售发票回退（2004-2）
├── visual/                              # AMIS 前端渲染层 E2E（1249-2 看板 + 1728-1/2330-2 报表，DOM 结构 + echarts canvas + 数值/subject token）
│   ├── _helper.ts                       # assertDashboardRendered + assertReportRendered（waitForResponse + 卡片结构 + renderHtml 注入断言）
│   ├── dashboards.visual.spec.ts        # 10 域看板 AMIS 渲染管线断言（2 非参数化域含数值 token）
│   └── reports.visual.spec.ts           # 24 域报表 AMIS 渲染管线断言（全报表域 renderHtml → DOM 注入）
└── reports/
    ├── _helper.ts                    # runReportSmoke + assertReportRenderedWithValue 共享函数
    ├── fin-balance-sheet.value.spec.ts   # 数值断言层
    ├── fin-income-statement.value.spec.ts # 数值断言层
    ├── fin-ar-ap-aging.value.spec.ts     # 数值断言层
    ├── ast-depreciation.value.spec.ts     # 数值断言层（运营域）
    ├── inv-inventory-trace.value.spec.ts  # 数值断言层（运营域）
    ├── prj-cost-summary.value.spec.ts     # 数值断言层（运营域）
    ├── mfg-production-variance.value.spec.ts  # 数值断言层（扩展域）
    ├── mfg-forecast-variance.value.spec.ts    # 数值断言层（扩展域）
    ├── mfg-crp-load.value.spec.ts             # 数值断言层（扩展域，工作中心负荷报表）
    ├── mnt-maintenance-history.value.spec.ts  # 数值断言层（扩展域）
    ├── qa-inspection-summary.value.spec.ts    # 数值断言层（扩展域）
    ├── crm-lead-conversion-funnel.value.spec.ts   # 数值断言层（扩展域 CRM，纯报表域无看板）
    ├── crm-forecast-accuracy.value.spec.ts        # 数值断言层（扩展域 CRM）
    ├── cs-ticket-sla-csat.value.spec.ts           # 数值断言层（扩展域 CS）
    ├── hr-employee-net-balance.value.spec.ts      # 数值断言层（扩展域 HR）
    ├── hr-payroll-simulation-comparison.value.spec.ts  # 数值断言层（扩展域 HR）
    ├── md-material-price-list.value.spec.ts       # 数值断言层（主数据域，物料价格清单）
    ├── md-partner-list.value.spec.ts              # 数值断言层（主数据域，往来单位清单）
    ├── reports.download.spec.ts                   # 下载产物回归层（24 报表 × {xlsx,pdf} = 48 用例，魔数+token）
    ├── reports.amis-download.spec.ts              # AMIS 下载按钮回归层（5 报表抽样 × {xlsx,pdf} = 10 用例，真实按钮→download 事件，0413-1）
    └── *.smoke.spec.ts               # 24 个报表 spec
```
