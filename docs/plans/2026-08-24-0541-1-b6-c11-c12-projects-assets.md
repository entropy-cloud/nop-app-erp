# 2026-08-24-0541-1-b6-c11-c12-projects-assets 批次 B6 用例实施：C11 项目成本归集→CIP→资产资本化→维护设备 + C12 项目工时过账与结算损益

> Plan Status: completed
> Mission: integration-test
> Work Item: B6（C11, C12；主导域 projects + assets）
> Last Reviewed: 2026-08-24
> Source: `docs/backlog/integration-test-roadmap.md`（B6 行，M0.3 追加）
> Related: `2026-08-24-0159-2-b5-c09-c10-quality-maintenance`（done）、`2026-08-24-0541-2-b7-c13-c14-finance`（同批兄弟计划，B7 排其后）
> Audit: required

## Current Baseline

（全部事实 2026-08-24 实仓核验）

- **前置依赖**：M0.1 设计文档（`docs/design/integration-testing.md` §6 C11/C12 用例规格 + §3.4 机制实证结论 + §4 粒度约定 + §5 xwf 规避清单）+ M0.2 基建 + B1-B5（均 done）。
- **基建就绪**（M0.2/B1-B5 产物）：基类 `ErpIntegrationTestCase`（机制 (c) 文件 H2 双模 + 10 个共享 step helper）；`_cases` 目录约定；surefire 模块级串行化已落地；已有 12 个集成用例类（Pilot + B1-B5 = 1+2+2+2+3+2）作为范本，**B6 按同范式实现（1 用例 = 1 类 1 方法 + @BeforeAll fresh-DB + RECORDING→CHECKING 往返）**。
- **prj 域既有测试资产（本批业务参照，不重复实现内部细节）**：`TestErpPrjProjectSettlement`（结算 createSettlement→submit→approve 链 + CLOSE）、`TestErpPrjTimesheetCost`（工时过账 PROJECT_COST_COLLECTION）、`TestErpPrjBudgetAndCollection`（成本归集单 `ErpPrjCostCollection`）、`TestErpPrjProjectPnl`/`TestErpPrjPnlCalcJob`（损益结转）、`TestErpPrjProjectPrecheck`；`PrjFrozenClockExtension` 存在（module-common-test 基类 `AbstractFrozenClockExtension` + prj-service 测试侧具体扩展）。
- **ast 域既有测试资产**：`TestErpAstPostingReverse`/`TestErpAstCapitalization`（资本化建卡 + CAPITALIZATION 凭证，设计文档 C11 引为过账先例）、`TestErpAstCip*`（CIP 在建工程——**实仓动作面（实仓核验）：`ErpAstCipBizModel` 无 save 后「完工审批」动作，实际为 `startConstruction`/`addCostItem`/`addProgressBilling`/`transferToAsset`/`reverseTransfer`；`ErpAstAssetCapitalizationBizModel` + `ErpAstAssetCapitalizationDocumentStateMachine`（approve: DRAFT→ACTIVE 双轴）**——设计文档 §6 C11 步骤 2/3 动作名存在漂移风险，实施期须裁决登记（对齐 B4/B5 动作名勘误先例））。
- **C11 前置 seed（实仓核验在位）**：`erp_prj_project.csv`（PRJ-2026-001 OPEN，含已发生成本 30000，项目类型/客户/币种齐备）；`erp_prj_cost_collection.csv`（PRJ-CC-2026-001 已审批**未**过账先例行，POSTED=false 供参照）；**设计文档 §6 C11 前置「2210-1 seed」为旧式码，实仓新式码 = PRJ-2026-001（语义前置满足，实施期登记勘误）**。
- **C12 前置 seed（实仓核验在位）**：`erp_hr_employee.csv`（HR-EMP-001/002 ACTIVE）；`erp_prj_timesheet.csv`（PRJ-TS-2026-001 APPROVED 先例行）；**`erp_prj_project_pnl.csv` 非空——含 1 行 PRJ-PNL-2026-001（CALCULATED/OPEN/APPROVED，revenue 50000/cost 30000/profit 20000），且绑定 project_id=1 = 本用例结算对象 PRJ-2026-001**——结算 CLOSE/损益结转可能再生成或改写该 seed 行（快照变更行 + seed 行被消费双风险），Phase 2 Decision ③ 须裁决 PNL 断言锚点与 seed 行交互（对齐「以当前实现为准」，不得静默修改 seed）；**设计文档 §6 C12 前置「1234-1 seed」为旧式码，实仓新式码 = HR-EMP-001/002（语义前置满足，实施期登记勘误）**。
- **C12 过账/门控（实仓核验）**：`ErpPrjConstants.CONFIG_DEFAULT_PAYROLL_SUBJECT_ID` = `erp-prj.default-payroll-subject-id`（默认空串，`ErpPrjConfigs` 登记，`TimesheetPostingDispatcher` 空科目报 `ARG_SUBJECT_CODE` 错误路径）——**工时过账科目 2211 依赖该 config，默认空串 → 须 `@NopTestProperty` 指定（对齐 C05/C06/C08/C10 门控同型处理）**；结算动作：`ErpPrjProjectSettlementBizModel` 实仓动作 = `createSettlement`/`submit`（内部委托 `submitForApprovalProcessor`，**无 GraphQL `submitForApproval` 动作**）/`approve`/`reject`/`cancel`/`reverseSettlement`/`returnRetention`（业务先例 `TestErpPrjProjectSettlement` 走 `submit` 驱动）——**设计文档 §6 C12 步骤 2 原述 `ErpPrjSettlement__createSettlement` 动作面漂移，实施期以实仓 `ErpPrjProjectSettlement__submit` 为准**。
- **运行互斥**：集成测试与 E2E live server 共用 `db/erp.mv.db`——执行前 `lsof -i :8011`（8080 同查）确认无 live server（e2e-runbook「集成测试」节纪律）。
- **基线计数**：known-good-baselines 2026-08-24 权威计数源 **3820 tests / 0 failures / 0 errors / 1 skipped / 629 报告文件**；app-erp-all **40/0/0/1**。B6 预期差量 **+2 tests / +2 报告文件**（2 用例类各 1 类 1 方法）。
- **流程改进登记（B3 closure MINOR-2 兑现，B5 已执行）**：最终 `clean install` 会清除 surefire XML 计数证据——全量回归后、clean 前先拷贝计数/XML 再登记基线（本计划 Phase 3 执行纪律）。
- **deferred 触发条件（B1/M0.2 watch-only residual）**：若执行期发生 seed 修正授权路径（roadmap 横切关注点 3），须同步测量登记重录耗时（以单用例 252K/类为预算参考）；本批默认自包含建数，预期不触发。
- **冻结时钟纪律**：C07 用例类以 `C07FrozenClockExtension`（`@RegisterExtension` 静态注册，app-erp-all it/ 内本地扩展）冻结日期；**C11/C12 涉结算期间/PNL 期间等日期敏感路径 → Phase 1/2 均按 C07 先例注册对应域 frozen clock（基类 `AbstractFrozenClockExtension` 在 module-common-test，具体扩展 `PrjFrozenClockExtension`/`AstFrozenClockExtension` 在各域 service 测试侧），并登记快照时间戳 `*` 通配处置（B2/B4/B5 先例）**。

## Goals

- **C11** = 测试类 `TestErpC11PrjCipCapitalizeMnt`（1 类 1 方法）：自包含成本归集单（`ErpPrjCostCollection__save`）→ CIP 建单（`ErpAstCip__save`）→ 完工/转固路径（**以当前实现为准裁决动作面**：startConstruction/transferToAsset 或 Capitalization 单 submit→approve [DIRECT]）→ 资产卡片生成（IN_SERVICE）+ CAPITALIZATION 凭证借贷平衡 → CIP 成本 = 资本化原值 → `ErpMntEquipment__save`（assetId 跨域引用核对）；三层全比对。
- **C12** = 测试类 `TestErpC12PrjTimesheetSettlement`（1 类 1 方法）：自包含工时单（引用 seed 员工 HR-EMP-001/002）→ submit → approve [DIRECT]（PROJECT_COST_COLLECTION 凭证：Dr 5101/Cr 2211，科目 config 实仓裁决）→ 结算 `ErpPrjProjectSettlement__createSettlement` → `submit` → `approve`（CLOSE + 结转凭证/资产卡片回退，以实仓行为为准）→ 损益结转（FINAL/INTERIM 或实仓既有动作，PNL 断言以实仓为准并裁决 seed PNL 行交互）；三层全比对。
- 两用例 **RECORDING→CHECKING 往返全绿**。
- roadmap B6 状态 `todo` → `done`（经独立 closure audit）+ 日志 + 基线差量登记 + 设计文档 §6 C11/C12 动作面/旧式码勘误登记（若发生）。

## Non-Goals

- B7-B10 用例（各归对应分批计划——B7 已由 `2026-08-24-0541-2-b7-c13-c14-finance` 承接）。
- 生产代码/ORM/契约/API 变更——用例实施中发现的产品缺陷按 roadmap 横切关注点 4 走保护区域门禁（accounting/finance postings = plan-first + owner doc + tests；其余 = auto + dual-agent-approval），**不得在本计划内直接改生产代码**。
- 默认修改 seed CSV——确需修正走 roadmap 横切关注点 3「seed 修正授权」流程；默认以自包含建数满足前置。
- 不接 CI（roadmap 已裁决出 scope）；不做浏览器层 E2E 页面验证（分工边界）。
- 不实现未覆盖功能（横切关注点 6「以当前实现为准」）：C11/C12 步骤若与实仓动作面不符，以实仓动作替代 + 设计文档勘误登记，不强行实现缺失动作。

## Task Route

- Type: `implementation-only change`（集成测试代码 + 快照 + 文档，零生产代码/契约/模型变更）
- Owner Docs: `docs/design/integration-testing.md`（§6 C11/C12 规格 + §3.4 编写纪律 + §4 粒度 + §5 xwf 清单）、`docs/backlog/integration-test-roadmap.md`（B6 行 + 横切关注点）、`docs/testing/e2e-runbook.md`（「集成测试」节）、`docs/testing/known-good-baselines.md`
- Skill Selection Basis: `nop-testing` 匹配测试基类选择/@NopTestConfig/@NopTestProperty（C12 科目 config = C06/C08/C10 同型）/快照录制回放（RECORDING→CHECKING）/三层验证模型；M0.2 试点 + B1-B5 十二用例类为同模式范本，prj/ast 既有 EndToEnd 测试为业务参照。

## Infrastructure And Config Prereqs

- 文件型 H2 `db/erp.mv.db` fresh 机制已就绪（基类每类 1 次清理）；**与 E2E live server 互斥**——执行前 `lsof -i :8011`（8080 同查）确认无 live server。
- **C12 config 门控（待实施期裁决）**：`erp-prj.default-payroll-subject-id` 默认空串（实仓 `ErpPrjConfigs`）——若工时过账科目缺省无法落地，则测试类 `@NopTestProperty(name="erp-prj.default-payroll-subject-id", value="2211")`（对齐 C06/C08/C10 `@NopTestProperty` 范式）；以实仓 TimesheetPostingDispatcher 的缺省科目解析行为为准（`ARG_SUBJECT_CODE` 报错路径若为空串须裁决）。
- C11 资本化过账科目/折旧计划生成以实仓默认配置为准（无新增 config 预期）。
- 无端口/密钥/外部服务/环境变量新增；surefire 串行化已在 app-erp-all pom 落地。

## Execution Plan

### Phase 1 — C11 项目成本归集 → CIP → 资产资本化 → 维护设备（混合类型：1/3 Add + 1/3 Proof + 1/3 Decision，逐项标注）

Status: completed
Targets: `app-erp-all/src/test/java/io/nop/app/all/it/TestErpC11PrjCipCapitalizeMnt.java`、`app-erp-all/_cases/io/nop/app/all/it/TestErpC11PrjCipCapitalizeMnt/`
Skill: nop-testing

- Item Types: `Add | Proof | Decision`
- Prereqs: M0.1 + M0.2 + B1-B5 done

- [x] Add：**C11 测试类** `TestErpC11PrjCipCapitalizeMnt`（1 类 1 方法）——按设计文档 §6 C11 规格 + `TestErpPrjBudgetAndCollection`/`TestErpAstCapitalization`/`TestErpAstPostingReverse` 业务参照：自包含成本归集单（`ErpPrjCostCollection__save`，引用 seed 项目 PRJ-2026-001）→ CIP 建单（`ErpAstCip__save`，sourceType=CIP）→ 转固/资本化路径（按下方裁决落地）→ 资产卡片生成（IN_SERVICE）+ CAPITALIZATION 凭证借贷平衡 → CIP 成本 = 资本化原值 → `ErpMntEquipment__save`（assetId 指向新资产，`ErpAstAsset__get` 核对）；静态载荷走 `input/N_step.json5` + `addVar`/`@var:` 动态 id。
  - Skill: nop-testing
- [x] Decision：**CIP→资本化动作面裁决**——实仓 `ErpAstCipBizModel` 无「完工审批」动作（实仓 = startConstruction/addCostItem/transferToAsset），设计文档 §6 C11 步骤 2/3 原述动作（save→完工审批→Capitalization save→submit→approve）须以实仓为准：核定 CIP 转固链路（transferToAsset 是否直建资产卡片/凭证，或需经 `ErpAstAssetCapitalization` 单 DRAFT→ACTIVE approve 建卡 + CAPITALIZATION 凭证），选定可行路径并记录备选与残留风险；同步裁决资产折旧计划生成/资产原值口径（CIP 成本 = 资本化原值断言锚点）。若与设计文档不符，Phase 3 登记 §6 C11 勘误。
  - Skill: nop-testing
- [x] Proof：**C11 RECORDING→CHECKING 往返**——RECORDING 录制 → CHECKING 复跑全绿；层 1 锚点：CIP 状态迁移（IN_CONSTRUCTION→TRANSFERRED 或实仓终态）、资本化后资产卡片（IN_SERVICE）+ CAPITALIZATION 凭证借贷平衡、CIP 成本 = 资本化原值、设备卡 assetId 指向新资产。
  - Skill: nop-testing

Exit Criteria:

- [x] C11 类存在且 RECORDING→CHECKING 往返绿（失败模式 = 任一断言/快照 diff 不通过）
- [x] CIP→资本化动作面裁决落盘（含设计文档 §6 C11 勘误登记，若与实仓动作面不一致）

### Phase 2 — C12 项目工时过账与结算损益（混合类型：1/3 Add + 1/3 Proof + 1/3 Decision，逐项标注）

Status: completed
Targets: `app-erp-all/src/test/java/io/nop/app/all/it/TestErpC12PrjTimesheetSettlement.java`、`app-erp-all/_cases/io/nop/app/all/it/TestErpC12PrjTimesheetSettlement/`
Skill: nop-testing

- Item Types: `Add | Proof | Decision`
- Prereqs: Phase 1（同基类同装配，先例链打通后无新机制风险）

- [x] Add：**C12 测试类** `TestErpC12PrjTimesheetSettlement`（1 类 1 方法）——按设计文档 §6 C12 规格 + `TestErpPrjTimesheetCost`/`TestErpPrjProjectSettlement` 业务参照：自包含工时单（引用 seed 员工 HR-EMP-001/002，hours × rate 金额断言）→ submit → approve [DIRECT]（PROJECT_COST_COLLECTION 凭证：Dr 5101/Cr 2211，科目按下方裁决落地）→ 结算 `ErpPrjProjectSettlement__createSettlement` → `ErpPrjProjectSettlement__submit` → `approve`（CLOSE + 结转凭证/资产卡片回退，以实仓行为为准）→ 损益结转（FINAL/INTERIM 或实仓既有动作，PNL 层 1 断言以实仓为准并按下方裁决处理 seed PNL 行）；静态载荷走 `input/N_step.json5` + `addVar`/`@var:` 动态 id。
  - Skill: nop-testing
- [x] Decision：**工时科目 config 与结算/损益结转动作面裁决**——① `erp-prj.default-payroll-subject-id` 默认空串：`TimesheetPostingDispatcher` 空科目报 `ARG_SUBJECT_CODE` 错误 → 须 `@NopTestProperty` 指定 2211（对齐 C08 simulation 门控同型），记录残留风险；② 实仓 `ErpPrjProjectSettlementBizModel` 动作 = `createSettlement`/`submit`/`approve`（**无 GraphQL submitForApproval**，设计文档 §6 C12 原述 `ErpPrjSettlement__createSettlement` 动作面漂移，Phase 3 登记勘误）；③ 损益结转路径（FINAL/INTERIM approve vs 实仓既有动作）与 PNL 断言锚点：**seed `erp_prj_project_pnl.csv` 已含 PRJ-PNL-2026-001 行（绑定 PRJ-2026-001）——结算/损益结转对其再生成或改写的交互须以实仓行为为准裁决（断言锚点选实仓新生成行或 seed 行变更断言，不得静默改 seed）**。与设计文档不符处 Phase 3 登记 §6 C12 勘误。
  - Skill: nop-testing
- [x] Proof：**C12 RECORDING→CHECKING 往返**——RECORDING 录制 → CHECKING 复跑全绿；层 1 锚点：工时审批后凭证借贷平衡 + 金额正确（hours × rate）、结算 CLOSE 后凭证 + 资产卡片回退（以实仓行为为准）、损益结转凭证 + 期间模块状态（如适用）。
  - Skill: nop-testing

Exit Criteria:

- [x] C12 类存在且 RECORDING→CHECKING 往返绿（失败模式 = 任一断言/快照 diff 不通过）
- [x] 工时科目 config 与结算/损益结转动作面裁决落盘（含设计文档 §6 C12 勘误登记，若与实仓不一致）

### Phase 3 — 收尾与回归（Proof-heavy）

Status: completed
Targets: `docs/backlog/integration-test-roadmap.md`（B6 → done + 头「最后更新」注记）、`docs/logs/2026/08-24.md`、`docs/testing/known-good-baselines.md`（差量登记）、`docs/design/integration-testing.md`（§6 C11/C12 勘误登记，若 Phase 1/2 触发）
Skill: none

- Item Types: `Proof | Add`
- Prereqs: Phase 2

- [x] Proof：**局部回归** `mvn test -pl app-erp-all` 全绿（含 2 个新用例类，串行 fork 单 JVM 顺序执行）+ **全量回归** `mvn test`（全 reactor）零新增失败 + `mvn clean install -DskipTests` BUILD SUCCESS——对照 known-good-baselines（B5 后基线）口径，+2 tests / +2 报告文件全额归因 B6，零新增失败；**clean 前先拷贝 surefire 计数/XML 证据**（B3 closure MINOR-2 流程改进兑现，B5 已执行）。
  - Skill: none
- [x] Add：roadmap B6 `todo` → `done` + 头「最后更新」注记 + `docs/logs/2026/08-24.md` 日志条目（按日志书写指南）+ known-good-baselines 差量登记 + 设计文档 §6 勘误登记（若触发，含 C11/C12 动作面/旧式码漂移点）。
  - Skill: none

Exit Criteria:

- [x] 局部 + 全量回归零新增失败（与已知基线口径一致）
- [x] roadmap B6 = done + 日志条目存在 + 基线差量登记 + 勘误登记（若触发）

## Draft Review Record

- Independent draft review iteration 1: needs revision (ses_fcf6c7147ffeUTlDV0N6pZEeoj，独立 general 子代理新会话) — 0 BLOCKER / 2 MAJOR / 3 MINOR。事实核验 19 项 17 PASS。MAJOR-1 `erp_prj_project_pnl.csv` 非空（含 PRJ-PNL-2026-001 CALCULATED 行，绑定 project_id=1 = 本用例结算对象 PRJ-2026-001）→ Baseline 已修订 + Phase 2 Decision ③ 补 seed PNL 行交互裁决义务；MAJOR-2 实仓 `ErpPrjProjectSettlementBizModel` 动作 = `createSettlement`/`submit`/`approve`（**无 GraphQL submitForApproval**，`submit` 内部委托 submitForApprovalProcessor）→ Baseline/Goals/Phase 2 Add/Decision ② 全部 `submitForApproval` → `submit` 修订；MINOR-1 冻结时钟「按需」反松弛词 → Baseline 改具体纪律（C07 先例 + Phase 1/2 均注册对应域 frozen clock）；MINOR-2 冻结时钟位置表述（AbstractFrozenClockExtension 基类在 module-common-test，具体扩展在各域 service 测试侧）→ 修订；MINOR-3 「已过账先例行」自相矛盾 → 「已审批未过账先例行」（POSTED=false 事实正确）→ 修订。
- Independent draft review iteration 2: needs revision（1 残留 MAJOR，ses_fcf68e7c4ffeWn00qNjxicocGT，独立 general 子代理新会话）——4/5 声明修复核验 PASS（PNL seed 行处理 / 冻结时钟纪律与位置 / 已审批未过账表述），MAJOR-2 修复漏网 1 处：Baseline L17 `TestErpPrjProjectSettlement` 先例链仍述 `createSettlement→submitForApproval→approve`（与 L21「业务先例走 submit 驱动」自相矛盾）→ 已修订为 `createSettlement→submit→approve 链 + CLOSE`。0 BLOCKER / 0 MINOR 其余全过，无新引入事实错误。
- Independent draft review iteration 3: accept (ses_fcf66f7afffejnUSye1pS58WJB，独立 general 子代理新会话) — 0 BLOCKER / 0 MAJOR / 0 MINOR。残留 MAJOR 修复核验 PASS（全文件 `submitForApproval` 仅剩 4 处合法语境：委托机制陈述 ×1 / 漂移勘误陈述 ×1 / 审查历史 ×2；Baseline L17 已 `createSettlement→submit→approve 链`，与 L21/L31/L88/L90 全链一致）；两处曾误事实实仓复核真（PNL seed 行 + Settlement 动作面）；冻结时钟四处扩展实仓在位；格式/内部一致性/roadmap B6 对齐/反松弛词全过。**共识达成，计划可执行。**

## Closure Gates

> 仅在所有项目和每个阶段的退出标准都勾选 `[x]` 后关闭。完整仓库验证在此处：结束时运行 `typecheck`/`build`/`lint`/`test` 一次。

- [x] 范围内行为完成（C11/C12 两用例三层全比对全绿 + 动作面/科目 config 裁决）
- [x] 相关文档对齐（设计文档 §6 ↔ 用例实现 ↔ roadmap B6 ↔ e2e-runbook ↔ known-good-baselines 无矛盾）
- [x] 已运行验证（`mvn test -pl app-erp-all` 全绿 + 全量 `mvn test` 零新增失败 + `mvn clean install -DskipTests` BUILD SUCCESS）
- [x] 无范围内项目降级为 deferred/follow-up
- [x] 独立草案审查已完成并记录
- [x] 文本一致性已验证：状态、阶段、门控和日志都一致
- [x] 结束审计由独立子代理（新会话）执行；执行者未自我审计且未将此留为 `[ ]` 作为人工门控占位符
- [x] 结束证据存在于文件中

## Deferred But Adjudicated

- （无范围内降级项——执行期零 seed 修正授权触发（自包含建数满足全部前置），B1/M0.2 watch-only residual 触发条件未发生；deferred 段留空）

## Closure

Status Note: C11/C12 两用例 RECORDING→CHECKING 往返全绿（联合复跑 2/0/0 连跑两次稳定）+ 局部回归 app-erp-all 42/0/0/1 + 全量回归 3822/0/0/1/631（对照 B5 3820/0/0/1/629 差量 +2 tests / +2 报告文件全额归因 B6，零新增失败；surefire 证据已拷贝 `_tmp/b6-surefire-evidence/`）+ `mvn clean install -DskipTests` 156 模块 BUILD SUCCESS；执行期裁决（C11 CIP 转固动作面 = transferToAsset 内建资本化链 + 资本化科目口径；C12 工时借记科目 = 项目类型 defaultSubjectId（seed 项目类型缺失 → 自包含项目，未触发 seed 修正授权）+ 员工引用 = md 域员工 + 结算 createSettlement/submit/approve + 损益结转 = FINAL 结算 approve + seed PNL 行零改写零消费）落盘测试类 javadoc + 设计文档 §6 C11/C12 勘误登记；执行期处置（C09 B5 预存 flake 复现修复 + C11/C12 同型时间戳 @var 不稳定 → `*` 通配）记录 roadmap/日志/基线；roadmap B6 done + 日志 + known-good-baselines 差量登记均完成。

Closure Audit Evidence:

- Auditor / Agent: 独立 general 子代理（新会话 `ses_fcf44cb36ffeIYMkbjGP5Hg7DE`，fresh 会话无执行者上下文）
- Evidence: 独立审计逐项核验 5 域（计划一致性 / 测试代码合规（extends ErpIntegrationTestCase + @NopTestConfig + @Inject 非 private + 三层验证）/ 验证证据（实跑 `mvn test -pl app-erp-all -Dtest=TestErpC11PrjCipCapitalizeMnt,TestErpC12PrjTimesheetSettlement` CHECKING 2/0/0 ×2 + app-erp-all 全量 42/0/0/1 + `_tmp/b6-surefire-evidence/` C11/C12 各 1/0/0 在位）/ 文档一致性（roadmap B6 done + known-good-baselines 3822/0/0/1/631 + app-erp-all 42/0/0/1 + 设计文档 §6 C11/C12 勘误 + 日志）/ 无范围内降级（git status 零 module-*/model/src/main/seed 变更，C09 快照通配属预存 flake 处置））——功能证据 5/5 PASS，唯一前置态 = 审计时点 Closure Gates 未勾选 + Closure 段占位（B5 先例流程，本段即其兑现）；总评 **accept**，0 BLOCKER / 0 MAJOR / 2 MINOR（非阻塞计数口径注记：①「78 处通配」为近似计数（独立重算约 79-82 处，计量口径差异）；② evidence 目录为 app-erp-all+module-common-test 子集（29 XML/68 tests），全量 631 报告文件计数以记录为准）。

Follow-up:

- 无（非阻塞跟进项：C09/C11/C12 响应快照时间戳 @var 跨 run 不稳定 → `*` 通配处置已随本计划落地；B7 分批（C13/C14）已由 `docs/plans/2026-08-24-0541-2-b7-c13-c14-finance.md` 承接）