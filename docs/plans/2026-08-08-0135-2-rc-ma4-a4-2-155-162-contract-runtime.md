# 2026-08-08-0135-2 rc-ma4-a4-2-155-162-contract-runtime 合同域总金额校验/版本/行复制/到期/失效/审批/折扣/OCR 隐式机制运行时确认

> Plan Status: active
> Last Reviewed: 2026-08-08
> Source: `docs/backlog/requirement-compliance-roadmap.md` Work Items A4.2.155 / A4.2.156 / A4.2.157 / A4.2.158 / A4.2.159 / A4.2.160 / A4.2.161 / A4.2.162
> Related: `docs/audits/2026-08-05-1400-1-rc-ma1-a1-45-46-contract-lifecycle-billing-rebate.md`（A1.45-46 §7 SP-1..SP-8 + P1-RC-072~079 + reuse P1-MA2-071）
> Audit: required
> Mission: requirement-compliance
> Work Item: A4.2.155-A4.2.162（contract 域 MA4 运行时验证）
> Approval: 只读审计（A4 MA4 类目），受 `docs/discussions/2026-08-07-1140-rc-approval-inventory-analysis.md` §3 A4 确认自动执行（Q2）覆盖；本计划不实施生产代码修复（P1 修复归 MR1）

## Current Baseline

contract 域切片（A1.45-46 生命周期与签署/计费与返利）MA1 报告 §7 共列出 8 项静态存疑点（SP-1..SP-8），全部为本计划范围（A4.2.155-A4.2.162）。该切片为 contract 域 MA1 首审，无专属 MA2 状态机报告（A2.14 覆盖合同/InvoicePlan 状态机迁移 + EquipmentStatusLinker，不含创建校验/审批工作流/计费/返利维度）。八项存疑点共同主题 = **"是否存在未被 grep 发现的隐式机制"**（前端编排/XMeta 跨字段校验/全局 job/其他审批引擎/外部服务/订单侧隐式调用）——全部倾向"无"，需运行时证实。finding 家族：P1-RC-072（UC-CT-01 创建校验/v1/预算特批）/ P1-RC-073（UC-CT-02 行复制/驳回恢复）/ P1-RC-074（UC-CT-03 invoiceTerm 生成/已开票锁）/ P1-RC-075（UC-CT-04 消耗计费）/ reuse P1-MA2-071（UC-CT-05 到期自动化，resolved-via-deferral 需 MR1 重新实现）/ P1-RC-076（UC-CT-06）/ P1-RC-077（UC-CT-07）/ P1-RC-078（UC-CT-08）/ P1-RC-079（UC-CT-10；UC-CT-09 签章已接受，无 finding）。

- **A4.2.155（SP-1 UC-CT-01 ① 运行时合同创建是否经前端/XMeta 层隐式校验 totalAmount=∑行金额）**：静态判定 = `ErpCtContractBizModel.defaultPrepareSave:57` 无金额校验 + XMeta mandatory 单字段非跨字段 → save 不拒绝金额不一致合同。运行时确认 GraphQL save（totalAmount ≠ Σ行金额）是否实际拒绝。
- **A4.2.156（SP-2 UC-CT-01 ④ 运行时合同到 NEGOTIATION 是否经隐式机制创建 v1 版本）**：静态判定 = 无 `submit`/`negotiate` mutation（grep 零命中）+ 测试/E2E 均手工 seed 版本（`TestErpCtContractTerminate#createVersion:183`/`TestErpCtContractPosting#createVersion:210`）→ 无自动 v1。运行时确认 GraphQL save 后是否自动生成版本。
- **A4.2.157（SP-3 UC-CT-02 ② 运行时 amend 后变更单是否经前端编排半自动复制原合同行）**：静态判定 = `ErpCtContractAmendProcessor#amend:35` 仅版本翻转零行复制 → 前端无补全。运行时确认前端是否补全行复制。
- **A4.2.158（SP-4 UC-CT-05 运行时是否有其他全局 job 扫描合同到期）**：静态判定 = grep 跨 module-contract 零 Job + scheduler.yaml 无 contract 到期 job → 仅手工 `ErpCtContractBizModel#expire:118`。运行时确认 scheduler.yaml/job 注册现状（reuse P1-MA2-071 维持，修复归 MR1）。
- **A4.2.159（SP-5 UC-CT-06 运行时 terminate 后 InvoicePlan 是否经合同头 TERMINATED 状态隐式失效）**：静态判定 = `ErpCtInvoicePlanTriggerInvoiceProcessor#triggerInvoice:35` 非 ACTIVE 态守卫拒绝（SUSPENDED 专属 `ERR_CT_CONTRACT_SUSPENDED` + 通用非 ACTIVE `ERR_CT_CONTRACT_NOT_ACTIVE` 双守卫，`testTriggerInvoiceRejectedForSuspendedContract:93` 证实 SUSPENDED 拒绝）→ TERMINATED 隐式失效无显式截停标记。运行时确认 TERMINATED 合同上未开票 InvoicePlan 触发时实际拒绝行为。
- **A4.2.160（SP-6 UC-CT-07 运行时是否有其他全局审批引擎驱动合同审批）**：静态判定 = `ErpCtApprovalMatrix`/`ErpCtApprovalRecord` 仅 CRUD 桩（17 行）→ 无工作流引擎。运行时确认合同提交是否触发任何审批流。
- **A4.2.161（SP-7 UC-CT-08 运行时 purchase/sales 订单行是否经隐式机制应用合同折扣）**：静态判定 = purchase/sales 域零调用 resolveDiscount → 无自动应用。运行时确认订单引用合同行时是否应用折扣。
- **A4.2.162（SP-8 UC-CT-10 运行时是否有外部 OCR 服务/异步任务处理 ErpCtDocument）**：静态判定 = grep 零 OCR 业务逻辑 → ocrText/fullTextSearch 永不填充。运行时确认文档上传后字段填充机制。

剩余差距：八项均为只读运行时确认。缺陷项（P1-RC-072~079 + reuse P1-MA2-071）维持分级，修复归 MR1（Q4 强制实现义务不因本计划改变）；本计划仅确认运行时行为以维持/细化裁决，不实施修复。

## Goals

- 对 A4.2.155-A4.2.162 八项存疑点产出运行时行为证据链，输出验证报告落盘 `docs/audits/`。
- 每项给出 §2 判据裁决：确认隐式机制缺失（前端零编排 / XMeta 零跨字段 / 零 job / 零审批引擎 / 零折扣调用 / 零 OCR）→ 与 A1.45-46 §6 既有 finding（P1-RC-072~079 + reuse P1-MA2-071）衔接维持，或登记 watch-only；若运行时发现任一隐式机制**存在**（grep 遗漏），则细化裁决并回写 A1.45-46 报告。预期不触发 MR0（无活跃数据破坏候选）。
- 完成后回写 roadmap A4.2.155-A4.2.162 `todo → done`，并按裁决更新 arm-index（维持注记，无未经比对新建）。

## Non-Goals

- 不实现 P1-RC-072~079 / P1-MA2-071 修复——修复义务归 MR1 R1.0 展开器（Q4=(a) 强制实现；P1-MA2-071 resolved-via-deferral 须 MR1 重新实现 `ErpCtContractExpiryJob` + 续期 + 分级通知）。
- 不修改任何真相源（product-scope/use-cases/owner doc 需求契约段落）。
- 不修改 ORM/生产代码/前端 view.xml（本计划为只读确认）。
- 不复跑 A2.14 状态机审计（A1.45-46 已声明复用其证据）；不重审 P1-MA2-072（已 resolved-via-implementation）。

## Task Route

- Type: `verification or audit work`
- Owner Docs: `docs/audits/2026-08-05-1400-1-rc-ma1-a1-45-46-contract-lifecycle-billing-rebate.md`（§5/§6/§7）+ `docs/design/contract/`（use-cases.md / state-machine.md / volume-discount.md）
- Skill Selection Basis: roadmap MA4 全部工作项指定 `docs/skills/multi-dimensional-audit-prompt.md`。本计划为只读审计，无代码变更。

## Infrastructure And Config Prereqs

- No infra prereqs beyond existing baseline. 八项存疑点均经静态证明运行时决定性事实得出（零前端按钮/零跨字段 XMeta/零 job 注册/零审批引擎/零调用方/零 OCR 逻辑 ⇒ 运行时行为确定）+ 既有 L4 测试重跑佐证（`TestErpCtContractPosting`/`TestErpCtContractTerminate`/`TestErpCtESignature`/`TestErpCtContractRebate`/`TestErpCtRebateSettlementEnd`，`mvn test -pl module-contract/erp-ct-service`）+ 前端 AMIS/XMeta grep census（`module-contract/erp-ct-web` + `erp-ct-meta`）。无需运行应用或 DB。

## Execution Plan

### Phase 1 - 运行时证据采集与验证报告撰写（A4.2.155-A4.2.162）

Status: planned
Targets: `docs/audits/2026-08-08-0135-rc-ma4-a4-2-155-162-contract-runtime.md`（新建验证报告）
Skill: `docs/skills/multi-dimensional-audit-prompt.md`

- Item Types: `Proof`
- Prereqs: A4.2 done ✓；A1.45-46 done ✓

- [ ] **A4.2.155 合同创建 totalAmount=∑行金额 跨字段校验确认（UC-CT-01 ①）**：确认 `ErpCtContractBizModel.defaultPrepareSave:57` 无金额校验 + XMeta（`erp-ct-meta`）无跨字段校验表达式 + 前端 AMIS 无金额一致性校验 → save 不拒绝金额不一致合同。裁决：确认无跨字段校验（维持 P1-RC-072，修复归 MR1）。
  - Skill: `docs/skills/multi-dimensional-audit-prompt.md`
- [ ] **A4.2.156 NEGOTIATION v1 版本自动创建确认（UC-CT-01 ④）**：确认无 `submit`/`negotiate` mutation + GraphQL save 路径（`_ErpCtContract` mutation）无版本创建钩子 + 测试手工 seed 版本（file:line 证据）→ save 后不自动生成版本。裁决：确认无自动 v1（维持 P1-RC-072，修复归 MR1）。
  - Skill: `docs/skills/multi-dimensional-audit-prompt.md`
- [ ] **A4.2.157 amend 后行复制前端编排确认（UC-CT-02 ②）**：确认 `ErpCtContractAmendProcessor#amend:35` 仅版本翻转零行复制 + AMIS 变更单编辑表单无行复制交互 → 变更单须手工重录行。裁决：确认无行复制（维持 P1-RC-073，修复归 MR1）。
  - Skill: `docs/skills/multi-dimensional-audit-prompt.md`
- [ ] **A4.2.158 全局合同到期 job 确认（UC-CT-05）**：确认 scheduler.yaml/job 注册 census（module-contract 零 Job bean + nop-job 目录零 contract job）+ 仅手工 `expire:118` → 无自动到期扫描。裁决：确认无自动 job（reuse P1-MA2-071 维持，修复归 MR1）。
  - Skill: `docs/skills/multi-dimensional-audit-prompt.md`
- [ ] **A4.2.159 TERMINATED 隐式失效 InvoicePlan 确认（UC-CT-06）**：确认 `triggerInvoice:35` 非 ACTIVE 态双守卫（SUSPENDED 专属 + 通用非 ACTIVE）+ TERMINATED 合同上未开票 InvoicePlan 触发时实际拒绝行为（重跑 `TestErpCtContractPosting#testTriggerInvoiceRejectedForSuspendedContract:93` + 引用 `:50-54` 通用非 ACTIVE 守卫证据，补充 TERMINATED 输入断言）。裁决：确认隐式失效（无显式截停标记，行为经状态守卫达成）。
  - Skill: `docs/skills/multi-dimensional-audit-prompt.md`
- [ ] **A4.2.160 全局审批引擎确认（UC-CT-07）**：确认 `ErpCtApprovalMatrix`/`ErpCtApprovalRecord` 17 行 CRUD 桩 + xbiz/审批流 config 零接线 + 合同提交（save/activate）无审批触发。裁决：确认无审批工作流（维持 P1-RC-076~077 分级）。
  - Skill: `docs/skills/multi-dimensional-audit-prompt.md`
- [ ] **A4.2.161 purchase/sales 订单行折扣应用确认（UC-CT-08）**：确认 purchase/sales 域零调用 resolveDiscount/contract 折扣接口（grep census 跨 module-purchase/sales）+ 订单行无 contract 引用折扣字段。裁决：确认无自动折扣应用（维持 P1-RC-078 分级）。
  - Skill: `docs/skills/multi-dimensional-audit-prompt.md`
- [ ] **A4.2.162 外部 OCR 服务确认（UC-CT-10）**：确认 grep 零 OCR/异步任务处理 `ErpCtDocument` + ocrText/fullTextSearch 零 writer → 文档上传后字段永不填充。裁决：确认无 OCR 处理（维持 P1-RC-079 分级）。
  - Skill: `docs/skills/multi-dimensional-audit-prompt.md`
- [ ] **验证报告撰写**：八项存疑点各出 §裁决（隐式机制缺失确认 + 既有 finding 衔接维持）+ §与既有 finding 衔接（P1-RC-072~079 + reuse P1-MA2-071 交叉引用）+ §过程纪律自检（checker 退出码门控——无代码变更 actual=baseline；closure-audit 独立性声明）+ §真相源冻结声明（不修改需求契约段落）。报告落盘。
  - Skill: `docs/skills/multi-dimensional-audit-prompt.md`

Exit Criteria:

> 本阶段为只读审计，无生产代码变更。

- [ ] 验证报告落盘，含八项存疑点各自裁决 + file:line 证据 + §2 判据命中分支
- [ ] 每项裁决明确：隐式机制缺失确认 / 隐式机制存在（细化裁决并回写 A1.45-46 报告）+ 既有 finding 衔接（维持分级），或升级触发 MR0

### Phase 2 - Finding 衔接、roadmap/log 同步

Status: planned
Targets: `docs/backlog/requirement-compliance-roadmap.md`（A4.2.155-162 done）、`docs/audits/arm-index.md`（维持注记追加）、`docs/logs/2026/08-08.md`
Skill: none

- Item Types: `Decision | Add`
- Prereqs: Phase 1 报告落盘

- [ ] `Decision` arm-index 衔接裁决：P1-RC-072~079 维持分级（Q4 强制实现，修复归 MR1）；P1-MA2-071 维持 resolved-via-deferral 注记（MR1 重新实现义务声明）；P1-MA2-072 不重审（已 resolved-via-implementation）。无新 finding 新建（全部维持）。
- [ ] `Add` roadmap A4.2.155-A4.2.162 `todo → done`；`docs/logs/2026/08-08.md` 追加完成条目。

Exit Criteria:

- [ ] roadmap 八项状态已更新为 done 且与报告裁决一致
- [ ] arm-index 维持注记已追加（无未经比对直接新建的 finding）

## Draft Review Record

- Independent draft review iteration 1: accept (task ses_0257cb4f7ffeZFwf4PGZDMsCh0) — 格式完整（front matter/全部 13 段/两阶段）、反松弛词零命中、全部 file:line 实仓复核命中（defaultPrepareSave:57 / amend:35 / expire:118 / triggerInvoice:35 + 双守卫 :46-54 / createVersion:183/210 / ApprovalMatrix 17 行桩 / resolveDiscount 零命中 / OCR 零命中）、roadmap 对齐（A4.2.155-162 todo + Deps 满足）、SP-5 探针确定。5 项非阻塞 Minor 中可操作项已处理：补 Approval 行（Q2 A4 预授权声明）、SP-5 探针收敛为「重跑 SUSPENDED 拒绝测试 + 引用 :50-54 通用非 ACTIVE 守卫 + 补 TERMINATED 输入断言」、finding 家族 UC-CT-09 无 finding 注释（签章已接受）、双守卫措辞（SUSPENDED 专属 + 通用非 ACTIVE）细化。②④ 圈号 marker 为 cosmetic 保留。

## Closure Gates

> 本计划为只读审计（零生产代码/ORM/api.xml/view.xml/真相源变更）。closure 时确认 checker 未触发 actual > baseline。

- [ ] 范围内行为完成（八项存疑点均有运行时证据 + 明确裁决）
- [ ] 相关文档对齐（报告落盘 + roadmap/log 同步 + arm-index 衔接裁决记录）
- [ ] 已运行验证（`mvn test -pl module-contract/erp-ct-service` 既有测试类重跑 + checker actual=baseline 确认）
- [ ] 无范围内项目降级为 deferred/follow-up
- [ ] 独立草案审查已完成并记录
- [ ] 文本一致性已验证：状态、阶段、门控和日志都一致
- [ ] 结束审计由独立子代理（新会话）执行；执行者未自我审计且未将此留为 `[ ]` 作为人工门控占位符
- [ ] 结束证据存在于文件中

## Deferred But Adjudicated

### P1-RC-072~079 + P1-MA2-071 修复实现

- Classification: `out-of-scope improvement`
- Why Not Blocking Closure: 本计划仅运行时确认；P1 修复义务归 MR1 R1.0 展开器（Q4=(a) 强制实现，无方案 B 例外；P1-MA2-071 系 resolved-via-deferral 须 MR1 重新实现）。本审计维持分级不撤销。
- Successor Required: yes（MR1 R1.0 展开为 RC-R1.n 时承接）

## Closure

Status Note: （待执行后填写）

Closure Audit Evidence:

- Auditor / Agent: （待独立结束审计填写）
- Evidence: （待填写）

Follow-up:

- （待填写）
