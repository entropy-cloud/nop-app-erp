# 2026-08-06-2243-3 rc-ma1-a1-51-notify-full notify 通知派发全域（A1.51）需求符合性审计

> Plan Status: active
> Last Reviewed: 2026-08-06
> Mission: requirement-compliance
> Work Item: A1.51（MA1 需求追踪矩阵审计 — notify 通知派发子系统 UC-SYS-01~07，7 UC）
> Source: `docs/backlog/requirement-compliance-roadmap.md` Work Item A1.51
> Related: `docs/plans/2026-08-02-1458-1-requirement-compliance-methodology.md`（M0.1 done）、`2026-08-02-1530-1-requirement-baseline-extraction.md`（M0.2 done — 含 notify use-cases 补写，解除 A1.51 的 0.2 依赖）、`2026-08-05-1400-3-rc-ma1-a1-48-drp-full.md`（同 mission MA1 全域审计范式）、`2026-08-06-2243-1-rc-ma1-a1-49-logistics-full.md`+`2026-08-06-2243-2-rc-ma1-a1-50-aps-full.md`（同批 N=1/N=2，共同完成 MA1 51 切片全覆盖）
> Audit: required

## Current Baseline

> 本计划是**审计工作项**（verification or audit work），结果表面 = 一份审计报告。基线盘点被审功能现状代码/测试/既有证据，**不修改任何代码**。notify 子系统为一个 roadmap 行（A1.51，7 UC），按 plan 指南规则 4 一个结果表面 = 一个计划。

- **方法论契约 + UC 锚点已就绪**：`docs/audits/requirement-compliance-methodology.md`（§1-§10 + §去重协议 + §4 三判据）已落盘；`docs/audits/rc-requirement-baseline-inventory.md:385` 已为 A1.51 给出 UC 清单 = `UC-SYS-01~07`（7 UC，0.2 Phase 2 补写后显式 UC 编号），覆盖率 `✅ Phase 2 补写后显式 UC 清单`（无基线分歧 D-xx）。本切片为 **notify 子系统首个 RC 审计切片**（7 UC 全覆盖）。notify 用例编号前缀 `UC-SYS-NN` 对齐 appName `erp-notify`/二级简称 `sys`/表前缀 `erp_sys_*`/实体 `ErpSys*`。

- **L1 需求契约（权威真相源）**：`docs/design/notify/use-cases.md`（0.2 补写，来源可追溯 README.md + inbox-patterns.md + notification-strategy.md 三份权威文档；补写纪律声明明确"未参照运行时代码"）：
  - **UC-SYS-01 通知模板生命周期与查找**（`:28`）：模板(ErpSysNotificationTemplate) subjectTpl/bodyTpl 为 ${var} 插值模板 → 按业务事件键 notificationType 查找模板 → 仅 status=ACTIVE 参与派发(DRAFT 不参与) → 业务事件无 ACTIVE 模板时 config-gated 静默跳过(WARN)不抛错 → 模板状态迁移 DRAFT→ACTIVE。
  - **UC-SYS-02 通知实例端到端派发链**（`:48`）：统一入口 IErpSysNotificationBiz.notify(eventType, context) → 派发链顺序（不可乱序）：①查找 ACTIVE 模板 ②模板渲染(${var} 插值) ③接收人解析(RecipientResolver) ④频控合并(MergeCoordinator) ⑤站内消息落库(ErpSysNotification) ⑥config-gated 外发通道派发 → 站内消息派发即落表(status=SENT) → 子系统对业务方 best-effort（派发失败不回滚调用方业务事实 / notify() 内部 try/catch + config-gated 静默跳过）。
  - **UC-SYS-03 接收人多策略解析**（`:74`）：recipientResolver=ROLE(角色名→NopAuthRole→NopAuthUserRole→userId 复用平台 nop-auth) / =ORG(deptId→NopAuthUser) / =USER_LIST(模板配置 userId 列表静态+${var}从 context 插值如 submitterUserId) / =PARTNER(partnerId→用户映射[占位：映射未建立 WARN 返回空 config-gated])。
  - **UC-SYS-04 频控窗口合并**（`:98`）：合并组 key=notificationType+"#"+recipientUserId → 频控窗口(ErpNotifyConstants)：业务提醒 5min(300s)/异常告警 1min(60s)/系统通知不合并 → 命中窗口内既有实例合并(mergeCount+=1, status=MERGED)/未命中新建(status=SENT) → 总开关 erp-notify.merge-enabled(默认 true)。
  - **UC-SYS-05 已读状态管理**（`:123`）：markRead(notificationId) 写 ErpSysNotificationRead → markAllRead 批量标记当前用户全部未读为已读 → 唯一键(notificationId, userId)防重复标记已读 → 已读状态派生自 ErpSysNotificationRead 关联(存在该行→已读/不存在→未读，非 ErpSysNotification.status lifecycle 字段) → countUnread 实时统计当前用户未读数。
  - **UC-SYS-06 用户面收件箱后端查询**（`:149`）：findUnread/countUnread/markAllRead 三方法 userId 参数扩展为 @Optional（留空回退 ctx.getUserId() 前端不传后端从 JWT 解析 / 显式传 userId 仍按 userId 过滤向后兼容 admin 跨用户+单元测试）→ 未读 tab(recipientUserId==userId 且 id NOT IN ErpSysNotificationRead) / 已读 tab(ErpSysNotificationRead.userId==userId 反查 ErpSysNotification) / 全部 tab(客户端拼接 findUnread+findRead 去重) → 禁用 `<filter><eq name="status" value="READ"/>`（ErpSysNotification.status 是 lifecycle 非 read 状态）。
  - **UC-SYS-07 外发通道 config-gated 降级**（`:174`）：站内消息(IN_APP)默认通道派发即落表 → 邮件(EMAIL)经 nop-integration IEmailSender.sendEmail(EmailMessage) SPI 派发 / 短信(SMS)经 nop-integration ISmsSender.sendMessage(SmsMessage) SPI 派发(不新建平行通道适配层) → config-gated 配置项(erp-notify.*): enabled 默认 true 总开关 / email-enabled 默认 false / sms-enabled 默认 false → 无供应商/通道未启用时 WARN 跳过不阻断业务(不抛给调用方 / 各业务消费者额外 try/catch warn-and-continue) → 失败降级: notify() 整体 try/catch 模板缺失/渲染失败/接收人解析失败/落库失败均不抛出给调用方(返回空列表)。

- **L3 代码实现现状（实测，`module-notify/erp-notify-service`）**——**notify 派发链 3 BizModel+Dispatcher+MergeCoordinator+RecipientResolver+Noop Sender 完整 + 候选缺口待逐 UC 核验**：
  - **UC-SYS-01 模板生命周期（✅ ErpSysNotificationTemplateBizModel CRUD）**：`ErpSysNotificationTemplateBizModel` 存在。**待核**：①subjectTpl/bodyTpl ${var} 插值模板；②按 notificationType 查找；③仅 status=ACTIVE 参与派发；④无 ACTIVE 模板 config-gated 静默跳过(WARN)不抛错；⑤DRAFT→ACTIVE 状态迁移。
  - **UC-SYS-02 端到端派发链（✅ notify mutation+NotificationDispatcher）**：`ErpSysNotificationBizModel.java`#notify(`:54`)+`NotificationDispatcher`。**待核**：①派发链顺序不可乱序（查找 ACTIVE 模板→渲染→接收人解析→频控合并→站内落库→config-gated 外发）；②站内消息 status=SENT；③best-effort（notify() 内部 try/catch + 失败不回滚调用方 + config-gated 静默跳过）。
  - **UC-SYS-03 接收人多策略解析（✅ NotificationRecipientResolver）**：`NotificationRecipientResolver`。**待核**：①ROLE(角色名→NopAuthRole→NopAuthUserRole→userId)；②ORG(deptId→NopAuthUser)；③USER_LIST(userId 列表静态+${var}从 context 插值如 submitterUserId)；④PARTNER(partnerId→用户映射[占位 WARN 返回空 config-gated])。
  - **UC-SYS-04 频控窗口合并（✅ NotificationMergeCoordinator）**：`NotificationMergeCoordinator`。**待核**：①合并组 key=notificationType+"#"+recipientUserId；②频控窗口业务提醒 5min/异常告警 1min/系统通知不合并(ErpNotifyConstants)；③命中 mergeCount+=1 status=MERGED / 未命中新建 status=SENT；④总开关 erp-notify.merge-enabled(默认 true)。
  - **UC-SYS-05 已读状态管理（✅ markRead/markAllRead/countUnread+ErpSysNotificationReadBizModel）**：`ErpSysNotificationBizModel.java`#markRead(`:62`)/#markAllRead(`:68`)/#countUnread(`:106`)+`ErpSysNotificationReadBizModel`。**待核**：①markRead 写 ErpSysNotificationRead；②markAllRead 批量；③唯一键(notificationId, userId)防重复；④已读状态派生自 ErpSysNotificationRead 关联(非 status lifecycle)；⑤countUnread 实时统计。
  - **UC-SYS-06 收件箱后端查询（✅ findUnread/findRead/countUnread/markAllRead @Optional userId）**：`ErpSysNotificationBizModel.java`#findUnread(`:74`)/#findRead(`:80`)/#countUnread(`:106`)/#markAllRead(`:68`) 均 @Optional userId。**待核**：①留空回退 ctx.getUserId()；②显式传 userId 按 userId 过滤（admin 跨用户+测试向后兼容）；③未读 tab(recipientUserId==userId 且 NOT IN Read)；④已读 tab(Read.userId==userId 反查)；⑤全部 tab(客户端拼接去重)；⑥禁用 status=READ filter。
  - **UC-SYS-07 外发通道 config-gated 降级（✅ NoopEmailSender+NoopSmsSender+NotificationDispatcher）**：`NoopEmailSender`+`NoopSmsSender`+`NotificationDispatcher`。**待核**：①站内 IN_APP 默认通道落表；②邮件经 nop-integration IEmailSender.sendEmail SPI；③短信经 nop-integration ISmsSender.sendMessage SPI(不新建平行适配层)；④config-gated(enabled 默认 true/email-enabled 默认 false/sms-enabled 默认 false)；⑤无供应商/未启用 WARN 跳过不阻断；⑥失败降级 notify() 整体 try/catch(模板缺失/渲染失败/接收人解析失败/落库失败均不抛出返回空列表)。

- **L4 测试证据现状**（`module-notify/erp-notify-service/src/test`）：
  - `TestErpSysNotificationDispatch`（端到端派发链）、`TestErpSysNotificationTemplateLifecycle`（模板生命周期）、`TestErpSysNotificationSubscription`（订阅）、`TestErpSysNotificationCrossDomain`（跨域调用）、`TestErpSysNotificationSeedTemplates`（种子模板）。
  - **待核**：①UC-SYS-01 ACTIVE 模板查找+DRAFT 不参与+无 ACTIVE config-gated 跳过断言；②UC-SYS-02 派发链顺序+best-effort 不回滚断言；③UC-SYS-03 四类 resolver(ROLE/ORG/USER_LIST/PARTNER 占位)断言；④UC-SYS-04 窗口合并 mergeCount+1/新建+三类通知窗口差异断言；⑤UC-SYS-05 唯一键防重+countUnread+status 非 read 断言；⑥UC-SYS-06 userId 可选回退 ctx+三态切换+禁用 status=READ filter 断言；⑦UC-SYS-07 config-gated 默认 false+无供应商 WARN 跳过不阻断+SPI 复用 nop-integration 断言。MA5 评级待引用。

- **L5 既有证据（MA2 复用输入，方法论 §去重协议）**：
  - **无 notify 专属 MA2 状态机报告**。本切片为 notify 子系统行为的首份证据（notify 域 MA1 首审）。
  - **notify 相关既有 finding**：arm-index notify/notification/sys/notification-dispatch/UC-SYS 域 finding 待 grep（候选：跨域 daoFor P1-MA1-022 命中[notify entities]，平台一致性维度 resolved 不重审；notify 作为跨域子系统被多域引用——R1.x 已落地多切片 notify 接线，属既有行为证据复用）。**无任何 UC-SYS-01~07 需求符合性 finding**。
  - 本切片须声明与 MA2 报告差异增量（报告段落 9）：无 notify 专属 MA2 报告；只补需求视角差异（模板生命周期/派发链顺序/接收人策略/频控合并/已读状态/收件箱查询/外发降级）。

- **arm-index 既有 finding 衔接**：grep arm-index notify/notification/notificationDispatch/notificationMerge/notificationRecipient/inbox/UC-SYS → **无 UC-SYS-01~07 finding**。本切片须 grep arm-index notify 同域同控制点后裁决复用 or 新建 `P*-RC-xxx`（续编，执行时取最新——当前至 P2-RC-072 / P1-RC-082）。

- **保护区域**：本审计为**只读审计**。属 roadmap 预授权类目。发现的 P0/P1 finding **不在本计划实施修复**——按方法论 §10。本切片候选偏差多为**代码逻辑**类（预授权——派发链顺序/resolver 策略/频控窗口/@Optional 回退/config-gated 降级）；若触及 ORM 结构 → **ORM 结构变更须 ask-first + 独立 plan-audit**；须在报告逐项标注触及保护区域。

- **剩余差距**：A1.51 切片五级追踪审计报告缺失 = MA4 及 MR1 该切片证据缺口来源。本计划产出 notify 子系统全域审计报告并登记 finding，解除 notify 子系统证据缺口（本切片完成后 notify 子系统 A1.51 done，notify 7 UC 全覆盖；同时 MA1 51 切片全 done，解除 MA4 A4.1/A4.2 的 MA1 done 依赖）。

## Goals

- 产出 A1.51 切片审计报告 `docs/audits/2026-08-06-2243-3-rc-ma1-a1-51-notify-full.md`，含方法论 §6 **9 段全部内容**。
- 对 UC-SYS-01~07 逐条核验**每条验收标准**（完整枚举，§3，禁止跳号）：逐 UC 五级追踪。
- 对候选缺口给出分级结论：UC-SYS-01 模板 ACTIVE 查找+config-gated 跳过（待核 P2）、UC-SYS-02 派发链顺序+best-effort（待核 P1/P2）、UC-SYS-03 四类 resolver（待核 P1/P2）、UC-SYS-04 频控窗口+三类通知（待核 P1/P2）、UC-SYS-05 已读唯一键+countUnread（待核 P2）、UC-SYS-06 @Optional 回退+三态+禁用 status=READ filter（待核 P1/P2）、UC-SYS-07 config-gated 降级+SPI 复用（待核 P1/P2）——按 §2 判据定级，若为 P0/P1 则新建 `P*-RC-xxx`（续编）并按 §10 触发 MR0/MR1（本计划仅登记，不实施修复；**ORM 类须 ask-first**）。
- 报告产出即更新 `docs/audits/arm-index.md`（新 RC finding 入对应分区；audit reports 表新增 A1.51 行——notify 域首审行；MA1 51 切片全 done 标记）。

## Non-Goals

- **不修复 finding**（修复属 MR0/MR1；本计划是审计）。
- **不修改真相源**（§9 冻结条款——分歧记入报告，不直改 use-cases.md/owner doc/product-scope.md）。
- **不修改代码/ORM/api.xml**（只读审计）。
- **不审计其他域**（logistics/aps 为各自独立 plan）。
- **不重审 P1-MA1-022**（跨域 daoFor 平台一致性维度，resolved，不复审）。
- **不重审 notify 作为被调用方的跨域接线**（多域 notify 接线属既有行为证据复用，本切片从 notify 子系统自身需求契约视角审）。
- **不执行 MA4 运行时探针展开**（本计划只产出静态存疑点清单）。

## Task Route

- Type: `verification or audit work`
- Owner Docs: `docs/audits/requirement-compliance-methodology.md`（§1-§10 + §去重协议 + §4 三判据）+ `docs/backlog/requirement-compliance-roadmap.md`（A1.51 工作项）+ `docs/audits/rc-requirement-baseline-inventory.md:385`（A1.51 UC 锚点）+ `docs/design/notify/use-cases.md`（L1 真相源，0.2 补写）+ `docs/design/notify/README.md`+`inbox-patterns.md`+`docs/architecture/notification-strategy.md`（L2 设计参考，非真相源——Deferred/Non-Goal 标注须 §4 三判据复核）+ `docs/audits/arm-index.md`（finding 衔接）
- Skill Selection Basis: `Skill: docs/skills/multi-dimensional-audit-prompt.md`（roadmap MA1 全部 A1.x 指定）。其必需输入均已就绪。

## Infrastructure And Config Prereqs

- No infra prereqs beyond existing baseline. 审计以读代码/测试/报告为主（纯分析）。若需即时行为确认可跑既有 JUnit（如 `mvn test -pl module-notify/erp-notify-service -Dtest=TestErpSysNotificationDispatch,TestErpSysNotificationTemplateLifecycle,TestErpSysNotificationSubscription,TestErpSysNotificationCrossDomain`）。§8 过程纪律自检需跑 `bash docs/audits/nop-compliance-checker.sh`（纯 reporter，退出码恒 0；本审计无生产代码变更）。

## Execution Plan

### Phase 1 - UC-SYS-01~07 五级追踪矩阵填充与逐 UC 符合性结论

Status: planned
Targets: `docs/audits/2026-08-06-2243-3-rc-ma1-a1-51-notify-full.md`（产出 §1-§5）
Skill: `docs/skills/multi-dimensional-audit-prompt.md`

- Item Types: `Proof | Decision`
- Prereqs: M0.1 + M0.2 done

- [ ] `Proof` 对 UC-SYS-01~07 **逐验收标准一矩阵行**填 L1-L5（§1 格式）：L1 逐字引用 `use-cases.md:28/48/74/98/123/149/174` 验收标准原文；L2 引用 `README.md`+`inbox-patterns.md`+`notification-strategy.md`（标注"设计参考，冲突以 L1 为准"）；L3 引用 `ErpSysNotificationBizModel`#notify/markRead/markAllRead/findUnread/findRead/countUnread + `ErpSysNotificationTemplateBizModel`/`ErpSysNotificationReadBizModel` + `NotificationDispatcher`/`NotificationMergeCoordinator`/`NotificationRecipientResolver`/`NoopEmailSender`/`NoopSmsSender` + grep ErpNotifyConstants/config key/EncryptionHelper/JWT ctx 站点（含行号）；L4 引用 `TestErpSysNotificationDispatch`/`TestErpSysNotificationTemplateLifecycle`/`TestErpSysNotificationSubscription`/`TestErpSysNotificationCrossDomain`/`TestErpSysNotificationSeedTemplates`#method（注明断言强度）；L5 标注无 notify 专属 MA2 报告。
      - Skill: `docs/skills/multi-dimensional-audit-prompt.md`
- [ ] `Proof` 重点核验**候选缺口**（逐条对照）：UC-SYS-01 **subjectTpl/bodyTpl ${var} 插值+按 notificationType 查找+仅 ACTIVE 参与+无 ACTIVE config-gated 静默跳过 WARN 不抛错+DRAFT→ACTIVE**（⚠️待核 P2）；UC-SYS-02 **派发链顺序不可乱序(查找 ACTIVE 模板→渲染→接收人解析→频控合并→站内落库→config-gated 外发)+status=SENT+best-effort(notify 内部 try/catch+失败不回滚+config-gated 静默跳过)**（⚠️待核候选 P1/P2）；UC-SYS-03 **ROLE(角色名→NopAuthRole→NopAuthUserRole→userId)+ORG(deptId→NopAuthUser)+USER_LIST(模板 userId 列表静态+${var} 插值)+PARTNER(partnerId→用户映射[占位 WARN 返回空 config-gated])**（⚠️待核候选 P1/P2）；UC-SYS-04 **合并组 key=notificationType+recipientUserId+频控窗口(业务提醒 5min/异常告警 1min/系统通知不合并 ErpNotifyConstants)+命中 mergeCount+=1 status=MERGED/未命中新建 status=SENT+总开关 erp-notify.merge-enabled 默认 true**（⚠️待核候选 P1/P2）；UC-SYS-05 **markRead 写 Read+markAllRead 批量+唯一键(notificationId,userId)防重复+已读状态派生自 Read 关联(非 status lifecycle)+countUnread 实时统计**（⚠️待核 P2）；UC-SYS-06 **findUnread/countUnread/markAllRead @Optional userId(留空回退 ctx.getUserId() JWT 解析/显式传向后兼容 admin 跨用户+测试)+未读 tab(recipientUserId==userId 且 NOT IN Read)+已读 tab(Read.userId 反查)+全部 tab(拼接去重)+禁用 status=READ filter**（⚠️待核候选 P1/P2）；UC-SYS-07 **站内 IN_APP 默认通道落表+邮件经 nop-integration IEmailSender.sendEmail SPI+短信经 ISmsSender.sendMessage SPI(不新建平行适配层)+config-gated(enabled 默认 true/email-enabled 默认 false/sms-enabled 默认 false)+无供应商/未启用 WARN 跳过不阻断+失败降级 notify 整体 try/catch(模板缺失/渲染失败/接收人解析失败/落库失败均不抛出返回空列表)**（⚠️待核候选 P1/P2）。
      - Skill: `docs/skills/multi-dimensional-audit-prompt.md`
- [ ] `Decision` 按 §2 判据对 UC-SYS-01~07 给出符合性结论（取最高）。UC-SYS-01 倾向**接受/P2**（模板 CRUD+ACTIVE 查找完整，config-gated 跳过待核）；UC-SYS-02 候选 **P1/P2**（§2 ①/⑤ 派发链顺序+best-effort）；UC-SYS-03 候选 **P1/P2**（§2 ①/⑤ 四类 resolver 策略）；UC-SYS-04 候选 **P1/P2**（§2 ①/⑤ 频控窗口+三类通知）；UC-SYS-05 倾向**接受/P2**（markRead+Read 完整，唯一键+countUnread 待核）；UC-SYS-06 候选 **P1/P2**（§2 ①/⑤ @Optional 回退+三态+禁用 status=READ filter）；UC-SYS-07 候选 **P1/P2**（§2 ①/⑤ config-gated 降级+SPI 复用）。每结论须列明命中判据编号 + 三源对照 + §4 三判据复核（**P1 项核 owner doc Deferred/Non-Goal 标注的人工批准痕迹**）+ 触及保护区域标注。
      - Skill: `docs/skills/multi-dimensional-audit-prompt.md`

Exit Criteria:

- [ ] 报告 §1-§5 已落盘：UC-SYS-01~07 矩阵行（逐验收标准进入 L5 判读，7 UC 无跳号），L1 逐字引用、L3 含行号 + grep 站点、L4 注明断言强度、L5 标注无专属 MA2
- [ ] UC-SYS-01~07 有符合性结论且列明 §2 判据编号；P1 项核 Deferred/Non-Goal 标注的人工批准痕迹；触及 ORM 保护区域项显式标注 ask-first

### Phase 2 - finding 登记 / arm-index 衔接 / 静态存疑点 / 过程纪律自检 / 报告完整性

Status: planned
Targets: `docs/audits/2026-08-06-2243-3-rc-ma1-a1-51-notify-full.md`（补 §6-§9）；`docs/audits/arm-index.md`（新 RC finding 入分区）
Skill: `docs/skills/multi-dimensional-audit-prompt.md`

- Item Types: `Decision | Add | Proof`
- Prereqs: Phase 1 完成

- [ ] `Decision` **复用 or 新增 裁决**（§7）：grep `arm-index.md` notify/notification/notificationDispatch/notificationMerge/notificationRecipient/inbox/UC-SYS/templateLifecycle/bestEffort/config-gated 同域同控制点后裁决。执行时 grep arm-index 取最新续编号避免冲突（当前至 P2-RC-072 / P1-RC-082）。禁止未经比对新建。
      - Skill: `docs/skills/multi-dimensional-audit-prompt.md`
- [ ] `Add` 报告 §6 与 arm-index 衔接段：列明每条 finding 复用/新增裁决 + 双向可追溯（finding ID ↔ 修复行预留 MR1）+ **ORM 结构类修复须 ask-first + 独立 plan-audit** + **UC-SYS-07 外发通道 SPI 派发须与 nop-integration 平台 SPI 协调**。
      - Skill: none
- [ ] `Add` 报告 §7 静态存疑点清单（供 MA4 展开）：登记 L5 无法静态定论、需运行时确认的点（逐存疑点一行；**P0 即时通道评估**——本切片无活跃数据破坏候选，notify 为 best-effort 通知子系统，派发失败设计上不破坏业务事实，候选缺口多为功能缺失/边界场景；若频控合并错误致通知丢失或派发链顺序错乱致落库失败吞异常则评估）。
      - Skill: none
- [ ] `Proof` 报告 §8 过程纪律自检段：实际运行 `bash docs/audits/nop-compliance-checker.sh` 附 actual vs baseline 表；closure-audit 独立性声明；与 arm-index 交叉去重声明。**不以 checker 退出码 0 为门控通过依据**（无生产代码变更，注明"无回归风险"）。
      - Skill: none
- [ ] `Add` 报告 §9 与 MA2 报告差异增量声明：无 notify 专属 MA2 报告；列明只补的需求视角差异（模板生命周期/派发链顺序/接收人策略/频控合并/已读状态/收件箱查询/外发降级）。
      - Skill: none
- [ ] `Add` 报告产出即更新 `docs/audits/arm-index.md`：新 RC finding 入 RC 发现追踪分区；audit reports 表新增 A1.51 行（notify 域首审行——7 UC 全覆盖；MA1 51 切片全 done 标记，解除 MA4 A4.1/A4.2 依赖）。
      - Skill: none
- [ ] `Proof` 报告 9 段完整性自检：落盘前自查 §1-§9 全部存在。
      - Skill: none

Exit Criteria:

- [ ] 报告 §6-§9 已落盘，9 段齐全；finding 复用/新增裁决均有 arm-index grep 依据
- [ ] 新 RC finding 已写入 `arm-index.md`；静态存疑点清单已登记（供 A4.1/A4.2 展开）；P0 候选评估有结论（频控合并错误致通知丢失/派发链吞异常致落库失败须评估）
- [ ] §8 自检段含 checker actual vs baseline 实测表 + 独立性 + 交叉去重声明

## Draft Review Record

- Independent draft review iteration 1: `accept`（独立子代理 ses_02d3908a4ffehQ71XonlKP3Nfk，fresh session，未起草本计划）。范围/UC 覆盖（A1.51=UC-SYS-01~07，7 UC 无跳号，0.2 Phase 2 补写 use-cases 解除依赖）/依赖（0.2 done，不依赖 MA2/MA4）/结果表面（只读审计 9 段报告）/方法论（9 段 §6 + §4 三判据 + §5 ask-first[ORM/nop-integration SPI 协调] + §7 reuse + §去重协议）/反 slack/模板全 PASS；load-bearing 引用经实仓复核全部 CONFIRMED TRUE：①use-cases.md UC-SYS-01~07 + 补写纪律声明 ✅；②3 BizModel 全存在 ✅；③ErpSysNotificationBizModel 6 方法(notify@BizMutation:54/markRead@BizMutation:62/markAllRead@BizMutation@Optional:68/findUnread@BizQuery@Optional:74/findRead@BizQuery@Optional:80/countUnread@BizQuery@Optional:106) ✅；④dispatch 组件(NotificationDispatcher/MergeCoordinator/RecipientResolver/NoopEmailSender/NoopSmsSender)全存在 ✅；⑤5 测试全存在 ✅；⑥README.md+inbox-patterns.md 存在 ✅；⑦arm-index 最高 P1-RC-082/P2-RC-072 ✅；⑧baseline-inventory:385 A1.51 一致 ✅；⑨arm-index 无 UC-SYS RC finding（首审切片）✅。INFO（非阻塞）：执行期须确认 P1-MA1-022 是否真为 notify 复用候选（计划已正确列为 Non-Goal 不重审）；"待核"候选缺口执行时须解析为具体 §2 裁决不得保持开放。共识达成，转 active。

## Closure Gates

> 本计划为**只读审计**（无代码/ORM/api.xml/view.xml/真相源变更），故删除完整仓库 `typecheck`/`build`/`lint`/`test` 验证命令门控。验证 = 报告 9 段完整性 + 五级矩阵逐验收标准覆盖 + finding arm-index 衔接 + §8 过程纪律自检 + 独立草案审查 + 文本一致性 + 独立结束审计。

- [ ] 范围内行为完成
- [ ] 相关文档对齐
- [ ] 已运行验证（指定哪些命令；如果需要，针对视觉/UX 域自定义）
- [ ] 无范围内项目降级为 deferred/follow-up
- [ ] 独立草案审查已完成并记录
- [ ] 文本一致性已验证：状态、阶段、门控和日志都一致
- [ ] 结束审计由独立子代理（新会话）执行；执行者未自我审计且未将此留为 `[ ]` 作为人工门控占位符
- [ ] 结束证据存在于文件中

## Deferred But Adjudicated

### finding 的修复实施

- Classification: `out-of-scope improvement`
- Why Not Blocking Closure: 本计划是审计，结果表面 = 报告 + arm-index 登记。finding 的修复按方法论 §10 经 MR0（P0 即时通道）/ MR1（R1.0 展开 RC-R1.n，P1 批量）实施；本切片候选偏差多为**代码逻辑**类（预授权——派发链顺序/resolver 策略/频控窗口/@Optional 回退/config-gated 降级）；**ORM 结构类须 ask-first + 独立 plan-audit**；**UC-SYS-07 外发通道 SPI 派发须与 nop-integration 平台 SPI 协调**。
- Successor Required: yes（MR0/MR1 按本报告 finding 交叉引用展开修复行；UC-SYS-07 须与 nop-integration 平台 SPI 协调）

## Closure

Status Note: <待执行后填充>

Closure Audit Evidence:

- Auditor / Agent: <待独立结束审计填充>
- Evidence: <待填充>

Follow-up:

- MR0/MR1 按 §10 展开本报告 finding 修复（ORM 结构类须 ask-first + nop-integration 平台 SPI 协调）。
- MA4 运行时探针展开 §7 静态存疑点清单。
