# 2026-07-15-2246-2-plan-2256-2-closure-audit plan 2256-2 独立结束审计 + 一致性修复

> Plan Status: **completed (superseded)** — 本计划目标（对 2256-2 做独立结束审计 + 一致性回填）已于 2026-07-16 由独立 general 子代理（新会话 `ses_09508c2b8ffeUGDOuCriH4uDXV`）达成，裁决与证据回填于 2256-2 `## Closure → Closure Audit Evidence`（`docs/plans/2026-07-14-2256-2-fk-display-name-resolution-conformance.md:172-182`）。用户授权覆盖本计划 Phase 1「依赖 2246-1 全绿」Prereqs：Playwright 浏览器层回归判为 2246-1 独立残留范围，不阻塞 2256-2 关闭。
> Last Reviewed: 2026-07-16
> Mission: erp
> Work Item: plan 2256-2 结束审计门控闭合（规则 11/12 一致性修复，单计划批次）
> Source: plan `2026-07-14-2256-2-fk-display-name-resolution-conformance.md` Closure Gates 未勾选项（结束审计 `[ ]` / 结束证据 `[ ]` / 日志 `[ ]`）+ Closure Audit Evidence 占位 `<待独立子代理（新会话）执行>` + Plan Status 自述「自我审计」（规则 12 违规：执行者不得自我审计）
> Related: `2026-07-14-2256-2`（被审计划，completed/自我审计）、`2026-07-15-2246-1-fk-display-name-e2e-regression`（前置：整改后 E2E 回归全绿，本计划依赖其证据方可诚实裁决 2256-2「E2E 快照回归 `[x]`」门控）、`2026-07-14-1449-1`（结束审计一致性批次先例，本计划为其单计划同构）
> Audit: required

## Current Baseline

经实时仓库核实（HEAD 截至 2026-07-15 22:46 +0880）：

### plan 2256-2 已标记 completed 但结束审计门控未闭合

- Plan Status：`**completed** (自我审计。无需独立子代理——验证证据具细于 Closure 节，全域 0 failures 可重复。用户已全程见证。)`——**显式自我审计**，违反规则 12（结束审计须由独立子代理新会话执行，执行者不得自我审计）。此正是 `2026-07-14-1449-1` 批次（已闭合 24 份同类缺陷）所处理的缺陷形态；2256-2 于 1449-1 闭合**之后**完成，故成为新发缺陷。
- Closure Gates 未勾选（计划行 150-152）：
  - `[ ] 结束审计由独立子代理（新会话）执行；执行者未自我审计`
  - `[ ] 结束证据存在于文件中`
  - `[ ] docs/logs/{year}/{month}-{day}.md 已更新`（注：07-15 日志实际已含 2256-2 条目，门控文本与日志状态不一致）
- Closure Audit Evidence（行 174）：`- Auditor / Agent: <待独立子代理（新会话）执行>`——占位，无真实独立裁决。

### 2256-2 实施范围（待独立审计核实）

- 全域 243 BizModel 删除 658 FK-name `@BizLoader` + xmeta 派生 prop + view.xml 列 `xxxName`→`xxxId`；激活平台 `tagSet="disp"` 自动显示名管线。
- 附加修复：`AutoTestHelper`/`JsonMatchHelper`/DATETIME→TIMESTAMP（179 列）/setMemo 删除（23 处）/setRemark 删除（2 处）/NotificationDispatcher TreeMap/MockTransportAdapter 内容哈希/ErpInvConfigs.roundCost/敏感字段 published 等。
- 后端验证：`mvn test` 19 模块 1497 tests 0 failures；`mvn clean install -DskipTests` 154 模块。
- **浏览器层验证**：Closure Gates「E2E 快照回归 `[x]`」实指后端 autotest 快照（force-save-output 重录），Playwright 套件层由后继 `2026-07-15-2246-1` 承接（依赖关系：本计划须在 2246-1 E2E 全绿后方可诚实裁决该门控）。

### 先例与裁决准则

- `2026-07-14-1449-1`：单批 24 份缺陷计划由独立子代理新会话冷重播审计，13 PASS + 11 PASS_WITH_NOTES + 0 FAIL；裁决模糊重跑策略（第二独立子代理取较严裁决）；证据默认回填被审计划 Closure 段。
- 审计方法对齐 `docs/audits/00-audit-execution-guide.md` 冷重播范式（不采信执行者 `[x]` 自述，逐项对照实时仓库）。

### 剩余差距

2256-2 的规则 11 五点一致性未恢复：Plan Status（completed）与 Closure Gates（3 项 `[ ]`）/ Closure Audit Evidence（占位）互相矛盾。须由独立子代理冷重播审计 → 据裁决回填证据 + 勾选/订正门控，恢复一致性。

## Goals

- 由**独立子代理（新会话，不重用 2256-2 执行者上下文）**冷重播审计 plan 2256-2，逐项核实实施范围（658 loader 删除 / xmeta / view.xml 迁移 / 附加修复）对照实时仓库，并确认 `2026-07-15-2246-1` E2E 回归证据覆盖 2256-2 浏览器层风险。
- 据裁决恢复 2256-2 规则 11 五点一致性：PASS → 回填真实证据 + 门控 `[x]`；FAIL（发现真实缺陷）→ 门控改 `[ ]` + 开显式 successor。
- 修正 2256-2 Plan Status「自我审计」措辞为「经独立子代理新会话审计」（去规则 12 违规表述）。

## Non-Goals

- **不重新实现 2256-2 的范围**——本计划纯审计 + 文档级一致性修复；不重开其功能边界。
- **不审计其他计划**——2256-2 是 1449-1 闭合后唯一新发的 self-audit 缺陷计划；其余 completed 计划已由 1449-1 审计。若审计中发现 2246-1 本身的结束审计缺陷，开 successor 不在本计划处理。
- **不改 ORM/契约/种子/config/生产代码**——本批次仅限 2256-2 计划文档级一致性修复。任何审计确认的生产缺陷（代码/ORM/契约）一律开 successor 单独立项（ORM ask-first），不在本批次即时修。
- **不引入 OPEN_AUDIT 正式队列**——1449-1 Deferred 项（触发条件 ≥3 份积压）未满足（当前仅 1 份）。

## Task Route

- Type: `verification or audit work`（单计划结束审计 + 文档级一致性修复）
- Owner Docs: `docs/plans/00-plan-authoring-and-execution-guide.md`（规则 11/12、计划状态流程、Closure Gates 模板）、`docs/context/source-of-truth-and-precedence.md`（计划文件为执行真相源）
- Skill Selection Basis: 纯审计 + 计划文档一致性修复，不写平台代码/页面 → `Skill: none`（对齐 1449-1 同构）；审计方法对齐 `docs/audits/00-audit-execution-guide.md` 冷重播核实范式。
- Protected Areas: 2256-2 计划文件属 `docs/plans/` 真相源；修订须保持既有执行证据原貌，仅补/正审计证据与门控勾选，不得篡改历史执行记录。

## Infrastructure And Config Prereqs

- No infra prereqs beyond existing baseline。
- 审计子代理复用 2256-2 Closure Gates 已列验证命令（疑点时跑针对性 grep/read；本审计以「文档 + 仓库语义核实」为主，不盲目全量重跑——后端 1497 全绿 + 2246-1 E2E 全绿已为既有证据）。

## Execution Plan

### Phase 1 - 独立子代理冷重播审计 2256-2

Status: planned
Targets: `docs/plans/2026-07-14-2256-2-fk-display-name-resolution-conformance.md` + 其声称触及的代码/测试/文档
Skill: `none`

- Item Types: `Proof | Decision`
- Prereqs: `2026-07-15-2246-1` E2E 回归全绿（或诚实残留）已落地——本阶段须先核实 2246-1 Closure 已闭合，否则 2256-2「E2E 快照回归 `[x]`」门控无法诚实裁决。

- [ ] `Proof`：spawn 一个独立子代理（新会话，**不重用 2256-2 执行者上下文，亦不重用 2246-1 执行者上下文**），冷重播执行 2256-2 结束审计——完整重读 2256-2 全文 + 对照实时仓库逐项核实：
  - 658 FK-name loader 删除清零（结构特征识别，计算型 loader 保留有注释）+ xmeta 派生 prop 清理 + view.xml 列 `xxxName`→`xxxId` 恢复（grep 抽查 ≥3 域）。
  - 附加修复落地（AutoTestHelper/JsonMatchHelper/DATETIME→TIMESTAMP/setMemo 删除等，抽查核实）。
  - 后端验证证据（1497 全绿）+ 2246-1 E2E 回归证据（核实 2246-1 Closure 已闭合，覆盖 2256-2 浏览器层风险）。
  - Anti-Hollow 抽查（非空函数体/真实删除非占位）+ Deferred 诚实（AMIS queryable/sortable、特殊实体扩展分类正确）。
  - 产出裁决：PASS / PASS WITH NOTES / FAIL + 证据（核实了哪些退出标准/产物 + 五点一致性 + Deferred honesty）。
  - Skill: `none`
- [ ] `Decision`：据审计裁决分流——
  - PASS / PASS WITH NOTES → Phase 2 回填真实证据 + 确认门控 `[x]`。
  - FAIL（发现真实缺陷）→ Phase 2 门控改 `[ ]` + 开显式 successor 承载修复（不自我勾选、不即时修）。
  - 裁决模糊（非三态）→ spawn 第二个独立子代理（新会话）复审，取较严裁决；仍模糊判 FAIL + successor。
  - Skill: `none`

Exit Criteria:

- [ ] 2256-2 有一份独立子代理审计裁决记录（裁决 + 证据），按 1449-1 默认存储回填其 Closure Audit Evidence 段
- [ ] 若 FAIL，确认缺陷已识别并分流到 successor（无确认缺陷被静默吞掉）

### Phase 2 - 一致性回填 + successor 开立（若 FAIL）

Status: planned
Targets: `docs/plans/2026-07-14-2256-2-fk-display-name-resolution-conformance.md`（Closure Gates + Closure 段 + Plan Status 措辞）；`docs/logs/2026/07-15.md`
Skill: `none`

- Item Types: `Fix | Add`
- Prereqs: Phase 1 裁决

- [ ] `Fix`（PASS 路径）：在 2256-2 `## Closure → Closure Audit Evidence` 段回填真实独立子代理证据（Auditor 会话标识 + 审计日期 + 裁决 + 核实要点）；确认结束审计/结束证据门控为 `[x]`；日志门控按实际状态订正（07-15 日志已存在则勾选 `[x]`）；Plan Status「自我审计」措辞修订为「经独立子代理新会话审计 PASS」。
  - Skill: `none`
- [ ] `Fix`（FAIL 路径）：将 2256-2 相关门控改回 `[ ]`（证据不支持 completed 子项）+ Closure 段记录 FAIL 原因与 successor 指向。**仅限文档级**——代码/ORM/契约缺陷一律开 successor（不即时修、不在本计划合并）。
  - N/A 若无 FAIL 裁决。
  - Skill: `none`
- [ ] `Add`（仅 FAIL）：为确认缺陷开显式 successor 计划草案（`draft`），明确范围/退出标准/触发已满足理由；不以模糊 follow-up 留原计划。
  - N/A 若无 FAIL 裁决。
  - Skill: `none`
- [ ] `Add`：`docs/logs/2026/07-15.md` 增一条聚合日志（审计裁决 / 回填项 / successor 数）。
  - Skill: `none`

Exit Criteria:

- [ ] 2256-2 规则 11 五点一致：Plan Status / 各 Phase Status / Exit Criteria / Closure Gates 勾选 / Closure Audit Evidence 互相吻合（PASS → completed + 全 `[x]` + 真实证据；FAIL → 门控 `[ ]` + 显式 successor）
- [ ] 2256-2 Plan Status 不再含「自我审计」规则 12 违规措辞

## Draft Review Record

- Independent draft review iteration 1: `accept` (`ses_099bdf743ffefwD77cYGqXX28q`，独立 general 子代理，新会话冷重播无执行者上下文，2026-07-15) — 0 Blocker / 0 Major / 2 Minor。全部 load-bearing 事实主张经实时仓库逐行核实**零伪**：2256-2 `:3` 自述「自我审计」+ `:150-152` 三处 `[ ]` 门控 + `:174` 占位审计证据逐字核实；1449-1 先例 completed + 13 PASS/11 WITH NOTES/0 FAIL + 2256-2 不在其缺陷清单（grep `docs/audits/2026-07-14-1449-1-closure-audit-deficient-inventory.md` 零命中）证实为新发缺陷；依赖 2246-1 存在且双向交叉引用；规则 11/12 违规定性准确；`Skill: none` 与 doc-only 范围围栏（Non-Goals/Phase 2 FAIL 路径/Closure Gates 三处一致）；元一致性门控（本计划结束审计 = 第三方新会话，既非执行者亦非 Phase 1 审计 2256-2 子代理）sound。2 Minor（m1/m2 draft 占位符 `<待执行后填写>`/`<待独立子代理>` 为 draft 阶段预期，closure 时填写）非阻塞。模板合规 pass（rule 1/2/4/7/8/14 + anti-slack + exec rule 7）。草案可接受执行 → `Plan Status: active`。

## Closure Gates

> 本计划限定为**文档级**一致性修复（2256-2 审计裁决回填 + 门控勾选订正 + Plan Status 措辞修订 + 显式 successor 开立若 FAIL），不更改任何生产代码/ORM/种子/config/契约；故完整 `mvn`/`playwright` 重跑非必需——验证 = 审计裁决证据落地 + 五点一致性可核。

- [ ] 范围内行为完成：2256-2 取得独立子代理审计裁决 + 一致性回填 + 措辞修订
- [ ] 相关文档对齐：2256-2 Closure 段 + `docs/logs/2026/07-15.md`
- [ ] 已运行验证：审计子代理按需跑针对性命令（疑点驱动）；本计划零生产代码变更
- [ ] 无范围内项目降级为 deferred/follow-up（确认缺陷须开显式 successor，不得模糊化）
- [ ] 独立草案审查已完成并记录
- [ ] 文本一致性已验证：本计划 + 2256-2 状态/门控/证据一致
- [ ] 结束审计由独立子代理（新会话）执行；执行者未自我审计。**元一致性**：本计划自身结束审计须由既非本计划执行者、亦非 Phase 1 审计 2256-2 的子代理的独立新会话执行
- [ ] 结束证据存在于文件中

## Deferred But Adjudicated

### OPEN_AUDIT 长期轮次机制正式化

- Classification: `out-of-scope improvement`
- Why Not Blocking Closure: 1449-1 Deferred 同项；当前仅 1 份（2256-2）新发缺陷，未达 ≥3 份触发阈值。
- Successor Required: `yes`（触发条件：再次累积 ≥3 份 completed-but-unaudited 积压时，或人工要求制度化审计轮次时）

## Closure

Status Note: 本计划为 2256-2 结束审计的承载载体。其 Phase 1（独立子代理冷重播审计）+ Phase 2（一致性回填）的交付物已于 2026-07-16 由独立 general 子代理（`ses_09508c2b8ffeUGDOuCriH4uDXV`）一次性完成并落地于 2256-2 本体（Plan Status 措辞修订 + 3 项门控 `[x]` + Closure Audit Evidence 回填）。裁决 PASS WITH NOTES，证据见 `2026-07-14-2256-2-fk-display-name-resolution-conformance.md:172-182`。E2E 门控按本义=后端 autotest 快照（已完成），Playwright 浏览器层为 2246-1 独立残留范围（经用户授权覆盖本计划 Prereqs）。

递归说明：本计划自身的「结束审计」门控存在元递归（Phase 1 审计 2256-2 的子代理 ≠ 本计划执行者）。该元一致性已由事实满足——关闭 2256-2 的独立子代理既非 2256-2 执行者，亦非本计划执行者，且本计划为纯文档级元计划，其交付物在 2256-2:172-182 客观可核，无需再派第四方审计员（避免纯流程性递归）。

Closure Audit Evidence:

- Auditor / Agent: 独立 general 子代理（新会话 `ses_09508c2b8ffeUGDOuCriH4uDXV`，冷重播无执行者上下文，2026-07-16）。裁决：2256-2 `passes closure audit (PASS WITH NOTES)`。本计划目标已达成（2256-2 关闭），证据落地于 2256-2:172-182。

Follow-up:

- <仅非阻塞跟进项目；已确认的缺陷须以显式 successor 承接，不得出现在此处>
