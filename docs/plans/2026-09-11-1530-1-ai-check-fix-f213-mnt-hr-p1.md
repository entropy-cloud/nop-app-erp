# 2026-09-11-1530-1 ai-check F2.13 mnt+hr P1 簇修复（hr2-001..005 五条 P1 修复 + mnt/hr 已修在位裁决）

> Plan Status: completed
> Last Reviewed: 2026-09-11
> Source: `docs/backlog/ai-check-roadmap.md` Work Item F2.13（mnt-001/002/003；hr-001；hr2-001..005）；findings 详情 `docs/audits/check/ck-maintenance.md` + `ck-hr-org.md` + `ck-hr-attendance-payroll.md`；索引 `docs/audits/check/ai-check-index.md`
> Related: `docs/plans/` r1 已闭合批 F1.1-F2.12（同方法基线 F0.2）；r3 复核注记（`docs/audits/check/ai-check-index.md` L727/L743/L752——mnt-001/002/003 已修在位实证 + hr-001 复用注记）
> Audit: required（独立草案审查 + 独立结束审计；r3 M2.9 后子代理通道已恢复，不再使用人工批准替代）

## Current Baseline

（2026-09-11 HEAD `120cebea8` 实仓逐一复核）

**已修在位（本计划只裁决 + 回填，不重复修复）：**

- `P1-CK-mnt-001/002/003`：索引已回填 `fixed`（M2.9 清账，r3 M1.15 HEAD `e331c55b2` 实证 validateNotConfirmed 接线状态机 / reverseConfirm rethrow / restoreToRunning 前置守卫）。本计划 Phase 1 在当前 HEAD 复验三处代码证据仍在即裁决闭合。
- `P1-CK-hr-001`（部门/职位删除无引用守卫）：r3 M1.10（HEAD `f40b4bbae`）复核修复在 HEAD 有效（defaultPrepareDelete 5 守卫 + 5 错误码 + `TestErpHrDepartmentPositionDeleteGuard` 8 用例绿），但索引行仍 `open`（回填缺口注记在案，M2.9 六行清账未含此行）。本计划复验后回填 `fixed`。

**待修复（五条 open P1，实仓复核确认现症全部在位）：**

- `P1-CK-hr2-001`：`PayrollCalculator.java` L43 `DEFAULT_REQUIRED_WORK_DAYS = 22` 硬编码；`summarizeAttendance` L307 无条件 `requiredDays = 22`；actualDays = 非缺勤打卡数，带薪假（ANNUAL 等，`AbstractErpHrShiftProcessor#upsertAttendanceForLeave` L79 写 `isAbsent=true`）被当缺勤折减；`deductUnpaidLeave=true` 时 SICK/PERSONAL 缺勤折减 + L78-82 显式扣减双扣；零打卡兜底=全勤与「打 20 天卡」口径矛盾。owner doc `payroll.md §5.2`「出勤比例 = 实际出勤日 / 应出勤日」应出勤日语义未落地。
- `P1-CK-hr2-002`：`ErpHrSalaryGenerateBankFileProcessor.generateBankFile`（L25-65）批量翻 PAID 全方法零 `postingDispatcher` 调用——银行文件路径 280 发放凭证缺失（对照 `ErpHrSalaryMarkPaidProcessor` L40 单笔路径有 `tryPostPayment`）。owner doc `payroll.md §5.2` 步骤 4 明确银行文件路径含过账；§7.2 文件格式「序号｜账号｜户名｜金额｜用途」实现为「序号,employeeId,净额,SALARY」缺账号/户名。
- `P1-CK-hr2-003`：`ErpHrSalaryRunPayrollProcessor.runPayroll`（L22-34）循环体无 try/catch——任一员工缺合同（`ERR_EMPLOYMENT_CONTRACT_NOT_FOUND`）/社保基数/税务配置即整批回滚，与 `use-cases.md` UC-HR-04「员工缺失合同或薪资配置时跳过并告警」明确分歧。
- `P1-CK-hr2-004`：`SocialInsuranceCalculator.findBase`（L87-94）零有效期过滤/排序（多行基数任取）；`findConfigs`（L96-101）仅 eq cityCode（同城同险种多行全量返回→重复计扣）；`findHousingFundConfig`（L103-110）取第一行无期限过滤。ORM `ErpHrSocialInsuranceBase/Config` 均含 `effectiveFrom/effectiveTo` 列（owner doc `payroll.md §2.2/§2.3` 年调语义）。
- `P1-CK-hr2-005`：`ErpHrSalarySimulationBizModel.readSalaryField`（L515-539 switch 仅 8 输入项 + `default: ZERO`）+ `AbstractErpHrSalarySimulationProcessor.readSalaryField`（L150-173 同副本）对照 `SALARY_ITEM_CODES`（L546-550，13 项含 `grossSalary/socialInsurance/housingFund/taxAmount/netSalary` 5 派生字段）——`getComparison`（L131-165）/`getProjectSummary` 等对 5 派生行恒返回 0，diff = −当期实际值。

**关联在位事实（消费不重做）：**

- `P2-CK-hr2-006` tryPostPayment 去重守卫已由 F1.2 修复在位（`SalaryPostingDispatcher.java` L171-178 dedup guard）——hr2-002 接线过账时直接复用，无需补守卫（r3 M1.11 复核「复用 2」在案）。
- 验证基线：`known-good-baselines.md` `ai-check-r3-mv1` 行（全 reactor `mvn test` 4084/0/0/1 + install 156/156 + compliance R2c=1543 / CJK `--strict` PASS / i18n checker PASS）。
- C17（`TestErpC17HrSalaryPayment`）2026-7 runPayroll 全员工已配置（seedPayrollEnvironment 自包含）；无考勤种子 → 全勤兜底路径，ratio=1，金额锚点（gross 15000/1200/1800/210/11790）对 hr2-001 分母变更**不敏感**；但 `requiredWorkDays` 持久化字段 22→23（2026-07 周一至周五 23 天）→ C17 层 2/3 快照良性漂移 → **快照重录义务**（M1.2a1 C13 先例）。
- 种子/测试数据面（hr2-004 过滤行为不变性前提）：init-data CSV `erp_hr_social_insurance_base.csv`（effectiveFrom=2026-01-01，effectiveTo 空）与 `erp_hr_social_insurance_config.csv`（SHANGHAI，effectiveFrom=2026-01-01）全部落在 2026-07 有效区间；C17（`TestErpC17HrSalaryPayment.seedSocialConfig` L377-387）与 module-hr 测试种子（`TestErpHrPayrollEngine.seedSocialInsuranceConfig` 等）的 config 行 effectiveFrom/effectiveTo 均 **null**（开区间语义必须命中——见 Phase 3 Decision 的 null 分支）。
- 快照重录面（hr2-001 分母变更的传播盘点）：除 C17（2026-07 → 23）外，module-hr `erp-hr-service/_cases/` 下 JUnitAutoTestCase 录制快照亦含引擎落库 salary 行（如 `TestErpHrPayrollEngine/testSocialInsuranceBaseClampingAndGrossNet`、`testHighTaxBracketIntegrationE2e`、`TestErpHrPayrollSimulation/testConvertToFormalDuplicateConflict` 等的 `output/tables/erp_hr_salary.csv` 含 `ACTUAL_WORK_DAYS/REQUIRED_WORK_DAYS` 列）——凡核算月份工作日数 ≠ 22 的案例（2026-05=21、2026-07=23）快照必漂移，Phase 7 统一盘点重录（2026-06=22 案例不漂移）。

## Goals

- F2.13 九条 finding 全部到达终态 `fixed`：mnt-001/002/003 + hr-001 复验裁决 + 索引回填；hr2-001..005 先失败测试后修复，测试与提交指针回填索引。
- hr2-001：应出勤日按月推导（config 兜底）+ 带薪假豁免折算 + 无薪假防双扣。
- hr2-002：银行文件路径接线 280 过账（复用 F1.2 去重守卫）+ 文件格式对齐 §7.2。
- hr2-003：runPayroll 逐员工失败隔离 + 跳过告警（UC-HR-04 语义落地）。
- hr2-004：社保基数/配置读取按有效期过滤 + 同险种多行取最新。
- hr2-005：readSalaryField 双副本补 5 派生字段（共享单源收敛）。
- owner docs（payroll.md / shift-scheduling.md / payroll-simulation.md）实现注记对齐；roadmap F2.13 → done。

## Non-Goals

- F2.14 / F2.15 / F3.x（后续批，独立 plan）。
- `P3-CK-hr2-019` 五处死副本全量收敛——本计划仅收敛 readSalaryField 一处（hr2-005 必需面），其余副本归 F3.5 批。
- `P2-CK-hr2-006`——F1.2 已修在位，仅复验。
- 银行文件按员工工资卡所在银行分组（`payroll.md §7.3`「按银行分组」）——现实现 bankId 入参即目标银行单文件语义，分组重构归 Deferred（见 Deferred But Adjudicated）。
- SICK 病假工资折算（中国实务部分有薪）——维持「视 SICK/PERSONAL 为无薪假」既有 documented simplification（javadoc 在案），仅修双扣。
- 法定节假日工作日历——工作日推导仅按周一至周五，节假日调休不建模（owner doc 注记 documented simplification）。
- `generateBankFile` 文件内容列变化的下游消费面改造（现无消费方断言具体列序，E2E 仅 negative-path 守卫）。

## Task Route

- Type: `bug investigation` + `implementation-only change`（含测试与文档）
- Owner Docs: `docs/design/human-resource/payroll.md`（§2.2/2.3/2.4/5.2/7.2/7.3/9.1）、`shift-scheduling.md` §6.1、`payroll-simulation.md` §3.2、`use-cases.md` UC-HR-04（义务源，不改）
- Skill Selection Basis: 修复为 hr 域 BizModel/Processor/Calculator 行为缺陷 → `nop-backend-dev`（平台反模式表 + BizModel 模式）；测试策略 → `nop-testing`；收官门控 → `closure-audit-prompt`（独立子代理）

## Infrastructure And Config Prereqs

- 无新增基础设施。新增 config 键 `erp-hr.required-work-days`（int，默认 0 = 按月工作日自动推导；>0 = 覆盖）沿 `ErpHrConfigs` 静态 reader 范式（`CONFIG_REQUIRED_WORK_DAYS` 常量入 `ErpHrConstants`）。
- C17 快照重录需 `app-erp-all` 集成测试 RECORDING 模式（既有机制，`SnapshotTest.RECORDING` 先例）。
- 回滚策略：单 commit 承载全部变更，`git revert` 即回滚；C17 快照重录文件单独目录（`app-erp-all` 测试资源）随 revert 还原。

## Execution Plan

### Phase 1 — 已修在位裁决（mnt-001/002/003 + hr-001）与索引回填

Status: completed
Targets: `docs/audits/check/ai-check-index.md`
Skill: none

- Item Types: `Proof`（4 条）+ `Fix`（索引回填 1 处）

- [x] Proof: HEAD 复验 mnt-001（validateNotConfirmed 接线状态机）/ mnt-002（reverseConfirm rethrow）/ mnt-003（restoreToRunning 前置守卫）三处代码证据仍在（grep + file:line 记录于本计划 Execution Record），索引三行维持 `fixed`
      - Skill: none
- [x] Proof: HEAD 复验 hr-001 修复在位（`ErpHrDepartment/Position` defaultPrepareDelete 守卫 + `TestErpHrDepartmentPositionDeleteGuard` 全绿——`mvn test -pl module-hr/erp-hr-service -Dtest=TestErpHrDepartmentPositionDeleteGuard`）
      - Skill: none
- [x] Fix: `ai-check-index.md` L434 `P1-CK-hr-001` 行 `open` → `fixed`（附 r3 M1.10 复核注记 + 本计划 HEAD 复验指针，lesson-11 回填缺口清账）
      - Skill: none

Exit Criteria:

- [x] 三条 mnt finding 复验证据（file:line）落本计划 Execution Record；hr-001 索引行回填 `fixed` 带证据指针

### Phase 2 — hr2-005 readSalaryField 5 派生字段（最小面先行）

Status: completed
Targets: `module-hr/erp-hr-service/src/main/java/app/erp/hr/service/entity/ErpHrSalarySimulationBizModel.java`、`app/erp/hr/service/processor/AbstractErpHrSalarySimulationProcessor.java`
Skill: `nop-backend-dev`

- Item Types: 混合（1 Decision + 1 Fix + 2 Proof，逐项标注）

- [x] Decision: 派生字段读取单源化——在 `AbstractErpHrSalarySimulationProcessor` 增加 `public static BigDecimal readSalaryItem(ErpHrSalary, String)` 单一 switch（8 输入项 + 5 派生项），两处实例 `readSalaryField` 委托之。替代方案：（a）两副本各自补 case（留漂移隐患，P3-CK-hr2-019 已证明副本分叉先例）；（b）BizModel 继承合并（两者分别 extends `AbstractErpCrudBizModel` / 独立基类，层次不容）。残留风险：无（纯委托重构，调用点签名不变）。
      - Skill: `nop-backend-dev`
- [x] Fix: 双副本 switch 补 `grossSalary/socialInsurance/housingFund/taxAmount/netSalary` 5 case（经单源）
- [x] Proof: 先行失败测试——`TestErpHrPayrollEngine`（或新建 `TestErpHrSalarySimulationComparison`）：构造全字段 `ErpHrSalary`，断言 13 项 `SALARY_ITEM_CODES` 读取值逐一非零且等于实体字段（修复前 5 派生项断言红）；修复后绿
      - Skill: `nop-testing`
- [x] Proof: 核对 `module-hr/erp-hr-web/.../payroll-simulation-comparison.page.yaml` 对比页消费列（5 派生行若被页面消费则记录用户面闭合；若不消费记录后端单侧闭合），结论写入 Execution Record
      - Skill: none

Exit Criteria:

- [x] 新增测试先红后绿；`mvn test -pl module-hr/erp-hr-service -Dtest=TestErpHrPayrollEngine`（含既有 10 方法）全绿；对比页消费面结论落 Execution Record

### Phase 3 — hr2-004 社保有效期过滤

Status: completed
Targets: `module-hr/erp-hr-service/src/main/java/app/erp/hr/service/payroll/SocialInsuranceCalculator.java`
Skill: `nop-backend-dev`

- Item Types: `Decision` + `Fix` + `Proof`

- [x] Decision: 有效期裁决口径——核算时点 asOf = 核算月月末（periodEnd）；命中条件 **`(effectiveFrom == null || effectiveFrom <= asOf) && (effectiveTo == null || effectiveTo >= periodStart)`**（双侧 null = 开区间，必须显式含 `effectiveFrom == null` 分支——实仓 C17/module-hr 测试种子的 config 行该列全为 null，字面 SQL NULL 比较不命中会把全部测试配置滤掉）；同险种多行命中取 `effectiveFrom` 最新（null 视为最旧）；`findBase` 加同过滤 + `orderBy effectiveFrom desc, id asc` 确定性取最新。理由：owner doc `payroll.md §2.2`「年调 7 月」多期间行并存语义；替代方案（写入侧互斥校验）不解决存量多行、且 README 已登记为后续 follow-up。残留风险：跨期间边界（effectiveTo = periodStart 前一日）行不再命中——语义正确（已失效）。
      - Skill: `nop-backend-dev`
- [x] Fix: `findBase`/`findConfigs`/`findHousingFundConfig` 落地上述过滤（`calculate` 对 configs 按险种去重取最新）
- [x] Proof: 先行失败测试（`TestErpHrPayrollEngine` 新增方法）：① 双基数行（2025 行 + 2026 行）→ 取 2026 行（修复前任取）；② 同城同险种两有效配置行 → 只计一行（修复前双倍扣缴）；③ 已失效配置行（effectiveTo < periodStart）→ 排除；④ 回归前提——base 行 effectiveFrom=2026-01-01（CSV 与测试种子）与 config 行双列 null（C17/module-hr 测试种子）在过滤后行为不变（修复前后同值）。修复后全绿
      - Skill: `nop-testing`

Exit Criteria:

- [x] 新增测试先红后绿；`mvn test -pl module-hr/erp-hr-service` 全绿（含 `TestErpHrPayrollEngine` 既有金额锚点零漂移）

### Phase 4 — hr2-001 出勤折算重做

Status: completed
Targets: `module-hr/erp-hr-service/src/main/java/app/erp/hr/service/payroll/PayrollCalculator.java`、`ErpHrConfigs.java`、`ErpHrConstants.java`
Skill: `nop-backend-dev`

- Item Types: `Decision` + `Fix` + `Proof`

- [x] Decision: 应出勤日口径——`erp-hr.required-work-days` config >0 时用之；否则按核算月周一至周五天数推导（无节假日日历，documented simplification 在 owner doc 注记）。替代方案：（a）排班天数聚合（排班数据稀疏域不成立 + 种子耦合面大）；（b）固定 21.75 法定均值（不修「短月少发」主缺陷）。残留风险：节假日多的月份应出勤日略偏高（调休补班不建模）——config 可覆盖。
      - Skill: `nop-backend-dev`
- [x] Decision: 带薪假集合 = `{ANNUAL, MARRIAGE, MATERNITY, FUNERAL, COMPENSATORY}`（dict `erp-hr/leave-type` 七值中扣除无薪 `{SICK, PERSONAL}`；`shift-scheduling.md §6.1`「年假有薪」+ 婚/产/丧/调休中国实务有薪）。带薪假天数（APPROVED 休假 durationDays 求和）计入 actualDays 豁免折算。残留风险：SICK 病假工资折算不建模（Non-Goal 在案）。
      - Skill: `nop-backend-dev`
- [x] Decision: 豁免/加回量口径——统一取 `ErpHrLeaveRequest.durationDays` 求和（镜像 `sumUnpaidLeaveDays` 的 APPROVED + 期间完全包含过滤 `ge(startDate)/le(endDate)`），**不以考勤缺勤行数计**。显式假设：`durationDays` 视为工作日口径（与排班缺勤行粒度一致；若按日历天录入，带薪豁免可能多补，由 cap 到 requiredDays 兜底不超全额）。替代方案（按期间内 `isAbsent && leaveRequestId 非空` 考勤行数计）可精确对齐缺勤折减粒度，但排班未落考勤的已批准假期（无行）将被排除在豁免外——惩罚无排班员工，否决。残留风险（登记）：① 跨核算期假期（startDate/endDate 未完全落在期间内）不参与豁免/加回——其期内缺勤行仍按缺勤折减（config off 无薪路径不变；config on 显式扣减不加倍）；② durationDays 含休息日的录入会把带薪豁免多补至 cap。两者均为 documented simplification，owner doc 注记。
      - Skill: `nop-backend-dev`
- [x] Decision: 防双扣——`deductUnpaidLeave=true` 时无薪假天数从缺勤折减中豁免（actualDays 加回 unpaidLeaveDays），仅由显式 deductionRatio 扣一次；=false 时维持缺勤即折减现状（默认路径向后兼容）。数学自洽性：加回与显扣在同一天数口径（durationDays）上恒等相消，净效果 = present/required（与考勤行是否存在无关）。替代方案（显式扣减分支里不再豁免缺勤）需区分考勤行是否休假产生（N+1 查询），否决。
      - Skill: `nop-backend-dev`
- [x] Fix: `summarizeAttendance` 计算并返回 derived requiredDays + paidLeaveDays；`calculate` 组装 actualDays（present + paidLeave + config-on 时 unpaid）并 cap 到 requiredDays；零打卡兜底=全勤维持（新员工语义，owner doc 注记口径）；config 键 + reader 落地
- [x] Proof: 先行失败测试（`TestErpHrPayrollEngine` 新增 4 方法）：① 短月（2026-02，20 个工作日）20 天全勤 → ratio=1（修复前 20/22≈0.909 少发）；② 带薪假豁免（2026-07：18 打卡 + 5 天 ANNUAL APPROVED → ratio=1 全薪；修复前 18/23 折减）；③ 防双扣（config on：17 打卡 + 5 天 SICK → basicSalary = 月薪×17/23 精确断言；修复前 = 月薪×12/22 双扣）；④ config 覆盖（`erp-hr.required-work-days=22` → 2026-07 requiredWorkDays=22）。修复后全绿
      - Skill: `nop-testing`

Exit Criteria:

- [x] 新增测试先红后绿；module-hr `_cases` 快照盘点完成——凡 `output/tables/erp_hr_salary.csv` 含核算月份工作日数 ≠ 22 的案例（2026-05=21 / 2026-07=23 等）经 RECORDING 重录 + CHECKING 复跑绿（金额锚点必须逐值不变，仅 ACTUAL/REQUIRED_WORK_DAYS 天数列漂移为良性），重录清单落 Execution Record
- [x] `mvn test -pl module-hr/erp-hr-service` 全绿；`requiredWorkDays` 派生值持久化字段随 salary 落库（`setRequiredWorkDays`）

### Phase 5 — hr2-002 银行文件 280 过账 + 文件格式对齐

Status: completed
Targets: `module-hr/erp-hr-service/src/main/java/app/erp/hr/service/processor/ErpHrSalaryGenerateBankFileProcessor.java`
Skill: `nop-backend-dev`

- Item Types: `Fix` + `Proof`

- [x] Fix: 循环内镜像 `markPaid` 序列——`postingDispatcher.tryPostPayment(s)`（内含 F1.2 去重守卫，hr2-006 复用点）→ reload → `paymentBatchNo/markPaidTargetStatus/paymentDate` → `updateEntity`；`@Inject SalaryPostingDispatcher`（非 private，沿 MarkPaid 先例）。既有第二循环（L60-63 `setBankFileId` 回填）保留原位，其 update 基于落库后的 bankFile 存在性，与循环内 reload-update 序列无冲突（回填仅写 bankFileId 单字段）
- [x] Fix: 文件内容对齐 `payroll.md §7.2` 五列「序号,账号,户名,金额,用途」——批量查询员工（`employeeId in (...)` 单查询防 N+1）取 `bankAccountId`/`fullName`，缺账号写空串（不阻塞出文件）
- [x] Proof: 扩展 `testGenerateBankFileTransfersSalariesToPaid`：① 断言 SALARY_PAYMENT 凭证生成（`ErpFinVoucherBillR` 按 billCode=`SAL-<yyyyMM>-<salaryId>` 反查 + businessType 匹配；修复前红）；② 文件内容含账号/户名列（修复前红）；③ 既有 PAID/batchNo 断言零回归。E2E `hr-payroll.action.spec.ts` negative 守卫不受影响（无 APPROVED 行前置路径不变）
      - Skill: `nop-testing`

Exit Criteria:

- [x] 扩展断言先红后绿；`mvn test -pl module-hr/erp-hr-service` 全绿；billCode 反查断言与 `SalaryPostingDispatcher.buildBillCode` 格式一致

### Phase 6 — hr2-003 runPayroll 逐员工隔离

Status: completed
Targets: `module-hr/erp-hr-service/src/main/java/app/erp/hr/service/processor/ErpHrSalaryRunPayrollProcessor.java`
Skill: `nop-backend-dev`

- Item Types: `Decision` + `Fix` + `Proof`

- [x] Decision: 隔离语义——循环体 per-employee try/catch（NopException 与 UnexpectedException 同捕），失败员工 id 收集 + `LOG.warn`（英文，r3 i18n 标准）+ 批后单次 `IErpSysNotificationBiz.notify("hr.salary-calculation-skipped", Map ctx, IServiceContext)` 告警（镜像 `SalaryPostingDispatcher.dispatchFailureAlert` 的 null-tolerant + try/catch degraded 范式）；**返回类型维持 `List<ErpHrSalary>`**（C17 L152 `instanceof List` + size 断言契约，GraphQL/xbiz 契约零变更）。理由：UC-HR-04「跳过并告警」；替代方案（返回结构体含失败清单）破坏 C17 快照与 xbiz 契约，否决。残留风险：① 失败清单仅日志+通知可见，无结构化返回——owner doc 注记该边界；② 员工 calculate 成功后 save 前异常等中间态的 REQUIRES_NEW 已提交凭证 + 外层回滚，由 F1.2 去重守卫在重跑时幂等收敛（与 markPaid 单笔/F1.2 语义同构）。
      - Skill: `nop-backend-dev`
- [x] Fix: 落地隔离 + 告警（`existsNonVoidSalary` 幂等跳过语义不变，非失败不计告警）
- [x] Proof: 先行失败测试：双员工（一人全配置 + 一人缺合同）runPayroll → 不抛、返回恰 1 条、缺配置员工无 salary 行（修复前整批抛 `ERR_EMPLOYMENT_CONTRACT_NOT_FOUND`）；补断言第二次 runPayroll 仅补算缺配员工（幂等跳过语义 + 隔离协同）。修复后全绿
      - Skill: `nop-testing`

Exit Criteria:

- [x] 新增测试先红后绿；`mvn test -pl module-hr/erp-hr-service` 全绿；返回契约（List）与 C17 兼容性在 Execution Record 声明

### Phase 7 — owner docs + C17 快照重录 + 全量验证 + 状态回填

Status: completed
Targets: `docs/design/human-resource/payroll.md`、`shift-scheduling.md`、`payroll-simulation.md`、`docs/audits/check/ai-check-index.md`、`docs/backlog/ai-check-roadmap.md`、`app-erp-all` C17 快照、`docs/logs/2026/09-11.md`、`docs/testing/known-good-baselines.md`
Skill: `nop-testing` + `closure-audit-prompt`

- Item Types: `Fix`（文档/索引/roadmap）+ `Proof`（全量验证）

- [x] Fix: `payroll.md`——§2.4 增「读取按有效期过滤 + 同险种取最新」实现注记；§5.2 增「应出勤日按月推导（config `erp-hr.required-work-days` 兜底）+ 带薪假豁免 + 无薪假防双扣 + 零打卡全勤兜底」口径注记 + §5.2 步骤 4 银行文件过账实现注记；§7.2 五列格式落地注记 + §7.3 按银行分组 Deferred 注记 + §七 实现约定 Deferred 注记勘误（L453「§7.3 生成流程…未接入」与实仓 `generateBankFile` mutation + bankFileId 回填已在矛盾，hr2-002 修复后更新为「已接入；按员工卡行分组仍未落地」）；§9.1 SALARY_PAYMENT 触发时机补「`markPaid` 或 `generateBankFile` 任一路径」对齐（消除 §5.2 vs §9.1 表述冲突，hr2-002 附随义务）
- [x] Fix: `shift-scheduling.md` §6.1 补带薪假集合豁免折算实现注记；`payroll-simulation.md` §3.2 补 5 派生行读取修复注记
- [x] Proof: C17 快照重录——`TestErpC17HrSalaryPayment` RECORDING 重录（预期漂移面 = salary 行 `requiredWorkDays` 22→23 及其传播响应/表快照；金额锚点 15000/1200/1800/210/11790 必须逐值不变，若金额漂移即修复缺陷回滚排查）→ CHECKING 二连跑绿；**快照重录合规声明**（双面义务自查：DOM/像素 E2E 快照不受影响——纯后端字段，`git diff --stat tests/e2e/visual/**-snapshots/` 为空）落本节 Draft Review Record
      - Skill: `nop-testing`
- [x] Proof: 全量验证——`mvn test -pl module-hr/erp-hr-service` 全绿 + `mvn clean install -DskipTests` 全 reactor BUILD SUCCESS + `mvn test -pl app-erp-all`（≥70 tests 0 failures）+ `mvn test` 全 reactor 与 `ai-check-r3-mv1` 基线一致或更优（零新增失败）+ `bash docs/audits/nop-compliance-checker.sh` ≤ 基线（R2c=1543；若本次新增 daoFor 站点触发漂移，按 per-site 证据 baseline-raise 或收敛实现复用既有 helper）+ `node tools/check-hardcoded-cjk.mjs --strict` PASS（新增注释/日志若触发新违规：LOG 英文化 + 注释改英文，不留违规）+ `bash docs/audits/i18n-coverage-checker.sh` PASS
      - Skill: `nop-testing`
- [x] Proof: E2E 回归——`tests/e2e/business-actions/hr-payroll.action.spec.ts` + `hr-salary-simulation` + `hr-leave-attendance` 三 spec 全绿（flux 引擎）
      - Skill: `nop-testing`
- [x] Fix: `ai-check-index.md` hr2-001..005 五行 → `fixed`（附测试方法名 + 本 plan 指针）；roadmap F2.13 行 → `done`（附本 plan + 结束审计证据）；`known-good-baselines.md` 新基线行（如全量命令有变化）；`docs/logs/2026/09-11.md` 条目
- [x] Fix: git 提交（用户指令：每个 plan 完成后主动提交一次；commit message 形如 `fix(ai-check): plan-2026-09-11-1530 f2.13 mnt+hr P1 簇 hr2-001..005 修复 + mnt/hr 已修在位裁决 + full-green verification`）

Exit Criteria:

- [x] 三 owner doc 注记落地且与代码行为一致；C17 重录后 CHECKING 二连跑绿且金额锚点逐值不变；全量验证命令全绿或漂移已按程序登记；索引/roadmap/日志回填完成

## Execution Record

（执行会话逐 Phase 证据，2026-09-11，HEAD `120cebea8`）

- **Phase 1**：mnt-001 证据 = `AbstractErpMntSparePartUsageProcessor.validateNotConfirmed:68-77` 接线 `documentStateMachine.assertCanConfirm`（原空守卫）；mnt-002 = `ErpMntSparePartUsageReverseConfirmProcessor:34-52` NopException rethrow 不吞；mnt-003 = `ErpMntVisitCompleteProcessor:48` + `ErpMntDowntimeEntryCompleteProcessor:25` 前置恢复调用链；hr-001 = `ErpHrDepartmentBizModel:54` / `ErpHrPositionBizModel:41` defaultPrepareDelete 守卫 + `TestErpHrDepartmentPositionDeleteGuard` 8/8 绿。索引 L434 回填 fixed ✓。
- **Phase 2**：红灯实证（`itemCode grossSalary 读取值不应恒为 0` 双副本）→ `AbstractErpHrSalarySimulationProcessor.readSalaryItem` 单源静态（13 case + null 护）+ 双副本委托 → 3/3 绿；`TestErpHrPayrollSimulation` 12/12 零回归。消费面结论：`getComparison` 无 erp-hr-web 页面绑定（`payroll-simulation-comparison.page.yaml` 走 `ErpHrReport__renderHtml`/`__download` 报表管线，其数据集构造器无 readSalaryField 同型 switch）——后端 GraphQL 面单侧闭合，用户面影响收窄与 finding 预判一致。
- **Phase 3**：3 新测试先红（`Expected NopException to be thrown, but nothing was thrown` / 任取行 / 双计）→ `SocialInsuranceCalculator` 落地（`isEffective` null 开区间 + `dedupeLatestPerType` 同险种取最新 + findBase orderBy effectiveFrom desc,id asc）→ `TestErpHrPayrollEngine` 13/13 绿（既有种子面 = 回归前提④ 实证）。
- **Phase 4**：4 新测试先红（短月 20/22 少发、年假折减、双扣 11818.18、config 覆盖路径）→ `PayrollCalculator`（`deriveRequiredWorkDays` 周一至周五推导 + `PAID_LEAVE_TYPES` 豁免 + config-on 加回防双扣 + cap）+ `ErpHrConstants.CONFIG_REQUIRED_WORK_DAYS` + `ErpHrConfigs.requiredWorkDaysOverride()` → 绿。执行期修正 1 项：Proof ③ 断言首版误按「17 打卡 + 22 分母」推 14782.62，实测 DEBUG（basic=15652.18/actual=23/unpaid=5）定位为测试期望值算错（7 月 23 工作日 → 18 打卡 + 5 病假 → (18+5)/23−5/23 = 18/23 → 15652.18），语义（单扣）与 plan 一致，仅数值口径修正。`_cases` 快照盘点 = 全 module-hr 套件实跑收敛 7 个漂移方法（Engine 5 + Simulation 2，非预判 3 处枚举的子集超集关系以实跑为准），`@EnableSnapshot(saveOutput=true)` 方法级重录 + CHECKING 复跑绿；diff 审计 = 仅 ACTUAL/REQUIRED_WORK_DAYS 列漂移（20/21/23），金额锚点逐值不变；附带 1 处 `nop_wf_step_instance.csv` 行序重排（内容相同）。Phase 4 Exit 第二项（module-hr 全绿）实跑 261/0/0 ✓。
- **Phase 5**：扩展断言先红（`银行文件路径 280 发放凭证已生成 expected <1> but was <0>`）→ Processor 接线 `tryPostPayment`（镜像 markPaid 序列）+ `loadEmployeesById` 批量单查询 + §7.2 五列 → 绿。执行期发现：该 case 原快照含 3 行 `erp_fin_posting_exception`（旧环境无账套时 approve 静默失败的录制痕迹）；过账环境补齐后新录制零异常行，框架只写非空表致旧文件残留为陈旧基线——删除该陈旧 CSV 后 CHECKING 绿（非掩盖：失败源 = 录制残留，非行为缺陷）。
- **Phase 6**：隔离测试先红（整批 ERR_EMPLOYMENT_CONTRACT_NOT_FOUND）→ Processor per-employee try/catch + 失败收集 + 批后单次 `hr.salary-calculation-skipped` 通知（null-tolerant degraded）+ LOG.warn 英文 → 绿（含二次 runPayroll 幂等协同断言）。返回契约 `List<ErpHrSalary>` 不变。
- **Phase 7 验证面**：`mvn clean install -DskipTests` 156 模块 BUILD SUCCESS（01:46）；module-hr 262/0/0（终态 surefire，含隔离测试）；执行中段快照 261/0/0（隔离测试加入前）；app-erp-all 73/0/0/1（基线 70/0/0/1 一致或更优）；C17 CHECKING 漂移 = `actualWorkDays/requiredWorkDays` 22→23 两字段 + 掩码变量漂移（`*`→`@var:ErpHrSalary@createTime` 等良性面；金额锚点 15000/1200/1800/210/11790 逐值不变）→ 类级 RECORDING 重录 + 注解还原 + CHECKING 绿；compliance checker exit 0，R2c 实测 1544 > 机器块 1543（+1 = `loadEmployeesById` 新 daoFor 站点）→ per-site baseline-raise 1543→1544（双写 + 注记，先例 0457-1 同型）；CJK `--strict` PASS（0 new violations）；i18n checker PASS；E2E hr-payroll/hr-salary-simulation/hr-leave-attendance 11/11 passed（flux 引擎 1.5m）。DOM/像素 E2E 快照零变更（`git status tests/e2e/visual/**-snapshots/` 空）——快照重录双面义务未触发（module-hr `_cases` 与 C17 均为纯后端表/响应快照，无 DOM/像素面）。

## Draft Review Record

- Independent draft review iteration 1: `needs revision`（agent `agent_0f2e70a5-3309-4602-859d-8ce8efa000ce`，2026-09-11）——方案主轴五项（分母推导/带薪豁免/防双扣、280 接线、失败隔离、有效期过滤、单源收敛）经实仓挑战全部成立；3 Major：M1 hr2-004 命中公式缺 `effectiveFrom == null` 分支（C17/module-hr 测试种子 config 行该列全 null，字面实现大面积红）+ Current Baseline 种子前提表述失真；M2 快照漂移盘点遗漏 module-hr `_cases`（JUnitAutoTestCase 录制快照含 `ACTUAL/REQUIRED_WORK_DAYS` 列，2026-05/07 案例必漂移）；M3 durationDays 与考勤行口径对齐假设未裁决（跨期假期/日历天两边界）。6 Minor（阶段 Item Types 标注、§七 Deferred 注记勘误、第二循环合并说明、notify 签名、REQUIRES_NEW 中间态披露、现状分母表述）。
- Independent draft review iteration 2: `accept`（同审查者 iteration 2 复核 `agent_0f2e70a5-3309-4602-859d-8ce8efa000ce`，2026-09-11：四处修订全部正确闭合——M1 公式 null 分支 + 种子面/快照面盘点精确、M2 Phase 4 Exit 三要素齐备、M3 口径 Decision 可审计；m1-m6 全部落实；2 条非阻塞观察（Phase 4/7 Targets 路径清单未含 `_cases/` 形式瑕疵 + 快照合规声明表述面）随 Execution Record 处理。裁决原文：「可按计划状态流程将 Plan Status 改为 active 开始实施；结束审计仍须由独立子代理（新会话）执行」）。

## Closure Gates

- [x] 范围内行为完成：F2.13 九条 finding 全部 `fixed` 终态（5 条带新测试 + 4 条带复验证据）
- [x] 相关文档对齐：payroll.md / shift-scheduling.md / payroll-simulation.md / ai-check-index.md / roadmap / known-good-baselines / logs
- [x] 已运行验证：Phase 7 全量命令清单（全 reactor build + test + compliance + CJK strict + i18n + E2E 三 spec）
- [x] 无范围内项目降级为 deferred/follow-up（Non-Goal 清单外无遗留）
- [x] 独立草案审查已完成并记录
- [x] 文本一致性已验证：状态、阶段、门控和日志都一致
- [x] 结束审计由独立子代理（新会话）执行；执行者未自我审计且未将此留为 `[ ]` 作为人工门控占位符
- [x] 结束证据存在于文件中（本计划 Execution Record + Closure 段）

## Deferred But Adjudicated

### 银行文件按员工工资卡所在银行分组（payroll.md §7.3）

- Classification: `out-of-scope improvement`
- Why Not Blocking Closure: 现实现 bankId 入参即目标银行单文件语义；按员工卡行分组需员工×银行主数据建模与多文件产出契约变更，超出本 P1 修复面
- Successor Required: `yes`（触发条件：银行代发对接需求要求单文件多银行混合批次时）

### SICK 病假工资折算（部分有薪）

- Classification: `watch-only residual`（owner doc javadoc 既有 documented simplification）
- Why Not Blocking Closure: hr2-001 修复聚焦分母/带薪豁免/防双扣三缺陷；病假折算比例属薪酬政策参数化需求
- Successor Required: `yes`（触发条件：薪酬政策要求病假按工龄折算比例发放时）

### 法定节假日工作日历

- Classification: `watch-only residual`
- Why Not Blocking Closure: 工作日推导按周一至周五 + config 覆盖兜底；节假日调休日历为年度更新外部数据源
- Successor Required: `yes`（触发条件：国务院年度节假日安排数据源接入需求时）

## Closure

Status Note: 本计划可闭合——F2.13 九条 finding（mnt-001/002/003 + hr-001 已修在位裁决；hr2-001..005 实修）全部到达 `fixed` 终态，每条附失败测试先行证据或 HEAD 复验证据；快照重录金额锚点逐值不变；全量验证矩阵（build 156 + 全 reactor 4095/0/0/1 + checker R2c=1544 双写 + CJK/i18n PASS + E2E 11/11）全绿；owner docs / 索引 / roadmap / 基线行 / 日志七面对齐。

Closure Audit Evidence:

- Auditor / Agent: 独立子代理（fresh session）`agent_ff779bb7-7f9b-496c-836d-4329fbcda5e5`，2026-09-11
- Evidence: 结束审计报告裁决 **`passes closure audit`**（0 Blocking；1 Major = git 提交时序——审计时变更尚在工作树未提交，审计要求 Closure 落账后立即提交，本段即该落账动作；3 Minor = 相关文档对齐门控勾选遗漏（已补 [x]）/ module-hr 计数 261→262 漂移（已按终态 surefire 修正）/ C17 漂移「恰两字段」表述未含掩码变量面（已修正）——三项均已在本次提交前落实）。审计报告逐项：计划完备性 PASS、九 finding 终态 PASS、修复行为抽查 PASS（含 QueryBean.addOrderField desc 语义查证）、测试在位 PASS（TestErpHrPayrollEngine 18/0/0）、快照重录合规 PASS（金额 15000/1200/1800/210/11790 + 8000/5000 逐值不变）、owner docs PASS、验证证据 PASS、roadmap PASS。

Follow-up:

- （无非阻塞跟进项；Deferred But Adjudicated 三项带触发条件在案）

