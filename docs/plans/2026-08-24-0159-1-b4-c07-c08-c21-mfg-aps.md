# 2026-08-24-0159-1-b4-c07-c08-c21-mfg-aps 批次 B4 用例实施：C07 制造工单全生命周期 + C08 MRP→APS→工单释放 + C21 APS 排程发布与产能负荷

> Plan Status: completed
> Mission: integration-test
> Work Item: B4（C07, C08, C21；主导域 manufacturing + aps）
> Last Reviewed: 2026-08-24
> Source: `docs/backlog/integration-test-roadmap.md`（B4 行，M0.3 追加）
> Related: `2026-08-23-2133-1-b1-c01-c02-purchase-p2p-return`（done）、`2026-08-23-2133-2-b2-c03-c04-sales-o2c-return`（done）、`2026-08-23-2133-3-b3-c05-c06-inventory-cost`（done）、`2026-08-24-0159-2-b5-c09-c10-quality-maintenance`
> Audit: required

## Current Baseline

（全部事实 2026-08-24 实仓核验）

- **前置依赖**：M0.1 设计文档（`docs/design/integration-testing.md` §6 C07/C08/C21 用例规格 + §3.4 机制实证结论 + §4 粒度约定 + §5 xwf 规避清单）+ M0.2 基建 + B1/B2/B3（均 done）。
- **基建就绪**（M0.2/B1-B3 产物）：基类 `ErpIntegrationTestCase`（机制 (c) 文件 H2 双模 + 10 个共享 step helper：`rpcMutation`/`idOf`/`submitForApproval`/`approve`/`reload`/`findBillLink`/`requireVoucherBalanced`/`findApItem`/`findStockMove`/`eqFilterQuery`）；`_cases` 目录约定 `app-erp-all/_cases/io/nop/app/all/it/<类>/<方法>/input|output`；surefire 模块级串行化已落地；已有 7 个集成用例类（Pilot + B1-B3 各 2 类 = 1+6）作为范本，**B4 按同范式实现（1 用例 = 1 类 1 方法 + @BeforeAll fresh-DB + RECORDING→CHECKING 往返）**。
- **mfg 域既有测试资产（本批业务参照，不重复实现内部细节）**：`TestErpMfgWorkOrderEndToEnd`（工单 save→submitForApproval→approve→start→reportCompletion（报工触发领料出库）→COMPLETED；JobCard startJob/recordWork/completeJob；`ErpMfgMaterialIssue__confirm`）、`TestErpMfgCostFlowEndToEnd`（完工入库 + 凭证行断言范式）、`TestErpMfgMrpEndToEnd`（`ErpMfgMrpPlan__runMrp` + `ErpMfgMrpPlanLine__releasePurchaseRequest`/`releaseWorkRequest`）、`TestErpMfgMrpSimulation`（`ErpMfgMrpScenario__runSimulation` → `promoteToFormalPlan`，**config 门控 `erp-mfg.simulation-enabled` 默认 false**）。
- **aps 域既有测试资产**：`TestErpApsWorkOrderToOperationOrder`（`ErpApsOperationOrder__scanReleasedWorkOrders`/`createOperationOrdersFromWorkOrder`/`scheduleForward`）、`TestErpApsScheduleManagement`（`ErpApsSchedule__save`/`publish`/`archive`，DRAFT→PUBLISHED→ARCHIVED 状态机）、`TestErpApsAutoDispatch`（时间窗 flake 已于 B3 修复）、`TestErpApsSchedulingEngine`、`TestErpApsOperationOrderStateGuards`（start/complete/cancel 守卫）。
- **C07 动作名以当前实现为准（潜在漂移登记点）**：设计文档 §6 C07 步骤 2 写「`ErpMfgWorkOrder__close`（完工：完工入库 + 完工过账凭证）」；实仓 `ErpMfgWorkOrderBizModel` 有独立 `close`（结案动作，`ErpMfgWorkOrderCloseProcessor`，STOPPED/IN_PROCESS→CLOSED 语义）与 `reportCompletion`（报工，带 completedQty 驱动完工入库 + 过账，`TestErpMfgWorkOrderEndToEnd` 实证走此路径至 COMPLETED）。**实施期须核实完工入库/过账的实际驱动动作并登记设计文档 §6 C07 勘误（B1-B3 C05/C06 同型先例）**。
- **C08 动作名以当前实现为准（潜在漂移登记点）**：设计文档 §6 C08 步骤 2 写「`ErpMfgMrpPlan__release`」；实仓为**行级** release：`ErpMfgMrpPlanLine__releaseWorkRequest`/`releasePurchaseRequest`/`releaseSubcontractRequest`（`ErpMfgMrpPlanLineBizModel` 三个 per-mutation Processor）。MRP 无 seed（94 CSV 实仓核验无 erp_mfg_mrp_*），须自包含建 MRP 方案 + demand。
- **C21 前置 seed（实仓核验在位）**：`erp_mfg_workcenter.csv`（WC-001，`WORK_HOURS_PER_DAY=8.00`）、`erp_mfg_workcenter_calendar.csv`（08:00~16:00，与 WC-001 同源）、`erp_mfg_workcenter_capacity.csv`（efficiencyFactor=1.0）、`erp_mfg_crp_load.csv`（WC-001 loadHours=4.00 → loadRate=0.50，workOrderId 弱指针→WO-2026-001）；`erp_mfg_work_order.csv` 4 行（WO-2026-001/002 COMPLETED、WO-2026-003 IN_PROCESS、WO-2026-004 STOCK_PARTIAL）——C21 负荷率断言 = **静态 crp_load 行不被重算覆盖**（`ErpMfgCrpRunJob` config `erp-mfg.crp-run-cron` 为空默认不调度，设计文档 §6 C21 步骤 4 口径；capacityHours=8.00 为 `CrpLoadCalculator.availableHours` 由日历推导（08:00~16:00 = 8h × eff 1.0），非工作中心 CSV 列直接值——数值期望 8.00/0.50 不变）。
- **aps 无部署 seed**（94 CSV 实仓核验无 erp_aps_*）→ C08/C21 的排程/工序订单全部自包含 GraphQL 建数。
- **运行互斥**：集成测试与 E2E live server 共用 `db/erp.mv.db`——执行前 `lsof -i :8011`（8080 同查）确认无 live server（e2e-runbook「集成测试」节纪律）。
- **基线计数**：known-good-baselines 2026-08-23 权威计数源 **3815 tests / 0 failures / 0 errors / 1 skipped / 624 报告文件**；app-erp-all **35/0/0/1**（7 集成类 + 基建类单 fork 顺序执行）；156 模块 BUILD SUCCESS。B4 预期差量 **+3 tests / +3 报告文件**（3 用例类各 1 类 1 方法）。
- **流程改进登记（B3 closure MINOR-2 兑现）**：最终 `clean install` 会清除 surefire XML 计数证据——全量回归后、clean 前先拷贝计数/XML 再登记基线（本计划 Phase 4 执行纪律）。
- **deferred 触发条件（B1/M0.2 watch-only residual）**：若执行期发生 seed 修正授权路径（roadmap 横切关注点 3），须同步测量登记重录耗时（以单用例 252K/类为预算参考）；本批默认自包含建数（C08 MRP 无 seed 即自包含），预期不触发。

## Goals

- **C07** = 测试类 `TestErpC07MfgWorkOrderLifecycle`（1 类 1 方法）：自包含工单（DIRECT 审批轴）save → submitForApproval → approve → start（开工）→ reportCompletion（报工；领料出库由独立 `ErpMfgMaterialIssue__confirm` 步骤驱动，以 `TestErpMfgWorkOrderEndToEnd` 业务参照为准）→ 完工（完工入库 + 完工过账凭证，**以当前实现为准核实驱动动作**）→ cost_variance 差异断言；三层全比对（response 快照 + output/tables 变更行 + JUnit 关键断言：状态机 APPROVED→IN_PROCESS→COMPLETED、完工入库 stock_move、凭证借贷平衡、varianceAmount = actual − standard）。
- **C08** = 测试类 `TestErpC08MrpApsRelease`（1 类 1 方法）：自包含 MRP 方案 → `runSimulation`（config 门控开启）→ `promoteToFormalPlan` → 行级释放（工单/采购建议）→ APS 排程 save → publish（PUBLISHED）→ 工序订单状态机推进；三层全比对（净需求 = 毛需求 − 在途 − 在手、下游单据存在、排程发布态、工序订单状态翻转）。
- **C21** = 测试类 `TestErpC21ApsCapacityLoad`（1 类 1 方法）：自包含工序订单 + 排程 → `ErpApsSchedule__publish` → `ErpMfgReport__renderHtml(reportName="crp-load-report")` 负荷率断言（WC-001/loadHours/capacityHours/loadRate 确定性 token，8.00/0.50 期望值）+ 静态行不被重算覆盖；三层全比对。
- 三用例 **RECORDING→CHECKING 往返全绿**（按 M0.2/B1-B3 往返范式）。
- roadmap B4 状态 `todo` → `done`（经独立 closure audit）+ 日志 + 基线差量登记 + 设计文档 §6 C07/C08 潜在动作名勘误登记（若发生）。

## Non-Goals

- B5-B10 用例（各归后续分批计划，本计划不实现——B5 已由 `2026-08-24-0159-2-b5-c09-c10-quality-maintenance` 承接）。
- 生产代码/ORM/契约/API 变更——用例实施中发现的产品缺陷按 roadmap 横切关注点 4 走保护区域门禁（accounting/finance postings = plan-first + owner doc + tests；其余 = auto + dual-agent-approval），**不得在本计划内直接改生产代码**。
- 默认修改 seed CSV——确需修正走 roadmap 横切关注点 3「seed 修正授权」流程；默认以自包含建数满足前置（C08/C21 的 MRP/APS 数据均自包含）。
- 不接 CI（roadmap 已裁决出 scope）；不做浏览器层 E2E 页面验证（分工边界）。
- 不实现未覆盖功能（横切关注点 6「以当前实现为准」）：如 CRpRunJob 默认关闭则只断言静态行。

## Task Route

- Type: `implementation-only change`（集成测试代码 + 快照 + 文档，零生产代码/契约/模型变更）
- Owner Docs: `docs/design/integration-testing.md`（§6 C07/C08/C21 规格 + §3.4 编写纪律 + §4 粒度 + §5 xwf 清单）、`docs/backlog/integration-test-roadmap.md`（B4 行 + 横切关注点）、`docs/testing/e2e-runbook.md`（「集成测试」节）、`docs/testing/known-good-baselines.md`
- Skill Selection Basis: `nop-testing` 匹配测试基类选择/@NopTestConfig/@NopTestProperty（C08 simulation 门控 = C06 ERP_PUR_RECEIVE 同型）/快照录制回放（RECORDING→CHECKING）/三层验证模型；M0.2 试点 + B1-B3 六用例类为同模式范本，mfg/aps 既有 EndToEnd 测试为业务参照。

## Infrastructure And Config Prereqs

- 文件型 H2 `db/erp.mv.db` fresh 机制已就绪（基类每类 1 次清理）；**与 E2E live server 互斥**——执行前 `lsof -i :8011`（8080 同查）确认无 live server。
- **C08 config 门控**：`erp-mfg.simulation-enabled` 默认 false → 测试类 `@NopTestProperty(name="erp-mfg.simulation-enabled", value="true")`（容器启动前声明，对齐 C06 `@NopTestProperty` 范式）；若实仓另有门控键，以实仓常量为准并登记勘误。
- C21 依赖 `erp-mfg.crp-run-cron` 为空默认（不调度）——不额外配置，断言静态 crp_load 行不被覆盖。
- 无端口/密钥/外部服务/环境变量新增；surefire 串行化已在 app-erp-all pom 落地。

## Execution Plan

### Phase 1 — C07 制造工单全生命周期与完工过账（Proof-heavy：2/3 Proof + 1/3 Add，逐项标注）

Status: completed
Targets: `app-erp-all/src/test/java/io/nop/app/all/it/TestErpC07MfgWorkOrderLifecycle.java`、`app-erp-all/_cases/io/nop/app/all/it/TestErpC07MfgWorkOrderLifecycle/`
Skill: nop-testing

- Item Types: `Add | Proof | Decision`
- Prereqs: M0.1 + M0.2 + B1-B3 done

- [x] Add：**C07 测试类** `TestErpC07MfgWorkOrderLifecycle`（1 类 1 方法）——按设计文档 §6 C07 规格 + `TestErpMfgWorkOrderEndToEnd`/`TestErpMfgCostFlowEndToEnd` 业务参照：自包含工单（引用 seed 物料/BOM/工作中心）save → submitForApproval → approve（DIRECT）→ start → reportCompletion（completedQty 驱动报工）+ `ErpMfgMaterialIssue__confirm`（领料出库独立步骤，`TestErpMfgWorkOrderEndToEnd` 先例）→ 完工（完工入库 + 完工过账凭证 + 差异）→ `ErpMfgCostVariance__findPage` 差异断言；静态载荷走 `input/N_step.json5` + `addVar`/`@var:` 动态 id（禁止内联动态 id 字面量）。
  - Skill: nop-testing
- [x] Decision：**完工驱动动作裁决**——核实完工入库/完工过账/差异计算的实际驱动动作链（reportCompletion 带 completedQty vs close 结案动作 vs 独立 Processor；`ErpMfgWorkOrderBizModel` 实仓三动作 start/close/reportCompletion，`ErpMfgWorkOrderCloseProcessor` 为 STOPPED/IN_PROCESS→CLOSED 结案语义，完工入库 + 过账由 `ErpMfgWorkOrderReportCompletionProcessor` 驱动——`TestErpMfgCostFlowEndToEnd` 实证），选择/备选/残留风险记录；与设计文档 §6 C07 步骤 2「close（完工）」表述不一致时按 B1-B3 先例登记设计文档 §6 勘误（动作名以当前实现为准）。
  - Skill: nop-testing
- [x] Proof：**C07 RECORDING→CHECKING 往返**——首次 RECORDING 录制（output/response + output/tables 变更行）→ 切回默认 CHECKING 复跑全绿；三层断言逐项核验（状态机 APPROVED→IN_PROCESS→COMPLETED、完工入库 stock_move 生成、完工过账凭证借贷平衡、cost_variance.varianceAmount = actual − standard）。
  - Skill: nop-testing

Exit Criteria:

- [x] C07 类存在且 RECORDING→CHECKING 往返绿（失败模式 = 任一断言/快照 diff 不通过）
- [x] 完工驱动动作裁决落盘（含设计文档 §6 C07 勘误登记，若与实仓动作不一致）

### Phase 2 — C08 MRP→APS→工单释放（混合类型：1/3 Add + 1/3 Proof + 1/3 Decision，逐项标注）

Status: completed
Targets: `app-erp-all/src/test/java/io/nop/app/all/it/TestErpC08MrpApsRelease.java`、`app-erp-all/_cases/io/nop/app/all/it/TestErpC08MrpApsRelease/`
Skill: nop-testing

- Item Types: `Add | Proof | Decision`
- Prereqs: Phase 1（同基类同装配，先例链打通后无新机制风险）

- [x] Add：**C08 测试类** `TestErpC08MrpApsRelease`（1 类 1 方法）——按设计文档 §6 C08 规格 + `TestErpMfgMrpEndToEnd`/`TestErpMfgMrpSimulation`/`TestErpApsWorkOrderToOperationOrder`/`TestErpApsScheduleManagement` 业务参照：自包含 MRP 方案（scenario + demand，引用 seed 物料/BOM）→ `ErpMfgMrpScenario__runSimulation`（@NopTestProperty 开启 simulation 门控）→ `promoteToFormalPlan` → 行级释放（`ErpMfgMrpPlanLine__releaseWorkRequest`/`releasePurchaseRequest`，生成工单/采购建议）→ `ErpApsSchedule__save` → `ErpApsSchedule__publish`（PUBLISHED）→ `ErpApsOperationOrder__*` 状态机推进（scanReleasedWorkOrders/createOperationOrdersFromWorkOrder/scheduleForward 以实仓为准）。
  - Skill: nop-testing
- [x] Decision：**释放动作名裁决**——设计文档 §6 C08 步骤 2「`ErpMfgMrpPlan__release`」vs 实仓行级 release 动作（`ErpMfgMrpPlanLine__releaseWorkRequest` 等）——按实仓为准执行并登记设计文档 §6 C08 勘误（B3 C05/C06 同型先例）；同时裁决 APS 衔接路径（release 后经 scanReleasedWorkOrders 进入 APS 排程 vs 自包含建排程）。
  - Skill: nop-testing
- [x] Proof：**C08 RECORDING→CHECKING 往返**——RECORDING 录制 → CHECKING 复跑全绿；层 1 锚点：MRP 净需求 = 毛需求 − 在途 − 在手、计划释放生成下游单据（工单/采购建议存在）、APS 排程发布后 `status=PUBLISHED`、工序订单状态机翻转。
  - Skill: nop-testing

Exit Criteria:

- [x] C08 类存在且 RECORDING→CHECKING 往返绿（失败模式 = 任一断言/快照 diff 不通过）
- [x] 释放动作名裁决落盘（含设计文档 §6 C08 勘误登记，若与实仓动作不一致）

### Phase 3 — C21 APS 排程发布与产能负荷（Proof-heavy：1/2 Add + 1/2 Proof，逐项标注）

Status: completed
Targets: `app-erp-all/src/test/java/io/nop/app/all/it/TestErpC21ApsCapacityLoad.java`、`app-erp-all/_cases/io/nop/app/all/it/TestErpC21ApsCapacityLoad/`
Skill: nop-testing

- Item Types: `Add | Proof`
- Prereqs: Phase 2（同装配，C21 侧重报表渲染 + 静态行断言，无新机制风险）

- [x] Add：**C21 测试类** `TestErpC21ApsCapacityLoad`（1 类 1 方法）——按设计文档 §6 C21 规格 + `TestErpApsScheduleManagement`（publish）+ `TestErpMfgReportRendering`（renderHtml crp-load-report）业务参照：自包含工序订单（引用 seed WC-001 工作中心链）→ `ErpApsOperationOrder__save`（排程候选）→ `ErpApsSchedule__save` → `ErpApsSchedule__publish` → `ErpMfgReport__renderHtml(reportName="crp-load-report")` 负荷率断言（WC-001/loadHours/capacityHours/loadRate 确定性 token：8.00/0.50，seed crp_load 行 loadHours=4.00/capacityHours=8.00）→ 静态行不被重算覆盖（CrpRunJob 默认不调度）。
  - Skill: nop-testing
- [x] Proof：**C21 RECORDING→CHECKING 往返**——RECORDING 录制 → CHECKING 复跑全绿；层 1 锚点：排程发布状态 PUBLISHED、crp-load 报表 HTML 含确定性 token（8.00/0.50）、crp_load 静态行未被覆盖。
  - Skill: nop-testing

Exit Criteria:

- [x] C21 类存在且 RECORDING→CHECKING 往返绿（失败模式 = 任一断言/快照 diff 不通过）

### Phase 4 — 收尾与回归（Proof-heavy）

Status: completed
Targets: `docs/backlog/integration-test-roadmap.md`（B4 → done + 头「最后更新」注记）、`docs/logs/2026/08-24.md`、`docs/testing/known-good-baselines.md`（差量登记）、`docs/design/integration-testing.md`（§6 C07/C08 勘误登记，若 Phase 1/2 触发）
Skill: none

- Item Types: `Proof | Fix`
- Prereqs: Phase 3

- [x] Proof：**局部回归** `mvn test -pl app-erp-all` 全绿（含 3 个新用例类，串行 fork 单 JVM 顺序执行）+ **全量回归** `mvn test`（全 reactor）零新增失败 + `mvn clean install -DskipTests` BUILD SUCCESS——对照 known-good-baselines 3815/0/0/1/624 口径，+3 tests / +3 报告文件全额归因 B4，零新增失败；**clean 前先拷贝 surefire 计数/XML 证据**（B3 closure MINOR-2 流程改进兑现）。
  - Skill: none
- [x] Fix：roadmap B4 `todo` → `done` + 头「最后更新」注记 + `docs/logs/2026/08-24.md` 日志条目（按日志书写指南）+ known-good-baselines 差量登记 + 设计文档 §6 勘误登记（若触发）。
  - Skill: none

Exit Criteria:

- [x] 局部 + 全量回归零新增失败（与已知基线口径一致）
- [x] roadmap B4 = done + 日志条目存在 + 基线差量登记 + 勘误登记（若触发）

## Draft Review Record

- Independent draft review iteration 1: acceptable as-is (ses_fd0319abdffes7bpUfM2GdGe8Y，独立 general 子代理新会话) — 0 BLOCKER / 0 MAJOR / 3 MINOR。九大核验域全过（基类 10 helper / mfg 4 先例 / aps 5 先例 / C07 close-vs-reportCompletion 漂移 / C08 行级 release 漂移 / simulation 门控默认 false / C21 seed 数值 + CrpRunJob 默认不调度 / 无 aps/mrp seed / 3815+35 基线）。MINOR-1 capacityHours=8.00 归因（实为 `CrpLoadCalculator.availableHours` 由日历推导，工作中心 CSV 列为 WORK_HOURS_PER_DAY）→ 已修订表述，数值期望不变；MINOR-2 集成用例类计数 6→7（Pilot + B1-B3 各 2 = 7）→ 已修订；MINOR-3 领料出库归因（实为独立 `ErpMfgMaterialIssue__confirm` 步骤非 reportCompletion）→ 已修订 Goals/Add 表述 + Decision 项补记 `ErpMfgWorkOrderReportCompletionProcessor` 驱动点。
- Independent draft review iteration 2: accept (ses_fd02dfcddffeAZ9Lta8HdNG4Yw，独立 general 子代理新会话) — 0 BLOCKER / 0 MAJOR / 0 MINOR。3/3 声明修复全部实仓独立核验 FIXED（MINOR-1 capacityHours 归因 = CrpLoadCalculator.availableHours 日历推导 + CSV 列名 WORK_HOURS_PER_DAY 实证；MINOR-2 计数 7 类 = Pilot+C01-C06 实仓 glob；MINOR-3 领料出库 = 独立 `ErpMfgMaterialIssue__confirm` 步骤 + `ErpMfgWorkOrderReportCompletionProcessor` 驱动完工入库/过账实证）；全量重扫（结构合规/单结果表面/94 CSV 无 aps·mrp seed/crp_load 数值/3815+35 基线）零新发现。**共识达成，计划可执行。**

## Closure Gates

> 仅在所有项目和每个阶段的退出标准都勾选 `[x]` 后关闭。完整仓库验证在此处：结束时运行 `typecheck`/`build`/`lint`/`test` 一次。

- [x] 范围内行为完成（C07/C08/C21 三用例三层全比对全绿 + 动作名裁决）
- [x] 相关文档对齐（设计文档 §6 ↔ 用例实现 ↔ roadmap B4 ↔ e2e-runbook ↔ known-good-baselines 无矛盾）
- [x] 已运行验证（`mvn test -pl app-erp-all` 全绿 + 全量 `mvn test` 零新增失败 + `mvn clean install -DskipTests` BUILD SUCCESS）
- [x] 无范围内项目降级为 deferred/follow-up
- [x] 独立草案审查已完成并记录
- [x] 文本一致性已验证：状态、阶段、门控和日志都一致
- [x] 结束审计由独立子代理（新会话）执行；执行者未自我审计且未将此留为 `[ ]` 作为人工门控占位符
- [x] 结束证据存在于文件中

## Deferred But Adjudicated

- （执行后按需填写——候选：seed 修正授权触发时的重录耗时测量（B1/M0.2 watch-only residual 触发条件登记）；无范围内降级项则留空）

## Closure

Status Note: C07/C08/C21 三用例 RECORDING→CHECKING 往返全绿 + 全量回归 3818/0/0/1（+3 tests / +3 报告文件全额归因 B4，零新增失败）+ app-erp-all 38/0/0/1 + `mvn clean install -DskipTests` 156 模块 BUILD SUCCESS；执行期裁决（完工驱动动作 / 释放动作名 / APS 衔接路径 / 数据来源）落盘测试类 javadoc + 设计文档 §6 C07/C08 勘误登记；roadmap B4 done + 日志 + known-good-baselines 差量登记均完成。

Closure Audit Evidence:

- Auditor / Agent: 独立 general 子代理（新会话 `ses_fcfc4ddaeffe2Ph6idHDkAUGSp`，fresh 会话无执行者上下文）
- Evidence: 独立审计逐项核验 6/6 PASS（计划完整性 / 测试代码合规 / 快照一致性 / 验证证据（实跑 `mvn test -pl app-erp-all -Dtest=TestErpC07MfgWorkOrderLifecycle,TestErpC08MrpApsRelease,TestErpC21ApsCapacityLoad` BUILD SUCCESS 3/0/0，surefire 三用例各 1/0/0）/ 文档一致性（roadmap B4 done + known-good-baselines 3818/0/0/1/627 + app-erp-all 38/0/0/1 + 设计文档 §6 C07/C08 勘误 + 日志）/ 无范围内降级（git status 零 module-*/model 或 src/main 变更）），总评 **accept**（0 BLOCKER / 0 MAJOR）

Follow-up:

- 无（非阻塞跟进项：C07/C08/C21 响应快照时间戳 @var 索引跨 run 不稳定 → `*` 通配处置已随本计划落地；B5 分批（C09/C10）已由 `docs/plans/2026-08-24-0159-2-b5-c09-c10-quality-maintenance.md` 承接）