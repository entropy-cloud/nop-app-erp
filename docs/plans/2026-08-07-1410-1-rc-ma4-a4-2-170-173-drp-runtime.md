# 2026-08-07-1410-1 rc-ma4-a4-2-170-173-drp-runtime drp 域 0 值补货单/预测消费/批量释放事务/安全库存口径运行时确认

> Plan Status: completed
> Last Reviewed: 2026-08-07
> Source: `docs/backlog/requirement-compliance-roadmap.md` Work Items A4.2.170 / A4.2.171 / A4.2.172 / A4.2.173
> Related: `docs/audits/2026-08-05-1400-3-rc-ma1-a1-48-drp-full.md`（A1.48 §7 SP-1..SP-4 + P2-RC-069~072 + P1-RC-081/082 + reuse P1-MA1-022）
> Audit: required
> Mission: requirement-compliance
> Work Item: A4.2.170-A4.2.173（drp 域 MA4 运行时验证）

## Current Baseline

drp 域切片（A1.48 全功能 8 UC）MA1 报告 §7 共列出 4 项静态存疑点（SP-1..SP-4），全部为本计划范围（A4.2.170-A4.2.173）。共同主题 = **"0 值/边界行为与事务语义"**（netReq≤0 是否产生 0 量补货单 / forecast-consume-enabled 默认 true 是否引入噪声 / releaseApproved 批量释放单事务失败语义 / 历史出库口径对安全库存计算偏差）——均倾向"确认/可接受"，需运行时证实。finding 家族：P2-RC-069（UC-DRP-02 netReq≤0 生成 0 值行 + runBy 未回写）/ P2-RC-070（UC-DRP-04 采购单单价=0 + 失败重试缺失合并）/ P2-RC-072（UC-DRP-06 20% 偏差告警缺失 + 联合变分 Deferred）/ reuse P1-MA1-022（跨域 daoFor resolved）。既有强测基础：`TestErpDrpEngine`/`TestErpDrpScheduleRelease`/`TestErpDrpSafetyStock`/`TestErpDrpForecastSource`/`TestErpDrpInventoryIntegration`/`TestErpDrpSimulation`。

- **A4.2.170（SP-1 UC-DRP-02 netReq≤0 生成的 0 值行经 approvePlan→releaseLine 是否产生 0 量 TransferOrder/PurchaseOrder；P2-RC-069）**：静态判定 = `DrpReleaseService` 未校验 approvedQty>0 + P2-RC-069 已登记"netRequirement≤0 仍生成 0 值 ErpDrpLine（L1 异常字面'不生成行'偏差，潜在 0 量补货单释放风险）"。运行时确认 0 值行经 approvePlan（approvedQty 回填 suggestedQty=0）→ releaseLine 后是否产生 0 量 TransferOrder/PurchaseOrder，以及是否被下游 inventory/purchase 拒绝。
- **A4.2.171（SP-2 UC-DRP-02 forecast-consume-enabled 默认 true 下无 APPROVED 预测时 forecastDemand=0 无回归）**：静态判定 = config-gated 已测（`TestErpDrpForecastSource`），无 APPROVED 预测时 forecastDemand=0。运行时确认默认 true 不引入噪声（查询 APPROVED 预测无数据→0 贡献）。
- **A4.2.172（SP-3 UC-DRP-04 releaseApproved 批量释放单事务内某行失败 → 全批回滚 vs L1"该行标记错误"语义；P2-RC-070）**：静态判定 = P2-RC-070 已登记"补货单创建失败 per-line 错误标记/重试缺失（事务回滚式无独立错误态）"。运行时确认事务边界（@BizMutation 单事务）与 per-line 错误态实际行为——全批回滚 vs L1"该行标记错误"语义。
- **A4.2.173（SP-4 UC-DRP-06 历史出库仅取 StockMove OUT（inventory），未取销售订单行（owner doc 列多源）对 SS 计算偏差）**：静态判定 = 主需求源 OUT 已满足。运行时确认历史出库数据源口径（仅 StockMove OUT vs owner doc 多源）对安全库存 STATISTICAL 法计算结果的实际偏差可接受。

剩余差距：四项均为只读运行时确认。缺陷项（P2-RC-069/070/072 登记不强制；P1-RC-081/082 不属本计划 §7 行）修复归 MR1；本计划仅确认运行时行为以维持/细化裁决，不实施修复。

## Goals

- 对 A4.2.170-A4.2.173 四项存疑点产出运行时行为证据链，输出验证报告落盘 `docs/audits/`。
- 每项给出 §2 判据裁决：确认边界行为（0 值补货单产生/拒绝 / forecast 无噪声 / 全批回滚 / SS 偏差可接受）→ 与 A1.48 §6 既有 finding（P2-RC-069/070/072 + reuse P1-MA1-022）衔接维持；若运行时发现任一行为**翻转**（如 0 量补货单被静默创建且下游接受、或存在 per-line 错误态机制、或 SS 偏差致错误补货决策），细化裁决并回写 A1.48 报告。预期不触发 MR0（A1.48 §7 P0 评估：netRequirement max(0,...) 防负 + 跨域补货单缺失属功能缺失类 + SS 偏差属计算正确性非会计/GL 破坏）。
- 完成后回写 roadmap A4.2.170-A4.2.173 `todo → done`，并按裁决更新 arm-index（维持注记，无未经比对新建）。

## Non-Goals

- 不实现 P2-RC-069/070/071/072 / P1-RC-081/082 修复——修复义务归 MR1 R1.0 展开器（P1-RC-081/082 Q4 强制实现；P2 登记不强制）。
- 不修改任何真相源（product-scope/use-cases/owner doc 需求契约段落）。
- 不修改 ORM/生产代码/SPI 实现（本计划为只读确认）。
- P1-RC-081/082（UC-DRP-07/08 CrossDock/LeadTime 功能完全缺失）与 P2-RC-071（UC-DRP-05 参数校验缺失）无对应 §7 SP 行（A1.48 §7 仅 SP-1..SP-4），不在 A4.2.170-173 范围（A1.48 §6 已裁决，本计划不重复验证其缺失性）。
- 不复跑 A1.48 §5 已裁决事项（UC-DRP-01/03/05 接受结论 + P1-MA1-022 resolved 不重审）。

## Task Route

- Type: `verification or audit work`
- Owner Docs: `docs/audits/2026-08-05-1400-3-rc-ma1-a1-48-drp-full.md`（§5/§6/§7）+ `docs/design/drp/`（use-cases.md / drp-engine.md / safety-stock.md）
- Skill Selection Basis: roadmap MA4 全部工作项指定 `docs/skills/multi-dimensional-audit-prompt.md`。本计划为只读审计，无代码变更。

## Infrastructure And Config Prereqs

- No infra prereqs beyond existing baseline. 四项存疑点均经静态证明运行时决定性事实得出（零校验/零分支/单事务结构 ⇒ 运行时行为确定）+ 既有 L4 测试重跑佐证（`mvn test -pl module-drp/erp-drp-service`：`TestErpDrpEngine`/`TestErpDrpScheduleRelease`/`TestErpDrpSafetyStock`/`TestErpDrpForecastSource`/`TestErpDrpInventoryIntegration`）。无需运行应用或 DB。

## Execution Plan

### Phase 1 - 运行时证据采集与验证报告撰写（A4.2.170-A4.2.173）

Status: completed
Targets: `docs/audits/2026-08-07-1410-rc-ma4-a4-2-170-173-drp-runtime.md`（新建验证报告）
Skill: `docs/skills/multi-dimensional-audit-prompt.md`

- Item Types: `Proof`
- Prereqs: A4.2 done ✓；A1.48 done ✓

- [x] **A4.2.170 0 值补货单释放确认（UC-DRP-02；P2-RC-069）**：确认 `DrpReleaseService` 无 approvedQty>0 校验 + 0 值 ErpDrpLine 经 approvePlan（approvedQty 回填 suggestedQty=0）→ releaseLine 的产物：是否创建 0 量 TransferOrder/PurchaseOrder（`TestErpDrpScheduleRelease` 既有用例重跑 + 0 值行释放探针）；下游 inventory/purchase 是否拒绝 0 量单。裁决：确认产生/确认被拒/确认不产生（三选一命中）→ 维持或细化 P2-RC-069。
  - Skill: `docs/skills/multi-dimensional-audit-prompt.md`
- [x] **A4.2.171 forecast-consume-enabled 默认 true 无预测回归确认（UC-DRP-02）**：确认 forecast 消费查询仅取 APPROVED 预测（无 APPROVED 数据→forecastDemand=0 无贡献）+ 重跑 `TestErpDrpForecastSource` config-gated 用例 → 默认 true 不引入噪声。裁决：确认无回归（维持 A1.48 接受 on 主路径）。
  - Skill: `docs/skills/multi-dimensional-audit-prompt.md`
- [x] **A4.2.172 releaseApproved 批量释放事务语义确认（UC-DRP-04；P2-RC-070）**：确认 `releaseApproved` 批量释放为单事务（@BizMutation 原子性）——某行跨域补货单创建失败（如 inventory/purchase 校验拒绝）→ 全批回滚 vs L1"该行标记错误"语义（错误态字段/重试机制 grep census：per-line error 标记是否不存在）。裁决：确认全批回滚（无 per-line 错误态）→ 维持 P2-RC-070。
  - Skill: `docs/skills/multi-dimensional-audit-prompt.md`
- [x] **A4.2.173 历史出库口径 SS 计算偏差确认（UC-DRP-06；P2-RC-072）**：确认安全库存 STATISTICAL 法历史需求源仅取 StockMove OUT（inventory），未取销售订单行（owner doc 列多源对照）+ 重跑 `TestErpDrpSafetyStock` 三法用例 → 偏差量化（口径差异对 SS 计算影响可接受）。裁决：确认偏差可接受（维持 A1.48 接受 on 主路径 + P2-RC-072 维持）。
  - Skill: `docs/skills/multi-dimensional-audit-prompt.md`
- [x] **验证报告撰写**：四项存疑点各出 §裁决（行为确认 + 既有 finding 衔接维持或细化）+ §与既有 finding 衔接（P2-RC-069/070/072 + reuse P1-MA1-022 交叉引用 + P1-RC-081/082[不在本切片，仅声明维持]）+ §过程纪律自检（checker 退出码门控——无代码变更 actual=baseline；closure-audit 独立性声明）+ §真相源冻结声明（不修改需求契约段落）。报告落盘。
  - Skill: `docs/skills/multi-dimensional-audit-prompt.md`

Exit Criteria:

> 本阶段为只读审计，无生产代码变更。

- [x] 验证报告落盘，含四项存疑点各自裁决 + file:line 证据 + §2 判据命中分支
- [x] 每项裁决明确：边界行为确认（维持）/ 行为翻转（细化裁决并回写 A1.48 报告）/ 升级触发 MR0

### Phase 2 - Finding 衔接、roadmap/log 同步

Status: completed
Targets: `docs/backlog/requirement-compliance-roadmap.md`（A4.2.170-173 done）、`docs/audits/arm-index.md`（维持注记追加）、`docs/logs/2026/08-07.md`
Skill: none

- Item Types: `Decision | Add`
- Prereqs: Phase 1 报告落盘

- [x] `Decision` arm-index 衔接裁决：P2-RC-069/070/072 维持 P2（登记不强制）；P1-RC-081/082 维持 P1（Q4 强制实现，修复归 MR1）仅声明不重审；reuse P1-MA1-022 维持注记。无新 finding 新建（全部维持）。
- [x] `Add` roadmap A4.2.170-A4.2.173 `todo → done`；`docs/logs/2026/08-07.md` 追加完成条目。

Exit Criteria:

- [x] roadmap 四项状态已更新为 done 且与报告裁决一致
- [x] arm-index 维持注记已追加（无未经比对直接新建的 finding）

## Draft Review Record

- Independent draft review iteration 1: accept (task ses_025311f0dffe31GzECMMlIWwlU) — 0 Major；2 Nit（P2-RC-071 无 §7 SP 行未在 Non-Goals 显式排除 / 测试清单 Infra 段缺 TestErpDrpSimulation）。Nit 1 已修复（Non-Goals 补 P2-RC-071 显式排除）；Nit 2 系系列惯例（参考计划同型），保留。全部 file:line/finding ID/P0 评估/roadmap 状态（A4.2.170-173 todo + Deps A4.2 done + Skill）经 live repo 核验命中。

## Closure Gates

> 本计划为只读审计（零生产代码/ORM/api.xml/view.xml/真相源变更；测试侧新增 2 个探针）。closure 时确认 checker 未触发 actual > baseline。

- [x] 范围内行为完成（四项存疑点均有运行时证据 + 明确裁决）
- [x] 相关文档对齐（报告落盘 + roadmap/log 同步 + arm-index 衔接裁决记录）
- [x] 已运行验证（`mvn test -pl module-drp/erp-drp-service` 既有测试类重跑 + 2 新增探针 = 36 tests 全绿 + checker actual=baseline 确认）
- [x] 无范围内项目降级为 deferred/follow-up
- [x] 独立草案审查已完成并记录
- [x] 文本一致性已验证：状态、阶段、门控和日志都一致
- [x] 结束审计由独立子代理（新会话）执行；执行者未自我审计且未将此留为 `[ ]` 作为人工门控占位符
- [x] 结束证据存在于文件中

## Deferred But Adjudicated

### P2-RC-069/070/072 + P1-RC-081/082 修复实现

- Classification: `out-of-scope improvement`
- Why Not Blocking Closure: 本计划仅运行时确认；P1-RC-081/082 Q4 强制实现归 MR1 R1.0 展开器，P2 登记不强制。本审计维持分级不撤销。
- Successor Required: yes（MR1 R1.0 展开为 RC-R1.n 时承接）

## Closure

Status Note: 四项存疑点（A4.2.170-173）全部经 live code 实测 + L4 重跑 + 2 新增探针实证收口：A4.2.170 确认 0 量补货单产生且被下游接受（P2-RC-069 潜在风险证实，细化维持 P2）/ A4.2.171 确认无回归（不新建）/ A4.2.172 确认全批回滚无 per-line 错误态（维持 P2-RC-070）/ A4.2.173 确认偏差可接受（维持 P2-RC-072）。A1.48 §7 静态判定无一翻转，零新 finding，不触发 MR0，不归 MR1（本审计）。验证报告 `docs/audits/2026-08-07-1410-rc-ma4-a4-2-170-173-drp-runtime.md` 9 段齐全。独立结束审计通过后关闭。

Closure Audit Evidence:

- Auditor / Agent: independent subagent（fresh session，task `ses_025122828ffeqqV5XOykhYmWqX`）
- Evidence: 8 项检查全 PASS —— (1) 计划一致性（Phase 1/2 全 [x] + Status completed，Closure Gates 待审计后勾选）；(2) 报告 9 段齐全 + 4 项裁决匹配 + 8 处 file:line 抽查命中；(3) 2 探针存在 + 独立重跑 `mvn test -pl module-drp/erp-drp-service` = 36 tests 全绿；(4) roadmap A4.2.170-173 全 done ✅ 且与裁决一致；(5) arm-index blockquote :310 + 3 行内联注记 + 零新 finding；(6) 日志顶部新条目；(7) checker 复跑 actual==baseline 零漂移；(8) git status 仅测试+文档变更，零生产代码/ORM/配置漂移。Verdict: **passes closure audit，无阻塞问题**。

Follow-up:

- （无非阻塞跟进项；四项存疑点全数收口，修复义务归 MR1 R1.0 展开器——P2-RC-069/070 纯 BizModel 预授权、P2-RC-072 纯 BizModel/config 预授权 + 联合变分 ORM ask-first 归 P1-RC-082、P1-RC-081/082 Q4 强制实现，均不在本计划范围）
