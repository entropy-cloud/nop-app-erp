# 2026-08-07-1533-1 rc-ma4-a4-2-182-185 notify 接收人解析器与 kill-switch 运行时确认

> Plan Status: completed
> Last Reviewed: 2026-08-07
> Source: `docs/backlog/requirement-compliance-roadmap.md` Work Items A4.2.182 / A4.2.183 / A4.2.184 / A4.2.185
> Related: `docs/audits/2026-08-06-2243-3-rc-ma1-a1-51-notify-full.md`（A1.51 §7 SP-1..SP-4 + P2-RC-081）/ `docs/design/notify/use-cases.md`（UC-SYS-03）/ `docs/design/notify/README.md`（§配置项）
> Audit: required
> Mission: requirement-compliance
> Work Item: A4.2.182-A4.2.185（notify 域 MA4 运行时验证）
> Approval: 只读审计 + 测试侧补充（A4 MA4 类目），受 `docs/discussions/2026-08-07-1140-rc-approval-inventory-analysis.md` §3 A4 确认自动执行（Q2）覆盖；本计划不实施生产代码修复（P2-RC-081 修复归 MR1）

## Current Baseline

notify 域切片（A1.51）MA1 报告 `2026-08-06-2243-3-rc-ma1-a1-51-notify-full.md` §7 共列出 4 项静态存疑点，全部为本计划范围（A4.2.182-A4.2.185）。A1.51 §5 对 UC-SYS-03 已裁决「接受」（ROLE/ORG/USER_LIST/PARTNER 四类 resolver L3 实现完整，运行时覆盖交 MA4）；唯一 finding P2-RC-081（kill-switch 失效）为 P2 登记不强制，修复归 MR1。§7 P0 即时通道评估：无活跃数据破坏候选，无需 MR0。

- **A4.2.182（A1.51 SP-1 UC-SYS-03 ROLE resolver 运行时 roleName 匹配）**：静态实现完整——`NotificationRecipientResolver.resolveRole:117` 经 `in("roleName", roleNames)` 查 NopAuthRole 后再 `roleNameSet.contains` 二次过滤（`:136-141`）→ `in("roleId", ...)` 查 NopAuthUserRole → userId 集合。运行时存疑：平台 nop-auth 角色数据 roleName 字段与模板配置 roles 列表的一致性 + 种子数据覆盖。**测试覆盖缺口**：erp-notify-service 全部 5 个测试类仅 USER_LIST 有 dedicated 测试（A1.51 §4 证实），ROLE/ORG/PARTNER 零 L4 覆盖。nop-auth 实体（NopAuthRole/NopAuthUser/NopAuthUserRole）测试侧种子先例：`TestErpPurPaymentApprovalNotifications.seedRole:259-286`（module-purchase）。
- **A4.2.183（A1.51 SP-2 USER_LIST ${var} 动态插值运行时覆盖）**：静态实现完整——`interpolateConfig:206` 对 recipientConfig 中 `${var}` 按 context 插值（`CFG_VAR_PATTERN`，`:62-63`）。运行时存疑：多域消费者（cs/finance/sales 提单人 submitterUserId）动态解析。**跨域消费者先例已存在**：`TestErpPurPaymentApprovalNotifications.seedResultTemplate:234-235` 已用 `{"userIds":["${submitterUserId}"]}` 配置 + 经 notify 链路（该测试类在 erp-pur-service，覆盖 `wf.pur-payment.result` 事件）。notify 域自身 L4 测试全部静态 userIds，无 dedicated ${var} 插值断言。
- **A4.2.184（A1.51 SP-3 ORG resolver deptId 精确匹配运行时）**：静态实现完整——`resolveOrg:158` 经 `eq("deptId", deptId)` 精确匹配 NopAuthUser；L1 `use-cases.md:83-84`「deptId → NopAuthUser」为精确匹配语义（未要求子部门级联）。运行时确认：精确匹配是否满足 L1、是否需子部门递归（静态语义符合 L1，运行时验证确认）。
- **A4.2.185（A1.51 SP-4 erp-notify.enabled=false kill-switch 运行时行为）**：静态已确认 `ErpNotifyConfigs.CONFIG_NOTIFY_ENABLED`（`:15-16`）声明但 `AppConfig.var` 零消费（`ErpSysNotificationNotifyProcessor.notify:40-63` 无总开关检查）→ notify 仍派发（P2-RC-081）。运行时确认：设 enabled=false 后 notify 是否仍落库（静态倾向确认仍落库）。**测试侧先例**：`TestErpCsSlaNotification.testNotifyDisabledSkipsDispatch:101-122` 用 `AppConfig.getConfigProvider().assignConfigValue(key, "false")` 模式 + finally 恢复。

剩余差距：四项均为只读运行时确认 + erp-notify-service 测试侧补充（ROLE/ORG/${var}/kill-switch 各补 dedicated 运行时测试，均限测试类目，不触生产代码）。P2-RC-081 维持 P2（登记不强制，修复归 MR1）；本计划仅确认运行时行为，不改变 Q4 强制实现义务（P2 不适用），不实施生产代码修复。

## Goals

- 对 A4.2.182-A4.2.185 四项存疑点产出运行时行为证据链，输出验证报告落盘 `docs/audits/`（对齐同批 MA4 运行时报告范式：§裁决 + file:line + L4 断言证据）。
- 每项给出 §2 判据裁决：A4.2.182/183/184 预期主路径闭合或接受（运行时证实 resolver 与插值行为）；A4.2.185 确认 kill-switch 失效运行时行为 → 维持 P2-RC-081（P2 登记不强制，修复归 MR1）。若运行时发现活跃数据破坏则触发 MR0（预期不触发——A1.51 §7 P0 评估无活跃破坏）。
- 完成后回写 roadmap A4.2.182-A4.2.185 `todo → done`，并按裁决更新 arm-index（P2-RC-081 维持注记，无未经比对新建）。

## Non-Goals

- 不实现 P2-RC-081（kill-switch 接线）修复——修复义务归 MR1 R1.0 展开器（纯 BizModel 逻辑预授权不触 ask-first，按 A1.51 §触及保护区域汇总）。
- 不修改任何真相源（product-scope/use-cases/owner doc 需求契约段落）。
- 不修改 ORM/生产代码/跨域 Facade 接线（本计划只读确认；唯一代码变更 = erp-notify-service 测试侧补充，不触生产代码）。
- 不覆盖 MR1 阻塞行 A4.2.3/A4.2.79/A4.2.119（MR1 R1.0 未启动，保留 todo 待 MR1 落地后回队）。
- 不复跑 MA2/A2.14 审计；不重审 P1-RC-081/082（非本域）。

## Task Route

- Type: `verification or audit work`
- Owner Docs: `docs/audits/2026-08-06-2243-3-rc-ma1-a1-51-notify-full.md`（§5/§6/§7）+ `docs/design/notify/use-cases.md`（UC-SYS-03）+ `docs/design/notify/README.md`（§接收人解析策略/§配置项）
- Skill Selection Basis: roadmap MA4 全部工作项指定 `docs/skills/multi-dimensional-audit-prompt.md`。本计划以只读审计为主 + 测试侧补充（erp-notify-service 测试类目），无生产代码变更。

## Infrastructure And Config Prereqs

- No infra prereqs beyond existing baseline. 四项存疑点经静态证据 + 测试侧补充确认：
  - A4.2.182 ROLE：seed NopAuthRole/NopAuthUser/NopAuthUserRole（对齐 `TestErpPurPaymentApprovalNotifications.seedRole:259-286` 模式）→ 模板 ROLE + roles 配置 → GraphQL `ErpSysNotification__notify` → 断言 recipientUserId 命中。
  - A4.2.183 ${var}：模板 USER_LIST `{"userIds":["${submitterUserId}"]}` + context 含 submitterUserId → notify → 断言插值后接收人 == 动态值；并 census cs/finance/sales 消费者 context 键传递（submitterUserId/escalationUserId 等）。
  - A4.2.184 ORG：seed NopAuthUser 含 deptId → 模板 ORG + deptId 配置 → notify → 断言精确匹配（同 deptId 命中、无子部门递归需求确认）。
  - A4.2.185 kill-switch：`AppConfig.getConfigProvider().assignConfigValue("erp-notify.enabled", "false")`（对齐 `TestErpCsSlaNotification:103` 模式）+ notify → 断言仍落库（kill-switch 失效运行时证实）→ finally 恢复 true。
  - 测试运行：`mvn test -pl module-notify/erp-notify-service`（新测试方法追加至既有测试类或新建测试类，限测试类目）。

## Execution Plan

### Phase 1 - 运行时证据采集与验证报告撰写（A4.2.182-A4.2.185）

Status: completed
Targets: `docs/audits/2026-08-07-1533-rc-ma4-a4-2-182-185-notify-runtime.md`（新建验证报告）
Skill: `docs/skills/multi-dimensional-audit-prompt.md`

- Item Types: `Proof | Add`
- Prereqs: A4.2 done ✓；A1.51 done ✓

- [x] **A4.2.182 ROLE resolver 运行时 roleName 匹配确认（UC-SYS-03 ROLE）**：补 dedicated 运行时测试——seed NopAuthRole（roleName）+ NopAuthUser + NopAuthUserRole（对齐 seedRole 模式）→ 模板 `recipientResolver=ROLE` + `recipientConfig={"roles":[...]}` → GraphQL notify → 断言 recipientUserId == 种子用户；同时验证不匹配角色名 → config-gated 空集（resolveRole:119-144 行为）。裁决：主路径闭合或接受（运行时证实 ROLE 链 `in("roleName")`+`roleNameSet.contains` 双过滤在平台 nop-auth 数据下正确）。✅ 已落地：`TestErpSysNotificationRecipientResolverRuntime#testRoleResolverMatchesRoleName` + `#testRoleResolverUnknownRoleNameConfigGatedEmpty` 全绿 → **主路径闭合**（报告 §2-1）
  - Skill: `docs/skills/multi-dimensional-audit-prompt.md`
- [x] **A4.2.183 USER_LIST ${var} 动态插值运行时确认（UC-SYS-03 USER_LIST）**：补 dedicated 运行时测试——模板 USER_LIST + `{"userIds":["${submitterUserId}"]}` + context 含 submitterUserId → notify → 断言接收人 == 动态值（interpolateConfig:206-221）；同时 census 多域消费者（cs/finance/sales 提单人）context 键传递现状（submitterUserId/escalationUserId），确认 ${var} 在多域调用链运行时可达。裁决：主路径闭合（运行时证实插值 + 消费者 census）。✅ 已落地：`testUserListVarInterpolation` 全绿 + 4 审批 wf listener（pur-payment/sal-receipt/ast-disposal/hr-salary）submitterUserId + cs escalationUserId + fin/sal 事件上下文 census → **主路径闭合**（报告 §2-2）
  - Skill: `docs/skills/multi-dimensional-audit-prompt.md`
- [x] **A4.2.184 ORG resolver deptId 精确匹配运行时确认（UC-SYS-03 ORG）**：补 dedicated 运行时测试——seed NopAuthUser 含 deptId → 模板 ORG + `recipientConfig={"deptId":...}` → notify → 断言精确命中；确认 L1「deptId → NopAuthUser」精确匹配语义运行时成立、无需子部门递归（resolveOrg:158-173）。裁决：主路径闭合（精确匹配语义符合 L1）。✅ 已落地：`testOrgResolverDeptIdExactMatch`（同 deptId 命中 + 异 deptId 零行）全绿 → **主路径闭合**（报告 §2-3）
  - Skill: `docs/skills/multi-dimensional-audit-prompt.md`
- [x] **A4.2.185 erp-notify.enabled=false kill-switch 运行时确认（P2-RC-081）**：补运行时探针——`assignConfigValue("erp-notify.enabled", "false")` → seed 模板 + notify → 断言通知仍落库（CONFIG_NOTIFY_ENABLED 零消费运行时证实）→ finally 恢复 true。裁决：维持 P2-RC-081（kill-switch 失效运行时确认，P2 登记不强制，修复归 MR1）。✅ 已落地：`testNotifyEnabledFalseStillDispatches` 全绿 → **确认仍落库，维持 P2-RC-081**（报告 §2-4）
  - Skill: `docs/skills/multi-dimensional-audit-prompt.md`
- [x] **验证报告撰写**：四项存疑点各出 §裁决（主路径闭合 / 接受 / 维持 P2-RC-081）+ file:line 证据 + L4 断言 + §与既有 finding 衔接（P2-RC-081 维持注记，无未经比对新建）+ §过程纪律自检（checker 退出码门控——测试补充不触生产代码 actual=baseline；closure-audit 独立性声明）+ §跨域探针纪律声明（A4.2.183 consumer census 只读）。报告落盘。✅ 已落地：`docs/audits/2026-08-07-1533-rc-ma4-a4-2-182-185-notify-runtime.md`（9 段齐全 + §0 差异增量声明）
  - Skill: `docs/skills/multi-dimensional-audit-prompt.md`

Exit Criteria:

> 本阶段以只读审计为主 + 测试侧补充（均限 erp-notify-service 测试类目，不触生产代码）。A4.2.183 触及跨域消费者 census——只读探针，不改他域代码。

- [x] 验证报告落盘，含四项存疑点各自裁决 + file:line 证据 + L4 断言 + §2 判据命中分支
- [x] 测试侧补充已落地并运行通过（`mvn test -pl module-notify/erp-notify-service`），作为运行时证据（23/23 全绿，含 5 新增）
- [x] 每项裁决明确：主路径闭合 / 接受 / 维持 P2-RC-081（P2 登记不强制）+ 运行时证据记录，或升级触发 MR0（未触发）

### Phase 2 - Finding 衔接、roadmap/log 同步

Status: completed
Targets: `docs/backlog/requirement-compliance-roadmap.md`（A4.2.182-185 done）、`docs/audits/arm-index.md`（P2-RC-081 维持注记追加）、`docs/logs/2026/08-07.md`
Skill: none

- Item Types: `Decision | Add`
- Prereqs: Phase 1 报告落盘

- [x] `Decision` arm-index 衔接裁决：P2-RC-081（kill-switch 失效）维持 P2（运行时证据，修复归 MR1，登记不强制）；A4.2.182/183/184 无新 finding（主路径闭合/接受不登记）；禁止未经比对新建 finding。✅ 已落地：arm-index 追加「A4.2.182-185 notify 运行时确认」RC 交叉引用注记 + P2-RC-081 行内联 `【RC MA4 A4.2.185 运行时确认】` 注记（维持 P2，0 新 finding）
- [x] `Add` roadmap A4.2.182-A4.2.185 `todo → done`；`docs/logs/2026/08-07.md` 追加完成条目。✅ 已落地：roadmap 四行 done ✅ + 文件头最后更新注记 + 日志条目

Exit Criteria:

- [x] roadmap 四项状态已更新为 done 且与报告裁决一致
- [x] arm-index P2-RC-081 维持注记已追加（无未经比对直接新建的 finding）

## Draft Review Record

- Independent draft review iteration 1: accept (task ses_024d93b79ffeU7hRGYNKeL5O0W) — 0 Major；3 项非阻塞 Minor：①Baseline 引 A1.51 §4 应为 §3（实质已实仓验证成立，仅引用 nitpick）；②Phase 2 日志目标 08-07.md 若执行跨日按实际执行日期落笔（执行时处理）；③resolveRole:119-144 vs :136-141 均为准确子区间无冲突。实仓复核全部代码 claims 成立（resolveRole:117/resolveOrg:158/interpolateConfig:206/notify:40-63/CONFIG_NOTIFY_ENABLED:15-16/seedRole:259-286/${submitterUserId}:235/assignConfigValue 模式）；A4.2.3/79/119 正确排除 + MR1 R1.0 人工门控尊重 + Q2 预授权覆盖确认。审查通过，转 active。

## Closure Gates

> 本计划以只读审计为主 + 测试侧补充（限 erp-notify-service 测试类目）。closure 时确认 checker 未触发 actual > baseline（测试补充不触生产代码，预期无漂移）。

- [x] 范围内行为完成（四项存疑点均有运行时证据 + 明确裁决）
- [x] 相关文档对齐（报告落盘 + roadmap/log 同步 + arm-index P2-RC-081 维持注记）
- [x] 已运行验证（`mvn test -pl module-notify/erp-notify-service` 相关测试类 23/23 全绿 + checker actual=baseline 确认）
- [x] 无范围内项目降级为 deferred/follow-up
- [x] 独立草案审查已完成并记录
- [x] 文本一致性已验证：状态、阶段、门控和日志都一致
- [x] 结束审计由独立子代理（新会话）执行；执行者未自我审计且未将此留为 `[ ]` 作为人工门控占位符
- [x] 结束证据存在于文件中

## Deferred But Adjudicated

### P2-RC-081 kill-switch 接线修复实现

- Classification: `out-of-scope improvement`
- Why Not Blocking Closure: 本计划仅运行时确认 + 测试侧补充；P2-RC-081 为 P2 登记不强制（A1.51 §5/§6），修复义务归 MR1 R1.0 展开器（纯 BizModel 逻辑预授权不触 ask-first）。本审计维持分级不撤销。
- Successor Required: yes（MR1 R1.0 展开为 RC-R1.n 时承接）

### MR1 阻塞行 A4.2.3/A4.2.79/A4.2.119 回队验证

- Classification: `watch-only residual`
- Why Not Blocking Closure: 三行均显式标注 MR1 阻塞（P1-RC-008/P1-RC-031/P1-RC-049 修复未落地 + MR1 R1.0 未启动），roadmap 已声明「保留 todo 待 MR1 落地后回队」；不属本计划范围。
- Successor Required: yes（MR1 落地后回队）

## Closure

Status Note: completed（独立结束审计通过 2026-08-07）

Closure Audit Evidence:

- Auditor / Agent: 独立结束审计子代理（fresh session，无执行者上下文复用；opencode 独立会话）
- 独立会话对实时仓库逐项核验，全部通过：
  1. **状态一致性**：Plan Status=completed（:3）；Phase 1/2 Status=completed（:56/:84）；Phase 1/2 全部 items + Exit Criteria 均 `[x]`（:63-71/:78-80/:91-92/:96-97）；Closure Gates 1-6 `[x]`（:107-112），末 2 门由本审计通过后勾选。
  2. **Phase 1 交付物**：报告 `docs/audits/2026-08-07-1533-rc-ma4-a4-2-182-185-notify-runtime.md` 存在，含四项裁决（A4.2.182/183/184 主路径闭合 §2-1/2-2/2-3；A4.2.185 确认仍落库→维持 P2-RC-081 §2-4）+ file:line 证据 + L4 断言 + §7 过程纪律自检 5 项全勾；测试类 `TestErpSysNotificationRecipientResolverRuntime.java` 存在，5 @Test 方法名与报告一一对应；seedRole 模式实仓比对 `TestErpPurPaymentApprovalNotifications.seedRole:259-286`（NopAuthRole[roleId+roleName]/NopAuthUser 九字段/NopAuthUserRole 三段一致）；kill-switch 测试 `assignConfigValue(...false)` + finally 恢复 true（:117/:127-129）；ORG 测试 seed D-100/D-200 两用户（:100-101）。
  3. **测试实测**：`mvn test -pl module-notify/erp-notify-service` → Tests run: 23, Failures: 0, Errors: 0, Skipped: 0 — BUILD SUCCESS（2026-08-07 实测）。
  4. **checker 零漂移**：`bash docs/audits/nop-compliance-checker.sh` 实测 R1d=14/R2a=34/R2b=229/R2c=1382/R2d=34/R8=0，与 `compliance-baseline.md:292-310` 机器可读 BASELINE 块逐项相等；git status 仅 docs + 测试源文件 + _cases 快照产物，零生产代码变更。
  5. **Phase 2 交付物**：roadmap A4.2.182-185 四行 `done ✅` + 报告引用（:343-346）+ 文件头更新注记（:3）；arm-index P2-RC-081 行内联 `【RC MA4 A4.2.185 运行时确认】`（:296）+ RC 交叉引用注记 blockquote（:308），0 新 finding；日志 `docs/logs/2026/08-07.md:3` 条目存在。
  6. **范围诚实**：Deferred But Adjudicated 仅含 out-of-scope（P2-RC-081 修复→MR1 R1.0）与 watch-only residual（A4.2.3/79/119→MR1 阻塞），与 Non-Goals（:31-35）一致，无范围内项目静默降级。
  7. **Anti-hollow（独立核验 file:line）**：`NotificationRecipientResolver.resolveRole:117`、`:136-141` roleNameSet.contains、`resolveOrg:158`、`interpolateConfig:206`、`CFG_VAR_PATTERN:62`、`ErpSysNotificationNotifyProcessor.notify:40-63`、`ErpNotifyConfigs:15-16` 全部与 live code 一致。
- 非阻塞观察：报告引 A1.51 §4 应为 §3 的 nitpick 已在 Draft Review Record Minor ① 记录并实质验证成立，不影响闭合。

Follow-up:

- 无（P2-RC-081 修复归 MR1 R1.0 展开器，属 successor 正常承接，非本计划缺陷）
