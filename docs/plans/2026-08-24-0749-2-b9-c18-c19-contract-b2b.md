# 2026-08-24-0749-2-b9-c18-c19-contract-b2b 批次 B9 用例实施：C18 合同生命周期与返利计提结算 + C19 B2B ASN 自动收货与物流到岸成本

> Plan Status: active
> Mission: integration-test
> Work Item: B9（C18, C19；主导域 contract + b2b）
> Last Reviewed: 2026-08-24
> Source: `docs/backlog/integration-test-roadmap.md`（B9 行，M0.3 追加）
> Related: `2026-08-24-0749-1-b8-c15-c16-c17-crm-cs-hr`（同批先行计划，B8 排其前）
> Audit: required

## Current Baseline

（全部事实 2026-08-24 实仓核验）

- **前置依赖**：M0.1 设计文档（`docs/design/integration-testing.md` §6 C18/C19 用例规格 + §3.4 机制实证结论 + §4 粒度约定 + §5 xwf 规避清单）+ M0.2 基建 + B1-B8（B8 为同批先行计划，B9 在其后执行——roadmap B9 行 Deps = M0.1 + M0.2，批内排序由本批计划序号确定）。
- **基建就绪**（M0.2/B1-B8 产物）：基类 `ErpIntegrationTestCase`（机制 (c) 文件 H2 双模 + 共享 step helper）；`_cases` 目录约定；surefire 模块级串行化已落地；已有 **16 个集成用例类**（Pilot + B1-B7 = 1+2+2+2+3+2+2+2，it/ 目录实仓核验；B8 先行落地后 = 19）作为范本，**B9 按同范式实现（1 用例 = 1 类 1 方法 + @BeforeAll fresh-DB + RECORDING→CHECKING 往返）**。
- **ct/b2b 域既有测试资产（本批业务参照，不重复实现内部细节）**：`TestErpCtContractRebate`（返利计提/结算 GraphQL 用例，`ErpCtRebateAgreementRunAccrualProcessor`/`ErpCtRebateSettlementPostSettlementProcessor`）+ ct 状态机矩阵测试 + Playwright `ct-contract-lifecycle.action.spec.ts`（生命周期六动作浏览器先例）、`TestErpB2bAsn*`（4 类：ASN 状态机/入库集成/状态机矩阵，含 `ErpB2bAsnCreateReceiveFromAsnProcessor`）、`TestErpLogPath2LandedCost`（物流 path-2 到岸成本自动创建先例）、`TestErpLogDeliveryBooking`/`TestErpLogShipmentPostingEnd`（物流送达/过账先例）。**C05（B3）已落地 `ErpInvLandedCost` 到岸成本分摊过账集成用例，C19 的 landed cost 单断言直接复用其 findPage 模式**。
- **C18 动作面（实仓核验在位）**：`ErpCtContractBizModel` 实仓动作 = `submit`/`activate`/`amend`/`suspend`/`resume`/`terminate`/`approveTermination`/`rejectTermination`/`expire`/`rejectAmend`——生命周期六动作与设计文档 §6 C18 步骤 1 一致，**但 `terminate` 为两阶段动作（RC-R1.34 法定门禁不可 config 关闭）：terminate 生成待法务审批记录且不改合同状态，TERMINATED 终态由 `approveTermination` 达成——六动作序列后断言 TERMINATED 必须先 `ErpCtContract__approveTermination`（设计文档 §6 C18 步骤 1 为单阶段假设，实施期登记勘误）**；返利计提/结算动作在**返利协议/结算实体**而非合同实体：`ErpCtRebateAgreementBizModel.runAccrual`（`ErpCtRebateAgreementRunAccrualProcessor`）+ `ErpCtRebateSettlementBizModel.postSettlement`（`ErpCtRebateSettlementPostSettlementProcessor`）——**设计文档 §6 C18 步骤 2/3 写为 `ErpCtContract__runAccrual`/`ErpCtContract__postSettlement`，动作宿主漂移预期（合同实体无此两动作），实施期以实仓（RebateAgreement/RebateSettlement）为准并登记勘误**。
- **C18 config（实仓核验）**：返利计提 config 门控键以 `ErpCtConfigs`/`ErpCtConstants` 实仓为准（runAccrual 聚合期间已过账 AP/AR 发票只读查询，postSettlement 生成跨域负额 credit memo + 计提翻转）——实施期核实门控键名与默认值，必要时 `@NopTestProperty`（B5-C10 spare-part-posting / B6-C12 payroll 科目 @NopTestProperty 先例）。
- **C18 前置 seed（实仓核验）**：`_init-data/` 下**无 `erp_ct_*` seed 文件**——合同/返利/版本数据全部走**自包含建数**（先例 C02 自包含合同 ErpCtContract__save+get 前置核对）；客户/物料复用 `erp_md_partner.csv`/`erp_md_material.csv` 既有 seed；已过账销售发票链走自包含建数（B2 C03/C04 先例链）。
- **C19 动作面（实仓核验在位）**：`ErpB2bAsnBizModel` 实仓动作 = `handleInboundWebhook`/`matchPurchaseOrder`/`createReceiveFromAsn`/`retryMatch`（`ErpB2bAsnCreateReceiveFromAsnProcessor`，config 门控 `erp-b2b.asn-auto-create-receive`，MATCHED→RECEIVED_TO_STOCK）——**与设计文档 §6 C19 步骤 1 一致**；收货走 `ErpPurReceive__submitForApproval`/`approve` [审批轴: DIRECT]（B1/B3 先例）；物流送达 `ErpLogShipment__handleTrackingWebhook`（`ErpLogShipmentHandleTrackingWebhookProcessor`，**config `erp-log.webhook-signature-required=false`——设计文档键名正确无漂移，宿主 = ErpLogConfigs（缺省 true）**）+ path-2 到岸成本自动创建（config `erp-log.path2-landed-cost-auto-create=true`，`TestErpLogPath2LandedCost` 先例）；`ErpInvLandedCost__findPage`（C05 先例）。
- **C19 config（实仓核验）**：`erp-b2b.asn-auto-create-receive`（`ErpB2bConfigs.CONFIG_ASN_AUTO_CREATE_RECEIVE`，缺省 false）→ 用例 `@NopTestProperty` 开启；`erp-log.webhook-signature-required`（`ErpLogConfigs.CONFIG_WEBHOOK_SIGNATURE_REQUIRED`，缺省 true，`ErpLogShipmentHandleTrackingWebhookProcessor` 消费）→ 关；`erp-log.path2-landed-cost-auto-create`（`ErpLogConstants.CONFIG_PATH2_LANDED_COST_AUTO_CREATE`，缺省 false，`AbstractErpLogShipmentDeliveredProcessor` 消费）→ 开——**三键缺省均不满足用例路径，必须 @NopTestProperty 指定（B5-C10 先例：config 门控 @NopTestProperty 开启）；注意 `erp-b2b.webhook-signature-required` 为 ASN 入站 webhook 独立键（`ErpB2bAsnHandleInboundWebhookProcessor` 消费），C19 流程不经 ASN webhook 无需关**。
- **C19 前置 seed（实仓核验）**：`_init-data/` 下**无 `erp_b2b_*`/`erp_log_*` seed 文件**——ASN/发运单全部自包含建数；供应商/物料/仓库复用 `erp_md_partner.csv`/`erp_md_material.csv`/`erp_md_warehouse.csv` 既有 seed（C05 先例：自包含承运商/发运单 ErpLogCarrier__save+ErpLogShipment__save）。
- **运行互斥**：集成测试与 E2E live server 共用 `db/erp.mv.db`——执行前 `lsof -i :8011`（8080 同查）确认无 live server（e2e-runbook「集成测试」节纪律）。
- **基线计数**：known-good-baselines 2026-08-24 权威计数源 **3824 tests / 0 failures / 0 errors / 1 skipped / 633 报告文件**；app-erp-all **44/0/0/1**。若 B8 已先行落地则以 B8 后基线为准。B9 预期差量 **+2 tests / +2 报告文件**（2 用例类各 1 类 1 方法）。
- **流程改进登记（B3 closure MINOR-2 兑现，B4-B8 已执行）**：最终 `clean install` 会清除 surefire XML 计数证据——全量回归后、clean 前先拷贝计数/XML 再登记基线（本计划 Phase 3 执行纪律）。
- **deferred 触发条件（B1/M0.2 watch-only residual）**：若执行期发生 seed 修正授权路径（roadmap 横切关注点 3），须同步测量登记重录耗时（以单用例 252K/类为预算参考）；本批默认自包含建数，预期不触发。
- **冻结时钟纪律**：C19 收货过账/到岸成本可能涉日期路径，但 C05（到岸成本）未用 frozen clock 全绿——**本批默认不引入 frozen clock，仅当 RECORDING 发现日期敏感漂移时按 C07/C13C14 先例引入本地扩展**，并登记快照时间戳 `*` 通配处置（B2/B4/B5/B6/B7 先例）。

## Goals

- **C18** = 测试类 `TestErpC18CtLifecycleRebate`（1 类 1 方法）：自包含合同（含返利条款）→ 生命周期六动作（submit/activate/amend/suspend/resume/terminate + **approveTermination 两阶段收尾**，状态机翻转断言 ACTIVE→SUSPENDED→ACTIVE→TERMINATED）→ 返利计提（`ErpCtRebateAgreement__runAccrual`，金额 = 条款 × 销售额，config 门控核实）→ 返利结算（`ErpCtRebateSettlement__postSettlement`：跨域负额 credit memo + 计提翻转红字）；三层全比对。
- **C19** = 测试类 `TestErpC19B2bAsnAutoReceiveLandedCost`（1 类 1 方法）：自包含 ASN（含行）→ 状态机推进（RECEIVED 等）→ `ErpB2bAsn__createReceiveFromAsn`（config 门控开启 → 采购收货草稿）→ `ErpPurReceive__submitForApproval`/`approve` [DIRECT]（收货过账）→ 自包含物流发运单 → `ErpLogShipment__handleTrackingWebhook`（DELIVERED，config 关签名）→ path-2 到岸成本自动创建（config 开）→ `ErpInvLandedCost__findPage`（自动创建的到岸成本单断言：FREIGHT 费用行 + 引用正确收货/运费）；三层全比对。
- 两用例 **RECORDING→CHECKING 往返全绿**。
- roadmap B9 状态 `todo` → `done`（经独立 closure audit）+ 日志 + 基线差量登记 + 设计文档 §6 C18/C19 动作宿主勘误登记（若发生）。

## Non-Goals

- B10 用例及 V.1/V.2（各归对应计划）。
- 生产代码/ORM/契约/API 变更——用例实施中发现的产品缺陷按 roadmap 横切关注点 4 走保护区域门禁（accounting/finance postings = plan-first + owner doc + tests；其余 = auto + dual-agent-approval），**不得在本计划内直接改生产代码**。
- 默认修改 seed CSV——确需修正走 roadmap 横切关注点 3「seed 修正授权」流程；默认以自包含建数满足前置。
- 不接 CI（roadmap 已裁决出 scope）；不做浏览器层 E2E 页面验证（分工边界）。
- 不实现未覆盖功能（横切关注点 6「以当前实现为准」）：C18/C19 步骤若与实仓行为不符，以实仓行为替代 + 设计文档勘误登记（如 C18 runAccrual/postSettlement 宿主 = RebateAgreement/RebateSettlement），不强行实现缺失功能。

## Task Route

- Type: `implementation-only change`（集成测试代码 + 快照 + 文档，零生产代码/契约/模型变更）
- Owner Docs: `docs/design/integration-testing.md`（§6 C18/C19 规格 + §3.4 编写纪律 + §4 粒度 + §5 xwf 清单）、`docs/backlog/integration-test-roadmap.md`（B9 行 + 横切关注点）、`docs/testing/e2e-runbook.md`（「集成测试」节）、`docs/testing/known-good-baselines.md`
- Skill Selection Basis: `nop-testing` 匹配测试基类选择/@NopTestConfig/@NopTestProperty（C19 三 config 门控 = B5-C10/B6-C12 同型）/快照录制回放（RECORDING→CHECKING）/三层验证模型；M0.2 试点 + B1-B8 集成用例类为同模式范本，ct/b2b/log 既有测试（ContractLifecycle/Rebate/Asn/Path2LandedCost）为业务参照。

## Infrastructure And Config Prereqs

- 文件型 H2 `db/erp.mv.db` fresh 机制已就绪（基类每类 1 次清理）；**与 E2E live server 互斥**——执行前 `lsof -i :8011`（8080 同查）确认无 live server。
- **C19 config（实仓核验，必配）**：`@NopTestProperty` 指定 `erp-b2b.asn-auto-create-receive=true` / `erp-log.webhook-signature-required=false` / `erp-log.path2-landed-cost-auto-create=true`（三键缺省值均不满足用例路径；webhook 键宿主 = `ErpLogConfigs`，`erp-b2b.webhook-signature-required` 为 ASN 入站 webhook 独立键不经 C19 流程）。
- **C18 config（待实施期核实）**：返利计提/结算门控键名与缺省值以 `ErpCtConfigs`/`ErpCtConstants` 实仓为准。
- 无端口/密钥/外部服务/环境变量新增；surefire 串行化已在 app-erp-all pom 落地。

## Execution Plan

### Phase 1 — C18 合同生命周期与返利计提结算（混合类型：1/3 Add + 1/3 Proof + 1/3 Decision，逐项标注）

Status: planned
Targets: `app-erp-all/src/test/java/io/nop/app/all/it/TestErpC18CtLifecycleRebate.java`、`app-erp-all/_cases/io/nop/app/all/it/TestErpC18CtLifecycleRebate/`
Skill: nop-testing

- Item Types: `Add | Proof | Decision`
- Prereqs: B8 done（批内排序）+ M0.1 + M0.2

- [ ] Add：**C18 测试类** `TestErpC18CtLifecycleRebate`（1 类 1 方法）——按设计文档 §6 C18 规格 + contract/rebate E2E 业务参照：自包含合同（含返利条款 `ErpCtRebateAgreement` 建数 + 前置核对）→ 生命周期六动作（`ErpCtContract__submit`→`activate`→`amend`→`suspend`→`resume`→`terminate`→`approveTermination`，状态机翻转断言 ACTIVE→SUSPENDED→ACTIVE→TERMINATED——**terminate 为两阶段：先 terminate 生成待法务审批记录（合同状态不变），后 approveTermination 达成 TERMINATED 终态（RC-R1.34 法定门禁不可 config 关闭）**）→ 返利计提 `ErpCtRebateAgreement__runAccrual`（金额 = 条款 × 已过账销售额，config 门控核实）→ 返利结算 `ErpCtRebateSettlement__postSettlement`（跨域负额 credit memo + 计提翻转红字同向取负）；静态载荷走 `input/N_step.json5` + `addVar`/`@var:` 动态 id。
  - Skill: nop-testing
- [ ] Decision：**C18 动作宿主与金额口径裁决**——① 返利计提/结算动作宿主（设计文档写 ErpCtContract__runAccrual/postSettlement，实仓在 ErpCtRebateAgreement/ErpCtRebateSettlement——以实仓为准登记勘误）+ **terminate 两阶段语义（terminate 不改状态返回合同实体，approveTermination 达成 TERMINATED——设计文档 §6 C18 步骤 1 单阶段假设为勘误；approveTermination 须插入动作序列，其 recordId 须查询 terminate 生成的 PENDING 审批记录（terminate 返回合同非记录，`TestErpCtContractRebate.pendingTerminationRecordId` 先例））**；② 计提金额口径（条款 × 已过账销售额，runAccrual 聚合期间已过账 AP/AR 发票只读查询，以实仓 Calculator 为准）；③ 结算凭证/负额 credit memo 口径（postSettlement 生成的跨域单据与计提翻转红字，以实仓为准）。与设计文档不符处 Phase 3 登记 §6 C18 勘误。
  - Skill: nop-testing
- [ ] Proof：**C18 RECORDING→CHECKING 往返**——RECORDING 录制 → CHECKING 复跑全绿；层 1 锚点：生命周期状态机翻转、计提金额 = 条款 × 销售额、结算后负额发票/凭证生成 + 计提行翻转。
  - Skill: nop-testing

Exit Criteria:

- [ ] C18 类存在且 RECORDING→CHECKING 往返绿（失败模式 = 任一断言/快照 diff 不通过）
- [ ] 动作宿主与金额口径裁决落盘（含设计文档 §6 C18 勘误登记，若与实仓行为不一致）

### Phase 2 — C19 B2B ASN 自动收货与物流到岸成本（混合类型：1/3 Add + 1/3 Proof + 1/3 Decision，逐项标注）

Status: planned
Targets: `app-erp-all/src/test/java/io/nop/app/all/it/TestErpC19B2bAsnAutoReceiveLandedCost.java`、`app-erp-all/_cases/io/nop/app/all/it/TestErpC19B2bAsnAutoReceiveLandedCost/`
Skill: nop-testing

- Item Types: `Add | Proof | Decision`
- Prereqs: Phase 1（同基类同装配，先例链打通后无新机制风险）

- [ ] Add：**C19 测试类** `TestErpC19B2bAsnAutoReceiveLandedCost`（1 类 1 方法）——按设计文档 §6 C19 规格 + `TestErpB2bAsn*`/`TestErpLogPath2LandedCost`/C05 业务参照：自包含 ASN（含行）→ 状态机推进（RECEIVED 等，以实仓为准）→ `ErpB2bAsn__createReceiveFromAsn`（@NopTestProperty 开启 `erp-b2b.asn-auto-create-receive` → 采购收货草稿生成）→ `ErpPurReceive__submitForApproval`/`approve` [DIRECT]（收货过账）→ 自包含物流发运单 `ErpLogShipment__save` → `ErpLogShipment__handleTrackingWebhook`（@NopTestProperty 关 `erp-log.webhook-signature-required`，DELIVERED）→ path-2 到岸成本自动创建（@NopTestProperty 开 `erp-log.path2-landed-cost-auto-create`）→ `ErpInvLandedCost__findPage`（自动创建到岸成本单断言：FREIGHT 费用行 + 引用正确收货/运费，C05 findPage 模式）；静态载荷走 `input/N_step.json5` + `addVar`/`@var:` 动态 id。
  - Skill: nop-testing
- [ ] Decision：**C19 状态机与到岸成本口径裁决**——① ASN 状态机推进动作序列与 createReceiveFromAsn 前置状态（RECEIVED/MATCHED 语义，以实仓为准）；② 收货草稿→审批→过账路径（approve 内建过账 vs 显式动作，B1/B3 先例口径）；③ path-2 到岸成本自动创建触发条件与到岸成本单内容（FREIGHT 费用行金额 = 运费、引用收货单/发运单，以实仓 Processor 行为为准）；④ **webhook 签名校验关闭键名确认（`erp-log.webhook-signature-required`——设计文档键名正确无漂移，宿主 = `ErpLogConfigs`（缺省 true），`ErpLogShipmentHandleTrackingWebhookProcessor` 消费；`erp-b2b.webhook-signature-required` 是 ASN 入站 webhook 独立键（`ErpB2bAsnHandleInboundWebhookProcessor` 消费），C19 流程不经 ASN webhook 无需关）**。与设计文档不符处 Phase 3 登记 §6 C19 勘误。
  - Skill: nop-testing
- [ ] Proof：**C19 RECORDING→CHECKING 往返**——RECORDING 录制 → CHECKING 复跑全绿；层 1 锚点：ASN 状态机终态、收货单自动生成（草稿→审批→posted）、物流送达触发到岸成本 DRAFT 单（FREIGHT 费用行）、到岸成本单引用正确收货/运费。
  - Skill: nop-testing

Exit Criteria:

- [ ] C19 类存在且 RECORDING→CHECKING 往返绿（失败模式 = 任一断言/快照 diff 不通过）
- [ ] 状态机与到岸成本口径裁决落盘（含设计文档 §6 C19 勘误登记，若与实仓不一致）

### Phase 3 — 收尾与回归（Proof-heavy）

Status: planned
Targets: `docs/backlog/integration-test-roadmap.md`（B9 → done + 头「最后更新」注记）、`docs/logs/2026/08-24.md`、`docs/testing/known-good-baselines.md`（差量登记）、`docs/design/integration-testing.md`（§6 C18/C19 勘误登记，若 Phase 1/2 触发）
Skill: none

- Item Types: `Proof | Add`
- Prereqs: Phase 2

- [ ] Proof：**局部回归** `mvn test -pl app-erp-all` 全绿（含 2 个新用例类，串行 fork 单 JVM 顺序执行）+ **全量回归** `mvn test`（全 reactor）零新增失败 + `mvn clean install -DskipTests` BUILD SUCCESS——对照 known-good-baselines（B8 后基线）口径，+2 tests / +2 报告文件全额归因 B9，零新增失败；**clean 前先拷贝 surefire 计数/XML 证据**（B3 closure MINOR-2 流程改进兑现，B4-B8 已执行）。
  - Skill: none
- [ ] Add：roadmap B9 `todo` → `done` + 头「最后更新」注记 + `docs/logs/2026/08-24.md` 日志条目（按日志书写指南）+ known-good-baselines 差量登记 + 设计文档 §6 勘误登记（若触发，含 C18 runAccrual/postSettlement 宿主漂移与 terminate 两阶段勘误）。
  - Skill: none

Exit Criteria:

- [ ] 局部 + 全量回归零新增失败（与已知基线口径一致）
- [ ] roadmap B9 = done + 日志条目存在 + 基线差量登记 + 勘误登记（若触发）

## Draft Review Record

- Independent draft review iteration 1: needs revision（0 BLOCKER / 2 MAJOR / 2 MINOR，ses_fcef73478ffeEixBO0NL5uCRCz，独立 general 子代理新会话）——MAJOR-1 C19 webhook 键裁决反转（实仓 `erp-log.webhook-signature-required` 与 `erp-b2b.webhook-signature-required` 双键并存各属其 webhook：C19 流程关 log 键，b2b 键为 ASN 入站独立键不适用；设计文档键名正确无漂移）；MAJOR-2 C18 terminate 两阶段（terminate 生成待法务审批记录不改状态，TERMINATED 由 approveTermination 达成——approveTermination 插入动作序列）；MINOR-3 用例类计数 23→16（实仓核验）；MINOR-4 `TestErpCtContractLifecycle*` 不存在改引实仓先例名。全部吸收修订。
- Independent draft review iteration 2: accept（0 BLOCKER / 0 MAJOR / 2 MINOR，ses_fcef2e3c2ffemec6tPUopegrnl，独立 general 子代理新会话）——5 项修订全部实仓核验通过；MINOR-1 Decision ① approveTermination recordId 获取口径修正（terminate 返回合同实体非记录，须查询 PENDING 审批记录——`TestErpCtContractRebate.pendingTerminationRecordId` 先例，本轮已修）；MINOR-2 Draft Review Record 占位未持久化（本轮即兑现）。**共识达成，计划可执行。**

## Closure Gates

> 仅在所有项目和每个阶段的退出标准都勾选 `[x]` 后关闭。完整仓库验证在此处：结束时运行 `typecheck`/`build`/`lint`/`test` 一次。

- [ ] 范围内行为完成（C18/C19 两用例三层全比对全绿 + 动作宿主/状态机/到岸成本口径裁决）
- [ ] 相关文档对齐（设计文档 §6 ↔ 用例实现 ↔ roadmap B9 ↔ e2e-runbook ↔ known-good-baselines 无矛盾）
- [ ] 已运行验证（`mvn test -pl app-erp-all` 全绿 + 全量 `mvn test` 零新增失败 + `mvn clean install -DskipTests` BUILD SUCCESS）
- [ ] 无范围内项目降级为 deferred/follow-up
- [ ] 独立草案审查已完成并记录
- [ ] 文本一致性已验证：状态、阶段、门控和日志都一致
- [ ] 结束审计由独立子代理（新会话）执行；执行者未自我审计且未将此留为 `[ ]` 作为人工门控占位符
- [ ] 结束证据存在于文件中

## Deferred But Adjudicated

- （执行后按需填写——候选：seed 修正授权触发时的重录耗时测量（B1/M0.2 watch-only residual 触发条件登记）；无范围内降级项则留空）

## Closure

Status Note: <why the plan can close>

Closure Audit Evidence:

- Auditor / Agent: <independent auditor or independent subagent>
- Evidence: <task id / log link / walkthrough record>

Follow-up:

- <仅非阻塞跟进项目；已确认的缺陷不得出现在此处>