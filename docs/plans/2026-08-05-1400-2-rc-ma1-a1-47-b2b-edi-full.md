# 2026-08-05-1400-2 rc-ma1-a1-47-b2b-edi-full b2b 全域（A1.47）需求符合性审计

> Plan Status: active
> Last Reviewed: 2026-08-05
> Mission: requirement-compliance
> Work Item: A1.47（MA1 需求追踪矩阵审计 — b2b 全功能 UC-B2B-001~008，8 UC）
> Source: `docs/backlog/requirement-compliance-roadmap.md` Work Item A1.47
> Related: `docs/plans/2026-08-02-1458-1-requirement-compliance-methodology.md`（M0.1 done）、`2026-08-02-1530-1-requirement-baseline-extraction.md`（M0.2 done，解除 A1.47 的 0.2 依赖）、`2026-08-05-1400-1-rc-ma1-a1-45-46-contract-lifecycle-billing-rebate.md`（同批 N=1）、`2026-08-06-0245-3-rc-ma1-a1-44-maintenance-f3-response-linkage-oee-dashboard.md`（同 mission MA1 末批范式）
> Audit: required

## Current Baseline

> 本计划是**审计工作项**（verification or audit work），结果表面 = 一份审计报告。基线盘点被审功能现状代码/测试/既有证据，**不修改任何代码**。b2b 域为一个 roadmap 行（A1.47，8 UC），按 plan 指南规则 4 一个结果表面 = 一个计划。

- **方法论契约 + UC 锚点已就绪**：`docs/audits/requirement-compliance-methodology.md`（§1-§10 + §去重协议 + §4 三判据）已落盘；`docs/audits/rc-requirement-baseline-inventory.md:381` 已为 A1.47 给出 UC 清单 = `UC-B2B-001~008`（8 UC），覆盖率 `✅ 一致`（无基线分歧 D-xx）。

- **L1 需求契约（权威真相源）**：`docs/design/b2b/use-cases.md`（机制细节引用 `integration-pattern.md`，L2 设计参考）：
  - **UC-B2B-001 EDI 格式配置**（`:3`）：管理员新增格式（名称/标准/方向/是否 WebService）→ ErpB2bEdiFormat 记录；业务规则：每种格式对应一个 `IErpB2bEdiProvider` bean（按 getCode() 匹配）；isActive=false 不参与派发。
  - **UC-B2B-002 EDI 出站发送**（`:35`）：业务单据审核事件触发→ErpB2bEdiRegistry 按 relatedBillType 查适用 Provider→生成 ErpB2bEdiDoc(TO_SEND)→Provider.generatePayload()→needsWebService 判断异步队列/cron vs 同步 HTTP POST→SENT+ErpB2bEdiLog；备选：格式不匹配 blocking_level=INFO；发送失败 ERROR；UNIQUE(formatId,relatedBillType,relatedBillCode) 幂等。
  - **UC-B2B-003 ASN 入站接收**（`:69`）：供应商推送→HMAC 验签→识别格式→Provider.parsePayload()→建 EdiDoc(RECEIVED,relatedBillType=PO_ORDER)+ErpB2bAsn(RECEIVED)+AsnLine→EdiLog→ARCHIVED；备选：签名失败 ERROR/解析失败 ERROR 保留报文/无匹配 PO ASN 仍创建 relatedBillCode 空；业务规则：ASN 不直接写库存。
  - **UC-B2B-004 代码映射管理**（`:105`）：选映射类型(MATERIAL/PARTNER/UOM)+内部代码+Trading Partner+外部代码→ErpB2bCodeMapping；规则：partnerId+mappingType+internalCode 唯一；无映射时出站用内部/入站用外部原值；双向使用。
  - **UC-B2B-005 Webhook 集成**（`:132`）：复用 ErpSysWebhookConfig/Log；出站 HTTP POST+HMAC→WebhookLog；入站 HMAC 验签→ASN 流程；路径约定 `/r/b2b/webhook/inbound/{formatCode}`。
  - **UC-B2B-006 错误处理与重试**（`:166`）：筛选 ERROR→查 error+EdiLog→判断原因(网络/签名/格式/不可恢复)→重试(TO_SEND/RECEIVED)或放弃(CANCELLED)；自动重试：WARN 每 30min 最多 3 次/ERROR 不自动/3 次失败升级 ERROR；规则：重试新 EdiLog 保留原记录。
  - **UC-B2B-007 合作伙伴上线**（`:220`）：REGISTERED→填档案/凭证→TESTING 交换测试报文(通过率≥90%)→CERTIFIED 认证清单全通过→PRODUCTION 切换+goLiveDate+监控 24h→TERMINATED 下线归档；异常：不达标不可推进。
  - **UC-B2B-008 MFT 配置**（`:234`）：配置 AS2/SFTP/FTPS 端点+凭证/证书+管道选项(压缩/加密/签名)+重试策略(maxRetries/retryIntervalMin/deadLetter)+SFTP 监控目录→ErpB2bMftConfig→传输记 ErpB2bMftLog+失败重试/DEAD_LETTER→证书管理 ErpB2bMftCertificate 有效期监控；异常：证书过期停用/端点不可达重试/fileHash 失败 FAILED。

- **L3 代码实现现状（实测，`module-b2b/erp-b2b-service`）**——**EDI Registry/Provider SPI + EdiDoc 状态机 + ASN 入站 + MFT Registry 完整 + webhook 端点/自动重试/伙伴上线状态机候选缺口待逐 UC 核验**：
  - **UC-B2B-001 格式配置（✅ 实体+CRUD）**：`ErpB2bEdiFormatBizModel.java` 存在。**待核**：①每种格式对应 IErpB2bEdiProvider bean（按 getCode() 匹配）——`IErpB2bEdiProvider.java` SPI 接口存在 ✅；②isActive=false 不参与派发（grep Registry 派发逻辑）。
  - **UC-B2B-002 出站发送（✅ EdiDoc 状态机+Registry+Provider）**：`ErpB2bEdiDocBizModel.java`#createOutbound(`:57`)/#markSent(`:65`)+`ErpB2bEdiRegistry.java`+`UblInvoiceEdiProvider.java`。**待核**：①ErpB2bEdiRegistry 按 relatedBillType 查适用 Provider；②needsWebService 判断异步队列/cron vs 同步 HTTP POST；③UNIQUE(formatId,relatedBillType,relatedBillCode) 幂等+已存在非终态返回已有记录；④格式不匹配 blocking_level=INFO。
  - **UC-B2B-003 ASN 入站（✅ Provider+测试完整）**：`ErpB2bAsnBizModel.java`+`ErpB2bAsnLineBizModel.java`+`UblDespatchAdviceEdiProvider.java`+`TestErpB2bAsnInbound.java`+`TestErpB2bAsnInventoryIntegration.java`。**待核**：①HMAC 签名验证；②Provider.parsePayload()→建 EdiDoc(RECEIVED)+Asn(RECEIVED)+AsnLine→ARCHIVED；③签名失败 ERROR/解析失败 ERROR 保留报文/无匹配 PO ASN 仍创建 relatedBillCode 空；④ASN 不直接写库存（L1 业务规则）。
  - **UC-B2B-004 代码映射（✅ 实体+CRUD）**：`ErpB2bCodeMappingBizModel.java` 存在。**待核**：①partnerId+mappingType+internalCode 唯一；②无映射时出站用内部/入站用外部原值；③generatePayload(内→外)+parsePayload(外→内) 双向使用。
  - **UC-B2B-005 Webhook 集成（⚠️ 复用 ErpSysWebhookConfig + 入站端点待核）**：**待核**：①出站 HTTP POST+HMAC→ErpSysWebhookLog；②入站 HMAC 验签→ASN 流程；③路径约定 `/r/b2b/webhook/inbound/{formatCode}`——grep webhook/inbound endpoint 跨 b2b-web。
  - **UC-B2B-006 错误处理与重试（✅ EdiDoc 状态机 retry/cancel/markError + 自动重试策略待核）**：`ErpB2bEdiDocBizModel.java`#retry(`:112`)/#cancel(`:130`)/#markError(`:97`)。**待核**：①自动重试策略 WARN 每 30min 最多 3 次/ERROR 不自动/3 次失败升级 ERROR（grep autoRetry/blockingLevel/retryCount/cron）；②重试新 EdiLog 保留原记录。
  - **UC-B2B-007 合作伙伴上线（⚠️ 实体完整 + 状态机推进/通过率/认证清单待核）**：`ErpB2bPartnerProfileBizModel.java`+`ErpB2bPartnerCredentialBizModel.java`+`ErpB2bTestExchangeBizModel.java`+`ErpB2bCertificationChecklistBizModel.java`。**待核**：①REGISTERED→TESTING→CERTIFIED→PRODUCTION→TERMINATED 状态机推进（grep status 转移 mutation）；②测试通过率≥90% 门槛；③认证清单全必检项通过门槛；④goLiveDate+监控 24h+archivedAt 归档。
  - **UC-B2B-008 MFT 配置（✅ MftConfig+Certificate+Log+TransportRegistry+测试）**：`ErpB2bMftConfigBizModel.java`+`ErpB2bMftCertificateBizModel.java`+`ErpB2bMftLogBizModel.java`+`ErpB2bMftTransportRegistry.java`+`TestErpB2bMftTransport.java`。**待核**：①协议 AS2/SFTP/FTPS 端点+凭证/证书；②管道选项(压缩 GZIP/ZLIB/加密 PGP/SMIME/签名 SHA-256)；③重试策略 maxRetries/retryIntervalMin/deadLetter+DEAD_LETTER 状态；④SFTP 监控目录+轮询间隔；⑤证书过期自动停用 active=false；⑥fileHash 校验失败 FAILED。

- **L4 测试证据现状**（`module-b2b/erp-b2b-service/src/test`）：
  - `TestErpB2bEdiPosting.java`（EDI 过账）、`TestErpB2bEdiEnvelope.java`（EDI 信封）、`TestErpB2bAsnInbound.java`（ASN 入站）、`TestErpB2bAsnCrudSmoke.java`（ASN CRUD 冒烟）、`TestErpB2bAsnInventoryIntegration.java`（ASN 库存集成）、`TestErpB2bMftTransport.java`（MFT 传输）、`B2bFrozenClockExtension.java`（时间冻结）。
  - **待核**：①UC-B2B-002 出站 generatePayload+needsWebService 异步/同步+幂等 UNIQUE 断言；②UC-B2B-004 代码映射双向使用断言（候选缺）；③UC-B2B-005 webhook 端点 HMAC 验签断言（候选缺）；④UC-B2B-006 自动重试策略 30min/3 次/升级断言（候选缺）；⑤UC-B2B-007 伙伴上线状态机+通过率≥90%+认证清单断言（候选缺）；⑥UC-B2B-008 MFT 重试策略/DEAD_LETTER/证书过期停用断言。MA5 评级待引用。

- **L5 既有证据（MA2 复用输入，方法论 §去重协议）**：
  - **无 b2b 专属 MA2 状态机报告**。本切片为 b2b 域行为的首份证据（b2b 域 MA1 首审）。
  - **b2b 相关既有 finding**：arm-index b2b/edi/asn/mft 域 finding 待 grep（候选：跨域 daoFor P1-MA1-022 命中[B2B entities]，平台一致性维度 resolved 不重审）。**无任何 UC-B2B-001~008 需求符合性 finding**。
  - 本切片须声明与 MA2 报告差异增量（报告段落 9）：无 b2b 专属 MA2 报告；只补需求视角差异。

- **arm-index 既有 finding 衔接**：grep arm-index b2b/edi/asn/mft/webhook/partner/UC-B2B → **无 UC-B2B-001~008 finding**。本切片须 grep arm-index b2b 同域同控制点后裁决复用 or 新建 `P*-RC-xxx`（续编，执行时取最新——当前至 P2-RC-059 / P1-RC-071）。

- **保护区域**：本审计为**只读审计**。属 roadmap 预授权类目。发现的 P0/P1 finding **不在本计划实施修复**——按方法论 §10。本切片候选偏差多为**代码逻辑**类（预授权——webhook 端点/自动重试策略/伙伴上线状态机/MFT 重试策略/证书过期监控）；ASN→inventory 集成修复（不直接写库存边界）属代码逻辑预授权；若触及 ORM 结构（如 EdiDoc 加 autoRetryCount/retryCount 字段）→ **ORM 结构变更须 ask-first + 独立 plan-audit**；须在报告逐项标注触及保护区域。

- **剩余差距**：A1.47 切片五级追踪审计报告缺失 = MA4 及 MR1 该切片证据缺口来源。本计划产出 b2b 域全域审计报告并登记 finding，解除 b2b 域证据缺口（本切片完成后 b2b 域 A1.47 done，b2b 域 8 UC 全覆盖）。

## Goals

- 产出 A1.47 切片审计报告 `docs/audits/2026-08-05-1400-2-rc-ma1-a1-47-b2b-edi-full.md`，含方法论 §6 **9 段全部内容**。
- 对 UC-B2B-001~008 逐条核验**每条验收标准**（完整枚举，§3，禁止跳号）：逐 UC 五级追踪。
- 对候选缺口给出分级结论：UC-B2B-005 webhook 入站端点/HMAC（待核 P1/P2）、UC-B2B-006 自动重试策略 30min/3 次/升级（待核 P1/P2）、UC-B2B-007 伙伴上线状态机+通过率≥90%+认证清单门槛（待核 P1/P2）、UC-B2B-008 MFT 重试策略/DEAD_LETTER/证书过期停用（待核 P1/P2）、UC-B2B-002/003/004 边界（幂等/双向映射/ASN 不写库存待核）——按 §2 判据定级，若为 P0/P1 则新建 `P*-RC-xxx`（续编）并按 §10 触发 MR0/MR1（本计划仅登记，不实施修复；**ORM 结构类须 ask-first**）。
- 报告产出即更新 `docs/audits/arm-index.md`（新 RC finding 入对应分区；audit reports 表新增 A1.47 行——b2b 域首审行）。

## Non-Goals

- **不修复 finding**（修复属 MR0/MR1；本计划是审计）。
- **不修改真相源**（§9 冻结条款——分歧记入报告，不直改 use-cases.md/integration-pattern.md/product-scope.md）。
- **不修改代码/ORM/api.xml**（只读审计）。
- **不审计其他域**（contract/drp/logistics/aps/notify 为各自独立 plan）。
- **不重审 P1-MA1-022**（跨域 daoFor 平台一致性维度，resolved，不复审）。
- **不执行 MA4 运行时探针展开**（本计划只产出静态存疑点清单）。

## Task Route

- Type: `verification or audit work`
- Owner Docs: `docs/audits/requirement-compliance-methodology.md`（§1-§10 + §去重协议 + §4 三判据）+ `docs/backlog/requirement-compliance-roadmap.md`（A1.47 工作项）+ `docs/audits/rc-requirement-baseline-inventory.md:381`（A1.47 UC 锚点）+ `docs/design/b2b/use-cases.md`（L1 真相源）+ `docs/architecture/integration-pattern.md`（L2 设计参考，非真相源）+ `docs/audits/arm-index.md`（finding 衔接）
- Skill Selection Basis: `Skill: docs/skills/multi-dimensional-audit-prompt.md`（roadmap MA1 全部 A1.x 指定）。其必需输入均已就绪。

## Infrastructure And Config Prereqs

- No infra prereqs beyond existing baseline. 审计以读代码/测试/报告为主（纯分析）。若需即时行为确认可跑既有 JUnit（如 `mvn test -pl module-b2b/erp-b2b-service -Dtest=TestErpB2bEdiPosting,TestErpB2bAsnInbound,TestErpB2bMftTransport`）。§8 过程纪律自检需跑 `bash docs/audits/nop-compliance-checker.sh`（纯 reporter，退出码恒 0；本审计无生产代码变更）。

## Execution Plan

### Phase 1 - UC-B2B-001~008 五级追踪矩阵填充与逐 UC 符合性结论

Status: planned
Targets: `docs/audits/2026-08-05-1400-2-rc-ma1-a1-47-b2b-edi-full.md`（产出 §1-§5）
Skill: `docs/skills/multi-dimensional-audit-prompt.md`

- Item Types: `Proof | Decision`
- Prereqs: M0.1 + M0.2 done

- [ ] `Proof` 对 UC-B2B-001~008 **逐验收标准一矩阵行**填 L1-L5（§1 格式）：L1 逐字引用 `use-cases.md:3/35/69/105/132/166/220/234` 验收标准原文；L2 引用 `integration-pattern.md`（标注"设计参考，冲突以 L1 为准"）；L3 引用 `ErpB2bEdiFormatBizModel` + `ErpB2bEdiDocBizModel`#createOutbound/markSent/markError/retry/cancel/createInbound/archive + `ErpB2bEdiRegistry` + `IErpB2bEdiProvider`/`UblInvoiceEdiProvider`/`UblDespatchAdviceEdiProvider` + `ErpB2bAsnBizModel`/`AsnLineBizModel` + `ErpB2bCodeMappingBizModel` + `ErpB2bPartnerProfileBizModel`/`PartnerCredentialBizModel`/`TestExchangeBizModel`/`CertificationChecklistBizModel` + `ErpB2bMftConfigBizModel`/`MftCertificateBizModel`/`MftLogBizModel`/`ErpB2bMftTransportRegistry` + grep webhook/inbound/HMAC/autoRetry/blockingLevel/retryCount/cron 站点（含行号）；L4 引用 `TestErpB2bEdiPosting`/`TestErpB2bEdiEnvelope`/`TestErpB2bAsnInbound`/`TestErpB2bMftTransport`#method（注明断言强度）；L5 标注无 b2b 专属 MA2 报告。
      - Skill: `docs/skills/multi-dimensional-audit-prompt.md`
- [ ] `Proof` 重点核验**候选缺口**（逐条对照）：UC-B2B-001 Provider bean getCode() 匹配+isActive 派发（⚠️待核 Registry 派发）；UC-B2B-002 **Registry 按 relatedBillType 查/needsWebService 异步同步/UNIQUE 幂等/格式不匹配 INFO**（⚠️待核）；UC-B2B-003 **HMAC 验签/parsePayload/无匹配 PO 仍创建/ASN 不写库存**（⚠️待核边界）；UC-B2B-004 **partnerId+type+code 唯一/无映射双向默认/双向使用**（⚠️待核）；UC-B2B-005 **webhook 入站端点 `/r/b2b/webhook/inbound/{formatCode}`+HMAC 验签**（⚠️ grep endpoint 待核候选 P1/P2）；UC-B2B-006 **自动重试 WARN 30min×3/ERROR 不自动/3 次升级+新 EdiLog 保留原**（⚠️ grep autoRetry/blockingLevel/cron 待核候选 P1/P2）；UC-B2B-007 **状态机推进 REGISTERED→TESTING→CERTIFIED→PRODUCTION→TERMINATED+通过率≥90%+认证全通过+goLiveDate+监控 24h**（⚠️待核候选 P1/P2）；UC-B2B-008 **协议端点/管道选项/重试 DEAD_LETTER/SFTP 监控目录/证书过期停用/fileHash FAILED**（⚠️待核候选 P1/P2）。
      - Skill: `docs/skills/multi-dimensional-audit-prompt.md`
- [ ] `Decision` 按 §2 判据对 UC-B2B-001~008 给出符合性结论（取最高）。UC-B2B-001/002/003/008 倾向**接受/P2**（实体+Provider+测试完整但边界待核）；UC-B2B-005 webhook 端点候选 **P1/P2**（§2 ①/⑤）；UC-B2B-006 自动重试候选 **P1/P2**（§2 ①/③/⑤）；UC-B2B-007 伙伴上线候选 **P1/P2**（§2 ①/③/⑤）；UC-B2B-004/008 边界候选 P2。每结论须列明命中判据编号 + 三源对照 + §4 三判据复核（**P1 项核 owner doc Deferred/Non-Goal 标注的人工批准痕迹**）+ 触及保护区域标注。
      - Skill: `docs/skills/multi-dimensional-audit-prompt.md`

Exit Criteria:

- [ ] 报告 §1-§5 已落盘：UC-B2B-001~008 矩阵行（逐验收标准进入 L5 判读，8 UC 无跳号），L1 逐字引用、L3 含行号 + grep 站点、L4 注明断言强度、L5 标注无专属 MA2
- [ ] UC-B2B-001~008 有符合性结论且列明 §2 判据编号；P1 项核 Deferred/Non-Goal 标注的人工批准痕迹；触及 ORM 保护区域项显式标注 ask-first

### Phase 2 - finding 登记 / arm-index 衔接 / 静态存疑点 / 过程纪律自检 / 报告完整性

Status: planned
Targets: `docs/audits/2026-08-05-1400-2-rc-ma1-a1-47-b2b-edi-full.md`（补 §6-§9）；`docs/audits/arm-index.md`（新 RC finding 入分区）
Skill: `docs/skills/multi-dimensional-audit-prompt.md`

- Item Types: `Decision | Add | Proof`
- Prereqs: Phase 1 完成

- [ ] `Decision` **复用 or 新增 裁决**（§7）：grep `arm-index.md` b2b/edi/asn/mft/webhook/partner/retry/onboarding 同域同控制点后裁决。执行时 grep arm-index 取最新续编号避免冲突（当前至 P2-RC-059 / P1-RC-071）。禁止未经比对新建。
      - Skill: `docs/skills/multi-dimensional-audit-prompt.md`
- [ ] `Add` 报告 §6 与 arm-index 衔接段：列明每条 finding 复用/新增裁决 + 双向可追溯（finding ID ↔ 修复行预留 MR1）+ **ORM 结构类修复（EdiDoc 加 autoRetryCount/retryCount 字段 / MFT 加 deadLetter 字段）须 ask-first + 独立 plan-audit** + **ASN→inventory 集成边界修复须与 inventory 域协调** + **UC-B2B-005 webhook 端点修复须与 integration-pattern 协调**。
      - Skill: none
- [ ] `Add` 报告 §7 静态存疑点清单（供 MA4 展开）：登记 L5 无法静态定论、需运行时确认的点（逐存疑点一行；**P0 即时通道评估**——本切片无活跃数据破坏候选，webhook/retry/onboarding 缺失属功能缺失类非数据破坏）。
      - Skill: none
- [ ] `Proof` 报告 §8 过程纪律自检段：实际运行 `bash docs/audits/nop-compliance-checker.sh` 附 actual vs baseline 表；closure-audit 独立性声明；与 arm-index 交叉去重声明。**不以 checker 退出码 0 为门控通过依据**（无生产代码变更，注明"无回归风险"）。
      - Skill: none
- [ ] `Add` 报告 §9 与 MA2 报告差异增量声明：无 b2b 专属 MA2 报告；列明只补的需求视角差异（webhook 端点/自动重试/伙伴上线/MFT 重试策略）。
      - Skill: none
- [ ] `Add` 报告产出即更新 `docs/audits/arm-index.md`：新 RC finding 入 RC 发现追踪分区；audit reports 表新增 A1.47 行（b2b 域首审行——8 UC 全覆盖）。
      - Skill: none
- [ ] `Proof` 报告 9 段完整性自检：落盘前自查 §1-§9 全部存在。
      - Skill: none

Exit Criteria:

- [ ] 报告 §6-§9 已落盘，9 段齐全；finding 复用/新增裁决均有 arm-index grep 依据
- [ ] 新 RC finding 已写入 `arm-index.md`；静态存疑点清单已登记（供 A4.2 展开）；P0 候选评估有结论（本切片倾向无 P0）
- [ ] §8 自检段含 checker actual vs baseline 实测表 + 独立性 + 交叉去重声明

## Draft Review Record

- Independent draft review iteration 1: `accept`（独立子代理 ses_02d7487b5ffeRFDFNMDxCBhcJH，fresh session，未起草本计划）。范围/UC 覆盖（A1.47=UC-B2B-001~008，8 UC 无跳号）/依赖（0.2 done）/结果表面/方法论（9 段 §6 + §4 三判据 + §5 ask-first + §7 reuse + §去重协议）/反 slack/模板全 PASS；load-bearing 引用经实仓复核 CONFIRMED TRUE：①ErpB2bEdiDocBizModel#createOutbound:57/markSent:65/markError:97/retry:112/cancel:130/createInbound:147/archive:157 ✅；②ErpB2bEdiRegistry + IErpB2bEdiProvider + UblInvoice/DespatchAdvice Provider + MftTransportRegistry 全存在 ✅；③7 测试文件全存在 ✅；④UC-B2B-006 自动重试缺失 grep 零命中（autoRetry/cron/Quartz/Scheduled 跨 module-b2b）✅；⑤UC-B2B-007 伙伴上线状态机仅 activate/suspend/deactivate 无 TESTING/CERTIFIED 门槛 ✅；⑥UC 清单三方一致 ✅；⑦arm-index 无 UC-B2B-001~008 finding ✅。修复 1 处 minor：integration-pattern.md 路径 docs/design/b2b/→docs/architecture/。共识达成，转 active。

## Closure Gates

> 本计划为**只读审计**（无代码/ORM/api.xml/view.xml/真相源变更），故删除完整仓库 `typecheck`/`build`/`lint`/`test` 验证命令门控。验证 = 报告 9 段完整性 + 五级矩阵逐验收标准覆盖 + finding arm-index 衔接 + §8 过程纪律自检 + 独立草案审查 + 文本一致性 + 独立结束审计。

- [ ] 范围内行为完成：A1.47 报告 9 段齐全 + UC-B2B-001~008 矩阵行（逐验收标准，8 UC 无跳号）+ finding 登记入 arm-index
- [ ] 相关文档对齐：报告与方法论 §1-§10 + §去重协议一致；与 rc-requirement-baseline-inventory A1.47 锚点一致
- [ ] 已运行验证：报告 9 段完整性自检 + §8 checker actual vs baseline 实测记录 + finding 复用/新增裁决可追溯（无代码变更故不跑 build/test）
- [ ] 无范围内项目降级为 deferred/follow-up
- [ ] 独立草案审查已完成并记录
- [ ] 文本一致性已验证：状态、阶段、退出标准、门控和日志都一致
- [ ] 结束审计由独立子代理（新会话）执行；执行者未自我审计且未将此项留作未勾选状态作为人工门控占位符
- [ ] 结束证据存在于文件中

## Deferred But Adjudicated

### finding 的修复实施

- Classification: `out-of-scope improvement`
- Why Not Blocking Closure: 本计划是审计，结果表面 = 报告 + arm-index 登记。finding 的修复按方法论 §10 经 MR0（P0）/ MR1（R1.0 展开 RC-R1.n，P1）实施；本切片候选偏差多为**代码逻辑**类（预授权——webhook 入站端点/自动重试策略/伙伴上线状态机/MFT 重试策略/证书过期监控）；**ORM 结构类（EdiDoc 加 autoRetryCount/retryCount / MFT 加 deadLetter 字段）须 ask-first + 独立 plan-audit**；**ASN→inventory 集成边界修复须与 inventory 域协调**；**webhook 端点修复须与 integration-pattern 协调**。
- Successor Required: yes（MR0/MR1 按本报告 finding 交叉引用展开修复行；UC-B2B-005 webhook 须与 integration 协调；UC-B2B-003 ASN 须与 inventory 协调）

## Closure

Status Note: <待执行后填写>

Closure Audit Evidence:

- Auditor / Agent: <待独立结束审计子代理填写>
- Evidence: <待填写>

Follow-up:

- MR0/MR1 按 §10 展开本报告 finding 修复（ORM 结构类[retry 字段]须 ask-first + 跨域 integration/inventory 协调）。
- MA4 运行时探针展开 §7 静态存疑点清单。
