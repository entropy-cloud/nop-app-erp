# 2026-07-31-1439-3-r3-7-i18n-checker-ci-gate R3.7 — F15 i18n-coverage-checker.sh 接入 CI workflow

> Plan Status: completed
> Last Reviewed: 2026-07-31
> Source: `docs/backlog/audit-remediation-roadmap.md` §MR3 R3.7（P1-MA7-007）
> Related: `docs/plans/2026-07-31-0958-1-r3-0-mr3-p1-finding-expansion.md`（R3.0 展开 R3.7）；`docs/audits/2026-07-29-1708-arm-ma7-ci-guard-activation.md`（P1-MA7-007）；`docs/audits/2026-07-29-1708-2-ma7-ci-guard-activation-verification.md`（A7.4 审计计划，裁决接入）；`docs/plans/2026-07-25-1057-1-compliance-baseline-drift-adjudication.md`（F8 compliance-checker CI 接入范式先例）
> Audit: required

## Current Baseline

**P1-MA7-007**：F15 i18n-coverage-checker.sh 是「dead armor」——回归门控已建（脚本 + 基线），但**未接入 CI workflow**，仅可手动运行。

- **CI 现状**（实测 `.github/workflows/`）：3 个 workflow——`compliance.yml`（F8 `nop-compliance-checker.sh` 已激活，含 baseline gate one-way tightening，option b「gate logic in CI / checker stays pure reporter」范式）、`e2e.yml`、`maven.yml`。**零 `i18n-coverage-checker.sh` 引用**（grep 确认）。
- **F15 工具现状**（`docs/audits/i18n-coverage-checker.sh`，164 行）：双模式——quality（默认，仅报 DEFECTS）+ `--strict`（COVERAGE GAPS 也判失败）。输出含可解析行：`DEFECTS (bad i18n-en values) : N`、`COVERAGE GAPS (...) : N in M files`、`RESULT: FAIL/PASS`；exit code 0=clean / 1=defects-or-strict-gaps。
- **F15 基线**（`docs/audits/compliance-baseline.md §F15`，210-226）：锚定 2026-07-29，扫描 373 文件（354 view.xml + 19 action-auth.xml），**DEFECTS 基线=0 / COVERAGE GAPS 基线=0 / 双模式 exit=0**。门控方向与 F8 同向收紧（quality 缺陷>0 或 strict 缺口>0 = 回归）。§F15 显式声明「接入 CI 由 A7.4 裁决，对齐 F8 模式」——**本 plan 就是该裁决的落地**。
- **A7.4 审计裁决**（`2026-07-29-1708-2`）：P1-MA7-007 是 A4.9 line 165 委托的唯一裁决产出，无重复。裁决 = 接入 CI workflow（对齐 F8 激活范式），关闭「dead armor→live guard」最后一公里。
- **基线干净**：F15 当前 0/0，接入后**不会立即触发 CI red**（零回归风险）。

剩余差距：F15 门控未激活；compliance-baseline §F15「门控状态」段仍标「未接入 CI workflow」。

## Goals

- 在 `.github/workflows/compliance.yml` 新增 i18n gate job（或与既有 compliance job 并列的新 job），执行 `bash docs/audits/i18n-coverage-checker.sh --strict` + 解析输出（DEFECTS / COVERAGE GAPS）对照 F15 基线（0/0）做**单向收紧**门控——actual > baseline => fail。
- 对齐 F8 option b 范式：**gate 逻辑在 CI（解析输出 + 比基线），checker 脚本保持纯 reporter**（不向脚本注入 fail-on-regression 逻辑；脚本双模式语义不变）。
- 更新 `docs/audits/compliance-baseline.md §F15`「门控状态」段：从「未接入 CI workflow」→「已接入 `.github/workflows/compliance.yml`（R3.7），live guard」。
- 验证：CI workflow yaml well-formed + 本地复跑 `i18n-coverage-checker.sh --strict` 绿（0/0，exit 0）+ grep workflow 含 i18n job + grep 基线文档门控状态已翻转。
- arm-index §P1 详细清单回填 P1-MA7-007 = `MR3 done (R3.7)`；roadmap R3.7 Status `todo`→`done`。

## Non-Goals

- 修复既有 i18n 缺陷/缺口（基线 0/0，无遗留；门控是 one-way tightening，当前不触发 red）。
- 改写 `i18n-coverage-checker.sh` 脚本内部逻辑（保持纯 reporter；双模式语义不变）。
- 调高 F15 基线（基线=0，无上调理由；上调须经独立计划裁决）。
- F8 compliance checker 的既有 job（已激活，本 plan 不动）。
- `e2e.yml` / `maven.yml`（独立 workflow，不在范围）。
- R3.5 / R3.6（独立 plan）。

## Task Route

- Type: `implementation-only change`（CI workflow 新增 job + 基线文档状态翻转；不改代码/ORM/API 契约）
- Owner Docs: `.github/workflows/compliance.yml`（CI gate 权威）；`docs/audits/compliance-baseline.md §F15`（基线权威）；`docs/audits/i18n-coverage-checker.sh`（工具权威）；`docs/audits/arm-index.md`（P1-MA7-007）
- Skill Selection Basis: none — CI workflow YAML 编辑 + 基线文档更新，不触及 ORM/BizModel/view/测试代码生成。无匹配 skill（nop-* skills 聚焦平台代码/测试，CI gate 是工程配置）。

## Infrastructure And Config Prereqs

- No infra prereqs beyond existing baseline（CI runner `ubuntu-latest` 已具备 python3 + bash，与既有 compliance job 同环境）

## Execution Plan

### Phase 1 - 门控机制 Decision（F8 范式对齐 + 解析策略）

Status: completed
Targets: `.github/workflows/compliance.yml`（既有 F8 gate 范式参考）；`docs/audits/i18n-coverage-checker.sh`（输出格式）；`docs/audits/compliance-baseline.md §F15`（基线）
Skill: none

- Item Types: `Decision | Proof`
- Prereqs: 无

- [x] Proof: 读既有 `compliance.yml` F8 gate 的 option b 实现范式（inline python3 解析 checker 输出 + 解析 baseline yaml 块 + one-way tightening 比较 + regressions/improvements 报告），确认 i18n gate 可复用同一范式（解析行不同：F8 解析 `R1a..R9`，F15 解析 `DEFECTS...N` / `COVERAGE GAPS...N`）。
  - Skill: none
  - **Decision: F8 option b 范式可复用。** `compliance.yml:25-98` 既有 F8 gate 结构（run checker→output.txt → inline python3 解析 + 比基线 → regressions/improvements 报告）经核实可被 i18n gate 复用。唯一差异=解析行：F8 用 `rule_re = ^\s*(R\d+[a-d]?)\b.*?(\d+)$` 解析汇总表 R 规则；F15 须解析 `DEFECTS (bad i18n-en values) : N`（冒号前后含空格）与 `COVERAGE GAPS (...): N in M files`（冒号前无空格）——以 Phase 1 实测输出为准，勿套 F8 的 R 规则正则。
- [x] Proof: 核实 F15 基线在 compliance-baseline.md 的承载形式——DEFECTS/GAPS 基线在 §F15 表格（非主 yaml 块）。裁决基线来源：方案 X（i18n gate 直接硬编码 0/0 + 注释指向 §F15 表格，最简）；方案 Y（在 §F15 增机器可读块供 CI 解析，对齐 F8 yaml 块范式但增文档维护面）。倾向方案 X（基线=0/0 稳定，硬编码 + 注释指针足够；§F15 表格已是人类可读权威）。
  - Skill: none
  - **Decision: 方案 X（硬编码 0/0 + 注释指针）。** §F15 DEFECTS/GAPS 基线（compliance-baseline.md:219-220）在人类可读表格，不在主 `## BASELINE (machine-readable)` yaml 块（该块仅 F8 R 规则）。基线=0/0 稳定（plan 2026-07-29-0749-3 锚定），硬编码 + 注释指向 §F15 足够；方案 Y 增机器可读块无收益（基线不漂移）反增维护面。
- [x] Decision: i18n gate 形态——方案 a（compliance.yml 新增**独立 job** `i18n`，与 `compliance` job 并列，各自独立 fail，故障定位清晰）vs 方案 b（在既有 `compliance` job 内追加 step）。倾向方案 a（独立 job：i18n 与 anti-pattern 是不同关注点，独立 job 使 CI 检查列表清晰 + 部分失败定位更好，对齐「每门控一 job」最佳实践）。记录选择 + 替代方案 + 残留风险（方案 a 多一个 job 几秒开销，可忽略）。
  - Skill: none
  - **Decision: 方案 a（独立 job `i18n`）。** i18n 覆盖与 anti-pattern 是不同关注点；独立 job 使 CI 检查列表清晰 + 部分失败（仅 i18n red / 仅 anti-pattern red）定位更好，对齐「每门控一 job」最佳实践。替代方案 b（compliance job 内追加 step）否决（耦合不同关注点）。残留风险：多一个 job 的 ~几秒 runner 开销，可忽略。

Exit Criteria:

- [x] F8 option b 范式可复用性确认（解析行差异已识别）
- [x] 基线来源 Decision 记录（方案 X/Y 选择 + 理由）
- [x] gate 形态 Decision 记录（独立 job vs 同 job step + 替代方案 + 残留风险）

### Phase 2 - CI i18n gate 落地 + 基线文档翻转

Status: completed
Targets: `.github/workflows/compliance.yml`；`docs/audits/compliance-baseline.md §F15`
Skill: none

- Item Types: `Add`
- Prereqs: Phase 1 Decision

- [x] Add: 在 `compliance.yml` 按方案 a 新增独立 job `i18n`（runs-on ubuntu-latest），步骤：checkout → run `bash docs/audits/i18n-coverage-checker.sh --strict > i18n-output.txt 2>&1; cat i18n-output.txt` → inline python3 按 Phase 1 Proof 确认的精确格式解析 DEFECTS 与 COVERAGE GAPS 行得 actual（注意脚本实测输出 `DEFECTS (bad i18n-en values) : N` 含空格、`COVERAGE GAPS (...): N in M files` 冒号无空格——以 Phase 1 Proof 实测为准，勿套用 F8 的 R 规则正则）→ 对照基线 0/0（硬编码 + 注释指向 §F15 表格）one-way tightening：defects>0 或 gaps>0 => `sys.exit(1)` 打印 regressions；否则 PASS。**fail-loud**：解析失败（输出无 DEFECTS/GAPS 行可解析）须 `sys.exit(2)`（对齐 F8 `compliance.yml` 缺基线即 exit 2 的 fail-loud 纪律），禁止静默放行。triggers 为 workflow 级（`compliance.yml:3-8`），新 job 同文件自动继承，无需重复声明。
  - Skill: none
- [x] Add: 更新 `compliance-baseline.md §F15`「门控状态」段（line 226）：从「F15 checker 当前**未接入 CI workflow**...属"可手动运行的回归门"」→「F15 checker 已接入 `.github/workflows/compliance.yml` i18n job（R3.7，plan 2026-07-31-1439-3），live guard——defects>0 或 gaps>0 触发 CI red」。§261 工具清单同步标注「已接入 CI」。
  - Skill: none

Exit Criteria:

- [x] compliance.yml 新增 i18n gate job（yaml 合法，triggers 对齐，解析逻辑正确）
- [x] §F15 门控状态翻转（live guard + R3.7 指针）

### Phase 3 - 验证 + arm-index 回填 + 日志

Status: completed
Targets: `docs/audits/arm-index.md` §P1 详细清单；`docs/backlog/audit-remediation-roadmap.md` §MR3 R3.7；`docs/logs/2026/07-31.md`
Skill: none

- Item Types: `Add | Proof`
- Prereqs: Phase 2

- [x] Add: arm-index §P1 详细清单 P1-MA7-007「修复状态」回填 `MR3 done (R3.7)`，附 gate 形态（独立 job）+ 基线来源 Decision + F8 option b 范式对齐。
  - Skill: none
- [x] Add: 更新 roadmap §MR3 R3.7 Status `todo`→`done`；追加 `docs/logs/2026/07-31.md` 条目（R3.7 F15 i18n checker CI 接入 + 基线文档翻转）。
  - Skill: none
- [x] Proof: 一致性复核——`python3 -c yaml.safe_load` 确认 compliance.yml 合法 + grep workflow 含 `i18n-coverage-checker.sh` 引用 + 本地复跑 `bash docs/audits/i18n-coverage-checker.sh --strict` exit 0（基线 0/0 零回归）+ grep §F15 门控状态已翻转 + arm-index P1-MA7-007 非裸 todo + roadmap R3.7 done。
  - Skill: none

Exit Criteria:

- [x] arm-index P1-MA7-007 回填，无裸 todo
- [x] roadmap R3.7 done；日志条目落地

## Draft Review Record

- Independent draft review iteration 1: accept (task `ses_049143594ffezGnPIeJDW9y6nZ`) — 全部 live 声明实测（compliance.yml F8 option-b gate + 零 i18n 引用 + checker 输出/exit 语义 + §F15 基线 0/0 在表格非主 yaml 块 + 本地复跑 `--strict` exit 0/373 文件零回归）+ 单一结果表面 + F8 范式对齐 + Phase-1 决策质量（baseline 来源 X/Y + job 形态 a/b 均带替代方案+残留风险）+ 命名合规。已采纳 3 项非阻塞 polish：(a) Phase 2 解析格式按 Phase 1 Proof 实测为准（COVERAGE GAPS 冒号无空格，勿套 F8 R 规则正则）+ (b) fail-loud 解析失败 `sys.exit(2)` 对齐 F8 + (c) triggers 为 workflow 级新 job 同文件自动继承无需重复声明。

## Closure Gates

- [x] 范围内行为完成（CI i18n gate job 落地 + 基线文档门控状态翻转）
- [x] 相关文档对齐（compliance-baseline §F15 + arm-index + roadmap + 日志）
- [x] 已运行验证（compliance.yml yaml 合法 + 本地 `i18n-coverage-checker.sh --strict` exit 0[基线 0/0 零回归] + grep workflow 含 i18n 引用 + grep §F15 翻转；compliance checker 不适用[本 plan 不动 F8 规则面]）
- [x] 无范围内项目降级为 deferred/follow-up
- [x] 独立草案审查已完成并记录
- [x] 文本一致性已验证：状态、阶段、门控和日志都一致
- [x] 结束审计由独立子代理（新会话）执行；执行者未自我审计且未将此留为 `[ ]` 作为人工门控占位符
- [x] 结束证据存在于文件中

## Deferred But Adjudicated

### F15 基线调高机制（当 i18n 缺口回归需放宽时）

- Classification: `watch-only residual`
- Why Not Blocking Closure: 基线=0/0 稳定；若未来某 PR 合法引入暂未英文化的 view.xml 导致 gaps>0，须经独立计划裁决调高 §F15 基线（对齐 F8「调高基线的唯一途径」纪律）。本 plan 仅激活门控，不预设调高路径。
- Successor Required: `yes`（触发条件 = 合法 PR 使 gaps>0 时，建立调高基线的独立计划）

## Closure

Status Note: R3.7（F15 i18n-coverage-checker.sh 接入 CI workflow）三 Phase 全 done——Phase 1 门控机制三决策落地（F8 option b 范式可复用 + 基线来源方案 X 硬编码 0/0 指针 + gate 形态方案 a 独立 job）；Phase 2 `compliance.yml` 新增独立 job `i18n`（inline python3 解析 DEFECTS/GAPS 行 → 对照 §F15 基线 0/0 单向收紧，actual>baseline => exit 1，解析失败 exit 2 fail-loud）+ `compliance-baseline.md §F15` 门控状态翻转；Phase 3 arm-index P1-MA7-007 `done (R3.7)` + roadmap R3.7 `done` + 一致性复核通过。零 Java/ORM/test 代码变更（CI YAML + 基线 markdown），关闭 F15「dead armor → live guard」最后一公里。

Closure Audit Evidence:

- Auditor / Agent: 独立结束审计子代理（新会话，fresh context，未重用执行者上下文）
- Evidence:
  - `.github/workflows/compliance.yml:107-191` 新增独立 job `i18n`（F15 i18n coverage regression gate），步骤=checkout → run `bash docs/audits/i18n-coverage-checker.sh --strict > i18n-output.txt` → inline python3 解析 DEFECTS（`^DEFECTS\b.*?:\s*(\d+)$`）/ COVERAGE GAPS（`^COVERAGE GAPS\b.*?:\s*(\d+)\s+in\s+\d+\s+files$`）行 → 对照基线 0/0（硬编码 + 注释指向 §F15 表格）单向收紧（defects>0 或 gaps>0 => `sys.exit(1)`）+ fail-loud 解析失败 `sys.exit(2)`（对齐 F8 缺基线 exit 2 纪律）+ upload artifact
  - `docs/audits/compliance-baseline.md:226` §F15 门控状态段已翻转：「未接入 CI workflow...属可手动运行的回归门」→「F15 checker **已接入 CI workflow** `.github/workflows/compliance.yml` i18n job（R3.7，plan 2026-07-31-1439-3），live guard」；§261 工具清单同步标注「已接入 CI」
  - `docs/audits/arm-index.md:285` P1-MA7-007 修复状态 `done (R3.7)` + :291 R3.7 闭合回填注记（gate 形态独立 job + 基线来源方案 X + F8 option b 范式 + fail-loud + 基线干净零回归 + 基线文档翻转 + 验证 + successor）
  - `docs/backlog/audit-remediation-roadmap.md:205` §MR3 R3.7 Status `done` + done 注记
  - `docs/logs/2026/07-31.md:3-10` R3.7 日志条目（3 Phase 全 done + 验证 full-green）
  - 审计期独立复核（fresh context）：`python3 -c yaml.safe_load` compliance.yml 合法（jobs=[compliance, i18n, web-pages-validation]）+ 本地复跑 `bash docs/audits/i18n-coverage-checker.sh --strict` EXIT=0（scanned 373 files / DEFECTS 0 / COVERAGE GAPS 0 in 0 files / RESULT PASS，零回归）+ 独立重跑 compliance.yml inline python3 parse 逻辑捕获 DEFECTS=0/GAPS=0 → gate 判定 PASS（anti-hollow：解析逻辑实测可达非空壳）+ grep workflow 含 `i18n-coverage-checker.sh` 引用 + grep §F15 门控状态「已接入 live guard」+ arm-index/roadmap `done`。零 Java/ORM/test 代码变更 → mvn test/build 不适用（Maven 不消费 `.github/workflows/` 且 checker 脚本未改）

Follow-up:

- 非阻塞跟进见 Deferred But Adjudicated（F15 基线调高机制，触发=合法 PR 使 gaps>0，须独立计划裁决）
