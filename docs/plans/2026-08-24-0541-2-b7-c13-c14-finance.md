# 2026-08-24-0541-2-b7-c13-c14-finance 批次 B7 用例实施：C13 期末结账全链与反结账 + C14 银行对账与坏账计提回收

> Plan Status: completed
> Mission: integration-test
> Work Item: B7（C13, C14；主导域 finance）
> Last Reviewed: 2026-08-24
> Source: `docs/backlog/integration-test-roadmap.md`（B7 行，M0.3 追加）
> Related: `2026-08-24-0541-1-b6-c11-c12-projects-assets`（同批兄弟计划，B6 排其前）
> Audit: required

## Current Baseline

（全部事实 2026-08-24 实仓核验）

- **前置依赖**：M0.1 设计文档（`docs/design/integration-testing.md` §6 C13/C14 用例规格 + §3.4 机制实证结论 + §4 粒度约定 + §5 xwf 规避清单）+ M0.2 基建 + B1-B6（B6 为同批先行计划，deps 链上须 B6 完成后执行——roadmap B7 行 Deps = M0.1 + M0.2，批内排序由本批计划序号确定）。
- **基建就绪**（M0.2/B1-B5 产物）：基类 `ErpIntegrationTestCase`（机制 (c) 文件 H2 双模 + 10 个共享 step helper）；`_cases` 目录约定；surefire 模块级串行化已落地；已有 12 个集成用例类（Pilot + B1-B5 = 1+2+2+2+3+2）作为范本，**B7 按同范式实现（1 用例 = 1 类 1 方法 + @BeforeAll fresh-DB + RECORDING→CHECKING 往返）**。
- **fin 域既有测试资产（本批业务参照，不重复实现内部细节）**：`TestErpFinPeriodCloseEndToEnd`（closePeriod 模块关账 + 汇兑重估 + 损益结转，设计文档 C13 引为先例）、`TestErpFinBankReconciliation`/`TestErpFinBankReconciliationEndToEnd`/`TestErpFinBankStatementImport`/`TestErpFinBankStatementMatch`/`TestErpFinBankStatementCounterpartyMatch`/`TestErpFinBankReconAutoReverseJob`（银行对账全套 6 类，设计文档 C14 引为先例）、`TestErpFinBadDebt`/`TestErpFinBadDebtProvisionReversal`（坏账核销/计提）；具体扩展 `FinFrozenClockExtension`（fin-service 测试侧，extends module-common-test `AbstractFrozenClockExtension`；REFERENCE_DATE 2026-07-17 ∈ 期间 2026-07）。
- **C13 动作面（实仓核验在位）**：`ErpFinAccountingPeriodBizModel` 实仓动作 = `preCheck`/`closePeriod`/`finalizePeriod`/`reverseClose`/`openPeriod`——**与设计文档 §6 C13 步骤 1-4 完全一致，无动作名漂移预期**；`ErpFinAccountingPeriodProcessor` 编排（`@BizMutation` 钉 Facade，期末凭证生成后再状态簿记 + flush）。
- **C13 前置 seed（实仓核验在位）**：`erp_fin_accounting_period.csv`（2026-07 OPEN，period id=1）；`erp_fin_accounting_period_status.csv`（period 1 AR/AP/INV/GL/ASSET 全 OPEN，4 凭证 4 POSTED）；`erp_fin_voucher.csv`（PZ-2026-001/002 POSTED 已过账凭证 = 设计「已过账业务链」语义满足）；`erp_fin_ar_ap_item.csv`（**OPEN 往来项 ARAP-EA-001 EMPLOYEE_ADVANCE 1000 + ARAP-EC-001 EXPENSE_CLAIM 300，partner EMP-PTN-001——设计文档 §6 C13 前置「1045-1 追加的 OPEN 往来项」M0.1 审查 MINOR-3 已落 seed，preCheck 期望列出的未核销项来源在位**）；`erp_fin_gl_balance.csv`（period 1 科目 2-6 余额行；按 B1 先例不作断言源）。**设计文档 §6 C13 前置「1445-1 seed」为旧式码，实仓新式码 = period 2026-07（语义前置满足，实施期登记勘误）**。
- **C14 动作面（实仓核验在位）**：`ErpFinBankReconciliationBizModel` 实仓动作 = `generate`/`post`/`reverse`；`ErpFinBadDebtBizModel` 实仓动作 = `writeOff`/`submit`/`approve`/`recover`/`reverseApprove`/`reject`/`runBadDebtProvision(periodId)`——**与设计文档 §6 C14 步骤 1-3 一致**；坏账审批轴 DIRECT（`ErpFinBadDebtApprovalStateMachine`）。
- **C14 前置 seed（实仓核验在位）**：`erp_md_bank_account.csv`（账户 1：工商银行 DEMAND，partner 1 华东科技；账户 2：建设银行，partner 3 北方钢铁）——**设计文档 §6 C14 前置「1234-1 seed」为旧式码，实仓新式码 = erp_md_bank_account 行（语义前置满足，实施期登记勘误）**；无 bank statement/recon/bad-debt seed 行 → 对账单/基金账户/OPEN AR 项按设计走**自包含建数**（先例 bank-recon E2E）。
- **C14 config（实仓核验）**：设计文档 §6 C14 提及 `erp-fin.ap-subject-code=2202` 等 4 键（`ErpFinConstants` 有键**无 DEFAULT_* 缺省值**——fin-service 期间关账测试在专用 test yaml 携带 4103/6603/2202/1122 + `auto-depreciation-on-close=false`，app-erp-all 装配无此 yaml → **4 键 + `auto-depreciation-on-close`/`exchange-revaluation-enabled` 在 app-erp-all 下大概率须 `@NopTestProperty` 指定（实施期核实，对齐 07-25 基线 E2E JVM args 已知键）**）；坏账计提门控 `erp-fin.bad-debt-allowance-gate-enabled` **默认 true**（`ErpFinAccountingPeriodProcessor`），坏账科目键 `bad-debt-allowance-subject-code`/`bad-debt-expense-subject-code` + 计提率 `bad-debt-loss-rate-*`（缺省 0.005/0.02/0.05/0.15/0.40）——实施期以实仓缺省/`@NopTestProperty` 为准并登记。
- **运行互斥**：集成测试与 E2E live server 共用 `db/erp.mv.db`——执行前 `lsof -i :8011`（8080 同查）确认无 live server（e2e-runbook「集成测试」节纪律）。
- **基线计数**：known-good-baselines 2026-08-24 权威计数源 **3820 tests / 0 failures / 0 errors / 1 skipped / 629 报告文件**；app-erp-all **40/0/0/1**。若 B6 已先行落地则以 B6 后基线为准。B7 预期差量 **+2 tests / +2 报告文件**（2 用例类各 1 类 1 方法）。
- **流程改进登记（B3 closure MINOR-2 兑现，B5/B6 已执行）**：最终 `clean install` 会清除 surefire XML 计数证据——全量回归后、clean 前先拷贝计数/XML 再登记基线（本计划 Phase 3 执行纪律）。
- **deferred 触发条件（B1/M0.2 watch-only residual）**：若执行期发生 seed 修正授权路径（roadmap 横切关注点 3），须同步测量登记重录耗时（以单用例 252K/类为预算参考）；本批默认自包含建数，预期不触发。
- **冻结时钟纪律**：C07 用例类以 `C07FrozenClockExtension`（`@RegisterExtension` 静态注册，app-erp-all it/ 内本地扩展）冻结日期——**C13 closePeriod/reverseClose 与 C14 对账/计提均涉期间与日期敏感路径（preCheck 未核销项 DUE_DATE、期间解析）→ 是否引入对应域 frozen clock 由 Phase 1 Decision ④ 裁决（C07FrozenClockExtension 为 app-erp-all 同装配直接先例）**，并登记快照时间戳 `*` 通配处置（B2/B4/B5 先例）。

## Goals

- **C13** = 测试类 `TestErpC13FinPeriodCloseReverse`（1 类 1 方法）：`ErpFinAccountingPeriod__preCheck`（未核销 AR/AP 列表 = seed OPEN 往来项 EMPLOYEE_ADVANCE/EXPENSE_CLAIM + 已过账链项，以实仓返回为准）→ `closePeriod`（模块关账 + 汇兑重估 + 损益结转 → CLOSED；凭证 billCode 反查）→ `finalizePeriod`（CLOSED_FINAL）→ `reverseClose`（→ OPEN）→ `closePeriod` 重新结账幂等（新结转凭证生成）；三层全比对。
- **C14** = 测试类 `TestErpC14FinBankReconBadDebt`（1 类 1 方法）：自包含对账单/银行账户 → `ErpFinBankReconciliation__generate` → `post`（BANK_RECON_ADJ 未达调整凭证，科目 config 以实仓为准）→ `reverse`（红冲，原凭证 isReversed）→ 自包含 OPEN AR 项 → `ErpFinBadDebt__writeOff` → `submit` → `approve` [DIRECT]（坏账核销凭证）→ `recover`（收回红冲）→ `runBadDebtProvision`（期末计提，金额 = 期末 AR × 计提率或实仓口径）；三层全比对。
- 两用例 **RECORDING→CHECKING 往返全绿**。
- roadmap B7 状态 `todo` → `done`（经独立 closure audit）+ 日志 + 基线差量登记 + 设计文档 §6 C13/C14 旧式码勘误登记（若发生）。

## Non-Goals

- B8-B10 用例（各归对应分批计划）。
- 生产代码/ORM/契约/API 变更——用例实施中发现的产品缺陷按 roadmap 横切关注点 4 走保护区域门禁（accounting/finance postings = plan-first + owner doc + tests；其余 = auto + dual-agent-approval），**不得在本计划内直接改生产代码**。
- 默认修改 seed CSV——确需修正走 roadmap 横切关注点 3「seed 修正授权」流程；默认以自包含建数满足前置。
- 不接 CI（roadmap 已裁决出 scope）；不做浏览器层 E2E 页面验证（分工边界）。
- 不实现未覆盖功能（横切关注点 6「以当前实现为准」）：C13/C14 步骤若与实仓行为不符，以实仓行为替代 + 设计文档勘误登记，不强行实现缺失功能（如坏账计提 config 默认关闭时以实仓门控行为为准）。

## Task Route

- Type: `implementation-only change`（集成测试代码 + 快照 + 文档，零生产代码/契约/模型变更）
- Owner Docs: `docs/design/integration-testing.md`（§6 C13/C14 规格 + §3.4 编写纪律 + §4 粒度 + §5 xwf 清单）、`docs/backlog/integration-test-roadmap.md`（B7 行 + 横切关注点）、`docs/testing/e2e-runbook.md`（「集成测试」节）、`docs/testing/known-good-baselines.md`
- Skill Selection Basis: `nop-testing` 匹配测试基类选择/@NopTestConfig/@NopTestProperty（C14 科目 config = C06/C08/C10 同型）/快照录制回放（RECORDING→CHECKING）/三层验证模型；M0.2 试点 + B1-B6 集成用例类为同模式范本，fin 既有 EndToEnd 测试（PeriodClose/BankReconciliation/BadDebt）为业务参照。

## Infrastructure And Config Prereqs

- 文件型 H2 `db/erp.mv.db` fresh 机制已就绪（基类每类 1 次清理）；**与 E2E live server 互斥**——执行前 `lsof -i :8011`（8080 同查）确认无 live server。
- **C14 config（待实施期核实）**：坏账核销科目（设计文档 1231/6701 提及）与计提科目 config 键/缺省值以实仓为准；若缺省无法满足断言，`@NopTestProperty` 指定（对齐既有范式并登记）。
- C13 closePeriod 汇兑重估/损益结转科目以实仓默认配置为准（若与 07-25 基线 4 键 JVM args 相关，实施期核对 application.yaml %test 与既有键位）。
- 无端口/密钥/外部服务/环境变量新增；surefire 串行化已在 app-erp-all pom 落地。

## Execution Plan

### Phase 1 — C13 期末结账全链与反结账（混合类型：1/3 Add + 1/3 Proof + 1/3 Decision，逐项标注）

Status: completed
Targets: `app-erp-all/src/test/java/io/nop/app/all/it/TestErpC13FinPeriodCloseReverse.java`、`app-erp-all/_cases/io/nop/app/all/it/TestErpC13FinPeriodCloseReverse/`
Skill: nop-testing

- Item Types: `Add | Proof | Decision`
- Prereqs: B6 done（批内排序）+ M0.1 + M0.2

- [x] Add：**C13 测试类** `TestErpC13FinPeriodCloseReverse`（1 类 1 方法）——按设计文档 §6 C13 规格 + `TestErpFinPeriodCloseEndToEnd` 业务参照：`ErpFinAccountingPeriod__preCheck`（未核销 AR/AP 列表断言 = seed OPEN 往来项 ARAP-EA-001/ARAP-EC-001 等，以实仓返回为准）→ `closePeriod`（period 2026-07 → CLOSED；模块关账状态 AR/AP/INV/GL/ASSET 翻转 + 汇兑重估/损益结转凭证生成，billCode 反查）→ `finalizePeriod`（CLOSED_FINAL）→ `reverseClose`（→ OPEN）→ `closePeriod` 重新结账（新结转凭证生成 = 幂等断言）；静态载荷走 `input/N_step.json5` + `addVar`/`@var:` 动态 id。
  - Skill: nop-testing
- [x] Decision：**期间/凭证断言口径裁决**——① preCheck 返回结构（未核销项列表的字段/口径）以实仓为准；② closePeriod 生成的汇兑重估/损益结转凭证断言锚点（billCode 反查键 + 科目/金额以实仓行为为准——`TestErpFinPeriodCloseEndToEnd` 先例口径）；③ 反结账→重新结账的幂等断言口径（新凭证生成 vs 复用，以实仓为准）；④ 若执行期需 frozen clock 冻结（期间解析/日期敏感），记录采用方案。与设计文档不符处 Phase 3 登记 §6 C13 勘误。
  - Skill: nop-testing
- [x] Proof：**C13 RECORDING→CHECKING 往返**——RECORDING 录制 → CHECKING 复跑全绿；层 1 锚点：preCheck 列出未核销项、closePeriod 后 CLOSED + 模块关账状态 + 汇兑重估/损益结转凭证（billCode 反查）、finalize 后 CLOSED_FINAL、reverseClose 后 OPEN、重新结账幂等。
  - Skill: nop-testing

Exit Criteria:

- [x] C13 类存在且 RECORDING→CHECKING 往返绿（失败模式 = 任一断言/快照 diff 不通过）
- [x] 期间/凭证断言口径裁决落盘（含设计文档 §6 C13 勘误登记，若与实仓行为不一致）

### Phase 2 — C14 银行对账与坏账计提回收（混合类型：1/3 Add + 1/3 Proof + 1/3 Decision，逐项标注）

Status: completed
Targets: `app-erp-all/src/test/java/io/nop/app/all/it/TestErpC14FinBankReconBadDebt.java`、`app-erp-all/_cases/io/nop/app/all/it/TestErpC14FinBankReconBadDebt/`
Skill: nop-testing

- Item Types: `Add | Proof | Decision`
- Prereqs: Phase 1（同基类同装配，先例链打通后无新机制风险）

- [x] Add：**C14 测试类** `TestErpC14FinBankReconBadDebt`（1 类 1 方法）——按设计文档 §6 C14 规格 + `TestErpFinBankReconciliationEndToEnd`/`TestErpFinBadDebt` 业务参照：自包含银行账户/对账单（或引用 seed 账户）→ `ErpFinBankReconciliation__generate` → `post`（BANK_RECON_ADJ 未达调整凭证，科目 config 按裁决）→ `reverse`（红冲：原凭证 isReversed 断言）→ 自包含 OPEN AR 项（partner 隔离）→ `ErpFinBadDebt__writeOff` → `submit` → `approve` [DIRECT]（坏账核销凭证，科目以实仓为准）→ `recover`（收回红冲）→ `runBadDebtProvision`（期末计提，金额口径按裁决）；静态载荷走 `input/N_step.json5` + `addVar`/`@var:` 动态 id。
  - Skill: nop-testing
- [x] Decision：**科目 config 与计提口径裁决**——① 未达调整凭证科目（`erp-fin.ap-subject-code` 等 4 键，`ErpFinConstants` 有键无 DEFAULT_*）与 `auto-depreciation-on-close`/`exchange-revaluation-enabled`：app-erp-all 装配缺 fin-service 专用 test yaml → 按 07-25 基线 E2E JVM args 已知键值 `@NopTestProperty` 指定（实施期核实）；② 坏账核销科目（`bad-debt-allowance-subject-code`/`bad-debt-expense-subject-code`，设计文档 1231/6701 提及）与计提率缺省（0.005/0.02/0.05/0.15/0.40）：**计提门控 `erp-fin.bad-debt-allowance-gate-enabled` 默认 true（已实仓核验）**，若 runBadDebtProvision 走门控断言则以缺省开启为准；③ 计提金额断言口径（期末 AR × 计提率 or 实仓 `BadDebtProvisionCalculator` 行为）。与设计文档不符处 Phase 3 登记 §6 C14 勘误。
  - Skill: nop-testing
- [x] Proof：**C14 RECORDING→CHECKING 往返**——RECORDING 录制 → CHECKING 复跑全绿；层 1 锚点：对账状态机（GENERATED→POSTED→REVERSED）、BANK_RECON_ADJ 凭证借贷平衡 + 红冲闭环（原凭证 isReversed）、坏账核销凭证 + 收回红冲、计提金额（按裁决口径）。
  - Skill: nop-testing

Exit Criteria:

- [x] C14 类存在且 RECORDING→CHECKING 往返绿（失败模式 = 任一断言/快照 diff 不通过）
- [x] 科目 config 与计提口径裁决落盘（含设计文档 §6 C14 勘误登记，若与实仓不一致）

### Phase 3 — 收尾与回归（Proof-heavy）

Status: completed
Targets: `docs/backlog/integration-test-roadmap.md`（B7 → done + 头「最后更新」注记）、`docs/logs/2026/08-24.md`、`docs/testing/known-good-baselines.md`（差量登记）、`docs/design/integration-testing.md`（§6 C13/C14 勘误登记，若 Phase 1/2 触发）
Skill: none

- Item Types: `Proof | Add`
- Prereqs: Phase 2

- [x] Proof：**局部回归** `mvn test -pl app-erp-all` 全绿（含 2 个新用例类，串行 fork 单 JVM 顺序执行）+ **全量回归** `mvn test`（全 reactor）零新增失败 + `mvn clean install -DskipTests` BUILD SUCCESS——对照 known-good-baselines（B6 后基线）口径，+2 tests / +2 报告文件全额归因 B7，零新增失败；**clean 前先拷贝 surefire 计数/XML 证据**（B3 closure MINOR-2 流程改进兑现，B5/B6 已执行）。
  - Skill: none
- [x] Add：roadmap B7 `todo` → `done` + 头「最后更新」注记 + `docs/logs/2026/08-24.md` 日志条目（按日志书写指南）+ known-good-baselines 差量登记 + 设计文档 §6 勘误登记（若触发，含 C13/C14 旧式码漂移点）。
  - Skill: none

Exit Criteria:

- [x] 局部 + 全量回归零新增失败（与已知基线口径一致）
- [x] roadmap B7 = done + 日志条目存在 + 基线差量登记 + 勘误登记（若触发）

## Draft Review Record

- Independent draft review iteration 1: acceptable as-is（0 BLOCKER / 0 MAJOR / 5 MINOR，ses_fcf6c5eefffedFocirY76giW1P，独立 general 子代理新会话）——18 项可核验基线事实全部 PASS/一致；roadmap B7 对齐（范围/Skill/横切关注点/DIRECT 审批轴/自包含建数/零生产代码变更）正确；B6 批内排序依赖表达正确；期间/关账/配置门控风险均绑定显式 Decision 项。5 MINOR 已作为润色全部吸收：F1 gl_balance 科目 2/3 → 2-6（按 B1 先例不作断言源）；F2 冻结时钟引用 C07FrozenClockExtension（app-erp-all it/ 同装配直接先例）+ FinFrozenClockExtension 位置精确化；F3 计提门控 `erp-fin.bad-debt-allowance-gate-enabled` 默认 true（实仓核验）+ 4 科目键无 DEFAULT_* → app-erp-all 下 `@NopTestProperty` 大概率必须（Baseline/Phase 2 Decision ② 修订）；F4 「按需」反松弛词 → 绑定 Phase 1 Decision ④；F5 bankrecon 补全 6 类。**共识达成，计划可执行。**

## Closure Gates

> 仅在所有项目和每个阶段的退出标准都勾选 `[x]` 后关闭。完整仓库验证在此处：结束时运行 `typecheck`/`build`/`lint`/`test` 一次。

- [x] 范围内行为完成（C13/C14 两用例三层全比对全绿 + 期间/科目/计提口径裁决）
- [x] 相关文档对齐（设计文档 §6 ↔ 用例实现 ↔ roadmap B7 ↔ e2e-runbook ↔ known-good-baselines 无矛盾）
- [x] 已运行验证（`mvn test -pl app-erp-all` 全绿 + 全量 `mvn test` 零新增失败 + `mvn clean install -DskipTests` BUILD SUCCESS）
- [x] 无范围内项目降级为 deferred/follow-up
- [x] 独立草案审查已完成并记录
- [x] 文本一致性已验证：状态、阶段、门控和日志都一致
- [x] 结束审计由独立子代理（新会话）执行；执行者未自我审计且未将此留为 `[ ]` 作为人工门控占位符
- [x] 结束证据存在于文件中

## Deferred But Adjudicated

- （无范围内降级项。执行期未触发 seed 修正授权路径（B1/M0.2 watch-only residual 条件未成立——C13/C14 全部自包含建数）；重录耗时测量项无触发。）

## Closure

Status Note: B7（C13/C14）三 Phase 全部完成。C13 `TestErpC13FinPeriodCloseReverse`（自包含 USD 应收项驱动汇兑重估 → preCheck → closePeriod 五模块关账 + FX-REVAL/PERIOD-CLOSE 凭证 → finalizePeriod → reverseClose 红冲闭环 → 重新结账幂等；9 键科目 config @NopTestProperty，三键必配 auto-post-on-close=true / bad-debt-allowance-gate-enabled=false / reverse-close-approval-required=false）与 C14 `TestErpC14FinBankReconBadDebt`（自包含资金账户/对账单 → generate/post/reverse 对账状态机 + BANK_RECON_ADJ 凭证 + 红冲闭环 → 坏账 writeOff/submit/approve [DIRECT] → recover → runBadDebtProvision 期末计提 RESERVE 55）两用例 RECORDING→CHECKING 往返全绿（联合复跑 2/0/0 连跑两次稳定）；局部 `mvn test -pl app-erp-all` 44/0/0/1 + 全量 `mvn test` 3824/0/0/1/633（对照 B6 3822/0/0/1/631 差量 +2 tests / +2 报告文件全额归因 B7，零新增失败，surefire 证据 `_tmp/b7-surefire-evidence/`）+ `mvn clean install -DskipTests` 156 模块 BUILD SUCCESS。执行期裁决：C13 preCheck 报告属性 getter 序列化 / closePeriod 同事务 FX 凭证未 flush 致 PERIOD_CLOSE 不含 FX 腿（1130 非 1280，勘误登记）/ 重结账幂等 = 新凭证 / 冻结时钟 C13C14FrozenClockExtension；C14 未达对方科目缺省键 2240OTHER + 坏账三键 @NopTestProperty / 计提口径 = 实仓 Calculator / 自包含建数未触发 seed 修正授权。响应快照跨 run 时间戳 @var 不稳定 `*` 通配（B4/B5/B6 先例）。roadmap B7 todo→done + 日志 + 基线差量登记 + 设计文档 §6 C13/C14 勘误登记全部落盘。

Closure Audit Evidence:

- Auditor / Agent: 独立 general 子代理新会话 `ses_fcf0c968affeLZ4sg0uTAYTJJL`（2026-08-24，读库审计零修改）
- Evidence: 审计逐项核验 = ① 计划文本一致性（Plan Status completed / 三 Phase completed / 全部 `[ ]` 勾选，审计时点 Closure Gates 未勾选为 B6 同型前置态，本段即审计兑现）；② 实施证据在位（两用例类 + C13C14FrozenClockExtension + `_cases` 快照 C13 6/6/8 + C14 13/13/11）；③ CHECKING 复跑 `mvn test -pl app-erp-all -Dtest=TestErpC13FinPeriodCloseReverse,TestErpC14FinBankReconBadDebt` **2/0/0 BUILD SUCCESS**（8011/8080 无 live server 先行确认）；④ 文档一致性（roadmap B7 done + 头注记 / 日志 B7 条目 / baselines 2026-08-24 B7 行 3824/0/0/1/633 + app-erp-all 44/0/0/1 / 设计文档 §6 C13/C14 勘误 + 头注记）；⑤ seed 交叉核对（period 1 OPEN / ARAP-EA-001/ARAP-EC-001 OPEN / PZ-2026-001/002 POSTED / 科目 1002/1122/1231/6701/2240OTHER/5001/4103/6603 在位 / 银行账户 seed）零矛盾 + 用例算术自洽（FX diff 150 / 计提 55 / 未达 600）。Verdict = **PASS**。

Follow-up:

- 无。全部范围内项目完成，零 deferred/follow-up。