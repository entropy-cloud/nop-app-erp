# 2026-08-08-0015-2 rc-ma4-a4-2-143-146-master-data-runtime 供应商价格解析器接线/条码唯一TOCTOU/价格校验级别/SKU引用检查删除运行时确认

> Plan Status: active
> Last Reviewed: 2026-08-08
> Source: `docs/backlog/requirement-compliance-roadmap.md` Work Items A4.2.143 / A4.2.144 / A4.2.145 / A4.2.146
> Related: `docs/audits/2026-08-06-0100-3-rc-ma1-a1-41-master-data-full.md`（A1.41 §7 SP-2..SP-5 + §6 P2-RC-058 DB UK 缺失 / P2-RC-056 阈值硬编码 / P1-RC-062 SKU 引用检查缺失 + reuse P1-MA2-093 行级权限[R1.29 resolved，A1.41 SP-1 已由 A4.1.25/A4.2.10 闭合，本计划不重复覆盖]）
> Audit: required
> Mission: requirement-compliance
> Work Item: A4.2.143-A4.2.146（master-data 域 MA4 运行时验证）

## Current Baseline

master-data 域切片（A1.41 全功能：扫码开单/多单位换算/价格优先级/最低价校验/默认 SKU/状态约束/看板）MA1 报告 §7 共列出 5 项静态存疑点（SP-1..SP-5），其中 **SP-1（UC-MD-07 ④ 看板行级权限 ErpOrgIsolationQueryTransformer 覆盖）已由 A4.1.25 + A4.2.10 合并闭合**（8 域 dashboard orgId 行级权限 P1-MA2-093 reuse R1.29 全覆盖，done ✅），本计划不重复覆盖。本计划覆盖 SP-2..SP-5 共四项（A4.2.143-A4.2.146）。master-data 是基础数据底座非业务流转域，不直接产生 GL 凭证；A4.2.146（被引用 SKU 删除）触及数据完整性探针——只读确认，不改 ORM/删除路径。存疑点分两类：(1) 接线/契约可达性（A4.2.143 IErpMdSupplierPriceResolver purchase 域接线 / A4.2.145 priceValidationLevel 种子分类 WARN 语义）；(2) 数据完整性/并发边界（A4.2.144 enforceBarcodeUnique TOCTOU 无 DB UK / A4.2.146 IErpMdSkuReferenceChecker 缺失下被引用 SKU 删除 P1-RC-062）。

- **A4.2.143（A1.41 SP-2 UC-MD-03 ④ IErpMdSupplierPriceResolver 在 purchase 域是否有未被 grep 发现的接线）**：HEAD 静态判定 = grep `implements IErpMdSupplierPriceResolver` 全生产代码 0 命中，但 purchase 域可能经其他机制（nop-dyn 动态 bean / Spring `@Component` + 配置类生成 / delta 层 beans.xml）注入。运行时确认 `ErpMdMaterialSkuBizModel.supplierPriceResolver` 实际注入值（null or instance）。
- **A4.2.144（A1.41 SP-3 UC-MD-01 ③ enforceBarcodeUnique 并发 save 的 TOCTOU 实际窗口；G1 无 DB UK 兜底）**：HEAD 静态判定 = `enforceBarcodeUnique:255-271` 应用层 findFirst 查无 → saveEntity，DB 无 UK 兜底；单测 `testBarcodeDuplicateRejected:182-214` 单线程顺序执行不触发。运行时实测并发 save 同 barcode 的实际数据库状态（两请求均通过 findFirst 查无 → 双双 saveEntity → 唯一性失效）。
- **A4.2.145（A1.41 SP-4 UC-MD-04 ① priceValidationLevel="20" 种子分类实际 WARN 语义影响面；G5）**：HEAD 静态判定 = 种子数据 `ErpMdMaterialCategory` 是否含 `priceValidationLevel="20"` + 生产新创建分类默认值。运行时复核种子数据 + 运行时新建分类的实际 WARN 派遣路径。
- **A4.2.146（A1.41 SP-5 UC-MD-06 ② IErpMdSkuReferenceChecker 生产缺失下被引用 SKU 删除的实际数据完整性事件；P1-RC-062）**：HEAD 静态判定 = `useLogicalDelete=true` 软删保留物理行（历史单据 skuId ORM to-one 仍可解析）BUT 软删后 SKU 在 operational 查询中消失（delVersion=0 过滤）+ 硬删路径若启用则物理破坏引用。运行时实测被活跃 AP/AR 发票引用的 SKU 经 GraphQL `ErpMdMaterialSku__delete` 是否实际成功（生产 skuReferenceChecker=null → validateSkuDeactivation 仅默认 SKU 守卫触发 → 被引用 SKU 删除成功 → 数据完整性事件）。**触及数据完整性探针——只读确认，不改 ORM/删除路径。**

剩余差距：四项均为只读运行时确认。缺陷项（A4.2.146 P1-RC-062）维持 P1，修复归 MR1（触 ORM/删除路径须 ask-first）；DB UK 缺失项（A4.2.144 P2-RC-058）维持 P2，修复归 MR1（触 ORM UK 须 ask-first）；接线/边界项（A4.2.143/145）确认行为正确或登记 watch-only。本计划仅确认运行时行为以维持/细化裁决，不改变 Q4 强制实现义务。

## Goals

- 对 A4.2.143-A4.2.146 四项存疑点产出运行时行为证据链，输出验证报告落盘 `docs/audits/`。
- 每项给出 §2 判据裁决：A4.2.146（P1-RC-062 SKU 引用检查缺失）维持 P1（Q4 强制实现，修复归 MR1 触 ORM/删除须 ask-first）+ 记录运行时证据；A4.2.144（P2-RC-058 DB UK 缺失）维持 P2（修复归 MR1 触 ORM UK 须 ask-first，登记不强制）；A4.2.143（supplierPriceResolver 接线）/ A4.2.145（priceValidationLevel WARN 语义）确认行为正确或登记 watch-only；若运行时发现活跃数据破坏则触发 MR0（A4.2.146 软删不物理破坏但硬删路径存在，预期不触发 MR0——软删主路径 + delVersion 过滤是设计简化非活跃破坏）。
- 完成后回写 roadmap A4.2.143-A4.2.146 `todo → done`，并按裁决更新 arm-index（维持注记，无未经比对新建）。

## Non-Goals

- 不实现 SKU 引用检查（P1-RC-062）/ 条码 DB UK（P2-RC-058）——修复义务归 MR1 R1.0 展开器；P1-RC-062 触 ORM/删除路径须 ask-first；P2-RC-058 触 ORM UK 须 ask-first。
- 不覆盖 A1.41 SP-1（看板行级权限 ErpOrgIsolationQueryTransformer 覆盖）——已由 A4.1.25 + A4.2.10 合并闭合（8 域 dashboard orgId P1-MA2-093 reuse R1.29 全覆盖 done ✅）。
- 不修改任何真相源（product-scope/use-cases/owner doc 需求契约段落）。
- 不修改 ORM/删除路径/supplierPriceResolver 接线（本计划为只读确认；roadmap §横切关注点 #5 ask-first 保护区域）。
- 不复跑 MA2 状态机审计（master-data 无专属 MA2 报告，无可复用行为证据——A1.41 §0 已声明）；不重审 P1-RC-062/P2-RC-058 维度（A1.41 已审，本计划仅运行时确认）。

## Task Route

- Type: `verification or audit work`
- Owner Docs: `docs/audits/2026-08-06-0100-3-rc-ma1-a1-41-master-data-full.md` §5/§6/§7 + `docs/design/master-data/`（use-cases.md / sku-multi-unit.md / price-list.md）
- Skill Selection Basis: roadmap MA4 全部工作项指定 `docs/skills/multi-dimensional-audit-prompt.md`。本计划为只读审计，无代码变更。

## Infrastructure And Config Prereqs

- No infra prereqs beyond existing baseline. 本计划为 grep 接线 census + TOCTOU 窗口静态确认 + 种子数据复核 + 删除路径追踪（grep census / 守卫追踪 / 种子数据复核 / 代码路径追踪），无需运行应用或 DB。各存疑点的运行时行为经由静态证明运行时决定性事实得出（A4.2.143 接口零实现 ⇒ 运行时注入为 null；A4.2.146 删除路径代码追踪 ⇒ 运行时实际路径；A4.2.145 种子/默认值即运行时数据；A4.2.144 静态确认 findFirst→save + 无 UK 的 TOCTOU 窗口存在，不实际并发执行）。

## Execution Plan

### Phase 1 - 运行时证据采集与验证报告撰写（A4.2.143-A4.2.146）

Status: planned
Targets: `docs/audits/2026-08-08-0015-rc-ma4-a4-2-143-146-master-data-runtime.md`（新建验证报告）
Skill: `docs/skills/multi-dimensional-audit-prompt.md`

- Item Types: `Proof`
- Prereqs: A4.2 done ✓；A1.41 done ✓

- [ ] **A4.2.143 IErpMdSupplierPriceResolver 在 purchase 域是否有未被 grep 发现的接线确认（UC-MD-03 ④）**：确认 grep `implements IErpMdSupplierPriceResolver` 全生产代码 0 命中；确认 purchase 域是否经其他机制（nop-dyn 动态 bean / Spring `@Component` + 配置类 / delta beans.xml）注入；确认 `ErpMdMaterialSkuBizModel.supplierPriceResolver` 实际注入值（null or instance）。裁决：确认接线完全缺失（接口零实现）或 delta 定制层注入（登记 watch-only）。
  - Skill: `docs/skills/multi-dimensional-audit-prompt.md`
- [ ] **A4.2.144 enforceBarcodeUnique 并发 save 的 TOCTOU 实际窗口确认（UC-MD-01 ③ G1 无 DB UK；P2-RC-058）**：确认 `enforceBarcodeUnique:255-271` 应用层 findFirst 查无 → saveEntity + DB 无 UK 兜底；确认并发 save 同 barcode 的实际数据库状态（两请求均通过 findFirst 查无 → 双双 saveEntity → 唯一性失效窗口）。裁决：维持 P2-RC-058 P2（DB UK 缺失 TOCTOU 窗口存在但单线程主路径 OK，修复归 MR1 触 ORM UK 须 ask-first，登记不强制）。
  - Skill: `docs/skills/multi-dimensional-audit-prompt.md`
- [ ] **A4.2.145 priceValidationLevel="20" 种子分类实际 WARN 语义影响面确认（UC-MD-04 ① G5）**：确认种子数据 `ErpMdMaterialCategory` 是否含 `priceValidationLevel="20"` + 生产新创建分类默认值；复核 WARN 派遣路径（价格低于 minPrice 时 WARN 而非阻断的实际影响面）。裁决：确认种子/默认值取值 + WARN 语义行为（登记 watch-only 部署配置决策）。
  - Skill: `docs/skills/multi-dimensional-audit-prompt.md`
- [ ] **A4.2.146 IErpMdSkuReferenceChecker 生产缺失下被引用 SKU 删除的实际数据完整性事件确认（UC-MD-06 ② P1-RC-062）**：确认 `useLogicalDelete=true` 软删保留物理行 + 软删后 operational 查询消失（delVersion=0 过滤）+ 硬删路径存在；确认被活跃 AP/AR 发票引用的 SKU 经 GraphQL `ErpMdMaterialSku__delete` 是否实际成功（skuReferenceChecker=null → validateSkuDeactivation 仅默认 SKU 守卫 → 被引用 SKU 删除成功）。**触及数据完整性探针——只读确认，不改 ORM/删除路径。** 裁决：维持 P1-RC-062 P1（被引用 SKU 删除无引用检查守卫，Q4 强制实现，修复归 MR1 触 ORM/删除路径须 ask-first；软删主路径 delVersion 过滤是设计简化非活跃物理破坏，不触发 MR0）。
  - Skill: `docs/skills/multi-dimensional-audit-prompt.md`
- [ ] **验证报告撰写**：四项存疑点各出 §裁决（接线缺失 / 维持 P1 + 运行时证据 / 维持 P2 / 登记 watch-only / 触发 MR0）+ §与既有 finding 衔接（P1-RC-062 + P2-RC-058 + reuse P1-MA2-093[R1.29] 交叉引用）+ §过程纪律自检（checker 退出码门控——无代码变更 actual=baseline；closure-audit 独立性声明）+ §A1.41 SP-1 排除声明（已由 A4.1.25/A4.2.10 闭合）+ §数据完整性探针纪律声明（A4.2.146 READ-ONLY）。报告落盘。
  - Skill: `docs/skills/multi-dimensional-audit-prompt.md`

Exit Criteria:

> 本阶段为只读审计，无生产代码变更。A4.2.146（被引用 SKU 删除）触及数据完整性探针——只读确认，不改 ORM/删除路径。

- [ ] 验证报告落盘，含四项存疑点各自裁决 + file:line 证据 + §2 判据命中分支
- [ ] 每项裁决明确：接线缺失 / 维持分级（P1 Q4 强制实现 / P2 登记 / watch-only）+ 运行时证据记录，或升级触发 MR0

### Phase 2 - Finding 衔接、roadmap/log 同步

Status: planned
Targets: `docs/backlog/requirement-compliance-roadmap.md`（A4.2.143-146 done）、`docs/audits/arm-index.md`（维持注记追加）、`docs/logs/2026/08-08.md`
Skill: none

- Item Types: `Decision | Add`
- Prereqs: Phase 1 报告落盘

- [ ] `Decision` arm-index 衔接裁决：P1-RC-062（SKU 引用检查缺失）维持 P1（运行时确认被引用 SKU 删除无守卫，Q4 强制实现，修复归 MR1 触 ORM/删除路径须 ask-first）；P2-RC-058（条码 DB UK 缺失）维持 P2（TOCTOU 窗口存在，修复归 MR1 触 ORM UK 须 ask-first，登记不强制）；P1-MA2-093（行级权限）维持 resolved R1.29。无新 finding 新建（全部维持）。
- [ ] `Add` roadmap A4.2.143-A4.2.146 `todo → done`；`docs/logs/2026/08-08.md` 追加完成条目。

Exit Criteria:

- [ ] roadmap 四项状态已更新为 done 且与报告裁决一致
- [ ] arm-index 维持注记已追加（无未经比对直接新建的 finding）

## Draft Review Record

- Independent draft review iteration 1: accept (mission-driver 2026-08-07-104509) — 格式合规（front matter/Current Baseline/Goals/Non-Goals/Task Route/Infrastructure/两阶段 Execution Plan/Draft Review Record/Closure Gates/Deferred But Adjudicated 齐全）；Exit Criteria 可测、覆盖四项 SP + 报告撰写 + roadmap/log/arm-index 同步；范围边界清晰（SP-1 排除已记录、ORM/删除 ask-first 保护区域贯穿）；引用文件均存在且 roadmap A4.2.143-146 实际为 todo。修复 1 处 Major：Infrastructure 段「TOCTOU 并发探针」与「无需运行应用或 DB」自相矛盾（并发竞态无法在不执行时探针），已改为「TOCTOU 窗口静态确认」并补注各存疑点运行时行为经由静态证明运行时决定性事实得出，消除矛盾且不削弱证据效力（A4.2.143 零实现⇒null、A4.2.146 路径追踪均为静态可决断）。残留 Minor：Goals/标题「运行时」措辞偏 aspirational，实际证据为 file:line 静态，下游 closure/deep 审计可复核。

## Closure Gates

> 本计划为只读审计（零生产代码/ORM/api.xml/view.xml/真相源变更）。closure 时确认 checker 未触发 actual > baseline。

- [ ] 范围内行为完成（四项存疑点均有 file:line 运行时证据 + 明确裁决）
- [ ] 相关文档对齐（报告落盘 + roadmap/log 同步 + arm-index 衔接裁决记录）
- [ ] 已运行验证（checker actual=baseline 确认；无代码变更故无 build/test 回归风险）
- [ ] 无范围内项目降级为 deferred/follow-up
- [ ] 独立草案审查已完成并记录
- [ ] 文本一致性已验证：状态、阶段、门控和日志都一致
- [ ] 结束审计由独立子代理（新会话）执行；执行者未自我审计且未将此留为 `[ ]` 作为人工门控占位符
- [ ] 结束证据存在于文件中

## Deferred But Adjudicated

### P1-RC-062 / P2-RC-058 修复实现

- Classification: `out-of-scope improvement`
- Why Not Blocking Closure: 本计划仅运行时确认；P1-RC-062（SKU 引用检查缺失）修复归 MR1 触 ORM/删除路径须 ask-first；P2-RC-058（条码 DB UK 缺失）修复归 MR1 触 ORM UK 须 ask-first（P2 登记不强制）。本审计维持分级不撤销。
- Successor Required: yes（MR1 R1.0 展开为 RC-R1.n 时承接）
