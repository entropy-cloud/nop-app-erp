# 2026-07-14-0930-1-1518-3-audit-revisions plan 1518-3 审计驱动修订（目标显示列存在性补核 + Goals 订正 + Deferred 补齐）

> Plan Status: completed
> Mission: erp
> Source Audits: audits/2026-07-14-0930-multi-1518-3-fk-name-resolution-audit.md, audits/2026-07-14-open-ended-1518-3-fk-name-resolution-audit.md
> Last Reviewed: 2026-07-14
> Related: `docs/plans/2026-07-13-1518-3-foundational-remaining-fk-name-resolution.md`（修订对象）、`docs/skills/multi-dimensional-audit-prompt.md`、`docs/skills/open-ended-audit-prompt.md`
> Audit: required

## Current Baseline

两份独立审计（多维审计 PASS + 开放式审计 needs revision）对 plan 1518-3 提出修订要求。多维审计确认 FK 列名（第一跳）全部正确、ext:relation 缺口完整、计数算术无误、保护区域无违规。开放式审计发现**第二跳（目标实体显示列存在性）系统性遗漏**，产生 1 P0 + 3 P1 + 1 P2 + 1 P3。

经独立子代理实时 ORM 全量核实，两份审计发现全部确认：

### P0 — Blocker

`configId@ErpB2bMftLog → ErpB2bMftConfig` 目标无任何显示列。`ErpB2bMftConfig` 列集合 = `{id, orgId, partnerId, protocol, transportEndpoint, localAs2Id, remoteAs2Id, ...}`，**无 `name`、无 `code`、无任何标识性字符串列**。计划声称可解析且未纳入 Deferred。

### P1 — Major（3 个 FK 目标仅有 Integer 行号/版本号）

| FK prop | 使用实体 | 目标实体 | 目标仅有候选列 | 有 to-one |
|---|---|---|---|---|
| `contractLineId` | ErpCtInvoicePlan / ErpCtConsumptionLine / ErpCtVolumeDiscount | ErpCtContractLine | `lineNo`(Int) / `description` | 是 |
| `contractVersionId` | ErpCtSignatureRequest | ErpCtContractVersion | `versionNo`(Int) | 是 |
| `drpLineId` | ErpInvDrpCrossDock | ErpDrpLine | `lineNo`(Int) | 是 |

### P2 — Minor

`providerRequestId@ErpCtSignatureRequest` 为 VARCHAR(200) 自引用 to-one，计划 Phase 3 表未列此列也未说明排除理由。

### P3 — 叙事错误

Goals（行 151）系统性将 ~15 个目标显示列写成 `.name`，实际部分目标仅有 `.code` 或其他列。

### MINOR-2 — 多维审计 advisory

前序 CRM/CS 批次（1518-1）发现 `orm_attached()` 守卫模式，manufacturing 批次发现 `safeBatchLoad` try-catch 降级模式。计划未显式提及。

## Goals

- P0：`configId@ErpB2bMftLog` 加入 1518-3 `Deferred But Adjudicated`（保留原始 ID），Phase 4 Decision + Goals 订正。
- P1：Phase 3/5 Decision 为 `contractLineId`/`contractVersionId`/`drpLineId` 给出明确裁决——用 `lineNo`/`versionNo`（Integer→String）作 fallback 显示，并相应修订 Goals。
- P2：Phase 3 Decision 增 `providerRequestId` 排除说明（VARCHAR 自引用，按规则排除）。
- P3：Goals 行 151 按实际 ORM 列名批量订正，或在 Goals 顶部加注"显示 getter 以实现时 ORM 实际列为准"。
- MINOR-2：在 1518-3 执行段或实施指导中增 `orm_attached()` / `safeBatchLoad` 防御模式注释。
- Draft Review Record 补一句：FK 列名已核、目标显示列存在性未核（本审计补核结果）。
- 零代码变更——本计划仅修订 1518-3 计划文档本身。

## Non-Goals

- **实施 1518-3 的任何 Phase**——本计划仅修订计划文档，实施仍由 1518-3 本体执行。
- **重跑草案审查**——修订内容为审计发现的直接修正，修订后状态保持 active（草案审查 3 轮已收敛，本次修订不改变范围结构）。
- **修改其他计划**——仅修订 1518-3。

## Task Route

- Type: `仅实现变更`（计划文档修订，无代码/模型/契约变更）
- Owner Docs: 无新增 owner doc 读取需求
- Skill Selection Basis: 计划文档修订不需要加载技能。`Skill: none`
- Protected Areas: 无 ORM/ask-first 变更。

## Infrastructure And Config Prereqs

No infra prereqs。

## Execution Plan

### Phase 1 — 修订 1518-3 计划文档（覆盖全部审计发现）

Status: completed
Targets: `docs/plans/2026-07-13-1518-3-foundational-remaining-fk-name-resolution.md`
Skill: none

- Item Types: `Decision | Edit`
- Prereqs: 无

- [x] `Decision`: P0 裁决——`configId@ErpB2bMftLog` 保留原始 ID（目标 `ErpB2bMftConfig` 无 name/code/任何标识性字符串列），加入 `Deferred But Adjudicated` 新条目。Phase 4 B2B 域实体表 ErpB2bMftLog 行从 `orgId, configId` 改为 `orgId`（configId 标注保留原始 ID）。Goals 删除 `config→configName 读 ErpB2bMftConfig.name` 条目。
  - **执行订正**：原指令"B2B 机制 D 实体数 13→12 / 总机制 D 实体数 66→65"经实时 ORM 复核为误算——ErpB2bMftLog 仍有 `orgId`（→orgName to-one），故**仍属机制 D 实体**，机制 D 实体数维持 **66**（B2B 域维持 **13**）。configId 是 66 机制 D 实体内的延后 FK 列（与 8 个 APS ext:relation 缺口 FK 同类），非独立实体计数项。全域算术不变：66 机制 D + 5 无 FK + 1 ext:relation 缺口 = 72。本条 Phase 1 行 94 的"但仍计入机制 D 实体数因 orgId 需解析"即此订正的依据。
  - Skill: none
- [x] `Decision`: P1 裁决——3 个 FK 目标仅有 Integer 行号/版本号，裁决如下：
  - `contractLineId`（InvoicePlan/ConsumptionLine/VolumeDiscount）→ `ErpCtContractLine.getLineNo()`（Integer→String）作 `contractLineName` 显示
  - `contractVersionId`（SignatureRequest）→ `ErpCtContractVersion.getVersionNo()`（Integer→String）作 `contractVersionName` 显示
  - `drpLineId`（InvDrpCrossDock）→ `ErpDrpLine.getLineNo()`（Integer→String）作 `drpLineName` 显示
  - 理由：`lineNo`/`versionNo` 比 raw database ID 对用户更有意义（"行 1"比"42"直观），且前序批次 `ErpHrTimesheet.getCode()` 先例已建立非 `.name()` getter 先例。这些 FK 有 to-one relation，`batchLoadProps` 可用。
  - Skill: none
- [x] `Decision`: P2 裁决——Phase 3 Contract 域表 ErpCtSignatureRequest 行增注释："`providerRequestId` 为 VARCHAR(200) 自引用 to-one，按 VARCHAR 非数值 FK 规则排除（与 `ownerId`/`approvedById` userId 列一致）"。
  - Skill: none
- [x] `Edit`: P3 修订——Goals（行 151）按实际 ORM 列名批量订正。在 Goals 顶部加注：**"以下显示 getter 以实现时 ORM 实际列为准；维度型主数据（Organization/Partner/Material 等）读 `.getName()`，交易型/父单型实体读 `.getCode()`，行号型读 `.getLineNo()`/`.getVersionNo()`（Integer→String）。`configId@ErpB2bMftLog` 目标无显示列保留原始 ID。"** 随后逐条订正 ~15 个 `.name` → 实际 getter。
  - Skill: none
- [x] `Edit`: MINOR-2 修订——在 Execution Plan Phase 1 首项后增实施指导注释：**"全域 BizLoader 统一加 `row.orm_attached() && row.getRelation() != null ? … : null` 短路守卫（防御返回内存聚合实体的 BizModel 方法，先例 CRM/CS 1518-1）；有复杂 mutation 的实体加 `safeBatchLoad` try-catch 降级（防御会话关闭，先例 manufacturing 1043-2）。"**
  - Skill: none
- [x] `Edit`: Draft Review Record 补记——iteration 3 记录末尾增：**"补注（2026-07-14 多维+开放式审计）：iteration 1-3 FK **列名**（leftProp / 第一跳）已逐一核实正确；**目标实体显示列存在性**（第二跳）本次审计补核，发现 1 P0 + 3 P1 已由修订计划 `2026-07-14-0930-1` 裁决修正。"**
  - Skill: none
- [x] `Edit`: 实体计数同步——Current Baseline 覆盖现状段更新：经实时 ORM 复核，机制 D 实体数**维持 66**（ErpB2bMftLog 因 orgId 仍计入，仅其 configId 一列转 Deferred）；Phase 4 Exit Criteria 维持"13 b2b 实体"（ErpB2bMftLog 仍为机制 D 实体，orgId 解析 + configId 保留原始 ID）；Closure Gates `xmllint` 计数维持 66。全域算术 66 机制 D + 5 无 FK + 1 ext:relation 缺口 = 72 不变。
  - **执行订正**：原指令"66→65 / 13→12"为误算（与原指令本条括注"但仍计入机制 D 实体数因 orgId 需解析"自相矛盾），已按 ORM 实际（orgId to-one 存在）订正为维持 66/13。
  - Skill: none

Exit Criteria:

- [x] 1518-3 计划文档全部审计发现已修订（P0/P1/P2/P3/MINOR-2 六项）
- [x] 实体计数算术内部一致（**66 机制 D + 5 无 FK + 1 ext:relation 缺口 = 72**；configId@ErpB2bMftLog 为 66 机制 D 实体内的延后 FK 列，与 8 个 APS ext:relation 缺口 FK 同类，非独立计数项。注：原 Exit Criteria 写"65 机制 D + ... + 1 configId Deferred = 72"为误算，经实时 ORM 复核 ErpB2bMftLog.orgId to-one 存在，机制 D 实体数维持 66）
- [x] Draft Review Record 含第二跳补核记录

## Draft Review Record

- Independent draft review: 本计划为审计驱动的直接修订（无范围结构变更），修订内容均为审计发现的逐条修正。草案审查由审计本身的独立子代理执行（多维审计 `passes` + 开放式审计 `needs revision` → 本计划为 `needs revision` 的响应）。

## Closure Gates

> 本计划仅修订文档，无代码变更。验证 = 人工核对 1518-3 计划文档全部修订项已落地 + 实体计数算术一致。

- [x] 范围内行为完成（6 项审计发现全部修订落地）
- [x] 相关文档对齐（1518-3 计划文档内部一致）
- [x] 无范围内项目降级
- [x] 独立草案审查已完成并记录（审计本身即为独立审查）
- [x] 文本一致性已验证
- [x] 结束审计由独立子代理执行
- [x] 结束证据存在于文件中

## Deferred But Adjudicated

无新增（本计划的 Deferred 项裁决在 Phase 1 Decision 中记录并同步到 1518-3 的 Deferred 段）。

## Closure

Status Note: closed

Closure Audit Evidence:

- Auditor / Agent: 独立子代理（新会话 `ses_0a1d5f125ffebn68tmtSfqWarx`，未参与实现），2026-07-14 针对实时仓库只读核实。
- Verdict: **PASS**。20 项核实全部确认：(1) 6 项审计发现（P0/P1/P2/P3/MINOR-2/Draft Review）在 1518-3 计划文档全部落地；(2) 关键 ORM 事实经实时仓库逐一确认——ErpB2bMftConfig 无 name/code、ErpB2bMftLog 有 orgId to-one、ErpCtContractLine/ErpCtContractVersion/ErpDrpLine 仅 lineNo/versionNo、providerRequestId VARCHAR 自引用、P3 各 getter（getCarrierName/getCode/getCertName/getOperationName 等）正确；(3) 实体计数算术内部一致 66 机制 D + 5 无 FK + 1 ext:relation 缺口 = 72。
- **执行订正确认**：审计独立确认"维持 66/13"为正确裁决（ErpB2bMftLog.orgId to-one 存在→仍属机制 D 实体，仅 configId 一列转 Deferred，与 7 个 APS 弱指针 FK 同类）。原计划"66→65/13→12"为自相矛盾误算，已订正。
- Minor（非阻塞、预先存在、非本次修订引入）：1518-3 Phase 5 行"APS 8 个无 ext:relation 的 FK"括注明细实为 7 APS（+1 MD = 8 总），与权威 ext:relation 缺口表/Deferred 段一致，不影响核心算术，超出本修订范围。

Follow-up:

- 1518-3 实施时遵循本计划裁决（configId 保留 ID / lineNo fallback / orm_attached 守卫）
