# 2026-08-23-2133-1-b1-c01-c02-purchase-p2p-return 批次 B1 用例实施：C01 P2P 采购到付款黄金路径 + C02 采购退货与退款闭环

> Plan Status: completed
> Mission: integration-test
> Work Item: B1（C01, C02；主导域 purchase）
> Last Reviewed: 2026-08-23
> Source: `docs/backlog/integration-test-roadmap.md`（B1 行，M0.3 追加）
> Related: `2026-08-23-1835-1-m01-integration-test-case-design`（M0.1，done）、`2026-08-23-1835-2-m02-infra-pilot-mechanism-adjudication`（M0.2，done）、`2026-08-23-2133-2-b2-c03-c04-sales-o2c-return`、`2026-08-23-2133-3-b3-c05-c06-inventory-cost`
> Audit: required

## Current Baseline

（全部事实 2026-08-23 实仓核验）

- **前置依赖**：M0.1 设计文档（`docs/design/integration-testing.md` §6 C01/C02 用例规格 + §3.4 机制实证结论 + §4 粒度约定 + §5 xwf 规避清单）+ M0.2 基建（均 done）。
- **基建就绪**（M0.2 产物）：基类 `ErpIntegrationTestCase`（`app-erp-all/src/test/java/io/nop/app/all/it/ErpIntegrationTestCase.java`，机制 (c) 文件 H2 双模 + 10 个共享 step helper：`rpcMutation`/`idOf`/`submitForApproval`/`approve`/`reload`/`findBillLink`/`requireVoucherBalanced`/`findApItem`/`findStockMove`/`eqFilterQuery`）；试点 `TestErpP2pPilot` 已覆盖 C01 前三段（PO→Receive→Invoice，12 动作，RECORDING→CHECKING 往返绿）作为本批实现范本；`_cases` 目录约定 `app-erp-all/_cases/io/nop/app/all/it/<类>/<方法>/input|output`（request 文件静态载荷 + `addVar`/`@var:` 动态 id 传递，ERP 主键 `seq-default` 不自动注册 @var）；surefire 模块级串行化已落地（forkCount=1/parallel=none）。
- **C01 付款审批轴未经 app-erp-all 装配复核**：M0.2 试点未触及 Payment 审批（设计文档 §5 要求 setUserId("0") 经 M0.2 复核后方可启用——复核未发生，须本计划 Phase 1 补齐）。既有先例：`TestErpPurPaymentWorkflowApproval`（pur-service）+ `TestErpPurReturnRefundEndToEnd:109-113`（`@BeforeEach` `ContextProvider.setUserId("0")` + `setUserName("SYS")` 后 Payment submit/approve 全链可达）。
- **seed 现状**（94 CSV + 1 SQL）：SUP-001(id=3)/MAT-001(id=1)/CUST-001(id=1)/org2/WH-RAW(id=2)/CNY(id=1)/2026-07 OPEN 期间（试点已实证引用）；`erp_fin_ar_ap_item` 1-4 行（AP_INVOICE/PAYMENT/AR_INVOICE/RECEIPT）全部 **SETTLED**，OPEN 项仅 HR（EMPLOYEE_ADVANCE/EXPENSE_CLAIM）→ **C02 前置「已过账采购链 + OPEN AP 项」在部署 seed 中不存在**，须用例内自包含建数（先建 Receive→Invoice posted 链得 OPEN AP 项，再退货），对齐「seed 引用 + 自包含 GraphQL 建数」纪律（设计文档 §3.4 风险②结论）。
- **无 `erp_ct_contract` seed**（94 CSV 实仓核验无 contract 文件）→ C02 步骤 1「合同退货条款 get」的数据来源须决策：默认自包含创建（`ErpCtContract__save` 经 GraphQL），或触发 seed 修正授权流程（roadmap 横切关注点 3）。
- **运行互斥**：集成测试与 E2E live server 共用 `db/erp.mv.db`——执行前 `lsof -i :8011`（8080 同查）确认无 live server（e2e-runbook「集成测试」节纪律）。
- **基线计数**：known-good-baselines 2026-08-23 权威计数源 **3809 tests / 0 failures / 0 errors / 1 skipped**（含试点 +1）；app-erp-all 29/0/0/1（13 测试类单 fork 顺序执行）。
- **GL 断言口径（实仓核实）**：`erp_fin_gl_balance` **不由过账引擎维护**（`ProfitLossClosingService.java:43`「ErpFinGlBalance 在当前阶段未由过账引擎维护，故以 VoucherLine 为权威本期发生额来源」；BudgetVoucherGenerator/AnnualCloseService 同口径；seed `erp_fin_gl_balance.csv` 为静态 5 行）→ C01「GL 应付余额联动断言」的数据源 = **应付账款科目 VoucherLine 聚合**（对齐 ProfitLossClosingService 权威口径），`gl_balance` 表不作断言源（Phase 1 Decision 项落地此裁决）。
- **付款核销机制（实仓核实）**：核销由**独立 `ErpPurPayment__settle` 动作**驱动（`PaymentSettler.java:29`「核销在付款审核后由独立 settle 动作触发（MVP 解耦审核与核销）」；`ErpPurPaymentSettleProcessor` + `TestErpPurPaymentSettlement:160-167` 先例：settle(paymentId, invoiceId, amount)）→ C01「核销 AP openAmount→0」挂靠 `ErpPurPayment__settle` 步骤，非审批步骤本身。

## Goals

- **C01** = 测试类 `TestErpC01P2pGoldenPath`（1 类 1 方法）：PO（自包含 2 行物料）→ submit/approve → Receive → submit/approve（入库移动 + 暂估凭证）→ Invoice → submit/approve（AP_INVOICE 凭证 + AP 项 OPEN）→ **Payment 全链**（save → 审批推进 → `ErpPurPayment__settle` 独立核销 → AP `openAmount→0`）→ GL 应付余额联动断言（**VoucherLine 聚合口径**，见 Baseline）；三层全比对（response 快照 + output/tables 变更行 + JUnit 关键断言）。
- **C02** = 测试类 `TestErpC02PurReturnRefund`（1 类 1 方法）：自包含已过账采购链 → 退货单 save → submit/approve（反向出库 + PURCHASE_RETURN 红字凭证 + 负 AP 辅助账）→ 退款核销与 open items 反查；三层全比对。
- 两用例 **RECORDING→CHECKING 往返全绿**（按 M0.2 试点往返范式）。
- **Decision**：C01 付款审批路径裁决落地（`setUserId("0")` 后端驱动 vs seed 已过账付款前置），选择/备选/残留风险记录入计划。
- roadmap B1 状态 `todo` → `done`（经独立 closure audit）+ 日志 + 基线差量登记。

## Non-Goals

- B2-B10 用例（各归后续分批计划，本计划不实现）。
- 生产代码/ORM/契约/API 变更——用例实施中发现的产品缺陷按 roadmap 横切关注点 4 走保护区域门禁（plan-first + owner doc / dual-agent-approval），**不得在本计划内直接改生产代码**。
- 默认修改 seed CSV——确需修正走 roadmap 横切关注点 3「seed 修正授权」流程（登记理由 + 同步重录受影响快照 + 评估既有 E2E 数值断言影响）；默认以自包含建数满足前置。
- 不接 CI（roadmap 已裁决出 scope）。
- 不做浏览器层 E2E 页面验证（分工边界，roadmap 已知）。

## Task Route

- Type: `implementation-only change`（集成测试代码 + 快照 + 文档，零生产代码/契约/模型变更）
- Owner Docs: `docs/design/integration-testing.md`（§6 C01/C02 规格 + §3.4 编写纪律 + §4 粒度 + §5 xwf 清单）、`docs/backlog/integration-test-roadmap.md`（B1 行 + 横切关注点）、`docs/testing/e2e-runbook.md`（「集成测试」节）、`docs/testing/known-good-baselines.md`
- Skill Selection Basis: `nop-testing` 匹配测试基类选择/@NopTestConfig/快照录制回放（RECORDING→CHECKING）/三层验证模型——必读 `02-core-guides/testing.md` + `03-runbooks/write-integration-test-with-noptestconfig.md` + `05-examples/test-examples.java`；M0.2 试点类 `TestErpP2pPilot` + 既有 `TestErpPurReturnRefundEndToEnd`/`TestErpPurPaymentWorkflowApproval` 为同模式范本。

## Infrastructure And Config Prereqs

- 文件型 H2 `db/erp.mv.db` fresh 机制已就绪（基类每类 1 次清理）；**与 E2E live server 互斥**——执行前 `lsof -i :8011`（8080 同查）确认无 live server。
- 无端口/密钥/外部服务/环境变量新增；surefire 串行化已在 app-erp-all pom 落地（forkCount=1/parallel=none）。
- 若 xwf setUserId("0") 复核触发 Payment 审批：测试类内 `@BeforeEach` 设 `ContextProvider.getOrCreateContext().setUserId("0")` + `setUserName("SYS")`（既有先例路径），无额外 infra。

## Execution Plan

### Phase 1 — C01 P2P 付款审批轴复核 + 用例实施（混合类型：2/4 Proof + 1/4 Add + 1/4 Decision，逐项标注）

Status: completed（2026-08-23 执行，见下方 Decision 记录与执行证据）
Targets: `app-erp-all/src/test/java/io/nop/app/all/it/TestErpC01P2pGoldenPath.java`、`app-erp-all/_cases/io/nop/app/all/it/TestErpC01P2pGoldenPath/`
Skill: nop-testing

- Item Types: `Proof | Add | Decision`
- Prereqs: M0.1 + M0.2 done（设计文档 + 基建就绪）

- [x] Proof：**xwf setUserId("0") × 机制 (c) 共存复核**（app-erp-all 装配）——**以 C01 类内先导方法形式**（不新增独立复核测试类，保证 Phase 3 测试数归因口径 = +2 稳定）`@BeforeEach` `setUserId("0")`+`setUserName("SYS")`，走 Payment `save` → `submitForApproval` → `approve` 全链。通过判据：approve 后 Payment `approveStatus=APPROVED`（对齐 `TestErpPurPaymentWorkflowApproval`/`TestErpPurReturnRefundEndToEnd` 行为）。结论成立 → C01 采用后端驱动审批轴；证伪 → 触发设计文档 §5 降级路径（Payment 审批轴从用例移除，seed 直置 posted 态 + 设计文档 §6 C01 修订登记）。
  - Skill: nop-testing
  - **执行证据（结论 = 成立，无降级）**：Payment save → submitForApproval → approve 在 app-erp-all 装配（机制 (c) 文件 H2 + 全量 94 seed 前置态）下全链 status=0；`approve` 后 `approveStatus=APPROVED` + `posted=true`（JUnit 断言 + 录制快照 `18_payment_approve_response.json5`：approveStatus=APPROVED / posted=true / nopFlowId 非空——wf 实例经 submit 启动，approve 为 DIRECT 推进直置终态，与 `TestErpPurReturnRefundEndToEnd` 同行为）。
- [x] Add：**C01 测试类** `TestErpC01P2pGoldenPath`（1 类 1 方法）——按设计文档 §6 C01 规格 + 试点前三段范本：PO 保存/行/提交/审批 → Receive 保存/行/提交/审批（入库移动 DONE + 暂估凭证 + AP/暂估断言）→ Invoice 保存/行/提交/审批（AP_INVOICE 凭证借贷平衡 + AP 项 OPEN openAmount 断言）→ **Payment 保存/提交/审批（按下方裁决路径）→ `ErpPurPayment__settle` 核销（AP `openAmount→0`）**；全部静态载荷走 `input/N_step.json5` + `addVar`/`@var:` 动态 id（禁止内联动态 id 字面量）。
  - Skill: nop-testing
  - **执行证据**：类 + 19 个 input 文件 + 20 个 response 快照（19 个 GraphQL 动作 + reconciliation create/post 2 步经 Biz 接口，post 结果输出 `20_reconciliation_post_response.json5`）+ 26 表 output/tables 变更行（376K 用例目录）；PO 2 行物料（10×5 + 4×5 = 70，含税 79.1）；RECORDING→CHECKING 往返全绿（见 Proof 项）。**实施期发现修正**：PURCHASE_INPUT 暂估凭证的业财回链 billCode = 库存移动单 code（`InvPostingDispatcher` 口径），非收货单 code——断言以 `findStockMove` 反查移动单后以其 code 反链（先例 `TestErpPurReturnRefundEndToEnd:171` 同型反查范式）。
- [x] Decision：**付款审批路径 + GL 断言口径双裁决**——(1) 付款审批路径：记录选择（默认 `setUserId("0")` 后端驱动，若复核成立）、备选（seed 已过账付款前置：`posted=true` + voucher 回链，roadmap 横切关注点 3 授权路径）与残留风险（xwf 链对 SYS caller 的依赖）；(2) GL 应付余额断言数据源：**VoucherLine 聚合**（应付账款科目，对齐 ProfitLossClosingService 权威口径），`gl_balance` 表为 seed 静态不作断言源——理由写入本段决策记录或引用的文档。
  - Skill: nop-testing
  - **决策记录（双裁决落地）**：
    - **(1) 付款审批路径 = `setUserId("0")` 后端驱动（选择，复核成立）**：证据见 Proof 项；备选 = seed 已过账付款前置（posted=true + voucher 回链，roadmap 横切关注点 3 授权路径）**未触发**；残留风险 = xwf 链对 SYS caller 的依赖（与既有 `TestErpPurReturnRefundEndToEnd`/`TestErpPurPaymentWorkflowApproval` 同型，wf 实例激活态不影响 DIRECT approve 推进）。
    - **(2) GL 应付余额断言数据源 = 应付账款科目（2202）VoucherLine 聚合**（对齐 `ProfitLossClosingService` 以 VoucherLine 为权威发生额的口径；`erp_fin_gl_balance` 为 seed 静态 5 行，不由过账引擎维护，**不作断言源**）。精确口径：businessType ∈ {AP_INVOICE, PAYMENT} 的 credit−debit 净额——PURCHASE_INPUT 暂估（同科目 2202）不生成 ar_ap_item，不计入 AP 联动。GL↔AP 一致性断言落在两处：发票审核后（GL=79.1 = AP 总额 79.1）与核销完成后（GL=0 = AP 总额 0）；付款审核后 GL 归零单点断言（PAYMENT 过账即借减 2202，AP 项核销前保持 OPEN——实现口径）。
- [x] Proof：**C01 RECORDING→CHECKING 往返**——首次 RECORDING 录制（output/response + output/tables 变更行）→ 切回默认 CHECKING 复跑全绿；三层断言逐项核验（PO/Receive/Invoice/Payment `approveStatus` 翻转、`posted=true`、AP_INVOICE 凭证借贷平衡（总借=总贷）、`ErpPurPayment__settle` 后 AP 辅助账 `openAmount=0`、GL 应付余额 = 应付账款科目 VoucherLine 聚合（与 AP 总额一致））。
  - Skill: nop-testing
  - **执行证据**：RECORDING 录制 1 次完成（`nop.err.autotest.snapshot-finished` 预期异常）→ 注解还原（grep 零 RECORDING 残留）→ CHECKING 复跑 **1/0/0 绿**（suite 6.09s）。三层断言逐项核验通过：PO/Receive/Invoice/Payment `approveStatus=APPROVED`（Invoice/Payment `posted=true`）；三张凭证借贷平衡（PURCHASE_INPUT 70/70、AP_INVOICE 79.1/79.1、PAYMENT 79.1/79.1，快照 `erp_fin_voucher.csv` 实证）；AP 辅助账核销后 `openAmount=0` + SETTLED（`erp_fin_ar_ap_item.csv` 实证：AP_INVOICE/PAYMENT 两行 settledAmount=79.1 / openAmount=0）；GL 应付余额 = 应付账款科目 VoucherLine 聚合（与 AP 总额一致，发票审核后 79.1=79.1、核销后 0=0）。
  - **实施期发现补记（settle 机制口径，本段即修正登记）**：实仓复核 `PaymentSettler` 确认 `ErpPurPayment__settle` 仅回写发票 `paidAmount`/`paidStatus` 与付款 `writtenOffStatus`（PaymentSettler.java:203-236 recompute 两处）；**ar_ap_item 的 SETTLED/openAmount→0 由财务正式核销单 `ErpFinReconciliation`（create+post）驱动**（先例 `TestErpPurProcureToPayEnd:264-273`，其 `testProcureToPayPartialSettlement` 快照实证 settle 后 AP 项仍 OPEN）。C01 按「以当前实现为准」执行 **settle（域级核销）→ reconciliation（辅助账核销）两步链**，"AP 辅助账 openAmount=0" 断言落于链末完成态（设计文档 §6 C01「AP 辅助账核销后 openAmount=0」语义一致；计划原述「settle 后 openAmount=0」按链末口径执行）。

Exit Criteria:

- [x] xwf 复核结论落盘（成立证据或降级登记 + 设计文档修订痕迹）——**成立**，证据落盘本 Phase（无设计文档修订需要，§5 降级路径未触发）
- [x] C01 类存在且 RECORDING→CHECKING 往返绿（三层断言全过；失败模式 = 任一断言/快照 diff 不通过）——**1/0/0 绿**
- [x] 双裁决记录（付款审批路径 + GL 断言口径，含备选与理由）——**落盘本 Phase Decision 项**

### Phase 2 — C02 采购退货与退款闭环（混合类型：1/3 Add + 1/3 Decision + 1/3 Proof，逐项标注）

Status: completed（2026-08-23 执行，见下方 Decision 记录与执行证据）
Targets: `app-erp-all/src/test/java/io/nop/app/all/it/TestErpC02PurReturnRefund.java`、`app-erp-all/_cases/io/nop/app/all/it/TestErpC02PurReturnRefund/`
Skill: nop-testing

- Item Types: `Add | Decision | Proof`
- Prereqs: Phase 1（C01 往返绿——同基类同装配，先例链打通后 C02 无新机制风险）

- [x] Add：**C02 测试类** `TestErpC02PurReturnRefund`（1 类 1 方法）——按设计文档 §6 C02 规格：自包含建已过账采购链（Receive→Invoice posted，得 OPEN AP 项）；合同数据来源按下方 Decision 落地（默认自包含 `ErpCtContract__save` 创建退货条款合同）；退货单 save → submit → approve（反向出库移动 + PURCHASE_RETURN 红字凭证 + 负 AP 辅助账）；退款核销/open items 反查（`findApItem` 负 openAmount 断言，对齐 `TestErpPurReturnRefundEndToEnd` 红字/负项断言范式）；静态载荷走 request 文件 + @var 动态 id。
  - Skill: nop-testing
  - **执行证据**：类 + 18 个 input 文件 + 18 个 response 快照 + 19 表 output/tables 变更行；链 = 合同 save→get（退货条款前置核对）→ PO（1 行 10×5）→ Receive（入库 + 暂估）→ Invoice（AP_INVOICE + OPEN AP 项 56.5）→ Return save→submit→approve（反向 OUTGOING 出库移动 ORIGIN_MOVE_ID 回链入库移动 + PURCHASE_RETURN 红字凭证 2202 借 20 / 1401 贷 20 + PUR_RETURN 负项 −20）。**实施期发现修正**：反向出库移动 `posted=false`（退货过账由红字凭证承载，移动单不置 posted——以当前实现为准，断言按设计文档「反向出库移动存在（relatedBill 反查）」口径，移动单 `docStatus=DONE` + `MOVE_TYPE=OUTGOING` + 相关单据反查）。
- [x] Decision：**合同数据来源裁决**——选择 = 自包含 `ErpCtContract__save` 经 GraphQL 创建（无 erp_ct_contract seed，实仓核验）；备选 = 触发 seed 修正授权流程（roadmap 横切关注点 3：改 seed CSV + 登记理由 + 同步重录受影响快照 + 评估既有 E2E 数值断言影响）；残留风险 = 退货条款字段须经 `ErpCtContract__get` 前置核对验证（设计文档 §6 C02 步骤 1 语义）。
  - Skill: nop-testing
  - **决策记录**：**选择 = 自包含 `ErpCtContract__save`**（经 GraphQL，`{data:{data:{...}}}` 形态，contractType=PURCHASE / contractDirection=INBOUND / partnerId=3 / status=DRAFT + description 承载退货条款文本——ORM 模型无独立退货条款列，实仓核验 `app-erp-contract.orm.xml` 仅 invoiceTerm 条款列，退货条款经 description 承载，设计文档 §6 C02 步骤 1「读取合同退货条款」按 description 核对落地）；**备选 = seed 修正授权流程（roadmap 横切关注点 3）未触发**（94 CSV 实仓核验无 erp_ct_contract seed，自包含建数满足前置，无需改部署 seed）；**残留风险 = 退货条款字段经 `ErpCtContract__get` 前置核对**——已实现（`2_contract_get_response.json5` 快照 + JUnit 断言 description 含「退货条款」）。
- [x] Proof：**C02 往返与三层断言**——RECORDING 录制 → CHECKING 复跑全绿；层 1 锚点：Return `approveStatus=APPROVED` + `posted=true`、反向出库移动存在（`findStockMove` relatedBill 反查）、PURCHASE_RETURN 凭证借贷平衡且红字方向、AP 项负向登记（openAmount = −total）、余额回减断言。
  - Skill: nop-testing
  - **执行证据**：RECORDING 录制 1 次完成（snapshot-finished 预期异常）→ 注解还原（grep 零 RECORDING/SnapshotTest 注解残留，仅 javadoc 提及）→ CHECKING 复跑 **1/0/0 绿**（suite 6.07s）。层 1 逐项核验：Return `approveStatus=APPROVED` + `posted=true`（JUnit 断言 + `18_return_approve_response.json5` 快照）；反向出库移动存在（`findStockMove(ERP_PUR_RETURN, returnCode)`，`MOVE_TYPE=OUTGOING` + `docStatus=DONE` + `ORIGIN_MOVE_ID` 回链入库移动，`erp_inv_stock_move.csv` 实证）；PURCHASE_RETURN 凭证借贷平衡（总借=总贷=20，`erp_fin_voucher.csv` 实证）+ 红字方向（2202 暂估应付 DEBIT 20 / 1401 库存商品 CREDIT 20 = 反向 PURCHASE_INPUT，`erp_fin_voucher_line.csv` 实证）；AP 项负向登记（`findApItem(PUR_RETURN, returnCode)` openAmount = −20 + OPEN，`erp_fin_ar_ap_item.csv` 实证）；余额回减（供应商 3 sumOpen = 56.5 − 20 = 36.5）+ 退款核销 open items 反查（`IErpFinArApItemBiz.findOpenItemsByPartner` 含退货负项 + 合计 36.5）。

Exit Criteria:

- [x] C02 类存在且 RECORDING→CHECKING 往返绿（失败模式 = 任一断言/快照 diff 不通过）——**1/0/0 绿**
- [x] 合同数据来源决策记录（含备选/残留风险；若触发 seed 修正授权则登记理由 + 快照重录 + E2E 影响评估）——**自包含建数，未触发 seed 修正授权**，决策记录落盘本 Phase Decision 项

### Phase 3 — 收尾与回归（Proof-heavy）

Status: completed（2026-08-23 执行，见下方执行证据）
Targets: `docs/backlog/integration-test-roadmap.md`（B1 → done + 头「最后更新」注记）、`docs/logs/2026/08-23.md`、`docs/testing/known-good-baselines.md`（差量登记）
Skill: none

- Item Types: `Proof | Fix`
- Prereqs: Phase 2

- [x] Proof：**局部回归** `mvn test -pl app-erp-all` 全绿（含 2 个新用例类，串行 fork 单 JVM 顺序执行）+ **全量回归** `mvn test`（全 reactor）零新增失败 + `mvn clean install -DskipTests` BUILD SUCCESS——对照 known-good-baselines 3809/0/0/1 口径，+2 tests 全额归因 B1（新用例按 1 类 1 方法计 2 测试），零新增失败。
  - Skill: none
  - **执行证据（全绿）**：`mvn test -pl app-erp-all` = **31/0/0/1**（29 基线 + 2 新用例类，单 fork 顺序执行，C01/C02/Pilot 各 ~2s 用例体）；全量 `mvn test`（13:32）BUILD SUCCESS，surefire XML 权威计数 **3811 tests / 0 failures / 0 errors / 1 skipped / 620 报告文件**（对照 3809/0/0/1/618 差量 +2 tests / +2 报告文件全额归因 `TestErpC01P2pGoldenPath` + `TestErpC02PurReturnRefund`，零新增失败，唯一 skipped = `ErpAllWebPagesCollectTest` @Disabled 预存维持）；`mvn clean install -DskipTests` **156 模块 BUILD SUCCESS**。
- [x] Fix：roadmap B1 `todo` → `done` + 头「最后更新」注记 + `docs/logs/2026/08-23.md` 日志条目（按日志书写指南）+ 如适用 known-good-baselines 差量登记。
  - Skill: none
  - **执行证据**：roadmap B1 行 `todo` → `done`（证据摘要：两用例往返绿 + 双裁决 + 全量回归差量）+ 头部「最后更新」注记（B1 done 摘要 + 下一步 B2）；`docs/logs/2026/08-23.md` 顶部新增 B1 执行条目（按日志书写指南：产出/执行期发现/验证状态/下一步）；`docs/testing/known-good-baselines.md` 新增 2026-08-23 integration-test B1 基线条目（commands passed 三命令 + 差量归因 + 执行期发现登记）。

Exit Criteria:

- [x] 局部 + 全量回归零新增失败（与已知基线口径一致）——**31/0/0/1 + 3811/0/0/1 + BUILD SUCCESS**
- [x] roadmap B1 = done + 日志条目存在 + 基线差量登记——**三处落盘**

## Draft Review Record

- Independent draft review iteration 1: needs revision (ses_fd1280d49ffeqlFoiIQ2R49071，独立 general 子代理新会话) — 0 BLOCKER / 1 MAJOR / 3 MINOR。基线事实 19 项独立核验全过（基类 10 helper / 试点 12 动作 / _cases 约定 / 94 CSV+1 SQL / 无 erp_ct_contract seed / AP 项 1-4 SETTLED / PAY-2026-001 posted / setUserId("0") @BeforeEach / 3809+29 基线 / e2e-runbook 互斥纪律）。MAJOR-1 C01「GL 应付余额联动断言」与当前实现矛盾（`gl_balance` 不由过账引擎维护，`ProfitLossClosingService.java:43` 以 VoucherLine 为权威）→ 双裁决 Decision 项（付款审批路径 + GL 断言口径 = VoucherLine 聚合）。MINOR-1 Phase 2 合同数据来源决策未显式化 → 独立 Decision 项；MINOR-2 Proof item 1 双形态致 Phase 3 归因歧义 → 钉死「C01 类内先导方法」；MINOR-3 付款核销独立 `ErpPurPayment__settle` 未点名（`PaymentSettler.java:29` + `TestErpPurPaymentSettlement:160-167`）→ Goals/Add/Proof 全链点名。修订全部落地。
- Independent draft review iteration 2: accept (ses_fd11a9bacffeYb5rGuVY08aH7r，独立 general 子代理新会话) — 0 BLOCKER / 0 MAJOR / 1 MINOR。迭代 1 四项发现全部核验 FIXED（GL 口径四处一致 / Phase 2 Decision 显式化 / 归因口径 = +2 稳定 / settle 点名）；实仓复核通过（ProfitLossClosingService:43 引文逐字 / ErpPurPaymentSettleProcessor + 先例 / 基类 10 helper / roadmap B1 行 / 设计文档 §6 C01/C02）。唯一 MINOR-4：Baseline「gl_balance 静态 2 行」实为 5 行（已修正为 5 行）。**共识达成，计划可执行。**

## Closure Gates

- [x] 范围内行为完成（C01 + C02 用例三层全比对全绿 + 付款审批路径裁决）
- [x] 相关文档对齐（设计文档 §6 ↔ 用例实现 ↔ roadmap B1 ↔ e2e-runbook 无矛盾）
- [x] 已运行验证（`mvn test -pl app-erp-all` 全绿 + 全量 `mvn test` 零新增失败 + `mvn clean install -DskipTests` BUILD SUCCESS）
- [x] 无范围内项目降级为 deferred/follow-up
- [x] 独立草案审查已完成并记录
- [x] 文本一致性已验证：状态、阶段、门控和日志都一致
- [x] 结束审计由独立子代理（新会话）执行；执行者未自我审计且未将此留为 `[ ]` 作为人工门控占位符
- [x] 结束证据存在于文件中

## Deferred But Adjudicated

- **seed 修正授权触发时的重录耗时测量**（M0.2 watch-only residual 的触发条件项）：Classification: `watch-only residual`。Why Not Blocking Closure: 本计划默认自包含建数（无 seed 修改面）；仅当 C02 合同数据来源裁决走 seed 修正授权路径时，按 roadmap 横切关注点 1 同步重录受影响快照并实测登记重录耗时（以试点单用例 252K/类为预算参考）。Successor Required: `yes`（触发条件：B1-Bn 执行期发生 seed 修正授权路径时测量登记）。

## Closure

Status Note: 三 Phase 全部完成并可关闭——C01（P2P 采购到付款黄金路径，含 Payment xwf setUserId("0") 复核成立 + settle/reconciliation 两步核销 + GL 应付余额 VoucherLine 聚合双裁决）与 C02（采购退货与退款闭环，含合同数据来源裁决 = 自包含建数）两用例类 RECORDING→CHECKING 往返全绿；全量回归 3811/0/0/1（+2 全额归因 B1，零新增失败）+ app-erp-all 31/0/0/1 + `mvn clean install -DskipTests` 156 模块 BUILD SUCCESS；roadmap B1 → done + 头部注记 + 日志 + known-good-baselines 差量登记；独立结束审计通过（0 BLOCKER / 0 MAJOR / 2 MINOR，MINOR 已兑现：C01 快照计数 19/20 修正 + B2/B3 兄弟计划文件观察项登记）。范围内零降级。

Closure Audit Evidence:

- Auditor / Agent: 独立 general 子代理（新会话 ses_fd0ec8976ffeJo7O5xyQ3Krt0U，零执行者上下文）
- Evidence: **CLOSURE AUDIT: PASS**——7 项核证全 PASS：① 计划内部一致性（三 Phase completed + 9/9 执行项 [x] + 7/7 Exit Criteria [x]，无「completed 但 [ ] 未勾」）；② 产物真实性（两用例类 1 类 1 方法 + RECORDING 注解零残留 + `_cases` 实仓计数 + 快照数值逐项核验：C01 AP 项 SETTLED openAmount=0/settledAmount=79.1、三凭证 70/70·79.1/79.1·79.1/79.1、C02 PUR_RETURN −20 OPEN + PURCHASE_RETURN 2202借20/1401贷20 + Payment approve 快照 APPROVED/posted）；③ 测试可复跑（审计者独立复跑 CHECKING 2/0/0 绿，suite C01 6.6s/C02 2.4s）；④ 回归口径（baselines 3811/0/0/1/620 + 31/0/0/1 + 156 模块 + 差量归因）；⑤ roadmap B1 done + 头部注记 + 日志条目；⑥ 范围纪律（git status 变更面 = 2 测试类 + 2 _cases + 3 .md + 计划自身，零生产代码/ORM/契约/seed 变更）；⑦ 文本一致性（Draft Review Record 两轮 + Deferred 仅 watch-only residual + Closure 段按先例由执行者凭审计结论回填）。2 MINOR 全部兑现：MINOR-1 = C01 快照计数 21→19 input/20 response 修正（计划/日志/roadmap/baselines 四处同步）；MINOR-2 = B2/B3 未跟踪兄弟计划文件为同批次会话产物，非本计划变更面，登记观察（随批次提交时纳入）。

Follow-up:

- 无（B2/B3 兄弟计划各自独立执行；watch-only residual「seed 修正授权触发时的重录耗时测量」维持登记于 Deferred But Adjudicated，触发条件未发生）