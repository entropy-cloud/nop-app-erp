# 2026-08-24-0749-1-b8-c15-c16-c17-crm-cs-hr 批次 B8 用例实施：C15 CRM 线索转化与销售预测 + C16 CS 工单 SLA 与通知派发 + C17 HR 薪酬发放闭环

> Plan Status: completed
> Mission: integration-test
> Work Item: B8（C15, C16, C17；主导域 crm + cs + hr）
> Last Reviewed: 2026-08-24
> Source: `docs/backlog/integration-test-roadmap.md`（B8 行，M0.3 追加）
> Related: `2026-08-24-0749-2-b9-c18-c19-contract-b2b`（同批兄弟计划，B8 排其前）
> Audit: required

## Current Baseline

（全部事实 2026-08-24 实仓核验）

- **前置依赖**：M0.1 设计文档（`docs/design/integration-testing.md` §6 C15/C16/C17 用例规格 + §3.4 机制实证结论 + §4 粒度约定 + §5 xwf 规避清单）+ M0.2 基建 + B1-B7（roadmap B8 行 Deps = M0.1 + M0.2，批内排序由本批计划序号确定——B8 须在 B7 之后执行）。
- **基建就绪**（M0.2/B1-B7 产物）：基类 `ErpIntegrationTestCase`（机制 (c) 文件 H2 双模 + 共享 step helper：`rpcMutation`/`executeRpc`/`idOf`/`submitForApproval`/`approve`/`reload`/`findBillLink`/`requireVoucherBalanced`/`findApItem`/`findStockMove`）；`_cases` 目录约定；surefire 模块级串行化已落地；已有 **16 个集成用例类**（Pilot + B1-B7 = 1+2+2+2+3+2+2+2，it/ 目录 `extends ErpIntegrationTestCase` 实仓核验）作为范本，**B8 按同范式实现（1 用例 = 1 类 1 方法 + @BeforeAll fresh-DB + RECORDING→CHECKING 往返）**。
- **crm/cs/hr 域既有测试资产（本批业务参照，不重复实现内部细节）**：`TestErpCrmReportRendering`（forecast-accuracy 报表渲染 + commitAmount/lineCount 断言先例）、crm-lead E2E（moveStage 先例）、cs-ticket E2E（六态状态机先例）、cs-canned-response E2E（applyCannedResponse 先例）、`TestErpCsReportRendering`（ticket-sla-csat-summary 渲染先例）、`TestErpHrPayrollEngine`/`TestErpHrSalaryWorkflowApproval`/`TestErpHrSalaryPostingChain`（薪资计算/xwf 三级审批/SALARY_PAYMENT 过账先例）、`HrFrozenClockExtension`（hr-service 测试侧，extends module-common-test `AbstractFrozenClockExtension`）。
- **C15 动作面（实仓核验在位）**：`ErpCrmLeadBizModel` 实仓动作 = `qualify`/`lose`/`cancel`/`moveStage`/`assignLead`/`reassignLead`/`convertToCustomer`/`convertToQuotation`/`convertToOpportunity`（另有 `getCreatedOpportunity` @BizMutation 非本用例路径）；`ErpCrmProductConfiguratorBizModel.generateQuote`（报价单生成，设计文档引 ErpCrmProductConfiguratorGenerateQuoteProcessor 先例）；`ErpCrmReportBizModel.renderHtml(reportName="forecast-accuracy")`——**与设计文档 §6 C15 步骤 1-4 基本一致**；报价→订单转化后销售订单走 `ErpSalOrder__save` + DIRECT 审批（B2 C03 先例链）。**实仓无 ErpCrmOpportunity 独立实体——商机 = ErpCrmLead leadType=OPPORTUNITY（B2 C03 已裁决先例），机会转化锚定 `convertToOpportunity`；设计文档 §6 C15 步骤 2 旧动作名 `ErpCrmOpportunity__save` 为漂移预期，实施期登记勘误**。
- **C15 前置 seed（实仓核验在位）**：`erp_crm_stage.csv`（STAGE-001 验证 30%/STAGE-002 报价 60%）、`erp_crm_lead.csv`（LEAD-2026-001/002 均 QUALIFIED、OPPORTUNITY 类型）、`erp_crm_forecast.csv`（commitAmount 50000 / weightedAmount 45000 / bestCaseAmount 80000 / opportunityCount 2）、`erp_crm_forecast_line.csv`（15000 COMMIT + 48000 BEST_CASE）、`erp_crm_forecast_period.csv`。**设计文档 §6 C15 期望值 50000/45000/80000/63000（1045-2）= seed 派生（63000 疑为 forecast_line 合计数，实施期以实仓报表输出为准）**。
- **C16 动作面（实仓核验在位）**：`ErpCsTicketBizModel` 实仓动作 = `assign`/`start`/`resolve`/`close`/`reopen`/`cancel`/`adoptKnowledge`/`escalateToQuality`/`matchAndAttachSla`——**六态状态机（NEW/ASSIGNED/IN_PROGRESS/RESOLVED/CLOSED/CANCELLED），无 `respond` 动作（B2 C04 已裁决先例「以当前实现为准：无 respond 动作」）——设计文档 §6 C16 步骤 1 提及 `respond` 为动作名漂移预期，实施期以实仓六态为准登记勘误**；`ErpCsCannedResponseBizModel.applyCannedResponse`；`ErpCsSurveyBizModel.createSurvey`/`submitSurvey`；`ErpCsReportBizModel.renderHtml(reportName="ticket-sla-csat-summary")`；通知走 `ErpSysNotification__notify`/`markRead`/`countUnread`（C09 B5 已实证先例：自包含模板 + notify 落库 + markRead + countUnread 归零）。
- **C16 前置 seed（实仓核验在位）**：`erp_cs_ticket_type.csv`（TT-COMPLAINT 投诉 / TT-INQUIRY 咨询）、`erp_cs_ticket.csv`（TKT-2026-001 RESOLVED isSlaCompleted=true、TKT-2026-002 IN_PROGRESS isSlaCompleted=false）、`erp_cs_survey.csv`（CSAT 5/NPS 9 关联 ticket 1）。**设计文档 §6 C16 期望 token 投诉桶/5.00/9.00（1045-2）与 seed 一致**；宏响应表无 seed → 自包含建数。
- **C17 动作面（实仓核验在位）**：`ErpHrSalaryBizModel` 实仓动作 = `calculateSalary`/`runPayroll`/`markPaid`/`voidSalary`/`generateBankFile`——**与设计文档 §6 C17 步骤 1/3 一致（步骤 2 xwf 审批走 submitForApproval + agree×3，TestErpHrSalaryWorkflowApproval 实证：hr-review → finance-review → manager-approval 三级 + cc-hr confirm，xwf 规避依据 setUserId("0") 后端驱动 = B1/B2 在 app-erp-all 装配已复核成立）**；markPaid 后 `ErpHrSalaryMarkPaidProcessor` 生成 SALARY_PAYMENT 凭证（Dr 6601 管理费用-工资 / Cr 2211 应付职工薪酬，`TestErpHrSalaryPostingChain` 先例：270/280/290/300 系列凭证，SALARY_PAYMENT = 280 口径）。
- **C17 前置 seed（实仓核验在位）**：`erp_hr_employee.csv`（HR-EMP-001 赵明 / HR-EMP-002 钱华，ACTIVE FULL_TIME，org 2）、`erp_hr_department.csv`、`erp_hr_salary_simulation*.csv`（薪资模拟 seed）——**设计文档 §6 C17 前置「1234-1 seed 员工」旧式码，实仓新式码 = erp_hr_employee 行（语义前置满足，实施期登记勘误）**；薪资单无 seed → 自包含建数。
- **config（实仓核验）**：`erp-hr.default-payroll-subject-id`（`ErpHrConstants.CONFIG_DEFAULT_PAYROLL_SUBJECT_ID`，payroll 科目）——app-erp-all 装配下缺省大概率不满足 2211 断言 → `@NopTestProperty` 指定（实施期核实键值，B6 C12 先例）；C17 若走 payroll 科目 config 同型处理。
- **运行互斥**：集成测试与 E2E live server 共用 `db/erp.mv.db`——执行前 `lsof -i :8011`（8080 同查）确认无 live server（e2e-runbook「集成测试」节纪律）。
- **基线计数**：known-good-baselines 2026-08-24 权威计数源 **3824 tests / 0 failures / 0 errors / 1 skipped / 633 报告文件**；app-erp-all **44/0/0/1**。B7 已 done，B8 预期差量 **+3 tests / +3 报告文件**（3 用例类各 1 类 1 方法）。
- **流程改进登记（B3 closure MINOR-2 兑现，B4-B7 已执行）**：最终 `clean install` 会清除 surefire XML 计数证据——全量回归后、clean 前先拷贝计数/XML 再登记基线（本计划 Phase 4 执行纪律）。
- **deferred 触发条件（B1/M0.2 watch-only residual）**：若执行期发生 seed 修正授权路径（roadmap 横切关注点 3），须同步测量登记重录耗时（以单用例 252K/类为预算参考）；本批默认自包含建数，预期不触发。
- **冻结时钟纪律**：C07/C11/C12/C13C14FrozenClockExtension 先例（`@RegisterExtension` 静态注册，app-erp-all it/ 内本地扩展）——**C16 SLA 计算/达标判断与 C17 薪资期间均涉日期敏感路径 → 是否引入对应域 frozen clock 由 Phase 1/2/3 Decision 裁决（hr-service 有 `HrFrozenClockExtension` 可参照拷贝为 app-erp-all 本地扩展）**，并登记快照时间戳 `*` 通配处置（B2/B4/B5/B6/B7 先例）。

## Goals

- **C15** = 测试类 `TestErpC15CrmLeadForecast`（1 类 1 方法）：自包含新线索 → `ErpCrmLead__save` → `moveStage`（阶段迁移）→ 机会转化（`convertToOpportunity`，B2 C03 先例——实仓无 ErpCrmOpportunity 独立实体）→ 报价单生成（`ErpCrmProductConfigurator__generateQuote` 或 `convertToQuotation`，以实仓为准）→ `ErpSalOrder__save` → DIRECT 审批（B2 先例链）→ `ErpCrmReport__renderHtml("forecast-accuracy")`（commitAmount/lineCount token 断言 = seed 派生 50000/45000/80000/63000 以实仓输出为准）；三层全比对。
- **C16** = 测试类 `TestErpC16CsSlaNotification`（1 类 1 方法）：自包含工单 → 六态状态机推进（assign/start/resolve/close，以实仓六态为准，无 respond）→ `matchAndAttachSla`（isSlaCompleted 断言）→ `ErpCsCannedResponse__applyCannedResponse`（usageCount 递增）→ 自包含调研 `ErpCsSurvey__createSurvey`/`submitSurvey`（CSAT/NPS）→ `ErpCsReport__renderHtml("ticket-sla-csat-summary")`（token 断言：投诉桶/5.00/9.00 以实仓输出为准）→ 通知派发 `ErpSysNotification__notify` + `markRead` + `countUnread` 归零；三层全比对。
- **C17** = 测试类 `TestErpC17HrSalaryPayment`（1 类 1 方法）：自包含薪资单 → `ErpHrSalary__calculateSalary`/`runPayroll`（金额口径先例 TestErpHrPayrollEngine）→ `submitForApproval` → xwf 三级 `agree`（setUserId("0") 后端驱动，hr-review → finance-review → manager-approval）→ `approve`（APPROVED）→ `markPaid`（SALARY_PAYMENT 凭证 Dr 费用/Cr 2211 借贷平衡 + 金额 = 薪资合计）→ 发放通知（notify 域）；三层全比对。
- 三用例 **RECORDING→CHECKING 往返全绿**。
- roadmap B8 状态 `todo` → `done`（经独立 closure audit）+ 日志 + 基线差量登记 + 设计文档 §6 C15/C16/C17 动作面勘误登记（若发生）。

## Non-Goals

- B9-B10 用例及 V.1/V.2（各归对应计划）。
- 生产代码/ORM/契约/API 变更——用例实施中发现的产品缺陷按 roadmap 横切关注点 4 走保护区域门禁（accounting/finance postings = plan-first + owner doc + tests；其余 = auto + dual-agent-approval），**不得在本计划内直接改生产代码**。
- 默认修改 seed CSV——确需修正走 roadmap 横切关注点 3「seed 修正授权」流程；默认以自包含建数满足前置。
- 不接 CI（roadmap 已裁决出 scope）；不做浏览器层 E2E 页面验证（分工边界）。
- 不实现未覆盖功能（横切关注点 6「以当前实现为准」）：C15/C16/C17 步骤若与实仓行为不符，以实仓行为替代 + 设计文档勘误登记，不强行实现缺失功能（如 C16 respond 动作、C15 报表期望值漂移）。

## Task Route

- Type: `implementation-only change`（集成测试代码 + 快照 + 文档，零生产代码/契约/模型变更）
- Owner Docs: `docs/design/integration-testing.md`（§6 C15/C16/C17 规格 + §3.4 编写纪律 + §4 粒度 + §5 xwf 清单）、`docs/backlog/integration-test-roadmap.md`（B8 行 + 横切关注点）、`docs/testing/e2e-runbook.md`（「集成测试」节）、`docs/testing/known-good-baselines.md`
- Skill Selection Basis: `nop-testing` 匹配测试基类选择/@NopTestConfig/@NopTestProperty（C17 payroll 科目 config = B6 C12 同型）/快照录制回放（RECORDING→CHECKING）/三层验证模型；M0.2 试点 + B1-B7 集成用例类为同模式范本，crm/cs/hr 既有测试（ReportRendering/六态状态机/xwf 审批/posting chain）为业务参照。

## Infrastructure And Config Prereqs

- 文件型 H2 `db/erp.mv.db` fresh 机制已就绪（基类每类 1 次清理）；**与 E2E live server 互斥**——执行前 `lsof -i :8011`（8080 同查）确认无 live server。
- **C17 config（待实施期核实）**：`erp-hr.default-payroll-subject-id` 缺省是否满足 2211 断言；若不满足 `@NopTestProperty` 指定（B6 C12 先例）。
- C16 通知走 notify 域既有机制（C09 B5 已实证），无新增 config。
- 无端口/密钥/外部服务/环境变量新增；surefire 串行化已在 app-erp-all pom 落地。

## Execution Plan

### Phase 1 — C15 CRM 线索转化与销售预测（混合类型：1/3 Add + 1/3 Proof + 1/3 Decision，逐项标注）

Status: completed
Targets: `app-erp-all/src/test/java/io/nop/app/all/it/TestErpC15CrmLeadForecast.java`、`app-erp-all/_cases/io/nop/app/all/it/TestErpC15CrmLeadForecast/`
Skill: nop-testing

- Item Types: `Add | Proof | Decision`
- Prereqs: B7 done（批内排序）+ M0.1 + M0.2

- [x] Add：**C15 测试类** `TestErpC15CrmLeadForecast`（1 类 1 方法）——按设计文档 §6 C15 规格 + `TestErpCrmReportRendering`/crm-lead E2E 业务参照：自包含线索 `ErpCrmLead__save` → `moveStage`（stageId 翻转断言）→ 机会转化（`convertToOpportunity`，实仓无 ErpCrmOpportunity 独立实体）→ 报价单生成（`ErpCrmProductConfigurator__generateQuote` 或 `ErpCrmLead__convertToQuotation`，以实仓为准）→ `ErpSalOrder__save` → `submitForApproval` → `approve` [DIRECT]（B2 C03 先例）→ `ErpCrmReport__renderHtml(reportName="forecast-accuracy")`（commitAmount/lineCount token 断言 = seed 派生 50000/45000/80000/63000 以实仓输出为准）；静态载荷走 `input/N_step.json5` + `addVar`/`@var:` 动态 id。
  - Skill: nop-testing
- [x] Decision：**C15 转化动作面与报表口径裁决**——① 机会转化动作（锚定 `convertToOpportunity`——实仓无 ErpCrmOpportunity 独立实体，设计文档旧动作名 `ErpCrmOpportunity__save` 为漂移登记勘误）；② 报价单生成动作（ErpCrmProductConfigurator__generateQuote vs convertToQuotation，以实仓为准）；③ forecast-accuracy 报表期望值（seed 派生 50000/45000/80000/63000 的实仓输出口径——report 数据集 buildForecastAccuracyDataset 聚合口径，`TestErpCrmReportRendering` 先例断言 commitAmount/lineCount）；④ 是否需 frozen clock（报表聚合含日期路径时）。与设计文档不符处 Phase 4 登记 §6 C15 勘误。
  - Skill: nop-testing
- [x] Proof：**C15 RECORDING→CHECKING 往返**——RECORDING 录制 → CHECKING 复跑全绿；层 1 锚点：moveStage 后 stageId 翻转、订单转化成功 + APPROVED、forecast-accuracy 报表 commitAmount/lineCount 符合实仓输出。
  - Skill: nop-testing

Exit Criteria:

- [x] C15 类存在且 RECORDING→CHECKING 往返绿（失败模式 = 任一断言/快照 diff 不通过）
- [x] 转化动作面与报表口径裁决落盘（含设计文档 §6 C15 勘误登记，若与实仓行为不一致）

### Phase 2 — C16 CS 工单 SLA 与通知派发（混合类型：1/3 Add + 1/3 Proof + 1/3 Decision，逐项标注）

Status: completed
Targets: `app-erp-all/src/test/java/io/nop/app/all/it/TestErpC16CsSlaNotification.java`、`app-erp-all/_cases/io/nop/app/all/it/TestErpC16CsSlaNotification/`
Skill: nop-testing

- Item Types: `Add | Proof | Decision`
- Prereqs: Phase 1（同基类同装配，先例链打通后无新机制风险）

- [x] Add：**C16 测试类** `TestErpC16CsSlaNotification`（1 类 1 方法）——按设计文档 §6 C16 规格 + cs-ticket/cs-canned-response E2E + `TestErpCsReportRendering` 业务参照：自包含工单 `ErpCsTicket__save` → 六态推进（assign/start/resolve/close，以实仓六态为准）→ `matchAndAttachSla`（isSlaCompleted 断言）→ `ErpCsCannedResponse__applyCannedResponse`（usageCount 递增断言）→ 自包含调研 `ErpCsSurvey__createSurvey`/`submitSurvey`（CSAT/NPS 回填）→ `ErpCsReport__renderHtml(reportName="ticket-sla-csat-summary")`（token 断言：投诉桶/5.00/9.00 以实仓输出为准）→ 通知 `ErpSysNotification__notify`（自包含模板，C09 先例）→ `markRead` → `countUnread` 归零；静态载荷走 `input/N_step.json5` + `addVar`/`@var:` 动态 id。
  - Skill: nop-testing
- [x] Decision：**C16 动作面与报表口径裁决**——① 六态推进动作序列（无 respond——B2 C04 先例「以当前实现为准」，assign/start/resolve/close 以实仓状态机为准）；② SLA 达标口径（matchAndAttachSla 的 isSlaCompleted 语义，以实仓为准）；③ ticket-sla-csat-summary 期望 token（投诉桶/5.00/9.00 的实仓输出口径）；④ 通知模板/落库路径（C09 自包含模板先例）与是否需 frozen clock（SLA 计算日期敏感）。与设计文档不符处 Phase 4 登记 §6 C16 勘误。
  - Skill: nop-testing
- [x] Proof：**C16 RECORDING→CHECKING 往返**——RECORDING 录制 → CHECKING 复跑全绿；层 1 锚点：工单终态 + isSlaCompleted、宏响应 usageCount 递增、SLA/CSAT 报表 token、通知已读翻转。
  - Skill: nop-testing

Exit Criteria:

- [x] C16 类存在且 RECORDING→CHECKING 往返绿（失败模式 = 任一断言/快照 diff 不通过）
- [x] 动作面与报表口径裁决落盘（含设计文档 §6 C16 勘误登记，若与实仓不一致）

### Phase 3 — C17 HR 薪酬发放闭环（混合类型：1/3 Add + 1/3 Proof + 1/3 Decision，逐项标注）

Status: completed
Targets: `app-erp-all/src/test/java/io/nop/app/all/it/TestErpC17HrSalaryPayment.java`、`app-erp-all/_cases/io/nop/app/all/it/TestErpC17HrSalaryPayment/`
Skill: nop-testing

- Item Types: `Add | Proof | Decision`
- Prereqs: Phase 2（同基类同装配，xwf 审批链为本批首个 xwf 集成用例——B1/B2 Payment/Receipt xwf setUserId("0") 复核先例）

- [x] Add：**C17 测试类** `TestErpC17HrSalaryPayment`（1 类 1 方法）——按设计文档 §6 C17 规格 + `TestErpHrSalaryWorkflowApproval`/`TestErpHrSalaryPostingChain`/`TestErpHrPayrollEngine` 业务参照：自包含薪资单（薪酬项）→ `ErpHrSalary__calculateSalary`/`runPayroll`（金额口径先例）→ `submitForApproval` → xwf 三级 `agree`（setUserId("0") 后端驱动 + cc-hr confirm）→ `approve`（APPROVED）→ `markPaid`（SALARY_PAYMENT 凭证 Dr 费用/Cr 2211 借贷平衡 + 金额 = 薪资合计）→ 发放通知（notify 域，C09 先例）；静态载荷走 `input/N_step.json5` + `addVar`/`@var:` 动态 id。
  - Skill: nop-testing
- [x] Decision：**C17 审批链与过账口径裁决**——① xwf 三级 agree 的调用形态（setUserId("0") 后端驱动 + invokeStep(hr-review/finance-review/manager-approval) 与 app-erp-all 装配的契合，B1/B2 先例）；② payroll 科目 config（`erp-hr.default-payroll-subject-id` 缺省 vs `@NopTestProperty`，B6 C12 先例）；③ SALARY_PAYMENT 凭证金额口径（薪资合计 vs 实仓 Processor 拆分口径——TestErpHrSalaryPostingChain 270/280/290/300 系列先例）；④ 通知触发路径（markPaid 自动触发 vs 自包含 notify，以实仓为准）。与设计文档不符处 Phase 4 登记 §6 C17 勘误。
  - Skill: nop-testing
- [x] Proof：**C17 RECORDING→CHECKING 往返**——RECORDING 录制 → CHECKING 复跑全绿；层 1 锚点：审批链终态 APPROVED（xwf submit→agree×3→approve）、markPaid 后 paymentStatus=PAID、SALARY_PAYMENT 凭证借贷平衡 + 金额 = 薪资合计、通知已读。
  - Skill: nop-testing

Exit Criteria:

- [x] C17 类存在且 RECORDING→CHECKING 往返绿（失败模式 = 任一断言/快照 diff 不通过）
- [x] 审批链与过账口径裁决落盘（含设计文档 §6 C17 勘误登记，若与实仓不一致）

### Phase 4 — 收尾与回归（Proof-heavy）

Status: completed
Targets: `docs/backlog/integration-test-roadmap.md`（B8 → done + 头「最后更新」注记）、`docs/logs/2026/08-24.md`、`docs/testing/known-good-baselines.md`（差量登记）、`docs/design/integration-testing.md`（§6 C15/C16/C17 勘误登记，若 Phase 1/2/3 触发）
Skill: none

- Item Types: `Proof | Add`
- Prereqs: Phase 3

- [x] Proof：**局部回归** `mvn test -pl app-erp-all` 全绿（含 3 个新用例类，串行 fork 单 JVM 顺序执行）+ **全量回归** `mvn test`（全 reactor）零新增失败 + `mvn clean install -DskipTests` BUILD SUCCESS——对照 known-good-baselines（B7 后基线）口径，+3 tests / +3 报告文件全额归因 B8，零新增失败；**clean 前先拷贝 surefire 计数/XML 证据**（B3 closure MINOR-2 流程改进兑现，B4-B7 已执行）。
  - Skill: none
- [x] Add：roadmap B8 `todo` → `done` + 头「最后更新」注记 + `docs/logs/2026/08-24.md` 日志条目（按日志书写指南）+ known-good-baselines 差量登记 + 设计文档 §6 勘误登记（若触发，含 C15/C16/C17 旧式码/动作名漂移点）。
  - Skill: none

Exit Criteria:

- [x] 局部 + 全量回归零新增失败（与已知基线口径一致）
- [x] roadmap B8 = done + 日志条目存在 + 基线差量登记 + 勘误登记（若触发）

## Draft Review Record

- Independent draft review iteration 1: needs revision（0 BLOCKER / 2 MAJOR / 3 MINOR，ses_fcef73fdaffecBQ3E21BhVUyhv，独立 general 子代理新会话）——MAJOR-1 集成用例类计数 20→16（实仓核验 = Pilot+B1-B7 16 类）；MAJOR-2 `ErpCrmOpportunity__save` 候选删除（实仓无该实体，B2 已裁决商机 = ErpCrmLead leadType=OPPORTUNITY，锚定 convertToOpportunity）；MINOR-3 引用 roadmap B7 行→B8 行；MINOR-4 「必要时」反松弛词改写；MINOR-5 getCreatedOpportunity 动作注记。全部吸收修订。
- Independent draft review iteration 2: accept（0 BLOCKER / 0 MAJOR / 1 MINOR，ses_fcef2edfeffeo30mHUaHYfCueI，独立 general 子代理新会话）——5 项修订全部实仓核验通过；唯一 MINOR = Draft Review Record 占位未持久化（本轮即兑现）。**共识达成，计划可执行。**

## Closure Gates

> 仅在所有项目和每个阶段的退出标准都勾选 `[x]` 后关闭。完整仓库验证在此处：结束时运行 `typecheck`/`build`/`lint`/`test` 一次。

- [x] 范围内行为完成（C15/C16/C17 三用例三层全比对全绿 + 动作面/报表/审批/过账口径裁决）
- [x] 相关文档对齐（设计文档 §6 ↔ 用例实现 ↔ roadmap B8 ↔ e2e-runbook ↔ known-good-baselines 无矛盾）
- [x] 已运行验证（`mvn test -pl app-erp-all` 全绿 + 全量 `mvn test` 零新增失败 + `mvn clean install -DskipTests` BUILD SUCCESS）
- [x] 无范围内项目降级为 deferred/follow-up
- [x] 独立草案审查已完成并记录
- [x] 文本一致性已验证：状态、阶段、门控和日志都一致
- [x] 结束审计由独立子代理（新会话）执行；执行者未自我审计且未将此留为 `[ ]` 作为人工门控占位符
- [x] 结束证据存在于文件中

## Deferred But Adjudicated

- （执行后按需填写——候选：seed 修正授权触发时的重录耗时测量（B1/M0.2 watch-only residual 触发条件登记）；无范围内降级项则留空）

## Closure

Status Note: B8（C15/C16/C17）四 Phase 全部完成。C15 `TestErpC15CrmLeadForecast`（自包含线索 save → qualify 入漏斗 → moveStage stageId 翻转 → convertToOpportunity 原地升格（勘误：无独立 ErpCrmOpportunity 实体）→ 自包含赢单阶段 → convertToQuotation 弱指针（裁决：弃 CPQ generateQuote 路径）→ ErpSalOrder quotationId 回链 → DIRECT approve → forecast-accuracy token 50,000.00/45,000.00/80,000.00/63,000.00（63000 = 行加权合计口径裁决））、C16 `TestErpC16CsSlaNotification`（save 后置自动挂载 SLA + matchAndAttachSla 幂等双证 → assign/start/applyCannedResponse(usageCount+1)/resolve(isSlaCompleted 达标 + 自动调研 token DB 反查)/close 六态（勘误：无 respond）→ submitSurvey CSAT5/NPS9（勘误：旧动作名漂移）→ ticket-sla-csat-summary 投诉桶 5.00/9.00 → 自包含通知 markRead/countUnread 归零（勘误：工单链 notify 静默降级不落库 → C09 先例））、C17 `TestErpC17HrSalaryPayment`（自包含核算环境 → calculateSalary net 11790 → runPayroll 全员批量幂等 → submitForApproval → xwf 三级 agree + cc-hr confirm（IWorkflowManager 后端驱动 setUserId("0")）→ APPROVED + 270/290/300 计提链 posted=true → markPaid PAID + 280 Dr 2211/Cr 1002=净额（勘误：270 口径误植 + ar_ap_item 断言删除 + 自动通知未实现）+ @NopTestProperty erp-hr.default-payroll-subject-id=2211）三用例 RECORDING→CHECKING 往返全绿（联合复跑 3/0/0 连跑六次稳定）；局部 `mvn test -pl app-erp-all` 47/0/0/1（44 基线 + 3）+ 全量续扫 XML 聚合 3827/0/30/1/636（对照 B7 3824/0/0/1/633 差量 +3 tests/+3 报告文件全额归因 B8；**30 errors 全量归因并发会话未提交 xwf starter 改造（四 xwf 08:23 落盘 + 快照迁移未完成），非归因 B8——同一签名 ACTOR_MODEL_ID=starter，波及 ast 5 类/pur 1 类/sal 3 类/hr 3 类；app-erp-all 侧 C01/C03/C17 同签名 3 处以 ACTOR_MODEL_ID 列 `*` 通配修复双态兼容**）+ `mvn clean install -DskipTests` 156 模块 BUILD SUCCESS；surefire 证据 `_tmp/b8-surefire-evidence/`。冻结时钟 C15C16C17FrozenClockExtension（2026-07-17）；响应快照实钟毫秒 @var 共享漂移全族 `*` 通配（B4-B7 先例推广）。roadmap B8 todo→done + 日志 + 基线差量登记 + 设计文档 §6 C15/C16/C17 勘误登记全部落盘。

Closure Audit Evidence:

- Auditor / Agent: 独立 closure 审计子代理（MISSION_DRIVER:2026-08-24-080841-mission-driver，新会话，非执行者上下文），2026-08-24
- Evidence: 实仓走查核验——① 三用例类与快照落盘：`app-erp-all/src/test/java/io/nop/app/all/it/TestErpC15CrmLeadForecast.java` / `TestErpC16CsSlaNotification.java` / `TestErpC17HrSalaryPayment.java` + `app-erp-all/_cases/io/nop/app/all/it/{TestErpC15CrmLeadForecast,TestErpC16CsSlaNotification,TestErpC17HrSalaryPayment}/` + `C15C16C17FrozenClockExtension.java` 均存在；② roadmap B8 行 + 头部「最后更新」= done（`docs/backlog/integration-test-roadmap.md` L3/L44）；③ 日志条目存在（`docs/logs/2026/08-24.md` 「integration-test mission：B8 分批执行完成」节，含验证 47/0/0/1 + 3827/0/30/1/636 与 30 errors 非归因 B8 的归因分析）；④ 基线差量登记存在（`docs/testing/known-good-baselines.md` 2026-08-24 B8 行）；⑤ 设计文档 §6 C15/C16/C17 三段实施期勘误登记存在（`docs/design/integration-testing.md` L440/L456/L472）；⑥ surefire 证据 `_tmp/b8-surefire-evidence/`（app-erp-all 31 XML）存在；⑦ Plan Status / 四 Phase Status / Exit Criteria / Closure Gates / Closure 全部 completed/[x] 文本一致；⑧ 无范围内项降级 deferred/follow-up（Follow-up 仅并发会话 xwf 快照迁移债务，非本计划范围，已在 Deferred But Adjudicated 语境与 Follow-up 中如实登记归因）。

Follow-up:

- 并发会话 xwf starter 改造的快照迁移债务（ast/pur/sal/hr 15 类 30 errors + 未落盘部分）归其自身会话收口，非 B8 范围；其收口后建议全量复跑回归刷新基线计数。
