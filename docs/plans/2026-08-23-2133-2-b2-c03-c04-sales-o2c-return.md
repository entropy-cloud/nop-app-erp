# 2026-08-23-2133-2-b2-c03-c04-sales-o2c-return 批次 B2 用例实施：C03 O2C 销售到收款黄金路径 + C04 销售退货与客服联动

> Plan Status: active
> Mission: integration-test
> Work Item: B2（C03, C04；主导域 sales）
> Last Reviewed: 2026-08-23
> Source: `docs/backlog/integration-test-roadmap.md`（B2 行，M0.3 追加）
> Related: `2026-08-23-1835-1-m01-integration-test-case-design`（M0.1，done）、`2026-08-23-1835-2-m02-infra-pilot-mechanism-adjudication`（M0.2，done）、`2026-08-23-2133-1-b1-c01-c02-purchase-p2p-return`（前置批次，xwf 复核结论可复用）
> Audit: required

## Current Baseline

（全部事实 2026-08-23 实仓核验）

- **前置依赖**：M0.1 设计文档（`docs/design/integration-testing.md` §6 C03/C04 用例规格 + §3.4 机制实证结论 + §4 粒度约定 + §5 xwf 规避清单）+ M0.2 基建（均 done）；B1 批次为前置顺序批次（非硬依赖——批间无强依赖可并行，但 B1 的 Payment xwf setUserId("0") 复核结论可直接复用为 Receipt 复核依据，见 Phase 1）。
- **基建就绪**（M0.2 产物）：基类 `ErpIntegrationTestCase`（10 个共享 step helper）+ 试点 `TestErpP2pPilot` 范式 + `_cases` 目录约定（`app-erp-all/_cases/io/nop/app/all/it/<类>/<方法>/input|output`，request 文件静态载荷 + `addVar`/`@var:` 动态 id）+ surefire 模块级串行化（forkCount=1/parallel=none）。
- **C03 收款审批轴**：同 C01 Payment——xwf setUserId("0") 须在 app-erp-all 装配下复核（B1 Phase 1 复核 Payment；本计划 Phase 1 复核/复用 Receipt 实体路径）；备选 = seed 已过账收款前置（`erp_sal_receipt.csv` 存在：REC-2026-001 posted=true/RECEIVED）。设计文档 §5 排除路径（非 SYS 调用 Receipt submitForApproval）不得依赖。
- **AR 核销机制（实仓核实）**：核销由**独立 `ErpSalReceipt__settle` 动作**驱动（`ReceiptSettler.java` 先例 `TestErpSalOrderToCashEnd:485`/`TestErpSalReceiptSettlement:167`：settle(receiptId, invoiceId, amount)）→ C03「核销 AR openAmount→0」挂靠 `ErpSalReceipt__settle` 步骤，非审批步骤本身。
- **seed 现状**（94 CSV + 1 SQL）：CUST-001(id=1)/MAT-001(id=1)/CNY 币种 + 仓库 **WH-MAIN(id=1)/WH-RAW(id=2)**（试点引用 WH-RAW；sales seed 链引用 WAREHOUSE_ID=1——实仓核验无「WH-FG」仓库）；`erp_sal_order`/`erp_sal_delivery`/`erp_sal_invoice` seed 行存在；`erp_fin_ar_ap_item` 3-4 行（AR_INVOICE/RECEIPT）**SETTLED**、OPEN 项仅 HR → **C04 前置「已过账销售链 + OPEN AR 项」须用例内自包含建数**（先建 posted 销售链得 OPEN AR 项，再退货）。
- **无 `erp_b2b_*` seed**（94 CSV 实仓核验无 b2b 文件）→ C03 步骤 1「B2B EDI 订单接收」须自包含建数（`ErpB2bEdiDoc`/EDI 消息经 GraphQL，先例 `TestErpB2bEdiEnvelope`/`TestErpB2bEdiPosting`）；**无 `erp_sal_return` seed** → C04 退货单自包含。
- **CS seed 存在**：`erp_cs_ticket.csv`/`erp_cs_ticket_type.csv`/`erp_cs_survey.csv`（1045-1 批次）——C04 工单前置可引 seed（以业务标识设计前置规格）或自包含追加。
- **运行互斥**：与 E2E live server 互斥（`lsof -i :8011`/8080 执行前确认）。
- **基线计数**：known-good-baselines 权威计数源 3809/0/0/1；app-erp-all 29/0/0/1。

## Goals

- **C03** = 测试类 `TestErpC03O2cGoldenPath`（1 类 1 方法）：B2B EDI 自包含接收 → `ErpSalOrder__save` → submit/approve（DIRECT）→ Delivery save/submit/approve（库存出库）→ Invoice save/submit/approve（AR_INVOICE + COGS 凭证 + AR 项）→ **Receipt 全链**（save → 审批推进 → `ErpSalReceipt__settle` 独立核销 → AR `openAmount→0`）→ COGS 金额断言；三层全比对。
- **C04** = 测试类 `TestErpC04SalReturnWithCs`（1 类 1 方法）：CS 工单（客户投诉，自包含或 seed 引用）→ 状态机推进 → 自包含已过账销售链 → 退货 save → submit/approve（反向入库 + SALES_RETURN 红字凭证 + 负 AR 辅助账）→ 退款核销/open items 反查；三层全比对。
- 两用例 **RECORDING→CHECKING 往返全绿**。
- 收款审批轴裁决落地（复用 B1 Payment 复核结论或本计划独立复核 Receipt；备选 = seed 已过账收款前置）。
- roadmap B2 状态 `todo` → `done`（经独立 closure audit）+ 日志 + 基线差量登记。

## Non-Goals

- 其他批次用例（B1/B3-B10 各归其计划）。
- 生产代码/ORM/契约/API 变更——产品缺陷走 roadmap 横切关注点 4 保护区域门禁，不在本计划内直接改生产代码。
- 默认修改 seed CSV——确需修正走横切关注点 3 授权流程（登记理由 + 快照重录 + E2E 数值断言影响评估）；默认自包含建数。
- 不接 CI；不做浏览器层 E2E 页面验证。

## Task Route

- Type: `implementation-only change`（集成测试代码 + 快照 + 文档，零生产代码/契约/模型变更）
- Owner Docs: `docs/design/integration-testing.md`（§6 C03/C04 + §3.4 + §4 + §5）、`docs/backlog/integration-test-roadmap.md`（B2 行 + 横切关注点）、`docs/testing/e2e-runbook.md`（「集成测试」节）、`docs/testing/known-good-baselines.md`
- Skill Selection Basis: `nop-testing` 匹配测试基类/录制回放/三层验证模型——必读 `02-core-guides/testing.md` + `03-runbooks/write-integration-test-with-noptestconfig.md` + `05-examples/test-examples.java`；范本 = 试点类 + `TestErpSalReturnRefundEndToEnd`（SALES_RETURN 红字/负 AR 断言）+ `TestErpB2bEdiEnvelope`（EDI 自包含建数）+ `TestErpCsTicketCreateEnrichment`（CS 状态机推进）。

## Infrastructure And Config Prereqs

- 文件型 H2 fresh 机制已就绪；与 E2E live server 互斥（`lsof -i :8011`/8080 执行前确认）。
- 无端口/密钥/外部服务/环境变量新增。
- 若走 Receipt xwf 审批：`@BeforeEach` `ContextProvider.setUserId("0")` + `setUserName("SYS")`（既有先例路径）。

## Execution Plan

### Phase 1 — C03 O2C 收款审批轴复核 + 用例实施（混合类型：2/4 Proof + 1/4 Add + 1/4 Decision，逐项标注）

Status: planned
Targets: `app-erp-all/src/test/java/io/nop/app/all/it/TestErpC03O2cGoldenPath.java`、`app-erp-all/_cases/io/nop/app/all/it/TestErpC03O2cGoldenPath/`
Skill: nop-testing

- Item Types: `Proof | Add | Decision`
- Prereqs: M0.1 + M0.2 done；优先在 B1 之后执行（复用其 Payment setUserId("0") 复核结论）

- [ ] Proof：**Receipt xwf setUserId("0") 复核**（app-erp-all 装配）——若 B1 已裁决 Payment 复核成立，本项仅核验 Receipt 实体同机制可行（对齐 `TestErpPurPaymentWorkflowApproval`/既有 Receipt 审批先例）；若 B1 未完成或证伪，本项独立走完 Receipt 全链复核。通过判据：approve 后 Receipt `approveStatus=APPROVED`。证伪 → 设计文档 §5 降级路径（Receipt 审批轴移除，seed 已过账收款前置 + 设计文档 §6 C03 修订登记）。
  - Skill: nop-testing
- [ ] Add：**C03 测试类** `TestErpC03O2cGoldenPath`（1 类 1 方法）——按设计文档 §6 C03 规格：自包含 B2B EDI 接收 → `ErpSalOrder__save`/行 → submit/approve（DIRECT）→ Delivery save/行/submit/approve（库存出库移动）→ Invoice save/行/submit/approve（AR_INVOICE 凭证 + COGS 凭证 + AR 项 OPEN）→ **Receipt save/提交/审批（按裁决路径）→ `ErpSalReceipt__settle` 核销（AR `openAmount→0`）**；COGS 金额 = 出库成本（FIFO/MovingAverage 口径，先例 `TestErpInvFifoCostingEndToEnd` 参照）；CRM 机会自包含建数（`ErpCrmOpportunity__save`，转化来源核对）；静态载荷走 request 文件 + `addVar`/`@var:` 动态 id。
  - Skill: nop-testing
- [ ] Decision：**收款审批路径裁决**——记录选择（默认 `setUserId("0")` 后端驱动，若复核成立）、备选（seed 已过账收款前置：REC-2026-001 posted=true/RECEIVED）与残留风险（xwf 链对 SYS caller 的依赖）；理由写入计划。
  - Skill: nop-testing
- [ ] Proof：**C03 RECORDING→CHECKING 往返**——RECORDING 录制 → CHECKING 复跑全绿；三层断言：Order/Delivery/Invoice/Receipt `approveStatus` 翻转、Invoice/Receipt `posted=true`、AR_INVOICE 凭证借贷平衡、COGS 凭证金额 = 出库成本、`ErpSalReceipt__settle` 后 AR `openAmount=0`、GL 应收余额与 AR 总额一致（VoucherLine 聚合口径，对齐 B1 裁决）。
  - Skill: nop-testing

Exit Criteria:

- [ ] Receipt xwf 复核结论落盘（复用 B1 结论或独立证据；证伪则降级登记 + 设计文档修订痕迹）
- [ ] C03 类存在且 RECORDING→CHECKING 往返绿（失败模式 = 任一断言/快照 diff 不通过）
- [ ] 收款审批路径决策记录（含备选与理由）

### Phase 2 — C04 销售退货与客服联动（Add-heavy：2/2 项为 Add）

Status: planned
Targets: `app-erp-all/src/test/java/io/nop/app/all/it/TestErpC04SalReturnWithCs.java`、`app-erp-all/_cases/io/nop/app/all/it/TestErpC04SalReturnWithCs/`
Skill: nop-testing

- Item Types: `Add`
- Prereqs: Phase 1（同基类同装配，先例链打通）

- [ ] Add：**C04 测试类** `TestErpC04SalReturnWithCs`（1 类 1 方法）——按设计文档 §6 C04 规格：CS 工单（客户投诉）save → 六态状态机推进（`assign`/`respond`/…，先例 `TestErpCsTicketCreateEnrichment`）→ 自包含已过账销售链（Delivery→Invoice posted，得 OPEN AR 项）→ `ErpSalReturn__save` → submit → approve（反向入库 + SALES_RETURN 红字凭证 + 负 AR 辅助账，对齐 `TestErpSalReturnRefundEndToEnd` 断言范式）→ 退款核销/open items 反查；静态载荷走 request 文件 + @var 动态 id。
  - Skill: nop-testing
- [ ] Add：**C04 往返与三层断言**——RECORDING 录制 → CHECKING 复跑全绿；层 1 锚点：工单终态（RESOLVED/CLOSED）、Return `approveStatus=APPROVED` + `posted=true`、反向入库移动存在（`findStockMove` 反查）、SALES_RETURN 凭证借贷平衡红字方向、AR 项负向登记（openAmount = −totalAmountWithTax，对齐先例 `TestErpSalReturnRefundEndToEnd` RETURN_WITH_TAX 含税口径）、余额回减断言。
  - Skill: nop-testing

Exit Criteria:

- [ ] C04 类存在且 RECORDING→CHECKING 往返绿（失败模式 = 任一断言/快照 diff 不通过）

### Phase 3 — 收尾与回归（Proof-heavy）

Status: planned
Targets: `docs/backlog/integration-test-roadmap.md`（B2 → done + 头「最后更新」注记）、`docs/logs/2026/08-23.md`、`docs/testing/known-good-baselines.md`（差量登记）
Skill: none

- Item Types: `Proof | Add`
- Prereqs: Phase 2

- [ ] Proof：**局部回归** `mvn test -pl app-erp-all` 全绿（含 2 个新用例类）+ **全量回归** `mvn test` 零新增失败 + `mvn clean install -DskipTests` BUILD SUCCESS——对照 known-good-baselines 口径（3809 基线 + B1/B2 各 2 用例），零新增失败。
  - Skill: none
- [ ] Add：roadmap B2 `todo` → `done` + 头「最后更新」注记 + `docs/logs/2026/08-23.md` 日志条目 + 如适用 known-good-baselines 差量登记（文档/状态回写，非缺陷修复）。
  - Skill: none
- [ ] Add：**设计文档 §6 C04 前置勘误登记**——`docs/design/integration-testing.md:244`「[seed] 既有已过账销售链（erp_sal_invoice posted + AR 项 OPEN）」为 stale 表述（实仓 AR 项 SETTLED，OPEN 仅 HR 行）→ 登记勘误注记（§3.4 风险②「自包含建数」纪律覆盖该前置；用例以自包含建数实现），保证 closure 门控「设计文档 ↔ 用例实现无矛盾」可满足。
  - Skill: none

Exit Criteria:

- [ ] 局部 + 全量回归零新增失败（与已知基线口径一致）
- [ ] roadmap B2 = done + 日志条目存在 + 基线差量登记
- [ ] 设计文档 §6 C04 前置勘误注记落盘（或 closure 记录 §3.4 覆盖前提）

## Draft Review Record

- Independent draft review iteration 1: needs revision (ses_fd127db62ffe8I3jTVyEoJbeNC，独立 general 子代理新会话) — 0 BLOCKER / 1 MAJOR / 3 MINOR。基线事实核验全过（基类 + 10 helper / _cases 约定 / 94 CSV+1 SQL / 无 b2b+sal_return seed / CS seed 存在 / AP 项 3-4 SETTLED / REC-2026-001 posted / 3809+29 基线 / e2e-runbook 互斥纪律 / `ErpSalReceipt` useWorkflow=true）。MAJOR-1 假基线「WH-FG 仓库」不存在（实仓仅 WH-MAIN(id=1)/WH-RAW(id=2)，试点引用 WH-RAW，sales seed 用 WAREHOUSE_ID=1）→ 修正。MINOR-1 Phase 1 类型分数算术错误（4 项非 1/3+1/3+1/3）→ 改 2/4 Proof + 1/4 Add + 1/4 Decision；MINOR-2 C04 AR 负值断言精度 → openAmount = −totalAmountWithTax（对齐 RETURN_WITH_TAX 含税口径）；MINOR-3 Phase 3 Fix 标签不适用 → Add（文档/状态回写）。另补充 `ErpSalReceipt__settle` 独立核销动作点名（Baseline/Goals/Add/Proof 四处）。修订全部落地。
- Independent draft review iteration 2: accept (ses_fd11a8a8dffeFXpgpI9QfIPAXF，独立 general 子代理新会话) — 0 BLOCKER / 0 MAJOR / 1 MINOR。迭代 1 四项发现全部核验 FIXED（WH-MAIN/WH-RAW 基线修正实仓确认 / 分数算术 / 含税口径逐字对齐先例 :187-188 / Phase 3 标签）；`ErpSalReceipt__settle` 四处点名逐字核实（ReceiptSettler.java + 先例行号 485/167 精确匹配 + settle 后 AR openAmount=0/SETTLED 语义实证）。唯一 MINOR（pre-existing 非阻塞）：设计文档 §6 C04 前置「AR 项 OPEN」为 stale 表述（实仓 SETTLED）——计划层已正确检测并以自包含建数解决，closure 时登记设计文档 §6 C04 前置勘误即可。**共识达成，计划可执行。**

## Closure Gates

- [ ] 范围内行为完成（C03 + C04 用例三层全比对全绿 + 收款审批路径裁决）
- [ ] 相关文档对齐（设计文档 §6 ↔ 用例实现 ↔ roadmap B2 ↔ e2e-runbook 无矛盾）
- [ ] 已运行验证（`mvn test -pl app-erp-all` 全绿 + 全量 `mvn test` 零新增失败 + `mvn clean install -DskipTests` BUILD SUCCESS）
- [ ] 无范围内项目降级为 deferred/follow-up
- [ ] 独立草案审查已完成并记录
- [ ] 文本一致性已验证：状态、阶段、门控和日志都一致
- [ ] 结束审计由独立子代理（新会话）执行；执行者未自我审计且未将此留为 `[ ]` 作为人工门控占位符
- [ ] 结束证据存在于文件中

## Deferred But Adjudicated

- （执行后按需填写——无范围内降级项，留空）

## Closure

Status Note: <why the plan can close>

Closure Audit Evidence:

- Auditor / Agent: <independent auditor or independent subagent>
- Evidence: <task id / log link / walkthrough record>

Follow-up:

- <仅非阻塞跟进项目；已确认的缺陷不得出现在此处>