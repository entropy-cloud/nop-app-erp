# 2026-08-23-2133-3-b3-c05-c06-inventory-cost 批次 B3 用例实施：C05 库存到岸成本分摊过账 + C06 库存成本流转与 COGS 核算

> Plan Status: active
> Mission: integration-test
> Work Item: B3（C05, C06；主导域 inventory）
> Last Reviewed: 2026-08-23
> Source: `docs/backlog/integration-test-roadmap.md`（B3 行，M0.3 追加）
> Related: `2026-08-23-1835-1-m01-integration-test-case-design`（M0.1，done）、`2026-08-23-1835-2-m02-infra-pilot-mechanism-adjudication`（M0.2，done）、`2026-08-23-2133-1-b1-c01-c02-purchase-p2p-return`（前置顺序批次）、`2026-08-23-2133-2-b2-c03-c04-sales-o2c-return`（前置顺序批次）
> Audit: required

## Current Baseline

（全部事实 2026-08-23 实仓核验）

- **前置依赖**：M0.1 设计文档（`docs/design/integration-testing.md` §6 C05/C06 用例规格 + §3.4 机制实证结论 + §4 粒度约定）+ M0.2 基建（均 done）；B1/B2 为前置顺序批次（非硬依赖——批间无强依赖可并行，但共享基类/范式先例链打通后再实施更稳）。
- **基建就绪**（M0.2 产物）：基类 `ErpIntegrationTestCase`（10 个共享 step helper）+ 试点 `TestErpP2pPilot` 范式 + `_cases` 目录约定 + surefire 模块级串行化。
- **seed 现状**（94 CSV + 1 SQL）：`erp_inv_cost_layer.csv` 2 行（id=1 WEIGHTED_AVERAGE 100@8.50 total 850 / id=2 MOVING_AVERAGE 80@120.00 total 9600）——C06 成本断言可引用或自包含；`erp_inv_stock_move.csv` id=1（MV-2026-001，PURCHASE_RECEIVE 来源，DONE/APPROVED/**posted=false**）；`erp_qa_inspection.csv`/`erp_mfg_work_order.csv` 存在（C06 质检门控可引 seed 或自包含）。
- **无 `erp_log_*` seed**（94 CSV 实仓核验无 logistics 文件）→ C05 步骤 1「物流运费来源核对」（`ErpLogShipment__get`）须自包含建数（`ErpLogShipment__save` 经 GraphQL，先例 logistics path2 E2E/`TestErpInvLandedCostEndToEnd`）。
- **C05 收货链前置**：seed 收货链（PRCV-2026-001）移动 posted=false——到岸成本用例以**自包含收货链**为准（先例 `TestErpInvLandedCostEndToEnd` 自建 receive 链），避免依赖种子未过账态；对齐「seed 引用 + 自包含 GraphQL 建数」纪律。
- **C05 审批轴（实仓核实）**：`ErpInvLandedCostBizModel` **无 `submitForApproval`**——仅 `approve`/`reverseApprove`/`allocate`/`generateFreightLandedCost`（`module-inventory/.../entity/ErpInvLandedCostBizModel.java`，approve-only；先例 `TestErpInvLandedCostEndToEnd` 即 approve-only）→ 设计文档 §6 C05 动作名「save → submitForApproval → approve」为漂移，本计划按 approve-only 实施并登记设计文档澄清（Phase 1 Decision）。
- **C06 质检门控机制（实仓核实）**：强制质检为 **config 门控**——`erp-qua.mandatory-inspection-bill-types`（`ErpQaConstants.java:59`，默认空 = 不强制，application.yaml 无 override，审计 `docs/audits/2026-08-07-2359-rc-ma4...` 裁决「部署启用决策非契约缺失」）；门控施加点是 **`ErpPurReceive__approve`**（`ErpPurReceiveProcessor.java:506`，`ERR_RECEIVE_INSPECTION_BLOCKED`，PENDING 阻塞/ACCEPTED 放行），**非** `generateMove`/`confirm` → C06 负路径须用例内显式 `@NopTestProperty(name="erp-qua.mandatory-inspection-bill-types", value="PUR_RECEIPT")` 启用门控，且阻断断言挂在 Receive 审批步骤（Phase 2 步骤重排）。
- **入库移动管道（实仓核实）**：`ErpPurReceive__approve` **内建触发入库移动**（`ErpPurReceiveApproveProcessor.java:44` → `ErpPurReceiveProcessor.triggerIncomingMove:393-397` → `stockMoveBiz.generateMove(request)`，业务联动 → 自动推进 DRAFT→CONFIRMED→DONE）；**显式 `ErpInvStockMove__confirm` 对 DONE 移动单非法**（`ERR_ILLEGAL_STATUS_TRANSITION`，`TestErpInvStockMoveBizModel.testIllegalTransitionRejected` 实证；confirm 仅对 manual 无源单移动单合法，`ErpInvStockMoveBizModel.java:64`）→ C06 正路径**不做显式 generateMove/confirm**，断言 approve 副作用产生的 DONE 移动单（`findStockMove` 反查）+ cost_layer（业务联动移动单成本层在 generateMove 内建管道创建，先例 `TestErpInvFifoCostingEndToEnd:177` 唯一调用即 generateMove 无 confirm）。
- **运行互斥**：与 E2E live server 互斥（`lsof -i :8011`/8080 执行前确认）。
- **基线计数**：known-good-baselines 权威计数源 3809/0/0/1；app-erp-all 29/0/0/1。

## Goals

- **C05** = 测试类 `TestErpC05InvLandedCost`（1 类 1 方法）：自包含已收货采购链 + 自包含物流运费 → `ErpInvLandedCost__save`（FREIGHT 费用行）→ **approve（approve-only，无独立 submit——动作名漂移裁决见 Phase 1）**（LANDED_COST 凭证 + cost_layer 更新）→ `ErpInvStockBalance`/`ErpInvCostLayer` 总成本断言；三层全比对。
- **C06** = 测试类 `TestErpC06InvCostFlowCogs`（1 类 1 方法）：QA 来料检验（`ErpQaInspection__save`）→ **Receive 审批门控**（`@NopTestProperty` 显式启用 `erp-qua.mandatory-inspection-bill-types=PUR_RECEIPT`；ACCEPTED 放行 / REJECTED 阻断 `ERR_RECEIVE_INSPECTION_BLOCKED`）→ 采购入库（**Receive approve 内建移动管道**：DONE 移动单 + cost_layer 创建，无显式 generateMove/confirm）→ 销售出库（delivery approve）→ COGS 核算（FIFO 锚定口径）→ 库存余额/成本层断言；含 REJECTED 负路径；三层全比对。
- 两用例 **RECORDING→CHECKING 往返全绿**。
- roadmap B3 状态 `todo` → `done`（经独立 closure audit）+ 日志 + 基线差量登记。

## Non-Goals

- 其他批次用例（B1/B2/B4-B10 各归其计划）。
- 生产代码/ORM/契约/API 变更——产品缺陷走 roadmap 横切关注点 4 保护区域门禁，不在本计划内直接改生产代码。
- 默认修改 seed CSV——确需修正走横切关注点 3 授权流程（登记理由 + 快照重录 + E2E 数值断言影响评估）；默认自包含建数。
- 不接 CI；不做浏览器层 E2E 页面验证。

## Task Route

- Type: `implementation-only change`（集成测试代码 + 快照 + 文档，零生产代码/契约/模型变更）
- Owner Docs: `docs/design/integration-testing.md`（§6 C05/C06 + §3.4 + §4）、`docs/backlog/integration-test-roadmap.md`（B3 行 + 横切关注点）、`docs/testing/e2e-runbook.md`（「集成测试」节）、`docs/testing/known-good-baselines.md`
- Skill Selection Basis: `nop-testing` 匹配测试基类/录制回放/三层验证模型——必读 `02-core-guides/testing.md` + `03-runbooks/write-integration-test-with-noptestconfig.md` + `05-examples/test-examples.java`；范本 = 试点类 + `TestErpInvLandedCostEndToEnd`（LANDED_COST 凭证/cost_layer 断言）+ `TestErpInvFifoCostingEndToEnd`（FIFO/MovingAverage COGS 断言）+ `TestErpQaInspectionTrigger`（质检门控正/负路径）。

## Infrastructure And Config Prereqs

- 文件型 H2 fresh 机制已就绪；与 E2E live server 互斥（`lsof -i :8011`/8080 执行前确认）。
- 无端口/密钥/外部服务/环境变量新增。
- **C06 门控显式启用**：C06 测试类须声明 `@NopTestProperty(name = "erp-qua.mandatory-inspection-bill-types", value = "PUR_RECEIPT")`（用例级 config 启用——该键默认空 = 门控关闭，属运维部署启用决策非契约缺失，审计 `docs/audits/2026-08-07-2359-rc-ma4-a4-2-101-112-quality-f1-f2-f3-runtime.md` 裁决口径）。`@NopTestProperty` 机制先例 = 基类文档 `ErpIntegrationTestCase.java:46`（「须经子类 @NopTestProperty 在容器启动前声明」）+ `TestErpP2pPilot.java:40`；`TestErpQaInspectionTrigger` 为域级 config 启用**语义**先例（其用运行时 `assignConfigValue` 赋值，机制不同，仅作语义参照）。除此外不新增/改其他 config。

## Execution Plan

### Phase 1 — C05 到岸成本分摊过账（混合类型：1/3 Add + 1/3 Decision + 1/3 Proof，逐项标注）

Status: planned
Targets: `app-erp-all/src/test/java/io/nop/app/all/it/TestErpC05InvLandedCost.java`、`app-erp-all/_cases/io/nop/app/all/it/TestErpC05InvLandedCost/`
Skill: nop-testing

- Item Types: `Add | Decision | Proof`
- Prereqs: M0.1 + M0.2 done；优先在 B1/B2 之后执行（先例链打通）

- [ ] Add：**C05 测试类** `TestErpC05InvLandedCost`（1 类 1 方法）——按设计文档 §6 C05 规格：自包含收货链（Receive → approve posted）+ 自包含物流运费（`ErpLogShipment__save` 或运费单据，来源核对 `ErpLogShipment__get`）→ `ErpInvLandedCost__save`（FREIGHT 费用行，引用收货/运费）→ **approve（approve-only，按下方 Decision）**（LANDED_COST 凭证 + cost_layer 更新，对齐 `TestErpInvLandedCostEndToEnd`）→ `ErpInvStockBalance__findPage`/`ErpInvCostLayer__findPage` 总成本断言；静态载荷走 request 文件 + `addVar`/`@var:` 动态 id。
  - Skill: nop-testing
- [ ] Decision：**C05 审批轴 + 数据来源双裁决**——(1) 审批轴：LandedCost **approve-only**（实仓 `ErpInvLandedCostBizModel` 无 `submitForApproval`，仅 approve/reverseApprove/allocate/generateFreightLandedCost；先例 `TestErpInvLandedCostEndToEnd` approve-only）——设计文档 §6 C05「save → submitForApproval → approve」动作名漂移登记澄清；(2) 数据来源：收货链/物流运费默认自包含（seed 收货链移动 posted=false、无 erp_log_* seed），备选 = seed 修正授权流程（roadmap 横切关注点 3：登记理由 + 快照重录 + E2E 影响评估）；残留风险 = 运费金额与收货金额口径须对齐分摊先例。
  - Skill: nop-testing
- [ ] Proof：**C05 往返与三层断言**——RECORDING 录制 → CHECKING 复跑全绿；层 1 锚点：LandedCost `approveStatus=APPROVED` + `posted=true`、LANDED_COST 凭证借贷平衡、cost_layer totalCost = 原成本 + 分摊运费、stock_balance.totalCost 联动。
  - Skill: nop-testing

Exit Criteria:

- [ ] C05 类存在且 RECORDING→CHECKING 往返绿（失败模式 = 任一断言/快照 diff 不通过）
- [ ] 审批轴 + 数据来源决策记录（含备选/残留风险；若触发 seed 修正授权则登记理由 + 快照重录 + E2E 影响评估）

### Phase 2 — C06 库存成本流转与 COGS 核算（混合类型：1/2 Add + 1/2 Proof，逐项标注）

Status: planned
Targets: `app-erp-all/src/test/java/io/nop/app/all/it/TestErpC06InvCostFlowCogs.java`、`app-erp-all/_cases/io/nop/app/all/it/TestErpC06InvCostFlowCogs/`
Skill: nop-testing

- Item Types: `Add | Proof`
- Prereqs: Phase 1（同基类同装配，先例链打通）

- [ ] Add：**C06 测试类** `TestErpC06InvCostFlowCogs`（1 类 1 方法）——按设计文档 §6 C06 规格（**门控与入库步骤重排**：门控施加点在 Receive 审批，入库移动由 approve 内建管道产生）：自包含物料/仓库/币种前置（或 seed 引用）+ QA 来料检验（`ErpQaInspection__save`，ACCEPTED）→ `ErpPurReceive__save` → **`ErpPurReceive__approve` 门控放行**（`@NopTestProperty` 启用 `erp-qua.mandatory-inspection-bill-types=PUR_RECEIPT`；ACCEPTED 放行过账 + 内建触发 DONE 入库移动 + cost_layer 创建，**无显式 generateMove/confirm**——显式 confirm 对 DONE 移动单非法 `ERR_ILLEGAL_STATUS_TRANSITION`，见 Baseline 入库移动管道）→ 销售出库（delivery approve，出库移动 + SALES_OUTPUT 过账）→ COGS 凭证金额 = 层成本（**FIFO 锚定口径**，对齐 `TestErpInvFifoCostingEndToEnd`）→ `ErpInvStockBalance__findPage`/`ErpInvCostLayer__findPage` 断言；**REJECTED 负路径**（检验 REJECTED → `ErpPurReceive__approve` 阻断，断言 `ERR_RECEIVE_INSPECTION_BLOCKED` 错误码）；静态载荷走 request 文件 + `addVar`/`@var:` 动态 id。
  - Skill: nop-testing
- [ ] Proof：**C06 往返与三层断言**——RECORDING 录制 → CHECKING 复跑全绿；层 1 锚点：approve 后 DONE 入库移动存在（`findStockMove` 反查）+ 入库后 stock_balance.totalQty/totalCost 正确、出库 COGS = 层成本（FIFO 口径，对齐 `TestErpInvFifoCostingEndToEnd` ledger.totalCost 负号/`SALES_OUTPUT` 凭证借贷断言）、REJECTED 阻断（错误码断言）、账本（ledger）与余额一致。
  - Skill: nop-testing

Exit Criteria:

- [ ] C06 类存在且 RECORDING→CHECKING 往返绿（含负路径断言；失败模式 = 任一断言/快照 diff 不通过）

### Phase 3 — 收尾与回归（Proof-heavy）

Status: planned
Targets: `docs/backlog/integration-test-roadmap.md`（B3 → done + 头「最后更新」注记）、`docs/logs/2026/08-23.md`、`docs/testing/known-good-baselines.md`（差量登记）
Skill: none

- Item Types: `Proof | Add`
- Prereqs: Phase 2

- [ ] Proof：**局部回归** `mvn test -pl app-erp-all` 全绿（含 2 个新用例类）+ **全量回归** `mvn test` 零新增失败 + `mvn clean install -DskipTests` BUILD SUCCESS——对照 known-good-baselines 口径（3809 基线 + B1/B2/B3 各 2 用例），零新增失败。
  - Skill: none
- [ ] Add：roadmap B3 `todo` → `done` + 头「最后更新」注记 + `docs/logs/2026/08-23.md` 日志条目 + 如适用 known-good-baselines 差量登记（文档/状态回写，非缺陷修复）。
  - Skill: none

Exit Criteria:

- [ ] 局部 + 全量回归零新增失败（与已知基线口径一致）
- [ ] roadmap B3 = done + 日志条目存在 + 基线差量登记

## Draft Review Record

- Independent draft review iteration 1: needs revision (ses_fd127c127ffegRrXBaPQRfBBBJ，独立 general 子代理新会话) — 0 BLOCKER / 2 MAJOR / 4 MINOR。基线事实核验全过（seed 计数/内容、参考测试自包含模式、3809/29 基线、基类 10 helper、其余链动作存在性）。MAJOR-1 `ErpInvLandedCost__submitForApproval` 不存在（BizModel 仅 approve/reverseApprove/allocate/generateFreightLandedCost，approve-only）→ Phase 1 新增 Decision 项（approve-only + 设计文档 §6 C05 动作名漂移登记）；MAJOR-2 C06 REJECTED 负路径空洞（质检门控 `erp-qua.mandatory-inspection-bill-types` config 门控默认关闭 + 门控点在 `ErpPurReceive__approve` 非 generateMove/confirm）→ 显式 `@NopTestProperty` + 步骤重排。MINOR×4（先例引用精度 / confirm 步骤 vs FIFO 先例 / 未标记 Decision 项 / soft config hedge）→ 全部落地。
- Independent draft review iteration 2: needs revision (ses_fd11a6f8dffebMXeeIrV8yPOHA，独立 general 子代理新会话) — 0 BLOCKER / 1 MAJOR / 2 MINOR / 1 NIT。迭代 1 两 MAJOR 修复核验通过（实仓证据链完整）；**MAJOR-NEW** C06 正路径「generateMove → confirm」自相矛盾（`ErpPurReceive__approve` 内建触发 DONE 移动单，显式 confirm 对 DONE 非法 `ERR_ILLEGAL_STATUS_TRANSITION`，`TestErpInvStockMoveBizModel.testIllegalTransitionRejected` 实证）→ 正路径删除显式 generateMove/confirm，改断言 approve 副作用 DONE 移动单 + cost_layer（选项 (a) 裁定）。MINOR-1' Infra 将 `TestErpQaInspectionTrigger` 当 @NopTestProperty 机制先例引用（实为 assignConfigValue 运行时赋值）→ 改为基类文档 + 试点类机制先例、域级测试作语义参照；MINOR-NEW A Phase 2 第 2 项类型标签 Add → Proof（阶段头声明同步）；NIT COGS 口径措辞统一（FIFO 锚定）。
- Independent draft review iteration 3: accept (ses_fd112c231ffeGd41yMSZWHa30T，独立 general 子代理新会话) — 0 BLOCKER / 0 MAJOR / 0 MINOR / 2 NIT。迭代 2 全部七项修复逐项实仓核验通过（approve 内建移动管道证据链 / Goals 与 Phase 2 步骤重排 / 类型标签 / @NopTestProperty 机制先例 / Draft Review Record / FIFO 口径统一）；门控点与错误码（`ERR_RECEIVE_INSPECTION_BLOCKED`）、approve-only 审批轴、confirm 非法性实证全部验证。NIT-1 cost_layer 归因精度（实由 costing 策略经 bookCompletion 链创建，断言对象为 approve 后状态不受影响）；NIT-2 confirm 合法性推断仅 DONE 方向实证（操作结论不受影响）。**共识达成，计划可执行。**

## Closure Gates

- [ ] 范围内行为完成（C05 + C06 用例三层全比对全绿）
- [ ] 相关文档对齐（设计文档 §6 ↔ 用例实现 ↔ roadmap B3 ↔ e2e-runbook 无矛盾）
- [ ] 已运行验证（`mvn test -pl app-erp-all` 全绿 + 全量 `mvn test` 零新增失败 + `mvn clean install -DskipTests` BUILD SUCCESS）
- [ ] 无范围内项目降级为 deferred/follow-up
- [ ] 独立草案审查已完成并记录
- [ ] 文本一致性已验证：状态、阶段、门控和日志都一致
- [ ] 结束审计由独立子代理（新会话）执行；执行者未自我审计且未将此留为 `[ ]` 作为人工门控占位符
- [ ] 结束证据存在于文件中

## Deferred But Adjudicated

- （执行后按需填写——无范围内降级项，留空）

## Closure

Status Note: <why the plan can close>

Closure Audit Evidence:

- Auditor / Agent: <independent auditor or independent subagent>
- Evidence: <task id / log link / walkthrough record>

Follow-up:

- <仅非阻塞跟进项目；已确认的缺陷不得出现在此处>