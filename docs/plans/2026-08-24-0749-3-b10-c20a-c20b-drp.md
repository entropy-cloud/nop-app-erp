# 2026-08-24-0749-3-b10-c20a-c20b-drp 批次 B10 用例实施：C20a DRP 净需求与补货释放 + C20b DRP 仿真与正式计划提升

> Plan Status: completed
> Mission: integration-test
> Work Item: B10（C20a, C20b；主导域 drp）
> Last Reviewed: 2026-08-24
> Source: `docs/backlog/integration-test-roadmap.md`（B10 行，M0.3 追加）
> Related: `2026-08-24-0749-2-b9-c18-c19-contract-b2b`（同批先行计划，B9 排其前）
> Audit: required

## Current Baseline

（全部事实 2026-08-24 实仓核验）

- **前置依赖**：M0.1 设计文档（`docs/design/integration-testing.md` §6 C20a/C20b 用例规格 + §3.4 机制实证结论 + §4 粒度约定 + §5 xwf 规避清单）+ M0.2 基建 + B1-B9（B9 为同批先行计划，B10 在其后执行——roadmap B10 行 Deps = M0.1 + M0.2，批内排序由本批计划序号确定）。
- **基建就绪**（M0.2/B1-B9 产物）：基类 `ErpIntegrationTestCase`（机制 (c) 文件 H2 双模 + 共享 step helper）；`_cases` 目录约定；surefire 模块级串行化已落地；已有 **16 个集成用例类**（Pilot + B1-B7 = 1+2+2+2+3+2+2+2，it/ 目录实仓核验；B8/B9 先行落地后 = 21）作为范本，**B10 按同范式实现（1 用例 = 1 类 1 方法 + @BeforeAll fresh-DB + RECORDING→CHECKING 往返）**。
- **drp 域既有测试资产（本批业务参照，不重复实现内部细节）**：`TestErpDrpEngine`（净需求引擎：毛需求 − 在途 − 在手 − 已分配）、`TestErpDrpScheduleRelease`（runDrp→approvePlan→releaseLine 调度释放链）、`TestErpDrpSimulation`（仿真运行/版本对比）、`TestErpDrpWiringRegression`/`TestErpDrpPlanCrudSmoke` 等——设计文档 C20a/C20b 全部引用先例在位。
- **C20a 动作面（实仓核验在位）**：`ErpDrpPlanBizModel` 实仓动作 = `runDrp`/`approvePlan`/`resetToDraft`；`ErpDrpLineBizModel` 实仓动作 = `releaseLine`/`approveLine`/`rejectLine`/`cancelLine` + `ErpDrpLineReleaseApprovedProcessor`（计划级释放 `releaseApproved` 处理器，设计文档步骤 3 备选）——**与设计文档 §6 C20a 步骤 1-3 一致**；计划状态机 DRAFT/COMPUTED/APPROVED/EXECUTED（无计划级 ORDERED/CANCELLED）+ 行状态机 SUGGESTED/APPROVED/ORDERED/CANCELLED，补货类型 TRANSFER/PURCHASE。**净需求实仓口径（DrpEngine）：`netRequirement = max(0, safetyStock + forecastDemand − currentStock + allocatedQty − onOrderQty)`——已分配量 allocatedQty 为**加项**（预留库存增加净需求），设计文档 §6 C20a 层 1「毛需求 − 在途 − 在手 − 已分配」符号相反为勘误预期，实施期以实仓为准登记勘误**。
- **C20a 前置 seed（实仓核验）**：`_init-data/` 下**无 `erp_drp_*` seed 文件**——DRP 计划/需求数据全部走**自包含建数**（`TestErpDrpEngine` 先例）；物料/仓库/往来复用 `erp_md_material.csv`/`erp_md_warehouse.csv`/`erp_md_partner.csv` 既有 seed。
- **C20b 动作面（实仓核验在位）**：`ErpDrpScenarioBizModel` 实仓动作 = `runSimulation`（`ErpDrpScenarioRunSimulationProcessor`，config 门控 `erp-drp.simulation-enabled` 缺省 false）/`compareVersions`（`DrpSimulationDiffResult` 返回）/`promoteToFormalPlan`（提升正式计划生成可执行 DRP 计划）——**与设计文档 §6 C20b 步骤 1-3 一致**；`ErpDrpPlan__runDrp` 步骤 4。
- **C20b config（实仓核验）**：`erp-drp.simulation-enabled`（`ErpDrpConstants.CONFIG_DRP_SIMULATION_ENABLED`，缺省 false，`ErpDrpErrors` 有「DRP仿真入口未启用」错误码）→ 用例 `@NopTestProperty` 开启（B5-C10/B6-C12 config 门控 @NopTestProperty 先例）。
- **C20b 前置 seed（实仓核验）**：`_init-data/` 下无 `erp_drp_*` seed——仿真场景/参数全部**自包含建数**（`TestErpDrpSimulation` 先例）；物料/仓库复用 md 域 seed。
- **运行互斥**：集成测试与 E2E live server 共用 `db/erp.mv.db`——执行前 `lsof -i :8011`（8080 同查）确认无 live server（e2e-runbook「集成测试」节纪律）。
- **基线计数**：known-good-baselines 2026-08-24 权威计数源 **3824 tests / 0 failures / 0 errors / 1 skipped / 633 报告文件**；app-erp-all **44/0/0/1**。若 B8/B9 已先行落地则以 B9 后基线为准。B10 预期差量 **+2 tests / +2 报告文件**（2 用例类各 1 类 1 方法）。
- **流程改进登记（B3 closure MINOR-2 兑现，B4-B7 已执行）**：最终 `clean install` 会清除 surefire XML 计数证据——全量回归后、clean 前先拷贝计数/XML 再登记基线（本计划 Phase 3 执行纪律；B8/B9 若已先行落地则同步延续）。
- **deferred 触发条件（B1/M0.2 watch-only residual）**：若执行期发生 seed 修正授权路径（roadmap 横切关注点 3），须同步测量登记重录耗时（以单用例 252K/类为预算参考）；本批默认自包含建数，预期不触发。
- **冻结时钟纪律**：drp 域存在 frozen clock 先例——`DrpFrozenClockExtension`（REFERENCE_DATE 2026-07-17，`TestErpDrpEngine`/`TestErpDrpScheduleRelease`/`TestErpDrpCrossDock`/`TestErpDrpLeadTimeStats` 使用）；**本批默认按同型引入本地扩展（若 RECORDING 发现日期敏感漂移即启用；引入则登记快照时间戳 `*` 通配处置，B2/B4/B5/B6/B7 先例）**。

## Goals

- **C20a** = 测试类 `TestErpC20aDrpNetRequirementRelease`（1 类 1 方法）：自包含 DRP 计划（需求数据）→ `ErpDrpPlan__runDrp`（净需求计算 = `max(0, safetyStock + forecastDemand − currentStock + allocatedQty − onOrderQty)` 实仓 DrpEngine 口径）→ `ErpDrpPlan__approvePlan`（approvedQty 回填）→ `ErpDrpLine__releaseLine`（行级释放 → 生成 TransferOrder/PurchaseOrder 建议；或 `releaseApproved` 计划级释放，以实仓为准）→ 下游单据 DIRECT 审批（生成的建议单审批后过账）；三层全比对。
- **C20b** = 测试类 `TestErpC20bDrpSimulationPromote`（1 类 1 方法）：自包含仿真场景（参数）→ `ErpDrpScenario__runSimulation`（@NopTestProperty 开启 `erp-drp.simulation-enabled` → 仿真版本生成）→ `ErpDrpScenario__compareVersions`（版本对比结构断言，`DrpSimulationDiffResult`）→ `ErpDrpScenario__promoteToFormalPlan`（提升正式计划 → 生成可执行 DRP 计划）→ `ErpDrpPlan__runDrp`（正式计划运行）；三层全比对。
- 两用例 **RECORDING→CHECKING 往返全绿**。
- roadmap B10 状态 `todo` → `done`（经独立 closure audit）+ 日志 + 基线差量登记 + 设计文档 §6 C20a/C20b 勘误登记（若发生）。

## Non-Goals

- V.1/V.2（全量验证与收尾里程碑，各归对应计划，须 B1-B10 全部 done 后执行）。
- 生产代码/ORM/契约/API 变更——用例实施中发现的产品缺陷按 roadmap 横切关注点 4 走保护区域门禁（accounting/finance postings = plan-first + owner doc + tests；其余 = auto + dual-agent-approval），**不得在本计划内直接改生产代码**。
- 默认修改 seed CSV——确需修正走 roadmap 横切关注点 3「seed 修正授权」流程；默认以自包含建数满足前置。
- 不接 CI（roadmap 已裁决出 scope）；不做浏览器层 E2E 页面验证（分工边界）。
- 不实现未覆盖功能（横切关注点 6「以当前实现为准」）：C20a/C20b 步骤若与实仓行为不符，以实仓行为替代 + 设计文档勘误登记，不强行实现缺失功能。

## Task Route

- Type: `implementation-only change`（集成测试代码 + 快照 + 文档，零生产代码/契约/模型变更）
- Owner Docs: `docs/design/integration-testing.md`（§6 C20a/C20b 规格 + §3.4 编写纪律 + §4 粒度 + §5 xwf 清单）、`docs/backlog/integration-test-roadmap.md`（B10 行 + 横切关注点）、`docs/testing/e2e-runbook.md`（「集成测试」节）、`docs/testing/known-good-baselines.md`
- Skill Selection Basis: `nop-testing` 匹配测试基类选择/@NopTestConfig/@NopTestProperty（C20b simulation 门控 = B5-C10/B6-C12 同型）/快照录制回放（RECORDING→CHECKING）/三层验证模型；M0.2 试点 + B1-B9 集成用例类为同模式范本，drp 既有测试（Engine/ScheduleRelease/Simulation）为业务参照。

## Infrastructure And Config Prereqs

- 文件型 H2 `db/erp.mv.db` fresh 机制已就绪（基类每类 1 次清理）；**与 E2E live server 互斥**——执行前 `lsof -i :8011`（8080 同查）确认无 live server。
- **C20b config（实仓核验，必配）**：`@NopTestProperty` 指定 `erp-drp.simulation-enabled=true`（缺省 false，缺省时 runSimulation 抛「DRP仿真入口未启用」错误）。
- C20a 无新增 config（runDrp/approvePlan/releaseLine 无门控）。
- 无端口/密钥/外部服务/环境变量新增；surefire 串行化已在 app-erp-all pom 落地。

## Execution Plan

### Phase 1 — C20a DRP 净需求与补货释放（混合类型：1/2 Add + 1/4 Proof + 1/4 Decision，逐项标注）

Status: completed
Targets: `app-erp-all/src/test/java/io/nop/app/all/it/TestErpC20aDrpNetRequirementRelease.java`、`app-erp-all/_cases/io/nop/app/all/it/TestErpC20aDrpNetRequirementRelease/`
Skill: nop-testing

- Item Types: `Add | Proof | Decision`
- Prereqs: B9 done（批内排序）+ M0.1 + M0.2

- [x] Add：**C20a 测试类** `TestErpC20aDrpNetRequirementRelease`（1 类 1 方法）——按设计文档 §6 C20a 规格 + `TestErpDrpEngine`/`TestErpDrpScheduleRelease` 业务参照：自包含 DRP 计划（需求数据）→ `ErpDrpPlan__runDrp`（净需求 = `max(0, safetyStock + forecastDemand − currentStock + allocatedQty − onOrderQty)` 实仓 DrpEngine 口径断言）→ `ErpDrpPlan__approvePlan`（approvedQty 回填断言）→ `ErpDrpLine__releaseLine`（行级释放 → TransferOrder/PurchaseOrder 建议生成断言；或 `releaseApproved` 计划级释放，以实仓为准）→ 下游单据 DIRECT 审批（生成的建议单审批后过账断言）；静态载荷走 `input/N_step.json5` + `addVar`/`@var:` 动态 id。
  - Skill: nop-testing
- [x] Decision：**C20a 净需求与释放口径裁决**——① 净需求计算口径（实仓 `DrpEngine`：`max(0, safetyStock + forecastDemand − currentStock + allocatedQty − onOrderQty)`，**allocatedQty 为加项**——设计文档「毛需求 − 在途 − 在手 − 已分配」符号相反，实施期显式核验符号并以实仓为准登记勘误）；② 释放动作面（行级 `releaseLine` vs 计划级 `releaseApproved`，设计文档步骤 3 双写——以实仓行为为准登记勘误）；③ 下游单据生成形态（TransferOrder/PurchaseOrder 建议 + DIRECT 审批过账路径，以实仓 Processor 为准）。与设计文档不符处 Phase 3 登记 §6 C20a 勘误。
  - Skill: nop-testing
- [x] Proof：**C20a RECORDING→CHECKING 往返**——RECORDING 录制 → CHECKING 复跑全绿；层 1 锚点：净需求正确、approvedQty 回填、释放生成下游单据（采购/调拨建议存在）、下游审批后过账（posted）。
  - Skill: nop-testing

Exit Criteria:

- [x] C20a 类存在且 RECORDING→CHECKING 往返绿（失败模式 = 任一断言/快照 diff 不通过）
- [x] 净需求与释放口径裁决落盘（含设计文档 §6 C20a 勘误登记，若与实仓行为不一致）

### Phase 2 — C20b DRP 仿真与正式计划提升（混合类型：1/2 Add + 1/4 Proof + 1/4 Decision，逐项标注）

Status: completed
Targets: `app-erp-all/src/test/java/io/nop/app/all/it/TestErpC20bDrpSimulationPromote.java`、`app-erp-all/_cases/io/nop/app/all/it/TestErpC20bDrpSimulationPromote/`
Skill: nop-testing

- Item Types: `Add | Proof | Decision`
- Prereqs: Phase 1（同基类同装配，先例链打通后无新机制风险）

- [x] Add：**C20b 测试类** `TestErpC20bDrpSimulationPromote`（1 类 1 方法）——按设计文档 §6 C20b 规格 + `TestErpDrpSimulation` 业务参照：自包含仿真场景（参数）→ `ErpDrpScenario__runSimulation`（@NopTestProperty 开启 `erp-drp.simulation-enabled` → 仿真版本生成断言）→ `ErpDrpScenario__compareVersions`（版本对比结构断言，`DrpSimulationDiffResult`）→ `ErpDrpScenario__promoteToFormalPlan`（提升正式计划 → 可执行 DRP 计划生成断言）→ `ErpDrpPlan__runDrp`（正式计划运行）；静态载荷走 `input/N_step.json5` + `addVar`/`@var:` 动态 id。
  - Skill: nop-testing
- [x] Decision：**C20b 仿真与提升口径裁决**——① 仿真版本生成形态（ScenarioVersion，runSimulation 返回，以实仓为准）；② compareVersions 断言粒度（结构非空 vs delta 数值——设计文档注明 ParamResolver 缓存约束先例浏览器层降级口径 JUnit 层不受限）；③ promoteToFormalPlan 后正式计划状态与后续 runDrp 可运行性。与设计文档不符处 Phase 3 登记 §6 C20b 勘误。
  - Skill: nop-testing
- [x] Proof：**C20b RECORDING→CHECKING 往返**——RECORDING 录制 → CHECKING 复跑全绿；层 1 锚点：仿真版本生成、compareVersions 结构断言、promote 后正式计划状态、正式计划 runDrp 可运行。
  - Skill: nop-testing

Exit Criteria:

- [x] C20b 类存在且 RECORDING→CHECKING 往返绿（失败模式 = 任一断言/快照 diff 不通过）
- [x] 仿真与提升口径裁决落盘（含设计文档 §6 C20b 勘误登记，若与实仓不一致）

### Phase 3 — 收尾与回归（Proof-heavy）

Status: completed
Targets: `docs/backlog/integration-test-roadmap.md`（B10 → done + 头「最后更新」注记）、`docs/logs/2026/08-24.md`、`docs/testing/known-good-baselines.md`（差量登记）、`docs/design/integration-testing.md`（§6 C20a/C20b 勘误登记，若 Phase 1/2 触发）
Skill: none

- Item Types: `Proof | Add`
- Prereqs: Phase 2

- [x] Proof：**局部回归** `mvn test -pl app-erp-all` 全绿（含 2 个新用例类，串行 fork 单 JVM 顺序执行）+ **全量回归** `mvn test`（全 reactor）零新增失败 + `mvn clean install -DskipTests` BUILD SUCCESS——对照 known-good-baselines（B9 后基线）口径，+2 tests / +2 报告文件全额归因 B10，零新增失败；**clean 前先拷贝 surefire 计数/XML 证据**（B3 closure MINOR-2 流程改进兑现，B4-B7 已执行，B8/B9 若先行落地则延续）。
  - Skill: none
- [x] Add：roadmap B10 `todo` → `done` + 头「最后更新」注记 + `docs/logs/2026/08-24.md` 日志条目（按日志书写指南）+ known-good-baselines 差量登记 + 设计文档 §6 勘误登记（若触发）+ **roadmap 注记「全部 Bn done，V.1/V.2 解锁」**。
  - Skill: none

Exit Criteria:

- [x] 局部 + 全量回归零新增失败（与已知基线口径一致）
- [x] roadmap B10 = done + 日志条目存在 + 基线差量登记 + 勘误登记（若触发）+ V.1/V.2 解锁注记

## Draft Review Record

- Independent draft review iteration 1: needs revision（0 BLOCKER / 3 MAJOR / 2 MINOR，ses_fcef72c89ffe6Sj4P757jhVg0G，独立 general 子代理新会话）——MAJOR-1 用例类计数 25→16（实仓核验 = Pilot+B1-B7 16 类）；MAJOR-2 净需求公式符号反转（实仓 DrpEngine `max(0, safetyStock + forecastDemand − currentStock + allocatedQty − onOrderQty)`——allocatedQty 为加项非减项，设计文档「毛需求 − 在途 − 在手 − 已分配」符号相反为勘误预期）；MAJOR-3 冻结时钟前提修正（drp 域有 `DrpFrozenClockExtension` REFERENCE_DATE 2026-07-17 先例，四测试使用）；MINOR-4 计划状态机 DRAFT/COMPUTED/APPROVED/EXECUTED（无计划级 ORDERED/CANCELLED）；MINOR-5 先行批执行状态 B4-B9→B4-B7。全部吸收修订。
- Independent draft review iteration 2: accept（0 BLOCKER / 0 MAJOR / 0 MINOR，ses_fcef2dba7ffe4QC1p5uLTk2iZA，独立 general 子代理新会话）——5 项修订全部实仓核验通过（公式符号/扩展名/状态机/计数/批状态逐一对照源码）；无新增发现。**共识达成，计划可执行。**

## Closure Gates

> 仅在所有项目和每个阶段的退出标准都勾选 `[x]` 后关闭。完整仓库验证在此处：结束时运行 `typecheck`/`build`/`lint`/`test` 一次。

- [x] 范围内行为完成（C20a/C20b 两用例三层全比对全绿 + 净需求/释放/仿真/提升口径裁决）
- [x] 相关文档对齐（设计文档 §6 ↔ 用例实现 ↔ roadmap B10 ↔ e2e-runbook ↔ known-good-baselines 无矛盾）
- [x] 已运行验证（`mvn test -pl app-erp-all` 全绿 + 全量 `mvn test` 零新增失败 + `mvn clean install -DskipTests` BUILD SUCCESS）
- [x] 无范围内项目降级为 deferred/follow-up
- [x] 独立草案审查已完成并记录
- [x] 文本一致性已验证：状态、阶段、门控和日志都一致
- [x] 结束审计由独立子代理（新会话）执行；执行者未自我审计且未将此留为 `[ ]` 作为人工门控占位符
- [x] 结束证据存在于文件中

## Deferred But Adjudicated

- seed 修正授权路径（B1/M0.2 watch-only residual）：B10 两用例全量自包含建数（库存余额 DAO fixture + 计划/参数/场景经 GraphQL save），未触发 seed 修正授权——重录耗时测量维持空转，Classification: watch-only residual / Successor Required: no。

## Closure

Status Note: 三 Phase 全部完成且退出标准全勾选——C20a `TestErpC20aDrpNetRequirementRelease` / C20b `TestErpC20bDrpSimulationPromote` RECORDING→CHECKING 往返全绿（各复跑两次稳定），净需求（allocatedQty 加项）/释放动作面/下游单据形态/仿真版本/compareVersions delta/promote 回落口径六项裁决全落盘设计文档 §6 勘误；局部回归 app-erp-all 54/0/0/1 + 全量 3834/0/0/1/642（+2/+2 全额归因 B10 零新增失败）+ clean install 156 模块 BUILD SUCCESS；roadmap B10 done + B1-B10 全 done V.1/V.2 解锁注记 + 日志 + 基线差量登记齐备。独立结束审计（新会话子代理）CLOSURE: PASS（10/10 项 PASS，0 BLOCKER/0 MAJOR，仅 3 条非阻塞观察）。

Closure Audit Evidence:

- Auditor / Agent: 独立 general 子代理（新会话，ses_fcc9c6440ffe6MeBoQgDLPW1jB）
- Evidence: 审计报告 10 项逐项 PASS——计划文本一致性 / 两测试类与快照目录实仓核验 / 冻结时钟扩展 / 独立 CHECKING 复跑 2/0/0 BUILD SUCCESS / roadmap-log-baselines-设计文档四源对齐 / 零生产代码变更（module-* 与 src/main 无改动）/ Closure Gates 无预勾 / `_tmp/b10-surefire-evidence/` 642 XML 聚合 3834/0/0/1 与声明完全一致。verdict `CLOSURE: PASS`。

Follow-up:

- （无阻塞跟进；V.1/V.2 已解锁，归各自计划执行）