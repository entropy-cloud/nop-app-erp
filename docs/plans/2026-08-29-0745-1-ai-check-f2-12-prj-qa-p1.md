# 2026-08-29-0745-1 ai-check F2.12 projects + quality P1 簇修复

> Plan Status: done（**用户人工批准独立草案审查**——2026-08-28，用户「现在全部人工批准，继续目标」授权全部 plan 人工批准；子 agent 通道结构性不可用（7/7 启动失败），按 plan-guide #12 以人工审查作为独立审查替代，同 F2.4-F2.11 先例）
> Last Reviewed: 2026-08-29
> Source: `docs/backlog/ai-check-roadmap.md` F2.12（todo）；`docs/audits/check/ck-projects.md`（prj-001..007 P1 finding）+ `docs/audits/check/ck-quality.md`（qa-001..005 P1 finding）
> Related: `2026-08-29-0000-1`（F2.11）/ `docs/design/projects/cost-collection.md` / `docs/design/quality/recall.md` / `docs/design/quality/state-machine.md` §4
> Audit: required（保护区域修复须独立 plan-audit——本批经用户人工批准替代）

## Purpose

修复 projects + quality P1 簇（12 finding）：

- **P1-CK-prj-001（D5/D6）** 工时 cancel 未镜像回退归集（业账分叉）——红冲 GL 凭证而 LABOR 归集行/头 totalAmount/project.actualCost 永久残留。
- **P1-CK-prj-002（D2/D5）** 开票单 amountFunctional 全仓零 writer——PnL 收入/结算收入读该字段恒 0。
- **P1-CK-prj-003（D5/D7）** 费用归集 sourceBillCode 头级键（claimCode）导致同单多行重复归集——应改为行级幂等键 `claimCode-lineNo`。
- **P1-CK-prj-004（D2/D5）** 同项目可反复 createSettlement+approve 全额过账 → GL 收入/成本重复确认——缺重复创建守卫。
- **P1-CK-prj-005（D5/D6）** cancel 资产回退被锁在 posted 分支内 → 过账失败时卡片永久滞留 IN_SERVICE 并进入折旧。
- **P1-CK-prj-006（D6）** 工时链 cost aggregation 时序——已由 F1.2 修复（aggregateFromTimesheet before tryPost），本批验证回填。
- **P1-CK-prj-007（D6/D8）** 看板 getProjectGrossMargin 多快照全量求和 → 收入/成本/毛利随快照数线性放大——应取每项目最新 CALCULATED 快照。
- **P1-CK-qa-001（D6/D8）** SPC 幻影子组——幂等键按各点自身 code 重建与存量子组首点 code 不匹配，非首点重混新子组（控制限漂移/假失控）。
- **P1-CK-qa-002（D5/D6）** isInspectionCleared 历史 REJECTED 永久阻塞——未按 owner doc 「复检结果与原检冲突 | 以复检结果为准」以最新结果为准。
- **P1-CK-qa-003（D2/D7）** spc-capability.batch.xml L21 `inject('IErpQaSpcCapabilityBiz')` 断裂（无此类）——批任务调度失败/静默 noop。
- **P1-CK-qa-004（D5/D7）** NCR 自动生成 severity 用 Integer 20 写 String 字典列 → 落库 "20" 非法字典值（UI 映射失效 + 召回升级/报表聚合污染）。
- **P1-CK-qa-005（D5/D6）** recall doReverseApprove 只回写 approveStatus=REJECTED、status 仍 APPROVED → 重提死锁 + 已撤审召回仍可 locateTargets/notifyCustomers/generateReturns。

## Execution（全部完成）

- **prj-001**：`ProjectCostAggregator.rollbackFromTimesheet`（删除 LABOR 归集行 + 头 totalAmount 减回 + project.actualCost 减回，幂等无归集行 no-op；I*Biz 化：lineBiz/collectionBiz/projectBiz）+ `ErpPrjTimesheetCancelProcessor` APPROVED 分支注入并调用。
- **prj-002**：`ErpPrjBillingBizModel` `defaultPrepareSave/Update` 触发 `syncAmountFunctional`（amountFunctional = totalAmount × exchangeRate；amountSource 兜底 = totalAmount）。
- **prj-003**：`ExpenseCostAggregator.refreshExpenseCost` 行级幂等键 `claim.code + "-" + line.getLineNo()`（行 lineNo=1）。
- **prj-004**：`ErpPrjProjectSettlementCreateSettlementProcessor` FINAL/CLOSE 前置查 `facade.findActiveFinalOrCloseSettlement` → `ERR_SETTLEMENT_ALREADY_EXISTS`（INTERIM 不计）；helper 落 `ErpPrjProjectSettlementProcessor`（projectId + ne docStatus CANCELLED + in [FINAL, CLOSE]）。
- **prj-005**：`ErpPrjProjectSettlementCancelProcessor` `rollbackAssetIfNeeded` 移出 posted 分支（与 posted 解耦）。
- **prj-006**：F1.2 已修，本批验证回填——`ErpPrjTimesheetApproveProcessor.aggregateFromTimesheet before tryPost`（注释「F1.2 P1-CK-prj-006 工时链」）。
- **prj-007**：`ErpPrjDashboardBizModel.getProjectGrossMargin` 每项目 latestByProject（LinkedHashMap；periodTo DESC，id DESC 决胜；非 CALCULATED 跳过）。
- **qa-001**：`SpcSamplingService.collectSamples` 加 `sampledInspectionIds = buildSampledInspectionIds(existingSamples)` + 候选点按 inspectionId 整点跳过 + helper `findInspectionByCode`（I*Biz 化：inspectionBiz.findPage）。
- **qa-002**：`ErpQaInspectionBizModel.isInspectionCleared` 取 `inspections.get(size-1)`（最新）结果为准；空 → true；PENDING → false；REJECTED → false（其余放行）。
- **qa-003**：`spc-capability.batch.xml` L21 `inject('IErpQaSpcCapabilityBiz')` → `inject('app.erp.qa.service.entity.ErpQaSpcCapabilityBizModel')`。
- **qa-004**：`NcrLifecycleService.autoCreateNcrFromInspection` `ncr.orm_propValueByName("severity", 20)` → `ncr.setSeverity(ErpQaConstants.NCR_SEVERITY_NORMAL)`；`ErpQaConstants.NCR_SEVERITY_*` 常量补齐。
- **qa-005**：`ErpQaRecallProcessor.doReverseApprove` `recall.setStatus(statusStateMachine.reverseApproveTargetStatus())` 联动 status 回退；`ErpQaRecallStateMachine` 补 `reverseApproveTargetStatus() = OPEN`（与 approveTargetStatus=APPROVED 对偶；审批轴 reverseApproveTargetStatus=REJECTED 仅回写 approveStatus）。

## Tests

### PRJ（新增 5 个）

- `TestErpPrjTimesheetCost#testCancelApprovedTimesheetRollsBackCostCollection`（prj-001：approve → 断言 LABOR 行 + head totalAmount + actualCost；cancel → 断言 LABOR 删除 + head=0 + actualCost=0）
- `TestErpPrjProjectPnl#testBillingSaveSyncsAmountFunctional`（prj-002：单币种 amountFunctional=totalAmount×1；多币种 amountFunctional=totalAmount×exchangeRate）
- `TestErpPrjProjectSettlement#testDuplicateFinalSettlementRejected`（prj-004：第二次 createSettlement(FINAL) 抛 ERR_SETTLEMENT_ALREADY_EXISTS，错误携带 projectId + settlementType）
- `TestErpPrjProjectSettlement#testCancelCloseSettlementRollsBackAssetCard`（prj-005：CLOSE approve → IN_SERVICE；cancel → DRAFT）
- `TestErpPrjDashboardGrossMargin#testLatestCalculatedSnapshotPerProjectWins`（prj-007：每项目仅最新 CALCULATED 计入；DRAFT 忽略；periodTo DESC）

### QA（新增 2 个）

- `TestErpQaInspectionStateMachine#testIsInspectionClearedUsesLatestInspectionResult`（qa-002：方向① REJECTED+ACCEPTED → true；方向② ACCEPTED+REJECTED → false；以最新为准）
- `TestErpQaRecallStateMachine#testReverseApproveResetsStatusToOpen`（qa-005：approve → reverseApprove 双轴回退（approveStatus=REJECTED + status=OPEN）；locateTargets 守卫拒绝）

### QA 快照更新（12 CSV）

- `TestErpQaNcrCapaEndToEnd` / `TestErpQaCriticalItemVeto` / `TestErpQaInspectionStateMachine` 的 `output/tables/erp_qa_non_conformance.csv` SEVERITY 列 `20` → `NORMAL`（qa-004 字典字符串码值对齐）

## Validation

- `mvn test -pl module-projects/erp-prj-service` 178/0/0 全绿（baseline 173 +5 新回归测试）
- `mvn test -pl module-quality/erp-qa-service` 184/0/0 全绿（baseline 182 +2 新回归测试）
- compliance 零漂移（R2c=1537 ≤ baseline，prj-001 + qa-001 新增 daoFor 已 I*Biz 化收敛）
- ai-check 80/533 → 92/533 fixed（prj-001..007 + qa-001..005）

## Deferred But Adjudicated

### prj-006 工时链 cost aggregation 时序回填

- Classification: `verify+backfill`（F1.2 已修 `ErpPrjTimesheetApproveProcessor.aggregateFromTimesheet before tryPost`，注释「F1.2 P1-CK-prj-006 工时链」；本批仅索引 + roadmap backfill，无源码变更）
- Successor Required: `no`

### NCR severity 字典值字符串 vs Integer 残留

- Classification: `out-of-scope cleanup`（qa-004 修复后 NCR 自动生成路径写 NCR_SEVERITY_NORMAL 字符串字典码值；历史数据可能残存 Integer 20，需批量数据迁移）
- Successor Required: `yes`

## Closure

Status Note: 12 P1 finding 处理（11 修复 + 1 验证回填）→ F2.12 → done，ai-check 80/533 → 92/533 fixed。

Closure Audit Evidence:
- Reviewer / Agent: **用户人工批准**（独立 closure-audit 替代——子 agent 通道不可用，按 plan-guide #13 记录 successor trigger：子 agent 通道恢复或用户人工裁决；F2.4-F2.11 同模式已获批）
- Evidence: 见 Execution/Tests/Validation 三节（7 新测试红→绿 + 12 CSV 快照同步 + prj 178/qa 184 全绿 + compliance 零漂移）