# 2026-09-11-2350-1 ai-check F2.14 crm+cs P1 簇修复（crm-001/002 + crm2-001/002 + cs-001/002/003 七条 P1）

> Plan Status: completed
> Last Reviewed: 2026-09-11
> Source: `docs/backlog/ai-check-roadmap.md` Work Item F2.14；findings 详情 `docs/audits/check/ck-crm-lead.md` + `ck-crm-cpq-forecast.md` + `ck-cs.md`；索引 `docs/audits/check/ai-check-index.md`（L496-497/515-516/535-537，七条全 `open`）
> Related: plan `2026-09-11-1530-1`（F2.13 同方法基线先例：失败测试先行 + 快照重录合规 + per-site baseline-raise）
> Audit: required（独立草案审查 + 独立结束审计；子代理通道可用）

## Current Baseline

（2026-09-11 HEAD `f90ad34a0` 实仓逐一复核，七条现症全部在位）

- **P1-CK-crm-001**：`QuotaRollupCalculator.accumulatePipeline`（L164-216）——公司级（territoryId=null）预测段 L187-188 与实际段 L206-207 只计 `isNull("territoryId")` 行（配额段 L76-77 聚合全部行，三段口径互斥）；实际段 L207 仅 `eq("docStatus", CONVERTED)` 无 `leadType=OPPORTUNITY` 过滤（convertToCustomer 链原 lead + 新建商机双计，`ErpCrmConversionProcessor:112` 复制 expectedRevenue）；预测/实际段均无期间过滤（periodLabel 只进 quota 段 L168-173）。
- **P1-CK-crm-002**：`ErpCrmLeadProcessor.doLose:153-161`/`doCancel:163-167` 与 `ErpCrmConversionProcessor.markLeadConverted:117-123` 均无 `writeConvLog`（对照 `doMoveStage:179-188` 逐一写）→ `ErpCrmLeadFunnelRefreshFunnelProcessor.refreshFunnel:63-67` 以「期间内有 ConvLog」圈定 leads → 期间内唯一动作是丢失/转化的线索从漏斗消失；`FunnelAggregationEngine.computeHeader:139-145` totalWon/totalRevenue 无 leadType 过滤（双计）且不校验 isWonStage。
- **P1-CK-crm2-001**：`ErpCrmLeadSequenceProgressBizModel.countConsecutiveOverdueSteps`（注释自称反向扫描，实现 L249-261 正向 0..currentIndex）——按期推进到第 3 步后历史步骤墙钟到期日必已过 → `scanOverdueSteps` ≥maxOverdue(3) 恒命中 → `crm.sequence-overdue` 系统性误报。既有测试仅覆盖真逾期场景（`TestErpCrmSequenceAndFunnel#testScanOverdueSteps`）。
- **P1-CK-crm2-002**：`PriceRuleEngine.ruleMatchesProduct:85-88` / `ruleMatchesCustomer:90-97` 无 productCategory/customerCategory 判定（productId=null 恒真）——类别限定规则退化为全局规则错误匹配定价；ORM 列集与 `ErpCrmPriceRule.view.xml` 前端列均宣示该维度可用。
- **P1-CK-cs-001**：`ErpCsTicketMatchAndAttachSlaProcessor.matchAndAttachSla:42-66`——`applyEntitlementSlaOverride`（L102+ maxResolutionTime 覆盖）先执行后被 L52-64 策略匹配 deadline 无条件覆写；slaPolicyId 写权益策略而 deadline 按另一策略计算（来源错配）；`SlaDeadlineCalculator.calculate` 返回 null（无 hours/days 配置）时 `Timestamp.valueOf(null)` 裸 NPE。
- **P1-CK-cs-002**：`ErpCsQualityDashboardBizModel:321-323` 与 `ErpCsReportBizModel:265-266` 调查聚合无 `respondedAt` 过滤、评分 null 按 0 填充分母——CSAT/NPS 均分系统性偏低（csat.md §4.3 `WHERE s.respondedAt IS NOT NULL` 语义未落地）；E2E `cs-ticket-sla-csat.value.spec.ts` 断言被种子（恰全响应）掩盖。
- **P1-CK-cs-003**：`ErpCsServiceCatalogItemCreateFromCatalogProcessor`——预填 slaPolicyId 使 `ErpCsTicketBizModel.enrichAfterCreate:135` 双空守卫跳过 → deadline 恒 null（目录工单永不超时升级、resolve 恒达标）；权益命中先扣一次后 matchAndAttachSla 对纯计次权益再扣一次（双计）；显式 `"TK-"+millis` 编号绕过 `cs-ticket-code` 规则且同毫秒撞 UK。

**消费不重做**：验证基线 = known-good-baselines `f213-mnt-hr-p1` 行（全 reactor 4095/0/0/1 + R2c=1544 + CJK/i18n PASS）；crm2-003（P2 预测准确率）归 F3.5 批不在本计划。

## Goals

- F2.14 七条 P1 finding 全部到达终态 `fixed`：先失败测试后修复，测试与 plan 指针回填索引。
- crm-001：管道三段口径统一（公司级全行 + OPPORTUNITY 过滤 + 期间过滤）。
- crm-002：丢失/取消/转化写 ConvLog（期间圈定闭合）+ totalWon/totalRevenue 消双计。
- crm2-001：逾期计数改「当前步逾期深度」语义（反向 + 当步未逾期即 0）。
- crm2-002：价格规则 productCategory 匹配落地（经 MaterialCategory.code 对齐）+ customerCategory 显式降级（无数据承载，view 隐藏列防误配置）。
- cs-001：SLA 挂载序修正（权益策略直接计算 + maxResolutionTime 最终权益覆盖 + null 安全）。
- cs-002：CSAT/NPS 分母剔除未响应调查（双站点 + 分项计数）。
- cs-003：目录建单 SLA deadline 计算 + 权益防双扣 + codeRule 兜底。
- owner docs（territory.md / lead-waterfall.md / sales-sequence.md / cpq.md / entitlement.md / csat.md / ticket.md 相关节）实现注记对齐；roadmap F2.14 → done。

## Non-Goals

- `P2-CK-crm2-003` 预测准确率（转化时间口径/期末终刷/多维准确率行）——F3.5 批。
- crm-001 实际段「转化时间」精确口径（Lead 无转化审计列的根治）——以 ConvLog 存在性近似（Phase 3 落地后可用），精确转化时间轴归 P2-CK-crm2-003 successor。
- crm2-001 per-step 完成时间戳数据模型（history 列）——数据模型限制下取「当前步逾期深度」口径。
- cs-002 E2E 种子补未响应调查样本——种子变更触发快照重录链（cs-ticket-sla-csat value spec + dashboard value spec + TestErpSeedDataIntegrity CSV 计数），口径固化以模块级测试承载；种子补样归 Deferred（触发条件：E2E 面需直接固化聚合口径时，随批重录）。
- cs-003 目录建单 xwf/审批流、SLA 暂停/恢复语义（现有体系外新能力）。

## Task Route

- Type: `bug investigation` + `implementation-only change`（含测试与文档）
- Owner Docs: `docs/design/crm/territory.md`、`lead-waterfall.md`、`sales-sequence.md`、`cpq.md`、`docs/design/customer-service/entitlement.md`、`csat.md`、`ticket.md`（实现注记落点）
- Skill Selection Basis: crm/cs 域 BizModel/Processor/Engine 行为缺陷修复 → `nop-backend-dev`（`.opencode/skills/nop-backend-dev/SKILL.md`，路由 nop-entropy docs-for-ai service-layer/error-handling/safe-api）；测试 → `nop-testing`（同目录，路由 testing.md/JunitAutoTestCase 快照机制）；收官 → `closure-audit-prompt`（`docs/skills/` 注册表实存）——前二为工具原生技能目录（`docs/skills/README.md` §与工具原生技能的关系）

## Infrastructure And Config Prereqs

- 无新增基础设施。crm2-002 若选跨域反查产品/客户类别，复用既有 `IDaoProvider` 只读范式（豁免注释登记，不新增跨域写）。
- 回滚策略：单 commit 承载，`git revert` 回滚；无 seed/ORM/契约变更（预期），无快照重录义务（crm/cs 既有 `_cases` 与 value spec 均不触本批行为面——执行期实跑验证，若漂移按 F2.13 先例重录并声明）。

## Execution Plan

### Phase 1 — crm2-002 PriceRuleEngine 类别维度

Status: completed
Targets: `PriceRuleEngine.java`、`ErpCrmProductConfiguratorGenerateQuoteProcessor.java`（调用方传参）、`module-crm/erp-crm-web/src/main/resources/_vfs/erp/crm/pages/ErpCrmPriceRule/ErpCrmPriceRule.view.xml`（隐藏列）
Skill: `nop-backend-dev`

- Item Types: `Decision` + `Fix` + `Proof`

- [x] Decision: 类别维度分侧裁决（数据模型前提经审查修正）——实仓事实：产品侧类别 = `ErpMdProduct.categoryId` → to-one `ErpMdMaterialCategory`（值域为实体 id，可经其 `code` 与规则的自由字符串语义对齐）；**客户侧 `ErpMdPartner` 无 customerCategory 列**（最近似 `customerGroup` 语义不同一），cpq.md 自由字符串语义（"SERVER" 等）在客户侧无数据承载。裁决：**productCategory 维度实施**——引擎入参扩展 productCategory 字符串（方案 a：调用方反查产品→category→code 一次传入，引擎保持纯函数，与 `TestPriceRuleEngine` 纯构造 Proof 兼容）；**customerCategory 维度显式降级**——无数据承载不可实施，按 finding 自带出口落地：owner doc cpq.md 实现注记显式降级 + `ErpCrmPriceRule.view.xml` 隐藏 customerCategory 编辑列（防误配置），`ruleMatchesCustomer` 保持仅 customerId 并注释降级缘由。替代方案（引擎内 IDaoProvider 反查）与纯单元 Proof 冲突且客户侧无列可查，否决。残留：customerCategory 维度降级登记 Deferred（触发条件：partner 类别数据模型落地时随批实施）。
      - Skill: `nop-backend-dev`
- [x] Fix: `ruleMatchesProduct` 增 productCategory 判定（rule 值非空时须与传入产品类别 code 相等）；`ErpCrmPriceRule.view.xml` 隐藏 customerCategory 列；调用方（`ErpCrmProductConfiguratorGenerateQuoteProcessor`）传参接线
- [x] Proof: 先行失败测试（纯单元，`new ErpCrmPriceRule()` 构造规则 + category 场景矩阵：类别限定规则对不匹配产品/客户不再命中、对匹配命中、双空规则仍全局）→ 修复后绿 + 既有 PriceRuleEngine 测试零回归
      - Skill: `nop-testing`

Exit Criteria:

- [x] 新测试先红后绿；`mvn test -pl module-crm/erp-crm-service -Dtest=<引擎测试类>` 全绿

### Phase 2 — crm2-001 序列逾期计数语义

Status: completed
Targets: `module-crm/erp-crm-service/src/main/java/app/erp/crm/service/entity/ErpCrmLeadSequenceProgressBizModel.java`
Skill: `nop-backend-dev`

- Item Types: `Decision` + `Fix` + `Proof`

- [x] Decision: 口径裁决——改为「当前步逾期深度」：从 currentIndex 反序扫描（先判当前步，当前步未逾期 → 0；已逾期则向更早步累计连续逾期数），与数据模型限制（已完成步骤无 per-step 完成时间戳，无法回溯判定其历史是否逾期）一致；同步修正方法注释/javadoc 与 owner doc sales-sequence.md §3 注记（「连续逾期」语义收敛为连续未按期完成的当前进度窗）。替代方案（按步完成时间戳精确判定）需 ORM 加列，否决（保护区域 + 超出缺陷面）。
      - Skill: `nop-backend-dev`
- [x] Fix: 反序实现 + 注释修正
- [x] Proof: 先行失败测试——按期推进到第 5 步（各步 dueDays 已过但推进正常）→ 修复前误报 ≥3，修复后按当前步到期状态计 0 或真实逾期深度；真逾期场景（既有测试）保持绿
      - Skill: `nop-testing`

Exit Criteria:

- [x] 新反向用例先红后绿；既有 `testScanOverdueSteps` 零回归

### Phase 3 — crm-002 ConvLog 补写 + 漏斗消双计

Status: completed
Targets: `ErpCrmLeadProcessor.java`、`ErpCrmConversionProcessor.java`、`FunnelAggregationEngine.java`
Skill: `nop-backend-dev`

- Item Types: `Decision` + `Fix` + `Proof`

- [x] Decision: ConvLog 语义扩展裁决——`doLose`/`doCancel`/`markLeadConverted` 补 `writeConvLog(lead, currentStageId, currentStageId, context)`（from=to=当前 stage，阶段轴无迁移、仅作期间事件留痕使期间圈定可见）；漏斗侧 `computeStageMetrics` 对 from==to 的日志按「当前态事件」计数豁免 exit 索引（entryByStage 按 leadId 去重不受影响）；**已知伴生值漂移（登记）**：哨兵日志流入 `computeSalesCycleDays`（赢单周期首→转化时刻，值变更准）与 `computeDaysInStage`（末端阶段停留时长含丢失/转化前滞留），两处漂移在 Phase 8 lead-waterfall.md 注记承接，既有测试数据均为直接构造 ConvLog 无哨兵零回归。totalWon/totalRevenue 计入条件补 `leadType=OPPORTUNITY`（消除 convertToCustomer 链双计；对齐注释/设计 isWonStage 语义，若 stage 数据缺失回退 leadType 判定）。替代方案（Lead 加转化/丢失时间列）触 ORM 保护区域，否决。
      - Skill: `nop-backend-dev`
- [x] Fix: 三处补写 ConvLog + 引擎 won 判定过滤
- [x] Proof: 先行失败测试（集成：lead 上月 moveStage、本月 lose → refreshFunnel 本月 totalLost 计 1（修复前 0）；转化链双计场景 → totalWon/totalRevenue 单计）→ 修复后绿 + 既有漏斗测试零回归
      - Skill: `nop-testing`

Exit Criteria:

- [x] 新测试先红后绿；`mvn test -pl module-crm/erp-crm-service` 全绿

### Phase 4 — crm-001 管道三段口径统一

Status: completed
Targets: `QuotaRollupCalculator.java`
Skill: `nop-backend-dev`

- Item Types: `Fix` + `Proof`（依赖 Phase 3 的 ConvLog 事件）

- [x] Fix: ① 公司级（territoryId=null）预测段与实际段去除 `isNull("territoryId")` 过滤（三段统一「全部行」，对齐配额段 rollup(null) 语义）；② 实际段补 `eq("leadType", OPPORTUNITY)`；③ 预测段补期间过滤——`ErpCrmForecast` 无 periodLabel 列，期间引用为 `periodId` → `ErpCrmForecastPeriod.label`，两步实现（按 label 查期间 id 集 → `in("periodId", ids)`）；实际段期间过滤 = 期间内有 ConvLog（Phase 3 事件）且当前 CONVERTED 的 lead 集合（近义口径，owner doc 注记 residual）
- [x] Proof: 先行失败测试（三段口径：公司级 forecast/actual 不再近零；OPPORTUNITY 过滤消双计；期间过滤生效）→ 修复后绿 + `getTerritoryPipeline` 既有测试零回归
      - Skill: `nop-testing`

Exit Criteria:

- [x] 新测试先红后绿；crm 模块测试全绿

### Phase 5 — cs-001 SLA 挂载序修正

Status: completed
Targets: `ErpCsTicketMatchAndAttachSlaProcessor.java`
Skill: `nop-backend-dev`

- Item Types: `Fix` + `Proof`

- [x] Fix: 重排计算序——权益 `slaPolicyId` 命中时直接按该策略计算 deadline（跳过 matcher 或 matcher 结果仅作无权益回退）；`maxResolutionTime` 覆盖在一切策略计算之后最终应用（恒胜出）；`SlaDeadlineCalculator.calculate` 返回 null 时跳过 deadline 写入（保持 null，不抛 NPE）
- [x] Proof: 先行失败测试（三场景：① 权益 maxResolutionTime 覆盖不被通用策略覆写；② slaPolicyId 与 deadline 同源（权益策略 resolveHours）；③ 无 hours/days 策略 → deadline null 不抛）→ 修复后绿 + 既有 SLA 测试/E2E 守卫零回归
      - Skill: `nop-testing`

Exit Criteria:

- [x] 新测试先红后绿；cs 模块测试全绿

### Phase 6 — cs-002 CSAT/NPS 分母修正

Status: completed
Targets: `ErpCsQualityDashboardBizModel.java`、`ErpCsReportBizModel.java`
Skill: `nop-backend-dev`

- Item Types: `Fix` + `Proof`

- [x] Fix: 双站点聚合补 `respondedAt IS NOT NULL` 过滤（`respondedAt` 列已核实 cs ORM :514）；分母改分项计数（csatScore 非空的计 CSAT 分母、npsScore 非空的计 NPS 分母，可独立缺失）；`surveyCount` 语义同步为已响应数
- [x] Proof: 先行失败测试（1 条响应 5 分 + 1 条未响应 → avgCsat=5.0 而非 2.5；全未响应 → 0/空态不除零）→ 修复后绿 + 看板/报表既有测试零回归（E2E cs-ticket-sla-csat 种子全响应，期望值不受影响——执行期 E2E 回归证实）
      - Skill: `nop-testing`

Exit Criteria:

- [x] 新测试先红后绿；cs 模块测试全绿

### Phase 7 — cs-003 目录建单 SLA 闭环

Status: completed
Targets: `ErpCsServiceCatalogItemCreateFromCatalogProcessor.java`、`ErpCsTicketBizModel.java`（enrichAfterCreate/matchAndAttachSla 权益守卫）；`module-cs/model/app-erp-cs.orm.xml` 不触（仅读 `catalogItemId` 列名）
Skill: `nop-backend-dev`

- Item Types: `Decision` + `Fix` + `Proof`

- [x] Decision: deadline 计算挂点裁决——catalog 处理器内 save 前直接计算（复用 `SlaDeadlineCalculator` + 最终生效策略 = 权益 slaPolicyId ?: item.slaPolicyId + maxResolutionTime 覆盖，与 Phase 5 语义一致）写入 ticketData，不调 matchAndAttachSla（沿 entitlement.md §8.1 既有取舍，避免实体状态冲突）；防双扣锚点 = 已持久化的 `ErpCsTicket.catalogItemId` 列（ticketData 旗标不可行——映射集外 key 不落实体）：`enrichAfterCreate`/`matchAndAttachSla` 的权益消费守卫对 `catalogItemId != null` 的工单跳过二次 `matchAndConsumeEntitlement`（覆盖残留子情形：权益与目录项均无 slaPolicyId 且无 maxResolutionTime → deadline 仍 null → 守卫放行路径，Proof ② 硬前提）；显式 `"TK-"+millis` code 移除交 codeRule fill-when-absent 兜底（`TestErpCsTicketCreateEnrichment` 既有断言为凭）。
      - Skill: `nop-backend-dev`
- [x] Fix: 落地 deadline 计算 + 防双扣 + code 兜底
- [x] Proof: 先行失败测试（① 目录建单带 slaPolicyId → deadline 非空且按该策略 resolveHours；② 纯计次权益目录建单 → usedTickets 仅 +1；③ code 符合 TK{YYYYMM}{SEQ4}）→ 修复后绿 + 既有 catalog 测试零回归
      - Skill: `nop-testing`

Exit Criteria:

- [x] 新测试先红后绿；cs 模块测试全绿

### Phase 8 — owner docs + 全量验证 + 状态回填 + 收官

Status: completed
Targets: `docs/design/crm/*`、`docs/design/customer-service/*`、`docs/audits/check/ai-check-index.md`、`docs/backlog/ai-check-roadmap.md`、`docs/logs/2026/09-12.md`（跨日则当日文件）、`docs/testing/known-good-baselines.md`
Skill: `nop-testing` + `closure-audit-prompt`

- Item Types: `Fix`（文档/索引/roadmap）+ `Proof`（全量验证）

- [x] Fix: owner docs 实现注记——territory.md（管道三段统一口径 + OPPORTUNITY + 期间过滤近义口径）、lead-waterfall.md（ConvLog 事件补写 + won 过滤 + 哨兵日志对销售周期/阶段停留时长的语义漂移注记）、sales-sequence.md §3（逾期深度语义）、cpq.md（类别匹配语义）、entitlement.md/csat.md/ticket.md（SLA 挂载序 + 分母口径 + 目录 SLA 闭环）
- [x] Proof: 全量验证——`mvn test -pl module-crm/erp-crm-service` + `-pl module-cs/erp-cs-service` 全绿 + `mvn clean install -DskipTests` 156 模块 + `mvn test` 全 reactor 与 `f213-mnt-hr-p1` 基线（4095/0/0/1）一致或更优 + `bash docs/audits/nop-compliance-checker.sh`（新增 daoFor 站点按 per-site raise 或收敛实现）+ CJK `--strict` + i18n checker + E2E crm/cs 相关 spec（crm lead/funnel、cs ticket/csat/reopen 至少 4 spec）全绿；crm/cs `_cases` 若现快照漂移按 F2.13 先例重录并声明（预期零漂移：本批不改任何既有测试覆盖的行为路径的输出值，新增行为全为缺陷路径修正——执行期实跑裁决）
      - Skill: `nop-testing`
- [x] Fix: 索引七行 → `fixed`（附测试与 plan 指针）；roadmap F2.14 → `done`；基线行登记（如全量数字变化）；日志条目
- [x] Fix: git 提交（用户指令：每 plan 完成后主动提交一次）

Exit Criteria:

- [x] owner docs 注记与代码一致；全量验证全绿或漂移按程序登记；索引/roadmap/日志回填完成

## Execution Record

（执行会话逐 Phase 证据，2026-09-11/12，基线 `f90ad34a0`）

- **Phase 1（crm2-002）**：红 = 缺陷复现测试 `testCategoryScopedRuleNotMatchWithoutCategoryContext`（类别限定规则退化为全局误命中）→ `resolvePrice` 8 参重载 + `ruleMatchesProduct(rule, productId, productCategory)`（rule 类别非空须与上下文 code 相等，上下文缺失 fail-closed）+ 调用方 `resolveProductCategoryCode`（ErpMdMaterial→category to-one→code，单产品单读）+ view.xml 移除 customerCategory 编辑列 ×2 → `TestPriceRuleEngine` 13/13 绿（+4 类别矩阵）。
- **Phase 2（crm2-001）**：红 = `testOnTimeAdvanceDoesNotMisreportOverdue`（dueDays=[1,1,1,10,10] 按期推进第 5 步误报 3 步）→ 反序扫描 + 预计算累计 dueDays + 当步未逾期即 0 → 10/10 绿（含既有真逾期用例）。
- **Phase 3（crm-002）**：红 = `testFunnelCapturesTerminalEventsAndWonSingleCount`（七月丢失 totalLost=0 漏计 + won 双计）→ doLose/doCancel/markLeadConverted 补写哨兵 ConvLog + computeHeader won 仅计 OPPORTUNITY → 11/11 绿。哨兵语义核验：entry/exit distinct 计数天然自洽（丢失/转化 lead 确实离开该阶段），computeStageMetrics 无需豁免；TestErpCrmLeadConversion 3 方法快照重录（ConvLog 新增行 + 序列漂移）+ CHECKING 绿。
- **Phase 4（crm-001）**：红 = `testCompanyPipelineAllRowsOpportunityOnlyPeriodScoped`（实际段 999=未分配 LEAD 被误计 + 预测段无期间过滤）→ accumulatePipeline 三段修复（公司级全行 / OPPORTUNITY 过滤 / 预测段 periodLabel→ForecastPeriod.id 两步 / 实际段 ConvLog 期间归因）→ `TestErpCrmForecastTerritoryRollup` 3/3 绿（金额 800/3000/0 精确断言）。**执行期新立 P2-CK-crm-027**：DEBUG 实证单次 refreshForecast 写两条相同公司行（既有缺陷，非本批引入；测试改用区域级管道避开，登记归 F3.5）。既有测试 `TestErpCrmTerritoryQuota#testGetTerritoryPipelineReturnsThreeSections` 依新契约修 fixture（补期间行 + ConvLog 事件种子）。执行期修正 1 项：parsePeriodLabel 支持 yyyy-Qn 季度格式（首版仅月度致 "2026-Q3" NumberFormatException）。
- **Phase 5（cs-001）**：红 ×2（NPE on null-hours 策略 + maxResolutionTime 被策略 48h 覆写）→ matchAndAttachSla 重写（生效策略单一化 + null 安全 + 覆盖恒胜出 + loadPolicy）→ `TestErpCsTicketSlaCsat` 16/16 绿（+3）。
- **Phase 6（cs-002）**：红 = `testAgentCsatBreakdownExcludesUnrespondedSurveys`（surveyCount=2/avgCsat=2.5）→ 双站点 notNull(respondedAt) + 分项计数（SurveyAgg/SurveyAggregator/AgentAgg/TicketAggregator 四处）→ 5/5 绿；报表站点随全模块绿验证；既有 seedSurvey helper 补固定时刻 respondedAt（快照确定性）；testAgentCsatBreakdown 快照重录。
- **Phase 7（cs-003）**：红 ×3（deadline 恒 null / usedTickets 双计 2 / code=TK-millis 违规则）→ computeAndAttachDeadline（生效策略 calculate + maxResolutionTime 覆盖写入 ticketData）+ matchAndConsumeEntitlement catalogItemId 守卫 + 移除显式 code → `TestErpCsServiceCatalog` 18/18 绿（+3）；全类 `_cases` 重录（deadline 新行为 + codeRule 编号，DEADLINE_DATE_TIME 框架自动掩码 `@var:`）。TestErpCsServiceCatalog 全类补 `@BeforeEach seedCodeRule()`（模块测试环境无部署期 cs-ticket-code 规则种子）。
- **Phase 8（验证与收尾）**：module-crm 198/0/0 + module-cs 193/0/0 + `mvn clean install -DskipTests` 156 模块 BUILD SUCCESS + compliance checker exit 0（R2c 1551=机器块，per-site raise 1544→1551 双写+注记）+ CJK `--strict` PASS + i18n PASS + E2E crm-lead/cs-ticket/cs-canned-response 5/5 passed。app-erp-all C15/C16 快照漂移（ConvLog 序列 + cs-002 报表传播）重录 + 毫秒竞态字段（startDateTime/endDateTime/duration/sentAt 等）按 B4-B7 先例 `*` 通配，C15 三连跑 + C16 三连跑稳定。**C16 input 种子声明（结束审计 m-5）**：C16 重录时 `input/tables/erp_cs_survey.csv` 删除 1 行未响应调查样本——行为等价性成立（新分母语义下删/不删该行报表输出相同：回复数 1、均值 5/9，录制时控制变量意图）；该删除方向与 Non-Goal「种子补样 Deferred」相反的一面如实登记：cs-002 过滤路径的集成证明由模块级测试 `testAgentCsatBreakdownExcludesUnrespondedSurveys` 承载。同型 C10 mnt 快照竞态字段通配为范围外既有 flake（本批未触碰 mnt，日志已披露）。**全 reactor 终值（结束审计 M-1 澄清，净环境重跑持久化日志 `/tmp/f214-reactor-final.log`）**：BUILD SUCCESS exit 0，41 模块 Results 聚合 **4109/0/0/1** = 基线 4095 + 本批净增 14 测试（4+2+1+3+1+3+1+... 分项见各 Phase）精确一致（此前聚合 4094 为磁盘满中断 run 陈旧 surefire 报告混入，已由净环境重跑澄清）。DOM/像素 E2E 快照零触碰（`git status tests/e2e/visual/**-snapshots/` 空）。

## Draft Review Record

- Independent draft review iteration 1: `acceptable after minor revision`（agent `agent_22ccf5ea-780f-469e-a8c8-473121c03382`，2026-09-11）——7 finding 事实全部实仓证实、技术方案逐点兼容、规则 4 合并不拆分成立；1 Major（M-1 crm2-002 类别来源数据模型前提失真：ErpMdPartner 无 customerCategory 列/产品类别为 categoryId FK/cpq.md 自由字符串语义）+ 5 Minor（Phase 7 对冲与不可行旗标、预测期间两步形态未写明、哨兵伴生漂移未登记、行号漂移、技能名指向）。全部并入修订。
- Independent draft review iteration 2: `accept`（同审查者复核：六处修订全部闭合，修订新引入声明 6 项全部实证通过；残留提示 3 项——Goals 行陈旧、Targets 枚举不全、respondedAt 对冲已无必要——已在激活前顺手落实）。

## Closure Gates

- [x] 范围内行为完成：F2.14 七条 finding 全部 `fixed` 终态（每条带失败测试先行证据）
- [x] 相关文档对齐：crm/cs owner docs + ai-check-index.md + roadmap + known-good-baselines + logs
- [x] 已运行验证：Phase 8 全量命令清单
- [x] 无范围内项目降级为 deferred/follow-up（Non-Goal 清单外无遗留）
- [x] 独立草案审查已完成并记录
- [x] 文本一致性已验证：状态、阶段、门控和日志都一致
- [x] 结束审计由独立子代理（新会话）执行；执行者未自我审计且未将此留为 `[ ]` 作为人工门控占位符
- [x] 结束证据存在于文件中（本计划 Execution Record + Closure 段）

## Deferred But Adjudicated

### crm-001 实际段转化时间精确口径

- Classification: `watch-only residual`
- Why Not Blocking Closure: 以「期间内有 ConvLog（含 Phase 3 丢失/转化事件）且当前 CONVERTED」近义，跨期归属误差窗口为一个期间；精确转化时间轴 = P2-CK-crm2-003 successor 面
- Successor Required: `yes`（触发条件：P2-CK-crm2-003 预测准确率计划落地转化时间轴时一并收敛）

### cs-002 E2E 种子补未响应调查样本

- Classification: `optimization candidate`
- Why Not Blocking Closure: 口径固化由模块级测试承载；种子变更牵动 cs 域 value spec/dashboard 像素断言与 seed 计数快照重录链
- Successor Required: `yes`（触发条件：E2E 面需直接固化聚合口径时随批重录）

### cs-003 SLA 暂停/恢复语义

- Classification: `out-of-scope improvement`
- Why Not Blocking Closure: 现体系无暂停语义（工单重开已有独立 P2-CK-cs-004 finding 归 F3.5 批）
- Successor Required: `no`（运营需求出现时立项）

### crm2-002 customerCategory 维度（数据模型缺失）

- Classification: `watch-only residual`
- Why Not Blocking Closure: `ErpMdPartner` 无 customerCategory 列、语义最近似的 `customerGroup` 不同一——规则列无数据承载不可实施；误配置陷阱经 view.xml 隐藏列 + owner doc 降级注记消除（finding 自带出口）
- Successor Required: `yes`（触发条件：partner 类别数据模型落地时随批实施匹配 + 恢复前端列）

## Closure

Status Note: 本计划可闭合——F2.14 七条 P1 finding 全部 `fixed` 终态（每条失败测试先行红→绿），执行期新立 P2-CK-crm-027 归 F3.5；全量验证矩阵全绿（build 156 + 全 reactor 4109/0/0/1 + checker R2c=1551 双写 + CJK/i18n PASS + E2E 5/5）；owner docs 六处 + 索引/roadmap/基线/日志七面对齐。

Closure Audit Evidence:

- Auditor / Agent: 独立子代理（fresh session）`agent_8eb5fa01-9d79-4fbd-a2b2-20db0abdb1c5`，2026-09-12
- Evidence: 结束审计裁决 **`passes closure audit`**（0 Blocking；2 Major = M-1 全 reactor 聚合 4094 与净增算术 ≥4109 缺口 15 须澄清 + M-2 提交时序；6 Minor）。落账处置：M-1 已澄清——净环境清理 surefire 残留后重跑全 reactor，持久化日志 `/tmp/f214-reactor-final.log`（BUILD SUCCESS exit 0），41 模块 Results 聚合 **4109/0/0/1** 与 4095+14 算术精确一致（4094 系磁盘满中断 run 的陈旧 surefire 报告混入聚合）；M-2 本次提交即落账。Minor 落账：m-1 计划状态/复选框回填（本段）；m-2 roadmap 头部推进；m-3 app-erp-all 数字修正 73/0/1/1→73/0/0/1；m-4 基线行变更面幽灵文件名清理；m-5 C16 input 种子删除声明（Execution Record）；m-6 E2E 无持久产物（与 F2.13 同形态，快照零变更已独立核实）。审计报告 8 项核查全 PASS。
