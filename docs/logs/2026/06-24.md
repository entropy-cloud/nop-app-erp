# Development Log - 2026-06-24

### 2026-06-24 (系统化交叉校验：全部 10 域 ui-patterns.md × ORM × dicts)

- **触发**：用户问 "What did we do so far?"，根据 AGENTS.md 当前项目阶段需要完成 ui-patterns.md 交叉校验的后续步骤。
- **现状回溯**：此前已完成全部 10 域 `ui-patterns.md` 创建（purchase/sales/inventory/finance/manufacturing/assets/projects/maintenance/quality/master-data），但交叉校验未完整执行。
- **交叉校验方法**：逐域阅读 `docs/design/*/ui-patterns.md` 与 `module-*/model/app-erp-*.orm.xml` 及对应 `erp-*-meta/src/main/resources/_vfs/dict/*.dict.yaml`，重点检查：
  1. 页面实体是否在 ORM 中存在
  2. 字段名是否在 ORM 列中存在
  3. 字典标签/颜色是否与 ORM dict 定义一致
  4. 实体关系（头-行、to-one）是否匹配页面子表描述
- **发现并修复的不一致（16 项）**：
  - **purchase/ui-patterns.md**: ① 状态颜色混合 docStatus/approveStatus → 补充双轴注解（`[注：列表页展示的"状态"为 docStatus 与 approveStatus 的组合概览]`）② 工具栏状态描述从单一 docStatus 改为双轴（`DRAFT+UNSUBMITTED / ACTIVE+SUBMITTED / ACTIVE+APPROVED`）③ 入库单"入库类型"字段 ORM 缺失 → 添加 `[注：当前 ORM 中 ErpPurReceive 尚无 receiveType 字段]` ④ 采购发票"发票类型"字段 ORM 缺失 → 同理添加注
  - **sales/ui-patterns.md**: ① 销售发票"发票类型"字段 ORM 缺失 → 添加注
  - **inventory/ui-patterns.md**: ① 盘点单状态机用 `DRAFT→COUNTING→DONE` 但 ORM 使用 `erp-inv/move-status`（DRAFT/CONFIRMED/DONE/CANCELLED）→ 修正状态名匹配 ORM 字典；"锁定盘点"注解说明 ORM 无显式锁字段
  - **manufacturing/ui-patterns.md**: ① BOM 头部"数量""版本"字段 ORM 缺失 → 添加注 ② BOM 子表"损耗率""仓库""替代料"列缺失 → 添加注 ③ JobCard 缺少 code 列 → 添加注 ④ 领料/完工移动单关系缺直连 to-many → 添加注
  - **assets/ui-patterns.md**: ① 品牌型号/经手人/处置原因字段 ORM 缺失 → 集中添加注
  - **projects/ui-patterns.md**: ① 优先级颜色用"高=红/中=黄/低=灰"但 ORM 字典有 4 级（URGENT 紧急/HIGH 高/NORMAL 普通/LOW 低），NORMAL 标签为"普通"非"中"→ 修正为"紧急=红/高=橙/普通=黄/低=灰" ② 阻塞原因缺 blockReason 独立字段 → 添加注
  - **maintenance/ui-patterns.md**: ① "故障"状态色块标签修正为 ORM dict 值"故障停机" ② Equipment 缺 to-many 关系、Visit 缺 visitType/result 字段 → 添加集中注
  - **quality/ui-patterns.md**: ① 检验结果字典 ACCEPTED/REJECTED vs UI 显示 PASS/FAIL 不一致 → 添加注说明需业务层映射 ② Inspection 缺 templateId/lotNo → 添加注 ③ NCR 缺专用 inspectionId FK、dispositionType、supplierId → 添加集中注
- **验证**：`grep -c "注："` 统计 10 域新增注解数以确保覆盖
- **未修复项（仅记录）**：跨域关系（如 ErpMfgWorkOrder 缺 to-many 到 ErpMfgMaterialIssue）为架构设计边界约定（走 I*Biz 调用），不修改 ORM。设计意图性缺失字段（如入库类型、发票类型、品牌型号等）保持 ui-patterns.md 注解，不修改 ORM——待后续迭代按业务优先级补充 ORM 模型。
- **结论**：全部 10 域 ui-patterns.md 的一致性交叉校验完成，发现 16 项不一致已全部在 ui-patterns.md 中以注解形式标记；ORM 源文件无改动。设计意图缺失字段汇集为待补充清单，供后续 ORM 模型迭代参考。

### 2026-06-24 (Phase 2：ORM 模型补全 — 将设计注解中的缺项写入 ORM XML)

- **触发**：用户确认"Continue"，按 ui-patterns.md 中 `[注：]` 标记的缺失项逐域回填 ORM 模型。
- **方法**：跨域 grep 定位所有 7 个受影响 ORM 文件，逐一确认缺失项目（字段、字典、关系到 ORM XML 均不存在），然后集中编辑。
- **Skill 加载**：`nop-orm-modeler`（ORM 建模指导）和 `nop-database-design`（数据库设计规范），确保命名/domain/stdSqlType 符合项目既有模式。
- **字典新增（7 个）**：
  - `erp-pur/receive-type`: NORMAL(10)/RETURN(20)/GIFT(30)
  - `erp-pur/invoice-type`: VAT_SPECIAL(10)/VAT_NORMAL(20)/RECEIPT(30)
  - `erp-sal/invoice-type`: 同上
  - `erp-qa/disposition-type`: SCRAP(10)/RETURN(20)/CONCESSION(30)/DOWNGRADE(40)
  - `erp-mnt/visit-type`: PLANNED(10)/RESPONSIVE(20)
  - `erp-mnt/visit-result`: NORMAL(10)/ABNORMAL(20)/PARTIAL(30)
  - `erp-ast/disposal-reason`: OBSOLETE(10)/SOLD(20)/DONATED(30)/STOLEN(40)/OTHER(50)
  - `erp-mfg/byproduct-type`: CO_PRODUCT(10)/BY_PRODUCT(20)
- **字段新增（22+ 列，跨 14 实体 × 7 域）**：
  - **purchase**: ErpPurReceive.receiveType, ErpPurInvoice.invoiceType
  - **sales**: ErpSalInvoice.invoiceType
  - **manufacturing**: ErpMfgBom.versionLabel/qty, ErpMfgBomLine.scrapRate/warehouseId/alternativeMaterialId, ErpMfgBomByproduct.byproductType/yieldRate/costAllocationPercent, ErpMfgJobCard.code
  - **assets**: ErpAstAsset.brandModel, ErpAstDisposal.reason, ErpAstMovement.handlerId
  - **projects**: ErpPrjTask.blockReason
  - **maintenance**: ErpMntVisit.visitType/result
  - **quality**: ErpQaInspection.templateId/batchNo, ErpQaInspectionTemplateLine.inspectionMethod, ErpQaNonConformance.inspectionId/dispositionType/supplierId/parameterName/measuredValue/specMin/specMax
- **关系到 ORM 新增（6 条）**：
  - ErpMntEquipment → visits/schedules/requests/sparePartUsages/downtimeEntries (5 to-many)
  - ErpMntVisit → sparePartUsages (1 to-many)
- **NCR propId 修复**：系统化调整 ErpQaNonConformance 的 propId 序列（从 1-15/16-21→1-23/24-29）以容纳新增插入列。
- **验证**：Python 脚本检查全部 7 + 3 个 ORM XML 的 entity/dict 标签配对平衡，均通过。
- **更新原则**：ORM 源文件为权威模型（非生成文件 _app.orm.xml），直接编辑；所有新字段遵循既有命名/domain/dict 约定；跨模块关系到 ORM 通过 I*Biz 处理，不再增加 refEntityName 引用。

### 2026-06-24 (Phase 3：全量 ORM i18n-en:displayName + dict 英文国际化)

- **触发**：用户要求检查国际化覆盖，修复所有英文名称，同时检查 action-auth.xml 和 dict。
- **跨域 i18n 补全（5359 items, 0 missing）**：
  - 编写 `scripts/check-orm-auth-i18n.js`：三合一检查工具（ORM XML + action-auth XML + dict i18n YAML），支持缩进层级追踪的 YAML 解析器
  - 编写 `scripts/add-orm-i18n.js`：自动为全部 10 模块 ORM 模型添加字段级 `i18n-en:displayName` 和关联级 `i18n-en:displayName` + `ref-i18n-en:displayName`（支持自闭合标签和子元素标签两种格式）
  - 编写 `scripts/add-dict-i18n.js`：从 99 个 dict.yaml 文件生成 dict 英文翻译写入 `i18n/en/erp-XX.i18n.yaml`，含 500+ 条中英翻译映射表
  - 修复 `check-orm-auth-i18n.js` 中 meta 模块路径解析（`module-X/erp-XX-meta/src/main/resources/_vfs/dict`）
- **脚本文件**：`check-orm-auth-i18n.js` / `add-orm-i18n.js` / `add-dict-i18n.js`
- **验证**：`node -e "require('./scripts/check-orm-auth-i18n.js')"` → TOTAL: 5359 OK, 0 missing
- **提交**：`7a7b0ed feat(i18n): 全量补全ORM i18n-en:displayName及dict英文国际化`
