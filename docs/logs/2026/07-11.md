# Development Log - 2026-07-11

### 2026-07-11（AMIS 前端显示质量收尾——审批按钮/系统字段隐藏/外键名称解析/看板名称解析 — plan 2026-07-11-1643-1）

- 执行 `docs/plans/2026-07-11-1643-1-amis-frontend-quality.md`，Phase 3+4 收尾完成（Phase 1+2 此前已完成）。分析报告 §4.2 P1×3 + P2×1 全部 ✅。
- **Phase 3（BizLoader 批量加载测试补齐）**：新建 `TestErpSalFkNameLoader`（2 测试：ErpSalOrder customerName/warehouseName/currencyName/orgName + ErpSalInvoice customerName/currencyName/orgName）+ `TestErpPurFkNameLoader`（2 测试：ErpPurOrder supplierName/warehouseName/currencyName/orgName + ErpPurInvoice supplierName/currencyName/orgName）。经 `IGraphQLEngine` findList + `FieldSelectionBean` 触发 `@BizLoader` 批量加载，验证名称对齐 master-data（防 N+1 + 名称正确）。
- **Phase 4（看板外键名称解析后端 join + 前端 adaptor）**：
  - purchase `ErpPurDashboardBizModel`：`findVendorTopN`/`findThreeWayMatchDiffAlert`/`findApOverdueAlert` join ErpMdPartner 增 supplierName/partnerName；`main.page.yaml` chart adaptor 改 `r.supplierName || '供应商#' + r.supplierId`，CRUD 列改 *Name。
  - inventory `ErpInvDashboardBizModel`：`findWarehouseDistribution` join ErpMdWarehouse 增 warehouseName；`findShortageAlert`/`findSlowMovingAlert`/`findBatchExpiryAlert` 增 materialName+warehouseName（批量加载 helper loadMaterialNames/loadWarehouseNames）；`main.page.yaml` chart adaptor 改 `r.warehouseName || '仓库#' + r.warehouseId`，CRUD 列改 *Name。
  - assets `ErpAstDashboardBizModel`：`getAssetCategoryDistribution` join ErpAstAssetCategory 增 categoryName（helper loadCategoryNames）；`main.page.yaml` chart adaptor 改 `r.categoryName || '类别#' + r.categoryId`。
  - sales `ErpSalDashboardBizModel`：`findArOverdueAlert` 增 partnerName（补齐此前仅 chart 改的遗漏）；`main.page.yaml` CRUD 列改 partnerName。
  - cs 看板页不存在（无前端页面无后端 BizModel），无工作项。
  - 测试更新：TestErpPurDashboard.testVendorTopN 断言 supplierName；TestErpInvDashboard.testWarehouseDistribution 断言 warehouseName（增 seedWarehouse helper）；TestErpAstDashboard.testAssetCategoryDistribution 断言 categoryName（增 seedCategory helper）。
- **验证（全绿）**：`mvn clean install -DskipTests` **154 模块 BUILD SUCCESS**；4 域 service `mvn test` **78 tests 0 failures/0 errors**（sales 8+purchase 10+inventory 8+assets 5 + 各域既有测试全绿）。
- **文档**：分析报告 §4.2 P1×3 + P2×1 状态 ⏳→✅；前序 plan 1225-1 Deferred 标 successor 已完成。

### 2026-07-11（销售发票审批 session-closed 修复——ErpSalInvoiceProcessor 对齐 Pur 范式）

- **问题**：`TestErpSalInvoiceApproval`（2 failures）+ `TestErpSalCreditHoldOnInvoice`（2 failures）共 4 测试失败，`ErpSalInvoice__approve` 返回 status=-1。
- **根因**：`ErpSalInvoiceProcessor.doApprove()` 在 `doPosting()`（经 `voucherBiz.post()` 的 `@Transactional(REQUIRES_NEW)` + `@SingleSession`）之后访问 `invoice.getId()` 重载实体。REQUIRES_NEW 事务回滚后 `orm.session_clear` 清空当前线程 Session，实体变为 detached 状态，`getId()` 触发 `OrmEntity.requireEnhancer` 抛 `nop.err.orm.session-closed`。对照 `ErpPurInvoiceProcessor`（同模式测试全绿）：Pur 在 `approve()` 中先 `doPosting` 再用 `String id` 参数重载，不触碰已分离实体。
- **修复**：`ErpSalInvoiceProcessor.approve()` 对齐 `ErpPurInvoiceProcessor.approve()` 范式——`doPosting` 上移至 `approve()`、用 `id` 参数重载、`doApprove` 签名改为 `(ErpSalInvoice, boolean posted, IServiceContext)` 仅负责状态推进 + 持久化。`module-sales/erp-sal-service/.../ErpSalInvoiceProcessor.java` 2 处编辑。
- **验证（全绿）**：`mvn test -pl module-sales/erp-sal-service -Dtest=TestErpSalInvoiceApproval,TestErpSalCreditHoldOnInvoice` **8 tests 0 failures**；全仓库 `mvn test` **BUILD SUCCESS**（154 reactor 模块全绿）。

### 2026-07-11（客服效能子域收尾——预设应答变量渲染/宏匹配/插入 + 客服绩效聚合看板 — plan 2026-07-11-1234-2）

- 执行 `docs/plans/2026-07-11-1234-2-cs-canned-response-agent-performance.md`，Phase 1-4 全部完成。解除 UC-CS-07（预设应答变量渲染）+ UC-CS-10（客服质量监控）Non-Goal。
- **Phase 1（渲染引擎 + 宏匹配）**：新建 `CannedResponseRenderer` 纯函数式工具（占位符替换 + JSON variableDefs 必填校验 + 合并覆盖，不注入 Dao 可独立单测）+ `IErpCsCannedResponseBiz` 加 3 方法声明 + `ErpCsCannedResponseBizModel` 扩展 `renderTemplate`（系统变量 customer_name/ticket_id/agent_name/today/now 自动解析 + customVars 覆盖）+ `suggestForTicket`（三级匹配精确>类型>全局兜底，内存过滤因 XMeta 不支持 isNull）+ 3 ErrorCode（NOT_FOUND/INACTIVE/REQUIRED_VAR_MISSING）+ 3 配置键（canned-response-enabled/macro-count/category-max-depth）。
- **Phase 2（插入流程 + 审计）**：`applyCannedResponse` @BizMutation——校验 active→渲染→usageCount+1→TicketAction NOTE 审计写入→返回渲染后 content。
- **Phase 3（客服绩效看板）**：新建 `ErpCsQualityDashboardBizModel`（服务型 BizObject，镜像 ErpFinDashboardBizModel 域隔离范式，经 IDaoProvider/IOrmTemplate 内存聚合）+ 3 @BizQuery：getDashboardKpi（SLA 达标率/平均解决时长/超时工单数/总工单数/平均首次响应时长）、getTeamSlaRanking（经 slaPolicy→teamId→team.name 关联按团队分组，对齐 sla.md §4.3）、getAgentCsatBreakdown（按 assignedToId 分组 CSAT/NPS/CES 均值）+ AMIS 看板页面（form 区间筛选 + service KPI 卡片 + chart 团队 SLA 排名柱状图 + crud 客服 CSAT 明细列表）+ action-auth 菜单注册（cs-quality-dashboard orderNo=50）。
- **Phase 4（测试）**：TestCannedResponseRenderer 9 cases（纯 JUnit）+ TestErpCsCannedResponseBiz 7 cases（renderTemplate 系统变量 + suggestForTicket 三级匹配 + applyCannedResponse usageCount+action 审计 + inactive 拒绝）+ TestErpCsQualityDashboard 4 cases（KPI 空集/聚合/团队排名/客服 CSAT）+ cs-canned-response.action.spec.ts 1 E2E（GraphQL suggestForTicket + renderTemplate + applyCannedResponse 全栈可达 + callQuery helper 新增）。
- **验证（全绿）**：`mvn clean install -DskipTests` **154 reactor 模块 BUILD SUCCESS**；`mvn test -pl module-cs/erp-cs-service` **95 测试全绿**（+20 新增）；`xmllint --noout erp-cs.action-auth.xml` well-formed；YAML 可解析；TypeScript E2E spec `npx tsc --noEmit` 无错。
- **文档**：canned-response.md 标注 §一~§三 已实现；sla.md §四 标注看板 KPI 已实现；extended-roadmap.md UC-CS-07/UC-CS-10 标 ✅ done。
- **Deferred**：预设应答 AMIS 前端插入流程页面（工单详情页侧栏「推荐应答」+ 渲染预览弹窗，前端 successor）+ 分类树最大深度/成环校验（category-max-depth 本期仅声明不强制）+ SLA 独立 .xpt.xml 报表模板（看板聚合已覆盖核心管理视角）。

### 2026-07-11（剩余 DIRECT 业务动作浏览器层 E2E——库存物理冲销 / 域 reverseApprove / Recall generateReturns — plan 2026-07-11-1234-1）

- 执行 `docs/plans/2026-07-11-1234-1-remaining-direct-business-action-e2e.md`，Phase 1+2+3 全部完成。解除 2004-2 两项 Deferred（reverseApprove DIRECT 子集 + 库存物理冲销浏览器层）+ 0335-1 一项 Deferred（Recall generateReturns 跨域建退货单编排）。
- **Phase 1（库存物理冲销）**：新建 `inventory-stock-move-reverse.action.spec.ts` 2 tests——`generateMove`(INCOMING)→`complete`→DONE→`reverse` 产生 OUTGOING 冲销移动单（docStatus=DONE + relatedBillType=REVERSAL + relatedBillCode 挂链 + originReturnedMoveId 追溯 + moveType/source/dest 互换）；非 DONE 守卫（CONFIRMED → ERR_REVERSE_NOT_DONE）。plan 原述 `confirm` 置 DONE 经 live code 核实修正为 `complete`（confirm 为 DRAFT→CONFIRMED 内部步骤，complete 为 CONFIRMED→DONE）。
- **Phase 2（域 reverseApprove DIRECT 轴）**：新建 `p2p-reverse-approve.spec.ts` 1 test——`runP2pChain` 产 posted Invoice → `ErpPurInvoice__reverseApprove` → approveStatus REJECTED + posted=false + postedAt/postedBy null + REVERSAL 红字凭证生成 + 原 NORMAL 凭证 isReversed=true。与 p2p-reverse.spec.ts（finance-initiated ErpFinVoucher__reverse → 域监听者回退）互补——入口不同但回退目标态一致。
- **Phase 3（Recall generateReturns 跨域建退货单）**：新建 `quality-recall-generate-returns.action.spec.ts` 1 test——自包含批次追溯编排（ErpInvBatch + INCOMING 批次备货 line.batchNo + ErpSalDelivery+Line 供 pickUoMId + OUTGOING 业务联动批次出库 relatedBillType=ERP_SAL_DELIVERY line.batchNo → batchTrace 追溯 → RecallTargetLocator.isSalesOutbound 命中）→ register→submit→approve→locateTargets(→IN_PROGRESS)→notifyCustomers→generateReturns → RecallTarget RETURNED + generatedReturnId 非空 + ErpSalReturn 创建（customerId=target.partnerId 匹配 + docStatus=DRAFT）。关键裁决：RecallTargetLocator 经 batchTrace（查 StockMoveLine.batchNo + StockLedger.batchNo）追溯批次出库移动，非直查 ErpSalDelivery；故须建立批次维度库存余额（INCOMING携batchNo→complete→DONE写余额）再出库（OUTGOING携batchNo→validateAvailable按batchNo余额键匹配通过）。
- **验证（全 workspace 无回归）**：`npx playwright test`（BASE_URL=http://127.0.0.1:8011）**184 passed**（180 基线 + 4 新增），0 failures（23.7m）。
- **文档**：`e2e-runbook.md` 增 3 新 spec 行 + 测试数 180→184；2004-2/0335-1 Deferred 标记承接 done。
- 纯测试新增，零生产代码/契约/ORM 模型变更。

### 2026-07-11（分析报告一致性修复——文档计数同步 + 代码质量小修 + findAll 改造 + 报告订正 — plan 2026-07-11-1225-1）

- 执行 `docs/plans/2026-07-11-1225-1-analysis-consistency-fixes.md`，4 Phase 全部完成，验证全绿（根 `mvn clean install -DskipTests` **154 reactor 模块 BUILD SUCCESS**；10 域 Dashboard/Report + CrpLoad 测试 **158 项 0 failures/0 errors**）。
- **Phase 1（文档计数同步 + reactor 口径裁决，无代码变更）**：reactor 口径裁决 = 根 `mvn validate` 完整 reactor **154**（含根 pom + app-erp-all + 18 业务域与 notify 子模块链；`find module-* -name pom.xml -maxdepth 3`=152 不含根与 app-erp-all）。`data-dependency-matrix.md:12` 279→447 实体并改"18 业务域 + 1 跨域通知派发子系统（module-notify）"口径；`product-scope.md:52/55/71` 1721→2758 Java / 146→154 reactor / 279→447 实体；`codebase-map.md:11` 154 保留 + 补 reactor 口径权威说明（含 find 152 差异解释）；`api-response-conventions.md:3` 删模板占位符；附带修正 `project-vision.md:41` 与 `competitive-comparison.md:165` stale 279→447。logs/plans 历史快照 146 保留（追加性历史，不改写）。
- **Phase 2（代码质量小修 3 处）**：`ErpHrRecruitmentBizModel.java:199` `System.currentTimeMillis()`→`CoreMetrics.currentTimeMillis()`（import 已存在）；2 处 mnt 测试 `System.out.println("[DEBUG confirm]...")` 调试残留删除。`mvn -pl hr,mnt -am test-compile` BUILD SUCCESS。
- **Phase 3（全仓库 findAll() 改造 10 域 25 处）**：决策四类——(A) 计数 → `dao.countByQuery(q)`；(B) Σ 聚合 → `QueryFieldBean.mainField(metric).sum().alias()` + `ormTemplate.findListByQuery(q)` DB 级聚合（实测无维度全局聚合会被 MdxQueryExecutor 强制注入主键维度生成非法 SQL `select sum(..), o.id` 无 group by，故全局 SUM 经按真实维度分组后汇总实现，null 维度单独成组被计入，等价）；(C) 单字段收集 → `findAllByQuery` + `setLimit(5000)` 硬上限；(D) 报表明细/配置数据 → `findAllByQuery` + `setLimit(5000)` + 注释裁决保留。额外修正同文件 `findAllByQuery().size()` 计数反模式 → `countByQuery`（conformance 规则）。覆盖：md(6)/inv(4)/prj(4)/mnt(2)/pur(1)/qa(1)/sal(1)/mfg-dashboard(1)/fin-dashboard(1) + crm-report(1)/fin-report(1)/mfg-report(1)/CrpLoadCalculator(1)。`grep "\.findAll()" module-*/erp-*-service/src/main/` 零命中。
- **Phase 4（分析报告自身订正）**：全报告 grep `279|332|2423|1721|167|18 vs 19|337` 逐行订正为 07-11 实测值（447/2758/154）；§2.5 残留表删除 INSPECTING/分布式事务两 stale 行（仓库已修）+ 加 2026-07-11 复核注；§1.6 findAll 表对齐 10 域 25 处 + 标注 Phase 3 落地；§1.7/§4.1 删 167 spec 计数→实时计数指引；§4.2 优先修复项表加"状态"列（✅ 落地 / ✅ stale / ⏳ 移出本批+successor）；§1.4 违规表加状态列；报告标题加维护说明注脚（2026-07-10 时间快照，最新计数见源文档）。
- **Non-Goals（移出本批，已命名 successor）**：AMIS 前端质量四项（审批按钮/审计字段/FK 显示 ID/Dashboard 图表 ID）→ 另开前端 owner plan；`SettlementAllocation` DTO 跨模块重复 → 另开代码重构 owner plan；`feature-inventory.md` 完成状态 + `roles-and-permissions.md` 权限映射 → 另开 owner-doc 对齐计划；sql-lib.xml 聚合查询范式引入（本批用平台原生 QueryBean + projection 已足够）；Playwright spec 125 vs 167 git 考古（仅删报告数字）。
- 无 ORM/契约/认证变更；findAll 改造仅改 BizModel/Report/Calculator Java 内部取数路径，看板/报表对外 GraphQL 契约（getDashboardKpi 返回 Map 字段名/数值语义）不变。

### 2026-07-11（质量 NCR 过账生命周期浏览器层 E2E 扩展——reverseNcr 红字凭证行 + RETURN 处置退货编排 — plan 2026-07-11-0730-2）

- 执行 `docs/plans/2026-07-11-0730-2-quality-ncr-posting-lifecycle-e2e.md`，Phase 1+2 全部完成。解除 1800-1 一项 Deferred（NCR reverseNcr 红字凭证行断言）+ 0335-2 一项 Deferred（NCR RETURN 处置退货编排 RETURN 子集）。
- **Phase 1（reverseNcr 红字凭证行断言）**：新建 `quality-ncr-reverse-voucher-line.action.spec.ts`——createViaSave 建 NCR（SCRAP + materialId=1）→ submitReview → resolve（AUTO_POST 产 NCR_SCRAP 正向凭证）→ reverseNcr 红冲。复用 0704-1 范式 `findVoucherIdByBillCode(ncrCode, 'REVERSAL')` + `assertVoucherLines`。断言红字凭证行同向取负（Dr 6711=-120 / Cr 1401=-120，buildReversalDraft dcDirection 不变金额取负）+ 原正向凭证 isReversed=true（markOriginalVoucherReversed O-8 统一行为，凭证级回退标记）。价值增量区别于 1800-1 仅断言 ncr.posted=false（实体级回退）。
- **Phase 2（RETURN 处置跨域退货编排）**：新建 `quality-ncr-return-disposition.action.spec.ts`——createViaSave 建 NCR（RETURN + supplierId=3 SUP-001 + materialId=1）→ submitReview → resolve。resolve 触发 NcrReturnOrchestrator.orchestrateReturn → IErpPurReturnBiz.save 创建采购退货单（code=PR-FROM-NCR-{ncrId}）+ ncr.returnCode 登记。断言 returnCode 格式 + ErpPurReturn 存在且 supplierId 匹配 + docStatus=DRAFT + approveStatus=UNSUBMITTED；CONCESSION 对照 returnCode=null 无退货副作用。无 CAPA 简化 setup 对齐 0335-2 无 CAPA 路径范式。
- **验证（business-actions 全绿）**：`npx playwright test tests/e2e/business-actions tests/e2e/orchestration`（BASE_URL=http://127.0.0.1:8011 SKIP_WEBSERVER=1）**44 passed**（含新增 3 test），0 回归。全 workspace 运行 88 failures 全在 visual/reports/crud/dashboards（AMIS UI 渲染超时 TimeoutError locator.waitFor/click/fill，预存环境问题非 DB 污染——business-actions+orchestration 数据完整性套件全绿）。
- **文档**：`e2e-runbook.md` 增 reverseNcr/RETURN spec 行 + 红字 NCR_SCRAP 凭证行期望值表 + 测试数 177→180；`backlog/README.md` 增 0730-2 工作项行。
- 纯测试新增，零生产代码/契约/ORM 模型变更。

### 2026-07-11（制造域 config-gated 浏览器层 E2E——批次基因追溯 + 质检门控阻断完工 — plan 2026-07-11-0730-1）

- 执行 `docs/plans/2026-07-11-0730-1-manufacturing-config-gated-e2e.md`，Phase 1+2 全部完成。解除 1800-2 两项 Non-Goals（质检门控阻断完工 + 批次基因追溯浏览器层）+ 0704-2 两项 Deferred。
- **Phase 1（批次基因追溯浏览器层）**：新建 `mfg-genealogy.spec.ts`。`runMfgChain` 扩展 `withBatchTracking` 可选参数——创建输入 `ErpInvBatch`（batchNo=`GEN-IN-{ts}`）+ 备货/领料移动携 batchNo + MaterialIssueLine.batchNo 引用之。满量完工触发 `BatchGenealogyWriter.writeOnCompletion` 写入 inputLot→outputLot 基因链（`ErpMfgBatchGenealogy`）。断言 findPage 非空 + inputMaterialId(组件)/outputMaterialId(成品) 正确 + `forwardTrace(outputLotId)`/`backwardTrace(inputLotId)` 返回非空消耗关系。
  - **批次余额维度裁决**：`StockMoveBookkeeper.upsertBalance` 以 batchNo 为余额键维度之一（material×warehouse×location×batch×owner），故备货 INCOMING 移动须携同一 batchNo 使余额按批次维度落入，否则领料 OUTGOING 时可用量不足（available=0 < required=20）。输入 ErpInvBatch 在备货前创建（batchNo 唯一值），备货行+领料行共用之。
  - `cleanupMfg` 扩展：增 ErpMfgBatchGenealogy 清理(filter workOrderId) + 输出批次(filter batchNo=`FG-{woCode}`) + 输入批次(by id)。
- **Phase 2（质检门控阻断完工 Gate 1）**：新建 `mfg-inspection-gate.spec.ts`。webServer JVM args 追加 `-Derp-mfg.inspection-gate-enabled=true`（`playwright.config.ts:18`）。`runMfgChain` 扩展 `inspectionRequired` 可选参数——BOM 经 `__save` 设 `inspectionRequired=true`，链路停在 completeJob（WO=IN_PROCESS，跳过 reportCompletion）。
  - 负路径：spec 驱动 `reportCompletion`（满量 willFinish=true）被 `ErpMfgWorkOrderProcessor.isInspectionGated` 拦截抛 `ERR_INSPECTION_REQUIRED`（GraphQL errors 含「质检」语义 token）+ docStatus 保持 IN_PROCESS。
  - 对照路径：`inspectionRequired=false`（默认）→ reportCompletion 成功 COMPLETED（Gate 1 未命中，门控精确性）。
  - 回归安全：config 全局生效但仅对 BOM.inspectionRequired=true 的工单生效；既有 mfg-chain/mfg-variance spec 的 BOM.inspectionRequired 默认 false → Gate 1 不命中 → 0 回归。
- **helper 扩展**：`orchestration/_helper.ts` 增 `callMutation` 导出（供 spec 断言 GraphQL errors 负路径）+ `MfgChainOptions` 增 `withBatchTracking`/`inspectionRequired` + `MfgResult` 增 `inputBatch`。
- **验证（full-green）**：`npx playwright test --workers=1`（PLAYWRIGHT_PORT=8011）**177 passed**（174 基线 + 3 新测试：1 genealogy + 2 inspection-gate），0 回归，23.1m。orchestration 局部 7 passed（1.1m）。
- **文档**：`e2e-runbook.md` 增基因追溯/质检门控 spec 行 + config-gated 制造域子段 + 测试数 174→177；`backlog/README.md` 增 0730-1 工作项行。
- **Deferred**：基因追溯链可视化前端（echarts 树/图，1606-2 Deferred）+ 召回报告降级标记消除（inventory 域按批次查询 successor）。

### 2026-07-11（制造域扩展 E2E——WorkOrder 异常分支 + 部分完工 + 生产差异浏览器层 — plan 2026-07-10-1800-2）

- 执行 `docs/plans/2026-07-10-1800-2-manufacturing-exception-variance-e2e.md`，Phase 1~3 全部完成。解除 0704-2 三项 Deferred（WorkOrder 异常分支 stop/resume/cancel + 部分完工 willFinish 门控 + 生产差异计算浏览器层）。
- **Phase 1（WorkOrder 异常分支 stop/resume/close/cancel + 非法迁移守卫）**：新建 `mfg-work-order-exception.action.spec.ts`——复用 0335-1 范式（DIRECT 审批轴 + 简单 BOM 无子件 → checkAvailability → STOCK_RESERVED → start → IN_PROCESS）。正路径 A: stop(IN_PROCESS→STOPPED)→resume(→IN_PROCESS)→close(→CLOSED+actualEndDate 回填)；正路径 B: stop→close(从 STOPPED 可达)；正路径 C: cancel(NOT_STARTED→CANCELLED)；非法迁移守卫: IN_PROCESS→cancel 拒绝(cancel 仅允许 DRAFT/SUBMITTED/NOT_STARTED) + CLOSED→stop/resume 拒绝。每步 verifyState __get 独立断言 docStatus。清理仅逻辑删除 WO+BOM（异常分支无过账/库存产物）。
- **Phase 2（部分完工 willFinish 门控）**：同 spec 新增 test case——reportCompletion(completedQty=10 < plannedQty=20) → willFinish=false → docStatus 保持 IN_PROCESS + completedQuantity=10 → close → CLOSED（非 COMPLETED）。验证 reportCompletion 内部 `newCompleted >= planned` 门控不触发 willFinish 分支（不产生差异计算 + 不设 COMPLETED）。清理复用 cleanupMfg（部分完工已生成 MANUFACTURE 入库移动 10 件 + 成品余额）。
- **Phase 3（生产差异计算浏览器层 config-gated auto-calc）**：
  - Decision: webServer JVM args 追加 `-Derp-mfg.variance-auto-calc-enabled=true`（全局生效）。回归隔离裁决：MAT-001（mfg-chain.spec.ts 产品）无 FIRMED ErpMfgCostRollupLine → calculateVariances 抛 ERR_VARIANCE_NO_STANDARD_COST → Processor try/catch 吞异常不阻断完工；差异 spec 使用测试专用成品物料 + 运行时 FIRMED rollup 完全隔离。
  - 种子 COA 补齐：`erp_md_subject.csv` +5 差异科目行（1410 制造差异-材料 / 1412 制造差异-人工 / 1413 在制品-人工 / 1414 制造差异-制造费用 / 1415 在制品-制造费用）使 ProductionVarianceAcctDocProvider 科目码可达。
  - 新建 `mfg-variance.spec.ts`——自包含链路（测试专用成品物料 FINISHED_PRODUCT/STANDARD + FIRMED rollup mat90/lab5/ovh5/unit100 + runMfgChain 变体 → 满量完工 willFinish=true → calculateVariances 5 类差异行 + dispatchIfApplicable PRODUCTION_VARIANCE 凭证）。断言 ErpMfgCostVariance 5 行（MATERIAL_USAGE/LABOR_EFFICIENCY/LABOR_RATE/OVERHEAD/VOLUME）+ posted=true + PRODUCTION_VARIANCE 凭证 6 行精确数值（Dr 1410=100/Cr 1411=100/Dr 1412=50/Cr 1413=50/Dr 1415=50/Cr 1414=50，标准 vs 实际成本差额派生）。
  - `cleanupMfg` 扩展：增 ErpMfgCostVariance 清理(deleteByFilter workOrderId) + PRODUCTION_VARIANCE 凭证清理(cleanupVoucherByBillCode woCode+'-PV')。`runMfgChain` 增 `MfgChainOptions`(productId/productUoMId) 支持测试专用成品物料。MAT-001 链路无差异记录，cleanupMfg 扩展对 MAT-001 为空操作安全。
- **验证（full-green）**：`mvn clean install -DskipTests` BUILD SUCCESS（154 模块）；`npx playwright test --workers=1` 174 passed（167 基线 + 7 新测试：6 exception/partial-completion + 1 variance），0 回归。Phase 3 回归 check（orchestration + business-actions/mfg- 14 tests 全绿）。
- **文档**：`e2e-runbook.md` 增制造异常分支 spec 行 + 生产差异 spec 行 + PRODUCTION_VARIANCE 凭证行期望值表 + 测试数 167→174；`backlog/README.md` 增 1800-2 工作项行。
- **Deferred**：质检门控阻断完工（config-gated `erp-mfg.inspection-on-completion-enabled`）+ 批次基因追溯浏览器层（config-gated `erp-mfg.genealogy-write-enabled`）各自 config gate successor。

### 2026-07-11（后半天 — 3 份活跃计划实现：SettlementAllocation DTO 提取 + 角色权限映射 + AMIS 前端显示质量收尾 Phase 3-4 进度）

- 执行 `docs/plans/2026-07-11-1643-2-settlement-allocation-dto-extraction.md`：Phase 1-2 全部完成。共享 `SettlementAllocation` 在 `app.erp.md.biz` 包创建，泛化 Javadoc；sales/purchase 8 处 import 迁移至 md-dao 共享类型；两旧文件删除。验证：`mvn test -pl module-sales/erp-sal-service,module-purchase/erp-pur-service -am` 3m3s BUILD SUCCESS 0 failures。
- 执行 `docs/plans/2026-07-11-1643-3-roles-permission-mapping.md`：Phase 1 完成。15 角色 × 域/菜单组粗粒度映射矩阵写入 `docs/design/roles-and-permissions.md`，引用 SUBM/FNPT ID 经 rg 核对全存在。纯文档变更。
- 执行 `docs/plans/2026-07-11-1643-1-amis-frontend-quality.md`：Phase 1（审批按钮 PurOrder/SalOrder）+ Phase 2（系统字段 `ui:show="R"` 标记）已为 `completed`（之前已完成）。Phase 3（高价值 FK 名称解析）完成 4 头实体（ErpPurOrder/ErpSalOrder 已有 + ErpPurInvoice/ErpSalInvoice 新增 BizLoader+xmeta+view.xml grid），其余 ~6-8 头实体标记 successor。Phase 4（看板 FK 名称解析）sales dashboard `findCustomerTopN` 后端追加 `customerName` 查询 + 前端饼图改用 `r.customerName`。验证：`mvn clean install -DskipTests` 相关模块 BUILD SUCCESS 全绿。
- P6 销售定价引擎 `2026-07-10-1100-1` 已 `completed`，但 backlog 仍标 `in-progress`——需更新 backlog README。

---
