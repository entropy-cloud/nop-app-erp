# Development Log - 2026-07-11

### 2026-07-11（质量 NCR 过账生命周期浏览器层 E2E 扩展——reverseNcr 红字凭证行 + RETURN 处置退货编排 — plan 2026-07-11-0730-2）

- 执行 `docs/plans/2026-07-11-0730-2-quality-ncr-posting-lifecycle-e2e.md`，Phase 1+2 全部完成。解除 1800-1 一项 Deferred（NCR reverseNcr 红字凭证行断言）+ 0335-2 一项 Deferred（NCR RETURN 处置退货编排 RETURN 子集）。
- **Phase 1（reverseNcr 红字凭证行断言）**：新建 `quality-ncr-reverse-voucher-line.action.spec.ts`——createViaSave 建 NCR（SCRAP + materialId=1）→ submitReview → resolve（AUTO_POST 产 NCR_SCRAP 正向凭证）→ reverseNcr 红冲。复用 0704-1 范式 `findVoucherIdByBillCode(ncrCode, 'REVERSAL')` + `assertVoucherLines`。断言红字凭证行同向取负（Dr 6711=-120 / Cr 1401=-120，buildReversalDraft dcDirection 不变金额取负）+ 原正向凭证 isReversed=true（markOriginalVoucherReversed O-8 统一行为，凭证级回退标记）。价值增量区别于 1800-1 仅断言 ncr.posted=false（实体级回退）。
- **Phase 2（RETURN 处置跨域退货编排）**：新建 `quality-ncr-return-disposition.action.spec.ts`——createViaSave 建 NCR（RETURN + supplierId=3 SUP-001 + materialId=1）→ submitReview → resolve。resolve 触发 NcrReturnOrchestrator.orchestrateReturn → IErpPurReturnBiz.save 创建采购退货单（code=PR-FROM-NCR-{ncrId}）+ ncr.returnCode 登记。断言 returnCode 格式 + ErpPurReturn 存在且 supplierId 匹配 + docStatus=DRAFT + approveStatus=UNSUBMITTED；CONCESSION 对照 returnCode=null 无退货副作用。无 CAPA 简化 setup 对齐 0335-2 无 CAPA 路径范式。
- **验证（business-actions 全绿）**：`npx playwright test tests/e2e/business-actions tests/e2e/orchestration`（BASE_URL=http://127.0.0.1:8011 SKIP_WEBSERVER=1）**44 passed**（含新增 3 test），0 回归。全 workspace 运行 88 failures 全在 visual/reports/crud/dashboards（AMIS UI 渲染超时 TimeoutError locator.waitFor/click/fill，预存环境问题非 DB 污染——business-actions+orchestration 数据完整性套件全绿）。
- **文档**：`e2e-runbook.md` 增 reverseNcr/RETURN spec 行 + 红字 NCR_SCRAP 凭证行期望值表 + 测试数 177→180；`backlog/README.md` 增 0730-2 工作项行。
- 纯测试新增，零生产代码/契约/ORM 模型变更。

### 2026-07-11（制造域 config-gated 浏览器层 E2E——批次基因追溯 + 质检门控阻断完工 — plan 2026-07-11-0730-1）

- 执行 `docs/plans/2026-07-11-0730-1-manufacturing-config-gated-e2e.md`，Phase 1+2 全部完成。解除 1800-2 两项 Non-Goals（质检门控阻断完工 + 批次基因追溯浏览器层）+ 0704-2 两项 Deferred。
- **Phase 1（批次基因追溯浏览器层）**：新建 `mfg-genealogy.spec.ts`。`runMfgChain` 扩展 `withBatchTracking` 可选参数——创建输入 `ErpInvBatch`（batchNo=`GEN-IN-{ts}`）+ 备货/领料移动携 batchNo + MaterialIssueLine.batchNo 引用之。满量完工触发 `BatchGenealogyWriter.writeOnCompletion` 写入 inputLot→outputLot 基因链（`ErpMfgBatchGenealogy`）。断言 findPage 非空 + inputMaterialId(组件)/outputMaterialId(成品) 正确 + `forwardTrace(outputLotId)`/`backwardTrace(inputLotId)` 返回非空消耗关系。
  - **批次余额维度裁决**：`StockMoveBookkeeper.upsertBalance` 以 batchNo 为余额键维度之一（material×warehouse×location×batch×owner），故备货 INCOMING 移动须携同一 batchNo 使余额按批次维度落入，否则领料 OUTGOING 时可用量不足（available=0 < required=20）。输入 ErpInvBatch 在备货前创建（batchNo 唯一值），备货行+领料行共用之。
  - `cleanupMfg` 扩展：增 ErpMfgBatchGenealogy 清理(filter workOrderId) + 输出批次(filter batchNo=`FG-{woCode}`) + 输入批次(by id)。
- **Phase 2（质检门控阻断完工 Gate 1）**：新建 `mfg-inspection-gate.spec.ts`。webServer JVM args 追加 `-Derp-mfg.inspection-gate-enabled=true`（`playwright.config.ts:18`）。`runMfgChain` 扩展 `inspectionRequired` 可选参数——BOM 经 `__save` 设 `inspectionRequired=true`，链路停在 completeJob（WO=IN_PROCESS，跳过 reportCompletion）。
  - 负路径：spec 驱动 `reportCompletion`（满量 willFinish=true）被 `ErpMfgWorkOrderProcessor.isInspectionGated` 拦截抛 `ERR_INSPECTION_REQUIRED`（GraphQL errors 含「质检」语义 token）+ docStatus 保持 IN_PROCESS。
  - 对照路径：`inspectionRequired=false`（默认）→ reportCompletion 成功 COMPLETED（Gate 1 未命中，门控精确性）。
  - 回归安全：config 全局生效但仅对 BOM.inspectionRequired=true 的工单生效；既有 mfg-chain/mfg-variance spec 的 BOM.inspectionRequired 默认 false → Gate 1 不命中 → 0 回归。
- **helper 扩展**：`orchestration/_helper.ts` 增 `callMutation` 导出（供 spec 断言 GraphQL errors 负路径）+ `MfgChainOptions` 增 `withBatchTracking`/`inspectionRequired` + `MfgResult` 增 `inputBatch`。
- **验证（full-green）**：`npx playwright test --workers=1`（PLAYWRIGHT_PORT=8011）**177 passed**（174 基线 + 3 新测试：1 genealogy + 2 inspection-gate），0 回归，23.1m。orchestration 局部 7 passed（1.1m）。
- **文档**：`e2e-runbook.md` 增基因追溯/质检门控 spec 行 + config-gated 制造域子段 + 测试数 174→177；`backlog/README.md` 增 0730-1 工作项行。
- **Deferred**：基因追溯链可视化前端（echarts 树/图，1606-2 Deferred）+ 召回报告降级标记消除（inventory 域按批次查询 successor）。

### 2026-07-11（制造域扩展 E2E——WorkOrder 异常分支 + 部分完工 + 生产差异浏览器层 — plan 2026-07-10-1800-2）

- 执行 `docs/plans/2026-07-10-1800-2-manufacturing-exception-variance-e2e.md`，Phase 1~3 全部完成。解除 0704-2 三项 Deferred（WorkOrder 异常分支 stop/resume/cancel + 部分完工 willFinish 门控 + 生产差异计算浏览器层）。
- **Phase 1（WorkOrder 异常分支 stop/resume/close/cancel + 非法迁移守卫）**：新建 `mfg-work-order-exception.action.spec.ts`——复用 0335-1 范式（DIRECT 审批轴 + 简单 BOM 无子件 → checkAvailability → STOCK_RESERVED → start → IN_PROCESS）。正路径 A: stop(IN_PROCESS→STOPPED)→resume(→IN_PROCESS)→close(→CLOSED+actualEndDate 回填)；正路径 B: stop→close(从 STOPPED 可达)；正路径 C: cancel(NOT_STARTED→CANCELLED)；非法迁移守卫: IN_PROCESS→cancel 拒绝(cancel 仅允许 DRAFT/SUBMITTED/NOT_STARTED) + CLOSED→stop/resume 拒绝。每步 verifyState __get 独立断言 docStatus。清理仅逻辑删除 WO+BOM（异常分支无过账/库存产物）。
- **Phase 2（部分完工 willFinish 门控）**：同 spec 新增 test case——reportCompletion(completedQty=10 < plannedQty=20) → willFinish=false → docStatus 保持 IN_PROCESS + completedQuantity=10 → close → CLOSED（非 COMPLETED）。验证 reportCompletion 内部 `newCompleted >= planned` 门控不触发 willFinish 分支（不产生差异计算 + 不设 COMPLETED）。清理复用 cleanupMfg（部分完工已生成 MANUFACTURE 入库移动 10 件 + 成品余额）。
- **Phase 3（生产差异计算浏览器层 config-gated auto-calc）**：
  - Decision: webServer JVM args 追加 `-Derp-mfg.variance-auto-calc-enabled=true`（全局生效）。回归隔离裁决：MAT-001（mfg-chain.spec.ts 产品）无 FIRMED ErpMfgCostRollupLine → calculateVariances 抛 ERR_VARIANCE_NO_STANDARD_COST → Processor try/catch 吞异常不阻断完工；差异 spec 使用测试专用成品物料 + 运行时 FIRMED rollup 完全隔离。
  - 种子 COA 补齐：`erp_md_subject.csv` +5 差异科目行（1410 制造差异-材料 / 1412 制造差异-人工 / 1413 在制品-人工 / 1414 制造差异-制造费用 / 1415 在制品-制造费用）使 ProductionVarianceAcctDocProvider 科目码可达。
  - 新建 `mfg-variance.spec.ts`——自包含链路（测试专用成品物料 FINISHED_PRODUCT/STANDARD + FIRMED rollup mat90/lab5/ovh5/unit100 + runMfgChain 变体 → 满量完工 willFinish=true → calculateVariances 5 类差异行 + dispatchIfApplicable PRODUCTION_VARIANCE 凭证）。断言 ErpMfgCostVariance 5 行（MATERIAL_USAGE/LABOR_EFFICIENCY/LABOR_RATE/OVERHEAD/VOLUME）+ posted=true + PRODUCTION_VARIANCE 凭证 6 行精确数值（Dr 1410=100/Cr 1411=100/Dr 1412=50/Cr 1413=50/Dr 1415=50/Cr 1414=50，标准 vs 实际成本差额派生）。
  - `cleanupMfg` 扩展：增 ErpMfgCostVariance 清理(deleteByFilter workOrderId) + PRODUCTION_VARIANCE 凭证清理(cleanupVoucherByBillCode woCode+'-PV')。`runMfgChain` 增 `MfgChainOptions`(productId/productUoMId) 支持测试专用成品物料。MAT-001 链路无差异记录，cleanupMfg 扩展对 MAT-001 为空操作安全。
- **验证（full-green）**：`mvn clean install -DskipTests` BUILD SUCCESS（154 模块）；`npx playwright test --workers=1` 174 passed（167 基线 + 7 新测试：6 exception/partial-completion + 1 variance），0 回归。Phase 3 回归 check（orchestration + business-actions/mfg- 14 tests 全绿）。
- **文档**：`e2e-runbook.md` 增制造异常分支 spec 行 + 生产差异 spec 行 + PRODUCTION_VARIANCE 凭证行期望值表 + 测试数 167→174；`backlog/README.md` 增 1800-2 工作项行。
- **Deferred**：质检门控阻断完工（config-gated `erp-mfg.inspection-on-completion-enabled`）+ 批次基因追溯浏览器层（config-gated `erp-mfg.genealogy-write-enabled`）各自 config gate successor。

---
