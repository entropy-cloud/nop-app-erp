# Development Log - 2026-08-17

### 2026-08-17

- **projects 工时过账多币种落地（RC-R1.64，plan `2026-08-17-0142-2`，P1-MA1-010 reuse 重开 业务逻辑层闭合[A4.2.116 投影]，L1 UC-PRJ-02/06「多币种折算到统一币种」工时侧，2026-08-12 B 类批量裁决降级预授权[行标签按 B 类裁决改写]）**：执行 Phase 1（Explore + D1 裁决，前次会话完成）+ Phase 2（实现）+ Phase 3（测试 + 文档 + 回填）。
- **实现**：**buildEvent 汇率解析三态**（D1 裁决 A）——`TimesheetPostingDispatcher` 增 `resolveExchangeRate`（currencyId=null/币种不存在[保守放行镜像 R1.42 D2]/本位币[ErpMdCurrency.isFunctional] → rate=1；非本位币经 `ErpMdExchangeRate` from+to+validFrom<=voucherDate<=validTo 边界[validFrom 降序最近生效优先 limit 1，rateType 信息性维度不过滤]解析，缺失抛 `ErpFinErrors.ERR_EXCHANGE_RATE_REQUIRED`[复用 finance R1.42 语义，工时路径守卫结构性失效消除]），替代 `setExchangeRate(BigDecimal.ONE)` 硬编码；**Provider 双字段折算**（D1 裁决 i）——`ProjectCostCollectionProvider.fact()` 增 `setAmount(functional)`+`setAmountSource(source)`+`setAmountFunctional(source×rate)`（镜像 PurAcctDocProvider 范式，amount 保持本位币语义，单币种 rate=1 三者相等向后兼容）；**缺失错误路径 D1(ii) 选项 α**——buildEvent 在 tryPost try 块外抛错传播 → approve 事务回滚 → 单据保持 SUBMITTED（与 buildEvent 校验错误先例同型，无告警派发）；跨域只读经 `IBizObjectManager` 按名解析 `IErpMdCurrencyBiz`/`IErpMdExchangeRateBiz`（零新增 daoFor 站点）；边界查询经 `dateBetween(epoch/2999 哨兵)` 表达（XMeta 过滤算子白名单 [eq,in,dateBetween,dateTimeBetween] 无 ge/le，对齐 contract/hr 域先例——执行期发现并修正：首版 le/ge 触 `nop.err.biz.prop-not-support-filter-op`）。
- **Phase 3 测试**：新增 `TestErpPrjTimesheetMulticurrencyPosting` **3 组全绿**（①非本位币 USD+CNY 本位 rate 7.0 → amountSource=8000/amountFunctional=56000/行级 exchangeRate=7.0/GL 借贷=56000 平衡+projectId 维度②汇率缺失 → ERR_EXCHANGE_RATE_REQUIRED[含 currencyCode=USD param] + 保持 SUBMITTED + 回链零落库③本位币 → rate=1 source==functional==8000 向后兼容）+ `TestTimesheetFxRateResolution` **1 组**（currencyId=null 派发器层 rate=1 短路——e2e null 币种因 `ErpFinVoucherLine.currencyId NOT NULL`[finance ORM 既有约束]不可落库，属先于本行的既有行为非回归面，改派发器层单测覆盖）。
- **验证基线**：`mvn test -pl module-projects/erp-prj-service` **172 tests 0 failures 0 errors**（168 基线 + 4 新增零回归）+ 全量 `mvn test` **BUILD SUCCESS**（13:01 min）+ `mvn clean install -DskipTests` 全量 BUILD SUCCESS + compliance checker **全 19 规则 actual == baseline 零漂移**（R2b=235/R2c=1434/R2d=35/R12a=70/R12b=66/R12c=40——IBizObjectManager 按名解析零新增 daoFor 站点，per plan 预期「零漂移」分支）。
- **文档回填**：owner doc `cost-collection.md §2.4/§六` 多币种工时过账实现注记（三态 + 双点协同 + D1(ii) 选项 α + dateBetween 哨兵 + P2-RC-050 successor 边界）+ arm-index P1-MA1-010 业务逻辑层闭合注记（+ successor 边界声明）+ roadmap RC-R1.64 → done ✅（行标签按 B 类裁决改写）+ 本日志条目；use-cases.md L1 不动。
- **Deferred（已裁定，见计划）**：费用归集头/PnL 汇率路径（ExpenseCostAggregator:134 + ProjectPnlCalculator:105 + ProjectCostAggregator:82/MaterialCostAggregator:73 metadata 面）——watch-only residual 归 P2-RC-050 successor；共享汇率解析 helper 抽取——optimization candidate（触发条件=P2-RC-050 回队/第二处汇率解析需求）。
- **BUILD_VERIFY**：分域测试 + 全量 `mvn test` + `mvn clean install -DskipTests` + checker 全 19 规则零漂移均绿。**独立结束审计**：独立子代理（新会话 task `ses_feef01871ffepCeQARuY506EjF`）verdict **PASS**（全 6 项：D1 代码逐条 file:line 核验 + 独立复跑测试 4/0/0 与 172/0/0 + 文档回填核验 + 计划一致性 + 反模式零命中 + checker 复跑零漂移）。

- **projects 质保金逻辑落地（RC-R1.63，plan `2026-08-17-0142-1`，P1-RC-052 UC-PRJ-07 ④⑤ 留存填充 + 到期返还 + 质保金凭证，2026-08-12 B 类批量裁决降级预授权[用户级裁决覆盖 A4.2.122 ask-first 备注]）**：执行 Phase 1（Explore + D1/D2/D3 裁决，前次会话完成）+ Phase 2（实现，前次会话完成）+ Phase 3（测试 + 文档 + 回填，本次完成）。
- **D1/D2/D3 裁决摘要（详见计划 Execution Record）**：**D1 留存填充=选项 A**（config 驱动仅 FINAL——`erp-prj.settlement-retention-ratio` 默认 0=设计性 opt-in + `erp-prj.settlement-retention-due-months` 默认 12，`createSettlement` 填 `retentionAmount=finalRevenue×ratio`（scale 4 HALF_UP）+ `retentionDueDate=businessDate+N月`，INTERIM/CLOSE 不填，手工覆盖保留，否决纯手工录入）+ **D2 质保金凭证=选项 A**（扩展 `ProjectSettlementAcctDocProvider` 保留 PROJECT_SETTLEMENT businessType 零字典变更——主结算凭证留存平衡腿[借 1122 应收账款-质保金 / 贷 2241 其他应付款-质保金，标 projectId，ACCOUNT_KEY_RETENTION_* 供 GL mapping 覆盖] + 返还独立凭证[billHeadCode=结算单号#RETURN + RETENTION_RETURN 标志，镜像腿 借 2241/贷 1122 对冲] + reverseSettlement/cancel 未返还守卫防返还凭证悬挂[Explore ⑥ 生命周期一致性]，否决新 businessType 枚举须 orm.xml 字典变更超 B 类边界）+ **D3 返还幂等与守卫=选项 A**（`returnRetention` @BizMutation + per-mutation `ErpPrjProjectSettlementReturnRetentionProcessor`：幂等=ErpFinVoucherBillR[billCode=结算单号#RETURN] 存在性反查镜像 R1.52 #CATCHUP 范式 + 守卫链 APPROVED+posted+retentionAmount>0+到期 + `ERR_RETENTION_RETURN_NOT_ALLOWED`/`ERR_RETENTION_RETURN_POSTING_FAILED` 新错误码，否决 remark 弱标记）。
- **Phase 3 测试**：新增 `TestErpPrjProjectSettlementRetention` **10 组全绿**（①FINAL 填充断言[500.0000=10000×0.05 + 2027-07-17 到期日推演，冻结时钟 2026-07-17]②INTERIM/CLOSE 零填充③到期返还成功[凭证回链 + 镜像对冲行级断言 + projectId]④幂等[重复调用零副作用]⑤非 APPROVED 拒绝⑥未过账拒绝⑦未到期拒绝⑧零质保金拒绝⑨主凭证留存腿 + GL 借贷平衡⑩已返还后红冲拒绝）+ `_cases` 快照。
- **执行期测试基建修复（非计划范围，阻塞零回归门控）**：`_vfs/_delta/test-prj-delta/erp/prj/beans/app-service.beans.xml`（R1.60 期 M2.4 DeltaOverride 测试资源）缺 `x:extends="super"`——delta 层激活时**整文件替换**基线 beans.xml（违反 `delta-customization.md` 规则 3 反模式），同 JVM 后续测试类容器解析 prj bean 报 `nop.err.ioc.not-find-bean-with-type`。本计划 baseline beans.xml 增 ReturnRetentionProcessor bean 触发暴露（`TestErpPrjBudgetAndCollection` 7 errors，顺序依赖——surefire `forkCount=1C, reuseForks=true, parallel=classes` + JUnit 平台类序文件集敏感）。定位过程：单类绿/全量红二分 + JUnit launcher 定序探针（ProbeOrderer）逐类回放锁定污染源=DeltaOverride；修复后 24 类 168 tests 单 JVM 全绿。仅测试资源 1 行变更，零生产代码影响。
- **验证基线（全绿）**：`mvn test -pl module-projects/erp-prj-service` **168 tests 0 failures 0 errors**（158 基线 + 10 新增零回归）+ 全量 `mvn test` **BUILD SUCCESS 3549 tests / 0 failures / 0 errors**（13:43 min）+ `mvn clean install -DskipTests` 全量 BUILD SUCCESS + compliance checker **R2c 1433→1434 + R12a 69→70 baseline-raise 登记**（`ErpPrjProjectSettlementProcessor.isRetentionReturned:311` `daoFor(ErpFinVoucherBillR.class)` 跨域回链反查 1 站点 + 其共生 `import ErpFinBusinessType`（:5/:314 businessType 枚举 name() 过滤），per-site 证据落 compliance-baseline.md，镜像 R1.52 #CATCHUP 先例；R12a 漏登记由独立结束审计发现后补登记——结束审计回合 1 FAIL→修复→回合 2 PASS；R2b=235/R2d=35 零变化）。
- **文档回填**：owner doc `profitability.md` §关键流程 2 补质保金实现注记（D1/D2/D3 裁决 + 科目/部署残留风险 + successor 登记）+ arm-index P1-RC-052 → done (RC-R1.63)（修复记录 + 历史保留）+ roadmap RC-R1.63 → done ✅（行标签按 B 类裁决改写，对齐 R1.61/62 先例）+ 本日志条目；use-cases.md L1 不动。
- **BUILD_VERIFY**：分域测试 + 全量 `mvn test` + `mvn clean install -DskipTests` + checker actual == updated baseline（R2c=1434）均绿。**独立结束审计**：由独立子代理（新会话）执行（Closure Gates 硬门控，执行者未自我审计）。

- **projects 工时成本率三级降级落地（RC-R1.60，plan `2026-08-16-2043-3`，P1-RC-048 UC-PRJ-02 AC-③ 用户级 > 角色级 > 活动类型费率载体，越界项 ORM 结构变更 A 类授权 + product-scope 确认 + 双独立子 agent 批准）**：执行三 Phase。落地 finding **P1-RC-048**（L1 `use-cases.md:38` 逐字「成本率解析(优先级: 用户费率 > 角色费率 > 活动类型费率)」三级，L3 `CostRateResolver.resolve` 实为「单填 > 活动类型 > 全局」三级但层级错位——用户级/角色级独立费率载体未实现；owner doc `cost-collection.md §2.2:58-61` AI 自标 Non-Goal 无人工批准痕迹 → §4 三判据不满足 → Q4=(a) 强制实现）。
- **Phase 1 裁决（D1-D5 + product-scope 确认闭环）**：**D1 角色费率载体=选项 A**（新增 `ErpPrjRole` 独立实体——code/name/costRate + 审计列 + UK_PRJ_ROLE_CODE(code)，propId 1-11；`ErpPrjProjectUser.role` 纯文本 trim 后与 `ErpPrjRole.code` 精确匹配；否决 B 用户/角色费率合一实体[scope 判别列数据歧义 + 用户级经 ProjectUser.costRate 天然承载]）+ **D2 tier 归位=选项 A**（**单填保持最高**——显式录入优先既有行为兼容；完整链 = 单填 > 用户级 > 角色级 > 活动类型 > 全局默认，五处皆无抛 `ERR_COST_RATE_NOT_AVAILABLE`；既有 `testCostRateFromTimesheetOverridesActivityType` 零断言调整）+ **D3 用户级查询载体**（`ErpPrjProjectUser` 按 (projectId, userId) `findFirstByQuery`，行缺失/costRate null 跳过）+ **D4 数据访问形态=选项 A**（`CostRateResolver` 纯组件保留 `daoProvider.daoFor` 同域站点——R2c +2 baseline-raise 带 per-site 证据；否决 IBiz 注入[resolve() 无 IServiceContext，改签名波及 Processor 与测试；对齐 R1.57 D4 先例]）+ **D5 product-scope 确认闭环**（product-scope.md grep 成本率/费率零命中未裁剪 → Q4=(a) 强制实现 + 2026-08-12 A 类裁决声明闭合，审计不复开）。orm.xml 变更草案落盘本计划（xmllint exit 0 well-formed，namespace 警告为既有 Nop `ext:` 约定），**live orm.xml 未先改**。
- **Phase 2 双独立子 agent 批准**：`ses_ff487f015ffeiZnJkElOIMHLXv` + `ses_ff487d533ffeGsDlBjbgNr74VB`（均 fresh session）**均 APPROVE，无强制修正**——纯加性最小性（ProjectUser 1 列 + 新实体，零删除/零迁移/零既有语义变更）、propId 连续（13 顺延 / 1-11）、约定逐项比对、UK 无碰撞、无 codegen 环/跨域影响、治理排序合规；双方同款 Optional 建议（costRate 物理位置置 updateTime 之后保持文件 propId 升序惯例，功能等价）被执行者采纳。批准记录落盘计划 Phase 2。
- **Phase 2 实施**：live orm.xml 纯加性双载体（`ErpPrjProjectUser.costRate` propId 13 可空无默认无索引无 UK + `ErpPrjRole` 实体段）+ `mvn clean install -DskipTests` 增量重生成 BUILD SUCCESS——生成产物核对（`_gen/_ErpPrjRole.java` getCode/getName/getCostRate + `ErpPrjRole.xmeta` + `IErpPrjRoleBiz` + api InputBean/OutputBean/Api + DDL 三方言 `erp_prj_role` 表 + `_ErpPrjProjectUser.java` costRate getter/setter + i18n yaml + `_service.beans.xml` 注册）；`CostRateResolver.resolve` 增用户级/角色级 tier（`findProjectMember:104` `daoFor(ErpPrjProjectUser.class)` + `findRoleRate:114` `daoFor(ErpPrjRole.class)`，均 `findFirstByQuery` 零 R1d 面 + E3 注释），javadoc 更新五级语义，单填/全局默认分支保持。
- **Phase 3 测试 + 文档 + 回填**：新增 `TestErpPrjCostRateTier` **8 组全绿**（①用户级费率命中覆盖活动类型[500 vs 300]②角色级费率低于用户级[用户 500 + 角色 400 → 500]③用户级缺失回落角色级[400]④三级皆缺回落全局 config[250]⑤单填覆盖全链[800 覆盖 500/400/300]⑥用户级/角色级费率为 null 跳过回落活动类型[300]⑦ErpPrjRole GraphQL CRUD 冒烟[save + findPage]⑧全缺抛 `ERR_COST_RATE_NOT_AVAILABLE`）+ 既有 7 组零回归（`testCostRateFromTimesheetOverridesActivityType` 单填最高断言不变）+ erp-prj-service **158 tests（150+8）0 failures 0 errors** + 全量 `mvn test` BUILD SUCCESS（1 次 flaky 命中 `TestErpQaSpcSamplingEvaluateBatch` 时间戳毫秒边界 `SAMPLE_TIME 15.999 vs 16.0` 快照比对——独立重跑 4/4 绿，quality 域既有非本行引入）+ `mvn clean install -DskipTests` 全量 BUILD SUCCESS + compliance checker **R2c 1431→1433 baseline-raise 登记**（`CostRateResolver` 2 处 projects 同域 daoFor per-site 证据落 compliance-baseline.md，findFirstByQuery 零 R1d 面，R2b=235/R2d=35 零变化）；owner doc `cost-collection.md §2.2` Non-Goal→已实现（五级解析链 + D2 裁决 + 载体 + 匹配语义）+ §七 规则 3 注记 + arm-index P1-RC-048 → done (RC-R1.60)（修复记录 + 历史保留）+ roadmap RC-R1.60 → done ✅ + 本日志条目；use-cases.md L1 不动（Non-Goal）。
- **验证基线（全绿）**：`mvn test -pl module-projects/erp-prj-service` **158 tests 0 failures 0 errors** + 全量 `mvn test` BUILD SUCCESS（除 1 次已复绿的既有 flaky 外全零）+ `mvn clean install -DskipTests` 全量 BUILD SUCCESS + compliance checker actual ≤ updated baseline（R2c=1433，全 19 规则与 `compliance-baseline.md` §BASELINE 块一致）。
- **Deferred（已裁定，见计划）**：角色管理 UI/角色 CRUD 业务页面——out-of-scope improvement，successor no（标准 CRUD 生成）；费率历史版本化/生效日期窗口——out-of-scope improvement，successor yes（触发条件=运营要求费率变更留痕时按 ORM ask-first 立项）；多币种费率——watch-only residual，successor yes（与 P1-MA1-010 多币种投影同域协同）。
- **BUILD_VERIFY**：分域测试 + 全量 `mvn test` + `mvn clean install -DskipTests` + checker actual ≤ updated baseline 均绿。**独立结束审计**：由独立子代理（新会话）执行（Closure Gates 硬门控，执行者未自我审计）。

- **VERIFY 全绿（full-green verification）——plan `2026-08-16-2043-2`（RC-R1.62 projects 报销归集守卫族）mission requirement-compliance 验证通过**：`mvn clean install -DskipTests` 全量 **BUILD SUCCESS**（01:37）+ 全量 `mvn test` **BUILD SUCCESS 0 failures 0 errors**（3531 tests，含 erp-prj-service `TestErpPrjExpenseAggregation` 8/0/0——4 基线 + 4 新增守卫测试）+ compliance checker exit 0 **actual == baseline 零漂移**（R2b=235 / R2c=1431 / R2d=35）。
- **修复 `TestErpAstIdleStateMachine` 日期敏感 time-bomb（VERIFY 阻塞项，非 RC-R1.62 范围）**：`mvn test` 全 reactor 在 `app-erp-assets-service` 失败——6 组测试全部 `REMARK value=闲置自 2026-08-17 expected=闲置自 2026-08-16` 快照漂移。根因：`ErpAstAssetSuspendResumeProcessor:43` 经 `CoreMetrics.today()` 写「闲置自 {date}」remark，`TestErpAstIdleStateMachine`（2026-08-16 创建，RC-R1.54）**未注册冻结时钟**——快照于 2026-08-16 录制，2026-08-17 日期翻滚即跨日漂移（Q6 时钟测试基础设施 `clock-test-infrastructure.md` §4.1/§4.2 同族 time-bomb，对齐 `TestErpDrpSafetyStock` 2026-08-11 修复先例）。修复：按 Q6 范式注册 `@RegisterExtension static AstFrozenClockExtension`（REFERENCE_DATE 2026-07-17，与 `TestErpAstDashboard` 同型）+ 6 个 `_cases/.../erp_ast_asset.csv` 快照 `闲置自 2026-08-16` → `闲置自 2026-07-17`（冻结参考日）；其余输出表日期（voucher/disposal/period）均 `@var:` 掩码或显式种子，零其他漂移。修复后 `mvn test -pl module-assets/erp-ast-service -Dtest=TestErpAstIdleStateMachine` 6/0/0。test-only 变更（1 测试类注解 + 6 CSV），零生产代码/ORM/契约变更。
- **验证说明**：首次全量 `mvn test` 失败于上述 assets time-bomb（阻断点，先于 master-data）；修复后第二次全量 `mvn test` 在 master-data 出现 1 次瞬时 `nop.err.graphql.unknown-operation`（`ErpMdMaterialSku__findDefaultSku`，9/9 errors）——单跑 `mvn test -pl module-master-data/erp-md-service -Dtest=TestErpMdSkuServices` 9/0/0 复绿，第三次全量 `mvn test` 12:14 全 reactor **BUILD SUCCESS 3531/0/0**（master-data `TestErpMdSkuServices` 9/0/0 复验），判定为前次中断 reactor 残留 target 状态的瞬时问题，非代码回归（master-data 与本 mission 零相关）。
- **下一步**：RC-R1.62 变更集提交（code + docs 双 commit，按仓库惯例审计通过后提交）。
