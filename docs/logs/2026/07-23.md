# Development Log - 2026-07-23

### 2026-07-23 (MISSION_DRIVER execute — F9 长尾域跨单据导航 successor plan 全 Phase 落地)

- **任务**：执行 plan `docs/plans/2026-07-23-1408-1-f9-long-tail-cross-doc-navigation.md`（11 长尾域跨单据导航，F9 核心 4 域先行计划的 successor）。Phase 0-3 全部本次落地。frontend-ui-roadmap §F9 状态 `done(核心4域;长尾defer)` → `done(核心4域 + 长尾15头实体全落地)` + §退出标准 F9 行追加 successor。零后端 delta（Task Route: implementation-only change，纯 view.xml + page.yaml + tests + docs，不改 ORM/BizModel/API）。
- **Phase 0（Decision | Proof）**：实时核实 11 长尾域 ORM to-many。产出范围确认表：**入范围 15 头实体**（crm Lead / cs Ticket / contract Contract / logistics Shipment+Carrier / projects Project+Task / maintenance Equipment+Visit / quality NCR+Inspection+Recall / hr Survey / drp Plan / assets Inventory），出范围低价值 6 + aps（零 to-many）+ hr Employee（无 to-many）。copy-line 裁决：长尾**无域同时满足**「已有头行编辑对 sub-grid-edit」+「源单 lines」（contract Template 无 lines 子实体；quality Inspection edit form 无 lines sub-grid-edit + ErpQaInspectionLine 无 sub-grid-edit grid 需先补 F4）→ 范围=空集，归 successor。
- **Phase 1（高价值 7 域 12 头实体）**：范式 §3.2 方案 A（row-action drawer + `ref-*.page.yaml` + `fixedProps`）。新增 23 ref page + 12 头实体 view 增 drawer row-action（bounded-merge）。link 跳转（§3.3）：projects Project「创建任务」(visibleOn OPEN) + maintenance Equipment「创建维修请求」(visibleOn !=DECOMMISSIONED)。
- **Phase 2（中价值 3 域）**：新增 4 ref page + 3 头实体 view（hr Survey / drp Plan / assets Inventory，其中 3 个原 `<crud name="main"/>` 无 rowActions → 新增 bounded-merge rowActions 块）。
- **关键缺陷修复（GenPage 自引用 cycle）**：实现期 `ErpAllWebPagesTest` StackOverflow。焦点探针定位：仅 ErpPrjTask 2 ref page cycle——ErpPrjTask 既是父实体（childTasks drawer）又是子实体（ref-project/ref-parent-task 生成 ErpPrjTask main），GenPage 递归解析 drawer 嵌套 page 引用形成无限递归（`-Xss16m` 不收敛=真 cycle 非 H-2 噪声）。修复：childTasks 导航由 drawer 改 **link** `/ErpPrjTask-main?filter_parentTaskId=${id}`（GenPage 不递归解析 link），删 `ref-parent-task.page.yaml`。固化范式文档 **§11 GenPage 自引用 cycle 规则** + §9 反模式表。修复后 `ErpAllWebPagesTest` 稳定通过。
- **Phase 3（文档 + E2E）**：`cross-doc-navigation-patterns.md` §6 拆 6.1 核心域 / 6.2 长尾域 15 行 + 新增 §11。`cross-doc-navigation.action.spec.ts` 新增第 2 个 describe block 7 用例（test 6-12，crm/cs/projects/contract/logistics create+非空，maintenance/quality seed 非空）。
- **验证（全绿）**：全 reactor `mvn clean install -DskipTests` → BUILD SUCCESS（154 模块）；`mvn test -pl app-erp-all -Dtest=ErpAllWebPagesTest`（validateAllPages，30 ref page + 15 view）→ Errors:0 BUILD SUCCESS（cycle 修复后稳定 2 次）；`mvn test -pl app-erp-all` → 11 tests Errors:0（仅 H-2 @Disabled CollectTest 跳过）；`BASE_URL=http://127.0.0.1:8011 SKIP_WEBSERVER=1 npx playwright test cross-doc-navigation.action.spec.ts` → **12 passed**（5 核心 + 7 长尾）。注：本机 zulu-26 H-2 环境，webServer 120s 超时不足 Quarkus 启动+91 CSV seed，改手启 8011 + BASE_URL 复用。**full-green verification**：MISSION_DRIVER 复核全 reactor `mvn test` → BUILD SUCCESS（154 模块，0 failures 0 errors，仅 H-2 @Disabled CollectTest 跳过）。
- **结论**：F9 跨单据导航全域收口（核心 4 + 长尾 15 头实体）。GenPage 自引用 cycle 反模式（§11）+ 双父实体无 cycle 判定规则（ErpPrjTimesheet 未加 drawer 故 ref-project/ref-task 均通过）为后续自引用导航场景提供范式。低价值头/aps/hr Employee/长尾 copy-line 归 successor。待独立结束审计。

### 2026-07-23 (MISSION_DRIVER execute — 跨切面敏感操作确认流程 plan 全 Phase 落地)

- **任务**：执行 plan `docs/plans/2026-07-23-1145-2-cross-cutting-sensitive-operation-confirmations.md`（删除引用预览扩展 + 反审核冲销预览 + 停用业务影响预览）。Phase 0-3 全部本次落地。frontend-ui-roadmap §退出标准「敏感操作确认流程落地」`❌ → ✅`。Task Route: app-layer design change（新增 @BizQuery 预览 = 公共读 API 契约扩展 + SPI 生产实现补全），触及 finance 保护区域（plan-first，已过 2 轮独立草案审查）。
- **Phase 0（Decision）**：3 项裁决。(1) **范式**：删除引用复用 F7 **blocker** 范式（一致性）；冲销预览用 **preview-then-confirm** 范式。(2) **实体身份纠正**（实现期关键发现）：`ErpHrEmployee`（HR 人事档案，表 `erp_hr_employee`）与 `ErpMdEmployee`（master-data 业务经办人，表 `erp_md_employee`）是**不同表**——计划原文让 ErpHrEmployee 删除经 `ErpParty__findReferences(EMPLOYEE)` 会错配实体 ID。故 ErpHrEmployee 删除引用改 HR 域内实体级 `countReferences`（合同/工时/薪酬/考勤/休假），ErpMdOrganization 走 master-data 实体级 `countReferences`。(3) **SPI 落域**：Organization checker 落 master-data（域内 ErpMdEmployee.orgId + ErpMdWarehouse.orgId，无环）；Employee checker 落 finance（finance→master-data 合法 DAG 边，计 ErpFinEmployeeAdvance）。
- **Phase 1（Employee/Organization 删除引用阻断）**：
  - 后端：新增 `ErpMdOrganizationReferenceChecker`（master-data service，注册 app-service.beans.xml）+ `ErpMdEmployeeReferenceCheckerImpl`（finance service，注册 app-service.beans.xml）；`ErpMdOrganizationBizModel`/`IErpMdOrganizationBiz` 增 `countReferences` @BizQuery（F7 范式，@Nullable SPI 委托）；`ErpHrEmployeeBizModel`/`IErpHrEmployeeBiz` 增 `countReferences` @BizQuery（HR 域内 daoProvider 同域只读聚合，对齐 ErpPartyBizModel 范式）。
  - 前端：`ErpMdOrganization.view.xml` 原 `<crud name="main"/>` 无 rowActions → override rowActions 增 row-delete-button blocker dialog（调 `ErpMdOrganization__countReferences`）；`ErpHrEmployee.view.xml` 裸 `<action id="row-delete-button"/>` 增 blocker dialog（调 `ErpHrEmployee__countReferences`）。
- **Phase 2（finance 反审核冲销预览）**：
  - 后端：新增 DTO `VoucherReversePreview`（原凭证信息 + 红字预估取负 + willSetReversed + billLinks）+ `ReconciliationReversePreview`（核销单信息 + revertedItems 辅助账回退列表 + willRefreshPartnerBalance，**无红字凭证段**对齐 reverse 真实副作用）；`ErpFinVoucherBizModel`/`IErpFinVoucherBiz` 增 `previewReverseVoucher` @BizQuery；`ErpFinReconciliationBizModel`/`IErpFinReconciliationBiz` 增 `previewReverse` @BizQuery。**关键**：DTO 严格镜像 mutation 真实副作用（reverseVoucher 仅置 isReversed，不生成红字凭证/不回退域单据）；子表经 daoProvider 显式查询（不依赖 to-many 懒加载，会话存活无关——初版用 `voucher.getLines()` 导致 test session-closed，已修正）。
  - 前端：`ErpFinVoucher.view.xml` row-reverse-button 从 `<confirmText>` 静态确认改为 preview-then-confirm（先 previewReverseVoucher 查询 → dialog 展示 → 确认调 reverseVoucher）；`ErpFinReconciliation.view.xml` 原 view 无 reverse row action → 新增 row-reverse-button（visibleOn POSTED）+ preview-then-confirm dialog。
- **Phase 3（测试 + 范式文档）**：4 新增后端 JUnit（可验证权威证据）：`TestErpMdOrganizationBiz`（countReferences 真实数据 employee+warehouse）+ `TestErpHrEmployeeReferences`（HR 域内 contract/leave 计数）+ `TestErpFinVoucherReversePreview`（预览结构 + 只读 + DRAFT 拒绝）+ `TestErpFinReconciliationReversePreview`（revertedItems 非空 + willBecomeStatus 与实际 reverse 一致）。**既有测试适配**：`TestErpPartyBiz.testFindReferencesPartnerPathAndMissingSpisReturnEmpty` 的 Organization 断言从「SPI 未注册返回空」改为「生产 SPI 已激活返回 {employee:0, warehouse:0}」。E2E：`reverse-preview.action.spec.ts`（GraphQL 全栈 previewReverseVoucher + 与实际 reverse 一致）+ `sensitive-operation-confirmations.visual.spec.ts`（4 页面 dialog 入口渲染契约）。文档：`visible-on-patterns.md` 增 §6.5（删除引用预览扩展：实体身份纠正 + SPI 落域裁决）+ §8.5（反审核冲销预览范式：preview-then-confirm + DTO 镜像真实副作用 + 反模式）。
- **验证（全绿）**：master-data(109)/finance(264)/hr(113) `mvn test` 全绿无回归；全 reactor `mvn clean install -DskipTests` → BUILD SUCCESS（154 模块）；4 新测试类全绿。零 ORM/API 契约变更（仅新增 @BizQuery 读方法 + SPI 实现 + view.xml/tests/docs）。
- **结论**：跨切面敏感操作确认流程收口。实体身份纠正（ErpHrEmployee≠ErpMdEmployee）+ SPI 落域裁决（Organization→master-data / Employee→finance）+ 冲销预览 DTO 镜像真实副作用 为后续域删除/冲销预览扩展提供范式。assets/maintenance 的 Employee 引用 + 全域停用预览归既有 Deferred successor。待独立结束审计。

### 2026-07-23 (MISSION_DRIVER execute — maintenance ErpMntVisit 前端补齐 plan 全 Phase 落地)

- **任务**：执行 plan `docs/plans/2026-07-23-1145-1-maintenance-visit-wizard-and-child-table-editor.md`（maintenance 域 F4 child-table-editor 缺失 + F12/F16 ErpMntVisit Deferred + 4 步执行向导 BLOCKED）。Phase 0-4 全部本次落地。frontend-ui-roadmap §F4 maintenance 收尾 + §F12 ErpMntVisit `❌ → ✅` + §F16 maintenance 向导 `✅ done`。零后端 delta（Task Route: implementation-only change，不改 ORM/BizModel）。
- **Phase 0（Fix + Explore + Decision）**：修正 `docs/design/maintenance/ui-patterns.md:64` 整段陈旧注释（ORM 现已含 visitType/result + tasks/sparePartUsages to-many + Equipment 的 visits/schedules/... to-many）。**Explore**：核实 SparePartUsage 5 mandatory 头字段（code+UK/docStatus/approveStatus/equipmentId/businessDate）经 `ErpMntVisit__save` 聚合保存时 ORM 级非空校验。**Decision 候选 a**：SparePartUsage 是嵌套文档（header+lines+审批/过账轴），不在 Visit form inline 编辑，用 sub-grid-view 只读展示 + 独立 CRUD/wizard 管理；候选 b（sub-grid-edit）因 5 mandatory 含唯一 code 不可行；候选 c（wizard-only）未达成 F4 可见性目标。
- **Phase 1（ErpMntVisitTask child-table-editor）**：`ErpMntVisitTask.view.xml` 增 `sub-grid-edit`/`sub-grid-view`（6 列：lineNo/taskDescription/status dict 5 值/completedBy picker ErpMdEmployee/completedAt/remark，对齐 §13 mfg WorkOrderLine 范式）。`ErpMntVisit.view.xml` edit form 增 `<cell name="tasks" grid="sub-grid-edit"/>` + view form 增 sub-grid-view。
- **Phase 2（ErpMntSparePartUsage 渲染）**：依 Decision 候选 a，`ErpMntVisit.view.xml` edit/view form 增 `<cell name="sparePartUsages" grid="sub-grid-view"/>`（只读展示 code/warehouseId/totalAmount/docStatus/approveStatus）。`ErpMntSparePartUsage.view.xml` 增 sub-grid-view grid（供 Visit 引用）+ edit/view form 增 `<cell name="lines">`（sub-grid-edit/view）。`ErpMntSparePartUsageLine.view.xml` 增 sub-grid-edit/sub-grid-view（8 列含 materialId/uoMId picker），使 SparePartUsage 独立 CRUD 成为 full child-table-editor（覆盖 successor）。
- **Phase 3（4 步执行向导）**：新建 `visit-wizard/main.page.yaml`（hand-written，遵循 §5 finance 先例）：select（page body 共享 scope）+ wizardService（get + spareParts findPage，adaptor 算 step 状态 + 预渲染 HTML 单 tpl 指示器）+ 4 step（信息确认+开始执行 start mutation / 备件消耗只读 table / 执行结果 form __save / 确认完成 complete + 取消 cancel）。cancel 从 IN_PROGRESS 可达（validateNotTerminal 允许，扩展标准列表页 cancel visibleOn）。`ErpMntVisit.view.xml` 增 "执行向导" row-action link（visibleOn SCHEDULED/IN_PROGRESS）。`erp-mnt.action-auth.xml` 注册 `mnt-visit-wizard` 路由（mnt-work 分组 orderNo=305）。**执行型变体**（区别 finance 结账流向导）记入 `page-structure-patterns.md §5.1`。
- **Phase 4（测试 + 范式文档）**：`maintenance-visit-wizard.visual.spec.ts`（DOM 断言：page 可达+step indicator+Step1 start+IN_PROGRESS Step2-4）+ `maintenance-visit-wizard.action.spec.ts`（GraphQL 驱动 save(DRAFT+tasks 聚合)→schedule→start→save(result)→complete 全链，含 tasks 子集合聚合保存可达性验证）。`--list` 编译通过（2 specs）。`page-structure-patterns.md` 增 §5.1 执行型向导变体段；`child-table-editor-patterns.md` 增 §18 maintenance 变体段（简单行 + 嵌套文档渲染裁决）。
- **验证（全绿）**：`mvn install -DskipTests -pl module-maintenance/erp-mnt-web -am` → BUILD SUCCESS；全 reactor `mvn clean install -DskipTests` → BUILD SUCCESS（154 模块）；playwright spec `--list` → 2 tests 编译通过。零后端 delta（git diff 仅 view.xml/page.yaml/action-auth.xml/tests/docs/plan，无 Java/ORM/API/xbiz）。
- **结论**：maintenance 域 F4 child-table-editor + F12/F16 ErpMntVisit 全部收口。嵌套文档子表渲染裁决（候选 a）+ 执行型向导变体（§5.1）为后续嵌套文档/执行向导场景提供范式。待独立结束审计。

### 2026-07-23 (MISSION_DRIVER execute — F12 Tier C 期末结账向导 successor plan 全 Phase 落地)

- **任务**：执行 plan `docs/plans/2026-07-23-0818-2-f12-finance-period-close-wizard.md`（finance ErpFinAccountingPeriod 期末结账向导，F12 Tier C 最后一项 Deferred）。Phase 0/1/2 全部本次落地。frontend-ui-roadmap §F12 ErpFinAccountingPeriod `❌ Deferred → ✅ done` + 落地清单 14/16→15/16 + §F16 maintenance 向导 successor 引用本计划为 wizard 范式先例。零后端 delta（Task Route: implementation-only change）。
- **Phase 0（Explore + 3 Decision）**：实时仓库核实后端 5 Facade mutation（`ErpFinAccountingPeriodBizModel.java:35-57`：preCheck @BizQuery / closePeriod / finalizePeriod / reverseClose @BizMutation / generateNextYearPeriods 内部）+ PeriodPreCheckReport 字段（`unpostedVoucherCodes`/`unsettledArApCodes`/`unresolvedPostingExceptionKeys` + 4 坏账 + `hasIssues()`/`issueCount()`）+ ErpFinAccountingPeriodStatus per-module（arStatus/apStatus/invStatus/glStatus/assetStatus，orm:669-673）。裁决：(a) roadmap §F12「5 步」属笔误，closePeriod 一次性多步；(b) 零后端 delta——前任「后端 mutation 重构授权」触发条件失效（moot），保护区域审查归独立子代理结束审计；(c) 组件选型——`form layoutControl="wizard"` AMIS 未实现（layout-syntax-reference.md:288）+ `type:steps` prop 不稳（§8.12）→ 手写 page.yaml + each+tpl 步骤指示器（执行期改单 tpl 预渲染 HTML，见下）。
- **Phase 1（period-close-wizard/main.page.yaml）**：落地向导页面——期间 select（source=findPage，置于 page body 共享 scope）+ wizardService（get+statusPage findPage，adaptor 算 flowSteps + can* 标志）+ 4 step（preCheck dialog / closePeriod actionType:ajax + per-module 结果卡 / 12 月年度结转 visibleOn / finalizePeriod）+ 反结账 dialog（红冲影响预览 + 二次确认）。**执行期反模式修正**：(1) confirmText 双引号嵌套 `${...?"...":"..."}` 改单引号包裹（§8.9）；(2) GraphQL field selection——`hasIssues`/`issueCount` 非 Nop bean 字段（has*/非 get 方法不暴露）→ 客户端按同语义计算；(3) select 置 form 内致 service 读不到 periodId（form scope isolation）→ 移至 page body 共享 scope；(4) each+tpl 渲染对象数组出 `[object Object]` 怪癖 → adaptor 预渲染 `stepIndicatorHtml` 单 tpl 消费；(5) orderBy 内联 GraphQL 语法不稳 → 改客户端 sort。
- **Phase 2（菜单 + 测试 + 文档 + roadmap）**：`erp-fin.action-auth.xml` 加 `fin-period-close-wizard`（fin-period 分组 orderNo=205，TestAppActionAuthMerge 通过）。action spec（建测试专用期间 2026-08，驱动 preCheck→closePeriod→finalize→reverseClose 全链 + 非法守卫 + REVERSAL 红冲凭证断言，cleanup 删凭证+状态行+期间）+ visual spec（page 可达+步骤指示器+preCheck dialog+反结账 dialog DOM）。**环境约束裁决**：E2E 共享 DB 种子 OPEN receivable item 5 触发 allowance gate 阻断 closePeriod + reverse-close-approval 阻断 reverseClose → `playwright.config.ts` webServer 追加 `-Derp-fin.bad-debt-allowance-gate-enabled=false -Derp-fin.reverse-close-approval-required=false`（test infra，核实无 E2E 依赖，fin-bad-debt provision 独立于 close gate）。文档：`period-close.md` 增「期末结账向导」段；`page-structure-patterns.md` §5 从占位回填为 wizard 范式（组件选型裁决表 + 落地范式 + 反模式 + GraphQL field selection 注意）+ §4 Deferred/§6 反模式更新。
- **验证（全绿）**：`mvn clean install -DskipTests` → BUILD SUCCESS（154 模块，01:35 min）；finance service `mvn test` → 261 tests 0 failures；`TestAppActionAuthMerge` + `ErpAllWebPagesTest` → 2 passed；`npx playwright test` 新增 fin-period-close-wizard action+visual spec → 2 passed；finance 回归 fin-bad-debt + finance-voucher-post → 4 passed 无漂移。零后端 delta（git diff 仅 page.yaml + action-auth.xml + tests + docs + plan，无 Java/ORM/API/xbiz）。**full-green verification**：MISSION_DRIVER 复核全 reactor `mvn test` → BUILD SUCCESS（154 模块，08:32 min，0 failures 0 errors）。
- **结论**：F12 Tier C 最后一项 Deferred 收口。期末结账向导落地，零后端 delta 编排既有 M4 mutation。wizard 范式首例（page-structure-patterns.md §5）供 maintenance 4 步向导 successor 参考。

### 2026-07-23 (MISSION_DRIVER execute — F15 i18n 国际化标签补充 plan 全 Phase 落地)

- **任务**：执行 plan `docs/plans/2026-07-23-0818-1-f15-i18n-label-internationalization.md`（19 业务域手写层 i18n 国际化标签补充）。Phase 0/1/2/3 全部本次落地。frontend-ui-roadmap §F15 `todo → done` + 退出标准 F15 `[x]` + 测试策略 F15 缺口移除。
- **机制核实（关键）**：i18n 机制分三层。(1) **view.xml / action-auth.xml（XML 层）**：`xmlns:i18n-en="i18n-en"` 命名空间 + 元素属性 `i18n-en:label`/`i18n-en:title`/`i18n-en:displayName`，运行时按 locale 选择默认中文或英文。codegen 自身对每个 `<form>` 产 `i18n-en:title`（平台既定范式，见 `_gen/_*.view.xml`）。(2) **action-auth i18n yaml**：codegen 模板 `_{moduleName}-web.i18n.yaml.xgen` 从 action-auth 的 `i18n-en:displayName` 提取生成 `_vfs/i18n/en/_erp-*-web.i18n.yaml`（按 resource id 索引）。(3) **page.yaml（YAML/AMIS）**：**无 `i18n-en:` 属性机制**（XML 专属）；运行时 `WebPageHelper.fixPage`（`nop-entropy/nop-frontend-support/nop-web/src/main/java/io/nop/web/page/WebPageHelper.java:100-112`）仅解析 `@i18n:key` 消息包引用，需配合 `_vfs/i18n/{locale}/*.i18n.yaml`。
- **Phase 0（glossary + 工具裁决）**：扫描脚本盘点——view.xml 手写层 353 文件 / 1366 处中文属性 / **414 去重唯一 token**；action-auth 19 域 erp-* 已全覆盖；page.yaml 782 文件 / 300 token。裁决工具方式 (a)（python 脚本批量回写）。建立 `docs/design/i18n-glossary.md`（核心术语 + 单据生命周期动词 + 翻译原则 + 冲突规则），机器可读映射覆盖全部 414 token。
- **Phase 1（核心+mfg 10 域 view.xml）**：`inject_i18n_view.py`（quote-aware tag 解析、保格式、幂等、`&`/`<`/`>`/`"` 转义）注入——10 域 210 文件 / +837 i18n-en 属性 / +210 命名空间。执行期发现并修正 `Disposal P&L Account` 未转义 `&`（→ `&amp;`，XML EntityRef 错误），脚本同步补全转义。
- **Phase 2（ext 8 域 view.xml + action-auth 复核 + page.yaml 裁决）**：ext 8 域 137 文件 / +502 属性；notify 3 文件 / +1 属性。**action-auth 执行期复核**：19 域已由 F14（plan `2026-07-22-0444-3`）完整覆盖，元素级校验 473 zh displayName / 0 缺 i18n-en，本次无变更。**page.yaml 重新裁决为 Deferred**：YAML 无 `i18n-en:` 机制 + `@i18n:` 消息包属 Non-Goal + 默认中文 locale 回归风险（54 文件 title 字面量→消息 key 需 zh-CN+en 双条目，漏一条显示裸 key），归 l10n/@i18n successor，记录于 plan §Deferred But Adjudicated。
- **Phase 3（CI 门 + 文档对齐）**：新增 `docs/audits/i18n-coverage-checker.sh`（python3 内嵌，元素级 DEFECT 检查=空/含 CJK/裸 `${}`/与中文同值 + COVERAGE GAP=zh 属性无 i18n-en 配对，`--strict` 时 gap 即失败）。anti-fake-green 双向验证：注入 3 类缺陷 + 1 处 gap 均被检出；恢复后默认/strict 双 PASS。owner doc 对齐 3 处：`notify/inbox-patterns.md:129`、`field-formatting-patterns.md:19,254`、`tree-entity-patterns.md:88` 的 F15 successor 挂账改「已落地」。
- **验证（全绿）**：`xmllint --noout` 全 view.xml 无真实 parser/EntityRef 错误（仅 Nop `ui:`/`c:` xdef 命名空间约定告警，HEAD 既有非本次引入）；`bash docs/audits/i18n-coverage-checker.sh --strict` → PASS（0 defects / 0 gaps，扫描 353 view.xml + 19 action-auth）；`mvn clean install -DskipTests` → BUILD SUCCESS（154 模块，02:38 min）；`mvn test` → BUILD SUCCESS（11:21 min）。
- **结论**：F15 i18n plan 全 Phase 落地。19 域 view.xml 手写层（351 文件 / 1340 处）+ action-auth（0 缺口）i18n-en 全覆盖；CI 回归门 + glossary + owner doc 对齐 + roadmap §F15 收口。page.yaml title 因机制不匹配重新裁决 Deferred（非静默缺口，已记录）。`<layout>` 文本内 `[字段标签]`/`====>分组名====` 为文本非属性，其 i18n 由 objMeta/xmeta prop `i18n-en:displayName`（codegen 已生成 `_erp-*.i18n.yaml`）在无显式 label 覆盖时提供，不在 `i18n-en:` 属性补充面。

### 2026-07-23 (MISSION_DRIVER execute — 跨域敏感字段脱敏 plan 全 Phase 落地 + Phase 2 logistics 缺陷修正)

- **任务**：执行 plan `docs/plans/2026-07-22-1400-3-cross-cutting-sensitive-field-masking.md`（hr 4 字段 + logistics 2 字段敏感脱敏）。Phase 0/1/2 前序已完成；本次完成 Phase 3（范式文档 + visual spec）。frontend-ui-roadmap §退出标准「敏感字段脱敏覆盖」todo→done。
- **Phase 3（范式文档 + visual spec）**：`docs/design/field-formatting-patterns.md` §9 已含完整脱敏段（9.1 决策/9.2 三类打码模板/9.3 编辑态 input-password/9.4 前后端边界/9.5 反模式/9.6 PoC），本次补「写回型凭据例外」+ 反模式行 + 边界表行。新增 `tests/e2e/visual/sensitive-masking.visual.spec.ts`（2 用例：hr idCardNo/mobilePhone/bankAccountId/socialSecurityNo DOM 打码断言 + logistics apiKey/apiSecret 静态 `******` + 明文不泄漏断言；确定性 GraphQL __save 种子 + cleanup + seed 失败 graceful skip）。
- **执行期发现并修正的 Phase 2 缺陷**：visual spec 暴露 logistics `ErpLogCarrierConfig.apiKey/apiSecret` 渲染垃圾值 `un****ined`。根因：该两字段在 xmeta 中 `published="false" queryable="false"`（写回型集成凭据：前端可录、后端永不回传明文），Phase 2 落地的动态 `${LEFT(apiKey,2)}****${RIGHT(apiKey,4)}` tpl 因 GraphQL 响应不含字段值而渲染 undefined。**修正**（`module-logistics/erp-log-web/.../ErpLogCarrierConfig.view.xml`）：sub-grid-view + form view 的 apiKey/apiSecret 改用静态全打码 `******`（与社交号同模板），编辑态仍 input-password。这比「发布明文 + 前端打码」更安全（明文永不离开服务端），是 Phase 0 Decision (c)「前端层脱敏」对集成凭据的合理例外。§9.2/§9.4/§9.5 同步补文档。
- **另一发现**：hr `bankAccountId` 是 FK→`ErpMdBankAccount`，visual spec 种子须引用既有银行账户 id（种子含 id 1/2），不能填任意 long（会报 unknown-entity）。list/view tpl 打码的是 FK id 本身。
- **验证（全绿）**：`mvn install -DskipTests` 154 模块 BUILD SUCCESS（logistics view.xml 重新打包）；`mvn -pl app-erp-all test -Dtest=ErpAllWebPagesTest` 全绿（view.xml gen-control tpl 校验）；`BASE_URL=http://127.0.0.1:8011 SKIP_WEBSERVER=1 npx playwright test tests/e2e/visual/sensitive-masking.visual.spec.ts` 2 passed（23.7s）；回归 field-format.value + status-tag 18 passed 0 回归。
- **MISSION_DRIVER 全绿复核（full-green verification）**：`mvn clean install -DskipTests` 全 reactor BUILD SUCCESS（02:37 min，含 app-erp-hr-web / app-erp-logistics-web 资源重打包）；`mvn test` 全 reactor BUILD SUCCESS（11:32 min）。两道 mission 验证命令均绿。
- **环境注记**：app `application.yaml` 配 `quarkus.http.port=8011`（非 8080），E2E 用 `BASE_URL=http://127.0.0.1:8011 SKIP_WEBSERVER=1`（与历史日志一致的既有模式）。
- **结论**：跨域敏感字段脱敏 plan 全 Phase 落地。hr 4 字段 gen-control tpl 动态打码（published 字段）+ logistics apiKey/apiSecret 写回型凭据静态 `******`（published=false）。范式固化到 `field-formatting-patterns.md §9`。frontend-ui-roadmap §退出标准「敏感字段脱敏覆盖」todo→done。
