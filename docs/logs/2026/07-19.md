# Development Log - 2026-07-19

### 2026-07-19

- 完成视图按钮需求覆盖差距修复（plan `2026-07-19-1122-1` 全 6 phase 全绿）：基于 23 域独立审计报告系统性补齐 view.xml 按钮 + BizModel action。

### 2026-07-19 (later)

- 创建 PPT 选择合并记录 `docs/ppts/nop-erp-loop-engineering-merge-record.md`：从 7 个 `.ppt.html` 文件中逐页选择来源，共 33 页，记录标题修改、概念重构、内容新增等定制说明。
- **范围**：Phase 1（10 只读实体 CRUD 移除）；Phase 2（核心域 blocker：purchase Rfq/Quotation、inventory StockMove/StockTake、finance Voucher/AccountingPeriod、assets Asset、manufacturing WorkOrder）；Phase 3（扩展 11 域 blocker：projects/quality/maintenance/crm/cs/hr/aps/logistics/b2b/contract/drp）；Phase 4（16 实体 row-cancel-button 系统性补齐）；Phase 5（ErpInvTransferOrder confirm、ErpCrmEvent complete/cancel）。
- **BizModel 新增方法**（domain 状态迁移）：ErpInvStockTake（startTake/completeTake/cancelTake）、ErpInvTransferOrder（confirm）、ErpPrjProject（startProject/holdProject/resumeProject/cancelProject）、ErpQaInspection（passInspection/failInspection/reInspect）、ErpHrTimesheet（submit）、ErpApsOperationOrder（start/complete/cancel）、ErpB2bPartnerProfile（activate/suspend/deactivate）、ErpDrpLine（approveLine/rejectLine/cancelLine）、ErpPurRfq/ErpPurQuotation（cancel）、ErpFinVoucher（postVoucher/reverseVoucher）。新增 ErrorCode 3 个（inv.stock-take.illegal-transition / drp.line.illegal-transition / fin.voucher.illegal-transition / hr.timesheet-illegal-transition）。
- **范围纪律**：纯应用层 Java + view.xml + ErrorCode；零 ORM/契约/字典/种子/config 变更；非阻塞 info 级差距（导出/打印/批量审核）和 ErpAstAsset 跳转向导按钮（占位）显式 Deferred successor。
- **裁决**：purchase Rfq/Quotation 设计文档状态机（SENT/BID_CLOSED/AWARDED）与 ORM 字典（DRAFT/ACTIVE/CANCELLED）不匹配，采用 ORM 字典为权威——以审批流按钮 + cancel 映射设计语义（submit→publish、approve→award、reject→lose/cancel、cancel→CANCELLED），完整状态机字段扩展作为 P2 跟进。ErpAstAsset move/value-adjust/dispose 当前为 confirmText 占位按钮（审计 gap 已闭合，按钮存在；完整跳转向导需 view 层引入 openPage 机制）。ErpMfgWorkOrder 加 start/reportCompletion/close/cancel 按钮补全 4 域专用按钮（其中 start/reportCompletion/close 复用 BizModel 既有方法）。
- **验证状态**：`mvn clean install -DskipTests` 154 模块 BUILD SUCCESS；`mvn test` 全套件绿（含 ErpAllWebPagesTest/ErpAllWebPagesCollectTest 0 错误——所有 view.xml 通过 XDef 加载验证，所有新加按钮合法可解析）。

- 完成 assets 维修资本化（CAPITALIZE）路径 linkedVisit 防双重扣减后端对齐 + 浏览器层 E2E（plan `2026-07-19-0849-3` 全 3 phase 全绿）。
- **范围**：后端 CAPITALIZE Provider 科目分支扩展 `MaintenanceCapitalizationAcctDocProvider.createFacts` 按 `BILL_DATA_MAINTENANCE_LINKED_VISIT` 分支 Cr 2502（中转清算）/1002（银行存款）（镜像 `MaintenanceExpenseAcctDocProvider` 2256-2 范式）+ Dispatcher `MaintenanceCapitalizationPostingDispatcher.buildEvent` 补加 `BILL_DATA_MAINTENANCE_CLEARING_SUBJECT_CODE = "2502"` 透传（CAPITALIZE 之前不透传，本计划补加）+ JUnit `TestErpAstMaintenance` +4 用例（linkedVisit=true Cr 2502 / linkedVisit=false Cr 1002 回归 / reverse 回退 / 既有 CAPITALIZE 路径凭证行断言增强）；浏览器层 1 新 spec（2 用例）`ast-maintenance-linked-visit-capitalize.action.spec.ts`（linkedVisit=true 正路径：自包含建测试专用备件物料 + INCOMING 备货 + 种子设备 + DRAFT ErpMntVisit + SparePartUsage + Line + 自包含建测试资产 + ErpAstMaintenance(maintenanceVisitId=visit.id) → confirm → MAINTENANCE_ISSUE Dr 6602=50/Cr 1403=50 → submit→startWork→completeWork→decideTreatment(CAPITALIZE)→approve→post → 资产原值增量 1000+50=1050 + MAINTENANCE_CAPITALIZATION 凭证 Dr 1601=50/Cr 2502=50 中转清算（非 Cr 1002 银行存款）+ reverse 回退 1050→1000 + 红字凭证同向取负 Dr 1601=-50/Cr 2502=-50）+ linkedVisit=false 对照（独立维修无 maintenanceVisitId → Dr 1601=80/Cr 1002 既有路径无回归与 0215-1 一致）；maintenance.md §MAINTENANCE_CAPITALIZATION 补 linkedVisit 分支实现注记；e2e-runbook 业务动作表 +1 + 凭证行断言表 +1 行（MAINTENANCE_CAPITALIZATION linked-visit Cr 2502）+ 套件计数 88→89。
- **裁决**：Phase 1 3 Proof + 2 Decisions——(a) **Decision 1** 引入分支（对齐 EXPENSE 范式）：linkedVisit=true → Cr 2502 中转清算（备件已由 mnt 出库避免虚增银行付出，会计实质一致性）；(a) **Decision 2** 无 config-gate（对齐 EXPENSE Provider 实际代码范式：`MaintenanceExpenseAcctDocProvider:60-64` 无条件分支不读 config，doc-vs-code drift pre-existing 超出本计划范围）。
- **范围纪律**：纯应用层 Java（Provider 科目分支扩展 + Dispatcher 透传补加 + JUnit）+ 测试 + 文档；零 ORM/契约/字典/种子/config 变更；2502 科目已在种子（2256-2 加性追加）；多费用来源行级科目分解（SPARE_PART/LABOR/SUBCONTRACT）+ capitalizedAmount 派生算法精细化 + 资产折旧重算浏览器层 E2E 均显式 Deferred successor（plan Deferred But Adjudicated 段）。
- **验证状态**：JUnit `mvn test -pl module-assets/erp-ast-service -am -Dtest=TestErpAstMaintenance` 15 passed（11 既有 + 4 新增，0 failures，5.3s）+ 新 spec 2/2 passed（16.7s）+ maintenance/assets 既有 spec 抽样回归（ast-maintenance + mnt-ast-linked-visit-anti-double-deduct + ast-depreciation + mnt-spare-part-posting）8/8 0 新增失败 + business-actions 全套件 238 passed（mfg-variance-recompute-reversal 预先存在的 flake 经 baseline `git stash` 复现确认非本计划引入）+ `mvn clean install -DskipTests` 154 模块 BUILD SUCCESS（1:34 min）。
- 解除：2256-2 Deferred「assets 维修资本化（CAPITALIZATION）路径」RELEASED。
- 完成 logistics path-2 采购运费→到岸成本自动创建浏览器层 E2E（plan `2026-07-19-0849-2` 全 3 phase 全绿）。
- **范围**：1 新 spec（2 用例）`tests/e2e/business-actions/log-path2-landed-cost-auto-create.action.spec.ts` 覆盖 path-2 完整链路正路径（advise→completeShipment→handleTrackingWebhook DELIVERED → DRAFT ErpInvLandedCost 自动创建：docStatus=DRAFT+approveStatus=UNSUBMITTED+totalCostAmount=100+currencyId=CNY+supplierId 透传 receive.supplierId+allocationMethod=BY_AMOUNT + FREIGHT 行 costElement=FREIGHT+amount=100+apPartnerId 透传）+ freightAmount=0 边界对照（DELIVERED+SETTLED+显式断言无 ErpInvLandedCost 创建）；`playwright.config.ts` webServer JVM arg 追加 `-Derp-log.path2-landed-cost-auto-create=true`；e2e-runbook 业务动作表 +1 logistics path-2 行 + 套件计数 88→89 + JVM arg 段补 path-2 启用项；state-machine.md §path-2 补浏览器层覆盖注记 + costing-methods.md 新增「logistics path-2 到岸成本自动创建衔接点」章节；0941-2 + 1005-1 Deferred RELEASED。
- **裁决**：Phase 1 5 Decisions——(a) ErpPurReceive 前置直 `__save` DRAFT/UNSUBMITTED/UNRECEIVED 轻量隔离（**dict 字典值约束校正**：erp-pur/doc-status 仅 DRAFT/ACTIVE/CANCELLED + erp-pur/receive-status 仅 UNRECEIVED/PARTIAL/RECEIVED，原 plan 措辞 UNSUBMITTED/NOT_RECEIVED 不在字典内，执行期已校正）/ (b) Shipment setup 字段 / (c) webhook payload 复用 0941-2 范式 / (d) ErpInvLandedCost 反查 `findFirst(eqFilter('receiveId', receive.id))` receiveId 为 Long FK 列在 GraphQL schema 层暴露，对齐 backend `TestErpLogPath2LandedCost.java:224` 范式 / (e) cleanup 范围删 ErpInvLandedCostLine+ErpInvLandedCost（DRAFT 无 GL 凭证）+Shipment+Carrier+Receive 不触 sales 域。
- **范围纪律**：纯测试 + config JVM arg + 文档；零生产 Java/ORM/契约/字典/种子变更；path-2 失败重试/scanForPolling 轮询驱动 DELIVERED + path-2 外币 freight 汇兑分支 + LandedCost 后续 approve/allocate 链深度编排 E2E 显式 Deferred successor（plan Deferred But Adjudicated 段）。
- **验证状态**：新 spec 2/2 passed（19.9s）+ business-actions 抽样回归（log-delivered-freight-posting + inv-landed-cost + inv-landed-cost-reversal）7/7 0 新增失败 + business-actions 全套件 236 passed 0 新增失败（mfg-variance-recompute-reversal 预先存在的 flake 经 baseline `git stash` 复现确认非本计划引入）+ `mvn clean install -DskipTests` 154 模块 BUILD SUCCESS（1:41 min）。
- 解除：0941-2 Deferred「logistics path-2 到岸成本自动创建 E2E（若 Explore 裁决降级）」RELEASED + 1005-1 Deferred「logistics path-2 / 运费过账浏览器层」RELEASED。
- 完成 b2b ASN→采购入库行级回填后端扩展 + 浏览器层 E2E（plan `2026-07-19-0849-1` 全 3 phase 全绿）。
- **范围**：后端 `ErpB2bAsnBizModel.createReceiveFromAsn` 扩展 iterate AsnLine → ErpPurReceiveLine 字段映射（materialId/uoMId/quantity/unitPrice/taxRate/orderLineId/amount/warehouseId/lineNo）+ 新增 private helper `fillReceiveLinesFromAsn` + 新 ErrorCode `ERR_B2B_ASN_LINE_MATERIAL_REQUIRED`（materialId null/material 不存在守卫）+ 空白 AsnLine 边界（0 行合法仅建头，Decision (e)①）+ @BizMutation 强一致回滚；JUnit `TestErpB2bAsnInventoryIntegration` +3 用例（multi-line / empty / config-gate）+ 既有 1 用例更新；浏览器层 1 新 spec 2 用例 `b2b-asn-line-level-receive-fill.action.spec.ts`（多行映射正路径 + 空白 AsnLine 边界对照）；`_helper.ts` 新增 `findItems` 导出（行级回填多行反查原语）；`docs/design/b2b/edi-formats.md` 附录「createReceiveFromAsn 行级回填实现注记」（字段映射规则权威表 + 边界与守卫 + config 门控 + 跨域依赖方向）；e2e-runbook 业务动作表 +1 + 套件计数 87→88。
- **裁决**：Phase 1 字段映射 7 Decisions——(a) unitPrice 反查 PO line 透传；(b) amount=unitPrice×qty HALF_UP scale=4 派生；(c) lineNo 透传 AsnLine.lineNo；(d) warehouseId 复用 receive.warehouseId；(e) 0 行 AsnLine 合法仅建头；(f) uoMId 经 materialId → ErpMdMaterial.uoMId 反查（mandatory 必然有值），materialId null/material 不存在 → 抛守卫；实现策略 (a) 简单迭代 saveEntity；(b) 强一致回滚。
- **范围纪律**：纯应用层 Java + 测试 + 文档；零 ORM/契约/字典/种子/config 变更；Receive 后续 approve/入库触发链 E2E + 价格差异处理显式 Deferred successor（plan Deferred But Adjudicated 段）。
- **验证状态**：JUnit `mvn test -pl module-b2b/erp-b2b-service` 31 passed（含本计划 6 用例）；新 spec 2/2 passed + b2b-asn-match-receive 回归 3/3 0 新增失败；`mvn clean install -DskipTests` 154 模块 BUILD SUCCESS。
- 解除：1005-1 Deferred「createReceiveFromAsn 跨域建单深度（行级回填）」RELEASED。
- 完成视图按钮需求覆盖审计（Phase 1-3）：基于计划 `2026-07-19-0825-1-view-button-against-requirements-audit.md`
- 创建 `docs/analysis/view-button-coverage/METHODOLOGY.md`：按钮推导规则、prose→button-id 翻译字典、严重级别定义
- 通过 23 个独立子代理逐一审计每域 view.xml 按钮与设计文档要求的一致性
- 自动采集工具 `tools/analyze-view-buttons.mjs` 完成重写（per-page 分组 + per-domain 文件输出）
- 审计结果：391 实体，78.5% 无差距，25 个 blocker（6.4%），12 个 major（3.1%）
- 关键发现：15 个 state-machine 状态流转完全未在 view 层接线；7 域系统性缺失 `row-cancel-button`；只读实体仍暴露 CRUD 按钮
- 审查会后修正 METHODOLOGY.md §1.4：新增审批按钮自动生成机制（DIRECT mode codegen 自动生成 view.xml 审批按钮；WORKFLOW mode 由 nop-wf 引擎注入）。修正 severity 示例，明确 blocker 差距仅针对域专用按钮，标准 submit/approve/reject 按钮不计为差距。
- 25 个 blocker 全部验证通过（均为域专用按钮，非审批按钮）
- 下一步：修复 blocker 差距的 ticket 应进入 `docs/backlog/` 或 `docs/plans/`
- 前端 UI 路线图二次修订（二轮审查修正）：修复 S1-S6 自洽性 + G1-G5 缺口 + 测试策略 + 优先标签 + 重排编号
- 全面审计 frontend-ui-roadmap.md 覆盖完整性：交叉验证 18+1 域 ui-patterns.md（8,164 行设计文档）+ docs-for-ai 平台约束 + view.xml 质量抽样 + 已知 bugs/analysis
- 审计发现 8 类重大缺口并修补：F12 表格从 10→16 项（加 finance 结账向导、hr 员工 tabs、contract 多标签页、timesheet 周网格等）、F13 扩展为 7 视图（CRM + CS + Project 看板）、新增 F16（16 复杂手写页面）、跨切面模式（通知收件箱、字段脱敏、引用预览）、依赖图/退出标准/测试策略全更新
- 撰写分析报告 `docs/analysis/2026-07-19-frontend-ui-design-completeness-and-quality-analysis.md`：覆盖完备性（✅ 良好）、设计合理性（⚠️ 2 个文档缺口 + 3 个平台约束不匹配）、补充需求（10 项 P0-P3 分类）
- 生成 V5 (Mixed-creative) HTML 演示文稿 `docs/ppts/.gen-variants/nop-erp-loop-engineering-v5.html`（32 页，122KB，基于 `nop-erp-loop-engineering-outline.md`）。布局：双列对比、统计网格、卡片网格、时间线、流程图、高亮框、金色引用混合使用。SVG 图表 16 个 + Mermaid 图 4 个 + Font Awesome 图标 104 个。验证：32 section 标签匹配闭合，所有 SVG 含显式宽高，0 个 `width="100%"`。
