# Development Log - 2026-07-19

### 2026-07-19（plan 2026-07-19-0730-1 外币应收票据贴现 exchangeGainLoss 派生缺失修复 + 真实 mutation 路径 E2E 迁移）

- **背景**：执行 `docs/plans/2026-07-19-0730-1-fx-notes-receivable-discount-exchange-gain-loss-builder.md`。承接 `2026-07-19-0120-1` + `2026-07-19-0330-1` 同型 Deferred「`ErpFinNotesReceivableProcessor.buildDiscount` 外币 exchangeGainLoss 派生缺失」successor——触发条件「外币票据业务实际生产路径触发 6051 派生缺失被复现时」经实时仓库核实**已满足**：0120-1 Phase 1 冷核实已实证 `ErpFinNotesReceivableProcessor.java:249` `discount.setExchangeGainLoss(BigDecimal.ZERO)` **无条件硬编码**，致 `discount` @BizMutation 永远不会触发 6051 凭证行；该缺陷使 owner doc `docs/design/finance/treasury.md §业财过账` 明示的「NOTES_RECEIVABLE_DISCOUNTED [借/贷] 汇兑损益(外币)」语义在生产路径不可达，属规则 13 不可降级的「owner-doc 漂移」。iter-1/iter-2/iter-3/iter-4 草案审查闭环（cash-at-spot plug 公式六处参考点一致性 + iter-1 BI-1 不平衡公式拒绝 + iter-2 误判合并纠正 + iter-3 owner-doc 注记公式一致性 + iter-4 accept）。
- **范围**：4 Phase 全绿。
  - **Phase 1（Decision）**：3 Decisions（contract extension shape + cash-at-spot plug formula + config-gate default false）+ 1 Proof（实时仓库核实决策依赖锚点：`ErpFinNotesDiscount.exchangeRate/exchangeGainLoss` 列 propId=10/11 已存在 + `NotesReceivableAcctDocProvider:64-80` 已 signum 分支无须改 Provider）+ `treasury.md §业财过账` 增「DISCOUNTED exchangeGainLoss 派生实现注记」段闭合 owner-doc 漂移。
  - **Phase 2（Fix）**：Java `ErpFinNotesReceivableProcessor.buildDiscount` cash-at-spot plug 公式派生 `exchangeGainLoss = amountFunctional − discountInterestFunctional − netAmount`（符号语义：外币升值 → exchangeGainLoss<0 → Cr 6051 汇兑收益 / 外币贬值 → exchangeGainLoss>0 → Dr 6051 汇兑损失）+ `discount.netAmount = netAmountSource × spotRate` cash-at-spot 折算；`discount` 公共方法 5 参数签名扩展（`@Optional @Name("exchangeRate") BigDecimal exchangeRate`）——执行期发现 Nop GraphQL BizModel **不允许同名 action 重载**（`方法[FunctionModel:discount(5)]和[FunctionModel:discount(6)]对应的action名称重复`），改为单 5 参数方法（偏离 iter-0 重载设计已纠正，IBiz/BizModel 同步单方法实现，旧 4 参数 GraphQL 调用经 `@Optional` 透明兼容）；`ErpFinConstants.CONFIG_NOTES_FX_GAIN_LOSS_ENABLED` 常量 + Processor 内联 `notesFxGainLossEnabled()` reader（对齐 `ExchangeRevaluationService:254` 范式不引入 ErpFinConfigs 抽象）+ `isForeignCurrency(note)` + `resolveFunctionalCurrencyId()` private helper（对齐 `ExchangeRevaluationService:280-287` 范式查询 `ErpMdCurrency.isFunctional=TRUE`）+ `ErpFinErrors.ERR_NOTES_DISCOUNT_FX_RATE_REQUIRED` ErrorCode 保留作 hard-fail successor 候选（默认不抛，null exchangeRate 走 ZERO 兜底对齐 Decision 2 公式语义）；config-gate 默认 false 向后兼容；4 JUnit 新测试方法全绿。
  - **Phase 3（E2E 迁移）**：`fin-notes-receivable-fx-discount.action.spec.ts` 0120-1 workaround（`ErpFinVoucher__post` 直驱）迁移至真实 `discount(exchangeRate=6.7000)` mutation 路径——(1) FX lifecycle + 6051 抑制（exchangeRate 省略兜底）3 行凭证 / (2) FX 6051 触发（cash-at-spot plug，外币升值场景）4 行凭证 Cr 6051=3.3000 复式平衡 Dr 663.3000+Dr 6.67=669.97 ≡ Cr 3.3000+Cr 666.67=669.97 / (3) CNY 对照 3 行凭证（无 6051）；直驱原语 `postVoucher` / `buildNotesReceivableDiscountedEvent` 一并清理；config JVM arg `-Derp-fin.notes-fx-gain-loss-enabled=true` 由 `playwright.config.ts` webServer 启用；3 测试全绿 + 1430-1 NR 7 测试 0 回归 + 1430-1 NP 5 测试 0 回归 + finance-voucher-post 1 测试 0 回归 = 16 passed。
  - **Phase 4（owner-doc + RELEASED + 日志）**：`treasury.md §业财过账` 实现注记段（已在 Phase 1 落地）+ e2e-runbook JVM arg 段 + 凭证行断言表 DISCOUNTED FX 行 + 业务动作表更新；0120-1 + 0330-1 Deferred RELEASED；backlog +1 done 行 + 本聚合条目。
- **关键裁决**：(a) **cash-at-spot plug 范式**（iter-1 BI-1 修订）—— `discount.netAmount = netAmountSource × spotRate` cash-at-spot 折算（覆盖原 functional 口径）+ `exchangeGainLoss = amountFunctional − discountInterestFunctional − netAmount` plug 平衡差额；iter-1 BI-1 已拒绝的 alt (b) 公式（`netAmountSource × (exchangeRate − note.exchangeRate)` 不动 `discount.netAmount`）经独立审查冷核实产生不平衡凭证（Dr 1002=660 + Dr 6603=6.67 + Dr 6051=3.30 = 669.97 ≠ Cr 1121=666.67），修订后数值示例经复式平衡验证：Dr 663.3000 + Dr 6.67 = Cr 666.67 + Cr 3.3000 = 669.97 ✓；(b) **Nop GraphQL 不允许 BizMutation 同名重载**（执行期发现）—— 改为单 5 参数方法 + `@Optional @Name("exchangeRate")` 使调用方可省略，下游既有 4 参数 GraphQL 调用透明兼容；(c) **null exchangeRate 走 ZERO 兜底**（不抛错）—— 守卫逻辑经实证与 Decision 2 公式（`未传入 → exchangeRate=note.exchangeRate 兜底 → ZERO`）冲突，改为静默 ZERO 兜底，`ERR_NOTES_DISCOUNT_FX_RATE_REQUIRED` ErrorCode 保留作未来 hard-fail 模式 successor 候选。
- **范围纪律**：本计划仅修复 DISCOUNTED 路径 buildDiscount 缺陷 + 5 参数 `discount` 契约扩展 + 0120-1 workaround 迁移 + owner-doc 注记；不引入 NR RECEIVED/ENDORSED/COLLECTION 路径 FX 分支（6051 行）+ 不引入 NP Provider FX 分支 + 不实现多账户现金预测分摊 + 不引入汇率源数据表 + 不做外币重估公式精细化（按到期日 vs 贴现日 vs 票面日汇率选择）+ 不做凭证化外的字段物化（`ErpFinNotesDiscount.exchangeGainLoss` 列 propId=11 已承载）+ 不做 `discount` 审批工作流（DIRECT 路径对齐 1430-1）。各 successor 详见 `Deferred But Adjudicated`。
- **验证状态**：(Phase 2) `mvn test -pl module-finance/erp-fin-service -am` JUnit 全绿（StateMachine 12/12 含新增 3 + Posting 6/6 含新增 1，含 `testDiscountFxPosts6051VoucherLineAndBalance` 复式平衡断言 Σ Dr=669.97 ≡ Σ Cr=669.97）；(Phase 3) `PLAYWRIGHT_PORT=8011 npx playwright test fin-notes-receivable-fx-discount.action.spec.ts` 3 passed（49.9s）+ 1430-1 NR 7/7 0 回归 + 1430-1 NP 5/5 0 回归 + finance-voucher-post 1/1 0 回归（共 16 passed）；(整体) `mvn clean install -DskipTests` 154 模块 BUILD SUCCESS。

### 2026-07-19（plan 2026-07-19-0330-2 外币应付票据 ISSUED/HONORED 浏览器层 E2E）

- **背景**：执行 `docs/plans/2026-07-19-0330-2-fx-notes-payable-issued-honored-browser-e2e.md`。承接 `2026-07-19-0120-1` Deferred「应付票据外币 ISSUED/HONORED 多币种路径浏览器层 E2E」successor——触发条件「外币应付票据业务路径浏览器层 E2E 需求落地时」经实时仓库核实**经解释为已满足**：precedent 0120-1/0330-1 同型裁决 + AGENTS.md §当前项目阶段明示「各域细化端到端验证」为当前重点；1430-1 已覆盖单币种，外币段同型规则但不同实体（`ErpFinNotesPayable` 非 `ErpFinNotesReceivable`）+ 不同科目分解（2202/2203 vs 1121/1122）+ 不同 Provider（`NotesPayableAcctDocProvider` 无 FX 分支）。iter-1/iter-2 草案审查闭环（iter-1 修正 MAJOR-1 单币种对照测试用例 + MAJOR-2 dishonor 永不产凭证核实 + 6 MINOR；iter-2 accept）。
- **范围**：3 Phase 全绿。Phase 1 Explore（4 file:line 锚点逐行核实 NP Provider/Processor/Dispatcher FX 透传链 + 2 Decisions 含 spec 结构 + 数值表）/ Phase 2（1 新 spec 5 用例全绿 46.6s + finance 抽样回归 20 passed 0 新增失败 + business-actions 全套件 232 passed / 1 pre-existing flake 经 git stash 验证无关 + 154 模块 BUILD SUCCESS 1:38min）/ Phase 3（e2e-runbook 业务动作表 +1 finance FX NP lifecycle 行 + 套件计数 86→87 + 0120-1 NP 外币 Deferred RELEASED + backlog +1 done + 日志聚合条目）。
- **新 spec**：`fin-notes-payable-fx-lifecycle.action.spec.ts` 5 用例——(1) FX ISSUED 路径（USD note → ISSUED + posted=true + 2 行凭证 Dr 2202=6666.7000/Cr 2203=6666.7000 全 functional CNY，无 6051——Provider ISSUED 路径无 FX 分支为设计选择）；(2) FX HONORED 路径（USD note ISSUED 入口 → HONORED + posted=true + 2 行凭证 Dr 2203=6666.7000/Cr 1002=6666.7000 经 NOTES_PAYABLE_HONORED）；(3) FX writeOff REVERSAL 路径（USD note ISSUED+posted=true → WRITE_OFF + posted=false + REVERSAL 红字凭证行同向取负 Dr 2202=-6666.7000/Cr 2203=-6666.7000 对原 ISSUED NORMAL 凭证，FX 正确性继承自原 NORMAL 已 functional）；(4) FX dishonor 路径（USD note ISSUED 入口 → DISHONORED 显式断言无凭证对齐 1430-1 test 3 + `doDishonor:141-144` 仅 setStatus 无 postingDispatcher + 非法迁移守卫，无 FX 特定语义 spec 行为对称纳入）；(5) 单币种对照测试用例（CNY note 同 issue/honor/writeOff 动作断言凭证行集合与 FX 路径科目+方向完全一致，唯一变量为金额 1000 vs 6666.7000，证明 Provider 无 FX 分支语义对单/外币一致）。
- **范围纪律**：纯测试 + 文档，零生产 Java/ORM/契约/config/种子变更。NP Provider FX 分支引入 6051（外币应付票据开出/兑付时认列汇兑损益）归 Deferred 显式 successor（须先有产品需求落地 + 独立 ask-first 计划，不阻塞本计划结束）。dishonor 路径无 postingDispatcher 调用 → 永不产凭证（FX 段亦然，无 FX 特定语义，本计划 spec 行为对称纳入非 FX 特定验证）。writeOff REVERSAL FX 正确性继承自原 NORMAL 已 functional，无须 REVERSAL 路径单独 FX 处理。
- **验证状态**：新 spec 5 passed（46.6s）+ finance 抽样回归 20 passed / 0 failed（fin-notes-payable 5 + fin-notes-receivable 7 + fin-notes-receivable-fx-discount 3 + fin-notes-receivable-fx-lifecycle 4 + finance-voucher-post 1 = 20 用例 2.4m）+ business-actions 全套件 232 passed / 1 pre-existing flake（mfg-variance-recompute-reversal 经 git stash --include-untracked 验证与本次 spec 改动无关，与 0120-1/0330-1 closure 同型 flake，27.7m）+ `git diff module-finance/erp-fin-service/src/main/java/` 输出空（零生产 Java 变更）+ `mvn clean install -DskipTests` 154 模块 BUILD SUCCESS（01:38 min）。

### 2026-07-19（plan 2026-07-19-0330-1 外币应收票据 honor/endorse/collect 浏览器层 E2E）

- **背景**：执行 `docs/plans/2026-07-19-0330-1-fx-notes-receivable-honor-endorse-collect-browser-e2e.md`。承接 `2026-07-19-0120-1` Deferred「外币票据 honor/dishonor/endorse/collect 路径浏览器层 E2E」successor——触发条件「外币票据 honor/endorse 业务路径浏览器层 E2E 需求落地时」经实时仓库核实**经解释为已满足**：precedent 0120-1 同型裁决 + AGENTS.md「当前项目阶段」明示当前重点含「各域细化端到端验证」。iter-1/iter-2/iter-3 草案审查闭环（iter-1 拆出 Java builder DISCOUNTED 缺陷独立 successor + iter-2 修正 collect/dishonor 凭证行为误述 + iter-3 accept），范围收窄至纯 FX NR honor/endorse/collect 浏览器层 E2E。

- **Phase 1（Explore）**：两 Proof + 三 Decision 落地，全部经实时仓库逐行核实：
  - **Proof 1**：`ErpFinNotesReceivableProcessor.java:42/53/62/68/76/82/91` Facade 签名（receive/discount/endorse/collect/honor/dishonor/writeOff）；`:157/doReceive:161` 调 postingDispatcher(NOTES_RECEIVABLE_RECEIVED)；`:184/doEndorse:192` 调 postingDispatcher(NOTES_RECEIVABLE_ENDORSED)；`:199/doHonor:203` 调 postingDispatcher(NOTES_RECEIVABLE_COLLECTION)；`:68-74` collect 仅 setStatus(NOTES_RECV_COLLECTION_PENDING)+updateEntity 无 posting；`:82-89` dishonor 仅 setStatus(NOTES_RECV_DISHONORED)+updateEntity 无 posting。`NotesPostingDispatcher.java:84` `BILL_DATA_FACE_AMOUNT = nz(note.getAmountFunctional())`。`NotesReceivableAcctDocProvider.java:82-89` ENDORSED 路径无 FX 分支；`:90-95` COLLECTION 路径无 FX 分支。`ErpFinConstants` NOTES_RECV_COLLECTION_PENDING="COLLECTION_PENDING"（无 COLLECTED 泄漏）。
  - **Proof 2**：`dishonor:82-89` 仅 setStatus 无 posting；`validateTransitionForHonorOrDishonor:124-129` 守卫 status != COLLECTION_PENDING 时抛 illegalTransition。结论：dishonor 不产凭证，spec 显式断言无凭证。
  - **Decision 1**：spec 结构采纳 (a) 新建独立 spec `fin-notes-receivable-fx-lifecycle.action.spec.ts`。
  - **Decision 2**：FX 数值表汇率 6.6667 + amountSource USD 1000 + amountFunctional CNY 6666.7000。
  - **Decision 3**：单币种对照测试用例放置采纳 (a) 本 spec 内独立 test()。

- **Phase 2（spec 落地 + 回归）**：新建 `tests/e2e/business-actions/fin-notes-receivable-fx-lifecycle.action.spec.ts`（4 用例）：
  - **(1) FX endorse 路径**：建 USD note（status=RECEIVED 直置入口）+ partnerId=1 → `endorse` mutation → ENDORSED + posted=true + 2 行凭证（Dr 2202=6666.7000 / Cr 1121=6666.7000 全 functional CNY，无 6051——Provider ENDORSED 路径无 FX 分支为设计选择）。
  - **(2) FX collect（无凭证）+ honor 路径**：建 USD note → `collect` → COLLECTION_PENDING + posted=false + 显式断言无凭证（对齐 1430-1 l.213-215）→ `honor` → HONORED + posted=true + 2 行凭证（Dr 1002=6666.7000 / Cr 1121=6666.7000 经 NOTES_RECEIVABLE_COLLECTION 业务类型）。
  - **(3) FX dishonor 路径**：建 USD note + `__save` 直置 status=COLLECTION_PENDING 入口 → `dishonor` → DISHONORED + 显式断言无凭证（对齐 1430-1 dishonor 范式）+ 非法迁移守卫（RECEIVED 态直接 dishonor 抛守卫）。
  - **(4) 单币种对照**：建 CNY note → 同 (1)(2) 动作 → 断言凭证行集合 = FX 路径科目+方向完全一致，唯一变量为金额（证明 Provider 无 FX 分支语义对单/外币一致）。
  - 全部凭证行翻转经 `verifyState`（`__get`）/`findVoucherIdByBillCode`/`assertVoucherLines` 独立断言。
  - **验证**：新 spec 4 passed（46.6s）+ finance 抽样回归 16 passed / 0 failed（fin-notes-receivable 7 + fin-notes-receivable-fx-discount 3 + fin-notes-payable 5 + finance-voucher-post 1 = 16 用例 1.9m）+ business-actions 全套件 227 passed / 1 pre-existing flake（mfg-variance-recompute-reversal 经 git stash --include-untracked 验证与本次 spec 改动无关，与 0120-1 closure 同型 flake，27.7m）+ `git diff module-finance/erp-fin-service/src/main/java/` 输出空（零生产 Java 变更）+ `mvn clean install -DskipTests` 154 模块 BUILD SUCCESS（01:38 min）。
  - **Driver 复核（commit 前）**：`mvn install -DskipTests` 154 模块 BUILD SUCCESS（01:28 min，省略 clean 复用 target/）+ `mvn test` 一次失败定位 `TestErpMfgProductionVariance.testManualCalculateVariancesIdempotent` 的 `DEL_VERSION` 1ms 时间戳 flake（1784411325724 vs 1784411325725，零 Java 改动，同型已知 flake），mfg-service 单模块重试 128/128 全绿 + 下游模块（mfg → ... → app-erp-all）全 SUCCESS（03:49 min）= 模块全绿基线成立。

- **Phase 3（文档对齐 + RELEASED 登记 + 日志）**：
  - `docs/testing/e2e-runbook.md`：业务动作表 +1 finance FX NR honor/endorse/collect 行 + 套件计数段补 0330-1 增量（85→86 spec，业务动作套件表头 85→86）；描述段补 FX NR honor/endorse/collect 路径覆盖项。
  - `docs/backlog/README.md`：+1 done 行（0330-1 ✅ done）。
  - `docs/plans/2026-07-19-0120-1-...md`：Deferred「外币票据 honor/dishonor/endorse/collect 路径浏览器层 E2E」段补 `**RELEASED by 2026-07-19-0330-1**` 行 + 实施摘要（FX NR honor/endorse/collect 完整生命周期 E2E 覆盖 + 单币种对照断言）。
  - **范围纪律注记**：Java builder `ErpFinNotesReceivableProcessor.buildDiscount:249` 外币 exchangeGainLoss 派生缺失（仅影响 DISCOUNTED 路径，与 honor/endorse/collect 完全无关）+ RECEIVED/ENDORSED/COLLECTION 路径 FX 分支引入 6051 均归 Deferred 显式 successor（属不同结果面，须独立 ask-first 计划）。

### 2026-07-19（plan 2026-07-19-0120-2 SPC 计数型（P/NP/C/U）控制图计算引擎 + 看板 echarts 接入）

- **背景**：执行 `docs/plans/2026-07-19-0120-2-spc-attributes-chart-engine-and-dashboard.md`。承接 `2026-07-17-2010-1` Deferred「计数型 SPC 计算引擎」successor——触发条件「计数型 SPC 计算引擎落地时」经实时仓库核实**已满足**：2010-1 落地的 `ErpQaDashboardBizModel.getSpcControlChartData` 已含顶层 chartType 字段（`:269`）但 per-sample 仅有 mean/isOutOfControl/violatedRules；`SpcControlLimitCalculator.recalculate` 仅按 X̄̄-R 范式不分支 chartType；计数型分支完全缺失。本计划落地计数型 SPC 计算引擎 + 看板 echarts 接入。

- **Phase 1（Decision）**：闭合 5 items——(a) **Proof** 经实时仓库核实 `ErpQaInspectionLine.result` 字段（ext:dict `erp-qa/inspection-result` 4 态）+ `ErpQaNonConformance.quantity` 字段（DECIMAL）+ `inspectionId` 反向挂链 inspection，确认计数型数据源生产路径可达；(b) **能力指数策略** 裁定 (a) 完全不算（计数型 cp/cpk/pp/ppk/cpm 全 null + capabilityLevel=null，保守不发明新指数对齐 Non-Goal）；(c) **采样数据派生路径** 裁定 P/NP 走 inspection line REJECTED 计数 / C/U 走 NCR.quantity 累计，按 APPROVED inspection 切分子组；(d) **看板 chartId 解析** 裁定 (a) 新增 config `erp-dash.qa-spc-default-attributes-chart-id` 与既有并列；(e) **ORM 加性列类型** 裁定 Integer（对齐 subgroupSize Integer 范式 + precision=10 满足子组样本量级）。

- **Phase 2（后端实现）**：
  - **ORM 加性追加**：`module-quality/model/app-erp-quality.orm.xml` ErpQaSpcSample 实体加 `defect_count Integer precision=10 nullable=true`（propId=23）+ `inspected_count Integer precision=10 nullable=true`（propId=24），向后兼容计量型 chart（不影响既有 calc/采样/看板）。codegen 增量重新生成经 `mvn install -pl erp-qa-codegen,erp-qa-dao -am` 触发，`_ErpQaSpcSample.java` 含 getDefectCount/setDefectCount/getInspectedCount/setInspectedCount（propId 23/24 验证通过）。
  - **新建 `AttributesControlLimitFormulas`**（`module-quality/erp-qa-service/.../spc/AttributesControlLimitFormulas.java`，210 行）：4 静态方法 calcP/calcNp/calcC/calcU 返回内部 `Result` POJO（cl/ucl/lcl 三元组）；公式严格按行业标准（P CL=p̄=Σd/Σn + UCL/LCL=p̄±3√(p̄(1−p̄)/n̄；NP CL=n·p̄；C CL=c̄=Σc/k；U CL=ū=Σc/Σn）；BigDecimal HALF_UP scale=6；负数下限钳 `BigDecimal.ZERO.setScale(SCALE, RoundingMode.HALF_UP)`；sqrt 经 `Math.sqrt(double)`。
  - **`SpcControlLimitCalculator.recalculate` chartType 分支**：重构为两分支（chartType==null 走计量型以向后兼容）；新增 `isAttributesChart(String)` public static helper + `calculateAttributes(String, List<samples>)` private 派发 4 chartType；<20 samples 守卫不变；既有计量型 X̄̄-R/d2/D3/D4 路径完整保留（包括 rangeUpper/rangeLower 本地变量）。
  - **`SpcSamplingService.collectSamples` chartType 分支**：入口增 `isAttributesChart` 分支派发；新增 `collectAttributesSamples(chart, parameterId, subgroupSize)` 方法——按 APPROVED inspection 切分子组（chart.subgroupSize 语义改为子组内 inspection 数），每子组 1 sample；P/NP 从 inspection line REJECTED 计数 defectCount + line 总数作 inspectedCount；C/U 从 NCR.quantity 累计 defectCount + inspection 数作 inspectedCount；幂等键 `ATTR#<firstInspectionCode>`；mean 字段语义保留为 defectRate 供既有 echarts line 渲染兼容；新增 `loadNcrsByInspectionId` + `buildAttributesSampledKeys` helpers。
  - **`SpcCapabilityCalculator.calculateCapability` chartType 分支**：计数型保守降级（Phase 1 Decision (a)）——defectRate 序列 mean/σ 派生 grandMean/overallStdDev + totalObservations = Σ inspectedCount；cp/cpk/pp/ppk/cpm/capabilityLevel 全 null；INADEQUATE 回写守卫 `if (!isAttributes && ...)` 计数型跳过；既有计量型 Cp/Cpk/Pp/Ppk/Cpm + classifyByCpk + QualityGoal/RiskRegister 回写链完整保留。
  - **`ErpQaDashboardBizModel.getSpcControlChartData` per-sample 响应加性补字段**：`loadSpcSamples` 加 defectCount/inspectedCount/defectRate（仅当 sample 字段非 null 时填入，计量型样本此三字段透明不破坏）；chartId 解析新增 `qa-spc-default-attributes-chart-id` fallback（入参 > qa-spc-default-chart-id > qa-spc-default-attributes-chart-id > 最近一张 by id desc）。
  - **新增 config** `erp-dash.qa-spc-default-attributes-chart-id`：`ErpQaConstants.CONFIG_DASH_QA_SPC_DEFAULT_ATTRIBUTES_CHART_ID` 常量 + `ErpQaConfigs.getDashQaSpcDefaultAttributesChartId()` reader。

- **Phase 3（JUnit + 种子 + 看板 echarts 分支）**：
  - **`TestErpQaSpcAttributesControlLimit`** 7 用例纯函数单测（P/NP/C/U 各 1 公式验证 + emptyListReturnsNulls + nonNegativeLclWhenHighDefectRate + sizeMismatchThrows 边界场景 3 个），全绿。
  - **`TestErpQaSpcAttributesSampling`** 5 用例端到端集成（P 图采样 + C 图采样 + recalculate 路由 AttributesControlLimitFormulas 公式 + 计量型 chart 0 回归守门 + 计数型采样幂等），全绿。
  - **种子**：`erp_qa_spc_chart.csv` 加性 4 chart 行（id=2/3/4/5 P/NP/C/U 各 1，calcStatus=CALCULATED + cl/ucl/lcl 公式派生值精确填入：P CL=0.046500/UCL=0.109670；NP CL=4.65/UCL=10.967；C CL=9.5/UCL=18.747；U CL=0.072362/UCL=0.153265）；`erp_qa_spc_sample.csv` 加性 80 sample 行（id=101~180，每 chart 20 子组，defectCount/inspectedCount 字段非空）。
  - **看板 page.yaml** `spcControlChart` adaptor 按 chartType 字符串值分支渲染：`isAttributes = chartType === 'P' || ...`；seriesData value 按 isAttributes 取 defectRate（s.defectRate 优先 fallback defectCount/inspectedCount 派生）或 mean（计量型）；tooltip 计数型增 defectCount/inspectedCount 显示；yAxis name 按 isAttributes 切换「缺陷率」/「均值」；title chartType 透明显示。
  - 154 模块 `mvn clean install -DskipTests` BUILD SUCCESS（1:31 min）+ qa-service 110 tests 0 failures/0 errors（既有 98 + 新增 12）。

- **Phase 4（E2E + owner-doc 对齐 + RELEASED 登记）**：
  - **`qa-spc-attributes-chart.action.spec.ts`** 4 用例（P/NP/C/U chartType 各 1）：建 20 子组 sample（满足 SPC_MIN_SUBGROUPS_FOR_CONTROL_LIMIT）→ `ErpQaSpcChart__recalculateControlLimit` mutation → calcStatus=CALCULATED + cl/ucl/lcl 非空 + UCL ≥ CL ≥ LCL 不变量（P 图 CL ∈ (0,1)；NP 图 CL ∈ (0, n=100)）。
  - **`qa-dashboard-spc-attributes.value.spec.ts`** 4 参数化用例：经 GraphQL `ErpQaDashboard__getSpcControlChartData(chartId=...)` 取种子 chartId=2/3/4/5 响应，断言顶层 chartType 字段 + per-sample defectRate/defectCount/inspectedCount 字段非空 + cl/ucl/lcl 一致性 + defectRate = defectCount/inspectedCount 派生关系。
  - **owner-doc 对齐**：`docs/design/quality/spc.md` 修正 chartType 数字编码标注为字符串值（iter-1 审查 MAJOR 1：`X_BAR_R=10/.../U=70` → `X_BAR_R/X_BAR_S/X_MR/P/NP/C/U`）+ §关键流程 2 计数型公式实现注记（P/NP/C/U 公式 + 负数下限钳 0 + chartType 分支）+ §ErpQaSpcSample 表补 defectCount/inspectedCount 2 列说明。
  - **2010-1 Deferred RELEASED**：补 `**RELEASED by 2026-07-19-0120-2**` 行。
  - **e2e-runbook 业务动作表 +1 qa 计数型 SPC 行** + 套件计数段补 0120-2 增量；**backlog README +1 done 行**。

- **验证**：
  - JUnit 12 passed（TestErpQaSpcAttributesControlLimit 7 + TestErpQaSpcAttributesSampling 5）
  - qa-service 110 tests 0 failures/0 errors（既有 98 + 新增 12）
  - 154 模块 `mvn clean install -DskipTests` BUILD SUCCESS（1:31 min）
  - 新 E2E spec TypeScript 编译对齐既有 spec 范式（Playwright config 无 tsconfig，预存 ES5/Promise 警告非本计划引入）

- **范围裁决 / ORM ask-first 授权**：
  - **范围内**：ORM 加性 2 列（保护区域 ask-first 经 mission-driver 显式指令授权）+ 5 后端 Java 类变更 + 1 AMIS page.yaml + 2 种子 CSV + 2 测试 java + 2 E2E spec + 3 文档对齐（spc.md/e2e-runbook/backlog）+ 1 RELEASED 登记。
  - **Non-Goal**：不改 chartType dict（已存在仅消费）/ 不动计量型路径（X_BAR_R/X_BAR_S/X_MR 既有 calc/采样/看板/测试一律不动）/ 不发明计数型能力指数（保守 (a) 完全不算）/ 不实现 WesternElectric 计数型适配（仅 UCL/LCL 越界判定）/ 不实现 np-chart sample size 自适应 / 不做 SPC 实时告警 / 不做计数型 echarts 高级图表类型 / 不做 SPC 多变量/short-run/CUSUM/EWMA / C/U opportunityCount 多机会维度归 successor。
  - 纯应用层 Java + 加性 ORM 列 + 种子 + 测试 + 文档。

- **后续**：2010-1 Deferred「计数型 SPC 计算引擎」已 RELEASED。下一步聚焦看板运行时视觉/浏览器回归（dashboards.snapshot/visual quality 段）+ 各域细化端到端验证。

### 2026-07-19（plan 2026-07-18-2251-2 Finance 坏账准备计提反向红冲闭环）

- **背景**：执行 `docs/plans/2026-07-18-2251-2-finance-bad-debt-provision-reversal.md`。承接跨域过账红冲缺口系统性审计 Phase 2 finance 域最后一个 successor（1745-3 Deferred「坏账计提（provision）红冲」——触发条件「provision 凭证化落地时」经实时仓库核实**已满足**：`BadDebtProvisionService.runBadDebtProvision` 经 `CloseVoucherWriter.writeVoucher` 实测已生成 BAD_DEBT_RESERVE/RELEASE 凭证 + ErpFinVoucherBillR 业财回链；原 1745-3 Deferred 措辞系采信 0413-2 旧记忆过时陈述）。正向链路完整，反向红冲入口完全缺失——一旦期末计提执行（误选期间/误算/业务变化需重提）无回滚入口。

- **Phase 1（Decision）**：闭合 3 Decisions——(a) **多凭证红冲策略**：反向全部（按 `ErpFinVoucherBillR` 反查所有 isReversed=false NORMAL 凭证原子红冲；经实时仓库核实 `ErpFinPostingProcessor.reverseProcess:199-247` 已内部循环 `findAllPostedVouchers` 返回的全部未冲销凭证 + `markOriginalVoucherReversed` 补标 isReversed=true，故 service 层只须按 (billCode, businessType) 调一次 `FinPostingExecutor.reverse`，平台内部原子红冲该 billCode 下所有未冲销凭证）；(b) **入参形态**：`reverseBadDebtProvision(periodId)` 单参对称正向 + 新建 `BadDebtProvisionReversalResult` DTO（避免既有 `BadDebtProvisionResult` 语义污染）；(c) **period 状态门控**：服务层显式守卫 `period.status != CLOSED_FINAL`（提供清晰错误 token + GraphQL 友好错误消息，不依赖 `ErpFinVoucher` 写入时的期间状态校验）。

- **Phase 2（后端实现）**：
  - **`BadDebtProvisionService.reverseBadDebtProvision(Long periodId, IServiceContext context) → BadDebtProvisionReversalResult`** 新方法（`module-finance/erp-fin-service/src/main/java/app/erp/fin/service/baddebt/BadDebtProvisionService.java`）：守卫 `period.status != CLOSED_FINAL` → 按 `ErpFinVoucherBillR` 反查该期间全部 `billCode = BAD_DEBT_RESERVE_BILL_CODE_PREFIX + period.code` 或 `BAD_DEBT_RELEASE_BILL_CODE_PREFIX + period.code` 凭证（**完全匹配非 LIKE**——`CloseVoucherWriter.writeVoucher` 实测 billCode 确定性派生无 UUID 后缀）+ 过滤 `isReversed=false` NORMAL 凭证 → 守卫未找到抛 `ERR_BAD_DEBT_PROVISION_NOT_FOUND` → 按 (billCode, businessType) 调一次 `FinPostingExecutor.reverse` → 返回 DTO。`findUnreversedProvisionVouchers` public 方法暴露供测试验证。
  - **`BadDebtProvisionReversalResult`** DTO（`module-finance/erp-fin-dao/src/main/java/app/erp/fin/dao/dto/BadDebtProvisionReversalResult.java`）：含 `periodId/periodCode/reversedReserveCount/reversedReleaseCount/reversedReserveAmount/reversedReleaseAmount` + 派生 `getTotalReversedCount()`。
  - **`IErpFinBadDebtBiz.reverseBadDebtProvision(@Name("periodId") Long, IServiceContext)`** 接口声明 + **`ErpFinBadDebtBizModel.reverseBadDebtProvision`** `@BizMutation` 委派 service。
  - **2 ErrorCode**：`ERR_BAD_DEBT_PROVISION_NOT_FOUND`（`erp.err.fin.bad-debt-provision.not-found`）+ `ERR_BAD_DEBT_PROVISION_PERIOD_FINAL_CLOSED`（`erp.err.fin.bad-debt-provision.period-final-closed`），ARG_PERIOD_ID/ARG_PERIOD_ID 参数声明（`ErpFinErrors.java`）。
  - 事务边界跟随 Facade `@BizMutation` 默认事务（REQUIRED）；任一 billCode 红冲失败抛 NopException 触发事务回滚（强一致：反审核为补救路径，须保证无残留半状态——对齐 1745-3 `reverseApprove` 范式）。

- **Phase 3（测试 + owner-doc 对齐）**：
  - **JUnit `TestErpFinBadDebtProvisionReversal`**（`module-finance/erp-fin-service/src/test/java/app/erp/fin/service/entity/TestErpFinBadDebtProvisionReversal.java`）5 用例：(a) 单凭证反向（runBadDebtProvision 产 1 BDR → reverseBadDebtProvision → 原凭证 isReversed=true + 红字凭证行同向取负 Dr 6701=-X/Cr 1231=-X + Allowance 余额回退至反向前）；(b) 多凭证累积反向（runBadDebtProvision + 直接 seed 额外 BDR 模拟累积 → 2 张全 isReversed=true + reversedReserveCount=2，参 Phase 1 Decision (a) 反向全部）；(c) 未找到凭证守卫（ERR_BAD_DEBT_PROVISION_NOT_FOUND）；(d) CLOSED_FINAL 期间守卫（置 period.status=CLOSED_FINAL → ERR_BAD_DEBT_PROVISION_PERIOD_FINAL_CLOSED）；(e) 混合 BDR/BDL 反向（直接 seed 1 BDR + 1 BDL → 两张全红冲）。**首次执行用 `SnapshotTest.RECORDING` 录制快照，后切换 CHECKING 模式校验通过**。
  - **E2E `fin-bad-debt-provision-reverse.action.spec.ts`**（`tests/e2e/business-actions/fin-bad-debt-provision-reverse.action.spec.ts`）2 用例：(a) runBadDebtProvision → reverseBadDebtProvision → GraphQL response `reversedReserveCount=1` + `reversedReserveAmount=requiredProvision-allowanceBalance` + 原 NORMAL 凭证 isReversed=true + 红字凭证行同向取负（Dr 6701=-X / Cr 1231=-X，经 `findVoucherIdByBillCode(billCode,'REVERSAL')` 反查）；(b) 守卫：无 BDR/BDL 凭证 → GraphQL errors 含「未找到」/「坏账准备」语义 token。
  - **owner-doc 对齐**：`docs/design/finance/bad-debt.md §步骤2b 反向红冲`（新增章节，详述 reverseBadDebtProvision 反向语义 + DIRECT 路径 + 多凭证策略 (a) + 期间状态门控 + 事务边界 + 重提路径 + 残留风险）。
  - **e2e-runbook**（`docs/testing/e2e-runbook.md`）：业务动作表 +1 finance 反向行（`fin-bad-debt-provision-reverse.action.spec.ts`）+ 套件计数段补 2251-2 增量（实际 83 含本计划新增）。

- **验证**：
  - JUnit 5 passed（TestErpFinBadDebtProvisionReversal）+ fin-service 206 tests 0 failures/0 errors（既有 201 + 新增 5）
  - 154 模块 `mvn clean install -DskipTests` 全绿
  - 新 E2E spec TypeScript 编译无错（Playwright config 无 tsconfig，经 `npx tsc --skipLibCheck` 验证）

- **范围裁决**：
  - **Non-Goal**：`CloseVoucherWriter` 无幂等检查根因修复（跨多个 caller，属不同结果面 successor，触发条件：writeVoucher 幂等缺失在生产引发其它域数据完整性事故时）/ 多期间批量反向入口（单期间反向已覆盖核心场景）/ 反向审批工作流（ErpFinBadDebt 无 useWorkflow tagSet，DIRECT 路径即可）。
  - **范围内**：service 反向方法 + 多凭证策略 (a) + 期间状态门控 + owner-doc §步骤2b 章节 + JUnit + E2E。
  - 纯应用层 Java（service 1 新方法 + BizModel 接线 + IBiz 声明 + DTO + ErrorCode）+ 测试 + 文档，零 ORM/契约/字典/种子/config 变更。

- **后续**：跨域过账红冲缺口系统性审计 finance 域 Phase 2 全部 successors 已闭合（1745-3 Deferred「坏账核销/收回 reverseApprove」+「员工借款现金还款 reverseCashRepay」+「坏账计提（provision）红冲」全部落地）。下一步聚焦看板运行时视觉/浏览器回归 + 各域细化端到端验证。

### 2026-07-19（plan 2026-07-19-0120-1 Finance treasury 多币种票据贴现浏览器层 E2E）

- **背景**：执行 `docs/plans/2026-07-19-0120-1-finance-treasury-fx-notes-discount-browser-e2e.md`。承接 1430-1 Deferred「多币种票据贴现浏览器层 E2E」successor——触发条件经实时仓库核实已满足。单币种票据贴现 1430-1 已覆盖；多币种（FX）6051 汇兑损益行 signum≠0 触发分支浏览器层零覆盖。

- **Phase 1（Explore + Decision）**：4 file:line 锚点逐行核实——(1) `NotesPostingDispatcher.java:78/:92` exchangeRate + EXCHANGE_GAIN_LOSS billData 透传 VERIFIED；(2) `NotesReceivableAcctDocProvider.java:40/:64-80/:72` 6051 + DISCOUNTED 4 件套 + signum 分支 VERIFIED；(3) `ErpFinNotesReceivableProcessor.java:226-251` buildDiscount 公式 + **`:249` 无条件 `setExchangeGainLoss(BigDecimal.ZERO)` 关键发现**——Java builder 无外币派生，经 `discount` mutation 永远不会触发 6051 行。Decisions：(a) setup 工程化采纳 `ErpFinVoucher__post` 直驱验证 Provider FX 6051 分支（对齐 `finance-voucher-post.action.spec.ts` 范式）+ `discount` mutation 段验证 FX 状态机生命周期（产 3 行凭证）；(b) 汇率方向经 `ExchangeRevaluationService.java:55` `diff = openAmountFunctional − (openAmountSource × 期末汇率)` 实测确认 `exchangeRate = functional/source`；(c) 种子 COA 加性追加 6051（id=44 经核实 `max(id)+1=43+1`，EXPENSE/DEBIT 对齐 ExchangeRevaluationService 既有用法）；(d) 对照测试用例采纳本 spec 内独立 `test()` 经同 post 入口形成 crisp signum 0/非0 对照。

- **Phase 2（spec + 种子）**：
  - `app-erp-all/src/main/resources/_vfs/_init-data/erp_md_subject.csv` 加性追加 `44,6051,汇兑损益,EXPENSE,DEBIT,ACTIVE,DEBIT,true`。
  - `tests/e2e/business-actions/fin-notes-receivable-fx-discount.action.spec.ts`（3 用例）：(1) FX 状态机生命周期（USD 票据 currencyId=2 + exchangeRate=6.6667 + amountSource=USD 100 + amountFunctional=CNY 666.67 → discount mutation → DISCOUNTED + posted=true + 3 行凭证 Dr 1002=660/Dr 6603=6.67/Cr 1121=666.67）；(2) FX 6051 触发分支（`ErpFinVoucher__post` 直驱 NOTES_RECEIVABLE_DISCOUNTED event 含 EXCHANGE_GAIN_LOSS=5.00 → 4 行凭证 Dr 1002=655/Dr 6603=6.67/Dr 6051=5.00/Cr 1121=666.67，NET_AMOUNT_FX_VOUCHER=655 经复式平衡派生：face − interest − fx）；(3) 对照 6051 抑制（同 post 入口 + EXCHANGE_GAIN_LOSS=0 → 3 行凭证无 6051）。**首次执行 Test 2 失败**（借贷不平衡：借 671.67 / 贷 666.67）经根因分析——netAmount=660 + interest=6.67 + fx=5 = 671.67 ≠ face=666.67；修正 netAmount=655（5 = 666.67 − 6.67 − 655 平衡），重跑全绿。
  - 凭证行翻转经 `verifyState`（`__get`）/`findVoucherIdByBillCode`/`assertVoucherLines` 独立断言。

- **Phase 3（文档对齐）**：e2e-runbook 业务动作表 +1 finance 多币种行 + 套件计数 83→84；1430-1 Deferred RELEASED；backlog README +1 done 行。

- **验证**：
  - 新 spec 3 passed（45.3s）
  - finance 抽样回归 13 passed（fin-notes-receivable 7 + fin-notes-payable 5 + finance-voucher-post 1，1.6m，0 新增失败）
  - business-actions 全套件 219 passed / 1 失败（mfg-variance-recompute-reversal 经 `git stash` 验证为预存 flake，与本计划无关——DB 共享状态累积致 `reportCompletion` docStatus 未翻 COMPLETED）
  - 154 模块 `mvn clean install -DskipTests` BUILD SUCCESS（1:30min）

- **范围纪律 / 种子加性影响面**：
  - **范围内**：种子 6051 加性追加（id=44，零 GL 余额，ACTIVE 行不影响看板基线）+ 新 spec + 文档对齐。
  - **Non-Goal**：Java builder 外币 exchangeGainLoss 派生修复归 `Deferred But Adjudicated` 显式 successor（plan 规则 13/14，不改生产代码即时修）。`ErpFinNotesReceivableProcessor.buildDiscount:249` 当前无条件 ZERO 致 discount mutation 永不触发 6051——本 spec 改用 `ErpFinVoucher__post` 直驱验证 Provider FX 分支。Java 修复触发条件：外币票据业务实际生产路径触发 6051 派生缺失被复现时，或票据业务多币种深化需求落地时。
  - 纯测试 + 种子 COA 加性追加 + 文档，零生产 Java/ORM/契约/config 变更。

- **后续**：1430-1 Deferred「多币种票据贴现浏览器层 E2E」已 RELEASED。下一步聚焦看板运行时视觉/浏览器回归 + 各域细化端到端验证（外币票据 honor/dishonor/endorse/collect 路径归不同结果面 successor，见 0120-1 Deferred）。
