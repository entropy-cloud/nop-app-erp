# Development Log - 2026-09-01

### 2026-09-01

- **ast/cs 冻结时钟逃逸当日修复 + full-green verification（bug `2026-09-01-0058` open→closed）**：mission verify 步按 0017 已验证配方当日落地，无需 successor 切片——①ast `ErpAstDepreciationScheduleRecalculateForCapitalizationMaintenanceProcessor` 重算基数月 `YearMonth.now()`→`YearMonth.from(CoreMetrics.today())`（fin 侧同款回归 IClock 时间线）；②cs 侧接缝：平台 `nopSysCalendar`（`DefaultSysCalendar` 裸系统时钟，绕过冻结扩展）经 cs 测试 delta `app-dao.beans.xml` 覆盖为新增 `CoreMetricsSysCalendar`（module-common-test，经 `CoreMetrics` 读日期，未冻结线程行为不变）；③`TestErpAstMaintenance` 补 `AstFrozenClockExtension`，`TestErpCsTicketCreateEnrichment`/`TestErpCsCatalogFulfillmentEngine` 补 `CsFrozenClockExtension`（参考日 2026-07-17）；④快照对齐：ast 5 用例重算行 PERIOD 序列 -1 月（种子行不动）、cs 11 文件 `TK202608*`→`TK202607*`/`cs_ticket_code_seq_202608`→`202607`。验证（full-green）：ast-service 339/339、cs-service 185/185、app-erp-all 69/69（1 pre-existing skip）全绿；全 reactor `mvn test -fae` **BUILD SUCCESS**（13:33，含原失败 ast/cs 两模块，无下游 skip 补跑需求）。
- **plan `2026-08-29-1913-2-page-graphql-to-rest-migration-phase2` complete-on-arrival 闭合（Plan Status: completed，独立结束审计 APPROVE）**：实仓重盘证实剩余 58 文件（10 模块）已由 commit `751749e17` 随 AMIS→flux 重写先行迁移——全仓 `.page.yaml` 零 `/graphql` 残留、10 模块 `@query:`/`@mutation:` 计数 14/7/6/6/5/5/5/4/4/2 与计划分布精确吻合、97/97 目标方法可达（60 自定义字面命中 + 37 平台标准 CRUD 经 `AbstractErpCrudBizModel extends CrudBizModel` 链确认）。验证：`mvn clean install -DskipTests` BUILD SUCCESS；flux 双测 2/2 绿 `FLUX_PAGE_ERROR_COUNT: 0`（erp/* 855 页全量编译含 58 文件）；5 代表页静态抽样（fin period-close-wizard / qa SPC dashboard / crm kanban / hr org-chart / b2b edi-detail）。全 reactor `mvn test -fae` 139 模块 SUCCESS，唯一失败 = 预存 ast/cs 冻结时钟时间炸弹 13 errors（bug record `2026-09-01-0058` open，零代码因果，本执行零代码变更）+ 15 跳过下游模块补跑全绿。3 MINOR 审计 findings 处置落地（Closure Gates 勾选 / 目标 split 算术文本修正 / 1913-1 Deferred 项补 successor 闭合指针）。
- **plan `2026-08-29-1913-1` 结束审计 APPROVED → Plan Status: completed**：独立子代理 fresh session（`ses_fa731ffc8ffeonogU9YtXFDZXS`）重放全部证据（grep 残留 0、4 文件抽样、5 方法源码定位、16/16 YAML、文本一致性、`751749e17^` 溯源 16/58 算术、工作树零代码变更），3 MINOR findings 非阻塞并已落处置注记：①per-module 文件计数虚增 2×（operative 总数 74/16/58 正确）；②Phase 1 处数 slip；③**`751749e17` 实际迁移了全部 74 个 `.page.yaml`**——Deferred 的「剩余 58 文件」target 已清空（complete-on-arrival），successor `2026-08-29-1913-2` 起草时须按最低规则 1 重盘实仓基线（大概率 supersede），不得沿用本计划计数。
- **plan `2026-08-29-1913-1-page-graphql-to-rest-migration` 复核闭合**（前次中断执行于 commit `751749e17` 落地迁移批次，plan 文件未更新；本次按 mission 驱动复核实仓后补全）：9 模块 16 个 `.page.yaml` 零 `/graphql` 残留、42 个 biz 方法目标全存在、YAML 0 错误；Phase 1/2 全勾 + 执行证据落盘。范围外发现 2 项登记：① master-data 4 个手写 view.xml 含 7 处 `/graphql`（Non-Goals 排除面，owner doc §119 禁止机械替换 `@query:`）→ plan Deferred But Adjudicated（successor 随 AMIS→flux 重写）；② 月初时钟时间炸弹（见下条）。
- **月初翻车税 ast/cs 逃逸新发现（open，未修）**：全 reactor `mvn test -fae`（HEAD `211572283`，与 08-31 全绿基线同 commit 零代码差异）13 errors——ast `TestErpAstMaintenance` 5（`erp_ast_depreciation_schedule.PERIOD` 2026-08→2026-09）+ cs `TestErpCsCatalogFulfillmentEngine` 1 / `TestErpCsTicketCreateEnrichment` 7（工单号 `TK202608*`→`TK202609*`、序列行 id `cs_ticket_code_seq_202608` 消失）。全部为日历派生值快照失配，同 `2026-09-01-0017` fin 侧同族；登记 `docs/bugs/2026-09-01-0058-frozen-clock-escape-ast-period-cs-ticket-code.md`（修复配方已写入，successor = 独立冻结时钟对齐切片）。08-31 已知 hr/drp 失败本轮通过（drp 补跑 98/98 绿），佐证其时钟/时点相关性。
- **验证状态**：`mvn clean install -DskipTests` BUILD SUCCESS（156 模块 01:45）；`ErpAllFluxPagesExportTest`+`ErpAllFluxPagesTest` 2/2 绿 FLUX_PAGE_ERROR_COUNT:0；全 reactor `-fae` 139 模块 SUCCESS（唯一失败 = 上述时间炸弹类）+ pur/prj/drp 补跑 98/98 绿。
- 下一步：ai-check F2.13+ 余批（mnt+hr、crm+cs、ct+b2b+drp+log+aps+notify）与 F3 P2/P3 波次；successor `2026-08-29-1913-2`（58 文件）已解除阻塞可启动；ast/cs 冻结时钟对齐切片待立项。

- **F2.4 plan 断点续跑闭合**：`docs/plans/2026-08-28-2054-2-ai-check-f2-4-finance-period-p1.md` 全 Phase 勾选 + `Plan Status: completed`。断点成因：commit `ae74a8613` 已落地全部代码修复/2 测试/索引回写，但 plan checkbox 未勾且 4 项遗留缺口未完成。
- **补齐项 1 — Phase 2 补测**：新增 `TestErpFinClosingMultiSchema`（`module-finance/erp-fin-service/src/test/java/app/erp/fin/service/entity/`）——多账套损益结转每账套独立（1000/3000 非全域 4000）+ 年初余额 populate 两账套快照独立共存。红→绿实证：服务层回退至 `ae74a8613^` 双测红，恢复后绿。断言取经营性科目 1001（`aggregateYearSubjectActivity` 排除 PROFIT_TO_RETAINED_EARNINGS 自身分录为既有语义，未动）。
- **补齐项 2 — dual-agent-approval 补办**（Phase 3 跨域接口变更）：2 独立子代理 fresh session 审查 `IErpFinIntercompanyTransferBiz.onTransferConfirmed` 签名扩展，均 `VERDICT: ACCEPT`（session `ses_fa7862959ffe9s66sanuS24P9q` / `ses_fa785ec5dffeebMswYJMMll4n4`）。共同 required 修正已落地：`ErpFinIntercompanyTransferBizModel` 注释「逐物料计价」改为如实登记首物料近似（多物料 = 首物料单价 × 跨物料 Σ数量，逐物料精确计价为 successor）。
- **补齐项 3 — owner doc 同步**（plan 原 Target `closing.md`/`intercompany.md` 不存在，改同步实际 owner doc）：`docs/design/finance/period-close.md` 新增银行存款 FX 重估账面基准累计口径注记；`docs/design/finance/multiple-accounting-schemas.md` 新增「期末结账写路径的账套维度」节；`docs/architecture/multi-company.md` 新增「调拨凭证金额口径（单价 × 数量）」注记。
- **补齐项 4 — r2-index 登记**：`docs/audits/check/2026-08-28-2049-ai-check-r2/ai-check-r2-index.md` M2 表 F2.4 `ready` → `done` + plan 表 draft → completed 指针。
- **验证终态**：fin 525 + inv 248 + app-erp-all 69 全绿（0 失败 0 错误）；`erp-fin-app` install BUILD SUCCESS。compliance：本 plan 零新增命中（F2.4 触及文件 daoFor 计数 pre/post/now 全等）；checker 实测 R2b=242/R2c=1542 vs 注册基线 240/1537 的 +2/+5 漂移归属 `b53b9234b`（F2.3）之后兄弟 mission 提交（ast Merge-Split/生命周期、fin OCR/AP-pipeline、dashboard 等），per-site 归属证据已登记 plan Phase 4，baseline-raise 登记留待 compliance 审计轮。
- **dual-agent 登记 successor**（下轮 OPEN_AUDIT 可再发现）：多物料调拨逐物料计价 + 物料级定价规则命中端到端测试 + 旧 5 参重载 `@Deprecated`。
- **VERIFY 月初翻车税测试侧逃逸修复**：fin 全量 `mvn test` 533 用例 3 errors——`TestErpFinBankReconAutoReverseJob`（1）+ `TestErpFinEmployeeAdvanceCashRepay`（2）未接 `FinFrozenClockExtension`，快照 `erp_fin_accounting_period` 字面 `2026-08` 于月初跨月失配（2026-08-01 同族 bug 的逃逸残例，登记 `docs/bugs/2026-09-01-0017-frozen-clock-escape-bankrecon-employee-advance.md`）。修复：两测试类补 `@RegisterExtension FinFrozenClockExtension` + BankRecon 裸 `LocalDate.now()`→`CoreMetrics.today()` + 3 快照期间行对齐参考日 2026-07。复绿：fin 533 全绿 + app-erp-all 69 全绿（BUILD SUCCESS）。
- 下一步：ai-check F2.13+ 余批（mnt+hr、crm+cs、ct+b2b+drp+log+aps+notify）与 F3 P2/P3 波次。
