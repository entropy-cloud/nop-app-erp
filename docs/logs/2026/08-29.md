# Development Log - 2026-08-29

### 2026-08-29（0745）

**F2.12 projects + quality P1 簇 done**（plan `2026-08-29-0745-1`，用户人工批准 plan-audit 替代——子 agent 通道结构性不可用，同 F2.4-F2.11 先例）

**12 P1 finding 处理（11 修复 + 1 验证回填）**（红→绿测试验证 + 零回归 + compliance 零漂移）：

- **P1-CK-prj-001**（工时 cancel 归集回退）：`ProjectCostAggregator.rollbackFromTimesheet` 删除 LABOR 归集行 + 头 totalAmount 减回 + project.actualCost 减回（幂等无归集行 no-op；I*Biz 化：lineBiz/collectionBiz/projectBiz）+ `ErpPrjTimesheetCancelProcessor` APPROVED 分支接线；`TestErpPrjTimesheetCost#testCancelApprovedTimesheetRollsBackCostCollection`
- **P1-CK-prj-002**（开票单 amountFunctional）：`ErpPrjBillingBizModel` `defaultPrepareSave/Update` 触发 `syncAmountFunctional`（amountFunctional = totalAmount × exchangeRate；amountSource 兜底）；`TestErpPrjProjectPnl#testBillingSaveSyncsAmountFunctional`（单币种+多币种）
- **P1-CK-prj-003**（费用归集行级幂等键）：`ExpenseCostAggregator.refreshExpenseCost` 行级键 `claim.code+lineNo`（修复前头级键吞掉跨项目行）；`TestErpPrjExpenseAggregation` 断言 EC-EXP-001-1（lineNo=1）
- **P1-CK-prj-004**（结算单重复创建守卫）：`ErpPrjProjectSettlementCreateSettlementProcessor` FINAL/CLOSE 前置 `findActiveFinalOrCloseSettlement` → `ERR_SETTLEMENT_ALREADY_EXISTS`（INTERIM 不计）；helper 落 `ErpPrjProjectSettlementProcessor`；`TestErpPrjProjectSettlement#testDuplicateFinalSettlementRejected`
- **P1-CK-prj-005**（cancel 资产回退解耦 posted）：`ErpPrjProjectSettlementCancelProcessor` `rollbackAssetIfNeeded` 移出 posted 分支（CLOSE 转固在过账前建卡 IN_SERVICE，过账失败 cancel 仍须回退资产卡）；`TestErpPrjProjectSettlement#testCancelCloseSettlementRollsBackAssetCard`
- **P1-CK-prj-006**（工时链 cost aggregation 时序）：F1.2 已修（aggregateFromTimesheet before tryPost，注释「F1.2 P1-CK-prj-006 工时链」）→ 验证回填（零源码变更）
- **P1-CK-prj-007**（看板毛利率每项目最新快照）：`ErpPrjDashboardBizModel.getProjectGrossMargin` 每项目 latestByProject（periodTo DESC，id DESC 决胜；非 CALCULATED 跳过）；`TestErpPrjDashboardGrossMargin#testLatestCalculatedSnapshotPerProjectWins`
- **P1-CK-qa-001**（SPC 幻影子组）：`SpcSamplingService.collectSamples` 加 `sampledInspectionIds = buildSampledInspectionIds(existingSamples)` + 候选按 inspectionId 整点跳过 + helper `findInspectionByCode`（I*Biz 化：inspectionBiz.findPage）
- **P1-CK-qa-002**（isInspectionCleared 以最新为准）：`ErpQaInspectionBizModel.isInspectionCleared` 取 `inspections[size-1]`（最新）结果为准；空 → true；PENDING → false；REJECTED → false；`TestErpQaInspectionStateMachine#testIsInspectionClearedUsesLatestInspectionResult`（双向 REJECTED+ACCEPTED / ACCEPTED+REJECTED）
- **P1-CK-qa-003**（spc-capability.batch.xml inject 断裂）：L21 `inject('IErpQaSpcCapabilityBiz')` → `inject('app.erp.qa.service.entity.ErpQaSpcCapabilityBizModel')`；qa 184 全绿
- **P1-CK-qa-004**（NCR severity 字典字符串码值）：`NcrLifecycleService.autoCreateNcrFromInspection` `ncr.orm_propValueByName("severity", 20)` → `ncr.setSeverity(ErpQaConstants.NCR_SEVERITY_NORMAL)`；`ErpQaConstants.NCR_SEVERITY_*` 常量补齐；qa 快照 12 CSV SEVERITY 20→NORMAL 同步（TestErpQaNcrCapaEndToEnd/CriticalItemVeto/InspectionStateMachine）；`TestErpQaNcrCapaEndToEnd#testRejectedAutoCreatesNcrAndFullCapaClosure` 追加 severity 断言
- **P1-CK-qa-005**（recall reverseApprove 双轴联动）：`ErpQaRecallProcessor.doReverseApprove` 补 `recall.setStatus(statusStateMachine.reverseApproveTargetStatus())`；`ErpQaRecallStateMachine` 补 `reverseApproveTargetStatus() = OPEN`（与 approveTargetStatus=APPROVED 对偶；审批轴 reverseApproveTargetStatus=REJECTED 仅回写 approveStatus）；`TestErpQaRecallStateMachine#testReverseApproveResetsStatusToOpen`（含 locateTargets 守卫拒绝）

**验证**：
- `mvn test -pl module-projects/erp-prj-service` 178/0/0 全绿（baseline 173 +5 新回归）
- `mvn test -pl module-quality/erp-qa-service` 184/0/0 全绿（baseline 182 +2 新回归）
- `bash docs/audits/nop-compliance-checker.sh` 零漂移（R2c=1537≤baseline，prj-001 + qa-001 新增 daoFor 已 I*Biz 化收敛）

**索引回填**：ai-check-index prj-001/002/003/004/005/007 + qa-001/002/003/004/005 `open` → `fixed`（prj-006 验证回填）；ai-check-roadmap F2.12 `todo` → `done` + header 更新（**92 findings 终态**）；plan `2026-08-29-0745-1` done。

**ai-check 修复进度**：F0-F2.12 全部 done → **92 finding 终态 fixed**（533 总）→ 441 open 余 441。

**过程注记**：
1. ErpQaRecallStateMachine 漏 `reverseApproveTargetStatus()` 方法导致 qa-005 编译失败（ErpQaRecallProcessor.doReverseApprove 引用未声明方法）→ 补 OPEN 目标态方法（与 approveTargetStatus 对偶），qa 编译恢复。
2. NCR 快照 12 CSV SEVERITY 字典对齐涉及跨多个 _cases 目录的 output/tables/erp_qa_non_conformance.csv——perl 列索引精准替换（$f[10]="NORMAL"）。
3. 测试 ErpPrjProjectPnl#testBillingSaveSyncsAmountFunctional 首次失败因 billingBiz.save 校验 org/currency FK——去除 orgId/currencyId（业务无关字段）并补 docStatus/approveStatus（DRAFT/UNSUBMITTED mandatory）→ 全绿。
4. 测试 TestErpPrjDashboardGrossMargin#testLatestCalculatedSnapshotPerProjectWins 首次失败因 projectId="P-LATEST" 不能 convert-to-Long——改用数字 id（5001/5002），与既有 seedPnl 模式一致。
5. 测试 TestErpQaInspectionStateMachine#testIsInspectionClearedUsesLatestInspectionResult 首次失败因 seedInspectionSharedBill 用 code.hashCode() 派生 id——对 "INS-LATEST-1/2" 哈希导致 REJECTED id > ACCEPTED id（首末颠倒）→ 补 explicitId 参数显式传 950001/950002 保证升序。
6. compliance refactor：rollbackFromTimesheet 3 daoFor 替为 lineBiz/collectionBiz/projectBiz（findPage/get/updateEntity/deleteEntity）；SpcSamplingService.findInspectionByCode 1 daoFor 替为 inspectionBiz.findPage——R2c 从 +4 回收到 baseline 1537。

**下一步**：F2.13 mnt+hr P1 簇修复。