# Development Log - 2026-07-22

### 2026-07-22 (MISSION_DRIVER verify erp — A3 多公司运营深度构建/测试复核全绿)

- **任务**：验证 mission `erp` 构建通过（A3 工作集未提交复核）。
- **验证（全绿）**：
  - 全仓库 `mvn clean install -DskipTests`：154 模块 BUILD SUCCESS（41.7 s）
  - finance service `mvn test`：243 测试全绿（0 failures/0 errors）
  - inventory service `mvn test`：114 测试全绿（config-gated 保护 ErpInvTransferOrderBizModel.confirm 钩子）
- **结论**：A3 多公司运营深度工作集（3 实体 + 4 字典 + 转移定价引擎 + 跨法人配对凭证 + 公司间配对 + 合并抵消候选 + GL Mapping intercompany 维度 + view.xml/菜单 + 14 新单测）构建与测试复核全绿，可提交。

### 2026-07-22 (MISSION_DRIVER execute — A3 多公司运营深度全 5 Phase 落地)

- **任务**：执行 plan `docs/plans/2026-07-22-1000-1-finance-multi-company-operational-depth.md`（A3，全 5 Phase）。跨法人内部交易凭证生成 + 转移定价引擎 + 公司间自动配对 + 合并抵消候选识别 + GL Mapping intercompany 维度接入。
- **Phase 0**：multi-company.md EXPAND（52→~200 行，6 可执行语义段 + 4 Decision + 7 反模式）+ master-data/README.md orgType/集团语义段。跨法人判定信号 = warehouse.orgId 沿 parentId 链向上找首个 orgType=COMPANY。
- **Phase 1**：3 新 ORM 实体（ErpFinIntercompanyTransferPrice / ErpFinIntercompanyMatch / ErpFinConsolidationElimination）+ 4 新字典（transfer-pricing-method / intercompany-match-status / elimination-type / elimination-status）+ account-key 字典增 4 INTERCOMPANY_* 键。codegen 全套产物落地。`stdDataType="BigDecimal"` 修正为省略（DECIMAL 类型由 stdSqlType 推断）。
- **Phase 2**：IErpFinTransferPriceResolver（3 策略 cost-plus/market/negotiated + 优先级链 + 缓存）+ IErpFinIntercompanyTransferBiz SPI + ErpFinIntercompanyTransferBizModel（跨法人判定 + 定价 + 配对凭证）+ IntercompanyVoucherGenerator（AR+AP 配对凭证）+ IntercompanyAcctDocProvider（doc-only）+ ErpInvTransferOrderBizModel.confirm 触发钩子（try-catch 不阻塞）+ GlMappingDimensions 增 fromOrgId/toOrgId。config-gated `erp-fin.intercompany-posting-enabled` 默认 false。
- **Phase 3**：IErpFinIntercompanyMatchBiz（runMatching 经 billCode 配对 SALE/PURCHASE + checkDualSideConsistency）+ IErpFinConsolidationEliminationBiz（generateEliminationCandidates 3 类 + postElimination DRAFT_VOUCHER）。config-gated `erp-fin.consolidation-elimination-enabled` 默认 false。
- **Phase 4**：3 view.xml bounded-merge 定制 + fin-intercompany 菜单分组 + 4 owner doc 回链（posting.md/gl-mapping-rules.md/integration-and-transaction-patterns.md/master-data README）+ roadmap §A3 `todo→done` + §8.9 落地证据。
- **关键教训应用**：(1) @BizMutation/@BizQuery/@Name 注解需同时在 IBiz 接口 + BizModel 实现（仅接口注解不生效，BizObjectImpl.requireAction 报 object-not-support-action）；(2) CrudBizModel 子类用 daoProvider() 方法（继承），勿 @Inject IDaoProvider 字段（shadowing 注入失败）；(3) 配对经 ErpFinVoucherBillR.billCode 而非 org pair（单凭证仅含单侧 orgId）；(4) resolver 缓存测试需调 invalidateCache() 后再 resolve（与 A1 范式一致）。
- **验证（全绿）**：
  - 全仓库 `mvn clean install -DskipTests`：154 模块 BUILD SUCCESS
  - finance service `mvn test`：243 测试全绿（229 既有 + 14 新增）
  - inventory service `mvn test`：114 测试全绿（config-gated 保护 ErpInvTransferOrderBizModel.confirm 钩子）
  - 3 新测试类：TestErpFinTransferPriceResolver 7 场景 / TestErpFinIntercompanyTransfer 2 场景 / TestErpFinIntercompanyMatchingAndElimination 5 场景

### 2026-07-22 (MISSION_DRIVER verify erp — TestErpMfgProductionVariance 幂等用例 DEL_VERSION 时钟型非确定性稳定化 + 全绿验证)

- **任务**：验证 mission `erp` 构建通过。`mvn clean install -DskipTests` 全绿（154 模块）；`mvn test` 首跑全绿，复跑在 `app-erp-manufacturing-service` 的 `TestErpMfgProductionVariance` 偶发失败。根因定位 + 修复 + 连续两次全绿复核。
- **根因**：`testManualCalculateVariancesIdempotent` 幂等重算经 `ProductionVarianceCalculator.deleteByWorkOrder` → `dao.batchDeleteEntities` 逻辑删除 5 行 `erp_mfg_cost_variance`（`useLogicalDelete` + `deleteVersionProp=delVersion`），框架按行写入时钟型 DEL_VERSION。快照把这 5 个软删行绑到单一 `@var:ErpMfgCostVariance@delVersion_22`，要求共享同一时间戳；删除跨毫秒边界时出现 `...360` vs `...361` 1ms 偏差 → `nop.err.match.not-equals-var-value` 偶发失败。与既有约定一致（`TestErpMfgRoutingCrudSmoke` 注释 + CREATE_TIME/UPDATE_TIME 列全用 `*`）。属预存 flaky，与本批 BT5 工作集无关（manufacturing 域无源码变更）。
- **修复**：`testManualCalculateVariancesIdempotent` 输出快照中 5 个软删行的 `@var:ErpMfgCostVariance@delVersion_22` 改为 `*` 通配（保留新增行 delVersion=0 的 `@var` 校验，幂等「删旧+建新」语义仍被验证）。
- **验证（全绿）**：
  - `mvn clean install -DskipTests`：154 模块 BUILD SUCCESS
  - `mvn test`：修复后连续两次全 reactor BUILD SUCCESS（含 `TestErpMfgProductionVariance` 10 测试 + BT5 `TestModuleMetaReader` 7 测试，0 failures/0 errors）
- **经验**：逻辑删除多行时框架按行打时间戳，快照用单一 `@var` 绑定多软删行会引入时钟型 flaky；时钟型非确定列（软删 DEL_VERSION / CREATE_TIME / UPDATE_TIME）应一律 `*` 通配，勿共享 `@var`。

### 2026-07-22 (MISSION_DRIVER execute — D2 业务模块元数据 BT5 风格全 3 Phase 落地)

- **任务**：执行 plan `docs/plans/2026-07-21-2225-3-business-module-metadata-bt5.md`（D2，全 3 Phase）。为全 19 域 `_module-meta.json` 增加 BT5 风格业务元数据（version + businessDependencies + optionalFeatures）+ 运行时读取器，**不触 ORM 源模型**。
- **Phase 1（owner doc NEW + 4 Decisions）**：NEW `docs/architecture/business-module-metadata.md`（8 节）。Decision A=三 optional 字段（version/businessDependencies/optionalFeatures）；B=SemVer 独立于 `ext:mavenVersion`；C=meta 模块手写 `module-meta.yaml` 源（否决 ORM ext: 新属性，D2 不在 §8 ORM 授权清单）；D=读取器归 `app-erp-all`。
- **Phase 2（生成管线扩展 + 运行时读取器 + 单测）**：
  - 全 19 个 `erp-*-meta/precompile/gen-meta.xgen` 在 `renderModel` 后叠加 overlay 段：`JsonTool.parseYaml`（平台 `nop-core` 内置 snakeyaml）解析手写 yaml → moduleId 一致性校验（不一致 `IllegalArgumentException` codegen 期报错）→ 合并 3 字段回写。向后兼容：yaml 缺失生成结果不变。
  - 读取器（`app-erp-all` 新增模块 `app/all`）：`ModuleMetaReader`（POJO，经 `ModuleManager`+`VirtualFileSystem` 扫描）+ `ErpModuleMetaBizModel`（`@BizQuery` 诊断）+ `ErpModuleMetaErrors`（`erp.err.module-meta.dependency-missing`/`version-mismatch`）+ `ModuleMetaBean`/`DependencyIntegrityResult`。
  - 关键坑：(1) XLang `<c:script>` 中 `&&` 触发 XML entity 解析错误 → 改嵌套 `if`；(2) XLang 不自动 import `java.lang` + 不支持 `.class` 字面量 → 显式 import + 用 `JsonTool.parseYaml(null,text)`；(3) JSON key `default` 不匹配 bean 属性 `defaultValue` 导致 strict 解析跳过该模块 → 统一用 `defaultValue`。
- **Phase 3（全 19 域应用 + 回链 + roadmap）**：全 19 `precompile/module-meta.yaml`（version=1.0.0 + businessDependencies 按真实跨域 Maven 依赖 + optionalFeatures 从 `Erp*Constants.java` `*_ENABLED` 镜像）。省略分类：master-data/notify 省 businessDependencies（DAG 根/跨域基础设施），aps/logistics/b2b 省 optionalFeatures（无 config-gated 特性）。
- **验证（全绿）**：
  - 全 workspace `mvn clean install -DskipTests`：BUILD SUCCESS（01:03 min，codegen 管线扩展 + 19 yaml + app-erp-all 新模块不破坏 154 基线）
  - `app-erp-all` `mvn test`：`TestModuleMetaReader` 7 场景全绿（多模块扫描/依赖完整性正负路径/特性查询/向后兼容/跨域矩阵抽样）；全模块 10 测试 0 failures
  - 19 个 `_module-meta.json` 全 well-formed（`python -m json.tool`）+ 含 version 字段
- **文档对齐**：`business-module-metadata.md` §8 落地证据 + README 介绍行 + `domain-module-split-analysis.md` §4.1 回链 + `deepening-roadmap.md` D2 `todo→done` + §8.8 证据。Roadmap §6 D2 ORM 变更 = 「否」严格遵守（仅 module-meta.json + meta yaml 变更，未触 `*.orm.xml`）。

### 2026-07-22 (MISSION_DRIVER verify erp — ErpFinBudgetScenario.view.xml A2 回归修复 + 全绿验证)

- **任务**：验证 mission `erp` 构建通过。`mvn clean install -DskipTests` 全绿（154 模块），但 `mvn test` 在 `app-erp-all` 的 `ErpAllWebPagesTest.testValidateAllPages` 失败。根因定位 + 修复 + 全绿复核。
- **根因**：`module-finance/erp-fin-web/.../ErpFinBudgetScenario.view.xml`（A2 预算结转计划 `2026-07-21-1206-2` 引入，commit `d0047f0b8`，该 commit 误标「full-green」实为 build-only）含两处结构性缺陷：
  1. **XML entity 错误**：picker URL `findList?filter=scenarioId&filter=scenarioId` 未转义 `&`（XML 解析为未知 entity `&filter;`）+ filter 语法错误（Nop 正确语法为 `filter_<field>=<value>`）+ 重复。
  2. **xview schema 违规**：`<crud>` 下放置 `<dialogs>`（plural），但 `xview.xdef` 的 `crud` 仅允许 `table/listActions/rowActions/itemActions/autoGenerateFilter/itemAction/defaultParams/beforeTable/afterTable`；且 actions 用非法的 `dialog="id"` 属性引用。
- **修复**：按仓库内 canonical 模式（`ErpHrRecruitment.view.xml` 的 scheduleInterview/makeOffer/hire 三 drawer + `<simple>` 提交页）重构：
  - 新增 2 个 custom 参数 form（`rollForward`：newFiscalYear input-number + strategy select 3 选项 / `carryForward`：targetScenarioId select+source(findList/value:id,label:name) + rule select 4 选项），cell 全部 `custom="true"` + 显式 `domain`。
  - 2 个 action 改为 `actionType="drawer"` + `<dialog page="rollForward|carryForward" size="md"/>` 子节点（替换非法 `dialog="..."` 属性）。
  - 新增 2 个 `<simple>` 提交页（`form=` + `useFormActions="true"` + `<api url="@mutation:ErpFinBudgetScenario__rollForward|carryForward/id" withFormData="true"/>` + cancel/confirm actions），删除非法 `<dialogs>` 块。
  - 字典选项值（FIXED_PERCENTAGE/ZERO_BASED/INCREMENTAL + REMAINING_FULL/REMAINING_RATIO/USED_FULL/NONE）取自 `app-erp-finance.orm.xml` dict 定义，避免漂移。
- **验证（全绿）**：
  - `mvn clean install -DskipTests`：154 模块 BUILD SUCCESS（02:00 min）
  - `mvn test`：全 reactor BUILD SUCCESS（12:02 min，含 `app-erp-all` `ErpAllWebPagesTest` 1 测试 0 failures/0 errors）
  - XML well-formed（xmllint exit 0，namespace 警告为 Nop `c:`/`gql:`/`j:` 自定义命名空间，平台解析器正常处理）
- **经验**：commit 标注「full-green verification」必须真实执行 `mvn test`（含 `ErpAllWebPagesTest` 页面 schema 校验），仅 `mvn clean install -DskipTests` 不覆盖 view.xml xdef 校验。A2 计划的 Phase 3 view.xml 定制未跑测试即误标全绿，导致 schema 违规 + XML entity 错误流入主干。
