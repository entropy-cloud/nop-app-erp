# Development Log - 2026-07-22

### 2026-07-22 (MISSION_DRIVER verify erp — ErpFinBudgetScenario.view.xml A2 回归修复 + 全绿验证)

- **任务**：验证 mission `erp` 构建通过。`mvn clean install -DskipTests` 全绿（154 模块），但 `mvn test` 在 `app-erp-all` 的 `ErpAllWebPagesTest.testValidateAllPages` 失败。根因定位 + 修复 + 全绿复核。
- **根因**：`module-finance/erp-fin-web/.../ErpFinBudgetScenario.view.xml`（A2 预算结转计划 `2026-07-21-1206-2` 引入，commit `d0047f0b8`，该 commit 误标「full-green」实为 build-only）含两处结构性缺陷：
  1. **XML entity 错误**：picker URL `findList?filter=scenarioId&filter=scenarioId` 未转义 `&`（XML 解析为未知 entity `&filter;`）+ filter 语法错误（Nop 正确语法为 `filter_<field>=<value>`）+ 重复。
  2. **xview schema 违规**：`<crud>` 下放置 `<dialogs>`（plural），但 `xview.xdef` 的 `crud` 仅允许 `table/listActions/rowActions/itemActions/autoGenerateFilter/itemAction/defaultParams/beforeTable/afterTable`；且 actions 用非法的 `dialog="id"` 属性引用。
- **修复**：按仓库内 canonical 模式（`ErpHrRecruitment.view.xml` 的 scheduleInterview/makeOffer/hire 三 drawer + `<simple>` 提交页）重构：
  - 新增 2 个 custom 参数 form（`rollForward`：newFiscalYear input-number + strategy select 3 选项 / `carryForward`：targetScenarioId select+source(findList/value:id,label:name) + rule select 4 选项），cell 全部 `custom="true"` + 显式 `domain`。
  - 2 个 action 改为 `actionType="drawer"` + `<dialog page="rollForward|carryForward" size="md"/>` 子节点（替换非法 `dialog="..."` 属性）。
  - 新增 2 个 `<simple>` 提交页（`form=` + `useFormActions="true"` + `<api url="@mutation:ErpFinBudgetScenario__rollForward|carryForward/id" withFormData="true"/>` + cancel/confirm actions），删除非法 `<dialogs>` 块。
  - 字典选项值（FIXED_PERCENTAGE/ZERO_BASED/INCREMENTAL + REMAINING_FULL/REMAINING_RATIO/USED_FULL/NONE）取自 `app-erp-finance.orm.xml` dict 定义，避免漂移。
- **验证（全绿）**：
  - `mvn clean install -DskipTests`：154 模块 BUILD SUCCESS（02:00 min）
  - `mvn test`：全 reactor BUILD SUCCESS（12:02 min，含 `app-erp-all` `ErpAllWebPagesTest` 1 测试 0 failures/0 errors）
  - XML well-formed（xmllint exit 0，namespace 警告为 Nop `c:`/`gql:`/`j:` 自定义命名空间，平台解析器正常处理）
- **经验**：commit 标注「full-green verification」必须真实执行 `mvn test`（含 `ErpAllWebPagesTest` 页面 schema 校验），仅 `mvn clean install -DskipTests` 不覆盖 view.xml xdef 校验。A2 计划的 Phase 3 view.xml 定制未跑测试即误标全绿，导致 schema 违规 + XML entity 错误流入主干。
