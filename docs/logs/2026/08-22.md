# Development Log - 2026-08-22

### id-string-migration mission：M2.4 构建验证 + 并发用例 flake 根因修复

- **执行**：mission VERIFY run——`mvn clean install -pl module-assets/erp-ast-{codegen,dao,meta,service,web,app,api} -DskipTests` 7 模块 BUILD SUCCESS；`mvn test -pl module-assets/erp-ast-service,module-assets/erp-ast-web` 首跑 319/320（1 error）。
- **flake 根因**：`TestErpAstDepreciation.testConcurrentFirstDepreciationNoDuplicate` 隔离复跑 3/5 失败（`output-row-not-exists id=56`）——两并发事务抢占分配 schedule id（56/57 顺序随机），赢家提交行 id 非确定；Phase 3 全量重录为其生成硬编码 `ID=56` 输出表基线（迁移前该用例无表快照；重录时连续 6 次录得 56 掩盖非确定性）。败者路径 `update-entity-not-found`（资产乐观锁）为测试明示合法并发拒绝，业务代码无缺陷。
- **修复**：`@EnableSnapshot(checkOutput = false)`（nop-testing「非确定路径不录制不可比基线」）+ 方法内确定性断言增强（EXECUTED/1000/1000/11000/posted，覆盖原快照列核对）+ 删除不可比输出表基线（`_cases` 1749→1739，用例恢复迁移前形态）。PK 列无法 `*` 通配（输出表按主键 `getEntityById` 行匹配）。
- **验证状态（全绿）**：单用例隔离复跑 **10/10 绿**（修复前 ~40-60% 失败）+ `mvn test -pl module-assets/erp-ast-service,module-assets/erp-ast-web` **320/320 绿 ×2**（web 0 tests 治理排除）。
- **文档**：plan `2026-08-22-0002-2` 增 Post-Closure Verification 节（修正「单发 flake 未复现」结论）；roadmap M2.4 行追加后置验证注记。

### id-string-migration mission：M2.4 执行完成（assets 域迁移，冻结序位次 7，位次 8 cs 解锁）

- **执行**：`docs/plans/2026-08-22-0002-2-bigint-id-m24-assets-migration.md` 四 Phase 全部完成（登记册消费 + orm 回写 → 7 模块链重生成修复 + A2 落桥 + **fin 桥接退役（B 义务，mission 首例晚域退役早域桥接点）** → 测试修复 + 快照重录 → grep 门控 + page.yaml Fix + 收尾登记）。
- **产出**：
  - Phase 1 消费登记册 + orm 回写：110 列 `stdDataType` long→string 落源（自有 98 = PK 18 + FK 80 + md stub 12；弱引用列 `ErpAstCip.projectId` 随域翻转）；dry-run 时点刷新 + 新鲜度门控（110 行 stdDataType-only）+ git diff 110/110 + scan 重扫 110 ok / 0 NEEDS FIX 三重证明。FQN 盲区复扫（A3' 先例）命中 2 处 → **补登 bridge-test-134**（`TestErpAstDisposalEquipmentLinkage:59` FQN 字段引用 mnt IBiz + `test-mock-mnt.beans.xml:9` ioc:type；登记册 §6.7 表行 + 盲区追注，service-bridge:test 31→32）；main 侧 backward-134 盲区（fin import 20 文件 vs 登记册 2 文件 id 语境子集）§6.7 追注。
  - Phase 2 no-am 7 模块链 main 绿：首轮 198 编译错/28 文件 → 0（posting 族 10 文件 = backward-134 兑付 + Dispatcher/Provider 族 PostingEvent/AcctDocContext String id 值流转适配；processor/report/dashboard 15 文件；**dao 手写 IBiz 8 文件 37 处实体 id 参数翻转**（fin 先例对齐，含 `List<Long> costItemIds`→`List<String>`）；8 BizModel + ~30 Processor 调用点同步；自域临时 ConvertHelper 桥 7 处移除）；A2 落桥 2 条（`ErpAstDisposalProcessor:266/:274` ConvertHelper.toLong(ast String→mnt Long)，登记册 024/025，退役 owner M3.2；mnt eq/filter 清扫零查询）；**B 义务兑付**：bridge-main-061/063/065 → retired（fin 侧 3 点核查为类型级/直传形态无显式转换代码——fin 5 模块链重建绿 + **fin-service 测试复跑 497/497 全绿**（快照零 diff）+ fin grep 复核仅剩 inv 064 active 桥）。
  - Phase 3 测试修复 + 快照重录：17 文件修复（200 错 4 轮 javac recovery 级联清零；`==` id 比较 2 处改 `.equals()`；AstTestSupport 基座 String 化）；A3 bridge-test-107/134 双退役（mnt mock 保持 Long 桩 = 适配形态 + 断言 ConvertHelper.toLong 桥，退役 owner M3.2）；C2 兑付（fin 12/md 3 覆盖 + notify 1 零穿越）；**平台 IoC 回归 self-wait 七域连续复现** → fin 修正版 delta 直接落位（`x:extends="super"`，六先例后首例免修正采用）；快照重录 RECORDING 320 测试 0 真实失败 + 109 预期 snapshot-finished，`_cases/` **1464→1749 = 321 内容 diff + 285 新增 + 0 删除**（CSV 列集刷新 + CRLF + json5 String id `"id": "22"` 实证；零非确定性单元格）。
  - Phase 4 收尾：grep 门控清零（`.longValue()`/`Map<Long`/`Set<Long`/`%d`/装箱 `==`/id 序专项全 0；`Long.parseLong` ×2 = epoch 日期 parse 合法非 id；残留 Long id 声明 6 处 vestigial = 3 翻 String + 3 删死代码；sql-lib 零存在）；page.yaml `:Long` 1 处就地 String 化（disposal-wizard:74，YAML 良构 + ast-web 重建绿）；手写 view 零改动；owner doc 注记 = `docs/design/assets/` 零 Long 陈述零变更；登记册 4 条 retired（061/063/065/107/134 共 5 条——含补登 134）；roadmap M2.4 → done。
- **验证状态（全绿）**：`mvn clean install -pl module-assets/erp-ast-{codegen,dao,meta,service,web,app,api} -DskipTests` 7 模块 BUILD SUCCESS + `mvn test -pl module-assets/erp-ast-service,module-assets/erp-ast-web` **320/320 绿 ×2**（web 0 tests 治理排除，successor M4.1）+ fin 5 模块链重建绿 + `mvn test -pl module-finance/erp-fin-service` **497/497 全绿** + 工具重扫 assets 段 0 残留。中间态（已登记，successor 愈合）：A2 main 桥接 2 条（bridge-main-024/025，M3.2 回收）+ A3 断言桥 2 处（随 mock 于 M3.2 回收）+ prj 被引用面（backward-174/-235，M2.7 C 定位面 + M4.1 兜底）+ `ErpAstWebPagesTest` 治理排除（M4.1）+ ast IoC delta（平台修复后移除）。
- **下一步**：位次 8 M3.5 cs（plan `2026-08-22-0002-3`）解锁。

### id-string-migration mission：M2.1 执行完成（finance 域迁移，冻结序位次 6，位次 7 assets / 8 cs 解锁）

- **执行**：`docs/plans/2026-08-22-0002-1-bigint-id-m21-finance-migration.md` 四 Phase 全部完成（Phase 1 前轮已完成，本轮 Phase 2-4）。
- **产出**：
  - Phase 2 增量重生成 + 主代码修复 + A2 落桥：**no-am 5 模块链**（codegen,dao,meta,service,api——web/app 延后冻结裁决）`-Dmaven.test.skip=true` BUILD SUCCESS（约 60 手写文件编译器驱动 Long→String：posting 引擎全链（ErpFinPostingProcessor/AcctDocContext/VoucherFact/VoucherReversedEvent/4 AcctDocProvider）、intercompany、budget（VoucherGenerator/Commitment/Scenario 链）、bankrecon、reconciliation、treasury、report/dashboard、SPI；默认 id 字面量 1L→"1"）；`_gen` md 关系胶水 97 处中间态自愈。A2 桥接 10 条落位：bridge-main-064 语义级 `ConvertHelper.toLong(period.getId())`（inv reclosePeriodCosts）+ 9 条类型级核证零转换点（ast schedule 按 period VARCHAR code 过滤实证 orm:334；pur/sal 按 code String 关联）+ **billData id 桥 asId ×5**（ArApItemGenerator/4 Provider：未迁移域派发器写入的 Long id 归一 String，注释标注退役 owner M2.5/M2.6）；语义级过滤值桥主动识别（eq/in/ge/le 全 grep）零静默空匹配风险点。零未登记破坏（no-am reactor 全绿）。
  - Phase 3 测试修复 + A3 退役 + 快照重录：93 测试类全 String 化（直接 16 文件 + 5 批并行子代理 54 文件；`TestErpOrgIsolation:72` M1.3 编译破坏兑付；A3 bridge-test-118 适配 = seed 侧 `setSupplierId(Long.valueOf(partnerId))` 局部桥；ProfitLossClosing 双重载冲突分名 netCreditById/netDebitById）。**平台 IoC 回归 self-wait 复现**（六域连续）→ 先例 delta 落位后**发现并修正先例缺陷**：五域 delta 缺 `x:extends="super"` → 整文件替换平台 `app-dao.beans.xml` → `nopOrmEntityChangeLogInterceptor` 静默丢失（finance audit tagSet 实体 2 用例 0 日志实证暴露；md/notify/aps/b2b/contract 未触及故潜伏）——fin delta 修正（x:extends="super" + 仅 replace nopSequenceGenerator + xmlns:feature），bug 登记 `docs/bugs/2026-08-22-ioc-delta-missing-extends-super.md`（五域文件待回收，M4.1 前）。**id 序语义修复**（contract idOrder 同型）：`findLatestUnreversedCashRepayLink` 改按 billCode 内嵌毫秒时间戳序（voucherId String 字典序≠时间序）；findPostedVouchers/report GlBalance 排序改 nullsLast/nullsFirst naturalOrder。**快照重录**（mission 最大单域）：RECORDING 497 测试 0 真实失败 + 279 预期 snapshot-finished；`_cases/` 2961→4108 = 554 内容 diff（CrudSmoke id `"2"` String 形态 + @var 绑定漂移 + nop_sys_change_log String sid 字典序行序）+ 1147 新增落盘 + 0 删除；零非确定性单元格；注解还原零残留。
  - Phase 4 收尾：grep 门控清零（`.longValue()` ×5 = asId 桥登记例外；`Long.parseLong` ×2 = epoch 日期 parse 合法非 id；`Map<Long`/`Set<Long`/`%d` 变体 0；装箱 id `==`/`!=` 仅 null 检查；id 序 comparator 专项 0 残留——已改 billCode/naturalOrder；残留 Long 逐条核清 = projectId 延后列（VoucherFact/GlMappingDimensions/BudgetVsActualRow）+ timing nanos + count 值 + epoch parse；sql-lib 零存在）；取消链 Long 内传清理（EmployeeAdvance/ExpenseClaim cancel-processor 链 4 文件 String 直传）；page.yaml `:Long` 7 处就地 String 化（period-close-wizard ×6 + dashboard ×1，`$lim:Int` 合法保留；YAML parse 良构校验 2/2 过；fin-web 不重建——运行时验证 successor M2.7/M4.1）；手写 view 零改动（git status 0 view.xml）；登记册 bridge-test-118 → retired（11 retired 总数）；owner doc 注记：`docs/design/finance/gl-mapping-rules.md` §2 就地注记 Java 层 String 化 + DB BIGINT 语义澄清（costing-methods.md Long 引用为 inv 域设计草图未迁移保持准确，零变更）；roadmap M2.1 → done（头部最后更新 + 位次 6 行证据摘要；**web/app 延后补做义务显式移交 M2.7 后续做载体，M4.1 兜底**）。
- **验证状态（全绿）**：`mvn clean install -pl module-finance/erp-fin-{codegen,dao,meta,service,api} -Dmaven.test.skip=true` 5 模块 BUILD SUCCESS + `mvn test -pl module-finance/erp-fin-service` **497/497 绿 ×2**（93 测试类，含 TestErpOrgIsolation 兑付；fin-web/fin-app 延后不建——`ErpFinWebPagesTest` 治理排除 successor M4.1）。中间态（已登记，successor 愈合）：A2 main 桥接 10 条（M2.4 回收 ast 3 / M2.2 回收 inv 5 / M2.5 pur 1 / M2.6 sal 1）+ asId billData 桥 ×5（随 M2.5/M2.6）、12 域被引用面编译破坏（各域 plan C 定位面 + M4.1 兜底）、6 条 A1 延后列 + prj stub 2 列（M2.7 同批）、fin web/app 重生成 + page.yaml 运行时验证（M2.7 补做 / M4.1 兜底）、fin IoC delta（平台修复后移除）、五域先例 delta 缺 x:extends="super"（bug 登记，各域 successor/M4.1 回收）。
- **下一步**：批内序 2 M2.4 assets（plan `2026-08-22-0002-2`）与批内序 3 M3.5 cs（plan `2026-08-22-0002-3`）已解锁（本批 M2.1 为首个）。
