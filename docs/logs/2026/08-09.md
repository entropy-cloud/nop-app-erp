# Development Log - 2026-08-09

### P2.3 负向隔离测试框架执行完成（mission permissions-enforcement，原语 + 脚手架层）

- **工作项**：P2.3 负向隔离测试框架——未授权动作拒绝 + 越权数据过滤断言原语/脚手架 + 1 冒烟 demo（plan `docs/plans/2026-08-09-2210-2-negative-isolation-test-framework.md`）
- **类型**：implementation-only change（测试侧 TS 原语 + 脚手架 + 1 demo + owner doc + 日志；不改 Java/ORM/契约/auth 种子，不翻转任何 enforcement 开关——三开关保持 OFF）—— 不写 BizModel/Processor/xbiz（纯 `.ts` 测试文件 + 文档，均 allow）
- **load-bearing 表征（Phase 1）**：enforcement 拒绝形状经平台源静态表征——动作级拒绝 `GraphQLActionAuthChecker.java:102` 抛 `NopException(ERR_AUTH_NO_PERMISSION)`=`nop.err.auth.no-permission`，message「没有访问权限」；GraphQL 序列化 **HTTP 恒 200**（非 403，401 仅认证失败不同源），body `{errors:[{message}], data:null, extensions:{"nop-error-code":...,"nop-status":-1}}`，errorCode 在**顶层** `extensions["nop-error-code"]`（不在每条 error entry 内）；data-auth 行过滤**静默**（`DefaultDataAuthChecker.getFilter` 注入 SQL WHERE → 行集减小无 errors，仅「角色不匹配」分支抛 `no-data-auth`）。**rejection-source-agnostic 成立**：业务拒绝 + enforcement 拒绝同经 `GraphQLEngine.buildGraphQLResponse` → 同 `{errors}` 信封 + 同 errorCode 位置。运行时确认 gated on P2.4（action-auth OFF 不可观测）。
- **产出**（三 Phase 全落地 `tests/e2e/negative/` + `e2e-runbook.md` + 本日志）：
  - **Phase 1 表征 + 三 Decision**：动作级 enforcement 拒绝形状静态表征（token/errorCode/HTTP/JSON 形状）+ `expectActionDenied` rejection-source-agnostic 采纳 + `expectRowsHidden/Visible` absent/present 采纳（data-auth 静默过滤无 errors，absent 唯一信号）+ 脚手架落位 `tests/e2e/negative/_helper.ts`（关注点分离，`.ts` 均 allow）+ `loginAsRole` 占位
  - **Phase 2 原语 + 脚手架模块**：`tests/e2e/negative/_helper.ts` 导出 `expectActionDenied(result, opts?)`（errors 真值 + 可选 token/errorCode，返回 errors 链式）+ `expectRowsHidden/expectRowsVisible`（封装 findItems/findPageTotal）+ `loginAsRole(page, roleOrUser)` 占位（回退 login）+ `ENFORCEMENT_ERROR_CODES` 常量对象（P2.4 收敛用）+ re-export callMutation/callQuery/findItems/findPageTotal；模块顶部 JSDoc 完整说明设计 + 形状 + Follow-up
  - **Phase 3 冒烟 demo + owner doc + 日志**：`action-denied.smoke.spec.ts`（hr-payroll markPaid UNSUBMITTED 守卫作业务拒绝载体，内联最小 setup + 反依赖链 cleanup 自包含）；`e2e-runbook.md` 新增「负向隔离测试原语」节（原语清单 + enforcement 形状表征 + demo 范式 + 三 Follow-up）；本日志条目
- **关键证据（Proof）**：(1) Phase 1 表征落盘 plan §Phase 1 Execution Evidence（平台源 + 文档双交叉核验）；(2) Phase 2 静态签名 Proof——`npx playwright test ... --list` 成功枚举（模块 import 解析 + 导出可达）+ demo 运行时正确；(3) Phase 3 demo 绿——`E2E_ENGINE=flux npx playwright test tests/e2e/negative/action-denied.smoke.spec.ts` → **1 passed (11.6s)**，`expectActionDenied(rejected, { token: '不允许执行该操作' })` 返回非空 errors + 守卫拒绝后状态不变（PENDING/UNSUBMITTED）断言成立
- **验证**：`mvn clean install -DskipTests` 全 reactor BUILD SUCCESS（本计划改 0 Java，build 用于 compliance 基线稳定）；冒烟 demo 跑通绿（flux 引擎，三开关 OFF）；`bash docs/audits/nop-compliance-checker.sh` 零漂移（checker R1-R12 扫生产 Java，本计划改 0 生产 Java，仅 `.ts` 测试 + 文档）
- **边界裁决**：Non-Goals 守住——不翻转任何 enforcement 开关（归 P2.4/E2.1）/ 不做逐域角色账号 + fixture 角色参数化（归 P2.2b，loginAsRole 占位回退 admin）/ 不做 E1.x/E2.x 真实负向测试用例（归 E1.1/E2.3，本计划仅原语 + 脚手架 + 1 demo）/ 不改既有业务断言语义（新原语不侵入既有 spec）/ 不改 ORM/Java/auth 种子 / 不运行时确认 enforcement 拒绝形状（Phase 1 静态表征，运行时确认 gated on P2.4）
- **状态**：roadmap `permissions-enforcement-roadmap.md` P2.3 ready→done；plan `Plan Status: completed`，全部 Phase + 退出标准 + Closure Gates 勾选（除「结束审计由独立子代理」项待独立审计）
- **follow-up（非阻塞 successor）**：动作级 enforcement 拒绝形状运行时确认（归 P2.4，触发=翻 enable-action-auth）/ data-auth 行过滤原语运行时 demo（归 E2.1，触发=翻 data-auth 双开关）/ 负向账号主体（归 P2.2b，触发=P2.2b 进入填充 loginAsRole）

### P2.2a admin 兜底 E2E 基线执行完成（mission permissions-enforcement，admin 兜底就绪门控）

- **工作项**：P2.2a admin 兜底 E2E 基线——skip-check E2E 生效 + nop→admin 运行时解析 + 全 E2E 套件绿基线（plan `docs/plans/2026-08-09-2210-1-admin-fallback-e2e-baseline.md`）
- **类型**：implementation-only change（E2E webServer 启动 config 变更 + owner doc 注记 + Proof；不改 Java 业务代码，不改运行时拦截行为——三开关保持 OFF）—— 不写 BizModel/Processor/xbiz（机制由平台 `DefaultActionAuthChecker` + IConfigReference 落地）
- **load-bearing 发现 + 闭环**：P2.1 在 `%dev`/`%test` 预置的 `skip-check-for-admin: true` 此前在 **E2E 运行时不生效**——Playwright webServer（`playwright.config.ts:18` + 镜像 `_tmp-server.sh:44`）以 `java -jar` 启动且**未激活任何 Quarkus profile**（`java -jar` 打包件默认激活空 default profile，`%dev` 仅 `mvn quarkus:dev` 自动激活），故 `%dev`/`%test` 块均不读 → E2E 有效 skip-check = 平台默认 false（核心 gap）。P2.2a 经两处单一真相源同步追加 `-Dquarkus.profile=test` 闭环
- **三项 Phase 1 裁决**：(1) 角色解析 introspection 机制确认——首选 (a) GraphQL `LoginApi__login` 响应 `userInfo.roleInfos`（忠实反映 `IUserContext.getRoles()`）+ 次选 (b) `nop.config.vars=` 启动日志（`nop.debug=true` 使 trace 默认开）**均可达，无需降级**；(2) skip-check 生效路径——**选 (a) `-Dquarkus.profile=test`**（P2.1 profile 化设计意图一致 + E2E=test 语义正向 + %test 仅 4 键无 Quarkus 内建键 + `devservices.enabled=false` 顶层始终生效 → 无 test-mode 副作用）；(3) playwright `allow-create-default-user` flag——**选 (a) 保留**（P1.5b 种子使 `addDefaultUser()` 守卫跳过，flag 退化为无害 fallback + 抗种子回退）
- **产出**（三 Phase 全落地 `playwright.config.ts` + `_tmp-server.sh` + `roles-and-permissions.md` + `e2e-runbook.md` + 本日志）：
  - **Phase 2 config 落地 + 运行时双 Proof**：webServer.command + `_tmp-server.sh` 两处追加 `-Dquarkus.profile=test`（单一真相源同步）；**Proof A**（config 侧）`_tmp/e2e-server.log` 的 `nop.config.vars=` 块实测 `nop.auth.skip-check-for-admin=true`（effective 来自 %test 块）+ 三开关保持 false + `nop.profiles.active:[test]`；**Proof B**（角色侧）`POST /graphql` `mutation{LoginApi__login(loginType:1,principalId:"nop",principalSecret:"123"){userInfo{userName roleInfos{roleId roleName}}}}` 响应 `userInfo.roleInfos=[{roleId:"admin",roleName:"平台管理员"}]` → `isUserInRole("admin")=true`（`buildUserContext` join 解析闭环）；Proof C 不触发（introspection 可达）
  - **Phase 3 全 E2E 套件零回归基线 + owner doc**：见 Closure 证据三元组
- **关键证据（Proof）**：双 Proof 响应落盘于 plan §Phase 2 Execution Evidence；全 E2E 套件绿三元组见 plan §Closure Gates / §Closure Audit Evidence
- **验证**：`mvn clean install -DskipTests` 全 reactor BUILD SUCCESS（runner jar 产出）；全 E2E 套件绿（skip-check 生效 + 三开关 OFF，零回归，enforcement OFF 时 skip-check 无可观测拦截效果，回归风险 = profile 激活连带效应 + 种子加载副作用，经 Proof 证零回归）；`bash docs/audits/nop-compliance-checker.sh` 零漂移（本计划改 0 生产 Java，checker R1-R12 扫生产 Java 不触）。VERIFY 步骤复跑全量 `mvn test`：仅 1 **预存无关**失败 `TestErpMdCurrencyRefreshRatesDate`（master-data 汇率日期测试，plan 2026-08-02-0650 引入，`JunitBaseTestCase` 的 `NOP_SYS_SEQUENCE` schema-init 问题，晚于上次全绿基线 2026-08-01，本计划 0 Java 变更不可能引入）——归 master-data/audit-remediation successor，非 permissions-enforcement 回归
- **边界裁决**：Non-Goals 守住——不翻转任何 enforcement 开关（归 P2.4/E2.1）/ 不证 skip-check live enforcement 充分性（归 P2.4——action-auth 翻启后 admin 跑绿）/ 不做逐域角色账号 + fixture 角色参数化（归 P2.2b）/ 不做负向隔离测试原语（归 P2.3）/ 不改 auth 表 CSV 种子（P1.5b 已交付）/ 不改 ORM/Java 业务代码 / 不裁决 prod profile
- **状态**：roadmap `permissions-enforcement-roadmap.md` P2.2a ready→done；plan `Plan Status: completed`，全部 Phase + 退出标准 + Closure Gates 勾选（除「结束审计由独立子代理」项待独立审计）
- **follow-up（非阻塞 successor）**：P2.4 dry-run 门控（直接后继——翻 `enable-action-auth` + admin 跑通全 E2E + 非 admin 子集登记 403 影响面，依赖本计划确立的 admin 兜底基线）/ P2.2b 角色化渐进（逐域补角色账号 + fixture 角色参数化）/ skip-check live enforcement 充分性证明（归 P2.4）

### P1.5b auth 表 CSV 种子执行完成（mission permissions-enforcement，账号基建层）

- **工作项**：P1.5b auth 表 CSV 种子（角色记录 + nop 测试账号 + 平台 admin 绑定）（plan `docs/plans/2026-08-09-2107-1-auth-table-csv-seeds.md`）
- **类型**：implementation-only change（3 个 auth CSV 种子文件 + 2 个 Proof 测试类；enforcement 保持 OFF，不改运行时拦截行为）—— 不写 BizModel/Processor/xbiz（仅 CSV 种子数据 + 测试）
- **产出**（四 Phase 全落地 3 个 CSV + 2 个测试类 + `docs/design/roles-and-permissions.md`）：
  - **Phase 1 编码方案 / userId 策略 / nop 落位 / 角色集 / role_resource 范围 裁决与探查**：密码编码往返 Proof（`TestAuthSeedEncodingProof#testPasswordEncoderRoundtripForSeedPassword`——`CompositePasswordEncoder`（SHA256→BCrypt）对 "123" 生成 SALT+PASSWORD 对，`passwordMatches` 往返成立）+ 小整数 userId 存活 Proof（Phase 4 加载核对）；五项 Decision 落地（采纳平台 CompositePasswordEncoder 一次性生成 / 显式 userId="1" / nop 入 CSV 使 addDefaultUser 跳过 / 24 条角色全集 / 不建 nop_auth_role_resource CSV）
  - **Phase 2 nop_auth_role 种子**：`app-erp-all/src/main/resources/_vfs/_init-data/nop_auth_role.csv` 24 条 ROLE_ID（21 业务中文字面逐字复用 P1.5a 词表 + 平台 admin/nop-admin/user），省略框架管理列（VERSION/CREATED_BY/CREATE_TIME/UPDATED_BY/UPDATE_TIME），DEL_FLAG=0、IS_PRIMARY=0
  - **Phase 3 nop 用户种子 + admin 绑定 + 编码密码**：`nop_auth_user.csv`（USER_ID=1 显式小整数、USER_NAME=nop、PASSWORD+SALT=Phase 1 编码 Proof 产出对、STATUS=1 ACTIVE、GENDER=1、USER_TYPE=1、TENANT_ID=0）+ `nop_auth_user_role.csv`（USER_ID=1, ROLE_ID=admin——B2 修复：平台 admin 角色非业务「管理员」）+ 编码密码往返 Proof（`TestAuthSeedEncodingProof#testCsvSeedPasswordRoundtrip`）
  - **Phase 4 种子加载 Proof + owner doc 对齐 + 日志**：`TestAuthSeedLoadingProof`（BaseTestCase + 手动 CoreInitialization + init-database-data=true 加载 /_init-data/）四项核对——24 角色 / nop userId="1" 且 STATUS=1 ACTIVE / (1,admin) 绑定 / 密码往返；`roles-and-permissions.md` §角色→权限点映射增「roleId 词表已物化为 DB 记录（P1.5b）」注记 + §action-level 增「auth 种子就绪」注记；本日志条目
- **B2 修复（双命名空间分离）**：nop 绑定平台 `admin` 角色（ROLE_ID="admin"）非业务「管理员」（ROLE_ID="管理员"）——`skip-check-for-admin` 经 `isUserInRole("admin")` 兜底放行。§角色体系双命名空间注记（L41-45）与实际种子绑定核验一致
- **关键证据（Proof）**：`TestAuthSeedEncodingProof` 2 tests 绿（编码往返 + CSV 值往返）；`TestAuthSeedLoadingProof` 3 tests 绿（24 角色 / userId=1 ACTIVE / admin 绑定 + 密码往返）；CSV ROLE_ID 列集合 == P1.5a 词表（21 业务 + admin/nop-admin/user）逐字一致
- **验证**：`mvn clean install -DskipTests` 全 reactor **BUILD SUCCESS**（156 模块）；`bash docs/audits/nop-compliance-checker.sh` 零漂移（变更仅 CSV 种子 + 测试 Java，checker R1-R12 扫生产 Java 代码，本计划改 0 生产 Java）；`app-erp-all` Proof 测试 5 tests 全绿
- **边界裁决**：Non-Goals 守住——不做逐域角色账号（归 P2.2b）/ 不做 admin 兜底 E2E 实测（归 P2.2a）/ 不翻转 enforcement 开关（归 P2.4/E1.x）/ 不建 nop_auth_role_resource CSV（P1.5a 静态种子运行时权威）/ 不改 ORM（USER_ID seq tag 保留，显式值存活经 Proof 确认）/ 不改 playwright allow-create-default-user flag（退化为无害 fallback，归 P2.2a 裁决）
- **状态**：roadmap `permissions-enforcement-roadmap.md` P1.5b ready→done；plan `Plan Status: completed`，全部 Phase + 退出标准 + Closure Gates 勾选（除「结束审计由独立子代理」项待独立审计）
- **follow-up（非阻塞 successor）**：P2.2a admin 兜底 E2E 基线（直接后继，触发=P2.2a 进入）/ P2.2b 逐域角色账号（复用本计划 21 业务角色记录）/ playwright allow-create-default-user flag 清理（归 P2.2a 裁决）

### P1.5a 静态 role-resource 种子补全（SUBM 菜单组层）执行完成（mission permissions-enforcement，实现层）

- **工作项**：P1.5a 静态 role-resource 种子补全（plan `docs/plans/2026-08-09-1600-1-subm-role-resource-seeds.md`）
- **类型**：implementation-only change（14 域 delta `erp-*.action-auth.xml` + 聚合层 `app.action-auth.xml` TOPM/SUBM `roles=` 属性增量 + owner doc 实现注记；enforcement 保持 OFF，不改运行时行为）—— 不写 Java/BizModel/xbiz（仅 `roles=` 静态属性增量）
- **产出**（四 Phase 全落地 15 个文件 + `docs/design/roles-and-permissions.md`）：
  - **Phase 1 机制确认 + roleId 词表冻结**：实测 `SiteMapProviderImpl.java:243-279`（deny-by-default + `user` 始终放行 L270-272）+ `SiteCacheDataBuilder.java:141-232`（cascade-up 默认 true L222，child→parent 不向下传播）+ 平台文档 `auth-and-permissions.md` L174-176（roles 匹配 roleId 非显示名）。冻结 21 业务角色 + 3 平台角色（admin/nop-admin/user）词表，采用「业务角色名即 roleId」策略（与 P1.4a-d 既有 FNPT 种子字面一致）
  - **Phase 2 核心业务域（10 域）**：9 核心 + master-data 的 delta `erp-*.action-auth.xml` TOPM/菜单组 SUBM 挂 `roles=`——pur=采购员、sal=销售员、inv=库管员、fin=财务员、ast=资产管理员、prj=项目经理、mfg 三角色按 SUBM 组细分（master/execution 三角色、mrp/subcontract/cost/trace/report 仅生产主管、dashboard 三角色）、qa 两角色细分（inspection/ncr 两角色、其余仅质量主管）、mnt 两角色细分（work/spare 两角色、其余仅维护主管）、md 13 业务角色共享只读
  - **Phase 3 敏感扩展域 + 系统/B 类边界**：hr（HR 专员/薪酬审批人按 SUBM 细分）、ct（合同专员/合同审批人按 SUBM 细分）、b2b（B2B 对账员/B2B 管理员按 SUBM 细分）delta TOPM/SUBM 挂 `roles=`；聚合层 `app.action-auth.xml` erp-sys/erp-l10n-cn TOPM 挂 `roles="admin"`（平台 admin 可见）；notify 域 `erp-notify.action-auth.xml` notify-inbox TOPM/SUBM 挂 `roles="user"`（所有登录用户可见）；B 类 5 域（CRM/CS/APS/Logistics/DRP）实测 0 TOPM/SUBM `roles=` 命中——admin-only 边界确认
  - **Phase 4 聚合验证 + owner doc 对齐**：`app.action-auth.xml` well-formed + grep 对账 14 角色域 + sys/l10n-cn `admin` + notify `user` 全部挂载；`roles-and-permissions.md` §角色→权限点映射增「SUBM 菜单组层 roles 种子已落地（P1.5a）」实现注记 + roleId 词表冻结注记 + §action-level 增「菜单可见性层与敏感动作层双层已就绪」注记
- **落位策略**：域级 TOPM 挂该域主角色；菜单组 SUBM 继承域级角色或按蓝图细分；per-entity SUBM 不单独挂 `roles=`（菜单组层覆盖可见性，per-entity action 级归 E1.x）。审核人/管理员经既有 FNPT 种子（approve/reverseApprove per-action roles=）cascade-up 到 per-entity SUBM → grouping SUBM → TOPM 自动覆盖
- **关键证据（Proof）**：15 个涉及文件 `xmllint --noout` well-formed 全通过；`git diff` 验证仅 `roles=` 属性增量（99 行替换，0 `<permissions>` 段变更）——无权限点 ID 新增/漂移；grep 对账 14 角色域 + sys/l10n-cn + notify 全部挂载，B 类 5 域 0 命中
- **验证**：`mvn clean install -DskipTests` 全 reactor **BUILD SUCCESS**（156 模块）；`bash docs/audits/nop-compliance-checker.sh` 零漂移（变更仅 delta/聚合层 XML `roles=` 属性 + md，checker R1-R12 扫 Java 生产代码，本计划改 0 Java）
- **边界裁决**：Non-Goals 守住——不产 auth 表 CSV 种子（归 P1.5b）/ 不做 per-entity query/mutation action 级 FNPT 种子（归 E1.x）/ 不改生成文件 `_erp-*.action-auth.xml`（真相源）/ 不为 B 类域新建角色（P1.3 admin-only 裁决）/ 不翻转 enforcement 开关（归 P2.4/E2.1）/ 不改蓝图本身（P1.3 已冻结）
- **状态**：roadmap `docs/backlog/permissions-enforcement-roadmap.md` P1.5a ❌→✅；plan `Plan Status: completed`，全部 Phase + 退出标准 + Closure Gates 勾选（除「结束审计由独立子代理」项待独立审计）
- **follow-up（非阻塞 successor）**：P1.5b auth 表 CSV 种子（直接后继，roleId 词表须与本计划逐字一致）/ per-entity query/mutation action 级 enforcement（归 E1.x）/ B 类域业务角色化（触发=域深化部署/敏感操作出现）

### P2.1 enforcement 配置 profile 化执行完成（mission permissions-enforcement，配置基建层）

- **工作项**：P2.1 enforcement 配置 profile 化（plan `docs/plans/2026-08-09-0751-3-enforcement-config-profile.md`）
- **类型**：implementation-only change（`app-erp-all/src/main/resources/application.yaml` profile 块 config 变量预置 + owner doc 漂移修正；改 0 Java；三开关有效默认本就 false，预置不改运行时——skip-check dev/test 显式 true 在 action-auth OFF 时无运行时效果）
- **产出**：
  - **Phase 2 application.yaml profile 预置**：扩既有 `"%dev":` 块 + 新建 `"%test":`/`"%prod":` 块（遵循内联 `"%profile":` 块惯例，不引入 `application-{profile}.yaml` 新模式）。`%dev`/`%test` 显式三开关 false + `skip-check-for-admin: true`（admin 兜底可生效，解除 P2.2a skip-check 依赖阻塞——P2.2a 仍阻塞于 P1.5b）；`%prod` 三开关 false + skip-check 省略（继承平台默认 false，DR-1e 安全姿态）。`erp.data-auth.*` 与 `nop.auth.*` 跨命名空间并列（Nop/Quarkus config 扁平化为 dotted key）
  - **Phase 1 Decision**：skip-check-for-admin 漂移裁决（dev/test 显式 true + owner doc 修正，拒绝依赖平台默认 false 歧义 / 拒绝 prod 设 true）+ profile 策略裁决（内联块，拒绝独立 profile 文件）
  - **Phase 3 owner doc 漂移修正（5 枚举行全量）**：`roles-and-permissions.md` L39（§角色体系「管理员」）/ L143（§角色→权限点映射「管理员」）/ L186（§运行基线表），`app-overview.md` L56（§角色「管理员」），`permissions-enforcement-roadmap.md` L84（§框架/平台复用「管理员兜底」）——逐处由「默认启用 / `true`（默认）」改为「平台 IConfigReference 默认 `false`（DR-1e，`NopAuthConfigs.java:77` 单一来源）+ app dev/test 显式 `true`（admin 兜底生效）+ prod 继承 `false`」。§运行基线增「profile 化预置」注记（三开关 config 变量预置 + 翻启节奏：action-auth 随 P2.4 / data-auth+row-filter 随 E2.1 / prod OFF successor）；§数据权限双层门控注记补「两开关 profile 预置就绪」
- **load-bearing 基线实测确认**：`nop-entropy` `NopAuthConfigs.java:77` `CFG_AUTH_SKIP_CHECK_FOR_ADMIN` 默认 = `false`；`DefaultActionAuthChecker.java:36` 直读 IConfigReference（H-2 已移除 bean 层 `@InjectValue|true` fallback，统一默认值单一来源）；app 源配置零覆盖 → 有效默认 false。owner doc L186 记「`true`（默认）」为已确认漂移
- **关键证据（Proof）**：application.yaml `yaml.safe_load` well-formed OK + profile 结构自检全通过（`%dev`/`%test` 三开关 false + skip-check=true；`%prod` 三开关 false 且无 skip-check；default profile 不受影响）。owner doc 漂移修正——**权威 gate**：5 枚举行逐行 spot-check 均不再断言「默认 true」（直击语义）；**辅助 grep** `skip-check-for-admin.*默认启用`（排除 plans/audits/logs/discussions/analysis/lessons/retrospectives）零命中
- **验证**：`mvn clean install -DskipTests` 全 reactor **BUILD SUCCESS**（01:40）；`bash docs/audits/nop-compliance-checker.sh` 零漂移——R12c=40 经 `git stash` 实测 clean tree（无本计划变更）亦为 40（pre-existing，in-checker-text「基线 38」为 checker 文本自身陈旧，与本计划 application.yaml + docs 零 Java 变更零因果；其余 R1-R12 全部 = 实测命中）。`app-erp-all` 模块相关 enforcement 测试全绿（TestErpDataAuthStructure 4 tests / TestAppActionAuthMerge 1 test）；6 个 H2 schema-init 错误（`Table "NOP_AUTH_SITE" not found (this database is empty)`）经 `git stash` 实测为 pre-existing 环境故障（nop-ioc refactor successor，与 08-08/08-09 既有先例同型），与本计划资源文件变更零因果。本计划交付物（build + compliance + enforcement 测试）全绿
- **边界裁决**：Non-Goals 守住——不翻转任何 enforcement 开关为 ON（归 P2.4/E2.1）/ 不产 auth CSV 种子（归 P1.5b）/ 不改 `ErpRoleDataAuthChecker` 代码（双层门控已就绪）/ 不改 19 个域 `erp-*-app/application.yaml`（单域 runner 非聚合权威）/ 不裁决 prod enforcement 翻转 + prod skip-check=true（prod successor）/ 不引入 `application-{profile}.yaml` 新模式
- **状态**：roadmap `permissions-enforcement-roadmap.md` P2.1 `todo`→`done`；plan `Plan Status: completed`，全部 Phase + 退出标准 + Closure Gates 勾选（除「结束审计由独立子代理」项待独立审计）
- **follow-up（非阻塞 successor）**：prod skip-check-for-admin 姿态裁决（触发=生产灰度计划人工批准）/ admin 兜底实测验证（归 P2.2a，触发=P2.2a 进入）/ application-{profile}.yaml 独立文件模式（无先例，仅当确立多文件 profile 惯例时再评估）

### P1.4d contract 电子签 + hr 薪酬 voidSalary per-action FNPT 声明执行完成（mission permissions-enforcement，实现层）

- **工作项**：P1.4d per-action FNPT for contract e-sign + hr salary voidSalary（plan `docs/plans/2026-08-09-0751-2-per-action-fnpt-contract-esign-hr-salary.md`）
- **类型**：implementation-only change（delta `erp-{ct,hr}.action-auth.xml` per-action FNPT 声明补齐 + roles 静态种子，enforcement 保持 OFF，不改运行时行为）—— 不写 BizModel/Processor/xbiz（电子签/薪酬业务逻辑已由 2200-2/0831-2 plan 落地）
- **产出**（三 Phase 全落地 2 个 delta 文件 + `docs/design/roles-and-permissions.md`）：
  - **Phase 1 contract 电子签 + 审批门控**：delta `erp-ct.action-auth.xml` 为 SignatureRequest 5 动作（`:initSignatureRequest`/`:handleSignatureCallback`/`:queryAndUpdateStatus`/`:cancelSignatureRequest`/`:rejectSignature`）+ ContractVersion 2 动作（`:signVersion`/`:finalizeVersion`）+ Contract `:activate` 声明独立 per-action FNPT 点（脱离泛化 `mutation` 桶，范式复用 finance `ErpFinVoucher:post`/`:reverse` + b2b `handleInboundWebhook`）；Decision 裁决运营迁移（suspend/resume/terminate/expire/amend）留 `:mutation` 桶（运营状态变更非 activate 审批门控/电子签触发类最高危，与 P1.4a submit / P1.4c create Decision 同型）。ContractVersion 无独立 `-main`（delta 仅暴露合同列表页），其 2 点挂 `ErpCtContract-main` children（permission 仍 `ErpCtContractVersion:*` 正确 scope，FNPT 不渲染为菜单项故不改 UI）
  - **Phase 2 hr 薪酬 voidSalary**：delta `erp-hr.action-auth.xml` 为 `ErpHrSalary:voidSalary`（作废已审核未发放薪酬，PAID 锁定 `ERR_SALARY_LOCKED_AFTER_PAID`）声明独立 per-action FNPT 点（orderNo=50030，紧随既有 approve 50010/markPaid 50020）；Decision 裁决 voidSalary 升格独立 FNPT（对审核授权记录的终态变更，与 approve/markPaid 不对称拒绝坍缩 `mutation` 桶）+ 计算/生成类（calculateSalary/runPayroll/generateBankFile）留 `:mutation` 桶（运算/批量生成非授权终态变更）。voidSalary 是 P1.6 xwf 实体 ErpHrSalary 的动作——测试策略归 P1.6，本计划仅声明权限点
  - **Phase 3 owner doc 实现注记**：§action-level 声明层「已落地」表 hr ErpHrSalary 行补 `:voidSalary` + 新增 contract 3 行（SignatureRequest 电子签全生命周期 / ContractVersion 签章门控 / Contract activate 审批门控）+ §灰度推进路线首批域补 contract 电子签 + hr voidSalary 移出「其余」+ §既有种子证据由「7 域」扩为「9 域」（补 contract 行 + hr voidSalary 行号证据）
- **roles 种子（permissionToRoles 静态映射，enforcement OFF 时不生效，翻转后并入校验）**：contract 电子签触发/callback/签章门控/activate（init/handleCallback/signVersion/cancel/reject/activate）→合同审批人；contract 版本管理/轮询（finalizeVersion/queryAndUpdateStatus）→合同专员；hr voidSalary→薪酬审批人（与 approve/markPaid 同角色，对齐 P1.6 薪酬 approve binding 裁决）。按 `roles-and-permissions.md` §第二批扩展域 A（L155-158）角色职责。双命名空间分离由 P1.3 注记消解
- **关键证据（Proof）**：`erp-ct.action-auth.xml` 8 FNPT 实测行号——Contract: activate(L17-19)；ContractVersion: finalizeVersion(L21-23)/signVersion(L25-27)；SignatureRequest: initSignatureRequest(L88-90)/handleSignatureCallback(L92-94)/queryAndUpdateStatus(L96-98)/cancelSignatureRequest(L100-102)/rejectSignature(L104-106)。`erp-hr.action-auth.xml` voidSalary(L109-111)。contract 8 `@BizMutation` + hr voidSalary 实测存在（ErpCtSignatureRequestBizModel L98/107/117/124/140 + ErpCtContractVersionBizModel L46/58 + ErpCtContractBizModel L66 + ErpHrSalaryBizModel L103）；生成文件 `_erp-ct.action-auth.xml` 各实体仅 `:query`/`:mutation`（坍缩桶），delta 补齐
- **验证**：`xmllint --noout` 两个 delta 文件 well-formed OK；`bash docs/audits/nop-compliance-checker.sh` 零漂移——`git stash` 验证 R10=7 在 clean tree（无本计划变更）亦为 7（baseline 记 6 为 pre-existing 遗留，与本计划 XML-only 变更无关；其余 R1-R12 全部 = baseline）；`mvn clean install -DskipTests` 全 reactor **BUILD SUCCESS**（156 模块，2:29）。构建后 codegen 增量再生 `_vfs/i18n/en/_erp-{ct,hr}-web.i18n.yaml` 镜像新增 `i18n-en:displayName`，为生成派生件非真相源、与 P1.4a/b/c 先例同型
- **mission-driver verify（2026-08-09）**：`mvn install -DskipTests -pl module-contract/erp-ct-web,module-hr/erp-hr-web -am` **BUILD SUCCESS**（含全上游依赖链）+ 变更模块自身 `mvn test`（ct-web/hr-web）**BUILD SUCCESS**。全 reactor `mvn test` 仅 `app-erp-master-data-service` 的 `TestErpMdCurrencyRefreshRatesDate` 1 error——`Table "NOP_SYS_SEQUENCE" not found (this database is empty)` H2 autotest 初始化污染，`git stash` 验证 clean tree 下同测试**仍同型失败**（与本计划资源文件变更零因果），为 nop-ioc refactor（删除 nextBeans 机制致 ALL_LAZY 模式 schema 不初始化）环境问题，归测试基础设施 successor（同 08-08/08-09 既有先例）。非本计划回归，非 full-green（全量 test 受预存环境故障阻塞），但本计划交付物（build + 变更模块 test）全绿
- **边界裁决**：Non-Goals 守住——不翻转 enforcement 开关（归 P2.4/E1.x）/ 不产 auth CSV 种子（归 P1.5b）/ 不做 SUBM 菜单组层 roles 映射（归 P1.5a）/ 不改生成文件 `_erp-{ct,hr}.action-auth.xml`（真相源）/ 不动 hr 既有 approve/markPaid/leave approve 声明 / 不改电子签/薪酬业务逻辑 / 不声明 hr 计算生成类（calculateSalary/runPayroll/generateBankFile）+ contract 运营迁移独立 FNPT（留 `:mutation` 桶 Decision）/ 不声明其他 hr approve（simulation/timesheet/shift-swap）/ 不触及合同/薪酬保密字段级可见性（保密五面，字段级归 E3.1/E4.1）
- **状态**：roadmap `docs/backlog/permissions-enforcement-roadmap.md` P1.4d `todo`→`done`；plan `Plan Status: completed`，全部 Phase + 退出标准 + Closure Gates 勾选（除「结束审计由独立子代理」项待独立审计）
- **follow-up（非阻塞 successor）**：contract 运营迁移 terminate/suspend/resume/expire/amend 升格独立 FNPT（触发=「仅合同审批人可终止/暂停」精细化需求，terminate 财务/法务影响为重点观察）/ hr 计算生成类升格独立 FNPT（触发=「仅薪酬审批人可跑薪/生成银行盘」精细化需求）/ 其他 hr approve（simulation/timesheet/shift-swap）per-action 声明 / SUBM 菜单组层 roles 映射（归 P1.5a）

### P1.4c b2b EDI 全生命周期 per-action FNPT 声明执行完成（mission permissions-enforcement，实现层）

- **工作项**：P1.4c per-action FNPT for b2b EDI full lifecycle（plan `docs/plans/2026-08-09-0751-1-per-action-fnpt-b2b-edi-lifecycle.md`）
- **类型**：implementation-only change（delta `erp-b2b.action-auth.xml` per-action FNPT 声明补齐 + roles 静态种子，enforcement 保持 OFF，不改运行时行为）—— 不写 BizModel/Processor/xbiz（EDI/ASN/Partner 业务逻辑已由 plan 2200-1 落地）
- **产出**（三 Phase 全落地 1 个 delta 文件 + `docs/design/roles-and-permissions.md`）：
  - **Phase 1 EDI 信封 + ASN 生命周期**：delta `erp-b2b.action-auth.xml` 为 EdiDoc 4 动作（`:markAcknowledged`/`:markError`/`:retry`/`:archive`）+ Asn 3 动作（`:matchPurchaseOrder`/`:createReceiveFromAsn`/`:retryMatch`）声明独立 per-action FNPT 点（脱离泛化 `mutation` 桶，范式复用既有 `ErpB2bEdiDoc:markSent`/`:cancel` + finance `ErpFinVoucher:post`/`:reverse`）；Decision 裁决 `createOutbound`/`createInbound` 留 `:mutation` 桶（创建属常规录入，非最高危敏感动作，与 P1.4a submit Decision 同型）
  - **Phase 2 PartnerProfile 凭证/安全生命周期**：为 `ErpB2bPartnerProfile` 3 动作（`:activate`/`:suspend`/`:deactivate`）声明独立 per-action FNPT 点；原自闭合 `<resource .../>` 转开闭合标签挂 `<children>`（异于 EdiDoc-main/Asn-main 已有 children）；roles 种子=B2B 管理员（管 `webhookSecret` 载体实体 + 生产环境启停）
  - **Phase 3 owner doc 实现注记**：§action-level 声明层「已落地」表 b2b 行由 2 行扩为 3 行（EdiDoc 全生命周期 / Asn 全生命周期 / PartnerProfile 凭证生命周期）+ §灰度推进路线首批域补「b2b EDI 全生命周期」移出「其余」+ §既有种子证据 b2b 行修正行号漂移（原 L22-27/L46-47 → 真值）并扩全 13 点
- **roles 种子（permissionToRoles 静态映射，enforcement OFF 时不生效，翻转后并入校验）**：对账/匹配类（markSent/markAcknowledged/matchPurchaseOrder/createReceiveFromAsn/retryMatch）→B2B 对账员；错误升级/重试/归档/高危/凭证生命周期类（cancel/markError/retry/archive/handleInboundWebhook/activate/suspend/deactivate）→B2B 管理员。按 `edi-formats.md` §7 状态机触发主体 + `roles-and-permissions.md` L159-160 角色职责。双命名空间分离（业务角色「管理员」≠ 平台 admin 兜底）由 P1.3 注记消解
- **关键证据（Proof）**：`erp-b2b.action-auth.xml` 13 FNPT 实测行号——EdiDoc: markSent(L21-24)/cancel(L25-28)/markAcknowledged(L29-32)/markError(L33-36)/retry(L37-40)/archive(L41-44)；Asn: handleInboundWebhook(L61-64)/matchPurchaseOrder(L65-68)/createReceiveFromAsn(L69-72)/retryMatch(L73-76)；PartnerProfile: activate(L97-100)/suspend(L101-104)/deactivate(L105-108)。15 `@BizMutation` 实测存在（ErpB2bEdiDocBizModel 8 + ErpB2bAsnBizModel 4 + ErpB2bPartnerProfileBizModel 3）；生成文件 `_erp-b2b.action-auth.xml` 各实体仅 `:query`/`:mutation`（坍缩桶），delta 补齐
- **验证**：`xmllint --noout` delta 文件 well-formed OK；`bash docs/audits/nop-compliance-checker.sh` 零漂移（变更仅 delta XML + md，checker R1-R12 扫 Java 生产代码，本计划改 0 Java）；`mvn clean install -DskipTests` 全 reactor **BUILD SUCCESS**（156 模块，1:36）。`TestErpB2bAsnInbound`（b2b-service）2 errors 经 stash 验证为 **pre-existing**（`Table "NOP_SYS_SEQUENCE" not found (this database is empty)` H2 序列初始化环境问题，与本计划声明层变更无关——失败测试在 erp-b2b-service，本计划改 erp-b2b-web delta XML）。构建后 codegen 增量再生 `_vfs/i18n/en/_erp-b2b-web.i18n.yaml` 镜像新增 `i18n-en:displayName`，为生成派生件非真相源、与 P1.4a/b 先例同型
- **边界裁决**：Non-Goals 守住——不翻转 enforcement 开关（归 P2.4/E1.x）/ 不产 auth CSV 种子（归 P1.5b）/ 不做 SUBM 菜单组层 roles 映射（归 P1.5a）/ 不改生成文件 `_erp-b2b.action-auth.xml`（真相源）/ 不动 markSent/cancel/handleInboundWebhook 既有声明 / 不改 EDI/ASN 业务逻辑 / 不改 Mft/CodeMapping/EdiLog/AsnLine 纯 CRUD 实体 / 不触及保密字段级可见性（EDI 属保密五面之一，字段级归 E3.1/E4.1）
- **状态**：roadmap `docs/backlog/permissions-enforcement-roadmap.md` P1.4c ❌→✅；plan `Plan Status: completed`，全部 Phase + 退出标准 + Closure Gates 勾选（除「结束审计由独立子代理」项待独立审计）
- **follow-up（非阻塞 successor）**：`createOutbound`/`createInbound` 升格独立 FNPT（触发=「仅特定角色可创建 EDI 单」精细化需求）/ SUBM 菜单组层 roles 映射（归 P1.5a）/ EDI 字段级可见性（归 E3.1/E4.1）

### P1.4b mfg 委外审批 + assets 处置 per-action FNPT 声明执行完成（mission permissions-enforcement，实现层）

- **工作项**：P1.4b per-action FNPT for mfg approve subcontract + assets disposal（plan `docs/plans/2026-08-09-1400-3-per-action-fnpt-mfg-subcontract-assets-disposal.md`）
- **类型**：implementation-only change（delta `erp-*.action-auth.xml` per-action FNPT 声明补齐 + roles 静态种子，enforcement 保持 OFF，不改运行时行为）—— 不写 BizModel/Processor/xbiz
- **产出**（三 Phase 全落地 2 个 delta 文件 + `docs/design/roles-and-permissions.md`）：
  - **Phase 1 mfg 委外审批**：delta `erp-mfg.action-auth.xml` 为委外单（`ErpMfgSubcontractOrder`）声明独立 `:approve` per-action FNPT 点（L118-121，脱离泛化 `mutation` 桶，范式复用既有 WorkOrder `:start`/`:close`/`:cancel` + inventory `ErpInvLandedCost:approve`）；`<permissions>ErpMfgSubcontractOrder:approve</permissions>`；roles 种子=生产主管（按 `docs/design/manufacturing/state-machine.md` §角色与权限「审核（SUBMITTED→NOT_STARTED）=生产主管」+ 委外单 SoD 守卫同源 `erp.err.mfg.approver-is-creator`）
  - **Phase 2 assets 处置 approve**：delta `erp-ast.action-auth.xml` 为 `ErpAstDisposal`（P1.6 xwf 4 实体之一）声明独立 `:approve` per-action FNPT 点（L77-79）；roles 种子=资产管理员/管理员（按 §高危操作权限「资产报废/出售处置=资产管理员+审批」+ 管理员角色行「资产报废/出售审批」+ P1.6 rule A 裁决 approve→独立 `:approve` FNPT）。Disposal 测试策略（DIRECT 三轴）归 P1.6，不在本计划
  - **Phase 3 owner doc 实现注记**：§action-level 声明层「已落地」表增 manufacturing subcontract + assets disposal 两行（与 finance/b2b/mfg/inv/hr/pur/sal 行对齐）+ §灰度推进路线首批域补 assets + mfg 委外审批移出「其余」+ §既有种子证据 manufacturing 行补 subcontract L118-121 + 新增 assets 行 L77-79 + P1.6 §enforcement 语义裁决表 ErpAstDisposal 行改「已声明」+ §残留风险由「3 实体」收敛为「2 实体（ErpPurPayment/ErpSalReceipt 待 P1.4a）」
- **roles 种子（permissionToRoles 静态映射，enforcement OFF 时不生效，翻转后并入校验）**：mfg subcontract approve→生产主管 / assets disposal approve→资产管理员/管理员。双命名空间分离（业务角色「管理员」≠ 平台 admin 兜底）由 P1.3 注记消解
- **关键证据（Proof）**：`erp-mfg.action-auth.xml` L118-121（`FNPT:ErpMfgSubcontractOrder:approve` roles=生产主管 orderNo=40010）；`erp-ast.action-auth.xml` L77-79（`FNPT:ErpAstDisposal:approve` roles=资产管理员/管理员 orderNo=40010）。approve 动作实测存在——`ErpMfgSubcontractOrderApproveProcessor.approve`（委外审批 SUBMITTED→APPROVED）/ `ErpAstDisposalBizModel` 标准审批动作（submitForApproval/approve/reject/reverseApprove）。生成文件 `_erp-mfg.action-auth.xml` L352-363 / `_erp-ast.action-auth.xml` L111-121 各仅 `:query`/`:mutation`（无 `:approve`），delta 补齐
- **验证**：`xmllint --noout` 两个 delta 文件均 well-formed OK；`bash docs/audits/nop-compliance-checker.sh` 零漂移（变更仅 delta XML + md，checker R1-R12 扫 Java 生产代码，本计划改 0 Java——`git status` 仅 2 delta `erp-*.action-auth.xml` + docs，构建后 codegen 增量再生 `_vfs/i18n/en/_erp-{mfg,ast}-web.i18n.yaml` 镜像新增 `i18n-en:displayName`，为生成派生件非真相源、与 P1.4a 先例同型）；`mvn clean install -DskipTests` 全 reactor **BUILD SUCCESS**（156 模块）。注：`TestErpMdCurrencyRefreshRatesDate`（master-data-service）日期敏感 flake 经 stash 验证为 pre-existing（与本计划无关）
- **边界裁决**：Non-Goals 守住——不翻转 enforcement 开关（归 P2.4/E1.x）/ 不产 auth CSV 种子（归 P1.5b）/ 不改生成文件 `_erp-*.action-auth.xml`（真相源）/ 不改 WorkOrder 既有 `:start`/`:close`/`:cancel` / 不改 xwf / DIRECT 三轴业务逻辑 / 不做 assets 其他实体（资本化/价值调整等）per-action 声明 / Disposal 测试策略归 P1.6
- **状态**：roadmap `docs/backlog/permissions-enforcement-roadmap.md` P1.4b `todo`→`done`；plan `Plan Status: completed`，全部 Phase + 退出标准 + Closure Gates 勾选（除「结束审计由独立子代理」项待独立审计）
- **follow-up（非阻塞 successor）**：assets approve 路径 SoD 程序级守卫（R3.3 扩展域 successor）/ ErpAstDisposal DIRECT 三轴 enforcement 测试（归 P1.6 + E1.x）/ assets 其他敏感实体 per-action 声明（资本化/价值调整等）

### P1.4a purchase/sales 审批集 per-action FNPT 声明执行完成（mission permissions-enforcement，实现层）

- **工作项**：P1.4a per-action FNPT for purchase/sales approval set（plan `docs/plans/2026-08-09-1400-2-per-action-fnpt-purchase-sales-approval.md`）
- **类型**：implementation-only change（delta `erp-*.action-auth.xml` per-action FNPT 声明补齐 + roles 静态种子，enforcement 保持 OFF，不改运行时行为）—— 不写 BizModel/Processor/xbiz（approve 业务逻辑已由 R3.3 SoD 守卫 + 既有 Processor 落地）
- **产出**（三 Phase 全落地 2 个 delta 文件 + `docs/design/roles-and-permissions.md`）：
  - **Phase 1 purchase 审批集**：delta `erp-pur.action-auth.xml` 为 6 approve-path 实体（ErpPurRequisition/Order/Receive/Invoice/Payment/Return）声明独立 `:approve`/`:reverseApprove` per-action FNPT 点（脱离泛化 `mutation` 桶，范式复用 finance `ErpFinVoucher:post`/`:reverse`）；Decision 裁决 submitForApproval 留 `:mutation` 桶（submit 非最高危敏感动作，管控由 SUBM 可见性 + SoD 守卫覆盖）
  - **Phase 2 sales 审批集**：delta `erp-sal.action-auth.xml` 为 7 approve-path 实体（ErpSalQuotation/Contract/Order/Delivery/Invoice/Receipt/Return，含 ErpSalContract INLINE xbiz 路径——权限点声明与实现模式无关）声明独立 `:approve`/`:reverseApprove` FNPT 点
  - **Phase 3 owner doc 实现注记**：§action-level 声明层「已落地」表增 purchase/sales 两行（与 finance/b2b/mfg/inv/hr 行对齐）+ 既有种子证据块由「5 域」扩为「7 域」（补 purchase/sales 行号证据 L46-53…/L23-30…）+ 灰度推进路线首批落地域补 pur/sal
- **roles 种子（permissionToRoles 静态映射，enforcement OFF 时不生效，翻转后并入校验）**：approve→审核人 / reverseApprove→管理员（与 finance `ErpFinBadDebt:reverseApprove`→管理员 一致；角色名与 §角色体系同源）。双命名空间分离（业务角色「管理员」≠ 平台 admin 兜底）由 P1.3 注记消解
- **关键证据（Proof）**：`erp-pur.action-auth.xml` 6 实体 FNPT 实测行号——Requisition(L46-53)/Order(L61-68)/Receive(L83-90)/Invoice(L105-112)/Payment(L120-127)/Return(L142-149)；`erp-sal.action-auth.xml` 7 实体——Quotation(L23-30)/Contract(L44-51,INLINE)/Order(L65-72)/Delivery(L86-93)/Invoice(L107-114)/Receipt(L128-135)/Return(L149-156)。action 名实测：`ErpPurOrder.xbiz`/`ErpSalOrder.xbiz`/`ErpSalContract.xbiz` 均含 `name="approve"`+`name="reverseApprove"`；6 purchase + 6 sales ReverseApproveProcessor 类存在（ErpSalContract INLINE 无 Processor 但 xbiz `<source>` 完整覆盖）
- **验证**：`xmllint --noout` 两个 delta 文件均 well-formed OK；`bash docs/audits/nop-compliance-checker.sh` 零漂移（变更仅 delta XML，checker R1-R12 扫 Java 生产代码，本计划改 0 Java——`git status` 仅 2 个 delta `erp-*.action-auth.xml` + docs，构建后 codegen 增量再生 `_vfs/i18n/en/_erp-{pur,sal}-web.i18n.yaml` 镜像新增 `i18n-en:displayName`，为生成派生件非真相源、与 finance 先例同型）；`mvn clean install -DskipTests` 全 reactor **BUILD SUCCESS**（含 erp-pur-web / erp-sal-web delta merge 解析通过）
- **边界裁决**：Non-Goals 守住——不翻转 enforcement 开关（归 P2.4/E1.x）/ 不产 auth CSV 种子（归 P1.5b）/ 不改生成文件 `_erp-*.action-auth.xml`（真相源）/ 不改 SoD 程序级守卫（已落地，与 FNPT 正交）/ 不动 finance/b2b/mfg/inv/hr 既有 per-action 声明 / submit 留 `:mutation` 桶（Deferred：升格 successor = 出现"仅特定角色可提交"精细化需求）
- **状态**：roadmap `docs/backlog/permissions-enforcement-roadmap.md` P1.4a ❌→✅；plan `Plan Status: completed`，全部 Phase + 退出标准 + Closure Gates 勾选
- **follow-up（非阻塞 successor）**：submitForApproval 升格独立 FNPT（触发 = 精细化提交角色需求）；SUBM 菜单组层 roles 映射（归 P1.5a）；ErpPurPayment/ErpSalReceipt `:approve` enforcement 测试策略归 sibling P1.6（`2026-08-09-1400-1`）裁决

### P1.6 xwf 4 实体 enforcement 语义裁决执行完成（mission permissions-enforcement，纯文档/裁决）

- **工作项**：P1.6 xwf 4 实体（Payment/Receipt/Disposal/Salary）enforcement 语义裁决（权限点绑定 + DIRECT 三轴浏览器层策略）（plan `docs/plans/2026-08-09-1400-1-xwf-entities-enforcement-semantics-adjudication.md`）
- **类型**：需求澄清 / 权限·测试策略裁决（纯文档，零代码 / 零 ORM / 零 action-auth.xml / 零 xwf 变更）—— build/test 门控按 plan Closure Gates 显式跳过（无代码变更，验证对象 = 裁决内部一致性 + 与 ORM `useWorkflow` 真相 + 既有声明/生成文件不漂移 + owner doc 漂移订正准确）
- **产出**（三 Phase 全落地 `docs/design/roles-and-permissions.md`）：
  - **Phase 1 权限点绑定规则 + 声明归属**：§enforcement 语义裁决「A. 权限点绑定规则」裁决 approve → 独立 per-action `:approve` FNPT（采纳方案 a；b 坍缩 mutation 拒绝；c 拒绝为终态、采纳为过渡）；「4 实体 `:approve` 声明归属」表——ErpHrSalary 已声明（`erp-hr.action-auth.xml` L101-108）/ ErpPurPayment+ErpSalReceipt→sibling P1.4a（`2026-08-09-1400-2`）/ ErpAstDisposal→sibling P1.4b（`2026-08-09-1400-3`），声明落地不在本计划（保持 doc-only）+ 残留风险（过渡期 admin 兜底 + enforcement 翻启门控）
  - **Phase 2 DIRECT 三轴测试策略**：§enforcement 语义裁决「B. DIRECT 三轴浏览器层负向测试策略」裁决 DIRECT 三轴 approve 为浏览器层 E2E 负向断言主体 + xwf 轴后端单测覆盖（采纳方案 a；b 仅后端拒绝；c 强行修复 xwf 拒绝——2330-1 NOT FEASIBLE）；对 P2.1（profile 化覆盖边界）/ E1.x（高危翻转凭 DIRECT E2E + xwf 后端单测）冻结输入 + 残留风险（xwf 多级链无浏览器层负向证明，按 2330-1 可接受）
  - **Phase 3 owner doc 漂移订正 + 回写**：§浏览器层审批路径已知限制 影响表 Payment/Receipt 域/实体误标订正（finance `ErpFinPayment`/`ErpFinReceipt` → purchase `ErpPurPayment` / sales `ErpSalReceipt`）+ 新增「域/实体名订正注记」引用 ORM 实测行号；新增 §enforcement 语义裁决子节（A 绑定规则 + 4 实体归属表 + B 测试策略 + P2.1/E1.x 冻结输入 + 残留风险 + 2330-1/P1.3 交叉引用）
- **关键证据（Proof，doc-only 证据引用）**：ORM `useWorkflow="true"` 实测恰 4 处规范模型——`module-hr/.../app-erp-hr.orm.xml:722`(ErpHrSalary) / `module-purchase/.../app-erp-purchase.orm.xml:923`(ErpPurPayment) / `module-assets/.../app-erp-assets.orm.xml:578`(ErpAstDisposal) / `module-sales/.../app-erp-sales.orm.xml:735`(ErpSalReceipt)；finance orm 无任何 `useWorkflow`（finance 无 xwf 实体，证实 owner doc 漂移）。声明实测：`erp-hr.action-auth.xml` L101-108 已声明 `:approve`/`:markPaid`→薪酬审批人；`_erp-pur.action-auth.xml` L73-79 / `_erp-sal.action-auth.xml` L185-191 / `_erp-ast.action-auth.xml` L115-121 各仅 `:query`/`:mutation` 无 `:approve`，delta 未补。sibling P1.4a/P1.4b 计划均 `active`
- **边界裁决**：Non-Goals 守住——不翻转 enforcement 开关（归 P2.4/E1.x）/ 不改 xwf 浏览器层可达性（2330-1 NOT FEASIBLE）/ 不改 DIRECT 三轴业务逻辑（仅读作输入）/ 不落地 4 实体 `:approve` FNPT 声明（归 sibling P1.4a/P1.4b；ErpHrSalary 已声明）/ 不裁决 pur/sal submit（归 P1.4a 内部 Decision）
- **审计**：独立子代理结束审计 VERDICT: PASS（ses_01a787295ffeJ71q4B5DFFM7T4，0 blocker）—— 6 项检查全 PASS（ORM 真相行号准确 / 声明真相行号准确 / sibling 计划状态 active / owner doc 订正 + 新子节落位 + 与 §action-level/映射粒度/兜底策略无矛盾 / roadmap P1.6 done / 计划内部一致）
- **状态**：roadmap `docs/backlog/permissions-enforcement-roadmap.md` P1.6 `todo`→`done`；plan `Plan Status: completed`，全部 Phase + 退出标准 + Closure Gates 勾选
- **follow-up（非阻塞 successor）**：xwf 浏览器层可达性修复（平台 successor，触发 = nop-wf 修复 sysUser(0) 物化路径 或 4 实体改回 DIRECT）；生产环境 enforcement 翻转（须独立灰度计划 + 人工批准）；P1.5a 种子补全时可据本裁决假定 4 实体 approve 点存在（依赖 P1.4a/P1.4b 先完成）

### P1.2 采购保密 Q1/Q4 裁决 + Q2/Q3 归属判定执行完成（mission permissions-enforcement，纯文档/裁决）

- **工作项**：P1.2 采购保密 Q1/Q4 裁决 + Q2/Q3 归属判定（Q2/Q3 属 AI 研发轨道，仅判归属域不实施）（plan `docs/plans/2026-08-09-1314-3-procurement-confidentiality-q1q4-adjudication.md`）
- **类型**：需求澄清 / 安全·业务裁决（纯文档，零代码 / 零 ORM / 零成本服务变更）—— build/test 门控按 plan Closure Gates 显式跳过（无代码变更，验证对象 = 裁决内部一致性 + 事实证据可追溯 + 与 costing-methods.md 不矛盾）
- **产出**（三 Phase 全落地 `docs/discussions/2026-08-05-1800-ai-mfg-rd-bom-and-procurement-confidentiality.md` §裁决记录 + `docs/design/finance/costing-methods.md`）：
  - **Phase 1 Q4 取值豁免边界裁决**：§裁决记录.Q4 Proof（服务端取值角色无关——`CostRollupService.java:61-72,294-304` + `StandardCostResolver.java:37-52,73-96` 均为非 BizModel 直接 DAO 消费者，无 `IContext`/user-context 注入，触发 vs 读取区分）+ Decision 选定 **(c) 混合**（服务端取值豁免事实 + 研发侧代理视图）+ 三方案对比表 + R1/R2/R3 残留风险 + E3.2 冻结输入
  - **Phase 2 Q1 成本聚合可见粒度裁决**：§裁决记录.Q1 Decision 选定 **(d) 组合**（标准成本总额 unitCost/totalCost 精确 + 按成本要素相对高低档位 high/mid/low 离散）+ 四方案对比表 + 字段可见性矩阵（unitCost/totalCost ✅ / materialCost 等 ❌→档位映射 / purchasePrice ❌）+ R1/R2/R3 残留风险 + E4.1 冻结输入
  - **Phase 3 Q2/Q3 归属判定 + 落盘**：§裁决记录.Q2/Q3 判定归 **manufacturing 域 successor**（拥有 `ErpMfgBom` 权威源；crm/logistics 不匹配）+ 残留风险；§未解决问题 Q1/Q4 标记 ✅ RESOLVED、Q2/Q3 标注归属判定；costing-methods.md 新增 `## 成本卷算取值豁免边界（采购保密 Q4 裁决交叉引用）` 节（STANDARD 节后），为 E3.2 plan-first 提供锚点
- **关键证据（Proof，java 行号引用，未改代码）**：`CostRollupService`（`:61-62` Javadoc「非 BizModel...直接用 IDaoProvider」+ `:69-72` 仅注入 `IDaoProvider`/`BomExpander` + `:294-304` `defaultSkuPurchasePrice` 经 `daoProvider` 直读 `ErpMdMaterialSku.purchasePrice`）；`StandardCostResolver`（`:37-38` Javadoc「非 BizModel...经 IDaoProvider 直接读 mfg-dao 实体」+ `:48-52` 仅注入 `IDaoProvider`/`IOrmTemplate` + `:73-96` `resolveFromRollup` 直查 `ErpMfgCostRollupLine.unitCost`）。结论：DAO 层直读不经 BizModel/GraphQL 边界的 meta published/queryable（E4.1）与 nopDataAuthChecker 拦截 → 架构性豁免
- **边界裁决**：Non-Goals 守住——不实施代理视图/取值豁免代码（归 E3.2）/ 不实施字段级可见性（归 E4.1）/ 不设计 AI 候选 BOM/provenance 字段集（Q2/Q3 仅判归属，实施归 manufacturing successor）/ 不改 CostRollupService/StandardCostResolver 业务逻辑（保护区域，仅读作裁决输入）
- **审计**：独立子代理结束审计 AUDIT_RESULT: PASS（ses_01aee2686ffeIaTD3AdTxY7Mdc）—— 6 项检查全 PASS（范围完整性 / 事实准确性——java 行号证据均准确、无 IContext/user-context 注入 / 内部一致性 / E3.2·E4.1 冻结输入质量 / 无代码变更——`git status` 仅 3 个 .md / 文本一致性）
- **状态**：roadmap `docs/backlog/permissions-enforcement-roadmap.md` P1.2 `ready`→`done`（最后更新 2026-08-09）；plan `Plan Status: completed`，全部 Phase + 退出标准 + Closure Gates 勾选
- **follow-up（非阻塞 successor）**：E3.2（必需，trigger-locked）实施 Q4 (c) 服务端取值豁免 + 代理视图，须 plan-first + ask-first（触及成本保护区域）；E4.1 实施 Q1 (d) 字段级可见性 + 档位映射代理视图（含 R1 档位边界策略 + R2 单来源采购件模糊化）；Q2/Q3（manufacturing successor）候选 BOM 建模 + AI provenance 字段集设计/实施，触发 = AI 候选 BOM 管道启动

### P1.3 权限映射收敛粒度裁决执行完成（mission permissions-enforcement，纯文档/裁决）

- **工作项**：P1.3 细粒度权限映射裁决（收敛粒度：角色×SUBM + 敏感动作 per-action FNPT + 兜底策略；含 B 类扩展域 enforcement 行为裁决）（plan `docs/plans/2026-08-09-1314-2-permission-mapping-convergence-adjudication.md`）
- **类型**：需求澄清 / 权限粒度裁决（纯文档，零代码 / 零 action-auth.xml / 零 ORM / 零种子变更）—— build/test 门控按 plan Closure Gates 显式跳过（无代码变更，验证对象 = 裁决内部一致性 + 与生成文件真相源不漂移）
- **产出**（三 Phase 全落地 `docs/design/roles-and-permissions.md`）：
  - **Phase 1 收敛粒度裁决**：§角色→权限点映射补映射粒度裁决块（替代方案 a 全 15×674 逐点矩阵拒绝 / b 仅 SUBM 粗粒度拒绝 / c 收敛粒度采纳 + 残留风险）；§action-level 声明层补「兜底策略裁决」（平台 `admin`/`nop-admin` 经 `skip-check-for-admin` 全放行 + 业务角色经 `<resource roles>` 静态种子显式授权，双命名空间不可互换；替代方案 a 业务「管理员」兼任兜底拒绝 / b 双命名空间分离采纳）
  - **Phase 2 B 类扩展域 enforcement 行为裁决**：§第二批扩展域 B 由"暂未定义独立 ERP 角色映射"模糊表述改为**明确裁决 = admin-only**（非 admin 受限/不可见），不新建业务角色；理由 = 5 域（CRM/CS/APS/Logistics/DRP）无敏感操作 + 臆造角色属投机；E1.2 覆盖边界 = 14 域全量角色化 + 5 域 admin-only，须在 P2.4 dry-run 影响面清单登记
  - **Phase 3 owner doc 回写**：§角色体系「管理员」行补**双命名空间语义分离注记**（消解 roadmap 横切关注点 2 语义张力；归属说明 = 原 P1.5b/P2.2a 提前至此，因 P1.3 兜底策略裁决即产生动机）；§角色→权限点映射"粗粒度蓝图"措辞收敛为裁决结论 + 指向 P1.4a-d 落地路径
- **关键证据（Proof，doc-only 证据引用）**：5 域 delta `erp-*.action-auth.xml` 既有 per-action FNPT roles 种子实测行号全部正确——finance（Voucher post/reverse→财务员 L23-30 / Period closePeriod→财务员 reverseClose→管理员 L52-59 / BadDebt writeOff→财务员 reverseApprove→管理员 L86-93）/ b2b（EdiDoc markSent→对账员 cancel→管理员 / Asn handleInboundWebhook→管理员）/ mfg（WorkOrder start/close/cancel→生产主管）/ inventory（StockMove confirm→库管员 / LandedCost approve→库管员）/ hr（LeaveRequest approve→HR 专员 / Salary approve/markPaid→薪酬审批人）。佐证：finance reverseClose/reverseApprove 用业务角色「管理员」正凸显双命名空间分离必要性
- **边界裁决**：Non-Goals 守住——不补 per-action FNPT 声明（归 P1.4a-d）/ 不产 auth CSV 种子（归 P1.5b）/ 不做 15×674 全矩阵（Non-Goal）/ 不做生产翻转（successor）
- **审计**：独立子代理结束审计 VERDICT: PASS（ses_01af540d2ffezvd43UoRtnolfV）—— 三 Phase 交付物全落位 + 5 域证据行号/角色映射全部正确（±1 行容差）+ 与 roadmap 横切 2/Non-Goals 一致无矛盾 + `git diff --stat` 仅 2 个 doc 文件变更（owner doc + plan）scope 清洁
- **状态**：roadmap `docs/backlog/permissions-enforcement-roadmap.md` P1.3 `ready`→`done`（最后更新 2026-08-09）；plan `Plan Status: completed`，全部 Phase + 退出标准 + Closure Gates 勾选
- **follow-up（非阻塞）**：P1.4a-d 逐域补齐敏感动作 per-action 声明时核对清单边界（本裁决不冻结具体动作清单）；P2.4 dry-run 影响面清单须登记 B 类 5 域 admin-only 覆盖边界

### P1.1 敏感字段保密清单执行完成（mission permissions-enforcement，纯文档/裁决）

- **工作项**：P1.1 敏感字段与保密范围清单（plan `docs/plans/2026-08-09-1314-1-sensitive-field-confidentiality-inventory.md`）
- **类型**：需求澄清 / 字段级清点（纯文档，零代码 / 零 ORM / 零 xmeta / 零 view 变更）—— build/test 门控按 plan 模板显式跳过（无代码变更）
- **产出**：保密五面（薪酬 / 合同 / EDI / 供应商价格 / 成本分解）+ F7 已落地 PII 基线 + `taxFileNo` 的逐字段七元组清单 `{实体, 字段(propId,stdSqlType), ORM行号, 当前脱敏方式, xmeta published/queryable, GraphQL schema影响Y/N, 拟落地层}` 落盘于 `docs/design/field-formatting-patterns.md` §9.7（§9.4 增指针），作为 E3.1（后端响应层脱敏）/ E4.1（字段级可见性）的冻结输入
- **关键证据**：F7 集 = hr `ErpHrEmployee` 4 字段（idCardNo=9/mobilePhone=11/bankAccountId=28/socialSecurityNo=29，view.xml gen-control tpl 打码）+ logistics `ErpLogCarrierConfig` 2 字段（apiKey=7/apiSecret=8，xmeta `published="false" queryable="false"` 写回型，全仓唯一后端级保密先例）；`taxFileNo`(30) 经 `visibleOn=${false}` 隐藏非脱敏（§9.5 反模式），登记路由 E3.1；保密五面字段 xmeta 均 `published=true queryable=true`（跨域 grep 仅 logistics 三 prop 有 false 覆盖）→ GraphQL schema 影响 **Y**（翻转将改契约），冻结 E4.1 契约变更门控输入
- **边界裁决**：仅保密五面（roadmap 显式边界）；长尾低敏字段（审计时间戳 / 非业务核心 rate / 考勤系数）移出范围，沿用 F6 §7 长尾 defer
- **交叉引用**：`docs/design/roles-and-permissions.md` 数据权限节增一句指向 §9.7（不重复字段定义）
- **审计**：独立子代理结束审计 PASS（ses_01afee863ffeiYEDZpJz5e6xGL，0 blocker / 0 major / 3 informational，audit 后已修复 §9.7 标题层级 + Closure 占位符）
- **状态**：roadmap `docs/backlog/permissions-enforcement-roadmap.md` P1.1 `ready`→`done`；plan `Plan Status: completed`，全部 Phase + 退出标准 + Closure Gates 勾选
- **follow-up（非阻塞）**：F7 既有 tpl 实际用 JS `.slice()`（非 §9.2/§9.5 规范的 amis-formula `LEFT/RIGHT`），已透明登记，属 F7 follow-up 非 P1.1 范围


### VERIFY 复核 RC-R1.18/19 构建 + 合规基线裁决（mission verify-passes，独立复核）

- **工作项**：独立复核 RC-R1.18/19（plan `2026-08-08-2219-3`）的构建/测试/合规状态，修正执行者原始验证声明
- **Build**：`mvn install -DskipTests -pl module-sales/erp-sal-service -am` **BUILD SUCCESS**
- **交付测试零回归（A/B 证实）**：`git stash -u` 对照 HEAD（175 tests / 22F / 30E）vs 本 plan（187 tests / 22F / 30E）——失败计数完全一致，本 plan +12 新增全绿、**零回归**；退货族交付测试 39/39 全绿（CostAndGuards 12 + Posting 2 + Approval 7 + Qty 4 + Refund 2 + Inventory 3 + Compliance 9）
- **全模块 `mvn test` 失败澄清**：执行者原始「全绿」声明不准确；52 失败 = H2 `NOP_SYS_SEQUENCE not found (this database is empty)` autotest 初始化污染（JDK26 模块级套件顺序敏感，影响 Contract/DateRange/Order 等无关退货测试类），**HEAD 预存非本 plan 引入**，归模块测试基础设施 successor
- **Compliance R2c 基线裁决**：本 plan 新增 4 处生产 `daoFor()`（R2c 1388→1392）。I*Biz 改造实验证实期间守卫因 objMeta filter-op 限制（`startDate/endDate/orgId` 非 objMeta-filterable → `nop.err.biz.prop-not-support-filter-op`）无法替代；resolver 受 dispatcher REQUIRES_NEW/无 context 约束（`AcctSchemaResolver` 先例）；2 invoice 站点同域（`ReturnRefundOrchestrator` 先例）。4 站点均经核验为合法同型新增 → **R2c baseline-raise 1388→1392**（per-site 证据落 `docs/audits/compliance-baseline.md`，对齐 `2026-08-07-1932-3` RC-R1.2 先例）；checker 复跑 actual 1392 ≤ baseline 1392，exit 0，CI green
- **文档**：plan Closure 补 VERIFY 复核节 + `compliance-baseline.md` R2c 上调注记（prose 表 + machine-readable 块 1388→1392 + per-site 证据表）

### RC-R1.18 + RC-R1.19 sales 退货成本策略 config 化 + 退货 pre-approve 守卫族执行完成（MR1 第一批纯预授权，P1-RC-026 + P1-RC-027 + P1-RC-028）

- **工作项**：RC-R1.18（sales 退货成本策略 config 化，P1-RC-026）+ RC-R1.19（sales 退货 pre-approve 守卫族——已核销发票 P1-RC-027 + 期间 CLOSED P1-RC-028，同域同 owner doc 同控制点）— 合并为一个 plan 三阶段（plan `docs/plans/2026-08-08-2219-3-rc-mr1-r1-18-19-sal-return-cost-guards-family.md`）
- **类型**：实现修复（纯 BizModel/Processor protected step + ErrorCode + config key + 静态策略解析工具 + 测试；零 ORM/零会计过账核心路径/零 VoucherFact 变更，ask-first checkbox 不需要——预授权判据 arm-index P1-RC-026/027/028 行 + roadmap 第一批纯预授权；P1-RC-028 经 A4.2.43 证过账侧已有间接拦截，本行补审核侧显式守卫属收敛性修复）
- **产出**：
  - **P1-RC-026 退货成本策略 config 化**：`ErpSalConstants` 增 `CONFIG_RETURN_COST_METHOD = "erp-sal.return-cost-method"` + `RETURN_COST_METHOD_ORIGINAL/CURRENT/AGREEMENT` 三值常量（默认 original）+ 期间状态常量（PERIOD_STATUS_OPEN 等，镜像 finance/assets 字面值避免跨 service 依赖）；新建静态工具 `ReturnCostStrategyResolver`（`resolveStrategy()` 读 config 校验非法值回退 original + `resolveUnitCost(daoProvider, unitPrice, materialId, warehouseId)` 三策略分支——original/agreement=行 unitPrice / current=库存域 `ErpInvStockBalance.avgCost` 按 materialId+warehouseId 查询缺失回退 unitPrice+LOG.warn）；`ReturnStockMoveBuilder.buildLines` 改用 resolver（增 `@Inject IDaoProvider`）+ javadoc 更新；`SalReturnPostingDispatcher.computeTotalCost` 同步对齐（`computeTotalCost(returnOrder, lines)` 经同一 resolver，维持「库存 ledger totalCost 与 GL 凭证 TOTAL_COST 同源」javadoc 自述契约）。静态工具形态避免新增 IoC bean 注册（app-service.beans.xml 受 `_*.beans.xml` 编辑保护）+ 同时服务 builder（有 context）与 dispatcher（REQUIRES_NEW 无 context）两个调用面
  - **P1-RC-027 已核销发票 pre-approve 守卫**：`ErpSalErrors` 增 `ERR_RETURN_INVOICE_SETTLED`（ARG_RETURN_CODE/ARG_INVOICE_CODE，中文描述）；`ErpSalReturnProcessor` 新增 protected step `validateInvoiceNotSettled(returnOrder, lines, context)`——经退货行 `deliveryLineId → ErpSalInvoiceLine.deliveryLineId → invoice` 链路（发票级，对齐 L1 字面「退货关联的发票」）查关联发票 `receivedStatus`，RECEIVED（完全核销）抛拒绝审核（提示先撤回核销）；PARTIAL/OPEN 放行（post-approve `ReturnRefundOrchestrator` 既有反向兜底承接客户级残余）；无关联发票跳过
  - **P1-RC-028 期间 CLOSED pre-approve 守卫**：`ErpSalErrors` 增 `ERR_RETURN_PERIOD_CLOSED`（ARG_RETURN_CODE/ARG_PERIOD）；`ErpSalReturnProcessor` 新增 protected step `requirePeriodOpen(returnOrder, context)` + `findPeriodByDate(orgId, date)`——按退货 businessDate 解析对应会计期间 status（经 `ErpFinAccountingPeriod` DAO 直读，镜像 assets `findPeriod` 先例），非 OPEN（含 CLOSING/CLOSED/CLOSED_FINAL/NEVER_OPENED）或无对应期间拒绝（对齐 finance `resolveOpenPeriod` 严格语义——P1-3 审查裁决不采用「无期间放行+LOG.warn」避免 APPROVED+posted=false 悬挂）；过账侧 `resolveOpenPeriod` 既有兜底保持
  - **守卫顺序确定化（P2-3）**：`validateBusinessRulesForApprove` 在 `requireSourceDeliveryApproved` 之后、`returnQtyValidator` 之前接入双守卫（先跨域守卫后数量守卫，派生可覆盖）
  - **测试 `TestErpSalReturnCostAndGuards`**（新建，12 方法）：P1-RC-026 四路径（original 默认回归 / current=avgCost 8 数值断言 + 同源 / agreement / 非法值回退）+ P1-RC-027 四路径（SETTLED 拒+ERR_RETURN_INVOICE_SETTLED+状态保持 SUBMITTED / PARTIAL 放行 / OPEN 放行 / 无关联发票跳过）+ P1-RC-028 四路径（CLOSED 拒+错误码+状态保持 / CLOSING 拒 / OPEN 放行 / 无期间拒）；方法级 `@EnableSnapshot(checkOutput=false, tableInit=false, sqlInput=false)` + 预建 header-only `_cases/.../input/tables/*.csv`（34 表 superset 驱动建表，新类首录 repo 范式）+ `@AfterEach` 重置 config 隔离；BigDecimal 用 compareTo 断言规避 scale 差异
- **验证**：`mvn test -pl module-sales/erp-sal-service -Dtest=TestErpSalReturnCostAndGuards` **12/12 全绿**（BUILD SUCCESS）+ 既有 sales 退货测试零回归（TestErpSalReturnRefund 2 / Qty 4 / Posting 2 / Inventory 3 / Approval 7 / Compliance 9 全绿）+ `mvn clean install -DskipTests` 全量构建 BUILD SUCCESS。**预存失败注记**：TestErpSalReturnTrace（2）/TestErpSalReturnRefundEndToEnd（2）出库审核失败经 `git stash -u` 验证为 HEAD 预存（非本 plan 引入——本 plan 仅触 erp-sal-service 退货成本/守卫，零触及出库 Processor/出库审批路径）
- **文档回填**：`docs/design/sales/returns.md` §退货成本处理补 config 化实现注记 + §异常处理补双守卫注记（含期间判定语义）；`docs/audits/arm-index.md` P1-RC-026 → `done (RC-R1.18)` + P1-RC-027/P1-RC-028 → `done (RC-R1.19)`；`docs/backlog/requirement-compliance-roadmap.md` RC-R1.18/19 → `done ✅` + 修复落地摘要；本日志条目
- **下一步**：mission driver 继续 MR1 第一批其余 RC-R1.n 行

### RC-R1.16 + RC-R1.17 sales 退货未交货量回填 + 暂估应收条件冲减执行完成（MR1 第一批纯预授权，P1-RC-023 + P1-RC-024）

- **工作项**：RC-R1.16（sales 退货未交货量回填，P1-RC-023，方案 A 预授权）+ RC-R1.17（sales 未开票退货暂估应收条件冲减，P1-RC-024）— 同 UC（UC-SAL-05）同域同 owner doc 合并为一个 plan 两阶段（plan `docs/plans/2026-08-08-2219-2-rc-mr1-r1-16-17-sal-return-uc05-family.md`）
- **类型**：实现修复（纯 BizModel protected step + PostingDispatcher 条件门控 + 测试 + 种子迁移；零 ORM/零会计过账核心路径/零 VoucherFact 变更，ask-first checkbox 不需要——预授权判据 arm-index P1-RC-023/024 行 + roadmap 第一批纯预授权）
- **产出**：
  - **P1-RC-023 未交货量回填（方案 A 零 ORM）**：`ErpSalReturnProcessor` 新增 protected step `updateUndeliveredQuantity(returnOrder, context)`——approve/reverseApprove 末尾按退货行 `deliveryLineId → ErpSalDeliveryLine.orderLineId` 定位订单行，聚合 APPROVED 出库行数量写 `ErpSalOrderLine.deliveredQuantity`（毛口径，对齐 ReturnQtyValidator 先例）；L1 公式 `未交货量 = quantity − deliveredQuantity + Σ退货量` 由读侧派生（退货量 Σ 从 APPROVED 退货行聚合，幂等重算）。`ErpSalReturnApproveProcessor.approve` + `ErpSalReturnReverseApproveProcessor.reverseApprove` 末尾接线（对称重算）
  - **P1-RC-024 暂估应收条件冲减**：`SalReturnPostingDispatcher.tryPost` 增运营代理门控——`delivery.posted=true`（SALES_OUTPUT 凭证存在）⇒ 已暂估 → 维持 SALES_RETURN 冲减路径（credit memo）；`posted=false` ⇒ 未暂估 → 跳过事件构造（零凭证/零 ArApItem，下游 SalAcctDocProvider/ErpFinArApItemGenerator 零变更）。`buildEvent` billData 增 `KEY_OFFSET_ESTIMATED_RECEIVABLE` 标记
  - **种子迁移（P0-1）**：既有 `TestErpSalReturnPosting/Approval/Refund/Inventory/Qty` 的 `newDeliveryApproved` seed `posted=false` → `posted=true`，保证门控语义下既有断言值不变（凭证 1401/6401=20 + ArApItem openAmount=-24 等保持）；快照经 force-save-output 重录
  - **测试 `TestErpSalReturnCompliance`**（新建，9 方法）：P1-RC-023 五路径（公式数值/无 deliveryLineId 跳过/多行聚合/幂等/对称回退）+ P1-RC-024 四路径（已暂估冲减/红字等价/未暂估跳过/posted=false 边界）；`_cases/input` 全表 header CSV 驱动建表 + force-save-output 快照录制
- **验证**：`mvn test -pl module-sales/erp-sal-service`（TestErpSalReturnPosting 2 + Approval 7 + Refund 2 + Inventory 3 + Qty 4 + Compliance 9 = 27 全绿 + 既有族零回归）BUILD SUCCESS

### RC-R1.14 + RC-R1.15 sales 促销定价族修复执行完成（MR1 第一批纯预授权，P1-RC-021 + P1-RC-022）

- **工作项**：RC-R1.14（sales 促销最低价校验，P1-RC-021）+ RC-R1.15（sales 促销价税分离，P1-RC-022，UC-SAL-08 reuse 合并）— 同域同组件同 owner doc 合并为一个 plan 两阶段（plan `docs/plans/2026-08-08-2219-1-rc-mr1-r1-14-15-sal-pricing-family.md`）
- **类型**：实现修复（纯 BizModel protected step + 跨域 IBiz 注入 + 测试；零 ORM/零会计过账核心路径/零新 config/零新 ErrorCode，ask-first checkbox 不需要——预授权判据 arm-index P1-RC-021/022 行 + roadmap 第一批纯预授权）
- **产出**：
  - **P1-RC-022 价税分离**：`ErpSalOrderBizModel.recomputeLineAmount` 按 L1 公式补 `line.setTaxAmount(net×rate/(1+rate))`（scale=4 HALF_UP，rate=taxRate.divide(100,6,HALF_UP)，零税率/null→taxAmount=0）+ `line.setAmountWithTax(net+taxAmount)`；`recomputeOrderTotals` 头级 totalTaxAmount/totalAmountWithTax 沿用新行值（既有循环已读 line.getTaxAmount()，零改动）
  - **P1-RC-021 最低价校验**：`ErpSalOrderBizModel` @Inject `IErpMdMaterialSkuBiz`（非 private，对齐既有 IErpMdPartnerBiz 范式）+ 新 protected step `validatePromotionPrices(order, lines, context)`（persistPricingResult 落地后逐行调 `mdMaterialSkuBiz.validatePrice(skuId, finalPrice, materialCategoryId, context)` 复用 master-data OFF/WARN/HARD 三级语义；finalPrice=line.amount/line.quantity 促销后净单价；materialCategoryId 经 line.getMaterial().getCategoryId() ORM 关系解析；WARN LOG.warn 放行 / HARD propagate ERR_PRICE_BELOW_MIN + @BizMutation 事务回滚）；**赠品行（amount==0）显式跳过**（L1 UC-SAL-08 要求赠品可成功生成）
  - 测试 `TestErpSalPricingCompliance`（新建，10 方法，JunitAutoTestCase + _cases/input header-only CSVs 驱动建表）：P1-RC-022 五路径（单档折扣 net=900 rate=13%→tax=103.5398 / 零税率→0 / 多档混合 13%+9%+6%+PERCENT_DISCOUNT+AMOUNT_OFF Σ恒等式 / A4.2.49 反转 5.7522≠11.5044 / 无折扣回归）+ P1-RC-021 五路径（OFF 放行 / WARN 放行 / HARD 拒绝 ERR_PRICE_BELOW_MIN+事务回滚行折扣不落库 / NoSku 跳过 / Gift 跳过 HARD 下含赠品促销成功生成）
  - `TestErpSalPricingEndToEnd` 7 场景快照重录（output/tables 行 taxAmount/amountWithTax 由 null→计算值）
- **验证**：`mvn test -pl module-sales/erp-sal-service -Dtest=TestErpSalPricingCompliance,TestErpSalPricingEndToEnd,TestErpSalPricingRuleEngine,TestErpSalPricingRuleCrud` **29 tests 全绿**（0 failures / 0 errors）+ `mvn clean install -DskipTests -pl module-sales/erp-sal-service -am` BUILD SUCCESS + git diff 仅 erp-sal-service Java + _cases 快照（零 ORM/会计过账核心路径变更）
- **文档回填**：`docs/design/sales/state-machine.md §9 场景D` 补价税分离+最低价校验实现注记；`docs/audits/arm-index.md` P1-RC-021→`done (RC-R1.14)` + P1-RC-022→`done (RC-R1.15)`；`docs/backlog/requirement-compliance-roadmap.md` RC-R1.14/RC-R1.15 `todo→done ✅`
- **下一步**：mission driver 继续 MR1 第一批后续 RC-R1.16+ 行
