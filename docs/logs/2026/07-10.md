# Development Log - 2026-07-10

### 2026-07-10（业财过账凭证行精确数值断言扩展——库存移动过账 + NCR SCRAP 过账 — plan 2026-07-10-1800-1）

- 执行 `docs/plans/2026-07-10-1800-1-inventory-move-ncr-scrap-voucher-line-e2e.md`，Phase 1~2 全部完成。解除 0704-1 两项 Deferred（库存移动过账凭证行断言 PURCHASE_INPUT/SALES_OUTPUT）+ 0335-2 一项 Deferred（NCR SCRAP 过账凭证行断言）。
- **Phase 1（种子科目补齐 + P2P/O2C 库存移动过账凭证行断言）**：种子 `erp_md_subject.csv` 补 6711 科目行（营业外支出-报废损失，EXPENSE/DEBIT）；`p2p-chain.spec.ts` 增 PURCHASE_INPUT 内联断言（Receive move posted=true → Dr 1401=50 / Cr 2202=50）；`o2c-chain.spec.ts` 增 SALES_OUTPUT 内联断言（Delivery move posted=true → Dr 6401=1200 / Cr 1401=1200）；清理覆盖核实（cleanupStockMove 内部已调 cleanupVoucherByBillCode，无需新增清理代码）。
- **Phase 2（NCR SCRAP 过账凭证行断言）**：新建 `quality-ncr-scrap-posting.action.spec.ts`——resolve AUTO_POST → NcrPostingDispatcher.dispatchScrap → NcrScrapAcctDocProvider → NCR_SCRAP 凭证行 Dr 6711=120 / Cr 1401=120 精确数值断言 + posted=true + reverseNcr 红冲清理。
- **avgCost 来源裁决**：NcrPostingDispatcher.resolveStockBalance 按 materialId(limit 1) 查 ErpInvStockBalance。MAT_1(materialId=1) 种子有唯一余额行（avgCost=120, MOVING_AVERAGE），故确定性返回 120。SCRAP posting 不扣减物理库存（NcrPostingDispatcher 注释），无需前置备货——备货会新增第二行致 limit 1 不确定。SCRAP_AMOUNT = qty(1) × 120 = 120。
- **acctSchemaId 配置 gap 修复**：NcrPostingDispatcher.resolveAcctSchemaId 读 config erp-qua.ncr-default-acct-schema（默认空→null），致凭证行 acctSchemaId 非空约束失败。playwright.config.ts webServer 命令增 `-Derp-qua.ncr-default-acct-schema=1`（种子 ACCT-FIN-01），解除阻塞。
- **预存启动阻断修复（非本计划引入）**：执行中发现并修复 5 处 view.xml / page.yaml 预存回归——ErpFinBudgetScenario.view.xml confirmText 属性违规、ErpHrLeaveRequest/ErpHrRecruitment.view.xml visibleOn 属性违规（9 处）、ErpInvLandedCost.view.xml reloadAfterSuccess 未定义子节点、pricing-rule/sales-price-list 页面 view.xml 引用路径断裂（4 文件）。
- **验证（full-green）**：`mvn clean install -DskipTests` BUILD SUCCESS（154 模块）；`npx playwright test --workers=1` 167 passed（166 基线 + 1 新 NCR SCRAP spec），0 回归。

---

### 2026-07-10（HR 休假/考勤/招聘/合同到期引擎全 6 Phase 实现 — plan 2026-07-10-1100-7）

- 执行 `docs/plans/2026-07-10-1100-7-hr-leave-attendance-recruitment-contract.md`，Phase 1~6 全部完成。承接 extended-roadmap M3 human-resource Non-Goal（UC-HR-02/05/06/07）。erp-survey 对标 Odoo/ERPNext/Axelor/AureusERP 4/4 核心内置。
- **Phase 1（ORM 模型变更）**：`module-hr/model/app-erp-hr.orm.xml` 新增 `ErpHrLeaveBalance`（休假额度，镜像 ERPNext Leave Allocation；usedDays 派生不落库，唯一键 employeeId+leaveType+fiscalYear+orgId）+ `ErpHrAttendance` 新增 `(employeeId, date)` 唯一约束（防止同日多条打卡）。`mvn clean install -DskipTests`（module-hr 链）触发增量代码生成 BUILD SUCCESS。
- **Phase 2（休假审批引擎）**：`IErpHrLeaveRequestBiz` 新增 submit/approve/reject/cancel/getBalance 5 方法；`ErpHrLeaveRequestBizModel` 实现审批状态机 DRAFT→SUBMITTED→APPROVED/CANCELLED + durationDays 自动计算（ChronoUnit.DAYS.between+1）+ 余额校验（entitled+carried−Σapproved）+ 日期重叠拦截（同员工同 leaveType APPROVED/SUBMITTED 区间）+ **激活 `shiftBiz.onLeaveApproved/onLeaveCancelled`** 排班联动（plan 0831-3 悬空钩子→触发源）；新增 `ErpHrLeaveBalanceBizModel` CRUD；3 新 ErrorCode。TestErpHrLeaveEngine 7 cases 全绿。
- **Phase 3（考勤打卡 + 无薪假扣减）**：`IErpHrAttendanceBiz` 新增 clockIn/clockOut/getTodayAttendance；`ErpHrAttendanceBizModel` 实现打卡端点（clockIn 幂等 + 重复签到拦截 + clockOut workHours=Duration.between 计算）；`PayrollCalculator.summarizeAttendance` 新增 `unpaidLeaveDays = Σ SICK/PERSONAL APPROVED` + basicSalary 按比例扣减（config-gated `erp-hr.deduct-unpaid-leave` 默认 false 向后兼容）。2 新 ErrorCode。TestErpHrAttendanceEngine 4 cases 全绿。
- **Phase 4（招聘状态机 + 入职联动）**：`IErpHrRecruitmentBiz` 新增 moveToScreening/scheduleInterview/makeOffer/hire/reject/close 6 方法；`ErpHrRecruitmentBizModel` 实现 OPEN→SCREENING→INTERVIEW→OFFERED→HIRED 状态机 + **hire 联动自动创建 `ErpHrEmployee` + ACTIVE 合同 + employeeId 回写**（经 IErpHrEmployeeBiz.save + IErpHrEmploymentContractBiz.save）。2 新 ErrorCode。TestErpHrRecruitmentEngine 4 cases 全绿。
- **Phase 5（合同到期扫描 Job）**：`IErpHrEmploymentContractBiz` 新增 scanExpiringContracts/expireOverdueContracts/renew；`ErpHrContractExpiryJob`（镜像 ErpCsEntitlementExpiryJob 范式：cron 空值跳过 + 预警通知 hr.contract-expiry-warning + 过期推进 ACTIVE→EXPIRED + 单条失败隔离）；`scheduler.yaml` 注册 `erp-hr-contract-expiry`（每日 01:00）；beans 注册于 `app-service.beans.xml`。1 新 ErrorCode。TestErpHrContractExpiry 7 cases 全绿。
- **Phase 6（前端页面增强）**：4 view.xml 增强——LeaveRequest 审批按钮（submit/approve/reject/cancel visibleOn 状态条件）+ Attendance 打卡按钮（clockIn/clockOut listActions）+ Recruitment 状态推进按钮（moveToScreening/ajax + scheduleInterview/makeOffer/hire drawer 表单 + reject）+ LeaveBalance CRUD 页面（codegen 已生成）。web 模块 BUILD SUCCESS。
- **验证（full-green）**：全 reactor `mvn clean install -DskipTests` BUILD SUCCESS（154 模块）；`mvn test -pl module-hr/erp-hr-service` 111 tests 全绿（新增 4 测试类 22 cases，回归全绿）。
- **文档**：`extended-roadmap.md` UC-HR-02/05/06/07 ❌→✅ done。Deferred：招聘多实体拆分（Candidate/Interview/Scorecard/Offer/Onboarding）successor + 移动端打卡/生物识别设备集成 successor + 休假日历可视化（无 successor）。

---

### 2026-07-10（维修备件消耗 GL 过账全 2 Phase 实现 — plan 2026-07-10-1100-6）

- 执行 `docs/plans/2026-07-10-1100-6-maintenance-spare-part-posting.md`，Phase 1~2 全部完成。承接 roadmap S-6（state-machine.md §实现偏离补注 Non-Goal successor 落地 + 部分解除 1018-3 Deferred）。erp-survey 对标 ERPNext/Metasfresh 维修费用化过账标配。镜像 plan 1100-5 制造领料过账范式（域侧独立 dispatcher）。
- **Phase 1（业务类型 + Provider + Dispatcher + 集成）**：`ErpFinBusinessType` 新增 `MAINTENANCE_ISSUE(492)`（492 空闲于 490/500 之间）+ business-type 字典补项；`MaintenanceIssuePostingDispatcher`（maintenance 域独立，config-gated `erp-mnt.spare-part-posting-enabled` 默认关；备件消耗单→加载出库移动单→读流水成本→装配 PostingEvent(businessType=MAINTENANCE_ISSUE, billHeadCode=usage.code+"-MI")→过账；billHeadCode 幂等判重防双重扣减）+ `MaintenanceIssueAcctDocProvider`（Dr 维修费用 6602 config / Cr 存货 1403，多物料贷方分列）+ `MntPostingExecutor`（跨域经 IErpFinVoucherBiz Facade）；`ErpMntSparePartUsageBizModel.confirm` 末尾在 `isStockIssued(move)` 后触发 `dispatchIfApplicable`；`ErpMntConstants` 补 3 配置键 + `ErpMntErrors` 补 `ERR_SPARE_PART_POSTING_FAILED`；mnt-service pom 新增 finance-service compile 依赖（maintenance→finance 单向无环）；beans 注册于 `app-service.beans.xml`。
- **关键决策（draft review 未决项裁决）**：复用 `posted` 字段（语义「库存已出库」，不改为 GL 标记——避免 ORM 模型变更触及保护区）；GL 幂等改用 billHeadCode+businessType 查 `ErpFinVoucherBillR` 判重（备件消耗单 posted 确认即 true，不可作 GL 标记，异于 manufacturing MaterialIssue posted=GL-only）；currencyId 兜底从账套 functionalCurrencyId 解析（ErpMntSparePartUsage 无 currencyId 列，出库流水亦可能未携带币种）。
- **Phase 2（设计文档对齐）**：`state-machine.md` §实现偏离补注 维修费用过账行标注 ✅已实现（备件消耗闭合，工时费用化留 successor）；`assets/maintenance.md` §五边界表 + 段落标注 MAINTENANCE_ISSUE(492) 已接线闭合（两域防双重：MNT_ISSUE 贷存货 / MAINTENANCE_EXPENSE linkedVisit 贷中转清算 2502）；`1018-3` Deferred 补部分解除注记。
- **验证（full-green）**：全 reactor `mvn clean install -DskipTests` BUILD SUCCESS（154 模块）；`mvn test -pl module-maintenance/erp-mnt-service` 43 tests 全绿（新增 TestErpMntSparePartPosting 4 场景：基本过账 + 多物料 + config 关闭向后兼容 + 幂等防双重扣减；回归 TestErpMntSparePartAndSchedule 等全绿）。
- **文档**：`core-business-roadmap.md` S-6 ✅ done；`backlog/README.md` P7 ✅ done。Deferred：维修工时费用化过账 successor（Dr 维修费用 / Cr 应付职工薪酬）+ 维修工单成本多维分摊 successor。

---

### 2026-07-10（制造完工入库与领料 GL 过账全 4 Phase 实现 — plan 2026-07-10-1100-5）

- 执行 `docs/plans/2026-07-10-1100-5-manufacturing-receipt-posting.md`，Phase 1~4 全部完成。承接 roadmap S-5（state-machine.md §实现偏离补注 Non-Goal successor 落地 + 解除 0704-2/2237-1 Deferred）。erp-survey 对标 Odoo mrp.account / ERPNext Stock Entry GL / Metasfresh 标配。
- **Phase 1（业务类型枚举 + InvPostingDispatcher 修正，前序 run 已完成）**：`ErpFinBusinessType` 新增 `MANUFACTURING_RECEIPT(500)`/`MANUFACTURING_ISSUE(501)`（490 已被 LANDED_COST 占用）；`InvPostingDispatcher.resolveBusinessType` 新增 MANUFACTURE→MANUFACTURING_RECEIPT 分支 + ERP_MFG_ISSUE 跳过（修复领料误派 SALES_OUTPUT）；`ErpInvConstants` 新增 `RELATED_BILL_TYPE_MFG_ISSUE` 本地副本（避免 inventory→manufacturing 上行依赖）。
- **Phase 2（完工入库 AcctDocProvider，前序 run 已完成）**：`InvAcctDocProvider` 扩展 MANUFACTURING_RECEIPT（Dr 产成品存货 1401 / Cr WIP 1411，config `erp-mfg.wip-subject-code` 默认 1411）；`TestErpMfgCompletionPosting` 3 计价方法（MOVING_AVERAGE/STANDARD/FIFO）全绿。
- **Phase 3（领料出库 PostingDispatcher + AcctDocProvider，本 run 完成）**：`ManufacturingIssuePostingDispatcher`（镜像 ProductionVarianceDispatcher 范式：领料单→加载出库移动单→读流水成本→装配 PostingEvent(businessType=MANUFACTURING_ISSUE, billHeadCode=issue.code+"-MI")→过账→成功回写 issue.posted=true）+ `ManufacturingIssueAcctDocProvider`（Dr WIP 1411 / Cr Inventory 1401，多物料贷方分列）+ `ErpMfgMaterialIssueBizModel.confirm` 末尾触发 `dispatchIfApplicable`。beans 注册于 `app-service.beans.xml`。`TestErpMfgIssuePosting` 2 场景全绿（单物料 materialCost=10 + 多物料 2 行贷方合计 18）。
- **Phase 4（E2E 断言更新 + 全链路验证，本 run 完成）**：`erp_md_subject.csv` 补种子科目 `1411`（WIP 在制品，同 1249-1 COA 完备范式）；`mfg-chain.spec.ts` posted=false→true（completionMove + issue）+ 凭证行数值断言（MANUFACTURING_ISSUE Dr 1411/Cr 1401=1000 + MANUFACTURING_RECEIPT Dr 1401/Cr 1411=1100）；`_helper.ts` cleanupMfg 增 issue 凭证清理（billCode=issue.code+"-MI"）；`TestErpMfgCostFlowEndToEnd` 全链路成本流转闭环（领料 Dr WIP/Cr Inv → 完工 Dr Inv/Cr WIP，WIP 净余额=0，无 SALES_OUTPUT 误派）全绿。
- **验证（full-green）**：全 reactor `mvn clean install -DskipTests` BUILD SUCCESS（154 模块）；`mvn test -pl module-manufacturing/erp-mfg-service,module-inventory/erp-inv-service` 102 tests 全绿（新增 TestErpMfgIssuePosting 2 + TestErpMfgCostFlowEndToEnd 1，回归 CompletionPosting/MaterialIssue/WorkOrder/BOM 全绿）。
- **文档**：`state-machine.md` §实现偏离补注 完工成本结转凭证 Non-Goal→已实现；`0704-2`/`2237-1` Deferred 解除；`core-business-roadmap.md` S-5 ✅ done；`backlog/README.md` P7 ✅ done。
- **关键决策**：领料过账由 manufacturing 域独占（WIP 借方需 WorkOrder 上下文，InvPostingDispatcher 跳过 ERP_MFG_ISSUE）；完工入库过账由 inventory 域 InvAcctDocProvider 处理（存货估值属 inventory 职责）；种子补 1411 是 E2E posted=true 的前置（同 1249-1 COA 完备范式）。Deferred：直接人工/制造费用 GL 过账 successor + 副产品/联产品成本分配 successor。

---

### 2026-07-10（预算管理全 4 Phase 实现 — plan 2026-07-10-1100-4）

- 执行 `docs/plans/2026-07-10-1100-4-budget-management.md`，Phase 1~4 全部完成。承接 roadmap S-4（budget.md 107 行设计落地 + 解除 0700-2 Follow-up）。erp-survey 对标 iDempiere GL Budget（PostingType=BUDGET 影子凭证范式）。
- **Phase 1（ORM 模型）**：`module-finance/model/app-erp-finance.orm.xml` 新增 3 实体 `ErpFinBudgetScenario`（预算方案头 + 状态机 + 控制级别）/`ErpFinBudgetLine`（明细行，科目×期间×维度，派生额不落库）/`ErpFinBudgetControlLog`（控制日志）+ 4 新字典（budget-scenario-type/budget-control-level/budget-status/budget-action）+ postingType 字典新增 BUDGET/COMMITMENT。代码生成全链 BUILD SUCCESS。**不动 ErpFinGlBalance 结构**。
- **Phase 2（编制 BizModel + 审批过账引擎 + BUDGET 隔离修复）**：**关键前置 Fix**——5 处实际数聚合（ProfitLossClosingService/BadDebtProvisionService/AnnualCloseService/ErpFinAccountingPeriodProcessor 试算平衡/ExchangeRevaluationService 汇兑）增加 `or(isNull(postingType), ne(postingType, BUDGET))` 过滤，确保 BUDGET 影子凭证不污染实际财务；`BudgetVoucherGenerator`（按 periodId 分组生成 postingType=BUDGET 影子凭证，借贷按 subject.direction，携带 costCenterId 维度）+ `ErpFinBudgetScenarioProcessor`（DRAFT→SUBMITTED→APPROVED→CANCELLED 状态机 + 凭证生成/红冲，protected step 可覆盖）；`IErpFinBudgetControlBiz` SPI（finance-dao）+ `ErpFinBudgetControlBiz`（HARD/WARN/NONE 三级，余量=ΣBUDGET−ΣNORMAL 均从 VoucherLine 聚合）；`ErpFinErrors` 增 ERR_BUDGET_EXCEEDED/状态机错误码。测试 `TestErpFinBudgetIsolation`（1）+ `TestErpFinBudgetEndToEnd`（6 scenarios：编制审批/红冲/HARD/WARN/NONE/对比）全绿。
- **Phase 3（预算控制跨域集成）**：`ErpPurOrderProcessor`/`ErpPurPaymentProcessor`（采购域）/`ErpFinExpenseClaimProcessor` 各增 `runBudgetCheckHook`（经 `erp-fin.budget-check-enabled` 门控默认 false 向后兼容，按配置科目 + 业务日期解析期间校验）；config 常量 `CONFIG_BUDGET_PURCHASE_EXPENSE_SUBJECT_CODE`/`CONFIG_BUDGET_EXPENSE_SUBJECT_CODE`（采购/报销无科目维度，按配置默认科目）。测试 `TestErpPurBudgetControlIntegration`（3 scenarios：HARD 拦截/WARN 放行写日志/无预算匹配放行）全绿。
- **Phase 4（对比报表 + 前端）**：`ErpFinBudgetLineBizModel.getBudgetVsActual`（@BizQuery，按 subjectId×periodId×costCenterId 聚合 VoucherLine 关联凭证 postingType 得 Budget/Actual/Available 三列）；`budget-vs-actual.xpt.xml` 种子报表 + `ErpFinReportBizModel.buildBudgetVsActualDataset`；前端 `ErpFinBudgetScenario.view.xml`（bounded-merge 列 + submit/approve/reject/cancel 审批 row actions）+ `ErpFinBudgetControlLog.view.xml` + 菜单 URL 重指向实体页（`erp-fin.action-auth.xml`）。
- **验证（full-green）**：全 reactor `mvn clean install -DskipTests` BUILD SUCCESS（154 模块）；`mvn test -pl module-finance/erp-fin-service` 183 tests 全绿（新增 TestErpFinBudgetIsolation 1 + TestErpFinBudgetEndToEnd 6，回归 P&L/坏账/年报/试算平衡/报表全绿）；`mvn test -pl module-purchase/erp-pur-service` 112 tests 全绿（新增 3，回归采购审批/过账全绿）。
- **文档**：`docs/design/finance/budget.md` 标注 ✅已实现 + Deferred 边界；`core-business-roadmap.md` S-4 ✅ done；`backlog/README.md` P7 ✅ done。
- **关键决策**：预算余额统一从 VoucherLine 聚合（不动 GlBalance，过账引擎本就不维护）；BUDGET 凭证按 periodId 分组（VoucherLine 无独立 periodId，继承凭证头，期间控制需 per-period 凭证）；采购/付款无科目维度，预算控制按配置默认科目（design gap 记录：采购单无 subjectId/costCenterId 列）。Deferred：承付款 COMMITMENT/滚动预算自动复制/调整版本链管理。

---

### 2026-07-10（到岸成本分摊全 4 Phase 实现 — plan 2026-07-10-1100-3）

- 执行 `docs/plans/2026-07-10-1100-3-landed-cost-allocation.md`，Phase 1~4 全部完成。承接 roadmap S-3（costing-methods.md §到岸成本 设计落地 + 解除 5 个 plan Deferred + logistics path-2 阻塞）。erp-survey 对标 ERPNext Landed Cost Voucher + Metasfresh 标配。
- **Phase 1（ORM 模型）**：新增 `ErpInvLandedCost`（头：code/receiveId/supplierId/allocationMethod/docStatus/approveStatus/posted/businessDate 等）+ `ErpInvLandedCostLine`（费用要素行：costElement/amount/apPartnerId）；2 新字典 `erp-inv/landed-cost-alloc-method`（BY_AMOUNT/BY_QUANTITY/BY_WEIGHT）+ `erp-inv/cost-element`（FREIGHT/INSURANCE/DUTY/CUSTOMS_CLEARANCE/OTHER）；`adjustType` 列精度 20→30 容纳 `LANDED_COST_SUPPLEMENT`(22 字符)。代码生成全链 BUILD SUCCESS。
- **Phase 2（分摊引擎 + BizModel + Processor）**：`LandedCostAllocationEngine`（纯函数式，3 种分摊方法，末行吸收舍入差）；`ErpInvLandedCostProcessor`（审核编排 6 步：加载→校验→分摊→创建 CostAdjust(LANDED_COST_SUPPLEMENT)→直接调 CostAdjustmentService.applyCostAdjust 更新成本层→LANDED_COST 过账）；`IErpInvLandedCostBiz` + `ErpInvLandedCostBizModel`（approve + allocate 预览）；inventory-service pom 增 purchase-dao 编译依赖（跨域只读 DAO 访问入库单头/行）。单元测试 5 场景全绿。
- **Phase 3（GL 过账 + 集成测试）**：`ErpFinBusinessType` 增 `LANDED_COST(490)`（码值 420-480 已占用，490 空闲）+ business-type 字典；`LandedCostAcctDocProvider`（借存货 1401 / 贷应付 2202，每入库行借方+每费用要素贷方）+ `LandedCostPostingDispatcher`（组装 PostingEvent → voucherBiz.post）；集成测试 `TestErpInvLandedCostEndToEnd` 4 场景全绿（BY_AMOUNT 2行分摊+凭证/BY_QUANTITY/多应付对象2条贷方行/防重复分摊拦截）。
- **Phase 4（前端页面）**：`ErpInvLandedCost.view.xml` 增强——列表精简列 + 编辑表单关键字段 + 审核 row action（调用 `ErpInvLandedCost__approve`）+ 分摊预览按钮（调用 `allocate` query）；action-auth 菜单已由代码生成注册。
- **验证（full-green verification）**：全 reactor `mvn clean install -DskipTests` BUILD SUCCESS（154 模块，1m31s）；全 reactor `mvn test` BUILD SUCCESS（7m21s，0 失败 0 错误）；inventory-service 新增 `TestErpInvLandedCostAllocationEngine`（5）+ `TestErpInvLandedCostEndToEnd`（4）共 9 tests 全绿（含 BY_AMOUNT/BY_QUANTITY/多应付对象/防重复分摊场景）。
- 文档：`docs/design/finance/costing-methods.md` §Non-Goal 到岸成本标注已收口 + 新增 plan 1100-3 实现注记段；`core-business-roadmap.md` S-3 ✅ done；`backlog/README.md` P7 ✅ done。

---

### 2026-07-10（信用冻结：出库/发票审核环节信用控制 — plan 2026-07-10-1100-2）

- 执行 `docs/plans/2026-07-10-1100-2-sales-credit-hold.md`，Phase 1~3 全部完成。承接 roadmap S-2（UC-SAL-11 Non-Goal successor）。erp-survey 对标 ERPNext Credit Limit 双点校验。**纯应用层增强，零 ORM 模型/契约变更**。
- **Phase 1（CreditLimitChecker 扩展 + 配置 + 错误码）**：`CreditLimitChecker` 新增 `checkCreditHold(customerId, billCode, billType, context)`（point-in-time，检查客户当前是否已超额 `available<0`，本单不叠加 outstanding——订单审核时额度已占用）；提取共享 `enforceOverLimit`（订单审核与信用冻结复用三级策略）+ `buildHardBlockException`（按 billType 分派错误码）；`ErpSalConstants` 增 `CONFIG_CREDIT_CHECK_ON_DELIVERY`/`CONFIG_CREDIT_CHECK_ON_INVOICE`（默认 false 向后兼容）+ `BILL_TYPE_ORDER/DELIVERY/INVOICE`；`ErpSalErrors` 增 `ERR_CREDIT_HOLD_DELIVERY`/`ERR_CREDIT_HOLD_INVOICE`（绑定 ARG_DELIVERY_CODE/ARG_INVOICE_CODE，与订单 `ERR_CREDIT_LIMIT_EXCEEDED` 区分）。
- **Phase 2（出库/发票 Processor 集成）**：`ErpSalDeliveryProcessor.validateBusinessRulesForApprove` 在 `requireCustomerActive` 之后、`enforceInspectionGate` 之前插入 `enforceCreditHold`（protected，config-gated）；`ErpSalInvoiceProcessor.validateBusinessRulesForApprove` 在 `requireCustomerActive` 之后插入 `enforceCreditHold`。两 Processor 新增 `@Inject CreditLimitChecker`（与 OrderProcessor 一致，IoC 按类型自动装配）。
- **Phase 3（GraphQL 集成测试）**：`TestErpSalCreditHoldOnDelivery` 6 场景（HARD_BLOCK 拦截/SOFT_WARNING 放行/SPECIAL_APPROVAL 持权限放行/config 关闭向后兼容/信用正常/多币种折算超额拦截）+ `TestErpSalCreditHoldOnInvoice` 3 场景（HARD_BLOCK 拦截/SOFT_WARNING 放行/config 关闭）。拦截场景断言 `ERR_CREDIT_HOLD_*` + 状态保持 SUBMITTED；放行场景走完 doApprove（出库经预置库存 + SALES_OUTPUT 科目 1401/6401）。
- **验证**：`mvn clean install -DskipTests` 全 reactor BUILD SUCCESS；`mvn test -pl module-sales/erp-sal-service` 119 tests 全绿（新增 9 + 回归 TestErpSalOrderApproval 14 + Delivery/Invoice/Quotation/CreditNotify 无失败）。现有订单审核 `check()` 路径行为不变（重构提取共享方法后 ORDER 仍抛 `ERR_CREDIT_LIMIT_EXCEEDED`）。
- **结束审计**：独立子代理 fresh-session closure audit `PASS`（6 审计域全 live-verified，无缺陷）。
- 文档：`docs/design/sales/README.md` §信用额度控制 Non-Goals 信用冻结行标注 ✅已实现 + 配置项补 2 键；`core-business-roadmap.md` S-2 ✅ done + Non-Goal 边界；`backlog/README.md` P6 ✅ done。

---

### 2026-07-10（销售定价引擎全 4 Phase 实现 — plan 2026-07-10-1100-1）

- 执行 `docs/plans/2026-07-10-1100-1-sales-pricing-engine.md`，Phase 1~4 全部完成。承接 roadmap S-1（UC-SAL-11 目标态）。**新增 ORM 实体 + 跨域 SPI + 引擎 + 测试 + 前端页面**。
- **Phase 1（ORM 模型变更）**：`ErpMdPartner` 新增 `customerGroup` VARCHAR(100)（propId 100，客户组维度，镜像 CRM `customerCategory` 范式）；新增 `ErpSalPriceList`（头/行：客户组/物料/SKU/单位/阶梯价/期间/优先级）+ `ErpSalPricingRule`（4 种 ruleType + 2 种 targetType + stackable + 期间 + 优先级）+ 新字典 `erp-sal/price-list-status`/`pricing-rule-type`/`pricing-target`；`ErpSalOrderLine`/`ErpSalQuotationLine` 各增 `discountRate`/`discountAmount`/`pricingSource`（propId 100-102）。
- **Phase 2（定价解析引擎 + 促销规则引擎）**：新增 `IErpMdCustomerPriceResolver` SPI（master-data-dao 端口）+ `ResolvedPrice` DTO；`ErpMdMaterialSkuBizModel.resolvePrice` 增客户价格清单层（优先级：手工 > 客户价格清单 > 供应商价格表 > SKU 默认档）+ 新增 `resolvePriceWithSource`；`ErpSalCustomerPriceResolver`（SPI 实现，QueryBean 查客户组/物料/阶梯/币种/期间匹配）；`ErpSalPricingRuleEngine`（纯函数式引擎，4 种 ruleType + stackable + 优先级 + 期间）；`ErpSalOrderBizModel.applyPricingRules`（@BizMutation，引擎评估→行折扣/赠品行写回→头合计重算）；`ErpSalErrors` 新增 `ERR_PRICING_RULE_CONFLICT`/`ERR_PRICE_LIST_EXPIRED`；`ErpSalConstants` 新增定价配置项 + PRICING_SOURCE 常量。单元测试 `TestErpSalPricingRuleEngine` 10 tests 全绿。
- **Phase 3（订单定价集成 + GraphQL API 测试）**：`ErpSalOrderProcessor.approve` 增强 pricingSource 分布审计日志；`ErpSalOrderLineBizModel` `defaultPrepareSave`/`defaultPrepareUpdate` 自动填充 pricingSource=MANUAL；配置项经 `AppConfig.var` 可运行时覆盖。GraphQL 集成测试 `TestErpSalPricingEndToEnd` 7 场景全绿（价格清单取价/百分比折扣/满减/买赠/叠加/优先级/价格覆盖）+ CRUD 冒烟 `TestErpSalPriceListCrud` 2 + `TestErpSalPricingRuleCrud` 2。快照修复：`TestErpSalQuotationCrudSmoke.testLineRelation` 因新增行字段更新 `_cases/` 快照（response.json5 + CSV 列追加）。
- **Phase 4（前端页面）**：`sales-price-list/main.page.yaml` + `pricing-rule/main.page.yaml` 占位→GenPage 替换；新增 `sales-price-list/edit.page.yaml` + `pricing-rule/edit.page.yaml`；`ErpSalOrderLine` view codegen 自动含 discountRate/discountAmount/pricingSource 列。
- **验证**：`mvn clean install -DskipTests` 全 reactor 154 模块 BUILD SUCCESS；`mvn test` 全 workspace 0 failures/0 errors；sales-service 110 tests 0 failures/0 errors（含新增 19 tests）。
- 文档：`docs/design/sales/use-cases.md` UC-SAL-11 标注 ✅ 已实现；`docs/design/sales/README.md` §7 赠品与折扣增定价引擎描述；`core-business-roadmap.md` S-1 ✅ done + Non-Goal 边界修正。

---

### 2026-07-10（后继业务功能 4 计划草案审查收敛 → active — mission-driver draft-from-roadmap）

- mission-driver 草案流程：roadmap 全 M1/M2/M3/M4/M5 + 历史批次 done，剩余 4 个 `ready` 后继工作项 S-1~S-4（销售定价/信用冻结/到岸成本/预算管理）已有完整草案（`draft` 状态，审查 pending）。本会话执行独立草案审查直至收敛，4 计划状态 draft→active。**零生产代码/契约/ORM 模型变更**（纯计划文档 + 草案审查）。
- **独立草案审查（4 并行 fresh-session 子代理）**：
  - `2026-07-10-1100-2`（信用冻结）：iter1 `accept`（baseline 8 项全核实：CreditLimitChecker 三级+AR :121-139/:196-234，唯一调用点 OrderProcessor:158，Delivery/Invoice Processor 确认无信用校验，config/错误码齐备）。
  - `2026-07-10-1100-1`（销售定价）：iter1 `needs revision`（客户组维度 Decision 缺失，字段内嵌"→ErpMdPartner 或新客户组"歧义）→ 修订（新增 Decision 选方案 B `ErpMdPartner.customerGroup` VARCHAR 镜像 CRM `customerCategory` 范式 + ask-first 标注 + Targets 增 master-data + 字段消歧 + 契约漂移 follow-up 重分类）→ iter2 `accept`。
  - `2026-07-10-1100-3`（到岸成本）：iter1 `needs revision`（businessType 码值 430 与 `PROJECT_SETTLEMENT(430)` 硬冲突；内部 `ErpInvCostAdjust` 载体经 Processor 会 420+LANDED_COST 双重过账，方法名 `execute` 不存在）→ 修订（码值 430→**490**；`execute`→`applyCostAdjust` :64 + 显式禁走 `ErpInvCostAdjustProcessor.applyCostAdjust` :120/:137 防双重过账；Related 补齐 5 Deferred）→ iter2 `accept`。
  - `2026-07-10-1100-4`（预算管理）：iter1 `needs revision`（6 blocking：isBudgetable 已存在 :830 误判；过账引擎不维护 GlBalance 架构前提错误；损益结转/坏账 :167-173/:176-182 无 postingType 过滤致 BUDGET 凭证污染实际财务；ORM ask-first 未标；字典漂移；GlBalance 无 costCenterId）→ **重大重规**（移除 GlBalance 结构变更全程改 VoucherLine 聚合消除 B2/B5/B6；新增关键前置 Fix 实际数聚合隔离 BUDGET 凭证 + 隔离回归测试解除 B3；Task Route 标 ask-first；postingType 仅扩凭证字典）→ iter2 `accept`。
- **关键发现（草案审查价值）**：预算计划原本会 (a) 在不维护 GlBalance 的引擎上假设 GlBalance 聚合、(b) 让 BUDGET 凭证污染实际损益结转/坏账——均为实施期才会暴露的 P0 财务正确性缺陷，经独立草案审查在编码前拦截。到岸成本 businessType 码值冲突为运行期 `ErpFinAcctDocRegistry` EnumMap 解析失败级缺陷，同样编码前拦截。
- 文档：4 计划 `> Plan Status: active` + Draft Review Record 收敛证据；`docs/backlog/README.md` 4 行 `ready`→`in-progress`（1100-4 描述同步去 GlBalance）；`core-business-roadmap.md` S-1~S-4 + Non-Goal 边界 + costing 注记 `ready`→`in-progress`。

---

### 2026-07-10（制造工单完整链路编排浏览器层 E2E — plan 2026-07-10-0704-2）

- 执行 `docs/plans/2026-07-10-0704-2-manufacturing-chain-orchestration-e2e.md`，Phase 1（WorkOrder 完整制造链编排 E2E）+ Phase 2（文档对齐）全部完成。承接 0335-1 Deferred「WorkOrder 完整制造链编排 E2E」+ 0814-2 Deferred「全 18 域全业务动作覆盖」manufacturing 子集。纯消费侧测试新增，**零生产代码/契约/ORM 模型变更**。
- **1 新 spec（1 test）+ `orchestration/_helper.ts` 新增 `runMfgChain`/`cleanupMfg`/`MFG_EXPECT` 编排原语**：经 GraphQL 驱动 WorkOrder+MaterialIssue+JobCard 三聚合根协作全链——前置备货→BOM+行→WorkOrder+OUTPUT/INPUT 行→审批轴（submit→approve）→齐套校验（checkAvailability→STOCK_RESERVED）→开工（start→IN_PROCESS）→领料出库（MaterialIssue.confirm→OUTGOING 移动+materialCost 回写）→报工（JobCard.recordWork→laborCost 回写→submitJob→completeJob）→完工入库（reportCompletion→MANUFACTURE 入库移动+totalCost/unitCost 重算+COMPLETED）。每步状态翻转在 helper 内 `verifyState` `__get` 独立断言。
- **跨聚合根协作产物断言**：(1) MaterialIssue.confirm 触发 ErpInvStockMove(ERP_MFG_ISSUE) OUTGOING + WorkOrder.materialCost=1000 回写；(2) JobCard.recordWork 回写 WorkOrder.laborCost=100；(3) reportCompletion 触发 ErpInvStockMove(ERP_MFG_WORK_ORDER) MANUFACTURE（DONE）+ WorkOrder COMPLETED + completedQuantity=10 + totalCost=1100 + unitCost=110 + posted=false。
- **执行期裁决（偏离草案之处）**：
  - **组件物料改用测试专用新建物料**（非种子 MAT-003）：MAT-003 在 WH-RAW 有种子余额（100@8.5，WEIGHTED_AVERAGE），内联备货混合致 materialCost 非确定性 + `cleanupStockMove` 整行删余额抹除种子 850 → 全套件首轮实测 inventory dashboard totalValue 10450→9600 回归。改新建测试专用物料（MOVING_AVERAGE，无种子余额）→ materialCost 确定性 1000 + 清理整行删余额安全（不污染种子库存基线）。
  - **`@RequestBean` 入参序列化**：`ErpMfgJobCard__recordWork` 的 `@RequestBean JobCardWorkRecord` 经 Nop GraphQL **展平为独立标量参数**（非 `record` 包装），经后端 `TestErpMfgWorkOrderEndToEnd#recordWorkRequest:199` 范式核实——直接 `callMutationOk` 内联标量（jobCardId/operatorId/durationMins/...），区别于 `generateMove` 的 `request: input(StockMoveRequest)` 包装。
  - **posted=false Non-Goal 确认**：completionMove.posted=false + WorkOrder.posted=false 经实测确认（MANUFACTURE 移动经 InvPostingDispatcher 优雅降级，MANUFACTURING_RECEIPT GL 过账依赖 finance 域制造过账 Provider，待 successor）。

#### 验证状态（full-green）

- `npx playwright test tests/e2e/orchestration/mfg-chain.spec.ts --workers=1`：1 passed（7.5s）。
- `npx playwright test tests/e2e/orchestration/ --workers=1`：5 passed（制造链 + P2P/O2C/反向 0 回归）。
- `npx playwright test --workers=1`：166 passed / 0 failed（21.7m，全套件 0 回归，inventory dashboard totalValue 10450 基线保持）。
- `mvn install -DskipTests`：154 模块 BUILD SUCCESS（纯 TS 测试变更，Java 不受影响）。
- 文档对齐：`docs/testing/e2e-runbook.md` 跨域编排层新增「制造链编排层」子段 + 表格增制造链行 + 套件计数校准（实测基线 165 + 1 = 166，runbook 预存 167 偏差 2 经实测校准）；`docs/backlog/README.md` 新增 0704-2 路标条目。

---

### 2026-07-10（业财过账凭证行精确数值浏览器层 E2E 断言 — plan 2026-07-10-0704-1）

- 执行 `docs/plans/2026-07-10-0704-1-voucher-line-numeric-assertion-e2e.md`，4 Phase 全部完成。承接 0814-2/1249-1/0335-1/2004-2 四条 Deferred「业财过账凭证 subjectCode + debitAmount/creditAmount 精确数值断言」。纯消费侧测试新增，**零生产代码/契约/ORM 模型变更**。
- **新增两原语（`orchestration/_helper.ts`）**：`findVoucherIdByBillCode(page, billCode, postingType?)`（经 ErpFinVoucherBillR 反查 voucherId，按 NORMAL/REVERSAL 区分原/红字凭证——红字凭证与原凭证共用同一 billHeadCode）+ `assertVoucherLines(page, voucherId, expectedLines[])`（按 voucherId 查 ErpFinVoucherLine，逐行断言 subjectCode + dcDirection + debit/credit 金额，BigDecimal 经 Number() 精确匹配）。
- **4 链路 spec 内联断言（无新 spec 文件/测试用例，套件数 167 不变）**：
  - p2p-chain AP_INVOICE（1403 Dr 50 / 2221 Dr 6.5 / 2202 Cr 56.5）+ o2c-chain AR_INVOICE（1131 Dr 113 / 6001 Cr 100 / 2221 Cr 13）。
  - p2p/o2c-reverse 红字同向取负（`buildReversalDraft:575` dcDirection 不变、金额取负：1403 Dr -50 / 2202 Cr -56.5 等）+ 原正常凭证行金额不变（正数，验证红冲仅新增红字凭证不修改原凭证行）。
  - pur-return PURCHASE_RETURN（2202 Dr 25 / 1401 Cr 25）+ sal-return SALES_RETURN（1401 Dr 50 / 6401 Cr 50）。
- **实现关键裁决**（plan 假设 vs 实际 Provider 行为差异，均已对齐）：
  - **红字凭证同向取负（非借贷互换）**：plan 草案审查 B1 修正——`ErpFinPostingProcessor.buildReversalDraft` 保持 dcDirection 不变、仅金额取负（原 2202 CREDIT 56.5 → 红字 2202 CREDIT -56.5，非翻转为 DEBIT）。`persistVoucher:646-648` 将取负值路由至正确借/贷列。断言据此写同向取负期望。
  - **PURCHASE_RETURN 读头 totalAmount（非行级聚合）**：`PurReturnPostingDispatcher:92` 置 `billData.TOTAL_AMOUNT = returnOrder.getTotalAmount()`（头字段，列 defaultValue=0 无自动 rollup）。故 pur-return spec 创建退货头时显式置 `totalAmount=qty5×price5=25`（对齐 Java 集成测试 `TestErpPurReturnPosting` setTotalAmount 范式），凭证行方为有意义非零金额。SALES_RETURN 则读行级 `computeTotalCost`（Σ qty×unitPrice=5×10=50），无需置头金额。

#### 验证状态（full-green）

- `npx playwright test tests/e2e/orchestration/ tests/e2e/business-actions/pur-return.action.spec.ts tests/e2e/business-actions/sal-return.action.spec.ts --workers=1`：8 passed（1.2m），0 回归（既有断言 + 新凭证行断言全绿）。
- `mvn clean install -DskipTests`：154 模块 BUILD SUCCESS（1m26s）。
- `mvn test`：全域单元测试 BUILD SUCCESS（6m36s，0 失败 0 错误）。
- 独立结束审计 PASS（ses_0b6c7943affenFhjf6u4J31etl，新会话冷重播），0 BLOCKER。
- 文档对齐：`docs/testing/e2e-runbook.md` 跨域编排层新增「凭证行精确数值断言」子段 + Provider 期望值表（套件计数 167→167，N=0 内联）；`docs/backlog/README.md` 新增 0704-1 路标条目。

---

### 2026-07-10（NCR resolve CAPA 门控 + maintenance Request 状态机浏览器层 E2E — plan 2026-07-10-0335-2）

- 执行 `docs/plans/2026-07-10-0335-2-ncr-capa-resolve-mnt-request-e2e.md`，Phase 1（NCR→CAPA 闭包 resolve 门控）+ Phase 2（maintenance Request 5 态状态机）全部完成。承接 2004-1 Deferred「NCR resolve 经 CAPA 闭包门控路径」+ 0335-1 Deferred「DIRECT 域剩余 maintenance Request 子集」。纯消费侧测试新增，**零生产代码/契约/ORM 模型变更**。
- **2 新 spec（3 test）**：`quality-ncr-resolve-capa-gate` / `mnt-request`。
  - **NCR resolve 门控**：验证跨实体依赖门控——负路径（CAPA 仍 PENDING 时 resolve 抛 `ERR_NCR_RESOLVE_CAPA_NOT_COMPLETED` 对应 message + status 不变 IN_REVIEW）+ CAPA 三步闭包（start→complete→verify）+ 正路径（全 CAPA 闭包后 resolve 成功 status=RESOLVED）。
  - **maintenance Request**：自定义 `status` 5 态（ORM 标 use-approval tagSet 但无 approveStatus 列，生命周期由自定义 status 驱动）正路径 OPEN→ACCEPTED→IN_PROGRESS→COMPLETED + 分支 rejectRequest/cancel + 非法守卫。
- **实现关键裁决**（plan 假设 vs 实际行为差异，均已对齐）：
  - **dispositionType=CONCESSION 非 RETURN**：plan 原注 RETURN「避免自动过账」，但 RETURN 在 `dispatchFinancialImpact` 触发 `NcrReturnOrchestrator.orchestrateReturn`（创建退货单副作用，属 Non-Goal 范围外），SCRAP 触发 config-gated 自动过账。CONCESSION 在 dispatchFinancialImpact 无分派（早返回），干净隔离 resolve 门控本身（本 spec 目标=门控 + status=RESOLVED）。与 quality-capa spec 一致。
  - **错误码断言**：Nop GraphQL 此配置仅回传 i18n message（不序列化 extensions.errorCode），故断言改为该错误码对应的标志性 message token（「CAPA」+「未完成」），唯一区分状态迁移类错误。
  - **Request accept visit 清理**：accept 生成响应式 visit（visitDate=today 落入 maintenance.value.spec 看板区间 07-01~07-31），按 code `VST-REQ-{id}` 删除避免污染 periodVisitCount 基线（1）。
- **清理**：CONCESSION 无过账/退货产物（NCR+CAPA 逻辑删除）；Request 分支路径无 visit 副作用（仅删 Request）。

#### 验证状态（full-green）

- `npx playwright test tests/e2e/business-actions/ tests/e2e/dashboards/{maintenance,quality}.value.spec.ts --workers=1`：28 passed（3.6m），0 回归（既有 25 business-actions + 3 value + 新增 3，看板基线 intact）。
- `mvn install -DskipTests`：154 模块 BUILD SUCCESS。
- 文档对齐：`docs/testing/e2e-runbook.md` 业务动作层新增 2 实体段 + 套件计数 10→12 代表域 / 164→167 测试。

---

### 2026-07-10（审批门控 DIRECT 业务动作浏览器层 E2E — plan 2026-07-10-0335-1）

- 执行 `docs/plans/2026-07-10-0335-1-approval-gated-direct-business-action-e2e.md`，Phase 1（WorkOrder）+ Phase 2（PurReturn/SalReturn）+ Phase 3（Recall）全部完成。承接 2004-1/2330-1 Deferred「useApproval DIRECT 子集（WorkOrder/Return/Recall）」。纯消费侧测试新增，**零生产代码/契约/ORM 模型变更**。
- **4 新 spec（11 test）**：`mfg-work-order` / `pur-return` / `sal-return` / `qa-recall`，覆盖 DIRECT useApproval 审批轴 `submitForApproval`→`approve`→`approveStatus` 翻转 + reject 守卫 + 审批后域特定状态迁移，全部经 `verifyState` `__get` 独立断言。
- **实现关键发现**（plan 假设 vs 实际状态机差异，均已对齐）：
  - **WorkOrder**：`start` 需 `STOCK_RESERVED`/`STOCK_PARTIAL`（非直接从 `NOT_STARTED`），需 `checkAvailability` 前置（需 BOM）。spec 创建无子件 BOM（`requiredByMaterial` 空 → `KitAvailabilityResult.reserved` → STOCK_RESERVED）作 start 门控 enabler，不验证齐套校验本身（归 orchestration successor）。
  - **PurReturn/SalReturn**：`approve` 需上游 approved Receive/Delivery（`requireSourceReceiveApproved`/`requireSourceDeliveryApproved`）+ 行 + qty 校验 + reason（config 默认必填）。spec 复用 `orchestration/_helper.ts` `runP2pChain`/`runO2cChain` 产 approved Receive/Delivery + 库存前置，return 行引用上游行、qty≤上游 qty。approve 后 `posted=true`（反向出/入库 + PURCHASE_RETURN/SALES_RETURN 过账）经 `verifyState` 断言。
  - **Recall**：`approve` **双字段翻转**（`approveStatus=APPROVED` + `status=APPROVED`，非 plan 假设的 status 仍 OPEN）；`locateTargets` 需 `batchId`（解析 ErpInvBatch → batchNo → batchTrace）；spec 创建 ErpInvBatch 作批次门控 enabler（无匹配出库目标产 0 target，迁移仍 IN_PROGRESS）。
- **清理**：Return approve 产 OUTGOING/INCOMING 移动 + 凭证 + AR-AP 辅助账（不可逆下游产物），spec 内联 `cleanupReturnDownstream`（cleanupVoucherByBillCode + cleanupArApByCode + 移动/流水/余额逐域删除）+ `cleanupP2p`/`cleanupO2c` 链路清理，保护共享 DB 数值断言基线。

#### 验证状态（full-green）

- `npx playwright test tests/e2e/business-actions/ --workers=1`：22 passed（2.7m），0 回归（既有 11 test + 新增 11 test）。
- 文档对齐：`docs/testing/e2e-runbook.md` 业务动作层新增 4 实体段 + 套件计数 6→10 代表域 / 153→164 测试；`docs/backlog/README.md` 新增 0335-1 路标条目。

---

### 2026-07-10（报表 AMIS 前端渲染层 DOM 断言扩展至全 24 报表域 — plan 2026-07-09-2330-2）

- 执行 `docs/plans/2026-07-09-2330-2-report-visual-regression-full-domain-coverage.md`，Phase 1（assertReportRendered 提取 + 20 报表 DOM 断言）+ Phase 2（文档对齐）全部完成。承接 1249-2 Deferred「报表 AMIS 前端渲染层 DOM 断言」+ 1728-1:182 残留风险 note。纯消费侧测试新增，**零生产代码/契约/ORM 模型变更**。
- **Phase 1**：`assertReportRendered` 由 `reports.visual.spec.ts` 内联提取至 `visual/_helper.ts`（与 `assertDashboardRendered` 共存共享 helper 结构一致性）。新增 20 报表 config（fin ar-ap-aging/cash-flow/period-close + mfg crp-load/production-variance/forecast-variance + ast depreciation/disposal + mnt downtime/maintenance-history + prj cost-summary/timesheet + qa inspection/ncr-capa + md material-price/partner + inv inventory-trace + cs ticket-sla-csat + crm forecast-accuracy + hr payroll-simulation），覆盖全 24 报表域「page form → AMIS service reload → renderHtml → DOM 注入」全路径含非平凡域参数报表表单→service 接线。
- **执行期发现并解决 AMIS 表单两种变量序列化问题**（value-spec 层直调后端不可见，属 AMIS 运行时交互层独有）：
  - **空字符串过滤**：AMIS 对空表单字段发送 `""` 而非 null，后端按 `""` 过滤返回空数据（15/20 报表首测失败根因）。解决方案：`page.route` 拦截器将 renderHtml 请求中 `""` → null（镜像 value-spec 层省略未用过滤参数的行为）。
  - **DatePicker 时间戳**：AMIS DatePicker 将日期序列化为 Unix 时间戳（如 `"1783440000"`），后端 `DateTimeFormatter` 无法解析（ar-ap-aging 的 `${NOW()}` 默认值亦产此问题）。解决方案：拦截器将 10 位纯数字 → ISO 日期字符串 + `fillDates` 经 label 定位填充有效日期覆盖 `${NOW()}` 默认值。
- **token 选择策略**：每报表 2-3 个 format-independent subject/label token（title/header/code/dict-label）。数值 ≥1000 因 DOM `#,##0.00` 千分位格式化（如 120000→"120,000.00"）仅在 value-spec 层断言。6 个无 value-spec 的报表 token 经 `.xpt.xml` 模板静态 title/header 派生。inv-inventory-trace 因 page.yaml 参数与后端不匹配（发送 batchNo/materialId/warehouseId，后端仅认 moveId）数据行为空，改用 title+header token（AMIS 渲染管线本身完整验证）。

#### 验证状态（full-green）

- `mvn clean install -DskipTests`（154 模块）BUILD SUCCESS。
- `npx playwright test tests/e2e/visual/reports.visual.spec.ts --workers=1`：24 passed（3.1m）。
- `npx playwright test`（全套件）：153 passed（20.2m），0 回归（既有 133 + 新增 20）。

---

### 2026-07-10（useWorkflow 审批轴浏览器层可行性探索 — plan 2026-07-09-2330-1）

- 执行 `docs/plans/2026-07-09-2330-1-xwf-approval-browser-e2e-feasibility.md`，Phase 1（可行性裁决）+ Phase 2（条件跳过）+ Phase 3（Deferred 收敛）全部完成。承接 1249-1/2004-2 Deferred「Payment/Receipt xwf 浏览器层」+ 2004-2 Deferred「useWorkflow 反向」指向同一未解阻塞，产出**单一权威裁决**。纯探索 + 文档，**零 ORM/契约/Java/spec 变更**（临时探针 spec 裁决后已删除）。
- **Phase 1 裁决结论：不可行（NOT FEASIBLE）**。经 nop-entropy 引擎源 + 临时探针 spec fresh-DB 实测定位 sysUser 兜底根因 + 评估 3 条候选路径：
  - **sysUser 兜底根因定位**：`WorkflowEngineImpl.newSteps:274-283`——start step 无 `<assignment>` + 无 selected actors → fallback `getManagerActor()`（`.xwf` 无 `<auth>` → null）→ **`getSysUser()`**（SYS_USER_ID="0"）。故 submit step actor/owner=sysUser(0)，`ApprovalFlowHelper.start:20` 调 `invokeAction(COMPLETE)` 经 `allowCallByUser:1053` 要求 caller=owner("0") 或可委托，nop uuid 不匹配 → submit 被拒。
  - **3 条路径评估**：① wf 委托（`NopAuthUserSubstitution` nop→0）——机制存在且 `DaoUserDelegateService` 已注册，但**实测阻断**：`NopAuthUser.userId` 列 `tagSet="seq"`（`nop-auth/model/nop-auth.orm.xml:38-39`）→ `__save` 时 seq 生成器覆盖显式 "0" 为 UUID（实测创建的 SYS 用户 userId=`3d0538b1...` 非 "0"），委托 FK「类型为[用户]，id为[0]的记录不存在」失败——sysUser(0) 是引擎虚拟用户，浏览器层 `__save` 无法物化。② 浏览器层无用户身份注入/伪装 API（`-Dnop.auth.service-public=true` 仅旁路 auth 不改 `ctx.getUserId()`）。③ `.xwf` submit step `<assignment>` 放宽属生产审批契约变更（削弱 sysUser 自动 complete 语义），超出「浏览器层可行性」范围。
  - **后续步骤驱动已确认可达但依赖 submit 突破**：wf 审批步骤（finance-approval/cc-finance 等 `actorType="all"`）经 `WorkflowService__invokeAction` GraphQL mutation（`@BizModel("WorkflowService")` + `@BizMutation`，`WfActionRequestBean` 字段作直接参数）浏览器层可达，但依赖先突破 submit user:$0 阻塞——submit 不突破则 wf 不启动。
- **Phase 2 条件跳过**：Phase 1 裁决不可行 → Phase 2（代表性实体覆盖）执行条件不满足，Exit Criteria 移入 Deferred But Adjudicated（触发条件：平台支持浏览器层身份映射 / 委托免 sysUser 物化 / sysUser 种子物化时）。
- **Phase 3 Deferred 收敛**：`docs/testing/e2e-runbook.md` 增「useWorkflow 审批轴浏览器层（xwf）— 可行性裁决：不可行」段（sysUser 兜底机制 + 3 路径评估 + successor 触发条件）；既有 Payment/Receipt xwf Deferred 段更新为权威裁决；1249-1/2004-2/2004-1 各 Deferred 增「经 2330-1 权威裁决不可行」交叉引用（消除散乱）。套件计数 133 不变（纯探索无新 spec）。
- 残留 successor：useWorkflow 审批轴浏览器层覆盖（Payment/Receipt/Disposal/Salary 含反向，触发条件=平台工作流引擎支持浏览器层测试用户身份映射/委托免 sysUser 物化时）。

#### 验证状态

- `npx playwright test` 全套件 0 回归（本计划纯探索+文档，探针 spec 已删除，无 spec/Java 变更）。
- `mvn clean install -DskipTests`（154 模块）BUILD SUCCESS（无后端污染）。

---

### 2026-07-10

- 执行并关闭计划 `docs/plans/2026-07-09-2200-1-doc-code-alignment-remediation.md`（文档-代码一致性修复，Plan Status → completed）。
- Phase 1：复核确认 `docs/architecture/domain-module-split-analysis.md` 四小节（§2.0 映射表/§2.1 目录布局/§4.1 DAG/§4.2 跨模块引用白名单）均已完整呈现 notify 域，合计行 "20 行" 与根 pom.xml 20 个 reactor 模块一致。无需改动，仅勾选。
- Phase 2：`docs/design/README.md` 全局文档表格补入遗漏的稳定 owner doc `dashboards.md`（现 7 份）；`codebase-map.md` 已用概括性 "多份"，与任意计数不矛盾。`erp-design-audit-checklist.md`/`use-case-authoring-guide.md` 属方法指南，不纳入。
- Phase 4：重审 `ErpPurSupplierScorecardBizModel`/`ErpPurOrderBizModel`——二者已完全符合 Facade+Processor 模式（逻辑在 `ScorecardCalculator`/`ScorecardStandingLinker`/`ErpPurOrderProcessor`/`RequisitionToOrderConverter`），裁决无需迁移。补齐 `docs/architecture/singlesession-strategy.md` 逐域审计表缺失的 hr 域。结论：当前 `@SingleSession` 使用均为必要场景，无防御性编程实例。
- Phase 5：刷新 `docs/testing/test-depth-classification.md` 至当前真实数据（255 测试类：34 深/207 中/14 浅），明确计数口径排除 `*CodeGen`/`*TestSupport*`/`TestStub*`。
- 验证（full-green）：`mvn clean install -DskipTests` 全 154 reactor 模块 BUILD SUCCESS + `mvn test` 全域单元测试 0 失败 0 错误（本计划为纯文档/度量变更，无 Java/ORM 改动）。
- Follow-up：9 域（assets/finance/hr/manufacturing/quality/inventory/master-data/projects/purchase）扩展实体设计描述对齐仍由各域后续功能计划自动覆盖（已在计划 `Deferred But Adjudicated` 记录）。
