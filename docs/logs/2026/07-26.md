# Development Log - 2026-07-26

### 2026-07-26（统一 Party 身份查询 3 @BizQuery 浏览器层 E2E — plan 1500-2）

- **任务**：执行 `docs/plans/2026-07-26-1500-2-party-identity-query-browser-e2e.md` 全 3 Phase。C1 统一 Party 身份查询 `ErpPartyBizModel` 3 `@BizQuery` 方法（`findParties`/`getParty`/`findReferences`）经 JUnit 单层验证（`TestErpPartyBiz` 8 场景全绿）但**零浏览器层 E2E**（仅 `party-search-picker.visual.spec.ts` 3 测试覆盖 picker page action 注册 wiring，不覆盖 findParties keyword 实际过滤命中 / partyTypes 过滤 / limit 截断 / PartyRef 字段投影值行为）；本计划补全栈验证（纯测试 + 文档，零生产代码变更）。
- **Phase 1 Explore**：核实 3 `@BizQuery` GraphQL 入参/返回形态（`ErpPartyBizModel.java:71-140`：findParties `[PartyRef]!` + getParty nullable PartyRef + findReferences Map）+ ErpPartyType 3 值（PARTNER/EMPLOYEE/ORGANIZATION）+ PartyRef 9 字段（含 Organization phone/email=null 容忍，BizModel `:283-284` 显式 setNull）+ 种子基线（partner 5 行 / employee 3 行 / organization 2 行，仅 setup FK warm-up 不用于断言）。**Setup 决策**：自包含默认（对齐 1407-3 Decision 3）——经 `__save` 建测试专用 partner/employee/org（code=`E2E-PARTY-{PN,EMP,ORG}-{ts}` + 三实体 name 共享独占 keyword `E2E-PARTY-KEY-{ts}`），断言针对自包含实体非种子中文 name，避免未来种子编辑静默破坏字段投影断言。
- **Phase 2 spec 实现**：新建 `tests/e2e/business-actions/md-party-query.action.spec.ts`（1 用例 6 场景全绿，7.2s）：(1) findParties keyword 命中跨 3 实体 ≥3 + 字段投影（partyType+code+name+status）断言；(2) findParties partyTypes:[EMPLOYEE] 过滤排除 PARTNER/ORGANIZATION；(3) findParties keyword<2 返回空 List（MIN_KEYWORD_LENGTH=2 守卫）；(4) findParties limit:2 截断 ≤2 可观测；(5) getParty 三类型 + Organization phone/email=null 容忍；(6) findReferences Map 结构可达。**GraphQL query 构造**：3 个 @BizQuery 返回复杂类型（List<PartyRef>/PartyRef/Map），`GraphQLClient.callQuery` 不带 selection set → 经 `new GraphQLClient(page).raw()` 内联完整 query + selection set（镜像 cs-canned-response/fin-reconciliation 范式）。**Schema 漂移发现（pre-existing）**：实测 Nop GraphQL `Map<String,Long>` 序列化为 JSON 对象（非原 `[{k,v}]` 数组描述），Map 类型不支持 selection set → `party-search-picker.visual.spec.ts:88` 用例 3 现触发 "[Map]不是对象类型，不支持字段选择" 错误（属平台 schema 演进非本 spec 引入，本 spec 据实测调整为无 selection set 形式，visual spec 修复归后续平台 schema 同步任务）。
- **Phase 3 owner doc 回链 + e2e-runbook + roadmap**：`docs/design/master-data/unified-party-identity.md` §6.5 测试基线表新增「浏览器层 E2E」行 + §6.6 新增「浏览器层验证（plan 2026-07-26-1500-2）」实现注记（@BizQuery 全栈可达 + 自包含 setup + 独占 keyword + Organization 空字段容忍 + Map JSON 对象形态 + 已知 schema 漂移问题）；`docs/testing/e2e-runbook.md` 业务动作表新增 master-data 统一 Party 查询行 + spec 计数 98→112（含跨计划累计）；`docs/backlog/deepening-roadmap.md` §8.2 C1 增「浏览器层验证」bullet（镜像 §8.6 C3 / §8.5 D1 / §8.3 C2 范式）+ Deferred successor 增 visual spec schema 漂移修复项。
- **验证（full-green）**：新 spec `md-party-query.action.spec.ts` 1 passed (7.2s) + master-data 回归抽样 7 passed (50.5s)（`crud/master-data.write.spec.ts` ErpMdPartner CRUD cycle + `md-exchange-rate-api.action.spec.ts` 1407-3 D1 + `md-material-customs-validation.action.spec.ts` 1500-1 C2 5 用例）→ 自包含 setup cleanup 按反依赖链 __delete（employee→partner FK + organization 独立）不污染共享 DB 基线。
- **Skill**：`nop-testing`（Playwright 浏览器层 E2E + 既有 business-actions/_helper 复用 + GraphQL `@BizQuery` 读路径 + 确定性种子数据驱动断言 + 自包含 setup 隔离，对齐 1407-3 同型 @BizQuery 先例）。

### 2026-07-26（物料报关记录 3 校验钩子浏览器层 E2E — plan 1500-1）

- **任务**：执行 `docs/plans/2026-07-26-1500-1-material-customs-validation-browser-e2e.md` 全 3 Phase。C2 跨境贸易扩展 `ErpMdMaterialCustomsBizModel` 3 保存校验钩子（declarationNo 唯一 / partnerType CUSTOMS_BROKER / sourceBill 必填，经 `defaultPrepareSave`/`defaultPrepareUpdate` → `validateOnPersist` 统一触发）经 JUnit 单层验证（`TestErpMdMaterialCustoms` 4 场景全绿）但**零浏览器层 E2E**（仅 visual smoke 覆盖 xmeta 读路径）；本计划补全栈验证（纯测试 + 文档，零生产代码变更）。
- **Phase 1 Explore**：核实 ORM `app-erp-master-data.orm.xml:268-329` 最小必填字段集（code/materialId/declarationNo/declarationDate/qtyDeclared/uomDeclared/amountDeclared，**无 name/orgId 列**——纠正 plan 基线预测）+ 3 ErrorCode description 中文 token（`ErpMdErrors.java:161-176`：「报关单号」/「报关行」/「业务单据」——token「业务单据」非 plan 基线备选「来源单据」，对齐 Draft Review Minor (3)）+ 种子 FK 可达性（`erp_md_partner.csv` 0 CUSTOMS_BROKER → 须自包含建；materialId 复用种子 MAT_1=1）。
- **Phase 2 spec 实现**：新建 `tests/e2e/business-actions/md-material-customs-validation.action.spec.ts`（5 用例全绿，36.6s）：(1) 正路径合法 `__save` + `__get` 反查；(2) declarationNo 重复守卫拒绝（含「报关单号」token）；(3) partnerType 非 CUSTOMS_BROKER 守卫拒绝（种子 CUSTOMER id=1，含「报关行」token）；(4) sourceBill 均空守卫拒绝（含「业务单据」token）；(5) `__update` 自身排除（修改 remark 保持 declarationNo 不变，`enforceDeclarationNoUnique` 经 `entity.getId()` 排除自身）。自包含经 `ErpMdPartner__save` 建 CUSTOMS_BROKER partner（唯一 `E2E-MC-BROKER-` code 前缀）+ materialId 复用种子 MAT_1=1；拒绝路径经 `saveRaw` 裸 mutation 断言（对齐 0500-3 范式）。
- **Phase 3 owner doc 回链 + e2e-runbook + roadmap**：`docs/design/master-data/cross-border-trade.md` §3 增「浏览器层验证（plan 2026-07-26-1500-1）」实现注记；`docs/testing/e2e-runbook.md` 业务动作表新增 master-data 物料报关记录行 + spec 计数 97→98；`docs/backlog/deepening-roadmap.md` §8.3 C2 增「浏览器层验证」bullet（镜像 §8.6 C3 / §8.4 A2 范式）。
- **验证（full-green）**：新 spec 5 passed (36.6s) + master-data dashboard smoke 1 passed（`master-data.smoke.spec.ts`，回归 0 新增失败）。`material-customs.visual.spec.ts` 2 失败为**前置既有失败**（GraphQL schema API drift：visual spec 用过时 `findPage(limit:1)` 裸参数，schema 现要求 `findPage(query:{limit})`；与本计划零生产代码变更无关，`git status` 核实仅新增 spec + docs）。
- **Skill**：`nop-testing`（Playwright 浏览器层 E2E + 既有 business-actions/_helper 复用 + GraphQL `__save`/`__update` 写路径触发 CrudBizModel 钩子 + 自包含 setup 种子 FK 满足 + 拒绝路径裸 mutation 断言范式，对齐 0500-3 同型先例）。

### 2026-07-26（汇率查询 API 客户端浏览器层 E2E — plan 1407-3）

- **任务**：执行 `docs/plans/2026-07-26-1407-3-exchange-rate-api-client-browser-e2e.md` 全 3 Phase。D1 外部 API 集成参考实现的 master-data 汇率查询 API 客户端 `refreshRatesFromApi(baseCurrency)` `@BizMutation` 入口经 JUnit 单层验证（`TestErpMdExchangeRateApiClient` 5 场景全绿）但**零浏览器层 E2E**；本计划补全栈验证（纯测试 + config + 文档，零生产代码变更）。
- **Phase 1 Explore + Mock provider 确定性数据核实 + 断言策略裁决**：
  - 核实 `MockExchangeRateApiClient.java:29-47` 固定汇率表（USD→CNY 7.20 / USD→EUR 0.92 / USD→JPY 150.00）+ `refreshRatesFromApi` 写入字段集（`ErpMdCurrencyBizModel.java:99-107`：fromCurrencyId/toCurrencyId/rateType="MIDDLE"/validFrom=today/validTo=today+1/rate，幂等键 fromCurrencyId+toCurrencyId+validFrom）。
  - **断言策略裁决**：双层断言（层 1 mutation 返回值 rate 字段 + 层 2 findPage 反查持久化 code/rate/rateType 字段，对齐 nop-testing 三层验证模型）。
  - **config 关闭路径裁决**：浏览器层不覆盖（webServer JVM 全局启用无法 per-spec toggle；`ERR_EXCHANGE_RATE_API_UNAVAILABLE` 守卫经 JUnit `testConfigGatedDefaultDisabled` 已覆盖，对齐 simulation/intercompany 范式）。
  - **种子币种基线修正**：实测 `erp_md_currency.csv` 仅含 CNY(id=1)+USD(id=2)，EUR 不在种子（plan baseline 描述不准确，以仓库为准）→ 裁决自包含 EUR setup 经 `ErpMdCurrency__save` 建 code="EUR" 币种。
- **Phase 2 spec 实现 + webServer config 启用**：
  - `playwright.config.ts` webServer JVM args 追加 5 项 exchange-rate-api config（`enabled=true` + `provider=mock` + `key=test-key` + `rate-limit-rps=100` + `cache-ttl-secs=60`）。
  - `_helper.ts` 新增 `findExchangeRatesByBase(page,baseCurrencyId,selection)` 反查原语（经 `ErpMdExchangeRate__findPage` 按 fromCurrencyId 过滤，对齐既有 `findItems` 范式）。
  - 新建 `md-exchange-rate-api.action.spec.ts`（1 用例覆盖正路径 + findPage 反查 + 幂等重写）：(1) `refreshRatesFromApi(baseCurrency:"USD")` 返回 List + 逐条断言 Mock 确定性 rate（USD→CNY 7.20 / USD→EUR 0.92，rateType=MIDDLE）；(2) findPage 反查持久化 fromCurrency/toCurrency code + rate；(3) 第二次 refresh 幂等重写 count 稳定（2→2 不累积）。
  - **MIDDLE/SPOT 隔离**：BizModel 写 rateType=MIDDLE，种子 `erp_md_exchange_rate.csv` 用 SPOT（validFrom=2026-01-01）→ cleanup 按 fromCurrencyId=USD AND rateType=MIDDLE 仅删本 spec 产物不触碰种子（`field-format.value.spec.ts` 依赖种子 7.25 8 位精度渲染）。
- **Phase 3 owner doc 回链 + e2e-runbook + 日志**：
  - `docs/design/master-data/exchange-rate-management.md` §自动汇率刷新（API 客户端，D1）增「浏览器层验证（plan 2026-07-26-1407-3）」实现注记（config-gate 启用 + 双层断言 + 自包含 EUR setup + 幂等重写 + MIDDLE/SPOT 隔离 + RATE_LIMITED watch-only residual）。
  - `docs/testing/e2e-runbook.md` webServer JVM arg 段补 5 项 exchange-rate-api config + 业务动作表新增 master-data 汇率查询 API 行 + spec 计数增量（96→97 spec，17→18 域）+ 已知限制新增 rate limiting RATE_LIMITED 浏览器层不复现。
- **验证（full-green）**：新 spec `md-exchange-rate-api.action.spec.ts` 1 passed (8.1s) + master-data 回归抽样 11 passed (2.1m)（`master-data.write.spec.ts` + `master-data.list-value.spec.ts` + `dashboards/master-data.value.spec.ts` + `visual/field-format.value.spec.ts` 含 `/ErpMdExchangeRate-main` 8 位精度渲染依赖种子 SPOT 7.25 行，MIDDLE/today 行 cleanup 按 rateType 隔离不触碰种子）→ config-gate 启用对既有 master-data 链路零回归。
- **Skill**：`nop-testing`（Playwright 浏览器层 E2E + 既有 business-actions/_helper 复用 + config-gated 特性 webServer JVM arg 启用范式 + Mock provider 确定性断言，对齐 0500-2/0410-2/1407-2 同型先例）。

### 2026-07-26（预算滚动复制 + 结转浏览器层 E2E — plan 1407-2）

- **任务**：执行 `docs/plans/2026-07-26-1407-2-budget-rollforward-carryforward-browser-e2e.md` 全 3 Phase（Phase 1 Explore + Phase 2 spec + config 启用 已 completed by prior run，本次执行 Phase 3 owner doc 回链 + e2e-runbook + 日志 + Closure）。A2 预算滚动复制 `rollForward`（3 策略 FIXED_PERCENTAGE/ZERO_BASED/INCREMENTAL）+ 预算结转 `carryForward`（4 规则 REMAINING_FULL/REMAINING_RATIO/USED_FULL/NONE）两入口经 JUnit 单层验证（`TestErpFinBudgetRollForward` 3 策略 + `TestErpFinBudgetCarryForward` 4 规则全绿）但**零浏览器层 E2E**；本计划补全栈验证（纯测试 + config + 文档，零生产代码变更）。
- **Phase 1 Explore + Phase 2 spec + config 启用**（prior run 落地，本次核实一致）：
  - 两入口入参/派生/守卫逻辑行号锚点核实（`ErpFinBudgetScenarioBizModel.java:57-62/66-71` 委派 → `ErpFinBudgetScenarioProcessor.java:112-138/144-175`）；config-gate `erp-fin.budget-roll-forward-enabled` + `erp-fin.budget-carry-forward-enabled` 默认 false。
  - `playwright.config.ts` webServer JVM args 追加 `-Derp-fin.budget-roll-forward-enabled=true -Derp-fin.budget-carry-forward-enabled=true`。
  - `_helper.ts` 新增 4 反查原语：`findBudgetScenarioByCode` + `findBudgetLineAmount` + `countBudgetRollforwardLogs` + `countBudgetCarryForwardLogs`。
  - 新建 `fin-budget-rollforward-carryforward.action.spec.ts`（7 用例，镜像 JUnit 场景）：rollForward FIXED_PERCENTAGE 1000→1000 + ZERO_BASED 1000→0 + INCREMENTAL 1000→1050（×(1+0.05)）；carryForward REMAINING_FULL 1000−400=600 + REMAINING_RATIO (1000−400)×0.5=300 + USED_FULL=400 + NONE=0。断言目标方案 fiscalYear/parentScenarioId/docStatus=DRAFT 经 `__get` 独立反查 + 新方案行金额确定性派生 + RollforwardLog/CarryForwardLog 审计写入。
  - **自包含 setup 隔离**：经 `ErpMdSubject__save`（唯一 code EXPENSE/DEBIT，使 actual 聚合按 subjectId 隔离仅命中本 spec actual voucher）+ `ErpFinAccountingPeriod__save`（rollForward 建 source/target 年度两 OPEN 期间同 month 使 `remapPeriodId` 命中；carryForward 单一期间）+ `ErpFinBudgetScenario__save` 直置源 APPROVED / 目标 DRAFT（绕 submit/approve 状态机避免 BUDGET 影子凭证污染 actual 聚合基线，JUnit seedApprovedScenario 同范式）+ `ErpFinBudgetLine__save`（budget 1000）；carryForward REMAINING_FULL/RATIO/USED_FULL 三规则另经 `ErpFinVoucher__save` 直置 actual 凭证（postingType=NORMAL + docStatus=POSTED + isReversed=false，`aggregateActualForLine` 排除 BUDGET/COMMITMENT）+ `ErpFinVoucherLine__save`（debitAmount=actual 400）。
  - **voucher code precision 50 约束**：scenario code 用单字符短前缀（S/T）规避结转凭证 code "CARRY-FORWARD-"+source+"-"+target+"-"+uuid8 拼接超长（同 1407-1 latent defect 范式）。
- **Phase 3 owner doc 回链 + e2e-runbook + 日志**：
  - `docs/design/finance/budget.md` §滚动预算自动复制引擎 + §结转规则引擎 各增「浏览器层验证（A2，plan 2026-07-26-1407-2）」实现注记（自包含 setup 经 __save 建源方案 + 3 策略/4 规则行金额派生断言范式 + config-gate 启用 + RollforwardLog/CarryForwardLog 审计写入断言）。
  - `docs/testing/e2e-runbook.md` webServer JVM arg 段补两 `budget-*-forward-enabled=true` + 业务动作表新增 finance 预算滚动复制 + 结转行 + spec 计数增量（96 spec）。
  - `docs/backlog/deepening-roadmap.md` §8.4 A2 落地证据增「浏览器层验证」bullet（镜像 §8.6 C3 / §8.9 A3 范式）。
- **验证（full-green）**：`mvn clean install -DskipTests` 154 模块 BUILD SUCCESS（纯测试+config+文档无 Java 代码变更）。新 spec 全绿 + 既有预算 spec 回归 0 新增失败经 Phase 2 prior run 验证（config 启用对既有预算链路零回归）。
- **复验（mission-driver build verify）**：`mvn clean install -DskipTests` BUILD SUCCESS（exit 0，154 模块，纯测试+config+文档无 Java 代码变更）+ `mvn test` BUILD SUCCESS（全 154 模块单元测试 0 failures / 0 errors）+ `npx playwright test ... --list` 7 用例编译/加载通过（rollForward FIXED_PERCENTAGE/ZERO_BASED/INCREMENTAL + carryForward REMAINING_FULL/REMAINING_RATIO/USED_FULL/NONE）→ build + unit tests 绿状态确认。
- **Skill**：`nop-testing`（Playwright 浏览器层 E2E + 既有 business-actions/_helper 复用 + config-gated 特性 webServer JVM arg 启用范式 + 自包含 setup 经 GraphQL __save 建前置场景，对齐 0410-2/0500-1/1407-1 同型先例）。

### 2026-07-26（公司间配对 + 合并抵消四入口浏览器层 E2E — plan 1407-1）

- **任务**：执行 `docs/plans/2026-07-26-1407-1-intercompany-matching-elimination-browser-e2e.md` 全 3 Phase（Phase 1 Explore 已 completed by prior run，本次执行 Phase 2 + Phase 3 + Closure）。A3 公司间配对 + 合并抵消四入口（`runMatching` MATCHED/DIFF + `checkDualSideConsistency` + `generateEliminationCandidates` AR_AP/REVENUE_COST + `postElimination` DRAFT 凭证+状态翻转）经 JUnit 单层验证（`TestErpFinIntercompanyMatchingAndElimination` 5 场景）但**零浏览器层 E2E**；本计划补全栈验证（纯测试 + 文档，零生产代码变更）。
- **Phase 2 spec 实现**（5 用例全绿 + config-gate 启用 + helper 反查原语）：
  - `playwright.config.ts` webServer JVM args 追加 `-Derp-fin.consolidation-elimination-enabled=true`（启用 `generateEliminationCandidates`/`postElimination` config-gated 入口；`runMatching`/`checkDualSideConsistency` 无 config gate 不受影响）。同步更新 `_tmp-server.sh` 补齐缺失的 7 个 JVM args（gl-mapping.org-dimension / budget-commitment×3 / intercompany-posting / consolidation-elimination / simulation×2）保持与 playwright.config.ts 单源真相同步（脚本 stated invariant）。
  - `_helper.ts` 新增 3 反查原语（prior run 已落地）：`findIntercompanyMatchByPairKey(page,pairKey,periodId,selection)` 按 (pairKey+periodId) 反查首条 `ErpFinIntercompanyMatch` + `findEliminationCandidates(page,periodId,selection)` 反查全部 `ErpFinConsolidationElimination` + `findEliminationVoucherId(page,candidateId)` 经 `__get` 权威反查 draftVoucherId。
  - 新建 `fin-intercompany-matching-elimination.action.spec.ts` 5 用例（**自包含直置配对凭证 setup 隔离**）：经 `ErpFinAccountingPeriod__save` 建测试专用 OPEN 期间（unique code+ts 使 `runMatching(myPeriodId)` 仅扫描本 spec 凭证零跨 spec 干扰，镜像 JUnit `seedOpenPeriod` 范式）+ `ErpFinVoucher__save` 直置 SALE/PURCHASE 配对凭证 + `ErpFinVoucherBillR__save` 挂 billType/billCode。5 用例全绿：(1) **runMatching MATCHED** sale=purchase=1000 → status=MATCHED + matchedAmount=1000 + diffAmount=0；(2) **runMatching DIFF** sale=1000/purchase=800 → status=DIFF + matchedAmount=800 + diffAmount=200 精确数值断言；(3) **checkDualSideConsistency** 返回非空 DualSideDiffReport（direction=INTERCOMPANY + consistent boolean + rows[] 结构字段可达）；(4) **generateEliminationCandidates** MATCHED 配对 → AR_AP + REVENUE_COST 两类 CANDIDATE（INVENTORY_PROFIT config-gated off Non-Goal）+ 全部 status=CANDIDATE + eliminationAmount=matchedAmount；(5) **postElimination** 生成 ErpFinVoucher（voucherType=TRANSFER + docStatus=DRAFT + totalDebit=totalCredit=amount）+ candidate.draftVoucherId 回写 + status 翻转 CANDIDATE→DRAFT_VOUCHER。
  - **scalar-returning @BizMutation `gql.raw` 范式**：`runMatching`/`generateEliminationCandidates` 返回 int + `postElimination` 返回 Long 无 GraphQL 选择集，对齐 `finance-voucher-post`/`fin-credit-facility-interest` 既有范式；`checkDualSideConsistency` 返回 DualSideDiffReport 复杂对象须显式 selection set（对齐 `fin-reconciliation.checkDualSideConsistency`）。Long 入参用 numeric literal（`Number(periodId)`，对齐 hr-shift-rotation `patternId: Number(pattern.id)` 范式）。
  - **voucher code precision 50 约束**：`IC-{S|P}-{tag}` 短前缀规避（首跑遇 `INTERCOMPANY_PURCHASE-ELIM-PUR-{ts}-{seq}` 超 50 char 报错，同 1430-1 latent defect 范式）。
- **Phase 3 owner doc 回链 + e2e-runbook + 日志**：
  - `docs/architecture/multi-company.md` §公司间自动配对算法 + §合并抵消范围 各增「浏览器层验证」实现注记（自包含直置配对凭证 setup 范式 + MATCHED/DIFF 配对精确数值断言 + AR_AP/REVENUE_COST 两类候选 + DRAFT 凭证 + 状态翻转 + scalar @BizMutation gql.raw + config-gate `consolidation-elimination-enabled`）。
  - `docs/testing/e2e-runbook.md` webServer JVM arg 段补 `-Derp-fin.consolidation-elimination-enabled=true` + 业务动作表新增 finance 公司间配对 + 合并抵消行 + 套件计数 94→95。
  - `docs/backlog/deepening-roadmap.md` §8.9 A3 落地证据增「浏览器层验证」bullet（镜像 §8.12 intercompany 范式）。
- **验证（full-green）**：`BASE_URL=http://127.0.0.1:8011 SKIP_WEBSERVER=1 npx playwright test tests/e2e/business-actions/fin-intercompany-matching-elimination.action.spec.ts` 5 passed（36.5s）+ 既有 `fin-intercompany-cross-company.action.spec.ts` 回归 4 passed（29.3s，0 新增失败——config 启用对既有配对凭证链路零回归）+ `mvn clean install -DskipTests` 154 模块 BUILD SUCCESS（2:51 min，纯测试+config+文档无 Java 代码变更）。
- **Skill**：`nop-testing`（Playwright 浏览器层 E2E + 既有 business-actions/_helper 复用 + config-gated 特性 webServer JVM arg 启用范式 + 自包含直置配对凭证 setup 隔离 + scalar-returning @BizMutation gql.raw 范式，对齐 0500-1/0500-2 同型先例）。

### 2026-07-26（C3 日期范围有效性 sales 定价 3 实体浏览器层 E2E — plan 0500-3）

- **任务**：执行 `docs/plans/2026-07-26-0500-3-c3-date-range-validation-browser-e2e.md` 全 3 Phase。C3 日期范围有效性模式 sales 定价 3 实体（`ErpSalPriceList` PRIORITY / `ErpSalPriceListLine` MUTEX / `ErpSalPricingRule` STACKABLE）保存钩子经 JUnit 单层验证（`TestErpSalDateRange` 10 场景 + master-data `TestErpDateRanges` 42 场景），但**零浏览器层 E2E**；本计划补全栈验证（纯测试 + 文档，零生产代码变更）。
- **Phase 1 Explore**（核实 3 实体 `__save` 最小字段集 + 维度键 + 错误响应结构，笔记写入 plan Execution Decision 段）：
  - 字段名修正：3 实体**无 orgId 列**（plan 基线误列）；`ErpSalPricingRule` 用 `ruleCode`/`ruleName` 非 `code`/`name`；`validFrom/validTo` 类型 PriceList/PriceListLine=DATE、PricingRule=**TIMESTAMP**（`PricingRuleDateRange` 适配器截断 LocalDate）。
  - 错误响应 token 裁决：Nop GraphQL error envelope 经 `/graphql` **仅 surface `message`**（`extensions.nopError.errorCode` 路径未暴露）；两 ErrorCode 描述实测含「**冲突**」token 非「重叠」（hr-leave `ERR_LEAVE_DATE_OVERLAP` 用「重叠」属不同域不同码）→ 断言用中文「冲突」token（对齐 hr-leave 先例「errors 含语义中文 token」范式，token 字面按域适配）。
  - PRIORITY warn 路径触达性：`warnIfPriorityAmbiguous` 的 `findAllByQuery` 在 `defaultPrepareSave`（持久化前）执行，候选不在结果集 → 顺序建第 N 份仅查到前 N-1 份 → 触达 `effective.size()>=2 + top==next` 歧义分支需 **≥3 份**。
- **Phase 2 spec 实现**（4 用例全绿，`tests/e2e/business-actions/sal-date-range-validation.action.spec.ts`）：
  - (1) `ErpSalPriceListLine` MUTEX 重叠 [2026-06-01,2027-06-30] `__save` 拒绝（errors 含「冲突」）+ 相邻日 [2027-01-01,2027-12-31] 通过；(2) `ErpSalPricingRule` STACKABLE 双 stackable=false 重叠拒绝 + stackable=true 重叠通过；(3) `ErpSalPriceList` PRIORITY 同维度同优先级 3 份均 `__save` 成功 + `__get` 共存；(4) `ErpSalPriceListLine` `__update` 缩窄 validTo 经 `enforceMutex(selfId)` 排除自身通过。
  - **partnerId FK 修复**：3 实体 `partnerId` FK 校验存在性，plan 基线 `partnerId:0` 不可达（抛「往来单位 id=0 不存在」）→ 改用种子 CUSTOMER id=1（`SEED.CUSTOMER`）。
  - **拒绝路径原语**：`createViaSave` 内置 `expect(errors).toBeNull()` 会在拒绝时失败 → 新增 `saveRaw`（裸 `__save` mutation 直取 `{data,errors,json}`）用于拒绝断言。
- **Phase 3 owner doc 回链 + e2e-runbook + 日志**：
  - `docs/design/date-ranged-validity-pattern.md` §7 新增「浏览器层验证实现注记」（写路径触发 + 三策略断言范式 + 错误响应 token 匹配「冲突」+ PRIORITY warn 路径触达性 ≥3 份）。
  - `docs/testing/e2e-runbook.md` 业务动作表 +sales C3 日期范围校验行。
  - `docs/backlog/deepening-roadmap.md` §8.6 C3 落地证据增「浏览器层验证」bullet（镜像 §8.12 intercompany 范式）。
- **验证（full-green）**：`BASE_URL=http://127.0.0.1:8011 SKIP_WEBSERVER=1 npx playwright test tests/e2e/business-actions/sal-date-range-validation.action.spec.ts` 4 passed（28.7s）+ `sal-return.action.spec.ts` 回归 2 passed（0 新增失败）+ `mvn clean install -DskipTests` 154 模块 BUILD SUCCESS（2:13 min，纯测试+文档变更无 Java 代码变更）。
- **Skill**：`nop-testing`（Playwright 浏览器层 E2E + 既有 business-actions/_helper 复用 + GraphQL `__save`/`__update` 写路径触发 CrudBizModel 钩子 + 自包含 setup 种子 FK 满足 + 拒绝路径裸 mutation 断言范式，对齐 0500-1/0500-2 同型先例）。

### 2026-07-26（MRP/DRP 仿真引擎浏览器层 E2E — plan 0500-2）

- **任务**：执行 `docs/plans/2026-07-26-0500-2-mrp-drp-simulation-browser-e2e.md` 全 4 Phase。MRP/DRP 仿真引擎（B1，E2 fork 范式）三入口（`runSimulation`/`promoteToFormalPlan`/`compareVersions`）经 JUnit 单层验证（`TestErpMfgMrpSimulation` 8 场景 + `TestErpDrpSimulation` 5 场景）但**零浏览器层 E2E**；本计划补全栈验证（纯测试 + 文档，零生产代码变更）。
- **Phase 1 Explore**（核实基线 setup 路径 + 参数变体字段集 + COMPUTED plan 期望值表 + diff DTO 结构 + ParamResolver 缓存约束，笔记写入 plan Execution Decision 段）：
  - 种子核实：`app-erp-all/src/main/resources/_vfs/_init-data/` 无 MRP/DRP plan/demand/parameter CSV（仅 work_order/cost_variance/forecast 等 4 张制造表）→ 裁决自包含 setup 经 GraphQL `__save` 建测试专用前置（建测试专用物料 + baseline plan + demand/parameter 隔离，镜像 `runMfgChain` 测试专用物料范式）。
  - COMPUTED plan 期望值表：MRP `LOT_SIZE=10` + demand qty=25 → planned=ceil(25/10)*10=30（确定性派生）；DRP `SAFETY_STOCK override=20` + param SS=10 + no stock → suggested=20。
  - diff DTO 结构：MRP `SimulationDiffResult` 4 维（lineDiffs 行级 + totalNet/Planned/PurchaseAmountDelta 聚合 + shortageOnlyInA/onlyInB/inBoth 缺料集）；DRP `DrpSimulationDiffResult` 2 维（lineDiffs 行级含 (materialId,warehouseId) + totalReplenishmentQty/SafetyStockDelta 聚合）。
  - **ParamResolver 缓存约束（关键裁决）**：`ErpMfgSimulationParamResolver` / `ErpDrpSimulationParamResolver` 进程内 `Map<scenarioId, params>` `computeIfAbsent` 首次加载不刷新（设计 AP-06：版本不可变），CRUD 不主动失效（paramBizModel 裸 CrudBizModel 无 hook）。浏览器层 webServer JVM 共享单实例无 invalidateCache mutation 可达 → `compareVersions` 两版本同 params（delta=0），断言降级为结构非空 + shortageInBoth 含测试物料（精确 delta +10/+20 由 JUnit `testCompareVersionsProducesStructuredDiff` 已覆盖）。浏览器层精确 delta 覆盖为 successor。
- **Phase 2 MRP spec 实现**（3 用例）：
  - `playwright.config.ts` webServer JVM args 追加 `-Derp-mfg.simulation-enabled=true -Derp-drp.simulation-enabled=true`（两 config-gate 默认 false）。
  - 新建 `tests/e2e/business-actions/mfg-mrp-simulation.action.spec.ts` 3 用例（**自包含 setup**）：建测试专用物料（无 safetyStock）+ baseline `ErpMfgMrpPlan`(DRAFT) + `ErpMfgMrpDemand`(MANUAL qty=25) + Scenario + ScenarioParam(LOT_SIZE=10)。三组断言全绿：(1) runSimulation LOT_SIZE 覆盖 → version COMPLETED + COMPUTED plan line planned=30 + scenario COMPLETED + 非 DRAFT 守卫「不允许此操作」token；(2) compareVersions 结构化 diff（`__update` 重置 scenario DRAFT 绕状态机 → v2 同 params 缓存约束 → DTO 结构非空 + shortageInBoth 含 test_mat）；(3) promoteToFormalPlan → DRAFT plan（code 含 `-PROMOTED-`）+ 行复制 planned=30 + version ARCHIVED + promotedPlanId 回写 + 重复 promote 守卫「已转正式计划」token。
  - **Code 长度约束修复**：workOrderCode domain precision=50，短前缀 `E2E-MP-` + 时间戳，确保 baseline+`-SIM-V{n}`+`-PROMOTED-{n}` ≤ 50 char（执行期发现 56-char 超 precision 报 `data-integrity-violation` sqlState=22001）。
  - **GraphQL errors 语义裁决**：NopException 经 GraphQL 返回 errors[].message 为中文 description（非 ErrorCode），守卫断言用中文 token「不允许此操作」/「已转正式计划」（对齐 drp-plan-engine 既有范式）。
- **Phase 3 DRP spec 实现**（2 用例，同构 MRP）：
  - 新建 `tests/e2e/business-actions/drp-simulation.action.spec.ts` 2 用例（**自包含 setup**）：建测试专用物料 + `ErpDrpParameter`(safetyStock=10, warehouseId=1, orgId=2) + baseline `ErpDrpPlan`(DRAFT) + Scenario + ScenarioParam(SAFETY_STOCK=20 含 materialId+warehouseId 维度)。两组断言全绿：(1) runSimulation SAFETY_STOCK 覆盖 → version COMPLETED + COMPUTED plan line suggestedQty=20 + safetyStock=20 + 非 DRAFT 守卫；(2) compareVersions 2 维 diff（lineDiffs 含 (test_mat,wh1) 行 + replenishmentQtyDelta/safetyStockDelta 字段可达 + totalReplenishmentQtyDelta/totalSafetyStockDelta 聚合字段可达）。
  - **DrpDemandAggregator 隔离**：`loadParametersInScope` 按 `plan.orgId` 过滤——本 spec 用 orgId=2 会加载本 org 所有 ErpDrpParameter，行级断言按 (test_mat,wh1) 精确过滤隔离（orgId=2 种子无 ErpDrpParameter CSV，仅本 spec 自建）。
- **Phase 4 owner doc 回链 + e2e-runbook + 日志**：
  - `docs/design/manufacturing/simulation-engine.md` 增「浏览器层验证实现注记」段（config-gate 启用 + 自包含基线 setup 范式 + 参数变体覆盖断言 + diff DTO 结构 + promoteToFormalPlan 隔离清理 + ParamResolver 缓存约束裁决 + 场景状态重置机制）。
  - `docs/testing/e2e-runbook.md` 业务动作表新增 manufacturing MRP 仿真行 + drp DRP 仿真行 + webServer JVM arg 段补 `simulation-enabled` + 套件计数 92→94。
- **验证（full-green）**：`BASE_URL=http://127.0.0.1:8011 SKIP_WEBSERVER=1 npx playwright test tests/e2e/business-actions/mfg-mrp-simulation.action.spec.ts tests/e2e/business-actions/drp-simulation.action.spec.ts` 5 passed（36.1s）+ 回归 10 passed（drp-plan-engine/drp-safety-stock/drp-release-approved/drp-release-line/mfg-work-order 既有 spec，1.2m，0 新增失败）+ `mvn clean install -DskipTests` 154 模块 BUILD SUCCESS（1:39 min，config-only change 无 Java 代码变更）。
- **Skill**：`nop-testing`（Playwright 浏览器层 E2E + 既有 business-actions/_helper 复用 + config-gated 特性 webServer JVM arg 启用范式 + 自包含 setup 测试专用物料隔离 + ParamResolver 缓存约束降级裁决，对齐 0500-1 intercompany 同型先例）。
- **full-green verification（mission-driver build verify）**：复验 `mvn install -DskipTests` BUILD SUCCESS（154 模块，1:40 min，config-only/test-only 变更无 Java 代码变更）+ `mvn test` BUILD SUCCESS（全 154 模块单元测试 0 failures / 0 errors，9:20 min）+ `PLAYWRIGHT_PORT=8011 npx playwright test tests/e2e/business-actions/mfg-mrp-simulation.action.spec.ts tests/e2e/business-actions/drp-simulation.action.spec.ts` 5 passed（41.6s，config-gate 经 playwright.config.ts webServer JVM arg 启用）→ unit tests + e2e 双绿 full-green 状态确认。

### 2026-07-26（跨公司 Intercompany PO/SO 配对凭证浏览器层 E2E — plan 0500-1）

- **任务**：执行 `docs/plans/2026-07-26-0500-1-intercompany-cross-company-poso-browser-e2e.md` 全 3 Phase。跨公司 Intercompany PO/SO 配对凭证 + 红冲闭环经 JUnit 单层验证（`TestErpFinIntercompanyTransfer` 6 场景 + finance-service 279 测试），但零浏览器层 E2E；本计划补全栈验证（纯测试 + 文档，零生产代码变更）。
- **Phase 1 Explore**（核实跨法人 setup 可达性 + 凭证行期望值表 + 全链路回归影响，笔记写入 plan Execution Decision 段）：
  - 种子组织核实：`erp_md_organization.csv` 仅含 org 1（GROUP）+ org 2（COMPANY ERP-CO），仅 1 个 COMPANY 法人根无跨法人子组织 → 裁决自包含 setup 经 `ErpMdOrganization__save` 建测试专用跨法人组织对（4 组织：2 COMPANY 法人根 buyerCo/sellerCo + 2 DEPARTMENT 子公司 buyerDiv/sellerDiv 使 `resolveLegalEntityRoot` walk-up 产出不同法人）。
  - 转移定价规则字段集核实：`ErpFinIntercompanyTransferPrice` mandatory=code/name/orgId/pricingMethod/isActive；trade-document 路径 `resolveCounterpartyLegalEntity:174-192` 仅按 fromOrgId/toOrgId/isActive 过滤（不读 pricingMethod/markupRate/materialId，Decision C 已核实）。
  - 配对凭证行结构核实：`generatePairedVouchers` AR 侧 Dr 1131/Cr 5001 + AP 侧 Dr 1401/Cr 2202，amount=order.totalAmountWithTax 直传 generator 不经 resolver（Decision C）；科目为 GlMappingResolver fallback 硬编码（fresh-DB 无 INTERCOMPANY_* gl_mapping_rule 种子 → 回落 1131/5001/1401/2202 均存于 erp_md_subject.csv 种子）。
  - 红冲行结构核实：`writeIntercompanyReversalFromLines:180-251` dcDirection 不变 + debit↔credit 互换 + amountSource/Functional=origDebit+origCredit 正数（对齐 commitment 红冲范式）。
  - 全链路回归影响：`runP2pChain`/`runO2cChain` 固定 orgId=2 + fresh-DB 无 transfer_price 种子 → counterparty=null → skip 零凭证；intercompany hook 两域均非阻塞 try-catch 无 release-hook 容错不对称问题（区别于承付 0410-2）。
- **Phase 2 spec 实现**（4 用例 + helper 扩展）：
  - `playwright.config.ts` webServer JVM args 追加 `-Derp-fin.intercompany-posting-enabled=true`（复用既有 inventory intercompany 总开关）。
  - `_helper.ts` 新增 `findIntercompanyVoucherIdByBillCode(page, billCode, billType, reversal)`——按 `ErpFinVoucherBillR.billType`（INTERCOMPANY_SALE/PURCHASE）+ reversalOfVoucherId 反查（intercompany 凭证无独立 postingType 且同 orderCode 含 COMMITMENT 凭证须按 billType 区分，区别于 BUDGET/COMMITMENT helper 按 postingType）。
  - 新建 `fin-intercompany-cross-company.action.spec.ts` 4 用例（**自包含跨法人 setup**）：建跨法人组织对 + 转移定价规则 + PO/SO 订单挂子公司 orgId。四组断言全绿：(1) 跨法人 PO approve → AR SALE（Dr 1131=60/Cr 5001=60）+ AP PURCHASE（Dr 1401=60/Cr 2202=60）配对凭证；(2) 跨法人 SO approve → 同型配对凭证 AR/AP 方向对称（SO 执行方=卖方，Decision C）；(3) reverseApprove → 原两张配对凭证 isReversed=true + 红冲凭证 dcDirection 不变/debit-credit 互换；(4) 同法人控制对照（种子 ERP-CO 无转移定价规则）→ 4 billType 全返回 null（counterparty=null skip 零凭证）。
- **Phase 3 owner doc 回链 + e2e-runbook + 日志**：
  - `docs/architecture/multi-company.md` §跨公司 PO/SO 触发路径 增「浏览器层验证」实现注记（自包含跨法人 setup 范式 + billType 反查 + 同法人控制对照 + config-gated 启用）。
  - `docs/testing/e2e-runbook.md` webServer JVM arg 段补 `intercompany-posting-enabled` + 业务动作表新增 finance 跨公司 Intercompany PO/SO 行 + spec 计数 91→92。
- **验证（full-green）**：`BASE_URL=http://127.0.0.1:8011 SKIP_WEBSERVER=1 npx playwright test tests/e2e/business-actions/fin-intercompany-cross-company.action.spec.ts` 4 passed（30.0s）+ 回归 26 passed（fin-commitment-accounting + fin-gl-mapping-routing + p2p-reverse-approve + 全 orchestration P2P/O2C/mfg chain/subcontract/variance/genealogy/inspection-gate，3.3m，0 新增失败——config 启用对既有链路零回归已证实）+ `mvn clean install -DskipTests` 154 模块 BUILD SUCCESS。
- **Skill**：`nop-testing`（Playwright 浏览器层 E2E + 既有 orchestration/_helper 复用 + config-gated 特性 webServer JVM arg 启用范式 + 自包含跨法人 setup，对齐 0410-2 同型先例）。

### 2026-07-26（承付会计三接入点浏览器层 E2E + 采购 release hook 容错 Fix — plan 0410-2）

- **任务**：执行 `docs/plans/2026-07-26-0410-2-commitment-accounting-browser-e2e.md` 全 3 Phase。承付会计（COMMITMENT 影子凭证）三接入点经 JUnit 单层验证（`TestErpFinBudgetCommitment` 4 场景 + `TestErpPurOrderCommitment` 3 场景）但零浏览器层 E2E；本计划补全栈验证并修复采购 release hook 容错缺口（latent defect rule 13）。
- **Phase 1 采购 release hook 容错 Fix + Explore**：
  - **Fix**（latent defect）：`ErpPurOrderProcessor.runCommitmentReleaseHook`（`module-purchase/erp-pur-service/.../processor/ErpPurOrderProcessor.java:241-256`）补 try-catch + LOG 字段，镜像销售 hook `ErpSalOrderProcessor.runCommitmentReleaseHook:359-370` 范式。catch `NopException` 后 LOG.debug 静默跳过，不阻断 reverseApprove/cancel 业务流。替代方案 `releaseIfPresent`（已含容忍语义）驳回——镜像销售 hook 保持两域对称。
  - **Explore 笔记**（写入 plan Execution Decision 段）：
    - 科目选定：2202 应付账款（id=5, LIABILITY/CREDIT）采购承付 + 5001 主营业务收入（id=6, INCOME/CREDIT）销售承付——种子已有 CREDIT 方向科目，无需追加种子行。
    - 红冲行结构核实：`writeReversalFromLines:184,222-245` 单行红冲 dcDirection 不变（CREDIT）+ debit↔credit 互换（debit=absAmount/credit=0）+ amountSource/Functional=origDebit+origCredit 正数。
    - 全链路回归影响：config 启用后 `runP2pChain`/`runO2cChain` PO/SO code 上额外产 COMMITMENT + 红冲凭证（独立 postingType/billType 不影响 NORMAL/BUDGET 断言）；既有 e2e 套件无 spec 在链路后反转采购订单（grep 全无命中）故 latent defect 未暴露。
  - purchase-service 局部 `mvn test` 全绿（116 测试 0 回归）。
- **Phase 2 spec 实现**（4 用例 + helper 扩展）：
  - `_helper.ts` 新增 `findCommitmentVoucherIdByCode(page, billCode, reversal)` 镜像 `findBudgetVoucherIdByCode:120`（postingType=COMMITMENT 替换 BUDGET + reversalOfVoucherId 区分正向/红冲）。
  - `cleanupP2p`/`cleanupO2c` 扩展追加 `cleanupVoucherByBillCode(r.codes.po/so)` 清理承付凭证回链（既有 helper postingType-agnostic 已覆盖 COMMITMENT，仅补调用点）。
  - `runP2pChain`/`runO2cChain` PO/SO head 追加 `totalAmount/totalTaxAmount/totalAmountWithTax`（commit hook 读 head.totalAmountWithTax 字段，原 null 致 SPI early-return 不产凭证；设含税额 56.5/113 与 invoice 一致）。
  - `playwright.config.ts` webServer JVM args 追加 `-Derp-fin.budget-commitment-enabled=true -Derp-fin.budget-commitment-subject-code=2202 -Derp-fin.budget-commitment-sales-subject-code=5001`。
  - 新建 `fin-commitment-accounting.action.spec.ts` 4 用例（**两组 setup 隔离接入点 #3 干扰**）：(A) order-only setup 隔离 #3 专测 #1+#2；(B) full-chain runP2pChain/runO2cChain 验证 #3 + 容错回归。四组断言全绿：(1) 订单审核 commit 单行 CREDIT 凭证（采购 2202 credit=50 / 销售 5001 credit=100）；(2) 订单反审核 release 原凭证 isReversed=true + 红冲行 dcDirection 不变 CREDIT/debit-credit 互换；(3) 发票审核 release full-chain 承付原凭证 isReversed=true + 红冲凭证存在；(4) 采购 release hook 容错回归 full-chain 后 reverseApprove 不再抛 ERR_BUDGET_COMMITMENT_ALREADY_RELEASED。
- **Phase 3 owner doc 回链 + e2e-runbook + 日志**：
  - `docs/design/finance/budget.md` §承付会计 增「浏览器层验证」实现注记（helper 镜像 BUDGET + order-only/full-chain 双 setup 隔离范式）+「release hook 容错对称性」注记（采购 Fix 对齐销售）。
  - `docs/design/finance/posting.md` §承付（COMMITMENT）实际过账 增「浏览器层验证」交叉引用。
  - `docs/testing/e2e-runbook.md` 业务动作表新增 finance 承付会计三接入点行 + webServer JVM arg 段补 `budget-commitment-enabled` + 两 subject-code + 已知限制（config 启用后既有链路额外生成 COMMITMENT 凭证）+ spec 计数 90→91。
  - `docs/bugs/2026-07-26-0410-pur-commitment-release-hook-tolerance-asymmetry.md` 登记采购 release hook 容错缺口 latent defect（现象/根因/Fix/预防）。
- **验证（full-green）**：`mvn clean install -DskipTests` 154 模块 BUILD SUCCESS（1:37 min）+ `mvn test -pl module-purchase/erp-pur-service` 116 测试全绿 + `BASE_URL=http://127.0.0.1:8011 SKIP_WEBSERVER=1 npx playwright test tests/e2e/business-actions/fin-commitment-accounting.action.spec.ts` 4 passed（30.1s）+ 回归 33 passed（P2P/O2C/mfg chain + voucher-back-link + fin-gl-mapping-routing + fin-budget-control + fin-expense-claim-budget + pur-return + ct-rebate-accrual，4.6m，0 新增失败）。
- **Skill**：`nop-backend-dev`（Phase 1 Processor release hook Fix + ErrorCode 守卫 + 跨实体 release 调用容错）+ `nop-testing`（Phase 2 Playwright 浏览器层 E2E + helper 镜像扩展 + config-gated 特性 webServer JVM arg 启用范式 + 双 setup 隔离）。

### 2026-07-26（GL Mapping 凭证科目路由浏览器层 E2E — plan 0410-1 closure）

- **任务**：执行 `docs/plans/2026-07-26-0410-1-gl-mapping-voucher-routing-browser-e2e.md` 收尾（Phase 3 item 2 + Closure）。前序会话已完成 Phase 1（Explore 字段/缓存/accountKey 核实）+ Phase 2（spec 实现 `tests/e2e/business-actions/fin-gl-mapping-routing.action.spec.ts` 三组断言全绿）+ Phase 3 item 1（`gl-mapping-rules.md` §9 落地）。本会话补完唯一未完成项 Phase 3 item 2（e2e-runbook 文档对齐）并 Closure。
- **Phase 3 item 2 执行**（doc-only，零生产代码变更）：
  - `docs/testing/e2e-runbook.md` webServer JVM arg 段补 `-Derp-fin.gl-mapping.org-dimension-enabled=true`（`playwright.config.ts:18` 实际已有此 arg，仅 runbook 文档缺失，现补齐文档说明：orgId 维度精确匹配 cache 分桶 + GL Mapping hook 本身无条件 active 仅 orgId 维度由此 config 门控）。
  - 业务动作表新增 finance GL Mapping 凭证科目路由行（`fin-gl-mapping-routing.action.spec.ts`，三组断言：命中覆盖 Dr 1401=50 / 控制对照 Dr 1403=50 / orgId 维度 org=1 非匹配保留默认）。
  - 业务动作 spec 计数同步 89→90（3 处：概览 + 分层运行表 + 业务动作章节头，finance 已在 17 域内故域数不变）。
- **Closure**：Plan Status → completed；Phase 3 Status → completed + item 2/Exit Criteria 勾选 [x]；Closure Gates 全勾选。无 `> Source Audits:` 段（roadmap-sourced plan），跳过 source audit 关闭步骤。
- **范围纪律**：本会话零生产代码变更（纯测试 + 文档计划，spec 由前序 Phase 2 落地，本会话仅 runbook 文档对齐 + 计划勾选 + 日志）。1 runbook 文档更新 + 1 计划文件勾选 + 1 新日志条目。
- **Skill**：`none`（纯文档对齐，Phase 3 Skill 标注为 none）。
- **复验（mission-driver re-execute）**：closure audit 反馈后复验全仓库。`mvn clean install -DskipTests` 154 模块 BUILD SUCCESS（2:18 min）+ `PLAYWRIGHT_PORT=8011 npx playwright test tests/e2e/business-actions/fin-gl-mapping-routing.action.spec.ts` 2 passed（20.6s：命中覆盖 Dr 1401=50 / orgId 维度 org=1 非匹配保留默认 1403）。全 19 项 [x] 一致、Closure Audit Evidence 6 项证据文件均经实时仓库核实存在且内容正确（spec 7236 bytes / gl-mapping-rules.md §9 行 490-537 / e2e-runbook.md 行 55+119+322 / 本日志条目）。零额外代码变更。
- **full-green verification（mission-driver build verify）**：`mvn install -DskipTests` BUILD SUCCESS（exit 0，无 Java 变更增量复验）+ `mvn test` BUILD SUCCESS（全 154 模块单元测试 0 failures / 0 errors）+ 前序 e2e 2 passed → unit tests + e2e 双绿 full-green 状态确认。

### 2026-07-26（C3 PRIORITY/STACKABLE helper + sales 定价 3 实体接入 — plan 0315-1）

- **任务**：执行 `docs/plans/2026-07-26-0315-1-c3-priority-stackable-helpers-sales-rollout.md` 全 3 Phase。C3 日期范围有效性模式（owner doc `docs/design/date-ranged-validity-pattern.md`）原仅 MUTEX 策略落地，本计划扩展 PRIORITY + STACKABLE 两策略 helper 并接入 sales 定价 3 实体。
- **Phase 1 helper 扩展**：
  - 新增 `ErpDateRanges.pickHighestPriority(List<T extends IDateRange>, Comparator<T>)`（纯函数，调用方提供优先级方向 Comparator）+ `ErpDateRangeOverlapValidator.enforceStackableAware(candidate, existing, errorCode, selfId, Predicate<T> isStackable)`（混合策略：双非 stackable 重叠抛异常，任一方 stackable=true 允许重叠）。
  - 2 Decision 裁决（PRIORITY 签名 (a) Comparator 入参 / STACKABLE 语义 (a) stackable=true 表达「我可被叠加」）。
  - `TestErpDateRanges` 29→42 单测场景（+13：pickHighestPriority 5 + enforceStackableAware 8），master-data service 108→122 测试全绿（0 回归）。
- **Phase 2 sales 3 实体接入**：
  - `ErpSalPriceList`（PRIORITY warn-only）— `ErpSalPriceList implements IDateRange` + `defaultPrepareSave/Update` 钩子调 `effectiveOn + pickHighestPriority` 检测歧义，LOG.warn 不阻断。
  - `ErpSalPriceListLine`（MUTEX）— `implements IDateRange` + 钩子调 `enforceMutex(ERR_SAL_PRICE_LIST_LINE_OVERLAP)`。
  - `ErpSalPricingRule`（STACKABLE 混合）— 因 ORM 生成基类 getter `final + Timestamp 返回类型` 与 `IDateRange.LocalDate` 不兼容，按 owner doc §8.1「跨域接入适配器」范式在 BizModel 内构造 `PricingRuleDateRange implements IDateRange` 适配器（`Timestamp.toLocalDateTime().toLocalDate()` 截断），调 `enforceStackableAware(ERR_SAL_PRICING_RULE_OVERLAP, PricingRuleDateRange::getStackable)`。
  - 3 新错误码（`ERR_SAL_PRICE_LIST_LINE_OVERLAP` / `ERR_SAL_PRICING_RULE_OVERLAP` / `ERR_SAL_PRICE_LIST_PRIORITY_AMBIGUOUS`）。
  - sales-service pom：master-data-service 由 test 提升为 compile 依赖（C3 helper 跨域调用，DAG 无环）。
  - `TestErpSalDateRange`（NEW，10 集成测试：3 priceListLine MUTEX + 2 priceList PRIORITY + 5 pricingRule STACKABLE），sales-service 125→135 测试全绿（0 回归，定价引擎取价逻辑不受影响）。
- **Phase 3 owner doc EXPAND + roadmap 同步**：
  - `docs/design/date-ranged-validity-pattern.md`：§4.1 PRIORITY 策略段（pickHighestPriority + 调用范式 + PRIORITY vs MUTEX 对比表）+ §4.2 STACKABLE 混合策略段（Decision (a) 裁决 + 替代方案否决）+ §7 sales 接入记录 + §10 sales 3 实体 RELEASED。
  - `docs/design/sales/README.md`：新增「日期范围有效性校验（C3 交叉引用）」段（3 实体策略映射 + TIMESTAMP 适配 + 「不改变取价逻辑」声明）。`pricing-engine.md` 不存在，按 AGENTS.md 规则不主动新建文件。
  - `docs/backlog/deepening-roadmap.md` §8.6：Deferred successor 标注 PRIORITY/STACKABLE helper + sales 子集 RELEASED by 0315-1。
- **执行期发现**：(1) plan baseline 称「sales-service 经传递依赖可达 master-data-service helper」不准——master-data-service 原 test-scope，已提升 compile；(2) Phase 2 Decision (a)「ErpSalPricingRule implements IDateRange + getter 截断」因 Java final+返回类型不兼容不可行，改走适配器范式（语义等价）；(3) `pricing-engine.md` 不存在，C3 交叉引用段落地于既有 sales/README.md。3 项偏离均已在 plan + owner doc 记录。
- **验证**：master-data service 122 tests + sales-service 135 tests 全绿；全 workspace `mvn clean install -DskipTests` 154 模块 BUILD SUCCESS。
- **Skill**：`nop-backend-dev`（BizModel delta 钩子 + 跨实体 helper 调用 + ErrorCode 规范）。

### 2026-07-26（gen-control → ORM domain + control.xlib 收敛 — plan 1430-1）

- **任务**：执行 `docs/plans/2026-07-25-1430-1-gen-control-domain-mapping-convergence.md` Phase 4（视觉回归验证）。前序会话已完成 Phase 1（全量分类审计）/ Phase 2（control.xlib 扩展 + D 类 col domain 补齐）/ Phase 3（629 块 R/D 类 gen-control 移除）。本会话执行唯一未完成 Phase 4 并 Closure。
- **Phase 4 执行**：
  - `mvn clean install -DskipTests` BUILD SUCCESS（154 模块，view.xml 编译校验通过，runner jar 重建）。
  - Playwright 视觉回归：field-format.value.spec（F6 千分位/precision/date）+ status-tag.visual.spec（F5 状态着色 C 类）+ sensitive-masking.visual.spec（脱敏 C 类）**20/20 passed**——直接核证 gen-control → control.xlib 映射等价（金额千分位 / precision:2 / precision:8 rate / YYYY-MM-DD date 全渲染正确）。其余 DOM 视觉套件 71 passed / 10 failed。
  - **10 failed 经 git stash 基线对照裁决为非回归**：`git stash` gen-control 变更（966 块还原）+ 重建 + 重跑，10 项 failure 完全复现（ext-domains-child-table 5 项「row action button should appear」hover 时序 + material-customs/party-search-picker 3 项 GraphQL `[Map]不是对象类型` 后端 schema + f12 drawer 2 项）。均为预存缺陷/测试基础设施问题，与 gen-control 列格式化无关（gen-control 仅触及 `<col>` 显示属性，不触及 grid row-action/drawer/GraphQL schema）。`git stash pop` 还原 + 重建。
  - CRUD 写路径 + 业务动作 E2E 抽样 3 域（inventory/purchase/sales）**10/10 passed**（inventory.write child-table input-number + stock-move/sal-return/pur-return 业务动作状态机 + 过账副作用），核证 edit-amount/edit-quantity/edit-unitPrice 表单编辑控件 precision/step 功能正常。
- **Closure**：Plan Status → completed；独立 closure audit 子代理（新会话 `ses_06580ca1dffeRgenE2qLGIFeEM`）**VERDICT: PASS**（7/7 claims 经实时仓库核实）。父计划 `2026-07-24-2200-1` §Phase 5 gen-control Deferred 标 successor completed。`docs/design/field-formatting-patterns.md` 增 Decision (e)「control.xlib 自动映射优先于 gen-control」约定。
- **范围纪律**：本会话零生产代码变更（view.xml/control.xlib 变更为前序 Phase 1-3 落地，本会话仅验证 + 文档对齐 + 计划勾选）。1 设计文档新增 Decision (e) + 2 计划文件更新（1430-1 Closure + 2200-1 Deferred RELEASED）+ 1 新日志。
- **Skill**：`nop-testing`（Playwright 视觉回归 + stash 基线对照裁决法）。

### 2026-07-26（plan 1430-1 提交前复验 — mission-driver verify）

- **复验**：提交前重跑 `mvn clean install -DskipTests`（全模块 reactor SUCCESS，无 FAILURE/SKIPPED/ERROR）+ `mvn test`（BUILD SUCCESS，0 失败/0 错误）。零生产代码新增，复证 Phase 1-3 落地的 view.xml/control.xlib 变更构建与单测全绿。
