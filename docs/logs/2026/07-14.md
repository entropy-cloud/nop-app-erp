# Development Log - 2026-07-14

### 2026-07-14（projects 工时/结算过账生命周期 + 凭证行数值浏览器层 E2E — plan 2026-07-14-0742-2）

- 执行 `docs/plans/2026-07-14-0742-2-projects-posting-lifecycle-voucher-line-e2e.md`，全 2 Phase 完成。源：`2026-07-14-0215-1` 同形 Deferred「全 18 域全业务动作覆盖（DIRECT 域剩余）」（Successor Required: yes，触发条件已满足：projects 域工时过账 + 结算过账为 DIRECT `@BizMutation` 浏览器层可达但零 E2E）+ 0704-1 凭证行数值断言范式；AGENTS.md 当前重点「各域细化端到端验证」。收口 projects 域过账凭证行断言完全缺失（工时/结算均无 posted E2E）。
- **Phase 1（工时单过账生命周期 + PROJECT_COST_COLLECTION 凭证行断言）**：
  - `Decision | Explore` 裁定：costAmount 确定性经 `timesheet.costRate` 单填驱动（CostRateResolver 优先级 1，>=0 即采纳），**无需** employee rate / config `default-labor-cost-rate` 回退（hours 8 × costRate 100 = 800）。**额外必填发现**：timesheet.orgId 虽 ORM 非 mandatory，但 TimesheetPostingDispatcher 经 `resolveAcctSchemaId(timesheet.getOrgId())` 解析 acctSchema，orgId=null → schemas=0 → voucherId=null → posted=false；故 setup 须显式置 orgId=2。
  - `projects-timesheet-posting.action.spec.ts`（1 测试）：submit(UNSUBMITTED→SUBMITTED)+costRate/costAmount 派生 → approve(SUBMITTED→APPROVED+posted=true) → **PROJECT_COST_COLLECTION 凭证行 Dr 5101=800/Cr 2211=800** + 2 非法守卫（UNSUBMITTED→approve / APPROVED→submit）→ cancel(APPROVED→UNSUBMITTED+posted=false) → **REVERSAL 红字 Dr 5101=-800/Cr 2211=-800**。后端日志确认 voucherId=100010 schemas=1 + 红冲 voucherId=100016。
- **Phase 2（结算过账生命周期 CLOSE + 凭证行断言 + 跨域资产卡片）**：
  - `Decision | Explore` 裁定：CLOSE 路程可达不降级。PnL 快照硬前置（先 refreshPnl 产 CALCULATED 快照，createSettlement 缺快照抛 ERR_SETTLEMENT_PNL_SNAPSHOT_MISSING）；finalCost 确定性经自包含 CostCollection(docStatus=OPEN erp-prj/project-status 合法值非 APPROVED)+Line(MATERIAL,1000)→refreshPnl→totalCost=1000→finalCost=1000。CLOSE approve 跨域持久化资产卡片（createAndActivateAsset → IErpAstAssetBiz.save IN_SERVICE），reverseSettlement 仅 rollbackAsset status→DRAFT 不删除卡片，spec cleanup 显式 deleteById(ErpAstAsset)。
  - `projects-settlement-posting.action.spec.ts`（1 测试）：refreshPnl→CALCULATED+totalCost=1000 → createSettlement(CLOSE)→UNSUBMITTED+transferToAsset+finalCost=1000 + 非法守卫（UNSUBMITTED→approve）→ submit→SUBMITTED → approve→APPROVED+posted+assetCardId(IN_SERVICE,originalValue=1000) → **PROJECT_SETTLEMENT 凭证行 Dr 1601=1000/Cr 1603=1000** → reverseSettlement→posted=false+asset DRAFT → **REVERSAL 红字 Dr 1601=-1000/Cr 1603=-1000**。后端日志确认 voucherId=100028 schemas=1 provider=ProjectSettlementAcctDocProvider + 红冲 voucherId=100032。
  - **cleanup 健壮性修复**：首次 Phase 2 运行 setup 失败（docStatus=APPROVED 非法字典项）致孤儿 OPEN 项目污染 projects dashboard（openProjectCount 1→2）；重构两 spec 将 setup 置于 try 内 + finally 全量 null 守卫清理（任一阶段失败亦清除已建产物），fresh-DB 后 dashboard 基线无漂移。
- **基础设施**：种子 `erp_md_subject.csv` +3 行（5101 项目成本 id=32 / 2211 应付职工薪酬 id=33 / 4103 本年利润 id=34）；webServer JVM arg 追加 `-Derp-prj.default-payroll-subject-id=2211`（科目编码非种子行 ID——config 键名含 `-id` 后缀但消费方按 code 解析，与借方 defaultSubjectId=数字ID 非对称）。
- **验证（full-green）**：`SKIP_WEBSERVER=1 PLAYWRIGHT_PORT=8011 npx playwright test tests/e2e/business-actions/projects-timesheet-posting.action.spec.ts tests/e2e/business-actions/projects-settlement-posting.action.spec.ts --workers=1` **2 passed**（15.6s）+ 回归抽样 projects/assets dashboard value（fresh-DB 后 openProjectCount=1/originalValue 基线无漂移，2+1 passed）。`mvn clean install -DskipTests`（154 模块 BUILD SUCCESS，1:26）。纯测试 + 种子 + config，零生产 Java/契约/ORM 模型变更。
- **mission verify（full-green verification）**：`mvn clean install -DskipTests`（154 模块 BUILD SUCCESS）+ `mvn test`（全 reactor 0 failures / 0 errors）全绿，确认 Java 基线未受影响（本计划零 Java 变更）。
- 文档对齐：`docs/testing/e2e-runbook.md` 业务动作表 +projects 工时/结算过账 2 行 + 套件计数 46→48 spec + webServer JVM arg 段 + payroll-subject + 种子 COA 段 +5101/2211/4103；`docs/backlog/README.md` +2026-07-14-0742-2 ✅ done 行。
- Plan Status: active → completed；全 2 Phase [x] + Status completed + Closure Gates 全 [x]（独立结束审计已执行）。

### 2026-07-14（assets 过账凭证行精确数值浏览器层 E2E 断言 — plan 2026-07-14-0742-1）

- 执行 `docs/plans/2026-07-14-0742-1-assets-posting-voucher-line-numeric-e2e.md`，Phase 1 全部完成。源：`2026-07-14-0215-1` Follow-up「assets 凭证行精确数值断言 successor」（触发条件已满足：0215-1 已交付 4 spec 全部断言 posted=true + voucherId 非空）。收口 assets 作为唯一「过账 happy-path E2E 已落地但凭证行级数值断言缺失」的核心域。
- **Phase 1（4 spec × 5 过账类型凭证行数值断言叠加）**：在 0215-1 既有 4 个 assets 业务动作 spec 内叠加凭证行精确数值断言（subjectCode + dcDirection + debitAmount/creditAmount），复用 `findVoucherIdByBillCode` + `assertVoucherLines` 两原语零生产代码变更。
  - `ast-depreciation.action.spec.ts`：DEPRECIATION 正向凭证行 Dr 6602=333.3333/Cr 1602=333.3333（金额=(12000−0)/36 SCALE=4 HALF_UP，经 executed.voucherId 直用）+ reverse 红冲同向取负 Dr 6602=−333.3333/Cr 1602=−333.3333（经 `findVoucherIdByBillCode(assetCode#period,'REVERSAL')`）。
  - `ast-cip-capitalization.action.spec.ts`：CAPITALIZATION 正向 Dr 1601=800/Cr 1603=800（金额=CostItem 汇总 PURCHASE 500+OTHER 300，经 `findVoucherIdByBillCode(capCode,'NORMAL')`）+ reverseTransfer 红冲 REVERSAL Dr 1603=−800/Cr 1601=−800。
  - `ast-inventory-count.action.spec.ts`：ASSET_INVENTORY_ADJUSTMENT-SHORTAGE 正向 Dr 6711=500/Cr 1601=500（金额=shortageAmount=bookValue，经 `findVoucherIdByBillCode(invCode,'NORMAL')`）+ reverse 红冲同向取负。
  - `ast-maintenance.action.spec.ts`：MAINTENANCE_EXPENSE 正向 Dr 6602=300/Cr 1002=300（金额=LABOR 200+SPARE_PART 100）+ MAINTENANCE_CAPITALIZATION 正向 Dr 1601=250/Cr 1002=250（金额=SUBCONTRACT 250）+ 两路径 reverse 红冲同向取负。
- **验证（full-green）**：`PLAYWRIGHT_PORT=8011 npx playwright test tests/e2e/business-actions/ast-*.action.spec.ts --workers=1` 8 测试全绿（plan Closure Gates 记录）。纯测试 + 文档，零生产代码/契约/ORM 模型变更。
- **mission verify（full-green verification）**：`mvn clean install -DskipTests`（154 模块 BUILD SUCCESS，1:30）+ `mvn test`（全 reactor 0 failures / 0 errors，7:27）全绿，确认 Java 基线未受影响（本计划零 Java 变更）。
- 文档对齐：`docs/testing/e2e-runbook.md` 凭证行断言表 +5 assets 行 + assets spec 描述注记 + 套件计数注记（套件总数不变）；`docs/backlog/README.md` +2026-07-14-0742-1 ✅ done 行。
- Plan Status: completed（草案审查 + 独立结束审计 PASS WITH NOTES 均已完成）；Phase 1 [x] + Closure Gates 全 [x]。

### 2026-07-14（到岸成本分摊 + 维修备件消耗 GL 过账生命周期浏览器层 E2E — plan 2026-07-14-0606-2）

- 执行 `docs/plans/2026-07-14-0606-2-landed-cost-spare-part-posting-e2e.md`，全 2 Phase 完成。源：`2026-07-10-1100-3`（到岸成本引擎 S-3 completed，零生命周期浏览器层 E2E）+ `2026-07-10-1100-6`（备件消耗 GL 过账 S-6 completed，零浏览器层 E2E）；R14 bundling justification：两 Phase 共享同一凭证行数值断言范式，合并收口「posting-landed-but-E2E-missing」缺口类别。
- **Phase 1（到岸成本审核生命周期 + LANDED_COST 过账 E2E）**：
  - `Decision | Explore` 裁定：**自包含 __save setup（非 runP2pChain）**。runP2pChain 硬编码 `SEED.MAT_1`，物料不可控——复用会为 MAT_1 在 WH-RAW 新增余额行且 CostAdjust 修改 avgCost，污染 inventory dashboard totalValue 基线。裁定：建测试专用物料（RAW_MATERIAL, MOVING_AVERAGE）+ `__save` 预置 APPROVED Receive（docStatus=ACTIVE erp-pur/doc-status, approveStatus=APPROVED，无业务副作用——__save 不触发审批工作流，对齐后端 TestErpInvLandedCostEndToEnd#seedReceive 范式）+ generateMove INCOMING 建库存余量。billHeadCode 模式核实：`landedCost.getCode()` 无后缀（LandedCostPostingDispatcher.java:69）。
  - `inv-landed-cost.action.spec.ts`（1 测试）：allocate @BizQuery 分摊预览 `List<Map>` 非空（allocatedAmount/materialId）→ approve @BizMutation 审核三步编排（分摊→CostAdjust→LANDED_COST(490) 过账）→ approveStatus=APPROVED + docStatus=DONE + posted=true + **LANDED_COST 凭证行 Dr 1401=50/Cr 2202=50** + CostAdjust avgCost 更新断言（MOVING_AVERAGE path 更新 StockBalance.avgCost）+ APPROVED→approve 非法守卫（ERR_LANDED_COST_ALREADY_APPROVED）。
- **Phase 2（维修备件消耗 confirm + MAINTENANCE_ISSUE 过账 E2E）**：
  - `Decision | Explore` 裁定：**测试专用备件物料隔离**（对齐 0704-2 mfg-chain 范式），避免清理抹除种子余额污染 inventory dashboard totalValue 基线。设备用种子 id=1（EQ-2026-001 RUNNING）——confirm 不改变设备状态（设备状态联动仅 Visit start/complete），无污染风险。billHeadCode 模式核实：`usage.code + "-MI"`（MaintenanceIssuePostingDispatcher.java:93,129）。
  - `mnt-spare-part-posting.action.spec.ts`（1 测试）：confirm(usageId) → SparePartIssueService.issue（OUTGOING 移动 ERP_MNT_SPARE_PART business-linked auto-DONE）→ docStatus=ACTIVE + posted=true + OUTGOING 移动单 docStatus=DONE + **MAINTENANCE_ISSUE 凭证行 Dr 6602=50/Cr 1403=50**（config `erp-mnt.spare-part-posting-enabled=true` 门控启用）。
  - webServer JVM arg 追加 `-Derp-mnt.spare-part-posting-enabled=true`（`playwright.config.ts`）。config-gated 关闭路径（posted=true 无凭证）经后端单测 TestErpMntSparePartPosting 场景 3 覆盖（Deferred watch-only residual）。
- **验证（full-green）**：`SKIP_WEBSERVER=1 PLAYWRIGHT_PORT=8011 npx playwright test tests/e2e/business-actions/inv-landed-cost.action.spec.ts tests/e2e/business-actions/mnt-spare-part-posting.action.spec.ts --workers=1` **2 passed**（15.2s）。纯测试 + config（JVM arg）+ 文档，零生产 Java/契约/ORM/种子变更。
- **mission verify（full-green verification）**：`mvn clean install -DskipTests`（154 模块 BUILD SUCCESS，1:28）+ `mvn test`（全 reactor 0 failures / 0 errors，7:57）全绿，确认 Java 基线未受影响（本计划零 Java 变更）。
- 文档对齐：`docs/testing/e2e-runbook.md` 业务动作表 +inventory landed-cost/maintenance spare-part 2 行 + 套件计数 44→46 spec / 207→209 / 306→308 测试；`docs/backlog/README.md` +2026-07-14-0606-2 ✅ done 行。
- Plan Status: active → completed；全 2 Phase [x] + Status completed + Closure Gates（独立结束审计 gate 待审计回填）。

### 2026-07-14（finance 预算方案生命周期 + 预算控制 hook 浏览器层 E2E — plan 2026-07-14-0606-1）

- 执行 `docs/plans/2026-07-14-0606-1-finance-budget-lifecycle-control-e2e.md`，全 2 Phase 完成。源：`2026-07-10-1100-4`（预算后端引擎 S-4 completed，零浏览器层 E2E successor）；AGENTS.md 当前项目重点「各域细化端到端验证」。
- **Phase 1（预算方案生命周期 + BUDGET 影子凭证）**：
  - `Decision | Explore` 裁定：BudgetVoucherGenerator.toFact 读 `budgetLine.subjectId → ErpMdSubject.direction` 决定借贷，金额取 `budgetAmountFunctional`；自包含 setup = scenario(DRAFT, controlLevel=NONE) + 2 行预算（DEBIT 6601 id=8 + CREDIT 5001 id=6 同 periodId=1）。**关键修正**：`approveStatus` 走 `wf/approve-status` 字典（UNSUBMITTED/SUBMITTED/APPROVED/REJECTED，**无 DRAFT**），初始态须 UNSUBMITTED（后端 ORM 设 DRAFT 仅绕过 GraphQL 字典校验，浏览器层 __save 受字典校验约束，首跑 `非法的字典项:DRAFT` 暴露）。
  - 新增 `findBudgetVoucherIdByCode(page, billCode, reversal)` 原语（`tests/e2e/orchestration/_helper.ts`）：既有 `findVoucherIdByBillCode` postingType 仅 `'NORMAL'|'REVERSAL'` 不能表达 BUDGET；正向/红冲 BUDGET 凭证同 postingType=BUDGET，按 `reversalOfVoucherId IS NULL/IS NOT NULL` 区分。
  - `fin-budget-scenario.action.spec.ts`（5 测试）：submit/approve/reject/cancel 状态翻转 + **BUDGET 凭证行数值断言**（正向 Dr 6601=100/Cr 5001=100，红冲同向取负 -100）+ 原正向凭证 isReversed=true + 2 非法迁移守卫（DRAFT→approve / APPROVED→submit 各「不允许此操作」）。
- **Phase 2（预算控制 hook + getBudgetVsActual）**：
  - `Decision | Explore` 裁定：触发点 = `ErpPurOrderProcessor.validateBusinessRulesForApprove → runBudgetCheckHook`（config 双门控 budget-check-enabled + budget-purchase-expense-subject-code=6601）；controlLevel 来自 scenario 实体字段（非 config）。**关键裁定（ControlLog 持久性）**：HARD 路径 `writeControlLog(BLOCKED)` 与 throw 同处 `@BizMutation` approve 事务，NopException 触发事务回滚 → BLOCKED 日志**不持久化**（断言 findPageTotal==0，经运行验证；与后端 `TestErpPurBudgetControlIntegration.testPurchaseOrderHardBlocked` 不断言日志一致）；WARN 路径 approve 提交事务 → WARNED 日志**持久化**（findFirst actionResult=WARNED，经运行验证）。
  - `fin-budget-control.action.spec.ts`（3 测试）：(a) HARD 阻断负路径 PO totalAmountWithTax=200 > 预算 100 → approve 抛 ERR_BUDGET_EXCEEDED「预算超支」+ approveStatus 保持 SUBMITTED + 无 ControlLog 持久化；(b) WARN 放行正路径 → APPROVED + ControlLog WARNED 持久化；(c) getBudgetVsActual @BizQuery 返回 BudgetVsActualRow 结构（复杂类型须显式 selection set，callQuery 不带 selection 报错故直接构造 query）+ budgetAmount>=setup 断言。
  - webServer JVM arg 追加 `-Derp-fin.budget-check-enabled=true -Derp-fin.budget-purchase-expense-subject-code=6601`（`playwright.config.ts`）。向后兼容：无匹配 APPROVED 预算方案维度 check 返回 PASS。
- **验证（full-green）**：`PLAYWRIGHT_PORT=8011 npx playwright test tests/e2e/business-actions/fin-budget-*.action.spec.ts --workers=1` **8 passed**（1.0m）；预算 config 启用后 `tests/e2e/orchestration/` **17 passed**（2.2m，含 P2P PO approve 向后兼容回归）。Playwright webServer 期望 8080 但 application.yaml `quarkus.http.port=8011`，须 `PLAYWRIGHT_PORT=8011`。纯测试 + config（JVM arg）+ 文档，零生产 Java/契约/ORM/种子变更。
- **mission verify（full-green verification）**：`mvn clean install -DskipTests`（154 模块 BUILD SUCCESS）+ `mvn test`（全 reactor 0 failures / 0 errors）全绿，确认 Java 基线未受影响（本计划零 Java 变更，2026-07-13 全绿基线再确认）。
- 文档对齐：`docs/testing/e2e-runbook.md` 业务动作表 +finance budget 2 行 + 套件计数 42→44 spec / 199→207 + 298→306 测试 + webServer JVM arg 文档 2 项。
- Plan Status: active → completed；全 2 Phase [x] + Status completed + Closure Gates（独立结束审计 gate 待审计回填）。

### 2026-07-14（aps + b2b + logistics 域 DIRECT 业务动作浏览器层 E2E — plan 2026-07-14-0508-1）

- 执行 `docs/plans/2026-07-14-0508-1-aps-b2b-logistics-direct-action-e2e.md`，全 3 Phase 完成。源：`2026-07-14-0215-1` Deferred「aps / b2b / logistics 域 DIRECT 业务动作 E2E」successor（触发条件已满足：按域推进剩余 DIRECT 业务动作浏览器层覆盖时；三域均无 useWorkflow/useApproval，纯 DIRECT @BizMutation，webhook/批量扫描动作排除）。
- **Phase 1（aps 排产方案状态机 + 工序排产引擎）**：
  - `Decision | Explore` 裁定：scheduleForward/scheduleBackward **自包含 setup 可达不降级**。理由：(1) `ErpApsOperationOrder.workOrderId/machineId` 列无 FK 约束（仅 BIGINT），Java 集成测试 TestErpApsSchedulingEngine 用任意 1L/100L 即可；(2) `ErpApsSchedulingEngine` 纯算法 POJO capacity=1 单工位，无工作中心日历/产能配置依赖；(3) `loadPendingOrders` 按 status=DRAFT + earliestStartDateT ∈ [horizonStart,horizonEnd] 过滤，horizon 内工序必被纳入。
  - `aps-schedule.action.spec.ts`（3 测试）：publish/archive 状态机 happy path DRAFT→PUBLISHED→ARCHIVED + archive-from-DRAFT 合法路径（archive 允许 DRAFT|PUBLISHED）+ ARCHIVED→publish 非法守卫 ERR_APS_SCHEDULE_ILLEGAL_STATUS token「不允许执行该操作」。
  - `aps-operation-order.action.spec.ts`（2 测试）：scheduleForward（SchedulingResult.feasible=true + scheduledOperationIds 非空 + op DRAFT→PLANNED + plannedStart/EndDateT 写回）+ scheduleBackward（需 latestEndDateT，同上断言）。
- **Phase 2（b2b EDI 信封状态机）**：
  - `Decision | Explore` 裁定 **(b)**：`createOutbound` happy-path 经 provider.generatePayload 跨域查 `ErpSalInvoice` by code（缺失抛 ERR_B2B_EDI_PARSE_FAILED）。跨域耦合度高违反单域隔离原则，**createOutbound 入口降级为 watch-only residual**：经 `__save` 预置 TO_SEND 态 ErpB2bEdiDoc（formatId=null）直接测后续状态迁移；createInbound 无跨域依赖落 spec 测 RECEIVED→archive 正路径。
  - `b2b-edi-doc.action.spec.ts`（5 测试）：(a) 出站正向链 TO_SEND→markSent(SENT)→markAcknowledged(ACKNOWLEDGED)；(b) 失败重试 TO_SEND→markError(ERROR)→retry(TO_SEND+retryCount++)；(c) 取消 TO_SEND→cancel(CANCELLED)；(d) 入站 createInbound(RECEIVED,formatCode=null 不查 format)→archive(ARCHIVED)；(e) 非法守卫 ACKNOWLEDGED→markSent / CANCELLED→retry 各抛 ERR_B2B_EDI_DOC_ILLEGAL_TRANSITION。
  - **B1 修复核实**：ErpB2bEdiDoc 状态字段为 `state`（非 `status`，ORM column name="state" app-erp-b2b.orm.xml:173），spec verifyState 全用 `state` 无回归。
- **Phase 3（logistics 发运单状态机）**：
  - `Decision | Explore` 裁定 **(a)**：`MockCarrierGatewayClientFactory`（gatewayId="mock"）已注册为 bean（_vfs/erp/log/beans/app-service.beans.xml），默认 failureMode=SUCCESS。自包含建 ErpLogCarrier(gatewayId="mock")+ErpLogShipment 即可让 MockClient.completeDeliveryOrder 返回 success + 写回 trackingNo/labelUrl。**三链完整可达，不降级**。
  - `log-shipment.action.spec.ts`（4 测试）：(a) happy forward DRAFT→advise(ADVISED)→completeShipment(DISPATCHED+trackingNo/labelUrl 写回)；(b) cancel from ADVISED；(c) cancel from DISPATCHED（经 mock client.cancelShipment）；(d) 非法守卫 CANCELLED→advise / DRAFT→completeShipment 各抛 ERR_LOG_SHIPMENT_ILLEGAL_TRANSITION。
- **验证（full-green）**：`PLAYWRIGHT_PORT=8011 npx playwright test tests/e2e/business-actions/aps-*.action.spec.ts tests/e2e/business-actions/b2b-*.action.spec.ts tests/e2e/business-actions/log-*.action.spec.ts --workers=1` **14 passed**（1.8m）；`PLAYWRIGHT_PORT=8011 npx playwright test tests/e2e/business-actions/ --workers=1` **103 passed**（12.4m，含 14 新增 + 89 既有测试无回归）。Playwright webServer 期望 8080 但 application.yaml `quarkus.http.port=8011`，须 `PLAYWRIGHT_PORT=8011`。纯测试+文档，零生产代码/契约/ORM/种子/config 变更。
- 文档对齐：`docs/backlog/README.md` +2026-07-14-0508-1 ✅ done 行；`docs/testing/e2e-runbook.md` 业务动作表 +aps/b2b/logistics 4 行 + 套件计数 38→42 spec / 14→17 域 / 284→298 测试。
- **独立结束审计**：经独立子代理（ses_0a28e9ae5ffeZeKJUhq05yFVGj，新会话）审计 Verdict: **PASS**。计划内部一致性 / 4 spec 存在+测试数 / 3 Explore Decision 落地 / 关键正确性（b2b `state` 字段 + logistics mock 网关注册 + aps SchedulingResult @DataBean 字段）/ 4 Deferred 项目分类 / 无反模式（无自我审计、无静默移除、无范围蔓延——Non-Goal 动作经 grep 确认全部不出现在 spec 中）均经验证。
- Plan Status: active → completed；全 3 Phase [x] + Status completed + Closure Gates 全 [x]（含独立结束审计 gate，已由本审计回填 evidence）。

### 2026-07-14（hr 域 DIRECT 业务动作浏览器层 E2E — plan 2026-07-14-0215-3）

- 执行 `docs/plans/2026-07-14-0215-3-hr-direct-action-e2e.md`，全 2 Phase 完成。源：`2026-07-09-2004-1` Deferred「全 18 域全业务动作覆盖（DIRECT 域剩余）」successor（触发条件已满足：当前项目重点各域细化端到端验证，hr 为 Tier-1 未覆盖域，45 个 @BizMutation 数量最多；14 域已覆盖含同批 assets/contract/drp）。
- **Phase 1（薪酬引擎 + 薪酬模拟）**：
  - `Decision | Explore` 裁定：calculateSalary 最小预置集 = ErpHrEmployee(ACTIVE) + ErpHrEmploymentContract(ACTIVE, monthlySalary) + ErpHrSocialInsuranceBase(cityCode) + ErpHrSocialInsuranceConfig(HOUSING_FUND, emp/coRate) + ErpHrTaxConfig(year, taxBrackets 七级累进 JSON)。SocialInsuranceCalculator 仅需 ≥1 config（HOUSING_FUND 即可，社保项可 0）+ HousingFund config 必需；IncomeTaxCalculator 需 TaxConfig(year)。**薪酬模拟源 salary**：直接 `__save` 源 ErpHrSalary(2025-6, PENDING) 作模拟源期间——runPayroll 遍历 ALL active 员工（含种子 HR-EMP-001/002 缺合同/社保配置）会抛错，不可行；adjustItem/convertToFormal 经 recalculateWithOverrides → incomeTaxCalculator 需 TaxConfig(目标年 2026)。**已知约束**：adjustmentReason VARCHAR(20)+ext:dict 须用合法 dict 值 SALARY_CHANGE；applyBatchAdjustment 返回 Map<String,Object> GraphQL 不支持字段选择须原始 mutation 无选择集。
  - `hr-payroll.action.spec.ts`（3 测试）：calculateSalary DIRECT 计算触发（gross/net 非空 + UNSUBMITTED/PENDING，配置链全预置）+ voidSalary 回退（PENDING→VOID）；markPaid negative-path 守卫（UNSUBMITTED salary→ERR_SALARY_ILLEGAL_STATUS_TRANSITION，硬守卫 approveStatus==APPROVED 浏览器层不可达）；generateBankFile negative-path 守卫（无 APPROVED 行→ERR_NO_APPROVED_SALARY_FOR_BANK_FILE）。
  - `hr-salary-simulation.action.spec.ts`（4 测试）：createSimulation(DRAFT 冻结源快照)→adjustItem(basicSalary 10000→12000 gross 重算)→submitForReview(IN_REVIEW)→approve(APPROVED)→convertToFormal(CONVERTED + 正式 salary 回链) 正向链；reject 路径(IN_REVIEW→REJECTED)；applyBatchAdjustment FIXED（返回 Map 原始 mutation 无选择集，affectedCount=1）；非法迁移守卫(DRAFT→approve ERR_HR_SIMULATION_ILLEGAL_TRANSITION + DRAFT→submitForReview 无调整项 ERR_HR_SIMULATION_NO_ADJUSTMENT)。
- **Phase 2（招聘漏斗 + 休假审批 + 考勤打卡）**：
  - `hr-recruitment.action.spec.ts`（4 测试）：6 动作状态机 OPEN→moveToScreening→SCREENING→scheduleInterview→INTERVIEW→makeOffer→OFFERED→hire→HIRED + employeeId 回写 + ErpHrEmployee/ErpEmploymentContract 自动创建联动（findFirst 反查 ACTIVE）；reject(OPEN→REJECTED)/close(→CLOSED)；非法守卫(HIRED→moveToScreening ERR_RECRUITMENT_ILLEGAL_STATUS_TRANSITION)。
  - `hr-leave-attendance.action.spec.ts`（4 测试）：休假审批 DRAFT→submit→SUBMITTED→approve→APPROVED→cancel→CANCELLED（LeaveBalance null 代码容忍不阻断）+ reject 路径 + 日期重叠守卫(ERR_LEAVE_DATE_OVERLAP)；考勤 clockIn(创建当日记录)+clockOut(workHours 派生)+重复 clockIn 守卫(ERR_ALREADY_CLOCKED_IN)。
- **验证（full-green）**：`npx playwright test tests/e2e/business-actions/hr-*.action.spec.ts --workers=1` **15 passed**（50.9s）；`npx playwright test tests/e2e/business-actions/ --workers=1` **89 passed**（10.7m，含 15 新增 hr 测试 + 74 既有测试无回归）。纯测试+文档，零生产代码/契约/ORM 模型变更。
- 文档对齐：`docs/backlog/README.md` +2026-07-14-0215-3 ✅ done 行；`docs/testing/e2e-runbook.md` 业务动作表 +4 hr 行 + 套件计数 269→284 / 38 spec 14 域。
- Plan Status: active → completed；全 2 Phase [x] + Status completed + Closure Gates 全 [x]（独立结束审计 gate 留待审计轮次，执行者未自我审计）。

### 2026-07-14（contract + drp 域 DIRECT 业务动作浏览器层 E2E — plan 2026-07-14-0215-2）

- 执行 `docs/plans/2026-07-14-0215-2-contract-drp-direct-action-e2e.md`，全 2 Phase 完成。源：`2026-07-09-2004-1` Deferred「全 18 域全业务动作覆盖（DIRECT 域剩余）」successor（触发条件已满足：各域细化端到端验证，contract/drp 为 Tier-1 未覆盖域，两域均无 useWorkflow/useApproval，纯 DIRECT @BizMutation 浏览器层可达）。
- **Phase 1（contract 合同生命周期 + 版本管理）**：
  - `ct-contract-lifecycle.action.spec.ts`（4 测试）：6 动作状态机 正向链 save(NEGOTIATION 入口，DRAFT→NEGOTIATION 无 @BizMutation 经 `__save` 直置)→activate→ACTIVE→suspend→SUSPENDED→resume→ACTIVE→terminate→TERMINATED；expire 路径(ACTIVE→EXPIRED)；amend 路径(ACTIVE→DRAFT + 新建修订版本 ErpCtContractVersion 回链)；非法守卫(DRAFT/TERMINATED→activate 抛 ERR_CT_ILLEGAL_STATUS_TRANSITION token「不允许执行该操作」)。activate 经 validateTypeDirectionCombo（SALES+OUTBOUND）放行。
  - `ct-contract-version.action.spec.ts`（2 测试）：finalizeVersion(DRAFT→FINALIZED)→signVersion(FINALIZED→SIGNED + isCurrent 翻转)；非法守卫(SIGNED→finalizeVersion 须 DRAFT)。
- **Phase 2（drp 净需求引擎 + 安全库存）**：
  - `drp-plan-engine.action.spec.ts`（2 测试）：runDrp(DRAFT→COMPUTED + ErpDrpLine 净需求行生成，net=safetyStock=100)→resetToDraft(COMPUTED→DRAFT + SUGGESTED 行清理)→重算→approvePlan(COMPUTED→APPROVED)；非法守卫(DRAFT→approvePlan 须 COMPUTED，token「不允许此操作」)。orgId=2（`__save` 强制 org FK 校验）+ DrpDemandAggregator 按 orgId 聚合参数行。
  - `drp-safety-stock.action.spec.ts`（1 测试）：calculate(calculatedSafetyStock 非空 + lastCalculatedAt 写回)→confirmWriteback(overrideSafetyStock=50 回写 ErpDrpParameter.safetyStock 0→50，三元组 (mat4,wh2,org2) 区别 plan-engine 精确命中)。
- **执行期修复引擎缺陷**：`SafetyStockEngine.monthlyDemands()`（`module-drp/erp-drp-service/.../safetystock/SafetyStockEngine.java:211`）无历史出库时 `moves.isEmpty()` 分支返回空列表，致 `mean()` 除零 `ArithmeticException`，使 `calculate` 在自包含无 posted 历史场景浏览器层不可达。对齐既有 `result.isEmpty()→add(ZERO)` 意图，改为返回 `[BigDecimal.ZERO]`（无历史=零需求）。关键约束：种子 stock_move 全 posted=false，且 `__save` 强制 `posted=false`（过账须经 complete 派发链，可能优雅降级），故无法经直建 posted 移动提供历史——引擎除零修复是使 action 可达的正确路径（非测试 workaround）。
- **验证（full-green）**：`mvn clean install -DskipTests` 154 模块 BUILD SUCCESS；`mvn test -pl module-drp/erp-drp-service` **30 passed**（含 TestErpDrpSafetyStock 6，0 回归）；`npx playwright test ct-* + drp-* --workers=1` **9 passed**（1.1m）。Playwright webServer 期望 8080 但 application.yaml `quarkus.http.port=8011`，须 `PLAYWRIGHT_PORT=8011`（+ `SKIP_WEBSERVER=1` 复用手启实例）。
- 文档对齐：`docs/backlog/README.md` +2026-07-14-0215-2 ✅ done 行；`docs/testing/e2e-runbook.md` 业务动作表 +4 contract/drp 行 + line 113 套件计数 260→269 / 22→26 业务动作 spec。
- Plan Status: active → completed；全 2 Phase [x] + Status completed + Closure Gates 可证项 [x]（独立结束审计 gate 留待审计轮次，执行者未自我审计）。

### 2026-07-14（assets 域 DIRECT 业务动作浏览器层 E2E — plan 2026-07-14-0215-1）

- 执行 `docs/plans/2026-07-14-0215-1-assets-direct-action-e2e.md`，全 2 Phase 完成。源：`2026-07-09-2004-1` Deferred「全 18 域全业务动作覆盖（DIRECT 域剩余）」successor（触发条件已满足：当前项目重点各域细化端到端验证，assets 为 Tier-1 未覆盖域，inventory/crm/cs/maintenance/projects/quality/manufacturing/purchase/sales/finance 10 域已覆盖）。
- **Phase 1（折旧引擎 + CIP 转固链）**：
  - `Decision | Explore` 裁定：实测种子 `erp_md_subject.csv` 缺 5 个 assets 过账 Provider 默认科目码（Depreciation 6602/1602、AssetInventory 1601/6301、Capitalization-CIP 1601/1603、Maintenance 1601/6602）。按 1800-1/0413-2 范式补齐 5 行（id 27-31: 1601 固定资产/1602 累计折旧/1603 在建工程/6301 营业外收入/6602 折旧费用）。无 webServer JVM arg 追加——各 Dispatcher 均硬编码默认科目码（经 `findByCode` 解析），非 config 驱动。补齐后 DEPRECIATION/CAPITALIZATION/ASSET_INVENTORY_ADJUSTMENT/MAINTENANCE_EXPENSE/MAINTENANCE_CAPITALIZATION 过账 happy-path 可达（posted=true），解除优雅降级。
  - `ast-depreciation.action.spec.ts`（2 测试）：executeDepreciation → schedule EXECUTED + STRAIGHT_LINE 数学 (12000-0)/36=333.3333 + 资产卡片 accumulatedDepreciation/netBookValue 回写 + posted=true → reverseDepreciation REVERSED + 卡片回退；非法守卫（无 schedule reverse 拒绝 + 不存在资产拒绝）。
  - `ast-cip-capitalization.action.spec.ts`（1 测试）：startConstruction→addCostItem(PURCHASE 500+OTHER 300)→addProgressBilling(400)→transferToAsset(TRANSFERRED+completedAssetId+资产原值800)→reverseTransfer(IN_CONSTRUCTION)；资本化单 posted=true（CAPITALIZATION 过账 Dr 1601/Cr 1603）。
- **Phase 2（盘点 + 维修生命周期）**：
  - `ast-inventory-count.action.spec.ts`（2 测试）：8 动作链 createInventory→submitForCount→reconcile→processVariance(SHORTAGE→SCRAPPED)→approve→post(POSTED+ASSET_INVENTORY_ADJUSTMENT Dr 6711/Cr 1601)→reverse(RECONCILING)；cancel 异常路径 + CANCELLED→submitForCount 守卫。范围隔离经自包含建测试资产类别+资产。MATCHED-only 零差异不产凭证故须实际差异达 POSTED。行 actualQuantity 更新经 `__update`（`__save` 拒绝已存在 id）。
  - `ast-maintenance.action.spec.ts`（3 测试）：(a) EXPENSE 路径 post→MAINTENANCE_EXPENSE 凭证(Dr 6602/Cr 1002)+reverse；(b) CAPITALIZE 路径 post→资产原值增量(1000+250)+MAINTENANCE_CAPITALIZATION 凭证(Dr 1601/Cr 1002)+reverse 资产回退；(c) cancel 路径 + IN_PROGRESS→cancel 守卫。
- **验证（full-green）**：`mvn clean install -DskipTests` 154 模块 BUILD SUCCESS（01:25）；`npx playwright test tests/e2e/business-actions/ast-*.action.spec.ts --workers=1` **8 passed**（58.3s，0 回归）；回归抽样 assets/finance dashboard value + ast-depreciation report value **3 passed**（种子科目补齐纯加性，0 回归）。
- e2e-runbook 业务动作表 +4 行（assets 4 spec）；种子 COA 段 +0215-1 补齐 5 assets 科目注记。
- 源 plan `2026-07-09-2004-1` Deferred「全 18 域全 DIRECT 业务动作覆盖」assets 部分标记已覆盖（11 域 done）。
- Plan Status: active → completed；全 2 Phase [x] + Status completed + Closure Gates 全 [x]。

### 2026-07-14（委外生命周期浏览器层 E2E 扩展 — plan 2026-07-14-0035-2）

- 执行 `docs/plans/2026-07-14-0035-2-subcontract-lifecycle-e2e-extension.md`，全 3 Phase 完成。源：`2026-07-13-0701-2` Deferred「委外生命周期浏览器层 E2E 扩展（MRP 释放 / 多行发料 / 部分收货 / 红冲）」successor（触发条件已满足：0701-2 正向链+非法迁移守卫已落地，本计划扩展 4 场景）。
- **Phase 1（MRP 释放→自动建单）**：`playwright.config.ts` webServer JVM arg 追加 `-Derp-mfg.subcontract-release-enabled=true`；`_helper.ts` 新增 `runSubcontractMrpRelease`/`cleanupSubcontractMrpRelease`/`SUBCONTRACT_MRP_EXPECT`（建 FINISHED_PRODUCT 物料 → MRP 计划 + SUBCONTRACT_REQUEST 行 → `ErpMfgMrpPlanLine__releaseSubcontractRequest` → 断言 `SUB-MRP-{lineId}` APPROVED 骨架单 processingFee=0/totalAmount=0 + 单行 qty=plannedQuantity + 计划行 isFirmed + plan status=FIRMED）。
- **Phase 2（多行发料 + 部分收货）**：`runSubcontractChain(page, options?)` 扩展 `MultiLineSubcontractOptions`（`lineCount` N 独立组件物料 + N 行 + N 备货移动；`stopAfterIssue` 发料后停）。`SubcontractResult`/`cleanupSubcontract` 重构为数组形态（componentMats/orderLines/setupMoves），保持单行向后兼容。多行 test 断言 3 StockMoveLine + Dr 1408 汇总 30 / Cr 1401 按物料分列 3 行（`SubcontractIssueAcctDocProvider` 镜像 ManufacturingIssue：Dr 汇总/Cr 按物料分列，故多行凭证行数 = 1+N 非 2）。部分收货 test 断言 receivedQty<lineQty → RECEIVED + MANUFACTURE 移动行 quantity=receivedQty。
- **Phase 3（cancel 路径）**：cancel 正路径 test（DRAFT/SUBMITTED/APPROVED→CANCELLED）+ cancel 守卫 test（单链 ISSUED→RECEIVED→COMPLETED 逐态 cancel 拒绝 + docStatus 不变）。
- **种子数据回归修复（plan 0035-1 引入，非本计划范围但阻塞 fresh-DB 启动）**：`app-erp-all/src/main/resources/_vfs/_init-data/erp_md_subject.csv` 重复主键 id=24（1408 委外物资 与 1416 制造差异-委外 冲突，0035-1 commit 03a7d449 新增 1416 时误用已占用 id=24）→ fresh-DB `-Dnop.orm.init-database-data=true` 初始化抛 `nop.err.orm.save-entity-replace-existing-entity` 启动失败。修复：1416 id 24→26（科目码引用不受影响；posting/voucher 按 subjectCode 匹配，非 id）。0035-1 的 `mvn test` full-green 未捕获此回归（测试框架 init 路径与 fresh-DB 服务端 init 路径不同）。
- **验证（full-green）**：`mvn clean install -DskipTests` 154 模块 BUILD SUCCESS（01:34）；`PLAYWRIGHT_PORT=8011 npx playwright test mfg-subcontract-chain` **7 passed**（既有 2 + 新增 5，57.1s，0 回归）。
- 源 plan `2026-07-13-0701-2` Deferred「委外生命周期 E2E 扩展」successor 标记为 部分已解除（MRP 释放/多行/部分收货/cancel 已落地；仅 reverseProcess 红冲仍 Deferred，后端未实现）。
- e2e-runbook 委外段表格新增 `mfg-subcontract-chain.spec.ts` 行（config-gated posting + release，7 测试）。
- Plan Status: active → completed；全 3 Phase [x] + Status completed。

### 2026-07-14（制造委外差异类型 SUBCONTRACT 落地 — plan 2026-07-14-0035-1）

- 执行 `docs/plans/2026-07-14-0035-1-manufacturing-subcontract-variance-type.md`，全 3 Phase 完成。源：`2026-07-13-0455-2` Deferred「subcontract 委外差异（ProductionVarianceCalculator SUBCONTRACT 差异类型）」successor（触发条件已满足：委外引擎 0455-1 + 委外费归集 0455-2 已落地，标准侧 `ErpMfgCostRollupLine.subcontractCost` 与实际侧 `ErpMfgWorkOrder.subcontractCost` 列均就位但差异引擎从未消费）。
- **6 层一致性闭合**：(1) `erp-mfg/variance-type` 字典补 `SUBCONTRACT`（委外费差异）码（`module-manufacturing/model/app-erp-manufacturing.orm.xml:152`，经 `mvn clean install` 增量重生成 `_app.orm.xml:135` 镜像）；(2) `ErpMfgConstants.VARIANCE_TYPE_SUBCONTRACT` 常量；(3) `ProductionVarianceCalculator` 第 6 类差异（标准 = `rollupLine.subcontractCost × 完工量`；实际 = `wo.subcontractCost`；仅当标准侧或实际侧非零时生成行，对齐既有「零差异不生成行」范式）；(4) `ProductionVarianceDispatcher` 第 4 要素桶（SUBCONTRACT 净差异聚合 + 全零守卫 + buildEvent）；(5) `ProductionVarianceAcctDocProvider` 科目对 1416（制造差异-委外）/ 1417（在制品-委外），方向与既有 3 要素一致；(6) `variance-analysis.md` + `costing-methods.md` 设计文档段（successor 注解 → 已落地标记 + 实现注记段）。
- **种子 COA**：`app-erp-all/.../_init-data/erp_md_subject.csv` 新增 1416/1417 科目行（对齐 1410-1415 范式）。
- **测试**：`TestErpMfgProductionVariance` 新增 3 测试——`testSubcontractVarianceGeneratedWhenNonZero`（6 类差异行 + SUBCONTRACT 金额精确断言）、`testSubcontractLineOmittedWhenBothSidesZero`（两侧零→5 类不变）、`testSubcontractVariancePosting`（SUBCONTRACT 净差异 +1 → PRODUCTION_VARIANCE 凭证 Dr 1416/Cr 1417 精确数值断言）。既有 7 测试零回归（subcontractCost 未设→guard 跳过→仍 5 行）。
- **验证（full-green）**：`mvn clean install -DskipTests` 154 模块 BUILD SUCCESS（01:28 min）；`mvn test -pl module-manufacturing/erp-mfg-service` **118 tests, 0 Failures, 0 Errors**（原 115 + 新增 3）；全 reactor `mvn test` **BUILD SUCCESS（07:26 min，0 Failures, 0 Errors）— full-green verification**。
- 源 plan `2026-07-13-0455-2` Deferred「subcontract 委外差异」successor 标记为 ✅ 已收口。
- Plan Status: active → completed；全 3 Phase [x] + Status completed。
