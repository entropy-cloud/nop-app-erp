# Development Log - 2026-09-05

### 2026-09-05（mission ai-check-r3 验证 run：mvn test 假红根因 = 兄弟仓快照重建漂移报表 CSS 基线，3 用例单行修补后全 reactor 全绿）

- **mission-driver 验证 run：全 reactor `mvn clean install -DskipTests` BUILD SUCCESS（01:44）+ `mvn test` BUILD SUCCESS（14:07，2026-09-05 22:24）**——首跑 `mvn test` 在 `app-erp-all` 3 个 renderHtml 集成用例 `check-output-fail` 假红（`TestErpC21ApsCapacityLoad` / `TestErpC15CrmLeadForecast` / `TestErpC16CsSlaNotification`）；本仓工作树零代码变更（脏面仅 docs）。根因：兄弟仓 `../nop-entropy` 当日重建 `nop-report-core` 2.0.0-SNAPSHOT（jar mtime 10:14，近期 commit 为报表 PDF 导出视觉审计），xpt-report CSS emit 修复 `line-height:normal}}`（非法：缺分号双括号）→ `line-height:normal;}`（合法），层 2 HTML 快照基线随之失配；数据行逐字节一致。修复 = 3 个 `_cases/**/output/*_response.json5` 单行外科对齐（`forceSaveOutput` 全量重录之不用，取「最小修补 + CHECKING 复跑反证等价」路径）：3 类复跑 3/3 绿 → 全 reactor `mvn test` 全绿。详见 bug note `docs/bugs/2026-09-05-2224-nop-entropy-snapshot-rebuild-report-css-baseline-drift.md`（含「SNAPSHOT 依赖 = 隐式字节级耦合」未来注意事项）。e2e 未在本 run 命令面内；同日闭包 run 的 e2e 交付面 71/71 全绿证据见上条。

### 2026-09-05（plan 2026-09-03-0400-1 关闭：M2.1 CRUD 像素断言扩面独立结束审计 APPROVE + Plan Status completed + roadmap M2.1 done）

- **mission-driver 闭包 run：plan `2026-09-03-0400-1-m21-crud-page-pixel-snapshot-expansion` 全计划闭合，Closure Gates 8/8 勾选**——闭包验证（fresh runner `./scripts/start-app.sh start`，fresh-DB 重置 12s ready）：交付面 `crud-pages.snapshot.spec.ts` + `material-customs.visual.spec.ts` **71/71 全绿（10.0m）**；`nop-compliance-checker.sh` **exit 0（R2c=1542**，与 known-good-baselines 2026-09-04 基线行零漂移）；`git diff --stat tests/e2e/visual/` 为空 = tracked 快照基线零修改。独立结束审计 **APPROVE**（fresh session `ses_f8e37faf9ffezxGV2CaeMAhTGo`，0 Blocker / 0 Major / 2 Minor 非阻塞——m-1 交付 commit message 标注 mission 批次 id 属行政卫生项、溯源未断；m-2 roadmap §M2 台账行 69 全量归属 successor，已随闭包补 44+25 归属澄清闭环）：审计独立复验计划 271 行、crud-pages 69 PNG ↔ 69 测试 1:1（`--list`）、`_helper.ts` 纯增量（引入 commit 0 行删除）、material-customs 修复形态恰 2 处、runbook §CRUD 像素层计数、checker exit 0、运行时探针 2/2 绿（18.8s）、工作树脏面仅限计划+日志两文件、文本一致性。收口落盘：plan `## Closure` Status Note + 审计证据 + Plan Status `completed`；roadmap `comprehensive-test-data-and-visual-coverage-roadmap.md` M2.1 行 `ready`→`done`（证据随行）+ §M2 台账归属澄清。验证状态：交付面全绿 + checker 零漂移（目录级三连跑证据见同日 Phase 3 条目）。

### 2026-09-05（plan 2026-09-03-0400-1 Phase 3 完成：M2.1 CRUD 像素断言扩面三连跑稳定 + 合规零漂移 + owner doc 对账闭合）

- **mission-driver 执行 run：plan `2026-09-03-0400-1-m21-crud-page-pixel-snapshot-expansion` Phase 3（末 Phase）执行完毕，全 3 Phase 项 + Exit Criteria 勾选闭合**——① 三连跑（M0.3 §3 稳定判据）：fresh runner（`./scripts/start-app.sh start` fresh-DB 重置 11s ready）后三轮 `BASE_URL=http://127.0.0.1:8011 SKIP_WEBSERVER=1 npx playwright test tests/e2e/visual/ --workers=1`，每轮 **299 测试 = 242 passed + 6 skipped + 51 failed**（58.7m/58.6m/58.6m），三轮失败集去时长后缀后 diff 为空（逐字节稳定），51 = 四族台账 A5+B34+D12 精确对账（失败 spec 14 文件全在本计划变更面外），交付面 crud-pages 69 + material-customs 2 + f13 三轮 100% 绿；三轮全程 `tests/e2e/visual/**-snapshots/` 零基线修改（`_exploration/` 采集副产物按设计刷新后还原）。② 合规：`nop-compliance-checker.sh` exit 0，R2c=1542 与 known-good-baselines 2026-09-04 基线行精确一致（本计划零 Java 变更）。③ owner doc：e2e-runbook.md 视觉段计数 + CRUD 像素层段 + `_exploration/` 口径说明（第五次重跑已落盘 commit `66ca8da9e`）本轮 `--list` 实测对账一致（dashboards 19 / reports 24 / crud-pages 69（PNG 1:1）/ business-actions 22 / exploration 8 测试 3 spec）；Draft Review Record 快照重录合规声明追记「对账复核」行（基线已入库，提交后 `git diff --stat` 仍空）。④ bug note 0945（material-customs findPage 非法参数）状态 fixed 在案，本轮复跑 2/2 绿实证。余项：独立结束审计 + Plan Status closure + roadmap M2.1 `ready`→`done`（同日后续条目）。

### 2026-09-05（plan 2026-08-25-1956-3 执行：R1-E1-2 审批邻接 xbiz auth 补齐 117 处落地 + census 归零 + 运行时 Proof 绿）

- **mission-driver 执行 run：plan `2026-08-25-1956-3` 全 3 Phase 执行完毕**——Phase 1 Decision 逐实体核验（38/39 实体三权限点齐备：`:mutation`=生成 FNPT、`:approve`/`:reverseApprove`=delta FNPT；唯一例外 `ErpCsTicket` 按预裁决 fail-closed admin-only）+ 映射表落盘 `docs/testing/permissions-enforcement-approval-adjacent-xbiz-auth-mapping.md`（39 文件 × 3 mutation，语义核验无状态机例外、无越权放大）；Phase 2 保留层 xbiz 39 文件 117 处 `<mutation>` 首子元素补 `<auth permissions="..."/>`（pur 8 / sal 7 / ast 6 / mfg 3 / prj 3 / qa 5 / fin 2 / mnt 2 / cs 1 / hr 1 / inv 1，E1.1 先例位置 xbix.xdef:35 同款），xmllint 全绿；Phase 3 census 复验 **195/195 全携带 `<auth>`、117/117 与冻结映射表 diff 一致、零缺 auth mutation**。运行时 Proof：新 spec `tests/e2e/negative/e1-3-approval-adjacent-auth.smoke.spec.ts` **11/11 绿**（负向 restricted 三动作族 × pur/sal/prj 全拒 NO_PERMISSION + cs 例外实体 restricted/审核人双主体 fail-closed 预期正确断言 + submitForApproval 零 `:mutation` 种子 fail-closed admin-only 边界断言；正向 admin 三动作族全通过 + 持权业务角色 审核人/项目经理 reject、业务「管理员」withdrawApproval 全通过），既有 E1.1/E1.2 spec 15/15 零回归。全 reactor `mvn clean install -DskipTests` + `mvn test` BUILD SUCCESS（failures=0）。owner doc `roles-and-permissions.md` §action-level 补 R1-E1-2 注记；roadmap R1-E1-2 勾选闭合。种子边界注记：`:mutation` 全 39 实体零角色种子 → `submitForApproval` 对业务角色 fail-closed admin-only（映射复用既有权限点、不新增 FNPT/种子 = 计划 Non-Goal 边界内既有姿态）。

### 2026-09-05（plan 2026-08-25-1956-2 关闭：Phase 2 item 1 按 successor 交接勾选 + roadmap R1-E1-1 done）

- **mission-driver 执行 run：plan `2026-08-25-1956-2` 全 Phase 项勾选闭合**——Phase 2 item 1（md 正向可见性 Proof）退出标准依赖本仓不可产出的保护区设计决策（fixStatus 塌缩语义裁决涉 nop-entropy），按关闭协议 tick `[x]` + `successor: <pending-plan-id>` 交接注记（断言留作缺口回归信号）；活仓复核通过（7 文件斜杠=0、逗号 12/6/4/4/3/3/3=35、commit a1b09d627 在案、md 源 auth 0 FNPT 种子、(e) 断言在 spec）。roadmap `permissions-enforcement-roadmap.md` R1-E1-1（E1 发现项 1）同步 tick done 并更正「保持未勾选」注记。验证（本 run 实测）：全 reactor `mvn test` BUILD SUCCESS exit 0（13:35 min，failures=0 / errors=0 / 1 skipped）；`nop-compliance-checker.sh` exit 0，16/19 规则 ==`## BASELINE` 块，预存漂移 R2b+2 / R2c+5 / R12a+1（本计划零生产代码变更，归因此前落地计划，successor 基线裁决——见 plan Phase 2 执行收尾 run 行）。

### 2026-09-05（plan 2026-08-25-1956-2 Phase 2 执行：md 正向断言运行时阻塞 + 菜单可见性设计缺口发现）

- **plan `2026-08-25-1956-2` Phase 2 执行结论：item 2 回归半 + item 3 完成，item 1（md 域正向菜单可见性）运行时阻塞**——Phase 1 逗号修正必要但不充分，需设计决策（详见 `docs/design/roles-and-permissions.md` P1.5a 注记更正段）。
- **根因（四层证据链，in-process 反射诊断复现）**：`SiteMapProviderImpl.filterAllowedMenu` 的 `containsRole`/`applyAuthFilter` 对 erp-md/erp-pur 判定**正确放行**（`resourceToRoles[erp-md]` 含 13 角色、erp-pur 含 采购员，直接调用后 status=1）——真正拒绝点在 `SiteResourceBean.removeInactive().fixStatus()`（nop-biz-auth-api）：**全部子菜单资源 inactive 时父资源自底向上置 DISABLED**。per-entity SUBM 按 P1.5a 设计不挂 `roles=`、依赖 FNPT cascade-up，md 全域 **0 FNPT**（grep 实证）→ per-entity 层恒 denied → 整域塌缩 → erp-md 对全部 13 业务角色（含业务「管理员」）不可见。
- **跨域拒绝面矩阵（in-process 全 20 角色×filterAllowedMenu 实测，enforcement ON）**：既有拒绝（与分隔符无关）= 采购员→erp-pur / 销售员→erp-sal / 作业员·生产计划员→erp-mfg / 质检员→erp-qa / 维护人员→erp-mnt + erp-md 全员；可见对照 = fin/inv/ast/prj/mnt-主管/mfg-主管/qa-主管/hr/ct×2/b2b×2/审核人→pur+sal（其域 FNPT 种子含该角色可级联）。排除项：Unicode NFC/NFD、DB 绑定（nop_auth_user_role 全对）、JWT claims、session cacheData（全 null 与可见性无关——Bypass 路径在内存 context）。
- **负向套件复跑（flux，52 pass / 10 fail）**：9 fail 与 roadmap E2 工作项白名单清单一一对应（e2-1×2/e2-2×2/e2-3×3 + role-login known-impact×2——后者 `ErpMdCurrency__findPage` 无 FNPT 种子 deny 同根因），**零新增回归、零可见面翻转**；1 fail = 本 plan (e) 断言（item 1 阻塞本体，红灯即缺口回归信号）。
- **设计缺口归属**：消除需三选一决策（per-entity SUBM 挂种子 / 补 per-action FNPT 种子 / 平台 fixStatus 语义裁决——末项涉 nop-entropy 保护区）+ owner doc 决策修订，按 auto + dual-agent-approval 另行立项；本 plan Non-Goals（仅分隔符修正）不含此变更。下一步：以本发现起草 successor 计划。

### 2026-09-05

- **mission `ai-check-r3` 验证 run（plan `2026-08-25-1956-1` 结束审计后的 build/test 复核）全绿**：`mvn clean install -DskipTests` BUILD SUCCESS（156 reactor 模块，01:44 min）；全 reactor `mvn test` **4006 tests / 0 failures / 0 errors / 1 skipped**（exit 0）。两个此前登记为预存失败的用例本轮实测转绿：`TestErpHrDepartmentPositionDeleteGuard`（8/8）与 `TestErpDrpCrossDock`（14/14）——较 2026-09-04 基线「一致或更优」；后续基线行可将该 2 项从 Known Failures 摘除。E2E 不在本 run 命令集，未运行。工作树遗留的 roadmap closure 变更面（ct/crm/cs/hr flux 公式页面修复 5 文件 + 视觉像素扩面 spec/快照基线 + docs 收口）按批拆 3 commit 落盘；plan `2026-08-25-1956-1` ledger `## Verification` 追加本 run `test` pass 行（`compliance` 键本 run 未运行，不写）。
