### BIGINT 主键/外键 stdDataType=string 改造工具（docs-for-ai 规则落地准备）

- **背景**：nop-entropy `docs-for-ai/02-core-guides/orm-model-design.md` 主键设计节新增强制规则——BIGINT 主键/外键必须显式 `stdDataType="string"`（JS long 精度问题，见 `../nop-entropy/ai-dev/logs/2026/08-13.md`）。本仓库 18+1 域模型当前全部为 `stdDataType="long"`，需集中改造。
- **工具**：新增 `tools/check-bigint-id-types.mjs`（Node，沿用 tools/ 正则解析约定），三模式：
  - `scan`：盘点所有 module-* / model 下 orm.xml 的 PK（primary="true"）+ FK（`*Id` 命名 / `*_ID` 编码 / to-one join leftProp 三路判定），报告 stdSqlType/stdDataType；to-one join 交叉校验（leftProp 必为 FK、rightProp 必为引用实体主键），0 告警即查找完整；notGenCode 外部引用实体单独标注。
  - `dry-run`：生成修改副本到 `_tmp/bigint-id-string-fix/`，xmllint 校验 XML、重扫副本确认零残留、幂等检查——**不修改任何源文件**。
  - `apply --yes`：审核后回写源文件（未执行）。
- **验证结果（scan 全绿）**：19 文件、PK 463 / FK 1182（含 notGenCode stub）、BIGINT 主/外键 1605 全部需改 string、其他类型 40（VARCHAR 等，n/a）、校验告警 0、domain 冲突 blocker 0（PK/FK 列均显式携带 stdDataType，无 domain 覆盖风险，已核 `OrmModelInitializer.syncDomains`）。
- **遗漏排查（第二轮加固）**：① 未分类 BIGINT 列 368 个全列出供排查 = 351 delVersion（保持 long 正确）+ 12 fileSize/durationMs（业务数值）+ 约 20 个 `*By/*To/*Person` 孤儿操作人列（`assignedTo`/`completedBy`/`acceptedBy`/`requestedBy`/`reconciledBy`/`calibratedBy` 等，无 to-one join，属既有建模问题——按 docs-for-ai 列域设计应 `stdDomain="userId"`，记 follow-up 待人工裁决，不在本次规则范围）；② 实测全部 1973 个 BIGINT 列属性顺序统一（stdSqlType 在前）+ 多行列标签 0 个，但 `fixTag` 仍加固为防御性（仅 BIGINT 才改）+ 5 个合成用例验证（stdDataType 在前 / 多行属性 / 缺 stdDataType 插入 / 幂等 / 非 BIGINT 不动）全 PASS。
- **计数自洽核对（第三轮）**：主键 `id` 463（全 BIGINT）+ 外键 `xx_id` 1182（BIGINT 1142 + 非 BIGINT 40）+ PK+FK 双角色 0 = 主外键合计 1645；实际修改 stdDataType 列 1605 = BIGINT 主键 463 + BIGINT 外键 1142，与 dry-run "修改列: 1605" 一致、无重复列。独立 grep 交叉验证：`id` BIGINT PK 463 ✓、`*Id` BIGINT FK 1140 ✓，差额 2 = `calibratedBy` ×2（ErpMntCalibration/ErpQaCalibration）——非 `*Id` 命名的 join 外键，命名约定 grep 会漏、工具 join 交叉校验不漏。分解核对表已固化进 `scan` 汇总输出。
- **dry-run 验证（全绿）**：19/19 文件 XML 校验通过、修改 1605 列、副本重扫残留 0、幂等 yes；抽查 master-data diff 仅 `stdDataType="long"→"string"` 且仅落在 `id`/`*Id` 列，delVersion 等非 PK/FK 列零误改。
- **下一步（待人工裁决）**：审核 `_tmp/bigint-id-string-fix/` 副本 → `apply --yes` → `mvn clean install -DskipTests` 增量重生成 → 全量测试回归。涉及 `model/*.orm.xml` 保护区域（ask first），需 plan + 审计。
# Development Log - 2026-08-13

### Projects 域 ErpPrjTimesheet + ErpPrjProjectSettlement 实体级状态机 Bean 落地（M3.10/M3.11/M3.12，plan `2026-08-13-0805-3`，§11.2 M3(iii)→M4 升级 plan-first）

- **工作项**：执行 `docs/plans/2026-08-13-0805-3-erpprj-timesheet-settlement-state-machine-beans.md` 三 Phase。落地 3 轴实体级 Bean（契约 `docs/architecture/entity-state-machine-bean.md`）+ Timesheet 3 Processor/BizModel reject inline + Settlement facade 接线 + 层 1 矩阵 + 层 2 四方对照 + 层 3 回归。**治理升级**：Timesheet approve（触发 `TimesheetPostingDispatcher`）与 Settlement approve（触发 `ProjectSettlementPostingDispatcher`+转固）均触发业财过账，依 §11.2 M3(iii) 升级 M4 plan-first（§11.2 M4 硬约束 (i)-(v)，人工门控 2026-08-13 已解除）。消费 M0.1 契约 + M1.3 模板 + M0.2 清单 §3.4 + cs/pur/ast Bean 范式。
- **Phase 1（3 Bean + 注册 + 层 1 矩阵）**：`ErpPrjTimesheetStateMachine`（status 单轴，5 边：submit/approve/reject + cancel 双源 SUBMITTED/APPROVED→UNSUBMITTED；APPROVED 可逆终态；REJECTED dict 死状态；`assertCanCancel` 对全部 dict 值放行=撤回语义行为保持）+ `ErpPrjProjectSettlementApprovalStateMachine`（approveStatus 轴，3 边；reverseSettlement 无边=纯 posted 轴；APPROVED 真终态）+ `ErpPrjProjectSettlementDocumentStateMachine`（docStatus 轴，3 边：approve DRAFT→APPROVED + cancel 双源；APPROVED dict-value drift 按 writer 建模；OPEN/ON_HOLD/COMPLETED 共享 dict 死状态）。3 Bean 严格无状态（零 @Inject），`app-service.beans.xml` FQN-id 注册（追加）。层 1 矩阵 `TestErpPrjTimesheetAndSettlementStateMachines` **31/31 绿**。
- **Phase 2（接线 + 层 3 回归，行为保持）**：Timesheet `Submit/Approve/Cancel`Processor + BizModel `reject` inline 注入 `ErpPrjTimesheetStateMachine`（非 private，R5），固定来源态守卫改委托 `assertCan<Action>`（try/catch common 码→领域 `ERR_TIMESHEET_ILLEGAL_STATUS_TRANSITION`/`ERR_SETTLEMENT_ILLEGAL_STATUS_TRANSITION` + 实体编号，common 作 cause），目标态经 `<action>TargetStatus()`。Settlement facade `validateTransitionFor{Submit,Approve,Reject,Cancel}` + `do{Submit,Approve,Reject,Cancel}` 接线双 Bean（doApprove 双轴同动 approveStatus+docStatus 各经对应 Bean 目标态）。**过账/红冲/转固/归集/双轴/config-gated STRICT-RELAXED 全原序保留**（§11.2 M4 (ii)/(iv)/(v)）；`reverseSettlement` 未接线（纯 posted 轴）。层 3 `mvn test -pl module-projects/erp-prj-service` **133/133 全绿**（含 TimesheetCost 7 / BudgetAndCollection 7 / ProjectSettlement 4 / PostingFaultInjection 1 / PostingFailureAlert 1），零行为回归。
- **Phase 3（层 2 四方对照 + owner doc 补章节 + 漂移 Fix/Decision）**：三轴 × 10 维度四方对照（dict↔owner-doc↔Bean↔writer）无未裁决漂移。owner doc `docs/design/projects/state-machine.md` 补 **§适用对象四：工时记录**（status 轴：5 边矩阵 + APPROVED 可逆终态 + cancel→UNSUBMITTED 撤回语义 + REJECTED 死状态 + Bean 元数据建模说明）+ **§适用对象五：项目结算单**（docStatus+approveStatus 双轴：双轴矩阵 + doApprove 双轴同动 + reverseSettlement 无轴写 + APPROVED dict-value drift successor + 共享 dict 死状态保留 + config-gated approve）。**Fix**：BizModel Javadoc 漂移就地修正（reject→UNSUBMITTED/cancel→UNSUBMITTED，原误述 →DRAFT/→CANCELLED）。**Decision**：cancel→UNSUBMITTED = intentional legacy behavior；共享 dict 死状态保留为预留语义入口。
- **验证基线（全绿）**：`mvn clean install -DskipTests`（全 reactor BUILD SUCCESS）+ `mvn test -pl module-projects/erp-prj-service`（133/133）+ `bash docs/audits/nop-compliance-checker.sh`（exit 0，R5=0/R11=0/R12 无漂移/R2c=1392=基线）。独立结束审计（子代理 `ses_004cd4b69ffe0JAoJa2YXOobu8`）= **PASS 无 blocker**。路线图 M3.10/M3.11/M3.12 → done。
- **Deferred（已裁定带 successor）**：Timesheet cancel→CANCELLED 独立作废态（PM 需求时）；Settlement docStatus APPROVED dict 补全/rebind（ORM ask-first）；M0.2 路线图表分类 reconcile M3→M4（非阻塞文案）；Delta 覆盖实证（M4 保护域归 M5.3）。

### Maintenance 域 ErpMntRequest 实体级 status 状态机 Bean 落地（M3.17，plan `2026-08-13-0805-1`）

- **工作项**：执行 `docs/plans/2026-08-13-0805-1-erpmnt-request-state-machine-bean.md` 三 Phase。落地 Maintenance 域 Request status 业务生命周期轴 Bean（契约 `docs/architecture/entity-state-machine-bean.md`）+ 5 per-mutation Processor 接线 + 层 1 矩阵 + 层 2 四方对照 + 层 3 回归。Request 6 态（OPEN/ACCEPTED/IN_PROGRESS/COMPLETED/REJECTED/CANCELLED），5 动作（accept/startRepair/complete/rejectRequest/cancel）7 边（rejectRequest/cancel 双源）。消费 M0.1 契约 + M1.3 模板 + M0.2 清单 §3.4 + cs Ticket Bean 范式。
- **Phase 1（Bean + 注册 + 层 1 矩阵）**：`ErpMntRequestStateMachine`（5 assertCan + 5 TargetStatus + isTerminal[COMPLETED/REJECTED/CANCELLED] + transitions 7 边[accept 1 + startRepair 1 + complete 1 + rejectRequest 双源 2 + cancel 双源 2] + initial[OPEN]），严格无状态。`app-service.beans.xml` FQN-id 注册（追加，沿用 cs Ticket Bean 范式）。层 1 矩阵 `TestErpMntRequestStateMachine` **9/9 绿**（无重复/冲突边 + 从 OPEN 全可达 + 三终态无出边 + reject/cancel 双源合法/其余非法 + transitions 元数据一致 + 终态/初始态集合）。
- **Phase 2（5 Processor 接线 + 层 3 回归）**：`AbstractErpMntRequestProcessor` 注入 `@Inject ErpMntRequestStateMachine stateMachine`（非 private，合规 R5）+ 补 cause 重载 `illegalRequestTransition(...,cause)`。5 Processor（accept/startRepair/complete/rejectRequest/cancel）内联 `Objects.equals(...REQUEST_STATUS_X)`/`validateTransition`/双源 guard 改委托 `assertCan<Action>`（try/catch common `ERR_ILLEGAL_STATUS_TRANSITION`→领域 `ERR_INVALID_REQUEST_STATUS_TRANSITION` + 实体编号，common 作 cause，契约 §7），目标态回写经 `<action>TargetStatus()`。**动态副作用保留原位**：accept 生成响应式 Visit（`visitBiz.save` 跨实体）、complete 仅置 `completedAt`（**无 completedBy writer，不新增**——Non-Goal）。共享 helper `validateTransition`/`illegalRequestTransition`/`requireRequest`/`requestDao` 保留。grep 证 5 接线点 0 残留内联 `Objects.equals(...REQUEST_STATUS)` 矩阵判断。层 3 `mvn test -pl module-maintenance/erp-mnt-service` **71/71 全绿**（含 `TestErpMntVisitRequestStateMachine` 10 项：accept/startRepair/complete/rejectRequest/cancel happy + 非法态领域错误码断言 + accept 生成 Visit 副作用 + 双源 reject/cancel），零行为回归。
- **Phase 3（层 2 四方对照 + owner-doc doc-drift Fix + Decision）**：10 维度审查裁决 **Verdict: pass**（0 P0/0 P1）。**IN_PROGRESS doc drift → Fix**：`docs/design/maintenance/state-machine.md §适用对象二` 状态表原仅 5 态（漏 IN_PROGRESS），迁移图将「维修中」描述为「维护访问执行中」混淆 visit/request 状态轴；就地补 `IN_PROGRESS（维修中）` 表行（对齐 dict 6 值）+ 图显式标注 `ACCEPTED → startRepair → IN_PROGRESS → complete → COMPLETED` 边 + blockquote 声明双源/IN_PROGRESS 仅可 complete 语义。**Decision（rejectRequest/cancel 双源 OPEN/ACCEPTED）**：保持代码现状，替代方案（单源收紧）被否决（破坏既有拒绝/取消已受理请求业务路径），残留风险无。dict 6 值全有 writer（OPEN=初始/CRUD 创建；余 5=Processor），**无 dict 死状态**。
- **验证基线（全绿）**：`mvn clean install -DskipTests`（全仓库 BUILD SUCCESS，156 reactor 模块含 app-erp-all）+ `mvn test -pl module-maintenance/erp-mnt-service`（71/71）+ `bash docs/audits/nop-compliance-checker.sh`（R5=0/R8=0/R11=0 无漂移）。路线图 M3.17 → done。
- **Deferred（已裁定带 successor）**：`ErpMntRequest.approveStatus` 轴（xbiz 另 5 mutation，非 M3.17 范围）；`ErpMntVisit.status`（M4.54 plan-first 业财过账域）；通用 CRUD 对 status 写入运行时禁止（M0.1 §9 选项 c 全局 successor）；Delta 覆盖实证（M3 非保护域可选，归 M5.3）。

### Contract 域 ErpCtContractVersion + ErpCtRebateAgreement 实体级状态机 Bean 落地（M3.18 + M3.19，plan `2026-08-13-1430-3`）

- **工作项**：执行 `docs/plans/2026-08-13-1430-3-erpct-version-rebate-state-machine-beans.md` 三 Phase。落地 Contract 域 Version status 轴（DRAFT/FINALIZED/SIGNED 线性）+ RebateAgreement status 轴（**退化轴**：dict 4 值 DRAFT/ACTIVE/EXPIRED/SETTLED，仅 DRAFT 可达，三死状态预留）各一实体级 Bean（契约 `docs/architecture/entity-state-machine-bean.md`）+ BizModel/Processor 接线 + 层 1 矩阵 + 层 2 四方对照 + 层 3 回归。按规则 14 合并同 owner doc `contract/state-machine.md` 的两轴。消费 M0.1 契约 + M1.3 模板 + M0.2 清单 + M2.18 Contract 范式。
- **Phase 1（两 Bean + 注册 + 层 1 矩阵）**：`ErpCtContractVersionStateMachine`（2 动作 assertCanFinalize[DRAFT]/Sign[FINALIZED] + 目标态 + isTerminal[SIGNED] + transitions 2 边[finalize+sign] + initial[DRAFT]，dict 3 值全可达无死状态）+ `ErpCtRebateAgreementStateMachine`（**退化分类 Bean**：transitions() 空 + initial[DRAFT] + terminalStatuses() 空 + isTerminal 全 false + 集中化 `isActive(ACTIVE)` 只读 accrual 守卫）。`app-service.beans.xml` FQN-id 注册两 Bean（追加，沿用 M2.18 Contract 范式）。层 1 矩阵 12/12 绿（Version 线性 + RebateAgreement 退化解 + 3 死状态排除断言）。
- **Phase 2（BizModel/Processor 接线 + 层 3 回归）**：Version `finalizeVersion`（BizModel）+ `signVersion`（SignVersionProcessor）已注入 Version Bean，内联 `Objects.equals` 来源态守卫改委托 `assertCanFinalize`/`assertCanSign`（try/catch common 码→`ERR_CT_ILLEGAL_STATUS_TRANSITION` 领域码，common 作 cause），目标态经 `*TargetStatus()`；**动态副作用保留原位**（signVersion 的 isCurrent 守卫 + isCurrent 原子翻转 + approvedAt + e-signature 独立 SPI）。Contract 激活→版本 SIGNED 级联经 `contractVersionBiz.signVersion` 间接委托（ActivateProcessor 不重复注入 Version Bean）。RebateAgreement `RunAccrualProcessor:46` + `RebateEngine.validateActive:141` 注入 RebateAgreement Bean，`status==ACTIVE` 只读守卫改委托 `isActive(status)`（`ERR_CT_REBATE_AGREEMENT_NOT_ACTIVE` 对外不变）。**amend 新建 DRAFT 不调 assertCan***（初始态写入 §9.2 选项 c）。执行期修复前置遗留编译 bug：SignVersionProcessor `illegalTransition` 补 3-arg cause 重载（前次接线遗漏）。grep 证 4 接线点 0 残留内联矩阵判断。层 3 `mvn test -pl module-contract/erp-ct-service -am` **75/75 全绿**（TestErpCtESignature 19 + TestErpCtContractRebate 8 + TestErpCtRebateSettlementEnd），零行为回归。
- **Phase 3（层 2 四方对照 + owner-doc 补章节 + Decision）**：双轴 10 维度审查，双轴裁决 pass（0 P0/0 P1）。Version 确认 3 值全可达 + 跨聚合 Contract→Version 级联（父驱子，守卫统一 signVersion）。RebateAgreement 裁定：**ACTIVE/EXPIRED/SETTLED = intentional reserved（死状态）**（dict 有值 + 零命名动作 writer；保留不删对齐 Contract CANCELLED/NEGOTIATION + hr SUSPENDED 先例）+ **退化轴**（transitions 空，如实反映非 drift）+ **owner-doc §Version/§RebateAgreement 缺口 = doc gap**。Fix：owner doc `docs/design/contract/state-machine.md` 补 **§适用对象二：合同版本**（3 值 + finalize/sign 迁移表 + 终态 SIGNED + Contract→Version 级联声明 + e-signature 边界）+ **§适用对象三：返利协议**（4 值 + 退化轴声明 + 3 死状态登记 + ACTIVE accrual 只读守卫 + 返利结算过账独立 Settlement 轴 M4.65 边界声明 + volume-discount.md 交叉引用），镜像 hr/projects 域多适用对象范式。
- **验证基线（全绿）**：`mvn clean install -DskipTests`（全仓库 BUILD SUCCESS，156 reactor 模块）+ `mvn test -pl module-contract/erp-ct-service -am`（75/75）+ `bash docs/audits/nop-compliance-checker.sh`（R5=0/R11=0 无漂移）。路线图 M3.18/M3.19 → done。
- **Deferred（已裁定带 successor）**：RebateAgreement activate/expire/settle mutation（PM 要求返利协议生命周期业务流落地时开独立 plan + 填充 Bean transitions 边）；ErpCtRebateSettlement.status（settlement-status DRAFT→POSTED = M4.65 plan-first 业财过账，独立轴）；contract 域其余状态轴（SignatureRequest e-signature SPI 派生 / ApprovalRecord 审计日志 / Document ocrStatus 自由 string）；Delta 覆盖实证（M3 非保护域可选，归 M5.3）；全局 CRUD 写锁（M0.1 §9 选项 c 排除）。

### HR 域 ErpHrEmployee + ErpHrTimesheet 实体级状态机 Bean 落地（M3.8 + M3.9，plan `2026-08-13-1430-2`）

- **工作项**：执行 `docs/plans/2026-08-13-1430-2-erphr-employee-timesheet-state-machine-beans.md` 三 Phase。落地 HR 域 Employee employmentStatus 轴（**退化解**）+ Timesheet status 轴各一实体级 Bean（契约 `docs/architecture/entity-state-machine-bean.md`）+ BizModel 接线 + 层 1 矩阵 + 层 2 四方对照 + 层 3 回归。按规则 14 合并同 owner doc `human-resource/state-machine.md` 的两轴。消费 M0.1 契约 + M1.3 模板 + M0.2 清单 + M2.11/M2.12 HR 范式（LeaveRequest/Contract Bean）。
- **Phase 1（两 Bean + 注册 + 层 1 矩阵）**：`ErpHrTimesheetStateMachine`（3 动作 assertCanSubmit[DRAFT/REJECTED]/Approve/Reject + 目标态 + isTerminal[APPROVED 严格终态 + REJECTED 可恢复终态] + transitions 4 边[submit 2 源 + approve 1 + reject 1] + initial[DRAFT]），严格无状态，**不编码 cancel**（dict 无 CANCELLED + RC-R1.8 权威）；`ErpHrEmployeeStateMachine`（**退化分类 Bean**：transitions() 空 + initial[ACTIVE/PROBATION] + terminal[RESIGNED/TERMINATED/RETIRED] 对齐 owner doc §3 + isTransferable/nonTransferableStatuses 集中化只读调动守卫）。`app-service.beans.xml` FQN-id 注册两 Bean（追加，沿用 LeaveRequest/Contract 范式）。层 1 矩阵 `TestErpHrTimesheetStateMachineMatrix`(11) + `TestErpHrEmployeeStateMachineMatrix`(7) = 18/18 绿（含 Timesheet 无 cancel 边断言 + Employee 退化解 + 死状态 isTerminal=true 断言）。
- **Phase 2（BizModel 接线 + 层 3 回归）**：Timesheet `ErpHrTimesheetBizModel` 注入 Bean，submit/approve/reject 内联 `Objects.equals` 来源态守卫改委托 `assertCanXxx`（try/catch common 码→`ERR_HR_TIMESHEET_ILLEGAL_TRANSITION` 领域码，common 作 cause），目标态经 `*TargetStatus()`；**动态副作用保留原位**（submit totalHours 重算 + 24h 跨表校验、reject reason 必填 + remark 写入）。Employee `ErpHrEmployeeBizModel` + `ErpHrEmployeeTransferEmployeeProcessor` 双副本 `isTransferable`/`nonTransferableStatuses`/`requireTransferableEmployee` 改委托 Bean（`ERR_EMPLOYEE_NOT_TRANSFERABLE` 领域码对外不变——调动守卫是只读判断而非状态迁移，领域有专属错误码）。**2 处初始态 ACTIVE 写入不调 assertCan***（`ErpHrRecruitmentHireProcessor:63`/`ErpHrRecruitmentBizModel:149`，§9.2 选项 c 创建路径）。grep 证 0 残留内联矩阵判断。层 3 `mvn test -pl module-hr/erp-hr-service` **214/214 全绿**（含 TestErpHrTimesheetFamily/TestErpHrEmployeeTransfer/TestErpHrEmployeeReferences/TestErpHrLeaveEngine/TestErpHrPayrollSimulation），零行为回归。
- **Phase 3（层 2 四方对照 + Decision）**：双轴 10 维度审查。**Timesheet cancel = doc drift**（§2 图表 `:199` 画 DRAFT→CANCELLED，但 dict 无 CANCELLED + RC-R1.8 仅 3 动作 + 无 writer）→ Fix：owner doc §2 图表补注 + successor。**Employee §3 终态对齐**（§11.4 显式裁定）：Bean `terminalStatuses()`=[三态] 对齐 §3 业务语义，三态零 writer = 死（无入边），但 isTerminal=true（§3 业务终点）；退化解下全部状态无出边，故「终态」按 §3 业务语义而非图论裁定。**PROBATION 零 writer 不对称 Decision**：PROBATION 归 initial（§1 业务语义），与三死状态归 terminal 对称。**退化轴 rule 9 替代方案**：选定落地退化 Bean（集中化双副本调动守卫为可测/Delta-overridable 单元 + M5.1 矩阵审计需机器可读元数据）而非 defer M3.8。owner doc §适用对象二补注 Bean 落地 + 死状态/终态/PROBATION 不对称裁定。
- **验证基线（全绿）**：`mvn clean install -DskipTests`（全仓库 BUILD SUCCESS）+ `mvn test -pl module-hr/erp-hr-service`（214/214）+ `bash docs/audits/nop-compliance-checker.sh`（R5=0/R11=0 无漂移）。路线图 M3.8/M3.9 → done。
- **Deferred（已裁定带 successor）**：Timesheet cancel mutation（PM 要求工时表取消业务流落地时开独立 plan + dict 加 CANCELLED + 触及数据 ask-first）；Employee 离职/退休/转正 mutation（方案 A 触及 nop-auth 用户禁用副作用，属保护区域 ask-first）；hr 域其余状态轴（Salary = M4.63/64 plan-first 业财过账、Survey = §适用对象五 Deferred CRUD 桩）；Delta 覆盖实证（M3 非保护域可选，归 M5.3）；全局 CRUD 写锁（M0.1 §9 选项 c 排除）。

### CRM 域 ErpCrmLead 实体级 docStatus 状态机 Bean 落地（M3.1，plan `2026-08-13-0945-3`）

- **工作项**：执行 `docs/plans/2026-08-13-0945-3-ercm-lead-state-machine-bean.md` 三 Phase。落地 CRM Lead docStatus 业务生命周期轴 Bean（契约 `docs/architecture/entity-state-machine-bean.md`）+ facade 接线 + 层 1 矩阵 + 层 2 四方对照 + 层 3 回归。Lead 5 态（NEW/QUALIFIED/CONVERTED/LOST/CANCELLED），4 动作（qualify/lose/cancel/convert）7 意图边。与 M2.2 Event 轴独立；与 stageId 维度独立（不纳入）。
- **Phase 1（Bean + 注册 + 接线 + 层 1 矩阵）**：`ErpCrmLeadStateMachine`（4 assertCan + 4 TargetStatus + isTerminal[CONVERTED/LOST/CANCELLED] + transitions 7 意图边 + initial[NEW]），严格无状态。`app-service.beans.xml` FQN-id 注册（追加，沿用 M2.2 Event Bean 范式）。**接线经 facade 注入**（Lead 架构 = facade `ErpCrmLeadProcessor` + per-mutation 委托，不同于 Event 自包含 Processor）：facade 注入 `@Inject ErpCrmLeadStateMachine`（非 private，合规 R5），`validateTransitionForQualify/Lose/Cancel` 改委托 `assertCanXxx`（try/catch common 码→`ERR_LEAD_ILLEGAL_STATUS_TRANSITION` 领域码，common 作 cause），`doQualify/Lose/Cancel` 目标态经 `*TargetStatus()`。Conversion facade `ErpCrmConversionProcessor` 注入 Bean，`validateNotConverted` 改委托 `assertCanConvert`（CONVERTED 幂等拒绝保持专属 `ERR_LEAD_ALREADY_CONVERTED`，Decision C carve-out），`markLeadConverted` 目标态经 `convertTargetStatus()`。**convert 漂移 Decision 分支 (a)**：Bean `assertCanConvert` 运行时**仅拒 CONVERTED**（匹配现行 `validateNotConverted`，保持 NEW→CONVERTED 合法），`transitions()` 编码意图矩阵 {NEW,QUALIFIED}→CONVERTED（声明态，有意区分）。grep 证 facade 内仅 `validateMovable`（stageId 维度，Non-Goal 排除）残留内联判断。层 1 矩阵 `TestErpCrmLeadStateMachineMatrix` 11/11 绿。
- **Phase 2（四方对照 + doc-drift Fix）**：dict 5 值 ↔ owner doc §Lead ↔ Bean 元数据 ↔ writer 全盘点。**漂移 1（convert 来源态 doc drift）→ Fix**：owner doc §2 原声明 QUALIFIED→CONVERTED 与代码实况（非 CONVERTED→CONVERTED 经 leadType 门控，含 NEW→CONVERTED）漂移，就地补正 §2 迁移图+表为 {NEW,QUALIFIED}→CONVERTED + 补注裁定结论。**漂移 2（运行时 vs 意图矩阵 latent gap，既有非本计划引入）→ watch-only residual**：assertCanConvert 运行时允许 LOST/CANCELLED→CONVERTED，意图矩阵限定 {NEW,QUALIFIED}；裁定保持运行时宽放（不新增来源态限制），owner doc 补注为 watch-only residual。
- **Phase 3（层 3 回归 + 一致性复核）**：`mvn test -pl module-crm/erp-crm-service -am` **162/162 全绿**（较 M2.2 基线 151 增 11 = 新矩阵测试），零行为回归。`TestErpCrmLeadConversion`(9) 覆盖全链路 LEAD→客户→商机→报价单、qualify/lose/cancel/convert 终态、领域错误码、跨域副作用、stageId 方向守卫均不变。Bean 命名/注册/无状态/元数据形状与 M2.2 `ErpCrmEventStateMachine` 一致。
- **验证基线（全绿）**：`mvn clean install -DskipTests`（全仓库 BUILD SUCCESS）+ `mvn test -pl module-crm/erp-crm-service -am`（162/162）+ `bash docs/audits/nop-compliance-checker.sh`（R5=0/R11=0/R2c=1392=baseline/R12c=40=baseline，零漂移）。路线图 M3.1 → done。
- **Deferred（已裁定带 successor）**：convert latent gap（漂移 2，watch-only residual，产品要求收窄时 successor）；Delta 覆盖实证（M3 非保护域可选，归 M5.3）；全局 CRUD 写锁（M0.1 §9 选项 c 排除）；crm Event BizModel 死代码清理（M2.2 遗留 follow-up）。

### 销售两实体 approveStatus 审批轴 StateMachine Bean 迁移（M3.6/M3.7，plan `2026-08-13-0945-2`）

- **工作项**：执行 `docs/plans/2026-08-13-0945-2-sales-approvestatus-state-machine-bean.md` 三 Phase。销售 2 单据实体（Order/Quotation）approveStatus 审批轴各落地一实体级 Bean（契约 `docs/architecture/entity-state-machine-bean.md`）+ PROC 路径接线 + 层 1 矩阵 + 层 2 四方对照 + 层 3 回归。审批轴 5 动作（submit/approve/reject/reverseApprove/withdraw）6 边矩阵，与 docStatus 轴（M2.9–M2.10 done）独立。跨实体 Decision 复用姊妹采购计划 `2026-08-13-0945-1`（N=4 done）。
- **Phase 1（Order Bean M3.7 + 跨实体接线 Decision 固化）**：`ErpSalOrderApprovalStateMachine`（5 assertCan + 5 TargetStatus + isTerminal[APPROVED 可逆终态] + transitions 6 边 + initial[UNSUBMITTED]），严格无状态。`app-service.beans.xml` 注册。5 Order per-mutation Processor（含 Reject）注入 Bean（非 private @Inject，合规 R5），覆写 `validateTransitionForXxx` 委托 `assertCanXxx`（try/catch common 码→`ERR_ORDER_ILLEGAL_STATUS_TRANSITION` 领域码），目标态经 getter（approvedStatus/rejectedStatus/unsubmittedStatus/submittedStatus）+ doReverseApprove 委托 Bean `*TargetStatus()`。SoD + commitment-commit/intercompany-approve/可用量只读预检保留原位（facade `ErpSalOrderProcessor` 经 `validateBusinessRules`/`afterStateChange` 钩子）。grep 证 5 Processor 内 0 残留内联 `Objects.equals` 矩阵判断。**reverseApprove 实仓纠正（关键 Decision，同采购计划 Phase 1）**：本计划草案 baseline §15 称「销售 reverseApprove→SUBMITTED（经骨架继承）」——执行期实仓核实为错误：`ErpSalOrderReverseApproveProcessor.doReverseApprove`（及 Quotation 同名）**均已显式覆写为 REJECTED**，即两实体 reverseApprove=REJECTED（已合规 `domain-design-guidelines.md §16.4`，与 owner doc `sales/state-machine.md` §2 反审核→REJECTED 一致）。Bean 据实统一 REJECTED（零行为回归）；骨架 `AbstractReverseApproveProcessor`→SUBMITTED 为经覆写绕过的死路径，§16.4 合规化移交显式 successor（与采购计划同源）。层 1 矩阵 11/11 绿 + `TestErpSalOrderApproval`(15) 回归全绿。
- **Phase 2（Quotation Bean M3.6 + 一致性复核）**：`ErpSalQuotationApprovalStateMachine`（同构矩阵，领域码 `ERR_QUOTATION_ILLEGAL_STATUS_TRANSITION`）+ 5 Quotation Processor 同范式接线（Quotation 无 commitment/intercompany 副作用）。层 1 矩阵 11/11 绿 + `TestErpSalQuotationCrudSmoke`(5)/`TestErpSalQuotationToOrder`(6) 回归全绿。
- **Phase 3（层 3 回归 + 两实体一致性复核）**：`mvn test -pl module-sales/erp-sal-service -am` **221/221 全绿**（较 docStatus 基线增 22 = 2 新矩阵测试 11+11），零行为回归。两 Bean 命名(`Approval` 后缀)/注册(同 beans.xml)/无状态(零真实 @Inject，grep 证实)/元数据形状一致（同 6 边矩阵 + reverseApprove=REJECTED 统一）；PROC 路径接线范式可追溯（5 Processor × 2 实体，grep 证 0 残留内联矩阵判断）。
- **验证基线（全绿）**：`mvn clean install -DskipTests`（全仓库 BUILD SUCCESS，156 reactor 模块）+ `mvn test -pl module-sales/erp-sal-service -am`（221/221）+ `bash docs/audits/nop-compliance-checker.sh`（R5=0/R11=0/R2c=1392=baseline/R12c=40=baseline，零漂移）。路线图 M3.6/M3.7 → done。
- **与采购 N=1 计划的关键差异**：销售 Order/Quotation 审批**均经 PROC 路径**（4 审批 Processor + 骨架守卫均存在，无采购 Quotation/Rfq 那种 INLINE xbiz 漂移），故**无需错误码 Decision**——两实体领域码 `ERR_ORDER/QUOTATION_ILLEGAL_STATUS_TRANSITION` 已存在，直接复用。层 2 四方对照预期无漂移核实通过（Bean 矩阵 6 边与 owner doc §审批轴完全一致）。
- **Deferred（已裁定带 successor）**：骨架 `AbstractReverseApproveProcessor` §16.4 合规化（触及 `module-common-service` 共享骨架跨域影响，独立 plan + 全域使用者盘点，与采购计划同源 successor）；Delta 覆盖实证（M3 非保护域可选，归 M5.3）；全局 CRUD 写锁（M0.1 §9 选项 c 排除）。

### 采购四实体 approveStatus 审批轴 StateMachine Bean 迁移（M3.2/M3.3/M3.4/M3.5，plan `2026-08-13-0945-1`）

- **工作项**：执行 `docs/plans/2026-08-13-0945-1-purchase-approvestatus-state-machine-bean.md` 四 Phase。采购 4 单据实体（Order/Requisition/Quotation/Rfq）approveStatus 审批轴各落地一实体级 Bean（契约 `docs/architecture/entity-state-machine-bean.md`）+ PROC/INLINE 双路径接线 + 层 1 矩阵 + 层 2 四方对照 + 层 3 回归。审批轴 5 动作（submit/approve/reject/reverseApprove/withdraw）6 边矩阵，与 docStatus 轴（M2.5–M2.8 done）独立。
- **Phase 1（Order Bean M3.5 + 跨实体 Decision 固化）**：`ErpPurOrderApprovalStateMachine`（5 assertCan + 5 TargetStatus + isTerminal[APPROVED 可逆终态] + transitions 6 边 + initial[UNSUBMITTED]），严格无状态。`app-service.beans.xml` 注册（四实体审批轴 Bean 一并）。5 Order per-mutation Processor 注入 Bean，覆写 `validateTransitionForXxx` 委托 `assertCanXxx`（try/catch common 码→`ERR_ORDER_ILLEGAL_STATUS_TRANSITION` 领域码），目标态经 getter/doReverseApprove 委托 `*TargetStatus()`。**reverseApprove 实仓纠正（关键 Decision）**：草案 baseline 称「Order/Requisition 经骨架继承 SUBMITTED」——执行期实仓核实为错误：`ErpPurOrderReverseApproveProcessor.doReverseApprove:84` 与 `ErpPurRequisitionReverseApproveProcessor.doReverseApprove:78` **均已覆写为 REJECTED**，即四实体 reverseApprove 全部已=REJECTED（全部已合规 §16.4）。Bean 据实统一 REJECTED（零行为回归）；骨架 `AbstractReverseApproveProcessor`→SUBMITTED 为经覆写绕过的死路径，§16.4 合规化移交显式 successor。层 1 矩阵 11/11 绿 + `TestErpPurOrderApproval`(7)/`TestErpPurQuotationRfqReverseApprove`(7) 回归全绿。
- **Phase 2（Requisition Bean M3.4）**：`ErpPurRequisitionApprovalStateMachine`（同构矩阵）+ 5 Requisition Processor 同范式接线（领域码 `ERR_REQ_ILLEGAL_STATUS_TRANSITION`）。层 1 矩阵 10/10 绿 + `TestErpPurRequisitionApproval`(4) 回归全绿。
- **Phase 3（Quotation+Rfq Bean M3.2/M3.3 + INLINE xbiz + 错误码 Decision）**：`ErpPurQuotation/RfqApprovalStateMachine`（同构矩阵）。两 BizModel 注入 Bean + 暴露 5 `@BizQuery prepare<Action>(code,approveStatus,docStatus)` helper（isCancelled 守卫 + Bean assertCanXxx + 返回 `*TargetStatus()`）。两 xbiz `<source>` 内联 `isCancelled`+来源态守卫改调 helper（`thisObj.invoke("prepare<Action>",{code,approveStatus,docStatus})`），目标态写入改用 helper 返回值。**错误码 Decision 分支 (b) 对齐领域码**：平台码 `nop.err.wf.approve.doc-cancelled`→既有 `ERR_QUOTATION/RFQ_ILLEGAL_DOC_STATUS_TRANSITION`（同 sales precedent + Order validateNotCancelled）；`nop.err.wf.approve.invalid-status`→新增 `ERR_QUOTATION/RFQ_ILLEGAL_STATUS_TRANSITION`（approveStatus 轴专属码，同 Order/Req 模式）。理由：四实体统一领域码 + mfg/qa/sales 既定 precedent + 无采购测试断言平台码。层 1 矩阵各 7/7 绿 + `TestErpPurQuotationRfqReverseApprove`(7) 回归全绿（grep 证 xbiz 内 0 处 `nop.err.wf.approve.*`）。
- **Phase 4（层 3 回归 + 一致性复核）**：`mvn test -pl module-purchase/erp-pur-service` **225/225 全绿**（较 docStatus 基线 190 增 35 = 4 新矩阵测试 11+10+7+7），零行为回归。四 Bean 命名(`Approval` 后缀)/注册/无状态/元数据形状一致；PROC（Order/Requisition per-mutation Processor 覆写）与 INLINE（Quotation/Rfq BizModel helper）双路径可追溯；reverseApprove 四实体统一 REJECTED。
- **验证基线（全绿）**：`mvn clean install -DskipTests`（全仓库 BUILD SUCCESS）+ `mvn test -pl module-purchase/erp-pur-service`（225/225）+ `bash docs/audits/nop-compliance-checker.sh`（exit 0，R5=0/R11=0/R2c=1392=baseline/R12c=40=baseline，零漂移）。路线图 M3.2/M3.3/M3.4/M3.5 → done。
- **owner-doc PROC/INLINE 矛盾（doc drift，移交 successor）**：`purchase/state-machine.md` §实现模式声明「Order/Requisition withdrawApproval=INLINE」「Requisition approve/reject=INLINE」与实仓 Order/Requisition 均有 per-mutation `*WithdrawApprovalProcessor`/`*Approve/RejectProcessor`（PROC）矛盾 → doc drift（实仓以 PROC 为准），owner doc 补正移交 successor（不改稳定设计语义，仅修正实现路径登记）。
- **Deferred（已裁定带 successor）**：骨架 `AbstractReverseApproveProcessor` §16.4 合规化（触及 `module-common-service` 共享骨架跨域影响，独立 plan + 全域使用者盘点）；owner doc §实现模式 PROC/INLINE 补正；Delta 覆盖实证（M3 非保护域可选，归 M5.3）；全局 CRUD 写锁（M0.1 §9 选项 c 排除）。

### CRM 域 ErpCrmEvent 实体级状态机 Bean 落地（M2.2，plan `2026-08-12-2142-2`）

- **工作项**：执行 `docs/plans/2026-08-12-2142-2-erpcrm-event-state-machine-bean.md` 三 Phase。落地 CRM 域首例实体级状态机 Bean（契约 `docs/architecture/entity-state-machine-bean.md`）+ 接线 + 三层测试 + 四方对照。Event 单轴（3 态 PLANNED/COMPLETED/CANCELLED，2 边 complete/cancel），非保护域、无审批子矩阵、无跨域过账副作用。
- **Phase 1（Bean + 注册 + 层 1 矩阵）**：`ErpCrmEventStateMachine`（2 动作 assertCanComplete/assertCanCancel + 目标态 + isTerminal[COMPLETED/CANCELLED] + transitions 2 边 + initialStatuses[PLANNED]），严格无状态（零 import DAO/IBiz/IServiceContext/事务）。`app-service.beans.xml` FQN-id 注册（追加，沿用既有 Processor 范式）。层 1 矩阵测试 `TestErpCrmEventStateMachineMatrix` 8/8 绿（无重复边 + 从 PLANNED 可达 COMPLETED/CANCELLED + 终态无出边且对 complete/cancel 均非法 + transitions 元数据一致 + 终态/初始态集合正确）。
- **Phase 2（Processor 接线 + 层 3 回归）**：两 per-mutation Processor（`ErpCrmEventCompleteProcessor` + `ErpCrmEventCancelProcessor`）注入 Bean（按类型、字段包级可见合规 R5），内联 `validatePlanned` 守卫替换为 `stateMachine.assertCanComplete/Cancel(from)` + 目标态写回改 `<action>TargetStatus()`，**删除两 Processor 重复的 `validatePlanned`**（矩阵收敛至 Bean）。Processor try/catch 捕获 Bean common 层码 → 映射领域 `ERR_EVENT_ILLEGAL_STATUS_TRANSITION`（eventCode/currentStatus/expectedStatus，common 码作 cause，对齐契约 §7 + M1.1 Option A）。**动态业务守卫保留原位**：requireEvent not-found、Lead 派生（flush + recalculateForLead）、relatedLeadId==null 跳过。层 3 既有回归全绿（`TestErpCrmEventReminderTimeline` 5 + `TestErpCrmEventReminderDisabled` 1 + `TestErpCrmEventPerEventReminder` 4 + `TestErpCrmEventReminderJob` 4），证明 PLANNED→COMPLETED/CANCELLED、illegal transition COMPLETED 拒绝、Lead 派生时序、reminder Job filter（status=PLANNED）均不变。
- **Phase 3（四方对照 + Delta）**：Event 单轴 10 维度对照——dict 3 值 ↔ Bean 2 边覆盖、**无死状态**（3 值均有 writer：PLANNED 经 sequence-progress 创建路径 + CRUD、COMPLETED/CANCELLED 经命名动作）；owner doc §Event 2 边 ↔ Bean 2 边**完全一致**，§Event 无 §实现约定 段→§11.4 内部漂移警示**不适用**；writer 全盘点（2 命名动作 + 4 初始态创建路径 + CRUD §9.4 残留 + 测试 fixture + reminder Job 只读消费）；BizModel 死代码 `validatePlanned:156-164` 归 Follow-up。Delta 双加载实证（`ErpCrmEventStateMachineDelta` **放宽** cancel 至 PLANNED+COMPLETED「soft void after completion」），`assertCanCancel(COMPLETED)` 基线抛异常/Delta 放行可区分，BaselineIoC 3/3 + DeltaOverride 3/3 绿。
- **VFS Delta 隔离兜底（复用 hr 域 M2.11 已落地范式）**：`TestErpCrmEventStateMachineDeltaOverride` 加 `@BeforeAll`/`@AfterAll` 调 `VfsConfigLoader.registerDefault(null)` 清理框架静态缓存，防止 delta 层文件级替换 `app-service.beans.xml` 后丢失基线 bean（如 `ErpCrmReportBizModel` 经 `ioc:type="@bean:id"`）污染同 fork 后续测试（`TestErpCrmReportRendering`）。ct 域因多核 fork 分布偶然未暴露，本域主动补防避免 flaky。
- **验证基线（全绿）**：`mvn clean install -DskipTests`（全仓库 BUILD SUCCESS）+ `mvn test -pl module-crm/erp-crm-service`（151/151：8 层 1 矩阵 + 6 Delta 证据 + 137 既有层 3）+ `bash docs/audits/nop-compliance-checker.sh`（exit 0，R5=0/R11=0 无漂移，两 Processor 重复 `validatePlanned` 收敛后 R11 维持 0）。路线图 M2.2 → done。
- **Deferred（已裁定带 successor）**：BizModel 死代码 `requireEvent`/`validatePlanned`/`deriveLeadFields` 清理（crm 下次代码整理时删除并核实无反射/测试引用）；ErpCrmLead.docStatus（M3.1 独立未来轴独立 Bean）；全局 CRUD 写锁（M0.1 §9 选项 c 显式排除，更强写锁 successor）。
