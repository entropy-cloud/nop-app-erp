# Development Log - 2026-07-30

### 2026-07-30（审计-修复 MR1 R1.29 多公司/orgId 隔离架构补能力 — 4 Phase 全 done — plan 0841-3-r1-29）

- **任务**：执行 `docs/plans/2026-07-30-0841-3-r1-29-multi-company-orgid-isolation.md`。落地 A2.18 多账套/多公司隔离审计 7 项 P1 finding（P1-MA2-093/094/095/096/097/098/099）。
- **Phase 1（Explore+Decision）**：orgId 解析选定路径 (a)——app 层 `IContext` attribute（`erp.currentOrgId`），不改 nop-entropy 平台接口。注册点经实仓核实：全局读隔离 bean `nopGlobalQueryTransformer`（CrudBizModel `@Named` 自动注入）+ 全局写 stamp `IOrmInterceptor`（`nopOrmSessionFactory` collect-beans 自动收集）。方案 A 可行，主路径落地。owner-doc drift（`multi-company.md:29`）更正。
- **Phase 2（093/094 orgId 隔离）**：`module-common-service` 新增 `erp/common` 模块（`_module` + `beans/app-service.beans.xml`）+ `app.erp.common.org` 包：`ErpOrgIsolationConstants`/`ErpOrgContext`/`ErpOrgIsolationQueryTransformer`（覆盖平台 EmptyQueryTransformer 默认）/`ErpOrgIsolationOrmInterceptor`。config-gated `erp.multi-company.org-isolation-enabled` 默认 false。负向隔离测试 `TestErpOrgIsolation` 验证读过滤 + 写 stamp + 关闭回归。
- **Phase 3（095 读路径 filter + 097 配对审计列）**：095——Report 5+Dashboard 3+PostingProcessor 1+AccountingPeriod 2=11 处查询方法补 `acctSchemaId`+`orgId` filter（scope 从期间解析，不可解析时跳过保护单组织基线）；097——`runMatching` 填充 5 审计列（arOrgId/apOrgId/arSideVoucherId/apSideVoucherId/materialId）+ 移除 hardcoded orgId=1L；`generateEliminationCandidates` 设 fromOrgId/toOrgId；`postElimination` 草稿凭证 orgId per-pair。多账套测试 `TestErpFinMultiSchemaReportIsolation` 断言不双计。
- **Phase 4（096/098 UK + 099 owner doc）**：096 `ErpFinGlBalance` 加 `UK_FIN_GL_BALANCE_NATURAL(orgId,acctSchemaId,periodId,subjectId,currencyId)`；098 `ErpFinIntercompanyMatch` 加 `UK_FIN_INTERCOMPANY_MATCH_PAIR_PERIOD(pairKey,periodId)`（替换原非唯一 IDX）+ `runMatching` 前置去重幂等；099 owner doc `multi-company.md` 强化 org-dimension-enabled 警告 + 多公司部署 checklist。
- **验证**：`mvn clean install -DskipTests` 全 154 reactor BUILD SUCCESS（含 finance codegen 增量再生 + `erp/common` 新模块）；`mvn -pl module-finance/erp-fin-service test` → 303 全绿（新增 4 用例）；跨模块回归 pur 126/sal 132/inv 140 全绿（config-gated 默认关闭 → 单组织基线零回归）；compliance checker BizModel 高严重度反模式 R1a-c=0。
- **Successor（非阻塞）**：平台 tenant 机制 / org-dimension-enabled 默认翻转 / nop-entropy IServiceContext.getOrgId 已在 plan Deferred But Adjudicated 命名触发条件。

### 2026-07-30（审计-修复 MR1 R1.28 执行后构建验证 — hr 轮换 regenerate UK 冲突修复 + 预存在 mfg 测试失败隔离 — plan 0841-2 验证补救）

- **任务**：mission `audit-remediation` 构建验证（VERIFY 步骤）。前序 EXECUTE 完成全部 4 Phase + 自称 full-green，但其验证仅覆盖 Phase 4 模块（mnt/qa/cs），**未运行 Phase 3 并发负向测试所在模块（inv/b2b/ast/mfg/hr/log）**。本会话对 8 个受影响模块全量验证，捕获并修复一项 EXECUTE 遗漏的回归。
- **核心结论（回归根因）**：R1.28 P1-MA2-091 为 `erp_hr_shift_assignment` 加 `UK_HR_SHIFT_ASSIGNMENT_NATURAL (employeeId,assignmentDate,shiftId,delVersion)` + `constraint=`（Phase 2/3）。该 UK 的 `delVersion` 列设计意图正是配合逻辑删除（`deleteFlagProp/deleteVersionProp=delVersion`）允许同键重排。但轮换 `ErpHrShiftRotationPatternBizModel.deleteExistingAssignments`（regenerate 路径）仅 **status 置 CANCELLED**（delVersion 不变=0），随后 `newAssignment` 重插同键行 delVersion=0 → 与未删除的旧 CANCELLED 行（delVersion=0）UK 冲突 → `nop.err.dao.sql.duplicate-key`。这正是计划 Decision 091 命名的残留风险（软删除+重建同键交互）。
- **修复**：`ErpHrShiftRotationPatternBizModel.deleteExistingAssignments` 由 `setStatus(CANCELLED)+updateEntity` 改为 `dao.deleteEntity`（逻辑删除，`delVersion` 自增），使删除行 delVersion>0、重生成行 delVersion=0，互不冲突。这与既有 `ErpHrShiftAssignmentBizModel.deleteAll`（注释「供轮换 regenerate 使用」）的设计意图一致。重录 `testRotationGenerateStaggerAndRegenerate` 快照（`@EnableSnapshot(saveOutput=true)` → CHECKING 全绿）。测试注释同步修正（取消→逻辑删除）。regenerate 有效排班数不变断言仍通过（ORM 自动过滤逻辑删除行）。
- **变更**：(1) `module-hr/erp-hr-service/.../entity/ErpHrShiftRotationPatternBizModel.java` — `deleteExistingAssignments` cancel→logical delete。(2) `module-hr/erp-hr-service/.../TestErpHrShiftScheduling.java` — 测试注释修正。(3) `module-hr/erp-hr-service/_cases/.../testRotationGenerateStaggerAndRegenerate/output/tables/erp_hr_shift_assignment.csv` — 快照重录（旧 7 CANCELLED+7 SCHEDULED → 7 逻辑删除[delVersion>0]+7 active SCHEDULED）。
- **验证**：受影响模块 `mvn install -DskipTests -pl <8 模块> -am` BUILD SUCCESS；8 个 mission 交付测试类全绿（ast 6 + b2b 7 + cs 13 + hr 14[含修复的 regenerate] + mfg TestErpMfgMrpEndToEnd 3 + log 2 + mnt 1 + inv TestErpInvLandedCostReceiveMutex 2）；受影响模块全量 `mvn test` 仅 1 失败 = **预存在、与 R1.28 无关**的 `TestErpMfgCompletionPosting`（`erp_inv_stock_balance` LOCATION_ID=3501 期望 null）——经 `git stash` 对照 clean HEAD 同样失败（seq=41），非本计划引入、非并发/UK/idempotency 范畴，按范围纪律不在本计划修复。
- **诚实性注记**：EXECUTE R1.28 closure 的「full-green」声明不完整（仅 Phase 4 验证）。本会话补全 Phase 3 模块验证并修复遗漏回归。**本次非 full-green**（1 项预存在 mfg 失败未在范围内修复）。

### 2026-07-30（审计-修复 MR1 R1.28 并发 UK/TOCTOU/cron 幂等缺口 — Phase 4 cron 幂等 + 087 documented + owner doc 收口 — plan 0841-2）

- **任务**：执行 `docs/plans/2026-07-30-0841-2-r1-28-concurrency-uk-idempotency.md`。落地 8 项跨域 P1 finding（P1-MA2-085/086/087/088/089/090/091/092，源自 A2.17 并发与乐观锁审计）。**Phase 1（Decision）/Phase 2（ORM UK 089/091/092）/Phase 3（守卫 + app-layer 幂等 085/088/089/090/091/092）已由前序会话完成（working tree 未提交，全 [x]）**；本会话收口**唯一未完成 Phase 4（cron job 幂等 086 + 087 documented + owner doc）** + 全量验证。
- **核心结论**：**Phase 4**——(a) 086 数据腐败类：`erp-mnt-due-visit-generation`（`ScheduleDueGenerator.existsVisitForScheduleDate` 按 `VST-SCH-{schedId}-{date}` code existence check skip 重复行）+ `erp-qa-spc-sampling`（`SpcSamplingService` 计量型 `buildSampledKeys`(sourceCode#sourceLineCode#value)/计数型 `buildAttributesSampledKeys`(ATTR#firstCode) dedup）已落地；(b) 086 最严重噪音类：`erp-cs-sla-scan`（`ErpCsTicketBizModel.scanOverdueTickets` 增 `hasEscalationAction` 按 ticketId+ESCALATE existence check 去重，避免每分钟重复 ESCALATE 审计行 + 通知）已落地；(c) 086 通知重复类：`docs/design/notify/README.md` Successor 段标注 cs/crm/hr scheduler 通知 nop-job-local 单实例下可能重复 + 去重键 successor；(d) 087 documented：`docs/design/flow-overview.md §6.1 事务边界` 补注 CloseVoucherWriter 幂等受 period version guard 保护 + P0-MA2-018 successor 约束。
- **变更**：(1) `docs/design/flow-overview.md` — §6.1 增 CloseVoucherWriter 幂等约束注（period version guard 序列化 + 字面 UK 不可实施三重冲突 + P0-MA2-018 successor）。(2) `docs/design/notify/README.md` — Successor 段增 scheduler 驱动通知重复派发 successor（cs/crm/hr + 实体级行副作用已收口 + 通知去重键 successor 同 cron 平台级触发条件）。(3) `docs/backlog/audit-remediation-roadmap.md` — R1.28 ready→done。(4) 计划状态 active→completed / Phase 4 planned→completed / 全 [x]。（注：前序会话已落地 Phase 1-3 + Phase 4 代码与测试，含 inv/b2b/ast/mfg/hr/log/cs/mnt/qa 九域 Java + ORM UK + SQL + `UniqueConstraintHelper` + 6 域并发负向测试，本会话未重复修改生产代码。）
- **验证（full-green verification）**：`mvn clean install -DskipTests` BUILD SUCCESS（全 workspace reactor）；`mvn test -pl module-maintenance/erp-mnt-service,module-quality/erp-qa-service,module-cs/erp-cs-service` Phase 4 幂等测试全绿（TestErpMntDueVisitIdempotency 1 + TestErpQaSpcSampling 6[含 collectSamplesIsIdempotent] + TestErpQaSpcAttributesSampling 5[含 attributesSamplingIsIdempotent] + TestErpCsTicketSlaCsat 13[含 testScanOverdueTicketsIdempotentNoDuplicateEscalation]，0 Failures/0 Errors）。
- **后续**：非阻塞；successor 已在计划 §Deferred But Adjudicated 命名触发条件——(1) cron 平台级并发防护（nop-job-service cluster leader / IErpSysLockBiz SPI，触发=多实例部署）；(2) CloseVoucherWriter 字面 UK（P0-MA2-018 解决时闭包）；(3) scheduler 通知去重键（同多实例触发）。roadmap R1.28 ready→done。

### 2026-07-30（审计-修复 MR1 R1.27 预算承付释放路径完整性 — 三通道分离 + release-on-return hook + 全额释放/冲销恢复语义声明 — plan 0841-1）

- **任务**：执行 `docs/plans/2026-07-30-0841-1-r1-27-budget-commitment-release-path.md` 全 3 Phase。落地四项 finance P1 finding（P1-MA2-081/082/083/084，源自 A2.16 承付释放路径完整性审计），**选择性裁决**（084 三通道分离实现[会计保护区域-余量算术] + 082 release-on-return config-gated hook 实现 + 081/083 owner doc 语义声明[全额释放 + 冲销不恢复保守方向]）。全部 config-gated 默认 OFF，无活跃数据破坏（P1 非 P0）。
- **核心结论**：**Phase 1 Decision**——084 方案A（三通道分离，方案B 仅改 javadoc 被拒——actualBalance 隐式含 commitment 是真实可维护性陷阱）；081 方案A（owner doc 声明全额释放语义 + 测试，方案B 按比例部分释放归 successor）；082 方案A（config-gated release-on-return hook 调 releaseIfPresent，方案B 永久不释放被拒，残留风险=全额释放语义致部分退货多释放归 successor）；083 方案B（documented simplification——冲销不恢复承付保守方向，方案A 自动恢复归 successor）。**Phase 2**——`ErpFinBudgetControlBiz.aggregateAmount` 重构为 `AmountChannel` enum（BUDGET/COMMITMENT/ACTUAL）+ `applyPostingTypeFilter`，ACTUAL 过滤器 `or(isNull, notIn(BUDGET, COMMITMENT))`（保留 RESERVATION 计入 actual），`check()` 显式三项式 `available = budget − actual − commitment`（数值等价旧公式）；`ErpPurReturnProcessor.approve` 后置 `runCommitmentReleaseOnReturnHook`（config-gate `erp-fin.commitment-release-on-return` 默认 false + `releaseIfPresent(PURCHASE_ORDER, poCode)` + PO code 经 `return.receive.order.code` 解析）；`IErpFinBudgetCommitmentBiz` 接口增 `releaseIfPresent`（容错 SPI）+ impl `@Override`；`ErpFinConstants` 增 `CONFIG_BUDGET_COMMITMENT_RELEASE_ON_RETURN`。**Phase 3**——budget.md §承付会计 接入点表 3→4 接入点（增 #4 release-on-return）+ 新增「全额释放语义」节（多发票容错 + 退货全额释放）+ 「冲销恢复语义」节（保守方向 + 残留风险 + successor 触发）+ 配置表增 release-on-return。
- **变更**：(1) `module-finance/erp-fin-service/.../budget/ErpFinBudgetControlBiz.java` — import ne→notIn + 类/方法 Javadoc 三通道化 + AmountChannel enum + applyPostingTypeFilter + check() 三项式。(2) `module-finance/erp-fin-service/.../ErpFinConstants.java` — CONFIG_BUDGET_COMMITMENT_RELEASE_ON_RETURN。(3) `module-finance/erp-fin-dao/.../biz/IErpFinBudgetCommitmentBiz.java` — releaseIfPresent 接口方法 + Javadoc。(4) `module-finance/erp-fin-service/.../budget/ErpFinBudgetCommitmentBizModel.java` — releaseIfPresent @Override + Javadoc。(5) `module-purchase/erp-pur-service/.../processor/ErpPurReturnProcessor.java` — LOG + IErpFinBudgetCommitmentBiz 注入 + ErpPurOrder import + approve() 后置 hook + runCommitmentReleaseOnReturnHook + resolvePurchaseOrderCode。(6) `docs/design/finance/budget.md` — §3 接入点表 4 接入点 + 全额释放语义节 + 冲销恢复语义节 + 配置表项。(7) 测试：`TestErpFinBudgetEndToEnd`（+2：084 commitment≠0 三段精确 1000−300−200=500 / commitment=0 等价回归+RESERVATION 计入 actual 1000−(300+100)−0=600）+ `TestErpFinBudgetCommitment`（+1：081 多发票全额释放单红冲 + 第二发票 ERR_BUDGET_COMMITMENT_ALREADY_RELEASED 守卫 + countCommitmentReversals 断言仅 1 张）+ 新增 `TestErpPurReturnCommitmentRelease`（082 ON 红冲 + 部分退货 qty5/qty100 全额释放）+ `return-commitment-test.yaml`。(8) `docs/backlog/audit-remediation-roadmap.md` — R1.27 ready→done。(9) 计划状态 active→completed / 3 Phase completed / 全 [x] / Closure 段填充独立 closure audit 证据（PASS）。
- **验证（full-green verification）**：`mvn clean install -DskipTests` BUILD SUCCESS（全 workspace reactor）；`mvn test -pl module-finance/erp-fin-service` Tests run: 含 084/081 新增全绿（TestErpFinBudgetEndToEnd 8 + TestErpFinBudgetCommitment 6）；`mvn test -pl module-finance/erp-fin-service,module-purchase/erp-pur-service` 两域全绿（082 TestErpPurReturnCommitmentRelease 2 + TestErpPurReturnApproval 6 + TestErpPurOrderCommitment 3 既有零回归）；grep 验证 084 三通道落地（AmountChannel enum + applyPostingTypeFilter + check() 三项式）+ 082 hook 落地（runCommitmentReleaseOnReturnHook + releaseIfPresent）+ 接口扩展向后兼容（IErpFinBudgetCommitmentBiz 仅 1 实现者）。
- **结束审计**：独立子代理（新会话，task ses_04f6c76abffeNUpvkMkTo6nLuq）closure audit = **Verdict: PASS**（084 三通道分离数值等价 + 082 双重 config-gate 默认 OFF 零回归 + owner doc 4 接入点/全额释放/冲销恢复语义齐全 + 测试覆盖 084/081/082 + 反模式自检无 @Inject private/无硬编码 postingType/ErrorCode 正确；审计者未自我审计）。
- **后续**：非阻塞；successor 已在计划 §Deferred But Adjudicated 命名触发条件——(1) 按开票金额比例部分释放（081 方案B：多组织预算硬约束启用 + 部分开票为常态）；(2) 发票冲销自动恢复承付（083 方案A：多组织预算硬约束启用 + 冲销频率上升）；(3) sales 侧承付释放路径对称接线（sales 承付控制启用时）。roadmap R1.27 ready→done。

### 2026-07-30（审计-修复 MR1 R1.26 hr 个税高档税率 NPE + 累计数据静默吞修复 + 计提/公司承担过账链路 Deferred — plan 0720-3）

- **任务**：执行 `docs/plans/2026-07-30-0720-3-r1-26-hr-payroll-tax-npe-silent-swallow.md` 全 4 Phase。落地三项 hr P1 finding（P1-MA4-016 + P1-MA4-017 + P1-MA4-018，源自 A4.4 hr 代码质量审计），**选择性裁决**（对齐 R1.17/R1.21/R1.25 先例：薪酬算术缺陷实现[016/018 containment 友好] + 业财过账链路功能缺口 Deferred[017 会计保护区域 + ORM 设计决策]）。
- **核心结论**：**Phase 1 Decision**——016 个税高档 NPE 实现（resolveBracket 末档 rangeUpperLimit=null 致 compareTo(null) NPE，高收入员工致 runPayroll 整批回滚）；018 parseCumulativeData 静默吞实现（catch-all 致累计损坏时静默重置少预扣个税）；017 计提+公司承担过账链路 Deferred（**显式声明偏离 arm-index 方案A 推荐**：会计保护区域 + ErpHrSalary 无 socialInsuranceER/housingFundER 列——方案A 须先裁决持久化设计属 ORM ask-first 设计决策 + 多 event 过账链路+approve 接线属 substantial slice 须独立 plan-audit）。**Phase 2**——`IncomeTaxCalculator.resolveBracket:225-232` compareTo 前增 `if (b.getRangeUpperLimit() == null) { selected = b; break; }`（末档表「无上限」语义），消除 NPE + 补 Javadoc。**Phase 3**——`ErpHrErrors` 增 `ERR_HR_CUMULATIVE_DATA_CORRUPT`（`erp.err.hr.cumulative-data-corrupt`，复用 ARG_EMPLOYEE_ID + ARG_YEAR）；`IncomeTaxCalculator.parseCumulativeData` 签名 +参数(employeeId, year)，移除 catch-all→LOG.warn + 抛 NopException（损坏响亮失败），null/空白 json 仍返回空 map（1 月无历史合法不变）；测试 `extractCumulativeData` 同步移除静默吞。**Phase 4**——payroll.md §6.5 表 + §9.1 表正式化 017 Deferred 标注 + 命名 successor 触发条件 + 持久化设计决策点 + 残留风险；§4.5 补 016/018 实现注记；SalaryPostingDispatcher javadoc（:32-38/:56-66）+ §6.1 posted 字段注（L370）核对一致（landing 于 R1.15/R1.16，无须重复编辑）。
- **变更**：(1) `module-hr/erp-hr-service/.../service/payroll/IncomeTaxCalculator.java` — SLF4J LOG + resolveBracket null 防御 + parseCumulativeData(employeeId, year) 移除静默吞改抛 ErrorCode + Javadoc。(2) `module-hr/erp-hr-service/.../service/ErpHrErrors.java` — ERR_HR_CUMULATIVE_DATA_CORRUPT。(3) `module-hr/erp-hr-service/.../service/TestErpHrPayrollEngine.java` — extractCumulativeData 移除静默吞 + 新增 testCorruptCumulativeDataThrowsNotSilentReset 集成测试（1 月核算→破坏 cumulativeData→2 月核算抛 ErrorCode）。(4) 新增 `module-hr/erp-hr-service/.../service/payroll/TestIncomeTaxCalculator.java`（纯函数单测 7 例：resolveBracket 末档/>960000/边界 960000/刚越界 + parseCumulativeData 损坏抛/null 空/合法解析）。(5) `docs/design/human-resource/payroll.md` — §4.5 注记 + §6.5 表 Deferred + §9.1 表 Deferred + 过账链路状态注。(6) `docs/backlog/audit-remediation-roadmap.md` — R1.26 todo→done。(7) 计划状态 active→completed / 4 Phase completed / 全 [x]。
- **验证（full-green verification）**：`mvn clean install -DskipTests` BUILD SUCCESS（全 workspace reactor，01:39 min）；`mvn test -pl module-hr/erp-hr-service` Tests run: 121, Failures: 0, Errors: 0, Skipped: 0（含新增 TestIncomeTaxCalculator 7 + TestErpHrPayrollEngine 增至 7 含 testCorruptCumulativeDataThrowsNotSilentReset；既有 13 测试类零回归）；grep 验证 016 落地（resolveBracket `getRangeUpperLimit()==null` 分支 :231）+ 018 落地（IncomeTaxCalculator/TestErpHrPayrollEngine 零 `catch.*ignored` + ERR_HR_CUMULATIVE_DATA_CORRUPT 抛出点 IncomeTaxCalculator:187）；compliance checker 本计划零新增生产代码命中（IncomeTaxCalculator 非 BizModel，无新增 daoFor/RuntimeException/@Inject private）。
- **后续**：非阻塞；successor 已在计划 §Deferred But Adjudicated + payroll.md §6.5/§9.1 双重命名触发条件——独立计提+公司承担过账链路 plan：(1) 裁决公司承担金额持久化设计（ORM ask-first 加 socialInsuranceER/housingFundER 列 or remark 暂存 or 过账时重算）；(2) 接线 tryPostAccrual（approve→APPROVED 联动）+ 持久化；(3) 新增 tryPostSocialInsuranceER/tryPostHousingFundER 组装 290/300 event；(4) 激活 posted writer；协同 P1-MA4-019[MR2 R2.13] 测试有效性。roadmap R1.26 todo→done。

### 2026-07-30（审计-修复 MR1 R1.25 aps OperationOrder 状态守卫实现 + aps/logistics cancel 审批门控 + logistics 部分签收 Deferred — plan 0720-2）

- **任务**：执行 `docs/plans/2026-07-30-0720-2-r1-25-aps-logistics-op-order-guards-cancel-approval-partial-receipt.md` 全 3 Phase。落地三项 aps+logistics P1 finding（P1-MA2-077 + P1-MA2-078 + P1-MA2-079，源自 A2.15 aps+logistics 状态机审查），**选择性裁决**（对齐 R1.17/R1.22 先例：状态守卫真实缺陷实现 + missing-feature/owner-doc-drift Deferred）。
- **核心结论**：**Phase 1 Decision**——077（aps OperationOrder start/complete/cancel 完全缺状态守卫）实现（arm-index 推荐方案A：start/complete/cancel 各加单点 status 守卫 + 1 ErrorCode，消除 if/else 死代码）；078（aps+logistics cancel 审批门控）+ 079（logistics 部分签收）Deferred（**078 显式声明偏离 arm-index 方案A 推荐**：工作流审批属 missing-feature 需审批 SPI + 角色-resource 种子，A2.18 确认审批基础设施全域不成熟，对齐 R1.18/R1.22/R1.23 missing-automation Deferred 范式；079 实现须 ORM ask-first 加列 + 承运商回调暂只发完整 DELIVERED，对齐方案A 方向）。**Phase 2**——`start` 删 if/else 死代码改守卫 status==PLANNED（非抛 ERR_APS_OP_ILLEGAL_TRANSITION expected=PLANNED）；`complete` 增守卫 status==IN_PROGRESS；`cancel` 增守卫 status∈{DRAFT,PLANNED,IN_PROGRESS}（对齐 owner doc §2 L41 + DRAFT 未排程可取消）。新增 ErrorCode `ERR_APS_OP_ILLEGAL_TRANSITION`（`erp.err.aps.op-illegal-transition`，复用 ARG_OP_CODE/ARG_CURRENT_STATUS + 新增 ARG_EXPECTED_STATUS）。**Phase 3**——aps/state-machine.md §6 cancel + logistics/state-machine.md §2/§4 部分签收 + §4 IN_TRANSIT→CANCELLED 均正式化 Deferred 标注 + 命名 successor 触发条件。
- **变更**：(1) `module-aps/erp-aps-service/.../service/ErpApsErrors.java` — ARG_EXPECTED_STATUS + ERR_APS_OP_ILLEGAL_TRANSITION。(2) `module-aps/erp-aps-service/.../entity/ErpApsOperationOrderBizModel.java` — import ErpApsErrors + start/complete/cancel 三守卫（NopException+ErrorCode+.param()，三处抛出点 :112/:127/:145，if/else 死代码消除）。(3) `module-aps/erp-aps-dao/.../biz/IErpApsOperationOrderBiz.java` — cancel Javadoc DRAFT/PLANNED→DRAFT/PLANNED/IN_PROGRESS。(4) `docs/design/aps/state-machine.md` — §3 终态守卫注 + §6 cancel Deferred+successor。(5) `docs/design/logistics/state-machine.md` — §2 部分签收 Deferred + §4 部分签收行 Deferred + §4 IN_TRANSIT→CANCELLED Deferred+successor。(6) 测试：`TestErpApsOperationOrderStateGuards.java`（11 例：start PLANNED 成功+FINISHED/CANCELLED 拒；complete IN_PROGRESS 成功+PLANNED/FINISHED 拒；cancel DRAFT/PLANNED/IN_PROGRESS 成功+FINISHED/CANCELLED 拒）。(7) `docs/backlog/audit-remediation-roadmap.md` — R1.25 todo→done。(8) 计划状态 active→completed / 3 Phase completed / 全 [x] / Closure 段填充独立 closure audit 证据（PASS）。
- **验证（full-green verification）**：`mvn clean install -DskipTests` BUILD SUCCESS（全 workspace reactor）；`mvn test -pl module-aps/erp-aps-service` Tests run: 36, Failures: 0, Errors: 0, Skipped: 0（含新增 `TestErpApsOperationOrderStateGuards` 11 + 既有 TestErpApsOperationOrderCrudSmoke 5 / TestErpApsScheduleManagement 5 / TestErpApsSchedulingEngine 6 / TestErpApsCapacityReservation 3 / TestErpApsCrossDomainIntegration 3 / TestErpApsDemandPlanning 3 零回归）；grep 验证 ERR_APS_OP_ILLEGAL_TRANSITION 三抛出点落地（BizModel:112/127/145）+ Containment 核实零内部/跨域调用方（mfg `ErpMfgScheduleToJobCardProcessor.java:40` 仅为注释引用，守卫不阻断级联路径）。
- **结束审计**：独立子代理（新会话，task ses_04fa2617fffecLGMA7nupYCAg1）closure audit = **Verdict: PASS**（9 项逐项经实仓 file:line 验证；审计者本人重跑 mvn test Tests run: 36, Failures: 0；无阻塞缺陷；保护区域 clearance 经 git diff --stat 确认仅触及 BizModel/Errors/IBiz javadoc + owner doc + plan + 测试，无 *.orm.xml schema / 会计过账 / 数据删除 / auth / 部署变更）。
- **后续**：非阻塞；successor 已在计划 §Deferred But Adjudicated 命名触发条件——(1) aps+logistics cancel-approve 审批工作流（审批工作流 SPI 落地时实现 cancel-approve 动作 + config-gated 角色-resource 门控 + _erp-*.action-auth.xml 种子）；(2) logistics 部分签收（承运商支持部分签收回调时实现 TRACKING_EVENT_PARTIAL 常量 + receivedQuantity/partialSignedQty 字段[须 ORM ask-first 加列] + 累计签收判定）。roadmap R1.25 todo→done。

### 2026-07-30（审计-修复 MR1 R1.24 crm stageId 单向递增守卫 + Event reminderMinutesBefore 死字段激活 — plan 0720-1）

- **任务**：执行 `docs/plans/2026-07-30-0720-1-r1-24-crm-stage-guard-reminder-dead-field.md` 全 3 Phase。落地两项 crm P1 finding（P1-MA2-075 + P1-MA2-076，源自 A2.14 crm 状态机审查）：(1) stageId 单向递增守卫未实现（owner doc §stageId 迁移规则「不能跳级回退」契约漂移）；(2) Event reminderMinutesBefore 字段死字段（ORM 列存在 + view 暴露为可编辑但后端从不读取）。
- **核心结论**：两项均实现（arm-index 推荐方案 A），不破坏已实现主路径。**Phase 1 Decision**——(a) 075 实现 config-gated STRICT 守卫（对齐 R1.17 owner doc 强制契约先例）；默认 `erp-crm.allow-stage-backward`=false（STRICT 拦截回退）；WARN-as-default 替代方案经考虑否决（会使 §审查提示「单向递增约束」永不强制）；行为变更影响核实——grep 测试仅前移路径 STAGE_QUALIFIED→STAGE_DEMO 零回退行使，STRICT 默认不破坏既有测试。(b) 076 实现激活既有字段（findDueReminders 单点改窗口计算：per-event reminderMinutesBefore 优先，null fallback 全局 window）。**Phase 2 落地**（075）—ErpCrmConstants（CONFIG_ALLOW_STAGE_BACKWARD + DEFAULT_ALLOW_STAGE_BACKWARD=false）+ ErpCrmConfigs（allowStageBackward()）+ ErpCrmErrors（ARG_FROM_SEQUENCE/ARG_TO_SEQUENCE + ERR_STAGE_BACKWARD_MOVE）+ ErpCrmLeadProcessor（moveStage 在 requireStage 后、doMoveStage 前增 validateStageDirection：fromStageId=null 跳过；toSeq<fromSeq 在 STRICT 默认抛 ERR_STAGE_BACKWARD_MOVE，true 时 LOG.warn 放行保留 convLog 审计；Javadoc 删「允许前移/回退」）。**Phase 3 落地**（076）—ErpCrmEventBizModel.findDueReminders 查询窗口上界取 max(global window, maxPerEventReminder) 覆盖 per-event reminder 大于全局窗口情况，流式过滤按 effectiveReminder=per-event?:global 仅保留 startDateTime∈[now, now+effectiveReminder] 的 event，@BizQuery 签名/@Optional windowMinutes/PLANNED 守卫/enabled=false 早返回不变。
- **变更**：(1) `module-crm/erp-crm-service/.../service/processor/ErpCrmLeadProcessor.java` — LOG 声明 + moveStage 调 validateStageDirection + 新增 protected validateStageDirection + doMoveStage/类 Javadoc 更新。(2) `ErpCrmConstants.java` — CONFIG_ALLOW_STAGE_BACKWARD + DEFAULT_ALLOW_STAGE_BACKWARD。(3) `ErpCrmConfigs.java` — allowStageBackward()。(4) `ErpCrmErrors.java` — ARG_FROM_SEQUENCE/ARG_TO_SEQUENCE + ERR_STAGE_BACKWARD_MOVE。(5) `module-crm/erp-crm-service/.../entity/ErpCrmEventBizModel.java` — findDueReminders per-event 窗口计算 + findMaxReminderMinutesBefore/toLocalDateTime 辅助 + 类 Javadoc 更新。(6) `docs/design/crm/state-machine.md` — §stageId 迁移规则 L40 + §异常路径 L56 + §审查提示 + Event §7 更新 + FunnelAggregationEngine monotonic 注记。(7) 测试：`TestErpCrmStageDirectionGuard.java`（4 场景：STRICT 回退拒/前移+convLog/fromStageId=null 跳过/同 sequence）+ `TestErpCrmStageDirectionGuardAllowBackward.java`（allow-backward=true 回退放行+convLog）+ `TestErpCrmEventPerEventReminder.java`（4 场景：reminder=1440 命中 now+1day/null fallback 60min/reminder=15 排除 now+45min/PLANNED 守卫）+ `TestErpCrmEventReminderDisabled.java`（enabled=false 返回空）+ 2 yaml（allow-stage-backward-test / event-reminder-disabled-test）。(8) `docs/backlog/audit-remediation-roadmap.md` — R1.24 todo→done。(9) 计划状态 active→completed / 3 Phase completed / 全 [x] / Closure 段填充独立 closure audit 证据（PASS）。
- **验证（full-green verification）**：`mvn clean install -DskipTests` BUILD SUCCESS（全 workspace 156 reactor 模块）；`mvn test -pl module-crm/erp-crm-service` Tests run: 137, Failures: 0, Errors: 0, Skipped: 0（含新增 4 个测试类共 10 测试全绿 + 既有 TestErpCrmLeadConversion 9 / TestErpCrmEventReminderTimeline 5 / TestErpCrmEventReminderJob 4 零回归）；grep 验证 075 validateStageDirection 落地（Processor:65/112）+ 076 getReminderMinutesBefore 读取落地（EventBizModel:116/141）；compliance checker 本计划零新增生产代码命中（Processor daoProvider 为既有既定模式，BizModel 用 doFindListByQueryDirectly 非新增 daoFor）。
- **结束审计**：独立子代理（新会话，task ses_04fbea97fffeKsMhUl9saqQaGP）closure audit = **Verdict: PASS**（A-G 七段逐项经实仓 file:line 验证；审计者本人重跑 mvn clean install -DskipTests BUILD SUCCESS + mvn test -pl module-crm/erp-crm-service -am Tests run: 137, Failures: 0；无阻塞缺陷；唯一非阻塞 nuance = findMaxReminderMinutesBefore 加一次额外查询拓宽扫描窗口，性能可忽略）。
- **后续**：非阻塞；successor 已在计划 Deferred But Adjudicated 命名触发条件——QUALIFIED 超过 7 天无 stageId 前移跟进提醒 Job（owner doc §L104 声明但 grep 零匹配，属 missing-automation 新 Job+scheduler，非两项 finding 范畴）。roadmap R1.24 todo→done。

### 2026-07-30（审计-修复 MR1 R1.23 b2b EDI 出站自动化全部缺失 Deferred 裁决 — plan 0631-3）

- **任务**：执行 `docs/plans/2026-07-30-0631-3-r1-23-b2b-edi-outbound-automation-deferred.md` 全 2 Phase。落地单项 P1 finding（P1-MA2-073，源自 A2.14 b2b 状态机审查）+ 伴随 2 项 P2（P2-MA2-068 文档内部不一致 + P2-MA2-069 TO_CANCEL dict 死状态）—— b2b EDI 出站自动化（自动发送 + ACK-timeout→ERROR + 自动重试指数退避 + ERROR>24h 升级）全部缺失。
- **核心结论**：纯 owner doc Deferred 标注，无代码/无 ORM/无 bean-comment 变更（mvn 门控按规则 7 例外删除）。**Phase 1 Decision**：073 Deferred（对齐 arm-index §P1-MA2-073 推荐方案 A 方向，无偏离）——理由：(1) 整个 b2b 子系统 config-gated OFF 默认（`erp-b2b.enabled` default false，`ErpB2bConfigs.java:9,27`）→ 默认 config 零生产暴露；(2) MFT transport 是 Mock-only Deferred SPI（`MockTransportAdapter` 唯一 impl，真实 AS2/SFTP/FTPS = `managed-file-transfer.md §Non-Goal`）→ 出站自动化属 Deferred transport 集成范畴；(3) 所有状态迁移方法（markSent/markAcknowledged/retry/cancel/archive）手工可达（状态机不破坏）；(4) LOG.warn/error 可见性；(5) 与 R1.18/R1.20 + 同批 R1.22 P1-MA2-071 missing-automation Deferred 范式一致。伴随 P2-068/069 一并 owner doc 标注收敛。**Phase 2**：state-machine.md 顶部整体 Deferred banner + §L-8/§4/§6（4 行）/§8/§9 场景 C 各控制点 Deferred 注 + §2 TO_CANCEL 中间态 Deferred 注 + §1 dict TO_CANCEL 预留 + 文末「实现偏离补注」（TransportManager wired-but-uncalled + bean comment vs 代码不一致）+「残留风险」（erp-b2b.enabled=true 运营依赖）。
- **变更**（仅文档）：(1) `docs/design/b2b/state-machine.md` — 顶部整体 Deferred banner + §L-8 自动化 Deferred 注 + §4 NACK/超时行自动重试 Deferred + §6 TO_SEND→SENT/SENT→ACKNOWLEDGED/SENT→ERROR/ERROR→TO_SEND 四行 Deferred + §8 ERROR>24h 升级 Deferred + §9 场景 C 性质澄清注 + §2 TO_CANCEL 中间态 Deferred 注 + §1 TO_CANCEL dict 预留注 + 新增「实现偏离补注」+「残留风险」两段。(2) `docs/backlog/audit-remediation-roadmap.md` — R1.23 todo→done。(3) 计划状态 active→completed / 2 Phase completed / 全 [x] / Closure 段填充独立 closure audit 证据（PASS）。
- **验证（full-green verification）**：纯文档计划零代码变更——`git status --short` 仅 `docs/design/b2b/state-machine.md` + 计划 + roadmap + 日志（零生产代码）。基线 grep 确认不变：`grep -rn TransportManager module-b2b/erp-b2b-service/src/main` 仅 bean 定义 + 类定义 + Mock 注释（**ErpB2bEdiDocBizModel 唯一 @Inject 为 ErpB2bEdiRegistry，不注入 TransportManager**，createOutbound:93 留 TO_SEND）；`grep -rln "class ErpB2b.*Job" module-b2b` 零匹配；`grep -rn ack-timeout module-b2b` 零匹配（无 ack-timeout config 键）；`DEFAULT_B2B_ENABLED = false`（config-gated OFF）；state-machine.md 15 处 Deferred 标注落地。
- **结束审计**：独立子代理（新会话，task ses_04fda636cffelZ2d1137iC5GKI）closure audit = **Verdict: PASS**（A-E 五段逐项经实仓 file:line 验证；无阻塞缺陷；唯一非阻塞 nuance = bean comment 修正 declared Non-Goal 已在实现偏离补注透明收敛）。
- **后续**：非阻塞；successor 已在 Deferred But Adjudicated + owner doc banner 双重命名触发条件——MFT transport 真实对接（AS2/SFTP/FTPS）上线时实现 `ErpB2bEdiOutboundJob`（cron-gated 调 TransportManager.send 推进 TO_SEND→SENT + ACK-timeout 扫描 SENT→ERROR + 自动重试 3 次指数退避 + ERROR>24h 升级经 `IErpSysNotificationBiz` + `erp-b2b.ack-timeout-seconds` config）；TO_CANCEL 两步取消 successor（业务要求时）；bean comment 修正随 TransportManager 出站接线收敛。roadmap R1.23 todo→done。

### 2026-07-30（审计-修复 MR1 R1.22 contract NEGOTIATION→TERMINATED 迁移实现 + EXPIRED Job Deferred — plan 0631-2）

- **任务**：执行 `docs/plans/2026-07-30-0631-2-r1-22-contract-negotiation-terminate-expiry-job-deferred.md` 全 3 Phase。落地两项 P1 finding（P1-MA2-071 + P1-MA2-072，源自 A2.14 contract 状态机审查），**选择性裁决**（对齐 R1.20 先例：便宜真实迁移缺陷实现 + missing-automation Deferred）。
- **核心结论**：**Phase 1 Decision**——072（NEGOTIATION→TERMINATED 迁移缺失）实现（arm-index 推荐方案A：terminate 守卫扩展接受 ACTIVE+NEGOTIATION 两源态）；071（EXPIRED 自动到期 Job + 续期草稿自动创建缺失）Deferred（**显式声明偏离 arm-index 方案A**：missing-automation + expire() 手工路径存在 + InvoicePlan 生成 unposted DRAFT 经人工审批兜底 + 不破坏业财一致，对齐 R1.18/R1.20 范式，successor `ErpCtContractExpiryJob` 命名）。**Phase 2**——`terminate` 守卫由「仅 ACTIVE」改为「既非 ACTIVE 也非 NEGOTIATION 抛 illegalTransition」，NEGOTIATION 路径行为与 ACTIVE 一致（仅 setStatus+updateEntity，无 signDate/version 归档差异，版本归档经 useLogicalDelete 既有语义）；新增 6 项 terminate 状态机测试。**Phase 3**——state-machine.md §2/§4/§7/§9 ACTIVE→EXPIRED + 续期草稿 + 到期提醒全 Deferred 标注 + InvoicePlan triggerInvoice EXPIRED 残留风险 watch-only 注。
- **变更**：(1) `module-contract/erp-ct-service/.../entity/ErpCtContractBizModel.java` — terminate 守卫扩展（`!eq(ACTIVE) && !eq(NEGOTIATION)` 抛 illegalTransition）+ NEGOTIATION 路径注释 + 类 Javadoc 补 NEGOTIATION→TERMINATED 迁移。(2) `module-contract/erp-ct-dao/.../IErpCtContractBiz.java` — terminate Javadoc ACTIVE→ACTIVE/NEGOTIATION。(3) `module-contract/erp-ct-service/src/test/.../TestErpCtContractTerminate.java` — 新增 6 例（ACTIVE/NEGOTIATION 成功 + DRAFT/SUSPENDED/EXPIRED/TERMINATED 拒绝）。(4) `docs/design/contract/state-machine.md` — §2 ACTIVE→EXPIRED Deferred + §3 NEGOTIATION→TERMINATED 实现偏离补注 + §4 续期草稿 Deferred + InvoicePlan EXPIRED 残留风险注 + §7 到期提醒 Deferred + §9 场景 A Deferred。(5) `docs/backlog/audit-remediation-roadmap.md` — R1.22 todo→done。(6) 计划状态 active→completed / 3 Phase completed / 全 [x] / Closure 段填充独立 closure audit 证据（PASS）。
- **验证（full-green verification）**：`mvn clean install -DskipTests` BUILD SUCCESS（全 workspace 154 reactor 模块）；`mvn test -pl module-contract/erp-ct-service` 43 tests 0 failures 0 errors（含新增 `TestErpCtContractTerminate` 6 + 既有 `TestErpCtContractCrudSmoke` 6 / `TestErpCtContractPosting` 4 / `TestErpCtContractRebate` 8 / `TestErpCtESignature` 19 零回归）；grep 确认 NEGOTIATION 在 terminate 守卫内（L110-112）。独立 closure audit（新会话子代理 ses_04fe12139ffeGiewfQfndqtIIb）CLOSURE_AUDIT_PASS：7 项验证全过（代码守卫/测试覆盖/owner doc Deferred/Deferred sanity 零 Job 零 config 键/计划一致性/无 ORM 会计保护区域触及/43 测试全绿）。
- **后续**：EXPIRED 自动到期 Job + 续期草稿自动创建（071 successor）登记于计划 §Deferred But Adjudicated——合同到期自动化需求时实现 `ErpCtContractExpiryJob`（cron-gated 扫描 ACTIVE 且 endDate<now 批量 expire，对齐 hr `ErpHrContractExpiryJob`）+ config-gated `erp-ct.auto-create-renewal-draft`（经 parentContractId 关联）+ 到期提醒经 `IErpSysNotificationBiz`；InvoicePlan triggerInvoice EXPIRED 守卫收敛随 Job successor 自然落地。属「被裁决的 successor」非降级 deferred（当前 expire() 手工路径存在 + unposted DRAFT 经人工审批兜底 + 不破坏业财一致）。

### 2026-07-30（审计-修复 MR1 R1.21 projects 状态机前置校验补齐 + Milestone/Billing/CostCollection dict 死状态 Deferred — plan 0631-1）

- **任务**：执行 `docs/plans/2026-07-30-0631-1-r1-21-projects-close-start-precondition-dict-deferred.md` 全 4 Phase。落地 3 项 P1 finding（P1-MA2-067 + P1-MA2-069 + P1-MA2-070，源自 A2.13 projects 状态机审查），**选择性裁决**（对齐 R1.20 先例：便宜真实前置缺陷实现 + dict 死状态 Deferred）。
- **核心结论**：**Phase 1 Decision**——067（closeProject 缺任务结束校验）实现（arm-index 推荐方向，单点前置 + 1 ErrorCode + config-gated STRICT/WARN）；070（startProject 缺字段前置 + cancelProject 多源）实现（a 加字段校验 + b owner doc 补多源声明）；069（Milestone/Billing/CostCollection dict 死状态）Deferred + dict 保留为预留（偏离 arm-index 推荐独立字典 + ORM ext:dict 改绑，对齐 R1.13/14/15/18-20 保留先例）。**Phase 2/3**——closeProject 增 `validateTasksFinished`（STRICT 拦截未结束任务 / WARN 放行）+ startProject 增 `validateStartPreconditions`（STRICT 拦截缺项目名/起止日期/预算 / WARN 放行）。**Phase 4**——state-machine.md 新增「CRUD 桩实体状态机（Deferred）」段 + cost-collection.md dict-value drift 注。
- **变更**：(1) `module-projects/erp-prj-service/.../ErpPrjConstants.java` — 增 `CONFIG_STRICT_PROJECT_TASK_COMPLETION_CHECK`/`CONFIG_STRICT_PROJECT_START_PRECHECK` 配置键。(2) `.../ErpPrjConfigs.java` — 增 `strictProjectTaskCompletionCheck()`/`strictProjectStartPrecheck()`（默认 true）+ DEFAULT 常量。(3) `.../ErpPrjErrors.java` — 增 `ERR_PROJECT_HAS_UNFINISHED_TASKS`/`ERR_PROJECT_START_PRECONDITION_FAILED` + `ARG_TASK_STATUSES`/`ARG_MISSING_FIELDS`。(4) `.../entity/ErpPrjProjectBizModel.java` — 注入 `IErpPrjTaskBiz` + closeProject 调 validateTasksFinished（OPEN 守卫后、refreshActualCost 前）+ startProject 调 validateStartPreconditions（transition 前）+ 两个 private 方法。(5) `TestErpPrjProjectPrecheck.java` — 新增 6 例（closeProject STRICT 拦截/全 DONE 放行/WARN 放行 + startProject STRICT 拦截/完整放行/WARN 放行）。(6) `TestErpPrjBudgetAndCollection.java` — 迁移 testCloseProjectFreezesAndRejectsNewTimesheet（任务 IN_PROGRESS→DONE 适配 STRICT 校验）+ 快照重录。(7) `_cases/.../erp_prj_task.csv` — 快照 STATUS IN_PROGRESS→DONE。(8) `docs/design/projects/state-machine.md` — §迁移完整性 DRAFT→OPEN/OPEN→COMPLETED 加校验说明 + cancelProject 多源声明（DRAFT/ON_HOLD→CANCELLED 业务扩展）+ §4 异常路径 + §审查提示 + 新增「§适用对象三 CRUD 桩实体状态机（Deferred）」段。(9) `docs/design/projects/cost-collection.md` — §4.2 dict-value drift 注 + §4.3 关闭前置注。(10) roadmap R1.21 todo→done。
- **实现注记**：ErpPrjTask.status 的 XMeta 仅允许 `[eq, in, dateBetween, dateTimeBetween]` 不允许 `notIn`，故 validateTasksFinished 用 `in {TODO, IN_PROGRESS, BLOCKED}` 计数未结束任务——等价于 `not in {DONE}`（task-status 字典仅 4 态无 CANCELLED）。
- **验证（full-green verification）**：`mvn clean install -DskipTests` BUILD SUCCESS（全 workspace 154 模块）；`mvn test -pl module-projects/erp-prj-service` BUILD SUCCESS——projects-service 全量 76 tests 0 failures / 0 errors（含新增 TestErpPrjProjectPrecheck 6 + 迁移 BudgetAndCollection 7 + ExpenseAggregation 4 + TaskDependency 12 等全绿）；grep 确认 validateTasksFinished:97/validateStartPreconditions:115 调用点 + 2 ErrorCode + 2 config 落地。
- **后续**：非阻塞；successor 已在 Deferred But Adjudicated 命名——069（项目管理全面状态机需求时 Milestone/Billing 状态机 + CostCollection 独立字典 + ORM ext:dict 改绑）、closeProject WARN 严格强制（业务裁决禁止任何未结束任务关闭时移除 WARN 分支）。roadmap R1.21 todo→done。

### 2026-07-30（审计-修复 MR1 R1.20 quality 业务作废联动 + dict 死状态 Deferred + NCR 无 CAPA 闭环门控实现 — plan 0512-3）

- **任务**：执行 `docs/plans/2026-07-30-0512-3-r1-20-quality-linkage-dict-ncr.md` 全 3 Phase。落地 3 项 P1 finding（P1-MA2-064 + P1-MA2-065 + P1-MA2-066，源自 A2.12 quality 状态机审查），**选择性裁决**（对齐 R1.15 先例：死状态/跨表面 Deferred + 便宜真实缺陷实现）。
- **核心结论**：**Phase 1 Decision**——064（业务作废联动取消质检单）Deferred（跨域 wiring purchase/sales/mfg cancel Processor 跨表面，偏离 arm-index 推荐实现）；065（6 处 dict 死状态 + CRUD 桩）Deferred + dict 保留为预留（偏离 arm-index 推荐删除，对齐 R1.13/14/15 保留先例）；066（NCR resolve 无 CAPA 闭环门控）实现（arm-index 推荐方向，本批次最 containment 修复：单方法门控 + 1 列 + 1 ErrorCode，针对闭环不变量非死状态/TODO 噪音）。**Phase 2**——state-machine.md §4 异常路径 + §实现偏离补注 业务作废联动正式化 Deferred（successor 命名）+ 新增「CRUD 桩实体状态机（Deferred）」段（QualityGoal/RiskRegister/Calibration/Review/SPC-CalcStatus/CAPA-OVERDUE dict 死状态合并标注零 writer + QMS successor）。**Phase 3**——ORM `noCapaReason` 列(propId=33) + `ERR_NCR_RESOLVE_NO_CAPA` ErrorCode + `allActionsCompletedAndVerified`/`requireResolveGate` 签名带 `noCapaReason`（空措施路径返回 `StringHelper.isNotBlank(noCapaReason)`，不再无条件 return true）+ `resolve`/`IErpQaNonConformanceBiz.resolve` 增 `@Optional @Name("noCapaReason")` 参 + owner doc §NCR 与 CAPA + inspection-integration §4.3。
- **附带修复**：qa-service pom 补 `app-erp-notify-service` test 依赖——修单域测试隔离缺口（qa→finance 编译依赖引入 `ErpFinPostingExceptionBizModel` 注入 `IErpSysNotificationBiz`，对齐 module-finance/erp-fin-service + module-sales/erp-sal-service 同型 test 依赖）。
- **变更**：(1) `module-quality/model/app-erp-quality.orm.xml` — ErpQaNonConformance 增 `noCapaReason` 列 propId=33。(2) `module-quality/erp-qa-service/.../ErpQaErrors.java` — 增 `ERR_NCR_RESOLVE_NO_CAPA`。(3) `module-quality/erp-qa-service/.../entity/NcrLifecycleService.java` — `allActionsCompletedAndVerified`/`requireResolveGate` 签名带 noCapaReason + `actionsGatePassed` 抽取 + 双错误码区分（空+blank→NO_CAPA；有措施未完成→CAPA_NOT_COMPLETED）。(4) `module-quality/erp-qa-service/.../entity/ErpQaNonConformanceBizModel.java` — resolve 增 `@Optional @Name("noCapaReason")` + setNoCapaReason 落库。(5) `module-quality/erp-qa-dao/.../IErpQaNonConformanceBiz.java` — resolve 接口签名对齐。(6) `module-quality/erp-qa-service/pom.xml` — 补 notify-service test 依赖。(7) `docs/design/quality/state-machine.md` — §4 异常路径 + §实现偏离补注 业务作废联动 Deferred + 新增「CRUD 桩实体状态机（Deferred）」段 + §NCR 与 CAPA noCapaReason 门控。(8) `docs/design/quality/inspection-integration.md` — §4.3 无 CAPA resolve 门控注。(9) `TestErpQaNcrCapaEndToEnd.java` — 新增 `testResolveNoCapaGate`（blank→ERR_NCR_RESOLVE_NO_CAPA + 非空→成功+落库）+ 既有 resolve 调用迁移注。(10) codegen 产物随 ORM regen（_gen 实体/xmeta/i18n/view.xml/SQL/InputOutputBean）。(11) roadmap R1.20 todo→done。
- **验证（full-green verification）**：`mvn clean install -DskipTests` BUILD SUCCESS（全 workspace 154 模块）；`mvn test -pl module-quality/erp-qa-service -am` BUILD SUCCESS——qa-service 全量回归 0 failures / 0 errors（含 TestErpQaNcrCapaEndToEnd 5 + TestErpQaNcrPosting 7 + SPC/recall/dashboard/inspection 全绿）；`bash docs/audits/nop-compliance-checker.sh` 本计划零新增命中（NcrLifecycleService 既非 BizModel 无新增 daoFor，gate 改动零 anti-pattern）；grep 确认空措施路径 `return StringHelper.isNotBlank(noCapaReason)`（非无条件 return true）+ `PROP_ID_noCapaReason = 33` regen。
- **结束审计**：独立子代理（新会话，task ses_05007067cffeJ50Z5O1D935wqL）closure audit = **CLOSURE_AUDIT: PASS**（9/9 check 逐项经实仓 file:line 验证；无阻塞缺陷）。
- **后续**：非阻塞；successor 已在 Deferred But Adjudicated 命名触发条件——064（业务作废自动取消质检需求时 cancelForBusinessBill Facade + Processor wiring）、065（计量管理/QMS 全面需求时各 CRUD 桩状态机迁移）、CAPA-always 严格强制（禁止任何无 CAPA resolve 时移除 noCapaReason 放行分支）。roadmap R1.20 todo→done。

### 2026-07-30（审计-修复 MR1 R1.19 inv StockTake 联动 + PickingOrder 死状态 Deferred 裁决 — plan 0512-2-r1-19）

- **任务**：执行 `docs/plans/2026-07-30-0512-2-r1-19-inventory-stocktake-picking-deferred.md` 全 2 Phase。落地 2 项 P1 finding（P1-MA2-062 + P1-MA2-063，源自 A2.11 inventory 状态机审查）—— StockTake completeTake 未自动生成盘盈/盘亏移动单 + PickingOrder PICKING/PICKED dict 死状态 + CRUD 桩。
- **核心结论**：纯 owner doc Deferred 标注，无代码/无 ORM 变更（mvn 门控按规则 7 例外删除）。**Phase 1 Decision**：两项均 Deferred（偏离 arm-index 推荐方案A，理由：062 实现 completeTake 自动比对→generateMove 触及移动单→过账链路跨表面；063 拣货执行属 WMS successor），对齐 R1.13/R1.14/R1.15「保留 dict 死状态为预留 + owner doc Deferred」先例，不破坏已实现主路径（StockTake DRAFT→CONFIRMED→DONE/CANCELLED 完整 + PickingOrder CRUD 主路径可用）。**Phase 2**：state-machine.md §盘点单状态机 ASCII 图移除幻影「自动生成」迁移 + 补 Deferred 标注（手工 `generateMove` 入口 + successor）；新增「拣货单生命周期（Deferred）」段（PICKING/PICKED 零 writer 预留 + CRUD 桩 + WMS successor）；§审查提示 增零-writer 一致性检查项。
- **变更**（仅文档）：`docs/design/inventory/state-machine.md`（+16/-3）。
- **验证**：grep 确认基线不变——`rg "PICKING_STATUS|setDocStatus.*PICKING" module-inventory/erp-inv-service/src/main` 退出码 1（PICKING/PICKED 零 writer）；`rg "generateMove" ErpInvStockTakeBizModel.java` 退出码 1（completeTake 仅置 DONE）；dict `erp-inv/picking-status` 4 值保留（orm:73-78）；Deferred 标注 4 处落地。
- **后续**：非阻塞；successor 已在 Deferred But Adjudicated + owner doc 双重命名（062：盘点闭环自动化需求时；063：WMS 上线时）。roadmap R1.19 todo→done。

### 2026-07-30（审计-修复 MR1 R1.18 assets IDLE 状态机迁移未实现 Deferred 裁决 — plan 0512-1-r1-18）

- **任务**：执行 `docs/plans/2026-07-30-0512-1-r1-18-assets-idle-state-machine-deferred.md` 全 2 Phase。落地单项 P1 finding（P1-MA2-061，源自 A2.10 assets 状态机审查）—— 资产卡片 `IN_SERVICE↔IDLE` 暂停/恢复迁移未实现（零 writer 死状态）。
- **核心结论**：纯 owner doc Deferred 标注，无代码/无 ORM 变更（mvn 门控按规则 7 例外删除）。**Phase 1 Decision**：方案 Deferred（与 arm-index §P1-MA2-061 推荐方向一致，对齐 R1.13/R1.14/R1.15 既有先例）—— IDLE 为低频资产管理场景，折旧引擎仅查 IN_SERVICE 已等价满足「IDLE 默认停提」语义，IN_SERVICE 折旧主路径完整不受影响；dict IDLE 值保留为预留语义入口（不删除）。**Phase 2**：state-machine.md Deferred 标注覆盖 §1 状态定义表 + §2 迁移完整性（ASCII diagram + 迁移表 + 集中 callout）+ §5 可达性 + §8 TODO 策略 + §实现模式与守卫边界 + §审查提示，每处命名 successor。
- **变更**（仅文档）：`docs/design/assets/state-machine.md`（+17/-7）—— IDLE 行标预留状态、迁移图/表标 Deferred、集中 Deferred callout（含 successor 触发条件）、§5 本期不可达注、§8 闲置超期 cron Deferred、§实现模式 IDLE Deferred 子节、§审查提示措辞对齐「折旧引擎仅查 IN_SERVICE 等价默认停提」。
- **验证**：grep 确认零 IDLE writer 基线不变（`setStatus(...IDLE)`/`setAssetStatus(...IDLE)` 全 module-assets 退出码 1）；`ErpAstConstants.java:67` ASSET_STATUS_IDLE="IDLE"；`ErpAstAssetBizModel.java` 17 行 CrudBizModel 桩（无 suspend/resume mutation）；折旧引擎 `ErpAstDepreciationScheduleProcessor:138` + KPI `ErpAstDashboardBizModel:179` 均仅查 IN_SERVICE；compliance checker 本计划零新增命中（R1d=17/R8=42/R2a=38/R2c=1238 均既有基线持平）。
- **结束审计**：独立子代理（新会话，task ses_05026887effe2xh4KPe9r17nDY）closure audit = **VERDICT: PASS**，无阻塞缺陷（基线事实 TRUE / owner doc Deferred 全落地 / 无 scope creep——git status 仅 state-machine.md + 计划文件变更 / successor 已命名）。
- **后续**：非阻塞；successor 已在 Deferred But Adjudicated 命名（PM 要求正式资产闲置/恢复工作流时实现 suspend/resume BizMutation + 折旧引擎扩展查 IN_SERVICE+IDLE + 闲置超期 TODO cron）。roadmap R1.18 todo→done。

### 2026-07-30（审计-修复 MR1 R1.17 pur/sal/ast reverseApprove + INLINE 守卫 + rollbackReceive 对称 — plan 0341-3-r1-17）

- **任务**：执行 `docs/plans/2026-07-30-0341-3-r1-17-pur-sal-ast-reverse-approve-state-machine.md` 全 3 Phase。落地 7 项 P1 finding（P1-MA2-049/050/051 [pur] + P1-MA2-056/057 [sal] + P1-MA2-058/059 [ast]），源自 A2.8/A2.9/A2.10 状态机审查。7 findings = reverseApprove 目标态漂移（SUBMITTED vs owner doc 强制 REJECTED）+ INLINE 动作缺守卫 + PurReversalListener.rollbackReceive 不对称。
- **核心结论**：纯一致性修复，不破坏红冲闭环（Quotation/Rfq/Contract/Movement 无 posted 副作用——不过账、无凭证需 reverse）。**Phase 1 Decision**：049/056/058 方案A（reverseApprove→REJECTED）；050/057/059 方案B（INLINE 补 isCancelled 守卫，业务守卫 submit 时点已门控不补）；051 方案A（rollbackReceive 对齐 REJECTED）。**Phase 2**：4 实体 reverseApprove 目标态 SUBMITTED→REJECTED + 清 approvedBy/At。**Phase 3**：38 处 INLINE isCancelled 守卫 + rollbackReceive 对称 + 三域 owner doc 实现模式节。
- **变更**：
  - **Phase 2**：`ErpPurQuotation.xbiz`/`ErpPurRfq.xbiz`（pur）、`ErpSalContract.xbiz`（sal）、`ErpAstMovement.xbiz`（ast）reverseApprove 块 `approveStatus='SUBMITTED'`→`'REJECTED'`（approvedBy/At 清空已在原代码）。
  - **Phase 3 守卫**：pur 8 实体（Quotation/Rfq 各 5、Receive/Invoice/Payment/Return 各 2、Requisition 3、Order 1=22）+ sal 7 实体（Contract 5、Delivery/Quotation/Return/Order/Invoice/Receipt 各 1=11）+ ast Movement 5 = **38 处 isCancelled 守卫**，前置 `if (entity.docStatus === 'CANCELLED') throw NopScriptError("nop.err.wf.approve.doc-cancelled")`（requireEntity 后、status 校验前；Processor 委派块无冗余守卫）。
  - **Phase 3 listener**：`PurReversalListener.rollbackReceive` 对齐 rollbackInvoice/Payment/Return（`if APPROVED setApproveStatus(REJECTED)`）+ Javadoc 更新。
  - **owner doc**：purchase/sales/assets `state-machine.md` 各补「实现模式与守卫边界」节（PROC vs INLINE 守卫边界）+ purchase 回退目标态表（receive→REJECTED）；`finance/posting.md` 裁决4 表 PURCHASE_INPUT 行更新。
  - **roadmap** R1.17 ready→done。
- **Proof 测试**：`TestErpPurQuotationRfqReverseApprove`（4 reverseApprove + 3 守卫阻断）+ `TestPurReversalListenerReceiveRollback`（2 回退对称）+ `TestErpSalContractReverseApprove`（2 reverseApprove + 2 守卫阻断）+ `TestErpAstMovementReverseApprove`（2 reverseApprove + 2 守卫阻断）。
- **验证（full-green verification）**：`mvn clean install -DskipTests` BUILD SUCCESS（全 workspace）；`mvn test` 范围内模块全绿——pur 130 / sal 140 / ast 96 全量回归 0 失败（守卫不破坏既有路径）；grep 验证 reverseApprove 段零 SUBMITTED + 38 守卫计数精确匹配；compliance checker R12a/b/c=69/66/38（=基线无增长），零新增生产代码高严重度违规。
- **结束审计**：独立子代理（新会话，task ses_0503e13d8ffeF3ML5TUGwOJe05）closure audit = **PASS**（7/7 finding 修复经实仓 file:line 验证；38 守卫位置正确；rollbackReceive 对称；owner doc 更新；无 scope creep——SalReversalListener.rollbackDelivery P2 watch-only + 死代码 Processor P2-054 正确排除）。
- **后续**：INLINE→完整大 Processor 迁移（方案A successor，若守卫边界被业务误用）+ SalReversalListener.rollbackDelivery 对齐（P2-MA2-057，若运营反馈成痛点）均登记为 Deferred But Adjudicated（触发条件已命名）。

### 2026-07-30（审计-修复 MR1 R1.16 业财过账错误传播分级策略整体裁决 — plan 0341-2）

- **任务**：执行 `docs/plans/2026-07-30-0341-2-r1-16-posting-error-propagation-grading-strategy.md` 全 4 Phase。落地 12 项 P1 finding（P1-MA2-032/048/060/068/074/080 [6 MA2] + P1-MA4-001/004/007/010/013/020 [6 MA4]），源自 A2.5a/A2.16/A4.1a/A4.1b/A4.2a/A4.2b/A4.3/A4.5 状态机与代码质量审查。12 findings = 同型根因——过账 dispatcher/编排层 `catch(Exception)` 过宽吞咽失败 → posted=false 悬挂 / GL 缺凭证 / 无告警闭环。
- **核心结论**：建立全域统一的**业财过账错误传播分级策略**（owner doc `posting-log.md` 权威），消除「catch-swallow → 静默悬挂」反模式。**G1 瞬时可重试**（sweep PENDING→retry）/ **G2 永久性失败**（MAX_RETRY 升级 MANUAL 终态 + 告警，消除 RETRYING 死状态）/ **G3 编排层跨域步骤**（impl 未就绪容错跳过 vs NopException 阻断结账）/ **G4 无 sweep 覆盖的域 dispatcher**（独立 IErpSysNotificationBiz 告警 + 期末前置检查兜底）。期末结账前置检查扩展覆盖 assets 折旧 + inventory 到岸成本 posted=false 悬挂。
- **变更**：
  - **Phase 1**（posting-log.md）：新增 §错误传播分级策略段（G1-G4 taxonomy + 处置规则 + 告警通道 + 期末前置检查覆盖矩阵）。
  - **Phase 2**（finance）：`ErpFinDeferredPostingRetryHelper` MAX_RETRY→MANUAL（REQUIRES_NEW 内重载受管实体）+ dispatchMaxRetryAlert；`ErpFinPostingExceptionBizModel` ignore() 补放弃态告警 + countUnresolved 加 MANUAL+voucherId-null；`ErpFinAccountingPeriodProcessor` 三处 catch 收窄（impl 未就绪容错 vs NopException 阻断）+ findUnresolvedPostingExceptionKeys 扩展覆盖 assets 折旧(status=EXECUTED)/inventory 到岸成本 posted=false；period-close.md §异常处理标注 G3 分级。
  - **Phase 3**（G4 dispatchers）：assets Depreciation/Capitalization/DisposalPostingDispatcher + inventory ErpInvLandedCostProcessor(reverse) + logistics GatewayDispatcher(deadLetter) 各补 @Inject IErpSysNotificationBiz + dispatchFailureAlert；assets depreciation-and-posting.md §错误处理 + state-machine.md（reverseApprove 不对称 deliberate 注）；logistics state-machine.md 网关死信告警注；inventory/logistics pom 补 notify-service test dep。
  - **Phase 4**（G3 dispatchers）：mfg ErpMfgWorkOrderProcessor(reportCompletion 差异，无 FIRMED 标准成本容错 vs 其他告警) + SubcontractPostingDispatcher(issue/receipt/fee 段告警) + hr SalaryPostingDispatcher + projects TimesheetPostingDispatcher + maintenance MaintenanceIssue/LaborPostingDispatcher 各补告警；mfg/hr/projects/maintenance state-machine.md/payroll.md/cost-collection.md 标注；projects/maintenance pom 补 notify-service test dep。
  - **Proof 测试**：TestErpFinPostingExceptionWorkbench#testMaxRetryEscalatesToManual（MAX_RETRY→MANUAL）+ TestDepreciationPostingFailureAlert（折旧告警+null-guard）+ TestTimesheetPostingFailureAlert（工时告警）。
  - roadmap R1.16 ready→done。
- **验证（full-green verification）**：`mvn clean install -DskipTests` BUILD SUCCESS（全 workspace 154 模块）；`mvn test` 范围内模块全绿——finance 294 / assets 90 / inventory 124 / logistics 23 / hr 113 / projects 69 / maintenance 56；mfg 140/141（1 项 `TestErpMfgCompletionPosting#testStandardCostCompletionPosting` 为本计划**前既有**快照漂移失败，经 git stash 核实与本次改动无关）；compliance checker 基线持平（R5 @Inject private=0，本计划新增 @Inject 全 package-private）。
- **结束审计**：独立子代理（新会话，task ses_0505f6731ffeOZEtlb1ktOcZ3u）closure audit = **PASS**（会计保护区域安全：MANUAL 终态正确、RETRYING 死状态消除、catch 收窄 accounting-safe fail-closed、12 dispatcher 告警派发 null-guard+降级）。审计发现 1 项非阻塞 correctness（assets 折旧前置检查应加 status=EXECUTED 排除 REVERSED schedule 误判，fail-safe 方向）——**执行者已即时修复**（finance 测试全绿）。
- **后续**：hr tryPostAccrual 死代码调用方接线归 R1.26（P1-MA4-017）；G4 告警通道噪声治理（config-gated `erp-fin.posting-alert.min-severity`）归运营 successor。均登记为 Deferred But Adjudicated（触发条件已命名）。

### 2026-07-30（审计-修复 MR1 R1.15 hr dict 死状态/CRUD 桩/硬编码/posted 死字段/排班无 dict — plan 0341-1）

- **任务**：执行 `docs/plans/2026-07-30-0341-1-r1-15-hr-state-machine-dict-dead-state.md` 全 4 Phase。落地 9 项 P1 finding（P1-MA2-039..047），源自 A2.7a/A2.7b hr 状态机审查 + A2.7/M4 code-quality。9 findings = dict 死状态/CRUD 桩/硬编码常量/posted 死字段/排班无 dict 绑定的 owner-doc 契约漂移。
- **核心结论**：9 findings 同型裁决（对齐 R1.13/R1.14 先例）。**方案B Deferred（6 项：039/040/041/042/043/045）**——owner doc 标注死状态为预留 successor（不动 dict/不实现 mutation）：员工 RESIGNED/TERMINATED/RETIRED、合同 SUSPENDED、调查 OPEN/CLOSED/ARCHIVED、发展计划 DRAFT/CANCELLED + 计划项 OVERDUE、工时单 APPROVED/REJECTED、银行付款文件 UPLOADED/CONFIRMED；每项命名 zero writer + successor 触发条件。**方案A 实现对齐（044）**——ErxHrConstants 新增 `TIMESHEET_STATUS_DRAFT/SUBMITTED` 常量，ErpHrTimesheetBizModel 替换硬编码 `"DRAFT"/"SUBMITTED"` 字面量。**方案A ORM ask-first（046）**——新增 `erp-hr/shift-assignment-status` dict（SCHEDULED/PRESENT/ABSENT/CANCELLED，以代码 `ASSIGNMENT_STATUS_*` 实际写入值为真相源）+ ErpHrShiftAssignment `status` 列补 `ext:dict` 绑定 + regen（不改列类型/不删列）。**方案B javadoc 对齐（047，与 R1.26 协同）**——posted 字段不删（R1.26 接线计提+公司承担过账后激活）；SalaryPostingDispatcher javadoc 移除「无 posted 字段」误导措辞，标注 posted Deferred + tryPostAccrual 死代码 + successor R1.26。
- **变更**：(1) `module-hr/erp-hr-service/.../ErpHrConstants.java` — 新增 TIMESHEET_STATUS_DRAFT/SUBMITTED 常量。(2) `module-hr/erp-hr-service/.../entity/ErpHrTimesheetBizModel.java` — 字面量→常量引用。(3) `module-hr/model/app-erp-hr.orm.xml` — 新增 shift-assignment-status dict 定义 + ErpHrShiftAssignment status 列 ext:dict 绑定。(4) `module-hr/erp-hr-meta/.../shift-assignment-status.dict.yaml` — codegen 生成产物。(5) `module-hr/erp-hr-service/.../posting/SalaryPostingDispatcher.java` — javadoc drift 修正（class + tryPostAccrual）。(6) `docs/design/human-resource/state-machine.md` — EmploymentStatus/Timesheet Deferred 注 + 新增「适用对象五：预留状态」节（Contract/Survey/DevPlan）。(7) `docs/design/human-resource/payroll.md` — BankFile §七 Deferred 注 + posted 字段 Deferred=R1.26 注。(8) roadmap R1.15 ready→done。
- **验证（full-green verification）**：`mvn clean install -DskipTests` BUILD SUCCESS（全 workspace 154 模块）；`mvn test -pl module-hr/erp-hr-service -am` BUILD SUCCESS——113 测试零 failures / 零 errors（含 TestErpHrShiftScheduling 13）；`xmllint --noout app-erp-hr.orm.xml` 退出 0（namespace 警告为平台常态）；`bash docs/audits/nop-compliance-checker.sh` 本计划零新增命中（R1a/b/c/R4/R5/R7/R11=0，R1d=17/R8=42 不变）；grep `"DRAFT"|"SUBMITTED"` 字面量 EXIT 1（零硬编码），XMeta `_ErpHrShiftAssignment.xmeta:72` dict 绑定生效。
- **结束审计**：独立子代理（新会话，task ses_050852461ffepLFJMdHUYz4xmZ）closure audit = PASS（0 Blocker / 0 Major / 0 Minor）。9 findings 逐项经实仓验证（非引用自述）。
- **后续**：员工离职/退休/转正 mutation（039，PM 要求正式工作流时）+ 各组件审批/确认/上传状态机（040/041/042/043/045）+ posted 字段激活（047，R1.26/P1-MA4-017 接线计提+公司承担过账时）均登记为 Deferred But Adjudicated（触发条件已命名）。

### 2026-07-30（审计-修复 MR1 R1.14 mfg dict 死状态 + owner doc 漂移 — plan 0143-3）

- **任务**：执行 `docs/plans/2026-07-30-0143-3-r1-14-mfg-dict-dead-state-owner-doc-drift.md` 全 3 Phase。落地三项 P1 finding：P1-MA2-035（作业卡 PARTIALLY_TRANSFERRED/MATERIAL_TRANSFERRED 死状态）+ P1-MA2-036（MRP CANCELLED + 预测 CONSUMED 死状态）+ P1-MA2-037（mrp.md RELEASED vs isFirmed 措辞漂移），源自 A2.6a/A2.6b manufacturing 状态机审查。**纯文档计划（零代码/ORM 变更，无 mvn/typecheck 门控）。**
- **核心结论**：三项均采纳 **Decision A**（保留 dict 值为预留 + owner doc 标注 Deferred，对齐 forecast CONSUMED 既有先例；不从 ORM 删 dict + 常量）。**Phase 1（P1-MA2-035）**——`state-machine.md` 作业卡图两态标 `⚠ 预留(Deferred)` + Deferred 说明块（零 setStatus writer 确认；successor = 转序功能）。**Phase 2（P1-MA2-036）**——`mrp.md` 增 MRP CANCELLED Deferred 注（零 cancelPlan writer 确认）+ forecast CONSUMED 自声明核对完整（mrp.md:88 + ErpMfgForecastBizModel.java:21 Javadoc）；`state-machine.md` 增一行 MRP-plan/Forecast 死状态指针节（不展开完整章节，P2-MA2-052 watch-only）。**Phase 3（P1-MA2-037）**——`mrp.md` 建议单释放 ASCII 图「状态标记为 RELEASED」修正为实际机制：行级 `isFirmed=true`（MrpReleaseService.markFirmed）+ 全部行 firmed 后 plan head → `FIRMED`（advancePlanToFirmedIfComplete）；补注块增 RELEASED→FIRMED/isFirmed 措辞更正注（mrp-status dict 无 RELEASED 值）。
- **变更**：(1) `docs/design/manufacturing/state-machine.md` — 作业卡图两态 Deferred 标注 + Deferred 说明块 + MRP-plan/Forecast 死状态指针节。(2) `docs/design/manufacturing/mrp.md` — 建议单释放 ASCII 图 RELEASED→isFirmed/FIRMED 修正 + 补注块 MRP CANCELLED Deferred + 预测 CONSUMED 自声明注 + RELEASED 更正注。(3) roadmap R1.14 todo→done（描述同步对齐 Decision A）。
- **验证**：纯文档计划——git status 仅两 mfg owner doc + 本计划文件变更（零代码/ORM 触及）。文本一致性 grep：全域 RELEASED 仅余 MaterialReservation.reservationStatus/lotStatus（无关）+ 本更正注；零 setStatus writer 经 rg EXIT 1 实仓确认（PARTIALLY_TRANSFERRED/MATERIAL_TRANSFERRED/MRP_STATUS_CANCELLED）。
- **结束审计**：独立子代理（新会话，task ses_0509f3f08ffeKGRzkkX00TlJOE）closure audit = PASS（0 Blocker / 0 Major / 1 Minor = 计划文件书签，非范围外代码变更）。逐项经实仓验证（非引用自述）。
- **后续**：转序/工序转移功能（作业卡 TRANSFERRED 两态 successor）+ MRP 取消/预测消费回写 successor 均登记为 Deferred But Adjudicated（触发条件已命名）。

### 2026-07-30（审计-修复 MR1 R1.13 finance 凭证/期间/预算状态机 — plan 0143-2）

- **任务**：执行 `docs/plans/2026-07-30-0143-2-r1-13-finance-voucher-period-budget-state-machine.md` 全 3 Phase。落地三项 P1 finding：P1-MA2-031（凭证 DRAFT→CANCELLED 不可达 + 红字凭证终态归属）+ P1-MA2-033（NEVER_OPENED→OPEN 迁移缺失）+ P1-MA2-034（carryForward 源年度全 CLOSED 前置未实现），源自 A2.5a/A2.5b finance 状态机审查。
- **核心结论**：**Phase 1（P1-MA2-034）**——`ErpFinBudgetScenarioProcessor.validateCarryForwardPreconditions` 增源年度 CLOSED 硬前置：`isSourceFiscalYearFullyClosed` 按 `source.fiscalYear` 查全部 `ErpFinAccountingPeriod` + 关联 `ErpFinAccountingPeriodStatus`，任一 `glStatus != CLOSED` 抛 `ERR_BUDGET_CARRY_FORWARD_RULE_INVALID`（带 rule + fiscalYear 参数，复用既有 ErrorCode）。**Phase 2（P1-MA2-033）**——**Decision A**：实现 `openPeriod` action。按强制顺序先 `IErpFinPeriodCloseBiz` 声明 `@BizMutation openPeriod(periodId, context)` → `ErpFinAccountingPeriodBizModel` Facade 委托 → `ErpFinAccountingPeriodProcessor.openPeriod` 实现（`requirePeriod` + `assertPeriodStatus(NEVER_OPENED, "开启")` 守卫 + 置 `PERIOD_STATUS_OPEN` + flush）。解除 NEVER_OPENED 可达性死锁，兑现 `IErpFinPeriodCloseBiz` javadoc 契约。**Phase 3（P1-MA2-031）**——**Decision A**：owner doc 对齐现状。`state-machine.md` §1/§2/§3/§5/§6 标注 CANCELLED 为「预留 dict 项（草稿废弃经 logical delete 承载）」；§3 红字凭证段对齐为「单边 `isReversed=true` 标记（原凭证保留 POSTED），不建立 `reversedVoucherId` 双向回链——已知简化」。
- **变更**：(1) `ErpFinBudgetScenarioProcessor.java` — `validateCarryForwardPreconditions` 增 CLOSED 前置 + `isSourceFiscalYearFullyClosed` helper + import `ErpFinAccountingPeriodStatus`。(2) `IErpFinPeriodCloseBiz.java` — 新增 `openPeriod` 接口声明 + javadoc。(3) `ErpFinAccountingPeriodBizModel.java` — 新增 `openPeriod` Facade。(4) `ErpFinAccountingPeriodProcessor.java` — 新增 `openPeriod` 实现。(5) `docs/design/finance/state-machine.md` — CANCELLED 预留 + 红字凭证单边 isReversed 对齐。(6) `TestErpFinBudgetCarryForward.java` — `seedOpenPeriod` 改 seed CLOSED/glStatus=CLOSED（既有 4 happy path 适配）+ 新增 `testRejectsWhenSourceFiscalYearNotFullyClosed` 负向测试。(7) `TestErpFinPeriodStateMachine.java` — 新增 `testOpenPeriodFromNeverOpened`（正向）+ `testOpenPeriodRejectsNonNeverOpened`（负向）。(8) roadmap R1.13 todo→done。
- **验证（full-green verification）**：`mvn clean install -DskipTests` BUILD SUCCESS（全 workspace 154 模块）；`mvn test -pl module-finance/erp-fin-service -am` BUILD SUCCESS——293 测试零 failures / 零 errors（含新增 carryForward 负向 1 + openPeriod 2，既有 4 carryForward happy path 在 CLOSED seed 下仍绿）。
- **后续**：cancelVoucher 显式作废工作流 + reversedVoucherId 双向回链登记为 Deferred But Adjudicated successor（触发条件：PM 要求「保留审计轨迹的显式作废动作」或双向回链报表需求）。closure audit gate（独立子代理）留待独立审计会话。

### 2026-07-30（审计-修复 MR1 R1.12 库存核算 SPECIFIC/STANDARD 成本不变量 — plan 0143-1）

- **任务**：执行 `docs/plans/2026-07-30-0143-1-r1-12-inventory-cost-specific-standard-invariants.md` 全 3 Phase。落地两项会计保护区域 P1 finding：P1-MA2-023（SPECIFIC 历史成本守卫缺失）+ P1-MA2-024（STANDARD 红冲成本不变量跨重估破缺），源自 A2.4 库存核算一致性审计。
- **核心结论**：**Phase 1（P1-MA2-023）**——`SpecificCostingStrategy.findSpecificLayers` 增 `businessDate` 末参 + `le(incomingDate, businessDate)` 过滤（对齐 FIFO/LIFO/BATCH），调用处传 `move.getBusinessDate()`，import `le`。出库不再消耗 businessDate 之后的同 batchNo 入库成本层（历史成本原则）。**Phase 2（P1-MA2-024）**——**Explore 证实**正常 STANDARD 采购入库 `StockMoveLine.unitCost` 持**实际采购价**（`ErpInvStockMoveProcessor.newLines:297-298` 写入；`InvPostingDispatcher.dispatchPurchasePriceVariance:125` 读此值为 actualUnitCost 算 PPV），非 0/null、非标准成本。**裁决 Choice B**（非 A）：`onOutgoing` 刷 `line.unitCost` 为出库时标准成本（对齐 FIFO:131-132）供 `reverse:144` 透传；`onIncoming` 仅当 `move.originReturnedMoveId != null`（冲销反向入库标记）且 `unitCost > 0` 时采用传入值（原出库扣减的旧标准成本），否则重解析当前标准成本——正常采购入库/内部调拨 `originReturnedMoveId=null` → 重解析（行为不变，PPV 不受影响）。跨 STANDARD_REVALUATION 红冲后 `balance.totalCost` 不变量恢复。**Phase 3**——`docs/design/finance/costing-methods.md` SPECIFIC 段补历史成本过滤契约 + STANDARD 段补红冲不变量契约。
- **变更**：(1) `SpecificCostingStrategy.java` — `findSpecificLayers` 增 businessDate 参+过滤 + import le + 调用处传 `move.getBusinessDate()`。(2) `StandardCostingStrategy.java` — 增 `@Inject IDaoProvider` + `onOutgoing` 刷 line.unitCost+saveOrUpdateEntity + `onIncoming` Choice B（originReturnedMoveId 守卫）。(3) `docs/design/finance/costing-methods.md` — SPECIFIC+STANDARD 两段契约补齐。(4) 新测试/强化：`TestErpInvSpecificCosting.testOutgoingIgnoresFutureDatedSameBatchLayer`（确定性拒绝守卫：qty=12 > 历史层 10 → ERR_COST_NOT_AVAILABLE；无过滤则成功）+ `TestErpInvStandardCosting.testReverseRestoresCostInvariantAcrossRevaluation`（跨重估红冲不变量，缺任一修复即失败）。(5) `TestErpInvStandardCosting` 快照 `erp_inv_stock_move_line.csv` 重录（onOutgoing 刷 line.unitCost 致 UNIT_COST 0→10 / VERSION 0→1，预期行为变更）。(6) roadmap R1.12 todo→done。
- **验证（full-green verification）**：`mvn clean install -DskipTests` BUILD SUCCESS（全 workspace 154 模块）；`mvn test -pl module-inventory/erp-inv-service` 全绿——124 测试零 failures / 零 errors（SPECIFIC 5 + STANDARD 6 含新增，CostAdjust 8 零回归）。
- **结束审计**：独立子代理（新会话）closure audit = PASS WITH NOTES（无 blocker）。审计指出 Phase 1 原 qty=8 测试为弱回归守卫，执行者据此强化为确定性拒绝变体。
- **后续**：`StandardCostResolver` as-of-date 解析登记为 Deferred successor（非阻塞，触发条件：历史报表重述需按业务日期回溯标准成本快照）。

### 2026-07-30（审计-修复 MR1 R1.10+R1.11 期末结账会计语义 + 反结账/凭证锁定 — plan 2322-3）

- **任务**：执行 `docs/plans/2026-07-29-2322-3-r1-10-r1-11-period-close-accounting-semantics.md` 全 4 Phase。R1.10+R1.11 裁决并落地六项会计保护区域 P1 finding：P1-MA2-017（auto-post-on-close 阻断分级）+ P1-MA2-018（年初余额非累计）+ P1-MA2-019（辅助账对账作用域）+ P1-MA2-020（反结账 kill-switch）+ P1-MA2-021（CLOSED_FINAL 凭证锁定）+ P1-MA2-022（FX 重估无前期 reversal）。
- **核心结论**：**Phase 1+2 裁决**——P1-MA2-017 实现（hasIssues 排除未核销 AR-AP + shortfall 独立硬阻断 + doc 默认值对齐代码 false）；P1-MA2-018 documented simplification（年初余额仅本年发生额，GL 余额引擎 successor）；P1-MA2-019 代码修复（sumArApOpenFunctional 增年度过滤，作用域一致）；P1-MA2-020 documented simplification（config kill-switch，审批流 successor）；P1-MA2-021 实现（postVoucher/reverseVoucher 增 assertPeriodNotLocked）；P1-MA2-022 documented simplification（当期 spot-rate 重估，IAS 21 残留风险）。**Phase 3 落地**——PeriodPreCheckReport API 重构 + closePeriod 阻断逻辑重构 + AnnualCloseService 年度过滤 + ErpFinVoucherBizModel 期间守卫 + ErpFinErrors 新增 ERR_FIN_VOUCHER_PERIOD_LOCKED + period-close.md/state-machine.md 已知简化标注。**Phase 4 验证**——TestErpFinVoucherPeriodLock 4 测试全绿 + 既有 E2E 零回归。
- **变更**：(1) `PeriodPreCheckReport.java` — hasIssues() 排除未核销 AR-AP + 新增 hasReminders()/hasAllowanceShortfall() + issueCount() 调整。(2) `ErpFinAccountingPeriodProcessor.java` — closePeriod allowance shortfall 独立硬阻断 + AR-AP 提示不阻断。(3) `AnnualCloseService.java` — sumArApOpenFunctional 增年度过滤（作用域统一）。(4) `ErpFinVoucherBizModel.java` — postVoucher/reverseVoucher 增 assertPeriodNotLocked。(5) `ErpFinErrors.java` — 新增 ERR_FIN_VOUCHER_PERIOD_LOCKED + ARG_PERIOD_STATUS。(6) `docs/design/finance/period-close.md` — auto-post-on-close 默认值对齐 + 已知简化与残留风险节。(7) `docs/design/finance/state-machine.md` — P1-MA2-020/021 标注。(8) 新测试：`TestErpFinVoucherPeriodLock.java`(4)。(9) 测试配置更新：period-close-end-to-end-test.yaml + auxiliary-recon-gate-test.yaml 增 bad-debt-allowance-gate-enabled: false。(10) TestErpFinBadDebt 快照 + 断言更新。(11) arm-index P1-MA2-017~022 ✅ resolved。(12) roadmap R1.10/R1.11 todo→done。
- **验证（full-green verification）**：`mvn clean install -DskipTests` BUILD SUCCESS（全 workspace 154 模块）；`mvn test`（全工作区）BUILD SUCCESS——finance-service 290 测试零 failures / 零 errors / 1 pre-existing skipped。
- **后续**：P1-MA2-018 successor（GL 余额维护）+ P1-MA2-020 successor（反结账审批流）+ P1-MA2-022 successor（FX 前期 reversal）登记为 Follow-up。closure audit gate（独立子代理）留待独立审计会话。

### 2026-07-30（审计-修复 MR1 R1.9 多币种 P2P/O2C 本位币凭证路径 + 收款核销汇兑损益 — plan 2322-2）

- **任务**：执行 `docs/plans/2026-07-29-2322-2-r1-9-multi-currency-p2p-o2c-voucher-fx-gain-loss.md` 全 3 Phase。R1.9 裁决并落地四项会计保护区域 P1 finding：P1-MA2-002（P2P 多币种未验证）+ P1-MA2-009（O2C 多币种+收款核销汇兑损益未实现）+ P1-MA3-025 MR1 侧（预算公式 javadoc 核实）+ P1-MA3-039 MR1 侧（persistVoucher amountSource=amountFunctional）。
- **核心结论**：**Phase 1 裁决**——(a) **Explore 证实 P1-MA3-039 bug**：`ErpFinPostingProcessor.persistVoucher:818-819` 写 `amountSource=amountFunctional=amt`（源币种），引擎未按 exchangeRate 折算。(b) **VoucherFact 方案 A**（增 `amountSource`/`amountFunctional` 双字段）：posting.md:488 明示本位币在文档创建时锁定，Provider 应显式传递而非引擎重新派生。方案 B（引擎折算）被拒。范围：迁移 PurAcctDocProvider+SalAcctDocProvider，其余域 Provider 单币种 fallback（successor 登记 Deferred But Adjudicated）。(c) **汇兑损益 plug 范围**：(a) RECEIPT 过账无需独立 plug（银行+应收同率同额自平衡）；(b) 核销环节补 realized FX plug（`ErpFinReconciliationBizModel.post` settleWithFx + CloseVoucherWriter 写 EXCHANGE_GAIN_LOSS 凭证），config-gate 默认 false。**Phase 2 落地**——VoucherFact 双字段 + persistVoucher 修正 + PurAcctDocProvider/SalAcctDocProvider 迁移 + ErpFinArApItemGenerator 折算 + ReconciliationSettler.settleWithFx + ErpFinReconciliationBizModel FX plug（generate + reverse）+ 预算 javadoc 修正。**Phase 3 验证**——TestErpPurMultiCurrencyPosting 2 + TestErpSalMultiCurrencyReconFx 1 全绿。
- **变更**：(1) `module-finance/erp-fin-service/.../service/posting/VoucherFact.java` — 增 `amountSource`/`amountFunctional` 双字段。(2) `ErpFinPostingProcessor.java` — persistVoucher 忠实写入双字段（debit/credit 按 functional）+ translateFactsForSchema 复制双字段。(3) `module-purchase/.../PurAcctDocProvider.java` — 迁移双字段（functional=source×rate）。(4) `module-sales/.../SalAcctDocProvider.java` — 同。(5) `ErpFinArApItemGenerator.java` — 按 event.exchangeRate 折算辅助账 functional。(6) `ReconciliationSettler.java` — 新增 `settleWithFx`（per-item functional + FX 差额计算）。(7) `ErpFinReconciliationBizModel.java` — 注入 IErpFinVoucherBiz + post()/reverse() FX plug 接入 + generateReconFxVoucher/reverseReconFxVoucher/hasFxVoucher/resolvePeriodId 辅助。(8) `ErpFinConstants.java` — `CONFIG_RECON_FX_GAIN_LOSS_ENABLED`。(9) `ErpFinBudgetControlBiz.java` — javadoc 修正（actualBalance 含 COMMITMENT 等价三项式）。(10) `docs/design/finance/posting.md` — §多币种处理 增实现契约段。(11) `docs/design/finance/ar-ap-reconciliation.md` — §汇兑损益核销规则 增实现契约段。(12) 新测试：`TestErpPurMultiCurrencyPosting.java`(2) + `TestErpSalMultiCurrencyReconFx.java`(1)。(13) arm-index P1-MA2-002/009 + P1-MA3-025/039 MR1 侧 ✅ resolved。(14) roadmap R1.9 todo→done。
- **验证（full-green verification）**：`mvn clean install -DskipTests` BUILD SUCCESS（全 workspace）；`mvn test`（全工作区）BUILD SUCCESS——零 failures / 零 errors / 1 pre-existing skipped。finance 135 + purchase（含新增 2）+ sales 136（含新增 1）+ 全域下游模块全绿。config-gate 默认 false → 既有单币种测试零回归。
- **后续**：VoucherFact 双字段全域迁移（其余 ~18 AcctDocProvider）登记为 Deferred But Adjudicated successor——触发条件：其余域 Provider 双字段迁移完成时复核。MR2 侧 doc 更新（posting.md/budget.md 多币种折算路径文档化）归 R2.4。closure audit gate（独立子代理）留待独立审计会话。
