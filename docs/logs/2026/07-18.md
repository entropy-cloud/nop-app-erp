# Development Log - 2026-07-18

### 2026-07-18（plan 2026-07-18-2251-1 Manufacturing 生产差异重算孤儿凭证修复）

- **背景**：执行 `docs/plans/2026-07-18-2251-1-manufacturing-variance-recompute-reversal.md`。承接跨域过账红冲缺口系统性审计 Phase 2（同型已 closed：1745-1 maintenance + 1745-2 inventory/manufacturing + 1745-3 finance）：manufacturing 域 `ErpMfgCostVariance.calculateVariances` 重算路径当前 `deleteByWorkOrder` 删数据行不红冲既有 PRODUCTION_VARIANCE 凭证，造成「数据行新金额 + GL 旧凭证金额」数据分叉（R13 不可降级项）。两同型 call site：A=`ErpMfgCostVarianceBizModel.calculateVariances:68` 手动重算入口 + B=`ErpMfgWorkOrderProcessor:229` config-gated 完工自动重算入口。

- **Phase 1（Decision）**：闭合 1 Decision——`reverseIfExists` 异常处理范式采用 (a) 始终调用 + 本地 try/catch 守护吞异常（对齐 `dispatchIfApplicable:109-115` 过账失败范式）。替代方案 (b)（按 posted 状态门控）拒绝：增加 BizModel 与 dispatcher 状态协调耦合，违反 dispatcher 自洽范式。(c)（显式查 voucher 存在性）拒绝：跨域 finance 读依赖无必要。残留风险：本地 catch 会吞所有异常（含真实红冲失败），mask 为"无凭证或红冲失败"，经 log warn 可观测 + finance 5.1 异常工作台兜底。Phase 3 JUnit (d) 用例覆盖容错路径。

- **Phase 2（后端实现）**：
  - **`ProductionVarianceDispatcher.reverseIfExists(Long workOrderId)`** 新方法（`module-manufacturing/erp-mfg-service/src/main/java/app/erp/mfg/service/posting/ProductionVarianceDispatcher.java:117-142`）：经 `daoProvider.daoFor(ErpMfgWorkOrder.class).getEntityById(workOrderId)` 取工单 → 构造 `billHeadCode = wo.code + "-PV"`（对齐正向 `buildEvent:122`）→ 调 `executor.reverse(billHeadCode, PRODUCTION_VARIANCE)`；工单不存在安全 return；catch Exception log warn 不抛（吞 `ERR_REVERSE_SOURCE_NOT_FOUND` 与真实红冲失败）。`dispatchIfApplicable` 既有幂等判定不动（重算后行 posted=false 幂等判定不触发，正常派发新凭证）。
  - **Call site A** `ErpMfgCostVarianceBizModel.calculateVariances:69` 在 `deleteByWorkOrder` 前增 `reverseIfExists` 调用（红冲→删旧→重算→派发四步链）。
  - **Call site B** `ErpMfgWorkOrderProcessor.reportCompletion:231` 同步增 `reverseIfExists`（同型修复，规则 14 bundling；try 块内 try/catch 语义不破坏）。

- **Phase 3（测试 + 文档）**：
  - **新 JUnit 类 `TestErpMfgVarianceRecomputeReversal`**（4 用例全绿）：(a)+(b) Call site A 单凭证反向 + 关键一致性断言（重算后 ErpFinVoucherBillR 反查仅 1 条 isReversed=false NORMAL 凭证）；(c) Call site B 完工自动重算反向（reportCompletion 重入触发 reverseIfExists）；(d) 红冲失败容错（手工注入 ThrowingMfgPostingExecutor 抛 RuntimeException → reverseIfExists log warn 不阻断 calculateVariances）；(e) 首次容错（首次 reverseIfExists 触发 SOURCE_NOT_FOUND → 吞异常 + 派发新凭证）。
  - **既有快照重录**：执行期发现 `IErpFinVoucherBiz.reverse` 在无原凭证场景，平台 `ErpFinPostingProcessor` 经 `ErpFinPostingExceptionRecorder` 以 REQUIRES_NEW 事务写入 `ErpFinPostingException` 行（消耗全局序列值），导致 `TestErpMfgProductionVariance` 4 个含过账快照失配。已用 `SnapshotTest.RECORDING` 重新录制 10 个 test method 的快照（含新增 `erp_fin_posting_exception.csv` + 漂移 voucher/sequence id + `testManualCalculateVariancesIdempotent` 现 3 张凭证反映「原 NORMAL 被红冲 + REVERSAL 红字 + 新 NORMAL」正确重算幂等闭环）。
  - **E2E `mfg-variance-recompute-reversal.action.spec.ts`** 新建（1 用例）：复用 1800-2 既有 `runMfgChain` + variance setup（测试专用成品物料 + FIRMED rollup，config 开）→ runMfgChain 满量完工产 NORMAL PRODUCTION_VARIANCE 凭证 → 再次 `ErpMfgCostVariance__calculateVariances` 触发 reverseIfExists → 断言原 NORMAL 凭证 isReversed=true + REVERSAL 红字凭证行同向取负（Dr 1410=-100/Cr 1411=-100 等）+ ErpFinVoucherBillR 反查仅 1 条 isReversed=false NORMAL + 数据行全 posted=true。
  - **owner-doc**：`docs/design/manufacturing/variance-analysis.md` 新增「重算幂等实现注记」章节（红冲→deleteByWorkOrder→重算→dispatchIfApplicable 四步链 + 数据/凭证一致不变量 + 异常处理范式 + 红冲失败孤儿凭证风险可观测）；`docs/testing/e2e-runbook.md` 业务动作表 +1 manufacturing 重算红冲行 + 套件计数段补 2251-1 增量。

- **验证状态**：`mvn test -pl module-manufacturing/erp-mfg-service -am` BUILD SUCCESS（含新 JUnit 4 用例 + 重录 snapshot 10 + 既有 mfg 测试套件）；154 模块 `mvn clean install -DskipTests` BUILD SUCCESS（1:31）；`mvn test` 全 154 模块 BUILD SUCCESS（7:54，0 failures）。

- **范围纪律**：纯应用层 Java（dispatcher 新方法 + 两 BizModel/Processor 触发点接线）+ 测试 + 文档。零 ORM/契约/字典/种子/config 变更。Deferred 已裁决：行级红冲（不同结果面 successor）+ `IErpFinVoucherBiz.reverse` 平台契约歧义修订（nop-entropy 上游 successor）+ 红冲失败孤儿凭证监控（被动监控，归 finance 异常工作台）。

Skill: `nop-backend-dev`（Phase 2 dispatcher.reverseIfExists + 两 call site 接线 + 跨域 `IErpFinVoucherBiz.reverse` 异常处理）+ `nop-testing`（Phase 3 JunitAutoTestCase 4 用例 + 浏览器层 E2E + 凭证行同向取负断言 + SnapshotTest.RECORDING 重录）

### 2026-07-18（plan 2026-07-18-1745-3 Finance 坏账 + 员工借款现金还款过账红冲闭环）

- **背景**：执行 `docs/plans/2026-07-18-1745-3-finance-posting-reversal-closure.md`。承接跨域过账红冲缺口系统性审计（同型缺口见 `1745-1` maintenance + `1745-2` inventory/manufacturing 已 closed）：finance 域 `ErpFinBadDebt` approve 内联过账无 reverse 入口 + `ErpFinEmployeeAdvance.cashRepay` 多次还款凭证无红冲入口（dispatcher.reverseSettle 已存在但 cashRepay 路径未接线）。两实体同属 finance 域，共享同一红冲范式，按规则 14 合并为单计划。

- **Phase 1（Decision）**：三 Decision 闭合（无 Explore 残留）：(a) `reverseCashRepay` 入参形态采用 (b) 简化版——单参 `advanceId` 内部反查最新 cashRepay 凭证，调用方无须持有 voucherId（业务语义"撤销最近一次现金还款"）；(b) cashRepay 凭证反查路径——经 `ErpFinVoucherBillR` `billCode LIKE 'EA-CASH-REPAY-<advanceCode>-%'` + `businessType=EMPLOYEE_ADVANCE_SETTLE` + voucher.isReversed=false 取最新一笔（执行期修正：原裁定提到的 SETTLE_TYPE=CASH 过滤维度不可达——SETTLE_TYPE 存于 voucher.billData 非持久化字段，但 billCode 前缀 `EA-CASH-REPAY-` 已唯一标识 CASH 路径，按前缀过滤等价区分）；(c) 坏账 reverseApprove DIRECT 路径——ErpFinBadDebt ORM tagSet 无 useWorkflow 故不经 xwf，调 FinPostingExecutor.reverse(debt.code, BAD_DEBT_WRITE_OFF|RECOVERY) + ArApItem 状态对称回退。

- **Phase 2（后端实现）**：
  - **ErpFinBadDebtProcessor.reverseApprove** 新方法（`module-finance/erp-fin-service/src/main/java/app/erp/fin/service/processor/ErpFinBadDebtProcessor.java`）：守卫 `approvalStatus=APPROVED && voucherId != null`（ErpFinBadDebt **无 posted 字段**——执行期裁定以 voucherId 非空作为已过账门控；plan 原文 posted=true 修正）→ 调 `FinPostingExecutor.reverse` 红冲凭证 → ArApItem 状态对称回退（writeOff: WRITTEN_OFF→OPEN, settled-=amount, open+=amount；recovery: OPEN→WRITTEN_OFF, settled+=amount, open-=amount）→ 翻 `approvalStatus=REJECTED`（保留 voucherId 不动——原凭证 isReversed=true 已表明状态，保留供审计回溯）。事务边界：红冲失败抛 NopException 触发事务回滚（反审核为补救路径强一致保证）。
  - **ErpFinEmployeeAdvanceBizModel.reverseCashRepay** 新方法（`module-finance/erp-fin-service/src/main/java/app/erp/fin/service/entity/ErpFinEmployeeAdvanceBizModel.java`）：经 `daoProvider().daoFor(ErpFinVoucherBillR.class)` 反查（**执行期修正**：BizModel 内不能用 `@Inject IDaoProvider` 字段——CrudBizModel 已提供 `daoProvider()` 方法，新 @Inject 字段在 IoC 容器中未被注入致 NPE，改用方法调用形式对齐 ErpFinReconciliationBizModel 范式）→ voucher.isReversed=false 取最新一笔 → 调既有 `advancePostingDispatcher.reverseSettle(billHeadCode)` 红冲 + 按原 voucher.totalDebit 回退字段。次序：**先红冲凭证、后回退字段**（与 cashRepay 正向"字段先于凭证"相反——反审核是补救路径需强一致，红冲失败抛异常事务回滚避免残留半状态）。
  - **IErpFinBadDebtBiz.reverseApprove + IErpFinEmployeeAdvanceBiz.reverseCashRepay** 接口声明（erp-fin-dao 模块）。
  - **2 新 ErrorCode**：`ERR_EMPLOYEE_ADVANCE_CASH_REPAY_VOUCHER_NOT_FOUND`（cashRepay 凭证缺失守卫）+ `ERR_BAD_DEBT_NOT_APPROVED_OR_NOT_POSTED`（坏账未过账守卫）。

- **Phase 3（测试 + 文档）**：
  - **2 JUnit 类全绿**（6 tests）：`TestErpFinBadDebtReversal`（writeOff/recovery 反向 + 未过账守卫 3 用例）+ `TestErpFinEmployeeAdvanceCashRepayReversal`（全额反向 + 多笔仅撤销最新 + 凭证缺失守卫 3 用例）。**执行期修复**：测试 seed period 须覆盖今天（reverseApprove 触发红冲凭证 voucherDate=today，参 TestErpFinEmployeeAdvanceCashRepay.seedCurrentMonthOpenPeriod 范式）；初次录制须 `-Dnop.autotest.force-save-output=true` 录制快照再 CHECKING。
  - **2 E2E spec 全绿**（4 tests，44.6s）：`fin-bad-debt-reverse-approve`（writeOff/recovery 反向 2 用例，红字凭证行同向取负 Dr 1231=-100/Cr 1122=-100 + Dr 1122=-100/Cr 1231=-100 + ArApItem 状态回退）+ `fin-employee-advance-cash-repay-reverse`（cashRepay 反向 + 凭证缺失守卫 2 用例，红字凭证行 Dr 1002=-500/Cr 1221=-500）。
  - **owner-doc 对齐**：`bad-debt.md §步骤 6 反审核红冲`（新增章节，详述 reverseApprove 反向语义 + DIRECT 路径 + 门控 + 事务边界 + voucherId 保留；执行期裁定：treasury.md 无 §坏账 章节，坏账 owner doc 在 bad-debt.md）；`expense-claim.md §红冲联动 l.196` 补 cashRepay 兑现注记（标记 owner-doc 漂移已修复，规则 13 不可降级项已兑现）。
  - **e2e-runbook** 业务动作表 +2 finance 红冲行 + 套件计数 79→81 + 计数段补 1745-3 增量。

- **验证状态**：`mvn test -pl module-finance/erp-fin-service` **201 passed**（195→201，+6 新 JUnit）；`BASE_URL=http://127.0.0.1:8011 npx playwright test tests/e2e/business-actions/fin-bad-debt-reverse-approve.action.spec.ts tests/e2e/business-actions/fin-employee-advance-cash-repay-reverse.action.spec.ts --workers=1` **4 passed**（44.6s）；`mvn clean install -DskipTests` **154 模块 BUILD SUCCESS**。

- **范围纪律**：纯应用层 Java（BizModel + Processor + IBiz + Errors）+ 测试 + 文档。零 ORM/契约/字典/种子/config 变更。Deferred 已裁决：坏账 provision 红冲（不同结果面 successor）+ EmployeeAdvance 三金额闭环 ORM 字段拆分（0718-2 ORM 保护区域 successor）+ 薪资扣回路径（HR 模块 successor）+ NotesPayable/Receivable HONORED 终态红冲（owner-doc 未声明义务 successor）。

Skill: `nop-backend-dev`（Phase 2 BizModel 新 `@BizMutation` + 跨实体 ArApItem 状态回退 + dispatcher 既有 API 接线 + owner-doc 漂移修复）+ `nop-testing`（Phase 3 JunitAutoTestCase + 浏览器层 E2E + 凭证行同向取负断言）


### 2026-07-18（plan 2026-07-18-1745-2 inventory 到岸成本 + manufacturing 领料过账红冲闭环）

- **背景**：执行 `docs/plans/2026-07-18-1745-2-inventory-manufacturing-posting-reversal.md`。承接跨域过账红冲缺口系统性审计（同型缺口见 `2026-07-18-1745-1` maintenance 域已 closed）：inventory `ErpInvLandedCost` 与 manufacturing `ErpMfgMaterialIssue` 正向过账已落地但反向红冲完全缺失，构成业财过账闭环缺口。两域共享同一红冲范式（dispatcher.reverse + executor.reverse + BizModel 反向入口）。本计划合并执行。

- **Phase 1（Decision：成本层回退路径裁定）**：两 Decision 闭合为采用既有 API：(a) `CostAdjustmentService.reverseCostAdjust` 已存在（`CostAdjustmentService.java:78`）按 `reverseLine` 反向 adjustAmount + `removeFifoAdjustLayer` 按 `-line.id` 哨兵删 FIFO 调整层 + `removeFirmedRollup` 删 STANDARD_REVALUATION rollup——直接复用，拒绝负金额重入 `applyCostAdjust`（FIFO 会追加新调整层双计）；(b) reverse 入参 billHeadCode 派生——`LandedCostPostingDispatcher` 正向=`landedCost.code`（无后缀）、`ManufacturingIssuePostingDispatcher` 正向=`issue.code + "-MI"`（无后缀），dispatcher `reverse(entity)` 直接重拼，无需经 `ErpFinVoucherBillR` 反查。

- **Phase 2（dispatcher.reverse 方法）**：
  - **`LandedCostPostingDispatcher.reverse(ErpInvLandedCost)`**：billHeadCode=`landedCost.code` 对称正向 `tryPost:69`，调 `InvPostingExecutor.reverse(..., LANDED_COST)`（executor.reverse 已存在无需扩展）；null/code 守卫。
  - **`ManufacturingIssuePostingDispatcher.reverse(ErpMfgMaterialIssue)`**：billHeadCode=`issue.code + "-MI"` 对称正向 `buildEvent:123`，调 `MfgPostingExecutor.reverse(..., MANUFACTURING_ISSUE)`（executor.reverse 已存在）。
  - inventory + manufacturing 既有测试全绿（112 + 122 = 234 tests 0 回归）。

- **Phase 3（BizModel 反向入口）**：
  - **`ErpInvLandedCostProcessor.reverseApprove(id, context)`** 新方法（protected step 方法）：守卫 `validateCanReverse`（posted=true + APPROVED，否则抛 `ERR_LANDED_COST_NOT_POSTED`）→ `doReverseApprove`（try `postingDispatcher.reverse` 红冲 LANDED_COST 凭证吞异常告警 → findCostAdjustForLandedCost 按 `LC-{code}` 命名约定反查 → `costAdjustmentService.reverseCostAdjust` 反向应用成本层 → 翻 posted=false / approveStatus=REJECTED / docStatus=CANCELLED + 同步 CostAdjust posted=false）。
  - **`ErpInvLandedCostBizModel.reverseApprove(id, context) @BizMutation`** + **`IErpInvLandedCostBiz.reverseApprove`** 接口声明。
  - **`ErpInvErrors.ERR_LANDED_COST_NOT_POSTED`** 新 ErrorCode。
  - **`ErpMfgMaterialIssueBizModel.reverseConfirm(issueId, context) @BizMutation`** 新方法：守卫 `validateCanReverse`（posted=true + DONE，否则抛 `ERR_MATERIAL_ISSUE_NOT_POSTED`）→ try `issuePostingDispatcher.reverse` 红冲 MANUFACTURING_ISSUE 凭证（吞异常告警）+ findIssueMove(issue.code) → try `stockMoveBiz.reverse(moveId, context)` 反向 OUTGOING 移动单（REVERSAL）→ reload issue → `doReverseConfirm`（posted=false + docStatus=CANCELLED）。
  - **`IErpMfgMaterialIssueBiz.reverseConfirm`** 接口声明同步加入。
  - **`ErpMfgErrors.ERR_MATERIAL_ISSUE_NOT_POSTED`** 新 ErrorCode。
  - inventory + manufacturing service 模块既有测试全绿（114 + 124 = 238 tests 0 回归）。

- **Phase 4（JUnit 4 用例 + 浏览器层 E2E 2 spec 4 用例）**：
  - **`TestErpInvLandedCostReversal`**（2 用例）：(1) approve 产 LANDED_COST 凭证+成本层 avgCost 更新（10→15）→ reverseApprove → 凭证红冲（原 isReversed=true + 红字凭证 postingType=REVERSAL）+ 成本层 avgCost 回退（15→10）+ posted=false/approveStatus=REJECTED/docStatus=CANCELLED；(2) 未过账守卫——未 approve 直接 reverseApprove 抛 ERR_LANDED_COST_NOT_POSTED + 状态不变。
  - **`TestErpMfgMaterialIssueReversal`**（2 用例）：(1) confirm 产 MANUFACTURING_ISSUE 凭证+OUTGOING 移动 → reverseConfirm → 凭证红冲（原 isReversed + 红字凭证 REVERSAL）+ REVERSAL 反向移动单 + posted=false/docStatus=CANCELLED；(2) 未过账守卫——未 confirm 直接 reverseConfirm 抛 ERR_MATERIAL_ISSUE_NOT_POSTED + 状态不变。
  - **E2E `inv-landed-cost-reversal.action.spec.ts`**（2 用例）：(a) approve→reverseApprove 全栈断言（凭证红字行同向取负 Dr 1401=-50/Cr 2202=-50 + 成本层 avgCost 回退 15→10）；(b) 未过账守卫。
  - **E2E `mfg-material-issue-reversal.action.spec.ts`**（2 用例）：(a) confirm→reverseConfirm 全栈断言（凭证红字行同向取负 Dr 1411=-50/Cr 1401=-50 + REVERSAL 反向移动单）；(b) 未过账守卫。
  - **e2e-runbook 业务动作表 +2 红冲行**（inventory LandedCost + manufacturing MaterialIssue）+ 套件计数 77→79。
  - 抽样回归：inv-landed-cost + mnt-spare-part-usage-reversal + inventory-stock-move + mfg-work-order + mfg-chain 8 passed 0 新增失败。

- **验证状态**：
  - `mvn clean install -DskipTests` 154 模块 **BUILD SUCCESS**（2026-07-18T17:32:00）。
  - `mvn test -pl module-inventory/erp-inv-service` **Tests run: 114, Failures: 0, Errors: 0**（2026-07-18T17:23:00，既有 112 + 新增 2，0 回归）。
  - `mvn test -pl module-manufacturing/erp-mfg-service` **Tests run: 124, Failures: 0, Errors: 0**（2026-07-18T17:23:00，既有 122 + 新增 2，0 回归）。
  - E2E `inv-landed-cost-reversal` + `mfg-material-issue-reversal` 各 2 用例全绿（2026-07-18T17:45:00）。
  - VERIFY 复核（full-green verification）：`mvn install -pl module-inventory/erp-inv-service,module-manufacturing/erp-mfg-service -am -DskipTests` BUILD SUCCESS + `mvn test` 两 service 模块 114 / 124 tests 0 failures/0 errors（2026-07-18，增量复用 target/ 未 clean）。

- **范围纪律**：纯应用层 Java（2 dispatcher 各 1 reverse 方法 + 1 processor reverseApprove + 1 BizModel reverseConfirm + 2 接口声明 + 2 ErrorCode + 1 BizModel reverseApprove 转发）+ 测试 + 文档；零 ORM/契约/字典/种子变更；红冲失败吞异常保持幂等，向后兼容。FIFO 已消耗调整层边界场景归 successor（触发条件：实际启用 FIFO 物料的到岸成本红冲遇此场景时）。

Skill: nop-backend-dev（Phase 2/3：I*Biz injection + protected step 方法 + 跨实体调用 + 异常处理）+ nop-testing（Phase 4：JunitAutoTestCase + IGraphQLEngine + 浏览器层 E2E 凭证行精确数值断言）

### 2026-07-18（plan 2026-07-18-1745-1 Maintenance 过账红冲闭环：cancel visit 红冲 ML 凭证 + reverseConfirm 红冲 MI 凭证+移动单）

- **背景**：执行 `docs/plans/2026-07-18-1745-1-maintenance-posting-reversal-closure.md`。承接两处 owner-doc 显式 successor：`docs/plans/2026-07-18-0949-1-...md` Deferred「cancel visit 触发已生成 ML 凭证红冲」（Successor Required: yes）+ `docs/plans/2026-07-10-1100-6-...md` Non-Goal「备件消耗 cancel 红冲」（同型 successor）。maintenance 域业财过账正向链路完整（1100-6 备件 + 0949-1 工时），反向红冲链路完全缺失——`ErpMntVisitBizModel.doCancel` 仅翻 status 不红冲 ML 凭证，`ErpMntSparePartUsageBizModel` 无 reverse 入口。本计划补齐红冲闭环。

- **Phase 1（MntPostingExecutor.reverse + 两 dispatcher reverse 方法）**：
  - **`MntPostingExecutor.reverse(billHeadCode, businessType)`**：镜像 `MfgPostingExecutor.reverse:35`，委派 `IErpFinVoucherBiz.reverse(billHeadCode, businessType, context)` 生成红字凭证 + 标记原凭证 isReversed=true；`IServiceContext.getCtx()` 兜底；platform 内置 billHeadCode+businessType 幂等守护（无凭证时安全 no-op）。
  - **`MaintenanceLaborPostingDispatcher.reverseLabor(ErpMntVisit)`**：billHeadCode=`visit.code + "-ML"` 对称正向 `postLabor`，调 `executor.reverse(...,MAINTENANCE_LABOR)`；null/code 守卫。
  - **`MaintenanceIssuePostingDispatcher.reverseIssue(ErpMntSparePartUsage)`**：billHeadCode=`usage.code + "-MI"` 对称正向 `dispatchIfApplicable`，调 `executor.reverse(...,MAINTENANCE_ISSUE)`。

- **Phase 2（BizModel 触发点接线）**：
  - **`ErpMntVisitBizModel.doCancel` 扩展**：updateEntity 持久化后追加 `if (laborPostingDispatcher.isPostingEnabled()) try { reverseLabor(visit) } catch LOG.warn`（吞异常保持 cancel 终态不阻断，对齐 `doComplete` 内 `postLabor` 失败语义）；config-gated 与正向对称（未开启时零回归）。
  - **`ErpMntSparePartUsageBizModel.reverseConfirm(usageId, context) @BizMutation`** 新方法：守卫 `validateCanReverse`（posted=true + docStatus=ACTIVE，否则抛 `ERR_SPARE_PART_USAGE_NOT_POSTED`）→ try `reverseIssue(usage)` 红冲 MI 凭证（吞异常告警保持幂等）+ findIssueMove(usage.code) → try `stockMoveBiz.reverse(moveId, context)` 反向 OUTGOING 移动单（REVERSAL，库存域成本引擎处理余额回滚）+ reload usage → `doReverseConfirm`（posted=false + docStatus=CANCELLED）。
  - **`IErpMntSparePartUsageBiz.reverseConfirm`** 接口声明同步加入（dao 模块）。
  - **`ErpMntErrors.ERR_SPARE_PART_USAGE_NOT_POSTED`** 新 ErrorCode。

- **Phase 3（JUnit 5 用例 + 浏览器层 E2E 2 spec 3 用例）**：
  - **`TestErpMntVisitCancelReversal`**（3 用例）：(1) cancel 触发 ML 凭证红冲——前置 postLabor 产 NORMAL 凭证 → cancel → 原凭证 isReversed=true + 红字凭证 Dr 6602=-80/Cr 2211=-80 同向取负；(2) config 关闭 → cancel 零红冲（向后兼容）；(3) 未过账 visit cancel → 无凭证（IErpFinVoucherBiz.reverse 内置无凭证安全 no-op）。
  - **`TestErpMntSparePartUsageReversal`**（2 用例）：(1) confirm 产 MI 凭证+OUTGOING 移动 → reverseConfirm → 凭证红冲（原 isReversed + 红字凭证同向取负）+ REVERSAL 反向移动单（relatedBillType=REVERSAL + relatedBillCode=原移动单 code）+ posted=false/docStatus=CANCELLED；(2) 未过账守卫——未 confirm 直接 reverseConfirm 抛 ERR_SPARE_PART_USAGE_NOT_POSTED + 状态不变。
  - **E2E `mnt-visit-cancel-reversal.action.spec.ts`**（1 用例）：complete→__update 改回 IN_PROGRESS（状态机纠错，validateNotTerminal 仅 DRAFT/SCHEDULED/IN_PROGRESS 可 cancel）→ cancel → 原凭证 isReversed=true + 红字凭证 Dr 6602=-80/Cr 2211=-80。
  - **E2E `mnt-spare-part-usage-reversal.action.spec.ts`**（2 用例）：(a) 正路径 confirm→reverseConfirm 全栈断言；(b) 未过账守卫 GraphQL errors 含「未过账」语义 token。
  - **e2e-runbook 业务动作表 +2 maintenance 红冲行** + 套件计数 75→77。

- **验证状态**：
  - `mvn clean install -DskipTests -pl module-maintenance/erp-mnt-service -am` **BUILD SUCCESS**（2026-07-18T16:32:44）。
  - `mvn test -pl module-maintenance/erp-mnt-service -Dtest='TestErpMntVisitCancelReversal,TestErpMntSparePartUsageReversal'` **Tests run: 5, Failures: 0, Errors: 0**（2026-07-18T16:32:54）。
  - `mvn test -pl module-maintenance/erp-mnt-service`（全量回归）**Tests run: 54, Failures: 0, Errors: 0**（2026-07-18T16:33:22，既有 49 + 新增 5，0 回归）。

- **范围纪律**：纯应用层 Java（executor 1 方法 + 2 dispatcher 各 1 方法 + 2 BizModel 触发点 + 1 接口声明 + 1 ErrorCode）+ 测试 + 文档；零 ORM/契约/字典/种子变更；config-gated 默认关与正向对称，红冲失败吞异常保持终态不阻断，向后兼容。解除 0949-1/1100-6 两处 Deferred「cancel 触发已生成 ML/MI 凭证红冲」。

Skill: nop-backend-dev（Phase 1/2：I*Biz injection + protected step 方法 + 跨实体调用 + 异常处理）+ nop-testing（Phase 3：JunitAutoTestCase + IGraphQLEngine + 浏览器层 E2E 凭证行精确数值断言）

### 2026-07-18（plan 2026-07-18-1600-1 nop-batch 迁移 Phase 3/4/5：全作业改造）

- **背景**：执行 `docs/plans/2026-07-18-1600-1-batch-migration-phase-1.md`。承接 `docs/analysis/batch-processing-audit-and-migration-analysis.md` + `docs/architecture/job-scheduling.md`。Phase 1（`nop-batch-dsl` 依赖）+ Phase 2（`erp-crm-lead-scoring-recalc` 参考实现）已 preexisting 完成；本日交付 Phase 3（8 个迁移类型 A）+ Phase 4（10 个迁移类型 B）+ Phase 5（scheduler.yaml 收尾 + 文档对齐）。

- **Phase 3（8 个 batch-candidate → nop-batch chunk 处理）**：
  - **8 个 `batch.xml`** 分落各域 service 模块 `_vfs/nop/batch-task/<domain>/`：`fin/{ar-ap-auto-recon,cash-forecast-refresh,deferred-posting-sweep}` + `ast/depreciation` + `qa/{spc-sampling,spc-capability}` + `prj/pnl-calc` + `mfg/jobcard-auto-generate`。
    - 真正可 chunk 化的作业（per-item IBiz 方法已存在）走 orm-reader：`ast.depreciation`（`ErpAstDepreciationSchedule(PENDING,period=now)` → `executeDepreciation(assetId,period)`）、`qa.spc-capability`（`ErpQaSpcChart(isActive)` → `calculateCapability(chartId,...)`）、`qa.spc-sampling`（→ `spcSamplingService.collectSamples` + `spcControlLimitCalculator.recalculate`）、`prj.pnl-calc`（`ErpPrjProject(active)` → `refreshPnl`）、`fin.deferred-posting-sweep`（`ErpFinPostingException(PENDING,retryCount<3,occurrenceTime>=now-24h)` → `erpFinDeferredPostingRetryHelper.retry`）。
    - 批量 IBiz 方法（无可分 chunk 的 per-item 方法）走 `<source>` loader 返回少量调用键：`fin.ar-ap-auto-recon`（返回 `[RECEIVABLE,PAYABLE]` → `runAutoReconciliation`）、`fin.cash-forecast-refresh`（返回单一 `[fromDate,toDate]` → `refreshForecast`）、`mfg.jobcard-auto-generate`（返回单一 trigger → `generatePendingJobCards`）。
  - **`ErpFinDeferredPostingRetryHelper`** 新帮助类（`module-finance/erp-fin-service/.../posting/`）：从原 `DeferredPostingSweepJob` 抽取单条重试逻辑（rebuildEvent/doRetry/markRetried/incrementRetryAndRethrow），供 batch processor 按记录调用；保留 REQUIRES_NEW 独立事务 + retryCount 递增 + RETRIED/RETRYING 状态机 + O-16 幂等补偿语义。注册为 `erpFinDeferredPostingRetryHelper` bean。
  - **8 个 `.job.yaml`** 分落 `app-erp-all/.../nop/job/conf/`：统一 `invoker.bean: nopBatchTaskRunner` + `method: executeAsync` + `params.taskPath` 指向各域 batch.xml；`enabled: "@cfg:nop.job.<name>.enabled|false"` 默认关闭。
  - **8 个 Java Job Bean 类删除** + 4 个 obsolete 测试删除（`TestErpFinAutoReconJob`/`TestErpFinCashForecastJob`/`TestErpFinDeferredPostingSweepJob`/`TestErpAstDepreciationJob`，仅测 cron 门控+委托，迁移后由 nop-batch 承载）。
  - **8 个 bean 注册清理**（fin 3 个 → 替换为 helper bean；ast/qa/prj 各删；mfg 删 1 个，CRP 保留至 Phase 4 后由 .job.yaml 持有）。
  - **scheduler.yaml 移除 8 个条目**。

- **Phase 4（10 个定点作业 `.job.yaml` 外移，迁移类型 B）**：
  - **10 个 `.job.yaml`** 分落 `app-erp-all/.../nop/job/conf/`：`erp-mfg-crp-run`/`erp-mnt-due-visit-generation`/`erp-cs-sla-scan`/`erp-crm-{forecast-recalc,event-reminder,sequence-overdue,funnel-aggregation}`/`erp-cs-{csat-reminder,entitlement-expiry}`/`erp-hr-contract-expiry`。
  - 统一格式：`invoker.bean: <原 Java Bean>` + `method: execute`（保留现有 `*Job.execute()` 实现不变，仅将 scheduler.yaml 内联条目外移为独立文件）；`enabled`/`cronExpr` 经 `nop.job.<name>.*` 配置门控（默认关闭）。
  - **scheduler.yaml 清空**：仅保留顶层 `enabled: true`，无 `jobs:` 段。

- **Phase 5（收尾 + 验证）**：
  - **scheduler.yaml**：核实仅 `enabled: true`，无内联作业。
  - **19 个 `.job.yaml` 全部就位**（`/nop/job/conf/` 下，由 nop-job-local 启动期扫描加载）：1（Phase 2 preexisting）+ 8（Phase 3）+ 10（Phase 4）= 19。
  - **9 个 A 类型 Java Job Bean 已清理**（8 本日 + 1 Phase 2 preexisting）；**10 个 B 类型 Java Job 保留**（不在删除范围）。
  - **`docs/architecture/job-scheduling.md` §2** 当前状态刷新：19 作业全部 `.job.yaml` 模式、9 batch.xml chunk、scheduler.yaml 清空、`nop-batch-dsl` 已就位。
  - **运行时验证测试**：新增 `TestErpAllJobYamlLoading`（`app-erp-all/src/test/java/io/nop/job/local/config/`），`mvn test` PASS——VFS 返回 19 资源 + 全部反序列化为 `LocalJobConfig` 成功（jobName/trigger.cronExpr/invoker.bean 三字段非空校验通过）。
  - **Proof**：`mvn clean install -DskipTests` BUILD SUCCESS（154 reactor 模块全绿）。

- **平台 Bug Fix（nop-entropy，运行时验证触发）**：
  - **Bug 1**：`LocalJobConfigLoader.DEFAULT_JOB_DIR = "/nop/job/conf/"` 尾部斜杠导致 `VirtualFileSystem.getAllResources` 抛 `invalid-path`（拒绝尾部 `/`），`scanJobConfigs` 静默吞异常返回空——**nop-job-local 的 `.job.yaml` 扫描机制自交付以来从未真正运行过**（旧 `scheduler.yaml` 内联 jobs 段不经 `getAllResources`，掩盖了此 Bug）。修复：去尾部 `/`。
  - **Bug 2**：`ConfigValueResolver.compile` 先 `substring(0,pos)` 截短 config 再用原 pos 调 `substring(pos+1)` 取默认值 → `StringIndexOutOfBoundsException`。所有经 `JsonTool.loadDeltaBean*` 加载的含 `@cfg:key|default` 的 YAML 字段全部命中（beans.xml 的 `@cfg:` 经 nop-ioc 独立 resolver，不受影响）。修复：调换顺序。
  - 两处修复均安装到本地 maven repo；nop-entropy 日志见 `nop-entropy/ai-dev/logs/2026-07/2026-07-18.md`。

- **关键决策记录**：
  - **批量 IBiz 方法的 chunk 迁移**：当现有 IBiz 方法是批量型（`runAutoReconciliation`/`refreshForecast`/`generatePendingJobCards` 无 per-item 重载），batch.xml 用 `<source>` loader 返回少量调用键（方向/日期区间/trigger），processor 调批量方法。保留"不改动 I*Biz 接口与实现"约束，同时获得 nop-batch 的 saveState + 配置门控 + 统一调度声明。
  - **`ErpFinDeferredPostingRetryHelper` 抽取**：原 `DeferredPostingSweepJob` 含复杂重试逻辑（rebuildEvent/REQUIRES_NEW/retryCount 状态机），不便内联到 batch processor xpl 脚本。抽取为独立帮助类 bean（非 Job、非 IBiz），batch processor 经 `inject('erpFinDeferredPostingRetryHelper').retry(item.id, ctx)` 按记录调用。业务逻辑逐行保留。
  - **测试删除取舍**：4 个 obsolete Job 测试仅覆盖 cron 门控+委托（迁移后由 nop-batch 承载），删除；`TestErpQaSpcSampling`/`TestErpQaSpcCapability` 测 `SpcSamplingService`/`IErpQaSpcCapabilityBiz`（不依赖 Job 类），保留。deferred-posting 重试逻辑覆盖应在后续为 helper bean 补测试。

- **下一步**：结束审计（独立子代理）+ 运行时启动日志验证 19 个 `.job.yaml` 注册。

### 2026-07-18（plan 2026-07-18-0949-1 Maintenance 维修工时费用化 GL 过账后端 + 浏览器层 E2E）

- **背景**：执行 `docs/plans/2026-07-18-0949-1-maintenance-labor-cost-posting.md`。承接 3 处 owner-doc 显式声明 successor（同一缺口面，触发条件经实时仓库核实可由本计划主动驱动满足）：`docs/plans/2026-07-17-2256-2-...md` Deferred「维修工时（labor）费用化过账」+ `docs/plans/2026-07-10-1100-6-...md` Deferred「工时费用化留 successor」+ `docs/plans/2026-07-03-1018-3-...md` Deferred「备件消耗闭合，工时费用化留 successor」；触发条件均为「维修工时成本核算后端落地时」——本计划即此后端。备件消耗侧 1100-6 已落地（MAINTENANCE_ISSUE(492) Dr 6602/Cr 1403），工时段为 maintenance 域业财过账最后一个 open successor。erp-survey 对标：ERPNext Maintenance Schedule + Stock Entry Maintenance + Metasfresh Maintenance Order 均内置维修工时费用化过账。

- **Phase 1（Decision）**：5 项 Decision 全部裁定并记录（科目方向 / 费率解析链 / 触发点 / 枚举编号 + ask-first 评估 / Dispatcher 签名）：
  - **(a) 科目方向 Dr 6602 / Cr 2211**——实测 `erp_md_subject.csv`：2211=应付职工薪酬（0742-2 已加性追加）、6602=折旧费用（1100-6 已用）、2221=应交税费（VAT 占用否决）；权责发生制语义正确（工时本质员工薪酬计提），与 projects 域 0742-2 工时贷方 + HR 域 0215-3 SALARY 贷方跨域一致；零种子 CSV 加性追加。
  - **(b) 单一 config 全局费率** `erp-mnt.default-labor-hourly-rate`（默认 0=关闭门控）——经实测 maintenance schema 无 `ErpMntEquipment.laborHourlyRate` 列、无 `ErpMntTemplate` 实体、无 `standardLaborMinutes` 列；设备级/模板级/员工级费率物化归各自 successor。
  - **(c) `doComplete` 末尾内嵌触发**（updateEntity 持久化后）——对齐 1100-6 范式 + 备件消耗一致触发时机（complete 时点 totalMinutes 已确定）；config-gated 默认 false 向后兼容。
  - **(d) `MAINTENANCE_LABOR(493)`**——实测 `ErpFinBusinessType` 既有 MAINTENANCE_ISSUE(492)、MANUFACTURING_RECEIPT(500)，493-499 空闲；同型先例 1100-6 已审计可接受。**latent defect 确认**：`business-type.dict.yaml` 经实测缺失 MAINTENANCE_ISSUE 字典项（1100-6 closure 声称已补但实际未落地），本计划同步补齐 MAINTENANCE_ISSUE + MAINTENANCE_LABOR。
  - **(e) `boolean postLabor(visit, context)`**——既有 `MaintenanceIssuePostingDispatcher.dispatchIfApplicable(Long)→void` 经 try/catch 吞异常实现「失败不阻断」；本计划 boolean 返回以便 BizModel 显式 `LOG.warn` 消费失败路径（语义等价，运维可见性更优）。

- **Phase 2（后端 Dispatcher + Provider + Visit.complete 触发扩展 + 枚举/字典/常量/ErrorCode）**：
  - **`MaintenanceLaborPostingDispatcher.postLabor(ErpMntVisit, IServiceContext) → boolean`**：config 读取经内联 `AppConfig.var` 范式（对齐 1100-6 `MaintenanceIssuePostingDispatcher:221-225`，非 ErpMntConfigs reader——ErpMntConfigs 仅有 autoGenerateDueVisits + equipmentStatusLinkEnabled 两 reader）+ 私有 `isPostingEnabled()` + `getDefaultLaborHourlyRate()` 两 helper；守卫 visit.totalMinutes != null && > 0 + rate > 0 + voucherAlreadyExists(billHeadCode=visit.code+"-ML", MAINTENANCE_LABOR) 幂等判重；计算 `laborCost = totalMinutes × rate / 60`（HALF_UP scale=4）；构造 PostingEvent（businessType=MAINTENANCE_LABOR + billHeadCode=`visit.code + "-ML"` + orgId + acctSchemaId 经 `AcctSchemaResolver.resolvePrimarySchemaId` + currencyId 经 schema.functionalCurrencyId 兜底 + exchangeRate=ONE + voucherDate=`visit.businessDate ?? CoreMetrics.today()` + billData 携带 `TOTAL/EQUIPMENT_CODE/TOTAL_MINUTES/HOURLY_RATE/VISIT_CODE`）+ 调 `executor.postEvent(event)` 返回 voucherId != null；catch Exception → log + return false。
  - **`MaintenanceLaborAcctDocProvider implements IErpFinAcctDocProvider`**：`getSupportedBusinessTypes()=EnumSet.of(MAINTENANCE_LABOR)`；`createFacts` 返回 Dr 6602 折旧费用（DC_DEBIT）+ Cr 2211 应付职工薪酬（DC_CREDIT），amount 来自 billData.TOTAL，partnerId=null；常量 `KEY_TOTAL/KEY_EQUIPMENT_CODE/KEY_TOTAL_MINUTES/KEY_HOURLY_RATE/KEY_VISIT_CODE`。
  - **`ErpMntVisitBizModel.doComplete` 扩展**：updateEntity 持久化后追加 `if (laborPostingDispatcher.isPostingEnabled()) { if (!laborPostingDispatcher.postLabor(visit, context)) LOG.warn(...); }`——config-gated 默认 false → 既有 visit complete 行为零回归；`@Inject MaintenanceLaborPostingDispatcher laborPostingDispatcher;`（具体类注入对齐既有 `issuePostingDispatcher` 范式）。
  - **`ErpFinBusinessType.MAINTENANCE_LABOR(493)`** 枚举值新增。
  - **`erp-fin/business-type.dict.yaml` 加 2 项字典项**：MAINTENANCE_LABOR（目标）+ MAINTENANCE_ISSUE（latent defect Fix）。
  - **`app-service.beans.xml`** 注册 2 个 `<bean>`（Provider + Dispatcher，bean id = 全限定类名对齐 1100-6 范式）。
  - **`ErpMntConstants`** 扩展：`CONFIG_LABOR_POSTING_ENABLED = "erp-mnt.labor-posting-enabled"` + `DEFAULT_LABOR_POSTING_ENABLED = false` + `CONFIG_DEFAULT_LABOR_HOURLY_RATE = "erp-mnt.default-labor-hourly-rate"` + `DEFAULT_LABOR_HOURLY_RATE_VALUE = "0"`（对齐既有 `CONFIG_SPARE_PART_POSTING_ENABLED` 命名无 `CONFIG_VALUE_` 前缀）。
  - **`ErpMntErrors`** 新增 ErrorCode（VISIT_TOTAL_MINUTES_MISSING / LABOR_RATE_NOT_CONFIGURED）。

- **Phase 3（JUnit 6 用例）**：`TestErpMntLaborPosting`（扁平 `service/` 包路径对齐 `TestErpMntSparePartPosting`）`@NopTestConfig(localDb=true, initDatabaseSchema=TRUE)`：
  - **测试 1（正路径 60min）**：setup equipment + visit(IN_PROGRESS, start=now-60min, end=now) + enablePosting(true,"80") → complete → status=COMPLETED + totalMinutes=60 + 凭证 NORMAL + Dr 6602=80/Cr 2211=80。
  - **测试 2（部分 30min）**：→ Dr 6602=40/Cr 2211=40。
  - **测试 3（totalMinutes=0 跳过）**：startTime=null → 不计算 totalMinutes → 无凭证。
  - **测试 4（config-gated 关闭零回归）**：config=false → 无凭证（既有 complete 行为不变）。
  - **测试 5（rate=0 跳过）**：config=true rate=0 → 无凭证（不抛错）。
  - **测试 6（Provider 单元）**：getSupportedBusinessTypes=EnumSet.of(MAINTENANCE_LABOR) + createFacts Dr 6602/Cr 2211 + total=0 守卫返回空。

- **Phase 4（浏览器层 E2E + 文档对齐 + 日志）**：1 新 spec `mnt-labor-posting.action.spec.ts` 3 用例（自包含 setup + finally cleanup，复用 0606-2 spec 范式）：
  - **测试 1（正路径 complete → MAINTENANCE_LABOR 凭证精确数值）**：自包含建 Equipment(种子 id=1 RUNNING) + Visit(DRAFT, startTime=now-60min ISO, endTime=now ISO) → schedule→start→complete(visitId) → verifyState status=COMPLETED + totalMinutes=60 + findVoucherIdByBillCode(visit.code+"-ML","NORMAL") + assertVoucherLines Dr 6602=80/Cr 2211=80（rate=80 元/小时，60×80/60=80）。
  - **测试 2（部分时长 30min）**：startTime=now-30min → Dr 6602=40/Cr 2211=40。
  - **测试 3（totalMinutes=0 守卫）**：不预置 startTime/endTime（null）→ complete → status=COMPLETED + findVoucherIdByBillCode 返回 null（无 MAINTENANCE_LABOR 凭证）。
  - 文档对齐：e2e-runbook 业务动作表 +1 maintenance 工时行 + 凭证行断言表 +1 行（MAINTENANCE_LABOR Dr 6602 / Cr 2211）+ webServer JVM arg 段补 `erp-mnt.labor-posting-enabled=true` + `erp-mnt.default-labor-hourly-rate=80` + 套件计数对齐（73→74 spec）；2256-2/1100-6/1018-3 三处 Deferred 段补 `**RELEASED by 2026-07-18-0949-1**` 行 + 实施摘要；`docs/design/maintenance/state-machine.md:163` 实现注记更新（备件段 + 工时段双闭合）+ `docs/design/assets/maintenance.md:111` 实现注记更新（与 MAINTENANCE_EXPENSE(470) 的语义差异注）；当日日志本条目；backlog README +1 done 行。

- **验证状态**：
  - `mvn install -pl module-maintenance/erp-mnt-service -am -DskipTests` **BUILD SUCCESS**（解除 Phase 3/4 测试与 E2E 阻塞；finance-dao 传递新枚举值 install 成功）。
  - `mvn test -pl module-maintenance/erp-mnt-service` **Tests run: 49, Failures: 0, Errors: 0**（既有 43 + 新增 6，0 回归）。
  - 新增 spec 独立运行：`BASE_URL=http://127.0.0.1:8011 SKIP_WEBSERVER=1 npx playwright test tests/e2e/business-actions/mnt-labor-posting.action.spec.ts --workers=1` **3 passed**（46.5s）。
  - maintenance 既有 spec 抽样回归：mnt-spare-part-posting + mnt-request + mnt-ast-linked-visit-anti-double-deduct + mnt-request-visit-orchestration 共 **6 passed** 0 新增失败。
  - **round 3 闭包修复（latent Fix 落地根因）**：经三轮独立子代理冷审计（round 1/2 NOT APPROVED → round 3 APPROVED），识别 `business-type.dict.yaml` 是 `# __XGEN_FORCE_OVERRIDE__` codegen 产物，round 1/2 失败因执行者直接编辑产物被 `mvn clean install` codegen 抹除（phantom fix）。Round 3 锚定 `module-finance/model/app-erp-finance.orm.xml` `<dict name="erp-fin/business-type">` 块（line 117-120）新增 2 个 `<option>` 元素（MAINTENANCE_ISSUE + MAINTENANCE_LABOR，含 i18n-en:label + 源 plan 注释），重新构建后 dict.yaml 经 codegen 正确重生成（line 196-202）且经实测在 `mvn clean install` 后保留。`mvn clean install -DskipTests` 154 模块 **BUILD SUCCESS**（2026-07-18T12:06:05）+ `mvn test -pl module-maintenance/erp-mnt-service` **49 tests / 0 failures / 0 errors**（2026-07-18T12:06:32）+ build idempotence 验证 dict.yaml 经 clean install 后 grep 仍返回 2 matches。

- **范围纪律**：触及 maintenance service 层生产代码（1 BizModel doComplete 钩子扩展 + 1 新 Dispatcher + 1 新 Provider + bean 注册 + 1 枚举值 + 2 字典项含 MAINTENANCE_ISSUE latent Fix + 常量 + ErrorCode）+ finance 域枚举/字典跨域契约扩展（ask-first 评估在 Phase 1 Decision 已明确：business-type 字典为业务类型登记表非会计科目保护区域，对齐 1100-6 同型扩展已审计可接受先例）+ 测试 + 文档；零 ORM/契约/codegen/种子 CSV 变更；config-gated 默认 false 向后兼容（关闭时 doComplete 行为不变，既有 visit complete 测试零回归）；自包含 setup（测试专用 equipment+visit 隔离）+ finally cleanup（凭证+visit 逐域删，不污染 maintenance/finance 看板数值断言基线，对齐 0606-2 隔离范式）。Deferred（设备级 / 模板级 / 员工级费率物化 / cancel 红冲 / 外币费率 / nop-job 定时批量 / 累计字段物化 / linkedVisit 工时联动 / ErpMntConfigs reader）均附触发条件列入 successor。

Skill: nop-backend-dev（Phase 1：Decision 门 / 跨实体访问 / 异常处理 / 产品化可定制性自检）+ nop-testing（Phase 2：JunitAutoTestCase 端到端 + 6 用例）+ Phase 4 Skill: none（DIRECT `@BizMutation` 经 GraphQL 浏览器层 + 凭证行精确数值断言 → E2E 测试本体 Skill: none，对齐 0606-2 范式裁决）

### 2026-07-18（plan 2026-07-18-0718-2 Finance 员工借款现金还款 EMPLOYEE_ADVANCE_SETTLE 现金还款路径后端 + 浏览器层 E2E）

- **背景**：执行 `docs/plans/2026-07-18-0718-2-employee-advance-cash-repay-backend-e2e.md`。承接 `docs/plans/2026-07-14-1218-2-finance-expense-claim-employee-advance-budget-e2e.md` Deferred But Adjudicated「员工借款现金还款 (EMPLOYEE_ADVANCE_SETTLE 现金还款路径)」— Successor Required: yes，触发条件「现金还款浏览器层入口落地时」。触发条件经实时仓库核实**可由本计划主动驱动满足**：owner doc `docs/design/finance/expense-claim.md §现金还款` 设计已就绪（Dr 1002 / Cr 1221 凭证承载范式），枚举 `EMPLOYEE_ADVANCE_SETTLE(180)` + 字典项 + 实体字段（settledAmount/outstandingAmount/docStatus/posted/employeeId）+ 种子 COA（1221/1002/2241）均已就绪，但 BizModel 无 cashRepay 入口、Provider SETTLE 分支无 SETTLE_TYPE 分派。

- **Phase 1（后端 cashRepay @BizMutation + Dispatcher.postCashRepay + Provider SETTLE_TYPE 扩展）**：
  - **3 项 Decision 裁决**：(a) cashRepay 实现层=BizModel Java `@BizMutation` 直落（非状态机迁移，与 cancel() 同属 Facade 层；Java `@BizMutation` 自动暴露 GraphQL 端点无需 xbiz action）；(b) 失败语义=字段先于凭证持久化 + post 失败不阻断（对齐 postSettle 范式，残留风险归异常工作台）；(c) docStatus 保持不变（字典无 SETTLED 值，outstandingAmount=0 由派生投影表达「已结清」）。
  - **`IErpFinEmployeeAdvanceBiz.cashRepay` 接口声明** + **`ErpFinEmployeeAdvanceBizModel.cashRepay` `@BizMutation`**：守卫 advance 存在/posted=true+approveStatus=APPROVED/amount>0/amount≤outstandingAmount → 更新 settledAmount+=amount + outstandingAmount-=amount → `updateEntity` 先持久化 → 委派 `advancePostingDispatcher.postCashRepay` 返回 boolean（失败 log warn 不阻断）→ 返回 advance。
  - **`EmployeeAdvancePostingDispatcher.postCashRepay`**：构造 PostingEvent（businessType=EMPLOYEE_ADVANCE_SETTLE + billHeadCode=`EA-CASH-REPAY-{advanceCode}-{millis}` + billData 携带 BILL_DATA_EMPLOYEE_ID=partnerId + TOTAL=amount + BILL_DATA_SETTLE_TYPE=CASH）→ executor.postEvent → catch Exception log + return false（镜像 postSettle）。
  - **`EmployeeAdvanceAcctDocProvider.createFacts` SETTLE 分支扩展**：按 `billData.SETTLE_TYPE` 分派——CASH → Dr 1002 / Cr 1221（现金还款）；OFFSET/null → Dr 2241 / Cr 1221（既有报销抵扣零回归）；新增 `asString` helper + 非支持 businessType 返回空列表守卫（对齐 CreditFacilityInterestAcctDocProvider 范式）。
  - **`ErpFinConstants`** 新增 `BILL_DATA_SETTLE_TYPE`/`SETTLE_TYPE_CASH`/`SETTLE_TYPE_OFFSET` 常量。
  - **`ErpFinErrors`** 新增 3 ErrorCode：`ERR_EMPLOYEE_ADVANCE_NOT_REPAYABLE`/`ERR_EMPLOYEE_ADVANCE_CASH_REPAY_AMOUNT_INVALID`/`ERR_EMPLOYEE_ADVANCE_CASH_REPAY_EXCEEDS_OUTSTANDING`。

- **Phase 2（JUnit 6 用例）**：`TestErpFinEmployeeAdvanceCashRepay`（包路径 `posting/` 对齐 Dispatcher）：
  - **测试 1（全额还款）**：setup posted advance(500) → cashRepay(500) → settledAmount=500 + outstandingAmount=0 + docStatus 保持 DRAFT + Dr 1002=500/Cr 1221=500 凭证行精确数值。
  - **测试 2（部分还款）**：cashRepay(200) → settledAmount=200 + outstandingAmount=300 + Dr 1002=200/Cr 1221=200。
  - **测试 3（未过账守卫）**：posted=false + UNSUBMITTED → 抛 NOT_REPAYABLE + 无凭证。
  - **测试 4（超额守卫）**：outstanding=300, cashRepay(500) → 抛 EXCEEDS_OUTSTANDING + 无凭证 + 字段不变。
  - **测试 5（金额非法守卫）**：cashRepay(0)/null/(-100) → 抛 AMOUNT_INVALID + 无凭证。
  - **测试 6（Provider SETTLE_TYPE 分派单元）**：CASH → Dr 1002/Cr 1221；OFFSET → Dr 2241/Cr 1221；null → 默认 OFFSET（零回归）；非支持类型 → 空列表。
  - **关键工程化**：seedCurrentMonthOpenPeriod——postCashRepay voucherDate=CoreMetrics.today()（实际日期），须 seed 覆盖今天的 OPEN 期间否则 resolveOpenPeriod 失败致过账静默失败（advance 字段已更新但凭证缺失）。

- **Phase 3（浏览器层 E2E + 文档对齐）**：1 新 spec `fin-employee-advance-cash-repay.action.spec.ts` 3 用例：
  - **(a) 全额还款**：setup partner+employee+advance(500) → submit→approve→posted → cashRepay(500) → settledAmount=500 + outstandingAmount=0 + Dr 1002=500/Cr 1221=500 凭证行经 assertVoucherLines 独立反查。
  - **(b) 部分还款**：cashRepay(200) → settledAmount=200 + outstandingAmount=300 + Dr 1002=200/Cr 1221=200。
  - **(c) 超额守卫**：先部分还 200（outstanding=300）→ cashRepay(500) → GraphQL errors 含「超过」/「未还余额」语义 token + 字段不变。
  - **关键工程化**：Nop GraphQL Map filter 的 `$type:'like'` 经实测被当作 eq 处理（SQL 输出 `BILL_CODE = '...%'` 非 LIKE），改用 `eqFilter('businessType') + JS startsWith 前缀过滤` 范式反查 `EA-CASH-REPAY-{code}-{millis}` 凭证。
  - 文档对齐：`e2e-runbook` 业务动作表 +1 finance 现金还款行 + 凭证行断言表 +1 行（EMPLOYEE_ADVANCE_SETTLE CASH Dr 1002/Cr 1221）+ 套件计数 74→75；`expense-claim.md §现金还款` 补实现注记；`2026-07-14-1218-2` Deferred 段补 `**RELEASED by 2026-07-18-0718-2**`。

- **验证状态**：
  - `mvn test -pl module-finance/erp-fin-service -am` 全绿（206 tests，含新增 6 tests，0 failures/0 errors）。
  - `mvn clean install -DskipTests` 154 模块 BUILD SUCCESS（closure gate 零后端污染）。
  - 新增 spec 独立运行全绿（3 passed）。
  - finance 既有 spec 抽样回归（fin-employee-advance + fin-expense-claim + fin-credit-facility-interest 11 passed）0 新增失败。
  - **full-green verification**（mission verify `erp`，2026-07-18 09:33–09:41）：reactor 全量 `mvn clean install -DskipTests` 154 模块 BUILD SUCCESS（01:33 min）+ `mvn test` 154 模块全绿（353 个 Tests run 段 BUILD SUCCESS / 0 failures / 0 errors，08:13 min）。

- **范围纪律**：全部改动为应用层 Java（service 层 BizModel 新增 @BizMutation + Dispatcher 新方法 + Provider 扩展 + 常量 + ErrorCode）+ 测试 + 文档；零 ORM/契约/codegen/字典/种子变更。Deferred（字典 SETTLED 值/三金额闭环 ORM 列/薪资扣回/cashRepay 审批/外币还款汇兑/特殊清算）均附触发条件列入 successor。

### 2026-07-18（plan 2026-07-18-0718-1 Finance 授信额度利息计提 CREDIT_FACILITY_INTEREST 后端 + 浏览器层 E2E）

- **背景**：执行 `docs/plans/2026-07-18-0718-1-credit-facility-interest-accrual-backend-e2e.md`。承接 `docs/plans/2026-07-17-2256-1-fin-treasury-cash-forecast-credit-facility-e2e.md` Deferred But Adjudicated「授信额度利息计提（CREDIT_FACILITY_INTEREST）」(l.186-190) — Successor Required: yes，触发条件「授信利息计提后端落地时」。触发条件经实时仓库核实**可由本计划主动驱动满足**：枚举/字典/owner doc 设计均已就绪（`ErpFinBusinessType.CREDIT_FACILITY_INTEREST(250)` + `business-type.dict.yaml:104-106` 字典项 + `treasury.md:148` Dr 6603 / Cr 1002 设计），但 BizModel 无利息计提入口、无 Provider、无 VoucherBuilder、无 bean 注册、无 config 常量、无 ErrorCode、无测试。

- **Phase 1（后端 accrueInterest + VoucherBuilder + Provider + bean 注册 + config 常量 + ErrorCode）**：
  - **3 项 Decision 裁决**：(a) 计息基数=`usedAmount`（占用资金成本会计语义）；(b) 天数基准=闭区间（`ChronoUnit.DAYS.between(from,to)+1`），360 天年化基准对齐贴现 `ErpFinNotesReceivableProcessor:231-234`；(c) `usedAmount` 取数时点=fromDate 当时的值（保守口径，避免日均计算复杂度）；幂等键为已知约束（IErpFinVoucherBiz.post 内置 alreadyPosted 按 `(billHeadCode, businessType)` 反查）。
  - **`ErpFinCreditFacilityBizModel.accrueInterest` `@BizMutation`**：守卫 facility 存在/fromDate≤toDate/usedAmount>0（=0 空操作 null）/rate>0（=0 抛 ERR_CREDIT_FACILITY_INTEREST_RATE_NOT_CONFIGURED）→ 计息 `interest = usedAmount × rate × days / 360`（HALF_UP scale=4）→ 委派 `CreditFacilityInterestVoucherBuilder.post` 返回 voucherId（含 null 幂等）。
  - **新建 `CreditFacilityInterestVoucherBuilder`**（注入 `IErpFinVoucherBiz` + `IDaoProvider`，对齐 `BankReconAdjustmentVoucherBuilder` 范式）：构造 PostingEvent（businessType=CREDIT_FACILITY_INTEREST + billHeadCode=`CFI-INT-{facilityId}-{fromDate}_{toDate}` 区间级幂等键 + orgId/acctSchemaId（经 `AcctSchemaResolver.resolvePrimarySchemaId`）/currencyId（**facility.fundAccount.currencyId → acctSchema.functionalCurrencyId 兜底**，对齐 plan「账套本位币兜底」裁决，执行期 E2E 发现 facility 未绑定资金账户时无兜底会触发「币种为 null」mandatory 校验失败，补 schema functionalCurrencyId 兜底）/exchangeRate=ONE/voucherDate=toDate）+ billData 携带 `TOTAL=interestAmount` + `FUND_ACCOUNT_ID` → 调 voucherBiz.post。
  - **新建 `CreditFacilityInterestAcctDocProvider implements IErpFinAcctDocProvider`**：`getSupportedBusinessTypes()=EnumSet.of(CREDIT_FACILITY_INTEREST)`；`createFacts` 返回 Dr 6603 财务费用-利息支出 + Cr 1002 银行存款（amount 来自 billData.TOTAL，partnerId=null），镜像 NotesPayableAcctDocProvider 范式（readDecimal/fact helper）。
  - **bean 注册**：`app-service.beans.xml` 追加 2 个 `<bean>`（Provider + VoucherBuilder），对齐 `<ioc:collect-beans by-type=IErpFinAcctDocProvider>` 显式声明范式。
  - **`ErpFinConstants.CONFIG_CREDIT_FACILITY_DEFAULT_INTEREST_RATE = "erp-fin.credit-facility-default-interest-rate"`**（treasury config 块 l.223+ 起）。
  - **`ErpFinErrors.ERR_CREDIT_FACILITY_INTEREST_INVALID_DATE_RANGE` + `ERR_CREDIT_FACILITY_INTEREST_RATE_NOT_CONFIGURED`**（中文 i18n 描述 + 作用域参数键 ARG_FROM_DATE/ARG_TO_DATE/ARG_CONFIG_KEY）。
  - **`IErpFinCreditFacilityBiz.accrueInterest`** 接口声明 + `@BizMutation` + Javadoc（含幂等契约说明）。

- **Phase 2（JUnit 6 用例）**：`TestErpFinCreditFacilityInterest`（包路径 `treasury/` 对齐 VoucherBuilder）：
  - **测试 1（正路径）**：setup facility(used=300,total=1000) + testConfigFile `credit-facility-interest-test.yaml` rate=0.05 → accrueInterest(2026-08-01, 2026-08-31) → voucherId 非空 + voucher.postingType=NORMAL + voucher.docStatus=POSTED + billHeadCode 反查格式 + Dr 6603 debit=1.2917 + Cr 1002 credit=1.2917 + facility 三值不变。
  - **测试 2（幂等命中）**：同 setup → 二次同参数调用 → 第二次返回 null + ErpFinVoucherBillR 仅 1 行 (billCode, businessType) + facility 不变。
  - **测试 3（usedAmount=0 空操作）**：setup facility(used=0) → 返回 null + 无凭证。
  - **测试 4（rate=0 守卫）**：经 `AppConfig.getConfigProvider().assignConfigValue(...)` 临时覆盖 rate=0（finally 恢复）→ 抛 ERR_CREDIT_FACILITY_INTEREST_RATE_NOT_CONFIGURED + 无凭证。
  - **测试 5（非法日期区间）**：fromDate>toDate → 抛 ERR_CREDIT_FACILITY_INTEREST_INVALID_DATE_RANGE + 无凭证。
  - **测试 6（Provider 单元）**：`new CreditFacilityInterestAcctDocProvider().createFacts` 返回 Dr 6603 / Cr 1002 + amount 来自 billData.TOTAL + 非支持 businessType 返回空列表。
  - 快照机制：经 `@NopTestConfig(snapshotTest=RECORDING)` 首录 → 切回默认 CHECKING 全绿（VoucherFact 非 DataBean 经 Map 转换后序列化）。

- **Phase 3（浏览器层 E2E + 文档对齐 + 日志）**：1 新 spec `fin-credit-facility-interest.action.spec.ts` 3 用例（自包含 setup + finally cleanup 经 `cleanupVoucherByBillCode` 保护共享 DB 基线）：
  - **测试 1（正路径）**：__save facility(used=300,total=1000,orgId=2) → accrueInterest(2026-07-01,2026-07-31) → GraphQL 返回 voucherId Long scalar + findVoucherIdByBillCode(`CFI-INT-{id}-2026-07-01_2026-07-31`,'NORMAL') 反查一致 + assertVoucherLines Dr 6603=1.2917 / Cr 1002=1.2917（HALF_UP scale=4，非 JS 浮点 1.29166…）+ facility 三值不变。**关键工程化：测试区间用 2026-07 而非 plan 原写 2026-08**（实测种子 `erp_fin_accounting_period.csv` 仅含 2026-07 OPEN 期间，2026-08 voucherDate 落种子外触发「未匹配到会计期间」拒绝）。
  - **测试 2（usedAmount=0）**：data null + errors null（返回 null 标量）+ findFirst 反查无凭证。
  - **测试 3（非法日期守卫）**：GraphQL errors 含「日期」语义 token（ERR_CREDIT_FACILITY_INTEREST_INVALID_DATE_RANGE i18n 中文描述，对齐 2256-1 守卫 token 范式）+ 无凭证生成。
  - `playwright.config.ts` webServer JVM args 追加 `-Derp-fin.credit-facility-default-interest-rate=0.05`（config-gated，对齐 `erp-fin.auto-reconcile=true` 既有范式）。
  - 文档对齐：e2e-runbook 业务动作表 +1 finance 利息计提行（套件计数 73→74）+ 凭证行断言表 +1 行（CREDIT_FACILITY_INTEREST Dr 6603 / Cr 1002 公式派生）；2256-1 Deferred 补 `**RELEASED by 2026-07-18-0718-1**` + 实施摘要；`treasury.md §业财过账` CREDIT_FACILITY_INTEREST 行下补实现注记（rate 来源 config / 公式 / 360 天基准 / billHeadCode 区间级幂等机制内置 / 币种解析顺序）；当日日志本条目；backlog README +1 done 行。

- **范围纪律**：触及 finance service 层生产代码（1 BizModel 新方法 + 1 IBiz 方法声明 + 1 新 VoucherBuilder 类 + 1 新 Provider 类 + bean 注册加性 + 1 config 常量 + 2 ErrorCode + 测试 + 文档）；字典项 `value: CREDIT_FACILITY_INTEREST` 已存在不动；config 键默认 0=关闭门控（向后兼容）；不改 ORM/契约/`_gen/`；保护区域遵守（利率属配置面，不加 facility.interestRate ORM 列）；执行期修复 1 处 latent defect（VoucherBuilder currencyId 兜底，对齐 plan 原裁决）；facility 级利率 / 累计字段物化 / nop-job 定时 / 多 facility 批量 / 外币利息汇兑 / 利息资本化 / 幂等键变体均为计划内 Non-Goal 附触发条件。

- **验证状态**：`mvn test -pl module-finance/erp-fin-service -am` **Tests run: 200, Failures: 0, Errors: 0**（既有 194 + 新增 6）；`BASE_URL=http://127.0.0.1:8011 SKIP_WEBSERVER=1 npx playwright test tests/e2e/business-actions/fin-credit-facility-interest.action.spec.ts --workers=1` **3 passed**（45.5s）；finance 既有 spec 抽样回归（fin-credit-facility + fin-cash-forecast + fin-notes-* + fin-reconciliation 共 5 spec 22 用例）**25 passed** 0 新增失败；`mvn clean install -DskipTests` **154 模块 BUILD SUCCESS**（closure gate，确认零后端污染）。

Skill: nop-backend-dev（Phase 1：BizModel 新方法 + VoucherBuilder + Provider + 跨实体访问 + 异常处理 + 产品化可定制性自检）+ nop-testing（Phase 2：JunitAutoTestCase 端到端 + 6 用例 + RECORDING→CHECKING 切换）

### 2026-07-18（verify: 全域 auto-test 快照跨日漂移根治 — 13 域 FrozenClockExtension + 部分快照通配）

- **背景**：mission verify `erp`。`mvn test`（reactor 全量）暴露 ~100+ errors 跨 13 服务域，全部为同一类日期翻页漂移（snapshots 录制于 2026-07-17，当日为 2026-07-18，BizModel `CoreMetrics.today()`/`currentDate()` 派生日期列 DUE_DATE/POSTED_AT/ACQUISITION_DATE/PERIOD_TO/SIGN_DATE/INSPECTION_DATE/VALID_FROM/VALID_TO/COSTING_VERSION/ACTUAL_START_DATE/ACTUAL_END_DATE 等全部 value=today expected=yesterday）。承接同日 HR FrozenClock 范式（见下条 l.35），本验证轮将同型根治推广至其余 13 域。
- **修复（测试基础设施，零生产代码变更）**：沿用 HR `HrFrozenClockExtension` 范式，新增 13 个同型 `*FrozenClockExtension`（Fin/Inv/Prj/Cs/Ct/B2b/Qa/Mnt/Log/Pur/Sal/Mfg/Drp/Crm），各冻结 `currentDate()`/`currentDateTime()` 到参考日 `2026-07-17`，保留 `currentTimeMillis()`/`nanoTime()` 走真实系统时钟。共 31 个日期漂移型测试类加 `@RegisterExtension static <Domain>FrozenClockExtension`。
- **3 类次生副作用裁决**：
  - **TestErpPrjProjectSettlement postedBy/approvedBy 退化**（freeze 致 `IUserContext.get()` 类加载时机提前 → CTX.getUserId() 退 null → resolveUserId 回退 "system"）：保留 freeze + 在该类加 `@BeforeAll static void fixCtxUser() { CTX.getContext().setUserId("autotest"); }` 复原用户。
  - **TestErpQaSpcCapability/TestErpQaSpcSampling/TestErpMntDowntimeAndE2E 时间算术次生失效**（freeze 致 SPC 样本顺序变化、downtime 时长 = now - startedAt 失真）：revert freeze + 7 个快照 CSV drifted 日期字段改 `*` 通配（PERIOD_FROM/PERIOD_TO/RISK_DATE/INSPECTION_DATE/VISIT_DATE）。
- **验证状态（full-green）**：`mvn clean install -DskipTests` **154 模块 BUILD SUCCESS**（1:31 min）；`mvn test`（reactor 全量）**BUILD SUCCESS 0 failures 0 errors**（含本批修改的 13 服务域 + 既有 hr/notification/master-data 等）。
- **范围纪律**：纯测试基础设施（13 新增测试辅助类 + 31 测试类注解 + 7 CSV 通配 + 1 测试类 @BeforeAll 用户复原）；零生产 Java/ORM/契约/codegen/config 变更；属 AGENTS.md「仅测试清理」低风险类（test-only date-stability），跳过正式计划。日期冻结到固定参考日是 Nop auto-test 对日期敏感快照的标准确定性范式（同 HR FrozenClock 范式裁决，非时间穿越/非业务逻辑变更）。

Skill: nop-testing（auto-test 快照 CHECKING 模式 + 日期确定性根治）

### 2026-07-18（plan 2026-07-18-0347-2 HR 休假审批→排班跨实体钩子联动浏览器层 E2E）

- **背景**：执行 `docs/plans/2026-07-18-0347-2-hr-leave-shift-linkage-e2e.md`。承接 `docs/plans/2026-07-18-0100-1-hr-shift-scheduling-orchestration-e2e.md` Deferred But Adjudicated「`ErpHrLeaveRequest.approve` → `ErpHrShiftAssignment` isAbsent 标记联动 E2E」（Successor Required: yes，触发条件「休假→排班联动浏览器层 E2E 需求落地时」）。触发条件经实时仓库核实**已满足**：AGENTS.md「当前项目阶段」/ project-context.md:34 明示当前重点含「各域细化端到端验证」；0100-1 已交付 3 hr-shift spec + 0215-3 已交付 hr-leave-attendance（仅测 leave 自身状态机，显式未建 ShiftAssignment 前置），但 LeaveRequest.approve/cancel 触发的 `onLeaveApproved`/`onLeaveCancelled` 跨实体钩子副作用（ShiftAssignment 4 字段翻转）浏览器层零覆盖。

- **Phase 1（hr-leave-shift-linkage 2 用例）**：Explore 实测 3 处工程化关键点：(a) `ErpHrShiftBizModel.findAssignmentsByEmployeeRange :224-233` 仅按 `employeeId + dateBetween(assignmentDate, startDate, endDate)` 过滤**无 status 过滤**——所有区间内行不论 SCHEDULED/CANCELLED/ABSENT/PRESENT 均被 onLeaveApproved 选中并标记；spec setup 显式将 3 测试 assignment 全置 SCHEDULED 态避免噪声，负路径（CANCELLED/ABSENT 在区间内也被翻转）归 Non-Goal successor；(b) `approve(id)`/`cancel(id)` 入参 String scalar（leave.id Long 经 Nop GraphQL 暴露为字符串 ID），对齐既有 hr-leave-attendance 范式 `callMutationOk(page, 'ErpHrLeaveRequest', 'approve', { id: leave.id }, 'id')`；(c) **GraphQL String/Long 类型桥**：钩子写入 `assignment.leaveRequestId=leave.id`（Long），后续 `findFirst` 反查时 GraphQL 返回 String，断言比较须 `Number(assignment.leaveRequestId) === Number(leave.id)`（同 `hr-shift-assignment.action.spec.ts:147-148` 范式）。2 用例：(1) **approve 正路径 + 区间范围准确性** — 自包含建 employee + shift + 3 SCHEDULED ShiftAssignment（2026-08-10/08-11 区间内 + 2026-08-13 区间外对照）+ ErpHrLeaveRequest(DRAFT, 2026-08-10~2026-08-12) → submit(SUBMITTED) → approve(APPROVED) **内部委派 `IErpHrShiftBiz.onLeaveApproved`** → 经 `findFirst` 按 (employeeId, assignmentDate) 反查 3 行断言：区间内 08-10/08-11 行 4 字段翻转 isAbsent=true/absenceReason=LEAVE/leaveRequestId=leave.id/status=ABSENT + 区间外 08-13 行字段不变；(2) **cancel 正路径还原** — approve 前置后 → cancel(CANCELLED) **内部委派 `IErpHrShiftBiz.onLeaveCancelled`**（仅解除由该 leaveRequestId 标记的行）→ 经 `findFirst` 反查原被标记的 2 行 4 字段还原 isAbsent=false/absenceReason=null/leaveRequestId=null/status=SCHEDULED。

- **范围纪律**：纯测试层 + 文档。零生产 Java/ORM/契约/codegen/config 变更；测试在根 `tests/e2e/` 非 reactor 模块；`onLeaveApproved`/`onLeaveCancelled` 钩子已由 0831-3 实现 + 0100-1 Closure 核实非空壳；LeaveBalance 余额扣减副作用 / Attendance 副作用 / 休假→排班 xwf 审批链 / 负路径（CANCELLED/ABSENT 在区间内被翻转）均归 Non-Goal（不同结果面或 successor）。

- **Closure（文档对齐）**：e2e-runbook 业务动作表 +1 hr 联动行（hr-shift-swap 之后，hr-assessment-dev-plan 之前，逻辑聚类于 hr-shift 段）+ 业务动作套件表头计数 72→73 spec + 业务动作小节标题「17 域 67 spec」→「17 域 73 spec」（先前漂移：0100-2 落地后未同步小节标题，本次一并订正）+ 套件计数历史段补 0347-2 增量；0100-1 Deferred 补 `**RELEASED by 2026-07-18-0347-2**` + 交付摘要；当日日志本条目（与 0347-1/0100-2/0100-1/FrozenClock 同日倒序）。

- **验证状态**：`PLAYWRIGHT_PORT=8011 SKIP_WEBSERVER=1 npx playwright test tests/e2e/business-actions/hr-leave-shift-linkage.action.spec.ts --workers=1` **2 passed**（15.7s）；`PLAYWRIGHT_PORT=8011 SKIP_WEBSERVER=1 npx playwright test tests/e2e/business-actions/ --workers=1` **194 passed**（23.5m，192 baseline + 2 新增，0 新增失败）；`mvn clean install -DskipTests` **154 模块 BUILD SUCCESS**（1:37 min，确认零后端污染——本计划纯测试 + 文档）。

Skill: none（DIRECT `@BizMutation` 经 GraphQL 浏览器层 + 跨实体钩子副作用断言 → E2E 测试本体 Skill: none，对齐 0215-3/0100-1/0100-2 范式裁决）

### 2026-07-18（plan 2026-07-18-0347-1 HR 员工调动 successor 合同码长度守护修复）

- **背景**：执行 `docs/plans/2026-07-18-0347-1-hr-employee-successor-code-length-guard.md`。承接 `docs/plans/2026-07-18-0100-2-hr-talent-development-e2e.md` Follow-up「`ErpHrEmployeeBizModel.buildSuccessorCode` 新合同码超 code precision=50 同 1430-1 类 buildCode overflow」（l.195，Successor Required: yes，触发条件经 0100-2 E2E 实证已满足：紧凑短码 `C${tag}-{ms}-{seq}` ≈24 字符规避才能跑通 hr-transfer spec）+ Deferred But Adjudicated 同源（离职/借调/晋升独立调动路径不在范围）。同型缺陷首次发现于 `docs/bugs/2026-07-17-1430-ar-ap-item-code-overflows-vouchercode-for-long-notes-codes.md`（finance `ErpFinArApItemGenerator.buildCode`），范式源 `docs/plans/2026-07-17-1600-1-ar-ap-item-code-length-guard.md`（已 completed）。本仓第二个同型 `buildXxxCode` 修复。

- **Phase 1（方案裁决 + buildSuccessorCode 守护）**：方案 1（应用层长度守护）vs 方案 2（放宽 `code` domain precision）—— 方案 2 否决（侵入面跨多域多实体 + DDL 迁移 + ORM 保护区域；1600-1 已对 voucherCode 否决同方案）。方案 1 落地：新增 `static final int SUCCESSOR_CONTRACT_CODE_MAX_LENGTH = 50;` + `private static final int SUCCESSOR_HASH_SUFFIX_LENGTH = 4;`（DAG 顶不反向依赖 ORM 元数据 API），短码路径（拼接 ≤ 50）逐字符不变保持原逻辑，超限路径保留固定段 `"TRF-" + employeeId + "-" + effectiveDate`（最坏 4 + 19(Long.MAX) + 1 + 10 = 34 < 50，留 ≥16 给 active.code 压缩段）+ active.code 头部截断 + MD5 前 4 hex 摘要保唯一。方法签名 `static String buildSuccessorCode(ErpHrEmployee, ErpHrEmploymentContract, LocalDate)` 不变（向后兼容）。引入 `io.nop.commons.util.StringHelper.md5Hash`（同 1600-1 范式）。零 ORM/DDL/契约/xbiz/view 变更。

- **Phase 2（长码回归测试 + 红绿反转证明）**：Decision 路径 (a) 扩展既有 `TestErpHrEmployeeTransfer`（JunitAutoTestCase harness + 端到端 transferEmployee）；路径 (b) 纯 POJO unit 不可行——`buildSuccessorCode` package-private 在 `app.erp.hr.service.entity`，`TestErpHrEmployeeTransfer` 在 `app.erp.hr.service` 父包，Java 跨包不可达；draft iter-1 M1 已禁用 `service/entity/` 测试子包路径。新增 `testLongActiveCodeDoesNotOverflowCodePrecision`（1 个测试方法覆盖 4 子用例）：(a) 短码 `"CTC-SHORT-001"` 路径逐字符不变（拼接结果 = `"TRF-" + empId + "-2026-07-08-CTC-SHORT-001"` 等值断言）；(b) 长码 `"HR-EMP-DEPT-2026-Q3-CONTRACT-001-X"`（34 字符，plan 标 35 笔误，仍满足 30+ 要求）端到端 transferEmployee 不抛 22001 + 新合同 code ≤ 50 + startsWith `"TRF-" + empId + "-2026-07-08"`；(c) 第二条长码尾部不同 → 经 MD5 摘要 successor code 不同；(d) 无 ACTIVE 合同走 base 路径（拼接 = `"TRF-" + empId + "-2026-07-08"` 无 `-active.code` 后缀）。
  - **红绿反转证明**（2026-07-18 04:15）：`git checkout HEAD -- ErpHrEmployeeBizModel.java` 暂回退 fix，重跑该测试 → **RED**：`sqlState=22001, vendorCode=22001, sqlName=insert:app.erp.hr.dao.entity.ErpHrEmploymentContract, desc=违反数据库完整性约束`（即长 active.code + empId + ISO date 总和 > 50 触发字符串右截断 → insert 失败 → transferEmployee 事务回滚）。恢复 fix 后 → **GREEN**：10 tests 全绿（既有 9 + 新增 1）。
  - hr-service 全套件 `mvn test -pl module-hr/erp-hr-service` → **Tests run: 112, Failures: 0, Errors: 0**（既有 111 + 新增 1）。

- **Phase 3（0100-2 Follow-up 关闭 + bug 文档交叉引用 + 日志）**：(1) 0100-2 l.195 Follow-up 该 successor 行追加 `**RELEASED by 2026-07-18-0347-1**` + 实施摘要（方案 1 / 覆盖 4 子用例 / 红绿反转证明）；(2) `docs/bugs/2026-07-17-1430-...` `## 未来重构注意事项` 段追加 HR 调动 `buildSuccessorCode` 同类 fix 交叉引用 + 8 处 `buildXxxCode`-style 拼接方法分布与各自 status（#1 finance 1600-1 修 / #2 hr 0347-1 修 / #3~#8 watch-only）；(3) 当日日志本条目（与 0100-1/0100-2/FrozenClock 同日条目并列，置于顶部倒序）。

- **范围纪律**：生产代码变更严格限定 1 文件 1 static 方法 + 2 常量（`ErpHrEmployeeBizModel.java`），1 处 import 新增（`StringHelper`）；测试代码 1 文件（`TestErpHrEmployeeTransfer.java`）1 新 `@Test` 方法 + 1 import 新增（`assertTrue`）；零 ORM/DDL/契约/xbiz/view/codegen/config 变更；HR service 层非会计/财务保护区域；同 1600-1 范式与裁决（方案 1 / MD5-4hex / 方法签名不变 / 短码逐字符不变）。

- **验证状态**：`mvn test -pl module-hr/erp-hr-service` **Tests run: 112, Failures: 0, Errors: 0**（含新增长码回归 + 既有方法零回归）；`mvn install -pl module-hr/erp-hr-service -am -DskipTests` BUILD SUCCESS；红绿反转证明：未修复时 `sqlState=22001 / insert ErpHrEmploymentContract / 违反数据库完整性约束`，修复后全绿。

Skill: nop-backend-dev（Phase 1：BizModel 静态 helper 修改 + 决策门 / 自检 / 异常处理）+ nop-testing（Phase 2：JunitAutoTestCase 端到端 + 4 子用例 + stash 红绿反转证明）+ nop-debugging（Phase 0 隐含：缺陷根因已定位 0100-2 执行期，本计划修复需防回归——stash 红绿反转证明满足）

### 2026-07-18（verify: HR 日期敏感型 auto-test 快照跨日漂移根治 — HrFrozenClockExtension）

- **背景**：mission verify `erp`。`mvn test -pl module-hr/erp-hr-service` 暴露 15 errors，全部为日期翻页漂移（snapshots 录制于 2026-07-17，当日为 2026-07-18，生产代码 `CoreMetrics.today()` 派生日期类列 PLAN_NAME/PAYMENT_DATE/SIGN_DATE/START_DATE/END_DATE/DATE/TARGET_DATE 全部 value=today expected=yesterday）。0100-2 执行期日志（见上条 l.11）已标记为 pre-existing 且与本批 GapAnalysis 改动无关（git stash 验证）；本验证轮根治。
- **根因**：HR 5 测试类（TestErpHrContractExpiry/TestErpHrAttendanceEngine/TestErpHrCompetencyManagement/TestErpHrPayrollEngine/TestErpHrPayrollSimulation）的 Nop auto-test 快照含 `CoreMetrics.today()` 派生日期，随系统日历翻页日漂移；平台 `CoreMetrics.registerClock(IClock)`（`nop-api-core/.../CoreMetrics.java:44`）提供冻结钩子但此前未用。
- **修复（测试基础设施，零生产代码变更）**：新增 `module-hr/erp-hr-service/src/test/java/app/erp/hr/service/HrFrozenClockExtension.java`——JUnit `@RegisterExtension`（BeforeAllCallback/AfterAllCallback），仅冻结 `currentDate()`/`currentDateTime()` 到参考日 `2026-07-17`，保留 `currentTimeMillis()`/`nanoTime()` 走真实系统时钟（避免破坏 ContextProvider/wf 等依赖时间单调推进的设施；快照时间戳列已 `*` 通配）。4 个非 WORKFLOW 测试类加 `@RegisterExtension static HrFrozenClockExtension`。
- **关键裁决（TestErpHrPayrollSimulation 不能整类冻结）**：该类 WORKFLOW 模式（`setUpWfUser` 设 userId=0 + submit 启动 xwf），整类冻结 `currentDateTime()` → past 致 wf 步骤用户解析异常 → `salary_simulation_item_adj.ADJUSTED_BY` 由 0 退化为 null（9 errors，含原本通过的 8 用例）。诊断经隔离运行 + 无冻结对照确认。**方案**：仅在该类唯一日期漂移用例 `testConvertToFormalPaidConflict` 内 `installFrozenClock()`/try/finally/`restoreSystemClock()`（该用例冻结后全绿）。
- **TestErpHrContractExpiry 额外对齐**：该类测试代码用 `LocalDate.now()` 播种（7 处），冻结 CoreMetrics 后种子日期仍走系统时钟会与快照再次错位 → 全部改 `CoreMetrics.today()`，使种子与生产派生同源（均冻结到参考日）。
- **验证状态（full-green）**：`mvn install -DskipTests -pl module-hr/erp-hr-service` **BUILD SUCCESS**；`mvn test -pl module-hr/erp-hr-service` **Tests run: 111, Failures: 0, Errors: 0**（由 15 errors→0）。GapAnalysis 改动的直测 TestGapAnalysisCalculator 5/5 + TestAssessmentAggregator 7/7 全绿。
- **范围纪律**：纯测试基础设施（1 新增测试辅助类 + 5 测试类注解/导入/局部冻结/LocalDate→CoreMetrics 对齐）；零生产 Java/ORM/契约/codegen/config 变更；>5 文件但属 AGENTS.md「仅测试清理」低风险类（test-only date-stability），跳过正式计划。日期冻结到固定参考日是 Nop auto-test 对日期敏感快照的标准确定性范式（非时间穿越/非业务逻辑变更）。

Skill: nop-testing（auto-test 快照 CHECKING 模式 + 日期确定性根治）

### 2026-07-18（plan 2026-07-18-0100-2 HR 人才发展域浏览器层 E2E：评估→差距→发展计划闭环 + 员工调动合同联动）

- **背景**：执行 `docs/plans/2026-07-18-0100-2-hr-talent-development-e2e.md`。承接 `docs/plans/2026-07-14-0215-3-hr-direct-action-e2e.md` Deferred「胜任力评估 / 差距分析 / 发展计划 / 员工调动 E2E」（l.150-154，Successor Required: yes）+ Non-Goal「员工调动（transferEmployee）跨域编排」（l.38）。触发条件经实时仓库核实**已满足**：AGENTS.md「当前项目阶段」/ project-context.md:34 明示当前重点含「各域细化端到端验证」；0215-3 已交付 4 spec 覆盖 payroll/simulation/recruitment/leave-attendance，但人才发展 4 实体（`ErpHrEmployeeAssessment`/`ErpHrGapAnalysis`/`ErpHrDevelopmentPlan`/`ErpHrEmployee.transferEmployee`）+ 调动 8 DIRECT `@BizMutation` 浏览器层零覆盖。

- **Phase 1（Explore + hr-assessment-dev-plan 4 用例）**：Explore 实测 3 处工程化关键点：(a) `competency.category` 字典 `erp-hr/competency-category` 仅允许 SKILL/BEHAVIOR/KNOWLEDGE（非 BASIC，Java 单测用 BASIC 走的是 `saveEntity` 绕过 dict 校验，浏览器层经 xmeta 强制校验）；(b) `Map<Long,Integer>` 入参 generic `:Map` scalar 经 JSON 反序列化后键为 String（GraphQL/JSON 限制），Java `Map<Long,Integer>.getOrDefault(Long,0)` 失败 → 一律返回 0 → gap 全部 CRITICAL，**为 latent 缺陷**——本计划执行期修复 `ErpHrGapAnalysisBizModel.normalizeLevelMap` 增加 String/Number 键 → Long 转换（同 1430-1/1600-1 类应用层 length/type 修复范式），新增 `ERR_GAP_INVALID_LEVEL_MAP` ErrorCode（仅类型不匹配时抛，正常调用零影响），hr-service JUnit 全绿（pre-existing 6 个 date snapshot 07-17→07-18 漂移与本修复无关，已 git stash 验证）；(c) gapSeverity 计算规则经 `GapAnalysisCalculator.severityOf`：gapValue≤0 NONE / 1 MINOR / 2 MODERATE / ≥3 CRITICAL（criticalThreshold 默认 3）。4 用例：(1) completeAssessment 自动触发链 — 3 details（c1=4/c2=3/c3=2 vs requiredLevel=5）→ submitAssessment SUBMITTED → completeAssessment COMPLETED + overallScore=3.00(mean(4,3,2)) + findFirst ErpHrGapAnalysis 3 行存在 + gapSeverity 多档（MINOR/MODERATE/CRITICAL）+ gapValue/requiredLevel/actualLevel 精确数值断言；(2) refreshGapAnalysis 双入口 — completeAssessment 后调 refreshGapAnalysis（internal aggregate）幂等覆盖同 3 行 + 调 refreshGapAnalysisWithLevels（custom levels all-5）→ 全 NONE 重算 + 无 position 守卫 ERR_GAP_NO_ROLE_REQUIREMENT；(3) generateDevelopmentPlan + 子项状态机 — 仅 CRITICAL+MODERATE 生成 2 items（MINOR 跳过）+ updatePlanItemStatus NOT_STARTED→IN_PROGRESS（startDate 写回）→ACHIEVED（endDate 写回）+ completePlan→COMPLETED；(4) 守卫 — submitAssessment 无 detail 抛 ERR_ASSESSMENT_NO_DETAILS + completePlan 已 COMPLETED 抛 ERR_DEV_PLAN_ILLEGAL_STATUS_TRANSITION + generateDevelopmentPlan 无 actionable gaps 返回 null（data null + errors null）。

- **Phase 2（Explore + hr-transfer 4 用例）**：Explore 实测 2 处工程化关键点：(a) GraphQL schema 标 `targetSuperiorId`/`handleContract` 为非空入参（即便 Java 方法签名 `Long targetSuperiorId`/`String handleContract` 允许 null）—— 浏览器层必须显式传值，`targetSuperiorId=1` 用种子 HR-EMP-001，`handleContract=AUTO` 显式传字符串字面值；(b) `buildSuccessorCode` 生成新合同码 = `"TRF-"+empId+"-"+effectiveDate+"-"+active.code`（前缀 22 字符 + active.code），contract `code` 列 precision=50（domain="code"），过长的 active.code 会触发 sqlState=22001 字符截断（同 1430-1 类 buildCode overflow 缺陷），spec 用紧凑短码 `C${tag}-{ms}-{seq}` 规避，**未在生产代码层修复（转移 successor）**。4 用例：(1) handleContract=YES — emp dept/position 翻转 + 源合同 TERMINATED（硬编码 CONTRACT_STATUS_TERMINATED 无 INACTIVE 分支）+ 新 ACTIVE 合同经 findFirst 反查 + signDate=effectiveDate；(2) handleContract=NO — emp dept/position 翻转 + 源合同 status 仍 ACTIVE + 计数仍 1 无新增；(3) handleContract=AUTO（config-gated `erp-hr.transfer-auto-handle-contract=true` 默认值）行为同 YES；(4) 守卫 — RESIGNED 员工抛 ERR_EMPLOYEE_NOT_TRANSFERABLE token「不可调动」+ dept 不变 + 目标 dept+position 不属 dept 抛 ERR_TRANSFER_TARGET_POSITION_NOT_FOUND token「目标职位」。

- **执行期 latent defect 修复（refreshGapAnalysisWithLevels Map<Long,Integer> 键类型不匹配）**：发现路径 — Phase 1 第一次运行 `refreshGapAnalysisWithLevels(employeeId, {c1:5,c2:5,c3:5})` 后断言 gapSeverity=NONE 失败，反查 DB 见 actualLevel=0/gapValue=5 全 CRITICAL，根因分析 — GraphQL generic `:Map` scalar 经 JSON 反序列化键为 String，Java `Map<Long,Integer>.getOrDefault(rc.getCompetencyId(),0)` 失败（Long.equals(String)=false）→ 默认值 0 → gapValue=required-0=5 → CRITICAL。修复 — `ErpHrGapAnalysisBizModel` 新增 `normalizeLevelMap` 静态 helper（接受 `Map<Long,Integer>` 声明类型但内部按 `Map.Entry<?,?>` 遍历键值，经 `toLongKey`/`toIntValue` 转 Long/Integer）+ 新 ErrorCode `ERR_GAP_INVALID_LEVEL_MAP`（仅 String/Number 之外的类型或不可解析字符串时抛，正常调用零影响）。验证 — git stash 对照测试 6 个 pre-existing date snapshot 漂移（07-17→07-18）与本修复无关；hr-service JUnit 编译通过。

- **Closure（文档对齐）**：e2e-runbook 业务动作表 +2 hr 人才发展行（hr-assessment-dev-plan / hr-transfer）+ 套件计数 70→72 + 计数段补 0100-2 增量；0215-3 Deferred 补 `**RELEASED by 2026-07-18-0100-2**`+ Non-Goal「员工调动跨域编排」标 RELEASED；roadmap README +1 行；当日日志本条目。

- **验证状态**：`PLAYWRIGHT_PORT=8011 SKIP_WEBSERVER=1 npx playwright test tests/e2e/business-actions/hr-assessment-dev-plan.action.spec.ts tests/e2e/business-actions/hr-transfer.action.spec.ts --workers=1` **8 passed**（59.2s）；`mvn install -pl app-erp-all -am -DskipTests` **154 模块 BUILD SUCCESS**。

- **范围纪律**：测试层 + 文档 + 1 处 DIRECT 动作 latent defect 应用层修复（`normalizeLevelMap` 键类型守护 + 新 ErrorCode）；零 ORM/契约/种子/config 变更；Map<Long,Integer> GraphQL 反序列化平台级限制（非应用 bug，但 DIRECT 动作暴露外部不可用须修）；`ErpHrEmployeeBizModel.buildSuccessorCode` 超 code precision=50 缺陷转 successor（生产代码修复须 ask-first）。

Skill: none（DIRECT `@BizMutation` 经 GraphQL 浏览器层 + 状态机/自动触发链/合同联动副作用断言 → E2E 测试本体 Skill: none，对齐 0215-3 范式裁决）

### 2026-07-18（plan 2026-07-18-0100-1 HR 排班深度编排浏览器层 E2E：批量分配 + 周期复制 + 考勤计算 + 轮换引擎 + 调换状态机 + approve 双向班次交换）

- **背景**：执行 `docs/plans/2026-07-18-0100-1-hr-shift-scheduling-orchestration-e2e.md`。承接 `docs/plans/2026-07-14-0215-3-hr-direct-action-e2e.md` Deferred「排班批量生成 + 调换审批深度编排 E2E」（l.143-147，Successor Required: yes）。触发条件经实时仓库核实**已满足**：AGENTS.md「当前项目阶段」/ project-context.md:34 明示当前重点含「各域细化端到端验证」；0215-3 已交付 4 spec 覆盖 payroll/simulation/recruitment/leave-attendance，但排班 4 实体（`ErpHrShift`/`ErpHrShiftAssignment`/`ErpHrShiftRotationPattern`/`ErpHrShiftSwapRequest`）9 DIRECT `@BizMutation` 浏览器层零覆盖。

- **Phase 1（Explore + hr-shift-assignment 5 用例）**：`assignBatch` 返回 `List<ErpHrShiftAssignment>` 经 callMutationOk selection 直接断言行数（data 字段即数组）；`copyFromPeriod` 同样 List 返回；`calcAttendance(employeeId, assignmentDate)` 入参仅 Long + LocalDate，内部按 attendance.lateMinutes/earlyLeaveMinutes/isAbsent + assignment.actualStartTime/actualEndTime/isAbsent 双落点写回（经 `ErpHrShiftBizModel.calcAttendance :56-110` 实测）。spec 自包含 setup（建测试专用 employee + shift），5 用例：(1) assignSingle SCHEDULED + (employeeId,date) 唯一约束守卫 `ERR_SHIFT_DUPLICATE_ASSIGNMENT` token「已存在」；(2) assignBatch 3 emp × 3 day 笛卡尔积 = 9 行 + 重复区间跳过 0 新增；(3) copyFromPeriod 3 天源周期复制到 +31 offset 目标周期；(4) calcAttendance late clockIn(09:30 > 09:00+15) → lateMinutes=30 + assignment.actualStartTime 回写 + status SCHEDULED→PRESENT + isAbsent=false 双落点；(5) calcAttendance on-time(09:05 ≤ 09:00+15) → lateMinutes=0 对照。

- **Phase 2（hr-shift-rotation 2 用例）**：Explore 实测 `patternData` 为 JSON 数组 of 班次 **code 字符串**（BizModel :124-150 `parseAndValidateSequence` + `buildShiftCodeMap` 按 `eq("code", code)` 查 ErpHrShift）；"OFF" 字面值跳过；`staggerDays·memberIdx` 加到 startDate 作为该成员起始日（:78-79）；`findActiveAssignment` 仅匹配 `status=SCHEDULED`（CANCELLED/PRESENT/ABSENT 不阻断，:96-105）；`regenerate=true` 先 CANCEL 区间内 SCHEDULED 后重建（:71-73 + :177-194）；`regenerate=false` 静默跳过（:85）。确定性样本：3 成员 × staggerDays=1 × cycleLength=3 × 3-day 区间 → 3+2+1=**6 行**。两用例：(1) 首次生成 regenerate=false 返回 6 行 + member0=3/member2=1 错峰断言；(2) regenerate=true 重生成：CANCEL 旧 6 SCHEDULED + 重建 6 + findItems 反查 CANCELLED 行存在；再 regenerate=false 静默跳过 0 新增对照。

- **Phase 3（hr-shift-swap 4 用例）**：B1 修订采纳——submit 无前置状态守卫、无重复守卫，spec 显式避测重复-submit 假设。4 用例：(1) happy path submit→PENDING→approve→APPROVED + 经 verifyState 双 assignment `__get` 断言 (a) source.shiftId 翻转=原 target shiftId；(b) target.shiftId 翻转=原 source shiftId；(c) 双 swapRequestId=swap.id；(d) 双 replacedByAssignmentId 双向回链 source↔target；(e) 双 status=SCHEDULED 重置；(2) reject 不交换；(3) cancel 不交换；(4) APPROVED→approve 与 APPROVED→reject 双守卫拒（`ERR_SHIFT_SWAP_ILLEGAL_STATUS_TRANSITION` token「不允许」）。

- **Closure（文档对齐）**：e2e-runbook 业务动作表 +3 hr 排班行（hr-shift-assignment / hr-shift-rotation / hr-shift-swap）+ 套件计数 67→70 + 计数段补 0100-1 增量；0215-3 Deferred 补 `**RELEASED by 2026-07-18-0100-1**`；当日日志本条目。

- **验证状态**：`PLAYWRIGHT_PORT=8011 npx playwright test tests/e2e/business-actions/hr-shift-*.action.spec.ts --workers=1` **11 passed**（1.4m）；business-actions 全套件回归 **184 passed**（22.3m，0 新增失败）；`mvn clean install -DskipTests` **154 模块 BUILD SUCCESS**（closure gate，确认零后端污染——本计划纯测试 + 文档，无 Java/ORM/契约/codegen/config 变更）。

- **范围纪律**：纯测试层 + 文档。零生产 Java/ORM/契约/codegen/config 变更；测试在根 `tests/e2e/` 非 reactor 模块；onLeaveApproved/onLeaveCancelled 钩子跨实体联动（休假→排班 isAbsent 标记）、ErpHrShiftRotationGroup 实体化、排班日历/看板 AMIS 可视化、出勤率/调换率报表、nop-job 定时提醒均归 successor/follow-up（Deferred But Adjudicated 已裁决）。

Skill: none（DIRECT `@BizMutation` 经 GraphQL 浏览器层 + 状态机/批量产物/双向副作用断言 → E2E 测试本体 Skill: none，对齐 0215-3 范式裁决）
