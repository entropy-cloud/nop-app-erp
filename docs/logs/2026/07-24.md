# Development Log - 2026-07-24

### 2026-07-24（daoFor Type 1 ORM 导航可替代重构第二批 + 收尾评估 — plan 2000-1，F1 successor 收尾）

- **任务**：执行 `docs/plans/2026-07-24-2000-1-daofor-type1-orm-navigation-refactor-batch2.md`（0605-3 §Deferred「Type 1 剩余域重构 safe 子集第二/三批」+ 治理审查 §F1 successor）。触发条件「第一批范式验证通过后按域推进」已满足（batch 1 closure audit PASS）。
- **Phase 1（全域 `getEntityById(FK)` 逐处分类 + MrpReleaseService 豁免裁决 + `findAllByQuery` 评估）**：单行 grep 枚举 15 处 chained 生产站点（drp 2/logistics 2/maintenance 2/mfg 7/hr 1/sales 1）。逐处三态分类：**safe** 15/15（ORM `<to-one>` 均预核存在）；**ORM-gap** 0；**not-Type-1** 排除 2 处 Type 5 dashboard + ErpCtRebateSettlementBizModel Non-Goal 排除。MrpReleaseService 裁决=选 A（局部重构 3 处同域只读 `getMrpPlan()`，豁免仅约束跨域写路径，posting-exemptions.md 无需改动）。`findAllByQuery` 评估：113 处站点可机械替换候选 <10 → watch-only residual（不开 successor）。
- **Phase 2（safe 子集重构）**：15 处 `daoProvider().daoFor(X).getEntityById(entity.getYId())` → ORM `<to-one>` 关系 getter：`scenario.getBaseDrpPlan()`/`version.getComputedDrpPlan()`/`shipment.getCarrier()`/`usage.getEquipment()`/`ledger.getMaterial()`/`issue.getWorkOrder()`/`line.getMrpPlan()`/`simulation.getSourceSalary()`/`line.getReceipt()`。行为不变（关系 getter 返回同一托管实体；原 ternary `xxxId != null ? getEntityById(...) : null` 与 getter 语义等价）。
- **Phase 3（checker 基线下降 + 文档对齐）**：`mvn clean install -DskipTests` 全 154 模块 BUILD SUCCESS；受影响域 `mvn test` 全绿（mfg 136/sal 119/mnt 54/hr 113/drp 34/log 23，0 failures）；checker 复跑基线下降：R2b 317→315 / R2c 1090→1075 / R2d 31→27。
- **闭包审计修正（关键）**：独立结束审计（`ses_06e56a3e3ffe25gFfoNMcE45E1`，fresh context）首轮 **FAIL**——发现单行 grep 漏看 4 处**多行 chained** `daoFor(X)\n.getEntityById(FK)` 站点（`SimulationMrpEngine:107-108,183-184`/`ErpHrSalarySimulationBizModel:651-652`/`ErpFinBudgetControlBiz:151-152`，均为已重构站点的多行镜像，ORM 关系均存在）。执行者据审计补重构 4 处 BLOCKER + 补录 variable-split 子模式 successor（`dao=daoFor(X); dao.getEntityById(FK)` ~15 处，需逐处 Type 1/Type 2 分类，非机械替换）+ 精确化「清零」声明（chained 形态清零，variable-split 形态 successor）+ 二次 `mvn clean install -DskipTests` BUILD SUCCESS + fin/mfg/hr 三域二次测试全绿（fin 264/mfg 136/hr 113）+ 二次 checker 复跑（R2b 315→314 / R2c 1075→1071）。最终累计重构 **19 处**。
- **产出**：plan 2000-1（Plan Status: completed，全 3 phase completed）/ compliance-baseline R2b=314/R2c=1071/R2d=27（双块一致 + 闭包审计修正注记）/ governed-path-cost-evaluation §3.6 / 治理审查 F1 收尾进展。**至此 F1 Type 1 `getEntityById(FK)` chained 工作流收尾**（两批累计 37 处）。
- **验证（full-green verification）**：154 模块 BUILD SUCCESS + 7 受影响域 `mvn test` 全绿 + checker 基线精确下降（R2b=314/R2c=1071/R2d=27，非回归）+ ORM 保护区域零变更（git diff 仅服务层 Java）。
- **Skill**：`nop-backend-dev`（Phase 1/2 ORM 关系导航 + daoFor 收敛 + E2/E3 自检）。
- **Deferred（已在 plan §Deferred But Adjudicated 登记）**：(1) variable-split 子模式 successor（~15 处，触发条件：逐处 Type 1/Type 2 分类完成）；(2) ErpCtRebateSettlementBizModel chained 只读站点（Non-Goal 排除，触发条件：豁免收敛或独立 carve-out）；(3) `findAllByQuery` Type 1 watch-only residual（<10 机械候选，触发条件未满足）；(4) Type 1 ORM-gap（=0）；(5) Type 4 跨域写/读（阻塞中）。
- **独立结束审计**：`ses_06e56a3e3ffe25gFfoNMcE45E1`（fresh context，新会话冷重播）首轮 **FAIL**（多行 grep 盲区 + variable-split successor 缺失）→ 执行者修复 4 BLOCKER + 补录 successor → 证据见 plan §Closure Audit Evidence。**教训**：daoFor 模式枚举须用多行 grep（`rg -U`）覆盖换行 chained 形态，单行 grep 会系统性漏看同型站点。

### 2026-07-24（daoFor Type 1 ORM 导航可替代重构第一批 — plan 0605-3，F1 successor）

- **任务**：执行 `docs/plans/2026-07-24-0605-3-daofor-type1-orm-navigation-refactor-batch1.md`（0930-3 §Follow-up「Type 1（~100-150 ORM 导航可替代）daoFor 按域分批重构」+ 治理审查 §F1 successor + governed-path-cost-evaluation §3.2/§4 裁决）。Phase 1 裁决=可安全重构，无前置阻塞。
- **Phase 1（Type 1 子集逐处枚举 + 三态分类）**：`grep -rEn 'daoFor\([^)]+\)\.getEntityById\([a-zA-Z_]+\.[gG]et[A-Z][a-zA-Z]*Id\(\)\)'` 覆盖 finance/manufacturing/inventory/assets/projects/purchase/sales 域 BizModel + Processor。逐处核实 ORM `<to-one>` 关系是否已建模。三态分类：**safe** 18 处（assets 12 / finance 5 / mfg 1，ORM 关系均预先存在）；**ORM-gap** 0 处（本批枚举的所有 safe 候选关系均已建模）；**not-Type-1** 排除集（Type 5 看板只读聚合 ErpPur/ErpSal Dashboard + Type 2 显式豁免 ErpFinVoucherBizModel countLines/loadBillLinks + 按属性查找 ErpFinBudgetCommitmentBizModel/ErpFinReportBizModel/ErpFinCashForecastBizModel）。
- **Phase 2（第一批 safe 子集重构）**：18 处 `daoProvider().daoFor(X).getEntityById(entity.getYId())` → ORM `<to-one>` 关系 getter：`disposal.getAsset()` / `asset.getCategory()` / `split.getSourceAsset()` / `adjustment.getAsset()` / `advance.getEmployee()` / `claim.getClaimant()` / `line.getSubject()` / `latestCashLink.getVoucher()` / `wo.getBom()`。行为不变（关系 getter 返回同一托管实体，后续 saveOrUpdateEntity/null 判定/字段读取语义完全一致）。移除 `ErpFinEmployeeAdvanceBizModel` 冗余 `IDaoProvider` import + 局部 `IDaoProvider dp` 变量。
- **Phase 3（checker 基线下降 + 文档对齐）**：`mvn clean install -DskipTests` 全 154 模块 BUILD SUCCESS；受影响域 `mvn test` 全绿（ast **78**/fin **264**/mfg **136** tests，0 failures）；checker 复跑基线下降：R2b 319→**317**（-2）/ R2c 1108→**1090**（-18）/ R2d 34→**31**（-3），与重构站点数精确匹配。`compliance-baseline.md` 基线表 + machine-readable 块更新（含增量注记）；`governed-path-cost-evaluation.md` §3.5 第一批落地证据（剩余 Type 1 估算≈82-132）；治理审查 §F1 successor 进展更新；0930-3 §Follow-up Type 1 项标 ✅。
- **产出**：plan 0605-3（Plan Status: completed，全 3 phase completed）/ compliance-baseline R2b/R2c/R2d 下降 + 注记 / governed-path-cost-evaluation §3.5 / 治理审查 F1 进展。**至此 F1 Type 1 第一批 safe 子集闭包**。
- **验证（full-green verification）**：154 模块 BUILD SUCCESS + 3 受影响域 `mvn test` 全绿 + checker 基线精确下降（R2b=317/R2c=1090/R2d=31，非回归）。
- **Skill**：`nop-backend-dev`（Phase 1/2 ORM 关系导航 + daoFor 收敛 + E2/E3 自检——关系 getter 替代 daoFor）。
- **Deferred（已在 plan §Deferred But Adjudicated 登记）**：(1) Type 1 剩余 14 域 safe 子集（触发条件：第一批范式验证通过后按域计数推进）；(2) Type 1 ORM-gap（本批=0，剩余域 successor 枚举时判定）；(3) Type 4 跨域写/读（阻塞中，待平台解耦）。
- **独立结束审计**：`ses_06e90cc9bffeIar1DtSRSI4LvQ`（fresh context，新会话冷重播）→ **PASS**（18 处重构逐项冷复核；ORM 保护区域零变更 git diff 确认；checker 独立复跑匹配；三域测试全绿独立复核；证据见 plan §Closure Audit Evidence）。

### 2026-07-24（硬编码状态字面量→Erp*DocStatus 常量收敛 — plan 0605-2，F2(d) successor RELEASED）

- **任务**：执行 `docs/plans/2026-07-24-0605-2-hardcoded-status-literal-constant-convergence.md`（0930-2 §Deferred「硬编码状态字面量全量替换」+ 治理审查 `2026-07-23-0000` §F2(d) successor）。触发条件「D1 接口全域落地」已满足。纯应用层 Java 常量引用替换，零 ORM/契约/字典/ext:dict 变更。
- **Phase 1（权威字面量清单 + 语义轴判定）**：grep 全 9 域 service src/main/java 的 7 个状态字面量，产出权威清单 `docs/audits/hardcoded-status-literal-inventory.md`（171 处命中逐条 file:line）。**关键纪律**：拒绝机械全局替换，逐条判三态——REPLACE（doc/approve 轴使用层）/ EXCLUDE-异语义（visit-status/recall-status/posted-status/schema-active 等）/ EXCLUDE-mfg doc-status（绑 work-order/issue/subcontract 域专属字典，Non-Goal）/ EXCLUDE-已封装常量定义与跨域镜像。替换框 = ~75 处使用字面量 + 6 处本地重复常量定义（inv×2 Processor + mfg MrpRelease）。
- **Phase 2（逐域常量引用替换）**：purchase/sales/finance/assets/quality/mfg(approve 轴) 各 Processor `illegalTransition(entity, status, "LITERAL")` → `Erp*Constants.APPROVE_STATUS_*`；inventory `ErpInvCostAdjustProcessor`/`ErpInvLandedCostProcessor` 删本地 `APPROVE_STATUS_*` 重复定义，全部 setter/compare/query-filter 改引用 `ErpInvConstants.*`（含 `ne("docStatus", DOC_STATUS_CANCELLED)` + `ErpInvStockMoveProcessor` ARG_EXPECTED_STATUS）；mfg `MrpReleaseService` 删本地 `PUR_DOC_STATUS_DRAFT`/`PUR_APPROVE_STATUS_UNSUBMITTED` 副本改引用 `ErpPurDocStatus.*`（mfg-service 已依赖 pur-dao entity）；qa `NcrReturnOrchestrator` 跨域 `data.put` 改引用 `ErpPurDocStatus`/`ErpSalDocStatus`；fin `ErpMdEmployeeReferenceCheckerImpl` 查询过滤改引用 `ErpFinConstants.DOC_STATUS_CANCELLED` + 补 import。组合显示串（"UNSUBMITTED 或 REJECTED"/"非已作废"）与排除集保留原样。
- **Phase 3（验证 + 基线同步）**：`mvn clean install -DskipTests` 全 154 模块 BUILD SUCCESS；受影响 7 service 模块 `mvn test` BUILD SUCCESS（0 failures/0 errors，常量值与原字面量等价故状态机行为逐字节不变）；`bash docs/audits/nop-compliance-checker.sh` 全 16 规则均 ≤ 基线（R3=19 / R11=0 / R2c=1108 等，无回归——字面量→常量不引入 `new Erp*()` 构造）。治理审查 §F2(d) 标注 RELEASED + (d) 证据行追加 RELEASED 注记；`compliance-baseline.md` 追加「R3/R11 同步注记（plan 0605-2）」；0930-2 §Deferred 硬编码字面量替换项标注 RELEASED。
- **产出**：plan 0605-2（Plan Status: completed，全 3 phase completed）/ `docs/audits/hardcoded-status-literal-inventory.md`（NEW 权威清单）/ 治理审查 F2(d) RELEASED / compliance-baseline R3/R11 注记 / 0930-2 Deferred RELEASED。**至此 F2(d) 服务层硬编码字面量 successor 闭包**。
- **验证**：154 模块 BUILD SUCCESS + 7 受影响域 `mvn test` 全绿 + checker 全 16 规则 ≤ 基线无回归。
- **Skill**：`nop-backend-dev`（Phase 1/2 语义轴判定 + 跨实体常量引用 + 产品化可定制性自检）。
- **Deferred（已在 plan §Deferred But Adjudicated 登记）**：(1) ORM ext:dict 统一（0930-2 既有 Deferred，ask-first 保护区域）；(2) 测试代码字面量替换（watch-only，机械替换收益低风险错）。
- **独立结束审计**：pending（执行者未自我审计；待新会话独立子代理执行 closure audit）。



- **任务**：执行 `docs/plans/2026-07-24-0605-1-generated-file-contentdiff-zero-drift-verification.md`（治理审查 `2026-07-23-0000` §闭包前必须项 #12（P2）+ §绿色信号表 caveat —— 12 项闭包项中最后一项，消除「全仓单 author `canonical`，无法 blame 区分人写 vs 重生成」caveat）。纯验证 + 文档，零生产代码变更。
- **Phase 1（权威人口 + 抽样框）**：裁决口径 (a)=git-tracked `_` 前缀 java/xml（实测 **797**，0 gitignored；370 java+427 xml / 702 `_gen/`+95 顶层）。**22,413↔797 口径调和**：22,413 不匹配任何 git-tracked 口径（max any-ext=2,238），推测口误/位序错排或含工作树未跟踪文件，以 797 为权威。残留风险=非 `_` 前缀生成产物（xmeta/xbiz/i18n 各 351 `_` 子集）登记 successor。
- **Phase 2（content-diff 抽样 + 漂移判定）**：(1) 19 个 `_app.orm.xml` 全量——全历史 **94/94** commit 配对 `model/*.orm.xml`，0 未配对；(2) 全局 commit 级配对扫描 115 commit——94 配 model 源 + **21 初筛候选**；(3) 21 候选逐个 content-diff 复核——修正配对口径（`_gen/_Erp*.view.xml` 源为 XMeta+`_templates`+parent view，非 orm model），全部判 codegen 驱动。关键可证伪证据：`d7fb77337` action-auth `x:post-extends` 经 3 轮 ORM-regen（dc03a3ff0/d0047f0b8/394c8cae4）存活；`171e4e651` view 布局变更纯机械字段重排跨 ~300 文件且经 2 轮 regen 保留；`_gen/_ErpFinAccountingPeriod.java` 5/5 配对 model 源。三态判定：codegen 驱动 115/115、已认可例外 0、**真手编辑漂移 0**（无需 Fix successor）。
- **Phase 3（闭包证据 + caveat 消除）**：治理审查更新 5 处（§执行摘要/§Design Review Matrix Truth/§绿色信号表 caveat→「已实证消除」/§闭包项 #12→✅ Done/§附录未执行核查→已完成）。产出独立审计证据 `docs/audits/2026-07-24-0605-generated-file-content-diff-evidence.md`（NEW：§1 决策 + §2 抽样框 + §3 content-diff 结果与判定 + §4 复现指南含可独立复跑命令）。
- **产出**：plan 0605-1（Plan Status: completed，全 3 phase completed）/ `docs/audits/2026-07-24-0605-generated-file-content-diff-evidence.md`（NEW 独立证据）/ 治理审查闭包项 #12 ✅ Done + 绿色信号 caveat 实证消除。**至此治理审查 12 项闭包前必须项全部 ✅ Done**（#1-#12）。
- **验证**：零生产代码变更，无构建验证门控（按计划 Closure Gates 明示）；验证门控=文档一致性 + 抽样可复现（命令记录在证据文件 §4）。
- **Skill**：`nop-debugging`（Phase 2 漂移候选根因分析——修正配对口径区分 codegen 源类型）。
- **Deferred（已在 plan §Deferred But Adjudicated 登记）**：(1) content-diff 自动化 CI 门控（触发条件：手编辑漂移复发或人工要求持续门控）；(2) 非 `_` 前缀生成产物漂移验证（触发条件：codegen 标记机制落地）。
- **独立结束审计**：`ses_06ee0672affe22d8HuUge44Jlu`（fresh context，新会话冷重播）→ **PASS**（797/0-gitignored 独立复测精确；94/94 配对独立复核；21 候选独立抽检 d7fb77337/171e4e651/FK-name-resol 全 CONFIRMED；可证伪性检查 PASS——三层检测能在漂移存在时失败；治理审查 5 处更新全部命中）。
- **VERIFY 步骤构建复核（build + unit-test green）**：`mvn clean install -DskipTests` 全 154 模块 BUILD SUCCESS（01:33 min）；`mvn test` BUILD SUCCESS（0 Failures/0 Errors，09:38 min，ErpAllWebPagesCollectTest 仍为已接受 @Disabled H-2）。零生产代码变更，复核确认既有全绿基线无漂移。

### 2026-07-24（F6+F7 跨域命名与常量收敛 — plan 1400-2）

- **任务**：执行 `docs/plans/2026-07-24-1400-2-cross-domain-naming-constant-convergence.md`（治理审查 `2026-07-23-0000` §F6（LOW）闭包项 #10 + §F7（LOW）闭包项 #11 —— 12 项闭包前必须项中最后两项 P2）。
- **Phase 1（F6 #10 mfg→qa 生成常量依赖消除）**：mfg 的 `ErpMfgWorkOrderProcessor` 跨域 import qa 的 codegen 产物 `_ErpQaDaoConstants`（唯一使用点 `INSPECTION_TYPE_FINAL` 完工检验触发）。**Decision（选 B 新建专用接口）**：按 D1 先例在 `erp-qa-dao` 新建非生成接口 `ErpQaInspectionType`（`module-quality/erp-qa-dao/.../constants/`，承载 4 个 inspection-type 值），而非扩展现有 `ErpQaDocStatus`——检验类型与 doc/approve 状态属不同语义轴，混入破坏 `ErpQaDocStatus` 语义内聚。`ErpMfgWorkOrderProcessor` import + 使用点切换到 `ErpQaInspectionType.INSPECTION_TYPE_FINAL`。`grep _ErpQaDaoConstants module-manufacturing`=0（checkpoint 满足）。
- **Phase 2（F7 #11 drp 实体命名裁决与登记）**：drp 4 实体（`ErpInvDrpSafetyStockCalc`/`ErpInvDrpCrossDock`/`ErpInvDrpDockAppointment`/`ErpInvDrpLeadTimeRecord`）类名沿用 `Inv` 历史段，表名 `erp_inv_drp_*`，物理归属已正确（`module-drp`，className `app.erp.drp.dao.entity.*`）。**Decision（登记命名例外，方案 b，零 ORM 风险）**：重命名触及 ORM 保护区域 + 表名迁移 + 66 文件生成产物连锁，风险高于收益（`Inv` 段已由 `Drp` 段 + 物理归属显式标识域归属）。`docs/design/drp/README.md` 新增 §`ErpInvDrp*` 实体命名例外登记（裁决理由 + 4 实体逐项登记表：类名/className/表名/所属域/消费 dict/豁免理由/收敛触发条件 + 覆盖范围声明）；`docs/architecture/domain-module-split-analysis.md §3` 追加已登记命名例外交叉引用。重命名移入 Deferred。`grep ErpInvDrp module-drp` 全部 66 命中落入登记覆盖范围（checkpoint "或登记"分支满足）。
- **Phase 3（验证）**：`mvn clean install -DskipTests` 全 154 模块 BUILD SUCCESS；`bash docs/audits/nop-compliance-checker.sh` 全 16 规则 = 基线零回归（R3=19 不变：新增 `ErpQaInspectionType` 为接口无 `new Erp*()`；R12 不变：非共享内核追踪类型）。
- **产出**：plan 1400-2（Plan Status: completed，全 3 phase completed）/ `ErpQaInspectionType.java`（NEW 非生成 dao 层接口）/ `ErpMfgWorkOrderProcessor.java`（import 切换）/ `docs/design/drp/README.md`（§实体命名例外登记）/ `docs/architecture/domain-module-split-analysis.md §3`（交叉引用）/ 治理审查闭包项 #10/#11 标 ✅ Done。
- **验证（full-green verification）**：`mvn clean install -DskipTests` 全 154 模块 BUILD SUCCESS；checker 16 规则全 = 基线（R1a-d=0/0/0/23, R2a-d=37/319/1108/34, R3=19, R4=0, R5=0, R6=7, R7=2, R8=42, R10=51, R11=0, R12a-c=69/66/38）。
- **Skill**：`nop-backend-dev`（Phase 1 跨实体调用 + dao 层常量接口 + D1 先例路由）；Phase 2 纯文档 Skill: none。
- **Deferred（已在 plan §Deferred But Adjudicated 登记）**：`ErpInvDrp*` 4 实体重命名 → `ErpDrp*`（触发条件：drp 域重大 ORM 变更时顺带，或人工批准 ORM/表名迁移）。
- **独立结束审计**：独立 closure-auditor 子代理（新会话冷重播，无执行者上下文，2026-07-24）→ **PASS**（Phase 1/2 退出标准逐项冷复核：`grep _ErpQaDaoConstants module-manufacturing`=0；`ErpQaInspectionType` 4 值接口在位；`grep ErpInvDrp module-drp` 66 命中全覆盖于 drp owner doc 登记范围；anti-hollow / five-point consistency / deferred honesty 全 PASS；证据见 plan §Closure Audit Evidence）。
- **VERIFY 步骤独立复核（full-green verification）**：`mvn install -DskipTests -pl erp-qa-dao,erp-mfg-service -am` BUILD SUCCESS；`mvn test -pl erp-qa-dao,erp-mfg-service` Tests run: 136, Failures: 0, Errors: 0；checker 16 规则全 = 基线零回归。

### 2026-07-24（F4 隐性共享内核显式化 — plan 1400-1）

- **任务**：执行 `docs/plans/2026-07-24-1400-1-shared-kernel-extraction.md`（治理审查 `2026-07-23-0000` §F4（MEDIUM）+ 闭包前必须项 #5（P1）—— 12 项闭包项中最后未启动的 P1）。目标是消除 finance/master-data 承担未声明公共内核职责的状态。
- **Phase 1（Explore + Decision）**：对 3 个跨域语义类型逐个分类实仓复核——`ErpFinBusinessType`（enum，56 常量，纯）/`PostingEvent`（纯 DTO）/`AcctSchemaResolver`（dao 耦合静态工具，import `ErpMdAcctSchema`+`IDaoProvider`）。跨域 import 基线 69/66/38（import 语句级，排除 test+所属域+_gen）；消费层全部位于 `-service`；3 类型 ORM 零引用。**关键证据**：enum 派发消费经 `== CONSTANT` 常量比较（7 跨域站点/4 文件：Inv/Pur/Sal AcctDocProvider+InvPostingDispatcher）+ finance 自身 `switch(enum)`（3 站点），强绑定具体 enum 类 → 不可降级 SPI 接口。**裁决=候选 (b)**：接受为显式共享内核（类型不迁移）。否决 (a) 抽 common-api（`AcctSchemaResolver` dao 耦合不可入零 dao/entity 模块；enum/DTO 虽可行但 135 站点改动零治理增量）与 (c) SPI 接口（enum 不可接口化）。产出 `docs/analysis/shared-kernel-extraction-decision.md`（NEW）。
- **Phase 2（落地裁决）**：`nop-compliance-checker.sh` 新增 R12a/R12b/R12c（追踪 3 类型跨域 import，实测 69/66/38，eval-find 计数复核）；`compliance-baseline.md` 增 R12 表行 + yaml 块 + 同步注记；`module-boundaries.md` 新增 §共享内核（所有权/消费域/变更影响/依赖方向 + 裁决约束）；`data-dependency-matrix.md` 新增 §3.3；`docs/design/finance/posting.md` 校正 `PostingEvent.businessType` 真实类型（enum，原误标 String）+ 引用裁决；`docs/design/master-data/README.md` 登记 `AcctSchemaResolver` 共享工具。CI gate python 模拟 PASS（19/19 规则，R12 被 summary 解析 → 强制，非 dead armor）。
- **Phase 3（验证）**：`mvn clean install -DskipTests` 全 154 模块 BUILD SUCCESS（01:31 min）；`mvn test` BUILD SUCCESS（0 Failures/0 Errors，含 ErpAllWebPagesTest 全 19 域页面校验）；`mvn dependency:tree -pl module-master-data` 仅自身（零业务依赖，叶模块 erp-md-service/dao 复核仅 `app-erp-master-data-*` 自身，checkpoint #5 满足）；DAG 无环（reactor 成功）。
- **产出**：plan 1400-1（Plan Status: completed，全 3 phase completed）/ `docs/analysis/shared-kernel-extraction-decision.md`（NEW 裁决文档）/ checker R12 + baseline / `module-boundaries.md §共享内核` / `data-dependency-matrix.md §3.3` / `posting.md` 契约校正 / `master-data/README.md` 共享工具登记 / 治理审查 §F4 + 闭包项 #5 标 ✅ Done。
- **验证（full-green verification）**：`mvn clean install -DskipTests` + `mvn test` 全 154 模块 BUILD SUCCESS；`mvn dependency:tree -pl module-master-data` 零业务依赖；CI compliance gate 模拟 PASS。
- **Skill**：`nop-backend-dev`（跨实体访问决策门 + 产品化可定制性自检判定抽取是否引入穿透风险）。
- **Deferred（已在 plan §Deferred But Adjudicated 登记）**：daoFor Type 1/4 真违规子集重构（~110-180 处，专项计划）/ finance 77 mutation owner-doc 逐项核对（触发条件：语义漂移时）。
- **独立结束审计**：ses_06f2cb4d0ffe2Eve6jifh7QEO7（fresh context，新会话冷重播）→ **CLOSURE AUDIT: PASS**（0 Blocker / 0 Major / 2 Minor）。Minor #1（裁决文档"10+ 站点"实为"7 站点/4 文件"）已修正；Minor #2（结束填表）即计划 Closure 节。



### 2026-07-24（F1 治理路径成本评估 + F1/F3/F5 架构文档对齐 — plan 0930-3）

- **任务**：执行 `docs/plans/2026-07-24-0930-3-governed-path-cost-eval-arch-doc-alignment.md`（治理审查 `2026-07-23-0000` §F1 闭包项 #1（P0）+ #2（P0）+ §F3 #7（P1）+ §F5 #8（P1））。纯文档对齐 + 评估，零生产代码变更。
- **Phase 1（F1 governed path 成本评估）**：实测 `mvn test -pl module-contract/erp-ct-service`（37 tests）+ `mvn test -pl module-aps/erp-aps-service`（22 tests）均 BUILD SUCCESS。根因：两域零跨域 `@Inject I*Biz` + 仅 DAO 级 Maven 依赖（ct→purchase-dao/sales-dao，aps→inventory-dao/manufacturing-dao），回避 I*Biz 以保单模块测试独立性。**裁决=分支 (b)**：I*Biz 强注入会破坏单模块测试（反事实经依赖结构验证）；Type 1（~100-150 ORM 导航可替代）可安全重构，Type 4（~10-30 跨域写/读）需 nop-entropy 平台 lazy/SPI 解耦或保留豁免。产出 `docs/analysis/governed-path-cost-evaluation.md`（NEW）。
- **Phase 2（F1 豁免登记 + F3 DAG 边）**：`posting-exemptions.md` 补登 `ErpB2bAsnBizModel`（b2b→pur，:215,226 + :266 行级回填，config-gated `erp-b2b.asn-auto-create-receive` 默认 false）；`data-dependency-matrix.md §5.6.2` 补 3 边（mfg→inv `ErpInvBatch`/mnt→ast `ErpAstAsset`/drp→inv `ErpInvStockMove`，均标"批准保留"）+ 同步 §6.5。
- **Phase 3（F5 notify owner doc）**：创建 `docs/design/notify/README.md`（NEW 域级入口，索引既有 inbox-patterns.md + notification-strategy.md，补全派发链/接收人解析/6 消费域+workflow/4 配置项/lifecycle/失败降级）；`module-boundaries.md §Owner Docs` 增 notify 行。注：异步总线 nop-message 经实仓核实为 Deferred，README 据实描述同步派发，未虚构。
- **产出**：plan 0930-3（Plan Status: completed，全 3 phase completed）/ `docs/analysis/governed-path-cost-evaluation.md`（NEW）/ `posting-exemptions.md`（+b2b）/ `data-dependency-matrix.md`（§5.6.2+§6.5）/ `docs/design/notify/README.md`（NEW）/ `module-boundaries.md`（+notify 行）/ 治理审查 F1/F3/F5 解决状态 + 闭包项 #1/#2/#7/#8 标 ✅。
- **验证（full-green verification）**：`mvn clean install -DskipTests` 全 154 模块 reactor BUILD SUCCESS（Total time 01:35 min，零生产代码回归）。
- **Skill**：`nop-backend-dev`（I*Biz/daoFor 机制 + 单模块测试启动验证）/ `nop-debugging`（根因依赖结构分析）。
- **独立结束审计**：ses_06f67b2b3ffeHgZuKMGSYmXqrF（fresh context）→ **CLOSURE AUDIT: PASS**（11/11 检查通过，含反虚构检查确认 notify README 未伪造异步基础设施）。



- **任务**：执行 `docs/plans/2026-07-24-0930-2-shared-dict-status-enum-unification.md`（治理审查 `2026-07-23-0000` §F2 HIGH + 闭包前必须项 #3（P0）/ #9（P2））。
- **Phase 0（Explore）**：已由前序会话完成（确认 8 份 per-domain approve-status.dict.yaml 无 ORM 消费者 + doc-status 7 域值矩阵相同 + drp 3 dict 归属裁决=登记例外）。
- **Phase 1（approve-status 字典去重 + javadoc 清理）**：移除 8 份字节级冗余 `approve-status.dict.yaml`（ast/cs/fin/mnt/mfg/pur/qa/sal；保留 inventory 5 处 ORM 引用 + cs time-entry-approve-status 1 处）；逐条清理 Phase 0 落盘的 13 条 stale 代码引用（javadoc/内联注释→`wf/approve-status`）。8 域 ORM 模型均无 `<dict name="erp-*/approve-status">` 定义，删除文件为非 ORM 生成产物，不会被 codegen 重新生成。
- **Phase 2（D1 共享接口全域推广）**：为 cs/mnt/mfg/qa 4 域创建 dao 层 `Erp*DocStatus` 接口 + `Erp*Constants extends`（消除 approve-status/doc-status 常量重复声明）。**事实修正**：`ErpMfgDocStatus` 仅承载 approve-status 轴——mfg 的 docStatus 列绑定域专属状态字典（work-order-status/issue-status/subcontract-status，均无 ACTIVE 值），纳入 DOC_STATUS_ACTIVE 会成死常量；cs/mnt/qa 含双轴（各有 erp-*/doc-status 字典）。D1 模式现覆盖 9 域（既有 pur/sal/fin/ast/inv + 新 cs/mnt/mfg/qa）。
- **Phase 3（drp 归属 + 文档对齐 + 基线同步）**：`docs/design/drp/README.md` 新增「命名例外登记」小节（3 dict 保留 erp-inv 命名空间，裁决理由：ORM 定义+物理文件+消费者全归属 module-drp，迁移触 ORM ext:dict 保护区域）；治理审查闭包项 #3 标 🔶 部分 Done（D1 全域+去重 done，共享 dict 统一 Deferred）/ #9 标 ✅ Done；`compliance-baseline.md` 追加 R3 同步注记（R3=19 不变，接口 extends 无 new Erp*()）。
- **产出**：plan 0930-2（Plan Status: completed，全 4 phase completed）/ 8 dict 删除 / 4 新 `Erp*DocStatus` + 4 `Erp*Constants` extends / `docs/design/drp/README.md`（命名例外）/ 治理审查 F2 解决状态 + #3/#9 标注 / `compliance-baseline.md` R3 注记。
- **验证（full-green verification）**：`mvn clean install -DskipTests` 全 154 模块 reactor BUILD SUCCESS；4 域 service（含 dao -am）`mvn compile` BUILD SUCCESS；4 域 `mvn test` Tests run: 95, Failures: 0, Errors: 0（cs/mnt/mfg/qa）；checker 全 16 规则 ≤ 基线（R2c=1108/R3=19/R11=0 等无回归）；既有 5 域 `Erp*DocStatus.` 引用在 23 实体文件在位。
- **Skill**：`nop-backend-dev`（dao 层常量接口 + Constants extends 模式）。
- **Deferred（已在 plan 登记）**：硬编码状态字面量→常量引用重构（126 "DRAFT"/73 "APPROVED" 等，逐文件低风险）/ ORM ext:dict 统一（inventory approve-status + doc-status 7 域合并候选，ask-first 保护区域）。
- **独立结束审计**：ses_06f82fc62ffez7wPSIGL5Rfpnj（fresh context，不复用执行者上下文）→ **PASS**（全 4 phase deliverable 实仓核验在位，mfg 事实修正判定 sound，无 anti-hollow / 无 silently-dropped scope）。


### 2026-07-24（F8+F9 守卫激活：compliance checker 接入 CI + 基线 + 19 web 测试解禁 — plan 0930-1）

- **任务**：执行 `docs/plans/2026-07-24-0930-1-compliance-guard-activation-ci-baseline.md`（治理审查 `2026-07-23-0000` F8 闭包项 #4 + F9 闭包项 #6）。激活既有但未发挥防护作用的 guard（dead armor → live guard），而非新建 wall。
- **Phase 1（F8 — checker CI 集成 + 基线落盘）**：
  - 实跑 `bash docs/audits/nop-compliance-checker.sh` 捕获精确基线（16 行可计数规则：R2c=1108, R3=19, R8=42, R10=51, R11=0 等，与 plan 记录一致）。
  - **Decision（方案 b）**：回归门控逻辑内嵌 CI workflow，checker 脚本保持纯报告工具（不侵入核心逻辑）。裁决依据：plan 明示"选不侵入 checker 核心逻辑的方案" + Draft Review iteration 2 注记倾向 b。
  - 新增 `.github/workflows/compliance.yml`：`compliance` job 跑 checker → 内嵌 python 解析汇总表 → 与基线 yaml 块逐规则比对（**单向收紧**：actual > baseline => FAIL）；`web-pages-validation` job 跑 app-erp-all ErpAllWebPagesTest。
  - 新增 `docs/audits/compliance-baseline.md`：16 行精确基线 + R9 定性说明 + R2c +143 delta 注记 + 门控规则 + 机器可读 ```yaml 块。
  - **Proof（anti-fake-green 三例）**：构造 R1a 0→1 违规 → exit 1（FAIL）；恢复 → exit 0；R3 19→18 改善 → exit 0。门控有效。
- **Phase 2（F9 — 19 web 测试解禁 + CI 页面校验）**：
  - 19 个 `Erp*WebPagesTest.java`：`@Disabled` → `@Tag("full-app")`（import `org.junit.jupiter.api.Tag`），消除系统性硬禁用。
  - 19 个 `erp-*-web/pom.xml`：`<plugins>` 增 maven-surefire-plugin `<excludedGroups>full-app</excludedGroups>`（模块级跳过，保留"仅全量 classpath 可运行"语义）。
  - **Decision（方案 a）**：CI 复用既有非 @Disabled 聚合测试 `ErpAllWebPagesTest`（PAGE_ERROR_COUNT=0），而非重跑 19 个域级 full-app 测试。19 域测试解禁仅为消除 suppression count。
- **产出**：plan 0930-1（Plan Status: completed，全 2 phase completed）/ 治理审查 audit `2026-07-23-0000` 闭包项 #4/#6 标 ✅ + 证据 / `.github/workflows/compliance.yml`（NEW）/ `docs/audits/compliance-baseline.md`（NEW）/ 19 测试 + 19 pom.xml 变更。
- **验证（full-green verification）**：`mvn install -DskipTests` 154 模块 BUILD SUCCESS（0 FAILURE/0 ERROR，增量 1:28 min）；模块级 `mvn test` 5 代表域（md/pur/sal/inv/mfg + fin）Tests run: 0（tag 排除生效，@Disabled=0 / @Tag=19 / excludedGroups=19）；`mvn test -pl app-erp-all -Dtest=ErpAllWebPagesTest` Tests run: 1, Failures: 0, Errors: 0（PAGE_ERROR_COUNT=0）；checker 复跑输出与基线逐行一致（无回归）；workflow YAML well-formed（`yaml.safe_load` OK）；19 pom.xml `xml.dom.minidom.parse` 全 OK；门控三例证明日志见会话。
- **Skill**：`nop-testing`（surefire tag/excludedGroups 配置 + CI 集成 + 测试行为验证）。
- **Deferred（已在 plan 登记）**：R8（42 Processor 无 xbiz）/ R10（51 REQUIRES_NEW）/ R1d（23 findAllByQuery）/ R3（19 new Erp*）命中项修复归后续专项计划（基线门控上线后触发）；ErpAllWebPagesCollectTest H-2 环境不稳定归 `docs/bugs/2026-07-20-2200` 登记条件。
