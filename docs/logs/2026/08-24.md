# Development Log - 2026-08-24

### integration-test mission：B8 VERIFY 复核（plan `docs/plans/2026-08-24-0749-1-b8-c15-c16-c17-crm-cs-hr.md`，full-green verification）

- **复核**：`mvn clean install -DskipTests` 156 模块 BUILD SUCCESS；全量单轮 `mvn test` BUILD SUCCESS（16:11）——surefire 权威计数 **3827/0/0/1 / 636 报告文件**（对照 B7 3824/0/0/1/633 差量 +3/+3 全额归因 B8，B8 执行期登记的 30 errors 全部消解归零）；app-erp-all 47/0/0/1。证据拷贝 `_tmp/b8-verify-surefire-evidence/`（636 XML）。
- **环境干扰处置一（本地 .m2 nop-batch WIP 构件污染）**：首轮全量 `mvn test` 于 `TestErpQaSpcSamplingEvaluateBatch` 稳定红（NCR_DATE 实际 2026-08-24 vs 期望冻结日 2026-07-17，独立复跑可再现，非 08-17 登记 SAMPLE_TIME 毫秒界 flake）。根因链：`nop-batch-core:2.0.0-SNAPSHOT` 等 6 构件于 09:39 被并发会话自 `nop-entropy-fix-ai-check` 工作树（含未提交 WIP）安装至本地仓库——其 `BatchTask.getExecutor(Executor)` 单参版**无条件** `GlobalExecutors.cachedThreadPool()`（master 源为双参版，`concurrency<=0` 走 `syncExecutor` 同线程；_BatchTaskModel javadoc 契约「未设 executor 时 SyncExecutor 当前线程串行」），spc-sampling.batch.xml（无 concurrency/executor）chunk 循环被移至池线程 `nop-cached-thread-pool-5-1`（surefire XML `nop.batch.run-chunk-loop:threadName` 实证），`ThreadLocalFrozenClock` 线程本地冻结失效 → `SpcOutOfControlHandler:102 CoreMetrics.today()` 取墙钟日期。修复 = 从 `nop-entropy-master` 工作树（已提交源码，零代码改动）`mvn install -f nop-batch/pom.xml -DskipTests` 重装 6 构件，字节码复核恢复双参 getExecutor，该类 4/4 绿。**登记教训：.m2 SNAPSHOT 构件可能来自兄弟工作树未提交 WIP，全量红时先核 jar 字节码与源树一致性。**
- **环境干扰处置二（并发 xwf starter 改造快照迁移收尾）**：B8 执行期登记的 30 errors（ast 5 类 9 / pur TestErpPurProcureToPayEnd 4 / sal 3 类 7 / hr 3 类 10，签名 = nop_wf_step_instance submit 行 ACTOR_MODEL_ID 空→starter）在本轮复核逐模块暴露并按快照维护义务补全迁移：`-Dnop.autotest.force-save-output=true` 按类重录（ast TestErpAstDisposal/DisposalEquipmentLinkage/CatchUpDepreciation/IdleStateMachine/PostingReverse 9 方法、pur TestErpPurProcureToPayEnd 4、sal TestErpSalOrderToCashEnd/ReceiptApproval/ReturnRefundEndToEnd 8、hr TestErpHrSalaryPostingChain/PayrollEngine/PayrollSimulation 27）+ CHECKING 复跑全绿；重录后 `*` 掩码回归审计（全量 diff 逐单元格比对 committed `*` → literal）发现 hr TestErpHrSalaryPostingChain 2 方法 erp_sys_notification PAYLOAD_JSON 手工 `*` 掩码（NopException seq 非确定）被 force-save 覆写为字面值 → `git checkout` 恢复该 2 文件（其余重录零掩码回归）。并发会话已重录部分（ast DisposalWorkflowApproval / hr SalaryWorkflowApproval / pur PaymentApprovalNotifications/PaymentWorkflowApproval/ReturnRefundEndToEnd / sal ReceiptWorkflowApproval）保持不动。
- **full-green verification**：B8 三用例 + 全 19 域既有测试全绿；执行期「30 errors 非归因 B8」结论经收尾后终局归零，B8 差量 +3/+3 维持全额归因。

### integration-test mission：B8 分批执行完成（C15/C16/C17，plan `docs/plans/2026-08-24-0749-1-b8-c15-c16-c17-crm-cs-hr.md`）

- **C15** `TestErpC15CrmLeadForecast`（CRM 线索转化与销售预测，11 步链）：自包含线索 `ErpCrmLead__save`（LEAD/NEW，客户 1）→ `qualify`（QUALIFIED + 默认 stage STAGE-001/probability 30）→ `moveStage`（STAGE-001→STAGE-002 stageId 翻转）→ **`convertToOpportunity` 原地升格**（leadType LEAD→OPPORTUNITY，docStatus 保持 QUALIFIED——**执行期勘误：实仓无独立 ErpCrmOpportunity 实体，设计文档旧动作名 `ErpCrmOpportunity__save` 漂移，B2 C03 裁决复认**）→ 自包含赢单阶段（id 9001 seq 30 isWonStage=true——seed 无 won 行）+ `moveStage` 前移 → **`convertToQuotation`**（**裁决：弃 `ErpCrmProductConfigurator__generateQuote` CPQ 路径（需配置器/定价规则全套建数），取 TestErpCrmLeadConversion 先例轻量路径**；报价 SQ-{leadId} + customerId 回链 + 商机弱指针 SALES_QUOTATION + CONVERTED）→ `ErpSalOrder__save`（**quotationId 回链断言**）+ 行（50×100=5000/650/5650）→ submit/approve [DIRECT]（B2 C03 先例链）→ `ErpCrmReport__renderHtml("forecast-accuracy")`（token **50,000.00/45,000.00/80,000.00/63,000.00——63000 = forecast_line 行加权合计（15000+48000，buildForecastAccuracyDataset 聚合），报表 numberFormat #,##0.00**）。
- **C16** `TestErpC16CsSlaNotification`（CS 工单 SLA 与通知派发，12 步链）：自包含 SLA 策略（类型精确匹配 resolveHours=8 日历模式）+ 宏响应（{customer_name}/{ticket_id} 变量）→ `ErpCsTicket__save`（**save 后置自动挂载 SLA：slaPolicyId+deadline 断言（D1 守卫）**；自动分配候选池空留 NEW）→ 显式 `matchAndAttachSla`（幂等重挂双证）→ `assign`（→ASSIGNED）→ `start`（→IN_PROGRESS + startDateTime）→ `applyCannedResponse`（usageCount 0→1 + 渲染正文含工单号）→ `resolve`（→RESOLVED + **isSlaCompleted = resolvedAt≤deadline 冻结时钟达标 true** + **自动创建调研（config-gated trigger=RESOLVED，token 随机经 DB 反查 addVar）**——**执行期勘误：无 respond 动作（B2 C04 裁决复认）**）→ `close`（→CLOSED 终态）→ `ErpCsSurvey__submitSurvey`（CSAT 5/NPS 9 与 seed 调研同值保持报表均值稳定——**勘误：旧动作名 ErpCsSurvey__save 漂移，实仓 = resolve 自动创建 + submitSurvey(surveyToken)**）→ `ErpCsReport__renderHtml("ticket-sla-csat-summary")`（投诉桶 token 5.00/9.00）→ 通知（**勘误：实仓工单链 notify 均模板缺失静默降级不落库 → C09 自包含模板先例** `ErpSysNotification__notify`→markRead→countUnread 归零）。
- **C17** `TestErpC17HrSalaryPayment`（HR 薪酬发放闭环，8 步链）：自包含核算环境（2026 税档 + seed emp1/emp2 合同 15000/8000 + 深圳社保/公积金基数与比例 + 6601.01/6601.02 科目——TestErpHrSalaryPostingChain.seedFullEnvironment 同型）→ `ErpHrSalary__calculateSalary`(emp1, 2026-7)（gross 15000/社保 EE 1200/公积金 EE 1800/个税 210/**net 11790** TestErpHrPayrollEngine 口径）→ `runPayroll`（全员批量：emp1 已存在幂等跳过 + emp2 新建——emp2 须同步建合同/社保前置）→ `submitForApproval`（salary-approval xwf 启动 nopFlowId）→ **xwf 三级 agree（hr-review→finance-review→manager-approval）+ cc-hr confirm**（IWorkflowManager 后端驱动 setUserId("0")——B1/B2 复核先例首次在 app-erp-all 全链实操）→ wf 回调 approve（APPROVED + **计提链 270 Dr 6601/Cr 2211=15000 / 290 6601.01/2211=2250 / 300 6601.02/2211=1800 三凭证 + posted=true**）→ `markPaid`（PAID + paymentDate=冻结日 + **280 SALARY_PAYMENT Dr 2211/Cr 1002 = 净额 11790 借贷平衡**——**执行期勘误：设计原文「Dr 费用/Cr 2211 = 薪资合计」为 270 计提口径误植（280 实仓 = 应付/银行 + 净额）；员工往来 ar_ap_item EMPLOYEE_ADVANCE 断言不成立（HR Provider 纯 GL 无辅助账）删除；markPaid 自动员工通知未实现 → C09 自包含模板先例**）→ 通知 markRead/countUnread 归零。**config 裁决：`erp-hr.default-payroll-subject-id=2211` @NopTestProperty（缺省空 → ERR_PAYROLL_SUBJECT_NOT_CONFIGURED G3 吞异常零凭证，B6 C12 同型）**。
- **执行期处置**：① 冻结时钟 `C15C16C17FrozenClockExtension`（2026-07-17，C13C14 先例）三用例共用；② 响应快照实钟毫秒 @var 共享漂移（approvedAt/duration/sentAt/createTime/updateTime 族跨字段共享 var 录制时同毫秒、回放差 1ms）→ 全量 `*` 通配（B4-B7 先例推广至 createTime/updateTime/sentAt 全族）；③ **并发会话干扰处置**：执行期检测到并发未提交 xwf starter 改造（payment/receipt/salary/asset-disposal approval 四 xwf 08:23 落盘 + hr/pur/ast 部分快照重录），致 ast 5 类/pur 1 类/sal 3 类/hr 3 类共 15 类 30 errors 同签名漂移（nop_wf_step_instance ACTOR_MODEL_ID=starter vs 旧快照 null）——非归因 B8（B8 仅动 app-erp-all 测试源），归并发改造快照迁移未完成；`mvn clean install` 重打包新 xwf 后 app-erp-all 侧 C01/C03/C17 同签名波及 3 处以 **ACTOR_MODEL_ID 列 `*` 通配**修复（双态兼容，不触碰并发会话生产改动）。
- **验证**：三用例 RECORDING→CHECKING 往返全绿（**联合复跑 3/0/0 连跑六次稳定**）；`mvn test -pl app-erp-all` **47/0/0/1**（44 基线 + 3，新 jars 下复跑两轮绿）；全 reactor 续扫 surefire XML 聚合 **3827/0/30/1/636**（对照 B7 3824/0/0/1/633 差量 **+3 tests/+3 报告文件全额归因 B8**，30 errors 全量归因并发 xwf 改造——详见 known-good-baselines 2026-08-24 B8 行；证据拷贝 `_tmp/b8-surefire-evidence/`）；`mvn clean install -DskipTests` 156 模块 BUILD SUCCESS。设计文档 §6 C15/C16/C17 勘误登记 + roadmap B8 done + 基线差量登记。

### integration-test mission：B7 VERIFY 复核（plan `docs/plans/2026-08-24-0541-2-b7-c13-c14-finance.md`，full-green verification）

- **复核**：`mvn clean install -DskipTests` 156 模块 BUILD SUCCESS；全量 `mvn test` BUILD SUCCESS——surefire 权威计数 **3824/0/0/1 / 633 报告文件**（与 known-good-baselines B7 基线完全一致），app-erp-all **44/0/0/1**（含 C13/C14 各 1/0/0）。零新增失败，基线无漂移。

### integration-test mission：B7 分批执行完成（C13/C14，plan `docs/plans/2026-08-24-0541-2-b7-c13-c14-finance.md`）

- **C13** `TestErpC13FinPeriodCloseReverse`（期末结账全链与反结账，6 步链）：自包含 USD OPEN 应收项（`ErpFinArApItem__save`，IT-C13-FX-001，100 USD / openFunctional 1000，驱动汇兑重估）→ `ErpFinAccountingPeriod__preCheck`(periodId=1)（未核销 AR/AP 列表 = seed OPEN 往来项 ARAP-EA-001/ARAP-EC-001 + 自包含外币项；另列出 seed 折旧计划未过账悬挂键 depreciation:2#2026-07）→ `closePeriod`（CLOSED + 模块 AR/AP/INV/AST/GL 全 CLOSED + 汇兑重估凭证 FX-REVAL-2026-07 Dr 6603 150/Cr 1122 150 + 损益结转凭证 PERIOD-CLOSE-2026-07 Dr 5001 1130/Cr 4103 1130）→ `finalizePeriod`（CLOSED_FINAL）→ `reverseClose`（OPEN + 模块回开 + 原结转/重估凭证红冲 isReversed=true + REV-* 红字回链）→ `closePeriod` 重新结账（新结转/重估凭证，链接 ≥2，金额与首轮一致）。**执行期裁决：① preCheck 返回结构 = PeriodPreCheckReport 属性 getter（GraphQL 序列化不含 hasXxx()/issueCount() 派生方法）；② closePeriod 同事务内汇兑重估凭证经 CloseVoucherWriter 直接 save 未 flush → 损益结转聚合（DB 直查）见不到 FX 凭证 → PERIOD_CLOSE 不含 FX 腿（合计 1130 非 1280，对比 TestErpFinProfitLossClosingIncludesFxGainLoss 先 seed 后 close 跨 session 场景，行为差异以实仓为准——设计文档 §6 C13 勘误登记）；③ 反结账→重新结账幂等 = 生成新凭证（红字凭证自身 isReversed=true 被聚合过滤天然排除）；④ 冻结时钟采用 C13C14FrozenClockExtension（2026-07-17 ∈ 2026-07 期间，C07/C11/C12 先例）+ 时间戳列 `*` 通配（B4/B6 先例）。科目 config 9 键 @NopTestProperty（auto-post-on-close=true 必配——seed 折旧计划悬挂致 hasIssues=true 否则 ERR_PRE_CHECK_BLOCKED；bad-debt-allowance-gate-enabled=false 必配——seed ARAP-EA-001 1000×0.005=5 必需 > 账面 0 致 shortfall 阻断；reverse-close-approval-required=false 必配；4103/6603/1122/2202/8.5 对齐 07-25 基线 E2E JVM args + fin-service 期间关账 test yaml 同型）**。
- **C14** `TestErpC14FinBankReconBadDebt`（银行对账与坏账计提回收，13 步链）：自包含资金账户（`ErpFinFundAccount__save`，BANK + 科目 1002，currentBalance=1000）→ 对账单导入（`ErpFinBankStatement__importStatement`，2026-07-31，3 行 CREDIT 300/200/100 未达，endingBalance=1600）→ `ErpFinBankReconciliation__generate`（恒等式 1600−1000=600=未达净值 → 平衡 DRAFT）→ `post`（BANK_RECON_ADJ 凭证 Dr 1002 600/Cr 2240OTHER 600 POSTED）→ `reverse`（CANCELLED + 原凭证 isReversed + 红字回链）→ 自包含 OPEN AR 项（IT-C14-AR-001，应收 1000，dueDate 61-90 账龄档）→ `ErpFinBadDebt__writeOff`（UNSUBMITTED）→ `submit`（SUBMITTED）→ `approve` [DIRECT]（APPROVED + 项 WRITTEN_OFF openAmount→0 + BAD_DEBT_WRITE_OFF 凭证 Dr 1231/Cr 1122 1000）→ `recover` → submit → approve（项回退 OPEN + BAD_DEBT_RECOVERY 凭证 Dr 1122/Cr 1231 1000）→ `runBadDebtProvision`(periodId=1)（计提 = seed EA 1000×0.005 + 自包含 1000×0.05 = 55 → RESERVE 凭证 Dr 6701 55/Cr 1231 55）。**执行期裁决：① 科目 config = 未达对方科目缺省键 2240OTHER（seed 在位）+ 坏账三键 @NopTestProperty（ar-subject-code=1122 / bad-debt-allowance-subject-code=1231 / bad-debt-expense-subject-code=6701，有键无 DEFAULT_*）；② 计提金额口径 = 实仓 BadDebtProvisionCalculator（账龄基准 dueDate，asOf=期间 endDate，计提范围=全应收未核销项无 org 过滤）；③ 数据来源 = 自包含建数（seed 无 fund account/bank statement/bad debt 行，未触发 seed 修正授权）；autoMatch 不调用（设计文档 C14 步骤不含，seed 账面行与导入行金额不匹配）**。
- **执行期处置**：C13/C14 响应快照跨 run 时间戳 @var 索引/millis 合并不稳定（updateTime_N/createTime_N/voucherDate_N/code_N 交叉引用）→ 响应快照不稳定单元格 `*` 通配（B4/B5/B6 通配先例；tables 变更行 @var 索引保持——C13 双跑实证稳定）。
- **验证**：两用例 RECORDING→CHECKING 往返全绿（联合复跑 2/0/0 连跑两次稳定）；`mvn test -pl app-erp-all` **44/0/0/1**（42 基线 + 2）；全量 `mvn test` **3824/0/0/1 / 633 报告文件**（对照 B6 3822/0/0/1/631 差量 +2 tests / +2 报告文件全额归因 B7 两用例类（各 1 类 1 方法），零新增失败；surefire 证据已拷贝 `_tmp/b7-surefire-evidence/`）；`mvn clean install -DskipTests` 156 模块 BUILD SUCCESS。
- **落盘**：roadmap B7 todo→done + 头「最后更新」注记；known-good-baselines 差量登记（2026-08-24 B7 行）；设计文档 §6 C13/C14 实施期勘误登记；本日志。

### integration-test mission：B6 VERIFY 复核（plan `docs/plans/2026-08-24-0541-1-b6-c11-c12-projects-assets.md`，full-green verification）

- **复核**：`mvn clean install -DskipTests` 156 模块 BUILD SUCCESS；全量 `mvn test` BUILD SUCCESS——surefire 权威计数 **3822/0/0/1 / 631 报告文件**（与 known-good-baselines B6 基线完全一致），app-erp-all **42/0/0/1**（含 C11/C12 各 1/0/0）。零新增失败，基线无漂移。

### integration-test mission：B6 分批执行完成（C11/C12，plan `docs/plans/2026-08-24-0541-1-b6-c11-c12-projects-assets.md`）

- **C11** `TestErpC11PrjCipCapitalizeMnt`（项目成本归集→CIP→资产资本化→维护设备，7 步链）：自包含成本归集单 save（引用 seed 项目 PRJ-2026-001，20000）→ 自包含资产类别（直线法 36 月 + 科目 1601/在建工程科目 1603）→ CIP save（projectId=1 弱引用关联 seed 项目）→ startConstruction（DRAFT→IN_CONSTRUCTION）→ addCostItem（PURCHASE 20000，sourceBillType=COST_COLLECTION 回链）→ **transferToAsset（完工转固：内部复用资本化审批链 submit→approve 建卡 + CAPITALIZATION 凭证——非独立 Capitalization save/submit/approve 步骤）** → 资产卡 IN_SERVICE + 原值=20000=CIP 累计成本 + 折旧计划 36 期 + CAPITALIZATION 凭证 Dr 1601/Cr 1603=20000 → 设备卡 save（assetId 跨域引用新资产 + 状态 RUNNING）。**执行期裁决：① CIP 转固动作面 = transferToAsset 内建资本化链（设计文档 §6 C11 步骤 2/3 原述 save→完工审批→Capitalization save→submit→approve 为漂移，勘误登记）；② 资本化科目 = 类别 subjectId/cipSubjectId（回退标准码 1601/1603）+ 折旧计划随建卡生成；③ 数据来源 = 自包含建数（seed 类别无科目且无 seed CIP，未触发 seed 修正授权）**。
- **C12** `TestErpC12PrjTimesheetSettlement`（项目工时过账与结算损益，12 步链）：自包含项目类型（defaultSubjectId=5101）+ 项目 + 任务（IN_PROGRESS）→ 工时单 save（**引用 md 域员工 EMP-001 id=1——工时 userId 实仓关系 = ErpMdEmployee 非 hr 域员工，设计文档旧式码勘误**）→ submit（costAmount=8×500=4000）→ approve（**@NopTestProperty 开启 erp-prj.default-payroll-subject-id=2211** → PROJECT_COST_COLLECTION 凭证 Dr 5101/Cr 2211=4000 + 归集行 LABOR + actualCost 回写）→ 结算 CLOSE（seed 项目 PRJ-2026-001）createSettlement → submit → approve（转固建卡 IN_SERVICE 原值=30000 + PROJECT_SETTLEMENT 凭证 Dr 1601/Cr 1603=30000）→ 损益结转 = FINAL 结算 createSettlement → submit → approve（PROJECT_SETTLEMENT 凭证 Dr 5101=30000/Dr 4103=20000/Cr 6001=50000）→ seed PNL 行 reload 原值不变（零改写零消费断言）。**执行期裁决：① 工时借记科目 = 项目类型 defaultSubjectId（seed 项目类型 PRJ-TYPE-IT 无 defaultSubjectId → 工时单落自包含项目，未触发 seed 修正授权）；② 结算动作 = createSettlement/submit/approve（无 GraphQL submitForApproval）；③ 损益结转 = FINAL 结算 approve（非独立动作）；④ seed PNL 行交互 = 不调用 refreshPnl → 快照行零改写（断言锚点 = 结算单 final* 派生 + 凭证金额 + seed 行原值）——设计文档 §6 C12 勘误登记**。
- **执行期处置**：C09（B5 预存 flake 本次全量回归复现——`3_p_incoming_move_response.json5` createTime 与 ErpInvStockMoveLine@createTime var 跨实体 ms 合并）+ C11/C12 响应快照跨 run 时间戳 @var 索引/millis 合并不稳定（含 var 未注册 case：`ErpPrjProjectSettlement@updateTime_5` varValue=null）→ 三用例 output 响应/变更行时间戳 @var 引用 `*` 通配共 78 处（B4/B5 通配先例；非时间戳 ref 如 code_N/delVersion_N 保持——C11/C12 已实证稳定）。
- **验证**：两用例 RECORDING→CHECKING 往返全绿（联合复跑 2/0/0 连跑两次稳定）；`mvn test -pl app-erp-all` **42/0/0/1**（40 基线 + 2）；全量 `mvn test` **3822/0/0/1 / 631 报告文件**（对照 B5 3820/0/0/1/629 差量 +2 tests / +2 报告文件全额归因 B6 两用例类，零新增失败；surefire 证据已拷贝 `_tmp/b6-surefire-evidence/`）；`mvn clean install -DskipTests` 156 模块 BUILD SUCCESS。
- **落盘**：roadmap B6 todo→done + 头「最后更新」注记；known-good-baselines 差量登记（2026-08-24 B6 行）；设计文档 §6 C11/C12 实施期勘误登记；本日志。

### integration-test mission：B5 分批执行完成（C09/C10，plan `docs/plans/2026-08-24-0159-2-b5-c09-c10-quality-maintenance.md`）

- **C09** `TestErpC09QaNcrCapaScrap`（质检门控与 NCR/CAPA/SCRAP 闭环，27 步链）：自包含产成品 P（**MOVING_AVERAGE——实施期发现：FIFO 余额 avgCost 恒 null 致 SCRAP 凭证金额 0**）+ BOM inspectionRequired=true（无子件行，齐套校验空需求即 STOCK_RESERVED）→ P 入库 10@5 → 工单 DIRECT 审批 → start → reportCompletion(1) 被完工门控阻断 `erp.err.mfg.work-order.inspection-required`（BOM 门控）→ close 结案放行（证明阻断点 = reportCompletion 非 close）→ 质检单 PENDING → recordResult REJECTED（实测 5 < 规格下限 10；自动生成 NCR sourceType=INSPECTION quantity=lotQuantity=2；isInspectionCleared=false）→ NCR submitReview → resolve 门控双侧（no-capa / capa-not-completed×2）→ CAPA 三步 startAction/completeAction/verifyAction → RESOLVED → dispositionType=SCRAP → postNcr（NCR_SCRAP 凭证 Dr 6711/Cr 1401 = 2×5=10 posted）→ notify 域可达断言路径（自包含模板 + `ErpSysNotification__notify` → markRead → countUnread 归零）。**执行期裁决：① 完工门控阻断点 = reportCompletion（设计文档 §6 C09 原述 close 为漂移）；② NCR→notification 实仓无自动触发机制 → notify 域既有机制作可达断言路径（设计文档 §6 C09 步骤 4 勘误）；③ SCRAP 不扣物理库存（NcrPostingDispatcher 设计语义，断言替换为凭证借贷平衡 + quantity×avgCost）——设计文档 §6 C09 勘误登记**。
- **C10** `TestErpC10MntRequestSparePart`（维护工单与备件消耗过账，14 步链）：自包含请求 save（设备 EQ-2026-001）→ 非法迁移守卫（OPEN startRepair 拒 `erp.err.mnt.request-illegal-status-transition`）→ accept（DRAFT RESPONSIVE Visit + requestId 回填）→ startRepair → complete（六态终态）→ visit schedule/start/complete（设备 UNDER_MAINTENANCE↔RUNNING）→ 自包含备件物料 M + 入库 10@5 → 消耗单 confirm（spare-part-posting-enabled 门控开启 → OUTGOING + MAINTENANCE_ISSUE 凭证 Dr 6602/Cr 1403=10 + 余额 10→8）→ `ErpAstAsset__get`（AST-2026-002 核对）。**执行期裁决：① 六态状态机（设计文档 §6 C10「5 态」勘误）；② visit_task 仅 PLANNED 访问（RESPONSIVE 无任务，勘误）；③ 备件库存自包含建数（seed MAT-003 余额无 location 与出库路径不匹配，B4 C07 同型裁决）——设计文档 §6 C10 勘误登记**。
- **执行期处置**：响应快照跨 run 时间戳 @var 索引/millis 合并不稳定（updateTime_N/createTime_N 及交叉引用）→ `*` 通配（B4 drp/logistics 通配先例；非时间戳索引 ref 如 code_N/delVersion_N 保持——C07 全量回归实证稳定）。
- **验证**：两用例 RECORDING→CHECKING 往返全绿（联合复跑 2/0/0）；`mvn test -pl app-erp-all` **40/0/0/1**；全量 `mvn test` **3820/0/0/1 / 629 报告文件**（对照 B4 3818/0/0/1/627 差量 +2 tests / +2 报告文件全额归因 B5，零新增失败；surefire 证据已拷贝 `_tmp/b5-surefire-evidence/`）；`mvn clean install -DskipTests` 156 模块 BUILD SUCCESS。
- **落盘**：roadmap B5 todo→done + 头「最后更新」注记；known-good-baselines 差量登记（2026-08-24 B5 行）；设计文档 §6 C09/C10 实施期勘误登记；本日志。

### integration-test mission：B4 分批执行完成（C07/C08/C21，plan `docs/plans/2026-08-24-0159-1-b4-c07-c08-c21-mfg-aps.md`）

- **C07** `TestErpC07MfgWorkOrderLifecycle`（制造工单全生命周期与完工过账，19 步链）：自包含 FIFO 产成品 P + MOVING_AVERAGE 原料 M1 → BOM 1P=2M1 → M1 入库 10@5 → 工单 planned=1 DIRECT 审批 → checkAvailability→start → 领料 confirm（MANUFACTURING_ISSUE Dr 1411/Cr 1401=10）→ FIRMED 标准成本 8/unit → reportCompletion(1)（完工入库 + MANUFACTURING_RECEIPT Dr 1401/Cr 1411=10 + PRODUCTION_VARIANCE Dr 1410/Cr 1411=2）→ findPage（MATERIAL_USAGE 差异 +2 = actual−standard）。**执行期裁决：完工驱动动作 = reportCompletion（非设计文档 §6 C07 原述 close，close 为 STOPPED/IN_PROCESS→CLOSED 结案语义）；领料出库 = 独立 confirm 步骤；数据来源 = 自包含建数（seed MAT-003 WEIGHTED_AVERAGE 余额无 location 与出库路径不匹配）——设计文档 §6 C07 勘误登记**。
- **C08** `TestErpC08MrpApsRelease`（MRP→APS→工单释放，18 步链）：自包含 P/M1 + BOM 1:1 + M1 onHand=4 → MRP 计划 + 手工需求 → runSimulation（simulation-enabled 门控）→ promoteToFormalPlan → 行级释放（WO-MRP-xxx planned 10 / PO-MRP-xxx qty 6）→ 净需求 10=10−0−0 / 6=10−4−0 → 排程 PUBLISHED + 工序订单 DRAFT→PLANNED→IN_PROGRESS→FINISHED。**执行期裁决：释放动作 = 行级 release*（非 ErpMfgMrpPlan__release）；APS 衔接 = 自包含建排程（释放工单 DRAFT 无 routing）——设计文档 §6 C08 勘误登记**。
- **C21** `TestErpC21ApsCapacityLoad`（APS 排程发布与产能负荷，4 步链）：自包含工序订单（WC-001 排程候选）→ 排程 publish（PUBLISHED）→ crp-load-report HTML token 断言（WC-001/4.00/8.00/0.50，seed crp_load 静态行）→ 静态行不被重算覆盖（CrpRunJob 默认不调度）。与设计一致，无勘误。
- **执行期处置**：C07/C08/C21 响应快照时间戳 @var 索引（updateTime_N/createTime_N）跨 run 不稳定（millis 合并）→ 变更行/响应快照时间戳列 `*` 通配（B2 drp/logistics 日期漂移通配先例）。
- **验证**：三用例 RECORDING→CHECKING 往返全绿；全量 `mvn test` **3818/0/0/1 / 627 报告文件**（对照 3815/0/0/1/624 差量 +3 tests / +3 报告文件全额归因 B4，零新增失败）；`mvn test -pl app-erp-all` **38/0/0/1**；`mvn clean install -DskipTests` 156 模块 BUILD SUCCESS。
- **落盘**：roadmap B4 todo→done + 头「最后更新」注记；known-good-baselines 差量登记（2026-08-24 B4 行）；设计文档 §6 C07/C08 实施期勘误登记；本日志。

### integration-test mission：B3 VERIFY 复跑全绿（mission driver 2026-08-23-183510）

- **执行**：mission driver VERIFY 步复跑 mission 命令：`mvn clean install -DskipTests` 156 模块 BUILD SUCCESS + 全量 `mvn test` BUILD SUCCESS，surefire XML 权威计数 **3815/0/0/1 / 624 报告文件**——与 known-good-baselines B3 基线（3815/0/0/1/624）零漂移；app-erp-all 35/0/0/1（含新用例类 `TestErpC05InvLandedCost` / `TestErpC06InvCostFlowCogs` 各 1/0/0 CHECKING）；aps-service 76/0/0（时间窗 flake 修复后维持）；C05/C06 快照 CHECKING 侧复跑全绿，full-green verification。