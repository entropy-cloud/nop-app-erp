# Development Log - 2026-08-20

### 2026-08-20

- **VERIFY 全仓复核：plan `2026-08-19-2040-2`（RC-R1.81/82）mission VERIFY 全量命令全绿（full-green verification：build + 全 reactor test + checker 零漂移）**：`mvn clean install -DskipTests` 156 模块 BUILD SUCCESS（1:42）+ 全仓 `mvn test` **BUILD SUCCESS 3716 tests / 0 failures / 0 errors / 1 skipped**（00:55-01:07，surefire XML 权威计数 604 文件；唯一 skip = 已知 `@Disabled ErpAllWebPagesCollectTest`；含 app-erp-all 页面校验组）+ `bash docs/audits/nop-compliance-checker.sh` 19 规则 **actual==baseline 零漂移 EXIT=0**（R1d=14 / R2a=34 / R2b=236 / **R2c=1483 / R2d=37** / R3=5 / R6=2 / R10=12 / R12a=70 / R12b=66 / R12c=40 = compliance-baseline.md BASELINE 机器可读块逐行一致，含本计划 R2c 1469→1483 / R2d 35→37 baseline-raise 登记后口径）。3716/0/0/1 与执行者/结束审计 XML 权威计数逐字一致（drp 97 + pur 328 零回归 + 新增 TestErpDrpCrossDock 14 / TestErpDrpLeadTimeStats 13 / TestErpAllJobYamlLoading 计数 29→30），零回归零失败。

- **plan `2026-08-19-2040-2`（RC-R1.81/82，P1-RC-081/082，UC-DRP-07/08）drp 越界修复族（越库状态机 + 提前期统计与供应商评分）执行完毕（full-green verification：drp 97/0/0 + pur 328/0/0 零回归 + 全仓 install 156 模块 + 全 reactor mvn test 3716/0/0/1 surefire XML 权威计数 + checker R2c/R2d baseline-raise 其余零漂移）**：**Phase 1（RC-R1.81 越库执行引擎，A 类 matchingStrategy 列 + B 类 BizModel/config）**——`ErpInvDrpCrossDock.matchingStrategy` propId 25 + dict `erp-inv/drp-xdock-strategy`（双独立子 agent 批准 ses_fe594ddacffeDgXos4Fwyx3qH1 + ses_fe594aef4ffeAIiCp7DJ7ARQYT 落盘 plan §ORM Approvals；本 run 接续中断执行现场：ORM/生成链/Processor/Job/TestErpDrpCrossDock 已在前次 run 落地，本 run 验证 14/0/0 全绿 + D1/D2 裁决落盘 + matrix/owner doc 登记）；`ErpInvDrpCrossDockProcessor` 状态机 mutation 族（receiveMark→STAGING / match 三策略[PRE_ALLOCATED 读预分配 + ON_RECEIPT 扫描待出库销售订单承诺发货日期 ASC + 创建时间 ASC + MANUAL 显式]→MATCHED 含直连 PENDING→MATCHED / load 生成出站移动[IErpInvStockMoveBiz.generateMove OUTGOING 弱指针 DRP_XDOCK 回链 business-linked DONE]→LOADED / complete→COMPLETED / cancel→CANCELLED；`erp-inv.drp-xdock-enabled` 默认 false 总门控）+ `ErpDrpCrossDockStagingTimeoutJob` 超时回退（`erp-drp-xdock-staging-timeout.job.yaml` 29→30 + TestErpAllJobYamlLoading 计数同步；R1.38 范式 cron 空值跳过 + SCAN_LIMIT 200 + 逐条失败隔离；staging→正常存储位 INTERNAL 移动 billType DRP_XDOCK_PUTAWAY + CANCELLED）+ D2 裁决选项 A 质检守卫（有效检验模板载体 + 快检凭证 ACCEPTED/CONDITIONAL + `erp-inv.drp-xdock-quality-gate-enabled` 默认 false）+ D1 裁决选项 A purchase 接线 `markReceivedFromPurchase`（pur-service→drp-dao @Nullable 容错 + 失败隔离 + 仅 PENDING 可迁移幂等）；TestErpDrpCrossDock **14 @Test**。**Phase 2（RC-R1.82 提前期统计与供应商评分，A 类评分汇总实体 + B 类统计/联合变分）**——`ErpInvDrpSupplierScore` 评分汇总实体（28 列 + UK(supplierId,materialId) + dict `erp-inv/drp-supplier-grade`）+ dict `erp-inv/drp-lt-flag` 物化（string 码值，10/20/30 int 时代遗留注记）+ `IErpInvDrpLeadTimeRecordBiz.recordFromPurchaseReceive` D4 裁决选项 A 收货审批后置 Facade（actualLeadTime=DATEDIFF(receiptDate,orderDate) + expected=deliveryDate−businessDate + 容差 0.1 闭区间三档 + expected 缺失 earlyLateFlag 留空不入准时率分母 + 幂等 purchaseOrderCode+materialId + dates-invalid 守卫[L1「跳过告警」]）+ `ErpInvDrpLeadTimeProcessor` 统计评分（`findLeadTimeStats` 三级粒度[供应商/供应商+物料/物料] μ/σ/中位数/极值/准时率/变异系数 + 窗口 `erp-inv.drp-lt-stats-window-days` 默认 365 Java 侧裁剪[biz 管道过滤操作集不保证 ge，对齐 CrossDockProcessor ne 先例]；`recalculateLeadTimeStats` 四维 40/30/20/10[数量准确率 drp→pur 读 APPROVED 单行 receivedQuantity 偏差 + 质量合格率 drp→qa 读 INCOMING 合格率 ACCEPTED/CONDITIONAL 口径与越库快检一致] + 等级 A/B/C/D 阈值 90/75/60 闭区间 + 无样本维度得分 0 + missingDimensions=QUANTITY,QUALITY 标注不静默忽略 + UK upsert 幂等）+ D5 裁决选项 A SafetyStockEngine 联合变分（`leadTimeSample` preferredSupplierId 解析 + 样本≥5 μ_lt 替换配置提前期[L1「样本<5 降级」] + cv>0.2 走 `Z×√(σ_d²×μ_lt+μ_d²×σ_lt²)`，中/高变异档统一联合值——高档「额外缓冲 30~80%」无 L1/owner doc 量化依据显式简化为联合变异值）+ `confirmWriteback` 增补 replenishmentLeadTime←μ_lt 回写建议（人工审查门 `erp-inv.drp-ss-auto-writeback` 默认 false 不变）；TestErpDrpLeadTimeStats **13 @Test**（含 90.00/75.00/60.00 等级边界精确构造 + 联合公式 inside=61600 精确数值 + cv=1/3 稳定性 20.00 构造）；**执行期事实修正**：isOnTime 列 DDL 默认 true 致 null 不可表示 → 已判定标记改 earlyLateFlag 非空（owner doc 实现注记登记口径精确化）。**越界矩阵登记**：pur-service→drp-dao（D1+D4）+ drp-service→sal-dao（ON_RECEIPT）+ drp-service→qa-dao（D2+质量维度）+ drp-service→pur-dao（数量维度，与 pur→drp 构成 Maven 菱形非环显式披露，对齐 assets↔mnt 先例）四边落 matrix §2.4。**验证收口**——erp-drp-service **97/0/0**（84 基线 + 13 LeadTimeStats，14 类全绿，surefire XML 权威计数）+ erp-pur-service **328/0/0** 零回归 + 全仓 `mvn clean install -DskipTests` 156 模块 BUILD SUCCESS + 全 reactor `mvn test` **BUILD SUCCESS 3716/0/0/1**（surefire XML 权威计数 604 文件；console 模块汇总口径 3731 为报告口径差异；唯一 skip = 已知 @Disabled ErpAllWebPagesCollectTest）+ checker **R2c 1469→1483（+14）/ R2d 35→37（+2）baseline-raise**（per-site 证据 = CrossDockProcessor 4 + TimeoutJob 5 + LeadTimeProcessor 2 + SafetyStockEngine 1 + PurReceiveProcessor 2 处 daoFor，全部 load-by-id/同域聚合非 chained Type 1 候选，compliance-baseline.md BASELINE 块 + 注记）其余 17 规则零漂移（R2b=236 持平——跨域消费全经 I*Biz 注入）；**附带修复（本计划范围外 CI-red 阻断，测试侧确定性修复）**：mnt `TestErpMntDowntimeAndE2E#testResponsiveRequestFullFlow` 日期翻转脆弱点（快照 VISIT_DATE=2026-08-19 硬编码 vs accept 生成 visitDate=CoreMetrics.currentDate() 随自然日滚动）→ 方法级线程本地冻结时钟（MntFrozenClockExtension.installFrozenClock try/finally；类级冻结否决——downtime 用例混用 currentDateTime[可冻结]与 currentTimeMillis[恒真实]会撕裂 totalMinutes 口径，实测 49096 min 证实）+ 快照 VISIT_DATE 重锚 2026-07-17；owner doc 回填 cross-dock.md §实现注记（D1/D2 裁决 + matchingStrategy 三策略 + 超时 job + 配置点实现口径七键表）+ lead-time-tracking.md §实现注记（D4/D5 裁决 + 字典容差口径 + 统计评分口径 + 高档简化）+ safety-stock-optimization.md §联合变分集成注记（σ_lt 来源/样本门槛/μ_lt 替换/分档简化声明/回写建议）+ arm-index P1-RC-081/082 → done (RC-R1.81/82) + P2-RC-072 联合变分分量闭合注记（残余 20% 告警维持 watch-only）+ roadmap 两行 done A+B 类混合改写 + 本日志条目；use-cases.md L1 不动；Deferred：月台预约调度引擎 / ASN×b2b 自动识别 crossDockFlag / 评分等级联动审批策略 / leadTimeStdDev 物化列（plan §Deferred But Adjudicated 四项维持）。
