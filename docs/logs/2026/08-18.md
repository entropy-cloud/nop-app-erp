# Development Log - 2026-08-18

### 2026-08-18

- **VERIFY 全绿（full-green verification）——plan `2026-08-17-2125-2`（RC-R1.66 cs 计时器 session）mission requirement-compliance 验证通过**：全仓 `mvn clean install -DskipTests` **BUILD SUCCESS**（156 模块，01:49）+ 全仓 `mvn test` **BUILD SUCCESS 0 failures / 0 errors**（首跑模块 1-16 全绿[common-service 10 tests] + `-rf :app-erp-master-data-service` 恢复跑 **3558 tests / 0 failures / 0 errors / 1 skipped**[ErpAllWebPagesCollectTest 既有 @Disabled]，覆盖模块 17-156，含 `TestErpCsTicketTimerSession` 7/0/0 与 erp-cs-service 137/0/0 零回归）+ `bash docs/audits/nop-compliance-checker.sh` **exit 0**（全 19 规则 actual ≤ updated baseline：R2c 1434→1439 +5 / R10 10→11 +1 本计划 baseline-raise 带 per-site 证据，其余 17 规则零漂移）。**首跑偶发注记**：全仓首跑 `TestErpMdSkuServices` 9 errors（`nop.err.graphql.unknown-operation ErpMdMaterialSku__findDefaultSku`，module-master-data 本计划零触及、末次变更 2026-08-15）——nop-entropy 父 POM surefire `forkCount=4 + parallel=classes` 跨类并行干扰疑似（同 run 内同操作 `TestErpMdSkuStatusConstraints` 绿 + 模块独立复跑 146/0/0 全绿 + `-rf` 恢复跑再现绿，三重证据非确定性）；暂不立 bugs 条目，复现二次再立档查干扰对。

- **cs 计时器 session 子系统落地（RC-R1.66，plan `2026-08-17-2125-2`，P1-RC-055 UC-CS-11 ①-⑨，2026-08-12 A 类批量裁决预授权 + 双独立子 agent fresh session 批准 `ses_fecdd7bb8ffegfJP40qaobK33b`/`ses_fecdd5153ffeR4c7aYJyarkyHq` 双 APPROVE）**：执行 Phase 2（ORM 落地 + 实现）+ Phase 3（测试 + 文档回填）。
- **ORM**：`app-erp-cs.orm.xml` 新增 `ErpCsTicketTimerSession`（17 列 propId 1-17：agentId[stdDomain=userId VARCHAR(36)]/ticketId/startTime/stopTime/pauseStartDateTime/cumulativePauseMinutes/pauseReason/status[dict erp-cs/timer-session-status RUNNING/PAUSED/STOPPED]/activeFlag + 审计五件套；UK_CS_TIMER_SESSION_AGENT_ACTIVE(agentId,activeFlag) + 2 索引）+ dict 三态；`cd module-cs && mvn clean install -DskipTests` 增量重生成（**执行注记：`-pl module-cs` 仅构建聚合 POM 不含子模块，域内重生成须以 module-cs 为构建根**）；产物核对（dao 实体/_gen/IBiz + meta xmeta 双层 + dict.yaml + web view 双层 + api 三件 + service BizModel/xbiz + DDL 三方言）。
- **实现（D1-D6 落地）**：`TimerSessionCalculator` 纯函数（isOpen/totalPauseMinutes/elapsedActiveMinutes/isOverdue/cappedStopTime/toEntryAgentId[数字直写/非数字 0 哨兵]）+ `ErpCsTicketTimerSessionOps` 共享步骤 bean（settleIfOverdue 当前事务 + **settleIfOverdueInNewTx REQUIRES_NEW 物化**——pause/resume 拒绝路径外层异常回滚与 findActiveTimer 只读会话不吞 12h 封顶结算，执行期发现同事务结算随拒绝回滚后修正）+ 四 per-mutation Processor（Start[守卫复查 DB 状态 + 超时旧会话先结算释放槽位]/Pause/Resume/Stop[结算先行幂等返回]）+ BizModel Facade（findActiveTimer 读取惰性结算）+ `ErpCsTimeEntryBizModel` submit/approve/reject（NULL=DRAFT + require-description 门控 + isBillable‖threshold→PENDING + auto-approve 直通 + 驳回原因 description 前缀 + 审批人链 assignedToId→teamLeaderId→config 兜底 advisory）+ `ErpCsTicketBizModel` 三聚合 @BizQuery（经 IErpCsTimeEntryBiz 注入 R2b 合规）+ config 七键（六键对齐 §七 + approver-id）+ 错误码 8 个（already-active/disabled/description-required/illegal-state 等）。
- **D1 方言验证假设执行期修正**：H2 init schema（与 deploy DDL 同生成链）仅物化 PK 不物化 ORM unique-keys（全实体既有特征）——「dao 直插断言 DB UK 拒绝」不可达；运行时强制机制 = mutation 守卫复查 DB 状态（测试覆盖直插行后 startTimer 仍拒绝），UK 保留 ORM 真相源作部署加固元数据。
- **Phase 3 测试**：新增 `TestErpCsTicketTimerSession` **7 组全绿**（①RUNNING+单计时器跨工单拒绝+守卫复查 DB 直插行+槽位释放重启+多客服独立 ②暂停累计 40min 数学断言+原因可选+RUNNING resume 非法 ③stop 条目 duration=300−45=255+TIMER_IMPORT+DRAFT+D2 映射 ④12h 惰性结算 pause/stop/findActiveTimer 三入口+封顶时刻反推 startTime+720+Σ暂停+720 边界不结算+PAUSED 态超时 ⑤config off 四 mutation 拒绝+MANUAL CRUD/submit 不受影响+require-description 双路径 ⑦审批链全路径（PENDING→approve/REJECTED 改后重提/threshold 500>480/auto-approve 直通/非法状态拒绝）⑧三聚合口径（totalTimeSpent=200 含 APPROVED 不可计费 40——§四不滤 billable））；冻结时钟（2026-07-17T00:00）确定性断言式 + 空 autotest.yaml 标记（镜像 R1.65 先例）。
- **文档回填**：owner doc `time-tracking.md §十 实现约定`（D1-D6 裁决 + 二次启动拒绝语义 + 惰性结算等价性与残留 + 聚合口径 + §3.3 responsibleId→assignedToId 命名漂移更正注记）+ arm-index P1-RC-055 → done (RC-R1.66)（修复记录 + 历史保留）+ roadmap RC-R1.66 → done ✅（行标签按 A 类裁决改写，对齐 R1.63/64/65 先例）+ compliance-baseline R2c/R10 baseline-raise per-site 注记 + 本日志条目；use-cases.md L1 不动。
- **Deferred（已裁定，见计划）**：每分钟实时刷新 + 无人值守 12h 扫描 job（惰性等价，successor=运营要求时）/ 费率优先级 2-3 级（ErpCsAgentRate/权益级）/ §3.1 按组织配置强制审批 + §3.2 PENDING→DRAFT 退回 / sales 开票跨域联动（successor=yes）/ HR 考勤导出。

- **VERIFY 全绿（full-green verification）——plan `2026-08-17-2125-1`（RC-R1.65 cs 工单创建自动富化族）mission requirement-compliance 验证通过**：`mvn clean install -DskipTests` 全量 **BUILD SUCCESS**（01:50）+ 全量 `mvn test` **BUILD SUCCESS**（全 reactor **3546 tests / 0 failures / 0 errors / 1 skipped**，skipped = ErpAllWebPagesCollectTest 既有 @Disabled；含 app-erp-all 03:53）+ `bash docs/audits/nop-compliance-checker.sh` **exit 0**（全 19 规则 actual == baseline 零漂移：R2b=235 / R2c=1434 / R2d=35 / R10=10[本计划 9→10 baseline-raise 后] / R12a=70 / R12c=40）。

- **cs 工单创建自动富化族落地（RC-R1.65，plan `2026-08-17-2125-1`，P1-RC-054 UC-CS-01 ②-⑧ 创建自动富化 6 项 + ⑧ 无匹配异常路径 + A1.40 UC-CS-09 普通保存权益联动 reuse，2026-08-12 B 类批量裁决降级预授权[roadmap 头 :49，行标签按 B 类裁决改写]）**：执行 Phase 1（D1-D6 裁决，前次会话完成）+ Phase 2（实现，前次会话完成主体 + 本次会话修正）+ Phase 3（测试 + 文档 + 回填）。
- **实现（D1-D6 落地）**：**D1 save 后置富化**——`ErpCsTicketBizModel.doSaveEntity` 钩子（recoverDeleted 跳过 + **`dao().flushSession()` 使 NEW→MANAGED**——OrmEntityDao.updateEntity 仅接受 MANAGED 态，同事务新建实体未 flush 会被拒，本次会话执行期发现修正）+ `defaultPrepareSave` fill-when-absent[status=NEW + priority←ticketType.defaultPriority] + 调用点守卫[slaPolicyId 与 deadlineDateTime 均空才自动调 matchAndAttachSlaProcessor——policy+deadline+权益三合一，Processor 本体零改动]；**D2 TK 编号**——派生 xmeta `ErpCsTicket.xmeta` **显式 `autoExpr when="save"`**（模块级派生文件不经 `_delta` 层时 `DefaultMetaPostExtends`/GenCodeRuleAutoExpr 不执行——biz:codeRule 属性不自动展开，本次会话定位后显式声明其标准生成体 `nopCodeRuleGenerator.generate(propMeta['biz:codeRule'])`；**根因教训：`-pl` 单模块测试解析兄弟 meta 模块用本地仓旧构件，xmeta 改动后须先 `mvn install -pl module-cs/erp-cs-meta`**）+ 种子 `nop_sys_code_rule`（module-cs deploy 三方言首用，seqName=default 非消费占位）+ `CsTicketMonthSeqCodeRuleVariable`（bean `nopCodeRuleVariable_csTicketMonthSeq`，按月懒建 `cs_ticket_code_seq_{yyyyMM}` REQUIRES_NEW 查插，避免 {@seq:4} 单序列 4 位回绕）；**D3/D4 自动分配**——`TicketAssignResolver`（策略 teamId 链→ErpCsTeam→同码 crm 团队→成员 userId 池 + ROUND_ROBIN/LEAST_OPEN 纯函数 + `erp-cs.assign-method` config）；成功→ASSIGNED+ASSIGN 审计，池空→留 NEW+升级通知；**D5 通知**——module-notify 三方言种子 7202/7203（cs.ticket-created USER_LIST ${submitterUserId} / cs.ticket-assign-no-match ROLE 客服主管）+ try/catch 静默降级；**D6 config 激活**——auto-assign-on-create 死 flag 转活跃（仅门控分配维度）。
- **测试基建（本次会话）**：cs→crm 依赖改道——crm-service test-scope 引入会拉入其全量 beans（`ErpCrmProductConfiguratorGenerateQuoteProcessor` 需 IErpSalQuotationBiz→sal/inventory/quality/finance 服务闭包），改 **erp-crm-meta test-scope + `app-test-mock-crm.beans.xml`**（app- 前缀自动合并进 cs 测试容器 + `TestMockCrmBizModels` CrudBizModel 直生 mock，findList 覆写 dao 直查——测试容器无 crm 生成 xbiz 时 BizObject action 派发不可达；生产由 app-erp-all 经 crm-service 提供真实实现）。
- **Phase 3 测试**：新增 `TestErpCsTicketCreateEnrichment` **8 组全绿**（①缺省填充+自动挂载+config off 隔离②守卫三态[显式 policy/显式 deadline 跳过]+UC-CS-09 恰好一次扣减+手动重匹配共存③TK 格式/按月递增 0001→0002/显式 code 共存④分配成功+ASSIGN 审计+确认通知 recipient=createdBy+⑦待办可见查询⑤池空留 NEW+ROLE 客服主管升级通知落库⑥config off 仅跳分配维度⑦算法纯函数+月序列工具⑧轮转[历史锚点 a→b]/最少未结 e2e）；断言式 + 空 autotest.yaml 标记（镜像 R1.37 TestErpLogDraftEscalationJob 先例——TK 编号/月序列行名内嵌真实时钟 yyyyMM[sysCalendar 不受冻结时钟控制]，录制表快照会随月漂移翻红）。
- **BUILD_VERIFY（full-green verification）**：`mvn test -pl module-cs/erp-cs-service` **130/0/0**（122 基线 + 8 新增零回归）+ 全仓 `mvn clean install -DskipTests` **BUILD SUCCESS**（01:39）+ 全仓 `mvn test` **BUILD SUCCESS 0 failures 0 errors** + checker exit 0（**R10 9→10 baseline-raise** 登记 per-site 证据：`CsTicketMonthSeqCodeRuleVariable.ensureMonthlySequenceRow:59` REQUIRES_NEW 镜像平台 SysSequenceGenerator.runLocal 序列行初始化边界，其余 18 规则零漂移）。**独立结束审计**：独立子代理（新会话 task `ses_fedb38a41ffec6qnPtlbxKMynj`）verdict **PASS**（0 MAJOR/0 MINOR/3 INFO：实现 file:line 逐条核验 + 独立复跑 130/0/0 与 checker 零漂移 + 文档回填五处核验 + 反模式零命中 + ORM 边界零编辑确认）。
- **文档回填**：owner doc `README.md §关键业务规则 1-2` 实现注记（富化语义 + 跨域同码团队约定 + config 激活）+ `sla.md §实现约定` auto-assign-on-create 死 flag 转活跃注记 + 挂载守卫边界 + arm-index P1-RC-054 → done (RC-R1.65)（修复记录 + 历史保留）+ roadmap RC-R1.65 → done ✅（行标签按 B 类裁决改写，对齐 R1.63/64 先例）+ 本日志条目；use-cases.md L1 不动。
- **Deferred（已裁定，见计划）**：手动重复 matchAndAttachSla 重复扣减 PAY_PER_TICKET（既有语义 watch-only，successor=权益扣减幂等化立项）+ 外部客户邮件/门户实际投递（IN_APP 占位语义，successor=nop-notification 独立面）。

- **projects 采购路径物料归集 budgetChecker.check 运行时确认（A4.2.119 回队行，plan `2026-08-17-0142-3`，A1.35 SP-4 / P1-RC-049 successor 落地后探查，MA4 verification 零生产代码变更）**：执行 Phase 1（既有证据复核）+ Phase 2（N/A 裁决）+ Phase 3（报告 + 回填）。
- **Phase 1 复核结论**：R1.61 既有测试断言强度逐组核对——①STRICT 双层（prj Facade `testBudgetStrictRejectsOverBudget` assertThrows NopException + errorCode 常量精确比对 + 零归集行；pur GraphQL 集成层 `testStrictBudgetRejectsApproval` resp.getCode() 精确比对 `ERR_BUDGET_EXCEEDED` + 入库单保持 SUBMITTED[事务回滚语义] + 零归集行）②WARNING 放行（added=1500 全额 + 归集行落库）③config 门控双侧零副作用④集成层真实（真实 IoC Facade / 真实 `IGraphQLEngine` RPC 非 mock）——SP-4 存疑点全要素预覆盖；实仓重跑 `mvn test -pl module-projects/erp-prj-service,module-purchase/erp-pur-service -Dtest='TestErpPrjMaterialAggregation,TestErpPurReceiveMaterialCostAggregation'` **8+4 全绿**（4.123s/4.064s，surefire 双证）。
- **Phase 2 N/A 裁决（显式分歧记录）**：证据缺口清单为空——purchase 链路（`ErpPurReceiveApproveProcessor.approve` + `collectProjectMaterialCost`）对预算控制模式零分支依赖（仅异常传播），唯一未直接组合场景「pur 超预算+WARNING+approve 成功」= 已覆盖分支并集（`testApproveTriggersMaterialAggregation` 运行于默认 WARNING 模式 ∪ Facade WARNING 放行测试），不触发新生产分支；requireReferenceable 前置拒绝 / 事务回滚状态一致性已有直接覆盖。与 A4.2.3 当年无预覆盖故无条件新增探针的先例显式分歧。
- **Phase 3 落盘 + 回填**：报告 `docs/audits/2026-08-18-0320-rc-ma4-a4-2-119-prj-material-budget-runtime.md`（维持接受 / 零新 finding / 零分级变更 / 零生产代码变更）+ roadmap A4.2.119 → done ✅（回队执行记录）+ arm-index P1-RC-049（:220）/P1-RC-051（:223）追加 A4.2.119 运行时确认注记 + 本日志条目。
- **验证基线（全绿）**：分域全量 `mvn test -pl module-projects/erp-prj-service,module-purchase/erp-pur-service` **BUILD SUCCESS**（prj **172** / pur **324** tests，0 failures 0 errors——prj 158→172 为 R1.63 +10 / R1.64 +4 后续增量）+ compliance checker **全 19 规则 actual == baseline 零漂移**（R2b=235/R2c=1434/R2d=35/R12a=70/R12b=66/R12c=40；docs-only 计划结构性零漂移，checker 复跑留证）。
- **BUILD_VERIFY**：分域测试 + checker 零漂移均绿（Closure Gates 分支 (b)：零代码变更——Phase 2 N/A，无探针落地，全量构建不适用）。**独立结束审计**：独立子代理（新会话 task `ses_feed3310bffePfPKEthZUlD3UN`）verdict **PASS**（8/8 项：计划一致性 / 测试独立复跑 8+4 绿 / checker 19 规则零漂移 / 零生产漂移 git diff 仅 5 预期文档 / 报告完整性 / 回填三处 / 测试源码与生产守卫链 file:line 抽查 / Non-Goals 尊重 + 2 条非阻塞信息性提示）。

- **VERIFY 全绿（full-green verification）——plan `2026-08-17-0142-2`（RC-R1.64 projects 工时过账多币种）mission requirement-compliance 验证通过**：`mvn clean install -DskipTests` 全量 **BUILD SUCCESS**（01:38）+ 全量 `mvn test` **BUILD SUCCESS 0 failures 0 errors**（erp-prj-service **172 tests 0 failures 0 errors**，168 基线 + 4 新增[TestErpPrjTimesheetMulticurrencyPosting 3 组 + TestTimesheetFxRateResolution 1 组]零回归；FLUX_PAGE_ERROR_COUNT: 0；1 skipped 为 ErpAllWebPagesCollectTest 既有 @Disabled）。
