# Development Log - 2026-07-27

### 2026-07-27（审计-修复 MA2 业务正确性 A2.2 O2C 端到端 — plan 1949-2，audit-only）

- **任务**：执行 `docs/plans/2026-07-27-1949-2-audit-remediation-ma2-order-to-cash-e2e.md` 全 2 Phase。MA2 业务正确性层第 2 个工作项 A2.2，对销售到收款（SO→Delivery→Invoice→Receipt + 退货反向 + 应收核销 + 多币种）全链按 `multi-dimensional-audit-prompt.md` 做系统性多维审计。audit-only 计划：零生产代码变更（仅文档：审计报告 + arm-index + scope matrix + roadmap + 计划 + 日志）。
- **审计方法**：13 维多维审计（需求正确性/owner-doc 对齐/可用量校验/信用额度/应收 openAmount/退货反向链/多币种汇兑损益/收入成本配比/架构边界/验证充分性/回归风险/路由技能/自主权漂移）+ 6 已识别控制点逐路径裁决 + MA1 finding 运行时影响复核 + 并发敏感点交接 A2.17。逐文件核实：8 sales BizModel + 4 plain Processor（Order/Delivery/Invoice/Receipt）+ Return Processor + SalAcctDocProvider + 4 PostingDispatcher + ErpFinArApItemGenerator + ReceiptSettler + ReturnRefundOrchestrator + SalReversalListener + CreditLimitChecker + inventory ErpInvStockMoveProcessor（validateAvailable/applyReservation/releaseReservation）+ InvAcctDocProvider.SALES_OUTPUT。
- **核心结论**：**passes multi-dimensional audit**。O2C 全链组件齐备（审批-触发-过账三段 + 跨域可用量裁决 + 域级/正式双层核销 + 退货反向链 + 冲销反写经 I*Biz/Facade），E2E 覆盖黄金路径、反向冲销、辅助账 openAmount 生命周期、退货负 credit memo（TestErpSalOrderToCashEnd 728 行 + TestErpSalReturnRefundEndToEnd 660 行）。**零 P0**（无凭证借贷失衡/状态机非法转移/跨域写脏数据/数据不一致——每张凭证自身平衡、迁移经守卫、辅助账层 openAmount 回减正确）。
- **finding**：1 项 P1 登记入 arm-index 待 MR1 + 6 项 P2 watch-only。
  - **P1-MA2-009（sales+finance）多币种 O2C + 收款核销汇兑损益未实现**：(a) `VoucherFact` 仅单一 amount 字段（无 amountSource/amountFunctional 分离），`SalAcctDocProvider` 产 source-currency fact，本位币折算依赖引擎未证路径；(b) **收款核销汇兑损益完全未实现**——`SalAcctDocProvider.RECEIPT` 只生成 借银行存款/贷应收（同金额），无 6051 汇兑损益科目插平；`ErpFinArApItemGenerator` + `ErpFinReconciliationBizModel` 不计算外币 AR 与外币 RECEIPT 的汇率差 plug。E2E 均单币种（exchangeRate=ONE）。**比 P2P P1-MA2-002 更严重**：P2P 是「未验证」，O2C 是「未实现」。MR1 与 P1-MA2-002 一并裁决（补多币种 E2E + 核实折算 + 在 RECEIPT 过账与核销环节补 6051 汇兑损益 plug + 必要时 VoucherFact 增 amountSource/amountFunctional）。
  - P2-MA2-010（invoice 超订单金额审批未实现，与 P2-MA2-007 同型）/ P2-MA2-011（returns.md 红字发票流程 vs 实现 SALES_RETURN credit memo，与 P2-MA2-006 同型）/ P2-MA2-012（owner doc credit check 漏述出库/发票信用冻结扩展点）/ P2-MA2-013（owner doc "按订单/发票维度"核销，实现仅发票维度）/ P2-MA2-014（ReceiptSettler 并发无锁，归 A2.17，与 P2-MA2-008 同型）/ P2-MA2-015（跨月收入成本配比 owner doc 漏述）。
- **MA1 finding 运行时复核（无升级）**：P1-MA1-022 sales `ErpSalOrderProcessor:377,389` daoFor ErpMdSubject/ErpFinAccountingPeriod → 纯只读查询（承付 hook，config-gated 默认关），不影响业务正确性；UC-SAL-10 并发扣批次 / UC-INV-08 乐观锁 → 乐观锁基础 ErpInvStockBalance.version 列存在（propId=17），但 reservedQuantity 双读双写 lost-update 风险归 A2.17 系统性裁决；P0-MA1-021 → 确认未回退（O2C 红冲经 SalPostingExecutor → Facade IErpFinVoucherBiz.reverse）。
- **回归基线（full-green verification）**：零 P0 即时修复 → 全量 `mvn clean install -DskipTests` BUILD SUCCESS（154 模块 reactor，Total time 01:35 min，2026-07-27T21:10:05+08:00）+ `mvn test` BUILD SUCCESS（Total time 09:26 min，2026-07-27T21:19:41+08:00，全绿，仅已知 `ErpAllWebPagesCollectTest` @Disabled skip）。审计零代码变更，build/test 门控为回归基线确认，全绿状态与 M0 锚点一致。
- **同步**：审计报告 `docs/audits/2026-07-27-1949-arm-ma2-order-to-cash-e2e.md`（Audit Status: closed）；arm-index 报告清单（新增本报告行 done）+ §P1 汇总头部计数更新 + §P1 详细清单（新增 P1-MA2-009 行）+ §P2 清单（新增 P2-MA2-010~015 共 6 行）；scope matrix §2.2「业财端到端」sales `❓` → `⚠️(P1)`，finance 保持 `⚠️(P1)`；roadmap A2.2 `ready` → `done` + header v3 → v4。**MA2 业财端到端 P2P+O2C 两条主链均完成，A2.3 期末结账为下一工作项**。

### 2026-07-27（审计-修复 MA2 业务正确性 A2.1 P2P 端到端 — plan 1949-1，audit-only）

- **任务**：执行 `docs/plans/2026-07-27-1949-1-audit-remediation-ma2-procure-to-pay-e2e.md` 全 2 Phase。MA2 业务正确性层首个工作项 A2.1，对采购到付款（PO→Receive→Invoice→Pay + 退货反向 + 应付核销）全链按 `multi-dimensional-audit-prompt.md` 做系统性多维审计。audit-only 计划：零生产代码变更（仅文档：审计报告 + arm-index + scope matrix + roadmap + 计划 + 日志）。
- **审计方法**：12 维多维审计（需求正确性/owner-doc 对齐/三单匹配边界/暂估↔正式应付衔接/付款核销 openAmount/退货反向链/多币种/架构边界/验证充分性/回归风险/路由技能/自主权漂移）+ 5 已识别控制点逐路径裁决 + MA1 finding 运行时影响复核。逐文件核实：6 purchase BizModel + 6 plain Processor + ThreeWayMatcher + PurAcctDocProvider/3 PostingDispatcher + ErpFinArApItemGenerator + PaymentSettler + ErpFinReconciliationBizModel + ErpPurReturnProcessor + PurReversalListener + ErpCtInvoicePlanBizModel。
- **核心结论**：**passes multi-dimensional audit**。P2P 全链组件齐备（审批-触发-过账三段 + 库存联动 + 域级/正式双层核销 + 退货反向链 + 冲销反写经 I*Biz/Facade），E2E 覆盖黄金路径与反向冲销（TestErpPurProcureToPayEnd 644 行 + TestErpPurReturnRefundEndToEnd 632 行）。**零 P0**（无凭证借贷失衡/状态机非法转移/跨域写脏数据——每张凭证自身平衡、所有迁移经守卫、P1-MA1-029 生成 unposted DRAFT 经正常管道）。
- **finding**：3 项 P1 登记入 arm-index 待 MR1 + 5 项 P2 watch-only。
  - **P1-MA2-001（purchase+finance）暂估冲回缺失**：PURCHASE_INPUT（贷 2202 暂估应付）与 AP_INVOICE（贷 2202 应付+借 1403 在途物资）在「先入库后开票」黄金路径无自动冲回；GL 2202 双计、1403 与 1401 双计存货。辅助账层不受影响。MR1 裁决：实现 GRNI 自动冲回 或 登记为期末人工清理 documented simplification。
  - **P1-MA2-002（purchase+finance）多币种 P2P 本位币凭证路径未验证**：VoucherFact 仅单一 amount 字段，PurAcctDocProvider 产 source-currency fact，本位币折算依赖引擎未证路径；E2E 均单币种（exchangeRate=ONE）。MR1 补多币种 E2E + 核实折算 + 必要时 VoucherFact 增 amountSource/amountFunctional。
  - **P1-MA2-003（purchase）付款核销缺三单匹配完成态复核**：PaymentSettler 仅校验发票 APPROVED，不复核 match 完成态（owner doc three-way-match.md §匹配时机:48 要求）；非严格默认下二次门禁缺失。
  - P2-MA2-004（qty 容差配置读未用）/ P2-MA2-005（three-way-match.md 内部不一致）/ P2-MA2-006（returns.md 红字发票流程偏离实现）/ P2-MA2-007（订单审核锁定价格无服务端守卫）/ P2-MA2-008（PaymentSettler 并发无锁，归 A2.17）。
- **MA1 finding 运行时复核（无升级）**：P1-MA1-022 跨域只读 daoFor ErpMd* → 不影响业务正确性（纯查询）；P1-MA1-029 contract 跨域写 pur 发票行 → 生成 unposted UNSUBMITTED DRAFT 经正常管道，无 APPROVED 脏发票，业务正确性不受影响（维持治理层）；P0-MA1-021 → 确认未回退（P2P 红冲经 Facade/I*Biz）。
- **回归基线**：零 P0 即时修复 → 全量 `mvn clean install -DskipTests -T 1C` BUILD SUCCESS（156 模块 reactor）+ `mvn test -T 1C` BUILD SUCCESS（9:12 min，全绿，仅已知 web 冒烟 @Disabled skip）。
- **同步**：审计报告 `docs/audits/2026-07-27-1949-arm-ma2-procure-to-pay-e2e.md`（Audit Status: closed）；arm-index 报告清单 + §P1（新增 P1-MA2-001/002/003 + P1-MA1-029 运行时复核注记）+ §P2（新增 P2-MA2-004~008）；scope matrix §2.2「业财端到端」finance/purchase `❓` → `⚠️(P1)`；roadmap A2.1 `ready` → `done`。**MA2 业务正确性层启动（A2.1 done）**。

### 2026-07-27（审计-修复 MA1 架构治理复审 A1.14 — plan 1430-3，audit-only）

- **任务**：执行 `docs/plans/2026-07-27-1430-3-audit-remediation-ma1-architecture-governance-review.md` 全 2 Phase。MA1 架构治理复审是 audit-remediation-roadmap MA1 工作项 A1.14（MA1 最后一项），对首审报告 `docs/audits/2026-07-23-0000-architecture-governance-review.md`（F1–F9 全闭包）做残留核验 + 自首审以来新漂移扫描 + scope matrix §2.1 架构治理行 12 ❓ 列补全。audit-only 计划：零生产代码变更（仅文档：复审报告 + arm-index + scope matrix + roadmap + 首审报告复审注记 + 计划 + 日志）。
- **审计方法**：自动化 grep（5 启发式：跨域写 / extends RuntimeException / @Inject private / @Transactional / System.currentTimeMillis+LocalDate.now）+ compliance checker 19 规则全量对照 M0.3 锚点（HEAD=0e963531d）+ 7 已 ⚠️ 域残留复核 + 12 ❓ 域四子维度（daoFor/字典/共享内核/guard）机械核查。
- **核心结论**：F1–F9 残留**全部未回退**；自首审（2026-07-23）以来 5 天密集审计-修复计划落地**未引入新 P0**；CI guard 19 规则 actual ≤ baseline（**R2c 实际 1226 < baseline 1228，合规改善 -2**，源于 P0-MA1-021 修复 + 代码清理）；R12 共享内核 import **零增长**（69/66/38 精确等于基线）；scope matrix §2.1 架构治理行**全域 19 列补全（0 ❓）**。
- **裁决**：**PASS**。0 P0 + 1 新 P1 + 1 新 P2。**MA1 里程碑（A1.1–A1.14）全部 done**。
- **关键 finding**：
  - **P1-MA1-029（contract）**：`ErpCtInvoicePlanBizModel:159,196` 经 `daoProvider().daoFor(ErpPurInvoiceLine/ErpSalInvoiceLine.class).saveEntity(...)` 跨域写——文件 Javadoc（line 41-45）已有 bypass rationale 但**未**登记 `posting-exemptions.md`。首审报告 line 130/144 已识别该文件为 "bypass rationale 文件" 但闭包时仅补登 ErpB2bAsnBizModel。与首审 F1 闭包项 #2 同型半治理。MR1 修复方式=补登 posting-exemptions.md 豁免条目（含位置/触发场景/理由/风险/补偿机制/收敛条件）。
  - **P2-MA1-030（master-data）**：`ErpMdCurrencyBizModel:60` 新增 `LocalDate.now()` 直调（programmer-error 路径，与 P2-MA1-019 同性质合并 watch-only）。
- **F1–F9 残留复审明细**：(F1) Type 1 findAllByQuery 实测 110 处 vs 首审估算 113（未恶化）；3 已登记跨域写豁免文件保持；daoFor Type 4 跨域写精确扫描 5 处（4 已登记 + ErpCtInvoicePlanBizModel 2 处未登记→P1-MA1-029）。(F2) 9 域 Erp*DocStatus 接口全在；doc-status.dict.yaml 仅在 module-common-service；approve-status.dict.yaml=0。(F3) §5.6.2 line 530/532/538 含 3 DAG 边。(F4) R12a/b/c=69/66/38 精确等于 M0.3。(F5) docs/design/notify/README.md 9215 字节 + module-boundaries.md notify 行。(F6) ErpQaInspectionType 在 erp-qa-dao；mfg 0 _ErpQaDaoConstants import。(F7) docs/design/drp/README.md §ErpInvDrp* 实体命名例外登记完整。(F8) compliance.yml + web-pages-validation job 仍存在。(F9) 19 web 测试 @Tag("full-app")，@Disabled=0。
- **scope matrix §2.1 架构治理行 19 列全域补全**：7 已 ⚠️ 域：finance/mfg/md/drp 维持 residual（首审 deferred successor）；b2b 升级 residual（F1 闭包项 #2 完成）；inv 维持 P1（P0-MA1-021 已闭包 + P1-MA1-022 待 MR1）；notify 升级 ✅。12 ❓ 域：hr/crm/cs/log/notify ✅；assets/pur/sal/qa/prj/mnt/aps ⚠️(P1)（合并 P1-MA1-022）；ct ⚠️(P1)（新增 P1-MA1-029）。
- **roadmap 同步**：`docs/backlog/audit-remediation-roadmap.md` MA1 A1.14 Status `todo` → `done`；header v2 → v3（"MA1 全部 done"）；§Work Item Details A1.14 项追加完成总结。**MA1 里程碑（A1.1–A1.14）全部 done，进入 MA2 业务正确性层**。
- **arm-index 同步**：报告清单新增本复审报告行 status=done；§P1 汇总头部数从"51+3+1+1+0"更新为"51+3+1+1+0+1"（追加架构治理复审 1 项 P1）；§P1 详细清单新增 P1-MA1-029 行；§P2 清单新增 P2-MA1-030 行。
- **首审报告复审注记**：`docs/audits/2026-07-23-0000-architecture-governance-review.md:9` 追加复审注记段（含 F1–F9 复审结论摘要 + 0 P0 + 1 P1-MA1-029 + R2c 改善 + R12 零扩张 + scope matrix 全域补全 + MA1 全部 done）。
- **验证（baseline-confirm，full-green verification）**：审计零代码变更，build/test 门控为回归基线确认。`mvn test` BUILD SUCCESS（1756 tests，0 failures / 0 errors / 1 skipped = `ErpAllWebPagesCollectTest @Disabled` M0 锚点已知接受项，08:46 min）；compliance checker 全 19 规则 actual ≤ baseline 实测通过（HEAD=8f53ed612）。全绿状态与 M0 锚点一致。
- **审计**：草案审查 iteration 2 = accept（plan §Draft Review Record，2 BLOCKER + 4 NON-BLOCKER 全部修复）；独立结束审计 = PASS（独立子代理 `ses_05cb18ec8ffeZzHRIW3b9AoVMN` 只读复核，裁决 pass，0 blocker，所有关键 claim 经实仓证据验证）。
- **Skill**：参考首审 arch-gov-review 方法（roadmap A1.14 指定，非单一 skill 文件，复用首审的 grep/核查方法）；F8/F9 子项 = compliance-checker。
- **后续 Follow-up**：(1) P1-MA1-029 + 合并 P1-MA1-022（9 域）+ 其他 MA1 P1 finding 经 R1.0 展开机制进入 MR1 统一裁决；(2) daoFor Type 4 / governed path 平台解耦维持首审 deferred successor（触发条件：nop-entropy 平台 lazy/SPI 解耦机制落地）；(3) MA1 全部 done，MA2 业务正确性审计对实现层结构完整性的依赖已解除。

### 2026-07-27（审计-修复 MA1 Nop 平台合规审计 BC 合并 A1.13 — plan 1430-2，audit-only）

- **任务**：执行 `docs/plans/2026-07-27-1430-2-audit-remediation-ma1-platform-conformance-bc-tier.md` 全 2 Phase。MA1 平台合规审计是 audit-remediation-roadmap MA1 工作项 A1.13（最后一批），按 `nop-platform-conformance-audit-prompt.md` 15 维度对剩余 12 域（crm / quality / projects / cs / contract / b2b / maintenance / drp / master-data / aps / logistics / notify）做系统性语义审计。audit-only 计划：零生产代码变更（仅文档：审计报告 + arm-index + scope matrix + roadmap + 计划 + 日志）。
- **审计方法**：自动化 grep（9 机械化规则）+ 15 维度语义分析 + 维度 15 owner-doc→代码漂移抽样（A 级 6 域 ≥4 核查点 / B+C 6 域 ≥2 核查点）。复用 A1.11（S 级）+ A1.12（A 级核心）报告模板与 grep 脚本。
- **核心结论（4 量化字段）**：12 域 15 维度合规率 **179/180** / 反模式实例 **0 真实命中**（0 javadoc 假阳）/ P0 **0** / P1 新增 **0**（5 域跨域只读 daoFor 扩展 P1-MA1-022 至 9 域，无新 ID）+ P2 新增 **2**（P2-MA1-027 / P2-MA1-028）。
- **裁决**：**PASS**。12 域跨模块外部实体引用全部经机制 B notGenCode 声明（与 A1.10 / A1.11 / A1.12 结论一致）；**跨模块写零违规**——mnt/prj/qa/logistics 等业财一体写全部经 `IErpFinVoucherBiz.post/reverse` Facade I\*Biz 跨域调用（platform 标准跨域写路径），与 S 级 finance + A 级 inventory `CostAdjustmentPostingDispatcher` 修复后范式一致；无 P0 blocker。
- **关键 finding**：
  - **P1-MA1-022（扩展至 9 域）**：A1.12 已登记 pur/sal/ast/inv 跨域只读 daoFor；A1.13 新增扩展 mnt/prj/qa/drp/aps 跨域只读 daoFor（mnt 读 ErpFinVoucherBillR/ErpInvStockMove/ErpMdAcctSchema；prj 读 ErpMdSubject；qa 读 ErpInvStockBalance/ErpMdMaterial；drp 读 ErpInvTransferOrder/ErpPurOrder/ErpMfgForecast/ErpMd*；aps 读 ErpInvReservation/ErpInvStockBalance/ErpMfgBom）。Dashboard/Report facade read-only 聚合永久接受。MR1 裁决方案 A（master-data/finance/inventory/manufacturing I\*Biz 补便捷只读方法后迁移）或方案 B（永久接受为 Helper 合法模式）。
  - **P2-MA1-027（contract Minor）**：owner doc `state-machine.md §1`（经 L-5 plan 2026-07-20-2200-1 补）列合同 7 态含 CANCELLED（DRAFT→CANCELLED 草稿废弃），但代码 dict `erp-ct/contract-status` + ErpCtConstants 仅 6 态，BizModel 仅实现 ACTIVE→TERMINATED，DRAFT 废弃实际走 useLogicalDelete。owner doc 与代码漂移，watch-only MR1 顺手更新。
  - **P2-MA1-028（maintenance Minor）**：owner doc `state-machine.md §适用对象二` 描述维护请求 5 态，代码 dict `erp-mnt/request-status` 含 6 态（多 IN_PROGRESS 维修中）。owner doc 漏 IN_PROGRESS，watch-only MR1 顺手更新。
- **已知 P1 平台合规影响复核**：crm DECIMAL↔double P1-MA1-009 / projects propId P1-MA1-010 / maintenance propId P1-MA1-011 / quality propId P1-MA1-012 / drp 命名 P1-MA1-014 均为 ORM 层机械维度问题，平台合规维度（5 平台辅助工具 / 1/13 codegen 产物安全）无新增偏差，MR1 修复时消除。
- **D1 字典 valueType int→string 复核**：12 域全部 dict 定义未发现新 D1 复现。D1 状态确认 watch-only residual，触发条件不变。
- **roadmap 同步**：`docs/backlog/audit-remediation-roadmap.md` MA1 A1.13 Status `todo` → `done`；MA1 §Work Item Details 同步 A1.11-A1.13 三批完成总结。scope matrix §2.1 "Nop 平台合规" 行 12 列全部 `❓` → 终态（5 ✅ [crm/qa/prj/cs/b2b/md/log/notify 内 5 个全维度通过] + 4 ⚠️(P2) [ct/mnt 含 P2 owner-doc drift] + 3 ⚠️(P1) [drp/aps 含合并 P1-MA1-022 跨域只读]），§2.1 顶部说明同步。**MA1 平台合规维度全域 19 列全部 ✅/⚠️(P1)/⚠️(P2)，无 ❓**。arm-index 报告清单新增本报告行 status=done + §P1 类型分布 P1-MA1-022 行扩展至 9 域 + §P1 详细清单 P1-MA1-022 行同步 + §P2 发现汇总新增 P2-MA1-027/028。
- **验证（baseline-confirm，full-green verification）**：审计零代码变更，build/test 门控为回归基线确认。合并态（含 P0-MA1-021 代码修复 + A1.13 全文档）独立 re-verify：`mvn clean install -DskipTests` BUILD SUCCESS（154 模块，01:41 min）+ `mvn test` BUILD SUCCESS（0 failures / 0 errors / 1 skipped = `ErpAllWebPagesCollectTest @Disabled` M0 锚点已知接受项，09:25 min）。全绿状态与 M0 锚点一致。
- **审计**：草案审查 iteration 2 = accept（plan §Draft Review Record，1 BLOCKER + 3 NON-BLOCKER 全部修复）；独立结束审计 = PASS（独立子代理 `ses_05cebb804ffe1uQEBAyk56pfwk` 只读复核，裁决 pass，0 blocker）。
- **Skill**：`nop-platform-conformance-audit-prompt.md`（roadmap A1.13 指定此 skill，与 A1.11/A1.12 同 skill 同维度不同域簇）。
- **后续 Follow-up**：(1) P1/P2 finding 经 R1.0 展开机制进入 MR1（P1-MA1-022 跨域只读扩展至 9 域统一裁决方案 A/B；P2-MA1-027/028 owner-doc drift 顺手收敛）；(2) MA1 平台合规审计（A1.11/A1.12/A1.13）全部 done，MA2 业务正确性审计对实现层平台合规的依赖已解除；(3) A1.14 架构治理复审按 roadmap 推进。

### 2026-07-27（P0 即时通道修复 P0-MA1-021 — plan 1430-1，inventory 成本调整红冲绕过 I*Biz）

- **任务**：执行 `docs/plans/2026-07-27-1430-1-arm-fix-p0-ma1-021-inv-cost-adjust-voucher-writeback.md` 全 2 Phase。P0-MA1-021 是 A1.12 平台合规审计在 inventory 维度 2 发现的 P0 实时缺陷：`CostAdjustmentPostingDispatcher.markOriginalVoucherReversed` 跨模块直接写 finance 实体 `ErpFinVoucher`（置 `isReversed=true` + `updateEntity`），绕过 `IErpFinVoucherBiz.reverse()` I*Biz 边界，触及 finance 凭证保护区域。
- **修复方案（选项 A）**：删除冗余 inventory 侧标记方法 + 其调用。实仓核实 finance 侧 `ErpFinPostingProcessor.reverseProcess:243 → markOriginalVoucherReversed:909-923` 已在 I*Biz 边界内、同一事务内做逐行等价的标记（NORMAL+POSTED+未冲销 → `isReversed=true`）；inventory `reverse():78` 已先调用 `voucherBiz.reverse(...)`，故 `:79` 是完全冗余的重复写。选项 B（新增 `markReversed` I*Biz 写方法）否决——会引入多余 I*Biz 表面且 `reverse()` 语义已涵盖标记。
- **代码变更**：
  - `module-inventory/erp-inv-service/.../CostAdjustmentPostingDispatcher.java`：删除 `markOriginalVoucherReversed` 私有方法（原 :121-137）+ 其在 `reverse()` 内的调用（原 :79）；清理随之失效的 7 个 import（`ErpFinVoucher`/`ErpFinVoucherBillR`/`ErpFinConstants`/`QueryBean`/`IEntityDao`/`java.util.Objects`/`FilterBeans.and/eq`）+ `POSTING_TYPE_NORMAL` 字段；保留 `IDaoProvider`（仍用于 `AcctSchemaResolver.resolvePrimarySchemaId`）。文件从 147 行降至 117 行。
  - `module-inventory/erp-inv-service/src/test/.../TestErpInvCostAdjust.java`：扩展 `testReverseRollsBackBalanceAndVoucher`，新增防回归断言——红冲后原 NORMAL 凭证 `isReversed=true`（经 `findOriginalNormalVoucherByBillCode` helper 按 `postingType==null||"NORMAL"` 过滤，排除 REVERSAL 凭证）。
- **owner doc 对齐**：`docs/design/finance/posting.md §冲销机制 §方向一` 补一句说明——引擎 `ErpFinPostingProcessor.markOriginalVoucherReversed`（`reverseProcess` 末段）承担原 NORMAL 凭证 `isReversed=true` 标记，业务域不应绕过 `IErpFinVoucherBiz.reverse()` I*Biz 边界直接跨模块写。
- **索引/矩阵回填**：`docs/audits/arm-index.md` §P0 追踪 P0-MA1-021 修复状态 `fix-plan-required (protected area gate)` → `done (plan 2026-07-27-1430-1)`；`docs/audits/audit-remediation-scope-and-dimension-matrix.md §2.1` Nop 平台合规行 inventory 列 `⚠️(P0)` → `⚠️(P1)`（P0 已闭包；P1-MA1-022 跨域只读 daoFor 仍待 MR1），§2.1 顶部 narrative 同步。
- **验证（full-green verification）**：`mvn clean install -DskipTests` BUILD SUCCESS（154 模块，01:38 min）；`mvn test` BUILD SUCCESS（1756 tests，0 failures / 0 errors / 1 skipped = `ErpAllWebPagesCollectTest @Disabled` M0 锚点已知接受项，08:51 min）；`mvn test -pl module-inventory/erp-inv-service`（120 tests，0 failures）+ `mvn test -pl module-finance/erp-fin-service`（285 tests，0 failures）确认 reverse 链路无回归。compliance checker R2c `daoFor()` 总量 1228 → 1226（−2 = 移除的两个 `daoFor(ErpFinVoucher*)` 调用），其余规则无增，基线不高于 M0 锚点。
- **审计**：草案审查 iteration 1 = accept（`ses_05da4746affe69cjWJASRZ0f4Q`，0 blocker）；独立结束审计 = PASS（`ses_05d89627dffeloAbAIB3Ec9YG3`，新会话只读复核 6 维度全部 ✅，0 blocker）。
- **Skill**：`nop-backend-dev`（Phase 1，I*Biz 跨实体访问契约 + 写边界）+ `nop-testing`（Phase 2，单模块集成测试防回归断言）。
- **后续 Follow-up**：P1-MA1-022（pur+sal+ast+inv 跨域只读 IDaoProvider 模式，~14 文件）经 R1.0 展开机制进入 MR1 统一裁决方案 A/B，与本 P0 修复独立。

### 2026-07-27（审计-修复 MA1 Nop 平台合规审计 A 级核心 A1.12 — plan 1227-3，audit-only）

- **任务**：执行 `docs/plans/2026-07-27-1227-3-audit-remediation-ma1-platform-conformance-a-tier-core.md` 全 2 Phase。MA1 平台合规审计是 audit-remediation-roadmap MA1 工作项 A1.12，按 `nop-platform-conformance-audit-prompt.md` 15 维度对 A 级核心四域（purchase / sales / assets / inventory）做系统性语义审计。audit-only 计划：零生产代码变更（仅文档：审计报告 + arm-index + scope matrix + roadmap + 计划 + 日志）。
- **审计方法**：自动化 grep（8 机械化规则）+ 15 维度语义分析 + 维度 15 owner-doc→代码漂移抽样（每域 ≥4 核查点，发现 ≥2 漂移则扩大）。复用 A1.11（S 级）报告模板与 grep 脚本。
- **核心结论（4 量化字段）**：四域 15 维度合规率 **59/60**（inventory 维度 2 失败）/ 反模式实例 **1 真实 + 17 javadoc 假阳** / P0 **1**（P0-MA1-021）/ P1 新增 **1**（P1-MA1-022 跨域合并）+ P2 新增 **4**（P2-MA1-023/024/025/026）。
- **裁决**：**条件性失败**（59/60 维度合规；唯一阻塞是 P0-MA1-021 业财一体写绕过 I\*Biz）。四域所有跨模块外部实体引用全部经机制 B notGenCode 声明（purchase 12 + sales 11 + assets 6 + inventory 10 = 39 处，与 A1.10 / A1.11 结论一致）。机械化规则全绿：0 extends RuntimeException / 0 @Inject private / 0 System.currentTimeMillis / 0 LocalDate.now / 0 真实 @Transactional+@BizMutation 共存（17 个 grep 命中全部 javadoc 假阳）/ 0 _gen 手改 / 0 __XGEN_FORCE_OVERRIDE__ 手改。
- **关键 finding**：
  - **P0-MA1-021（inventory blocker）**：`CostAdjustmentPostingDispatcher.markOriginalVoucherReversed:127-141` 跨模块写 `ErpFinVoucher`（`voucherDao.updateEntity(voucher)` 置 `isReversed=true`），绕过 `IErpFinVoucherBiz.reverse()`，属 plan P0 类别「业财一体写绕过 I\*Biz」。**功能风险**（高于平台合规）：仅置标志未生成红字冲销凭证，破坏 finance 凭证审计轨迹。触及 finance 凭证保护区域 → **异步注入 fix plan**（plan Phase 2 合法路径），状态 `fix-plan-required (protected area gate)`，建议先于 MR1 执行 + 独立 plan-audit + closure-audit。修复路径选项 A（推荐）：替换为 `IErpFinVoucherBiz.reverse(billHeadCode, COST_ADJUSTMENT)`；选项 B：新增 `markReversed(voucherId)` I\*Biz 写方法。
  - **P1-MA1-022（跨域 Major 合并）**：pur+sal+ast+inv 跨域只读访问经 `IDaoProvider.daoFor(OtherModuleEntity)` 而非 I\*Biz（~14 文件，与 P1-MA1-016 finance 读 assets 同根因）。Dashboard facade read-only 聚合永久接受。MR1 裁决方案 A（master-data/finance I\*Biz 补便捷只读方法后迁移 Processor/Dispatcher）或方案 B（永久接受为 Helper 合法模式）。
  - **P2-MA1-023/024（assets Minor）**：owner doc `state-machine.md §1` 资产状态表 5 态（缺 DISPOSED）/ `§折旧计划条目状态` 3 态（缺 CANCELLED）漏更新，代码 dict 实际 6 态 / 4 态。兄弟 owner doc split-merge.md / depreciation-and-posting.md 与代码一致（漂移源在 state-machine.md 过时）。watch-only MR1 顺手更新。
  - **P2-MA1-025（inventory Minor）**：owner doc `state-machine.md §盘点单状态机` 用 `COUNTING`，代码实际用 dict `erp-inv/move-status` 中 `CONFIRMED`（盘点单复用 move-status）。兄弟 ui-patterns.md 表述正确。watch-only MR1 顺手更新。
  - **P2-MA1-026（purchase Minor）**：`app-erp-purchase.orm.xml:451` column `status ext:dict="erp-pur/scorecard-status" defaultValue="10"`，dict value 为 DRAFT/FINALIZED 但 defaultValue 残留 int「10」。属 D1（int→string 已知 deferred）复现。watch-only **不重复裁决 D1**。
- **扩大抽样触发**：assets 发现 2 处漂移（DISPOSED + CANCELLED），按 skill 维度 15 规则执行扩大抽样（共 7 核查点），确认漂移源在 state-machine.md 过时而非代码缺陷，故严重性降级 Minor（P2）。
- **roadmap 同步**：`docs/backlog/audit-remediation-roadmap.md` MA1 A1.12 Status `todo` → `done`。scope matrix §2.1 "Nop 平台合规" 行：pur=`✅` / sal=`✅` / assets=`⚠️(P2)` / inv=`⚠️(P0)`，§2.1 顶部说明同步。arm-index 报告清单新增行 status=done + §P0 追踪新增 P0-MA1-021 + §P1 详细清单新增 P1-MA1-022 + §P2 发现汇总新增 4 项。
- **验证（baseline-confirm，full-green verification）**：审计零代码变更，build/test 门控为回归基线确认。closure audit 运行 `mvn clean install -DskipTests -pl app-erp-all -am` BUILD SUCCESS（01:09 min，154 模块）；`mvn test -pl app-erp-all -am` BUILD SUCCESS（08:45 min），0 failures / 0 errors / 1 skipped（`ErpAllWebPagesCollectTest @Disabled`，M0 锚点已知接受项）。VERIFY 步骤重跑增量确认（docs-only 无模块变更，no clean）：`mvn install -DskipTests -pl app-erp-all -am` BUILD SUCCESS（01:06 min）；`mvn test -pl app-erp-all -am` BUILD SUCCESS（08:50 min），0 failures / 0 errors / 1 skipped。全绿状态与 M0 锚点一致；P0-MA1-021 修复在独立 fix plan 内验证。
- **Skill**：`nop-platform-conformance-audit-prompt.md`（roadmap A1.12 指定此 skill，15 维度 Nop 平台合规审计专用方法，与 A1.11 同 skill 同维度不同域簇）。
- **后续 Follow-up**：(1) 起草 P0-MA1-021 fix plan（建议命名 `docs/plans/YYYY-MM-DD-HHmm-arm-fix-p0-ma1-021-inv-cost-adjust-voucher-writeback.md`），先于 MR1 执行；(2) P1/P2 finding 经 R1.0 展开机制进入 MR1；(3) A1.13（B+C 合并）按 roadmap 推进。

### 2026-07-27（审计-修复 MA1 Nop 平台合规审计 S 级 A1.11 — plan 1227-2，audit-only）

- **任务**：执行 `docs/plans/2026-07-27-1227-2-audit-remediation-ma1-platform-conformance-s-tier.md` 全 2 Phase。MA1 平台合规审计是 audit-remediation-roadmap MA1 工作项 A1.11，按 `nop-platform-conformance-audit-prompt.md` 15 维度对 S 级三域（finance / manufacturing / hr）做系统性语义审计（决策顺序 / 跨实体访问 / 异常处理 / IoC 与事务 / 平台辅助工具 / 标准服务模式 / 机制 B / 状态机与规则引擎 / 审批流与作业 / 定制能力 / 多租户与本地化 / 测试 / codegen 产物安全 / 聚合完整性 / owner-doc→代码漂移抽样）。audit-only 计划：零生产代码变更（仅文档：审计报告 + arm-index + scope matrix + roadmap + 计划 + 日志）。
- **审计方法**：自动化 grep（8 机械化规则）+ 15 维度语义分析 + 维度 15 owner-doc→代码漂移抽样（每域 ≥4 核查点，发现 ≥2 漂移则扩大）。
- **核心结论（4 量化字段）**：三域 15 维度合规率 **45/45** / 反模式实例 **3**（fin 1 javadoc-only 假命中 + 1 LocalDate.now + 1 IllegalArgumentException，mfg 0，hr 0）/ P0 **0**（无即时通道触发）/ P1 新增 **1**（P1-MA1-018）+ P2 新增 **2**（P2-MA1-019 / P2-MA1-020）。
- **裁决**：PASS。三域跨模块外部实体引用全部经机制 B notGenCode 声明（finance 12 + mfg 9 + hr 7 = 28 处，与 A1.10 结论一致），无 P0 blocker。
- **关键 finding**：
  - P1-MA1-018（finance Major）：`ErpFinBusinessType` enum 名 ↔ dict `erp-fin/business-type` value 漂移 4 项（`MANUFACTURING_COST_CLOSE(100)`↔`PRODUCTION_COST` / `PROJECT_COST_COLLECTION(110)`↔`PROJECT_COST` / `PERIOD_CLOSE(120)`↔`PERIOD_CLOSING` / `EXCHANGE_GAIN_LOSS(130)`↔`FX_REVALUATION`）。代码以 `enum.name()` 持久化到 `voucher_bill_r.businessType`，UI dict 下拉值与 DB 存储值不符，筛选查询漏命中。owner doc `posting.md §业务类型映射` 明示「常量 code 与字典数值逐一一致」但实际不一致。MR1 修复方案 A（推荐 enum 重命名对齐 dict）或 B（dict 改值+数据迁移）。
  - P2-MA1-019（finance Minor）：`ErpFinBusinessType.fromCode:86` 抛 `IllegalArgumentException`（应 NopException）+ `ErpFinVoucherTemplateBizModel:95` 用 `LocalDate.now()`（应 `CoreMetrics.currentDate()`）。programmer-error 路径，非 GraphQL 面向，watch-only。
  - P2-MA1-020（hr Minor）：orphan dict `erp-hr/salary-approval-status`（6 态）残留，owner doc `payroll.md §审批状态标准化` 明示已废弃（拆为 wf/approve-status 4 态），实际 column 已正确引用 wf/approve-status；orphan dict 仅在 i18n + javadoc 残留引用。watch-only。
- **合规基线复核**：M0 锚点（HEAD=0e963531d）落锚后所有前序系统性偏差修复（D2 BizModel 安全 API / D5 移除多余 IOrmTemplate / R3/R7 收敛 / F1 daoFor Type 1 重构 / F8 compliance checker CI 接线）效果显著：R4=0 / R5=0 / R7=0 全绿；R6=2 / R10=6 已登记基线（owner doc `processor-extension-pattern.md` 硬规则 1 背书的跨域失败隔离事务）；跨模块外部实体引用 100% 经机制 B 声明。
- **roadmap 同步**：`docs/backlog/audit-remediation-roadmap.md` MA1 A1.11 Status `todo` → `done`。scope matrix §2.1 "Nop 平台合规" 行：finance=`⚠️(P1)` / mfg=`✅` / hr=`⚠️(P2)`，§2.1 顶部说明同步。arm-index 报告清单新增行 status=done + §P1 详细清单新增 P1-MA1-018 + §P2 发现汇总新建。
- **验证（baseline-confirm）**：审计零代码变更，build/test 门控为回归基线确认。`mvn test -pl app-erp-all -am` BUILD SUCCESS（08:39 min，2026-07-27 13:23），全模块汇总 0 failures / 0 errors / 1 skipped（`ErpAllWebPagesCollectTest @Disabled`，M0 锚点已知接受项），与 M0 锚点状态一致。
- **Skill**：`nop-platform-conformance-audit-prompt.md`（roadmap A1.11 指定此 skill，15 维度 Nop 平台合规审计专用方法）。

### 2026-07-27（审计-修复 MA1 跨模块依赖与 DAG 审计 A1.10 — plan 1227-1，audit-only）

- **任务**：执行 `docs/plans/2026-07-27-1227-1-audit-remediation-ma1-cross-module-dag-audit.md` 全 2 Phase。MA1 跨模块依赖审计是 audit-remediation-roadmap MA1 工作项 A1.10，按 `cross-module-dependency-audit-prompt.md` 7 维度（DAG 合规 / 外部实体声明完整性机制 B / 跨模块引用范式选择合理性 / 业财一体边界一致性 / 冗余字段策略 / Maven 依赖与 orm 声明对齐 / 与 data-dependency-matrix.md 一致性）对全 19 域做系统性跨模块依赖审计。audit-only 计划：零生产代码变更（仅文档：审计报告 + 自动化脚本 + arm-index + scope matrix + roadmap + 计划 + 日志）。
- **审计方法**：Python 自动化扫描（`docs/audits/scripts/cross-module-dep-extract.py`）扫描全 19 域 ORM，正则提取 625 个跨模块 to-one 边 + 111 个 `<entity notGenCode="true">` 外部声明，DFS 三色法构图检测循环，对照 ALLOWED 字典验证方向合法性。输出完整原始清单（`cross-module-dep-extract-output.md`，979 行含 file:line 锚点）。
- **核心结论（4 量化字段）**：跨模块引用边总数 **625**（625 to-one + 0 to-many）/ DAG 合规边数 **625/625 = 100%** / 循环数 **0** / 外部实体声明完整覆盖率 **108/108 = 100%**。owner doc §5.6.2 自述"~369 to-one / ~68 external"偏低 69%/63%，但 owner doc 文字本身已声明"待 codegen 后跑脚本精确统一"——本审计即该脚本。
- **裁决**：PASS。DAG 零循环、零禁止方向、外部实体声明 100% 完整、零 P0。F1–F9 全部已闭包确认（不重开）。
- **P1 发现（3 项登记 MR1）**：
  - P1-MA1-015：owner doc §5.6.2 数值偏差（待用脚本核验值更新）
  - P1-MA1-016：finance `ErpFinAccountingPeriodProcessor.reverseDepreciation` 用 `daoProvider.daoFor(ErpAstDepreciationSchedule.class)` 跨域 DAO 查询，违反"跨域查询必须经 I*Biz 接口"规则
  - P1-MA1-017：owner doc §3.2/§4.4 "finance 纯读不回写"规则不完整（未覆盖期末结账 command 编排）
- **P2 观察项（5 项 watch-only）**：fin phantom `ErpAstAsset` 声明 / inv §5.6.2 行缺 Employee+Partner / voucher_bill_r 字段名 doc vs 实现偏差 / finance→assets 文字 vs 实现 / L1 冗余字段策略偏差（机制 B 单独使用合法）
- **roadmap 同步**：`docs/backlog/audit-remediation-roadmap.md` MA1 A1.10 Status `todo` → `done`。scope matrix §2.1 "跨模块依赖/DAG" 行全域覆盖：finance `⚠️(P1)` + 其余 18 域 `✅`。
- **独立 closure audit**：子代理 `ses_05e174434ffe44l4QlGBZtmElB`（general，新会话）VERDICT = PASS-WITH-NOTE。2 项初始 cosmetic 笔误（+75%→+69%；25 DAG 边→24 DAG 边）已按建议修正。
- **验证（baseline-confirm-skip-justified）**：审计零代码变更（git status 100% docs/），Maven reactor 输出与 M0.3 基线（HEAD=0e963531d，154 模块 BUILD SUCCESS，1756 单元测试 0 failures）完全一致；按 roadmap §其他纪律（"审计 plan 不改代码，BUILD_VERIFY 跑全量 mvn test 会浪费 ~20min/次"）+ plan §Closure Gates 注 BUILD_VERIFY 跳过合理。脚本可重跑（`python3 docs/audits/scripts/cross-module-dep-extract.py` exit=0）作可复现性证据。
- **Skill**：`cross-module-dependency-audit-prompt.md`（roadmap A1.10 指定此 skill，7 维度跨模块 DAG 审计专用方法）。

### 2026-07-27（审计-修复 MA1 ORM 模型审计批 A1.1–A1.9 — plan 1015-2，audit-only）

- **任务**：执行 `docs/plans/2026-07-27-1015-2-audit-remediation-ma1-orm-model-audit.md` 全 4 Phase。MA1 ORM 审计批是 audit-remediation-roadmap 第 2 个里程碑的工作项 A1.1–A1.9，按 `orm-model-audit-prompt.md` 9 维度（类型规范 / 长度精度 / 字典设计 / 标准字段完整性 / 业务字段完整性 / 关系设计 / 跨模块引用一致性 / 命名一致性 / 需求覆盖）对全 19 域 ORM 做系统性审计。audit-only 计划：零生产代码变更（仅文档：3 份审计报告 + arm-index + scope matrix + roadmap + 计划 + 日志）。
- **审计方法**：Python 自动化扫描（`orm_audit.py`，641 行）覆盖 19 域 20733 行 ORM 全量；按 9 维度逐列核查；输出 per-domain JSON + aggregate + tier-summary。人工抽样核实三类高信号维度（type_spec / cross_module_ref / naming）的 false positive 与 false negative。
- **Phase 1 S 级（finance/mfg/hr，131 实体）**：finance 9 维度全通过；mfg 7 major（propId 缺失，D3/D4 多币种补字段未重编号）+ 4 minor；hr 23 minor（DECIMAL↔decimal 配对，裁决保留 decimal——物理量/系数字段参与数值计算）。产出 `docs/audits/2026-07-27-1015-arm-ma1-s-tier-orm.md`。
- **Phase 2 A 级（pur+sal+assets+inv+crm+qa+prj，195 实体）**：purchase/sales/inventory 9 维度全通过；assets 29 major（propId 缺失）；crm 7 major（DECIMAL↔double 类型偏离——浮点精度损失，影响前端展示）；quality 1 major（propId 缺失）；projects 5 major（propId 缺失）。共 42 P1。产出 `docs/audits/2026-07-27-1015-arm-ma1-a-tier-orm.md`。
- **Phase 3 B+C 级（md+cs+ct+b2b+mnt+drp+aps+log+notify，166 实体）**：master-data 1 false-positive（ErpSysConfig §19.2 已登记命名异常）；cs/contract/b2b/aps/logistics/notify 9 维度全通过（notify 49 列 0 finding，全域最干净子系统）；maintenance 5 major（propId 缺失）；drp 4 major（ErpInvDrp* 命名异常——className+tableName 前缀混用 Inv+Drp）。共 9 P1。产出 `docs/audits/2026-07-27-1015-arm-ma1-bc-tier-orm.md`。
- **Phase 4 P0/P1 处理**：全域 0 P0 blocker，无即时通道触发。51 项 P1 全部登记 arm-index §P1 汇总（去重后实际工作项：propId 缺失 46 列 + crm DECIMAL↔double 7 列 + drp 命名异常 4 实体），待 R1.0 展开机制转化为 MR1 具体修复工作项行。scope matrix §3.2 "daoFor Type 4 设计边界错误" 标记 **MA1 已覆盖**（ORM dim 7 跨模块引用一致性 0 blocker，daoFor Type 4 在 ORM 层无残留；下游调用层归 MA4 A4.5）。
- **关键发现**：
  - (1) **propId 缺失 46 列**：D3/D4 多币种四件套补字段时未触发 propId 重编号，集中在 assets/mfg/projects/maintenance/quality。codegen 不强制 propId 连续，但 delta-merge 依赖 propId 作稳定锚点。MR1 修复方式：codegen 增量再生自动补全。
  - (2) **crm DECIMAL↔double 7 列**：ForecastAccuracy/PriceRule/LeadFunnel/FunnelStageMetrics 比率字段使用 stdDataType=double（IEEE 754 浮点）。这是唯一影响业务正确性的 P1（其他 propId 不影响）。double 序列化导致 0.1+0.2=0.30000000000000004 类偏差。
  - (3) **drp ErpInvDrp* 4 实体命名异常**：className=`ErpInvDrp*`+tableName=`erp_inv_drp_*` 不符合 §19.1 单一前缀规范。审计建议重命名为 `ErpDrp*`/`erp_drp_*`（语义完全属 drp 域，与 inventory 无写耦合）。
  - (4) **D1 字典 int→string 已全域无残留**：前序 D1 裁决 Deferred 成果保持，新域字典全部使用 string valueType。
  - (5) **机制 B（notGenCode 外部实体）全覆盖**：19 域跨模块引用经 notGenCode 声明 + to-one + join 范式落地，零 blocker。
- **roadmap 同步**：`docs/backlog/audit-remediation-roadmap.md` MA1 A1.1–A1.9 Status `todo` → `done`（9 工作项）。scope matrix §2.1 "ORM 模型规范" 行全域覆盖：10 ✅ + 9 ⚠️（待 MR1）+ 0 ❓ + 0 blocker。
- **验证（full-green baseline confirm）**：审计不改代码，build/test 门控为回归基线确认。`mvn clean install -DskipTests` 154 模块 BUILD SUCCESS（01:35 min，2026-07-27 11:43 落锚）；`mvn test` BUILD SUCCESS（0 failures/0 errors/1 skipped=已知 `ErpAllWebPagesCollectTest`，08:39 min，2026-07-27 11:52 落锚）。VERIFY 步骤复测：`mvn clean install -DskipTests` BUILD SUCCESS（154 模块，01:37 min，12:08）+ `mvn test` BUILD SUCCESS（0 failures/0 errors/1 skipped，08:49 min，12:16），full-green 保持。
- **Skill**：`orm-model-audit-prompt.md`（roadmap A1.1–A1.9 全部指定此 skill，机械 ORM 审计 9 维度）。

### 2026-07-27（审计-修复 M0 编排基线实测落锚 — plan 1015-1，verification-only）

- **任务**：执行 `docs/plans/2026-07-27-1015-1-audit-remediation-m0-baseline.md` 全 2 Phase。M0 是 audit-remediation-roadmap 第 1 个里程碑，是 MA1-MA7 审计工作项的前置依赖。三件交付物（0.1 scope matrix / 0.2 arm-index / 0.3 compliance+build/test 绿色基线）状态推进 `todo` → `done` 解锁 MA1-MA7。verification-only 计划：除对 0.1/0.2 文件补缺外零生产代码变更。
- **Phase 1 独立 closure audit**：0.1 scope matrix 7 核实点全过——实体计数抽样核对（finance=48 / hr=42 / master-data=25 / notify=3 全部精确匹配 `grep -c '<entity '`）；mutation/query 抽样（notify=6/6、hr=92/35 精确匹配）；F1-F9 闭包证据 6 plan 文件全部存在；3.2 残留风险 12 项全有严重性+处理去向。0.2 arm-index 4 张表结构健全、归档纪律 5 条与 roadmap §报告归档纪律一致。**就地补缺 2 处**：(1) scope matrix §1.2 增加 aps 特殊处理裁决注记（aps mut=19 略超 C 级 mut<15 阈值，但作为全域最小域之一并入 C 级合并审计）；(2) scope matrix §2.5 修正 v2 新增维度工作项编号（原误标 A2.16/A2.17，roadmap 实际为 A2.17/A2.18，因 A2.16 是预算与承付正确性）。
- **Phase 2 实测落锚**：HEAD=`0e963531d4b07d44b593828a7aab048ea0c9d3db`。(a) compliance checker `bash docs/audits/nop-compliance-checker.sh` 全 19 规则 actual ≤ baseline（精确匹配，0 漂移：R1a/R1b/R1c=0/0/0、R1d=17、R2a=37、R2b=315、R2c=1228、R2d=28、R3=5、R4=0、R5=0、R6=2、R7=0、R8=42、R10=6、R11=0、R12a=69、R12b=66、R12c=38）；(b) `mvn clean install -DskipTests` BUILD SUCCESS（156 reactor 模块，0 errors，01:42 min）；(c) `mvn test` BUILD SUCCESS（**0 failures / 0 errors / 1 skipped**=已知 `ErpAllWebPagesCollectTest` `@Disabled`，1756 单元测试方法，09:29 min）。
- **锚点登记**：`docs/audits/compliance-baseline.md` 新增 §M0 锚点注记段（HEAD + 规则快照表 + 测试计数说明）；`docs/audits/arm-index.md` front matter 新增交叉引用行指向锚点段。
- **文档计数漂移**：roadmap §框架/平台复用 + §当前基线 的「~2890 测试」与实测 1756 不符（起草期粗估），归 G.4 文案修正处理（非 CI red，不阻塞 M0 闭包）。
- **roadmap 同步**：`docs/backlog/audit-remediation-roadmap.md` §M0 三工作项 Status `todo` → `done`，Work Item Details §M0 段更新。
- **验证（full-green）**：compliance checker 19 规则全 actual ≤ baseline + `mvn clean install -DskipTests` 156 模块 BUILD SUCCESS + `mvn test` 0 failures/0 errors/1 skipped。零生产代码变更（仅文档：scope matrix / arm-index / compliance-baseline / roadmap / 计划 / 日志）。
- **Skill**：none（M0 是基线确认 verification-only，不触发任何审计 skill；compliance-checker 与 mvn 是工具而非 skill）。

### 2026-07-27（审计-修复 roadmap + mission 编写 — 文档产物，非代码变更）

- **任务**：为 nop-app-erp 设计可由 Mission Driver 自主执行的全面审计-修复 roadmap。项目已多次审计但体量大（19 域、154 模块），需结构化的流水线编排确保 P0/P1 彻底修复。
- **复杂度数据采集**：实测 19 域 9 维度复杂度指标（实体数/mutation/query/Java文件/Processor/状态机实体/测试/view.xml/跨域daoFor）。分级结果：S 级（finance 48实体/137mut/331java、manufacturing 41/74/246、hr 42/92/127）；A 级 10 域；B 级 3 域；C 级 3 域（aps/logistics/notify）。hr 测试/mutation 比 0.16 为全域最低。
- **提示词编写**：`docs/skills/audit-remediation-roadmap-authoring-prompt.md`（647 行）。核心设计：(1) 流水线模式 P0 即时止血 + P1 维度内批量修复；(2) 复杂度分析驱动工作项拆分（S 级按功能模块拆 / C 级合并）；(3) 审计报告 `arm-` 前缀归档规范 + arm-index.md 索引机制；(4) ORM 变更已授权（改模型源 + 重新生成，仍禁手编生成产物）。
- **产物**：
  - `docs/skills/audit-remediation-roadmap-authoring-prompt.md`（元提示词，647 行）
  - `docs/audits/audit-remediation-scope-and-dimension-matrix.md`（M0 核心交付物：复杂度矩阵 + 维度×域覆盖矩阵 + 未闭包发现清单）
  - `docs/audits/arm-index.md`（审计报告索引骨架）
  - `docs/backlog/audit-remediation-roadmap.md`（主产物：14 里程碑 + 68 工作项，执行中扩展至 80-100+）
  - `missions/audit-remediation.json`（Mission Driver 配置）
- **里程碑结构**：M0 基线 → MA1-MA7 分维度审计（52 工作项，S 级域功能模块拆分）→ MR1-MR3 P1 批量修复（流水线：MA1+MA2→MR1 等）→ MR4 跨维度裁决 → MV 全量验证 → MG guard 沉淀。P0 走即时通道不进入批量修复。
- **验证**：无代码变更，无需 mvn 验证。mission.json JSON 有效性校验通过。roadmap 工作项粒度自检通过（S 级域已按功能模块拆分、C 级域已合并）。
- **Skill**：`audit-remediation-roadmap-authoring-prompt.md`（本次新建的元提示词本身）。

### 2026-07-27（R1d/R10/R6 合规基线裁决 + checker 注释校准 — plan 0823-1）

- **任务**：执行 `docs/plans/2026-07-27-0823-1-r1d-r10-r6-compliance-baseline-adjudication.md` 全 3 Phase。R1d baseline=23 / actual=28 → **CI red +5 漂移**；R10/R6 grandfathered 基线（51/7）为注释虚假命中主导。三规则 checker 原始 grep 不区分代码行与 javadoc/注释行，与 R8 content-based 排除先例（1057-1/1057-2）同型缺陷。纯合规治理（无 ORM/契约/字典/Java 业务代码变更）。
- **Phase 1 Explore**：三态分类（A 真实代码 / B javadoc `*` / C 行注释 `//`）落盘——R10 51=A6+B41+C4（真实 6 = ErpFinVoucherBizModel post/reverse `@Transactional(REQUIRES_NEW)` ×2 + ErpFinPostingExceptionRecorder/ErpFinDeferredPostingRetryHelper `runInTransaction(...REQUIRES_NEW...)` ×4）/ R6 7=A2+B3+C2（真实 2 = R10 BizModel 子集）/ R1d 28=A17+B9+C2（真实 17 = crm 6 + master-data 8 + sales 3，全部同域只读内部辅助查询绕过 findList objMeta 管道）。R1d +5 漂移源 `git diff bd8540037..HEAD` 核实**全部来自 0315-1**（3 真实调用 + 2 javadoc，sales 域），master-data（2225-1/1206-1）均早于基线锚点已计入 baseline 23。Decision 1=行级 grep 注释排除校准（option b per-rule）；Decision 2=基线裁决 R1d 23→17 / R10 51→6 / R6 7→2。
- **Phase 2 实施**：`docs/audits/nop-compliance-checker.sh` R1d/R6/R10 三段 grep 管道追加 `grep -vE ':[0-9]+:[[:space:]]*(\*|//|/\*|\*/)' | grep -vE '\{@code|\{@link'`（per-rule，不动 `rgrep_bizmodel`/`rgrep_prodjava` helper，避免影响已稳定规则）；`docs/audits/compliance-baseline.md` 基线表 + machine-readable YAML 块同步（R1d=17/R6=2/R10=6）+ §R1d/R10/R6 注释校准+基线裁决注记；`docs/analysis/governed-path-cost-evaluation.md` §3.8 checker 校准范式矩阵（0941-2 R3 / 1057-1 R8 / 1057-2 R8 / 0823-1 R1d/R6/R10 四先例）。
- **Phase 3 验证**：`bash docs/audits/nop-compliance-checker.sh` R1d=17 / R6=2 / R10=6（注释校准后精确对齐 A 子集，其余 13 规则不变）；`mvn clean install -DskipTests` 154 模块 BUILD SUCCESS（01:35 min，exit 0）；CI workflow baseline gate python 模拟 `baseline rules: 19 | parsed actual rules: 19 → OK: no rule exceeds baseline. Gate PASSED.`（exit 0），R1d CI red 解除。
- **验证（full-green）**：154 模块 BUILD SUCCESS + checker 全 16 规则 actual ≤ baseline（CI gate 等价验证通过）+ 独立结束审计 verdict `pass`（独立 general 子代理新会话冷重播，1 Minor 非阻塞：预存「16 规则」表头措辞实际 19 规则）。
- **Skill**：`nop-backend-dev`（合规路径评估 + 跨实体 daoFor 收敛 + 事务边界 + checker 校准，对齐 0941-1/0941-2/1057-1 同型基线/校准计划）。

### 2026-07-27（doc-status 共享字典统一 6 域 ext:dict + logistics stale 清理 — plan 0300-1）

- **任务**：执行 `docs/plans/2026-07-26-0300-1-doc-status-shared-dict-unification.md` 全 3 Phase。F2（HIGH）闭包项 #3 doc-status 子项：6 域（purchase/sales/assets/quality/maintenance/cs）28 列 ORM `ext:dict="erp-<domain>/doc-status"` 碎片化 + 6 份字节级重复 per-domain YAML + logistics stale 残留（内联 ORM dict + YAML + en i18n label）；统一为项目级共享 dict `erp/doc-status`（DRAFT/ACTIVE/CANCELLED），镜像 inventory 1600-1 approve-status 统一先例。
- **Phase 1**：共享 dict 选址裁决=**候选 (c)** `module-common-service/src/main/resources/_vfs/dict/erp/doc-status.dict.yaml`——候选 (a) `app-erp-all` 否决（pom 依赖方向：app-erp-all 位于依赖树顶端依赖所有 `*-web` 模块，`mvn test -pl erp-pur-service -am` 的 `-am` 不含 app-erp-all，resources 不在单模块测试 classpath）；候选 (c) 成立（6 域 service pom 均已声明 `app-erp-common-service` 依赖，dict 文件置于 common-service resources 自然进入所有 6 域测试 + runtime classpath）+ logistics stale 全清（内联 ORM `<dict name="erp-log/doc-status">` + `doc-status.dict.yaml` + en i18n label 残留，含 iteration 3 Minor 闭环）+ `mvn clean install -DskipTests` 154 模块 BUILD SUCCESS + `mvn test -pl module-purchase/erp-pur-service -am` 单模块 dict 可达性验证 BUILD SUCCESS。
- **Phase 2**：6 域 28 列 `ext:dict` → `erp/doc-status`（8+7+6+4+2+1=28）+ **6 处内联 `<dict name="erp-<short>/doc-status">` 移除**（关键发现：plan 原文仅列 ext:dict 变更 + YAML 删除，但若内联 dict 保留 codegen 会重新生成已删 YAML——镜像 inventory 1600-1 先例必须同时移除内联 dict）+ 6 份 per-domain YAML 删除 + **`ErpMntDaoConstants extends _ErpMntDaoConstants, ErpMntDocStatus` 兼容链修复**（关键发现：移除内联 dict 后 codegen 从 `_ErpMntDaoConstants` 移除 DOC_STATUS_* 常量，maintenance 有 13 处 `ErpMntDaoConstants.DOC_STATUS_*` 直接引用——若不加 extends 会编译失败；其余 5 域 Java 代码直接引用 `Erp*DocStatus`（手写接口）不受影响）+ `mvn clean install -DskipTests` 154 模块 BUILD SUCCESS（codegen 增量：`_app.orm.xml` 28 列 `erp/doc-status` + 0 stale；`_Erp*DaoConstants.java` DOC_STATUS_* 已移除；per-domain YAML 未重生成）。
- **Phase 3**：6 域 + logistics 7 service `mvn test` BUILD SUCCESS（0 failures，02:51 min）+ checker R3=5 不变零回归（本计划 0 BizModel/service Java 变更，git diff 确认）+ F2 闭包项 #3 🔶→✅（doc-status RELEASED + cs `time-entry-approve-status` 永久裁决特化 Successor: no）+ 0930-2 §Deferred RELEASED 标注 + compliance-baseline R3 同步注记。
- **验证（full-green）**：`mvn clean install -DskipTests` 154 模块 BUILD SUCCESS（01:31 min）+ `mvn test -pl module-{purchase,sales,assets,quality,maintenance,cs}/erp-*-service,module-logistics/erp-log-service` BUILD SUCCESS（0 failures）+ `bash docs/audits/nop-compliance-checker.sh` R3=5 不变。R1d=28 vs baseline=23 漂移经 git diff 确认非本计划引入（本计划 0 BizModel Java 文件变更），属前序未同步基线计划遗留。
- **Skill**：`nop-backend-dev`（ORM ext:dict 变更 + codegen 增量重新生成 + Java 常量兼容链 extends 修复，对齐 inventory 1600-1 同型 ext:dict 统一先例）。

### 2026-07-27（业务模块元数据诊断 4 @BizQuery 浏览器层 E2E — plan 0930-1）

- **任务**：执行 `docs/plans/2026-07-27-0930-1-module-metadata-diagnostic-browser-e2e.md` 全 3 Phase。D2 业务模块元数据 BT5-style 诊断 `ErpModuleMetaBizModel` 4 `@BizQuery`（`listModules`/`getModule`/`checkDependencyIntegrity`/`listOptionalFeatures`，非实体 BizModel 经 `app-service.beans.xml` 显式注册）经 JUnit 单层验证（`TestModuleMetaReader` 7 场景全绿）但**零浏览器层 E2E**；本计划补全栈验证（纯测试 + 文档，零生产代码变更），收口 deepening-roadmap 全 11 项浏览器层 E2E 覆盖里程碑（D2 是唯一后端代码已落地但零浏览器层 E2E 的项）。
- **Phase 1 Explore**：核实 4 `@BizQuery` GraphQL 入口名/入参/返回选择集可达性（`ErpModuleMetaBizModel.java:31-49`）+ `ModuleMetaBean`/`DependencyIntegrityResult`/`ModuleFeature` 字段集（`defaultValue` 为 `Object` scalar，GraphQL 序列化为 JSON 值）+ seed 派生确定性期望值表（全 19 域 `precompile/module-meta.yaml` 统一 version=1.0.0；purchase 6 businessDependencies + 1 optionalFeature 样本；master-data/notify 省 businessDependencies；aps/logistics/b2b 省 optionalFeatures）+ bean 注册经 `app-service.beans.xml:9`。**裁决**：(a) `listModules()` 返回 classpath 全部启用模块（含平台 nop/*），spec 经 `erp/` 前缀过滤后精确 == 19（收紧 JUnit `>= 19`）；(b) `getModule` 三路径覆盖 erp/md(DAG 根 businessDependencies=null) + erp/pur(6 deps) + erp/nonexistent(返回 null 容忍)；(c) 无 setup/cleanup（运行时 codegen 产物非 DB 行，非实体 BizModel 无 `__save` 入口）；(d) 无 config-gate。
- **Phase 2 spec 实现**：新建 `tests/e2e/business-actions/all-module-meta-diagnostic.action.spec.ts`（1 用例 6 场景全绿，7.2s）：(1) listModules `erp/*` 前缀 == 19 + 全部 version="1.0.0" + 含 erp/md(businessDependencies=null) + erp/pur(6 deps + 1 feature)；(2) getModule(erp/md) 非 null + businessDependencies=null；(3) getModule(erp/pur) 6 deps 含 {erp/md,1.0.0} + supplier-scorecard-red-gate；(4) getModule(erp/nonexistent) 返回 null；(5) checkDependencyIntegrity ok=true + missing=[] + mismatches=[]；(6) listOptionalFeatures 含 `{supplier-scorecard-red-gate, erp-pur.scorecard-prevent-on-red, defaultValue:true}`。**GraphQL selection set 构造**：4 `@BizQuery` 全返回复杂类型，`callQuery` 不带 selection set → 经 `new GraphQLClient(page).raw()` 内联完整 query + selection set（镜像 1500-2 md-party-query + cs-canned-response/fin-reconciliation 范式）。
- **Phase 3 owner doc 回链 + e2e-runbook + roadmap**：`docs/architecture/business-module-metadata.md` 新增 §4.4「浏览器层验证」实现注记（4 `@BizQuery` 全栈可达 + seed 派生断言 + 无 config-gate + 无 setup/cleanup + selection set 范式 + 负路径归 JUnit 单层）；`docs/testing/e2e-runbook.md` 业务动作表新增 app-erp-all 模块元数据诊断行 + spec 计数 112→113（按仓库实测对齐）；`docs/backlog/deepening-roadmap.md` §8.8 D2 增「浏览器层验证」bullet（镜像 §8.2 C1 / §8.5 D1 / §8.6 C3 / §8.9 A3 范式）。
- **验证（full-green）**：新 spec `all-module-meta-diagnostic.action.spec.ts` 1 passed (7.2s) + finance 抽样回归 `finance-voucher-post.action.spec.ts` 1 passed (7.2s)（GraphQL schema 无漂移）→ 零生产代码变更（仅 .ts + .md）不污染共享 DB 基线。全 workspace `mvn install -DskipTests` BUILD SUCCESS（154 模块）。**已知环境抖动**：首次运行命中冷启动 `fetchMenuConfig` 500 瞬态（app 刚启动 menu 预热期），app 预热后重跑全绿；同实例 `md-party-query.action.spec.ts`（同型先例）并行全绿佐证非本 spec 问题。
- **Skill**：`nop-testing`（Playwright 浏览器层 E2E + 既有 business-actions/_helper `GraphQLClient.raw` 读路径复用 + 非实体 BizModel `@BizQuery` 全栈可达性 + 运行时 codegen 产物非 DB 行无 setup/cleanup，对齐 1500-2 同型 @BizQuery 先例）。
