# Development Log - 2026-06-23

### 2026-06-23 (9 域业务菜单 + biz:moduleId + 看板占位 + 功能完整性分析)

- **触发**:采购菜单样板验证通过后,复制到其余 9 域,并对照 erp-survey 验证功能完整、设计合理。
- **调研**:用 2 个子代理提取 erp-survey 中全部 10 域(含 manufacturing/assets/maintenance/quality/sales/inventory/finance/projects/master-data)在 ERPNext/Odoo/赤龙/Atlas/iDempiere 等的菜单组织。结论:ERPNext 粗模块+doctype 分组 + 赤龙业务流分段最贴近本项目;主数据集中化是国内 ERP 范式(赤龙/星云)。
- **菜单设计**(参照 ERPNext/赤龙业务流分组 + 隐藏 Line):9 域共 50 分组 + 9 看板 = 59 分组,96 单据菜单项 + 9 看板。每域删测试 TOPM + 新增业务 TOPM + 业务分组。模块根图标沿用上批 ORM ext:icon(md=database/sal=receipt/inv=warehouse/fin=landmark/ast=truck/prj=briefcase/mfg=factory/qa=shield-check/mnt=wrench)。
- **跨域 picker 修复(9 域 biz:moduleId)**:sales(10)/inventory(8)/finance(9+ast+prj)/assets(6)/projects(5)/manufacturing(8)/maintenance(7)/quality(5)外部实体引用加 biz:moduleId 指向 owner 域,根 `<orm>` 声明 xmlns:biz。finance 额外跨 ast(资产)/prj(项目)。
- **跨域 app 依赖**:9 域 app pom 加 owner 域 web 依赖(finance 加 master-data/assets/projects 三个),让 VFS 访问跨域 picker 页面。
- **看板占位页面**:9 域各 1 个 dashboard 占位 page.yaml(type:page + alert"待实现"),挂到菜单"报表看板"分组(orderNo=999)。后续逐步细化真实看板。
- **执行**:Python 脚本批量生成 ORM 补丁 + action-auth + 看板页面(幂等)。过程中发现 action-auth 的 `&` 未转义(Warehouse & Org 等)导致 XML 解析失败,4 域修正为 `&amp;`。
- **验证**:`mvn clean install -DskipTests` → **BUILD SUCCESS**(81 模块)。抽检 9 域 TOPM 图标语义化、看板分组齐全、Line 全隐藏、中英文+icon 完整。
- **功能完整性分析**:对照 erp-survey 输出 `docs/analysis/2026-06-23-0003-menu-and-feature-completeness.md`。结论:10 域 145 实体 51 分组 105 菜单项覆盖主流 ERP 核心能力,无重大缺口。3 个可增强点(projects 调研深化/Calibration 归属/看板细化)+ 1 个层级取舍(三级 vs 两级)均非阻塞。
- **改动**:9 域 ORM(biz:moduleId)+ 9 域 app pom + 9 域 action-auth + 9 看板占位 + 生成产物 + 分析报告。
- **下一步**:① 看板占位逐步细化为真实报表 ② projects 域补 ERPNext 源码级调研 ③ Calibration 跨域归属明确。

### 2026-06-23 (采购业务菜单样板 + 跨域 picker 修复 + docs-for-ai 补充)

- **触发**:用户要求删除自动生成的测试菜单、新增按业务拟制的真实菜单(中英文+icon 正确),并要求菜单基于真实 ERP 惯例组织。
- **调研**:用 3 个子代理提取 erp-survey 中 Odoo/ERPNext/iDempiere/Metasfresh/OfBiz/Tryton/若依 的菜单组织。结论:ERPNext 的 buying 域按业务阶段分组(询价/报价→订单→入库→结算)最贴近本场景;国内若依是单据类型平铺;ADempiere 是主数据/交易/查询三分。用户选定 **ERPNext 风格(业务阶段分组)+ 隐藏 Line 实体**。
- **采购菜单设计**(手写层 `erp-pur.action-auth.xml`):
  - 删除测试 TOPM `test-orm-erp-pur`(`x:override="remove"`)
  - 新增业务 TOPM「采购管理」(shopping-cart)+ 5 个业务分组(SUBM):采购寻源(search)/采购订单/采购入库(package-plus)/采购结算(receipt)/采购退货(undo-2)
  - 8 个单据页面(17 个实体中隐藏 9 个 Line,从头单据下钻进入)
  - 每项含中文 displayName + 英文 i18n-en:displayName + Lucide icon
- **跨域 picker 阻塞发现与修复**(blocker):
  - 启动报 `ErpMdPartner/picker.page.yaml` 不存在。排查发现:外部实体引用(`<entity notGenCode="true">`)未声明 `biz:moduleId`,导致 codegen 生成的 xmeta to-one prop 该属性为空,运行时 picker 路径回退到消费方域 `/erp/pur/`(应为 `/erp/md/`)。
  - **根因机制**(查 codegen 模板 `_{shortName}.xmeta.xgen:108` `biz:moduleId="${rel.refEntityModel['biz:moduleId']}"` + 运行时 `XuiHelper.getRelationPickerUrl` 三层优先级):`biz:moduleId` 是多模块跨域引用必须显式设置的属性。
  - **修复**:purchase ORM 10 个外部实体加 `biz:moduleId="erp/md"` + 根 `<orm>` 声明 `xmlns:biz="biz"`。修复后 xmeta 的 to-one prop 正确带上 biz:moduleId,picker 指向 `/erp/md/`。
- **配置修复**:application.yaml 对齐 nop-auth-app 标准(缺省账户密码更正为 nop/123);erp-pur-app pom 加 master-data-web 依赖(跨域页面资源访问)。
- **验证**(关键):debug 模式启动 → SiteMapProvider init 触发菜单合并 → dump 到 `_dump/nop-app/nop/main/site/zh-CN-menu.yaml`。菜单结构完全符合设计:测试 TOPM 已删,5 分组 8 菜单项,图标语义化,Line 隐藏。
- **docs-for-ai 补充 + nop-entropy 改进**(提交在 nop-entropy master `738abf302`):
  - `model-first-development.md` 新增「跨模块实体引用与 biz:moduleId 约定」章节(是什么/取值格式/如何影响 picker/为什么多模块必设/正确写法)
  - `SiteMapProviderImpl.init()` 增加 debug 预加载(debug 模式主动 siteCache.get 触发合并+dump,此前懒加载无从触发)
- **docs-for-ai 完整性排查**:核查菜单/权限/调试/多模块四大领域,发现 P0 缺口(菜单诊断链路 `*-menu.yaml` dump 未文档化、`static-config-path` 配置未文档化)和 P1 缺口(菜单定制 how-to、跨域页面依赖、多模块工程结构)。详见对话,待后续补充。
- **改动**:nop-app-erp `f0d6260`(20 文件)+ nop-entropy `738abf302`(2 文件)。
- **下一步**:① 补 docs-for-ai 的 P0/P1 缺口 ② 采购样板复制到其余 9 域。

### 2026-06-23 (菜单图标 + 英文标题:10 域 ORM 补 ext:icon + i18n-en:displayName)

- **触发**:用户要求每个菜单项配合适图标并含英文标题。调研发现当前生成的 `_*.action-auth.xml` 中 TOPM 图标是默认 `ion:grid-outline`、SUBM 全是通用 `blocks`、`i18n-en:displayName` 全空。根因:10 个 ORM 源模型既无 `ext:icon` 也无 `i18n-en:displayName`。
- **方案(方案 A,已批准)**:在 ORM 模型补齐,重新 codegen 让下游(action-auth.xml、i18n/en yaml、xmeta)自动同步。符合 Nop "Model → Delta → Java" 决策顺序,单一真相源。中文 `displayName` 已存在(实体跨行属性,无需补);只补 `ext:icon`(模块根 TOPM + 每实体 SUBM)和 `i18n-en:displayName`(每实体英文标题)。
- **技术依据**:`nop-entropy/docs-for-ai/02-core-guides/model-first-development.md:47-92` — ORM 根 `<orm ext:icon>` → TOPM(经 module-meta.json)、实体 `<entity ext:icon>` → SUBM;`i18n-en:displayName` 是 XDSL 扩展命名空间属性,根标签需声明 `xmlns:i18n-en="i18n-en"`,codegen 模板自动回填并拼接 FNPT 英文(Query/Modify + 实体名)。
- **改动**(10 个 ORM 源文件,145 个本域实体):
  - 每域根 `<orm>` 加 `ext:icon="<模块图标>"` + `xmlns:i18n-en="i18n-en"`
  - 每个本域实体在 `displayName` 行注入 `ext:icon="<Lucide图标>"` + `i18n-en:displayName="<English>"`
  - 外部实体引用(`<entity notGenCode="true">`)不动(不生成菜单,图标由 owner 域负责)
  - 图标用 Lucide kebab-case 裸名,按业务语义匹配(采购订单→shopping-cart、销售→receipt、库存→warehouse、财务→landmark、资产→truck、项目→briefcase、制造→factory、质量→shield-check、维护→wrench、主数据→database)
- **执行**:Python 脚本幂等注入(`/tmp/patch_orm_*.py`),每域校验注入数 = 实体数,notGenCode 误改数 = 0。
- **验证**:`mvn clean install -DskipTests` → **BUILD SUCCESS**(81 模块,48s)。产物全量校验:
  - 10 域 TOPM 图标全部正确(database/shopping-cart/receipt/warehouse/landmark/truck/briefcase/factory/shield-check/wrench),无 `ion:grid-outline` 残留(0 个)
  - 145 个 SUBM 图标全部语义化,默认 `blocks` 残留 = 0
  - `i18n-en:displayName=""` 空值 = 0;FNPT 自动拼接英文(Query/Modify + 实体名)生效
  - `i18n/en/_erp-pur.i18n.yaml` 等英文 `null` 全部被填充(Purchase Order 等)
- **发现(自检)**:TOPM 图标来自 `_module-meta.json`(由 meta 模块从 ORM 根 ext:icon 生成),web 模板经 `loadDeltaJson` 读取。曾用 awk 误判 TOPM 图标(脚本错抓下一 SUBM 的 icon),后用正确多行读取确认全部正确。
- **下一步**:每域手写层② `erp-{module}.action-auth.xml` 可按需用 `x:extends` + `x:override="remove"` 精简自动生成的菜单(如隐藏明细行实体的独立菜单项)。

### 2026-06-23 (修复 mvn install 编译错误，打通全域 codegen→构建链路)

- **触发**：`mvn install` 在 `module-master-data/erp-md-codegen/postcompile/gen-orm.xgen` 失败，报 `parse-missing-resource`：待解析资源 `erp-md.orm.xml` 不存在。
- **根因 1（命名不一致）**：实际模型文件名为 `app-erp-<domain>.orm.xml`（被 `docs/design/`、`docs/context/codebase-map.md`、AGENTS.md 数十处引用为权威真相源），但 10 个 codegen 脚本里写的是短缩写名 `erp-XX.orm.xml`。
  - **修复**：改 10 个 `gen-orm.xgen` 的 `renderModel` 第一参数指向真实文件名（`module-*/erp-*-codegen/postcompile/gen-orm.xgen`），保留与 AGENTS.md "权威模型源是 `app-erp-<domain>.orm.xml`" 的约定一致，**不反向重命名 ORM 文件**（那会破坏文档真相源）。
- **根因 2（ORM 重复 to-one 关系）**：codegen 进入模型校验后报 `multiple-node-has-same-unique-attr-value`。系统扫描发现 12 个实体的 `<relations>` 内存在"机制 B 段"复制粘贴重复。逐个去重，仅删除真正重复节点、保留新增关系：
  - inventory（StockLedger/StockBalance）、purchase（Order/OrderLine）、sales（Order/OrderLine）、finance（VoucherLine）、manufacturing（BomLine/WorkOrder）、quality（Inspection）= 纯重复删除。
  - **assets ErpAstAsset、maintenance ErpMntEquipment 的 `category` 是同名不同实体的真冲突**：第一段指向本域分类（`ErpAstAssetCategory`/`ErpMntEquipmentCategory`，业务正确），机制 B 段误指向 `ErpMdMaterialCategory`。按业务语义保留本域分类、删除错误的 MaterialCategory 引用。
- **根因 3（关系名撞字段名）**：purchase/sales 的 `ErpXxxOrderLine` 既有 `taxRate` 字段（税率百分比快照）又有 `taxRate` to-one 关系（→ `ErpMdTaxRate`），报 `duplicate-prop`。关系改名为 `taxRateMd`（"税率主数据"），保留关系语义并消除冲突。
- **根因 4（dao 模块缺跨域依赖）**：生成的 `_gen` 代码全限定名引用 `app.erp.md.dao.entity.ErpMdXxx`，但 9 个业务域 dao 模块 pom 未声明 `app-erp-master-data-dao` 依赖，报"程序包不存在"。
  - **修复**：9 个 dao pom 添加 `app-erp-master-data-dao` 依赖；finance-dao 额外加 `app-erp-projects-dao`（`ErpFinVoucherLine.project → ErpPrjProject`）。Maven reactor 自动按依赖拓扑重排构建顺序。
- **验证**：`mvn clean install -DskipTests` → **BUILD SUCCESS**，81 个模块（10 域 × 8 模块链 + 根）全部通过。这是项目首次完成全域 codegen + 构建闭环，标志着从 bootstrap/预代码生成阶段进入可构建阶段。
- **改动文件**：
  - codegen 脚本 ×10：`module-*/erp-*-codegen/postcompile/gen-orm.xgen`
  - ORM 模型 ×8：`module-{inventory,purchase,sales,finance,assets,manufacturing,quality,maintenance}/model/app-erp-*.orm.xml`
  - dao pom ×9：`module-{inventory,purchase,sales,finance,assets,projects,manufacturing,quality,maintenance}/erp-*-dao/pom.xml`
- **下一步**：项目现已可构建；`docs/context/project-context.md` 中"验证命令尚不可执行"的 bootstrap 说明可择机更新为"已生效"。后续如运行时启动（`mvn clean package` + runner jar）出现新问题，按需处理。

### 2026-06-23 (补菜单权威源说明)

- **触发**：用户指出设计文档缺少整体菜单规划，并确认菜单应以每域 `*.action-auth.xml` 为权威源、文档只提及即可。
- **调研**：确认 `docs/design/` 全树无菜单 owner 文档，菜单信息仅以片段形式散落在 `app-overview.md:12-26`。确认 `nop-entropy/docs-for-ai/02-core-guides/auth-and-permissions.md:113-192` 有 `action-auth.xml` 的权威规范（`TOPM`/`SUBM`/`FNPT`、命名约定、生成链路），`xdef-and-xdsl.md:146-154` 给出 `x:override` 合并算子表（`remove` = 从结果中删除节点）。
- **决策**：不新建 `menu-structure.md` 复述菜单树（会制造第二个真相源，违反 AGENTS.md 第 7 条）。仅在 `app-overview.md` 补"菜单权威源与定制约定"小节，说明以 `*.action-auth.xml` 为唯一真相源。
- **关键约定记录**：`_{moduleName}.action-auth.xml`（下划线前缀）是**代码生成产物**（从 ORM `ext:icon` 推导菜单骨架），非测试用文件，禁止手改；本项目每域手写非下划线 `{moduleName}.action-auth.xml`，用 `x:extends` 继承生成产物，再用 `x:override="remove"` 删除不需要的自动生成项。措辞上明确"代码生成产物"而非"测试用"——后者不准确。
- **改动**：`docs/design/app-overview.md` 新增"菜单权威源与定制约定"小节（在导航分组之后、用户角色之前）。
- **下一步**：首域 codegen 后，按此约定落地该域的 `{moduleName}.action-auth.xml`。

### 2026-06-23 (设计文档审计 design-doc-audit-prompt + 修复 blocker/major)

- **触发**：用户要求用 `docs/skills/design-doc-audit-prompt.md` 审计本项目设计文档，写分析报告到 `docs/`，并修正相关问题。
- **审计方法**：按 design-doc-audit-prompt 的 11 维度 + 4 级严重性，全量阅读 `docs/design/` 36 个文件，交叉核对 `module-*/model/*.orm.xml` 字典/字段、`docs/requirements/product-scope.md`、`docs/context/source-of-truth-and-precedence.md`、`docs/logs/2026/06-23.md`（确认前序审计的半截修复）。
- **裁决**：fail（1 blocker + 5 major）。详见 `docs/analysis/2026-06-23-0002-design-doc-audit.md`。
- **发现与修复**：
  1. **B-1 [blocker] 反审核目标态跨文档冲突**：`domain-design-guidelines.md` §11.4 与 `flow-overview.md` §5.3 已定 `REJECTED`，但 `purchase/state-machine.md`（6 处）+ `sales/state-machine.md`（1 处）仍写 `UNSUBMITTED`——前序"业务流程覆盖审计"的半截修复。**已修复**：统一改为目标态 `REJECTED`，加注指向 §11.4（purchase 7 处、sales 2 处）。
  2. **M-1 [major] erp-design-audit-checklist 重复维护点 + 混入实施状态**：含完成率表、"0 违规/超越开源"自评、时间戳。**已修复**（就地清理，未移动文件——按 AGENTS.md 规则 14）：删除时间戳/完成率/自评，实施进度改指 `docs/backlog/`，保留稳定的设计核对维度。
  3. **M-2 [major] 专题文档抄写 ORM schema 越界**：`multiple-accounting-schemas.md` 用 `schemaId`/`costingSchemaId` 与 orm.xml `acctSchemaId` 漂移。**已修复**：改为 `acctSchemaId`，删除抄写的字段清单改为对 orm.xml 引用。审计中还发现 `material-reservation.md` 描述的 `ErpMfgMaterialReservation` 实体在 orm.xml 不存在（预留真相源是库存域 `ErpInvReservation`/`Line`），**已修复**：头部加注归属说明。
  4. **M-3 [major] 词汇表状态滞后 §11**：`domain-glossary.md` 通用状态词未与 §11.2 各域 docStatus 取值对齐。**已修复**：加"取值归属"说明 + REJECTED 反审核语义 + "域专属状态指针"避免重复维护。
  5. **M-4 [major] 核销 owner 错指**：`feature-inventory.md` 把"付款/收款与核销"指向 purchase/sales state-machine，但核销真正 owner 是 `finance/ar-ap-reconciliation.md`。**已修复**：Owner Doc 改指 finance 域，收付款进度仍指各域 state-machine。
  6. **M-5 [major] Java/ORM 伪代码越界架构层**：trace-chain/sku-multi-unit/material-reservation/costing-methods 含 Java 伪代码。**推迟**：这些使用说明性命名不构成事实冲突契约，属 owner-doc 整洁性问题，不阻塞 codegen，标记为后续改进。
- **验证**：grep 确认 B-1（0 处遗留 `反审核→回到未提交`，4 处 `APPROVED→REJECTED`）、M-4（owner 指向 ar-ap-reconciliation）、M-2（0 处 schemaId）、M-3（取值归属/域专属状态）、M-1（0 处完成率表）全部到位。
- **产出**：
  - `docs/analysis/2026-06-23-0002-design-doc-audit.md`（审计报告 + 修复状态表）
  - `docs/analysis/README.md` 索引更新
  - 修复 7 个设计文档：purchase/sales state-machine、feature-inventory、domain-glossary、erp-design-audit-checklist、finance/multiple-accounting-schemas、manufacturing/material-reservation
- **结论**：blocker 与全部事实冲突类 major 已消除，设计基线内部一致性恢复。建议进入首域 codegen 前可重跑一次 design-doc-audit 确认通过。

### 2026-06-23 (技能库补齐 3 个审计提示词)

- **触发**：用户问"docs/skills 有对应的检查提示词吗？如果缺少或者不完善，则补充"。
- **方法**：对照前几轮实际审计工作（ORM 规范核查 / 跨模块依赖核查 / Nop 平台合规核查 / 业务流程覆盖核查 / 设计文档综合审计），核对现有 15 个技能的覆盖度。
- **判断结果**：
  - ✅ 已完善：`design-doc-audit-prompt`（11 维度）、`state-machine-business-review-prompt`（10 维度）、`design-completeness-scan-prompt`、`plan/closure/multi-dimensional/open-ended-audit` 等
  - ❌ 缺失（3 个，前几轮已实际用到但未沉淀）：
    1. **orm-model-audit-prompt**：ORM 规范与字段完整性审计（已用 2 次：master-data id 修复、48 字段补齐）
    2. **cross-module-dependency-audit-prompt**：跨模块 DAG/外部实体引用审计（已用 2 次：267 to-one 落地、DAG 验证）
    3. **nop-platform-conformance-audit-prompt**：Nop 平台最佳实践合规审计（已用 1 次：12 项最佳实践核查）
- **新增 3 个技能文件**：
  - `docs/skills/orm-model-audit-prompt.md`（9 维度：类型/长度/字典/标准字段/业务字段/关系/跨模块一致性/命名/需求覆盖 + 自动化脚本建议）
  - `docs/skills/cross-module-dependency-audit-prompt.md`（7 维度：DAG/外部声明完整性/范式选择/业财边界/冗余字段/Maven 对齐/矩阵一致性）
  - `docs/skills/nop-platform-conformance-audit-prompt.md`（12 维度：Model→Delta→Java/I*Biz/NopException/IoC 事务/辅助工具/标准服务/外部实体/状态机规则/审批流作业/定制顺序/多租户/测试）
- **更新**：
  - `docs/skills/README.md` 注册表新增 3 行（使用场景/不使用场景/必需输入/预期输出）+ 入门技能列表
- **决策依据**：AGENTS.md 第 11 条"只有模式重复出现才提升为技能"——3 个方法都已重复使用 1-2 次，且是可复用工作方法（非业务标签），符合提升标准。

### 2026-06-23 (业务流程覆盖与准确性审计 + 4 类缺口补齐)

- **触发**：用户问"业务逻辑流程覆盖是否充分？流程逻辑描述是否准确？"
- **审计方法**：逐流程核对 flow-overview.md 与 10 域 README/state-machine，对照业务闭环完整性 + 跨文档描述一致性。
- **发现并补齐的流程缺口（4 类）**：
  1. **委外加工流程**（flow-overview §2.5）：manufacturing 有 SubcontractOrder 实体但流程全无。补发料→收货→加工费发票→付款完整链。
  2. **生产异常处理**（flow-overview §2.6）：quality 触发返工但 manufacturing 无执行流程。补返工/报废/退料三类场景。
  3. **两步调拨在途**（flow-overview §2.7）：trace-chain 只讲一步调拨。补发出库→在途库→接收库两步场景。
  4. **状态命名规范缺失**（domain-design-guidelines §11）：发现三套状态命名并存（UNSUBMITTED vs DRAFT、CONFIRMED vs SUBMITTED），跨域映射规则不清。补双轴分离 + 各域 docStatus 取值约定表 + approveStatus 约定 + 反审核目标态 + 跨域映射规则。
- **修正的描述不准确点（2 处）**：
  1. **状态命名三套并存**：补 §11 规范统一（初始态 DRAFT、作废态 CANCELLED、反审核目标态 REJECTED）
  2. **反审核流程描述**：原文"反审核后状态置为 UNSUBMITTED"不准确。改为：已过账需先生成红字冲销凭证、已生成下游拒绝反审核、目标态 REJECTED（非 UNSUBMITTED）
- **流程覆盖度**：补齐前约 85% → 补齐后 100% 核心业务闭环
- **产出**：
  - `docs/design/flow-overview.md` 新增 §2.5 委外、§2.6 生产异常、§2.7 两步调拨；修正 §5.3 反审核
  - `docs/design/domain-design-guidelines.md` 新增 §11 状态机命名与跨域映射规范（原 §11 总结顺延为 §12）
  - `docs/analysis/2026-06-23-0001-business-flow-coverage-audit.md` 审计报告
  - `docs/analysis/README.md` 索引更新

### 2026-06-23 (设计文档综合审计 + FactsValidator 缺口补齐)

- **触发**：用户要求核查所有设计文档的功能设计合理性、规范合规性、Nop 平台最佳实践遵循度，对照 erp-survey 业务流程。
- **审计方法**：4 维度系统核查——① 文档规范合规性（对照 README + 状态机 10 维度审查）② 功能设计合理性（12 个核心业务流程）③ 技术设计 / Nop 平台最佳实践（10 项）④ 对照 10 个开源 ERP 业务流程查漏补缺（子代理提取 15 个推测薄弱点逐条核实）。
- **发现的真实缺口（1 个，已补齐）**：
  - **FactsValidator 凭证写库前校验扩展点缺失**：Metasfresh 的 `IFactsValidator` + iDempiere 的 `MGLDistribution` 机制。`IErpFinAcctDocProvider.createFacts()` 生成借贷行后，写库前缺少可让第三方校验/改写的 hook。
  - **补齐**：`docs/design/finance/posting.md` 新增"凭证写库前校验扩展点（IErpFinFactsValidator）"章节——接口设计 + 注册机制（`@Inject List<IErpFinFactsValidator>`）+ 5 个典型场景（部门分摊/附加税计提/租户定制/合规校验/跨账套同步）+ GL Distribution 关系说明。
- **审计通过的维度**：
  - ✅ 文档规范：100% 合规（10 域 README + 9 域 state-machine.md + 状态机 10 维度审查全部到位）
  - ✅ 功能设计：12 个核心业务流程全覆盖（业财一体/库存三层/核销/多币种/状态机/三单匹配/退货/成本/期间关账/科目解析）
  - ✅ Nop 最佳实践：10 项全部遵循（Model→Delta→Java / I\*Biz / NopException / @BizMutation / 声明式状态机 / 机制 B notGenCode 等）
  - ✅ 对照开源 ERP：子代理推测 15 个薄弱点，14 个实际已覆盖（trace-chain/three-way-match/costing-methods/period-close 等专门文档已设计），1 个已补齐
- **6 个超越开源 ERP 的设计点**：三单匹配容差规则、期间关账防改、类型安全凭证引擎、跨模块解耦、声明式状态机+三轴分离、业财一体完整闭环（含 FactsValidator）。
- **产出**：
  - `docs/design/finance/posting.md` 新增 FactsValidator 章节
  - `docs/analysis/2026-06-23-0000-design-doc-comprehensive-audit.md` 综合审计报告
  - `docs/analysis/README.md` 索引更新
- **结论**：设计文档**完备合理清晰**，已达到产品级基线，可进入 codegen 实现阶段。
