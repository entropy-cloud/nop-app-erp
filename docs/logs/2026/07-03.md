# Development Log - 2026-07-03

### 2026-07-03（批次召回事件：NCR 升级召回 + 召回全场景端到端 + 文档对齐）

- 执行 `docs/plans/2026-07-03-1707-3-quality-recall-event.md`（active→completed）。落地 roadmap 工作项 2.11（批次召回事件聚合：登记/审批/目标定位/客户通知/批量退货）。实现 `docs/design/quality/recall.md`（召回事件头/目标实体 + 5 态状态机 + 目标定位算法 + 业务规则 + 反模式）。Phase 1/2 此前已完成（召回实体建模 + 状态机 + 强制审批 + 目标定位 + 客户通知门控 + 批量退货编排）；本轮完成 Phase 3（NCR 升级召回 + 端到端 + 文档对齐）。
- **任务路由**：`app-layer design change + implementation`——quality ORM 新增 2 实体（additive）+ codegen + 召回状态机 + 目标定位 + NCR 升级 + 批量退货编排；跨域 quality→inventory（追溯查询 `IErpInvStockMoveBiz.batchTrace` 只读 R）+ quality→sales（`IErpSalReturnBiz` 退货写触发）。
- **Phase 3（NCR 升级召回 + 端到端 + 文档/日志）**：
  - `IErpQaNonConformanceBiz.upgradeToRecall(ncrId)`（@BizMutation）：NCR.status→ESCALATED_TO_RECALL（字典值已存在 value 35，无须加值）+ 生成 ErpQaRecall（triggerType=BATCH_NCR_UPGRADE + sourceNcrId 关联 + 继承 NCR 物料 materialId + 严重程度 severityLevel——NCR severity 与 recall severity 码值对齐 LOW/NORMAL|MED/HIGH/CRITICAL=10/20/30/40）。经 `IErpQaRecallBiz.register` 走标准登记（→OPEN）。
  - `TestErpQaRecallE2E`（3 tests：NCR 升级→建召回 OPEN + 继承物料/严重程度 + submit/approve + 批次补登 + locateTargets 追溯链生成 target + notifyCustomers + generateReturns 退货单 RETURNED + close 门控通过；MANUAL 触发全链路；severityLevel=CRITICAL 标记全链路）。
  - 文档对齐：`recall.md` 补注——目标定位算法以 `batchTrace(batchNo)`（批次聚合查询）为唯一入口（澄清原文「反向追溯…正向追溯找到销售出库」自相矛盾：召回从批次起无单一源移动单，`backwardTrace(moveId)` 上溯方向相反、`forwardTrace(moveId)` 需已知源起点，均 rejected）+ batchId(Long)→batchNo(String) 类型桥残留风险 + serialNo 单件追溯本期 Non-Goal（config-gated 降级）+ 召回不过账/不改余额/多级审批 Non-Goal 显式声明；`extended-roadmap.md` 工作项 2.11 标 done。
- **Non-Goal（计划内显式延后，已补注 recall.md）**：召回直接库存调整（反模式——召回库存变动经标准销售退货移动单，召回只登记编排）；召回事件财务过账（召回本身不产生凭证，触发的退货走 sales 标准红字过账）；多级召回审批工作流引擎（本期单级强制审批，CRITICAL 标记需高层，触发：多级审批引擎需求）；serialNo 单件追溯目标定位（batchTrace 按批次聚合不覆盖，触发：inventory 单件追溯查询就绪 + 单件召回需求）。
- **验证状态：全绿（full-green verification）**——`mvn test -pl module-quality/erp-qa-service -am` = 33 tests / 0 Failures（新增 11：RecallStateMachine 6 + RecallLocateNotifyReturn 2 + RecallE2E 3；既有 22 无回归）；inventory 既有套件 45 tests 无回归；根 `mvn clean install -DskipTests` BUILD SUCCESS。


### 2026-07-03（健康检查：修复 recall 跨域 sales 注入导致多模块测试红）

- **触发**：mission 'erp' 健康检查（typecheck/build/test 全绿）。`mvn clean install -DskipTests` 全绿（146 模块）；但 `mvn test` 红——`app-erp-quality-service`（5 测试）+ `app-erp-purchase-service`（22 测试）全部因 IoC 容器构建失败报 `nop.err.ioc.not-find-bean-with-type`。
- **根因（Phase 1 回溯）**：召回特性 `ErpQaRecallBizModel`（`salReturnBiz`/`salDeliveryBiz`）与 `RecallTargetLocator`（`deliveryBiz`）硬 `@Inject` sales 域 `IErpSalReturnBiz`/`IErpSalDeliveryBiz`。quality-service 仅依赖 sales-dao/meta（非 sales-service，避免 reactor 环），故 sales I*Biz 不在 classpath。凡依赖 quality-service 且无 sales-service 的模块（quality 自测、purchase、manufacturing）启动 IoC 容器即失败。被掩盖的原因：召回开发仅跑了 quality-service 测试（2 个用 `testBeansFile` 载入 `test-mock-sales.beans.xml` 桩），未跑全 reactor。
- **修复（源端根因，最小改动）**：3 个跨域 sales `@Inject` 字段加 `jakarta.annotation.Nullable`——Nop IoC 经 `DefaultBeanClassIntrospection` 识别 `@Nullable` 标注的 `@Inject` 为 optional（bean 缺失时跳过注入置 null，容器仍可构建）。sales bean 存在处（app-erp-all 真实实现、quality 召回测试桩）照常注入。未改生成文件、未改生产 beans、未扩散 mock。
- **影响面安全分析**：`@Nullable` 仅作用于 sales 缺席的上下文（部分模块单测）；召回→sales 编排（locateTargets/generateReturns）只在实际调用处触发，而该流程仅在 sales 在场时运行（app-erp-all / quality 召回桩测试），故无 NPE 风险。
- **验证状态：全绿**——`mvn test`（全 146 模块）BUILD SUCCESS / 0 Failures / 0 Errors；`mvn clean install -DskipTests` BUILD SUCCESS。

### 2026-07-03（起草全域字典 int→string 重构计划，draft→active）

- 新建 `docs/plans/2026-07-03-2108-1-dict-int-to-string-refactor.md`。动机：对齐平台 `nop-entropy/docs-for-ai/02-core-guides/code-style.md`（dict valueType 推荐 string、允许 int），并为后续接入平台 `use-approval` 能力（需标准 `wf/approve-status` string 字典）清场。用户指令："所有字典改 string、所有引用字段改 varchar"。
- **基线（实时 grep 精确）**：18 ORM 源文件，222 dict（int 198 / string 24，CRM 23 + md `batch-strategy` 1 已是目标态）；321 ext:dict 字段（INTEGER 295 / VARCHAR 26）。受影响手写代码：10 个 `Erp*Constants.java`（239 常量）、~105 业务文件/~1054 引用、~92 测试文件（55 含数值字面量 `setStatus(10)` 类 158 处 / 4 含数值比较）、1 个 task.xml（顺带修 `FULLY_RECEIVED` vs 字典 `RECEIVED` 命名 bug）、5 个 master-data json5 输入。
- **计划结构**：5 Phase — ① ORM 源模型统一（198 dict + 295 字段）② codegen 增量重生成（`mvn clean install -DskipTests`，不重跑 nop-cli gen）③ 10 常量文件迁移 ④ ~105 业务 + ~92 测试（比较语义 `!=`/`==`→`.equals()`/`Objects.equals()`，业务约 300 处 + 测试 4 处最高风险）⑤ 5 json5 + 物理库决策 D5（bootstrap 重建优先）。Closure Gates 含 grep 门控（无 int dict / 无 INTEGER ext:dict / 无 `set[A-Za-z]*(Status|Type|Direction)\([0-9]+\)`. 残留）+ `mvn clean install -DskipTests` + `mvn test` 全绿。
- **两轮独立审查（规则 12）**：iter-1 `needs revision`（ses_0d7e48fa）—— 1 阻塞 B1：手写 Java 测试代码（~92 文件）完全遗漏（explore agent 搜 `*Test.java` 模式误判，实际命名 `Test*.java`），Phase 2 codegen 后测试将编译失败；4 改进（业务文件数 73→105 / 风险区分类型迁移 vs 比较语义 bug / 全量验证归 Closure Gates / 补 api.xml+view.xml 无影响），已修订。iter-2 `accept`（ses_0d7dbf27）—— B1 经独立 grep 精确验证（92/92、55/55 命中）已解决，N1-N4 采纳；3 项非阻塞（I1 比较 bug 实测 ~300 非 49 / I2 grep 门控收宽 / I3 Goals 数字同步）已采纳。Plan Status→active。
- **保护区域**：ORM 模型修改属 `project-context.md` 硬停止项，Phase 1 执行前需再次向用户确认范围。
- **验证状态**：计划级（未执行代码）；执行后 Closure Gates：`mvn clean install -DskipTests`（146 模块）+ `mvn test` 全绿。

### 2026-07-03（供应商评分卡周期评分 + AVL 准入联动）

- 执行 `docs/plans/2026-07-03-1707-2-supplier-scorecard-avl.md`（active→completed）。落地 roadmap 工作项 2.9（供应商评分卡计算 + AVL 准入 + standing→RFQ 联动）。实现 `docs/design/purchase/supplier-evaluation.md`（ERPNext 8-doctype 范式：criteria×formula×weight + 周期快照 + standing 三档联动）。
- **任务路由**：`app-layer design change + implementation`——跨 master-data + purchase ORM 新增 4 实体（additive）+ codegen + AVL 状态机 + 评分引擎 + 跨域写 purchase→master-data I*Biz（既定模式）。
- **Phase 1（AVL 准入建模 + 状态机，master-data）**：master-data ORM 新增 `ErpMdSupplierApproval` + 2 字典（`erp-md/supplier-approval-type`/`supplier-approval-status`），codegen。`IErpMdSupplierApprovalBiz` 6 态状态机：apply/approve/probate/suspend/reinstate/reject + suspendByPartner（跨域联动入口）+ findEffectiveByPartner（RFQ 校验读取）。非法迁移抛 `ERR_INVALID_APPROVAL_STATUS_TRANSITION`；批准校验资质/有效期（`ERR_APPROVAL_QUALIFICATION_MISSING`）。单步操作不拆 Processor（对齐反模式表）。`TestErpMdSupplierApprovalStateMachine`（6 tests）。
- **Phase 2（评分卡建模 + 周期评分引擎，purchase）**：purchase ORM 新增 `ErpPurSupplierScorecard`/`Criteria`/`Variable` + 2 字典（`erp-pur/supplier-standing`/`scorecard-status`），codegen。
  - **Explore 结论 + Decision 锁定（公式引擎选型）**：nop-rule 文档明确「纯算术/公式计算直接用 XLang 表达式即可，不必引入规则引擎」。故选用平台 XLang 表达式（`XLang.newCompileTool().allowUnregisteredScopeVar(true).compileSimpleExpr(...)` + `EvalExprProvider.newEvalScope(inputs)`），purchase-service 已传递依赖 nop-core/nop-xlang 无需新增依赖。variable.path 取值仍由 Java 装配（测试/装配器写入 variable.value，计算器读取喂入公式）。新增维度=配置 criteria+variable 不改代码（满足「公式非硬编码」）。
  - `IErpPurSupplierScorecardBiz.finalizeScorecard` 委托 `ScorecardCalculator`：criteria×formula×weight→score→weightedScore=score×weight/100→totalScore=Σ→按 warn/hold 阈值落 standing（GREEN≥warn / hold≤YELLOW<warn / RED<hold）；status DRAFT→FINALIZED。校验：权重和=100（`ERR_SCORECARD_WEIGHT_NOT_100`）、无维度拒绝（`ERR_SCORECARD_NO_CRITERIA`）、FINALIZED 不可重算（`ERR_SCORECARD_ALREADY_FINALIZED`）、公式失败（`ERR_SCORECARD_FORMULA_EVAL_FAIL`）。`TestErpPurScorecardCalc`（7 tests）。
- **Phase 3（standing→AVL/RFQ 联动 + 端到端）**：
  - `ScorecardStandingLinker`（独立 Bean）：finalize 后 standing=RED → 调 `IErpMdSupplierApprovalBiz.suspendByPartner`（跨域 purchase→master-data I*Biz，单事务跟随 @BizMutation）使 AVL 暂停立即生效。
  - `SupplierEligibilityChecker` + `ErpPurQuotationBizModel.defaultPrepareSave` 钩子：AVL status∈{SUSPENDED,REJECTED} 或无资格 → prevent；standing=RED → `erp-pur.scorecard-prevent-on-red`（默认 true）门控 prevent/hold；YELLOW → warn（日志提示不阻止）。`TestErpPurScorecardLinkage`（8 tests：RED→AVL SUSPENDED 联动 / SUSPENDED/REJECTED/无资格 prevent / YELLOW warn 不阻止 / GREEN 正常 / config prevent-on-red=false 时 RED=hold）。
  - **设计偏离补注（`supplier-evaluation.md`）**：RFQ 头 `ErpPurRfq` 无 supplierId（一份询价发多个供应商），供应商参与点为报价单 `ErpPurQuotation`（含 supplierId）。故「RFQ 创建校验」钩子落在 `ErpPurQuotationBizModel.defaultPrepareSave`。设计意图（SUSPENDED/RED 供应商不可参与询价）不变。
- **Non-Goal（计划内显式延后，保持对齐无降级）**：实时评分累加（评分按 period 时点快照，反模式）；评分财务过账（绩效产物不产生凭证）；AVL/评分卡多级审批（本期单级简化）；质检/价格/交货原始数据维护（评分只读源实体）；variable.path→value 自动取值装配器（本期测试预置 value，路径解析留 out-of-scope 装配器）。
- **验证状态：全绿（full-green verification）**——`mvn test -pl module-master-data/erp-md-service,module-purchase/erp-pur-service -am` = 96 tests / 0 Failures（新增 21：AVL 状态机 6 + ScorecardCalc 7 + ScorecardLinkage 8；既有 master-data 7 + purchase 81 无回归）；根 `mvn clean install -DskipTests` BUILD SUCCESS（146 模块）。

### 2026-07-03（CRP 负荷计算引擎：产能四要素建模 + 负荷快照 + 报表 + 超负荷告警）

- 执行 `docs/plans/2026-07-03-1707-1-manufacturing-crp-load-engine.md`（active→completed）。落地 roadmap 工作项 2.8（CRP 产能需求计划：工作中心日历/班次 + 按产品产能子实体 + 负荷快照 + workcenter×date 负荷聚合 + 超负荷告警）。
- **Phase 1（产能建模 ORM 新增 + codegen）**：manufacturing ORM 新增 3 实体 + 2 字典（additive，不改既有表）：
  - `ErpMfgWorkcenterCalendar`（workcenterId R→Workcenter；calendarName；shiftType dict `erp-mfg/shift-type` ONE_SHIFT/MORNING/AFTERNOON/NIGHT；workDatePattern dict `erp-mfg/work-date-pattern` ALL_WEEK/WEEKDAYS/WEEKEND；startTime/endTime；effectiveFrom/To；isActive）。
  - `ErpMfgWorkcenterCapacity`（workcenterId R；materialId R→Material；capacityPerHour；setupTime；cleanupTime；efficiencyFactor 默认 1.0；isActive）——取代既有 `Workcenter.capacity` 单一标量（反模式，保留不删不依赖）。
  - `ErpMfgCrpLoad`（workcenterId R；workOrderId 弱指针 R→WorkOrder；loadDate；loadHours；setupHours）——负荷快照行。
  - codegen 增量生成 dao/entity/IBiz/BizModel/meta/web 全套（`mvn clean install -DskipTests` 触发 gen-orm.xgen）。
- **Phase 2（CRP 负荷计算引擎 + 报表 + 告警 + 测试）**：
  - `IErpMfgCrpBiz.calculateLoad(periodFrom, periodTo, workcenterIds?)`（@BizMutation，`@Optional` 标可选 list 参数）：`CrpLoadCalculator` 扫描 WorkOrder（plannedStart/EndDate 落区间、docStatus≠CANCELLED）经 RoutingOperation 分派 workcenterId，standardTime 换算小时后均匀分派到区间内日（setupTime 计首日），按 workcenter×loadDate×workOrder 写 CrpLoad 行；重算前清区间既有快照。
  - `IErpMfgCrpBiz.getLoadReport(periodFrom, periodTo, workcenterIds?)`（@BizQuery）：返回 `CrpLoadReportItem`（workcenter×date 聚合 loadHours/capacityHours/loadRate/overloaded）；capacityHours = WorkcenterCalendar 出勤工时(Σ endTime−startTime 满足 workDatePattern/生效区间) × WorkcenterCapacity.efficiencyFactor；loadRate = loadHours/capacityHours；overloaded = loadRate > `erp-mfg.crp-overload-threshold`（默认 1.0，经 AppConfig.var）。
  - `CrpLoadReportItem`（@DataBean DTO，`app.erp.mfg.biz` 包）；`IErpMfgCrpBiz`（新契约，`IErpMfgCrpLoadBiz extends ICrudBiz, IErpMfgCrpBiz` 挂 ErpMfgCrpLoad 实体暴露 GraphQL `ErpMfgCrpLoad__calculateLoad/getLoadReport`）；`CrpLoadCalculator`（`app.erp.mfg.service.crp`，对齐 MrpEngine 服务助手范式，IDaoProvider 域内只读）；`ErpMfgErrors.ERR_CRP_PERIOD_INVALID` + `ErpMfgConstants`（SHIFT_TYPE/WORK_DATE_PATTERN/CONFIG_CRP_OVERLOAD_THRESHOLD）；beans.xml 注册 CrpLoadCalculator。
  - `TestErpMfgCrpLoad`（7 tests：单工单负荷+日历可用工时+效率折算 capacityHours+loadRate+overloaded；多工单同工作中心同日累加；重算清旧快照；空 WorkOrder capacityHours 非零 loadHours 零 loadRate=0；阈值门控默认1.0/调高3.0；多日均匀分派+setup 首日；CANCELLED 工单排除）。
- **Non-Goal（计划内显式延后，已补注 `crp.md`）**：APS 工序级排产（写 OperationOrder，本期 fallback 为 WorkOrder 计划日期）；maintenance 停机扣减工作中心可用时段（事件驱动，需 maintenance 停机事件）；标量 Workcenter.capacity 迁移（保留为旧显示字段）；班次级粒度（本期日级）；AMIS 可视化（本期交付 GraphQL 查询）；CRP 定时运行 cron（on-demand calculateLoad 入口可被周期调度复用）。
- **验证状态：全绿（full-green verification）**——`mvn test -pl module-manufacturing/erp-mfg-service -am` = 43 tests / 0 Failures（新增 7：CrpLoad，既有 36 无回归）；根 `mvn clean install -DskipTests` BUILD SUCCESS（146 模块）。

### 2026-07-03（维护执行切片 Phase 3：停机记录 + 维护全场景端到端）

- 执行 `docs/plans/2026-07-03-1018-3-maintenance-visit-request-sparepart-downtime.md` Phase 3（active→completed）。Phase 1/2 此前已完成；本轮落地停机记录 + 端到端，工作项 2.7 全部交付。
- **Phase 3（停机记录 + 维护全场景端到端）**：
  - `IErpMntDowntimeEntryBiz.record/complete`（@BizMutation）：record→设备 DOWN（`EquipmentStatusLinker.linkToDown`，`erp-mnt.equipment-status-link-enabled` 门控）；complete→endTime + totalMinutes 计算（数值写入 VARCHAR/DECIMAL 异常列，见 baseline）+ 设备恢复（`restoreToRunning`）。终态保护：已结束（endTime 非空）不可再 record/complete（`ERR_DOWNTIME_ALREADY_COMPLETED`）；未开始无 startTime 不可 complete（`ERR_DOWNTIME_NOT_STARTED` 防御）。`relatedJobOrderId` 仅存值（不依赖坏掉的 EQL 自引用导航）。
  - `ErpMntErrors` 补 `ERR_DOWNTIME_ALREADY_COMPLETED`/`ERR_DOWNTIME_NOT_STARTED`。复用 Phase 1 `EquipmentStatusLinker.linkToDown/restoreToRunning`（已就绪）+ beans 注册（既有）。
  - `TestErpMntDowntimeAndE2E`（4 tests：停机 record→DOWN+complete→totalMinutes+恢复+终态保护；totalMinutes 反映 2 小时停机（BigDecimal 解析 DECIMAL 读回值）；维护全场景：计划到期生成 PLANNED 访问→schedule→start(UNDER_MAINTENANCE)→备件消耗出库→complete(恢复)；报修请求→accept 生成 RESPONSIVE 访问→schedule/start→complete+请求 complete）。
  - **Fix（测试种子）**：`startTime` 列 DB NOT NULL → 停机记录种子须带 startTime（生产流：创建即记录停机时刻，record 主效果为设备 DOWN）；`assignVisit` 对 generateDueVisits 创建的 MANAGED 访问须于 session 内修改（非 saveEntity）；`totalMinutes` 列 DB 为 DECIMAL（基线类型异常）读回 "120.00" 须 BigDecimal 解析。
- **Non-Goal（计划内显式延后，已补注 `maintenance/state-machine.md`）**：停机通知制造域（事件驱动，避免 maintenance→manufacturing 反向依赖成环）；维修费用过账（备件消耗本期仅触发 inventory 出库，finance MAINTENANCE_ISSUE 凭证延后，明知偏离 `data-dependency-matrix.md` maintenance→finance）；预测性维护（IoT 未就绪）；校准管理全流程；设备-资产价值联动；多级审批。
- **验证状态：全绿（full-green verification）**——`mvn test -pl module-maintenance/erp-mnt-service -am` = 22 tests / 0 Failures（新增 4：DowntimeAndE2E，既有 Phase1 9 + Phase2 4 + smoke 5 无回归）；inventory/finance 既有套件无回归；根 `mvn clean install -DskipTests` BUILD SUCCESS（146 模块）。

### 2026-07-03（维护执行切片验证 + 备件消耗 posted 语义回归修复）

- 执行 `docs/plans/2026-07-03-1018-3-maintenance-visit-request-sparepart-downtime.md`（Phase 1 已完成 → Phase 2 验证完成）。落地 roadmap 工作项 2.7（设备维护执行：访问/请求状态机 + 备件消耗出库 + 维护计划到期生成）。Phase 3（停机记录 + 端到端）仍 planned，计划保持 active。
- **Phase 1（访问 5 态 + 请求 6 态 + 设备状态联动）**：`IErpMntVisitBiz`（schedule/start/complete/cancel）、`IErpMntRequestBiz`（accept→生成 RESPONSIVE 访问/startRepair/complete/reject/cancel）、`EquipmentStatusLinker`（start→UNDER_MAINTENANCE / complete→恢复前置快照，`erp-mnt.equipment-status-link-enabled` 门控）。`TestErpMntVisitRequestStateMachine` 9 tests 全绿。
- **Phase 2（备件消耗→inventory 出库 + 维护计划到期生成）**：`IErpMntSparePartUsageBiz.confirm`（`SparePartIssueService` 调 `IErpInvStockMoveBiz.generateMove` OUTGOING 业务联动自动 DONE 扣余额 + totalAmount 聚合 + 可用量不足回滚）、`IErpMntScheduleBiz.generateDueVisits`（`ScheduleDueGenerator` 扫描到期 active 计划生成 PLANNED 访问 + nextDueDate 推进，`erp-mnt.auto-generate-due-visits` 门控）。`erp-mnt-service` 新增 `app-erp-inventory-dao` compile 依赖。`TestErpMntSparePartAndSchedule` 4 tests 全绿。
- **Fix（posted 语义回归，根因修复）**：`mvn test` 首跑 `TestErpMntSparePartAndSchedule.testSparePartConfirmIssuesStockAndPosts` 失败（`posted` 期望 true 实为 false）。根因有二：① `ErpMntSparePartUsageBizModel.applyIssueResult` 误以 `usage.setPosted(move.getPosted())` 取 inventory 的**财务过账**标记；但 plan 明确 `posted=「库存已出库」`语义、维修费用过账（MAINTENANCE_ISSUE）为本期 Non-Goal。② `InvPostingDispatcher` 将维护领料 OUTGOING 移动误派为 `SALES_OUTPUT` 财务过账（无科目映射→抛错吞异常→`move.posted` 恒 false）。修复：maintenance 侧改 `posted = (move.docStatus == DONE)`（`ErpMntConstants` 补 `STOCK_MOVE_DOC_STATUS_DONE=30` 调用方副本，main 不依赖 inventory-service）；inventory 侧 `ErpInvConstants` 补 `RELATED_BILL_TYPE_MNT_SPARE_PART` + `resolveBusinessType` 跳过该联动（与 PUR_RETURN/SAL_RETURN 同模式，避免误派 SALES_OUTPUT 凭证）。经 `nop-debugging` 技能 Phase 1-4 系统定位。
- **验证状态：全绿（full-green verification）**——根 `mvn clean install -DskipTests` 146 模块 BUILD SUCCESS；完整 `mvn test` BUILD SUCCESS（全仓 0 failures / 0 errors；maintenance 18、inventory 45、finance 93 均无回归）。

### 2026-07-03（核心域 BizModel 产品化重构：单体 → Facade + Processor）

- 执行 `docs/plans/2026-07-03-1000-1-bizmodel-productization-refactor.md`（active→completed）。将核心域多步状态机 BizModel 从单体 Java 实现重构为 `processor-extension-pattern.md` 两层结构（Facade 入口/事务 + Processor protected step 编排，下游可逐 step 派生覆盖）。零业务行为变更、零 ORM/xbiz 变更。
- **产出 10 个 Processor + 10 个 Facade 改写**：
  - Finance（5）：`ErpFinEmployeeAdvanceProcessor` / `ErpFinExpenseClaimProcessor` / `ErpFinNotesPayableProcessor` / `ErpFinNotesReceivableProcessor` / `ErpFinAccountingPeriodProcessor`（`module-finance/erp-fin-service/.../processor/`）。
  - Manufacturing（2）：`ErpMfgWorkOrderProcessor` / `ErpMfgJobCardProcessor`。
  - Assets（3）：`ErpAstAssetCapitalizationProcessor` / `ErpAstDepreciationScheduleProcessor` / `ErpAstDisposalProcessor`。
  - Sales（6）+ Inventory（`ErpInvStockMoveProcessor`）：上轮已落地，本轮验证一致 + 勾选。
- **bean 注册**：3 个 `app-service.beans.xml`（fin/mfg/ast）登记 Processor bean（Nop IoC 非自动扫描，须显式注册）。
- **辅助改动**：`ErpAstErrors.java` 补 `ERR_CAPITALIZATION_NOT_FOUND` / `ERR_DISPOSAL_NOT_FOUND`（Processor 经 `dao().getEntityById()` 直加载，与 Phase 1 一致用域级 NOT_FOUND）；`TestErpFinModuleCloseOrder` 改注入 `ErpFinAccountingPeriodProcessor` + 其 `public Module` 枚举（原 BizModel 内部成员随编排下沉）。
- **关键回归修复**：跨域过账后 reload 的 `do*` 方法（doApprove/doReverseApprove/doCancel/doIssue/doHonor/doReceive/doDiscount/doEndorse）必须**返回** reload 后实体（非 void），否则 Facade 返回的实体缺 posted 标志（EmployeeAdvance/ExpenseClaim 审批断言 expected 30 was 20）→ 已修正。
- **决策（非静默降级，均带理由）**：`ErpFinReconciliationBizModel`（step 已 protected + 经 `get()` 管道，迁移会丢管道→保留）、`StockMoveBookkeeper`/`InvPostingDispatcher`（单调用方辅助类→保留）、`ErpFinCreditFacility`(2)/`ErpFinCashForecast`(1)/`ErpMfgMaterialIssue`(1)/`ErpMfgBom`（单步或已委托独立类→保留）。
- **Deferred**：task.xml 迁移、扩展域（projects/quality/maintenance/crm/cs/hr/aps/contract/drp/logistics/b2b）BizModel → 后续 plan。
- **验证状态：全绿**。Per-module `mvn test -pl <module> -fae -o`：Sales 69 / Inv 45 / Fin 93 / Mfg 36 / Ast 19，均 0 失败 0 错误。全量 `mvn clean install -DskipTests -o` 146 模块 BUILD SUCCESS（跨模块无下游破坏）。独立结束审计 PASS（无 Blocker/Concern）。
- 下一步：可选（a）task.xml 迁移计划（拓扑可变场景）；（b）扩展域 BizModel 产品化。



- **任务路由**：`survey/analysis work`——对 `~/ai/wukong-aicrm` 进行源码级深度调研，生成结构化分析报告。
- **新建 `docs/analysis/erp-survey/2026-07-03-0000-wukong-aicrm.md`**（198 行）：覆盖 AI-Native 架构（6 ChatApplication 上下文、10 AI Tool、SSE 流式、动态 ChatClient）、模块清单（CRM 核心/通用业务/AI 基础设施/基础架构/第三方集成/桌面端）、多租户、双桶 AI 计费、8 条对 nop-app-erp 的可借鉴设计点、ERP 差异提醒。
- **更新 `docs/analysis/erp-survey/2026-06-22-0000-survey-index.md`**：新增第六节「AI CRM 专项」、CRM/营销能力矩阵加入 Wukong 行、速查导航新增 2 场景（AI CRM + 多租户数据权限）。
- **更新 `docs/analysis/README.md`**：erp-survey 计数 18→19。
- 前端 AI 界面调研结论：基于 `fetch()` + `ReadableStreamDefaultReader` + `TextDecoder` 消费 SSE，非 `EventSource`/WebSocket；`stores/chat.ts` Pinia 管理消息/流式/草稿持久化；AI 工具调用在服务端执行，结果以 Markdown 嵌入回复中返回前端。
- 数据结构对比结论：nop-erp CRM（34 实体）在传统 CRM 完整度上远超 Wukong（评分/预测/区域/CPQ/Sequence/漏斗分析等 9 子模块为 Wukong 不具备），Wukong 唯一优势在 AI 原生集成。定位区别：流程驱动型 vs 对话驱动型。
- 验证状态：文档变更，无代码验证。

### 2026-07-03（项目成本归集：工时成本 + 预算控制 + 归集汇总 + 业财过账 + 费用报销归集）

- 执行 `docs/plans/2026-07-03-1018-2-projects-cost-collection.md`（active→completed）。落地 roadmap 工作项 2.6（projects 域成本归集完整链路）。
- **任务路由**：`app-layer design change + implementation`——纯服务层 + 既有实体（不新增实体/列/字典/枚举），一处 pom 依赖变更（erp-prj-service→finance-service compile + master-data-service test）。
- **Phase 1（工时状态机 + 成本率解析 + PROJECT_COST_COLLECTION 过账）**：
  - `IErpPrjTimesheetBiz` 扩 submit/approve/reject/cancel；`ErpPrjTimesheetBizModel` 实现（@BizMutation+@SingleSession）。
  - `CostRateResolver`：Timesheet.costRate → ActivityType.costRate → `erp-prj.default-labor-cost-rate`（实现偏差：设计 §2.2 用户/角色级无载体，本期 Non-Goal）。
  - `ProjectCostCollectionProvider implements IErpFinAcctDocProvider`（businessType=PROJECT_COST_COLLECTION(110)，复用既有枚举；设计 §8 写作 PROJECT_LABOR_COST 为命名偏差，已补注）。
  - `ProjectPostingExecutor`（跨域 IErpFinVoucherBiz 调用）+ `TimesheetPostingDispatcher`（组装 PostingEvent）。
- **Phase 2（预算控制 + 归集汇总 + 项目状态引用校验）**：
  - `BudgetChecker.check(projectId, addAmount)`：项目级 `erp-prj.budget-control-mode`（WARNING 默认 / STRICT 抛 ERR_BUDGET_EXCEEDED）。
  - `ProjectCostAggregator.aggregateFromTimesheet`：工时 APPROVED 同事务生成/追加 ErpPrjCostCollectionLine（costCategory=LABOR）+ 增量回写 actualCost。
  - `IErpPrjProjectBiz` 扩 refreshActualCost/closeProject/requireReferenceable（OPEN→COMPLETED 冻结 + 非 OPEN 拒绝引用）。
- **Phase 3（费用报销归集：projects 驱动只读聚合）**：
  - `ExpenseCostAggregator.refreshExpenseCost`：经 `IErpFinExpenseClaimBiz` 只读 R 查已审核报销单（行 projectId 命中）+ projects 自写 erp_prj_cost_collection（对齐 data-dependency-matrix §3.2 finance 从不写业务表 + §4.2 projects 触发）。
  - `IErpPrjCostCollectionBiz.refreshExpenseCost`（config-gated：`erp-prj.expense-aggregation-enabled` 默认 true）。
  - closeProject 关闭前强制刷新费用归集（保证关账数据完整）。
- **设计文档偏差补注**：`docs/design/projects/cost-collection.md` §2.2（成本率解析优先级）、§3.1（预算控制项目级 config）、§4.1（费用归集 projects 驱动）、§8（businessType=PROJECT_COST_COLLECTION）。
- **验证状态：全绿（full-green verification）**——`mvn test -pl module-projects/erp-prj-service -am` Tests run: 23, Failures: 0, Errors: 0（4 测试类：CRUD 冒烟 5 + 工时成本 7 + 预算归集 7 + 费用归集 4）；finance 既有套件 Tests run: 93 无回归；根 `mvn clean install -DskipTests` BUILD SUCCESS（146 模块）。复跑确认：完整 `mvn test` BUILD SUCCESS（全仓）+ erp-prj-service 23 tests 0 failures。

### 2026-07-03（M4 业财一体端到端：财务核销层补全 + 退货退款连续链 E2E）

- 执行 `docs/plans/2026-07-03-1018-1-m4-business-finance-e2e-tests.md`（active→completed）。落地 roadmap 工作项 4.1/4.2/4.4（端到端集成测试补全，不改公共契约/业务逻辑）。
- **任务路由**：`verification work`——纯测试扩展/新增（程序化 `IGraphQLEngine.newRpcContext` + `JunitAutoTestCase` + `autotest.yaml` 快照），finance 核销层经 `@Inject IErpFinReconciliationBiz`/`IErpFinArApItemBiz`（对齐 `TestErpFinReconciliation` 既有模式）。Skill: `nop-testing`。
- **4.1 P2P 财务核销层（扩 `TestErpPurProcureToPayEnd`）**：新增 `testFinanceReconciliationLayerPayable`（AP_INVOICE/PAYMENT 过账→DIRECTION_PAYABLE 辅助账 openAmount=56.5→`ErpFinReconciliation` 核销→双方归零 SETTLED→账龄 totalOpen=0）+ `testFinanceReconciliationLayerExceptions`（未审核发票无辅助账拒绝/超额拒绝）。
- **4.2 O2C 财务核销层（扩 `TestErpSalOrderToCashEnd`）**：新增 `testFinanceReconciliationLayerReceivable`（AR_INVOICE/RECEIPT→DIRECTION_RECEIVABLE 辅助账 openAmount=113→核销归零→账龄）+ `testFinanceReconciliationLayerExceptions`（超额拒绝/已 SETTLED 辅助账重复核销 item-not-open 拒绝）。
- **4.4 退货退款连续链（新增）**：`TestErpPurReturnRefundEndToEnd`（已入库+已开票+已付款→核销→退货审核→反向出库+PURCHASE_RETURN 红字凭证+负 openAmount(-20) 辅助账→sumOpen 回减→反审核 cancelOnReverse 归零+余额恢复；异常：终态重复审批/无库存/核销超额）+ `TestErpSalReturnRefundEndToEnd`（已出库+已开票+已收款→核销→退货→反向入库+SALES_RETURN 凭证+负 openAmount(-24)→回减→归零；异常同构）。首条将退货审批→反向库存→红字过账→辅助账回减→归零串成连续场景的用例。
- **Fix-1（compile blocker，已修复）**：`module-sales/.../processor/ErpSalReturnProcessor.java`（draft 1000-1 未提交文件）误导入 `io.nop.dao.api.IOrmTemplate`（实属 `io.nop.orm`），致 sales-service 全模块编译失败。已改 `import io.nop.orm.IOrmTemplate`（1 行，中立）。
- **Follow-up-1（非阻塞，带触发条件移交）**：退货反向移动单存货侧过账抛 `ErpFinVoucherLine.acctSchemaId mandatory-prop-is-null`（移动单保持 DONE/posted=false），不影响退货主链（业财凭证+辅助账+库存余额均正确）。触发条件：退货移动单需生成存货侧 GL 凭证时修复 acctSchemaId 透传（inventory 域）。
- **设计澄清**：`ErpFinReconciliation` 对称结算器仅支持同号项核销；退货负项 credit memo 归零经 `cancelOnReverse`、余额回减经 `sumOpen`——0300-3 既定设计，据实断言，未改业务逻辑。
- **验证状态：全绿**——`mvn test -pl module-purchase/erp-pur-service` Tests run: 74, Failures: 0, Errors: 0；`mvn test -pl module-sales/erp-sal-service` Tests run: 69, Failures: 0, Errors: 0（既有套件无回归）；根 `mvn clean install -DskipTests` BUILD SUCCESS（146 模块）。

### 2026-07-03（架构文档：审批能力平台化迁移 + 应用层瘦身）

- 审批能力定位修正为 **nop-wf-core 平台级通用能力**（非 ERP 专属）。平台设计与使用指南迁移到 nop-entropy：
  - 平台设计：`nop-entropy/ai-dev/design/nop-wf/approvable-entity-design.md`（`use-approval`/`IApprovableBiz`/objMeta/codegen/两流分离/wf 回调）
  - 使用指南：`nop-entropy/docs-for-ai/03-runbooks/enable-approval-on-entity.md`
- `wf-integration-design.md` 瘦身为 ERP 应用层落位文档（平台设计迁出）：ERP 单据标 `use-approval`、ERP 业务联动在 xbiz 注入、首批 WORKFLOW 实体（付款/收款/资产处置加 `flowInstanceId`）。
- 关键修正：业务联动**不用 Java 钩子**，在 xbiz 层注入（`approve` action source `x:override="append"` 或 `<observes>` 监听 action 触发，依据 `xbiz.xdef` §observes）。
- `approval-framework.md` §与 nop-wf 集成 更新指向 nop-entropy 平台文档；`architecture/README.md` `wf-integration-design.md` 条目改为应用层定位。
- 验证状态：文档变更，无代码验证。引用一致性经核验（approval-framework / wf-integration-design / README 互引 + 指向 nop-entropy 平台文档）。
- 关联：nop-entropy 侧记录见 `nop-entropy/ai-dev/logs/2026/07-03.md`。

### 2026-07-03（架构文档：审批流集成设计 + document-engine.md 去过时）

- **任务路由**：`architecture change`——审批流（nop-wf）与业务流（nop-task）集成技术方案设计 + 过时架构文档清理。无代码变更。
- **新建 `docs/architecture/wf-integration-design.md`**：定义两流正交分离的集成方案。核心决策：① 审批用 nop-wf，业务处理用 nop-task，唯一接触点是"通过/驳回"回调信号；② **状态归 task**——approveStatus 由业务处理 task 改写，不用 wf 的 `bizEntityStateProp` 自动回写（跨层、脱离事务、语义泄露）；③ 串联用 nop-wf `<listener>`（`.xwf` 声明式 XML 元素，源码锚点 `WfRuntime.java:302` triggerEvent）的 `*end` 事件直接 `inject('biz_*)` 调 BizModel，**无 Java listener 中间层**；④ DIRECT/WORKFLOW 双模殊途同归到同一 task；⑤ WORKFLOW 实体加 `flowInstanceId`（对齐 `nop_wf_instance.wfId` VARCHAR32），`approvalMode` 走单据类型配置表不入业务行。基于 nop-entropy 源码确认（WorkflowService/WorkflowEngineImpl/wf.xdef/NopWfCoreConstants）。
- **重写 `docs/architecture/document-engine.md`**：查证发现该文档是探索性概念设计，其中 `DocumentEngine`/`IDocumentAction`/`DocumentHookRegistry`/`ApprovalCallbackListener`/声明式转换 DSL/`EventBus` 异步过账 全是**未实现伪代码**（全量检索 nop-app-erp + nop-entropy 源码均不存在）。但"三轴状态分离"（docStatus/approveStatus/postedStatus）是真实落地基线（多模块 ORM 均含），过账机制真实实现是 `PostingEvent` + `IErpFinAcctDocProvider`/`ErpFinAcctDocRegistry`（见 `docs/design/finance/posting.md` 三层模型，默认 SYNC）。故**重写而非删除**：剔除全部伪代码，保留真实三轴基线 + 单据流转真实实现（状态字段 + task.xml/Processor），过账指向 `finance/posting.md`，审批指向 `wf-integration-design.md`，各域状态机指向 `docs/design/<domain>/state-machine.md`。加「设计演进说明」记录设想 vs 现状差距。
- **引用一致性校正**：更新 `architecture/README.md`（document-engine.md 条目描述去过时词、加 wf-integration-design.md 条目）；`approval-framework.md` §与 nop-wf 集成 指向 wf-integration-design.md 并明确"approveStatus 唯一写者是 task"；修正 wf-integration-design.md 对 document-engine.md 的失效行号/重复澄清段/已完成步骤。
- 验证状态：文档变更，无代码验证。引用一致性经 rg 全量校正（document-engine.md 引用点：README/wf-integration-design/approval-framework/analysis 均已核对，analysis 为历史调研记录保留原引用）。
- Deferred（留后继）：按计划落地的首个 WORKFLOW 切片（建议付款单：`.xwf` + flowInstanceId 字段 + 单据类型审批配置表 + BizModel 双模分流）；`document-engine.md` 的 `EventBus` 异步过账设想与真实 `PostingEvent`/SYNC 默认的差异若需追溯可入 `docs/retrospectives/`。



- 执行 `docs/plans/2026-07-02-2237-3-quality-inspection-trigger-ncr-capa.md`（active→completed）。落地 roadmap 工作项 2.4「质检触发 + NCR/CAPA 流程」。实现 `docs/design/quality/state-machine.md`（质检单 4 态 + NCR 5 态 + CAPA 闭环）。
- **任务路由**：`app-layer design change + implementation`——纯服务层 + 既有实体（Inspection/InspectionLine/NonConformance/Action/Template/TemplateLine），无 ORM 变更/不加实体列字典，故无 ask-first 保护区域门控。
- **Phase 1（质检单 4 态状态机 + 行级评测 + 结果汇总 + posted + 反查 + 测试）**：
  - `IErpQaInspectionBiz.recordResult`（PENDING→ACCEPTED/CONDITIONAL/REJECTED；行级评测 + 汇总 + posted=true）+ `findByRelatedBill`（业务域查结论，跨域只读经 I*Biz）+ `isInspectionCleared`（强制质检门控）。
  - `InspectionResultEvaluator`（新）：specMin/specMax vs measuredValue 数值比较（解析失败视为不合格）；汇总全合格→ACCEPTED、含不合格+让步→CONDITIONAL、含不合格未让步→REJECTED。无规格上下限（外观类）实测非空即合格。
  - `ErpQaConstants`（INSPECTION_RESULT_*/NCR_STATUS_*/ACTION_STATUS_*/APPROVE_STATUS_*/RELATED_BILL_TYPE_*/CONFIG_*）+ `ErpQaErrors`（ERR_INSPECTION_*/NCR_*/ACTION_*）+ `ErpQaConfigs`（配置读取助手）。
  - `TestErpQaInspectionStateMachine`（7 tests：全合格/部分不合格+让步/关键不合格/解析失败/终态不可恢复/findByRelatedBill/isInspectionCleared）。
- **Phase 2（业务触发 createForBusinessBill + 模板匹配 + 强制质检阻塞 + 测试）**：
  - `IErpQaInspectionBiz.createForBusinessBill`（生成 PENDING 质检单 + 模板匹配复制行）+ `InspectionTemplateMatcher`（materialId×inspectionType→active 模板；无匹配走 `erp-qua.default-inspection-template`；仍无则无行人工补录）+ `InspectionTrigger`（quality-dao 共享门控助手）。
  - config-gated 业务触发接线：`ErpPurReceiveBizModel.approve`（INCOMING）/`ErpSalDeliveryBizModel.approve`（OUTGOING）/`ErpMfgWorkOrderBizModel.reportCompletion`（FINAL，连 N=1 inspection-gate-enabled）按行物料调 `InspectionTrigger.enforceGate`——首次生成 PENDING 质检单并阻塞，质检合格/让步后再次流转放行。purchase/sales/manufacturing-service compile 依赖 quality-dao + test 依赖 quality-service。
  - `TestErpQaInspectionTrigger`（6 tests：采购 INCOMING+模板行复制/销售 OUTGOING/工单 FINAL/无匹配走默认/无模板无行/强制阻塞 PENDING→ACCEPTED 放行）。
- **Phase 3（NCR 状态机 + CAPA 生命周期 + 自动生成 + 端到端 + 文档）**：
  - NCR 自动生成：`recordResult` REJECTED 时（`erp-qua.auto-create-ncr-on-reject=true` 默认）经 `NcrLifecycleService` 生成 `ErpQaNonConformance`（sourceType=INSPECTION/sourceCode=质检单号/status=OPEN/severity=NORMAL/dispositionType 待裁决）。
  - `IErpQaNonConformanceBiz` NCR 5 态：submitReview（OPEN→IN_REVIEW）、resolve（IN_REVIEW→RESOLVED，**须全部关联 ErpQaAction.status=COMPLETED + verificationPerson/verificationDate 已填**）、escalateToRecall（IN_REVIEW→ESCALATED_TO_RECALL 终态）、cancel（OPEN/IN_REVIEW→CANCELLED）。
  - `IErpQaActionBiz` CAPA：startAction（PENDING→IN_PROGRESS）、completeAction（IN_PROGRESS→COMPLETED+completedAt）、verifyAction（COMPLETED+验证人/验证日期，效果验证闭环）。
  - `TestErpQaNcrCapaEndToEnd`（4 tests：REJECTED→自动NCR→评审→CAPA执行/完成/验证→resolve 门控（未完成/未验证拒绝）→RESOLVED；escalateToRecall 终态；cancel；非法迁移拒绝）。
- 范围保护：无 ORM 变更；purchase(70)/sales(65)/mfg(36) 既有套件全绿为回归门控；NCR 财务过账/召回(2.11)/让步多级审批流/抽检方案/校准/QMS 高级 均为计划内 Non-Goal（已补注 state-machine.md）。
- 验证状态：**全绿（full-green verification）**。`mvn test -pl module-quality/erp-qa-service -am` = 22 tests / 0 Failures（新增 17：StateMachine 7 + Trigger 6 + NcrCapaEndToEnd 4，既有 5 无回归）；purchase/sales/mfg 既有套件无回归；根 `mvn clean install -DskipTests` = BUILD SUCCESS。
- Deferred（Non-Goal，留后继）：NCR 财务过账（退货/返工/报废凭证，触发：finance NCR 驱动过账 Provider 落地）；召回事件 ErpQaRecall（2.11）；让步接收多级审批流（触发：多级审批需求）；抽检方案自动计算（触发：AQL/GB2828 统计抽样）；校准管理（触发：计量管理）；QMS 高级（风险登记/质量目标/评审）。

### 2026-07-03（计划 2237-2 执行：MRP 计算引擎 需求整合→BOM 展开→净需求→计划订单→释放）

- 执行 `docs/plans/2026-07-02-2237-2-manufacturing-mrp-engine.md`（active→completed）。落地 roadmap 工作项 2.3「MRP 计算引擎」。实现 `docs/design/manufacturing/mrp.md`（需求整合 + BOM 多级展开 + 净需求 + 按期分单 + 计划订单生成 + 释放转单）。
- **任务路由**：`app-layer design change + implementation`——纯服务层 + 既有实体（MrpPlan/PlanLine/Demand），无 ORM 变更/不加实体列字典，故无 ask-first 保护区域门控。
- **Phase 1（需求整合 + BOM 展开 + 净需求 + 计划订单 + 测试）**：
  - `DemandAggregator`（新，`service.mrp`）：整合独立需求 → `ErpMfgMrpDemand`——销售订单行未交量（`ErpSalOrder`/`ErpSalOrderLine` 只读，排除作废单）+ 安全库存补货（`ErpMdMaterial.safetyStock` − `ErpInvStockBalance` 可用量 < 0）+ 手工需求；幂等（清除 SALES_ORDER/SAFETY_STOCK 合成行保留 MANUAL）。
  - `MrpEngine`（新，`service.mrp`）：DRAFT→RUNNING→COMPLETED。自顶向下递归：制造件经 `BomExpander.explode`（单级按计划量缩放，递归到采购件，DFS 路径回溯防环）；净需求 = 毛需求 − 可用量（负值归零）；类型判定（有默认 BOM → WORK_ORDER_REQUEST，否则 PURCHASE_REQUEST）；提前期偏移（采购件 `material.leadTimeDays`，制造件 BOM 工序累计 standardTime × `erp-mfg.mfg-leadtime-days-per-routing-hour`）；lot sizing（lot-for-lot，`erp-mfg.default-lot-size`>0 按倍数取整）；写入 `ErpMfgMrpPlanLine`（全列 + `parentLineId` pegging）。
  - `IErpMfgMrpPlanBiz.runMrp` + `ErpMfgMrpPlanBizModel`（aggregate→runMrp，需求列表直接传入避免同事务查询可见性问题）。
  - `erp-mfg-service/pom.xml` 加 `app-erp-sales-dao`/`app-erp-purchase-dao` compile 依赖（manufacturing→sales/purchase 单向 R，DAG 无环）。
  - `ErpMfgConstants`（MRP_STATUS_*/MRP_ORDER_TYPE_*/MRP_DEMAND_SOURCE_*/CONFIG_*）+ `ErpMfgErrors`（ERR_MRP_PLAN_NOT_FOUND/PLAN_LINE_NOT_FOUND/INVALID_PLAN_STATUS/LINE_ALREADY_FIRMED/RELEASE_*）+ beans 注册 DemandAggregator/MrpEngine/MrpReleaseService。
  - `TestErpMfgMrpEngine`（5 tests：需求整合销售+安全+手工/类型判定/pegging、lot-for-lot+固定批量+提前期、负净需求归零、多级 pegging 链、非 DRAFT 拒绝重跑）。
- **Phase 2（释放转单 + 端到端 + 文档）**：
  - `MrpReleaseService`（新）+ `IErpMfgMrpPlanLineBiz`（`releasePurchaseRequest`/`releaseWorkRequest`，purpose-built 两个方法避免 GraphQL 可空参数）+ `ErpMfgMrpPlanLineBizModel`。PURCHASE_REQUEST→`ErpPurOrder`(+`ErpPurOrderLine`，物料/数量/计划到货日，supplierId/currencyId 由调用方提供因 ORM 必填)；WORK_ORDER_REQUEST→`ErpMfgWorkOrder`(productId/bomId=默认BOM/plannedStartDate)。回写 isFirmed/convertedBillCode，全部行 firmed→plan FIRMED；幂等（已 firmed 拒绝）。
  - **Decision（释放实现偏离 Task Route「调 I*Biz」）**：IErpPurOrderBiz/IErpMfgWorkOrderBiz 仅订单头级通用 CRUD（无 createFromMrpLine），故释放直接持久化目标域实体（service-helper 范式，对齐 BomExpander/CostRollupService 与测试种子），仅写 MRP 已知字段。残留：采购单单价/金额=0、币种由参数提供，须采购员补录（已补注 mrp.md）。
  - **Decision（释放分两个方法）**：GraphQL 把 @Name 参数视作非空，单一 `releasePlanLine(planLineId, supplierId, currencyId)` 无法对工单释放传 null supplier。故拆 `releasePurchaseRequest`(supplierId/currencyId 必填) + `releaseWorkRequest`(仅 planLineId)，purpose-built 且语义清晰。
  - `TestErpMfgMrpEndToEnd`（2 tests：销售订单→MRP→释放采购单+工单→isFirmed/convertedBillCode→plan FIRMED→重复释放拒绝；类型不匹配释放拒绝）。
- 范围保护：无 ORM 变更（纯服务层 + 既有表）；既有 CRUD 套件全绿为回归门控；FORECAST（无 ErpMfgForecast 实体）/物料批量字段（minOrderQty/fixedLotSize/lowLevelCode 物化列）/CRP/AUTO_SCHEDULED/需求时界/委外释放/scrapRate 均为计划内 Non-Goal（已补注 mrp.md）。
- 验证状态：**全绿（full-green verification）**。`mvn test -pl module-manufacturing/erp-mfg-service -am` = 36 tests / 0 Failures（新增 7：MrpEngine 5 + MrpEndToEnd 2，既有 29 无回归）；根 `mvn clean install -DskipTests` = BUILD SUCCESS。
- Deferred（Non-Goal，留后继）：FORECAST 需求来源（触发：ErpMfgForecast 实体建模落地）；物料级批量策略字段（触发：精细化批量需求，须 ask-first 加列）；CRP 产能校验（2.8）；MRP 自动定时运行；需求时界；委外 SUBCONTRACT_REQUEST 释放；scrapRate 纳入净需求；释放生成的采购单/工单单据字段完善（供应商默认/单价预填/币种默认）。

### 2026-07-03（计划 2237-1 执行：WorkOrder/JobCard 状态机 + 齐套校验 + 领料/报工/完工 + 成本归集）

- 执行 `docs/plans/2026-07-02-2237-1-manufacturing-workorder-jobcard-state-machine.md`（active→completed）。落地 roadmap 工作项 2.2「WorkOrder/JobCard 状态机 + 审批」。实现 `docs/design/manufacturing/state-machine.md`（工单 10 态 + 作业卡 8 态 + 齐套/领料/报工/完工）。
- **任务路由**：`app-layer design change + implementation`——工单/作业卡状态机 + 齐套校验 + 领料/报工/完工 + 成本归集 + ask-first ORM 类型修正（JobCardTimeLog 工时/费率 VARCHAR→DECIMAL、MaterialIssueLine 实领数量 BOOLEAN→DECIMAL，对齐既有 domain）。服务层为主。
- **保护区域授权**：修正 `module-manufacturing/model/app-erp-manufacturing.orm.xml` 5 列类型——`ErpMfgJobCardTimeLog.durationMins`/`setupMins`/`runMins`（VARCHAR→DECIMAL timeInMins）+ `hourlyRate`（VARCHAR→DECIMAL）+ `ErpMfgMaterialIssueLine.issuedQuantity`（BOOLEAN→DECIMAL quantity）。domain 已声明 DECIMAL，VARCHAR/BOOLEAN 为模型类型缺陷（同 1538-2 范式），pre-production 无数据故安全。经独立草案审查共识（iter-2 accept）+ MISSION_DRIVER 完整执行指令授权；仅改列类型对齐，不加实体/列/字典。重新 codegen 增量。
- **Phase 1（ORM 类型修正 + codegen + 回归）**：5 列类型修正后 codegen 重新生成 `_gen` 实体为 `BigDecimal`（解除报工成本归集数值计算阻塞）；CRUD 套件 0 回归（16 tests 全绿）。
- **Phase 2（工单 10 态状态机 + 三轴审批 + 齐套校验 + 测试）**：`IErpMfgWorkOrderBiz` 状态机方法 submit/approve/checkAvailability/start/stop/resume/close/cancel/reportCompletion（@BizMutation）+ `KitAvailabilityChecker`（新，`service.workorder`）+ `KitAvailabilityResult` DTO。状态机经 `docStatus` 10 态显式守卫（DRAFT→SUBMITTED→NOT_STARTED→STOCK_RESERVED/STOCK_PARTIAL→IN_PROCESS→COMPLETED/STOPPED/CLOSED/CANCELLED），非法迁移抛 `ERR_INVALID_STATUS_TRANSITION`。齐套校验经 `BomExpander.explode` 展开 × plannedQuantity 对照 `ErpInvStockBalance.availableQuantity`（只读，对齐 BomExpander IDaoProvider 范式），全齐→STOCK_RESERVED、部分→STOCK_PARTIAL（缺料明细）。部分齐套强制开工受 `erp-mfg.allow-partial-kit-start`（默认 false）门控，违例抛 `ERR_PARTIAL_KIT_START_FORBIDDEN`。`ErpMfgConstants`（WORK_ORDER_STATUS_*/APPROVE_STATUS_*/CONFIG_*）+ `ErpMfgErrors`（ERR_WORK_ORDER_NOT_FOUND/INVALID_STATUS_TRANSITION/PARTIAL_KIT_START_FORBIDDEN）+ beans 注册 KitAvailabilityChecker。`TestErpMfgWorkOrderStateMachine`（8 tests：主路径/齐套全齐/部分/部分强制开工/stop-resume-close/cancel/非法迁移/超产）。
- **Phase 3（领料确认→出库移动单 + 材料成本回写 + 测试）**：`IErpMfgMaterialIssueBiz.confirm` + `ErpMfgMaterialIssueBizModel` + `MaterialIssueStockMoveBuilder`（新）。confirm：issue-status DRAFT→CONFIRMED→DONE；DONE 调 `IErpInvStockMoveBiz.generateMove`（业务联动自动 DRAFT→DONE 扣减库存，幂等键 `(ERP_MFG_ISSUE, issue.code)`）；回写 `WorkOrderLine.actualQuantity`；汇总出库流水 `ErpInvStockLedger.totalCost`（取 abs）→ `WorkOrder.materialCost`，同步重算 totalCost/unitCost。`erp-mfg-service/pom.xml` 加 `app-erp-inventory-service` test 依赖（领料/完工移动单生成需 inv-service Bean 注册；manufacturing→inventory R，DAG 无环）。`TestErpMfgMaterialIssue`（2 tests：领料出库+成本汇总、幂等）。
  - **Decision（领料出库 moveType 偏离计划字面）**：库存域 `StockMoveBookkeeper.bookCompletion` 按 moveType 决定方向——MANUFACTURING(40) 视为**入库**（仅完工入库用），OUTGOING(20) 才扣减余额写负号流水。故领料出库用 `MOVE_TYPE_OUTGOING_ISSUE(20)`（偏离计划「moveType=MANUFACTURE」字面，已补注 state-machine.md；完工入库 Phase 4 用 MANUFACTURING 入库）。替代：MANUFACTURING 出库（库存域加库存，与领料语义相反，rejected）。
- **Phase 4（报工 + 完工入库 + 成本归集 + 质检钩子 + 端到端 + 文档）**：`IErpMfgJobCardBiz` 作业卡 8 态（startJob/recordWork/submitJob/completeJob/holdJob/resumeJob/cancelJob）+ `ErpMfgJobCardBizModel` + `JobCardWorkRecord` @RequestBean DTO（@DataBean，erp-mfg-dao/biz）。recordWork：WORK_IN_PROGRESS/SUBMITTED 录 `JobCardTimeLog`，人工成本 = `durationMins/60 × hourlyRate`（Phase 1 类型修正后数值）→ timeLog.laborCost，累加 `WorkOrder.laborCost`（+ JobCard completedQuantity/scrappedQuantity 累加）。`reportCompletion` Phase 4 完整版：累加完工量 + 重算 totalCost/unitCost + 生成产成品入库移动单（MANUFACTURING 入库方向，relatedBillType=ERP_MFG_WORK_ORDER）+ 完工达量→COMPLETED + 完工质检 config-gated 钩子（BOM.inspectionRequired=true 且 `erp-mfg.inspection-gate-enabled`=true 且达量→抛 `ERR_INSPECTION_REQUIRED` 待质检，不生成入库移动单保持 IN_PROCESS；gate 默认 false）。`TestErpMfgWorkOrderEndToEnd`（3 tests：端到端 happy path 材料成本=10/人工成本=30/总成本=40/单位成本=40、质检门控 gate=true 拒绝/gate=false 正常、作业卡状态机）。
  - **Decision（人工/制造费用分列）**：工作中心仅有单一 `hourlyRate`（无独立制造费率分列，同 1538-2），故报工工时成本统一计入 `laborCost`，`overheadCost`=0；待工作中心费率拆分后细化（Follow-up）。
  - **Decision（质检软依赖 config-gated 钩子）**：选 config-gated 钩子（`erp-mfg.inspection-gate-enabled` 默认 false）替代向 work-order-status 加 INSPECTING 态（字典无此态，设计文档 §质检约束声明漂移；加态需 ORM 字典变更 + 状态机复杂化，rejected）。gate=false 时完工不等待质检（与 §质检约束声明 有偏差，已补注 state-machine.md；2.4 quality 落地后 flip config）。
  - **Decision（齐套校验只读 + cancel 逻辑释放）**：checkAvailability 仅读余额可用量置状态，不写库存预留（实际扣减由开工后领料出库移动单完成，对齐 cross-domain §余量校验规则）；cancel 仅状态迁移（无预留记录需释放）。
- 范围保护：ORM 变更仅 5 列类型对齐既有 DECIMAL domain，重新 codegen 增量；既有 CRUD 套件全绿为回归门控；未触动质检/NCR/CAPA（2.4）、完工成本结转凭证（依赖 finance 制造过账 Provider）、APS 排产、BOM 版本快照、联副产品分摊、返工工单、超产配置、INSPECTING 态（均计划内 Non-Goal）。
- 验证状态：**全绿（full-green verification）**。`mvn test -pl module-manufacturing/erp-mfg-service -am -Dtest=TestErpMfg*` = 29 tests / 0 Failures（新增 13：WorkOrderStateMachine 8 + MaterialIssue 2 + WorkOrderEndToEnd 3，既有 16 无回归）；根 `mvn clean install -DskipTests` = BUILD SUCCESS。
- Deferred（Non-Goal，留后继）：完整质检/NCR/CAPA（2.4，触发条件：quality 落地后 flip inspection-gate-enabled=true）；完工成本结转凭证（触发条件：finance 制造业财一体过账 Provider 落地）；APS 排产集成（3.10）；BOM 版本快照；联副产品分摊；返工工单；超产配置；work-order-status INSPECTING 态裁决（2.4 后）；制造费率独立分列。
