# 2026-07-06 开发日志

## 域运营事件通知消费者接线 — plan 2026-07-06-0642-1

### 本次完成的工作

执行 `docs/plans/2026-07-06-0642-1-operational-notification-consumers.md`（6 个域运营事件接入通知派发通道 + 2 个提醒类 scheduler job 接线），3 Phase 全部完成，full-green verification（`mvn clean install -DskipTests` BUILD SUCCESS；`mvn test -pl module-cs/erp-cs-service,module-finance/erp-fin-service,module-sales/erp-sal-service,module-crm/erp-crm-service,module-manufacturing/erp-mfg-service -am` 全绿，含新增 6 个测试类 20+ test methods）。

1. **Phase 1 — 既有种子模板消费者接线（CS SLA / 过账异常 / 销售信用）**：
   - CS (`ErpCsTicketBizModel`)：`scanOverdueTickets` 升级 + `findSlaWarnings` 预警均调 `notify("cs.sla-overdue", ctx)`，config-gated `erp-cs.sla-notify-enabled`（默认 true）。新增 master-data-dao 编译依赖 + IErpMdPartnerBiz 注入（查 customerName）。复用既有 `erp-cs-sla-scan` scheduler job 自动派发 SLA 通知（无需新增 job）。
   - Finance (`ErpFinPostingExceptionRecorder`)：异常记录以 REQUIRES_NEW 提交后，在第二个 REQUIRES_NEW 事务内调 `notify("fin.posting-exception", ctx)`，config-gated `erp-fin.posting-exception-notify-enabled`（默认 true）。双 REQUIRES_NEW 隔离确保通知独立于主过账事务回滚。金额字段从 eventData JSON 派生（无则模板不渲染）。
   - Sales (`CreditLimitChecker`)：SOFT_WARNING 路径放行后调 `notify("sal.credit-over-limit", ctx)`，config-gated `erp-sal.credit-notify-enabled`（默认 true）。HARD_BLOCK 拒绝路径不通知。新增 orderCode 参数到 `check()` 签名。
2. **Phase 2 — 提醒类 scheduler job 接线（CRM 活动 / CSAT 调查）**：
   - 新增 `ErpCrmEventReminderJob`：cron 双层门控（`erp-crm.event-reminder-cron`）→ `findDueReminders(windowMinutes)` → 逐事件 `notify("crm.event-reminder", ctx)`，单条失败隔离。设计默认 cron `0 0/15 * * * ?`（每 15 分钟）。
   - 新增 `ErpCsCsatReminderJob`：cron 双层门控（`erp-cs.csat-reminder-cron`）→ `findSurveyReminders` + `findExpiredSurveys` → 逐调查 `notify("cs.csat-reminder", ctx)`。设计默认 cron `0 0 2 * * ?`（每日 02:00）。
   - 完整三件套：scheduler.yaml 新增 2 条 job + 各域 `app-service.beans.xml` 注册 bean + 2 个新种子模板（mysql/oracle/postgresql 三套）。
3. **Phase 3 — 生产差异阈值告警接线**：`ProductionVarianceCalculator.calculateVariances()` 计算结果落定后按 `erp-mfg.variance-alert-threshold`（默认 100）判定最大净差异行，超阈值时调 `notify("mfg.production-variance", ctx)`，config-gated `erp-mfg.variance-alert-enabled`。Decision：旁路告警（不在过账 Dispatcher 内）保持过账纯事务性。新增 1 个种子模板（异常告警类，1 分钟合并含次数，三套 SQL）。

### 关键决策与执行现实

- **跨域依赖方向**：所有 6 个消费者单向 `→ erp-notify-dao`（compile），notify 是干净叶子模块（仅依赖 parent+codegen），无环。erp-notify-dao 编译依赖 + erp-notify-service test 依赖的模式与既有 `purchase→inventory-dao`/`sales→finance-dao` 范式一致。
- **可选注入（ioc:optional）**：`ErpFinPostingExceptionRecorder` 与 `CreditLimitChecker` 的 notificationBiz 注入用 `<ioc:inject type="..." ioc:optional="true"/>` 标记可选。理由：单域测试（如 inventory 引入 finance-service 但无 notify-service）时不强制要求 notify bean 存在，避免下游测试全量失败。生产环境（app-erp-all 聚合）notify bean 总是注册。
- **Finance 双 REQUIRES_NEW 隔离**：notify 不能在主过账事务中（外层正在回滚会吞掉通知），也不能在第一个 REQUIRES_NEW 内（afterCommit 需事务提交但语义混乱）。采用"第一个 REQUIRES_NEW 提交异常记录 → 第二个 REQUIRES_NEW 调 notify"双层隔离，对齐 plan §Phase 1 Finance 项 Decision。
- **notify 失败降级**：所有 6 个消费者在 notify 调用外包 try/catch（warn-and-continue），与通知派发通道侧"config-gated 静默跳过不阻断业务"语义一致。
- **scheduler.yaml 双层门控**：scheduler.yaml 的 cronExpr 是设计值（job 总会触发），实际执行门控由 job bean 内 `resolveCronConfig()` 读取 config 键（空=跳过）。这是 nop-job-local 三件套范式（见 `0306-1`）。

### 验证基线（全绿）

- `mvn clean install -DskipTests` → BUILD SUCCESS（154 reactor 模块）
- `mvn test -pl module-cs/erp-cs-service -am` → 27 tests, 0 failures（含新增 `TestErpCsSlaNotification` 3 tests + `TestErpCsCsatReminderJob` 4 tests）
- `mvn test -pl module-finance/erp-fin-service -am` → 全绿（含新增 `TestErpFinPostingExceptionNotify` 2 tests）
- `mvn test -pl module-sales/erp-sal-service -am` → 全绿（含新增 `TestErpSalCreditNotify` 3 tests）
- `mvn test -pl module-crm/erp-crm-service -am` → 全绿（含新增 `TestErpCrmEventReminderJob` 4 tests）
- `mvn test -pl module-manufacturing/erp-mfg-service -am` → 73 tests, 0 failures（含新增 `TestErpMfgVarianceAlert` 3 tests）
- `mvn test -pl module-inventory/erp-inv-service -am` → 63 tests, 0 failures（验证可选注入修复后无回归）

### 下一步

- 解除三处前置 Deferred 触发条件（本计划已满足）：`0306-1` 提醒类 job、`1838-2` 差异告警通道、`0504-1` 全量消费者接线。
- 精确接收人路由（角色基础设施落地后，DEFERRED 见计划末尾）。
- 审批工作流通知（归 `2026-07-06-0642-2` 独立结果面）。

## 报表渲染子系统 module-finance — plan 2026-07-06-0504-2

### 本次完成的工作

执行 `docs/plans/2026-07-06-0504-2-report-rendering-subsystem.md`（报表渲染子系统，8+ completed 计划 deferred 项的渲染基础设施），4 Phase 全部完成，full-green verification（`mvn clean install -DskipTests` 154 reactor 模块 BUILD SUCCESS；`mvn test -pl module-finance/erp-fin-service` 170 测试全绿，0 failures/0 errors，含新增 `TestErpFinReportRendering` 8 tests）。

1. **Phase 1 — 2 项 Decision + 渲染 BizModel 骨架**：(a) 种子报表用 `.xpt.xml`（XML 序列化，与 `.xpt.xlsx` 等价、便于随仓版本化/审计），运行时 DB 定义共存按"VFS 种子优先 + DB 覆盖"记录于 `print-template.md`；(a) 复用平台 `NopReportDefinition` 拒绝自建 `ErpSysDocumentTemplate` 平行实体。新增 `ErpFinReportBizModel`（`@BizModel("ErpFinReport")`，注入 `IReportEngine`，`renderHtml(reportName,data)`/`download(reportName,renderType,data)` @BizQuery，`IServiceContext context` 末参；模板名 `StringHelper.isValidVPath` 校验防注入 → `ERR_REPORT_NAME_INVALID`/`ERR_REPORT_RENDER_TYPE_INVALID`）。注册于 `app-service.beans.xml`（`ioc:type="@bean:id"`）。
2. **Phase 2 — 财务三大表**：3 张 `.xpt.xml` 模板（资产负债表/利润表/现金流量表，单元格 `*=^ds!<field>` 展开 + `<valueExpr>SUM</valueExpr>` 合计）；数据集构造 `buildBalanceSheetDataset`（`ErpFinGlBalance` 按 `subject.subjectClass` 汇总 ASSET/LIABILITY/EQUITY，期末 closingDebit/Credit 按方向取净值）/ `buildIncomeStatementDataset`（INCOME/EXPENSE/COST 本期 periodDebit/Credit 净额）/ `buildCashFlowDataset`（`ErpFinVoucherLine` 现金类科目 `1001/1002/1012/1031` 借-贷净变动 INFLOW/OUTFLOW，简化直接法，完整 IAS7 归后续）。
3. **Phase 3 — AR/AP 账龄 + 期末结账报告**：`buildArApAgingDataset`（`ErpFinArApItem` OPEN/PARTIAL，dueDate/businessDate 基准，分桶 0-30/31-60/61-90/90+）；`buildPeriodCloseDataset`（期间/模块状态 AR/AP/INV/GL/AST + 凭证统计 + PERIOD_CLOSE/EXCHANGE_GAIN_LOSS 业财回链凭证计数，经 `ErpFinVoucherBillR` 按 voucherId 集合 scope）。
4. **Phase 4 — 菜单接入 + 文档修正**：`erp-fin.action-auth.xml` fin-report 分组扩展 2 项（ar-ap-aging、period-close-report，共 5 项可达）；5 张 AMIS 报表页面（`pages/report/*.page.yaml`，调 `ErpFinReport__renderHtml`/`__download`）；`print-template.md` 全文重写（复用 `NopReportDefinition` + `IReportEngine`，移除 `ErpSysDocumentTemplate`）；`dashboards.md` 标注看板归后续（渲染能力已就绪）。

### 关键决策与执行现实

- **数据集构造路线**：选"程序构造"（BizModel 聚合 ORM 实体 → `IEvalScope` 注入模板 → 模板 `*=^ds!<field>` 展开）而非 `UseOrmDataSet`/`UseJdbcDataSet` 模板内 SQL。理由：可单元测试、不耦合 DB schema 到模板、口径逻辑收敛在 BizModel。数据集聚合全部包在 `ormTemplate.runInSession` 内，避免直接调用（非 GraphQL 上下文）时懒加载关系（`GlBalance.getSubject()`）session-closed。
- **PDF + CJK 字体**：`nop-report-core` 仅 html+xlsx；PDF 走 `nop-report-pdf`（PDFBox 直接渲染，新增 compile 依赖）。PDFBox 标准 14 字体（Helvetica）不支持中文，`FontManager` 回退 `/fonts/default.ttf`（VFS）。从 `nop-report-pdf` 测试资源复制 14MB CJK TTF 到 `erp-fin-service/_vfs/fonts/default.ttf`（随仓 ship，prod + test 均生效）。字体瘦身/上提到 app-erp-all 归后续。
- **路径注入防护**：`resolveReportPath` 双层校验——`StringHelper.isValidVPath` 拒绝 `../`/空串 + 渲染类型白名单 `html/xlsx/pdf`。测试 `testPathInjectionRejected` 覆盖三类非法输入。
- **跨实体访问合规**：`ErpFinReportBizModel` 经 `IDaoProvider` 只读 `ErpFinGlBalance/ErpFinVoucher/ErpFinVoucherLine/ErpFinArApItem/ErpFinAccountingPeriod/ErpFinAccountingPeriodStatus/ErpFinVoucherBillR/ErpMdSubject`（报表只读聚合，无写操作，无 `I*Biz` 跨域调用）。

### 验证基线（全绿）

- `mvn clean install -DskipTests` → BUILD SUCCESS（154 reactor 模块）
- `mvn test -pl module-finance/erp-fin-service -Dsurefire.failIfNoSpecifiedTests=false` → Tests run: 170, Failures: 0, Errors: 0, Skipped: 0（含 `TestErpFinReportRendering`：五报表 renderHtml 非空、download xlsx/pdf 文件有效、三大表数据集口径断言、账龄分桶合计=辅助账开口项、结账报告覆盖模块状态+损益/汇兑凭证、路径注入防护）

### 下一步

- 解除下游 Deferred（渲染能力就绪即可启动）：1000-3 结账报告细化、0540-2 年度三大表年度口径、1707-1 CRP 报表、1838-2 差异报表、0427-1 预测差异报表、0700-1 追溯可视化、0700-2 员工净余额报表、2200-3 薪酬模拟对比报表（各域按 owner doc 独立接线 `IReportEngine`）。
- 经营看板前端实现（独立前端计划，渲染能力 + KPI 聚合方法已就绪）。
- 单据打印/套打（复用 `NopReportDefinition` + `IReportImage.print=false` 套打机制，独立计划）。

---

## 通知派发子系统 module-notify — plan 2026-07-06-0504-1

### 本次完成的工作

执行 `docs/plans/2026-07-06-0504-1-notification-dispatch-subsystem.md`（通知派发子系统，8+ completed 计划 deferred 项的通道基础设施），4 Phase 全部完成，full-green verification（`mvn clean install -DskipTests` 154 reactor 模块 BUILD SUCCESS；`mvn test -pl module-notify/erp-notify-service -am` 9 测试全绿：5 dispatch + 4 seed-template）。独立结束审计通过（OVERALL: close，8 项验证全通过）。

1. **Phase 1 — 3 项 Decision**：(a) 新建 `module-notify` 独立模块（非 app-erp-all 内置/复用 sys 域）；(a)+(b) 接收人解析 ROLE（nop-auth 角色名→NopAuthUserRole→userId）+ USER_LIST/ORG/PARTNER，动态组织层级 Deferred；不接入 nop-message 异步总线（同步 + txn().afterCommit，生产部署/多实例/削峰触发后继）。
2. **Phase 2 — ORM + codegen 骨架**：`module-notify/model/app-erp-notify.orm.xml`（3 实体 ErpSysNotificationTemplate/ErpSysNotification/ErpSysNotificationRead + 5 字典 `erp-notify/*`）；`nop-cli gen` 生成 7 子模块标准链；根 pom + app-erp-all（+nop-integration-api）接线；gen-orm.xgen 路径修正（`erp-notify.orm.xml`→`app-erp-notify.orm.xml`）。
3. **Phase 3 — 派发引擎 + 频控合并**：`NotificationDispatcher`（模板渲染 ${var} 插值 + 接收人解析 + 频控合并 + 站内落库 + config-gated 外发 IEmailSender/ISmsSender）；`NotificationRecipientResolver`（ROLE/USER_LIST/ORG/PARTNER）；`NotificationMergeCoordinator`（窗口内同 user+eventType 合并，状态含 MERGED 可二次合并，mergeCount 递增）；`ErpSysNotificationBizModel`（notify/markRead/markAllRead/findUnread/countUnread）；4 ErrorCode + NoopEmailSender/NoopSmsSender（bootstrap 满足 IoC 注入，生产真实实现覆盖）。
4. **Phase 4 — 种子模板 + 端到端 + owner-doc 修正**：3 类种子模板 SQL（cs.sla-overdue 业务提醒 5min/MERGE_BY_USER_TYPE、fin.posting-exception 异常告警 1min/MERGE_BY_USER_TYPE、sal.credit-over-limit 系统通知 NONE）；`TestErpSysNotificationDispatch`(5) + `TestErpSysNotificationSeedTemplates`(4) 端到端全绿；`notification-strategy.md` 实现方案修正（外发走 nop-integration、站内消息为本子系统、nop-message 异步总线归后继，纠正"使用 nop-message 组件"漂移）。

### 关键决策与执行现实

- **模板渲染用 ${var} 插值而非完整 XLang 表达式求值**：平台 Expression AST 经编译管道求值入口较重，bootstrap 用正则 `${var}` 插值（与 XLang TemplateStringExpression 语义兼容），复杂表达式求值归后继。
- **NoopEmailSender/NoopSmsSender 满足 IoC**：bootstrap 无真实供应商，`@Inject IEmailSender` 需 bean 存在；no-op 默认 bean（ioc:default=true）满足注入，config-gated 默认不调用，生产真实实现覆盖。
- **merge 查询状态含 MERGED**：合并后状态置 MERGED，窗口内三次合并需 findMergeable 匹配 PENDING/SENT/MERGED（否则第三次新建），mergeCount=3。
- **daoProvider 不在子类重声明**：CrudBizModel 已有私有 daoProvider 字段，子类同名字段会 shadow 父类导致 requireEntity NPE；用父类 `daoProvider()` 方法访问。
- **markRead userId 取 recipientUserId**：与 findUnread/countUnread 口径一致（按投递接收人），非上下文当前用户。

### 后续

- 8 域消费者接线（CRM 提醒/CSAT/SLA 升级/差异预警/工作流抄送/签章超时/排班提醒，通道已就绪各自独立计划）；nop-message 异步总线（生产部署触发）；webhook 通道；通知中心前端；动态组织层级精确路由；角色基础设施（均 Deferred）。

---

## WORKFLOW 模式 .xwf 审批流定义 — plan 2026-07-06-0315-1

### 本次完成的工作

执行 `docs/plans/2026-07-06-0315-1-workflow-approval-xwf.md`（use-approval 迁移 2050-1 的 WORKFLOW 后继），3 Phase 全部完成，full-green verification（`mvn clean install -DskipTests` 146 模块 BUILD SUCCESS；全量 `mvn test` BUILD SUCCESS 0 failures / 0 errors；4 域 pur 94 / sal 80 / ast 31 / hr 39 全绿 0 回归；4 新增 WORKFLOW 测试类 12 测试全绿）。

1. **Phase 1 — Decision + ORM/xmeta WORKFLOW 配置**：3 项 Decision（审批链组成 / HR actor 路由 / wf 回调配置）；4 实体 ORM 加 `useWorkflow="true"`（codegen 自动补 `nopFlowId` 列，propId 30/30/29/92）；4 xmeta 加 `wf:wfName`（payment/receipt/asset-disposal/salary-approval）。
2. **Phase 2 — 4 .xwf + listener + submitForApproval wf 启动**：`salary-approval`（三级 hr-review→finance-review→manager-approval）/`payment-approval`/`receipt-approval`/`asset-disposal-approval`（两级）`.xwf` 落地；listener 回调业务 approve/reject（按 `wf-approval.xlib` notifyResult 标签源码内联，因 listener XPL 作用域 xlib 标签不可解析）；3 Processor 实体 xbiz submitForApproval 追加 wf 启动 + ErpHrSalary 新增 submitForApproval 覆盖（应对平台标准 source 对 Long 主键 CCE 的已知限制）。
3. **Phase 3 — WORKFLOW 测试 + 回归**：4 新增 WORKFLOW 测试类（submit→agree→approve 闭环 + disagree→reject + REJECTED 重提）；8 类既有测试因 submit 现启动 wf 加 `@BeforeEach setUpWfUser`（SYS 用户）适配。

### 关键决策与执行现实

- **bootstrap actor 用 `actorType="all"`**（原 Decision 2 选 StarterManager）：ERP 无组织/角色基础设施 + 测试库无 NopAuthUser 种子，StarterManager 解析失败。多级语义由步骤链体现，精确角色路由 Deferred。
- **驳回整单走 oa.xwf `disagree`**（非 `reject`）：`reject` 是退回上一步（不结束 wf），`disagree` 才驳回整单并 `<to-end/>`。listener 按 `lastAction==='disagree'` 判定。
- **`*end` 双触发幂等**：引擎 BEFORE/AFTER_END 常量同值，listener 在两处都触发，已终态（APPROVED/REJECTED）时跳过。
- **平台 approval-support.xbiz 对 Long 主键 CCE**：Non-Goal 不改平台源码，应用层 xbiz 显式 `'' + entity.id` 转 String。Processor 零改动（wf 启动由 xbiz 承担）。

### 后续

- 金额阈值条件路由 / 前端审批 UI / 委托转办 / 通知抄送 / HR 精确角色路由（均 Deferred，见计划）。

---

## 存货成本调整单 + 标准成本重估 — plan 2026-07-05-2352-3

### 本次完成的工作

执行 `docs/plans/2026-07-05-2352-3-inventory-cost-adjustment.md`（成本调整单 + 标准成本重估，costing 模块完整性收口 deferred successor），4 Phase 全部完成，验证全绿（`mvn clean install -DskipTests` 146 模块 BUILD SUCCESS；`mvn test` 全量 0 failures / 0 errors；inventory-service 63 tests 含新增 `TestErpInvCostAdjust` 8 场景全绿）。

1. **Phase 1 — 实体建模 + 业务类型 + 4 项 Decision**：`erp-inv/adjust-type` 字典（PURCHASE_PRICE_ADJUST/COST_DIFFERENCE/STANDARD_REVALUATION/LANDED_COST_SUPPLEMENT）；`ErpInvCostAdjust` 头 + `ErpInvCostAdjustLine` 行实体落地（头-行 Decision，UK_INV_COST_ADJUST_CODE_ORG + 索引）；`ErpFinBusinessType.COST_ADJUSTMENT(420)` 枚举 + `erp-fin/business-type` 字典加性；codegen 重新生成实体/DAO/IBiz/BizModel 空壳/xbiz 通过。
2. **Phase 2 — 成本调整引擎 + 审批门控**：`CostAdjustmentService`（纯成本变更：按行更新 `ErpInvStockBalance.avgCost/totalCost` + 写 `ErpInvStockLedger` 流水 quantity=0/moveId=0 哨兵 + FIFO 追加 delta 调整层 `incomingMoveId=-行ID` 负值哨兵 + STANDARD_REVALUATION 发布 FIRMED `ErpMfgCostRollup` 行）；`ErpInvCostAdjustProcessor`（DIRECT 审批状态机 5 action + `applyCostAdjust`/`reverseCostAdjust`，`erp-fin.cost-adjust-approval` config-gated）；5 个 ErrorCode 加性。
3. **Phase 3 — 过账 Provider + 派发器 + 接线**：`CostAdjustmentAcctDocProvider`（方向相关——成本增加 借存货(1401)/贷成本差异(6603)，成本减少反向）；`CostAdjustmentPostingDispatcher`（构造 `PostingEvent` → `IErpFinVoucherBiz.post`，resolveAcctSchemaId 从 orgId，reverse 红字 + markOriginalVoucherReversed）；4 个 Bean 注册 `app-service.beans.xml`；`applyCostAdjust` 仅在过账成功（voucherId≠null）置 posted=true（对齐 asset 价值调整范式）。
4. **Phase 4 — 行为测试 + owner doc**：`TestErpInvCostAdjust` 8 类场景（MA 增/减 + FIFO 调整层 + STANDARD 重估 FIRMED 发布 + 审批门控开/关 + 重复 apply 防护 + reverse 红冲回退 + 无余额/负成本拒）全绿；`costing-methods.md §成本调整` Non-Goal 转 ✅ 落地 + 实现注记（头-行/FIFO delta 层/标准成本重估 rollup 发布/制造件重估归 successor）；1538-1/0427-2 Deferred 状态更新。

### 关键决策

- **头-行结构**：成本调整常涉及多物料/多仓库一次调整，采用头-行 cascade（对齐采购订单/退货单），而非 costing-methods.md :404 草稿的单层结构。
- **FIFO delta 调整层**：FIFO 物料成本调整追加「delta 调整层」（unitCost=新旧单位成本差，incomingMoveId=-行ID 负值哨兵），保持 FIFO 先进先出不变量；reverse 据此精确删除。残留风险：FIFO 队列长度增长。
- **纯成本变更流水 moveId 哨兵**：成本调整无 StockMove（Non-Goal 不生成移动单），流水 moveId/moveLineId 置 0 哨兵标识非移动单来源；成本调整过账经独立 `CostAdjustmentPostingDispatcher`（不经 `InvPostingDispatcher` 的 moveId 通道），故 0 哨兵不冲突。
- **posted=true 仅在过账成功**：apply 执行成本变更（幂等已记账），但 posted=true 仅在 voucherId≠null 时置位（对齐 asset 价值调整范式），避免过账失败但 posted=true 的不一致态。

### 下一步

- 到岸成本（Landed Cost）分摊算法（Deferred，触发：到岸成本模块；已预留 `adjustType=LANDED_COST_SUPPLEMENT` 码值）。
- 制造件标准成本重估编排（Deferred，触发：制造件周期重估需求；归制造域 `rollupCost` successor）。
- BATCH/INDIVIDUAL 计价成本调整（Deferred，触发：BATCH/INDIVIDUAL 计价落地）。
- 成本调整批量/多账套/报表（Deferred，触发：相关需求）。

---

## 全域高频过滤索引设计 — plan 2026-07-05-2352-1

### 本次完成的工作

执行 `docs/plans/2026-07-05-2352-1-db-index-design.md`（O2 残留「非 code 高频过滤索引」deferred successor），3 阶段全部完成，验证全绿（`mvn clean install -DskipTests` 146 模块 BUILD SUCCESS；`mvn test` 全量 1532 tests / 0 failures / 0 errors）。独立结束审计 VERDICT: PASS。

1. **Phase 1 — 索引策略决策**：5 项 Decision 裁决并固化于计划——命名 `IDX_{TABLE_NO_ERP_PREFIX}_{COLS}`（与 1000-1 的 `UK_*` 一致，`orgId` 段缩写 `ORG`）；多租户 `orgId` 复合前缀（最左前缀）；`delVersion` 不入索引（Decision 3 选择性原则）；选择性门槛（FK `*Id` 必加、`docStatus`/`approveStatus` 加、`code` 由 UK 覆盖不重复、`posted`(BOOLEAN)/`status`(active-status 二态) 跳过）；异步入口核查（Decision 5：无独立 webhook 入口，`ErpB2bMftLog.messageId` 由 FK 单列索引覆盖，无需组合索引）。
2. **Phase 2 — 18 域批量补充**：新增幂等脚本 `scripts/add-orm-indexes.js`（确定性规则映射，可重复运行），为 18 域源模型补齐 **916 个 `<index>`**（覆盖 279 个非桩实体）。规则：含 orgId 实体 `(orgId, docStatus)`/`(orgId, businessDate)` 复合 + 外键 `*Id` 单列导航；全局主数据按业务列。脚本同步生成派生 DDL `module-<domain>/deploy/sql/{mysql,postgresql,oracle}/_create_index.sql`（54 文件）——**平台 `_create_*.sql.xgen` 模板不输出 CREATE INDEX**（对照 nop-app-mall 模式确认），运行时由 `ddl.xlib AddIndex` 从模型自动应用，`_create_index.sql` 仅用于手工生产部署对齐。
3. **Phase 3 — 验证与文档对齐**：3 方核对（source `<index>` == 生成 `_app.orm.xml` == `_create_index.sql` `CREATE INDEX`）全域 18 域 916/916，0 mismatch；`mvn test` 全量 0 回归；`docs/architecture/idempotency-pattern.md` 规则 2 新增「过滤索引全域落地」+「异步入口组合幂等键核查」两条记录；`docs/plans/2026-07-01-1900-1-platform-compliance-remediation.md` O2 标 ✅ 闭环（O2 至此全部闭环）。

### 关键决策

- **批量编辑脚本化**：419 个实体声明（279 非桩 + 桩）×逐实体索引分析，手工不可行；沿用仓库 `scripts/*.js` 惯例写 `add-orm-indexes.js`（确定性、幂等：列解析限定 `<columns>` 段避免索引 `<column>` 污染；实体正则要求 `className=` 以跳过桩和注释中的 `<entity>` 字面量）。
- **DDL 生成机制澄清（计划基线订正）**：草案假设「`_create_*.sql` 自动含 CREATE INDEX」与平台实际不符（`_create.sql.xgen`/ddl.xlib CreateTables 不输出索引；AddIndex 仅运行时差分）。订正为：ORM `<indexes>` 为唯一真相源 + 派生 `_create_index.sql` 工件，与 nop-app-mall 既定模式一致。
- **状态列选择性细化**：草案 Decision 4 一概「状态列加」，执行期按 Decision 3 推广——`docStatus`/`approveStatus`（多态）加，`status`(active-status 二态)/`posted`(BOOLEAN) 跳过，避免无用低选择性索引。

### 下一步

- 查询性能基准测试（Deferred，触发：首次性能测试 / 生产数据规模）。
- 数据库特定索引优化（Deferred，触发：目标生产数据库确定）。
- 业务功能深化（见 `docs/backlog/`）。

---

## NCR 财务过账引擎 — plan 2026-07-05-2352-2

### 本次完成的工作

执行 `docs/plans/2026-07-05-2352-2-ncr-financial-posting.md`（NCR 驱动自动退货/报废过账 quality→finance 业财一体面，2237-3 deferred successor），4 Phase 全部完成，验证全绿（`mvn clean install -DskipTests` 146 模块 BUILD SUCCESS；`mvn test` quality-service 40 tests / 0 failures / 0 errors）。

1. **Phase 1 — 4 项 Decision**：(a) posted 机制选加 `posted`/`postedAt`/`postedBy` 列 + `returnCode` 列到 `ErpQaNonConformance`；(b) 返工映射选 (b) 不加 REWORK 字典/NCR_REWORK 业务类型，返工经制造域返工工单；(c) 业务类型仅新增 `NCR_SCRAP(410)`（退货编排退货域，NCR 侧不另过账）；(d) DOWNGRADE 同 CONCESSION 无凭证拒。
2. **Phase 2 — 业务类型 + Provider + 派发器**：`ErpFinBusinessType.NCR_SCRAP(410)` 枚举 + `erp-fin/business-type` 字典同步；`NcrScrapAcctDocProvider`（借 6711 营业外支出/贷 1401 库存商品，承接 PPV 范式）；`NcrPostingDispatcher`（从 `ErpInvStockBalance.avgCost` 算金额 → 构造 `PostingEvent` → `IErpFinVoucherBiz.post` → 置 posted 三件套）；`NcrPostingExecutor`（跨域经 Facade）；`erp-qa-service` pom 加 `app-erp-finance-service`/`app-erp-purchase-dao` compile 依赖。
3. **Phase 3 — 触发接线 + 退货编排**：`ErpQaNonConformanceBizModel.resolve` 末尾按 `erp-qua.ncr-posting-mode`（AUTO_POST/MANUAL_POST）config-gated 分派；`postNcr(ncrId)`/`reverseNcr(ncrId)` @BizMutation 人工入口 + posted-flag 守卫；`NcrReturnOrchestrator` 按 supplierId 判定采购/销售退货域（`@Nullable` optional inject 避免 reactor-cycle bean 初始化失败）；5 个 ErrorCode 加性新增。
4. **Phase 4 — 行为测试 + owner doc**：`TestErpQaNcrPosting` 7 类场景（SCRAP happy/RETURN 编排/CONCESSION 拒/AUTO vs MANUAL/重复过账防护/reverseNcr 红冲/未 RESOLVED 拒）全绿；test-mock-posting beans stub（purchase + sales return）；`state-machine.md §NCR 财务影响规则` Non-Goal 转 ✅ 落地标记 + 实现偏离补注。

### 关键技术发现

- **`@Nullable` optional inject 解决 reactor-cycle**：quality-service 测试不引入 purchase-service/sales-service（避免 reactor 环），`NcrReturnOrchestrator` 的 `@Inject IErpPurReturnBiz`/`IErpSalReturnBiz` 在无桩测试中导致 bean 初始化失败。加 `@jakarta.annotation.Nullable` 使 Nop IoC `DefaultBeanClassIntrospection` 检测到 `isAnnotationPresent(Nullable.class)` → `optional=true` → `resolveBeanByType` 返回 null → `buildInjectResolver` 返回 null → 属性跳过注入。运行时（全 app）或测试（test-mock beans）有对应 bean 时正常注入。
- **物理出库简化 Decision**：NCR_SCRAP 凭证贷记存货（1401）已表达报废消耗会计影响；物理库存量（ErpInvStockBalance.totalQuantity）同步扣减需调 `IErpInvStockMoveBiz.generateMove`，但出库移动会触发 InvPostingDispatcher SALES_OUTPUT 双计存货贷方。本期简化为凭证过账独占，物理量扣减归 inventory 域 successor。

### 下一步

- 独立子代理结束审计（Closure Gates 最后一项 `[ ]`）。
- 返工工单制造编排（Deferred，触发：制造域返工工单编排）。
- 报废批次/序列号追踪（Deferred，触发：批次召回/序列号报废核算）。

---
