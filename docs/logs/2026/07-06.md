# 2026-07-06 开发日志

## 剩余 6 域看板 AMIS 前端（资产/项目/制造/维护/质量/主数据）— plan 2026-07-06-1606-2

### 本次完成的工作

执行 `docs/plans/2026-07-06-1606-2-remaining-domain-dashboards-frontend.md`（6 域看板 AMIS 前端页面，替换 6 个占位 `main.page.yaml`），3 Phase 全部完成，full-green verification（`mvn clean install -DskipTests` BUILD SUCCESS 154 模块；全 workspace `mvn test` BUILD SUCCESS 0 failures/0 errors；6 action-auth `xmllint --noout` well-formed；6 page.yaml YAML 可解析；6 域 `ErpXxxDashboard__*` GraphQL 调用逐一映射到 1606-1 已审计 BizModel 真实 `@BizQuery` 方法 ast 4 / md 3 / prj 4 / mfg 4 / mnt 4 / qa 4）。

1. **Phase 1 — 资产看板 + 主数据看板（establish non-core pattern）+ 区块组合 Decision**：
   - `module-assets/erp-ast-web/.../pages/dashboard/main.page.yaml`：`form`（periodId 区间筛选）+ `service` KPI 卡片（原值合计/累计折旧/资产净值/本期折旧/在建工程余额）+ `chart` 资产类别分布占比图 + `chart` 折旧趋势图 + `crud` 折旧未计提预警。
   - `module-master-data/erp-md-web/.../pages/dashboard/main.page.yaml`：`service` KPI 卡片（物料总数/客户数/供应商数/停用物料/停用往来单位）+ `crud` 无 SKU 物料预警 + `crud` 无价格 SKU 预警（主数据看板无趋势图，对齐 §说明「指标少且静态」）。
   - Decision：沿用 1247-2 已验证的 AMIS 组合（`grid`+`card` + `chart` line/pie + `crud`），区间筛选用 `form`，数据经 `/api/GenericApi` GraphQL 消费。
2. **Phase 2 — 项目看板 + 制造看板（replicate pattern）**：
   - `module-projects/erp-prj-web/...`：KPI（在手项目数/总预算/已发生成本/预算执行率）+ 项目状态分布占比图 + 成本超支预警 + 项目延期预警（无毛利率卡片，见 Non-Goal）。
   - `module-manufacturing/erp-mfg-web/...`：`form`（日期区间）+ KPI（在制工单数/本期完工量/准时率/齐套待产）+ 工单状态分布占比图 + 产出趋势图 + 工单延期预警（齐套仅状态计数，无缺件明细，见 Non-Goal）。
3. **Phase 3 — 维护看板 + 质量看板（replicate pattern）**：
   - `module-maintenance/erp-mnt-web/...`：`form`（日期区间）+ KPI（设备总数/运行中/待处理请求/本期访问数）+ 设备状态分布占比图 + 设备停机预警 + 维护逾期预警（无 OEE 精确卡片，见 Non-Goal）。
   - `module-quality/erp-qa-web/...`：`form`（日期区间）+ KPI（本期质检数/合格率/不合格数/开放 NCR）+ 合格率趋势图 + 不合格原因 TOP 占比图 + CAPA 逾期预警（无 SPC 失控预警，见 Non-Goal）。

### 关键决策与执行现实

- **6 域复用既有 `*-dashboard` 菜单组**（与 1247-2 采购域补建不同）：6 域 action-auth.xml 均已存在 `ast-dashboard`/`prj-dashboard`/`mfg-dashboard`/`mnt-dashboard`/`qa-dashboard`/`md-dashboard` 菜单组（各含一 `*-dashboard-main` 子资源指向占位页）。本计划仅替换占位页内容，不新建菜单组。
- **GraphQL 变量类型对齐 1247-2 已验证范式**：`periodId`→`String`（资产 BizModel 真实签名为 `String`）、日期区间→`String`（镜像销售页，Nop GraphQL 引擎 String→LocalDate 强制转换）、`months`/`limit`→`Int`。
- **三处后端 Non-Goal 传导至前端**（由 1606-1 裁定）：项目看板无「项目毛利率」卡片；制造看板「齐套待产」为状态计数卡片（无缺件明细预警列表）；质量看板无「SPC 失控预警」列表。前端页面不渲染这些缺失指标对应区块。
- **质量 `defectType` 偏离沿用**：不合格原因 TOP 按 `dispositionType`（SCRAP/RETURN/CONCESSION/DOWNGRADE）聚合（1606-1 已在 `dashboards.md` §实现状态补注）。

### 验证状态

- `mvn clean install -DskipTests`：BUILD SUCCESS（154 reactor 模块）。
- 全 workspace `mvn test`：BUILD SUCCESS，0 failures/0 errors（无回归）。
- 6 action-auth `xmllint --noout`：全 well-formed。
- 6 page.yaml YAML 可解析；6 域 `ErpXxxDashboard__*` 调用逐一映射到 BizModel 真实 `@BizQuery`（ast 4/4、md 3/3、prj 4/4、mfg 4/4、mnt 4/4、qa 4/4）。
- 文件交付清单：6 page.yaml（替换占位）+ `dashboards.md` §实现状态更新 + `core-business-roadmap.md` 新增完成条目。

---

## 剩余 6 域看板后端聚合 API（资产/项目/制造/维护/质量/主数据）— plan 2026-07-06-1606-1

### 本次完成的工作

执行 `docs/plans/2026-07-06-1606-1-remaining-domain-dashboards-backend.md`（6 域看板后端聚合 API），3 Phase 全部完成，full-green verification（`mvn clean install -DskipTests` BUILD SUCCESS 154 模块；6 域看板测试 `mvn test -pl ... -Dtest=TestErp*Dashboard` 29 tests：ast 5 + md 4 + prj 5 + mfg 5 + mnt 5 + qa 5，0 failures/0 errors）。

1. **Phase 1 — 资产看板 + 主数据看板（prove non-core pattern）+ Non-Goal 裁定**：
   - `ErpAstDashboardBizModel`（`@BizModel("ErpAstDashboard")`）：`getDashboardKpi`/`getAssetCategoryDistribution`/`getDashboardTrend`/`findDepreciationMissingAlert`。
   - `ErpMdDashboardBizModel`（`@BizModel("ErpMdDashboard")`）：`getDashboardKpi`/`findMaterialWithoutSkuAlert`/`findSkuWithoutPriceAlert`。
   - Decision：三处数据源缺失指标裁定为 Non-Goal（项目毛利率 `ErpPrjProjectPnl` / 制造齐套缺件明细 `ErpMfgMaterialReservation` / 质量 SPC `ErpQaSpcSample`）。
2. **Phase 2 — 项目看板 + 制造看板**：
   - `ErpPrjDashboardBizModel`：`getDashboardKpi`（预算执行率）/`getProjectStatusDistribution`/`findCostOverrunAlert`/`findDelayedProjectAlert`。
   - `ErpMfgDashboardBizModel`：`getDashboardKpi`（准时率）/`getWorkOrderStatusDistribution`/`getDashboardTrend`/`findDelayedWorkOrderAlert`。
3. **Phase 3 — 维护看板 + 质量看板 + 阈值种子**：
   - `ErpMntDashboardBizModel`：`getDashboardKpi`/`getEquipmentStatusDistribution`/`findEquipmentDowntimeAlert`/`findMaintenanceOverdueAlert`。
   - `ErpQaDashboardBizModel`：`getDashboardKpi`（合格率）/`getDashboardTrend`/`findDefectTopN`/`findCapaOverdueAlert`。
   - 新增 `erp-dash.mnt-maintenance-overdue-days`/`erp-dash.qa-capa-overdue-days` 配置键（`ErpMntConstants`/`ErpQaConstants`，默认 0=直接 `< today`）。

### 关键决策与执行现实

- **6 域镜像 0935-1 范式**：专用看板 BizModel（`@BizModel("ErpXxxDashboard")` 服务型 BizObject）+ `IDaoProvider`/`IOrmTemplate` 注入 + `QueryBean` 过滤 + `ormTemplate.runInSession` 聚合 + `@BizQuery` 暴露。每域 bean 注册经 `app-service.beans.xml` `<bean id="..." ioc:type="@bean:id"/>`。
- **6 域均自包含**（无跨域 I*Biz 依赖，区别于 0935-1 销售采购跨域 AR/AP 经 `IErpFinArApItemBiz`）。
- **维护域状态常量**引用 `app.erp.mnt.dao.ErpMntDaoConstants`（生成的字典常量接口），而非 `ErpMntConstants`（后者 header 注明状态码权威在 dao 层）。
- **质量 `defectType` 字段未物化**：设计文档 §9「不合格原因 TOP 按 defectType 聚合」中的 `defectType` 在 ORM 不存在，本期以 `ErpQaNonConformance.dispositionType`（处置决定 SCRAP/RETURN/CONCESSION/DOWNGRADE）为聚合维度（语义最接近且规范枚举），在 `dashboards.md` §实现状态补注偏离。
- **维护 `ErpMntSchedule` 无 visitId FK**：Schedule→Visit 关系在 Visit 端（`ErpMntVisit.scheduleId`），逾期预警判定逻辑为 `nextDueDate < cutoff AND isActive=1 AND 不存在关联 Visit`。
- **维护 `ErpMntDowntimeEntry` 无 status 字段**：停机恢复状态以 `endTime != null` 隐式判定（`endTime = null` → 未恢复/进行中停机）。

### 验证状态

- `mvn clean install -DskipTests`：BUILD SUCCESS（154 reactor 模块）。
- 6 域看板测试：29 tests，0 failures/0 errors（ast 5 + md 4 + prj 5 + mfg 5 + mnt 5 + qa 5）。
- 文件交付清单：6 BizModel + 6 测试类 + 6 bean 注册 + 2 配置键（`ErpMntConstants`/`ErpQaConstants`）+ `dashboards.md` §实现状态更新 + `core-business-roadmap.md` 新增完成条目。

---

## 域业务报表 AMIS 菜单/页面（制造/库存/HR）— plan 2026-07-06-1247-3

### 本次完成的工作

执行 `docs/plans/2026-07-06-1247-3-domain-reports-frontend.md`（3 域业务报表 action-auth 菜单组 + 6 个 report page.yaml），5 Phase 全部完成，full-green verification（`mvn clean install -DskipTests` BUILD SUCCESS 154 模块；全 workspace `mvn test` BUILD SUCCESS 0 failures/0 errors；3 action-auth `xmllint --noout` well-formed；6 page.yaml YAML 可解析；reportName 集合 ⊆ 后端 `.xpt.xml` 模板名集合 6==6）。

1. **Phase 1 — 制造报表菜单组 + CRP 负荷报表页面（establish pattern）**：
   - `erp-mfg.action-auth.xml` 新增 `mfg-report` 菜单组（orderNo=700，介于 mfg-trace 600 与 mfg-dashboard 999）+ `crp-load-report` 子资源。
   - 新建 `module-manufacturing/erp-mfg-web/.../pages/report/crp-load-report.page.yaml`：参数 workcenterId/startDate/endDate + 渲染 `ErpMfgReport__renderHtml` + 下载 XLSX/PDF `ErpMfgReport__download` + html 容器。
2. **Phase 2 — 制造生产差异 + 预测差异报表页面（replicate pattern）**：
   - `production-variance-report.page.yaml`（参数 workOrderId/startDate/endDate）+ `forecast-variance-report.page.yaml`（参数 materialId/periodStart/periodEnd）+ action-auth 补 2 子资源。
3. **Phase 3 — 库存报表菜单组 + 库存追溯可视化报表页面**：
   - `erp-inv.action-auth.xml` 新增 `inv-report` 菜单组（orderNo=800）+ `inventory-trace-report` 子资源。
   - 新建 `module-inventory/erp-inv-web/.../pages/report/inventory-trace-report.page.yaml`：参数 batchNo/materialId/warehouseId + 渲染/下载 `ErpInvReport__*`。
4. **Phase 4 — HR 报表菜单组 + 员工净余额报表页面**：
   - `erp-hr.action-auth.xml` 新增 `hr-report` 菜单组（orderNo=800）+ `employee-net-balance` 子资源。
   - 新建 `module-hr/erp-hr-web/.../pages/report/employee-net-balance.page.yaml`：**无参数表单**（后端 `buildEmployeeNetBalanceDataset(context)` 不取 data 参数，聚合全部员工）+ 渲染/下载 `ErpHrReport__*`。
5. **Phase 5 — 薪酬模拟对比报表页面**：
   - `payroll-simulation-comparison.page.yaml`（参数 simulationId）+ action-auth 补子资源。

### 关键决策与执行现实

- **page.yaml 参数严格对齐后端真实签名**（非计划散文描述）：核验各域 `ErpXxxReportBizModel.prepareDataset`/`buildXxxDataset` 后——mfg 三报表 workcenterId/workOrderId/materialId + 日期；inv `batchNo`（String，**非** 计划散文写的 batchId）/materialId/warehouseId；hr `employee-net-balance` 后端零 data 参数（故页面无输入字段，避免装饰性无效输入）；`payroll-simulation-comparison` 仅 simulationId（后端不接受部门参数）。GraphQL 变量类型对齐：Long→`BigDecimal`（与 finance 范式一致）、LocalDate→`String`、String→`String`。
- **reportName 一致性硬门控**：`rg -o 'reportName: "[^"]*"'` 6 个 page.yaml 输出集合 == `ls module-*/erp-*-service/.../report/{mfg,inv,hr}/*.xpt.xml` 去扩展名集合（6==6，精确匹配）。
- **菜单 orderNo 选位**：mfg-report=700（trace 600 与 dashboard 999 间）；inv-report=800（cost 700 与 dashboard 999 间）；hr-report=800（recruit 700 之后，hr 无 dashboard）。各域报表组 icon 统一 `bar-chart-3`（对齐 finance `fin-report`）。
- **范式镜像**：6 个 page.yaml 完整复刻 0504-2 finance report page.yaml 三件套（参数 form + ajax 渲染 button + download blob toolbar + html 容器），无新 AMIS 模式引入。

### 验证

- `mvn clean install -DskipTests`（全 reactor）：BUILD SUCCESS，154 模块全绿。
- 全 workspace `mvn test`：BUILD SUCCESS，0 failures/0 errors（5:11，无回归）。
- 3 action-auth `xmllint --noout`：mfg/inv/hr 全 well-formed。
- 6 page.yaml `python3 yaml.safe_load`：全可解析。
- reportName 集合 ⊆ 后端模板名集合（6==6）；3 菜单组在 target/classes 构建 action-auth 中确认存在。

### 下一步

- 报表运行时浏览器视觉回归（Playwright successor，触发条件=报表 e2e 套件建立时）。
- 其余域报表前端（资产/项目/维护/质量/主数据/CRM/客服，触发条件=对应域报表后端落地时）。
- 单据打印/套打、定时报表批量（独立能力面，0504-2 已裁定）。

## 核心业务看板 AMIS 前端（财务/销售/采购/库存）— plan 2026-07-06-1247-2

### 本次完成的工作

执行 `docs/plans/2026-07-06-1247-2-core-dashboards-frontend.md`（4 核心业务域看板 AMIS 页面 + 采购域菜单组补建），3 Phase 全部完成，full-green verification（`mvn clean install -DskipTests` BUILD SUCCESS 154 模块；全 workspace `mvn test` BUILD SUCCESS 0 failures/0 errors；4 action-auth `xmllint --noout` well-formed；4 page.yaml YAML 可解析）。

1. **Phase 1 — 财务看板页面（establish AMIS pattern）**：
   - 替换 `module-finance/erp-fin-web/.../pages/dashboard/main.page.yaml` 占位为真实页面：`form`+`input-number`(periodId) 区间筛选 + `service`+`grid`+`wrapper` 6 KPI 卡片（revenue/expense/netProfit/bankBalance/arBalance/apBalance）+ `chart` 收支利润三线趋势图（`getDashboardTrend`，adaptor 转 echarts config）+ `crud` 现金流预警（`findCashFlowAlert`，adaptor 转 items+count）。菜单组 `fin-dashboard` 复用既有。
2. **Phase 2 — 销售 + 库存看板页面（replicate pattern）**：
   - 销售替换占位：`input-date`(startDate/endDate) 筛选 + 4 KPI 卡片（salesAmount/orderCount/conversionRate/arBalance）+ 销售趋势 line + 客户 TOP10 pie（`findCustomerTopN`）+ 应收超期 `crud`（`findArOverdueAlert`）。
   - 库存替换占位：4 KPI（totalValue/incomingQty/outgoingQty/turnoverRate）+ 净变动 bar（`getDashboardTrend`）+ 仓库分布 pie（`findWarehouseDistribution`）+ 缺料/滞销/批次效期 3 个 `crud`（`findShortageAlert`/`findSlowMovingAlert`/`findBatchExpiryAlert`）。
3. **Phase 3 — 采购看板页面 + 菜单组新建（fill gap）**：
   - `erp-pur.action-auth.xml` 新增 `pur-dashboard` 菜单组 + `pur-dashboard-main` 子资源（镜像 fin/sal/inv 范式，orderNo=999）。
   - 新建 `module-purchase/erp-pur-web/.../pages/dashboard/main.page.yaml`：4 KPI（purchaseAmount/orderCount/apBalance/onTimeRate）+ 采购趋势 line + 供应商 TOP10 pie（`findVendorTopN`）+ 三单匹配差异 `crud`（`findThreeWayMatchDiffAlert`）+ 应付超期 `crud`（`findApOverdueAlert`）。

### 关键决策与执行现实

- **AMIS 数据流**：每页用 `service`（KPI）/ `chart`（趋势·占比）/ `crud`（预警）三类组件，各自 `api` POST `/api/GenericApi` 发 GraphQL query，`adaptor`（JS 函数体）将 GraphQL 响应 `{data:{ErpXxxDashboard__method:...}}` 转换为 amis 期望格式（KPI→`{status,data:map}` / chart→`{status,data:echartsConfig}` / crud→`{status,data:{items,count}}`）。adaptor 同时兼容 `payload.data.data`（GraphQL envelope）与 `payload.data`（已解包）两种路径。
- **GraphQL 标量映射**（经 `GraphQLScalarType.java` 实证）：Java `Long`→GraphQL `Long`、`Integer`→`Int`、`LocalDate`→`String`（ISO）、`BigDecimal`→`BigDecimal`、`Map<String,Object>`→`Map`（字段可选）。页面 query 变量类型严格对齐（如 `$periodId:Long`、`$startDate:String`、`$months:Int`）。变量值用 `${var || null}` 兜底，保证未填时传 null（后端 `@Optional` 处理）而非空串（避免标量解析失败）。
- **GraphQL 方法签名静态核验**：`rg -o 'ErpXxxDashboard__[A-Za-z]+' main.page.yaml` 的输出集合 ⊆ BizModel 源 `@BizQuery` 方法集（fin 3/3、sal 4/4、inv 6/6、pur 5/5 全对齐）。
- **运行时视觉验证 Deferred**：本计划静态门控（构建+well-formed+GraphQL 签名一致）对齐 0504-2 已验证的 page.yaml+action-auth 范式；浏览器级视觉回归（Playwright）作为非阻塞 successor（触发条件=看板 e2e 套件建立时）。

### 验证

- `mvn clean install -DskipTests`（全 reactor）：BUILD SUCCESS，154 模块全绿。
- 全 workspace `mvn test`：BUILD SUCCESS，0 failures/0 errors（dashboard 后端 4 域 27 tests + 全量既有测试无回归）。
- 4 action-auth `xmllint --noout`：fin/sal/inv/pur 全 well-formed。
- 4 page.yaml `python3 yaml.safe_load`：全可解析。

### 下一步

- 看板运行时视觉/浏览器回归（Playwright successor，触发条件=看板 e2e 套件建立时）。
- 其余 6 域看板前端（资产/项目/制造/维护/质量/主数据，同范式 successor，触发条件=对应域看板后端+前端需求落地时）。
- 定时刷新/WebSocket 实时推送/物化视图缓存（optimization candidate，触发条件=实时性/性能需求落地时）。

## 剩余域业务报表后端（库存追溯可视化 + HR 报表）— plan 2026-07-06-1247-1

### 本次完成的工作

执行 `docs/plans/2026-07-06-1247-1-remaining-domain-reports-backend.md`（inventory + HR 两域报表渲染入口 + 3 张报表），3 Phase 全部完成，full-green verification（`mvn clean install -DskipTests` BUILD SUCCESS 154 模块；`TestErpInvReportRendering` 8 + `TestErpHrReportRendering` 13 = 21 新增报表测试全绿；inv 79 + hr 52 全模块 `mvn test` 无回归）。

1. **Phase 1 — inventory 域报表渲染入口 + 库存追溯可视化报表（prove pattern）**：
   - **Decision**：新建专用 `ErpInvReportBizModel`（`@BizModel("ErpInvReport")`），模板根 `/nop/main/report/inv/`。镜像 `ErpMfgReportBizModel`/`ErpFinReportBizModel` 域隔离范式（finance→`/fin/`，manufacturing→`/mfg/`，inventory→`/inv/`）。
   - **落地**：`ErpInvReportBizModel`（`renderHtml`/`download` @BizQuery + `resolveReportPath` 经 `StringHelper.isValidVPath` 防注入 + `ALLOWED_RENDER_TYPES` 校验 + inventory 域 `ErpInvErrors.ERR_REPORT_NAME_INVALID`/`ERR_REPORT_RENDER_TYPE_INVALID` ErrorCode，镜像 finance/mfg 不跨域 import）+ `buildInventoryTraceDataset`（经同域 `IErpInvStockMoveBiz` 的 4 个追溯方法 forwardTrace/backwardTrace/returnTrace/batchTrace 聚合，优先级 batchNo > moveId > materialId/warehouseId）+ `inventory-trace-report.xpt.xml`（追溯链路表 + 退货标记列）+ IoC 注册 + `TestErpInvReportRendering`。
2. **Phase 2 — HR 域报表渲染入口 + 员工净余额报表**：
   - **Decision**：员工净余额经跨域只读 `IErpFinArApItemBiz.findOpenItems(direction, ctx)` 聚合 finance 辅助账（不跨域 import 实体）。`findOpenItems` 不带 sourceBillType 过滤，故按 RECEIVABLE/PAYABLE 两方向取数后在聚合层按 sourceBillType=EMPLOYEE_ADVANCE/EXPENSE_CLAIM 二次过滤（避免混入客户 AR / 供应商 AP）。
   - **落地**：`ErpHrReportBizModel`（镜像范式 + HR 域 `ErpHrErrors.ERR_REPORT_*`）+ `buildEmployeeNetBalanceDataset`（净额=预支应收−报销应付，partnerName 经 `ErpMdPartner` 批量解析）+ `employee-net-balance.xpt.xml`（净额正负方向标记）+ 扩展 `ErpHrErrors`（现有接口追加 4 项，不动既有 ErrorCode）+ IoC 注册 + `TestErpHrReportRendering` 净余额用例。
3. **Phase 3 — 薪酬模拟对比报表**：
   - **落地**：`buildPayrollSimulationComparisonDataset`（从同域 `ErpHrSalarySimulationItemAdjustment`（2200-3）按 simulationId 聚合源 vs 调整三列对比 + 差异，末尾追加按部门小计行，employeeName/departmentId 经 `ErpHrEmployee` 批量解析）+ `payroll-simulation-comparison.xpt.xml`（三列对比 + 差异高亮 + 部门小计）+ 测试扩展。

### 关键决策与执行现实

- **`findOpenItems(direction)` 无 sourceBillType 参数**：员工身份经 sourceBillType 区分（EMPLOYEE_ADVANCE=应收 / EXPENSE_CLAIM=应付，经 `ErpFinArApItemGenerator.java:150,153` 印证）。`findOpenItems` 只按方向过滤，须在 HR 聚合层二次过滤 sourceBillType，否则会混入客户 AR / 供应商 AP。对齐 plan Phase 2 Decision + draft review iteration 3 结论。
- **`FilterBeans.or` 签名**：返回 `TreeBean`（非 `QueryFilterBean`），`or(TreeBean...)`。`findCandidateMoves` 按 materialId 反查 move lines → moveIds 时用 `or(TreeBean[])` 构造 IN 风格过滤。首次编译因误用 `@SafeVarargs`（非 varargs 方法）失败，移除即可。
- **`ErpHrEmployee.lastName` 空串被 ORM 转 NULL**：H2 NOT NULL 约束拒绝。测试 seed 用非空 lastName。
- **inv/hr-service 经 finance-service 传递依赖获得 `nop-report-core`**：finance-service 已在 0504-2 显式声明 `nop-report-core`/`nop-report-pdf` compile 依赖，inv/hr-service 均依赖 finance-service（既有看板 R 聚合已建立），传递可用，无需新接线。

### 验证

- `mvn test -pl module-inventory/erp-inv-service -Dtest=TestErpInvReportRendering`：8 tests，0 failures/0 errors。
- `mvn test -pl module-hr/erp-hr-service -Dtest=TestErpHrReportRendering`：13 tests，0 failures/0 errors。
- `mvn test -pl module-inventory/erp-inv-service,module-hr/erp-hr-service -am`：inv 79 + hr 52 全绿（既有 CRP/追溯/看板/薪酬/排班/模拟测试无回归）。
- `mvn clean install -DskipTests`：BUILD SUCCESS，154 模块全绿。

### 下一步

- inventory/HR 报表 AMIS 菜单/页面接入（Deferred，前端 successor `2026-07-06-1247-3`，触发：本计划后端 API 落地后报表前端定制启动）。
- 追溯链交互式树/图可视化（Deferred，前端能力面）。
- 其他域业务报表（资产/项目/维护/质量/主数据/CRM/客服，同范式 successor）。

## 制造运营报表（CRP 负荷/生产差异/预测差异）— plan 2026-07-06-0935-2

### 本次完成的工作

执行 `docs/plans/2026-07-06-0935-2-manufacturing-operational-reports.md`（制造运营报表渲染入口 + 3 张制造运营报表），3 Phase 全部完成，full-green verification（`mvn clean install -DskipTests` BUILD SUCCESS 154 模块；`TestErpMfgReportRendering` 13 tests 全绿；全 workspace `mvn test` BUILD SUCCESS）。

1. **Phase 1 — 制造报表渲染入口 + CRP 负荷报表（prove pattern）**：
   - **Decision**：新建专用 `ErpMfgReportBizModel`（`@BizModel("ErpMfgReport")`），模板根 `/nop/main/report/mfg/`。镜像 `ErpFinReportBizModel` 域隔离范式（finance→`/fin/`，manufacturing→`/mfg/`），避免跨域 R 依赖。
   - **落地**：`ErpMfgReportBizModel`（`renderHtml`/`download` @BizQuery + `resolveReportPath` 经 `StringHelper.isValidVPath` 防注入 + `ALLOWED_RENDER_TYPES` 校验 + 制造域 `ErpMfgErrors.ERR_REPORT_NAME_INVALID`/`ERR_REPORT_RENDER_TYPE_INVALID` ErrorCode，镜像 finance 不跨域 import）+ `buildCrpLoadDataset`（委托 `IErpMfgCrpLoadBiz.getLoadReport` 复用 1707-1 已审计负荷/产能/超负荷计算，区间未指定时从 CrpLoad 快照推导 [min,max] loadDate）+ `crp-load-report.xpt.xml`（负荷表 + 超负荷高亮）+ IoC 注册（`app-service.beans.xml` `<bean ... ioc:type="@bean:id"/>`）+ `TestErpMfgReportRendering` CRP 测试。
2. **Phase 2 — 生产差异报表**：
   - **落地**：`buildProductionVarianceDataset`（从 `ErpMfgCostVariance` 1838-2 产物按 工单×差异类型 聚合 standardAmount/actualAmount/varianceAmount，workOrderCode 经关系 getter）+ `production-variance-report.xpt.xml`（差异汇总表 + 差异金额合计）+ 测试扩展。
3. **Phase 3 — 预测差异报表**：
   - **落地**：`buildForecastVarianceDataset`（`ErpMfgForecast`/`ErpMfgForecastLine` 0427-1 APPROVED forecastQty vs `ErpMfgWorkOrder` 完工数量 actualQty，按物料聚合 forecastQty/actualQty/variance/varianceRatio，区间相交过滤）+ `forecast-variance-report.xpt.xml`（预测/实际/差异/差异率表 + 合计）+ 测试扩展。

### 关键决策与执行现实

- **CRP 负荷数据集委托而非重算**：`CrpLoadCalculator.getLoadReport` 已审计（1707-1）含复杂产能计算（日历出勤×效率系数×超负荷阈值），重算会违反 DRY。Decision：注入 `IErpMfgCrpLoadBiz` 调用 `getLoadReport`，将 `CrpLoadReportItem` 转为 Map 行。`getLoadReport` 要求 periodFrom/periodTo 非空，本报表 API `startDate`/`endDate` 可选——未指定时从 CrpLoad 快照 min/max loadDate 推导窗口（无数据返回空）。
- **非实体 BizModel 需显式 IoC 注册**：`@BizModel` 注解由 bizObject 管理器自动识别，但非 CrudBizModel（无 entityName）的服务型 BizObject 仍需在 `app-service.beans.xml` 显式注册（`<bean id="..." ioc:type="@bean:id"/>`）。镜像 finance `ErpFinReportBizModel` 注册范式。首次测试因未注册报 `nop.err.ioc.not-find-bean-with-type`。
- **预测"实际"口径 Decision**：`ErpMfgForecastLine` 无 actualQty 字段（0427-1 仅记录预测侧）。本期 actualQty = Σ `ErpMfgWorkOrder.completedQuantity`（docStatus=COMPLETED）按 productId 聚合，区间相交过滤。语义：预测 vs 实际完工产出对比（对齐 mrp.md「预测作为 FORECAST 需求来源，完工产出为实际」）。
- **`ErpMfgCostVariance.costElement` 非空**：ORM 标记 `mandatory="true"`，测试 seed 必须设置（用 `ErpMfgConstants.COST_ELEMENT_MATERIAL`/`COST_ELEMENT_LABOR`）。

### 验证

- `mvn test -pl module-manufacturing/erp-mfg-service -am -Dtest=TestErpMfgReportRendering`：13 tests，0 failures/0 errors。
- `mvn test -pl module-manufacturing/erp-mfg-service -am`：全绿（既有 CRP/差异/预测引擎测试无回归）。
- `mvn clean install -DskipTests`：BUILD SUCCESS，154 模块全绿。
- 全 workspace `mvn test`：BUILD SUCCESS（5:05 min）。

### 下一步

- 独立子代理结束审计（Closure Gates 最后一项 `[ ]`）。
- 制造报表 AMIS 菜单/页面接入（Deferred，触发：制造报表前端展示需求，镜像 0504-2 财务报表菜单接入范式）。
- 库存追溯可视化报表 / HR 报表（Deferred，触发：对应域报表需求落地）。

---

## 核心业务看板后端聚合 API — plan 2026-07-06-0935-1

### 本次完成的工作

执行 `docs/plans/2026-07-06-0935-1-core-business-dashboards-backend.md`（财务/销售/采购/库存 4 核心业务域看板聚合 API），4 Phase 全部完成，full-green verification（`mvn clean install -DskipTests` BUILD SUCCESS 154 模块；4 域 dashboard 测试 27 个全绿）。

1. **Phase 1 — 财务看板（prove pattern）**：
   - **Decision**：每域新建专用看板 BizModel（`ErpXxxDashboardBizModel`，`@BizModel("ErpXxxDashboard")` 服务型 BizObject），而非塞入既有订单/发票 BizModel。镜像 `ErpFinReportBizModel` 域隔离范式，注入 `IDaoProvider`/`IOrmTemplate`，后续 3 域沿用同范式。
   - **落地**：`ErpFinDashboardBizModel`（getDashboardKpi: 收入/支出/净利润 from GlBalance 损益类 periodActivity + 银行余额 from FundAccount BANK + AR/AP 余额 from ArApItem OPEN+PARTIAL；getDashboardTrend: 近 N 月月度序列；findCashFlowAlert: 阈值配置化）+ `ErpFinConstants.CONFIG_DASH_FIN_CASH_FLOW_THRESHOLD`（默认 0=关闭）+ bean 注册 + `TestErpFinDashboard` 5 tests。
2. **Phase 2 — 销售看板**：
   - **跨域接口扩展**：`IErpFinArApItemBiz` 新增 `findOpenItems(direction, context)`（既有 `findOpenItemsByPartner` 按 partnerId 过滤，无法满足看板"全量 AR/AP 余额"需求），finance→sales/purchase R 只读聚合。
   - **落地**：`ErpSalDashboardBizModel`（销售额/订单量/转化率/AR 余额跨域/趋势/客户 TOPN/AR 超期预警）+ 2 个 sal 域配置键 + bean 注册 + `TestErpSalDashboard` 6 tests。
3. **Phase 3 — 采购看板**：
   - **落地**：`ErpPurDashboardBizModel`（采购额/订单量/应付余额跨域/到货及时率 order.deliveryDate vs receive.businessDate/趋势/供应商 TOPN/三单匹配价格差异 invoice line vs order line 经 receive line 链路 5% 容差/应付超期预警）+ 1 个 pur 域配置键 + bean 注册 + `TestErpPurDashboard` 8 tests。
4. **Phase 4 — 库存看板**：
   - **落地**：`ErpInvDashboardBizModel`（库存总值 Σ totalCost/出入库量 StockMove DONE 按行/周转率 出库成本÷平均库存/仓库分布/缺料 StockBalance.availableQuantity < Material.safetyStock/滞销 最后出库日期物料级/批次效期 Batch.expiryDate）+ 2 个 inv 域配置键 + bean 注册 + `TestErpInvDashboard` 8 tests。

### 关键决策与执行现实

- **跨域 AR/AP 余额接口扩展**：既有 `findOpenItemsByPartner(partnerId, ...)` 按 partnerId 过滤无法满足看板全量需求；新增 `findOpenItems(direction, context)` 接口方法保持 finance 域实体所有权，遵循 nop-backend-dev skill「跨实体访问优先 I*Biz」。
- **预警阈值 NopSysVariable 配置化**：项目无既有 NopSysVariable seed 文件先例（grep 全仓零命中），沿用既有 `AppConfig.var(key, default)` + `IErpXxxConstants.CONFIG_DASH_*` 常量声明模式（与 `erp-fin.ar-aging-base` 等既有 config-gated 开关一致），默认 0=关闭。
- **实体字段名校正**：执行期发现计划文本与实际 schema 多处偏差（direction 值为 RECEIVABLE/PAYABLE 非 CUSTOMER/VENDOR；StockBalance 字段为 totalQuantity/totalCost/availableQuantity 非 qty/value；safetyStock 在 Material 非 StockBalance；receiveDate 实为 businessDate；StockMove direction 实为 moveType；销售用 customerId 采购用 supplierId 非 partnerId；deliveryDate 在订单头非行）。代码严格按实际 schema 实现，与 owner doc 口径对齐。
- **服务型 BizObject bean 注册**：`@BizModel` 注解的服务型 BizObject（非 CrudBizModel）需在域 `app-service.beans.xml` 显式注册为 bean（`ioc:type="@bean:id"`），否则 IoC 容器找不到。镜像既有 `ErpFinReportBizModel` 注册模式。

### 验证基线（全绿）

- `mvn clean install -DskipTests` → BUILD SUCCESS（154 reactor 模块）
- `mvn test -pl module-finance/erp-fin-service,module-sales/erp-sal-service,module-purchase/erp-pur-service,module-inventory/erp-inv-service -Dtest=TestErpFinDashboard,TestErpSalDashboard,TestErpPurDashboard,TestErpInvDashboard -am` → 4 域 dashboard 测试全绿：fin 5 + sal 6 + pur 8 + inv 8 = 27 tests, 0 failures/0 errors

### 文件变更

- BizModel（4 新）：`ErpFinDashboardBizModel`/`ErpSalDashboardBizModel`/`ErpPurDashboardBizModel`/`ErpInvDashboardBizModel`
- 接口扩展（1）：`IErpFinArApItemBiz.findOpenItems` + `ErpFinArApItemBizModel` 实现
- 常量（4）：ErpFin/ErpSal/ErpPur/ErpInvConstants 各增 `CONFIG_DASH_*` 看板预警阈值键
- bean 注册（4）：fin/sal/pur/inv 域 `app-service.beans.xml` 各增 1 bean
- 测试（4 新）：4 域 `TestErpXxxDashboard`
- 文档（3）：dashboards.md §实现状态更新 + core-business-roadmap.md M4 work item ✅ + 本日志

---

## 审批工作流通知与抄送 — plan 2026-07-06-0642-2

### 本次完成的工作

执行 `docs/plans/2026-07-06-0642-2-approval-workflow-notifications.md`（4 实体 WORKFLOW 审批结果通知提单人 + cc step 抄送知会 + 任务到达通知候选审批人），3 Phase 全部完成，full-green verification（`mvn clean install -DskipTests` BUILD SUCCESS 154 模块；4 域 service + notify service 测试全绿）。

1. **Phase 1 — 审批结果通知提单人 + wf 事件钩子 Explore**：
   - **Explore**：`NopWfCoreConstants` 确认完整事件清单（`enter-step`/`activate-step`/`exit-step`/`after-action`/`*end`/`on-no-assign` 等）+ 步骤级 `<on-enter>`/`<on-exit>` XPL 钩子可用，解除 Phase 3 阻塞。
   - **Decision**：**发现 `<observes>` 是 dead 特性**（xbiz.xdef 解析 `BizObserveModel` 但运行时无任何 `getObserves()` 调用点——通知永不触发）。改用 on-wf-end listener 内 approve/reject 后注入 `notify('wf.<entity>.result')`。**增强 `NotificationRecipientResolver`** 支持 USER_LIST recipientConfig 中 `${var}` 从 context 插值（`resolve(template, context)` 重载 + dispatcher 传 context），修复动态提单人接收人 + 0642-1 CRM `${ownerUserId}` 既有缺口。`ErpSysNotificationBizModel.notify` 改为内部 catch 所有异常（best-effort，因 XLang 不执行 try/catch 语句）。
   - **落地**：4 域 `.xwf` on-wf-end listener + 3 套 SQL 各增 4 行（ID 7111-7114，`wf.<entity>.result`，USER_LIST `${submitterUserId}`）。
2. **Phase 2 — 抄送（CC）步骤接线**：
   - **Decision**：cc step assignment 用 `actorType="all"`（`actorType="role"` 在角色不存在时抛 `ERR_WF_ACTOR_NOT_EXISTS` 破坏 wf；"all" 始终可解析）。CC 通知接收人用 ROLE resolver（config-gated 静默跳过）。cc step 需 `confirm` 后 wf 结束（标准 OA 抄送语义）；4 域既有 WORKFLOW 测试 agree 方法扩展为 agree+confirm cc。
   - **落地**：4 域 `.xwf` 各增 cc step（payment→cc-finance、receipt→cc-sales、disposal→cc-assets、salary→cc-hr）+ cc step `<on-enter>` 调 `notify('wf.<entity>.cc')` + 3 套 SQL 各增 4 行（ID 7131-7134）。
3. **Phase 3 — 任务到达通知候选审批人**：
   - **Decision**：方案 (A) 步骤级 `<on-enter>` 钩子（Explore 确认可用），覆盖多级链每级审批人（HR 薪酬三级 hr-review/finance-review/manager-approval 各自 onEnter），规避降级方案 (B) 仅覆盖第一步的残留风险。
   - **落地**：4 域审批步骤 `<on-enter>` 调 `notify('wf.<entity>.task-assigned')` + 3 套 SQL 各增 4 行（ID 7121-7124）。

### 关键决策与执行现实

- **`<observes>` dead 特性发现**：计划原优先 xbiz `<observes>`，执行期核实其运行时未触发（grep `getObserves()` 调用点为零），果断切换到 on-wf-end listener 注入。记录于 plan Phase 1 Decision。
- **XLang 不执行 try/catch**：XLangParser.g4 定义了 tryStatement 但执行器抛 `not-supported-node`。改由 `ErpSysNotificationBizModel.notify` Java 层统一 catch（best-effort），脚本层无 try。
- **wf 步骤钩子用 kebab-case**：`<onEnter>` → `<on-enter>`（wf.xdef stdDomain 命名）。
- **USER_LIST 动态接收人**：recipientConfig `${var}` 插值增强，向后兼容。
- **cc 非阻塞**：nop-wf cc step 结构上需 confirm 后 wf 结束；agree 路径经 cc，disagree 直接 to-end（不经 cc）。残留风险（业务 approve 在 cc confirm 后触发）记录于 plan，对 bootstrap 参考应用可接受。

### 验证基线（全绿）

- `mvn clean install -DskipTests` → BUILD SUCCESS（154 reactor 模块）
- `mvn test -pl module-notify/erp-notify-service` → 9 tests, 0 failures（resolver/dispatcher/BizModel 增强无回归）
- `mvn test -pl module-purchase/erp-pur-service,module-sales/erp-sal-service,module-assets/erp-ast-service,module-hr/erp-hr-service` → 全绿（含 `TestErpPurPaymentApprovalNotifications` 3 tests：结果通知 approved/rejected + cc step 实例 + CC 通知 + 任务到达通知；4 域 WORKFLOW 测试 12 tests agree+cc-confirm 扩展）

### 文件变更

- `.xwf`（4）：payment-approval/receipt-approval/asset-disposal-approval/salary-approval v1.xwf（onEnter 通知 + cc step + listener 结果通知）
- `module-notify`（4）：NotificationRecipientResolver.java（${var} 插值）+ NotificationDispatcher.java（传 context）+ ErpSysNotificationBizModel.java（catch all）+ 3 套 _seed_erp-notify.sql（+12 模板行）
- 测试（5）：TestErpPurPaymentApprovalNotifications.java（新）+ 4 域 WORKFLOW 测试 agree 方法 cc-confirm 扩展
- pom（3）：erp-pur/ast/hr-service 增 erp-notify-service test 依赖（erp-sal-service 已有）
- 文档（3）：approval-framework.md（抄送/通知章节）+ notification-strategy.md（审批通知类型）+ core-business-roadmap.md（5.5 work item）

---

## 域运营事件通知消费者接线 — plan 2026-07-06-0642-1

### 本次完成的工作

执行 `docs/plans/2026-07-06-0642-1-operational-notification-consumers.md`（6 个域运营事件接入通知派发通道 + 2 个提醒类 scheduler job 接线），3 Phase 全部完成，full-green verification（`mvn clean install -DskipTests` BUILD SUCCESS；`mvn test -pl module-cs/erp-cs-service,module-finance/erp-fin-service,module-sales/erp-sal-service,module-crm/erp-crm-service,module-manufacturing/erp-mfg-service -am` 全绿，含新增 6 个测试类 20+ test methods）。

1. **Phase 1 — 既有种子模板消费者接线（CS SLA / 过账异常 / 销售信用）**：
   - CS (`ErpCsTicketBizModel`)：`scanOverdueTickets` 升级 + `findSlaWarnings` 预警均调 `notify("cs.sla-overdue", ctx)`，config-gated `erp-cs.sla-notify-enabled`（默认 true）。新增 master-data-dao 编译依赖 + IErpMdPartnerBiz 注入（查 customerName）。复用既有 `erp-cs-sla-scan` scheduler job 自动派发 SLA 通知（无需新增 job）。
   - Finance (`ErpFinPostingExceptionRecorder`)：异常记录以 REQUIRES_NEW 提交后，在第二个 REQUIRES_NEW 事务内调 `notify("fin.posting-exception", ctx)`，config-gated `erp-fin.posting-exception-notify-enabled`（默认 true）。双 REQUIRES_NEW 隔离确保通知独立于主过账事务回滚。金额字段从 eventData JSON 派生（无则模板不渲染）。
   - Sales (`CreditLimitChecker`)：SOFT_WARNING 路径放行后调 `notify("sal.credit-over-limit", ctx)`，config-gated `erp-sal.credit-notify-enabled`（默认 true）。HARD_BLOCK 拒绝路径不通知。新增 orderCode 参数到 `check()` 签名。
2. **Phase 2 — 提醒类 scheduler job 接线（CRM 活动 / CSAT 调查）**：
   - 新增 `ErpCrmEventReminderJob`：cron 双层门控（`erp-crm.event-reminder-cron`）→ `findDueReminders(windowMinutes)` → 逐事件 `notify("crm.event-reminder", ctx)`，单条失败隔离。设计默认 cron `0 0/15 * * * ?`（每 15 分钟）。
   - 新增 `ErpCsCsatReminderJob`：cron 双层门控（`erp-cs.csat-reminder-cron`）→ `findSurveyReminders` + `findExpiredSurveys` → 逐调查 `notify("cs.csat-reminder", ctx)`。设计默认 cron `0 0 2 * * ?`（每日 02:00）。
   - 完整三件套：scheduler.yaml 新增 2 条 job + 各域 `app-service.beans.xml` 注册 bean + 2 个新种子模板（mysql/oracle/postgresql 三套）。
3. **Phase 3 — 生产差异阈值告警接线**：`ProductionVarianceCalculator.calculateVariances()` 计算结果落定后按 `erp-mfg.variance-alert-threshold`（默认 100）判定最大净差异行，超阈值时调 `notify("mfg.production-variance", ctx)`，config-gated `erp-mfg.variance-alert-enabled`。Decision：旁路告警（不在过账 Dispatcher 内）保持过账纯事务性。新增 1 个种子模板（异常告警类，1 分钟合并含次数，三套 SQL）。

### 关键决策与执行现实

- **跨域依赖方向**：所有 6 个消费者单向 `→ erp-notify-dao`（compile），notify 是干净叶子模块（仅依赖 parent+codegen），无环。erp-notify-dao 编译依赖 + erp-notify-service test 依赖的模式与既有 `purchase→inventory-dao`/`sales→finance-dao` 范式一致。
- **可选注入（ioc:optional）**：`ErpFinPostingExceptionRecorder` 与 `CreditLimitChecker` 的 notificationBiz 注入用 `<ioc:inject type="..." ioc:optional="true"/>` 标记可选。理由：单域测试（如 inventory 引入 finance-service 但无 notify-service）时不强制要求 notify bean 存在，避免下游测试全量失败。生产环境（app-erp-all 聚合）notify bean 总是注册。
- **Finance 双 REQUIRES_NEW 隔离**：notify 不能在主过账事务中（外层正在回滚会吞掉通知），也不能在第一个 REQUIRES_NEW 内（afterCommit 需事务提交但语义混乱）。采用"第一个 REQUIRES_NEW 提交异常记录 → 第二个 REQUIRES_NEW 调 notify"双层隔离，对齐 plan §Phase 1 Finance 项 Decision。
- **notify 失败降级**：所有 6 个消费者在 notify 调用外包 try/catch（warn-and-continue），与通知派发通道侧"config-gated 静默跳过不阻断业务"语义一致。
- **scheduler.yaml 双层门控**：scheduler.yaml 的 cronExpr 是设计值（job 总会触发），实际执行门控由 job bean 内 `resolveCronConfig()` 读取 config 键（空=跳过）。这是 nop-job-local 三件套范式（见 `0306-1`）。

### 验证基线（全绿）

- `mvn clean install -DskipTests` → BUILD SUCCESS（154 reactor 模块）
- `mvn test -pl module-cs/erp-cs-service -am` → 27 tests, 0 failures（含新增 `TestErpCsSlaNotification` 3 tests + `TestErpCsCsatReminderJob` 4 tests）
- `mvn test -pl module-finance/erp-fin-service -am` → 全绿（含新增 `TestErpFinPostingExceptionNotify` 2 tests）
- `mvn test -pl module-sales/erp-sal-service -am` → 全绿（含新增 `TestErpSalCreditNotify` 3 tests）
- `mvn test -pl module-crm/erp-crm-service -am` → 全绿（含新增 `TestErpCrmEventReminderJob` 4 tests）
- `mvn test -pl module-manufacturing/erp-mfg-service -am` → 73 tests, 0 failures（含新增 `TestErpMfgVarianceAlert` 3 tests）
- `mvn test -pl module-inventory/erp-inv-service -am` → 63 tests, 0 failures（验证可选注入修复后无回归）

### 下一步

- 解除三处前置 Deferred 触发条件（本计划已满足）：`0306-1` 提醒类 job、`1838-2` 差异告警通道、`0504-1` 全量消费者接线。
- 精确接收人路由（角色基础设施落地后，DEFERRED 见计划末尾）。
- 审批工作流通知（归 `2026-07-06-0642-2` 独立结果面）。

## 报表渲染子系统 module-finance — plan 2026-07-06-0504-2

### 本次完成的工作

执行 `docs/plans/2026-07-06-0504-2-report-rendering-subsystem.md`（报表渲染子系统，8+ completed 计划 deferred 项的渲染基础设施），4 Phase 全部完成，full-green verification（`mvn clean install -DskipTests` 154 reactor 模块 BUILD SUCCESS；`mvn test -pl module-finance/erp-fin-service` 170 测试全绿，0 failures/0 errors，含新增 `TestErpFinReportRendering` 8 tests）。

1. **Phase 1 — 2 项 Decision + 渲染 BizModel 骨架**：(a) 种子报表用 `.xpt.xml`（XML 序列化，与 `.xpt.xlsx` 等价、便于随仓版本化/审计），运行时 DB 定义共存按"VFS 种子优先 + DB 覆盖"记录于 `print-template.md`；(a) 复用平台 `NopReportDefinition` 拒绝自建 `ErpSysDocumentTemplate` 平行实体。新增 `ErpFinReportBizModel`（`@BizModel("ErpFinReport")`，注入 `IReportEngine`，`renderHtml(reportName,data)`/`download(reportName,renderType,data)` @BizQuery，`IServiceContext context` 末参；模板名 `StringHelper.isValidVPath` 校验防注入 → `ERR_REPORT_NAME_INVALID`/`ERR_REPORT_RENDER_TYPE_INVALID`）。注册于 `app-service.beans.xml`（`ioc:type="@bean:id"`）。
2. **Phase 2 — 财务三大表**：3 张 `.xpt.xml` 模板（资产负债表/利润表/现金流量表，单元格 `*=^ds!<field>` 展开 + `<valueExpr>SUM</valueExpr>` 合计）；数据集构造 `buildBalanceSheetDataset`（`ErpFinGlBalance` 按 `subject.subjectClass` 汇总 ASSET/LIABILITY/EQUITY，期末 closingDebit/Credit 按方向取净值）/ `buildIncomeStatementDataset`（INCOME/EXPENSE/COST 本期 periodDebit/Credit 净额）/ `buildCashFlowDataset`（`ErpFinVoucherLine` 现金类科目 `1001/1002/1012/1031` 借-贷净变动 INFLOW/OUTFLOW，简化直接法，完整 IAS7 归后续）。
3. **Phase 3 — AR/AP 账龄 + 期末结账报告**：`buildArApAgingDataset`（`ErpFinArApItem` OPEN/PARTIAL，dueDate/businessDate 基准，分桶 0-30/31-60/61-90/90+）；`buildPeriodCloseDataset`（期间/模块状态 AR/AP/INV/GL/AST + 凭证统计 + PERIOD_CLOSE/EXCHANGE_GAIN_LOSS 业财回链凭证计数，经 `ErpFinVoucherBillR` 按 voucherId 集合 scope）。
4. **Phase 4 — 菜单接入 + 文档修正**：`erp-fin.action-auth.xml` fin-report 分组扩展 2 项（ar-ap-aging、period-close-report，共 5 项可达）；5 张 AMIS 报表页面（`pages/report/*.page.yaml`，调 `ErpFinReport__renderHtml`/`__download`）；`print-template.md` 全文重写（复用 `NopReportDefinition` + `IReportEngine`，移除 `ErpSysDocumentTemplate`）；`dashboards.md` 标注看板归后续（渲染能力已就绪）。

### 关键决策与执行现实

- **数据集构造路线**：选"程序构造"（BizModel 聚合 ORM 实体 → `IEvalScope` 注入模板 → 模板 `*=^ds!<field>` 展开）而非 `UseOrmDataSet`/`UseJdbcDataSet` 模板内 SQL。理由：可单元测试、不耦合 DB schema 到模板、口径逻辑收敛在 BizModel。数据集聚合全部包在 `ormTemplate.runInSession` 内，避免直接调用（非 GraphQL 上下文）时懒加载关系（`GlBalance.getSubject()`）session-closed。
- **PDF + CJK 字体**：`nop-report-core` 仅 html+xlsx；PDF 走 `nop-report-pdf`（PDFBox 直接渲染，新增 compile 依赖）。PDFBox 标准 14 字体（Helvetica）不支持中文，`FontManager` 回退 `/fonts/default.ttf`（VFS）。从 `nop-report-pdf` 测试资源复制 14MB CJK TTF 到 `erp-fin-service/_vfs/fonts/default.ttf`（随仓 ship，prod + test 均生效）。字体瘦身/上提到 app-erp-all 归后续。
- **路径注入防护**：`resolveReportPath` 双层校验——`StringHelper.isValidVPath` 拒绝 `../`/空串 + 渲染类型白名单 `html/xlsx/pdf`。测试 `testPathInjectionRejected` 覆盖三类非法输入。
- **跨实体访问合规**：`ErpFinReportBizModel` 经 `IDaoProvider` 只读 `ErpFinGlBalance/ErpFinVoucher/ErpFinVoucherLine/ErpFinArApItem/ErpFinAccountingPeriod/ErpFinAccountingPeriodStatus/ErpFinVoucherBillR/ErpMdSubject`（报表只读聚合，无写操作，无 `I*Biz` 跨域调用）。

### 验证基线（全绿）

- `mvn clean install -DskipTests` → BUILD SUCCESS（154 reactor 模块）
- `mvn test -pl module-finance/erp-fin-service -Dsurefire.failIfNoSpecifiedTests=false` → Tests run: 170, Failures: 0, Errors: 0, Skipped: 0（含 `TestErpFinReportRendering`：五报表 renderHtml 非空、download xlsx/pdf 文件有效、三大表数据集口径断言、账龄分桶合计=辅助账开口项、结账报告覆盖模块状态+损益/汇兑凭证、路径注入防护）

### 下一步

- 解除下游 Deferred（渲染能力就绪即可启动）：1000-3 结账报告细化、0540-2 年度三大表年度口径、1707-1 CRP 报表、1838-2 差异报表、0427-1 预测差异报表、0700-1 追溯可视化、0700-2 员工净余额报表、2200-3 薪酬模拟对比报表（各域按 owner doc 独立接线 `IReportEngine`）。
- 经营看板前端实现（独立前端计划，渲染能力 + KPI 聚合方法已就绪）。
- 单据打印/套打（复用 `NopReportDefinition` + `IReportImage.print=false` 套打机制，独立计划）。

---

## 通知派发子系统 module-notify — plan 2026-07-06-0504-1

### 本次完成的工作

执行 `docs/plans/2026-07-06-0504-1-notification-dispatch-subsystem.md`（通知派发子系统，8+ completed 计划 deferred 项的通道基础设施），4 Phase 全部完成，full-green verification（`mvn clean install -DskipTests` 154 reactor 模块 BUILD SUCCESS；`mvn test -pl module-notify/erp-notify-service -am` 9 测试全绿：5 dispatch + 4 seed-template）。独立结束审计通过（OVERALL: close，8 项验证全通过）。

1. **Phase 1 — 3 项 Decision**：(a) 新建 `module-notify` 独立模块（非 app-erp-all 内置/复用 sys 域）；(a)+(b) 接收人解析 ROLE（nop-auth 角色名→NopAuthUserRole→userId）+ USER_LIST/ORG/PARTNER，动态组织层级 Deferred；不接入 nop-message 异步总线（同步 + txn().afterCommit，生产部署/多实例/削峰触发后继）。
2. **Phase 2 — ORM + codegen 骨架**：`module-notify/model/app-erp-notify.orm.xml`（3 实体 ErpSysNotificationTemplate/ErpSysNotification/ErpSysNotificationRead + 5 字典 `erp-notify/*`）；`nop-cli gen` 生成 7 子模块标准链；根 pom + app-erp-all（+nop-integration-api）接线；gen-orm.xgen 路径修正（`erp-notify.orm.xml`→`app-erp-notify.orm.xml`）。
3. **Phase 3 — 派发引擎 + 频控合并**：`NotificationDispatcher`（模板渲染 ${var} 插值 + 接收人解析 + 频控合并 + 站内落库 + config-gated 外发 IEmailSender/ISmsSender）；`NotificationRecipientResolver`（ROLE/USER_LIST/ORG/PARTNER）；`NotificationMergeCoordinator`（窗口内同 user+eventType 合并，状态含 MERGED 可二次合并，mergeCount 递增）；`ErpSysNotificationBizModel`（notify/markRead/markAllRead/findUnread/countUnread）；4 ErrorCode + NoopEmailSender/NoopSmsSender（bootstrap 满足 IoC 注入，生产真实实现覆盖）。
4. **Phase 4 — 种子模板 + 端到端 + owner-doc 修正**：3 类种子模板 SQL（cs.sla-overdue 业务提醒 5min/MERGE_BY_USER_TYPE、fin.posting-exception 异常告警 1min/MERGE_BY_USER_TYPE、sal.credit-over-limit 系统通知 NONE）；`TestErpSysNotificationDispatch`(5) + `TestErpSysNotificationSeedTemplates`(4) 端到端全绿；`notification-strategy.md` 实现方案修正（外发走 nop-integration、站内消息为本子系统、nop-message 异步总线归后继，纠正"使用 nop-message 组件"漂移）。

### 关键决策与执行现实

- **模板渲染用 ${var} 插值而非完整 XLang 表达式求值**：平台 Expression AST 经编译管道求值入口较重，bootstrap 用正则 `${var}` 插值（与 XLang TemplateStringExpression 语义兼容），复杂表达式求值归后继。
- **NoopEmailSender/NoopSmsSender 满足 IoC**：bootstrap 无真实供应商，`@Inject IEmailSender` 需 bean 存在；no-op 默认 bean（ioc:default=true）满足注入，config-gated 默认不调用，生产真实实现覆盖。
- **merge 查询状态含 MERGED**：合并后状态置 MERGED，窗口内三次合并需 findMergeable 匹配 PENDING/SENT/MERGED（否则第三次新建），mergeCount=3。
- **daoProvider 不在子类重声明**：CrudBizModel 已有私有 daoProvider 字段，子类同名字段会 shadow 父类导致 requireEntity NPE；用父类 `daoProvider()` 方法访问。
- **markRead userId 取 recipientUserId**：与 findUnread/countUnread 口径一致（按投递接收人），非上下文当前用户。

### 后续

- 8 域消费者接线（CRM 提醒/CSAT/SLA 升级/差异预警/工作流抄送/签章超时/排班提醒，通道已就绪各自独立计划）；nop-message 异步总线（生产部署触发）；webhook 通道；通知中心前端；动态组织层级精确路由；角色基础设施（均 Deferred）。

---

## WORKFLOW 模式 .xwf 审批流定义 — plan 2026-07-06-0315-1

### 本次完成的工作

执行 `docs/plans/2026-07-06-0315-1-workflow-approval-xwf.md`（use-approval 迁移 2050-1 的 WORKFLOW 后继），3 Phase 全部完成，full-green verification（`mvn clean install -DskipTests` 146 模块 BUILD SUCCESS；全量 `mvn test` BUILD SUCCESS 0 failures / 0 errors；4 域 pur 94 / sal 80 / ast 31 / hr 39 全绿 0 回归；4 新增 WORKFLOW 测试类 12 测试全绿）。

1. **Phase 1 — Decision + ORM/xmeta WORKFLOW 配置**：3 项 Decision（审批链组成 / HR actor 路由 / wf 回调配置）；4 实体 ORM 加 `useWorkflow="true"`（codegen 自动补 `nopFlowId` 列，propId 30/30/29/92）；4 xmeta 加 `wf:wfName`（payment/receipt/asset-disposal/salary-approval）。
2. **Phase 2 — 4 .xwf + listener + submitForApproval wf 启动**：`salary-approval`（三级 hr-review→finance-review→manager-approval）/`payment-approval`/`receipt-approval`/`asset-disposal-approval`（两级）`.xwf` 落地；listener 回调业务 approve/reject（按 `wf-approval.xlib` notifyResult 标签源码内联，因 listener XPL 作用域 xlib 标签不可解析）；3 Processor 实体 xbiz submitForApproval 追加 wf 启动 + ErpHrSalary 新增 submitForApproval 覆盖（应对平台标准 source 对 Long 主键 CCE 的已知限制）。
3. **Phase 3 — WORKFLOW 测试 + 回归**：4 新增 WORKFLOW 测试类（submit→agree→approve 闭环 + disagree→reject + REJECTED 重提）；8 类既有测试因 submit 现启动 wf 加 `@BeforeEach setUpWfUser`（SYS 用户）适配。

### 关键决策与执行现实

- **bootstrap actor 用 `actorType="all"`**（原 Decision 2 选 StarterManager）：ERP 无组织/角色基础设施 + 测试库无 NopAuthUser 种子，StarterManager 解析失败。多级语义由步骤链体现，精确角色路由 Deferred。
- **驳回整单走 oa.xwf `disagree`**（非 `reject`）：`reject` 是退回上一步（不结束 wf），`disagree` 才驳回整单并 `<to-end/>`。listener 按 `lastAction==='disagree'` 判定。
- **`*end` 双触发幂等**：引擎 BEFORE/AFTER_END 常量同值，listener 在两处都触发，已终态（APPROVED/REJECTED）时跳过。
- **平台 approval-support.xbiz 对 Long 主键 CCE**：Non-Goal 不改平台源码，应用层 xbiz 显式 `'' + entity.id` 转 String。Processor 零改动（wf 启动由 xbiz 承担）。

### 后续

- 金额阈值条件路由 / 前端审批 UI / 委托转办 / 通知抄送 / HR 精确角色路由（均 Deferred，见计划）。

---

## 存货成本调整单 + 标准成本重估 — plan 2026-07-05-2352-3

### 本次完成的工作

执行 `docs/plans/2026-07-05-2352-3-inventory-cost-adjustment.md`（成本调整单 + 标准成本重估，costing 模块完整性收口 deferred successor），4 Phase 全部完成，验证全绿（`mvn clean install -DskipTests` 146 模块 BUILD SUCCESS；`mvn test` 全量 0 failures / 0 errors；inventory-service 63 tests 含新增 `TestErpInvCostAdjust` 8 场景全绿）。

1. **Phase 1 — 实体建模 + 业务类型 + 4 项 Decision**：`erp-inv/adjust-type` 字典（PURCHASE_PRICE_ADJUST/COST_DIFFERENCE/STANDARD_REVALUATION/LANDED_COST_SUPPLEMENT）；`ErpInvCostAdjust` 头 + `ErpInvCostAdjustLine` 行实体落地（头-行 Decision，UK_INV_COST_ADJUST_CODE_ORG + 索引）；`ErpFinBusinessType.COST_ADJUSTMENT(420)` 枚举 + `erp-fin/business-type` 字典加性；codegen 重新生成实体/DAO/IBiz/BizModel 空壳/xbiz 通过。
2. **Phase 2 — 成本调整引擎 + 审批门控**：`CostAdjustmentService`（纯成本变更：按行更新 `ErpInvStockBalance.avgCost/totalCost` + 写 `ErpInvStockLedger` 流水 quantity=0/moveId=0 哨兵 + FIFO 追加 delta 调整层 `incomingMoveId=-行ID` 负值哨兵 + STANDARD_REVALUATION 发布 FIRMED `ErpMfgCostRollup` 行）；`ErpInvCostAdjustProcessor`（DIRECT 审批状态机 5 action + `applyCostAdjust`/`reverseCostAdjust`，`erp-fin.cost-adjust-approval` config-gated）；5 个 ErrorCode 加性。
3. **Phase 3 — 过账 Provider + 派发器 + 接线**：`CostAdjustmentAcctDocProvider`（方向相关——成本增加 借存货(1401)/贷成本差异(6603)，成本减少反向）；`CostAdjustmentPostingDispatcher`（构造 `PostingEvent` → `IErpFinVoucherBiz.post`，resolveAcctSchemaId 从 orgId，reverse 红字 + markOriginalVoucherReversed）；4 个 Bean 注册 `app-service.beans.xml`；`applyCostAdjust` 仅在过账成功（voucherId≠null）置 posted=true（对齐 asset 价值调整范式）。
4. **Phase 4 — 行为测试 + owner doc**：`TestErpInvCostAdjust` 8 类场景（MA 增/减 + FIFO 调整层 + STANDARD 重估 FIRMED 发布 + 审批门控开/关 + 重复 apply 防护 + reverse 红冲回退 + 无余额/负成本拒）全绿；`costing-methods.md §成本调整` Non-Goal 转 ✅ 落地 + 实现注记（头-行/FIFO delta 层/标准成本重估 rollup 发布/制造件重估归 successor）；1538-1/0427-2 Deferred 状态更新。

### 关键决策

- **头-行结构**：成本调整常涉及多物料/多仓库一次调整，采用头-行 cascade（对齐采购订单/退货单），而非 costing-methods.md :404 草稿的单层结构。
- **FIFO delta 调整层**：FIFO 物料成本调整追加「delta 调整层」（unitCost=新旧单位成本差，incomingMoveId=-行ID 负值哨兵），保持 FIFO 先进先出不变量；reverse 据此精确删除。残留风险：FIFO 队列长度增长。
- **纯成本变更流水 moveId 哨兵**：成本调整无 StockMove（Non-Goal 不生成移动单），流水 moveId/moveLineId 置 0 哨兵标识非移动单来源；成本调整过账经独立 `CostAdjustmentPostingDispatcher`（不经 `InvPostingDispatcher` 的 moveId 通道），故 0 哨兵不冲突。
- **posted=true 仅在过账成功**：apply 执行成本变更（幂等已记账），但 posted=true 仅在 voucherId≠null 时置位（对齐 asset 价值调整范式），避免过账失败但 posted=true 的不一致态。

### 下一步

- 到岸成本（Landed Cost）分摊算法（Deferred，触发：到岸成本模块；已预留 `adjustType=LANDED_COST_SUPPLEMENT` 码值）。
- 制造件标准成本重估编排（Deferred，触发：制造件周期重估需求；归制造域 `rollupCost` successor）。
- BATCH/INDIVIDUAL 计价成本调整（Deferred，触发：BATCH/INDIVIDUAL 计价落地）。
- 成本调整批量/多账套/报表（Deferred，触发：相关需求）。

---

## 全域高频过滤索引设计 — plan 2026-07-05-2352-1

### 本次完成的工作

执行 `docs/plans/2026-07-05-2352-1-db-index-design.md`（O2 残留「非 code 高频过滤索引」deferred successor），3 阶段全部完成，验证全绿（`mvn clean install -DskipTests` 146 模块 BUILD SUCCESS；`mvn test` 全量 1532 tests / 0 failures / 0 errors）。独立结束审计 VERDICT: PASS。

1. **Phase 1 — 索引策略决策**：5 项 Decision 裁决并固化于计划——命名 `IDX_{TABLE_NO_ERP_PREFIX}_{COLS}`（与 1000-1 的 `UK_*` 一致，`orgId` 段缩写 `ORG`）；多租户 `orgId` 复合前缀（最左前缀）；`delVersion` 不入索引（Decision 3 选择性原则）；选择性门槛（FK `*Id` 必加、`docStatus`/`approveStatus` 加、`code` 由 UK 覆盖不重复、`posted`(BOOLEAN)/`status`(active-status 二态) 跳过）；异步入口核查（Decision 5：无独立 webhook 入口，`ErpB2bMftLog.messageId` 由 FK 单列索引覆盖，无需组合索引）。
2. **Phase 2 — 18 域批量补充**：新增幂等脚本 `scripts/add-orm-indexes.js`（确定性规则映射，可重复运行），为 18 域源模型补齐 **916 个 `<index>`**（覆盖 279 个非桩实体）。规则：含 orgId 实体 `(orgId, docStatus)`/`(orgId, businessDate)` 复合 + 外键 `*Id` 单列导航；全局主数据按业务列。脚本同步生成派生 DDL `module-<domain>/deploy/sql/{mysql,postgresql,oracle}/_create_index.sql`（54 文件）——**平台 `_create_*.sql.xgen` 模板不输出 CREATE INDEX**（对照 nop-app-mall 模式确认），运行时由 `ddl.xlib AddIndex` 从模型自动应用，`_create_index.sql` 仅用于手工生产部署对齐。
3. **Phase 3 — 验证与文档对齐**：3 方核对（source `<index>` == 生成 `_app.orm.xml` == `_create_index.sql` `CREATE INDEX`）全域 18 域 916/916，0 mismatch；`mvn test` 全量 0 回归；`docs/architecture/idempotency-pattern.md` 规则 2 新增「过滤索引全域落地」+「异步入口组合幂等键核查」两条记录；`docs/plans/2026-07-01-1900-1-platform-compliance-remediation.md` O2 标 ✅ 闭环（O2 至此全部闭环）。

### 关键决策

- **批量编辑脚本化**：419 个实体声明（279 非桩 + 桩）×逐实体索引分析，手工不可行；沿用仓库 `scripts/*.js` 惯例写 `add-orm-indexes.js`（确定性、幂等：列解析限定 `<columns>` 段避免索引 `<column>` 污染；实体正则要求 `className=` 以跳过桩和注释中的 `<entity>` 字面量）。
- **DDL 生成机制澄清（计划基线订正）**：草案假设「`_create_*.sql` 自动含 CREATE INDEX」与平台实际不符（`_create.sql.xgen`/ddl.xlib CreateTables 不输出索引；AddIndex 仅运行时差分）。订正为：ORM `<indexes>` 为唯一真相源 + 派生 `_create_index.sql` 工件，与 nop-app-mall 既定模式一致。
- **状态列选择性细化**：草案 Decision 4 一概「状态列加」，执行期按 Decision 3 推广——`docStatus`/`approveStatus`（多态）加，`status`(active-status 二态)/`posted`(BOOLEAN) 跳过，避免无用低选择性索引。

### 下一步

- 查询性能基准测试（Deferred，触发：首次性能测试 / 生产数据规模）。
- 数据库特定索引优化（Deferred，触发：目标生产数据库确定）。
- 业务功能深化（见 `docs/backlog/`）。

---

## NCR 财务过账引擎 — plan 2026-07-05-2352-2

### 本次完成的工作

执行 `docs/plans/2026-07-05-2352-2-ncr-financial-posting.md`（NCR 驱动自动退货/报废过账 quality→finance 业财一体面，2237-3 deferred successor），4 Phase 全部完成，验证全绿（`mvn clean install -DskipTests` 146 模块 BUILD SUCCESS；`mvn test` quality-service 40 tests / 0 failures / 0 errors）。

1. **Phase 1 — 4 项 Decision**：(a) posted 机制选加 `posted`/`postedAt`/`postedBy` 列 + `returnCode` 列到 `ErpQaNonConformance`；(b) 返工映射选 (b) 不加 REWORK 字典/NCR_REWORK 业务类型，返工经制造域返工工单；(c) 业务类型仅新增 `NCR_SCRAP(410)`（退货编排退货域，NCR 侧不另过账）；(d) DOWNGRADE 同 CONCESSION 无凭证拒。
2. **Phase 2 — 业务类型 + Provider + 派发器**：`ErpFinBusinessType.NCR_SCRAP(410)` 枚举 + `erp-fin/business-type` 字典同步；`NcrScrapAcctDocProvider`（借 6711 营业外支出/贷 1401 库存商品，承接 PPV 范式）；`NcrPostingDispatcher`（从 `ErpInvStockBalance.avgCost` 算金额 → 构造 `PostingEvent` → `IErpFinVoucherBiz.post` → 置 posted 三件套）；`NcrPostingExecutor`（跨域经 Facade）；`erp-qa-service` pom 加 `app-erp-finance-service`/`app-erp-purchase-dao` compile 依赖。
3. **Phase 3 — 触发接线 + 退货编排**：`ErpQaNonConformanceBizModel.resolve` 末尾按 `erp-qua.ncr-posting-mode`（AUTO_POST/MANUAL_POST）config-gated 分派；`postNcr(ncrId)`/`reverseNcr(ncrId)` @BizMutation 人工入口 + posted-flag 守卫；`NcrReturnOrchestrator` 按 supplierId 判定采购/销售退货域（`@Nullable` optional inject 避免 reactor-cycle bean 初始化失败）；5 个 ErrorCode 加性新增。
4. **Phase 4 — 行为测试 + owner doc**：`TestErpQaNcrPosting` 7 类场景（SCRAP happy/RETURN 编排/CONCESSION 拒/AUTO vs MANUAL/重复过账防护/reverseNcr 红冲/未 RESOLVED 拒）全绿；test-mock-posting beans stub（purchase + sales return）；`state-machine.md §NCR 财务影响规则` Non-Goal 转 ✅ 落地标记 + 实现偏离补注。

### 关键技术发现

- **`@Nullable` optional inject 解决 reactor-cycle**：quality-service 测试不引入 purchase-service/sales-service（避免 reactor 环），`NcrReturnOrchestrator` 的 `@Inject IErpPurReturnBiz`/`IErpSalReturnBiz` 在无桩测试中导致 bean 初始化失败。加 `@jakarta.annotation.Nullable` 使 Nop IoC `DefaultBeanClassIntrospection` 检测到 `isAnnotationPresent(Nullable.class)` → `optional=true` → `resolveBeanByType` 返回 null → `buildInjectResolver` 返回 null → 属性跳过注入。运行时（全 app）或测试（test-mock beans）有对应 bean 时正常注入。
- **物理出库简化 Decision**：NCR_SCRAP 凭证贷记存货（1401）已表达报废消耗会计影响；物理库存量（ErpInvStockBalance.totalQuantity）同步扣减需调 `IErpInvStockMoveBiz.generateMove`，但出库移动会触发 InvPostingDispatcher SALES_OUTPUT 双计存货贷方。本期简化为凭证过账独占，物理量扣减归 inventory 域 successor。

### 下一步

- 独立子代理结束审计（Closure Gates 最后一项 `[ ]`）。
- 返工工单制造编排（Deferred，触发：制造域返工工单编排）。
- 报废批次/序列号追踪（Deferred，触发：批次召回/序列号报废核算）。

---
