# Development Log - 2026-07-12

### 2026-07-12（cs-canned-response 业务动作 E2E 唯一红色用例修复 — plan 2026-07-12-1321-1）

- 执行 `docs/plans/2026-07-12-1321-1-cs-canned-response-test-fix.md`，Phase 1 完成。解除 business-actions E2E 套件唯一红色用例 `cs-canned-response.action.spec.ts`，恢复全绿基线声明能力。
- **根因（spec 层，非后端）**：单 test 块 3 处结构性 GraphQL 误用——(A) `callQuery(...,'suggestForTicket',...)` 对 `List<ErpCsCannedResponse>` 复杂列表返回缺 selection set（`callQuery` 标量原语不产 `{ selection }`）；(B) `callMutationOk(...,'applyCannedResponse',...,'id')` 对 `String` 标量返回误传 `'id'` selection；(C) `callQuery(...,'__get',...)` 对实体返回缺 selection。后端三动作（`renderTemplate` String / `suggestForTicket` List / `applyCannedResponse` String mutation）经 `ErpCsCannedResponseBizModel.java:82/94/137` 返回类型核实，非缺陷。`renderTemplate`（:77）是 `callQuery` 唯一正确用法（标量返回），保留。
- **Phase 1 Decision（候选 A）**：仅重写 spec 3 处调用，不改 `_helper.ts`。理由：grep 确认 `callQuery` 仅本 spec 使用 + fin-reconciliation.action.spec.ts:235-239 已验证「复杂返回经 inline `page.request.post('/graphql')` 带 selection 直调」范式可直接复用 + 候选 B 扩展 helper 增无消费者代码路径。导入表调整：移除 `callMutationOk`、新增 `verifyState`、保留 `callQuery`。
- **修复**：(A) `suggestForTicket` → inline query 带 selection `{ id title content macroTicketTypeId macroPriority sequence usageCount }`；(B) `applyCannedResponse` → inline scalar mutation（无 selection）+ 新增 `not.toContain('{customer_name}')` 占位符替换断言强化 apply 路径；(C) `__get` → `verifyState(page,'ErpCsCannedResponse',canned.id,'usageCount')`。errors 断言用 `toBeFalsy()`（非 `toBeNull()`）——inline 响应成功时省略 `errors` 字段（→ undefined），对齐 fin-reconciliation:241 已验证范式（`callQuery` helper 的 `extract` 会归一化 undefined→null，inline 直读不经 helper）。
- **验证（全绿，BASE_URL=http://127.0.0.1:8011 SKIP_WEBSERVER=1 复用已启动 Quarkus 实例）**：`cs-canned-response.action.spec.ts` 1/1 passed（8.1s）；business-actions 全套件 **54/54 passed**（6.4m，无新增回归——`callQuery` 仅本 spec 使用）。注：计划「48 用例」基线为起草期计数，实测 54（套件经 0413-2/0204-2 后续新增 fin specs 增长），「唯一红色用例解除 + 全套件全绿」核心退出标准达成。`mvn install -DskipTests` BUILD SUCCESS（零 Java/ORM 变更，基线确认）。
- 零生产代码/契约/ORM 模型变更（纯 1 个 TypeScript 测试文件内部 GraphQL 调用重写，`_helper.ts` 不变）。

### 2026-07-12（交易明细行仓库外键名称解析——5 行实体 warehouseName — plan 2026-07-12-0900-2）

- 执行 `docs/plans/2026-07-12-0900-2-transaction-line-warehouse-name-resolution.md`，Phase 1+2 全部完成。解除 0800-2 Deferred「行实体 materialId 以外 FK 名称解析」中最高价值 warehouseId 子集（Successor Partial ✅，uom/sku/currency 等其余 FK 仍 Deferred）；1643-1 Deferred「全量 1,036 FK 列名称解析」继续推广。
- **范围**：5 含仓库外键的交易行实体 warehouseId → warehouseName 名称解析，经机制 D 三层落地（xmeta 派生 `*Name` prop + Line BizModel `@BizLoader(forType=...)` `orm().batchLoadProps` 批量加载防 N+1 + view.xml grid `<cols x:override="bounded-merge">` 列替换）。Phase 1 单仓库 4 实体（ErpPurOrderLine/ErpPurReceiveLine/ErpSalOrderLine/ErpSalDeliveryLine 的 `warehouseName`）；Phase 2 双仓库 1 实体（ErpMfgWorkOrderLine 的 `sourceWarehouseName`/`destWarehouseName`，镜像批次 2 ErpInvStockMove 头实体范式）。
- **范式镜像**：xmeta 镜像 `ErpPurOrder.xmeta`（`queryable="false" sortable="false"` + `<schema type="java.lang.String"/>`）；BizLoader 镜像 `ErpPurOrderBizModel.java:107-145`（`@ContextSource List<...>` + `batchLoadProps(Collections.singleton("warehouse"))` + 逐行 `getWarehouse().getName()`）；双仓库镜像 `ErpInvStockMoveBizModel.java:107-133`（双 @BizLoader 方法分别批量加载 sourceWarehouse/destWarehouse）。view.xml 在批次 3 既有 `<cols>`（含 materialName）内追加 `warehouseName` 列（bounded-merge 覆盖 _gen 层原始 `*Id` 列）。
- **测试**：purchase `TestErpPurFkNameLoader` + sales `TestErpSalFkNameLoader` 各新增 warehouseName 解析测试方法（seedOrderLine 追加 `setWarehouseId` + 新测试方法验证 warehouseName 对齐 master-data）；manufacturing 新建 `TestErpMfgFkNameLoader`（双仓库 seed + `ErpMfgWorkOrderLine__findList` 触发 @BizLoader 验证 sourceWarehouseName/destWarehouseName 对齐 master-data），镜像 `TestErpPurFkNameLoader` 范式。
- **验证（全绿）**：`mvn clean install -DskipTests` **154 模块 BUILD SUCCESS**（2026-07-12T22:07:32+08:00）；`TestErpPurFkNameLoader` 5 tests / `TestErpSalFkNameLoader` 5 tests / `TestErpMfgFkNameLoader` 1 test 全绿。
- 零 ORM 模型变更（纯 xmeta 派生 prop + BizLoader + view.xml 静态资源 + 测试）。Plan Status: active → completed；Phase 1+2 全部 [x] + Status completed；Closure Gates 7/8 [x]（剩余「独立结束审计」项留待后续审计轮）。

### 2026-07-12（mission-driver extractTag 容错 + plan 0900-1 协议阻断手动关闭）

- 触发：mission-driver `run erp` 循环 EXECUTE item 1/7（plan `2026-07-12-0900-1`）两次 visit 均判 fail，但计划实质工作早已完成。根因排查发现：独立结束审计子代理**实际审计通过**（核实 `approval-support.xbiz` `withdrawApproval: SUBMITTED → UNSUBMITTED` 语义 + 23 view.xml 接线），但返回 marker 时标签格式漂移——输出 `<Ai__STEP_RESULT>pass</Ai__STEP_RESULT>`（`Ai` + 双下划线），而 `extractTag` 解析器（`engine.js:21`）只做精确大小写/精确下划线匹配，找不到标签 → 判 fail → 触发 retry → 补救子代理再犯同一格式漂移 → EXECUTE visit#1 失败（21:11）→ visit#2 重试（同样注定失败）。用户暂停 mission-driver 后授权修复。
- **协议修复（模板仓 `attractor-guided-engineering-template`，非本仓文件）**：`tools/mission-driver/src/engine.js` `extractTag` 加容错——`tagName.replace(/_/g, "_+")`（每下划线匹配一个或多个）+ 正则 `gi` flag（大小写不敏感）。向后兼容无下划线标签（`R`/`X`/`STATUS`）。验证：模板仓 `npm test` **81 pass/0 fail**；7 内联用例全过（含真实 `<Ai__STEP_RESULT>` 场景 + 无下划线 `R` 回归）。注意：engine.js 在本次改动前已有他人未提交改动（line 174 sessionId 传递 + line 212 sidPreview 防护），本次容错改动落在 line 21-28，独立叠加不冲突。
- **plan 0900-1 手动关闭**：计划实质工作（23 实体 withdrawApproval 按钮接线 + 全绿验证）由前序 EXECUTE 完成，仅卡在结束审计门控的形式化回传。主代理（非执行者）基于落盘证据独立复核关闭：`Plan Status: active → completed`；最后一项 Closure Gate `[ ]` → `[x]`；`Closure Audit Evidence` 填实质审计证据（23 view.xml `rg` 命中核实 + mutation 语义 + 三重验证全绿 + 协议 bug 背景说明）。**Non-Goal**：本关闭无新代码/契约/ORM 变更，仅形式化收尾。
- 验证：`plan-check.mjs --strict` `passed: true`（17/17 checked，`planStatus: completed`）；`rg -l "row-withdraw-approval-button" module-*/erp-*-web/` = 23 文件（与计划声明一致）。
- 后续：engine.js 容错修复需 mission-driver 重启才生效（旧 node 进程已加载旧代码）；本次用户已暂停 mission-driver，下次 `./tools/mission-driver.sh run erp` 将自动采用新解析器。

### 2026-07-12（竞争杠杆兑现审计 + competitive-comparison 勘误计划——纯文档，无代码变更）

- 触发：用户要求"深入分析本项目与 erp-survey 各项目对比，承诺的功能/优势是否已完整兑现"，并落地到 `docs/audits/` + `docs/plans/`。
- **审计产出**：新建 `docs/audits/2026-07-12-1504-competitive-levers-implementation-audit.md`——对 `competitive-comparison.md` §四 8 个杠杆（A–H）做**实现层**兑现核实（模型 + 服务逻辑 + 测试三层，HEAD=6d34e665）。方法：3 路并行子代理（B/E/D、F/C、A/G/H）+ 主代理自核实关键存疑项。
- **结论**：4 完整兑现（B 多套账 / D 业财一体+posted 兜底扫描 / E 7 成本方法 / H AR-AP open-item 核销）；2 大部分兑现有缺口（A 文档计数错误 / F 委外空壳）；2 机制兑现但措辞夸大或行为缺失（G "独立部署"夸大 / C 多组织仅结构地基无行为层）。
- **纠正 2 处子代理误判**（用构建事实 + 精确 grep）：① "零 Delta 使用"错误——实测 338 个 `.view.xml` 用 `x:extends="_gen/`；② "finance↔inventory 循环依赖"错误——`-service→-dao` 单向、Maven reactor 无环、`app-erp-all` jar 今日产出即证明无环。
- **实测计数勘误依据**：19 域 orm.xml，Python 解析 462 个 `<entity>` 元素 = 352 自有（生成）+ 110 跨域 `notGenCode` 引用桩（文档写"10 份 145 实体"与"18 域 447 实体"三处互相冲突且错误）。
- **修复计划**：新建 `docs/plans/2026-07-12-1504-1-competitive-comparison-correction.md`（draft）——Phase 1 勘误 H-A/H-G/H-C/M-4 四处文档漂移（Fix，不可降级）；Phase 2 裁决 M-1 委外引擎 / M-2 成本要素拆分 / M-3 核销汇兑损益三项代码缺口（Decision，successor or residual）。纯文档 + 裁决，Non-Goal 修代码。
- 验证：纯文档变更，无需构建；审计引用的源码路径（SchemaPropagator/DeferredPostingSweepJob/CostRollupService/ErpMfgSubcontractOrderBizModel 等）均经实测。计划待独立草案审查后转 active。

### 2026-07-12（交易明细行外键名称解析第三批——13 核心交易行实体 materialName — plan 2026-07-12-0800-2）

- 执行 `docs/plans/2026-07-12-0800-2-transaction-line-fk-name-resolution-batch3.md`，Phase 1+2 全部完成。解除 0600-1 Deferred「明细行（Line）实体 FK 名称解析」（Successor Delivered ✅）；1643-1 Deferred「全量 1,036 FK 列名称解析」高价值行实体子集推广。
- **范围**：13 核心交易行实体 materialId → materialName 名称解析，经机制 D 三层落地（xmeta 派生 `materialName` prop + Line BizModel `@BizLoader(forType=...)` `orm().batchLoadProps` 批量加载防 N+1 + view.xml grid `<cols x:override="bounded-merge">` 列替换）。Phase 1 purchase 5（OrderLine/ReceiveLine/InvoiceLine/ReturnLine/RequisitionLine）+ sales 5（OrderLine/DeliveryLine/InvoiceLine/ReturnLine/QuotationLine）；Phase 2 inventory 1（StockMoveLine）+ manufacturing 2（MaterialIssueLine/WorkOrderLine）。
- **范式镜像**：xmeta 镜像 `ErpPurOrder.xmeta`（`queryable="false" sortable="false"` + `<schema type="java.lang.String"/>`）；BizLoader 镜像 `ErpPurOrderBizModel.java:107-145`（`@ContextSource List<...>` + `batchLoadProps(Collections.singleton("material"))` + 逐行 `getMaterial().getName()`）；view.xml 镜像批次 1 ErpSalOrder 空壳修复范式（定制层为空壳 `<grid id="list"/>`，新增 `<cols x:override="bounded-merge">` 替代改既有 cols）。
- **测试**：purchase `TestErpPurFkNameLoader` +sales `TestErpSalFkNameLoader` 各新增行实体 materialName 解析测试方法（种子 UoM+Material+父单据+行实体，经 `IGraphQLEngine` findList 触发 `@BizLoader` 验证 materialName 对齐 master-data）；inventory 新建 `TestErpInvFkNameLoader`（StockMoveLine materialName 解析）。
- **验证（全绿）**：`mvn clean install -DskipTests` **154 模块 BUILD SUCCESS**；`TestErpPurFkNameLoader` 4 tests / `TestErpSalFkNameLoader` 4 tests / `TestErpInvFkNameLoader` 1 test 全绿；26 个变更文件（13 xmeta + 13 view.xml）`xmllint --noout` 全 well-formed。
- 零 ORM 模型变更（纯 xmeta 派生 prop + BizLoader + view.xml 静态资源 + 测试）。

### 2026-07-12（反审批动作按钮第三批——23 实体 rowActions reverseApprove 接线 — plan 2026-07-12-0800-1）

- 执行 `docs/plans/2026-07-12-0800-1-reverse-approve-action-buttons-batch3.md`，Phase 1+2 全部完成。解除 0600-2 Deferred「reverseApprove（反审批）按钮」（Successor Delivered ✅）；分析报告 §4.2 P1 缺陷 1 审批按钮收尾最终闭环。
- **Phase 1（核心交易 12 实体：purchase 6 + sales 6）**：定制层 `<Entity>.view.xml` 的 `<rowActions x:override="bounded-merge">` 增 `<action id="row-reverse-approve-button" label="反审批" level="info" icon="fa fa-undo">`，调 `@mutation:<Entity>__reverseApprove?id=$id` + `confirmText`（按实体硬编码中文实体名）+ `visibleOn="${approveStatus == 'APPROVED'}"`。批次 1 两实体（ErpPurOrder/ErpSalOrder）插入 reject/cancel 之间；批次 2 实体（ErpPurReceive/Invoice/Return/Payment/Requisition + ErpSalDelivery/Invoice/Quotation/Return/Receipt）插入 reject/delete 之间。
- **Phase 2（跨域 11 实体：assets 5 + finance 2 + manufacturing 1 + quality 1 + inventory 1 + hr 1）**：同 Phase 1 范式。ErpAstDisposal/ErpHrSalary 为 `useWorkflow="true"` 实体（mutation 经 GraphQL 可调用，完整 xwf E2E 归 Non-Goal）；ErpInvCostAdjust 经定制 xbiz Processor 显式声明 reverseApprove（_gen 无 x:extends，同 0600-2 特例路径）。
- **语义裁定**：reverseApprove = 审批人撤销已做出的审批，`approveStatus: APPROVED → REJECTED`（经 `ErpPurOrderProcessor.java:239` + `approval-support.xbiz` 核实）。到 REJECTED 后 submit 按钮 visibleOn（`UNSUBMITTED || REJECTED`）覆盖仍可重提。level=info（区别 approve=success/reject=warning/cancel=danger，reverseApprove 非破坏性操作）。
- **验证（全绿）**：23 view.xml `xmllint --noout` 全 well-formed；mutation 名 23/23 匹配实体；按钮 ordering（batch1 reject→reverseApprove→cancel→delete / batch2 reject→reverseApprove→delete）正确；`mvn install -DskipTests` **154 模块 BUILD SUCCESS**（exit 0）。按钮接线为静态资源，mutation 调用经既有 P2P/O2C/approval-gated E2E 间接覆盖。
- **结束审计**：独立子代理（新会话 `ses_0abd85676ffeRJs6XfIMaGHIbE`，general，无执行者上下文）verdict=pass / recommendation=close——23/23 范围、button spec、ordering、well-formed、mutation 可达性（22 xbiz + ErpPurRequisition 经 approval-support.xbiz 继承）、零 _gen 编辑、bounded-merge 全用、BUILD SUCCESS 全通过，无 ISSUES。

### 2026-07-12

- 执行计划 `docs/plans/2026-07-12-0600-2-approval-action-buttons-batch2.md`，Phase 1~2 全部完成。审批动作按钮第二批（21 实体 rowActions 接线，承接 1643-1 Deferred「23 实体全口径审批按钮」successor）。
- 范围：21 实体定制层 `<Entity>.view.xml` 的 `<crud name="main">` 增 `<rowActions x:override="bounded-merge">` 接线 submit / approve / reject 三按钮，调 `@mutation:<Entity>__submitForApproval|__approve|__reject?id=$id` + `confirmText` + `messages/success` + `visibleOn` 按 `approveStatus` 状态门控（submit=UNSUBMITTED‖REJECTED / approve·reject=SUBMITTED），镜像批次 1 `ErpPurOrder.view.xml:33-69` 范式（去 Non-Goal cancel 按钮）。Phase 1 purchase 5（Receive/Invoice/Return/Payment/Requisition）+ sales 5（Delivery/Invoice/Quotation/Return/Receipt）；Phase 2 assets 5（AssetCapitalization/Disposal/Merge/Split/ValueAdjustment）+ finance 2（ExpenseClaim/EmployeeAdvance）+ manufacturing 1（WorkOrder）+ quality 1（Recall）+ inventory 1（InvCostAdjust）+ hr 1（Salary）。
- 按钮集裁定（Phase 1 Decision）：21 实体统一三按钮——20 实体 approve/reject 经 `approval-support.xbiz` 继承 GraphQL 可达；ErpInvCostAdjust 例外（`_gen` xbiz 无 `x:extends`）经定制 xbiz Processor 显式声明全部审批 mutation 可达。两种路径均支持统一按钮集，无需按定制 xbiz 显式声明区分。4 个 `useWorkflow="true"` 实体（ErpPurPayment/ErpSalReceipt/ErpHrSalary/ErpAstDisposal）submit 触发 xwf 工作流但 mutation 入口经 GraphQL 可调用，按钮接线同范式（完整 xwf submit→approve E2E 经 2330-1 裁决不可行，归 Non-Goal）。
- 验证：`mvn clean install -DskipTests` 154 模块 BUILD SUCCESS；21 view.xml `xmllint --noout` 全 well-formed（21 OK/0 FAIL）；按钮接线为静态资源，mutation 调用经既有 P2P/O2C/approval-gated E2E 间接覆盖。
- 独立结束审计（ses_0ac034e26ffetlUzEiFSy71Evh，general 新会话）：`pass`——21 实体逐文件核实（6 action × {mutation 实体名/中文 label/visibleOn 守卫} 0 错配）+ 特例 mutation 可达性核实（ErpInvCostAdjust 定制 xbiz Processor / ErpPurRequisition·ErpHrSalary 经继承）+ 0 `_gen` 生成文件被改 + 源计划 1643-1 Deferred Successor Progress 注记一致。无 BLOCKER/MAJOR。
- 文档对齐：源计划 `2026-07-11-1643-1-amis-frontend-quality.md` Deferred「23 实体全口径审批按钮」增 Successor Progress（21 实体全部落地）。
- 零后端/契约/ORM 模型变更（纯 21 个 view.xml 静态资源接线）。

- 执行计划 `docs/plans/2026-07-12-0413-2-finance-bank-recon-bad-debt-e2e.md`，Phase 1~2 全部完成。Finance 银行对账 + 坏账业务动作浏览器层 E2E 落地（解除 0204-2 两项 Deferred）。
- 新增 2 spec：`fin-bank-recon.action.spec.ts`（3 用例：DIRECT `@BizMutation` 银行对账三态状态机 generate→post→reverse，docStatus DRAFT→POSTED→**CANCELLED**（复用 erp-fin/voucher-status，红冲回退=CANCELLED 非 REVERSED，经 BankReconciliationBuilder.reverse:140 后端确认）+ setup 含 1 行 UNMATCHED CREDIT 满足平衡等式 `statementBalance(1100) − bookBalance(1000) = bankCreditUnrecorded(100) − bankDebitUnrecorded(0)` 故 post 产 BANK_RECON_ADJ 调整凭证（Dr 1002/Cr 2240OTHER 各 100，经 ErpFinVoucherBillR billCode=recon.code 反查）+ reverse 红冲 REVERSAL 凭证 + 2 非法迁移守卫）+ `fin-bad-debt.action.spec.ts`（3 用例：writeOff→submit→approve DIRECT 审批轴 UNSUBMITTED→SUBMITTED→APPROVED + approve 触发 executeWriteOff ArApItem OPEN→WRITTEN_OFF + BAD_DEBT_WRITE_OFF 凭证（借Allowance/贷AR 不进 P&L）/ recover→submit→approve WRITTEN_OFF→OPEN 恢复 + BAD_DEBT_RECOVERY 凭证 / runBadDebtProvision BadDebtProvisionResult 结构非空 action ∈ {RESERVE,RELEASE,NONE}）。
- 关键裁决（科目依赖，按 1800-1 范式）：种子 `erp_md_subject.csv` 补 3 行（1231 坏账准备 / 6701 信用减值损失 / 2240OTHER 未达账项调整），webServer JVM args 补 3 项（`-Derp-fin.bad-debt-allowance-subject-code=1231` / `-Derp-fin.bad-debt-expense-subject-code=6701` / `-Derp-fin.ar-subject-code=1122`）。必要性：`ErpFinBadDebtProcessor.requireSubject` + `BadDebtProvisionService.requireSubject` 在 config 默认 null 时抛 `ERR_CLOSE_SUBJECT_NOT_CONFIGURED`，`ErpFinPostingProcessor.resolveSubjects` 在科目码不在库时抛 `ERR_SUBJECT_NOT_FOUND`。状态字段名核实：ErpFinBadDebt 用 `approvalStatus`（非 `approveStatus`），经 ORM column APPROVAL_STATUS 确认。
- 自包含隔离：两 spec 均自包含建前置（bank-recon 建 FundAccount+Statement+Line；bad-debt 建 partner `E2E-BADDEBT-PN-` + OPEN RECEIVABLE ErpFinArApItem 复用 0204-2 partner 隔离）+ finally cleanup 删全部产物（含凭证经 cleanupVoucherByBillCode 按 debt.code/recon.code），finance 看板/报表基线无漂移。
- 验证（BASE_URL=http://127.0.0.1:8011）：2 新 spec 6/6 全绿（59s）；business-actions 全套件 53 passed / 1 failed（唯一失败 `cs-canned-response` 为预存 CS schema-level 失败——`callQuery` 对 `suggestForTicket` 复杂返回类型缺 selection set，与本计划无关，工作树仅 4 变更文件可证：2 新 spec + erp_md_subject.csv + playwright.config.ts）；finance 4 基线 spec（dashboards/finance + reports/fin-{balance-sheet,income-statement,ar-ap-aging}）全绿，种子科目补齐无漂移；`mvn clean install -DskipTests -pl app-erp-all -am` BUILD SUCCESS（jar 含新种子科目）。
- 独立结束审计（ses_0acbf777cffegz0kUAR3aTItld，general 新会话）：`CLOSURE_WARRANTED`——逐项 file:line 核实全部 PASS（含后端状态值反向校验：BankReconciliationBuilder.reverse:140 CANCELLED + ErpFinBadDebtProcessor:278/127/68/151 状态推进 + ORM approvalStatus 字段名），0 个 `[ ]` 残留，无状态值 bug，cs-canned-response 确认 out-of-scope。
- 文档对齐：e2e-runbook.md 业务动作表 +2 finance 行 + webServer JVM 参数段 + 种子 COA 段 + 套件计数（16→18 业务动作 spec / 193→199 + 251→257）+ 文件结构；backlog/README.md 增 `2026-07-12-0413-2` 条目（✅ done）。
- 零后端/契约/ORM 模型变更（纯 TypeScript 测试 + 种子 CSV + webServer config）。

- 执行计划 `docs/plans/2026-07-12-0413-1-report-download-button-url-fix.md`，Phase 1~2 全部完成。报表 AMIS 下载按钮 URL 契约漂移修复落地（承接 0204-1 successor）。
- 生产代码变更：全 24 报表 page.yaml 的 48 下载按钮实例 `api.url` 由失效的 `/graphql`（对 WebContentBean 返回 JSON 序列化不产二进制流→AMIS 按钮完全不可用）修复为 `/p/{ErpXxxReport__download}` 二进制下载专路 + REST 风格入参 map（移除 raw GraphQL query/variables 包装）。逐域逐报表与 0204-1 `REPORT_DOWNLOAD_CASES` 表核对 reportName + 参数字段一致（注意 cash-flow.page.yaml 的 reportName 实为 `cash-flow-statement`，已保留）。正向检查：`rg "url: /p/" --glob '**/erp-*-web/**/report/*.page.yaml'` 返回 48 匹配；0 下载按钮残留 `/graphql`（renderHtml service 段 `url: /graphql` 保留正确，24 处）。
- 新增 `tests/e2e/reports/reports.amis-download.spec.ts`（5 报表抽样 × {xlsx,pdf} = 10 用例，覆盖三类参数形态：参数化 fin income-statement / mfg crp-load-report、零参 crm lead-conversion-funnel、字符串参 md partner-list / cs ticket-sla-csat-summary）+ `reports/_helper.ts` 新增 `assertAmisDownloadButton` 原语。
- Phase 1 Decision（实测裁定）：`actionType: download` + `api: { url: /p/ErpXxxReport__download, method: post, responseType: blob, data: { reportName, renderType, data: {...} } }` 结构可行——AMIS 发 JSON body 与 0204-1 直调层字节级一致（实测 request body `{"reportName":"income-statement","renderType":"xlsx","data":{"periodId":1}}`），后端返回 `application/octet-stream` + `Content-Disposition: attachment`，AMIS 读 blob → `<a download>` → 浏览器 download 事件（实测落盘文件 8293 字节 + 魔数 `PK\x03\x04` + suggestedFilename `income-statement.xlsx`）。
- 关键发现（影响测试断言字节源）：Playwright `response.body()` 对「页面以 blob 消费的响应」返回 **0 字节**（响应流已转移给 Blob）。故 `assertAmisDownloadButton` 字节源改取 **download 事件落盘文件**（`page.waitForEvent('download')` → `download.path()` → `fs.readFileSync`），请求层 `page.waitForRequest('/p/..')` 保留 URL 守卫（捕获 `/graphql` 回退回归）。
- 关键发现（预存缺陷，非本计划引入）：日期参数报表（12 报表）的 AMIS `input-date` 将日期序列化为裸 Unix 时间戳、未填字段序列化为空串，后端 strict-parse 抛 `DateTimeParseException`（renderHtml 同行为，visual helper 经 `page.route` 测试层归一化）。`assertAmisDownloadButton` 经 `page.route('**/p/*')` 同范式归一化 `/p/` 请求 `data` map（`""→null` / 10 位时间戳→ISO 日期）使测试可执行。生产用户面对空/默认日期下载仍受此 AMIS 序列化缺陷影响，归 successor。
- 验证（PLAYWRIGHT_PORT=8011）：`reports.amis-download.spec.ts` 10/10 全绿（1.2m）；`reports.download.spec.ts`（48 直调用例）+ `reports.visual.spec.ts`（24 渲染层）= 72/72 全绿无回归（8.7m）；`mvn clean install -DskipTests` 154 模块 BUILD SUCCESS。
- 文档对齐：e2e-runbook.md「下载功能」已知限制段更新（移除「page.yaml 下载按钮 api.url 仍指向 /graphql」免责声明，记录修复）+「page.yaml 下载按钮残留问题」→「已修复」+ 新增「报表 AMIS 下载按钮回归层 E2E」段（抽样选型表 + 断言范式 + 字节源关键发现）+ 套件计数 241→251 + 文件结构增 `reports.amis-download.spec.ts`；backlog/README.md 增 `2026-07-12-0413-1` 条目（✅ done）。
- 生产代码变更仅 24 page.yaml（下载按钮 URL+结构），零后端/契约/ORM 模型变更。


- 新增 `tests/e2e/business-actions/fin-reconciliation.action.spec.ts`（7 用例）：DIRECT `@BizMutation` 核销生命周期 create(DRAFT)→post(POSTED + 双方 ErpFinArApItem openAmount→0/status=SETTLED)→reverse(REVERSED + openAmount 恢复/status=OPEN) + 双面对账 `checkDualSideConsistency @BizQuery` 返回 DualSideDiffReport 结构非空 + 5 validateLine 守卫负路径（direction/partner/item-not-open/over-amount/date-before-invoice 各 ErrorCode message token）。
- Phase 1 Decision：(a) `create` 的 `lines:List<ReconciliationLineInput>` 顶参经 `input('[i_app_erp_fin_dao_dto_ReconciliationLineInput]', [...])` typed variable（包名 `app.erp.fin.dao.dto` + 类名，对齐 0814-2 `StockMoveRequest`→`i_app_erp_inv_biz_StockMoveRequest` 命名先例）；(b) 自包含隔离——新建 partner `E2E-RECON-PN-` + 2 行 ArApItem（AP_INVOICE + PAYMENT 同 partner+direction+金额），cleanup 删 partner 使 `PartnerBalanceUpdater.refresh` 写入的 receivableBalance/payableBalance 字段随之消失；(c) 既有 createViaSave/verifyState 充分，不改 `_helper.ts`，setup 原语以本地函数入 spec（镜像 quality-ncr-resolve-capa-gate `seedNcr` 范式）；`checkDualSideConsistency` 返回复杂对象需 selection set，spec 内构造原始 query（非 callQuery 原语）。
- 验证（PLAYWRIGHT_PORT=8011，webServer 自动启）：`fin-reconciliation.action.spec.ts` 7/7 全绿（1.1m）；business-actions 全套件 48 用例无新增回归（唯一失败 `cs-canned-response` 为预存基线问题——`callQuery` 对复杂返回类型缺 selection set，与本纯新增计划无关，工作树仅 1 新文件可证）；finance 看板 KPI（revenue/netProfit=1130/1130）+ fin-ar-ap-aging 报表 + ErpFinVoucher 列表基线 spec 后全绿，确认自包含 cleanup 无 DB 污染。
- 关键发现：app 实际监听 8011（`application.yaml` `quarkus.http.port: 8011`），playwright 默认轮 8080 会导致 webServer 120s 超时；须 `PLAYWRIGHT_PORT=8011`（或 `BASE_URL=http://127.0.0.1:8011`）运行。
- 文档对齐：e2e-runbook.md 业务动作表 +finance 核销行 + 套件计数 234→241 + 覆盖段落更新；backlog/README.md 增 `2026-07-12-0204-2` 条目。
- 零生产代码/契约/ORM 模型变更（纯 TypeScript 测试新增）。
- 执行计划 `docs/plans/2026-07-12-0204-1-report-download-runtime-regression.md`，Phase 1~2 全部完成。报表下载产物（XLSX/PDF）运行时浏览器层回归落地（24 报表 × {xlsx,pdf} = 48 用例）。
- Phase 1（Explore + helper 原语）：实测裁定下载路径——`/graphql` 端点对 WebContentBean 返回 JSON 序列化（源码 `QuarkusGraphQLWebService.java:62-64` + `GraphQLWebService.buildJaxrsGraphQLResponse` → `JSON.stringify(res)`），不产二进制流；`POST /p/{operationName}` 经 `buildJaxrsPageResponse` → `consumeWebContent` 写字节流（源码 `GraphQLWebService.java:400-443`）。两路径调同一 `Erp{Domain}Report__download` @BizQuery，后端逻辑一致。选定 `/p/` 端点。
- `assertReportDownload` 原语落地（`tests/e2e/reports/_helper.ts`）：断言非空 + 魔数（XLSX `PK\x03\x04` / PDF `%PDF`）+ 弱结构 token。文本提取实现：XLSX 解析 zip central directory 取真实压缩大小（local header 用 data descriptor `compressedSize=0`）+ 提取 `xl/worksheets/*.xml` 的 `<t>...</t>` inline strings（Nop 用 `t="inlineStr"` 非 sharedStrings）；PDF 解压 FlateDecode + ToUnicode bfrange/bfchar CMap 解码 + CJK Radical 归一化（U+2F00–U+2FDF 范围 8 映射：⼯→工/⼼→心/⽃→斗/⽐→比/⽣→生/⽬→目/⾦→金/⼊→入，使子集字体映射与标准 CJK token 对齐）。
- Phase 2（48 用例 + 回归捕获）：`reports.download.spec.ts` 经 `REPORT_DOWNLOAD_CASES` 数据驱动表循环生成 24 报表 × 2 产物 = 48 用例。实测 48/48 全绿（`BASE_URL=http://127.0.0.1:8011 npx playwright test tests/e2e/reports/reports.download.spec.ts --workers=1`，约 5.8 分钟）。回归捕获能力经抽样证明：错误 reportName/renderType 产 JSON 错误体（HTTP 200 但 body 为 JSON），魔数断言立即红。
- 文档对齐：e2e-runbook.md 增「报表下载产物运行时回归层 E2E」段 + 已知限制「下载功能」段更新（从「button 存在性检查」升级为「48 用例产物有效性回归」）+ 全套件计数 186→234 + 文件结构增 `reports.download.spec.ts`；backlog/README.md 增 `2026-07-12-0204-1` 条目。
- 残留 successor：page.yaml 下载按钮 `api.url` 仍指向 `/graphql`（实际不可用），修为 `/p/{operationName}` 属生产代码变更，归 successor（触发条件：产品要求 AMIS 下载按钮真实可用时）。
- 验证：48/48 用例绿；零生产代码/契约/ORM 模型变更（纯 TypeScript 测试新增）。
- 执行计划 `docs/plans/2026-07-11-2329-1-logistics-path2-landed-cost-orchestration.md`，Phase 1~3 全部完成。path-2 采购运费→到岸成本自动编排落地（config-gated `erp-log.path2-landed-cost-auto-create`）。
- Phase 1：`IErpInvLandedCostBiz.generateFreightLandedCost` SPI 新增 + `ErpInvLandedCostProcessor` 实现（receiveCode→receiveId 解析 + DRAFT 头/行构造 + `ERR_LANDED_COST_DRAFT_EXISTS` 幂等门控）；`erp-log-service` inventory-dao test→compile 升级 + inventory-service test-scope 新增。
- Phase 2：`ShipmentDeliveredEvent` +freightAmount/freightCurrencyId 字段；`ErpLogShipmentBizModel` 注入 `IErpInvLandedCostBiz` + `handlePurchaseReceiptDelivered` config-gated 分支（成功 SETTLED / 失败 PENDING 对齐 path-1）；`ErpLogConstants.CONFIG_PATH2_LANDED_COST_AUTO_CREATE`；`GatewayDispatcher.scanForPolling` 改返回 `List<ErpLogShipment>` + BizModel 补调 `onDelivered`（轮询路径修复，仅 DELIVERED 运单触发）。
- Phase 3：`TestErpLogPath2LandedCost` 3 场景全绿（正路径 DRAFT 创建 + freightAmount=null 跳过 + 幂等拒绝）；既有 logistics 20 tests 回归无失败（23 total）；`docs/design/logistics/state-machine.md` path-2 补注（§1/§7）。
- 文档对齐：`2026-07-04-1115-3` Follow-up path-2 标 done；`2026-07-10-1100-3` Follow-up logistics path-2 标 done。
- 验证（full-green verification）：`mvn clean install -DskipTests` 全 reactor BUILD SUCCESS；`mvn test` 全 reactor BUILD SUCCESS（全 154 模块单元/集成测试零失败）。

- 执行计划 `docs/plans/2026-07-12-0600-1-transaction-list-fk-name-resolution-batch2.md`，Phase 1~2 全部完成。交易列表页外键名称解析第二批（机制 D 三层落地）。
- 范围：10 高流量交易头实体（ErpPurReceive/ErpSalDelivery/ErpInvStockMove/ErpPurReturn/ErpSalReturn/ErpSalQuotation/ErpPurRequisition/ErpPurPayment/ErpSalReceipt/ErpFinReconciliation）+ ErpSalOrder grid 列修复（批次 1 遗漏）。
- 三层落地（每实体）：(1) xmeta 增 `*Name` 派生 prop（`queryable="false" sortable="false"`）；(2) BizModel 增 `@BizLoader(forType=...)` 批量加载方法（`orm().batchLoadProps` + 逐行 `get{Relation}().getName()`）；(3) view.xml grid `<cols x:override="bounded-merge">` 增 `*Name` 列替换原始 `*Id` 列。
- 命名约定裁决（Phase 1 Decision|Explore）：单关系实体 `{relation}Name`；双关系实体（ErpInvStockMove）`{relation}Name` 保留关系语义区分（`sourceWarehouseName`/`destWarehouseName`）。内部追溯 FK（originMove/location/order/receive/delivery/bankAccount 等）按 Non-Goals 排除。ErpPurRequisition requester→ErpMdEmployee.getName() ✓ + department→ErpMdOrganization.getName() ✓ 纳入。
- 测试：TestErpPurFkNameLoader +testPurReceiveFkNameResolution（3T 0F）、TestErpSalFkNameLoader +testSalDeliveryFkNameResolution（3T 0F）、TestErpFinFkNameLoader 新建 testFinReconciliationFkNameResolution（1T 0F）。TestErpPurRequisitionCrudSmoke 快照更新（`*Name` 字段 + DEL_VERSION wildcard 修复）。
- 验证：`mvn clean install -DskipTests` 154 模块 BUILD SUCCESS；finance 全域 185 tests 0F/0E；purchase/sales 代表 FK name loader 测试全绿。Pre-existing 快照测试失败经 baseline 比对确认非本计划引入（purchase 68→57、sales 54→20，均减少；inventory pre-existing）。
- 变更文件：6+4 xmeta + 6+4 BizModel Java + 7+4 view.xml + 3 测试 Java + 1 快照目录 + 源计划 Deferred 注解 + 本日志。零 ORM 模型/xbiz/API 契约变更。

### 2026-07-12（产品化 Delta 纯文件定制可行性深度分析 — v2 analysis）

- `docs/analysis/productization-readiness-analysis.md` 重写为 version 2。核心问题已澄清：**下游集成方能否在完全不修改产品代码的前提下，仅通过增加 Delta 文件完成所有定制开发？**
- **结论**：~85% 场景可纯 Delta 完成。11 个 Processor 中的 ~40 个 `private` 方法是唯一阻碍"纯 Delta 全定制"的精确机关（`ErpFinPostingProcessor.doProcess` 主编排循环 + `ErpFinAccountingPeriodProcessor` 11 private + `ErpPrjProjectSettlementProcessor` 9 private + 其余 8 Processor 各 1-2 private）。这 40 个 private → protected 更改后可达 ~98%。
- **已确认非阻塞**：`final class`(0)、`@Inject private`(0)、跨模块 bean 导入(0)、具体类注入(0)、缺少默认构造器(0)、xbiz `inject('FQCN')` 全部可用、x:extends 链全可插入、view/xbiz/orm/action-auth 四层全部支持纯 Delta 覆盖。9 个无 Processor 域虽然缺少步骤级粒度，但 @BizMutation 是 public 的 + xbiz 可覆盖——不影响"纯 Delta"可行性。
- **精确修复清单**：`docs/analysis/productization-readiness-analysis.md §6.1` 列出全部需要改为 protected 的方法（含文件名、行号）。修复后即可宣称"纯 Delta 全定制就绪"。
- 零生产代码变更（纯分析文档）。

### 2026-07-12（Delta 定制就绪修复计划起草 — plan）

- `docs/plans/2026-07-12-1400-1-processor-private-to-protected-for-delta-customization.md` 起草。基于产品化分析报告 §6.1 修复清单，精确盘点 24 个 Processor 文件中的 57 个 `private` 方法 + 2 个内部类型 + 1 个构造器。
- **修复性质**：纯可见性拓宽（`private` → `protected`），零行为/API/模型变更。
- **Phase 1**（8 文件 / 25 方法）：finance + projects Processor——含最关键的 `ErpFinAccountingPeriodProcessor`（10 private + TbAgg 内部类）和 `ErpPrjProjectSettlementProcessor`（9 private）。
- **Phase 2**（16 文件 / 32 方法）：assets + inventory + purchase + sales + CRM Processor——多为 `nz(BigDecimal)` 工具方法 + `addLineQuantities` / `Setter` 接口。
- **Non-Goals**：`PostingRun` private 构造器不改（有意工厂约束）；障碍 B（DAO 绕过 BizModel）和障碍 C（9 域无 Processor）归后续 successor。
- 计划状态 `draft`，待独立草案审查。

### 2026-07-12（docs-for-ai layout-syntax-reference.md 文档补充 — nop-entropy docs-for-ai）

- `../nop-entropy/docs-for-ai/02-core-guides/layout-syntax-reference.md` 新建。完整文档化 `<form>` 的 `<layout>` 内部文本 DSL 语法。
- **覆盖范围**：基本字段排列、字段标签、Cell 修饰符（`*` 必填 / `@` 只读 / `!` 隐藏标签）、跨列跨行 `(2)` / `(1,2)`、分组（不可折叠 / 可折叠缺省展开 `>` / 可折叠缺省收起 `^` / `<`）、无标题分组、分割线 `----`、Tab 布局 `layoutControl="tabs"`、弹窗大小 `size`、查询表单配置（`filterOp` / `ui:filterOp` / `ui:queryMandatory` / `allowFilterOp`）、ID 字段处理。
- **重要语义澄清**（按产品要求）：`*` 必填标记仅用于 xmeta prop 非 mandatory 但本表单需要必填的场景；如果 prop 已声明 `mandatory="true"`，页面自动显示必填，不需要在 layout 中加 `*`。
- **交叉引用更新**：`INDEX.md` 新增路由条目；`view-and-page-customization.md` Form 章节和默认修改路径表添加引用；`frontend-rendering-pipeline.md` 头部引用块添加条目；`page-dsl-pattern-catalog.md` 先记住列表添加条目。
- `docs/analysis/view-form-layout-gap-analysis.md` §7.5 标注已修复。
- 变更位于 `nop-entropy` 仓库（5 个 docs-for-ai 文件），非本项目代码。

### 2026-07-12（view.xml 表单布局优化计划起草 — plan）

- `docs/plans/2026-07-12-1500-1-view-form-layout-overhaul.md` 起草。基于 `docs/analysis/view-form-layout-gap-analysis.md` 三个差距（id 显示 / 零分组 / 查询表单空壳），拟制全域表单布局优化计划。
- **Phase 0**：codegen 模板修复（`view-gen.xlib:GenForm`）——跳过 `seq-default` 主键，全域重生成后 338 实体的 _gen 表单不再含 `id[ID]`。
- **Phase 1**：核心 5 域 22 实体（purchase 6 / sales 6 / inventory 3 / finance 3 / master-data 4）——手工定制分组 layout（基本信息/金额/业务关联/审计，审计组折叠）+ 查询表单（5 字段 + filterOp like/date-between）。
- **Phase 2**：扩展域 18 实体（manufacturing/crm/hr/logistics 等）——同范式，重点覆盖最大实体 ErpMfgWorkOrder(43字段)/ErpCrmLead(40字段)/ErpLogShipment(40字段)。
- **Deferred**：~298 低频实体分组（Phase 0 已解决 id 隐藏）；codegen tagSet→分组自动推导。
- 计划状态 `draft`，待独立草案审查。
