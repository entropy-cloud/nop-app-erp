# Development Log - 2026-08-21

### id-string-migration mission：M0.2 执行完成（前向耦合登记册 + 工具豁免机制，M1.2 前置就绪）

- **执行**：`docs/plans/2026-08-21-1657-1-bigint-id-m02-forward-coupling-registry.md` 三 Phase 全部完成（Phase 1 扫描此前已落，本次完成 Phase 2 登记册数据 + 两工具豁免机制 + Phase 3 逐域汇编 + roadmap 消费协议）。
- **产出**：
  - **登记册**：机器可读权威 `tools/id-migration-registry.json5`（254 active = orm-column-deferral 8（fin→prj 6 + hr→prj 2，evidence 行号逐行吻合 M0 裁决 §10.1）+ service-bridge main 94 / test 30 + backward-pointer main 60 / test 62）；人类可读逐域汇编 `docs/audits/2026-08-21-1657-id-m02-forward-coupling-registry.md`（§1 消费协议 + §2 总览矩阵 + §6 逐域 A 前向义务 / B 退役义务 / C 消费集 + §7 特殊行）。
  - **工具豁免机制**：`check-bigint-id-types.mjs`（loadRegistry fail-closed + buildDeferredMap + scan 报 `DEFERRED(registry)` + dry-run 延后列保持 long + 延后条目命中核对）；`verify-id-fix-copy-diff.mjs` 对齐（延后列差异行 = `[DEFERRED-FLIPPED]` FAIL + 变更行计数显式排除延后列 + 同 fail-closed）。实现期修复门控既有缺陷：已迁移域无副本（dry-run 不生成）在全 19 域模式恒 FAIL → `pendingChangeCount` 判定零待改列 = `[NO-COPY-NEEDED]` 合法缺席（否则 M2.1 完成后 gate module-finance 永久误报）。
  - **roadmap**：M0.2 → `done` + 「M0.2 产出指针」（登记册路径 + 消费协议 ①起草/Phase 1 强制消费本域条目全集 ②退役机制 M2.7/晚域 plan/早域自身 ③工具门控仅消费 orm-column-deferral + fail-closed ④规则 6 联动）+ M1-M3 标准结构 Phase 1 补强制消费注记 + mermaid M0_2 done。
  - **M1.2 notify 消费走查**（可操作性 Proof）：从登记册工件直接读出 notify 视角 = 零前向义务（0 orm 延后 + 0 main/test 桥接）+ 零晚域退役义务 + 后向被引用清单（14 域 main 43 文件 / 14 域 test 28 文件，backward refDomain=notify 28 条，successor 均为引用方自身工作项）——与机器可读登记册程序化核验一致；M1.2 plan 起草可直接消费。
- **验证状态（全绿，零源文件落盘）**：`node tools/check-bigint-id-types.mjs scan` = **1586 NEEDS FIX（1594−8）+ 8 DEFERRED(registry) + md 段 0**（exit 0）；`dry-run` = 18 文件/1586 列、XML 18/18、幂等、副本程序化核验 fin 6/6 延后保持 long + 208/208 其余翻转、hr 2/2 + 136/136；`verify-id-fix-copy-diff.mjs module-finance` = 变更行 208（排除 6 延后）门控通过；负面测试 4 项（副本人为翻转延后列 → `[DEFERRED-FLIPPED]` exit 1；registry 缺失/不可解析 → 两工具 `[FAIL]` exit 1；有待改列域副本缺席 → exit 1）；全 19 域 gate = 1586 + md NO-COPY-NEEDED + exit 0。本计划零 Java/orm 变更，Maven 门控不适用（Closure Gates 已声明）。
- **下一步**：M1.2 notify 迁移（plan `2026-08-21-1657-2` 已起草，登记册消费协议强制生效）。

### id-string-migration mission：M1.1 执行完成（master-data 根域先导试点，D6 判据成立）

- **执行**：`docs/plans/2026-08-21-1045-3-bigint-id-m11-master-data-migration.md` 四 Phase 全部完成（Phase 1/2 此前已完成 + M0 裁决 Decision D 恢复取证；本次完成 Phase 3/4）。
- **产出**：
  - Phase 3 测试修复 + 快照重录：20 个测试类 Long→String 改型 + 5 个 `TestStub*` SPI 桩随新 dao 签名改型；快照 RECORDING→CHECKING 重录足迹 = 93 个已跟踪文件标记修改（44 个内容 diff + 49 个 CRLF 行尾差异）+ 11 方法新增 input/output 落盘（22 未跟踪目录）；`_cases` json5 数字形式实体 id 重扫 **0 残留**。先导 Proof 域级落点：`TestErpMdPartnerCrudSmoke.testPartnerAddressRelation`（关系导航无显式 id 保存）产物 `"id": "2"` String 非空。
  - Phase 4 grep 门控清零：`.longValue()`/`Long.parseLong(`/`Map<Long`/`String.format("%d` md 手写（main+test，除 `_gen`）**0 命中**；Long 装箱 id `==`/`!=` 0；sql-lib.xml 仓内 0 存在（注明）；残留 Long 核清 = `Map<String,Long>` 引用计数返回值（非 id）+ `delVersion`（Non-Goal）+ `SettlementAllocation.invoiceId`（语义 FK 指向未迁移 fin，已登记随 M2.1 翻转）。手写 view.xml 零改动（erp-md-web 变更 100% 在 `_gen/`）。owner doc 注记 3 处（`ui-patterns.md`、`unified-party-identity.md`×2）；`domain-design-guidelines.md` §16A 无 md 实体行（清理归 M4.1）。
  - 偏差登记：`ErpMdWebPagesTest` 按已提交治理决策（plan 2026-07-24-0930-1 `@Tag("full-app")` + surefire `<excludedGroups>`）模块级排除；JUnit Launcher 直跑实证其依赖全量 classpath（`/erp/xlib/control.xlib` 模块级缺失）——页面校验 successor = **M4.1** app-erp-all `ErpAllWebPagesTest`，与本计划 Non-Goal「不跑全量构建」一致。
  - 执行期发现修复：平台 IoC 回归（nop-entropy 08-21 09:33 重装 jar 后 `nopSequenceGenerator` bean-init-self-wait，bisect 与本迁移无关）——test-scope VFS delta（`ioc:lazy-property`）修复 + bug 登记 `docs/bugs/2026-08-21-nop-sequence-generator-ioc-self-wait-after-platform-reinstall.md`。
  - roadmap M1.1 → `done`（含先导试点结论：**D6 判据成立**——自身模块链全绿 + 破坏仅限已登记 `_gen` 耦合点 prj-dao 27/fin-dao 97（successor M2.7/M2.1 + M4.1 兜底），冻结总序维持）；下一步 = M0.2 前向耦合登记册（M1.2 前置）→ M1.2 notify。
- **验证状态（全绿）**：`mvn clean install -pl module-master-data/erp-md-{codegen,dao,meta,service,web,app,api} -DskipTests` 7 模块 BUILD SUCCESS + `mvn test -pl module-master-data/erp-md-service,module-master-data/erp-md-web` service 24 类 **155/155 绿**（两次独立复跑）+ `node tools/check-bigint-id-types.mjs` md 段零 `NEEDS FIX`。中间态（已登记，successor 愈合）：prj-dao/fin-dao `_gen` 关系胶水破坏、`ErpMdWebPagesTest` 页面校验（M4.1）、`SettlementAllocation.invoiceId` Long（M2.1）。

### id-string-migration mission：M1.1 rule-6 停止（闭包内惰性 dao `_gen` md 耦合证伪 M0.1 审计 ③，回报 M0 裁决）

- **执行**：`docs/plans/2026-08-21-1045-3-bigint-id-m11-master-data-migration.md` Phase 1 完成；Phase 2 md 范围完成后在 19 模块闭包门控处触发 rule-6 停止条款，Plan Status → `blocked`（证据全文见 plan §Rule-6 Stop Report）。
- **已完成产出（md 域本体，保留在工作树待裁决后恢复）**：
  - Phase 1 orm 回写：时点 dry-run（19 文件/1662 列幂等）+ `verify-id-fix-copy-diff.mjs module-master-data` 门控通过（68 变更行零非 stdDataType）+ 落源；git diff 证明 68×`stdDataType="long"→"string"`、归一化 diff=0；工具 scan md 段零 `NEEDS FIX`。
  - Phase 2 增量重生成 + md 手写主代码修复（dao 11 文件 + service 12 文件，签名/容器 Long→String，见 plan 执行记录）；md 5 模块链 `mvn clean install -Dmaven.test.skip=true` **BUILD SUCCESS**。
- **rule-6 停止证据**：19 模块 reactor 在 `app-erp-projects-dao` FAILURE（28 错，全在**生成件** `_gen/_ErpPrj*.java` to-one 关系胶水 `internalSetRefEntity(..., () -> setXxxId(refEntity.getId()))`，refEntity=md 实体 String getId、prj FK 仍 Long；例 `_ErpPrjProject.java:1286` manager→ErpMdEmployee）。prj orm md 关系 27 处、fin 97 处（fin-dao `_gen` 32 文件同胶水，预期同败）、notify 0 处。**证伪机理**：M0.1 审计 ③ 仅枚举 dao 手写跨域 import，漏生成件关系胶水的编译级耦合；prj orm 未迁移则重生成亦无法修复。md 手写码零错误（审计手写层结论仍成立）。
- **验证状态（部分绿）**：md 链 5 模块 main 全绿；19 模块闭包 red（prj-dao 生成件，rule-6 停止不改他域）；`mvn test` 未达（Phase 3 阻塞，TestStub* 测试编译断为预期中间态）。
- **下一步**：M0 裁决（选项 a 合并域 plan md+fin+prj / b 调整冻结序重跑 freeze 脚本 / c 修订规则 3 域级 verify 闭包口径）→ 本 plan 从 Phase 2 Exit Criteria 断点恢复（md 主代码修复无需重做）→ Phase 3/4 照旧。roadmap M1.1 保持 `todo` + 停止注记；冻结总序其余部分不受影响。

### id-string-migration mission：M1.3 执行完成（common-service 组织隔离 orgId String 语义适配）

- **执行**：`docs/plans/2026-08-21-1045-2-bigint-id-m13-common-service-orgid-string.md` 三 Phase 全部完成（先于 M1.1 master-data，md orm 6 个 orgId FK 列前置约束满足）。
- **产出**：
  - 三文件 String 语义：`ErpOrgContext`（`currentOrgId` 返 String、`setCurrentOrgId(String)`、`toLong` → `toStringValue` 校验式宽容归一：String 数字合法性校验合法直返/非法 null、Number `String.valueOf`、null/空白 null）；`ErpOrgIsolationOrmInterceptor.stampOrgId`（String orgId + 列值经 `toStringValue` String 归一比较，写入 String——消除迁移后 `Long.equals(String)` 恒 false 重复 stamp）；`ErpOrgIsolationQueryTransformer.transform`（`FilterBeans.eq(PROP_ORG_ID, orgId)` 过滤值 String）。
  - 新增 `TestErpOrgContext` 纯单测 12 用例（转换矩阵 String/Long/Integer/abc/空白/null + 写读回 + provider 路径 + `isActive` config-gated 双侧锁定）。
  - **中间态登记**：`TestErpOrgIsolation`（fin-service test）`setCurrentOrgId(ctx, 2L)` 编译破坏已实际发生，按路线图预先裁决登记（plan Deferred But Adjudicated + M0.1 审计 §6 successor 标注 M2.1；不在 M1.3/M1.1 verify 闭包内，破坏面为零）。
  - roadmap M1.3 → `done`（状态表 + Work Item Details 执行记录）。
- **验证状态（全绿）**：`mvn clean install -pl module-common-service -am -DskipTests` BUILD SUCCESS；`mvn test -pl module-common-service` **22/22 绿**（TestErpOrgContext 12 + TestMaskAuditRecorder 10）；grep 门控：`orgId.equals|FilterBeans.eq(...PROP_ORG_ID` 2 命中逐条核对 String 语义、`Long` 代码零残留（2 处 Javadoc 历史注记例外逐条核可）。
- **下一步**：M1.3 结束审计（独立子代理）→ M1.1 master-data 迁移（plan `2026-08-21-1045-3`，依赖 M0.1 ✅ + M1.3 ✅ 已满足）。

### id-string-migration mission：M0.1 执行完成（冻结序 + 跨域审计 + Proofs + 路线图写回）

- **执行**：`docs/plans/2026-08-21-1045-1-bigint-id-m0-order-freeze-audit-proofs.md` 四个 Phase 全部完成、Plan Status → `completed`（Phase 1-3 各自 Evidence 落盘计划文件；Phase 4 本条；结束审计证据在计划 Closure 节）。
- **产出**：
  - `tools/freeze-id-migration-order.mjs`（冻结序脚本：compile/test 闭包分开建模、service 锚点判据、`--edges/--why/--format json`，两次运行输出一致）+ `tools/verify-id-fix-copy-diff.mjs`（回写新鲜度门控）+ `tools/scan-cross-domain-id-coupling.mjs`（跨域耦合扫描器，域 plan 门控复用）。
  - **冻结总序**：`master-data → notify → aps → b2b → contract → finance → assets → cs → hr → inventory → maintenance → projects → quality → manufacturing → purchase → sales → crm → drp → logistics`（19 域无死锁；finance 唯一全域构建纠缠域，web/app 重生成延后至 projects 后）。
  - **惰性 dao 19/19 证实零证伪**（判据编译耦合解释：跨域 import 仅 crm 3 文件类型级 + pur/sal 4 行非实体）；另登记 dao 层语义 FK Long 参数 82 处/11 域（随域翻转，附录 C）+ service 层 205 耦合文件/289 跨域边（附录 A）+ orgId 调用点全量清单（附录 D）。
  - **seq-string Proof 落地（载体 (a)，未降级）**：`module-common-test` `TestSeqStringIdProof` **4/4 绿**——seq-default 生成 id instanceof String 非空、显式 `5L`/`"5"` 存活且 String、FK 形态列 String 往返。零 pom 改动（3 实质新文件 + `_module` 标记；spike 判据①③未触发、②按实质口径 3≤3 未触发，计数口径已透明登记）。
  - **Maven `-am` 实测**（3.9.12）：test 闭包全量入 reactor 且遍历中间模块 test 边（sal-service `-am` 49 模块，两变体一致）；脚本模型与 md 16/19、sal 49 实测精确吻合；DAG 抽样 12/12 零偏差。
  - **Decision A 回写机制 = 时点 dry-run + 新鲜度门控**（apply 沙箱实测根本不写源文件，被否）。
  - **审计工件**：`docs/audits/2026-08-21-1045-id-migration-m0-freeze-audit.md` + 附录 `...-cross-domain-coupling-appendix.md`（A 耦合清单 / B dao import / C 语义 FK / D orgId / E 计数复测 / F E2E 影响面 874/11/36）。
- **roadmap 写回**：M2/M3 合并为冻结总序表（原工作项编号不变 + 精确依赖链替换占位）；M1 内部序核验（md orm 6 个 orgId FK 列实证，M1.3 先于 M1.1 维持）；§当前基线刷新 08-21 实况（477/1185/1662/42/380、getId 1176、Long 2586/2896）；「共识达成」行笔误修正；§框架复用「工具」与 §M1-M3 Phase 1 两处「复制回写」旧文本改为裁定机制；E2E 清单与审计工件指针登记。
- **验证状态**：`mvn test -pl module-common-test` 4/4 绿（本计划唯一 Java 测试变更面）；`check-bigint-id-types.mjs dry-run` 19/19 xmllint+零残留+幂等；`verify-id-fix-copy-diff.mjs` 19/19 门控通过（1662 行全 stdDataType-only）；冻结脚本双重运行一致；`git status` 零 `model/*.orm.xml` 与生产 Java 变更（新增仅 tools 3 脚本 + module-common-test test-only 4 文件 + docs）。
- **下一步**：M0.1 独立结束审计已 `passes closure audit`（ses_fdd7d3f54ffeuesGsgCqk2so5J）→ roadmap M0.1 `done`、M1.3 转 `ready`（plan 1045-2 已批准待执行）。

### id-string-migration mission：M0.1/M1.3/M1.1 三计划起草 + 独立审查达成共识（首批执行序）

- **起草**：`docs/plans/2026-08-21-1045-1-bigint-id-m0-order-freeze-audit-proofs.md`（M0.1 顺序冻结+审计+Proofs，N=1）→ `...-2-bigint-id-m13-common-service-orgid-string.md`（M1.3，N=2）→ `...-3-bigint-id-m11-master-data-migration.md`（M1.1 先导试点，N=3），全部 `Plan Status: active`。
- **审查过程拦截并修复的关键事实错误**（独立子代理 fresh session 多轮，全部记录在各 plan Draft Review Record）：
  - **`_tmp/bigint-id-string-fix/` 副本过期**：实况 scan 为 PK 477/BIGINT FK 1185=1662 列（非 roadmap 08-16 的 1605）；14/19 域副本含非 stdDataType 漂移（mnt 334 行缺整实体、md 3 处）——盲 cp 将回滚 RC mission 经双批准的增量。M0.1 新增副本全量刷新执行项 + 回写机制裁定改为「结构性绑定实况源 + 新鲜度门控」，M1.1 Phase 1 自带同门控。
  - **`-pl <聚合器> -am` 是恒绿 no-op**：Maven 不展开聚合 pom 子模块（实测 reactor 仅 1 模块）——M1.1 全部构建/验证命令改显式子模块列表 `-pl module-master-data/erp-md-api,module-master-data/erp-md-app -am`（实测 19 模块）。
  - **md verify 闭包含未迁移惰性 dao**：`-pl erp-md-service -am` 实测 16 模块，经 common-test optional 依赖拉入 fin/prj/notify dao+codegen——M1.1 补规则 6 停止条款。
  - **`-DskipTests` 仍编译测试源**：5 个 `TestStub*` 桩实现 Phase 2 改签名的 dao SPI，Phase 2 门控必断——改 `-Dmaven.test.skip=true` + 测试编译错误清单移交 Phase 3。
  - **测试口径**：33 文件 = 25 个 `@Test` 类（service 24 + web 1）+ 8 支撑文件；web 测试须显式并入 test 命令。
- **roadmap 变更**：M0.1 → `ready`（plan 1045-1 双批准转 active，roadmap 规则 1）；M1.3/M1.1 工作项保持 `todo`（依赖前置项未 done）。roadmap §当前基线数字块刷新与冲突旧文本修订归 M0.1 Phase 4 执行时落盘。
- **验证状态**：无代码变更（仅 plans + roadmap 状态列 + 本日志）；`node tools/check-bigint-id-types.mjs` scan 实况计数 477/1185/1662/42/380 已复核；reactor 口径（1/16/19 模块）均本机实测。
