# Development Log - 2026-08-30

## Flux 页面发现修复计划 Phase 4：valuesPath 根因簇 + 视觉/行为回归全绿（plan 2026-08-30-2238-1）

**上下文**：修复验证工具首跑发现集（892 erp error → 0）后的 Phase 4 视觉回归阶段。像素基线重录（KPI 卡片激活 35% 漂移，diff 目检确认）后 KPI 断言套件仍 10 挂，深挖出三簇新根因 + 一项用户指令文档任务，全部按「修复=激活真实缺陷」义务收口。

**V1 看板过滤失效簇（P1，产品级）**：9 域看板 + hr payroll 的页面级 data-source 模板读 `${filterForm?.x}` 恒为 null——flux named form **仅经 `valuesPath` 才向父作用域发布值**（`form-runtime.ts setupExternalPublication`；探针证实请求体 `periodId:null`，过滤参数从未发出）。修复：filterForm 补 `valuesPath: filterForm`（fin/sal/pur/inv/ast/mnt/mfg/qa/prj + payroll 同源重写）。修后 KPI 断言 10/10 翻绿，全部命中 value-spec 权威值（fin 1130/sal 1000/pur 850/inv 10450/ast 135000/mfg 180/qa 0.67）。

**V2 @query/@mutation 整形文档（用户指令，人工批准，保护区域）**：调研确认 nop-chaos-next `nopRpcResolver.ts` 的 operationRegistry 参数整形——尾缀命中注册 CRUD 操作（get/findPage/save/batch* 等）时请求体重建为仅含特定命名参数（query/data/id/ids），`filter_*` 转 TreeBean；未注册 @query 透传、未注册 @mutation 兜底 `{data}`。docs-for-ai 原只覆盖 @selection/清洗/includeScope——已补 `docs-for-ai/02-core-guides/flux-rendering.md`（整形表 + /p/ 行 + valuesPath 跨作用域规则 + 页面作者规则），两树提交 1584f4a2cf / 4c275d80c2。

**V3 行为激活范式修正**：
- asn-flow 行点击 `onRowClick+setValue` 不可用：行事件 action 上下文=行作用域，`scope.update` 本地写在行内遮蔽（flux 写不向父级走）；crud selection 组合态固定写 `$_crud.<id>.*` 命名空间（`selectionStatePath` 仅为受控读取覆盖）且 data-source reaction 不订阅该命名空间。重写为操作列「流程」按钮 + `openDialog`（行绑定 `${id}` 进 surface 作用域，flux-guide crud-with-dialog 范式）。
- voucher/recon 反冲预览模板 flux 化：`<b>/<br/>` HTML 标签非法（纯文本渲染）→ 多 text 节点；`previewData.x` 非可选访问在数据源未就绪时抛模板错误 → `previewData?.x` + 「加载预览中…」占位；`| default:` 管道 → `??`。

**V4 测试侧腐化修复**：`_helper.ts`——REST `/r/` 等待（8/29 迁移遗留断 /graphql）、KPI 取值选择器 `span.h3`→`h3`（flux tag:h3）、图表断言 `canvas`→`svg.recharts-surface`（recharts）、`pickFluxDate` 日历驱动（react-day-picker 天按钮全日期 aria-label；day-15 探测显示月份防上月格振荡）、过滤重载改 fill 自动重载等待（模板依赖跟踪：args 模板读 filterForm 即隐式依赖，防抖 ~4s 自动重发；click refreshSource 被去重不再发请求）；spec 补 6 域权威 seed 窗口（global-setup 钉当前期间后月度默认不再覆盖 7 月种子）；`reverse-preview.action.spec.ts` 数据构造修复（postVoucher `assertBalancedFromLines` 会把 header 合计重算为行和——补 Dr1401/Cr2202 平衡分录行 + acctSchemaId + ErpMdSubject/eqFilter 形状）。

**验证状态**：dashboards.snapshot 10/10 + dashboards.visual 10/10 + reverse-preview + f16-p2 + hr-payroll 全绿（BASE_URL 复跑）；validate:flux erp 子树 exit 0（平台残余 137 为已知 Deferred）。ERP 提交 c677f76cc（V1+V4+基线重录）、9a4427d84（V3+spec 修复）。

## Flux 页面批量导出与编译验证工具链落地（三仓，plan 2026-08-30-1126-1）

**上下文**：用户 /goal——通用工具验证生成的 flux 页面符合 flux 框架要求（Java 导出 + JS 验证，工具内置 nop-entropy，ERP 验证）。设计文档 `docs/architecture/flux-page-export-and-validation.md`（决策 D1–D7），计划 `docs/plans/2026-08-30-1126-1-flux-page-export-and-validation.md`（双独立子代理批准后 active）。

**三仓落地**：

- **nop-entropy**（master + fix-ai-check 两工作树同源落地）：`PageProvider.renderPageTo` flux 回退修复（此前导出绕过 flux.yaml 回退）+ 新增 `WebPageExporter`/`PageExportOptions`/`PageExportResult`（enabled-modules 枚举、生产语义缺省、失败收集、manifest.json）+ demo `render-pages.xrun` 升级为薄包装（`$scope.containsValue` 判存可选参数）+ `TestWebPageExporter` 4/4 + cli.md 更新。验证：nop-web 91/0/1、`TestNopCli#testRenderPages+testParameter` 2/2（需 deactivate 类级 @Disabled）、`-am install` 绿。日志 `nop-entropy/ai-dev/logs/2026/08-30.md`。
- **nop-chaos-flux**：`flux-guide/scripts/validate-pages.mjs`（通用编译验证 CLI：L0 禁止静默跳过 / L1 validateSchema / L2 compile-throw 去重 / `xui:*` 剥离计数 / `--report` / 退出码 0-1-2）+ `shared.mjs` 抽取 `buildRegistry(packages?)`/`buildFullRegistry()`（validate.mjs 保持历史 9 包输出不变，与 git HEAD 对照 errors=37/warnings=76 完全一致）+ 根 package.json `flux:validate-pages` script。自举：金标 87 文件 0 error exit 0（strippedXui=1）；坏 fixture 4 类全报 exit 1。该仓**预存**状态已登记其日志：`pnpm build` 因 `flux-renderers-content` TS2339（HEAD `01770f770` 移除 `ApiResponse.ok` 未迁移）失败。日志 `nop-chaos-flux/docs/logs/2026/08-30.md`。
- **nop-app-erp**：`ErpAllFluxPagesExportTest`（999 页 14s 导出、failedPages 断言、3 个手写 flux.yaml 孪生页面「导出 == 生产 getPage」全等内置断言）+ `scripts/validate-flux-pages.sh` + 根 package.json `validate:flux` + e2e-runbook「flux 页面静态门禁」节。

**端到端验证（全绿/发现即报告）**：`npm run validate:flux` 全链打通。三项对账：manifest pageCount 999 == 磁盘 999 文件；`erp/*` 855 == 源清单 `find module-* -name '*.page.yaml' | wc -l`；孪生页面导出内容 == `getPage(path,'zh-CN')` 序列化全等。

**首跑发现集（1172 error / 41341 warning / 999 文件，分级登记，修复归后续切片）**：

- **A 类——Java 生成管线 amis 泄漏（nop-entropy flux-web 管线或页面侧修复）**：
  - 未知 renderer type 249：`static` 94 / `service` 53 / `tpl` 46 / `button-toolbar` 25 / `code` 16 / `number` 6 / `panel` 4 / `_hidden` 3 / `wrapper` 2。经 registry 实证（验证器 11 包 ⊃ 浏览器 bundle 6 包，均无这些类型）：**浏览器 flux 运行时同样无法渲染这些节点**（真实缺陷，非验证器误报）。
  - 属性枚举越界 303：`button.variant=primary` 296 + `success` 1（flux variant 枚举无 primary/success）+ `container.direction=vertical` 6（应映射 column）。
  - 分布：ERP 476 文件（855 中）+ 平台页（nop/wf 136、nop/auth 43、nop/sys 24、nop/report 9）。
- **B 类——编译器规则 vs 运行时容忍（nop-chaos-flux 裁决）**：`invalid-reaction-deps` 532（`/body/loadAction/dependsOn` 空——运行时容忍、页面数据加载正常，规则是否应对 loadAction 豁免需裁决）、`invalid-action-shape` 51、`unresolved-action-selector` 5（plain selector 如 `notify` 无 `xui:actions` 定义）、`unhandled-compilation-error` 14 及零星 region/slot/shape 7。
- `strippedXui=9`：9 页面导出含 `xui:*` 壳层键，剥离逻辑按设计工作（设计 D7 已按实测修正措辞）。

**环境事件与处置（m2 混装）**：master 工作树的 `-am install` 将 nop-dao 等内核 jar 刷为 master 版（master 的 `SnowflakeSequenceGeneator` 拼写与 ERP 基线所在的 fix-ai-check 分支 `SnowflakeSequenceGenerator` 不一致），导致 `ErpAllFluxPagesExportTest` NoClassDefFoundError。处置：Phase 1 变更同源移植到 `nop-entropy-wt/nop-entropy-fix-ai-check` 并从该工作树 `-am install` 恢复 m2 一致性（12:29 后导出测试通过）。两工作树变更同源（diff 仅本计划文件）；master 侧为规范集成位，分支侧为 ERP 当前平台基线。

**ERP HEAD 预存编译缺陷 ×2（drive-by Fix，范围外披露，见 plan「Phase 3 执行中的两项范围外事件」）**：F2.8-F2.12 提交批（08-28 23:49 ~ 08-29 07:46，晚于最后一次全绿基线）引入两处源码编译错误，ERP HEAD 此前从未全量源码编译暴露：

1. `module-quality/erp-qa-service/.../SpcSamplingService.java` 使用 `PageBean` 未 import → 补 `import io.nop.api.core.beans.PageBean;`。
2. `module-sales/erp-sal-service/.../ErpSalInvoiceProcessor.java:421` 对 Biz 接口误用 DAO 方法 `voucherBiz.getEntityById(...)`（`ICrudBiz` 无此方法，`IEntityDao` 才有）→ 按等价语义改 `voucherBiz.get(voucherId, true, null)`（ignoreUnknown=true，未找到返回 null，与原意图一致）。全仓其余 1300+ 处 `getEntityById` 均为 DAO 接收者，无同类误用。

**结束审计整改（iteration 1 REJECTED → 复验）**：独立结束审计发现 Blocker——`module-notify/.../ErpSysNotification/inbox.page.yaml`（plan 2026-08-29-1913-1 未提交批次）三处同级 `then:` 键重复，且初始 999/0 验证语料是全量构建刷新 m2 前的**陈旧 classpath 产物**（`-pl` 消费 m2 jar）。整改（drive-by #3，范围外披露）：三处改嵌套 then 链（按 inv dashboard 语料规范，python yaml 重复键检测 NONE）→ `mvn -pl module-notify/erp-notify-web install` → `ErpAllFluxPagesExportTest` 复验 **999/855/0 绿（当前工作树语料）** → `npm run validate:flux` 复跑新报告 **1104 error / 41477 warning / strippedXui 9**（8/29 重写页入语料后 service/button-toolbar 泄漏消失；A 类 unknown-type 249→172、ERP 出错文件 476→448）→ Phase 4 分级数字按新语料刷新。e2e-runbook 补「m2 新鲜度警示」；fix-ai-check 树 cli.md 补移植（其日志声明对齐）。教训：scoped `-pl` 测试消费 m2 jar，全量构建刷新 m2 后必须复跑消费方验证。

**验证状态（本切片口径，整改后终态）**：

- ERP 全量 `mvn clean install -DskipTests` — **BUILD SUCCESS 01:39 min**（156 reactor 模块，含上述 2 处修复后；未运行全量 `mvn test`，测试验证口径 = app-erp-all scoped `ErpAllFluxPagesExportTest` 1/1 绿（整改后以当前工作树语料复验）+ 导出/对账/端到端全链）。
- `bash docs/audits/nop-compliance-checker.sh` exit 0，规则命中计数与修复前**零漂移**（qa/sal/notify 三处 drive-by 不触 checker 站点）。
- `npm run validate:flux` 端到端 exit 1（发现即报告，语义正确）；`_tmp/flux-page-validation-report.json` 留存本机（`_tmp/` gitignore）。
- 三仓日志：`nop-entropy/ai-dev/logs/2026/08-30.md`（master + fix-ai-check 双工作树）、`nop-chaos-flux/docs/logs/2026/08-30.md`、本文件。

**结束审计（独立子代理 fresh session）**：iteration 1 REJECTED（Blocker：inbox.page.yaml 重复 `then` 键 + 初始验证语料为陈旧 m2 classpath 产物，详见上方整改段）→ 整改 → iteration 2 **APPROVED**（审计员独立重放：导出测试 999/855/0 绿、对账复验、报告总账全平、两树 cli.md diff 空；残留 r1/r2/r3 Minor 已登记，r3 已在 plan 2026-08-29-1913-1 补交接注记）。计划 `2026-08-30-1126-1` **Plan Status → completed**（Closure Gates 全勾，审计指针落盘计划 Closure 节）。
