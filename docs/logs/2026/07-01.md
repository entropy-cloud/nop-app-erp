# Development Log - 2026-07-01

### 2026-07-01（计划 1132-2 执行：销售出库审批状态机 + 出库触发库存移动——sales→inventory→finance 跨域打通，含销售独有可用量校验）

- 执行 `docs/plans/2026-07-01-1132-2-sales-delivery-approval-inventory-trigger.md`（Phase 1-3 全部完成）。`ErpSalDeliveryBizModel` 此前为 `CrudBizModel` 空壳，本计划落地三轴审批状态机 + 出库审核触发 `IErpInvStockMoveBiz.generateMove`(OUTGOING)（首个 sales→inventory 跨域调用方，且是首个依赖「出库可用量校验」的业务域）+ `posted` 接线 + 发货状态回写 + 反向冲销前置。与采购镜像对称（N=1），核心差异：出库类 CONFIRM 校验可用量（销售独有）、`unitCost` 由库存域按移动加权平均 `avgCost` 快照（调用方不传）、出库的反向冲销=入库移动单。
- **Phase 1（三轴审批状态机 + 客户启用校验）**：`IErpSalDeliveryBiz` 增 `submit/withdrawSubmit/approve/reject/reverseApprove/cancel` 签名（`@SingleSession @Transactional`）；`ErpSalDeliveryBizModel` 实现迁移，校验前置 `approveStatus`/`docStatus`，违反抛 `NopException(ERR_ILLEGAL_STATUS_TRANSITION/ERR_ILLEGAL_DOC_STATUS_TRANSITION)`；`submit`/`approve` 双点校验 `ErpMdPartner.status`（客户启用值，停用抛 `ERR_PARTNER_INACTIVE`）；反审核目标态 REJECTED（对齐 state-machine §3）。`ErpSalConstants`（状态码 + OUTGOING=20 副本）/`ErpSalErrors`。4 个行为测试全绿。状态机自检（state-machine-business-review-prompt 10 维）= pass。
- **Phase 2（出库触发 + 可用量校验 + posted 接线 + 发货回写 + 反向冲销）**：`erp-sal-service/pom.xml` 新增结构性依赖——compile `app-erp-inventory-dao`（`IErpInvStockMoveBiz`+DTO）、compile `app-erp-master-data-dao`（`ErpMdPartner` 机制 B）、test `app-erp-inventory-service`/`app-erp-projects-dao`/`app-erp-assets-dao`（finance ORM 关系引用所需）；`master-data-service`(test) 已存在。`DeliveryStockMoveBuilder`（非 BizModel bean，经 `app-service.beans.xml` 注册）构造 `StockMoveRequest`(OUTGOING)：`relatedBillType=ERP_SAL_DELIVERY`/`relatedBillCode=delivery.code`（幂等键）、`sourceWarehouseId=delivery.warehouseId`、`destWarehouseId=null`（发往客户）、`acctSchemaId` 按 `orgId` 解析 `ErpMdAcctSchema`、行 `quantity` 直读 DECIMAL / **不传 `unitCost`**（出库由库存域 `avgCost` 快照，售价 `unitPrice` 不得作为成本传入）、`sourceLocationId`=null 走整仓余额（`deliveryLine.warehouseId` displayName 出库库位但无关系声明、语义未定，整仓 locationId=null 合法）。`approve` 幂等（已 APPROVED 空操作）→ `generateMove`（业务联动自动 DONE，**可用量不足由库存域 CONFIRM 内 `validateAvailable` 抛 NopException 致整个出库单审核回滚**，state-machine §4 销售独有）→ `delivery.posted=move.posted` + `postedAt`/`postedBy` → 回写源 `ErpSalOrder.deliveryStatus`（UNDELIVERED/PARTIAL/DELIVERED，BigDecimal 按行比较）。`reverseApprove`/`cancel` 按 Design A 内部冲销：`(ERP_SAL_DELIVERY,code)` 纯查询定位原单 → `(REVERSAL,原单.code)` 幂等防双冲销 → `stockMoveBiz.reverse(moveId)`（出库的反向=入库）→ 状态迁移。6 个行为测试全绿。
- **关键实现发现/坑**：(1) **可用量校验归属**——不在 sales 侧预检，`approve` 直接调 `generateMove(OUTGOING)`，由库存域 `confirm()` 内 `validateAvailable` 裁决；不足抛 `NopException` 上抛致整个 approve 回滚（出库单保持 SUBMITTED、余额未扣减、移动单未 DONE）。(2) **可用量不足回滚的真实行为**：移动单实体在 CONFIRM 拒绝前已 `saveEntity`（DRAFT），会话内 DRAFT 实体残留（对齐 inventory 域 `testConfirmInsufficientAvailableRejected` 基线——「整个业务审核回滚由调用方事务保证」）；业务效果回滚由「出库单 SUBMITTED / 余额未扣减 / 移动单未 DONE / 无过账」证明，而非「无移动单实体」。(3) **负库存配置**：`erp-inv.allow-negative-stock=true` 经库存域 `AppConfig.var` 读取，销售侧无须额外处理（`testNegativeStockConfigAllowsShortage` 用 `AppConfig.getConfigProvider().assignConfigValue` 测试期覆盖证明）。(4) 出库 `unitCost` 由库存域维护（`bookOutgoing` 读 `balance.avgCost`），sales 调用方不传（传了也会被覆盖）。(5) SALES_OUTPUT 凭证科目：借 6401 主营业务成本 / 贷 1401 库存商品（与采购 PURCHASE_INPUT 借存货/贷暂估应付 方向相反）。(6) 库存域 `reverse()` 不传播 `acctSchemaId` 致冲销单红字过账失败、`posted=false`（库存域已知边界，purchase 侧已记 Follow-up，sales 侧同样），sales 侧契约只保证冲销移动单生成 + 余额恢复。
- **Phase 3（端到端 + 收尾）**：`TestErpSalOrderToDeliveryEnd` 全链路（建订单+出库单 UNSUBMITTED→submit→approve→出库移动单 DONE+余额 20→10+存货凭证(SALES_OUTPUT:借主营业务成本50/贷库存商品50)+posted=true+订单 deliveryStatus DELIVERED→reverseApprove→内部冲销(入库)+余额恢复 20+APPROVED→REJECTED）。1 个端到端测试全绿。
- **范围保护**：未改任何 `model/*.orm.xml`/`.api.xml`（保护区域）；sales 侧纯新增 Java（BizModel/Builder/Constants/Errors/接口签名）+ 测试（4 状态机+6 出库触发+1 端到端）+ beans.xml；未注册 sales 过账 Provider（`SALES_OUTPUT` 由库存域 `InvAcctDocProvider` 独占，避免 `ERR_DUPLICATE_PROVIDER` 启动 fail-fast）。未改 inventory 代码（消费 `generateMove`/`reverse` 契约）。
- 验证状态：**全绿**。`mvn test -pl module-sales/erp-sal-service -am` = BUILD SUCCESS（审批状态机 4 + 出库触发 6 + 端到端 1 + CRUD 冒烟 5 = 16 测试，Failures=0/Errors=0）。完整仓库 `mvn test -fae` 归 Closure Gates。
- 下一步：独立子代理结束审计；订单审核状态机 + 客户信用额度 / nop-wf 审批流 / 销售发票·收款 Provider / 库存 reverse() 传播 acctSchemaId（Follow-up）。

### 2026-07-01（计划 1132-1 执行：采购入库审批状态机 + 入库触发库存移动——purchase→inventory→finance 跨域打通）

- 执行 `docs/plans/2026-07-01-1132-1-purchase-receipt-approval-inventory-trigger.md`（Phase 1-3 全部完成）。`ErpPurReceiveBizModel` 此前为 `CrudBizModel` 空壳，本计划落地三轴审批状态机 + 入库审核触发 `IErpInvStockMoveBiz.generateMove`（首个 purchase→inventory 跨域调用方）+ `posted` 接线 + 收货状态回写 + 反向冲销前置。
- **Phase 1（三轴审批状态机 + 供应商启用校验）**：`IErpPurReceiveBiz` 增 `submit/withdrawSubmit/approve/reject/reverseApprove/cancel` 签名（`@SingleSession @Transactional`）；`ErpPurReceiveBizModel` 实现迁移，校验前置 `approveStatus`/`docStatus`，违反抛 `NopException(ERR_ILLEGAL_STATUS_TRANSITION/ERR_ILLEGAL_DOC_STATUS_TRANSITION)`；`submit`/`approve` 双点校验 `ErpMdPartner.status`（启用值，停用抛 `ERR_PARTNER_INACTIVE`）；反审核目标态 REJECTED（非 UNSUBMITTED，对齐 state-machine §3/§11.4）。`ErpPurConstants`（状态码 + INCOMING=10 副本）/`ErpPurErrors`（错误码）。4 个行为测试全绿。状态机自检（state-machine-business-review-prompt 10 维）= pass。
- **Phase 2（入库触发 + posted 接线 + 收货回写 + 反向冲销）**：`erp-pur-service/pom.xml` 新增结构性依赖——compile `app-erp-inventory-dao`（`IErpInvStockMoveBiz`+DTO）、compile `app-erp-master-data-dao`（`ErpMdPartner` 机制 B）、test `app-erp-inventory-service`/`app-erp-master-data-service`/`app-erp-projects-dao`/`app-erp-assets-dao`（finance ORM 对 projects/assets 实体关系引用所需，与 inventory-service 测试依赖模式一致）。`ReceiveStockMoveBuilder`（非 BizModel bean，经 `app-service.beans.xml` 注册）构造 `StockMoveRequest`(INCOMING)：`relatedBillType=ERP_PUR_RECEIVE`/`relatedBillCode=receive.code`（幂等键）、`destWarehouseId=receive.warehouseId`、`acctSchemaId` 按 `orgId` 解析 `ErpMdAcctSchema`（凭证行非空约束所需）、行 `quantity` 直读 DECIMAL / `unitCost=new BigDecimal(unitPrice)` 解析 VARCHAR（空则 null，非法抛 `ERR_INVALID_UNIT_PRICE`）。`approve` 幂等（已 APPROVED 空操作）→ `generateMove`（业务联动自动 DONE）→ `receive.posted=move.posted` + `postedAt`/`postedBy` → `receive.receiveStatus=RECEIVED` → 回写源 `ErpPurOrder.receiveStatus`（UNRECEIVED/PARTIAL/RECEIVED，BigDecimal 按行比较）。`reverseApprove`/`cancel` 按 Design A 内部冲销：`(ERP_PUR_RECEIVE,code)` 纯查询定位原单 → `(REVERSAL,原单.code)` 幂等防双冲销 → `stockMoveBiz.reverse(moveId)` → 状态迁移；原单缺失抛 `ERR_MOVE_NOT_FOUND`。4 个行为测试全绿。
- **关键实现发现/坑**：(1) **跨域 `generateMove` 调用扰动会话脏跟踪**——approve 内调 `stockMoveBiz.generateMove`（嵌套 @SingleSession）后，receive/order 的脏属性在同事务内不再被查询/flush 可见，故改用 `dao().updateEntity()` 显式持久化 + 重新 `getEntityById`；订单回写时当前单行始终计入（不依赖同事务查询可见性）。(2) **`acctSchemaId` 缺口**：入库单实体无 `acctSchemaId` 列（计划「取自入库单」与 ORM 不符），改按 `orgId` 解析 `ErpMdAcctSchema`；存货估值凭证行 `acctSchemaId` 为非空约束，未解析到则过账失败置 `posted=false`（不阻塞入库终态）。(3) **库存域 `reverse()` 不传播 `acctSchemaId`**（移动单实体无该列）→ 冲销单 SALES_OUTPUT 红字过账因凭证行 `acctSchemaId` 非空约束失败、冲销单 `posted=false`；属库存域 `reverse()` 已知边界（红字凭证生成需库存域传播 `acctSchemaId`，记 Follow-up），purchase 侧契约只保证冲销移动单生成 + 余额冲回。(4) Nop 跨域实体读（master-data 机制 B）用 `daoFor(ErpMdPartner.class)` 直接加载（纯实体读、无 I*Biz 业务逻辑，AGENTS.md 允许并加注释）。(5) `IUserContext.get()` 无上下文时返回 null（非抛），`approvedBy`/`postedBy` 在无认证测试中为 null 安全。
- **Phase 3（端到端 + 收尾）**：`TestErpPurOrderToReceiveEnd` 全链路（建订单+入库单 UNSUBMITTED→submit→approve→移动单 DONE+余额+10+存货凭证(借存货50/贷暂估50)+posted=true+订单/入库单收货 RECEIVED→reverseApprove→内部冲销+余额归零+APPROVED→REJECTED）。1 个端到端测试全绿。
- **范围保护**：未改任何 `model/*.orm.xml`/`.api.xml`（保护区域）；purchase 侧纯新增 Java（BizModel/Builder/Constants/Errors/接口签名）+ 测试（9 行为+端到端）+ beans.xml；未注册 purchase 过账 Provider（`PURCHASE_INPUT` 由库存域 `InvAcctDocProvider` 独占，避免 `ERR_DUPLICATE_PROVIDER` 启动 fail-fast）。未改 inventory 代码（消费 `generateMove`/`reverse` 契约）。
- 验证状态：**全绿**。`mvn test -pl module-purchase/erp-pur-service -am` = BUILD SUCCESS（审批状态机 4 + 入库触发 4 + 端到端 1 + CRUD 冒烟 5 = 14 测试，Failures=0/Errors=0）。**[mission-driver 提交复核 full-green]** 根 `mvn clean install -DskipTests` + 根 `mvn test` 均 BUILD SUCCESS（146 reactor 模块，全绿，无回归）。
- 下一步：独立子代理结束审计；订单审核状态机 / nop-wf 审批流 / 采购发票·付款 Provider / 库存 reverse() 传播 acctSchemaId（Follow-up）。

### 2026-07-01（计划 1800-1 执行：文档修正收敛——16 问题落地）

- 执行 `docs/plans/2026-07-01-1800-1-document-correction-convergence.md`（Phase 1-4 全部 34 个落地动作完成）。本计划是纯文档型，无代码/ORM 变更，落地分析报告 `docs/analysis/2026-07-01-0000-design-doc-readiness-and-structure-assessment.md` 的 16 条选定方案到对应 owner doc。
- **Phase 1（落位映射 + 18 域口径，#1/#8）**：`domain-module-split-analysis.md` 新增 §2.0 工程命名映射表（19 行 = 18 域 + 1 聚合，含逻辑工程名/物理目录/子模块前缀/appName/moduleId/简称/实体前缀/表前缀）。修正 moduleName↔moduleId 推导规则（`moduleId = moduleName.replace('-','/')`，故 `erp-md`↔`erp/md`）。全局 10 域口径统一为 18 域：product-scope/project-vision/project-context/system-baseline/module-boundaries/data-dependency-matrix/customization-capabilities/competitive-comparison/architecture-README/design-README/b2b-README/logistics-README + 11 个域 README 的 appName（三级 `app-erp-md` → 两级 `erp-md`）。`app-erp-app` 旧名全部更正为 `app-erp-all`。
- **Phase 2（审批+过账+跨域+边界，#2/#4/#5/#11/#12/#13）**：审批四态（UNSUBMITTED/SUBMITTED/APPROVED/REJECTED）统一于 approval-framework/flow-overview/service-layer-orchestration（清 APPROVING/PENDING_APPROVAL + order.status→approveStatus）。`posting.md` 改三层模型（① 业务+库存强制 SYNC / ② 凭证时序可配 / ③ posted 兜底）+ businessType vs billType 分工 + 稳定约束小节 + postNow 同步测试缝。`cross-domain-constraints.md` 删除 @RefLink，替换为平台原生机制 B+D。`module-boundaries.md` 删英文孤儿段、细化 manufacturing/quality/projects 分层方向。`data-dependency-matrix.md` 加 §2.0 裁决原则 5 条 + §4.1/§4.4 过账时序可配 + §5.2 billType 反向引用。`domain-design-guidelines.md` §3.1 主数据引用分级（机制 B/D/写 I*Biz/Delta）。
- **Phase 3（requirements+权限+测试，#3/#6/#7）**：`mvp.md` 废弃标注（走完整产品渐进式演化，不走 MVP）。`product-baseline.md` 重写（技术机制迁出、占位符填充、18 域）。`roles-and-permissions.md` 拆设计能力基线/运行基线 + portal 指针。`testing-strategy.md` 声明唯一 owner doc + 四类资产边界 + 跨域归属三层 + 异步时序模型 + P0/P1/P2 清单；`test-strategy.md` superseded。`seed-data.md` 标注部署资产。新建 `app-erp-test-data` 模块骨架（pom.xml + load-order.txt + tables/README.md）。
- **Phase 4（路由+清理，#9/#10/#14/#15/#16）**：design/architecture README B2B 双层分工（业务语义归 design、集成契约归 architecture）。`feature-inventory.md` 去重 + APS 统一指 aps/README。`portal/README.md` STATUS 横幅 + 支付/SSO/mall 降级 (future)；新建 `portal/identity-and-access.md`（外部主体模型骨架，ask-first）。`app-overview.md` 补 portal 指针。`domain-design-guidelines.md` 顶部定位 + 三处技术实现规则迁出指针。`integration-and-transaction-patterns.md` 去模板口吻。`erp-design-audit-checklist.md` 历史标注。
- **关键执行决策**：(1) `mvp.md`/`test-strategy.md` 因 AGENTS.md 规则 14（未经批准不入 archive）采用 superseded 标注而非物理移动（计划允许此选项）。(2) `domain-design-guidelines.md` 技术实现规则用"概要+迁出指针"明确 design vs architecture 边界，物理搬迁留作低风险整理（非 deferred）。(3) §2.0 映射表 VFS moduleId 初稿误写 `app/erp/md`（三级），经核实 `vfs-and-resource-resolution.md:121` 的 moduleName↔moduleId 推导规则更正为 `erp/md`。
- **范围保护**：未改任何 `model/*.orm.xml`/`.api.xml`；纯 owner doc 更新 + 1 个新模块骨架（`app-erp-test-data`，资源模块非业务域）。
- 验证状态：**文本一致性全绿**（活跃 owner doc 无 10 域口径、无 `app-erp-app` 旧名、无三级 appName、无 @RefLink 传播、无 APPROVING/PENDING_APPROVAL 残留用法）。无构建/测试命令（文档型计划）。
- **结束审计**（独立子代理 ses_0e3f9bc1fffe7jYZMo9jYsau6i）：结论 PASS WITH MINOR → 修正后 PASS。审计逐 Phase 打开文件核实 34 项落地动作（非采信自查），4 组退出标准全部满足。发现 3 处外围文档残留（不在计划显式目标清单），已按审计建议修正：`l10n/cn-golden-tax.md:9`(10→18 域)、`logistics/carrier-integration.md:80`(`app-erp-log-service`→`erp-log-service`)、`customization-capabilities.md:111`(虚构 beans 文件名改为准确 VFS 路径 `_vfs/erp/fin/beans/app-service.beans.xml`)。Closure Gates 6 项全勾选。
- **人工批准追加**：经人工批准，`docs/requirements/mvp.md`、`docs/architecture/test-strategy.md` 从 superseded 标注改为**直接删除**（非 archive），并清理 3 处引用（`requirements/README.md`、`references/document-naming-and-timeliness.md`、`testing-strategy.md` 顶部声明）。活跃 owner doc 无悬空引用。
- **清理历史演变叙述**（原则：稳定 owner doc 只呈现当前状态，演变历史归 logs/）：移除 cross-domain-constraints 的 @RefLink 取代说明、domain-design-guidelines §3.1"原禁令已废止"+两处"旧版指南曾用"注、domain-module-split-analysis §1.3 纠正段+"10 域历史口径"、data-dependency-matrix 裁决原则"孤儿草稿"条+终态统计"原/新增"、testing-strategy "旧稿已删除"+seed-data "之前提及"、feature-inventory/design-README/contract-README 的"此前曾标记/现已升级"、b2b/logistics 的"已纳入"、product-baseline "不是 MVP 路线"、erp-design-audit-checklist "历史审计清单"标注。owner doc 现直接呈现当前机制与状态。
- 方法学备注：本环境 `rg`(ripgrep)未安装，残留扫描须用 `grep -rn`，避免假阴性。
- 下一步：可选低风险整理（guidelines ErrorCode/删除策略物理搬迁至新建 architecture 文档）；codegen 后更新 data-dependency-matrix 全量 to-one 统计。

### 2026-07-01（计划 0811-2 执行：库存移动单 BizModel——状态机+流水/余额+可用量校验+存货过账）

- 执行 `docs/plans/2026-07-01-0811-2-inventory-stockmove-bizmodel.md`（Phase 1-3 全部完成）。StockMove BizModel 此前为 `CrudBizModel` 空壳，本计划落地状态机、跨域 `generateMove` 契约、不可变流水、移动加权平均余额、可用量/负库存、存货过账端到端。
- **Phase 1（契约+状态机+幂等）**：`IErpInvStockMoveBiz` 增 `generateMove/confirm/complete/cancel/reverse`（`StockMoveRequest`/`StockMoveLineRequest` DTO 落 `erp-inv-dao/biz`）；`ErpInvStockMoveBizModel` 实现状态迁移（`@SingleSession @Transactional`，迁移校验前置 docStatus，违反抛 `NopException(ERR_ILLEGAL_STATUS_TRANSITION)`）；业务联动（`relatedBillType` 非空）自动 DRAFT→CONFIRMED→DONE，独立创建停 CONFIRMED；幂等键 `(relatedBillType, relatedBillCode)`；冲销生成反向新单（非反审核）。`StockMoveBookkeeper`（记账器）+ `InvPostingDispatcher`（过账派发器）作为非 BizModel 服务经 `app-service.beans.xml` 注册。6 个行为测试全绿。状态机自检（state-machine-business-review-prompt 10 维）= pass。
- **Phase 2（流水+余额+可用量+负库存）**：`StockMoveBookkeeper.bookCompletion` DONE 时按行写不可变 `ErpInvStockLedger`（含 `balanceQuantity`/`balanceTotalCost` 结存快照、quantity 按方向带符号、unitCost 快照）并按 `materialId×warehouseId×locationId×batchNo` 维度更新 `ErpInvStockBalance`——移动加权平均（入库 `avgCost=(旧totalCost+入库totalCost)/(旧totalQty+qty)`、出库 `unitCost=当前avgCost`）；入库增/出库扣/内部调拨扣源加目的。`confirm` 增可用量校验（`available≥required`，不足抛 `ERR_AVAILABLE_INSUFFICIENT`；`erp-inv.allow-negative-stock=true` 跳过，经 `AppConfig.var` 读取）。5 个记账测试全绿。
- **Phase 3（存货过账端到端）**：`erp-inv-service` 新增 compile 依赖 `app-erp-finance-service`（inventory→finance 单向无环）；`InvPostingDispatcher` DONE 后按 moveType 派生 businessType（入库→PURCHASE_INPUT、出库→SALES_OUTPUT、同法人内部调拨跳过），从流水汇总构造 `PostingEvent.billData`，经 `InvPostingExecutor`（**`@Transactional(REQUIRES_NEW)`** 独立新事务）调 `ErpFinPostingService.post`——过账失败 try/catch 吞异常记日志、保持 DONE+`posted=false`（不阻塞终态，符合 cross-domain 合约）；成功置 `posted=true`。`InvAcctDocProvider implements IErpFinAcctDocProvider`（非默认，声明 PURCHASE_INPUT/SALES_OUTPUT，Registry 中优先于默认 fallback）按 `TOTAL_COST` 产 2 条存货估值 `VoucherFact`（入库：借库存商品1401/贷暂估应付2202；出库：借主营业务成本6401/贷库存商品1401）。3 个端到端测试（过账生成凭证+posted=true / 内部调拨不过账 / 过账失败不阻塞终态）全绿。
- **关键实现发现/坑**：(1) Nop 新建实体（status=SAVING）只能 `saveOrUpdateEntity` 不能 `updateEntity`（`nop.err.orm.dao.update-entity-not-managed`）；(2) 同事务内多次改同一余额，查询前须 `ormTemplate.flushSession()` 否则查不到未刷盘的预留量；(3) move 头无 `acctSchemaId` 列（ledger 有），故经 `StockMoveRequest` 携带到 `doComplete` 参数；(4) `AppConfig.var(name, defaultValue)` 读全局配置、`getConfigProvider().assignConfigValue` 测试期覆盖；(5) **过账失败隔离须 `REQUIRES_NEW`**——REQUIRED 会把主事务标 rollback-only，吞异常仍致 DONE 回滚；(6) **跨计划 Fix**：finance `ErpFinPostingService.buildVoucherCode` 用枚举 `name()`（PURCHASE_INPUT=14 字符）+ 32hex uuid = 51 字符超 `CODE VARCHAR(50)`，改用 `type.getCode()`（int）降至 ≤40 字符（已重装 finance-service）。
- **范围保护**：未改任何 `model/*.orm.xml`/`.api.xml`（保护区域）；inventory 侧纯新增 Java+测试+beans.xml。finance 侧仅 1 行 `buildVoucherCode` 修复（blocking bug，记于此）。
- 验证状态：**全绿**。`mvn test -pl module-inventory/erp-inv-service -am` = BUILD SUCCESS（StockMoveBizModel 6 + Bookkeeping 5 + Posting 3 + CRUD 冒烟 5 = 19 测试，Failures=0/Errors=0）。完整仓库 `mvn test -fae` 归 Closure Gates。
- 下一步：独立子代理结束审计；purchase/sales 调用方接入 `generateMove`（Follow-up）。

### 2026-07-01（计划 0811-1 执行：业财过账引擎基础落地——SPI+注册中心+编排服务+默认模板 Provider+红冲）

- 执行 `docs/plans/2026-07-01-0811-1-finance-posting-engine-foundation.md`（greenfield 业财过账引擎；Phase 1-3 全部完成）。过账引擎此前全仓不存在（`IErpFinAcctDocProvider`/`Registry`/`ErpFinPostingService` 0 命中），`posted` 标志已在 8 业务域落地但无代码驱动——本计划是该能力的基座。
- **Phase 1（SPI 契约 + 注册中心）**：新增包 `app.erp.fin.service.posting`——`ErpFinBusinessType`（13 常量 code 与 `business-type.dict.yaml` 10..130 逐一一致）、`IErpFinAcctDocProvider`（`getSupportedBusinessTypes`/`createFacts`/`isFallback`）、`IErpFinFactsValidator`（`validate`+`getOrder`）、`PostingEvent`/`AcctDocContext`/`VoucherFact` 契约、`ErpFinAcctDocRegistry`（`@PostConstruct` 先装非默认 Provider 建 EnumMap、同 key 冲突 fail-fast，默认 fallback 填充空缺 key）、`ErpFinPostingErrors`（ErrorCode 集中定义）。2 个 Registry 契约单测（fallback 优先 / 重复 fail-fast）全绿。
- **Phase 2（编排服务 + 默认模板 Provider）**：`ErpFinTemplateAcctDocProvider`（fallback，声明 6 核心类型，按 businessType+acctSchemaId 查模板、amountKey 填金额、subjectCode 占位符替换）；`ErpFinPostingService.post`（幂等回链反查→查 Provider→createFacts→Validator 链→期间门控仅 OPEN→科目反查 ErpMdSubject→BigDecimal 借贷平衡→写 Voucher+Line+BillR+docStatus=POSTED）。4 个服务层集成测试（happy/幂等/不平衡/期间结账）全绿。**关键实现发现**：Nop IoC 的 `@Inject List<T>` 字段按 `List.class` 解析、不自动收集接口实现（已读 nop-ioc 源码 `BeanDefinitionBuilder.resolveBeanByType`/`getRawClass` 核实），故 Provider/Validator 列表改用 `app-service.beans.xml` 的 `<ioc:collect-beans by-type=.../>` 注入——语义等价、平台文档推荐机制；`@SingleSession`+`@Transactional` 经 `orm-defaults.beans.xml`/`dao-defaults.beans.xml` 全局 pointcut 拦截器自动应用于非 BizModel bean。凭证状态机自检（state-machine-business-review-prompt）= pass，无 P0/P1，2 项 P2 残留风险（多币种汇率强校验、并发 post 竞态）记入计划。
- **Phase 3（红字冲销 + 收尾）**：`ErpFinPostingService.reverse`（按回链反查非红字已过账凭证→复制分录金额取负→`isReversed=true`/`reversalOfVoucherId` 关联原凭证→走正常平衡/期间校验→写新红字凭证+回链；反查失败抛 `ERR_REVERSE_SOURCE_NOT_FOUND`；currency/exchangeRate 经 AcctDocContext 从原分录携带）。2 个红冲测试（成功：红字负金额/双向回链/净额为 0；未找到：NopException）全绿。
- **范围保护**：未改任何 `model/*.orm.xml`/`.api.xml`（保护区域）；纯新增 Java + 测试 + beans.xml + 计划/日志。引擎只负责凭证侧（凭证+回链+docStatus=POSTED），源单据 `posted` 由域调用方置位（与 inventory 同批 0811-2 Non-Goals 对齐）。
- 验证状态：**全绿**。`mvn test -pl module-finance/erp-fin-service -am` = BUILD SUCCESS（Registry 2 + Posting 6 + VoucherTemplate 冒烟 5 = 13 测试，Failures=0/Errors=0）；8 个计划内行为测试全部通过。完整仓库 `mvn test -fae` 归 Closure Gates。
- 下一步：独立子代理结束审计；inventory 同批 `2026-07-01-0811-2` 可消费本引擎 `ErpFinPostingService.post(PostingEvent)`。

### 2026-07-01（工具：mission-driver CLI 改 k8s 风格子命令 run/draft/list；源码归属模板，删除本仓本地 fork）

- 背景澄清：mission-driver 引擎源码的唯一真相源是 AGE 模板仓 `attractor-guided-engineering-template/tools/mission-driver/`，本仓经 `tools/mission-driver.sh` 薄 launcher 调用。本仓此前另有一份本地 fork `tools/mission-driver/`（git 跟踪），与模板已分叉，故本次重构落在模板，并删除本地 fork。
- **CLI 重构（落在模板仓）**：`src/main.js` `parseArgs` 由「伪 flag 命令」改为显式子命令 `run <mission>` / `draft <desc>` / `list [missions|steps]`。原 `--list-missions`/`--list-steps`/`--draft-mission` 是命令伪装成 flag（`config.js` 里 `--list-missions` 甚至在配置解析阶段 `process.exit(0)`），现统一为动词；`--step`/`--dry-run`/`--agent`/`--model`/`--max-*` 仍为 `run` 的真正修饰符。裸定位参数不再隐式等于 run（破坏性变更），须显式 `run <mission>`，否则报错并提示。
- **config.js 重构（模板仓）**：移除 `resolveConfig` 内 `process.exit(0)` 副作用与 draft 分支，拆出纯函数 `resolveProjectRoot`/`resolveMissionsDir`/`resolveCommon`，新增 `resolveDraftConfig`；resolver 仅 throw 不自杀，命令分发与输出归 main.js；新增 `-h/--help` 与未知命令友好提示。
- **删除本仓本地 fork** `tools/mission-driver/`（40 文件）：核对文件名层面两树一致、无独有文件；内容差异中 3 处（`src/runner.js` 的 `shell: IS_WIN32`、`prompts/draft-from-audit.md` 的 `> Source Audits:` 溯源、`prompts/execute.md` 的关闭源审计步骤）均为模板较新，本地是旧快照；`flows/mission-driver.json` 一处行为分叉（本地 `DRAFT_PLANS` vs 模板 `REVIEW_PLANS`）经确认为本地漂移、模板正确。删除安全性已核实：plan-check 经 `flow-loader.js:4` 进程内 `import`（相对模板 src/），与 projectRoot 无关；prompt 缺省回退模板、project 可覆盖。
- **同步调用面**：模板仓 `mission-driver.sh.example`/`package.json`/`design/mission-design.md`/`tools/README.md` 示例改新 CLI；本仓保留 `tools/mission-driver.sh` launcher（用法注释改新 CLI，指向模板）并重写 `tools/README.md` Mission Driver 段为本仓实况（launcher→模板、不持有源码、记录 run/draft/list）；修 `docs/plans/00-plan-authoring-and-execution-guide.md` 对本地 prompt 路径的悬空引用为指向模板。
- **help 体系 k8s 化（模板仓）**：`main.js` 引入 `COMMANDS` registry 单一真相源，命令清单由 `Object.keys(COMMANDS)` 派生（自动发现）；新增 `help` 真子命令，`help`/`help <cmd>`/`<cmd> --help` 三者从 registry 渲染、同构；修掉 `help` 被误判未知命令的 bug。本仓 launcher `./tools/mission-driver.sh help run` 经端到端验证可用。
- 验证状态：**全绿**。模板仓 `npm test` = 77 pass/0 fail；模板 `main.js` CLI 自检（`--help`/`list steps`/未知命令/裸调用）符合预期；**端到端**：经本仓 launcher `./tools/mission-driver.sh --help|list missions` 跑的是模板新 CLI 且正确读本仓 `missions/`（crud/erp），旧式裸调用 `./tools/mission-driver.sh erp` 正确报错并提示 `run erp`。
- 注意：模板仓在我改动前已有未提交改动（`flows/mission-driver.json`、2 个 prompts、`tools/README.md`、删 `check-active-doc-code-anchors.mjs`），属既有工作；本次 5 个文件（main.js/config.js/.example/package.json/design doc）为独立叠加，提交时注意区分。本仓与模板仓改动均未 commit（待确认）。

### 2026-07-01（文档：统一计划命名规则为日期时间+序号）

- `docs/plans/00-plan-authoring-and-execution-guide.md` 新增「计划命名规则」小节：文件名采用 `{YYYY-MM-DD-HHmm}-{N}-{slug}.md`（日期时间 + 一位执行序号），不使用纯数字前缀（`01-`/`02-`）；与 mission driver 的 `draft-from-roadmap.md` / `draft-from-audit.md` 命名约定对齐；指南为计划格式权威源（mission-design.md），原仅 mission driver 端有此规则而指南缺失。
- `docs/plans/README.md` 同步：移除与指南冲突的建议（原 `YYYY-MM-DD-HHmm-topic-plan.md` 无序号、以及 `NNN-` 大前缀回退），推荐文件名改为现网既有示例。
- 元文件（本指南 `00-`、`README`）保留固定前缀，不参与此命名约定。

### 2026-07-01（计划 0215-1 执行：收尾延迟 CRUD 冒烟测试，18/18 域全绿）

- 执行 `docs/plans/2026-07-01-0215-1-complete-deferred-crud-smoke-tests.md`（Phase 1-3 全部完成，CRUD 路线图收尾至 18/18）。
- **Phase 1 — manufacturing 字典缺陷修复**：
  - 新增 `module-master-data/erp-md-meta/.../dict/erp-md/posted-status.dict.yaml`（`valueType: string`，3 态 DRAFT/POSTED/CANCELLED，语义对齐 `docs/design/finance/state-machine.md` 凭证过账生命周期；格式对齐现有 `batch-strategy.dict.yaml`）。
  - `module-manufacturing/erp-mfg-service/pom.xml` 新增 test-scope `app-erp-master-data-service` 依赖（关键：`erp-md-dao` 不依赖 `erp-md-meta`，字典资源不传递；该依赖同时使字典上 mfg-service 测试 classpath，同 inventory 先例）。
  - 解除 `TestErpMfgRoutingCrudSmoke` 的 `@Disabled`；**录制并审查** `_cases/` 快照（基线中「快照均已就绪」不准确，实际仅有空 `autotest.yaml`，首次以 RECORDING 录制后切 CHECKING；删除用例 `DEL_VERSION` 列置 `*`）。
- **Phase 2 — customer-service 幽灵实体缺陷修复**：
  - 删除 `module-cs/model/app-erp-cs.orm.xml` 末尾 2 个死声明实体 `app.erp.pro.{ErpProProject,ErpProTask}`（零关系依赖、无对应 Java、无「pro」域），保留合法跨域引用（`ErpMdPartner`/`ErpMdOrganization`/`NopAuthUser`）。
  - regen：`mvn generate-test-resources -pl module-cs/erp-cs-codegen`（exec-maven-plugin 实际绑定 `generate-sources` 阶段执行 `CodeGenTask`→`postcompile/gen-orm.xgen`）；核实生成 `_app.orm.xml` 不再含 `app.erp.pro`。
  - 解除 `TestErpCsTicketTypeCrudSmoke` 的 `@Disabled`；录制并审查快照（`ErpCsTicketType`→`ErpCsSlaPolicy` 头-行对，cs 内部无强制跨域外键故无需 master-data 依赖），删除用例 `DEL_VERSION` 置 `*`。
- **Phase 3 — 收尾**：`crud-roadmap.md` Phase 4 逐域表 mfg/cs 两行 ⏸→✅（含抽样实体）+ 状态文字 16/18→18/18；前置计划 `2026-06-30-2328-2` Deferred 的 mfg/cs 两项追加「已由 0215-1 解决」后继指针（不改其 Plan Status，保持历史可追溯）。
- 关键执行发现：mfg/cs 两域 `_cases/` 快照此前从未录制（模块从未成功加载）；解除 `@Disabled` 后须先 RECORDING 录制、人工审查、`DEL_VERSION` 通配、再切 CHECKING——与 runbook 沉淀的首次录制流程一致，非「解除 @Disabled 即绿」。
- 验证状态：**全绿**。`mvn clean install -DskipTests` = BUILD SUCCESS；根 `mvn test -fae` = BUILD SUCCESS（18 域 × 5 方法 = 90 测试，Failures=0/Errors=0/**Skipped=0**，无 `@Disabled`）；逐域 `mvn test -pl module-{manufacturing,cs}/erp-*-service -am` 与批量一致。
- 下一步：独立子代理结束审计；CRUD 路线图闭合，后续重心转 `implementation-roadmap.md`（BizModel/状态机/业财过账深化）。

### 2026-07-01（计划 2328-2 执行：Phase 4 CRUD 冒烟测试，16/18 域通过）

- 执行 `docs/plans/2026-06-30-2328-2-phase4-crud-smoke-tests.md`（Phase 1-4 全部完成，计划标记 completed）。
- **Phase 1（脚手架样板）**：`module-master-data/erp-md-service` 新增 `TestErpMdPartnerCrudSmoke`（5 方法：新建/查询/编辑/逻辑删除/关系导航），继承 `JunitAutoTestCase`，`_cases/` 快照。确立可复制模式 + 4 条关键约束。
- **Phase 2/3（16 域冒烟测试）**：每域 `Test<Head>CrudSmoke` 置于 `-service/src/test/java/app/erp/<short>/service/`。抽样（主实体 + 头-行对）：purchase(Requisition→Rfq)、sales(Quotation→QuotationLine)、inventory(StockMove→StockMoveLine)、finance(VoucherTemplate→VoucherTemplateLine)、assets(Asset→DepreciationSchedule)、projects(Project→Task)、quality(InspectionTemplate→InspectionTemplateLine)、maintenance(Visit→VisitTask)、crm(BundlePricing→BundlePricingLine)、hr(Survey→SurveyQuestion)、aps(OperationOrder→OpRouting)、contract(Contract→ContractLine)、drp(Plan→Line)、logistics(Shipment→ShipmentLine)、b2b(Asn→AsnLine)。
- **关键执行发现（已沉淀到 testing-strategy.md）**：(1) `save`=插入、`update`=合并；(2) ERP 主键 `seq-default` 不被自动变量机制识别，多步 id 在 Java 内传递；(3) `ObjMetaBasedValidator.validateRefValue` 强制校验跨域引用——跨域强制外键需在被测 `-service` 加 `app-erp-master-data-service` test 依赖 + `createPrereqs()` 自建主数据（sales/inventory/contract/drp/finance 如此；finance 另需 assets-dao/projects-dao）；(4) `delVersion`=currentTimeMillis 与 decimal 域列（VARCHAR 存储的 quantity/amount 等）录制↔校验刻度不一致，统一在 `output/tables/*.csv` 置通配符 `*`。
- **2 域阻塞（前置模型缺陷，保护区域，已 @Disabled + Deferred）**：manufacturing（`ErpMfgSubcontractOrder.xmeta` 引用全仓不存在的字典 `erp-md/posted-status`）；customer-service（cs ORM 声明了全仓不存在的实体 `app.erp.pro.*`）。
- **Phase 4（收尾）**：`crud-roadmap.md` Phase 4 标 done + 逐域通过表；`testing-strategy.md` 新增「Nop 测试 runbook 落地」小节并修正 `app-erp-seed` 漂移（模块不存在，改述自包含设计）。
- 验证状态：**全绿**。`mvn clean install -DskipTests` = BUILD SUCCESS；16 域 `mvn test -pl module-<domain>-service -am` 逐域 + 批量 = BUILD SUCCESS（Errors=0）；根 `mvn test -fae` = BUILD SUCCESS（mfg/cs `@Disabled` Skipped=5）。
- 下一步：独立子代理结束审计；mfg/cs 待模型缺陷修复后补测。

### 2026-07-01（计划 2328-1 执行：8 新域接入聚合 app app-erp-all）

- 执行 `docs/plans/2026-06-30-2328-1-phase3-new-domains-app-aggregation.md`（Phase 1-3 全部完成，计划标记 completed）。
- **Phase 1（wiring）**：`app-erp-all/pom.xml` 新增 8 个新域 `-app` 依赖（`app-erp-{crm,cs,hr,aps,contract,drp,logistics,b2b}-app`，artifactId 经各域 pom 逐一核实）；`app-erp-all/.../app.action-auth.xml` `x:extends` 追加 8 个新域 action-auth 路径，注释 10→18 业务域。两文件 `xmllint --noout` 通过。
- **Phase 2（构建+菜单验证）**：
  - 脏工作树处置：~885 个未提交 regen 产物确认为合法 codegen 增量（`_` 前缀 / `_gen/` / sql / beans），保留工作树供构建；提交决策 deferred（无人工批准，自主执行不擅自 commit）。
  - 构建证据：根 reactor `mvn clean install -DskipTests` BUILD SUCCESS（146 reactor 模块，01:16 min，Quarkus `quarkus-run.jar` 产出）。注：因各域 `maven.install.skip=true`，必须从根 reactor 构建，`-pl app-erp-all -am` 无法解析依赖。
  - 菜单合并证据：新增单测 `app-erp-all/src/test/java/io/nop/app/all/auth/TestAppActionAuthMerge.java`，使用 `DslNodeLoader`（运行时合并菜单同一机制）验证 18 域 `x:extends` 合并。PASS：全部 18 域 TOPM id 出现于 `site=main`，零 resource id 重复（静态校验独立确认 415 个 resource id 无重复）。
  - 关键决策：测试手动初始化核心链（Reflection→XLang→VFS→RegisterModel），跳过 IoC/ORM 以隔离已知 `ErpProProject` 类加载问题（projects 域，阻碍完整 IoC 启动，不在本计划范围，记录为 follow-up）。
- **Phase 3（owner-doc 对齐）**：`crud-roadmap.md` Phase 3 翻 ❌→✅（页面/菜单）、状态 planned→done、备注更新；`project-context.md` 10 域/145 实体/82 模块/1096 Java → 18 域/279 实体/146 reactor 模块/1721 Java；`codebase-map.md` 11 模块→19 reactor 模块、ORM 表追加 8 新域行+纠正原 10 域陈旧行（master-data 20→22、finance 13→17、manufacturing 21→23）、标题改「18 域 × 279 实体」、入口点表同步。
- 验证状态：**全绿**。`mvn clean install -DskipTests` = BUILD SUCCESS；`mvn test -Dtest=TestAppActionAuthMerge` = PASS。
- 下一步：独立子代理结束审计；紧邻后续 `2026-06-30-2328-2-phase4-crud-smoke-tests.md`（本计划解除其阻塞）。
