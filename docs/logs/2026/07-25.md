# Development Log - 2026-07-25

### 2026-07-25（全套件 Playwright test-isolation 污染清理 + 绿基线确立 — plan 1945-1）

- **任务**：执行 `docs/plans/2026-07-24-1945-1-playwright-test-isolation-pollution-cleanup.md`（bug `2026-07-23-1408-full-suite-regression-gate-findings.md` Category (a) 5 项预存 test-isolation 污染清理，目标：全套件 `npx playwright test` 达 0 失败除 master-data.write.amis test-infra Non-Goal）。
- **Phase 1（污染源定位 — 诊断 dump 方法）**：采用「诊断 spec 尾部 dump + fresh-DB 累积执行」替代成本高昂的人工二分（方法沉淀 `docs/analysis/2026-07-24-1945-1-test-isolation-pollutant-map.md` §1）。**核心发现：5 项 Category (a) 污染不再复现**——全量执行后诊断 dump 显示残留状态与 pristine 种子基线精确匹配（materials=4 种子行 / balances=∅ / inv totalValue=10450 / mfg inProcessCount=1,periodCompletedQty=180 / md materialCount=4,customerCount=2,vendorCount=2 / noSkuAlert=∅）。5 项受害 spec 全 PASS。消除归因：累积 cleanup 纪律充分（`cleanupStockMove` 删 ledger/line/move/balance 完整链 + `cleanupMfg` 用测试专用组件物料隔离非 MAT-001）+ 07-24 后生产侧变更（daoFor ORM 导航重构等）改善删除语义；测试 cleanup 代码自 commit `149ea745b`（07-23）未变更。Skill: `nop-debugging`。
- **Phase 1 执行期新发现（第 6 项 fin-period-close-wizard）**：全量执行中 `fin-period-close-wizard` FAIL（隔离 PASS）。error-context 定位：`ErpFinAccountingPeriod__closePeriod` 报「期末结账所需科目/汇率未配置：配置键 erp-fin.ap-subject-code」。根因双层：① config 缺口（主导）——`ExchangeRevaluationService.revalueArAp`（`fx/ExchangeRevaluationService.java:103-155`）全局查询未核销外币 `ErpFinArApItem`，命中非空时 `requireSubject` 强制解析 AR/AP/FX 科目；webServer JVM args 仅有 `ar-subject-code=1122`，缺 `ap-subject-code` / `exchange-gain-loss-subject-code`（finance 单测 yaml `period-close-end-to-end-test.yaml` 均设此两键）。② cleanup 残留（触发条件）——某前置写测试残留 OPEN 外币 AR/AP 项触发 FX 重估路径。原 bug doc #19 修复（加 period-end-exchange-rate）不完整。
- **Phase 2（清理缺口修复 + wizard config 完整收口）**：5 项 Category (a) 经诊断证实无 A/B/C cleanup 缺口（无需修复）。wizard 经 config 补齐收口：`playwright.config.ts` + `_tmp-server.sh` webServer JVM args 追加 4 键——`-Derp-fin.ap-subject-code=2202` / `-Derp-fin.exchange-gain-loss-subject-code=6603` / `-Derp-fin.current-year-profit-subject-code=4103` / `-Derp-fin.auto-depreciation-on-close=false`（对齐 finance 单测 yaml 基线）。**执行期连锁发现并修复**：补 ap/exchange-gain-loss 科目后 closePeriod 进度推进至 AST 模块关账，`auto-depreciation-on-close` 默认 true（`ErpFinAccountingPeriodProcessor:687`）致折旧种子资产污染 assets dashboard KPI（accumulatedDepreciation 6000→8416.67）；补 `auto-depreciation-on-close=false` 后 assets KPI 恢复 pristine。`revalueArAp` 只读 AR/AP 项 + 写 FX 凭证（不 mutate 项），FX 凭证 billCode=`FX-REVAL-{period.code}` 经 wizard `cleanupPeriod` 清理，无残留。Skill: none（项目级 Playwright 测试基础设施，无平台技能匹配）。
- **Phase 3（全套件门控验证 + 基线登记）**：fresh-DB 非 visual 全套件 `BASE_URL=http://127.0.0.1:8011 SKIP_WEBSERVER=1 npx playwright test --workers=1`（business-actions/crud/dashboards/examples/orchestration/pages/reports）= **490 passed / 1 failed / 3 skipped（1.0h）**。唯一失败 `crud/master-data.write.amis` ErpMdPartner AMIS form-button（selectOption↔switch test-infra Non-Goal）。诊断 dump（尾 spec）证实残留=pristine 基线。`docs/testing/known-good-baselines.md` 增全套件绿基线条目（2026-07-25）；`docs/bugs/2026-07-23-1408` Category (a) 5 项 + #19 wizard 标记修复；frontend-ui-roadmap「回归测试全绿」项注记更新。
- **清理**：临时诊断 spec `tests/e2e/zzz-diag-isolation-check.spec.ts` + `zzz-diag-residuals.spec.ts` 删除（诊断方法已沉淀分析报告 §1，可按需重建）；`_tmp-server.sh` header 更新（去除 TEMPORARY 标记，注明须与 playwright.config.ts webServer.command 同步）。
- **验证（full-green verification）**：fresh-DB 非 visual 全套件 490p/1f-amis-Non-Goal/3s（诊断 dump clean）。本计划纯测试 config（playwright.config.ts + _tmp-server.sh）+ 文档，零 Java/ORM/契约变更，故 plan Closure Gates 显式注明 `mvn` 门控对交付物不适用。mission-driver 验证步骤追加确认 Java 侧基线全绿：`mvn clean install -DskipTests`（154 模块 BUILD SUCCESS）+ `mvn test`（336 surefire 报告，0 failures / 0 errors / 1 skipped），无回归。
- **Skill**：Phase 1 `nop-debugging`（test failure 根因定位 + error-context 分析 + GraphQL 诊断 dump）；Phase 2/3 none（Playwright 测试基础设施 + 基线登记）。
