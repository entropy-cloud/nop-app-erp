# Development Log - 2026-08-12

### M0.2 全域状态轴清单与 M2/M3/M4 展开（mission entity-state-machine）

- **工作项**：路线图 M0.2（`docs/backlog/entity-state-machine-migration-roadmap.md`）——全域状态轴清单 + 逐条分类裁定 + M2/M3/M4 原子工作项展开。
- **类型**：verification or audit work（全集盘点 + 分类裁定 + 路线图展开，零代码）。
- **产出**：
  - 新增 `docs/analysis/2026-08-12-entity-state-axis-inventory.md`——穷举 19 模块全部候选状态字段（~120 ERP 业务候选 + ~45 posted + ~30 md-ext-ref），逐条按 M0.1 四类分类法裁定。**纳入 103 轴**：M2=19（简单生命周期）+ M3=19（复杂业务/审批）+ M4=65（财务影响/保护域）；每个纳入轴登记八属性（迁移语义 / owner doc / dict / 生产 writer / 既有测试 / 保护区域 / 财务影响 / 跨域副作用）。排除项均记录理由 + 重开触发条件。死状态登记 17 项 + dict 漂移 1 项 + dict 定义缺失 3 项。
  - **writer 盘点**：从实际代码 grep 重新建立范围（~937 行状态写入），不沿用「149 Processor」历史统计。关键发现：全部写入在命名业务动作路径，零 PROD-CRUD writer；~149 个 `protected setDocStatus/setApproveStatus` delegation 方法是天然注入缝。
  - **Ticket SLA drift 裁决**：分类为 `intentional legacy behavior`（`startDateTime = 首次 IN_PROGRESS`，§1 表述被 §实现约定 取代）。owner-doc 补注已落地 `docs/design/customer-service/state-machine.md` §实现约定。解除 M1.1 矩阵固化前置。
  - 路线图 M2/M3/M4 三表展开 103 行（一实体一轴 + 行级 Deps + Owner Doc + Skill）；依赖图更新（节点标注 x19/x19/x65）；M0.2 Status `todo → done`；M1 试点纪律 SLA drift 条标注已完成。
- **边界裁决**：Non-Goals 守住——零代码/零 ORM/零契约变更；master-data 10 个 ACTIVE/INACTIVE 排除 + notify 2 个排除（有理由 + 重开触发条件）；`posted` 一律排除；不固化迁移矩阵字面值。
- **验证**：文档完整性检查（非构建）——(1) 候选穷举覆盖 17 域 + master-data + notify 全集；(2) 每个纳入轴八属性非空可追溯；(3) 每个排除项有理由 + 重开触发条件；(4) M2/M3/M4 每行一实体一轴 + 行级 Deps；(5) `git diff --stat` 证实改动仅在 `docs/analysis/`、`docs/backlog/`、`docs/design/`、`docs/logs/` 下——无生产代码。
- **状态流转**：路线图 M0.2 `todo → done`。M1.1 Ticket 试点 SLA drift 前置解除。M2/M3/M4 共 103 项可按行级依赖排期（均依赖 M1.3 批量模板批准）。
- **下一步**：M1.1 客服 Ticket 试点（SLA drift 已裁决，data-deletion 人工批准已记录）。

### M0.1 实体级 StateMachine Bean 契约定稿（mission entity-state-machine）

- **工作项**：路线图 M0.1（`docs/backlog/entity-state-machine-migration-roadmap.md`）——实体级 `ErpXxxStateMachine` Bean 的技术架构契约 owner doc + CRUD 写入边界裁定 + Bean/Delta 注册与测试策略 + 最小运行时探针。
- **类型**：architecture change（稳定架构契约文档 + 测试作用域探针；非业务实体迁移）。
- **产出**：
  - 新增 `docs/architecture/entity-state-machine-bean.md`（10 节稳定契约：颗粒度/命名、无状态约束、三轴边界、方法形状[含只读 `TransitionDefinition` 元数据接口]、Bean ID 与注册、Delta 同名覆盖、错误/拒绝语义、分工边界表、CRUD 写入边界裁定、测试策略分层[含既有 8 个集成测试基线登记]）。
  - **CRUD 写入边界裁定 = (c) 显式排除**：探索证据（生成 xmeta 中 `status`/`docStatus`/`approveStatus` 均 `insertable/updatable=true`；全仓 `notUpload=0`；通用 CRUD 可写状态字段，无全局写锁）。否决 (a) 禁止（触 ORM/xmeta 保护区域 + 破坏既有创建时写初始态）/ (b) 限制（同保护区域约束）。残留风险：无全局写锁，故迁移项只能宣称「命名动作路径经 Bean 唯一治理」，不得宣称「运行时无任何路径可写状态」；全局写锁 = successor（需独立计划改 ORM/xmeta，ask-first）。
  - **最小运行时探针**（`module-cs/erp-cs-service/src/test/`）：合成 `ErpProbeStateMachine`（不绑定真实实体）+ `ProbeProcessorStub`（按类型注入、非 private `@Inject`）+ `TestErpProbeStateMachineContract`（6 tests）+ `test-probe-statemachine.beans.xml`；在真实 cs IoC 容器证明 (1) Bean 可解析 (2) 按类型注入 (3) 非法边→领域 ErrorCode 映射可观测；不断言 Delta（归 M1.2）。
  - 交叉引用：`docs/architecture/processor-extension-pattern.md` 增「固定迁移矩阵的承载者：实体级 StateMachine Bean」节 + 关系表行；`docs/architecture/customization-capabilities.md` 登记 Delta 覆盖定制点（successor=M1.2）+ 决策矩阵行。
- **边界裁决**：Non-Goals 守住——不迁移真实实体（M1.1 归客服 Ticket）、不证明业务级 Delta（归 M1.2）、不改 ORM/API、`posted` 不作迁移轴、不建全局泛型调度器。
- **验证**：`mvn test -pl module-cs/erp-cs-service -Dtest=TestErpProbeStateMachineContract` → 6/6 绿；`mvn clean install -DskipTests` 全 reactor BUILD SUCCESS；`bash docs/audits/nop-compliance-checker.sh` exit 0（零漂移）；`git diff` 证实零生产代码/生产 beans 变更。
- **独立结束审计**：PASS（`ses_00cf64f9affeZ55oY7HG4edcJG`，6 域全 PASS，0 blocker）。
- **状态流转**：路线图 M0.1 `todo → done`（独立草案审查 2 轮收敛 + 独立结束审计 PASS）。M0.2（全域清单展开）解除硬门控，可起草。
- **下一步**：M0.2 全域状态轴清单与展开（`docs/plans/2026-08-12-0617-2-entity-state-machine-m0-2-inventory.md`），以本契约的方法形状与 CRUD 边界裁定为输入。
