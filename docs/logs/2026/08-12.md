# Development Log - 2026-08-12

### M1.3 客服试点评估 + 批量迁移模板裁定（mission entity-state-machine）

- **工作项**：路线图 M1.3（`docs/backlog/entity-state-machine-migration-roadmap.md`）——客服 `ErpCsTicketStateMachine` 试点矩阵一致性审计 + go/no-go 裁定 + M2/M3/M4 批量迁移模板。计划 `docs/plans/2026-08-12-0738-2-cs-ticket-state-machine-pilot-evaluation.md`（消费 N=1 计划 M1.1+M1.2 产物，不重写 Bean/不重做 Delta 证明）。
- **类型**：verification or audit work（四方对照审计 + go/no-go 裁定 + 模板工件产出；零迁移代码、零模型/契约变更）。
- **Skill**：`state-machine-business-review-prompt.md`（10 维度审查 + dict 可达性 + writer 对照）+ `nop-testing`（层 1/层 3/Delta 证据独立复跑）。
- **产出**：
  - **Phase 1 四方对照审计**（dict ↔ owner-doc ↔ StateMachine 元数据 ↔ writer，10 维度 + dict 可达性第 11 维）：`Verdict: pass`。9 维一致 + 1 维 doc drift（owner-doc §2 迁移表 cancel 来源不全：2 vs §实现约定 4）。每维结论可追溯（引用 Bean `ErpCsTicketStateMachine.java` / owner doc 章节 / dict `app-erp-cs.orm.xml:30-37` / writer 类:行）。
  - **Fix F-1（doc drift，已落地）**：`docs/design/customer-service/state-machine.md §2` 迁移表追加 ASSIGNED→CANCELLED / RESOLVED→CANCELLED 行 + M1.3 审计补注，使 §2 表与 §实现约定 + Bean 元数据 + 生产 writer 三方对齐（路线图规则 5 非静默排除）。
  - **Phase 1 行为回归独立复跑**：`mvn test -pl module-cs/erp-cs-service -am` → 116/116 绿（层 1 矩阵 8 + 层 3 `TestErpCsTicketSlaCsat` 13 + Delta 基线/覆盖 6 + 既有 89）。独立复跑非引用 N=1 断言，满足 M1.3 硬门控。复核外部错误码/SLA 起算（= 首次 IN_PROGRESS）/data-deletion reopen/审计 actionType/Delta 生效均无回归。
  - **Phase 2 go 裁定**：`go`（判据：试点无行为回归 + 元数据可审计 + Delta 生效 + owner-doc 对齐）。残留风险登记（通用 CRUD 可写残留 M0.1 §9.4 / 全域 103 轴审计归 M5.1 / M4 财务细节归各 plan / common 码参数名映射作文档对齐 successor）。
  - **Phase 2 批量迁移模板工件**：`docs/architecture/entity-state-machine-bean.md §11`「迁移模板（M1.3 产物）」——§11.1 七步标准序列（Bean 形状→注册→接线→层 1 矩阵→层 2 四方对照→层 3 回归→Delta 适用性）；§11.2 M2/M3/M4 三类变体点（M2 简单生命周期 / M3 审批轴独立 Bean + 复杂分支 / M4 全部 plan-first + 过账时序不改 + `posted` 不入轴）；§11.3 覆盖性自检（8 代表样例无空白槽）；§11.4 警示条目（owner-doc 内部漂移 / SLA intentional legacy / cancel 多来源态与终态领域异常重叠）。
- **边界裁决**：Non-Goals 守住——零迁移代码/零模型/零契约变更；不抽样全域 103 轴（归 M5.1）；不裁定 M0.1 契约（消费之）；不重写 Bean/重做 Delta 证明（消费 N=1 产物）；不修复 dict 死状态（Ticket 无死状态维持）；不声称全域 Delta 验证（归 M5.3）。
- **验证**：审计完整性（非构建）——(1) 四方对照每维可追溯；(2) Fix F-1 登记 + 落地；(3) go 判据明确；(4) 模板覆盖 M2/M3/M4 代表样例无空白槽；(5) 行为回归 = 独立复跑 `mvn test -pl module-cs/erp-cs-service -am` 116 全绿；(6) `git diff --stat` 证实改动仅在 `docs/` 下（无 `module-*/` 生产代码、无 `model/`、无 ORM）。
- **状态流转**：路线图 M1.3 `todo → done`。M2/M3 各 19 项 Deps（M1.3）门控解除，可启动独立 plan（各项 `todo → ready` 仍需各自草案审查）。M4 各 65 项保持 `todo` + plan-first。
- **下一步**：M2.1 `ErpMdSupplierApproval.status`（既有 `TestErpMdSupplierApprovalStateMachine` 层 3 基线，首个 M2 迁移项候选）；独立结束审计待执行（Closure Gates 末项）。

### 新方法论文章：Mission Driver 概念澄清（docs/articles）

- **产出**：新增 `docs/articles/mission-driver-key-clarifications.md`（文件名由 `loop-engineering-mission-driver-clarification.md` 更名）——标题「Mission Driver：Loop Engineering 通用实现的概念澄清——它解决什么问题，为什么这样设计」。面向读过《Mission Driver 通用参考实现》与《Loop Engineering × Attractor》PPT 的读者，定位为概念澄清文。
- **内容**：概念体系定位前置（Mission Driver / Loop Engineering / AGE 三层次）；解决什么问题（Vibe Coding 困境、In→On、与普通工作流引擎的区别）；为什么这样设计（Plan 关闭契约与中间状态合法性、生成与验收分离与 Proof Drift、文件化状态与断点恢复、循环嵌套局部容错、循环即智能——多时间尺度/结构记忆/递归闭环）；能力边界（不定义方向、期望吸引子 vs 实际吸引子、非中性执行器与 Meta-Audit、对 AGE 完整性的压力测试、误解清单）；AI 全自动开发三层次。
- **素材来源**：旧文 `beyond-mission-driver-from-loop-control-to-attractor-guided-engineering.md`（保留循环即智能等核心节）、`age-and-mission-driver-key-clarifications.md`（补充工作流引擎区分/压力测试/实际吸引子/中间状态合法性/Proof Drift 五块）、Mission Driver 源码（`attractor-guided-engineering-template/tools/mission-driver/`）。
- **独立子代理评审**：大纲 4/5、清晰度 4/5、启发性 4/5、事实准确性 3/5。已修正：占位符名错误（`{{missionConfig}}`/`{{roadmapIndex}}` 不存在，换为 `{{planGuide}}`/`{{roadmapPath}}`/`{{plansDir}}`/`{{testCmd}}`）、web server 绝对化（限定执行路径 + 监控看板 `:9300` 旁路说明）、DEEP_AUDIT 为 DRAFT 的 `nothing` 分支而非线性第五步、CLOSURE_AUDIT 属 EXEC_PLANS 子流、审计触发时机"路线图工作项耗尽"、删"全新 session"措辞、幂等句式、2.1 压缩+2.2 补 plans/ 共享队列异步注入机制、4.5 误解表拆回两行、4.3/4.4 补真实案例（07-12 竞争杠杆审计、DEEP_AUDIT 推翻虚假完成）。
- **下一步**：待用户裁决旧文 `beyond-mission-driver-*.md` 与 `age-and-mission-driver-key-clarifications.md` 的去留；README 索引已收录新文。

### M1.1 + M1.2 客服 ErpCsTicket 实体级状态机 Bean 试点（mission entity-state-machine）

- **工作项**：路线图 M1.1（矩阵 Bean + Processor/BizModel 接线 + 矩阵完备性测试 + 层 3 回归）+ M1.2（Delta 同名 Bean 覆盖运行时证明）。计划 `docs/plans/2026-08-12-0738-1-cs-ticket-state-machine-bean-pilot.md`。
- **类型**：implementation-only change（消费已定稿 M0.1 契约 + M0.2 清单，落地单个轴 Bean + 接线 + 测试 + Delta 证明；不改契约/模型/公共 API）。
- **Skill**：`nop-backend-dev`（Bean 形状/注册/接线/错误码映射/Delta 覆盖）+ `nop-testing`（层 1 表驱动测试/层 3 回归/Delta 双加载断言）。
- **产出**：
  - **Phase 1**：新增 `ErpCsTicketStateMachine`（无状态 Bean，6 动作 assertCan + targetStatus + isTerminal + transitions 9 边 + TransitionDefinition 元数据）；在 `app-service.beans.xml` 以 FQN id 注册。common 错误码裁定 = **Option A 复用既有 `ErpCommonErrors.ERR_ILLEGAL_STATUS_TRANSITION`**（`currentStatus`/`expectedStatus`）+ `action` 补充诊断参数。层 1 矩阵完备性测试 `TestErpCsTicketStateMachineMatrix`（8 用例，覆盖 (a) 无重复/冲突边 (b) NEW 可达性+终态无出边 (c) cancel 四非终态合法/两终态非法 (d) transitions 元数据一致 (e) 终态/初始态集合）。
  - **Phase 2**：BizModel `assign`/`start`/`close`/`cancel` + Resolve Processor + Reopen Processor 接线——内联 `Objects.equals(from, ...)` 矩阵守卫改调 `stateMachine.assertCan<Action>(from)` + 目标态写回改 `stateMachine.<action>TargetStatus()`；BizModel/Processor 捕获 Bean common 层非法边映射为领域 `ERR_INVALID_TICKET_STATUS_TRANSITION`（common 码作 cause 保留）；cancel 终态走领域 `ERR_TICKET_ALREADY_TERMINAL`（防冲突）。动态守卫原位不变：close-breached 检查、start 设 startDateTime（= 首次 IN_PROGRESS，保持 drift 裁决行为）、审计 actionType/fromStatus/toStatus、SLA duration/isSlaCompleted、CSAT 触发、reopen data-deletion（删除未答问卷，2026-08-06 批准范围内）。
  - **Phase 3**：Delta 运行时证明——派生类 `ErpCsTicketStateMachineDelta`（覆盖 `assertCanAssign` 放开 RESOLVED 来源态）+ VFS Delta 层 `test-cs-delta`（`_delta/test-cs-delta/erp/cs/beans/app-service.beans.xml` 同名 bean id 覆盖，经 `@NopTestProperty(nop.core.vfs.delta-layer-ids=test-cs-delta)` 激活）。`TestErpCsTicketStateMachineBaselineIoC`（基线加载，3 用例）+ `TestErpCsTicketStateMachineDeltaOverride`（Delta 加载，3 用例）——日志证实容器解析 bean 为 `ErpCsTicketStateMachineDelta`；`assertCanAssign(RESOLVED)` 基线非法/Delta 合法，二者可区分。Delta 仅替换 assign，resolve/start/isTerminal 等非覆盖动作继承基线不变。
- **边界裁决**：Non-Goals 守住——不修改 ORM/API/字典 yaml（status 字段语义/dict 不变）；不改业务状态值/动作名/错误码值/权限/审计/SLA 时序/CSAT 触发/reopen 副作用；不裁定批量迁移模板（归 M1.3）；不建全局 CRUD 写锁（M0.1 §9 裁定选项 c）；不修改保护区架构 doc `entity-state-machine-bean.md`（Phase 1 Decision 选 A，参数名映射作 successor 文档对齐）。
- **验证**：`mvn test -pl module-cs/erp-cs-service` → 116/116 绿（含层 1 矩阵 8 + 层 3 回归 13 + Delta 基线 3 + Delta 覆盖 3 + 既有 89）；`mvn clean install -DskipTests` 全仓库 BUILD SUCCESS（156 模块）；`bash docs/audits/nop-compliance-checker.sh` exit 0（R5=0 @Inject private 零、R11=0 Processor 重复状态判断零，无漂移）。
- **状态流转**：路线图 M1.1 `todo → done`、M1.2 `todo → done`。计划 Plan Status `active → completed`（三 Phase 全 completed，Closure Gates 执行项全绿；结束审计待独立子代理）。
- **下一步**：M1.3 试点评估 + 批量迁移模板裁定（`docs/plans/2026-08-12-0738-2-cs-ticket-state-machine-pilot-evaluation.md`，依赖本计划 M1.1+M1.2 产物）。

### M0.2 全域状态轴清单与 M2/M3/M4 展开（mission entity-state-machine）

- **工作项**：路线图 M0.2（`docs/backlog/entity-state-machine-migration-roadmap.md`）——全域状态轴清单 + 逐条分类裁定 + M2/M3/M4 原子工作项展开。
- **类型**：verification or audit work（全集盘点 + 分类裁定 + 路线图展开，零代码）。
- **产出**：
  - 新增 `docs/analysis/2026-08-12-entity-state-axis-inventory.md`——穷举 19 模块全部候选状态字段（~120 ERP 业务候选 + ~45 posted + ~30 md-ext-ref），逐条按 M0.1 四类分类法裁定。**纳入 103 轴**：M2=19（简单生命周期）+ M3=19（复杂业务/审批）+ M4=65（财务影响/保护域）；每个纳入轴登记八属性（迁移语义 / owner doc / dict / 生产 writer / 既有测试 / 保护区域 / 财务影响 / 跨域副作用）。排除项均记录理由 + 重开触发条件。死状态登记 17 项 + dict 漂移 1 项 + dict 定义缺失 3 项。
  - **writer 盘点**：从实际代码 grep 重新建立范围（~937 行状态写入），不沿用「149 Processor」历史统计。关键发现：全部写入在命名业务动作路径，零 PROD-CRUD writer；~149 个 `protected setDocStatus/setApproveStatus` delegation 方法是天然注入缝。
  - **Ticket SLA drift 裁决**：分类为 `intentional legacy behavior`（`startDateTime = 首次 IN_PROGRESS`，§1 表述被 §实现约定 取代）。owner-doc 补注已落地 `docs/design/customer-service/state-machine.md` §实现约定。解除 M1.1 矩阵固化前置。
  - 路线图 M2/M3/M4 三表展开 103 行（一实体一轴 + 行级 Deps + Owner Doc + Skill）；依赖图更新（节点标注 x19/x19/x65）；M0.2 Status `todo → done`；M1 试点纪律 SLA drift 条标注已完成。
- **边界裁决**：Non-Goals 守住——零代码/零 ORM/零契约变更；master-data 10 个 ACTIVE/INACTIVE 排除 + notify 2 个排除（有理由 + 重开触发条件）；`posted` 一律排除；不固化迁移矩阵字面值。
- **验证**：文档完整性检查（非构建）——(1) 候选穷举覆盖 17 域 + master-data + notify 全集；(2) 每个纳入轴八属性非空可追溯；(3) 每个排除项有理由 + 重开触发条件；(4) M2/M3/M4 每行一实体一轴 + 行级 Deps；(5) `git diff --stat` 证实改动仅在 `docs/analysis/`、`docs/backlog/`、`docs/design/`、`docs/logs/` 下——无生产代码。
- **状态流转**：路线图 M0.2 `todo → done`。M1.1 Ticket 试点 SLA drift 前置解除。M2/M3/M4 共 103 项可按行级依赖排期（均依赖 M1.3 批量模板批准）。
- **下一步**：M1.1 客服 Ticket 试点（SLA drift 已裁决，data-deletion 人工批准已记录）。

### M0.1 实体级 StateMachine Bean 契约定稿（mission entity-state-machine）

- **工作项**：路线图 M0.1（`docs/backlog/entity-state-machine-migration-roadmap.md`）——实体级 `ErpXxxStateMachine` Bean 的技术架构契约 owner doc + CRUD 写入边界裁定 + Bean/Delta 注册与测试策略 + 最小运行时探针。
- **类型**：architecture change（稳定架构契约文档 + 测试作用域探针；非业务实体迁移）。
- **产出**：
  - 新增 `docs/architecture/entity-state-machine-bean.md`（10 节稳定契约：颗粒度/命名、无状态约束、三轴边界、方法形状[含只读 `TransitionDefinition` 元数据接口]、Bean ID 与注册、Delta 同名覆盖、错误/拒绝语义、分工边界表、CRUD 写入边界裁定、测试策略分层[含既有 8 个集成测试基线登记]）。
  - **CRUD 写入边界裁定 = (c) 显式排除**：探索证据（生成 xmeta 中 `status`/`docStatus`/`approveStatus` 均 `insertable/updatable=true`；全仓 `notUpload=0`；通用 CRUD 可写状态字段，无全局写锁）。否决 (a) 禁止（触 ORM/xmeta 保护区域 + 破坏既有创建时写初始态）/ (b) 限制（同保护区域约束）。残留风险：无全局写锁，故迁移项只能宣称「命名动作路径经 Bean 唯一治理」，不得宣称「运行时无任何路径可写状态」；全局写锁 = successor（需独立计划改 ORM/xmeta，ask-first）。
  - **最小运行时探针**（`module-cs/erp-cs-service/src/test/`）：合成 `ErpProbeStateMachine`（不绑定真实实体）+ `ProbeProcessorStub`（按类型注入、非 private `@Inject`）+ `TestErpProbeStateMachineContract`（6 tests）+ `test-probe-statemachine.beans.xml`；在真实 cs IoC 容器证明 (1) Bean 可解析 (2) 按类型注入 (3) 非法边→领域 ErrorCode 映射可观测；不断言 Delta（归 M1.2）。
  - 交叉引用：`docs/architecture/processor-extension-pattern.md` 增「固定迁移矩阵的承载者：实体级 StateMachine Bean」节 + 关系表行；`docs/architecture/customization-capabilities.md` 登记 Delta 覆盖定制点（successor=M1.2）+ 决策矩阵行。
- **边界裁决**：Non-Goals 守住——不迁移真实实体（M1.1 归客服 Ticket）、不证明业务级 Delta（归 M1.2）、不改 ORM/API、`posted` 不作迁移轴、不建全局泛型调度器。
- **验证**：`mvn test -pl module-cs/erp-cs-service -Dtest=TestErpProbeStateMachineContract` → 6/6 绿；`mvn clean install -DskipTests` 全 reactor BUILD SUCCESS；`bash docs/audits/nop-compliance-checker.sh` exit 0（零漂移）；`git diff` 证实零生产代码/生产 beans 变更。
- **独立结束审计**：PASS（`ses_00cf64f9affeZ55oY7HG4edcJG`，6 域全 PASS，0 blocker）。
- **状态流转**：路线图 M0.1 `todo → done`（独立草案审查 2 轮收敛 + 独立结束审计 PASS）。M0.2（全域清单展开）解除硬门控，可起草。
- **下一步**：M0.2 全域状态轴清单与展开（`docs/plans/2026-08-12-0617-2-entity-state-machine-m0-2-inventory.md`），以本契约的方法形状与 CRUD 边界裁定为输入。
