# Development Log - 2026-08-19

### 2026-08-19

- **plan `2026-08-19-0445-2`（RC-R1.73/74/75，P1-RC-064/065/067，UC-MAIN-02/01/05）maintenance 调度触发与访问生成链补全执行完毕（full-green verification：mnt 133/0/0 + 全 reactor 3666/0/0/1 + 全仓 install + TestErpAllJobYamlLoading=29 + checker R2c baseline-raise 其余零漂移）**：**Phase 1（RC-R1.73 运行时长触发）**——`ErpMntEquipmentStatusLog` ORM 纯加性新实体（equipmentId+fromStatus+toStatus+changeAt+source dict `erp-mnt/status-log-source`[VISIT/DOWNTIME/MANUAL，DISPOSAL dangling 不入]+sourceBillCode，索引 equipmentId+changeAt）+ `ErpMntSchedule` 纯加性 3 列（triggerType propId 20 dict `erp-mnt/trigger-type`[TIME/RUNTIME，null=TIME 派生兼容] + thresholdHours 21 + runtimeBaselineHours 22，2026-08-12 A 类批量裁决 + 双独立子 agent 批准在案 plan 批准记录）；载体 = `EquipmentStatusLogWriter`（linker 三迁移 + changeStatus 同事务追加，写点单一真相源）+ `EquipmentRuntimeCalculator`（Σ RUNNING 段查询时聚合，否决累计列物化+采集 Job[漂移需对账]；遗留基线双分支：无日志+当前 RUNNING=createTime 起算 / 非 RUNNING=0 直至首条日志）+ `ScheduleDueGenerator.generateForRuntimeSchedules`（同一 generateDueVisits 入口，触发 = 累计 ≥ baseline+threshold → 生成 DRAFT + 同事务 baseline 重置幂等；nextDueDate 不推进；**无新 job**——due-visit job 承载）；TestErpMntRuntimeTrigger 11 @Test（Σ 数学/遗留双分支/触发+重置/未达阈值/同日幂等/TIME 零回归/非 RUNNING 段/手动+linker 写日志 + `_cases/` 快照）；**Phase 2（RC-R1.74 任务模板套用）**——`ErpMntTaskTemplate` + `ErpMntTaskTemplateLine` 新实体 + `Schedule.templateId` 23 + `VisitTask.standardMinutes` 8；`applyTaskTemplate` protected step（显式 templateId 优先不校验 isActive / 空则 categoryId 唯一 active 回退 / 零·多匹配 LOG.warn 跳过不阻断）→ 逐行复制 ErpMntVisitTask（standardMinutes 透传行级缺失回落模板级，status=PENDING，totalMinutes 不预填）；RESPONSIVE 不套用；备件 materialId/quantity 提示字段不产生消耗单（successor：预填 DRAFT 消耗单）；TestErpMntTaskTemplate 7 @Test（显式套用/回退/零·多匹配/备件零消耗/CRUD 冒烟+快照/无模板零回归）；**Phase 3（RC-R1.75 visit↔request 联动）**——`ErpMntVisit.requestId` propId 27 + to-one request（无索引，Q3 禁既有实体索引改造）；生成侧 `generateResponsiveVisit` 显式回填；完成侧 `ErpMntVisitCompleteProcessor.completeLinkedRequest` protected step（注入实体级 ErpMntRequestStateMachine：IN_PROGRESS→既有 complete 边 / ACCEPTED→startRepair+complete 合成迁移**不加新边**[否决 ACCEPTED→COMPLETED 直边：加边属契约变更] / 终态 no-op warn 不阻断 / 写回失败传播回滚 visit complete[L1 硬语义非 best-effort] / PLANNED requestId null 零影响）；TestErpMntVisitRequestLinkage 7 @Test + TestErpMntDowntimeAndE2E#testResponsiveRequestFullFlow 手工两步改写为断言自动联动（行为变更属 UC-MAIN-05 语义本身）；REQUEST_ID 列新增经快照列子集比对零既有基线漂移（无需重录）；**Phase 4 验证收口**——erp-mnt-service **133/0/0**（108 基线 + 11 + 7 + 7 零回归）+ 全仓 `mvn clean install -DskipTests` 156 模块 BUILD SUCCESS + 全仓 `mvn test` **BUILD SUCCESS 3666 tests / 0 failures / 0 errors / 1 skipped**（唯一 skip = 已知 @Disabled ErpAllWebPagesCollectTest）+ TestErpAllJobYamlLoading=**29 不变**（零新 job.yaml）+ checker **R2c 1460→1468 baseline-raise**（8 per-site 证据 = VisitCompleteProcessor 3[D6 写回] + ScheduleDueGenerator 2[RUNTIME 分支] + StatusLogWriter/Calculator 3，全部 mnt 同域，compliance-baseline.md BASELINE 块 + 注记）其余 18 规则零漂移（R2b=235/R2d=35/R10=12/R12a=70 持平）；owner doc 回填 equipment-integration.md §5.1/5.2/5.3 实现注记 + §3.3 StatusLog 衔接注记 + state-machine.md §4 维护请求联动注记（D6 合成迁移裁决）+ job-scheduling.md §3.13 注记（RUNTIME 评估并入 due-visit job，usage-based-trigger 行收窄为产量 successor）+ arm-index P1-RC-064/065/067 → done (RC-R1.73/74/75) + roadmap 三行 done A 类改写 + 本日志条目；use-cases.md L1 不动；Deferred：产量周期 OUTPUT 触发（数据源落地时）/ 备件自动预填消耗单（预占需求立项时）/ mfg 报工反写运行时长（精度需求立项时，与 RC-R1.78 OEE 协调）。
- **VERIFY 全仓复核：plan `2026-08-19-0445-1`（RC-R1.72）mission VERIFY 全量命令全绿（full-green verification：build + 全 reactor test）**：`mvn clean install -DskipTests` 156 模块 BUILD SUCCESS（1:37）+ 全仓 `mvn test` **BUILD SUCCESS 3626 tests / 0 failures / 0 errors / 1 skipped**（11:46，唯一 skip = 已知 `@Disabled ErpAllWebPagesCollectTest` JDK26/ANTLR H-2；含 app-erp-all 页面校验组）。3626 = 3601 基线 + 25 新增（md 146→155 含 TestErpMdSkuStatusConstraints 5→12 + 聚合 2；pur/sal/inv/mfg checker 各 4），零回归零失败；fork 隔离（9 模块 reuseForks=false）后 DeltaOverride/汇率时钟测试确定性全绿。
- **plan `2026-08-19-0445-1`（RC-R1.72）结束审计反馈再执行 + 测试秩序脆弱性确定性修复（full-green verification：md 155/0/0 ×2 确定性 + mfg 286/0/0 + 全仓 mvn test 156 模块 BUILD SUCCESS 12:31 + 全仓 clean install + checker 零漂移 R2c=1460=已登记 baseline-raise）**：结束审计反馈 4 项（勾选 2 Closure Gates / Plan Status completed / Closure 占位符替换 / plan-check passed）核验均已在案（22 checked / 0 unchecked / Plan Status=completed / 五处回填 live-repo 复核一致）。**再验证发现确定性测试失败**（非 RC-R1.72 语义回归）：①md 全量 `mvn test`（clean 后仍复现）`TestErpMdSupplierApprovalStateMachineDeltaOverride` 2 失败 + `TestErpMdCurrencyRefreshRatesDate` 1 错误；②五域 reactor 中 mfg `TestErpMfgForecastStateMachineDeltaOverride` 同型 2 失败。**根因（二分定位 + 平台源码核读）**：RC-R1.72 新增测试类（TestErpMdSkuReferenceAggregation×2 / TestStubSkuReferenceCheckerSecondary / 四域 checker 测试）改变 surefire 缺省 filesystem 类秩序，暴露两类跨类 JVM 状态残留——(a) **VFS delta 层失效**：`VfsConfigLoader._default`（@GlobalInstance 静态缓存，nop-core `VfsConfigLoader.java:33-40`）+ ConfigStarter 在同 JVM 前置容器类 ≥5 时跳过 VFS 重建（实证：任意 5 容器类前缀组合均失败、4 类均通过、与具体类无关；失败运行全日志零 `_delta` 资源构建记录）→ Delta 覆盖测试拿到基线 bean；(b) **限流器残留**：`ErpMdExchangeRateApiClientFactory.limiters` 令牌桶随 provider 常驻（rate 绑定创建时配置、resetStats 不清桶），`TestErpMdExchangeRateApiClient`（rps=1.0 场景）先跑后 `TestErpMdCurrencyRefreshRatesDate`（冻结时钟 + 零超时 tryAcquire）被残留 nextFreeTicket 时间戳误伤。**修复（镜像 cs RC-R1.67 Phase 3 先例，测试作用域）**：①`resetTestState()` 增 `limiters.clear()`（javadoc 注记测试必须语义；汇率日期测试 r1/r2 间既有 resetTestState 调用使每次 acquire 均为新鲜限流器首取，确定性与秩序无关）；②**10 个含 delta-layer 测试的 service 模块**（grep delta-layer-ids 全量清点：aps/b2b/ct/crm/cs/drp/hr/mfg/md/prj，其中 cs 已有 RC-R1.67 覆写——本轮对其余 9 模块）pom 增 surefire 隔离覆写 `forkCount=4 + reuseForks=false + parallel=classes`（每测试类独立 JVM，保留 4 路并行与 jacoco argLine，配置镜像 cs RC-R1.67）；rejected 替代：reversealphabetical 秩序钉死（实证不可行——重排后未知类承接 stale 态 28 错误 unknown-operation/not-find-bean，地雷平移非消除）。**判定修正**：plan Phase 3 所记 12:42 全仓跑 TestErpMdSkuServices 9×unknown-operation「一次性环境毛刺」与本轮发现同根因族（VFS/组件静态缓存跨类残留），已由 fork 隔离确定性消除，非偶发。**验证**：md 全量 155/0/0 两连跑（确定性）+ mfg 286/0/0 + 全仓 `mvn test` **156 模块 BUILD SUCCESS**（12:31，含 app-erp-all 页面校验组）+ 全仓 `mvn clean install -DskipTests` BUILD SUCCESS（1:47）+ checker 19 规则零漂移（R2c=1460 = compliance-baseline.md 已登记 baseline-raise，其余全持平）。测试作用域变更（1 测试钩子 + 9 pom），产品行为零改动，compliance 零漂移符合预期。
- **plan `2026-08-19-0445-1`（RC-R1.72，P1-RC-062，UC-MD-06 ③④）master-data SKU 独立停用 + 跨域引用检查执行完毕（full-green verification：五域 reactor test + 全仓 install + checker R2c baseline-raise 其余零漂移）**：**Phase 1**——`ErpMdMaterialSku.status` ORM 纯加性列（propId 19，可空无默认 null=ACTIVE 派生零迁移，dict 复用 erp-md/active-status，2026-08-12 A 类批量裁决 + **双独立子 agent 批准在案**：`ses_fe93c4124ffegZ1TP2Aof0ocHa` / `ses_fe93c1427ffezM91qPOPq3KP2g` 双 APPROVE）经 `mvn clean install -DskipTests` 模块链增量重生成；`defaultPrepareUpdate` ACTIVE→INACTIVE 迁移检测（orm_propOldValueByName，非停用编辑/再启用不触发）调 `validateSkuDeactivation` 双守卫；读侧 findSkuByBarcode/findDefaultSku/resolveSku/hasOtherActiveSku 跳过 INACTIVE（与 isMaterialActive 同层）；**级联旁路堵闭**——`ErpMdMaterialBizModel.defaultPrepareDelete` 经 IErpMdMaterialSkuBiz.validateSkuReference 逐子 SKU 仅引用检查；**执行期事实修正**：CrudBizModel.deleteReferences（nop-biz :1217-1265）对 cascade-delete 子对象逐一经子 BizModel `__delete` 级联（非裸 ORM 级联），故守卫 1 增父物料会话态豁免（`orm_state().isGone()`）实现迭代 2「整体删除守卫 1 不适用」裁决；view.xml grid status 列；TestErpMdSkuStatusConstraints 5→12 @Test（①-⑦ Proof 含负控⑦）+ `_cases` 快照录制；**Phase 2**——四域 checker 生产实现（pur：Order/Receive/ReturnLine docStatus≠CANCELLED；sal：+PriceListLine isActive+validTo≥当日或空过期不阻断；inv：在手量/预留 OPEN+PARTIALLY_CONSUMED+qty>0/成本层/批次 OPEN+qty>0/序列 IN_STOCK+RESERVED/四单据行 DRAFT+CONFIRMED/拣选 PENDING+PICKING，StockLedger 历史不阻断；mfg：BOM isActive/工单终态双 ne CLOSED+CANCELLED/领料 DRAFT+CONFIRMED）各域同域 daoFor exists setLimit(1)，关联属性路径 header 过滤（BankLedgerQuery 先例）；md `ErpMdSkuReferenceCheckerRegistry` List 收集器（ioc:collect-beans by-type，镜像 b2b ErpB2bEdiRegistry）任一命中拒绝/空集合放行（**教训：beans.xml 用 ioc: 前缀须根元素 xmlns:ioc="ioc"，md app-service.beans.xml 原未声明首插致整文件解析失败，补声明修复**）；测试：pur/sal/inv/mfg 各 4 @Test 开放/取消/终态/过期对照 + md 聚合 2（双桩 OR 对称 + 空收集器放行）；**Phase 3**——全仓 `mvn clean install -DskipTests` 156 模块 BUILD SUCCESS + 全仓 `mvn test` **BUILD SUCCESS 全绿**（12:42，含 app-erp-all 页面校验组——R1.68/69 教训落实；一次全仓跑中 TestErpMdSkuServices 9×unknown-operation 环境毛刺经 4 种范围复跑不可复现，判定 clean 后 VFS 组件缓存竞态非代码回归，同 JVM 邻居类同 RPC 全绿佐证）+ checker **R2c 1439→1460 baseline-raise**（21 per-site 证据 = 四域 checker 同域 daoFor，compliance-baseline.md BASELINE 块 + 注记）其余 18 规则零漂移（R2b=235/R2d=35/R10=12/R12a=70 持平）；owner doc 回填 sku-multi-unit.md §SKU 状态管理 §实现注记（D3 口径表 + List 收集器 + null 派生语义）+ README.md SKU 独立停用实现位 + arm-index P1-RC-062 → done (RC-R1.72) + roadmap 行 done A 类改写 + 本日志条目；use-cases.md L1 不动。
- **VERIFY 全仓复核：plan `2026-08-18-1849-3`（RC-R1.71）mission VERIFY 全量命令全绿（full-green verification：build + 全 reactor test + checker 零漂移）**：`mvn clean install -DskipTests` 156 模块 BUILD SUCCESS（02:05）+ 全仓 `mvn test` **BUILD SUCCESS 3601 tests / 0 failures / 0 errors / 1 skipped**（唯一 skip = 已知 `@Disabled ErpAllWebPagesCollectTest` JDK26/ANTLR H-2；FLUX_PAGE_ERROR_COUNT=0）+ `bash docs/audits/nop-compliance-checker.sh` 19 规则对齐更新后基线（R2b=235 / R2c=1439 / R2d=35 / **R10=12 baseline-raise 已在 compliance-baseline.md 登记** / R12a=70），module-cs 合计 185/0/0。R1.68/69 教训落实：本次验证范围含 app-erp-all 页面校验组（全 reactor test 覆盖），无 view.xml 损坏类漏检。
- **plan `2026-08-18-1849-3`（RC-R1.71，P1-RC-061，UC-CS-12 ②③④+后置+异常）cs 目录履行引擎执行完毕（full-green verification：分域 185/0/0 + 全仓 install + TestErpAllJobYamlLoading 29 + checker R10 baseline-raise 其余零漂移）**：**Phase 1**——`ErpCsTicketFulfillmentStep` ORM 纯加性新实体（表 erp_cs_ticket_fulfillment_step，status[dict `erp-cs/fulfillment-step-status` PENDING/IN_PROGRESS/DONE/SKIPPED/FAILED]/retryCount/lastError/executedAt/executedBy + **UK(ticketId, fulfillmentId)** 幂等物化，2026-08-12 A 类批量裁决授权）+ dict 声明，增量重生成；`ErpCsCatalogFulfillmentExecuteFulfillmentStepsProcessor` 实化重构：物化（存在即复用 + 模板快照）→ sequence 升序推进，actionConfig 驱动五动作（ASSIGN_TEAM/ASSIGN_AGENT{mode}→R1.65 TicketAssignResolver 真实分配 + NEW-guard 迁移 / REQUEST_APPROVAL{approverRole,timeoutHours}→cs-local 轻量审批[否决 nop-workflow，successor] / CREATE_CHILD_TICKET→IErpCsTicketBiz.save 真实子单 + 双向弱指针 / NOTIFY_CUSTOMER→notify 7207 / UPDATE_STATUS{status}→状态机守卫迁移 + 同态幂等 no-op）+ 失败暂停（FAILED+lastError+中断+7206 通知）+ 末步前 ensureInProgress 终态推进（NEW 自动指派→assign→start；尾部 UPDATE_STATUS(RESOLVED) 组合达成「按配置 RESOLVED」零新配置列）；**关键修复**：步骤行状态写入走 ORM session 脏字段跟踪（CrudBizModel.updateEntity 拒绝同事务未 flush 新建实体 → `nop.err.orm.dao.update-entity-not-managed`，对齐 ErpCsTicketBizModel.doSaveEntity flush 先例）；弱断言 testFulfillmentCreateTicketStepRegistered 改造为步骤执行行强断言（快照经 @EnableSnapshot(saveOutput=true) 单方法重录）；**Phase 2**——重试双入口：手动 `retryFulfillment`（仅 FAILED 步骤 retryCount+1 + 刷新模板 actionConfig；>=max 拒绝 + **REQUIRES_NEW 超限通知**[同事务通知行随拒绝异常回滚丢失 → notifyStepFailedRequireNew 独立事务提交]）+ 新 `ErpCsFulfillmentRetryJob`（R1.37 范式：cron 空=跳过 + autoApproveTimedOut 超时自动审批 + retryForJob 未超限重试逐张隔离，驳回终局 retryCount=max 天然排除）+ `erp-cs-fulfillment-retry.job.yaml`（TestErpAllJobYamlLoading 28→**29**）+ beans 注册 + 种子模板 **7206**（cs.fulfillment-step-failed ROLE 客服主管）/**7207**（cs.fulfillment-notify-customer ROLE 客服员转达占位）三方言 + config 三键 fulfillment-retry-cron/retry-max/approval-timeout-hours + `findFulfillmentProgress` 投影查询 + AMIS 最小接线（ErpCsTicketFulfillmentStep/ref-ticket.page.yaml + 工单行「履行进度」drawer + 「重试履行」按钮）；**测试** `TestErpCsCatalogFulfillmentEngine` 16 @Test 全绿（①物化幂等 ②RR 真实分配[尾随步骤使 NEW→ASSIGNED 可达——单步 ASSIGN 链被末步前铺底先推 IN_PROGRESS] ③审批通过链恢复 ④驳回终局 ⑤客户通知落库 ⑥UPDATE_STATUS 合法/非法/缺配置 ⑦子单弱指针[FilterBeans.like 不自动包 %] ⑧失败暂停+管理员通知 ⑨全 DONE→IN_PROGRESS ⑩尾部 RESOLVED 组合 ⑪手动重试+待审批不重执行 ⑫超限拒绝+通知 ⑬job 自动重试+超时边界+驳回排除 ⑭cron 空 ⑮进度投影 ⑯RPC 冒烟）；erp-cs-service **185/0/0**（169 基线零回归）+ app-erp-all 28 测试全绿 + notify 23 全绿；**Phase 3**——全仓 `mvn clean install -DskipTests` BUILD SUCCESS + checker：R2b=235/R2c=1439/R2d=35/R12a=70 全持平，**R10 11→12 baseline-raise**（per-site 证据：notifyStepFailedRequireNew 拒绝路径通知提交边界，镜像 R1.65 REQUIRES_NEW 先例；compliance-baseline.md BASELINE 块 + 注记更新）；owner doc 回填 service-catalog.md §9.1「产品基线外」→ 已实现注记（actionConfig 契约表 + 编排说明 + INVOKE_WORKFLOW/CLOSE_TICKET L1 未枚举边界声明）+ 配置表补 3 键 + arm-index P1-RC-061 → done (RC-R1.71)[INVOKE_WORKFLOW 残留显式登记防重开] + roadmap 行 done A 类改写；use-cases.md L1 不动。
- **plan `2026-08-18-1849-2`（RC-R1.70，P1-RC-059，UC-CS-08 ①②+后置+异常）cs 调查延迟派发与重试执行完毕（full-green verification：分域 169/0/0 + 全仓 install + checker 零漂移）**：**Phase 1**——`ErpCsSurvey` ORM 纯加性 2 列 `status`（propId 18，dict `erp-cs/survey-status` PENDING/SENT/COMPLETED/FAILED，2026-08-12 A 类批量裁决授权）+ `failureCount`（propId 19）+ dict 声明，`mvn clean install -DskipTests` 增量重生成（DDL 三方言 NULL 无默认核对 + survey-status.dict.yaml + SURVEY_STATUS_* DaoConstants 自动产物）；写路径接线 createSurvey 按 delay 写 PENDING/SENT + submitSurvey 写 COMPLETED，遗留行 null 派生兼容（findSurveyReminders/findExpiredSurveys 零改动）；**Phase 2**——新 `ErpCsSurveySendJob`（R1.37 简单 job 范式：cron 空=跳过 + batch-limit 200 + 逐条失败隔离 + runInSession MANAGED 原位修正；扫描 = surveySentAt 空 + respondedAt 空 + createTime+delay 到期统一队列——isNull 算子被 ObjMetaBasedFilterValidator 拒绝[xmeta 默认仅 eq/in/dateBetween/dateTimeBetween]，改 dao 直查[CsatReminderJob job 只读先例同型，R2c +1 消化基线余量] + 终态 FAILED Java 侧守卫）+ notify 派发 `cs.survey-invitation`（种子模板 **7205** ROLE 客服员转达 + IN_APP 占位，三方言）成功转 SENT、异常 FAILED+failureCount++ 重试至上限 3；config 三键 survey-send-cron/send-retry-max/send-batch-limit；`erp-cs-survey-send.job.yaml`（TestErpAllJobYamlLoading 27→28）+ beans 注册 erpCsSurveySendJob；**测试** `TestErpCsSurveySendJob` 10 @Test 全绿（①cron 空 ②到期 SENT+notify 落库 ③delay 窗口跳过 ④遗留 null 派生 ⑤FAILED+count=1[子类覆写 dispatchSurvey 注入异常] ⑥重试 FAILED→SENT ⑦超限终态 ⑧submitSurvey COMPLETED ⑨reopen 删 PENDING/FAILED 未响应[COMPLETED 保留对比 + FAILED 对称断言] ⑩execute 无参 public；断言式 + 空 autotest.yaml 脚手架镜像 R1.65/67/68 先例——冻结时钟与 ORM createTime 真实 millis 自动填充冲突[threshold 冻结 2026-07-17 vs createTime 真实 2026-08-19 恒未到期]，真实时钟下 delay 语义自然成立）；erp-cs-service **169/0/0**（159 基线零回归）；**Phase 3**——全仓 `mvn clean install -DskipTests` BUILD SUCCESS + checker 19 规则 actual≤baseline 零漂移（R2b=235 / R2c=1439=基线[job dao 直查 +1 消化 1438→1439 余量，per-site 注记在 job javadoc] / R2d=35 / R10=11 / R12a=70）；owner doc 回填 csat.md §实现约定 Non-Goal 三条改已实现注记 + 配置表补 3 键 + README.md 回访问卷条目改写 + arm-index P1-RC-059 → done (RC-R1.70) + roadmap 行 done A 类改写；use-cases.md L1 不动。
- **VERIFY 修复收口：plan `2026-08-18-1849-1`（RC-R1.68+69）ErpCsTicket.view.xml 重复追加损坏 → 重建后全仓全绿（full-green verification：build + 全 reactor test + checker 零漂移）**：mission requirement-compliance VERIFY 首跑全仓 `mvn test`，app-erp-all 4 页面校验测试失败（`TestAuthSeedLoadingProof`/`TestErpSeedDataIntegrity`/`ErpAllWebPagesTest`/`ErpAllFluxPagesTest`，根错误 `string-not-end-properly` @ ErpCsTicket.view.xml:153 + FLUX_PAGE_ERROR_COUNT=9）；根因 = view.xml **4196 行 vs HEAD 335 行**（forms/pages 重复追加 ~22 份残缺拷贝 + 首拷贝 add 表单 kbSuggestion 两属性截断），结束审计验证范围未含页面校验组故漏检（审计盲区 + 行号证据失真分析见 `docs/bugs/2026-08-19-cs-ticket-view-xml-append-corruption.md`）；修复 = 截断至首拷贝 + 从残缺拷贝回收预期片段重建（⑨ 空态三元文案 / ⑦ adopt 按钮 ajax→dialog[adoptKnowledge] add+edit 双表单 + minRows 引号规范化），最终 diff vs HEAD = 预期最小变更集 96+/26−。修复后验证：4 失败测试 7/0/0（FLUX_PAGE_ERROR_COUNT=0）+ 全仓 `mvn clean install -DskipTests`（156 模块 BUILD SUCCESS）+ 全仓 `mvn test` **BUILD SUCCESS 全绿** + `bash docs/audits/nop-compliance-checker.sh` 19 规则零漂移（R2b=235 / R2c=1438≤1439 / R2d=35 / R10=11 / R12a=70 / R12b=66 / R12c=40）。**流程教训**：view.xml/page.yaml 变更的验证命令必须含 app-erp-all 页面校验组；审计行号证据须与基线行数量级核对。
- plan `2026-08-18-1849-1` Closure 节补记 Post-Closure Correction（损坏 + 重建 + 真实全绿证据）。
