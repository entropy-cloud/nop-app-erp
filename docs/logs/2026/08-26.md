# Development Log - 2026-08-26

### F2.1 修复落地：finance-过账 P1 余项（plan 2026-08-26-0630-1，fin-001 posted 回写通道 / fin-002 平衡校验 / fin-004 ACTIVE 过滤）

- **fin-002**：postVoucher 过账边借贷平衡断言（DRAFT→POSTED 迁移守卫，state-machine.md L40 依据）+ 头合计重算写回——不平衡拒保持 DRAFT、平衡/红字负数风格过、stale 头合计修正（顺带修复 PeriodLock 旧种子头/行不一致 60→30）。
- **fin-004**：AcctSchemaResolver/SchemaPropagator 两处 `eq("status", STATUS_ACTIVE)` + 常量提取 + statusScore 死代码化简——仅 INACTIVE → null → F1.4 fail-closed 闭环。
- **fin-001 一期**：`VoucherPostedEvent`/`IErpFinVoucherPostedListener`/`ErpFinPostedListenerRegistry`（镜像反向对偶，collect-beans 装配）+ **两重试通道派发**（sweep doRetry + 手动 retry；引擎路径不派发——审查 R1 修正论证：反写契约域自治 + 硬规则 6 原子性，原「乐观锁冲突」论被平台共享 session 语义证伪）+ FinPostedListener（EXPENSE_CLAIM/EMPLOYEE_ADVANCE/NOTES_*）+ pur/sal/inv/mfg 四域 ReversalListener 扩展 dual 接口（inv 含 -PPV strip、mfg 含 -MI/-PV strip）。listener 失败经 recorder 落工作台 eventData 透传自愈。
- **悬挂闭环集成测试**（`TestPostedListenerSuspensionRecovery`）：seed posted=false 报销单 + POSTED 凭证 + PENDING 异常 → sweep retry → **RETRIED + posted=true**（修复前 posted 永久 false）。
- **快照工程**（F1.4 fail-closed 的全域连锁收尾）：inv 7 类 38 case + pur 5 类 + mfg 8 类的 period-not-found 失败路径测试全部补种 ACTIVE 账套 input 行（保留「无期间失败」原意图），非确定性单元格（seq/updateTime/PAYLOAD_JSON）按 hr/mfg 先例 `*` 通配；pur STRICT 预算拒绝用例的库存残留期望清除（F1.2 编排修复后拒绝即干净回滚——旧行为是 bug 快照）。
- **验证**：fin **504/504** + 六域 **3748/3748** 全绿（RoleRowFilter 隔离一次偶发 JVM 顺序竞态复跑绿）；全 reactor install SUCCESS；compliance R2c=1506 零新增。
- **owner doc**：posting.md §反写契约 新增「悬挂补写：两重试通道派发 VoucherPostedEvent」节（F2.1 立法）。
- **索引回填**：fin-001/002/004 → **fixed**。open 余 497（fixed 35——含并行 mission 期间 sal-018/019 证伪裁决面）。Deferred：二期 5 域 listener（assets/hr/prj/mnt/qa，各归 F2.x 域簇）+ fin 内部 4 closer 服务同病控制点 + findFirstByOrg INACTIVE 泄漏（均落计划 Deferred 节）。
- **独立结束审计（agent_16349aed）PASS on substance**：6 实质面全过（测试独立重跑 3/3+4/4/抽验 inv 3/3、commit 佐证 reactor 3889 全绿、compliance 联合裁决链闭合、R1 乐观锁伪论修正落地、anti-hollow 核验）；3 簿记项（index 计数口径 495→497 修正+注记、Deferred 补登 closer 服务、收口提交）当日完成。Plan Status: completed，F2.1 closed，F2.2 置 ready。


### F2.1 修复落地：finance-过账 P1 余项（plan 2026-08-26-0630-1，fin-001 悬挂补写通道 / fin-002 平衡守卫 / fin-004 ACTIVE 过滤；收口 2026-08-27）

- **fin-001 基础设施**：`VoucherPostedEvent` + `IErpFinVoucherPostedListener` + `ErpFinPostedListenerRegistry`（镜像反向对偶：IoC collect-beans by-type 聚合、per-listener try/catch 失败隔离返 ListenerFailure、空监听启动 warn）+ `ERR_POSTED_LISTENER_FAILED` / `FAILED_STAGE_NOTIFY_POSTED_LISTENER`。
- **两通道派发裁决**：仅 sweep `ErpFinDeferredPostingRetryHelper#doRetry` 与手动 `ErpFinPostingExceptionRetryProcessor#retry` 两「调用方不在场」通道在 `post()` 返回非 null（含 F1.1 幂等命中返回既有 id）后派发；REVERSAL 分支不派发；引擎 `process()` 零引用（grep 证实）。失败经 recorder 落工作台且 eventData 透传（下轮 sweep 幂等命中再派发 → 自愈闭环）；原异常记录仍 markRetried（凭证法律效力不回滚）。
- **5 核心域 listener**：pur/sal/inv/mfg 扩展现有 ReversalListener 类 dual 接口 + fin 新建 `FinPostedListener`（EXPENSE_CLAIM/EMPLOYEE_ADVANCE/NOTES_*；EA-CASH-REPAY 合成码 miss no-op）。回写 posted 三字段、已 true 跳过（防 version 无谓递增）、findByCode miss no-op；inv 增 stock-move 分支与 `-PPV` strip；mfg `-SF`/`-MI`/`-PV` strip 对齐勘察域清单（PRODUCTION_VARIANCE→工单名下 CostVariance 行，该实体无 postedAt/postedBy 列——注记）。
- **决策落档**：手动 retry `post()==null` 分支不派发（null 无凭证存在性证据，与 doRetry 同语义，代码注释 + posting.md 双落档）；二期 5 域 listener（assets/hr/projects/maintenance/quality）Deferred 归 F2.5/F2.8/F2.9/F2.12/F2.13；sal-003 finance 侧 fixed（sales 遗留面归 F2.10 联动收口）；`ErpMdAcctSchemaBizModel#findFirstByOrg` 同型 INACTIVE 面 Deferred（md 域簇遇控制点收口）。
- **owner doc**：`posting.md §反写契约` 新增 §悬挂补写（VoucherPostedEvent 契约表 + 派发通道裁决含引擎不派发三点依据）+ 反写方向汇总「正常过账成功」行补两通道派发语义（审查必改 R2）。
- **测试**：registry 镜像单测 4 例 + 悬挂闭环集成 3 例（retry→RETRIED+posted=true / 失败隔离落工作台 eventData 透传 / REVERSAL 重试零派发守卫）+ 5 域 listener 单测（fin4/pur3/sal3/inv4/mfg4）。回归中修复两处与本计划无关的快照漂移：`TestErpPurSupplierPriceResolver` 三例 VALID_FROM/VALID_TO 日期滚动漂移（`*` 通配，34ab1dcb2 先例）；`TestErpMfgWorkOrderEndToEnd` record_work updateTime 跨实体毫秒竞态（`*` 通配）。
- **验证**：六模块 `mvn test` 全绿——fin 514 / pur 340 / sal 312 / inv 242 / mfg 299 / md 160；全 reactor `mvn clean install -DskipTests` BUILD SUCCESS；compliance checker exit 0 零真实漂移。
- **索引/路线图**：fin-001/002/004 → fixed（ai-check-index L149/150/152）；roadmap F2.1 → done（F2.2 next，open 余 497）。
- **独立结束审计（agent_fc038f56）首轮 PASS**：四组独立复核（infra 镜像/两通道契约/引擎零引用/beans 注册/双接口监听守卫/测试断言实义/文档回填/反模式零命中）+ 定向复跑 fin 15/15、inv 4/4 绿。Plan Status: completed。


### F1.4 修复落地：过账引擎账套解析空 fail-closed（plan 2026-08-26-0530-1，第一批 P0+横切高危簇全部闭环）

- **守卫**：`ErpFinPostingProcessor#process` try 块首语句（幂等短路后）——`acctSchemaId=null` 抛新错误码 `erp.err.fin.posting.no-active-schema`（R1 裁决：try 内抛经 catch→recordPostFailure→PENDING→sweep 重试链 + G2 告警，达成 fin-005 可观测性目标；try 前抛会绕过记录链）。
- **R2 两条传播链显式裁决**：qa NCR dispatchScrap（无内部 catch）与 prj 保留金返还（catch 包装重抛）——mutation 将失败（原为静默零凭证假成功留断账），接受为 fail-closed 改进；全域其余吞异常 dispatcher 行为不变（+warn +异常工作台记录）。
- **测试**：testNullSchemaFailsClosed（错误码/参数/零回链断言）+ fin **498/498 全绿**；两既有审批测试（EmployeeAdvance/ExpenseClaim Approval）快照重录——无种子环境下首个失败成因从 period-not-found 变为 no-active-schema（更准确的根因，异常工作台链不变）。
- **Deferred**：fin 内部 4 closer 服务（AnnualClose/PL/FX/BadDebtProvision 自循环 resolveTargetSchemas 同病控制点，不进 process()）+ resolver null 语义统一 + 2 VoucherBuilder 硬编码 "1"——watch-only，期末门控已覆盖悬挂面。
- **验证补全**：全 reactor `mvn clean install -DskipTests` BUILD SUCCESS（结束审计独立复跑佐证）；compliance R2c=1506 零漂移（+1 归属 F1.2 已登记，F1.4 守卫零新增 daoFor）。
- **里程碑**：**第一批（F1.1-F1.4）全部闭环**（F1.4 结束审计 agent_a77244d0 首轮 PASS）——P0-CK-mfg-001 + fin-003 幂等链 + REQUIRES_NEW 孤儿凭证族 + CRUD 守卫族 + fin-005 fail-closed，共 31 findings 终态（30 fixed + 1 部分注记），四计划均经独立草案审查与结束审计。**open 余 501**（F2.x P1 域簇 → F3.x P2/P3 簇 → V/G）。


### F1.3 修复落地：CRUD 无状态守卫族统一基类（plan 2026-08-26-0430-1，一次收口 19 条族 findings，19 域 7249/7249 全绿）

- **基类**（common-service）：`AbstractErpCrudBizModel`（posted/approveStatus=APPROVED 拒通用 __update/__delete，判定抽 `ErpCrudStatusLock` 纯函数 + config kill-switch）+ `AbstractErpImmutableCrudBizModel`（台账全拒）+ 两错误码。平台挂点与内部 helper 旁路由草案审查代理实证（CrudBizModel L981/L1179/L1988-2048）。
- **接入**：363 个实体 BizModel 机械替换 extends（守卫对无列实体惰性）+ inv ledger/balance 接 Immutable；M2 super 链完整性 grep 证明（全域 31+7 处钩子覆盖零缺失）。
- **测试**：pur 五分支（TestErpPurCrudStatusLock：DRAFT 放行/APPROVED 拒/posted update+delete 拒/kill-switch 放行）+ inv 不可变（TestErpInvLedgerImmutable）+ common 纯单测。
- **回归中发现并修正三处此前被掩盖的问题**：①mnt TestErpMntLaborPosting 2 例快照 ID 漂移（F1.2 工时引入——此前 ast/mnt 联跑经管道 grep 吞掉 mvn 退出码误判绿，**教训：后台验证必须读 exit code 而非管道尾行**）；②mfg 3 类（CompletionPosting/CostFlowEndToEnd/BatchGenealogy）VERSION 漂移（F1.2 报工前移消除冗余二次写，四域联跑同被掩盖）；③hr 故障注入裸构造 dispatcher 撞 F1.2 守卫 NPE（alreadyPosted 补 null 安全）。
- **验证**：19 域 **7249/7249 failures=0 errors=0**；全 reactor install SUCCESS；compliance 与基线零真实漂移（R7 本地 1 = `_tmp/` git-ignore 草稿误报，裁决不改基线——`module-*/src/main` 零命中 + CI 干净检出不命中）。
- **索引回填**：P1-CK-pur-003/sal-004/inv-003/inv-005 → fixed（主控制点）+ 14 条同型 → fixed（基类接入自动生效）。**累计 30 findings 终态（29 fixed + 1 部分注记），open 余 502**（F1.3 净收 18 条）。owner doc processor-extension-pattern.md 增硬规则 5'（CRUD 状态锁）。
- **独立结束审计（agent_71e4cedb）首轮 PASS**：6 门控全过（super 链 36 处独立全量验证/R7 误报裁决正确/索引 18 行吻合）；4 minor（计数口径/冗余 import/mfg-006·notify-006 裁决注记）当日修正。Plan Status: completed，F1.3 closed，F1.4 置 ready。


### F1.2 修复落地：REQUIRES_NEW 孤儿凭证族（plan 2026-08-26-0330-1，勘察→计划→独立草案审查修订→三 Phase 实施）

- **勘察**（agent_95b25d9f）：11 编排单元逐站点读码——6 站可前置修复、1 站结构拆分、1 站前移变体、8 子站已被 F1.1 引擎三态化收敛、2 子站证伪。
- **Phase 1（纯前置三站）**：pur Invoice/Payment SoD 守卫前移 SoD-first（对齐 Receive 范式）+ Invoice 承付 hook 前移（R3，release SYNC 同事务）；hr2 `tryPostPayment` 补 alreadyPosted 去重守卫（镜像计提链）。新测试：pur 双站 SoD 拒绝**零凭证**断言（countBillLinks==0）+ hr2 守卫短路（bill-link 计数不变）。
- **Phase 2（联动前置五处）**：mfg 报工链（WO 持久化+预留释放前移）+ 领料链（预留消耗+actualQuantity 回写前移；残余归 Deferred——InvPostingDispatcher 幂等键 move.code 重试重建不稳定，勘察+审查 R2 双确认）；mnt 设备恢复+request 联动前移 + 幂等 warn 降 info；prj 工时聚合前移（STRICT 预算拒绝提前=行为改善）；ast VA applyAssetValueChange 无条件前移 + **R1 配对**（executeReverseApprove 无条件回滚 NBV——审查抓住的前移反向不对称陷阱）。
- **Phase 3（Disposal 拆分）**：catchUp 拆数据段（计划行+资产字段前置供 gainLoss）/凭证段（`postCatchUpVoucherOnly`）+ 计划行回填 `backfillCatchUpSchedules` 推迟至两凭证全提交后。**实施中发现重要 ORM 交互**：主事务脏计划行在内层 REQUIRES_NEW flush 上的跨连接 update-entity-not-found（内层连接看不见主事务未提交 INSERT）——首轮测试即暴露（disposal posted=false），调试定位后以回填后置修复。TestErpAstDisposalEquipmentLinkage 扩展**双零回链断言**（联动失败 → DISPOSAL 与 #CATCHUP 双凭证零提交）。
- **(c) 收敛分析**：8 站 billHeadCode 稳定性逐一核对（全部单据编码稳定；唯一不稳定 = mfg move.code 已 Deferred）→ F1.1 引擎幂等返回既有 id → 重试收敛；证伪 2 子站书面裁决。
- **快照重录/人工回正**：pur CommitmentRestore 33 文件（COMMITMENT 凭证先于 AP_INVOICE 创建=hook 前移预期 + ID 位移）；mfg WorkOrderEndToEnd（version 8→7=消除冗余二次写 + **AVG_COST 5.600020→5.6000——根因实证：avg_cost 列 DECIMAL(20,4)，DB 真值恒 scale4，旧录制为实体内存 scale6 未落库伪值**，前移改变 flush 时序使真值显形）；prj 三类 42 文件（凭证 ID 11→13 位移）。
- **验证（全绿）**：pur 336 / hr / mfg 293 / mnt / ast / prj 173 全绿；全 reactor `mvn clean install -DskipTests` BUILD SUCCESS；compliance R2c 1505→1506 **baseline-raise 登记**（backfillCatchUpSchedules +1 站点，per-site 证据落 compliance-baseline.md）其余 15 规则零漂移。
- **文档立法**：processor-extension-pattern.md 硬规则新增第 5 条「REQUIRES_NEW 凭证后无可抛步骤」编排不变量（含幂等键稳定性要求 + 跨连接 flush 交互注记）。
- **索引回填**：pur-002/hr2-006/mnt-007/prj-006/ast-015/ast2-021/qa-012/inv-012 → **fixed**（8 条）；mfg-011 保持 open（报工链已修+领料链 residual 归 F2.5 successor）。**open 余 520**。
- **独立结束审计（agent_34b8fb70）复裁 PASS**：首轮 FAIL 3 必改（ast2-021 回填遗漏——前缀笔误 P2/P3、Phase 2 两断言虚勾、Exit Criteria 漏勾）→ 修复（正确回填 + 新增 VA posted=false 鉴别用例 7/7 + mfg 消耗注入实测受阻于容器 duplicate-bean-definition 按规则 10 改判移出并记录证据与 successor + Exit 勾选）→ 复裁 PASS（审计者独立重跑 VA 测试绿、9 处修复真实/R1 配对/快照良性全部维持确认）。Plan Status: completed，F1.2 closed，F1.3 置 ready。


### F1.1 修复落地：P0-CK-mfg-001 + P1-CK-fin-003（plan 2026-08-26-0120-1，独立草案审查修订后实施，三 Phase 全绿）

- **Phase 1（fin-003）**：`ErpFinPostingProcessor#process` 幂等命中分支由返回 null 改为返回既有 POSTED 凭证 id（新增 `findPostedVoucher` 账套过滤 4 参重载，3 参委托保持兼容）；契约 javadoc 三处同步（引擎/IErpFinVoucherBiz/RetryHelper O-16 注释）；R1 两处既有 assertNull 断言强化为 assertEquals(首单 id) + 快照 `1_idempotent_responses.json5` 重录 [9,9]。审查者实证三处返回值消费者（doRetry/retry/ast NBV 增量联动）全部兼容。
- **Phase 2（mfg-001）**：completion move 关联单号采用**首单精确/后续后缀**方案——`completionMoveBillCode`：首张（findByRelatedBill 未命中）保持 wo.code 精确值（E2E `_helper.ts`/既有单测零破坏），后续报工 `wo.code + "-C" + 累计完工量规范化串`（严格单调保唯一）；VARCHAR(50) 超长显式抛新错误码 `ERR_COMPLETION_MOVE_BILL_CODE_TOO_LONG`（R3 裁决：显式失败优于静默截断）。新错误码 + ARG_COMPLETED_QUANTITY 落 ErpMfgErrors。
- **Phase 3（传导回归）**：`TestErpPrjTimesheetMulticurrencyPosting#testIdempotentRepostKeepsPostedTrue`——approve 后 dispatcher 二次 tryPost 断言 true 且凭证仍 1 张。**先红后绿实证**：旧 fin 构件下失败（null→false）、install 修复构件后通过——直接证明传导站点悬挂被引擎层修复消除。
- **验证（全绿）**：`mvn test -pl module-finance/erp-fin-service` **497/497**；`-pl module-manufacturing/erp-mfg-service` **293/293**（279 基线 + 新增量用例）；`-pl module-projects/erp-prj-service` 定向 4/4；全 reactor `mvn clean install -DskipTests` **BUILD SUCCESS**（156 模块）；compliance checker 与 C0.2 快照**零漂移**（R2c=1505/R1d=14/R2b=237/R3=5/R6=2/R10=12）。
- **索引回填**：P0-CK-mfg-001/P1-CK-fin-003/P2-CK-qa-013/P1-CK-log-001 → **fixed**（附测试证据）；P1-CK-prj-006 幂等部分已修、REQUIRES_NEW 部分保持 open 归 F1.2。open 余 529。
- **独立结束审计（agent_209b4974）复裁 PASS**：首轮 FAIL 3 必改（qa-013 编号笔误/RetryHelper javadoc 未生效/守卫零测试）→ 当日修复（守卫测试 TestCompletionMoveBillCodeGuard 2/2 绿 + 三处 javadoc/编号更正）→ 复裁三项独立复验全过。Plan Status: completed，F1.1 closed，F1.2 置 ready。


### 2026-08-26

- 执行 E2 对照确认计划（plan `docs/plans/2026-08-26-0735-1-e2-survey-cross-confirmation.md`，零代码里程碑），三份对照产物落盘 + roadmap 收口：
  - E2.1：`docs/design/finance/posting.md` 新增「记账内核审计性对照（Beancount，E2.1）」章节——以 beancount 报告 §2/§3 为清单权威源逐项核对（9 项：7 已覆盖 / 1 不适用 / 1 部分覆盖），代码锚点均经实时仓库 grep/读文件复核（`ErpFinPostingProcessor` balanceTotals:758 / assertBalanced:772 / buildReversalDraft:780 同向取负 / O-8 markOriginalVoucherReversed:963、`ErpFinPostingExceptionRecorder:43`、属性测试 `PropertyErpFinDebitCreditBalance`/`PropertyErpFinMultiCurrencyBalance` 等）。
  - 关键决策（E2.1 唯一差距登记）：零金额分录无引擎级显式拒绝（Beancount「金额非零」约束部分覆盖）——ERP 语境下免税单据 TAX_AMOUNT=0 模板行为合法来源，触发条件驱动（GL 噪声问题时经 `IErpFinFactsValidator` 补校验），已登记 roadmap §4.1 新行，不立项。
  - E2.2：新建 `docs/analysis/erp-survey/2026-08-26-0000-lowcode-boundary-and-toolchain-notes.md`（落点裁决：独立成文 vs survey-index 增段，理由见其 §0）——Frappe/Baserow vs Nop 模型驱动路径差异（引用 frappe-framework.md §2.1/§2.3、baserow.md §2）+ 工具链对照（frappe bench vs build.sh/nop-cli，以 `docs/context/project-context.md` 验证命令为权威）。
  - E2.3：`docs/analysis/plugin-hot-management-research.md` 新增 §11 三方对照复核（InvenTree plugin registry / Fleetbase extensions vs NocoBase 路径 3 vs Decision R1）——结论：佐证无分歧（两形态均落 D-Select/D-Switch 维度，无 D-Load；与 Delta+SPI 正交互补；签名分发对齐 roadmap §4.1 erpclaw 行）。
- roadmap 收口：§2 计数 todo 12→9 / done 7→10；§5 E2.1/E2.2/E2.3 → done；§6 交付物补 ✅；§4.1 新增「金额非零校验」行 + 更新「工具链对照」行为已产出。
- 验证状态：零代码计划（无生产代码/ORM/页面/测试/config 变更），按 plan Closure Gates 以文档事实核查门控替代 build/test 门控——对照段引用的全部代码锚点（类/文件/行为）经实时仓库复核存在且语义相符；未跑 mvn（机器验证不适用，plan 明示）。
- 下一步：E2 计划独立结束审计（closure gates 中「结束审计由独立子代理执行」项留给 CLOSURE_VERIFY 步骤）；同批计划 2（E3 整体实现，`2026-08-26-0735-2`）为后续主线。

- 批量验证落盘（mission erp-enhancement VERIFY 步骤，覆盖未提交工作集：plan `2026-08-25-1956-2` 逗号修正代码 + `2026-08-26-0735-1` E2 文档 + ai-check C6.4/C7.1-C7.3 报告）：`mvn clean install -DskipTests` 156 模块 BUILD SUCCESS；全 reactor `mvn test` **3847 tests / 0 failures / 0 errors / 1 skipped**（646 报告文件，与 known-good 基线一致）；compliance checker 全规则 actual==baseline 零漂移（R7=1 为预存合理偏离，与 1956-2 Phase 1 记录一致）。e2e（e1-2 `(e)` 正向可见性 spec）本次未运行，flux 运行绿证据归 plan 1956-2 Phase 2 后续执行（其两项 Proof checkbox 尚未 tick）。
