# Development Log - 2026-08-16

### quality 业务作废联动取消质检落地（RC-R1.59，plan `2026-08-16-1634-3`，P1-RC-041 UC-QA-08，纯代码逻辑 B 类预授权 + product-scope 硬性分级确认）

- **工作项**：执行 `docs/plans/2026-08-16-1634-3-rc-mr1-r1-59-qa-business-cancel-linkage.md` 三 Phase。落地 finding **P1-RC-041**（UC-QA-08 业务单据作废联动取消 L1 验收标准经 R1.20 deferral 未满足——`IErpQaInspectionBiz` 无 `cancelForBusinessBill` + 三域 cancel Processor 零 quality 联动；A1.31 §5/§6 登记 + A4.2.103 运行时维持 P1 + 2026-08-12 B 类裁决降级预授权，不再须 ask-first checkbox）。
- **Phase 1 裁决（D1-D3 + product-scope 分级确认闭环）**：**product-scope 确认**——product-scope.md quality 域未裁剪「业务作废联动取消质检」→ L1 UC-QA-08 显式硬性要求（use-cases.md:133-145）→ Q4=(a) P1 强制实现无例外（对齐 RC-R1.56 先例）+ **D1 取消载体=方案 A**（useLogicalDelete 软删 PENDING——ErpQaInspection useLogicalDelete 已启用 orm.xml:169，平台逻辑删除 delVersion 置位非物理删除，规范解法三权威源一致[arm-index P1-MA2-064 方案 A + R1.20 plan:64 + state-machine.md:190]→ 不触发 data deletion 保护区域双 agent 批准[本计划解释性声明]；方案 B[result dict 加 CANCELLED + 状态机迁移]不选——dict 追加超 RC-R1.59 B 类枚举[2026-08-08 不可类推规则]须双 agent 批准 + L1「取消(CANCELLED)」语义经软删已满足）+ **D2 config 门控**（`erp-qua.business-cancel-linkage-enabled` **默认 true**——仅取消 PENDING 且关联已作废单据的质检单，零活跃数据危害，对齐 R1.20 batch-expiry-check-enabled TRUE 先例）+ **D3 接线点**（三域 cancel 成功后置调用；**billType 键值关键细节**——pur 创建路径经 `ErpPurConstants.RELATED_BILL_TYPE_PUR_RECEIVE`="ERP_PUR_RECEIVE"（非 qa 镜像常量 ERP_PUR_RECEIPT），接线必须用本域创建路径同源常量保证 relatedBillType 匹配）。
- **Phase 2 实施**：`IErpQaInspectionBiz.cancelForBusinessBill(billType, billCode, context)` @BizMutation 契约（返回取消数 int）+ per-mutation `ErpQaInspectionCancelForBusinessBillProcessor`（extends AbstractErpQaInspectionProcessor，protected step：isLinkageEnabled config 门控 → findPendingByRelatedBill[关联键+result=PENDING 查询，平台 delVersion=0 过滤自动生效] → softDeletePending[`inspectionDao().deleteEntity`——useLogicalDelete 实体 dao.deleteEntity 自动转 UPDATE delVersion=currentTimeMillis，初稿误用 removeEntity 编译失败修正]；幂等 + 无匹配零副作用）+ `ErpQaInspectionBizModel` 委托 + beans.xml 注册 + `ErpQaConstants.CONFIG_BUSINESS_CANCEL_LINKAGE_ENABLED` + `ErpQaConfigs.isBusinessCancelLinkageEnabled()`（默认 true）；三域接线——`ErpPurReceiveCancelProcessor`/`ErpSalDeliveryCancelProcessor` 增 @Inject IErpQaInspectionBiz（模块依赖链 pur/sal→qa-dao 已存在）+ `ErpMfgWorkOrderProcessor` 复用既有注入，cancel 主流程成功后置调 protected step `cancelLinkedInspections`（try/catch Exception → LOG.warn 降级不阻断作废主流程，联动为辅助语义）。
- **Phase 3 测试 + 文档 + 回填**：`TestErpQaBusinessCancelLinkage` **5 组全绿**（①PENDING 取消[findByRelatedBill 查无 + 软删行 orm_logicalDeleted=true 审计可追溯]②ACCEPTED/REJECTED 不动[历史完整]③无匹配零副作用④幂等重复取消⑤config 关闭跳过）+ `_cases` 快照录制（含 delVersion 列）；跨域接线测试落接线所在模块——`TestErpPurReceiveCancelInspectionLinkage`/`TestErpSalDeliveryCancelInspectionLinkage`/`TestErpMfgWorkOrderCancelInspectionLinkage` 各 **3 组全绿**（cancel 后关联 PENDING 消失[ErpQaInspection__findByRelatedBill 跨域查无] + 终态不动 + config 关闭跳过 + 无关联零副作用，对齐 R1.48 先例 + qa-service pom 禁止反向依赖约束[真实 wiring 测试不落 qa-service]）+ `_cases` 快照；erp-qa-service **182** / erp-pur-service **320** / erp-sal-service **299** / erp-mfg-service **282** tests 全绿零回归（172/317/296/279 基线 + 新增）+ 全量 `mvn test` BUILD SUCCESS 0 failures 0 errors（1 skipped 既有）+ `mvn clean install -DskipTests` 全量 BUILD SUCCESS + compliance checker **actual == baseline 零漂移**（R2b=235 / R2c=1422 / R2d=35 与 `compliance-baseline.md` §BASELINE 块逐值一致——新 daoFor 面复用 AbstractErpQaInspectionProcessor.inspectionDao() 既有站点零新增计数；R12c=40 vs 脚本 F4 头 38 为 HEAD 既有漂移非本计划引入[非 CI 门控块]）；owner doc `state-machine.md` §4 + §实现约定:190 Deferred→已实现（D1-D3 裁决 + config 门控语义 + 残留风险）+ arm-index P1-RC-041 → done (RC-R1.59)（修复记录 + 历史保留）+ roadmap RC-R1.59 → done ✅ + 行标签按 B 类裁决改写（消除「越界项…checkbox」歧义，对齐 R1.48 先例）+ 本日志条目；use-cases.md L1 不动（Non-Goal）。
- **验证基线（全绿）**：`mvn test -pl module-quality/erp-qa-service,module-purchase/erp-pur-service,module-sales/erp-sal-service,module-manufacturing/erp-mfg-service` **182/320/299/282 tests 0 failures 0 errors**（含四新测试类 14 组）+ 全量 `mvn test` BUILD SUCCESS + `mvn clean install -DskipTests` 全量 BUILD SUCCESS + compliance checker actual == baseline 零漂移（全 19 规则与 `compliance-baseline.md` §BASELINE 块一致）。
- **Deferred（已裁定，见计划）**：CANCELLED 结果态显式字典/状态机（方案 B）——out-of-scope improvement，successor no（方案 A 软删满足 L1 语义，dict 追加超 B 类枚举须另行批准）；作废联动通知派发——watch-only residual，successor no（L1 未要求 notify）；软删质检单「已取消」审计展示——optimization candidate，successor no（审计经 delVersion/删除时间追溯，独立视图无 L1 契约）。
- **BUILD_VERIFY**：分域测试 + 全量 `mvn test` + `mvn clean install -DskipTests` + checker 零漂移均绿。**独立结束审计**：由独立子代理（新会话）执行（Closure Gates 硬门控，执行者未自我审计）。

### quality 关键项否决落地（RC-R1.58，plan `2026-08-16-1634-2`，P1-RC-040 UC-QA-06 关键项否决运行时成立 + UC-QA-03 断言复用，越界项 ORM 结构变更 A 类授权 + 双独立子 agent 批准）

- **工作项**：执行 `docs/plans/2026-08-16-1634-2-rc-mr1-r1-58-qa-critical-item-veto.md` 三 Phase。落地 finding **P1-RC-040**（UC-QA-06 关键项否决核心验收标准完全缺失——ORM 双实体无 isCritical 列 + `InspectionResultEvaluator.aggregate` 无否决分支，关键项不合格+allowConcession=true 错误 CONDITIONAL；A1.31 §2 P1①+P1⑤；Q4=(a) 强制实现 + product-scope 确认义务经 A4.2.101 裁决 + 本计划裁决声明闭合）。
- **Phase 1 裁决（D1-D4）**：**D1 列定义=选项 C**（`ErpQaInspectionTemplateLine.isCritical` propId 17 + `ErpQaInspectionLine.isCritical` propId 18，INTEGER 可空无默认无索引无 UK，DDL 三方言 `IS_CRITICAL INTEGER NULL` 纯加性实证；否决 A[mandatory+defaultValue 与计划自身纯加性边界冲突]、B[BOOLEAN 跨方言不一致]）+ **D2 否决语义**（`aggregate` 循环内统一读 `line.getResult()`[显式结果优先/缺省 evaluateLine]，任一关键项行[isCritical=1] REJECTED → 强制整体 REJECTED **跳过 allowConcession**，对齐 L1 `use-cases.md:108-109`「关键项否决，无论其他项」；非关键项 REJECTED + 让步 → CONDITIONAL 保持）+ **D3 复制链**（`TemplateLineSpec` 增 isCritical → `InspectionTemplateMatcher.match` 传值 → `copyTemplateLinesToInspection` 复制；无模板手工建行 CRUD 直设；recordResult 不传 isCritical）+ **D4 门控=不加 config**（否决为验收标准硬契约，门控化掩盖契约缺失；A4.1.4 config-gate 范式仅适用可配置行为）。orm.xml 草案 well-formed（xmllint 通过，namespace 警告为既有 Nop 约定）。
- **Phase 2 双独立子 agent 批准**：`ses_ff5e77093ffebK3Ky48ji3uUf5` + `ses_ff5e75a4cffeNMs0BAp1dUMfOx`（均 fresh session）**均 APPROVE，无强制修正**——纯加性最小性（恰 2 列 + 2 注释行，零删除/零重排）、propId 严格顺延（17/18，updateTime 16/17 之后）、无 mandatory/defaultValue/索引/UK、xmllint WELL-FORMED、IS_CRITICAL 无冲突、生成产物一致（_gen PROP_ID 18/17 + XMeta + DDL 三方言 + api bean 仅新增可选字段）、无其他域模型/API 契约影响。批准记录落盘计划 Phase 2。
- **Phase 2 实施**：live orm.xml 双实体加列 + `mvn clean install -DskipTests -pl module-quality/erp-qa-codegen,erp-qa-api,erp-qa-dao,erp-qa-meta,erp-qa-service,erp-qa-web,erp-qa-app -am` 增量重生成 BUILD SUCCESS——生成产物核对（`_ErpQaInspectionLine`/`_ErpQaInspectionTemplateLine` getter/setter + XMeta propId 18/17 + DDL 三方言 `IS_CRITICAL INTEGER NULL`）；`InspectionResultEvaluator.aggregate` 增否决分支（循环内 collect 关键项 REJECTED → 强制 REJECTED，javadoc 更新三态语义）；`TemplateLineSpec` 增 isCritical + `InspectionTemplateMatcher.match` 传值 + `copyTemplateLinesToInspection` 复制。
- **Phase 3 测试 + 文档 + 回填**：`TestErpQaCriticalItemVeto` **5 组全绿**（①关键项 REJECTED + allowConcession=true → REJECTED[否决覆盖让步 + approveStatus 保持 UNSUBMITTED + 自动建 NCR]②关键项 ACCEPTED + 非关键项 REJECTED + 让步 → CONDITIONAL[不否决]③关键项 REJECTED + 无让步 → REJECTED④模板行 isCritical=1 → 质检单行 isCritical=1[复制链，快照实证 IS_CRITICAL 列]⑤手工直设 isCritical=1 + 让步 → 否决 REJECTED）+ `_cases` 快照录制（IS_CRITICAL 列含值）；`testRejectedCriticalGoesRejected` 更名 `testRejectedNoConcessionGoesRejected`（P1⑤ 断言误导修复，Q1 根因）+ `_cases` 目录同步更名；erp-qa-service **177 tests 全绿**（172 基线 + 5 新增零回归）+ 全量 `mvn clean install -DskipTests` BUILD SUCCESS + compliance checker **actual == baseline 零漂移**（R2c=1422 / R2b=235——isCritical 经同实体 getter/模板行复制，零新增 daoFor 面，全 19 规则与 `compliance-baseline.md` §BASELINE 一致）；owner doc `inspection-integration.md §5.3` 补关键项否决实现注记（D1-D4 + isCritical 列 + 复制链 + aggregate 否决语义 + 非目标）+ arm-index P1-RC-040 → done (RC-R1.58)（修复记录 + 历史保留）+ roadmap RC-R1.58 → done ✅ + 本日志条目；`use-cases.md` L1 不动（Non-Goal）。
- **验证基线（全绿）**：`mvn test -pl module-quality/erp-qa-service` **177 tests 0 failures 0 errors**（172 基线 + 5 新增）+ `mvn clean install -DskipTests` 全量 BUILD SUCCESS + compliance checker actual == baseline 零漂移（全 19 规则与 `compliance-baseline.md` §BASELINE 块一致）。
- **Deferred（已裁定，见计划）**：关键项行级 UI 标记样式（AMIS badge/高亮）——out-of-scope improvement，successor no（isCritical 经 XMeta 生成展示，视觉定制属产品化打磨）；关键项必检联动（isRequired × isCritical）——watch-only residual，successor no（L1 无联动语义，当前正交实现）。
- **BUILD_VERIFY**：分域测试 + 全量 `mvn clean install -DskipTests` + checker 零漂移均绿。**独立结束审计**：由独立子代理（新会话）执行（Closure Gates 硬门控，执行者未自我审计）。
- **VERIFY 复跑（2026-08-16 mission-driver，scoped 增量验证）**：变更模块增量构建 `mvn clean install -DskipTests -pl module-quality/erp-qa-codegen,module-quality/erp-qa-api,module-quality/erp-qa-dao,module-quality/erp-qa-meta,module-quality/erp-qa-service,module-quality/erp-qa-web,module-quality/erp-qa-app -am` BUILD SUCCESS（含 codegen 从 ORM 重生成 + 上游依赖模块）+ `mvn test`（同 scope）BUILD SUCCESS——**erp-qa-service 177 tests 0 failures 0 errors**（含 `TestErpQaCriticalItemVeto` 5 组 + `TestErpQaInspectionStateMachine` 13 组，`TestErpQaCriticalItemVeto`/`_cases` 快照在案）+ 上游依赖模块全绿（common/notify/master-data/sales/inventory/finance/assets 等 0 failures）+ compliance checker **actual == baseline 零漂移**（R2b=235 / R2c=1422 / R2d=35，与 `compliance-baseline.md` §BASELINE 块逐值一致）。非 full-green 声明（scoped `-pl` 验证，全量 `mvn test` 全绿证据见 plan Closure Gates 已记录）。

### crm 团队分配 ROUND_ROBIN/LOAD_BALANCED 落地（RC-R1.57，plan `2026-08-16-1634-1`，P1-RC-036 UC-CRM-11 三方法运行时成立，越界项 ORM 结构变更 A 类授权 + 双独立子 agent 批准）

- **工作项**：执行 `docs/plans/2026-08-16-1634-1-rc-mr1-r1-57-crm-team-member-allocation.md` 三 Phase。落地 finding **P1-RC-036**（TerritoryAssignmentEngine.toResult 显式将 ROUND_ROBIN/LOAD_BALANCED 降级 MANUAL、ownerId 永不设置；A1.28 §4 三判据复核重开 → 入 MR1；2026-08-12 批量裁决 A 类 ORM 授权[roadmap:37] + product-scope 未裁剪显式裁决 → Q4=(a) P1 强制实现）。
- **Phase 1 裁决（D1-D4）**：**D1 成员模型=选项 A**（独立实体 `ErpCrmTeamMember`：tableName `erp_crm_team_member`、propId 1-10 连续、列集 {id, teamId FK BIGINT mandatory, userId stdDomain=userId VARCHAR36 mandatory, remark} + 审计 6 列 + UK `UK_CRM_TEAM_MEMBER_TEAM_USER`(teamId,userId) + to-one team + 2 索引；**不设 orgId 冗余列**（经 team 解析）；否决 ErpCrmTeam 内嵌成员 JSON 子表[无 UK/不可查询/无审计行语义]）+ **D2 挑人语义定稿**（ROUND_ROBIN=成员 id 升序 + 上次分配记录[lead.teamId=teamId 且 ownerId 非空 createTime desc limit 1]取下一位循环、无历史首位；LOAD_BALANCED=活跃线索计数[docStatus 非 CONVERTED/LOST/CANCELLED]最少者、平手 id 升序首个；成员空/resolver 不可用/无团队/查询失败 → MANUAL 降级 degraded 保持）+ **D3 config 门控=定稿 TRUE**（`erp-crm.territory.assignment-method-enabled` 默认开启对齐 auto-assign-on-create 先例，关闭=MANUAL 降级零回归；残留风险=默认 TRUE 时成员数据存在的部署行为变化，owner doc 注记 config-gate 语义）+ **D4 注入形态=选项 A**（`TeamMemberResolver` 函数式接口 + **BizModel 调用时传入**——引擎纯函数式可测、config 门控在 BizModel 侧、beans.xml 零变更；否决构造器注入[beans.xml 面变更 + 门控逻辑进引擎]与选项 B[引擎失挑人自含性]）。orm.xml 草案文本落计划（xmllint --noout 通过），live orm.xml 未先改。
- **Phase 2 双独立子 agent 批准**：`ses_ff627bd01ffek61BuHJk5fx1oo` + `ses_ff627a22effeuranOcZ0uUl9OT`（均 fresh session）**均 APPROVE，无强制修正**——纯加性实证（全仓 grep 零命中 + git status module-crm/model 干净）、propId 1-10 连续、约定逐项比对 ErpCrmTeam/ErpCrmTerritoryAssignmentRule、UK 命名无碰撞、to-one 同域引用（对齐 orm.xml:25 跨工程零 refEntityName）、无 codegen 环、治理排序（批准 checkbox 在 orm.xml 写入前）。批准记录落盘计划 Phase 2。
- **Phase 2 实施**：live orm.xml 新增 ErpCrmTeamMember 实体段（ErpCrmTeam 之后）+ `mvn clean install -DskipTests -pl module-crm/erp-crm-codegen,erp-crm-dao,erp-crm-meta -am` 增量重生成 BUILD SUCCESS——生成产物核对：`_ErpCrmTeamMember`（getTeamId/getUserId/setTeamId/setUserId 齐备）+ XMeta + i18n + DDL 三方言 `erp_crm_team_member` 表（mysql/oracle/postgresql）；`TerritoryAssignmentEngine` 重构（新增 `TeamMemberResolver` 嵌套接口 + `assign(lead, rules, defaultRule, resolver, ctx)` 重载 + `toResult` 条件化挑人[RR pickRoundRobin/LB pickLoadBalanced] + `degradeToManual` 保留降级语义；既有 3 参 assign 委托 null resolver 零变化）；`ErpCrmLeadBizModel` 实现 resolver（`buildTeamMemberResolver`——config 门控关闭返回 null；成员列表查询/上次分配/活跃计数三方法经 `teamMemberDao()`+`leadDao()` daoFor，E3 自检注释）；`assignLead`/`defaultPrepareSave` 两入口传 resolver；`ErpCrmConstants.CONFIG_TERRITORY_ASSIGNMENT_METHOD_ENABLED` + `ErpCrmConfigs.isAssignmentMethodEnabled()`；beans.xml 按 D4 定稿保持无参 bean（BizModel 侧传入，功能零变更，`_vfs` 注释更新被仓库编辑策略拦截——非功能项）。
- **Phase 3 测试 + 文档 + 回填**：新增 `TestErpCrmTerritoryAssignment` **8 组全绿**（①RR 轮流[A→B 下一位 + B→A 循环]②RR 无历史首位③LB 最少线索[A=3/B=1→B]④LB 平手→id 升序首个⑤无成员/无 resolver/无团队 → MANUAL 降级+degraded⑥config 关闭 assignLead 集成降级⑦assignLead GraphQL 集成回写 ownerId=userA+_cases 快照⑧defaultPrepareSave 自动分配路径+_cases 快照；纯逻辑单测经恒真 ConditionMatcher 注入绕过 ORM 元数据未初始化[对齐 SequenceAssignmentEngine matchCustomField 纯单测范式]）+ erp-crm-service **188 tests 全绿**（180 基线 + 8 新增零回归）+ 全量 `mvn test` BUILD SUCCESS 0 failures 0 errors（1 skipped 既有 `ErpAllWebPagesCollectTest` @Disabled）+ `mvn clean install -DskipTests` 全量 BUILD SUCCESS + compliance checker **R2b 233→235 / R2c 1420→1422 baseline-raise 登记**（`ErpCrmLeadBizModel.teamMemberDao():377` + `leadDao():381` 同域 crm BizModel resolver 2 站点，per-site 证据落 `docs/audits/compliance-baseline.md` 注记，对齐 R1.48/R1.51/R1.56 baseline-raise 先例；R2a/R2d/R8 零变化，其余 17 规则零漂移）；owner doc `territory.md` §实现注记 2 Deferred→已实现（成员模型 + D2 挑人算法 + D3 config 门控语义 + 分配执行流程 :158-160 三方法字面全部成立）+ arm-index P1-RC-036 → done (RC-R1.57)（修复记录 + 历史保留）+ roadmap RC-R1.57 → done ✅ + 本日志条目；`use-cases.md` L1 不动（Non-Goal）。
- **验证基线（全绿）**：`mvn test -pl module-crm/erp-crm-service` **188 tests 0 failures 0 errors**（180 基线 + 8 新增）+ 全量 `mvn test` BUILD SUCCESS + `mvn clean install -DskipTests` 全量 BUILD SUCCESS + compliance checker actual ≤ updated baseline（R2b=235 / R2c=1422，全 19 规则与 `compliance-baseline.md` §BASELINE 块一致）。
- **Deferred（已裁定，见计划）**：LOAD_BALANCED 平手策略（id 升序首个）——optimization candidate，successor no（L1 未规定平手裁决策略，id 升序首个为确定性实现）；成员维护 UI 页面——Non-Goal（标准 CRUD 生成）。
- **BUILD_VERIFY**：分域测试 + 全量 `mvn test` + `mvn clean install -DskipTests` + checker actual ≤ updated baseline 均绿。**独立结束审计**：由独立子代理（新会话）执行（Closure Gates 硬门控，执行者未自我审计）。
- **VERIFY 复跑（2026-08-16 mission-driver，scoped 增量验证）**：变更模块增量构建 `mvn clean install -DskipTests -pl module-crm/erp-crm-codegen,module-crm/erp-crm-api,module-crm/erp-crm-dao,module-crm/erp-crm-meta,module-crm/erp-crm-service,module-crm/erp-crm-web,module-crm/erp-crm-app -am` BUILD SUCCESS（52/52 模块，含 codegen 从 ORM 重生成）+ `mvn test`（同 scope）BUILD SUCCESS——**erp-crm-service 188 tests 0 failures 0 errors**（含 `TestErpCrmTerritoryAssignment` 8 组）+ 上游依赖模块全绿（common/notify/master-data/sales/inventory/quality 等 0 failures）+ compliance checker **actual ≤ baseline**（R2b=235 / R2c=1422，与 `compliance-baseline.md` §BASELINE 块逐值一致，零漂移）。非 full-green 声明（scoped `-pl` 验证，全量 `mvn test` 全绿证据见 plan Closure Gates 已记录）。

### inventory 盘点完成自动差异移动单落地（RC-R1.56，plan `2026-08-16-0904-3`，P1-MA2-062 reuse 重开 UC-INV-07 五断言，越界项代码逻辑预授权 + product-scope 确认义务 + 双独立子 agent 批准）

- **工作项**：执行 `docs/plans/2026-08-16-0904-3-rc-mr1-r1-56-inv-stocktake-diff-move.md` 三 Phase。落地 finding **P1-MA2-062**（StockTake completeTake stub 不自动生成盘盈/盘亏移动单；A1.27 §4 三判据复核不满足 → 重开 P1 入 MR1；product-scope.md:16 复核**未裁剪**「盘点自动生成移动单」→ P1 强制实现，Q4=(a) 禁止方案 B）。**治理**：越界项按 ai-autonomy-policy:79/83 `auto + dual-agent-approval` 双独立子 agent 批准 + 独立 plan-audit（草案审查 3 轮收敛 accept，iter-1 MAJOR-2 修正 D2/D3 判别载体一致性）。
- **Phase 1 裁决（D1-D4，product-scope 确认义务闭环）**：**D1 差异口径=选项 A**（L1 逐字公式 `差异 = actualQuantity − bookQuantity`，use-cases.md:129，盘点行字段快照对账；否决实时 StockBalance.totalQuantity 比对——偏离 L1 字面 + 跨实体实时查询）+ **D2 生成语义=选项 A**（独立移动单 `relatedBillType=ERP_INV_STOCK_TAKE` + `relatedBillCode=null` → `isBusinessLinked()==false` → generateMove 停 CONFIRMED 待库管员二次确认，对齐 state-machine.md:129 独立创建产生待办；否决 business-linked 自动 DONE——绕过二次确认 + 触发过账误派）+ **D3 过账处理=选项 A**（`InvPostingDispatcher.resolveBusinessType` 跳过集加 `ERP_INV_STOCK_TAKE`——null relatedBillType 仍按 moveType 误派 PURCHASE_INPUT/SALES_OUTPUT，类型键+跳过集条目为必要组合；差异移动单 DONE 零凭证 posted=false；否决专属 businessType+Provider——L1 断言面无过账断言，会计化登记 successor）+ **D4-a 关联载体=选项 A**（remark 承载「盘点差异 {code} 盘盈/盘亏」，零 ORM 变更）+ **D4-b 告警=选项 A**（失败逐行隔离 + LOG.warn + config `erp-inv.stocktake-diff-alert-enabled` 默认 false 门控派发 `inv.stocktake-diff-generation-failed` 告警，对齐 A4.2.4 dispatchVarianceFailureAlert 范式）+ **D4-c=选项 A**（守卫错误码 `ERR_INV_STOCK_TAKE_MOVE_GENERATE`）。product-scope.md:16 未裁剪确认登记（对齐 Q5 换货先例）。
- **Phase 2 双独立子 agent 批准**：`ses_ff6b3c141ffea8fvB8QYYaFgLK` + `ses_ff6b39419ffeeN5T1BmhZkE9yw`（均 fresh session）**均 APPROVE**——6/7 硬门判据全 PASS（D1-D4 与 L1 契约一致；既有 generateMove 链零改动；盘点单本身不改余额原则保持[bookCompletion 全仓唯一调用点=doComplete:131 结构性保证]；D3 跳过集语义安全[grep 生产代码零既有 ERP_INV_STOCK_TAKE 生产者 + PPV 侧通道一并门控]；D2 判别载体成立[isBusinessLinked 双字段非空才 true]；零 ORM/api.xml 变更）。**reviewer B 1 强制修正（实施前落盘）**：(a) 逐行失败孤立 DRAFT 移动单 = 同事务补偿删除（失败面集中于 confirm 可用量/效期校验，预留/余额变更之前，DRAFT 删除零副作用；applyReservation DB 故障边缘登记 residual risk）；(b) differenceQuantity/differenceAmount 回填不依赖生成成败（DONE 后差异数据完整，运维可经手工 generateMove 补录）。批准记录落盘计划 Phase 2。
- **Phase 2 实施**：新 per-mutation `ErpInvStockTakeCompleteTakeProcessor`（beans.xml 注册）——`completeTake` 完整盘点闭环（protected step：validateTransition 源态守卫[Bean 委托 + 领域码映射]→ loadLines → backfillDifference[D1 公式 + differenceAmount=差异×unitCost]→ 逐行 generateDiffMoveIfNeeded[零差异跳过 / 盘盈 INCOMING dest=盘点仓+行库位 / 盘亏 OUTGOING source=盘点仓+行库位，行量=|差异|，unitCost 沿用盘点行，D2 remark 关联，失败 catch → deleteOrphanDiffMove 补偿删除 + WARN + dispatchDiffGenerationFailureAlert 告警]→ finalizeComplete[DONE]）；`ErpInvStockTakeBizModel.completeTake` 委托 Processor（Facade 保留 requireEntity 权限管道）；`ErpInvConstants.RELATED_BILL_TYPE_STOCK_TAKE` + `CONFIG_STOCKTAKE_DIFF_ALERT_ENABLED`；`ErpInvConfigs.isStocktakeDiffAlertEnabled()`；`ErpInvErrors.ERR_INV_STOCK_TAKE_MOVE_GENERATE`；`InvPostingDispatcher` 跳过集扩展。**既有 generateMove 链零改动**（git diff 7 文件 = 0 行变更实证）。
- **Phase 3 测试 + 文档 + 回填**：`TestErpInvStockTakeCompleteDiffMove` **10 组全绿**（①盘盈 INCOMING 移动单[行量=5/remark/D2 停 CONFIRMED/回填 +5/+10]②盘亏 OUTGOING 移动单[行量=4/回填 −4/unitCost 空不回填金额]③零差异不生成④盘点单本身不改余额[断言④：completeTake 后余额不变]⑤移动单 DONE 后余额变化[断言⑤：库管员 complete 后 10→15 + posted=false]⑥重复 completeTake 拒绝[ERR_INV_STOCK_TAKE_ILLEGAL_TRANSITION] + DRAFT 态拒绝⑦部分行失败隔离[盘盈行成功 + 盘亏行可用量不足隔离 + 孤立 DRAFT 补偿删除零残留 + 失败行回填完整 + config 开启告警落库 eventType/recipient 断言] + config 关闭静默跳过⑧回填持久化 + D3 零凭证[移动单 DONE 后 countBillLinks=0]）；owner doc `state-machine.md` §盘点单状态机 Deferred→已实现（D1-D4 裁决 + 断言④⑤ + successor「盘点差异会计化」触发条件 + 盘点期间冻结运营建议）+ 审查提示同步 + `ErpInvStockTakeStateMachine` javadoc 同步 + `ui-patterns.md` flux 映射/盘点节同步 + `stock-take-flow/main.page.yaml` 三处文案（「未生成盘盈/盘亏移动单」→已实现）；回填 arm-index P1-MA2-062 → done (RC-R1.56)（修复记录追加）+ roadmap RC-R1.56 → done ✅ + 本日志条目；`use-cases.md` L1 不动。
- **验证基线（全绿）**：`mvn test -pl module-inventory/erp-inv-service` **223 tests 全绿**（结束审计独立复验口径：全新 run surefire 实测 223/0/0，含 `TestErpInvStockTakeCompleteDiffMove` 10/10 + 状态机矩阵零回归；原「228（218 基线 + 10 新增）」为计数口径差异，以 223 实测为准）+ 全量 `mvn test`（Closure Gates）+ `mvn clean install -DskipTests` 全量 BUILD SUCCESS + compliance checker **R2c baseline-raise 1415→1420 登记**（`ErpInvStockTakeCompleteTakeProcessor` 5 处 inv 同域 daoFor，per-site 证据落 `docs/audits/compliance-baseline.md` 注记，对齐 R1.48/R1.51 baseline-raise 先例；R2a/R2b/R2d/R8 零变化，其余 18 规则零漂移）。
- **回填**：owner doc state-machine.md/ui-patterns.md/页面文案 + arm-index P1-MA2-062 → done (RC-R1.56) + roadmap RC-R1.56 → done ✅ + 本日志条目。
- **Deferred（已裁定，见计划）**：盘点差异会计化（盘盈/盘亏 GL 凭证）——out-of-scope improvement，successor yes（触发条件=运营/审计要求盘点差异会计化时按会计核心路径立项 + 双独立子 agent 批准）；盘点期间出入库冻结运营建议——watch-only residual，successor no（D1 bookQuantity 快照口径，运营惯例冻结账面快照，owner doc 注记引导）。
- **BUILD_VERIFY**：分域测试 + 全量 `mvn test` + `mvn clean install -DskipTests` + checker 均绿。**独立结束审计**：由独立子代理（新会话）执行（Closure Gates 硬门控，执行者未自我审计）。
- **VERIFY 复跑（2026-08-16 mission-driver，scoped 增量验证）**：变更模块增量构建 `mvn install -DskipTests -pl module-inventory/erp-inv-dao,module-inventory/erp-inv-service,module-inventory/erp-inv-web -am` BUILD SUCCESS（erp-inv-web 含 page.yaml 资源打包）+ `mvn test -pl module-inventory/erp-inv-service` **228 tests 0 failures 0 errors**（surefire 汇总，含 `TestErpInvStockTakeCompleteDiffMove` 10 组 + `TestErpInvStockMoveAndStockTakeStateMachines` 状态机矩阵零回归；228 vs 223 为既有计数口径差异，以 0 failures 门控为准）+ compliance checker **actual ≤ baseline**（R2c=1420 与 `compliance-baseline.md` §R2c 基线上调注记（plan 2026-08-16-0904-3）一致，R2a/R2b/R2d/R8 零变化）。非 full-green 声明（scoped `-pl` 验证，全量 `mvn test` 全绿证据见 plan Closure Gates 已记录）。

### sales 退货换货落地（RC-R1.51，plan `2026-08-16-0904-2`，P1-RC-025 UC-SAL-06 四断言，越界项 A 类 ORM 授权 + 双独立子 agent 批准）

- **工作项**：执行 `docs/plans/2026-08-16-0904-2-rc-mr1-r1-51-sal-return-exchange.md` 五 Phase。落地 finding **P1-RC-025**（sales 退货换货完全缺失；Q5 已裁决不裁剪 → P1 强制实现，Q4=(a) 禁止方案 B）。**治理**：2026-08-12 A 类批量授权（returnType 列）+ exchangeDeliveryId/exchangeReturnId 2 列超字面授权 → 越界回落双独立子 agent 批准（ai-autonomy-policy:79）+ 独立 plan-audit（草案审查 4 轮收敛 accept，iter-1 MAJOR-1 修正批准门控顺序）。
- **Phase 1 裁决（D1-D4）**：**D1 触发点=选项 A**（独立 `generateExchangeDelivery(returnId, lines[], ctx)` @BizMutation，操作员显式触发——换货商品/数量需操作员决策，L1「换发等值或不同货物」；否决 approve 后置自动生成）+ **D2 关联载体=选项 A**（两 FK 列 `ErpSalReturn.exchangeDeliveryId` propId 30 + `ErpSalDelivery.exchangeReturnId` propId 29 双向互指，同事务双写，FK 即关联契约[域内无 sourceBill 字符串模式]；否决独立 Link 实体）+ **D3 价差=选项 A**（头级口径 Δ = 换货出库 totalAmountWithTax − 退货 totalAmountWithTax；Δ>0 补差价开票 / Δ<0 退款 / Δ=0 无动作）+ **D4 幂等=选项 A**（exchangeDeliveryId 非空拒绝 `ERR_EXCHANGE_DELIVERY_ALREADY_GENERATED`）。
- **Phase 2 双独立子 agent 批准**：`ses_ff71efedaffeo38tPDhnJNNAEq` + `ses_ff71d6568ffeOIzmiurzEmL1um`（均 fresh session）**均 APPROVE**——propId 空闲实证（两实体末列 remark propId 28）+ 纯加性核对（2 关联列无 NOT NULL/默认/索引/UK，唯一 defaultValue=returnType"RETURN" R1.40 先例）+ RETURN 路径零行为变化（doApprove 链 :211-226 + R1.19 守卫族 :188-197 零修改）+ 零新增过账 Provider/VoucherFact（Δ>0 复用 IErpSalInvoiceBiz.save / Δ<0 复用 ReturnRefundOrchestrator）+ Q4 收敛 + 政策符合；批准记录落盘计划 Phase 2。
- **Phase 3 ORM 纯加性变更**：`ErpSalReturn.returnType`（propId 29，dict `erp-sal/return-type` RETURN/EXCHANGE，defaultValue="RETURN"）+ `exchangeDeliveryId`（propId 30）+ `ErpSalDelivery.exchangeReturnId`（propId 29）+ 双向 to-one。**codegen 循环依赖修正**：双向 to-one 触发 `nop.err.orm.model.ref-depends-contains-loop`（OrmModelTopEntryBuilder 拓扑序）→ `exchangeReturn` to-one 标 `ignoreDepends="true"`（entity.xdef :148 官方循环标注机制）破环。`mvn clean install -DskipTests` BUILD SUCCESS + 五同步核对（Entity getter/setter + xmeta propId 29/30 + DDL 三方言 + dict.yaml 生成 + propId 无冲突）。
- **Phase 4 换货流程编排**：`ErpSalExchangeDeliveryLine` DTO（app.erp.sal.biz，镜像 ErpCtInvoicePlanGenerateItem 入参化先例）+ `IErpSalReturnBiz.generateExchangeDelivery` @BizMutation + Facade 委托 + per-mutation `ErpSalReturnGenerateExchangeDeliveryProcessor`（beans.xml 注册）——守卫链（returnType==EXCHANGE + APPROVED + 源出库已审核 + 期间 OPEN + 发票未核销，复用 `ErpSalReturnProcessor` 同包 protected helper）+ 换货行解析（入参缺省复制退货行）+ 复制退货头建 DRAFT 出库单（code 前缀 `EX-`，行金额 scale 4 HALF_UP 聚合头金额，`exchangeReturnId` 建单即写）+ 同事务回写 `exchangeDeliveryId`（双向关联双写）+ 价差分支（Δ>0 `IErpSalInvoiceBiz.save` 建 DRAFT 补差价发票 code 前缀 `EXDIFF-` / Δ<0 `refundOrchestrator.orchestrateRefund` 既有 reverse-settlement 退款兜底 / Δ=0 无动作；价差金额+方向写换货出库 remark 审计可追溯）+ 新 ErrorCode 2 枚（中文描述 + define 参数表）；**RETURN 类型既有路径零变更**（git diff 实证 approve/posting/refund 链零改动，仅新增 mutation + returnType 判别）。测试桩 `TestStubErpSalReturnBiz`（qa 模块）补 generateExchangeDelivery 占位。
- **Phase 5 测试 + E2E + 文档 + 零回归**：`TestErpSalReturnExchange` **7 组全绿**（①returnType 默认值物化 + RETURN 零回归②EXCHANGE 审核库存恢复[断言①，INCOMING 移动单 + 余额 +3]③generateExchangeDelivery 生成 + 双向关联[断言②④，EX- 前缀 + 换货改发不同货物]④Δ>0 补差价开票[EXDIFF- DRAFT totalAmountWithTax=Δ]⑤Δ<0 退款[退货审核后客户再核销时序 → 反向核销行 + 发票回 UNRECEIVED]⑥守卫族[RETURN 类型/未审核/期间 CLOSED/源出库未审核] + 幂等拒绝⑦换货出库经既有状态机审核 → OUTGOING 移动单扣库存[断言②运行时闭合，余额 −3]）+ E2E 新 spec `sal-return-exchange.action.spec.ts`（O2C 链 → EXCHANGE 建单 → 审核 → generateExchangeDelivery → 出库审核 → OUTGOING 移动单 + 双向关联断言 + 幂等拒绝 + 全链清理）flux 全绿 + 既有 `sal-return.action.spec.ts` 2/2 零回归。
- **验证基线（全绿）**：`mvn test -pl module-sales/erp-sal-service` **296 tests 全绿**（289 基线 + 7 新增零回归）+ 全量 `mvn test` **3463 tests 0 failures 0 errors**（1 skipped 既有 `ErpAllWebPagesCollectTest` @Disabled）+ `mvn clean install -DskipTests` 全量 BUILD SUCCESS + compliance checker **R2c 1413→1415 基线上调登记**（per-mutation Processor 2 处同域 daoFor：`returnDao()` + `loadReturnLines`，逐站点 per-site 证据落 `docs/audits/compliance-baseline.md` 注记，镜像 R1.18/R1.19 same-domain daoFor 先例；R2a/R2b/R2d 零漂移——跨实体创建全经 I*Biz 注入）。
- **回填**：owner doc `returns.md §退货类型` 补换货实现注记（returnType/dict/D1-D4 裁决/价差语义/双向关联/操作引导/多轮换货边界）+ arm-index P1-RC-025 → done (RC-R1.51)（修复记录 + 历史保留）+ roadmap RC-R1.51 → done ✅ + 本日志条目 + compliance-baseline R2c 基线上调注记。
- **Deferred（已裁定，见计划）**：多轮/多次换货链（successor yes，触发条件=运营要求退货→换货→再换货链式跟踪时按标准立项）；换货出库单独立审批流/自动审核（out-of-scope improvement，successor no——复用既有出库状态机）。Δ<0 退款客户级边界（Phase 1 Proof ② 显式登记：标准场景退货审核已先行反转已核销发票，换货生成时点通常无已核销发票 → 该分支为退款兜底 + remark 审计记录）。
- **BUILD_VERIFY**：分域测试 + 全量 `mvn test` + `mvn clean install -DskipTests` + E2E 2 spec（新 + 既有）flux 全绿 + checker actual ≤ updated baseline 均绿。**full-green verification**。
- **独立结束审计（2026-08-16）**：独立子代理（新会话）执行完毕——`plan-check.mjs --strict` 30/30 通过 + 语义复核全绿（ORM/dict/Processor/E2E/回填逐项实证 + `TestErpSalReturnExchange` 7/7 + erp-sal-service 296 tests 0 failures 0 errors 复跑一致 + 反空洞测试⑦库存 23→20 闭合）；Closure Gates 8/8 勾选，证据落盘计划 Closure 段，plan `2026-08-16-0904-2` **Plan Status: completed**。执行者未自我审计。
- **VERIFY 复跑（2026-08-16 mission-driver）**：全 reactor `mvn clean install -DskipTests` BUILD SUCCESS + 全量 `mvn test` **3463 tests 0 failures 0 errors 1 skipped**（surefire 聚合，1 skipped = 既有 `ErpAllWebPagesCollectTest` @Disabled）+ compliance checker **actual == baseline 零漂移**（R2c=1415，全 19 规则与 `compliance-baseline.md` §BASELINE 精确一致）。**full-green verification**。

### A4.2.3 跨工单并发预留 + reserved/available 一致性运行时确认（MA4 回队行，plan `2026-08-16-0424-3`，零生产代码变更）

- **工作项**：执行 `docs/plans/2026-08-16-0424-3-rc-ma4-a4-2-3-mfg-reservation-runtime-confirmation.md` 单 Phase（MA4 A4.2.3 回队行运行时确认——MR1 P1-RC-008 预留写路径落地后 reservedQty/availableQuantity 实时一致性 + 跨工单并发预留 lost-update 防护运行时核验）。**零生产代码变更**（验证类计划：只读确认生产面 + **无条件新增跨工单并发测试探针**，对齐 A4.2.79 探针先例）。
- **Proof 既有测试复跑（全绿）**：`mvn test -pl module-inventory/erp-inv-service,module-manufacturing/erp-mfg-service` **inv 218 + mfg 269 tests 0 failures 0 errors**——含 `TestErpInvReservationWriteApi#testConcurrentCreateReservationNoLostUpdate`（写 API 层多线程并发双写预留断言 4+4=8 无丢失 + available=2）+ 创建/释放/消耗后余额断言链（reserved+/reserved− 恢复/reserved− 与 total 守恒全覆盖，`testCreateReservationReservesBalance:97-99` 显式 available=total−reserved 恒等式）。
- **Proof 并发防护机制运行时确认**：`StockMoveBookkeeper.updateBalanceWithRetry:256-328`——MANAGED 路径 `tryUpdateWithVersionCheck` versionProp 乐观锁（:271）+ TRANSIENT/SAVING 路径 flush 捕获 `UK_INV_STOCK_BALANCE_NATURAL` 冲突（:274-278,334-344）+ 冲突 evict+reload（主键/自然键）重试 + `erp-inv.concurrent-deduct-max-retry`=5 耗尽抛 `ERR_INV_CONCURRENT_DEDUCT_CONFLICT`/`ERR_INV_BALANCE_INSERT_CONFLICT`（:408-415）+ `recordOptimisticLockFailure` 计数（:290）——与 A4.2.1 前置结论（无 silent split-quantity corruption）对照无回退。
- **Add 跨工单并发探针（无条件）**：`TestErpMfgReservationLifecycle#testConcurrentCrossWorkOrderApproveNoLostUpdate`——两工单同物料（M1）经 **GraphQL 引擎 mfg approve 集成层**并发 submitForApproval+approve（ExecutorService+CountDownLatch+ContextProvider 模式，镜像写 API 层并发测试）→ 断言：两工单预留均落库（各 reservedQuantity=4）+ reservedQuantity 累加 4+4=8 无丢失 + available=total−reserved=10−8=2 恒等式保持 + 无异常/无重试耗尽（firstError null + 全 status=0）。**测试通过**：mfg 269→**270 tests** 全绿零回归。
- **验证基线（全绿）**：`mvn test -pl module-inventory/erp-inv-service,module-manufacturing/erp-mfg-service` **218 + 270 tests 全绿** + `mvn clean install -DskipTests` 全量 BUILD SUCCESS + compliance checker **actual == baseline 零漂移**（探针零新增 daoFor/import 生产面，19 规则与 `compliance-baseline.md` §BASELINE 一致）。
- **回填**：验证报告落盘 `docs/audits/2026-08-16-rc-ma4-a4-2-3-mfg-reservation-write-path-runtime.md`（对齐 A4.2.79 报告结构：TL;DR 裁决表 + 存疑点原文 + 双实证证据链 + 探针设计/结果 + 与 A4.2.1/2 前置衔接 + 无新 finding 声明）+ roadmap A4.2.3 → `done ✅`（todo→done，含运行时确认摘要）+ arm-index P1-RC-008 行追加 **A4.2.3 运行时确认注记**（不新建 finding，维持 done）+ 本日志条目。
- **裁决**：**0 新 finding / 0 翻转 / 不触发 MR0 / 不归 MR1**——跨工单并发预留 lost-update 防护成立（写 API 层既有断言复跑 + mfg 集成层跨工单探针双实证）+ reserved/available 一致性恒等式成立；不重新裁决 P1-RC-008 分级（RC-R1.48 已落地，无分级裁决义务）；真并发 check-then-act over-commitment 窗口归 A2.17 既有追踪（不重复登记）。
- **BUILD_VERIFY**：分域测试 + 全量 `mvn clean install -DskipTests` + checker 均绿。**VERIFY 复跑（2026-08-16 mission-driver）**：全 reactor `mvn test` **6856 tests 0 failures 0 errors 2 skipped**（2 skipped = 预存 `ErpAllWebPagesCollectTest` @Disabled JDK26/ANTLR H-2）+ `mvn clean install -DskipTests` BUILD SUCCESS + checker actual==baseline 零漂移；`TestErpMfgReservationLifecycle` 10/10 全绿（含新探针）。**full-green verification**。
- **独立结束审计（2026-08-16）**：待独立子代理（新会话）执行——审计前 Closure Evidence 占位 + 审计门控 `[ ]` 实证（执行者未自我审计）。

### 主键/外键 stdDataType string 化迁移 mission 创建 + 2 轮独立审查（`id-string-migration`，roadmap `docs/backlog/id-string-migration-roadmap.md`）

- **背景**：nop-entropy `docs-for-ai/02-core-guides/orm-model-design.md` §主键设计强制规则——BIGINT 主键/外键必须显式 `stdDataType="string"`（JS long 精度静默截断问题）；本仓库 19 个 orm.xml 当前全部 `stdDataType="long"`（2026-08-13 已建工具 `tools/check-bigint-id-types.mjs` + scan/dry-run 验证通过，副本 `_tmp/bigint-id-string-fix/`，1605 列待改：PK 463 + FK 1142，DB 列保持 BIGINT）。
- **用户请求**：「将主键和外键的数据类型全部改成 string」+ 策略要求（一个 mission 内完成；一开始跳过全量编译和测试、逐个模块修复；考虑全局搜索 Long id vs 逐模块；解决每 plan build verify 的可行性问题）+「用独立子 agent 反复审查改进 mission 和 roadmap，直到达成共识」。
- **mission 产物**：`missions/id-string-migration.json`（roadmapPath/plansDir/auditsDir/commands 对齐既有 mission 范式）+ `docs/backlog/id-string-migration-roadmap.md`（00-roadmap-authoring-guide 规范：目的/Work Item Status/Milestones M0-M4/依赖图/横切关注点/规则/Draft Review Record）+ `docs/backlog/README.md` 登记行（P0 优先级，ORM 保护区域 plan-first + 双独立子 agent 批准）。
- **核心策略裁决**：
  1. **逐域增量迁移而非一次性全局 apply**：每域一个原子工作项（复制该域 dry-run 副本回写 → 增量重生成 → 编译器驱动修复手写代码 → 域级 `-am` build + 域级测试），按 M0 冻结的模块级依赖拓扑序推进。全局 apply 会使全仓 156 模块同时不可编译、无法定位错误源、无法回滚。
  2. **不采用全局盲搜替换 `Long id`**：`Long` 在非 id 语义大量存在（delVersion/fileSize/durationMs/业务数值），盲改必错；编译器强制捕获类型迁移类错误（对齐 dict int→string 先例风险 (a)），语义陷阱（装箱 `==`、`.longValue()`、`Map<Long>` 键、E2E `Number(id)`）用 grep 门控清零（先例风险 (b)）。
  3. **build verify 域级化（回答「如何避免改后无法编译且通过测试」）**：中间态全量构建必然失败属设计使然——每域 plan verify 限定 `mvn clean install -pl <域模块> -am -DskipTests` + `mvn test -pl <域 service> -am`；`-am` 闭包 = 已迁移上游（全绿）∪ 未迁移域 dao 模块（实测惰性可编译——手写文件无跨域实体 import，M0.1 审计 ③ 逐域证实）∪ 自身；M0 冻结顺序保证闭包内不存在「未迁移 service 引用已迁移域类型」的组合；全量构建/全量测试/E2E 修复/compliance/baseline 只在 M4 收尾 plan。
  4. **域级依赖存在环**（assets↔finance、purchase↔finance、projects↔finance 等 dao/service 交叉边；**编译级模块图实测无真环**——sal→qa/mfg→qa 为 test-scope 边，qa 侧用 `test-mock-sales.beans.xml` 桩规避）由 M0.1 跨域 id 调用点审计裁决（finance 先行可解开全部合并级环）；执行中发现顺序不可行时停止 plan 回报 M0 裁决，不自行重排。
  5. **保护区域**：`model/*.orm.xml` 变更 → 每域 plan 须独立 plan-audit + 双独立子 agent 批准（`ai-autonomy-policy.md` 表 `auto + dual-agent-approval`），批准记录落盘计划文件；design 证据 = `orm-model-design.md` §方案 B + `domain-design-guidelines.md` §16A + M0.1 审计结论。
  6. **common-service 组织隔离适配（审查新增，M1.3）**：`ErpOrgContext`/`ErpOrgIsolationOrmInterceptor`/`ErpOrgIsolationQueryTransformer` 三文件的 orgId Long 语义（orgId 是 226 列真实 FK，orm.xml 中 `name="orgId"` 404 次含 178 处 `<index>` 索引成员引用；走反射 API 编译器不报错，`Long.equals(String)` 恒 false）——独立工作项，**须先于 M1.1 master-data 完成**（master-data orm 含 6 个 orgId FK 列）。
- **独立审查（2 个 fresh-session 子代理并行第 1 轮）**：审查者 A（技术/执行视角，ses_ff8120249ffei5weQaLfh7sVOx）`needs revision` — 4 MAJOR（① M0.1 冻结判据公式与实测可构建性矛盾 → 改写为「closure ∩ 未迁移域 = 仅含惰性 dao 模块 ∪ 自身」；② M2 占位顺序与实测 compile DAG 矛盾，projects 闭包含 ast-service + fin-service → M2 重排 finance 先行、projects 靠后；③ common-service orgId 代码无归属 → 新增 M1.3；④ M2/M3 dep 列仅写 M0.1 与依赖图冲突 → 改「M0.1 + 冻结序全部前置项 done」）+ 5 MINOR（Proof② 防空证加 coercion 用例 / test-scope 闭包分开建模 / sql-lib 零存在注明 / 计数附 grep 命令 / view.xml 零手改注明）。审查者 B（治理/规范视角，ses_ff811e11cffeOOAbak517x3UQo）`passes draft review` — 6 MINOR（M2/M3 依赖单元格占位标注 / mission description 补保护区域+M0 门控 / 保护区域证据补 design doc / Skill 指引 / Draft Review Record 段 / build 命令中间态说明）。**全部 MAJOR + MINOR 已修订落地**（roadmap 依赖图、M2 占位序、新增 M1.3、M0.1 判据、mission description、横切 §3/§5、Draft Review Record 段）。
- **独立复审（2 个 fresh-session 子代理并行第 2 轮）**：复审者 A（技术视角，ses_ff80483f1ffeDZo47R47YagROG）`needs revision` — 2 MAJOR（① 快照兼容「实测已全部为字符串」被证伪——实测 35 个输出快照含数字实体 id（约 12%），改为如实陈述由 M0.1 Proof ④ 实证裁定；② dao 模块「已核跨域 import 为零」被证伪——crm-dao 3 处手写 I*Biz 跨域 import（类型级用法），降级为「待审计③逐域证实」）+ 7 MINOR（orgId 计数 404→226 真实 FK 列、未分类 BIGINT 368 口径、Long 计数口径、E2E Number( 519→801、M2.2 inventory 描述、M1.3 时序约束、行号引注）。复审者 B（治理视角，ses_ff8046f9fffeyYTzdXiq5AarhK）`passes draft review` — 1 MAJOR（M1.3 前置约束未入依赖表：master-data orm 含 6 个 orgId FK 列，M1.3 必须先于 M1.1 → M1 表序重排 M1.3→M1.1→M1.2 + 依赖图补边）+ 4 MINOR。**全部 MAJOR + MINOR 已修订落地**（快照陈述如实化、dao 惰性假设降级、orgId 226 口径、M1 表序重排、M2 描述措辞、Draft Review Record 补第 2 轮）。
- **用户裁决（2026-08-16）**：① **快照策略改为每域重录**——编译错误修复完成后每域执行 RECORDING→CHECKING 重录，**不依赖 `JsonMatchHelper` Number 宽容**（roadmap 框架/平台复用「测试兼容」、M1-M3 标准结构 Phase 3、横切 §4、M0.1 Proof ④、mission json 已同步）；② **Java 层全覆盖确认**——mission 完成后全部 PK/FK 在 Java 层均为 String：PK 463 全为 id 命名 BIGINT（无复合/非 BIGINT 主键）全覆盖；FK 1182 = BIGINT 1142（含 orgId 226 列、calibratedBy 等）改造 + 非 BIGINT 40 列实测全 VARCHAR（除 1 处缺省外显式 stdDataType=string）本就 string 零误改（dry-run 副本核查通过）；**数据库层面零改动**（stdSqlType 不变，DDL/种子/序列引擎不受影响）；未分类 BIGINT 368 列（delVersion/fileSize/durationMs/孤儿操作人列）非 PK/FK 不在范围。
- **独立复审（2 个 fresh-session 子代理并行第 3 轮）**：复审者 A（技术视角，ses_ff7f8300cffe5Jiz5TAgsDnbm4）与复审者 B（治理视角，ses_ff7f81cc9ffedA8Lx2i6dPasGo）**均 `passes draft review`**（0 BLOCKER / 0 MAJOR）——第 2 轮 3 MAJOR 修复经独立实证全部属实（M1 表序三方一致、快照 35/291 准确、crm-dao 3 处类型级 import 准确）；余 5+4 MINOR 均为文书级（E2E 计数口径 865/822、368 构成分解、M2.2 11 域、dao 非实体跨域 import 4 行补全、行号/术语、日志残留旧口径、快照分母 35/291、M0 小结补 M1 内部序核验）**已全部清理**。
- **共识达成（2026-08-16）**：连续第 2、3 轮 `passes draft review`（0 BLOCKER / 0 MAJOR），第 3 轮仅文书级 MINOR 已全部清理。**roadmap 与 mission 达成共识，可放行 M0.1 计划起草。**
- **M0.1 工作项内容**：工具 per-domain scope 裁定、依赖序冻结脚本（compile/test 闭包分开建模 + 先实测本机 Maven `-am` 含 test-scope 行为）、跨域实体引用与 id 用法审计（含 common-service 三文件）、Proofs（seq-string 行为 / 快照兼容 JsonMatchHelper Number 宽容 + coercion 最小用例 / E2E 影响面计数）、顺序冻结写回 roadmap。
- **BUILD_VERIFY**：本日条目为纯文档创建与修订（mission json + roadmap + README 登记行 + 日志），无代码/模型变更；不适用构建验证。
- **Deferred**：M0.1 计划起草与执行（共识已达成，待 mission driver 按 roadmap 规则启动）；M1-M3 各域迁移计划（M0 冻结序后逐项展开）。

### assets 折旧补提/处置凭证/闲置状态机族落地（RC-R1.52/53/54，plan `2026-08-16-0424-2`，B 类预授权零 ORM 变更）

- **工作项**：执行 `docs/plans/2026-08-16-0424-2-rc-mr1-r1-52-53-54-ast-depreciation-disposal-family.md` 三 Phase。落地 finding **P1-RC-029**（方式B 补提 + 出售补提 reuse）/ **P1-RC-030**（处置凭证 1606 中间科目腿）/ **P1-MA2-061 reuse 重开**（IDLE 闲置状态机）。**治理**：2026-08-12 批量裁决 B 类（isCatchUp/idleSince 列均「可选非必需」→ 纯逻辑 mutation/重构预授权自动执行，零 ORM 结构变更）+ 独立 plan-audit（草案审查 2 轮收敛 acceptable，ses_ff8e4bacfffehyvIUzot9Wun3D needs revision → ses_ff8dd0b93ffem4OBkjKMaVIgrr acceptable）。
- **Phase 1（RC-R1.52 方式B 补提）**：**D1 裁决（选项 A 单张汇总凭证）**——多月漏提额在开放 currentPeriod 一次性汇总为单张凭证（billHeadCode `资产编码#currentPeriod#CATCHUP` + 凭证行 memo「补提 {periods}」标注；已结账漏提期无法逐期过账——财务引擎按凭证日期 resolveOpenPeriod 落账；残留风险：汇总凭证无法按漏提期单期红冲登记 follow-up）。`ErpAstDepreciationScheduleCatchUpDepreciationProcessor` per-mutation Processor（beans.xml 注册）+ `IErpAstDepreciationScheduleBiz.catchUpDepreciation(assetId, currentPeriod, missedPeriods[])` @BizMutation：守卫链[资产存在 + IN_SERVICE（IDLE 不允许补提）+ currentPeriod OPEN] + 逐漏提期复用 DepreciationCalculator（elapsed 含已执行期+漏提期序）+ 计划落行 EXECUTED + 累计折旧/净值回写 + 已 EXECUTED 期幂等跳过 + 漏提期不晚于 currentPeriod 守卫（新 `ERR_DEPRECIATION_CATCHUP_PERIOD_INVALID`）+ UK 并发兜底；`DepreciationPostingDispatcher.tryPostCatchUp` + `DepreciationAcctDocProvider` memo 标注；**出售补提接线**（reuse P1-RC-029 投影）——`ErpAstDisposalProcessor` 新 protected step `catchUpDepreciationToDisposalPeriod`（损益计算前补提 (最近已执行期, 出售期] 含当期；无已执行折旧/IDLE 跳过；出售期 CLOSED fail-fast）；`TestErpAstCatchUpDepreciation` 7 组全绿 + `_cases/` 快照。
- **Phase 2（RC-R1.53 处置凭证 1606 腿）**：`DisposalAcctDocProvider.createFacts` 两步流重构——Step1 借 1602 累计折旧/借 1606 固定资产清理（净值）/贷 1601 固定资产 + Step2 借 1002 处置收入/贷 1606 + 损益从 1606 结转至 6711/6301（收益借 1606/贷 6301，损失借 6711/贷 1606）；GL 恒等式 Dr Σ = Cr Σ + 1606 网为零；`SUBJECT_DISPOSAL_CLEARING="1606"` + `ACCOUNT_KEY_DISPOSAL_CLEARING`（resolver 空匹配回退 Provider 科目编码）+ 种子科目表 `erp_md_subject.csv` 纯加性补 1606 行（ID 46，R1.50 1404 先例）——1606 缺失致过账 ERR_SUBJECT_NOT_FOUND，4 处置测试类 seed 补 1606 + 快照重录；`TestErpAstAcctDocProviderAccountKey` 断言更新 + 新增 4 组合（SCRAPPED±/SOLD±）行级结构断言 + 1606 网为零/GL 平衡恒等式。
- **Phase 3（RC-R1.54 IDLE 状态机）**：**D1 PENDING 处理=选项 A**（suspend 期间保留，resume 后自然执行，处置时 cancelPendingSchedules 统一取消）+ **D2 处置路径=选项 A**（`ErpAstAssetStateMachine.assertCanDispose` 扩展接受 IDLE 对齐 owner doc §2 契约 + transitions() 7→11 边 + 矩阵测试更新）+ **D3 闲置超期 cron=登记 successor**（非 L1 UC-AST-03 字面断言，触发条件=运营要求自动提醒时按 R1.4 job 范式立项）；`ErpAstAssetBizModel` 增 `suspend`/`resume` @BizMutation + per-mutation `ErpAstAssetSuspendResumeProcessor`（守卫委托 StateMachine Bean，非法边 common 作 cause → 领域码 `ERR_AST_ASSET_ILLEGAL_STATUS_TRANSITION`）+ 暂停时点 remark「闲置自 {date}」强制记录（idleSince 列不落 ORM，闲置时长派生时间基准）；折旧语义天然成立（批量 IN_SERVICE-only + validateAssetInService 拒绝 IDLE + catchUp 拒绝 IDLE）；`TestErpAstIdleStateMachine` 6 组 + 矩阵测试扩展全绿 + `_cases/` 快照。
- **验证基线（全绿）**：`mvn test -pl module-assets/erp-ast-service` **317 tests 全绿**（299 基线 + 18 新增零回归，含处置族快照重录）+ 全量 `mvn test` **3461 tests 0 failures 0 errors**（1 skipped 既有）+ `mvn clean install -DskipTests` 全量 BUILD SUCCESS + compliance checker **baseline-raise 登记**（R2c 1405→1408，per-site 证据落 `docs/audits/compliance-baseline.md` 注记——2 per-mutation Processor 3 处 daoFor：补提计划落行 + 资产回写 + suspend/resume assetDao helper，镜像 ExecuteDepreciationProcessor/DisposalProcessor 既有范式，R2a/R2b/R2d 零漂移）。
- **回填**：owner doc `depreciation-and-posting.md` §三 补 1606 实现注记 + `state-machine.md` §1/§2/§5/§8 + §实现模式 Deferred 注记更新为已实现（cron 标注 successor）+ arm-index P1-RC-029/P1-RC-030/P1-MA2-061 三行 → done（RC-R1.52/53/54，修复摘要 + 历史保留）+ roadmap RC-R1.52/53/54 三行 done ✅（行字样按 B 类裁决改写）+ 文件头 2026-08-16 最后更新条目 + 本日志条目。
- **Deferred（已裁定）**：闲置超期提醒 cron（successor yes，触发条件=运营要求闲置资产自动提醒决策时按 R1.4 job 范式立项）；isCatchUp/idleSince 结构列（watch-only residual，successor yes，触发条件=审计/报表需要结构化补提标记或闲置时点列时按 ORM ask-first 流程立项）；补提汇总凭证按漏提期单期红冲（follow-up，触发条件=需要单期冲销补提时按 finance reverse 键契约扩展）。
- **BUILD_VERIFY**：全量 `mvn clean install -DskipTests` + 全量 `mvn test` + 分域测试 + checker 均绿。**full-green verification**。
- **独立结束审计（2026-08-16）**：task `ses_ff860e2caffe8yZ6hT91qDaY7G` 逐 Phase PASS（Phase 1/2/3 代码实装 + GL 恒等式四组合代数验证 + 反模式零命中 + 317 tests + checker 零漂移），1 项文书修正（arm-index P1-RC-029 行未标 done，执行者已补齐）；复审最终裁决 **acceptable**（全部发现已解决，无剩余修正项）；执行者未自我审计（审计前 Closure Evidence 占位 + 审计门控 `[ ]` 实证）。
# Development Log - 2026-08-16

### mfg BOM 快照落地（RC-R1.49，plan `2026-08-16-0904-1`，P1-RC-009 UC-MFG-10 断言④⑤⑥，越界项 A 类 ORM 授权 + 双独立子 agent 批准）

- **工作项**：执行 `docs/plans/2026-08-16-0904-1-rc-mr1-r1-49-mfg-bom-snapshot.md` 五 Phase。落地 finding **P1-RC-009**（BOM 快照原则缺失——断言④提交时快照 BOM 完全未实现/断言⑤同 bomId 内容编辑无隔离/断言⑥已实现）。**治理**：2026-08-12 批量裁决 A 类（ORM 变更批准：快照实体 或 WorkOrder 加列）+ **双独立子 agent 批准硬门**（Phase 2 前置：`ses_ff7cde424ffeMWzqBEhU89gxRq` + `ses_ff7cdbfb6ffeVu4BBb6JQl2mSF` 均 APPROVE，批准记录落盘计划；三前置条件[纯加性/DRAFT 零回归/Q4 收敛]双 agent 均满足）。
- **Phase 1（D1-D3 裁决 + 调用面 census）**：D1 快照载体 = **三实体族 + 工单二列**（选项 A 选定：`ErpMfgWorkOrderBomSnapshot`[workOrderId/bomId/productId/versionLabel/qty] + `ErpMfgWorkOrderBomLineSnapshot`[镜像 BOM 行] + `ErpMfgWorkOrderBomOperationSnapshot`[镜像工艺行] + `ErpMfgWorkOrder.snapshotBomVersion` propId 45 / `snapshotBomId` propId 46；否决 B=WorkOrderLine 冻结列+JSON 化）；D2 读侧切换 = 齐套 + 工序标准切快照、**卷算保留实时读**（CostRollupService 无工单上下文 + FIRMED 材料标准冻结先行；与 MA4 §7 字面偏离显式声明）；D3 AUTO_UPGRADE = 全局键两值（逐物料粒度 successor）；调用面全集 census 落盘（5 消费方 + 三方法内部消费 + submit 路径 + R1.48 接线 + config 零 override）。
- **Phase 3（ORM 纯加性 + 增量重生成）**：三快照实体（propId 1 连续、全业务列可空无默认）+ WorkOrder 2 列（45/46 空闲实证）+ to-many `bomSnapshot`（cascade-delete）+ 外键 IDX（无 UK）；`mvn clean install -DskipTests` 生成链 EXIT 0 + 产物核对（Entity/_gen getter-setter/xmeta/DDL 三方言 `CREATE TABLE erp_mfg_work_order_bom_snapshot` + `SNAPSHOT_BOM_VERSION VARCHAR(50) NULL`/`SNAPSHOT_BOM_ID BIGINT NULL` 纯加性实证/BizModel/API Bean）。
- **Phase 4（快照复制 + 读侧切换 + config）**：`ErpMfgWorkOrderProcessor.doSubmit` 接线 protected step `snapshotBomOnSubmit`（DRAFT→SUBMITTED 同事务复制头+行+工艺行 + 写 snapshotBomVersion/snapshotBomId；幂等=快照已存在跳过；无 BOM LOG.warn 不阻断；来源 BOM 经 `wo.getBom()` to-one + `bomExpander.findDefaultBomOrNull` 零多余 daoFor）；`KitAvailabilityChecker` 增 `hasBomSnapshot`/`explodeRequirements(wo, qty)` 快照感知入口（check + approve 预留路径复用；AUTO_UPGRADE 分支 re-resolve 默认 BOM）；`ProductionVarianceCalculator` 三方法（sumBomOperationStandardMins/deriveStandardLaborRate/resolvePrimaryWorkcenterId）快照工艺行优先（无快照/AUTO_UPGRADE/快照空均正确回退；快照空=锁定空不回落实时）；`BomExpander` 增 `explodeFromSnapshot`（复用 expandLines 算法 + 环/深度检测，子级 BOM 实时递归）+ `loadLines`/`loadOperations` public 化；config `erp-mfg.bom-snapshot-strategy`（Constants 键 + ErpMfgConfigs 读取助手，空值=默认 LOCK_AT_CREATION）；快照读侧全经 ORM to-many 关系 getter（`wo.getBomSnapshot()`/`snap.getOperations()`）零 daoFor 新增面。
- **Phase 5（测试 + 文档 + 零回归）**：`TestErpMfgBomSnapshot` **9 组全绿**（①快照落库头/行/工艺行+版本 ②BOM 行编辑后齐套读快照不变[需求侧 4≠实时 10] ③工序标准编辑后 variance 标准工时 120≠1200 ④新建工单提交时点当前 BOM ⑤AUTO_UPGRADE re-resolve 10>7 部分齐套+不写回快照 ⑥幂等重复提交不重复快照+无 BOM 跳过 ⑦approve 预留经快照化[min(4,10)=4≠实时 10]+GraphQL findPage 冒烟）；既有 `_cases` 快照重录 4 类（TestErpMfgWorkOrderStateMachine/CompletionPosting/WorkOrderEndToEnd/CostFlowEndToEnd——新 seq 面 NEXT_VALUE 漂移 + 响应新列机械重录，RECORDING→CHECKING 核验）；erp-mfg-service **279 tests 全绿**（270 基线 + 9 新增零回归）；owner doc `bom-and-routing.md` §BOM 版本快照规则补实现注记（载体/时机/读侧切换范围/AUTO_UPGRADE 语义/卷算保留实时读声明/存量数据回退姿势）+ §实现注记 Non-Goal 清单移除「BOM 版本快照」+ `state-machine.md` §4 同步；arm-index P1-RC-009 → done (RC-R1.49) + roadmap RC-R1.49 行 → done ✅。
- **验证基线（全绿）**：`mvn test -pl module-manufacturing/erp-mfg-service` **279 tests 0 failures 0 errors** + `mvn clean install -DskipTests` 全量 BUILD SUCCESS + compliance checker **actual ≤ baseline**（**R2c 1408→1413 基线上调登记**：5 新站点 per-site 证据落 `compliance-baseline.md`[snapshotBomOnSubmit 3 处快照实体 newEntity/save + BomExpander.loadOperations 1 处 + ProductionVarianceCalculator 快照分支工作中心费率 1 处]，读侧零新增 daoFor 面；其余 18 规则零漂移）。
- **结束审计（2026-08-16）**：独立子代理 `ses_ff7753e24ffe0NqWzdLgEZEK3Q`（fresh session，未共享执行者上下文）执行——**首轮 FAIL**（五项文本/卫生级缺口：Closure Gates 未勾选 + Status Note 残留 / arm-index+roadmap R2c「1408→1412」数值漂移 / DebugSnapshotProbe 探针目录遗留 / AUTO_UPGRADE 无默认 BOM 回退语义与计划字面不符 / log 条目位置非实质）→ **全部修复落地**（Closure Gates 8/8 勾选 + Closure 段完成态注记 / 两处 1413 修正 / 探针目录删除 / `KitAvailabilityChecker.explodeRequirements` 无默认 BOM 防御回退快照 + test⑤ 回退断言 / 位置不实质不改）→ **同会话复审 PASS**（逐项复核 + 实测 9/9 + checker 零漂移，剩余缺口无）→ plan `2026-08-16-0904-1` **Plan Status: completed**。执行者未自我审计，门控由独立审计执行并落盘计划 Closure 段。
- **BUILD_VERIFY**：分域测试 + 全量构建 + checker 均绿。**VERIFY 复跑（2026-08-16 mission-driver）**：全 reactor `mvn test` **3456 tests 0 failures 0 errors 1 skipped** + `mvn clean install -DskipTests` BUILD SUCCESS + checker actual==baseline 零漂移（R2c=1413≤1413，全 19 规则基线一致）；`TestErpMfgBomSnapshot` 9/9 全绿（erp-mfg-service 279 tests 全绿，含快照族）。**full-green verification**。


### purchase 价格差异过账落地（RC-R1.50 / P1-RC-018，plan `2026-08-16-0424-1`，B 类预授权零 ORM 变更）

- **工作项**：执行 `docs/plans/2026-08-16-0424-1-rc-mr1-r1-50-pur-price-variance-posting.md` 两 Phase。落地 finding **P1-RC-018**（L1 `use-cases.md:130-145` UC-PUR-05「策略 ∈ {拒绝, 审批后接收, 接收并过账差异}」+「让步接收时存在过账行: 科目 == 价格差异科目 且 金额 == 差异 * 数量」——`PurAcctDocProvider.createFacts` AP_INVOICE 仅 3 行无 PPV 行 + `PurInvoicePostingDispatcher.buildEvent` 无差异数据 + `ThreeWayMatcher` 仅 1/3 策略）。**治理**：2026-08-12 批量裁决 B 类（createFacts 加 PPV 行纯代码逻辑，零 ORM 结构变更）降级预授权自动执行 + 独立 plan-audit（草案审查 3 轮收敛 acceptable）。
- **Phase 1（差异数据传递 + PPV 过账）**：**D1 裁决（选项 A）**——科目载体 = 新增 `1404 材料成本差异` seed 行（`erp_md_subject.csv` 44→45 纯加性行）+ `ACCOUNT_KEY_PRICE_VARIANCE` GL 映射键 + `SUBJECT_PRICE_VARIANCE="1404"` fallback（否决 1403 语义分裂 B）；**1403 拆分** = 1403 行金额 = TOTAL_AMOUNT − 差异 + PPV 行（|差异| 量值，涨价借 1404 / 降价贷 1404），合计恒等于原 TOTAL_AMOUNT（Dr Σ = Cr Σ 保持，否决「增行不减 1403」）；差异 0/键缺失走既有三行零变化；残留风险登记（极端降价致 1403 负金额借方边界）。`PurInvoicePostingDispatcher.buildEvent` 增差异键 `PRICE_VARIANCE_AMOUNT` 写入（`tryPost` 差异重载 + 写入门控：非空非零才写）+ `PurAcctDocProvider.createFacts` 1403 拆分分支。
- **Phase 2（策略可达性收敛）**：**D2 裁决（选项 A）**——`erp-pur.price-diff-strategy` config 键（默认空 = 拒绝族既有行为零变化；`POST_DIFFERENCE` = 「接收并过账差异」）；「审批后接收」= L1 三选一子集语义（strict 拒绝即该策略审批前置，独立 diff 审批流无 owner doc 契约支撑登记 Deferred successor）；`ThreeWayMatcher.match` 价格超容差分支策略化（POST_DIFFERENCE warn 放行含 strict 价格维度覆盖，数量维度语义不变，复用既有 `ERR_INVOICE_PRICE_MISMATCH` 零新 ErrorCode）+ 新增 `computeOverTolerancePriceVariance`（同回链路径同容差口径 Σ[(发票单价−订单单价)×数量]，复用既有加载器**零新增 daoFor 面**）+ `ErpPurInvoiceProcessor` 新 protected step `resolvePriceVariance` 门控 → `doPosting` 传差异 → `tryPost` 重载。
- **测试 + 快照**：`TestErpPurPriceVariancePosting` 9 组全绿（涨价 PPV 借 50 + 1403=50 拆分 + 借贷恒等 113==113 / 降价 PPV 贷 50 + 1403=100 / 容差内 2% 零差异三行不变 / 无 receiveLineId 跳过 / 订单价 0 跳过 / 无订单回链差异 0 / 默认拒绝族差异键不写零 PPV 回归 / strict 抛错不变保持 SUBMITTED / POST_DIFFERENCE 覆盖 strict 放行 + PPV）+ `_cases/` 快照录制（9 方法 RECORDING→CHECKING 全绿）；erp-pur-service **317 tests 全绿**（308 基线 + 9 新增零回归，含 TestErpPurThreeWayMatch/TestErpPurSettleThreeWayMatchRecheck/R1.11 超收校验）。
- **验证基线（全绿）**：`mvn clean install -DskipTests` 全量 BUILD SUCCESS + 全量 `mvn test` **6886 tests 0 failures 0 errors**（含 TestErpSeedDataIntegrity 种子完整性 45 行 subject + ErpAllFluxPagesTest/ErpAllWebPagesTest）+ compliance checker **exit 0 actual == baseline 零漂移**（R2c=1405 / R10=9 精确一致——差异计算复用 ThreeWayMatcher 既有加载器，零新增 daoFor/import 面）。
- **回填**：owner doc `three-way-match.md` §价格差异 + §不匹配的处理策略 补实现注记（策略选择 + PPV 载体 + 1403 拆分 + 测试）+ arm-index P1-RC-018 → `done (RC-R1.50)`（修复摘要 + 历史保留）+ roadmap RC-R1.50 → done ✅（**行字样改写**：旧「越界项须…双独立子 agent 批准 checkbox」→「第一批（2026-08-12 批量裁决 B 类降级为预授权自动执行，不再须 ask-first checkbox）」+ 文件头 2026-08-16 最后更新条目）+ 本日志条目。
- **Deferred（已裁定）**：独立 diff 审批流（「审批后接收」完整工作流，successor yes，触发条件 = 业务要求价格差异单独审批工作流时按 owner doc 契约补充 + 独立立项）；财务域成本核算消费 PPV（three-way-match.md:114 财务域处理，watch-only residual，successor yes）。
- **BUILD_VERIFY**：全量 `mvn clean install -DskipTests` + 全量 `mvn test` + 分域测试 + checker 均绿。**full-green verification**。
- **独立结束审计（2026-08-16）**：待独立子代理（新会话）执行——审计前 Closure Evidence 占位 + 审计门控 `[ ]` 实证（执行者未自我审计）。

### mfg 物料预留写路径落地（RC-R1.48 / P1-RC-008，plan `2026-08-15-2119-3`，B 类预授权零 ORM 变更）

- **工作项**：执行 `docs/plans/2026-08-15-2119-3-rc-mr1-r1-48-mfg-material-reservation-write-path.md` 四 Phase。落地 finding **P1-RC-008**（L1 `use-cases.md:90-104,144-156` UC-MFG-05 ①-④ + UC-MFG-08 ⑤-⑦ + UC-MFG-06 ⑧⑨⑩⑪ 预留写路径完全缺失）。**治理**：2026-08-12 批量裁决 B 类（inventory 已有 `ErpInvReservation`/`Line` 表结构完备 + mfg 侧跨域调用 → 降级预授权自动执行，零 ORM 结构变更）+ 独立 plan-audit（草案审查 2 轮 acceptable，ses_ffa618947ffeaXC0w2LJTa846R / ses_ffa56a4d0fferPtUsmcCMTDpPc）。
- **Phase 1 裁决**（落盘于 plan）：**D1** 超预留 = `erp-mfg.over-pick-warning` 默认 true LOG.warn 放行（否决默认拒绝/两态混合）+ **D2** 状态映射 = dict 五态承载（创建 OPEN / 消耗 PARTIALLY_CONSUMED/CONSUMED / 取消释放 CANCELLED / 完工释放 PARTIALLY_CONSUMED|CONSUMED；L1 ⑦ RELEASED 语义等价映射不新增字面态）+ **D3** `reservation-enabled` 默认 true（原 `reservation-on-approve` 并入，避免死配置键）+ **D4** BizModel 直接实现（注入 StockMoveBookkeeper 复用 updateBalanceWithRetry，不拆 per-mutation Processor）；四接线点 + config 声明位 + reverseApprove 保留预留 + close 路径登记 Deferred But Adjudicated 证据产出。
- **Phase 2 inventory 写接口**：`IErpInvReservationBiz` 增 3 purpose-built 写方法——`createReservation`（ReservationCreateRequest+ReservationLineRequest：头 status=OPEN + 行 reservedQuantity=min(requested,可用) + 余额 reservedQuantity+= 经 `StockMoveBookkeeper.updateBalanceWithRetry` 乐观锁+UK 冲突重试[A4.2.3 SP-3 义务]，同源单幂等）/ `releaseReservation`（sourceBillType+sourceBillCode+reason：未消耗部分全释放 + 余额 -= + 头状态 D2 映射）/ `consumeReservation`（consumedQuantity+= + 余额 -= + 超量 min 封顶；**行 reservedQuantity 固定初始预留量**——L1 UC-MFG-08「reservedQty−pickedQty」语义载体，D1 超预留公式一致）+ **no-op 语义**（查无预留静默返回 null 零写入，MINOR-4）+ 4 DTO；`ErpInvReservationBizModel` 实现（code `RSV-`+UUID，findOrNewBalance 镜像 StockMoveBookkeeper.findBalance 口径）；**执行期 BUG 修复**：子类 `daoProvider` 字段遮蔽基类 CrudBizModel 同名私有字段致注入落空（改 `daoProvider()` 基类访问器）。
- **Phase 3 mfg 四接线**：`ErpMfgConstants` 3 config keys + `SOURCE_BILL_TYPE_WORK_ORDER`；approve `doApprove` 增 protected step `createReservations`（config-gated + BOM 展开经 KitAvailabilityChecker 复用[M1：`aggregateRequirements`/`resolveBomId` 改 public + 新增 `explodeRequirements`] + 行仓库 `ErpMfgWorkOrderLine.sourceWarehouseId` 缺失跳过 LOG.warn 不阻断[M8] + 无 BOM `ERR_DEFAULT_BOM_NOT_FOUND` 跳过不阻断[M5] + uom 回退物料主数据[ORM 强制非空]）；cancel 增 `releaseReservations`（reason=CANCELLED）；完工 `ErpMfgWorkOrderReportCompletionProcessor` 达量后 `releaseRemainingReservations`（reason=COMPLETED，config auto-release-on-complete）；领料 `ErpMfgMaterialIssueConfirmProcessor.confirm` 增 `consumeReservations`（超预留 warnIfOverPick 按 D1 放行 + consume 前直读预留行算剩余量——对齐 KitAvailabilityChecker 跨域只读先例）；领料移动单主链 generateMove 零改动。
- **Phase 4 测试 + 零回归 + 回填**：`TestErpInvReservationWriteApi` 10 组（创建占用/min 语义/幂等/取消释放/完工部分释放/完工全领/消耗追踪/超预留封顶/no-op/多线程并发 lost-update 4+4=8 无丢失）+ `TestErpMfgReservationLifecycle` 9 组（①审核创建②取消释放③完工释放④领料消耗⑤超预留放行⑥config 关闭全链跳过⑦头状态终态断言 CANCELLED/CONSUMED/PARTIALLY_CONSUMED⑧旧数据 no-op⑨无 BOM 不阻断）；owner doc `material-reservation.md` Deferred→已实现（跨域调用点 + config 表映射[reservation-on-approve 并入] + D1-D4 + 残留边界声明）；arm-index P1-RC-008 → done (RC-R1.48) + roadmap RC-R1.48 行 done（旧越界项字样按 B 类裁决改写）+ **A4.2.3 行解锁注记**（回队触发条件满足）。
- **验证基线（全绿）**：`mvn test -pl module-inventory/erp-inv-service` **218/218**（208 基线 + 10 新增）+ `mvn test -pl module-manufacturing/erp-mfg-service` **269/269**（260 基线 + 9 新增）零回归 + `mvn clean install -DskipTests` 全量 BUILD SUCCESS + compliance checker **baseline-raise 登记**（R2b 230→233 / R2c 1399→1405 / R2d 34→35，per-site 证据落 `docs/audits/compliance-baseline.md` 注记——预留写接口三实体编排 + mfg Processor uom 回退/预留只读聚合，对齐 RC-R1.29/R1.33 先例）。

### finance 现金流量三分类 + 间接法落地（RC-R1.45 / P1-RC-007，plan `2026-08-15-2119-2`，A 类 ORM 批量授权）

- **工作项**：执行 `docs/plans/2026-08-15-2119-2-rc-mr1-r1-45-fin-cash-flow-classification.md` 四 Phase。落地 finding **P1-RC-007**（L1 `use-cases.md:334-336` UC-FIN-16 断言⑧「按科目现金流分类(经营/投资/筹资)调整 / 间接法: 净利润 + 非现金项目 + 营运资金变动」——`buildCashFlowDataset:314` 硬编码全 OPERATING + 间接法缺失 + `ErpMdSubject` 无分类字段）。**治理**：2026-08-12 ORM 变更批量裁决 A 类（Q3 纯加性 1 列）+ 报表读侧分类/间接法逻辑预授权 + 独立 plan-audit（草案审查 3 轮收敛 acceptable，ses_ffa61b383ffemOOkupqtuLoc15 / ses_ffa56b4bbffeTJc0YjYXm2GA63 / ses_ffa5020d4ffeMs6nETSz5wwLHz）。
- **Phase 1 裁决**（落盘于 plan）：**D1** 分类语义 = dict `erp-md/cash-flow-type` 四值（OPERATING/INVESTING/FINANCING/NON_CASH）+ 列可空无默认 + null/NON_CASH 回退 OPERATING（否决 column defaultValue B——改变物化行为；否决三值 C——间接法需非现金调整维度）+ **D2** 现金科目准入维持 `isCashSubjectCode` 前缀硬编码（cashFlowType 只作 section 分类维度；数据驱动准入重构 Deferred successor）+ **D3** 间接法数据源 = VoucherLine 聚合（否决 GlBalance 期初/期末差额：月中口径缺失 + P2-RC-008 波及）+ **D3 子裁决**（MAJOR-1）间接法三组件共享 voucher 头侧 `postingType notIn(BUDGET, COMMITMENT)` 过滤（`or(isNull, notIn)` 对齐 RC-R1.46 模式——BUDGET/COMMITMENT 影子凭证损益行 6601/6001 不得污染净利润）+ **D4** 返回契约 = `cashFlowStatementData` 单契约扩展（直接法 + 间接法行）+ `indirectCashFlowData` 姊妹方法 + 模板双数据集渲染（ds/indirectDs，直接法块与现金净增加合计零改动）。
- **Phase 2 ORM + dict + 种子**：`ErpMdSubject.cashFlowType`（propId 25，VARCHAR 20，可空无默认无索引无 UK——Q3 纯加性，18-23 审计列 + remark=24 不占用）；手写 dict yaml `cash-flow-type.dict.yaml`（对齐 `posted-status` 非 orm.xml `<dicts>` 先例，codegen 零 `CASH_FLOW_TYPE_*` 常量生成实证，常量落 `ErpFinConstants`）；种子 `erp_md_subject.csv` 补 `CASH_FLOW_TYPE` 列逐行分类（44 行：现金/经营类 OPERATING + 6602 折旧费用/6701 信用减值损失/6702 资产减值损失 → NON_CASH；1012/1031 生产种子不存在为 draft review 非门控观察，测试面补作 INVESTING/FINANCING 示例）；`mvn clean install -DskipTests` 生成链 BUILD SUCCESS + 四产物核对（orm.xml / Entity propId 25 / XMeta dict / DDL mysql+oracle+postgresql 三方言）。
- **Phase 3 三分类 + 间接法**：`buildCashFlowDataset` 硬编码 OPERATING → `sectionOfCashFlowType(subject.cashFlowType)`（null/NON_CASH 回退 OPERATING）+ section 排序（OPERATING→INVESTING→FINANCING）；`buildIndirectCashFlowDataset` = 净利润[损益类 net，INCOME credit−debit / EXPENSE+COST debit−credit，businessType!=PERIOD_CLOSE 排除] + 非现金项目[NON_CASH 损益科目 Σ(debit−credit)] + 营运资金变动[ASSET(DEBIT,非现金前缀,非160x) ∪ LIABILITY(CREDIT) Σ(credit−debit)；CREDIT 方向资产（1231/1602/1604）经 direction 规则排除防与 NON_CASH 加回重复计算]；`loadPostedVoucherLines(periodId, excludeShadowPostings)` 重载（vq 层过滤）；模板 `cash-flow-statement.xpt.xml` 新增间接法补充块（indirectDs 展开）+ section 样式。
- **Phase 4 测试 + 零回归 + 回填**：`TestErpFinReportRendering` 新增 4 组全绿——`testCashFlowThreeSectionClassification`（基础 seed 80 OPERATING + 1012 INVESTING 200 + 1031 FINANCING 50，总额 330 零回归）/ `testIndirectCashFlowDataset`（净利润 100 = 100+50−30−20，非现金 20，营运资金 |−20|（应收 −50 + 应付 +30），间接法净额 100 == 直接法经营恒等）/ `testCashFlowNullFallbackToOperating`（null 回退断言）/ `testIndirectExcludesShadowVoucherLines`（自包含 seed：实际 200 + BUDGET −500/+400 + COMMITMENT −100，过滤后净利润 200 非 0）；科目编码规避（5001/1012/1031/6602/1602 独立编码，禁 4103 撞生产语义）；基础 `seed()` 零改动 → 既有 6 方法 `_cases` 快照零重录（CHECKING 仅比对录制列，AutoTestCaseResultChecker.checkRowMatch 实证）；owner doc `multiple-accounting-schemas.md §账套查询与报表` 补实现注记（三分类 + 间接法口径 + postingType 过滤子裁决 + cashFlowType dict 语义 + 种子分类义务 + P2-RC-008/P2-RC-085 边界声明）。
- **验证基线（全绿）**：`mvn test -pl module-finance/erp-fin-service` **497/497**（493 基线 + 4 新增）+ `mvn test -pl module-master-data/erp-md-service` **146/146** 零回归 + 全量 `mvn test` BUILD SUCCESS（含 TestErpSeedDataIntegrity 种子完整性 + ErpAllFluxPagesTest/ErpAllWebPagesTest）+ `mvn clean install -DskipTests` 全量 BUILD SUCCESS + compliance checker **actual == baseline 零漂移**（R2c=1399 / R12a=69 / R12b=66 / R12c=40 精确一致——分类经 `line.getSubject()` to-one 惰性加载 + 间接法消费已加载 VoucherLine 内存数据，零新增 daoFor/import 面）。
- **回填**：arm-index P1-RC-007 → `done (RC-R1.45)`（修复摘要 + A 类授权 + D1-D4 裁决 + 测试/验证摘要）+ roadmap RC-R1.45 → done ✅（**行字样改写**：旧「须…双独立子 agent 批准 checkbox」→「2026-08-12 ORM 变更批量裁决 A 类…越界回落双独立子 agent 批准」，对齐 RC-R1.43 行先例）+ 本日志条目。
- **Deferred（已裁定）**：现金科目准入数据驱动化（isCashSubjectCode → cashFlowType 驱动，successor yes，触发条件 = 科目体系定制化需新增非标准现金科目）；影子凭证 postingType 过滤直接法侧（P2-RC-085 watch-only，间接法侧本行已自带过滤）；CLOSED 期间门控（P2-RC-008 out-of-scope，间接法不读 GlBalance）。
- **BUILD_VERIFY**：全量 `mvn clean install -DskipTests` + 全量 `mvn test` + 两分域测试 + checker 均绿。**full-green verification**。
- **独立结束审计（2026-08-16）**：task `ses_ff9428f18ffeMtkwr9qLWMQGIi` 全 8 门控 PASS，总体裁决 PASS，零 MAJOR；1 MINOR（erp-fin-service 测试总数声称 497 vs surefire .xml 聚合 489 差 8）经复跑 `mvn clean test` 实证 reactor 摘要恒 497（= 493 基线 + 4 新增）——差值归因 jqwik 容器级计数机制（surefire 控制台摘要含 jqwik property container 计数，xml testsuite 属性不含），三处回填数字与 reactor 权威计数一致无需修正；执行者未自我审计（审计前 Closure Evidence 占位 + 审计门控 `[ ]` 实证）。

### finance 反结账审计轨迹落地（RC-R1.44 / P1-RC-006，plan `2026-08-15-2119-1`，A 类 ORM 批量授权 + 双独立子 agent 批准）

- **工作项**：执行 `docs/plans/2026-08-15-2119-1-rc-mr1-r1-44-fin-reverse-close-audit-trail.md` 四 Phase（Phase 1 契约语义与载体裁决 + Phase 2 ORM 3 列 + Phase 3 契约变更/审计落库 + Phase 4 前端/E2E/测试/回填）。落地 finding **P1-RC-006**（L1 `use-cases.md:140` UC-FIN-07 RC-9「全程审计(记录反结账操作人/原因)」——reverseClose 无 reason 参数 + `ErpFinAccountingPeriod` 无审计列，A4.1.20 降级证据评估维持 P1 不降级[①无 reason 合规硬伤 + ②非专属时间戳 + ③易被覆盖 + ④无结构化日志]）。**治理**：2026-08-12 ORM 变更批量裁决 A 类（roadmap :34 授权 3 列或新实体）+ 会计过账逻辑[反结账]变更按 roadmap 预授权声明 + 2026-08-15 用户裁决升级「auto + dual-agent-approval」须**双独立子 agent 批准 + 独立 plan-audit**（计划 Draft Review Record 已含 2 轮独立草案审查收敛 acceptable）。
- **Phase 1 裁决**（落盘于 plan）：**D1** reason **必填**（选项 A 选定；缺失抛新 `ERR_REVERSE_CLOSE_REASON_REQUIRED`[erp.err.fin.period-close.reverse-close-reason-required]，守卫置于状态守卫前 fail-fast；否决可选 B 无法保证合规）+ **D2** `ErpFinAccountingPeriod` 加 3 可空列（选项 A 选定：`reversedBy`[propId 20, stdDomain=userId, VARCHAR 36] / `reverseCloseReason`[propId 21, VARCHAR 500] / `reverseCloseAt`[propId 22, tagSet=clock, TIMESTAMP]，对称 closedBy/closedAt + ErpFinPostingException 三列先例；否决 ReverseCloseLog 实体 B；多次反结账覆盖仅留末次记录 = 显式残留风险，多级 change-trail 归 Deferred successor）+ **D3** 结构化操作日志不纳入本行（tagSet audit 增强超 A 类授权 → Deferred 登记，successor yes）+ **D4** 前端最小接线（选项 A 选定：wizard main.page.yaml/main.flux.yaml reason 输入 + view.xml 行操作参数化 dialog 对齐 ErpFinBudgetScenario rollForward 范式）。调用面全集实证：生产 4 点（Facade + wizard 2 文件 + view.xml）+ action-auth（FNPT 权限名不变零改动）+ E2E 2 spec + 6 测试类 7 调用点（`TestErpFinAccountingPeriodStateMachineMatrix` 零调用点 grep 实证）。
- **Phase 2 ORM**：3 列 propId 20/21/22（14-19 审计列不占用），无 NOT NULL/默认/索引/UK（Q3 纯加性）；`mvn clean install -DskipTests` 生成链 BUILD SUCCESS + 四产物核对（orm.xml / Entity `_ErpFinAccountingPeriod.java` 3 getter/setter / XMeta propId 20/21/22 + i18n-en displayName / DDL mysql+oracle+postgresql 三方言同步，`_app.orm.xml` 同同步）。
- **Phase 3 契约 + 审计落库**：**双独立子 agent 批准落盘**（`ses_ff99a6ee4ffezeRyfTUCGcc4X5` + `ses_ff99a4b57ffeAo1NiyzVPy0fkk` 均 APPROVE——红冲/回开五行为零变更核验 + 全仓 reverseClose 调用面 census 完备[2 个 negative smoke spec 经论证 enforcement 前置免疫，traceability 追记] + 守卫/门控排序无害性分析 + @BizMutation 同事务保证 + compliance 零漂移论证）；契约链 `IErpFinPeriodCloseBiz.reverseClose(periodId, reason, context)` → BizModel Facade → `ErpFinAccountingPeriodReverseCloseProcessor`（reason 守卫在状态守卫前；setStatus(OPEN) 后、reopenModules 前写 `setReverseCloseReason/setReversedBy(facade.currentUserId())/setReverseCloseAt(CoreMetrics.currentTimestamp())`，红冲/回开逻辑零变更 git diff 实证）；6 测试类 7 调用点签名适配。
- **Phase 4 前端/E2E/测试/回填**：wizard main.page.yaml（dialog 增 input-text reason 必填 + mutation 传 `$reason` + disabledOn 空值禁用）/ main.flux.yaml（reverseClose 步增 input-text + args.data.reason）/ view.xml（行反结账 action 改参数化 dialog：reverseClose 表单 + simple 提交页 `@mutation:...?periodId=$id` withFormData，view 表单 status 段增 3 审计列展示）；E2E action spec reason 传参 + 审计三列回读断言 + visual spec reason 输入渲染核对（flux wizard `mountOnEnter` 需先「下一步」导航至反结账步——首跑失败修正）+ 清理路径 reason 传参；新增 `TestErpFinReverseCloseAuditTrail` 3 组（reason 落库三列断言[reversedBy=当前用户 IUserContext / reverseCloseReason=reason / reverseCloseAt 非空] + 缺失拒绝[错误码 + 状态保持 CLOSED_FINAL + 零审计写入] + 空白拒绝）；owner doc `period-close.md §反结账流程` 补审计轨迹实现注记（3 列 + reason 契约 + 写入范式 + kill-switch 关系 + 多次反结账覆盖残留 + 结构化日志 successor 声明）；i18n：生成 xmeta i18n-en displayName 已含 3 列英文 label + ErrorCode 中文描述内联（仓库 `*.i18n.yaml` 手改受保护规则拒绝，已注记）。
- **验证基线（全绿）**：`mvn test -pl module-finance/erp-fin-service` **493/493**（490 基线 + 3 新增，0 failures/0 errors）+ E2E 2 spec flux 全绿（`fin-period-close-wizard.action.spec.ts` + `visual.spec.ts`，webServer `-Derp-fin.reverse-close-approval-required=false`）+ `mvn clean install -DskipTests` 全量 BUILD SUCCESS + compliance checker **actual == baseline 零漂移**（R2c=1399 / R12a=69 / R12b=66 / R12c=40 精确一致——审计写入零新增 daoFor/import 面，纯实体 setter + 既有 helper）。
- **回填**：arm-index P1-RC-006 → `done (RC-R1.44)`（修复摘要 + 双 agent 批准指针，A4.1.20 降级证据保留为历史注记）+ roadmap RC-R1.44 → done ✅（含 D1-D4 裁决 + 批准 + 测试 + 验证摘要）+ 本日志条目。
- **Deferred（已裁定）**：结构化操作日志（NopSysChangeLog tagSet `audit,audit-save` 增强——超 A 类授权，successor yes，触发条件：审计/合规场景要求多级 change-trail 时按 ORM ask-first 流程立项）；完整反结账审批流（P1-MA3-036/P1-MA2-020 既有 successor，kill-switch 行为不变）。
- **BUILD_VERIFY 复验（2026-08-16 晚）**：mission-driver BUILD_VERIFY 复跑全绿——`mvn install -DskipTests -pl module-finance/erp-fin-service,module-finance/erp-fin-web -am` BUILD SUCCESS（增量，target/ 保留）+ `mvn test -pl module-finance/erp-fin-service` **493/493**（0 failures/0 errors）+ compliance checker EXIT=0（actual ≤ baseline 零漂移）。**full-green verification**。
