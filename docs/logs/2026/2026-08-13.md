# Development Log - 2026-08-13

### HR 域 ErpHrEmployee + ErpHrTimesheet 双轴实体级状态机 Bean 落地（M3.8 + M3.9，plan `2026-08-13-1430-2`）

- **工作项**：执行 `docs/plans/2026-08-13-1430-2-erphr-employee-timesheet-state-machine-beans.md` 三 Phase（按规则 14 合并同 owner doc `human-resource/state-machine.md` 的 Employee + Timesheet 两轴为一计划）。落地 hr 域 Employee + Timesheet 双轴实体级状态机 Bean（契约 `docs/architecture/entity-state-machine-bean.md`）+ BizModel/Processor 接线 + 三层测试 + 双轴四方对照 + 漂移裁定。姊妹范式 `2026-08-12-1118-3`（LeaveRequest/Contract 轴 Bean/接线/测试/Delta 范式，其 Deferred But Adjudicated 显式将 M3.8/M3.9 列为 successor）。M3.8/M3.9 均为 M3 无财务影响非保护域轴（实仓核实 Timesheet approve 不过账——工时归集到 projects/cost-collection 为 successor；薪酬计提过账在 Salary 独立轴 M4.63/64）。
- **Phase 1（双 Bean + 注册 + 层 1 矩阵）**：`ErpHrTimesheetStateMachine`（status 轴，3 动作 submit/approve/reject + submit 双源 {DRAFT,REJECTED} + 4 边 + 终态 {APPROVED,REJECTED}，REJECTED 为可恢复终态，**不编码 cancel**——dict 无 CANCELLED 值 + RC-R1.8 仅 3 动作）+ `ErpHrEmployeeStateMachine`（employmentStatus 轴，**退化解**：transitions() 空 + 零命名动作 writer；initial={ACTIVE,PROBATION} + terminal={RESIGNED,TERMINATED,RETIRED} 对齐 owner doc §3 + 集中化 `isTransferable`/`nonTransferableStatuses` 只读调动守卫）。两 Bean 严格无状态。`app-service.beans.xml` FQN-id 注册（沿用 LeaveRequest/Contract 范式）。层 1 矩阵测试 `TestErpHrTimesheetStateMachineMatrix` 11/11 + `TestErpHrEmployeeStateMachineMatrix` 7/7 绿（含 Timesheet 无 cancel 边断言 + Employee 退化解断言：三终态死状态不在 transitions/initial 但 isTerminal=true）。
- **Phase 2（BizModel 接线 + 层 3 回归）**：`ErpHrTimesheetBizModel`（submit/approve/reject 3 处内联 `Objects.equals` 来源态守卫改调 `timesheetStateMachine.assertCan<Action>` + `<action>TargetStatus()` 写回；common→`ERR_HR_TIMESHEET_ILLEGAL_TRANSITION` 映射 cause 保留；动态副作用保留原位：submit 的 totalHours 重算 + 24h 跨表校验 `ERR_TIMESHEET_DAILY_HOURS_EXCEEDED`、reject 的 reason 必填守卫 + remark 写入、审计字段、乐观锁）+ `ErpHrEmployeeBizModel`（`isTransferable`/`nonTransferableStatuses`/`requireTransferableEmployee` 改委托 `employeeStateMachine`）+ `ErpHrEmployeeTransferEmployeeProcessor`（调动只读守卫 `isTransferable` 同步委托 Bean）。**2 处初始态 ACTIVE 写入不调 assertCan***（RecruitmentHireProcessor:63 / RecruitmentBizModel:149，§9.2 选项 c 创建路径）。**transferEmployee 不改 employmentStatus**（保持）。层 3 既有回归全绿（`TestErpHrTimesheetFamily` 24h 校验 + submit/approve/reject 生命周期 + `TestErpHrEmployeeTransfer` 调动守卫 + `TestErpHrEmployeeReferences` 入职→ACTIVE + `TestErpHrLeaveEngine`/`TestErpHrPayrollSimulation`），证明错误码、3 边、24h 校验、调动守卫、入职→ACTIVE 均不变。
- **Phase 3（双轴四方对照 + 漂移裁定）**：双轴 10 维度对照。Timesheet 裁定 1 项 doc drift：owner doc §2 图表 `state-machine.md:199` 画 `DRAFT→CANCELLED` 但 dict 无 CANCELLED + RC-R1.8 仅 3 动作 → Fix（owner doc §2 图表 `:204` 补注已落地）。Employee 裁定：(i) 3 死状态（RESIGNED/TERMINATED/RETIRED）= intentional reserved（dict 值保留，successor 填充入边）；(ii) §3 终态对齐 Decision（Bean `terminalStatuses()` 对齐 §3 业务语义，死=无入边 vs 终态=业务终点不矛盾）；(iii) PROBATION 零 writer 不对称 Decision（归 initial 基于 §1 业务语义）；(iv) 退化轴 rule 9 替代方案记录（选定落地退化 Bean 而非 defer M3.8）。
- **验证基线（全绿）**：`mvn clean install -DskipTests`（全仓库 BUILD SUCCESS）+ `mvn test -pl module-hr/erp-hr-service`（214 tests, 0 failures：18 层 1 矩阵 + 既有层 3 含 TimesheetFamily/EmployeeTransfer/EmployeeReferences/LeaveEngine/PayrollSimulation）+ `bash docs/audits/nop-compliance-checker.sh`（R5=0/R11=0 无漂移）。路线图 M3.8 + M3.9 → done。
- **Deferred（已裁定带 successor）**：Timesheet cancel mutation（PM 要求工时表取消业务流落地时，dict 加 CANCELLED + ask-first）；Employee 离职/退休/转正 mutation（PM 要求正式工作流落地时，触及 nop-auth 用户禁用 ask-first）；hr 域其余状态轴（Salary approveStatus/paymentStatus = M4.63/M4.64；Survey Deferred CRUD 桩；DevelopmentPlan 死状态）；Delta 覆盖运行时实证（M5.3）；全局 CRUD 写锁（M0.1 §9 选项 c 显式排除）。

### 制造域 ErpMfgJobCard + ErpMfgMrpPlan 双轴实体级状态机 Bean 落地（M3.13 + M3.14，plan `2026-08-13-1430-1`）

- **工作项**：执行 `docs/plans/2026-08-13-1430-1-erpmfg-jobcard-mrpplan-state-machine-bean.md` 三 Phase（按规则 14 合并同组件同结果表面的 JobCard + MrpPlan 两轴为一计划）。落地制造域 JobCard + MrpPlan 双轴实体级状态机 Bean（契约 `docs/architecture/entity-state-machine-bean.md`）+ Processor/引擎接线 + 三层测试 + 双轴四方对照 + 漂移裁定。姊妹范式 `2026-08-12-1841-3`（Forecast 轴 Bean/接线/测试范式）。M3.13/M3.14 均为 M3 无财务影响非保护域轴（实仓核实 JobCard/MrpPlan 状态变更零过账副作用）。
- **Phase 1（双 Bean + 注册 + 层 1 矩阵）**：`ErpMfgJobCardStateMachine`（status 轴，6 状态变更动作 startJob/submitJob(2源)/completeJob/holdJob/resumeJob/cancelJob(3源) + 9 边 + recordWork validation-only 来源校验 {WIP,SUBMITTED} + 终态 {COMPLETED,CANCELLED}，**PARTIALLY_TRANSFERRED/MATERIAL_TRANSFERRED 预留死状态不编码任何边/不入终态集**）+ `ErpMfgMrpPlanStateMachine`（status 轴，3 动作 run(null|DRAFT)/complete(RUNNING)/firm(COMPLETED) + 3 边 + 终态 {FIRMED}，**CANCELLED 预留死状态排除 + 不编码 COMPLETED→DRAFT revert（清单漂移）**）。两 Bean 严格无状态。`app-service.beans.xml` FQN-id 注册（沿用 Forecast 范式）。层 1 矩阵测试 `TestErpMfgJobCardStateMachineMatrix` 13/13 + `TestErpMfgMrpPlanStateMachineMatrix` 9/9 绿（含 TRANSFERRED 死状态全动作拒绝 + CANCELLED 死状态拒绝 + 无 COMPLETED→DRAFT 边断言 + recordWork validation-only 不计入 transitions）。
- **Phase 2（Processor/引擎接线 + 层 3 回归）**：JobCard 6 status Processor + RecordWorkProcessor 注入 Bean，固定来源态/目标态判断改调 `assertCan<Action>`/`assertCanRecordWork` + `<action>TargetStatus()` 写回；`ErpMfgJobCardProcessor` 删除内联 `requireStatus`（含 `Objects.equals`），新增 cause-aware `illegalTransition` 重载（common 码作 cause，保持既有误命名 `ERR_INVALID_STATUS_TRANSITION`，归 watch-only successor）。MrpPlan formal 引擎 `MrpEngine.runMrp`（run 守卫 + complete 目标态）+ simulation 平行 writer `SimulationMrpEngine`（computed plan DRAFT→RUNNING→COMPLETED 双链，`:124`/`:195` 新建 DRAFT seed 按 §9.2(c) 不调 assertCan*）+ `MrpReleaseService.advancePlanToFirmedIfComplete`（firm 守卫，「全部 line 释放」动态前置保留原位）均注入 Bean 改调 `assertCanRun/Complete/Firm` + 目标态；common→`ERR_MRP_INVALID_PLAN_STATUS` 映射 cause 保留。动态副作用（recordWork TimeLog/报工累计/laborCost 回写、MRP 净需求计算/pegging、释放生成 WorkOrder/Subcontract/PurchaseOrder、乐观锁）保留原位。层 3 既有回归全绿（`TestErpMfgWorkOrderEndToEnd` JobCard happy-path + `TestErpMfgMrpEngine` DRAFT→RUNNING→COMPLETED + 非-DRAFT 重跑拒绝 + `TestErpMfgMrpEndToEnd` COMPLETED→FIRMED + 幂等拒绝 + `TestErpMfgMrpSimulation` 仿真 fork + `TestErpMfgScheduleToJobCard` 初始 OPEN），证明错误码、6 边 + recordWork 不改 status、cancelJob 3 源、formal+simulation 双 run/complete 链、FIRMED 释放时序均不变。
- **Phase 3（双轴四方对照 + 漂移裁定）**：双轴 10 维度对照（JobCard dict 8 值 ↔ owner doc §适用对象二 ↔ Bean 9 边 ↔ writer 含 6 Processor + recordWork + 初始 OPEN 写入 + CRUD；MrpPlan dict 5 值 ↔ owner doc §MRP ↔ Bean 3 边 ↔ writer 含 formal+simulation 双链 + release firm + 新建 DRAFT seed + CRUD），无新增死状态。四项漂移裁定：(i) **JobCard TRANSFERRED 死状态** intentional reserved（owner doc Deferred，successor = 转序/工序转移）；(ii) **JobCard WorkOrder 级联取消 doc drift** → Fix（owner doc `state-machine.md:200` 补注「目标行为未落地 + cancelJob 为独立命名动作 + successor」，实仓 `ErpMfgWorkOrderProcessor.cancel` 无 JobCard writer）；(iii) **MrpPlan CANCELLED 死状态** intentional reserved（owner doc Deferred，successor = cancelPlan）；(iv) **MrpPlan COMPLETED→DRAFT 清单漂移** → 登记漂移（`entity-state-axis-inventory.md:415` 删除线 + 漂移注记，Bean 不编码此不存在边，successor = revertMrpPlan）。
- **验证基线（全绿，两连绿确定性）**：`mvn clean install -DskipTests`（全仓库 BUILD SUCCESS）+ `mvn test -pl module-manufacturing/erp-mfg-service`（201/201 两连绿：22 层 1 矩阵 + 既有层 3 含 JobCard/MrpPlan/MrpSimulation/ScheduleToJobCard + 其余 mfg 集成）+ `bash docs/audits/nop-compliance-checker.sh`（exit 0，R5=@Inject private=0/R11=Processor 重复状态判断=0 无漂移）。路线图 M3.13 + M3.14 → done。
- **Deferred（已裁定带 successor）**：JobCard 转序/工序转移（PARTIALLY_TRANSFERRED/MATERIAL_TRANSFERRED 落地时）；JobCard WorkOrder 取消级联取消（PM 要求时，跨聚合业务行为 ask-first）；MrpPlan cancelPlan（CANCELLED 落地时）；MrpPlan COMPLETED→DRAFT revert（PM 要求 MRP 计划可重算时，业务行为变更 ask-first）；JobCard 错误码误命名重命名（work-order→job-card，PM/owner 要求语义对齐时开独立 Fix plan）；Delta 覆盖运行时实证（M5.3 最终跨域 Delta 覆盖回归）；全局 CRUD 写锁（M0.1 §9 选项 c 显式排除）。

### CRM 域 ErpCrmLead docStatus 实体级状态机 Bean 落地（M3.1，plan `2026-08-13-0945-3`）

- **工作项**：执行 `docs/plans/2026-08-13-0945-3-ercm-lead-state-machine-bean.md` 三 Phase。落地 crm 域 Lead 轴实体级状态机 Bean（契约 `docs/architecture/entity-state-machine-bean.md`）+ per-mutation Processor/Conversion Processor 接线 + 三层测试 + 四方对照 + convert 来源态 doc-drift Fix。姊妹范式 `2026-08-12-2142-2`（crm Event 轴 M2.2 首立 Bean 范式）。
- **Phase 1（Bean + 注册 + 层 1 矩阵）**：`ErpCrmLeadStateMachine`（docStatus 单轴，4 动作 qualify/lose/cancel/convert + 7 意图边含 convert {NEW,QUALIFIED}→CONVERTED + 终态 {CONVERTED,LOST,CANCELLED} + 只读元数据 `transitions()`/`terminalStatuses()`/`initialStatuses()`），严格无状态（零 DAO/IBiz/事务注入）。`app-service.beans.xml` FQN-id 注册（沿用 M2.2 `ErpCrmEventStateMachine` 范式）。层 1 矩阵测试 `TestErpCrmLeadStateMachineMatrix` 全绿（无重复边 + 从 NEW 可达全部声明状态 + QUALIFIED 不可回 NEW + `assertCanConvert` 仅拒 CONVERTED + 终态全动作拒绝 + 意图矩阵 vs 运行时宽放有意区分 + 初始/终态集）。
- **Phase 2（接线 + 层 3 回归）**：`ErpCrmLeadProcessor`（qualify/lose/cancel 3 处来源态守卫改调 `assertCanQualify/Lose/Cancel` + `*TargetStatus()` 写回；common→领域码 `ERR_LEAD_ILLEGAL_STATUS_TRANSITION` 映射 cause 保留）+ `ErpCrmConversionProcessor`（convert 接线 `assertCanConvert` 仅拒 CONVERTED + `convertTargetStatus()` 写回；幂等拒绝保持专属码 `ERR_LEAD_ALREADY_CONVERTED`，common 码作 cause）。动态守卫保留原位：leadType 校验、联系人/lostReasonId 必填、stageId 方向守卫 `validateStageDirection`/`validateMovable`、跨域报价单/客户创建 + relatedBill 写入、`requireOpportunityPartner`。
- **Phase 2 四方对照 + 漂移 Fix**：单轴对照（dict `erp-crm/lead-doc-status` 5 值 ↔ owner doc `crm/state-machine.md §Lead`（§2 矩阵 + §5 可达性）↔ Bean 元数据 ↔ 全部 writer 含创建写 NEW + 通用 CRUD §9.4），无 dict 死状态。**漂移 1（convert 来源态 doc-drift）→ Fix**：owner doc §2 原「QUALIFIED→CONVERTED」与代码「非 CONVERTED→CONVERTED 经 leadType 门控」（含 NEW→CONVERTED，`TestErpCrmLeadConversion.testAlreadyConvertedRejected` 以 NEW lead 证之）漂移 → 就地补正为 {NEW,QUALIFIED}→CONVERTED + 补注裁定结论 + 引用本计划。**漂移 2（运行时 vs 意图矩阵 latent gap，既有非本计划引入）→ watch-only residual**：`assertCanConvert` 运行时仅拒 CONVERTED 故 LOST/CANCELLED→CONVERTED 技术上允许，但意图矩阵限定 {NEW,QUALIFIED}→CONVERTED；裁定保持运行时宽放（避免行为变化），产品要求收窄须 successor/ask-first。
- **Decision（convert 来源态，路线图规则 5）**：分支 a（Bean 据实仅拒 CONVERTED，保持既有外部行为 + owner doc §2 doc-drift Fix）落地；分支 b（PM 要求 QUALIFIED-only 行为变化）移交 successor。
- **验证基线（全绿）**：`mvn clean install -DskipTests` BUILD SUCCESS + `mvn test -pl module-crm/erp-crm-service -am`（162 tests, 0 failures）+ `bash docs/audits/nop-compliance-checker.sh`（R5=0/R11=0 无漂移）。路线图 M3.1 → done。
- **Deferred（已裁定带 successor）**：convert latent gap（LOST/CANCELLED→CONVERTED 运行时宽放，产品要求收窄时开 successor）；全局 CRUD 写锁（M0.1 §9 选项 c 显式排除）；crm Event BizModel 死代码清理（M2.2 遗留 follow-up，crm 域低风险清理批次启动时）；Delta 覆盖运行时实证（M5.3 最终跨域 Delta 覆盖回归）。

### APS 域 ErpApsOperationOrder 实体级状态机 Bean 落地（M2.13，plan `2026-08-12-2142-3`）

- **工作项**：执行 `docs/plans/2026-08-12-2142-3-erpaps-operation-order-state-machine-bean.md` 三 Phase。落地 aps 域**首例**实体级状态机 Bean（契约 `docs/architecture/entity-state-machine-bean.md`）+ INLINE BizModel 接线 + 抢单回退 Processor 接线 + 三层测试 + 四方对照 + Delta 适用性。姊妹范式 `2026-08-12-1118-1`（合同单轴 INLINE + 多源 cancel）+ `2026-08-12-1841-1`（引擎/服务类写 status + 隐式门控 advance）。
- **Phase 1（Bean + 注册 + 层 1 矩阵）**：`ErpApsOperationOrderStateMachine`（status 单轴，4 动作 start/complete/cancel/revertToDraft + cancel 三源 {DRAFT,PLANNED,IN_PROGRESS} + 7 边含 DRAFT→PLANNED 引擎边（无 assertCan 守卫，仅供可达性分析声明）+ PLANNED→DRAFT 回退边 + 终态 {FINISHED,CANCELLED} + 只读元数据），建立域内首例 `statemachine/` 包。`app-service.beans.xml` FQN-id 注册（沿用既有 Processor 范式）。层 1 矩阵测试 `TestErpApsOperationOrderStateMachineMatrix` 9/9 绿（含 cancel 三源全集 + 终态全动作拒绝 + 引擎边跳过 assertCan 一致性核对 + 从 DRAFT 可达全部状态 + PLANNED→DRAFT 回退环）。
- **Phase 2（BizModel/Processor 接线 + 层 3 回归）**：`ErpApsOperationOrderBizModel`（INLINE 路径，注入 Bean，start/complete/cancel 3 处内联 `Objects.equals` 守卫改调 `assertCanStart/Complete/Cancel` + 目标态写回；cancel 三源经 Bean 正向枚举；common→领域码 `ERR_APS_OP_ILLEGAL_TRANSITION` 映射 cause 保留；动态守卫 requireEntity + 乐观锁保留原位；`updateSchedule` 参数校验非状态守卫保留不动）+ `ErpApsSchedulingInsertRushOrderProcessor`（回退循环 1 处改调 `assertCanRevertToDraft` + `revertToDraftTargetStatus()`；IN_PROGRESS 不可重排硬守卫 `:53-59` + 优先级选择 + 预留释放保留原位）。`illegalTransition` helper 收紧为 common→领域码映射（带 cause）。层 3 既有回归全绿（`TestErpApsOperationOrderStateGuards` 11 + `TestErpApsSchedulingEngine` 6 + `TestErpApsCapacityReservation` + scheduling/crud/cross-domain/demand-planning），证明错误码 + 参数、start 仅 PLANNED、complete 仅 IN_PROGRESS、cancel 三源、IN_PROGRESS 不可重排、容量预留获取/释放时序、抢单优先级规则均不变。
- **Phase 3（四方对照 + 引擎边界 Decision + Delta）**：单轴 10 维度对照（dict `erp-aps/operation-order-status` 5 值 ↔ owner doc `aps/state-machine.md §2 :21-42` 7 边 ↔ Bean `transitions()` 7 边 ↔ 全部 writer：BizModel 3 + InsertRushOrderProcessor 回退 1 + 引擎算法 DRAFT↔PLANNED + rush 规范化 + CTP 影子非持久化 + CRUD 路径 §9.4 + 测试 fixture），dict 无死状态，7 边全对齐无漂移。三条 Decision 登记：(i) **引擎算法写 status 不在 Bean 治理范围**（intentional architecture boundary，纯算法 POJO 按可行性写无守卫可集中，DRAFT→PLANNED 边仅供可达性分析）；(ii) **预留释放 Deferred** P1-MA2-077 MR1（watch-only residual）；(iii) **IN_PROGRESS cancel 审批 Deferred** P1-MA2-078（watch-only residual）。OperationOrder 轴 Delta 双加载实证（`ErpApsOperationOrderStateMachineDelta` 收紧 cancel 仅 DRAFT/PLANNED，移除 IN_PROGRESS 异常终止源，`assertCanCancel(IN_PROGRESS)` 基线放行/Delta 抛异常可区分）6/6 绿。
- **VFS Delta 隔离兜底（复用 hr/mfg 域范式）**：delta 层对 `app-service.beans.xml` 做文件级替换（非 XDsl 合并）+ `VfsConfigLoader._default` 静态单例不在测试类间清理，aps 域其余 `TestErpAps*`（StateGuards/SchedulingEngine/CapacityReservation 等）排在 DeltaOverride 之后会受污染（基线 Scheduling Processors/AtpCtpService 丢失 + cancel(IN_PROGRESS) 行为翻转）。对齐 hr/mfg 域范式：`TestErpApsOperationOrderStateMachineDeltaOverride` 加 `@BeforeAll`/`@AfterAll` 调 `VfsConfigLoader.registerDefault(null)` 强制重建 VFS 配置；经「DeltaOverride 强制首位 + 后续 StateGuards/SchedulingEngine/CapacityReservation」次序验证无污染。
- **验证基线（全绿）**：`mvn clean install -DskipTests`（全仓库 BUILD SUCCESS）+ `mvn test -pl module-aps/erp-aps-service`（51/51：9 层 1 矩阵 + 11 既有层 3 StateGuards + 6 SchedulingEngine + CapacityReservation/ScheduleManagement/CrudSmoke/CrossDomainIntegration/DemandPlanning + 3 BaselineIoC + 3 DeltaOverride）+ `mvn test -pl module-manufacturing/erp-mfg-service`（CRP load source SPI 跨模块消费无破坏 BUILD SUCCESS）+ `bash docs/audits/nop-compliance-checker.sh`（exit 0，R5=@Inject private=0/R11=Processor 重复状态判断=0 无漂移；R10=7 为既有基线非本计划引入，已核实变更文件零 REQUIRES_NEW/@Transactional）。路线图 M2.13 → done。
- **Deferred（已裁定带 successor）**：start/complete/cancel 容量预留释放（P1-MA2-077 MR1 落地时）；IN_PROGRESS cancel 审批工作流（P1-MA2-078 落地时）；全局 CRUD 写锁（M0.1 §9 选项 c 显式排除）；aps 其他状态轴（Schedule.status 独立轴待 M0.2 复核 / DispatchLog 审计日志）。

### 主数据域 ErpMdSupplierApproval 实体级状态机 Bean 落地（M2.1，plan `2026-08-12-2142-1`）

- **工作项**：执行 `docs/plans/2026-08-12-2142-1-erpmd-supplier-approval-state-machine-bean.md` 三 Phase。落地 master-data 域**首例**实体级状态机 Bean（契约 `docs/architecture/entity-state-machine-bean.md`）+ INLINE BizModel 接线 + 单 Processor 接线 + 三层测试 + 四方对照 + owner doc 漂移 Fix + Delta 适用性。姊妹范式 `2026-08-12-1118-1`（合同单轴）。
- **Phase 1（Bean + 注册 + 层 1 矩阵）**：`ErpMdSupplierApprovalStateMachine`（status 单轴，6 动作 apply/approve/probate/suspend/reinstate/reject + apply 多源 {null,REJECTED} + approve 多源 {APPLIED,PROBATION} + suspend 多源 {APPLIED,APPROVED,PROBATION} + 10 边 + **无严格终态**（REJECTED 经 apply 可恢复 → isTerminal 全 false / terminalStatuses 空集）+ 只读元数据），建立域内首例 `statemachine/` 包。`app-service.beans.xml` FQN-id 注册（沿用既有 Processor 范式）。层 1 矩阵测试 `TestErpMdSupplierApprovalStateMachineMatrix` 11/11 绿（含 apply null/REJECTED + 多源全集 + REJECTED 可恢复环 + 无严格终态断言）。
- **Phase 2（BizModel/Processor 接线 + 层 3 回归）**：`ErpMdSupplierApprovalBizModel`（INLINE 路径，注入 Bean，apply/approve/probate/reinstate/reject/doSuspend 6 处内联 `Objects.equals` 守卫改调 `assertCan<Action>` + 目标态写回；doSuspend 幂等 SUSPENDED 短路保留在 Bean 调用前；common→领域码 `ERR_INVALID_APPROVAL_STATUS_TRANSITION` 映射 cause 保留；动态守卫 requireQualificationValid + C3 MUTEX enforceNoOverlapIfEffective 保留原位）+ `ErpMdSupplierApprovalSuspendByPartnerProcessor`（doSuspend 1 处改调 Bean，幂等短路 + 批量跳过 REJECTED + daoProvider.updateEntity 保留）。`illegalTransition` helper 收紧为 common→领域码映射（带 cause）。层 3 既有回归 6/6 绿（`TestErpMdSupplierApprovalStateMachine` happy path 链 + illegal transitions + approveRequiresQualification + rejectFromApplied + suspendByPartnerSuspendsAllActive + findEffectiveByPartner），全 md-service 143/143 绿。
- **Phase 3（四方对照 + 漂移裁定 + Delta）**：单轴 10 维度对照（dict `erp-md/supplier-approval-status` 5 值 ↔ 跨域 owner doc `purchase/supplier-evaluation.md:54` ↔ Bean `transitions()` ↔ 全部 writer：BizModel 6 + Processor 1 + CRUD 路径 §9.4 + 测试 fixture），dict 无死状态。漂移裁定：(i) **跨域 owner doc** intentional doc organization（master-data 无 state-machine.md，supplier-evaluation.md 承载）；(ii) **REJECTED 可恢复性** doc 表达不完整 → Fix（owner doc 补 apply(null|REJECTED→APPLIED) + REJECTED 非严格终态注记）；(iii) **APPLIED→SUSPENDED 补充边** implementation-supplemented edge → Fix（owner doc 原列 APPROVED/PROBATION，实现含 APPLIED，补注）；(iv) **`defaultValue="10"` ORM 异常** watch-only residual（pre-existing，保护区 ask-first）。owner doc `supplier-evaluation.md:54-55` 补注落地。SupplierApproval 轴 Delta 双加载实证（`ErpMdSupplierApprovalStateMachineDelta` 收紧 approve 仅 APPLIED，`assertCanApprove(PROBATION)` 基线放行/Delta 抛异常可区分）6/6 绿。
- **验证基线（全绿）**：`mvn clean install -DskipTests`（全仓库 BUILD SUCCESS）+ `mvn test -pl module-master-data/erp-md-service`（143/143：11 层 1 矩阵 + 6 既有层 3 SupplierApproval + 10 DateRangePilots + 6 Delta + 其余 md-service）+ `bash docs/audits/nop-compliance-checker.sh`（exit 0，R5=0/R11=0 无漂移）。路线图 M2.1 → done。
- **Deferred（已裁定带 successor）**：`defaultValue="10"` ORM 异常（master-data 数据模型清理时 ask-first）；`findActiveByPartner` 死代码清理（master-data 下次代码整理时）；master-data 独立 state-machine.md（master-data 增独立多步审批实体时评估）。

### 制造域 ErpMfgForecast 实体级状态机 Bean 落地（M2.19，plan `2026-08-12-1841-3`）

- **工作项**：执行 `docs/plans/2026-08-12-1841-3-erpmfg-forecast-state-machine-bean.md` 三 Phase。落地制造域首例实体级状态机 Bean（契约 `docs/architecture/entity-state-machine-bean.md`）+ 接线 + 三层测试 + 四方对照 + Delta 适用性。
- **Phase 1（Bean + 注册 + 层 1 矩阵）**：`ErpMfgForecastStateMachine`（status 轴，2 动作 approve/cancel + cancel 多源 {DRAFT,APPROVED} + 3 边 + 终态 {CANCELLED}，**CONSUMED 预留死状态不编码任何边/不入终态集** Decision A）+ 只读元数据 `transitions()`/`terminalStatuses()`/`initialStatuses()`，建立域内首例 `statemachine/` 包。`app-service.beans.xml` FQN-id 注册（沿用既有 Processor 范式）。层 1 矩阵测试 `TestErpMfgForecastStateMachineMatrix` 8/8 绿（含 cancel 多源 + refuse-terminal + refuse-dead-state(CONSUMED) + CONSUMED 无边/不入终态集断言）。
- **Phase 2（BizModel 接线 + 层 3 回归）**：`ErpMfgForecastBizModel` approve/cancel 两处内联 `Objects.equals(status, FORECAST_STATUS_*)` 矩阵判断删除，改调 `stateMachine.assertCanApprove/Cancel(from)` + `<action>TargetStatus()` 写回；`@Inject ErpMfgForecastStateMachine`（包级，非 private）。BizModel 捕获 Bean common 层非法边映射领域 `ERR_FORECAST_ILLEGAL_STATUS_TRANSITION`（forecastCode/currentStatus/expectedStatus 参数不变，common 码作 cause）。层 3 既有回归全绿：`TestErpMfgForecastSource.testStateMachineApproveCancel`（approve/cancel + 非法迁移码）+ MRP 聚合用例 + `TestErpMfgForecastCrudSmoke` —— 证明错误码 + 参数、cancel 多源、refuse-terminal、CONSUMED 只读引用均不变。
- **Phase 3（层 2 四方对照 + CONSUMED Decision A + Delta）**：单轴 10 维度对照（dict `erp-mfg/forecast-status` 4 值 ↔ owner doc `mrp.md:88,90` + `state-machine.md:287` ↔ Bean `transitions()` ↔ 全部 writer：approve/cancel INLINE BizModel + 只读 filter 引用 `DemandAggregator:175`/`ErpMfgReportBizModel:403` + CRUD 路径 §9.4），dict 无新增死状态。CONSUMED 处置与 owner doc Decision A 完全一致（保留 dict 值、零 writer、Bean 不编码边、Deferred successor = consume mutation）。Forecast 轴 Delta 双加载实证（`ErpMfgForecastStateMachineDelta` 收紧 cancel 仅 DRAFT，`assertCanCancel(APPROVED)` 基线放行/Delta 抛异常可区分）6/6 绿。
- **VFS Delta 隔离兜底（复用 hr 域范式）**：delta 层对 `app-service.beans.xml` 做文件级替换（非 XDsl 合并）+ `VfsConfigLoader._default` 静态单例不在测试类间清理，mfg 域 ~20 个 `TestErpMfg*` 排在 DeltaOverride 之后会受污染（基线 bean 丢失放大跨模块偶发不稳定）。对齐 hr 域 plan 2026-08-12-1118-3 范式：`TestErpMfgForecastStateMachineDeltaOverride` 加 `@BeforeAll`/`@AfterAll` 调 `VfsConfigLoader.registerDefault(null)` 强制重建 VFS 配置，使隔离不依赖 fork 调度。
- **验证基线（全绿，两连绿确定性）**：`mvn clean install -DskipTests`（全仓库 BUILD SUCCESS）+ `mvn test -pl module-manufacturing/erp-mfg-service`（179/179：8 层 1 矩阵 + 既有层 3 含 4 ForecastSource + 3 ForecastCrudSmoke + 3 BaselineIoC + 3 DeltaOverride + 其余 mfg 集成）+ `bash docs/audits/nop-compliance-checker.sh`（exit 0，R5=0/R11=0 无漂移）。路线图 M2.19 → done。
- **Deferred（已裁定带 successor）**：consume 命名动作（预测消费回写需求上线时，触及 dict 行为 ask-first）；制造域其他状态轴（WorkOrder/JobCard/MrpPlan/SubcontractOrder = M4.35-/M3.13/M3.14/M4.37-）；全局 CRUD 写锁（M0.1 §9 选项 c 显式排除）。
