# Development Log - 2026-07-17

### 2026-07-17（plan 2026-07-17-0900-1 结束审计第二轮清零 — Round 2 closure audit sweep）

- **背景**：`1449-1` 第一轮已清零截至 2026-07-14 14:34 的 24 份 deficient 计划。该截止后新完成的 2 份计划重新引入 rule-11/12 缺陷（门控假勾选 ↔ 证据 pending / executor self-audit）。本计划第二轮清零——独立子代理冷重播补审 + 文档级一致性回填，零生产代码变更。

- **Phase 1 增量清单复核**：以 `git log --since=2026-07-14 14:34` + 全量遍历 `docs/plans/2026-07-{14,15,16,17}-*.md` 的 `Plan Status` + `Auditor / Agent` 行，定位扫描截止后新完成/finalized 计划共 10 份（剔除 superseded 1 份）。逐份核实独立审计信号——8 份已合规（1449-1 自身 meta-audit / 1825-1 / 1934-1 / 2256-2 / 2246-1 / 0012-1 / 2134-1 + 2246-2 superseded），**确认缺陷 2 份**：`2256-1`（`Auditor: <待独立子代理（新会话）执行>` 占位 + 门控 7 已勾 `[x]` 双重违规）、`1022-1`（`Auditor: self-audit` 执行者自我审计违规）。与 Current Baseline 零差异。

- **Phase 2 独立结束审计执行**：为 2 份各 spawn 一个独立子代理新会话（`ses_0925d8d91ffeg7SFaGOe5UFp2O` for 2256-1、`ses_0925d2694ffeh3QRoUDhPWzfDE` for 1022-1，均冷重播无执行者上下文），8 维度核实（不盲信 `[x]`）：
  - **`2256-1` → PASS_WITH_NOTES**：0 BizModel 残留 `@SingleSession`（仅 javadoc 引用合规）、0 残留 import、`ErpFinPostingProcessor.java:115,198` 2 处 code-level 保留、5 Processor javadoc 全更新、`tools/check-bizmodel-annotations.mjs` 输出 `OK: 361 BizModel files checked, 0 violations`、3 finance 测试类抽查 anti-hollow 真实 `ormTemplate.runInSession(...)` 包裹（共 109 测试文件）。
  - **`1022-1` → PASS_WITH_NOTES**：18 模块 ORM 全含 tagSet、clock=170（与 ~165 申明吻合）、var=273（其中 ~19 计划枚举 sourceBillCode/relatedBillCode 全部命中 + ~254 commit 253fcdeb8 捆绑邻近工作）、DATE 类型 validFrom/validTo cleanup 验证、xmllint 解析通过。
  - **2/2 PASS_WITH_NOTES，0 FAIL** → 全部走回填路径，无确认生产缺陷须开 successor。

- **Phase 3 一致性回填 + 顺手订正**：
  - `2256-1` Closure 段：替换 `<待独立子代理（新会话）执行>` 占位为真实独立审计块（`Independent Closure Audit (2026-07-17-0900-1 batch)`），门控 7 现合法满足。
  - `1022-1` Closure 段：self-audit 行后追加独立审计裁决块；同步修订 Status Note var 计数（19 → 19 计划枚举范围 + 273 实时仓库实测，含 ~254 捆绑邻近 tagSet 补全工作），消除 rule-11 文本不一致。
  - `docs/backlog/README.md` P8 HR 引擎行 `ready`→`✅ done`（对齐 `2026-07-10-1100-7` 实际 `completed`），消除 1 行文档漂移。
  - 无 FAIL 裁决 → 无 successor 计划开立。`1022-1` 审计的 4 DATE clock 列（crm:217/218/549, hr:1704）与捆绑 commit 归属为非阻塞观察，不构成确认缺陷。

- **范围纪律**：纯文档级一致性修复（计划 Closure 段 + README 1 行 + 本日志）。零 ORM/契约/种子/config/生产代码变更，故无 `mvn`/`playwright` 全量重跑（与本计划 Closure Gates 申明一致）。

Skill: none

### 2026-07-17（plan 2026-07-16-2134-1 全域单元测试验证 + Phase 5 两处回归修复）

- **背景**：计划原标记 completed 并声明 BUILD SUCCESS，但未对全部受影响 service 模块逐一比对 clean-HEAD 单测基线。本次 mission-driver 验证执行全量构建+单测，发现 Phase 5 引入 2 处真实回归，已修复。

- **验证方法**：对每个受 DDD 变更影响的 service 模块（finance/inventory/purchase/sales/assets/cs/crm/notify），先在工作树运行 `mvn test`，再用 `git stash --include-untracked` 切回 clean HEAD 运行同一模块单测，逐模块比对 errors/failures 计数。

- **预存失败（非本计划引入，全模块与 clean HEAD 完全一致）**：
  - finance 27 errors / inventory 9 errors / purchase 17 errors+1 failure / sales 18 errors / assets 10 errors / crm 2 errors / cs 17 errors —— 全部为日期翻转快照失败（`value=2026-07-17 expected=2026-07-15` 及 ±2 天偏移的 BUSINESS_DATE/VOUCHER_DATE/START_DATE/VALID_TO 等），`TestErpPurRequisitionApproval.testReqIllegalTransitionRejected` 为已知预存失败（计划结束审计已记录）
  - notify 18 tests 0 errors（全绿）
  - 所有 DDD 实体方法相关测试（TestErpInvCostAdjust/TestErpSalOrderApproval/TestErpAst* 等）在变更后与 clean HEAD 均一致通过

- **回归 1 修复 — ErpSalPricingRuleEngine（sales）**：
  - 根因：Phase 5 将 `new ErpSalOrderLine()` 改为 `daoProvider.daoFor(ErpSalOrderLine.class).newEntity()` 并注入 `@Inject IDaoProvider`，但该引擎 Javadoc 明确标注"纯函数式/调用方负责持久化"，`TestErpSalPricingRuleEngine` 直接 `new ErpSalPricingRuleEngine()` 无 IoC → `daoProvider` 为 null → `testGiftLine` NPE（clean HEAD 该测试全绿）
  - 修复：赠品行是评估快照（调用方持久化），与 FunnelAggregationEngine 内存聚合同模式 —— 还原为 `new ErpSalOrderLine()` + 注释说明，移除未注入的 `@Inject IDaoProvider` 字段及 imports（计划对此案例的分类有误，本应同 FunnelAggregationEngine 一并排除）
  - 验证：TestErpSalPricingRuleEngine 10 tests 0 errors；sales 模块回到 18 errors（= clean HEAD，较修复前 19 少 1）

- **回归 2 修复 — ErpCsSurveyBizModel.createSurvey（cs）**：
  - 根因：Phase 5 重构为 `saveEntity`→`survey.getTicket()`→`updateEntity` 流程，`updateEntity(survey,...)` 报 `nop.err.orm.dao.update-entity-not-managed`（saveEntity 后实体仍为 ORM "new" kind，不可 update），导致 `ErpCsTicket__resolve`（resolve 触发 createSurvey 副作用）+ `ErpCsSurvey__createSurvey` 共 4 个测试失败（clean HEAD 该测试类全绿）
  - 修复：所有字段在 saveEntity 前设置完毕 → 单次 `saveEntity` → 经 ORM to-one `survey.getTicket()` 懒加载校验工单存在（实体已入 session）→ 移除失败的 `updateEntity`；保留计划目标（无 daoFor 跨聚合访问 + 无与 ErpCsTicketBizModel 循环依赖）
  - 验证：TestErpCsTicketSlaCsat 12 tests 0 failures；cs 模块回到 17 errors / 0 failures（= clean HEAD，较修复前多 4 failures）

- **最终验证**：`mvn clean install -DskipTests` 全 154 模块 reactor BUILD SUCCESS（1:37 min）。两处修复后全部受影响模块单测计数与 clean HEAD 完全一致，零回归。注：预存日期翻转快照失败非本计划引入（环境日期推进所致），非 full-green 状态。

Skill: nop-backend-dev, nop-debugging

### 2026-07-17（执行 DDD 实体方法上提与 daoFor() 收敛计划 — plan 2026-07-16-2134-1）

- **背景**：差距分析 `docs/analysis/2026-07-16-ddd-implementation-gap-analysis.md` 发现 Gap A（338 实体贫血，59 个状态方法在 21 Processor 重复）+ Gap B（daoFor Type 1+4 反模式）+ 防护机制缺口。本计划全 6 阶段执行完成。

- **Phase 1 决策与基础设施**：5 个 dao 层状态常量接口创建（`ErpPur/ErpSal/ErpFin/ErpAst/ErpInv DocStatus`），service 层 `ErpXxxConstants` 通过 `extends` 复用保持向后兼容。Decision D1 选择轻量接口 extends 方案否决"实体类内 static final"/"全量 Constants 下沉"/"硬编码字面量"三替代方案。

- **Phase 2-3 实体状态方法上提**：
  - 21 个实体添加状态方法（`isApproved`/`isRejected`/`isCancelled`）：purchase 6（Order/Requisition/Invoice/Return/Receive/Payment）+ sales 6（Order/Quotation/Invoice/Return/Receipt/Delivery）+ finance 3（ExpenseClaim/EmployeeAdvance 全三方法；BadDebt 仅 isApproved，用 approvalStatus 而非 approveStatus）+ assets 5（Merge/Split 仅 isApproved/isCancelled；ValueAdjustment/Disposal/AssetCapitalization 全三方法）+ inventory 1（ErpInvCostAdjust）
  - 21 Processor 中 `isAlreadyApproved`/`isAlreadyRejected` 方法定义全部删除，调用点改为 `entity.isApproved()`/`entity.isRejected()`；`validateNotCancelled` 保留在 Processor（含异常抛出）但内部改用 `entity.isCancelled()`
  - ErpInvCostAdjustProcessor.validateNotCancelled 签名补齐 IServiceContext 参数统一

- **Phase 4 daoFor Type 1 ORM 导航替代**：6 处 `loadLines`/`loadOrderLines` 改为 ORM 导航 `entity.getLines()`（purchase Order/Invoice/Requisition/Return/Receive Processor + ErpSalOrderBizModel）；ErpPurReceiveProcessor.loadOrderLines 保留并加注释（跨聚合访问，仅 orderId 无实体）

- **Phase 5 daoFor Type 4 + new Erp*() 修复**：
  - `ErpCsSurveyBizModel.createSurvey` 重构：先 saveEntity 后 `survey.getTicket()` ORM 导航校验，移除 `daoFor(ErpCsTicket.class)` 跨聚合访问
  - 4 处 `new Erp*()` 改为 `dao.newEntity()`/`daoProvider.daoFor().newEntity()`：ErpSalPricingRuleEngine（+ 注入 IDaoProvider）、NotificationDispatcher（+ 注入 IDaoProvider）、ErpSysNotificationBizModel ×2
  - FunnelAggregationEngine 经验证为纯内存聚合引擎（测试直接 new 构造、无 DI）→ 加注释保留 `new ErpCrmFunnelStageMetrics()`

- **Phase 6 防护机制**：
  - `nop-backend-dev/SKILL.md` 第二层自检表新增 E1（状态方法上提）/E2（ORM 关系 getter 优先）/E3（daoFor 注释原因）三项
  - `nop-compliance-checker.sh` 新增 R11 规则——Processor 中 `isAlreadyApproved`/`isAlreadyRejected` 定义计数（执行后=0）

- **验证**：`mvn clean install -DskipTests` 全 154 模块 reactor BUILD SUCCESS（1:40 min）；compliance checker R11=0、R3 降至 17（仅余合法 DTO/support/Engine 类）；预存测试失败经 git stash 验证非本计划引入（`TestErpPurRequisitionApproval.testReqIllegalTransitionRejected` + 日期相关快照失败 `value=2026-07-17 expected=2026-07-15`）

- **Deferred（已裁决）**：daoFor 类型 2（同域子实体 ~300-350 处）/ 类型 3（Processor 架构约束 ~150-200 处）/ 类型 5（看板只读聚合 ~80-120 处）/ 类型 6（历史残留 ~100-150 处）均不在本计划范围；实体纯计算逻辑按需上提（无具体候选清单）；Processor 共享泛型基类（实体方法上提后收益不再显著）

- **范围外文件**：零 ORM 模型/契约/config 变更；零 view.xml 变更；仅 Java 实体类、Processor、BizModel、Engine、Dispatcher、SKILL.md、compliance checker、plan/analysis 文档

Skill: nop-backend-dev
