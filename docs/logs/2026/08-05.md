# Development Log - 2026-08-05

### 2026-08-05（AUDIT — plan 2026-08-05-2200-3 projects-F2 预算与 DAG 需求符合性审计）

- **任务**：执行 A1.35 只读审计（`docs/plans/2026-08-05-2200-3-rc-ma1-a1-35-projects-f2-budget-dag.md`），对 UC-PRJ-04/05 五级追踪。Skill: `multi-dimensional-audit-prompt`。**只读审计——零生产代码变更**。projects 域**第二批 RC 切片**（projects 域共 3 切片 A1.34/A1.35/A1.36 之 2，本切片覆盖项目预算 STRICT 超支拦截 + 任务依赖 DAG 成环校验 2 UC）。
- **结论**：2 UC 逐矩阵行 + 候选缺口逐条复核。整体裁决：**接受 on 主路径 + 零 P0、1 新 P1、1 新 P2、6 复用 reuse**。
  - UC-PRJ-04 项目预算 STRICT 超支拦截 = **接受 on ①工时时机主路径 + ③④ STRICT/WARNING 双模式[默认 WARNING] + P1 on ①采购/报销子维度 + P2 on ②余量公式**（`BudgetChecker.check:44-69` config-gated STRICT 抛 `ERR_BUDGET_EXCEEDED`/WARNING LOG.warn + 默认 WARNING `ErpPrjConfigs` + 工时路径唯一调用点 `ErpPrjTimesheetSubmitProcessor.runBudgetCheckHook:103-104` + `TestErpPrjBudgetAndCollection` 7 @Test 强测）。**P1 = new P1-RC-051** 预算检查采购/报销两时机缺失（grep 全 `module-projects` `BudgetChecker|budgetChecker.check|runBudgetCheck|budgetCheck` **仅 2 文件命中**[BudgetChecker 定义 + TimesheetSubmitProcessor 单调用]，采购 Processor + 报销 Processor 零 projects BudgetChecker 调用；finance `ErpFinExpenseClaimProcessor.runBudgetCheckHook:199-211` + purchase `ErpPurOrderProcessor.runBudgetCheckHook:177-189` 存在但调 finance `IErpFinBudgetControlBiz.check(subjectId, periodId)` 财务预算非 projects `BudgetChecker.check(projectId)` 项目预算，**不同控制点不同维度**；owner doc `cost-collection.md §3.3:127-131` 显式三时机表与 L1 一致未声明 Deferred→§4 三判据均不成立→P1 强制实现，**纯 BizModel 代码逻辑修复预授权不触 §5 ask-first**；与 P1-RC-050 ON_HOLD 费用门控[UC-PRJ-09]互补一次性协同修复 + 与 P1-RC-049 物料归集 successor 衔接[采购路径被检查对象尚不存在→合并修复触发条件]）。**P2 = new P2-RC-049** 余量公式缺"已承诺成本"项（`BudgetChecker.check:52-58` 二项式 `used+addAmount>total`，第三项 commitment 零实现；owner doc `cost-collection.md §3.1:113-116` documented simplification 但 git log 全 AI commits 无人工批准痕迹→§4 三判据(ii)不满足，声明 Q4=(a) 张力，主路径已归集成本拦截可用 + projects 域无生产承诺源[物料/分包归集缺失 P1-RC-049]致第三项实际为零，watch-only）。
  - UC-PRJ-05 任务依赖 DAG 成环校验 = **接受 on 全部 4 验收标准**（`TaskDependencyValidator.detectCycle:44-86` 与 task-dag.md §2.1 算法 1:1[dependsOnId null→放行 / taskId==dependsOnId→ERR_TASK_SELF_DEPENDENCY 自环优先 / visited HashSet+chainOrder+cursor 上行链 / depth++>maxDepth→ERR_TASK_DEPENDENCY_DEPTH_EXCEEDED / cursor∈visited→ERR_TASK_DEPENDENCY_CYCLE] + 跨项目校验 `ErpPrjTaskBizModel.validateDependency:88-94`[ERR_TASK_DEPENDENCY_CROSS_PROJECT] + 前置未完成 config-gated `validatePredecessorDone:172-193` STRICT `ERR_TASK_PREDECESSOR_NOT_DONE`/WARN + 任务 4 态状态机[startTask/completeTask/blockTask/unblockTask] + `TestErpPrjTaskDependency` 319 行强测全路径覆盖 + E2E `projects-task.action.spec.ts`；**复用 A2.13 §2.5 已证实行为**不重验，无新 finding）。
- **产出**：报告 `docs/audits/2026-08-05-2200-3-rc-ma1-a1-35-projects-f2-budget-dag.md`（9 段齐全，59KB）；`arm-index.md` 新增 2 finding（P1-RC-051 + P2-RC-049，序号续 A1.34 P1-RC-050/P2-RC-048）+ A1.35 报告清单行 + A1.35 RC 交叉引用注记 + A2.13/P1-MA2-069/P1-MA1-010/022/P1-RC-003/P1-MA2-084/P1-RC-049/P1-RC-050/P2-RC-048 复用裁决（grep arm-index 全分区 prj budget/check/strict/warning/commitment/predecessor/dag/cycle 后确认新根因）；`requirement-compliance-roadmap.md` A1.35 todo→done。复用 A2.13（任务 4 态 + DAG 成环检测 + P1-MA2-069 resolved-via-deferral）已证实行为，只补需求视角差异（预算检查仅工时路径 / 采购+报销时机缺失 / 余量已承诺成本项 / WARN 默认值）。
- **验证**：只读审计无生产代码变更，build/test 回归风险为零。门控 = 报告 9 段完整性 + §8 checker actual vs baseline 实测（R1d/R2a/R2b/R2c/R2d 实测均 ≤ baseline；R3+ 因 pre-existing tooling bug[orm.xml 多行格式 commit 738810aa5 打破 R3 ENTITY_WHITELIST 单行 grep 管道致脚本早退，pre-existing 与本审计无关]未实测）+ 独立结束审计 **pass**（独立子代理 fresh session `MISSION_DRIVER:2026-08-04-224309-mission-driver`，未执行本计划任何 Phase，验证区全 PASS：五点一致性 / 交付物真实性[报告 9 段 + arm-index 三处更新经 grep 实仓核实] / 关键代码断言 ACCURATE[`BudgetChecker.check:44-69` 二项式 + grep 全 module-projects 仅 TimesheetSubmitProcessor:104 单调用确认 P1-RC-051 finding 真实] / 零 P0 声明 / Deferred honesty / Anti-Hollow / 独立草案审查已记录[Draft Review ses_030950687ffef7vOhORRRgkCqJ accept]；docs sync 缺口[本日志条目补齐]已修复）。

### 2026-08-05（AUDIT — plan 2026-08-05-2200-2 projects-F1 立项与成本归集需求符合性审计）

- **任务**：执行 A1.34 只读审计（`docs/plans/2026-08-05-2200-2-rc-ma1-a1-34-projects-f1-startup-cost-collection.md`），对 UC-PRJ-01/02/03/09 五级追踪。Skill: `multi-dimensional-audit-prompt`。**只读审计——零生产代码变更**。projects 域**首批 RC 切片**（projects 域共 3 切片 A1.34/A1.35/A1.36 之 1，本切片覆盖立项 + 工时人工成本凭证 + 多来源成本归集 + 暂停/关闭约束 4 UC）。
- **结论**：4 UC 逐矩阵行 + 候选缺口逐条复核。整体裁决：**接受 on 主路径 + 零 P0、3 新 P1、1 新 P2、4 复用 resolved、2 复用 todo MR1、1 接受 drift**。
  - UC-PRJ-01 项目立项 = **接受 on ①②③主路径 + P2 on requireReferenceable 单一咽喉未使用**（startProject:89-102 Facade + validateStartPreconditions:136-165 STRICT/WARN config-gated + DRAFT→OPEN 守卫齐全；`requireReferenceable:62-73` 实现[仅 OPEN 通过]正确**但生产代码 grep 零调用方**——咽喉未被消费）。**P2 = new P2-RC-048**。
  - UC-PRJ-02 工时人工成本凭证 = **接受 on ①②④⑤⑥主路径 + P1 on ③成本率三级降级**（项目 OPEN 校验 `validateProjectReferenceable:65-78` ✅ / 人工成本 `computeCostAmount:82-87` ✅ / 过账凭证 Dr 5101/Cr 2211 + projectId 维度 `ProjectCostCollectionProvider:67,71` ✅ / posted 条件回写 `approve:42-52` ✅）。**P1 = new P1-RC-048** 成本率三级降级——L1 `:38` 逐字「用户费率 > 角色费率 > 活动类型费率」，`CostRateResolver.resolve:40-67` 实为「单填 > 活动类型 > 全局默认」，用户/角色独立费率载体未实现[ORM `ErpPrjProjectUser.role` 纯文本]；Javadoc `:22-26` AI 声明 Non-Goal，git log 无人工批准痕迹[§4 三判据：AI 声明 ≠ 人工批准，对齐 A1.24 UC-AST-03 先例]。**接受 drift**：businessType `PROJECT_LABOR_COST`（L1 文本）vs `PROJECT_COST_COLLECTION`（实际枚举，行为满足）→ §9 真相源 drift 登记。
  - UC-PRJ-03 多来源成本归集 = **接受 on ③费用 ✅ + P1 on ①②④物料/领料/分包三类缺失**（LABOR `ProjectCostAggregator:59` ✅ + EXPENSE `ExpenseCostAggregator:189` ✅ = 2/4 生产 writer；MATERIAL 采购入库→项目 inventory 模块对 `ErpPrjCostCollection/projectId` 零引用 `InvPostingDispatcher.resolveBusinessType:152-178` 仅 emit PURCHASE_INPUT 等 / MATERIAL 领料→项目本仓为制造专用 WIP 写 1411/1401 从不写项目成本 / SUBCONTRACT 常量+PnL 读侧有但零生产 writer；owner doc `cost-collection.md §4.1:171` 显式 Non-Goal[successor] 但 git log 无人工批准）。**P1 = new P1-RC-049**。
  - UC-PRJ-09 暂停/关闭约束 = **接受 on ②③④⑤主路径 + P1 on ①ON_HOLD 费用门控**（closeProject 任务结束校验 `validateTasksFinished:80-95` 复用 P1-MA2-067 resolved ✅ / 冻结 + 历史保留[useLogicalDelete] ✅）。**P1 = new P1-RC-050** 工时路径 `validateProjectReferenceable:65-78` 拒非 OPEN ✅，**但费用路径 `ExpenseCostAggregator.refreshExpenseCost:56-126` 仅查 project==null 从不校验状态**——ON_HOLD/COMPLETED/CANCELLED 报销行仍归集，违反 L1 `:153`「拒绝新费用归集」。
- **产出**：报告 `docs/audits/2026-08-05-2200-2-rc-ma1-a1-34-projects-f1-startup-cost-collection.md`（9 段齐全，69KB）；`arm-index.md` 新增 4 finding（P1-RC-048/049/050 + P2-RC-048，序号与 A1.33 P1-RC-042/P2-RC-047 协调）+ A1.34 报告清单行 + A1.34 交叉引用注记 + P1-MA2-067/068/069/070 RC A1.34 复核注记（HEAD 复核无回退）；`requirement-compliance-roadmap.md` A1.34 todo→done。复用 A2.13（P1-MA2-067/068/069/070 resolved + 项目 5 态/任务 4 态/工时审批轴状态机已证实）+ P1-MA1-010/022（维持 todo MR1），只补需求视角差异（物料/分包缺失 / 成本率三级降级 / businessType drift / ON_HOLD 费用门控 / requireReferenceable 未用）。
- **验证**：只读审计无生产代码变更，build/test 回归风险为零。门控 = 报告 9 段完整性 + §8 checker actual vs baseline 实测 + 独立结束审计 **pass**（独立子代理 fresh session，未执行本计划任何 Phase，6 项验证区全 PASS：五点一致性 / 交付物真实性[报告 9 段 + arm-index 四处更新经 grep 实仓核实] / 零 P0 声明 / Deferred honesty / Anti-Hollow / 独立草案审查已记录[Draft Review ses_030952f8 accept]）。

### 2026-08-05（AUDIT — plan 2026-08-05-2200-1 quality-F3 SPC 与看板需求符合性审计）

- **任务**：执行 A1.33 只读审计（`docs/plans/2026-08-05-2200-1-rc-ma1-a1-33-quality-f3-spc-dashboard.md`），对 UC-QA-09/10/11/12 五级追踪。Skill: `multi-dimensional-audit-prompt`。**只读审计——零生产代码变更**。quality 域**第三批（最后一批）RC 切片**（A1.31/A1.32/A1.33 之 3，本切片覆盖 SPC 引擎[失控预警/能力分析/数据聚合] + 质量看板 4 UC，quality 域 MA1 三切片至此全 done）。
- **结论**：4 UC 逐矩阵行 + 候选缺口逐条复核。整体裁决：**接受 on 主路径 + 零 P0、1 新 P1、4 新 P2、1 复用 P1**。
  - UC-QA-09 SPC 失控预警 = **接受 on ①②③主路径 + P1 on ④⑤ + P2 on ⑥⑦**（采样聚合 `SpcSamplingService.collectSamples:105` + ≥20 门槛 `SpcControlLimitCalculator.recalculate:101` + 手动规则评估 `SpcRuleEngine.evaluate:82-133` + NCR/CAPA 创建主路径 `SpcOutOfControlHandler.cascadeNcrAndCapa:78-91` afterCommit + sourceType=SPC + 幂等预检，复用 A2.12 SPC 引擎 5 组件 1:1 + `TestErpQaSpcSampling` 6 @Test + `TestErpQaSpcOutOfControl` 4 @Test 强测[反射绕过 afterCommit]）。**P1 = new P1-RC-042** 自动调度链断裂（`spc-sampling.batch.xml:23-24` processor 仅调 collectSamples+recalculate **漏调 evaluateRules** → 生产调度路径[nop-job cron→batch]永不触达规则评估→NCR/CAPA 自动级联断裂，仅手动 `ErpQaSpcChartBizModel.evaluateRules:63-65` 可达，**新根因**[既有 arm-index 无 finding 涉及 SPC scheduler 接线断点]，§4 三判据均不成立→Q4=(a) 强制实现，修复=纯调度接线预授权不触 §5 ask-first）。**P2 = new P2-RC-044** CAPA 1:1 非 per-ruleSet + `actionType="CAPA"` 硬编码 `:119` + `SEVERITY_LOW :41` 死分支。
  - UC-QA-10 SPC 过程能力分析 = **接受 on ①②③④⑦主路径[公式/阈值逐字一致] + P2 on ⑤⑥**（`classifyByCpk:265-280` 与 L1 逐字一致 INADEQUATE<1.0/ACCEPTABLE 1.0-1.33/CAPABLE 1.33-1.67/EXCELLENT>1.67 + `TestSpcCapabilityFormulas` 6 @Test 强）。**P2 = new P2-RC-045** QualityGoal 名称约定回写[`:284-287` 非 FK + 无匹配静默 no-op `:288-290`] + RiskRegister 硬编码值[likelihood=3/severity=4/riskScore=12 `:305-307` 非 Cpk 量级派生] + 调度 30 天窗口硬编码。
  - UC-QA-11 SPC 数据聚合 = **接受 on ①②④⑤主路径[聚合不重复存+单一真相源+subgroupSize+measuredValues] + P2 on ③**（`TestErpQaSpcSampling` 6 @Test 强 mean=30/range=40 + 幂等三元组；InspectionLine 从不写回）。**P2 = new P2-RC-046** samplingFrequency 死字段（`service/spc/` grep `samplingFrequency|getSamplingFrequency` 零命中，实际节奏由全局 nop-job cron 决定，字段是死元数据）。
  - UC-QA-12 质量看板 = **接受 on ①②⑤主路径[KPI 实时聚合+期间过滤+CAPA 阈值 config 化] + P1（复用）on ③④⑥ + P2 on 不合格原因**（`TestErpQaDashboard` 5 @Test + `TestErpQaDashboardSpc` 5 @Test + `TestErpQaDashboardSpcChart` 7 @Test 强 + E2E `quality.value.spec.ts` 强值断言）。**行级权限 reuse P1-MA2-093**（A2.18 `:101` 显式列 `ErpQaDashboardBizModel` 为 11 dashboard 直访绕过认证管道之一，plain `@BizModel` 不继承 CrudBizModel + IServiceContext 全文零引用 + 所有 QueryBean 零 orgId，resolved R1.29 全局 ErpOrgIsolationQueryTransformer，追加 RC A1.33 交叉引用不新建；与 A1.7/A1.11/A1.21/A1.24/A1.27 行级权限复用先例一致；SP：R1.29 是否覆盖 ErpQaDashboardBizModel 直访路径交 MA4 A4.1）。**P2 = new P2-RC-047** 不合格原因 dispositionType 替代 defectType（`findDefectTopN:142-143` GROUP BY dispositionType 非 defectType，Javadoc `:56-57` 显式声明 defectType 未物化）。
- **产出**：报告 `docs/audits/2026-08-05-2200-1-rc-ma1-a1-33-quality-f3-spc-dashboard.md`（9 段齐全）；`arm-index.md` 新增 5 finding（P1-RC-042 + P2-RC-044/045/046/047，序号与 A1.32 P2-RC-042/043 协调——P1/P2 独立序列故 P1-RC-042 与 P2-RC-042 不冲突）+ A1.33 报告清单行 + A1.33 RC 交叉引用注记 + P1-MA2-093 RC A1.33 交叉引用注记；`requirement-compliance-roadmap.md` A1.33 todo→done。复用 A2.12（SPC 引擎 5 组件 1:1 + 计数型分支 + P1-MA2-065 dict 死状态 resolved R1.20）+ A2.18（P1-MA2-093 看板 orgId/行级权限 resolved R1.29）已证实行为，只补需求视角差异。
- **验证**：只读审计无生产代码变更，build/test 回归风险为零。门控 = 报告 9 段完整性 + §8 checker actual vs baseline 实测（可见规则 R1a-R2d/R6/R7/R10/R12a actual 精确等于 baseline；checker R3 段有预存环境性 set -euo pipefail 中断非回归——本审计零代码变更无回归风险归因）+ 独立结束审计 **pass**（ses_0300b6a52ffeI24bXWcYV2sPdL，8/8 验证区全 PASS：9 段齐全 / 4 UC 逐矩阵行 / 5 finding 序号 / arm-index 四处更新 / **P1-RC-042 代码断言 ACCURATE**[spc-sampling.batch.xml:21-24 调 collectSamples+recalculate 未调 evaluateRules 实仓核实] / §去重协议遵守 / 真相源冻结[git diff use-cases.md/spc.md/dashboards.md/product-scope.md = empty] / §8 自检齐全）。

### 2026-08-05（AUDIT — plan 2026-08-05-1830-3 quality-F2 NCR-CAPA 闭环需求符合性审计）

- **任务**：执行 A1.32 只读审计（`docs/plans/2026-08-05-1830-3-rc-ma1-a1-32-quality-f2-ncr-capa-closure.md`），对 UC-QA-05 五级追踪。Skill: `multi-dimensional-audit-prompt`。**只读审计——零生产代码变更**。quality 域**第二批 RC 切片**（A1.31/A1.32/A1.33 之 2，本切片覆盖 NCR-CAPA 闭环效果验证门禁全链，仅 1 UC 但为质量闭环核心）。
- **结论**：UC-QA-05 逐矩阵行 + 3 候选缺口逐条复核。整体裁决：**接受 on 主路径 + 零 P0、零 P1、2 新 P2**。
  - UC-QA-05 NCR-CAPA 闭环 = **接受 on ①②③④⑥⑦主路径 + P2 on ⑤⑦边界**（主路径 OPEN→IN_REVIEW→CAPA 纠正/预防→执行→效果验证→RESOLVED + resolve 门禁 + R1.20 noCapaReason 逃逸门 + 全程记录 person/date 完整实现且强测 `testRejectedAutoCreatesNcrAndFullCapaClosure:61-110` + `testResolveNoCapaGate:158`）。复用 A2.12 P1-MA2-066 resolved R1.20（noCapaReason 显式标注门禁落地）。
  - 候选缺口① 验证失败显式回退路径 = **P2（语义等价形式差异）**（L1 `:92` + L2 inspection-integration §四.3 `:168,184` 要求"验证失败→返回 IN_REVIEW 重新制定"，实现为被动阻塞——resolve 抛异常 NCR 保持 IN_REVIEW，Facade grep `reopen|rejectVerification|backToReview` 零命中无 dedicated reopen；语义等价因 NCR 既已在 IN_REVIEW[resolve 是 IN_REVIEW→RESOLVED 唯一出边]，**new P2-RC-042**）。
  - 候选缺口② noCapaReason 逃逸门 = **接受（§4 三判据 (i)+(ii) 满足）**（L1 UC-QA-05 未提及此逃逸门，R1.20 引入的合法逃逸门经独立 plan-audit 通过记录 + owner doc state-machine.md §NCR 与 CAPA:148 + inspection-integration §四:172 显式标注误开/降级合法场景 → legitimate documented escape hatch 非静默降级，不新建 finding，arm-index P1-MA2-066 追加 RC A1.32 交叉引用注记）。
  - 候选缺口③ 全程记录 verificationResult 字段 = **P2（隐式满足）**（L1 `:94`「全程记录...验证结果」，`ErpQaAction` ORM grep `verificationResult` 跨 module-quality 零业务命中——无显式列；verifyAction:58-77 仅写 person+date 调用即隐式通过，由 person+date+verifyAction 调用事实隐式满足，**new P2-RC-043**）。
- **产出**：报告 `docs/audits/2026-08-05-1830-3-rc-ma1-a1-32-quality-f2-ncr-capa-closure.md`（9 段齐全）；`arm-index.md` 新增 2 finding（P2-RC-042/043，序号 042+ 与 A1.31 040+/A1.30 036+ 协调）+ A1.32 报告清单行 + P1-MA2-066 RC A1.32 交叉引用注记（noCapaReason 逃逸门经 L1 视角 §4 复核接受）；`requirement-compliance-roadmap.md` A1.32 todo→done。
- **验证**：只读审计无生产代码变更，build/test 回归风险为零。门控 = 报告 9 段完整性 + §8 checker actual vs baseline 实测 + 独立结束审计 **pass**（独立子代理 invocation `MISSION_DRIVER:2026-08-04-224309-mission-driver` fresh session，验证区全 PASS：9 段齐全 / UC-QA-05 逐矩阵行 / 2 finding 序号 042+ / arm-index 三处更新 / 关键证据实仓复核 TRUE[grep reopen/verificationResult 零命中确认 finding 真实] / §去重协议遵守 / 真相源冻结 / §8 自检齐全）。

### 2026-08-05（AUDIT — plan 2026-08-05-1830-2 quality-F1 检验门控需求符合性审计）

- **任务**：执行 A1.31 只读审计（`docs/plans/2026-08-05-1830-2-rc-ma1-a1-31-quality-f1-inspection-gating.md`），对 UC-QA-01/02/03/04/06/07/08 五级追踪。Skill: `multi-dimensional-audit-prompt`。**只读审计——零生产代码变更**。quality 域**首批 RC 切片**（A1.31/A1.32/A1.33 之 1，本切片覆盖检验门控 7 UC）。
- **结论**：7 UC 逐矩阵行 + 4 新 finding 分级。整体裁决：**零 P0、2 新 P1、2 新 P2**。
  - UC-QA-01 来料强制质检阻塞 = **接受**（`InspectionTrigger.enforceGate:35-50` config-gated 同步 I\*Biz 写触发 + `ErpQaInspectionBizModel.isInspectionCleared:79-94` PENDING/REJECTED 阻塞 / ACCEPTED+CONDITIONAL 放行，复用 A2.12 + `testMandatoryInspectionBlockedWhenPendingClearedWhenAccepted:131` 强测）。
  - UC-QA-02 质检不合格触发退货 = **接受**（`ErpQaInspectionFailInspectionProcessor:19-27` REJECTED→`autoCreateNcrFromInspection:25` + `NcrReturnOrchestrator.orchestrateReturn:72`→`IErpPurReturnBiz` Facade，复用 A2.12+A4.5 + `testReturnDispositionOrchestratesPurchaseReturn:111` 强测）。
  - UC-QA-03 让步接收 = **接受 on ①②③**（`InspectionResultEvaluator.aggregate:73-92` allowConcession→CONDITIONAL + `RecordResultProcessor:55-67` 记录让步，**关键项否决断言归 UC-QA-06 复用 P1-RC-040 控制点**）。
  - UC-QA-04 完工检验不合格→返工 = **倾向接受 / P2 successor**（**new P2-RC-041** 完工返工跨域触发链 successor——quality 仅生成 NCR + mfg 门控闭环经 A1.9 UC-MFG-09 倾向接受证实[工单保持 IN_PROCESS 不在终态] + 自动建返工工单属 owner doc §3 successor[无 originalWorkOrderId/无自动建单]，§4 三判据[i] A1.9 plan-audit 通过成立→P2 documented simplification，交叉引用 A1.9 SP-2，§去重协议 MA1 同 mission 兄弟切片不重开 A1.9 结论）。
  - UC-QA-06 关键项否决 = **P1**（**new P1-RC-040** 关键项否决核心验收标准完全缺失——ORM `ErpQaInspectionTemplateLine:326` 仅 `isRequired`="是否必检"[语义≠关键项]，**无 isCritical/criticalItem/关键项 列**[grep 跨 quality dao/service + orm.xml 零业务命中] + `InspectionResultEvaluator.aggregate:73-92` 无关键项否决分支[关键项不合格+allowConcession=true→错误 CONDITIONAL 违反 L1 `:108-109`]，`testRejectedCriticalGoesRejected:84` 命名误导实为 allowConcession=false 通用拒绝[Q1 根因：测试名误导致基线误判]，§2 P1①[功能完全缺失]+P1⑤[断言误导]，须人工确认 product-scope，**修复触及 ORM[ErpQaInspectionTemplateLine+ErpQaInspectionLine 加 isCritical 列]须 ask-first + 独立 plan-audit §5**）。
  - UC-QA-07 质检模板优先级解析 = **P2**（**new P2-RC-040** 类别级模板解析缺失——`InspectionTemplateMatcher.match:30-47` 仅两级[物料级→全局默认]，L1 `:122` 要求三级[物料级>类别级>全局]，主路径 OK 边界弱，§2 P2①，修复纯代码逻辑不触发 §5 ask-first）。
  - UC-QA-08 业务单据作废联动取消质检 = **P2（documented simplification with approval）+ 新 P1-RC**（**new P1-RC-041** 业务作废联动 L1 验收标准经 R1.20 deferral 未满足——`IErpQaInspectionBiz`[85 行]无 `cancelForBusinessBill` + grep 跨 quality/purchase/sales/mfg 零命中，**但 R1.20 plan `2026-07-30-0512-3:37,64` 显式裁决 P1-MA2-064 Deferred[owner doc 正式化非方案 A 实现] + state-machine.md §实现约定:190 标 Deferred** → Facade 缺失是 deliberate deferral 非丢失合入/回退；**不重开 P1-MA2-064**[§去重协议]，从 L1 视角登记新 P1-RC-041 + §4 三判据[i] R1.20 plan-audit 通过成立→倾向 P2 documented simplification with approval + 声明 Q4=(a) 张力 + **arm-index:361 状态澄清**[「✅ resolved (R1.20 done)」未披露 resolution 是 deferral 而非 implementation——误导性「done」标签致基线初判误为已实现，澄清为 resolved-via-deferral]）。
- **产出**：报告 `docs/audits/2026-08-05-1830-2-rc-ma1-a1-31-quality-f1-inspection-gating.md`（9 段齐全）；`arm-index.md` 新增 4 finding（P1-RC-040/041 + P2-RC-040/041，序号 040+ 与 A1.30 协调）+ A1.31 报告清单行 + A1.31 RC 交叉引用注记 + P1-MA2-064 状态澄清（resolved-via-deferral）；`requirement-compliance-roadmap.md` A1.31 todo→done。复用 A2.12（P0-MA2-017 inspection 状态守卫 resolved + 强制质检门控 + NCR 过账引擎齐全）+ A4.5（NcrReturnOrchestrator Facade）+ A1.9（UC-MFG-09 完工返工 mfg 侧闭环倾向接受）已证实行为，只补需求视角差异。
- **验证**：只读审计无生产代码变更，build/test 回归风险为零。门控 = 报告 9 段完整性 + §8 checker actual vs baseline 实测（R2c actual 1382 vs baseline 1380 +2 delta 反映审计前既有仓库状态，非本审计引入）+ 独立结束审计 **pass**（ses_030b635b7ffeP1NmGvSV5aGk5M，8 项验证区全部 PASS：9 段齐全 / 7 UC 逐矩阵行 / 4 finding 序号 040+ / arm-index 四处更新 / 关键证据实仓复核 TRUE / §去重协议遵守 / 真相源冻结 / §8 自检齐全）。

### 2026-08-05（ANALYSIS — 「view.xdef complex × flux 控件能否覆盖全部复杂页」深度分析）

- **任务**：回答「view.xdef 定义的 complex 界面结合 nop-chaos-flux 最新控件能否完成本项目所有复杂页面」，产出逐页分析报告。Skill: none（能力边界研究）。
- **结论**：**rev.1（初版，embed 前）**：**不能**。complex 是标准容器编排外壳（四槽位 header/footer/aside/body 仅容纳 crud/picker/simple/tabs/wizard/group），flux 专有控件（kanban/gantt/calendar/timeline/tree/diff-view/steps/loop/chart/cards/collapse/data-source）非 UiContainerModel 子类型且生成器无 xjson 透传通道（flux-web 全库 0 消费 beforeForm/afterForm；容器级 picker 抛 `nop.err.web.unknown-page-type`；高级 wizard valuesPath/formId/statusPath/onComplete 不被 container_wizard 映射）。实证：全仓库 **0 个 view.xml 使用 `<complex>`**，20 个复杂页全为 `.flux.yaml` 直写；30 核心复杂页 complex 可覆盖 0 个，view.xml 层 19 个复杂形态（15 tabs/60 子表/4 树形）全部可覆盖。
- **rev.2（embed 加入）→ rev.3（曾误判「embed 不存在」）→ rev.4（定稿 2026-08-05，恢复）**：经确认 **`<embed>` 定义被接受为当前定义**——`xview.xdef` 中 `<embed>` 既是 `UiContainerModel` 第七子类型（可置 complex 槽位/tabs/wizard/group），亦为 `<pages>` 页面级类型（`path/page/grid` + `<data>`/`<override>`）；`WebPageHelper.applyViewOverride`（JsonMerger delta 合并）已落。**rev.3 曾据一次「全仓 grep 零命中 / Non-Goal」错误下修为「embed 不存在于 schema」——该下修为误判，作废**；按下 rev.4 恢复到「embed 存在 → view.xml 声明层（complex 外壳 + 槽位 embed + 页面级 embed + cell view）可覆盖全部复杂页，交互叶子由被引用外部页面维护」的正确立场，并标记此覆盖判定为**设计可行性（prospective）**——渲染接线（page_embed.xpl/container_embed.xpl/GenDispView override 消费）待 plan 331，非 live 已验证渲染。**live 事实**：`beforeForm/afterForm`（`<simple>` xjson 槽 :144-145）与 `beforeTable/afterTable`（crud）已声明但 flux-web 0 消费；`disp.xdef <view>` 无 `<override>`。**命名维持 `<embed>`**（`view` 平台过载；embed=页面/槽位级引用整页、cell `view`=字段级、beforeForm/afterForm=inline JSON，三语义互补不合并）。**embed vs beforeForm/afterForm**：`beforeForm/afterForm` **不需要改造成 embed 格式**（inline JSON vs 引用外部整页，互补）；embed **不能直接写死 inline JSON**（只引用外部页 + override delta）；要「view.xml 直接嵌入 JSON」应走**激活 beforeForm/afterForm 的 flux-web 消费**。
- **产出**：`docs/analysis/2026-08-05-1600-xview-complex-and-flux-controls-coverage-analysis.md`（结论摘要 + 平台生成链实证 + 控件盘点 + 30 页逐页判定表 A/B/C/D/E/F/报表/44 非标准页 + 既有设计一致性校验 + 建议）。发现 3 处文档漂移：`flux-complex-pages.md` §2.5/§7#9 与 `non-standard-views-patterns.md` §0「complex 覆盖全部复杂页」表述未兑现（登记 §6.4，非阻塞）。
- **验证**：doc-only 零代码变更；全部结论基于源码实证 + 实现层文件逐一核对。

### 2026-08-05（AUDIT — plan 2026-08-05-1830-1 crm-F3 CPQ/漏斗推进需求符合性审计）

- **任务**：执行 A1.30 只读审计（`docs/plans/2026-08-05-1830-1-rc-ma1-a1-30-crm-f3-cpq-funnel-advancement.md`），对 UC-CRM-06/13 五级追踪。Skill: `multi-dimensional-audit-prompt`。**只读审计——零生产代码变更**。CRM 域**第三（末）个 RC 切片**（A1.28/A1.29 同批次先行）。
- **结论**：2 UC 逐矩阵行 + 12 候选缺口分级。整体裁决：**零 P0、零 P1、4 新 P2**。
  - UC-CRM-06 漏斗阶段推进 = **接受 on 主路径 + P2 on 等值边界**（**复用 P1-MA2-075 resolved R1.24** stageId 单向递增守卫 validateStageDirection:91-110 STRICT 默认抛 ERR_STAGE_BACKWARD_MOVE + config-gated allow-stage-backward；convLog 四字段完整性 writeConvLog:163-166 + ORM 四列齐全；doMoveStage 仅 setStageId 不触 docStatus 满足 ⑦；**new P2-RC-036** 等值边界——L1 `:122` `<=`[等值拒绝] vs 代码 `:99` `<`[等值放行]，L4 testEqualSequenceForwardSucceeds 主动断言等值成功）。
  - UC-CRM-13 CPQ 配置-定价-报价 = **接受 on 主路径 + P2**（配置规则引擎 ProductConfigRuleEngine REQUIRED/EXCLUDED/RECOMMENDED/OPTIONAL + EXCLUDED 禁用优先；价格规则引擎 PriceRuleEngine ruleType rank CUSTOMER_SPECIFIC>PROMOTIONAL>VOLUME + priority tie-break；捆绑定价 BundlePricingCalculator bundleAmount→PERCENTAGE→FIXED；跨域建报价单 ErpCrmProductConfiguratorGenerateQuoteProcessor 经 IErpSalQuotationBiz.save + lead 弱指针回写；**new P2-RC-037** 前端配置向导 wizard successor watch-only；**new P2-RC-038** createFromConfig→save 方法名漂移 + configSnapshot 落 remark 截断 500 字符 watch-only；**new P2-RC-039** configSnapshot 落库内容断言弱 watch-only）。
- **产出**：报告 `docs/audits/2026-08-05-1830-rc-ma1-a1-30-crm-f3-cpq-funnel-advancement.md`（9 段齐全）；`arm-index.md` 新增 4 finding（P2-RC-036~039）+ A1.30 报告清单行 + A1.30 RC 交叉引用注记；`requirement-compliance-roadmap.md` A1.30 todo→done（CRM 域 MA1 三切片 A1.28/A1.29/A1.30 全 done）。同步起草 A1.31 quality-F1 检验门控（`2026-08-05-1830-2`）+ A1.32 quality-F2 NCR-CAPA 闭环（`2026-08-05-1830-3`）审计计划（Plan Status=active，待执行）。
- **验证**：只读审计无代码变更，build/test 回归风险为零（门控 = 报告 9 段完整性 + §8 checker actual vs baseline + 独立结束审计 pass）。

### 2026-08-05（AUDIT — plan 2026-08-03-1341-3 crm-F2 营销/预测/配额/序列/漏斗/事件提醒需求符合性审计）

- **任务**：执行 A1.29 只读审计（`docs/plans/2026-08-03-1341-3-rc-ma1-a1-29-crm-f2-marketing-forecast-quota-sequence-funnel.md`），对 UC-CRM-05/07/08/10/12/14/15 五级追踪。Skill: `multi-dimensional-audit-prompt`。**只读审计——零生产代码变更**。CRM 域**第二批 RC 切片**（A1.28 同批次先行）。
- **结论**：7 UC 逐矩阵行 + 5 候选缺口分级。整体裁决：**零 P0、3 新 P1、1 新 P2**。
  - UC-CRM-05 活动/事件记录 = **接受 on 主路径**（Event complete/cancel + LeadActivityDerivationHelper.recalculateForLead:36-49 push model 强测；lastContactDate 按 COMPLETED 过滤登记 SP-1 交 MA4，倾向接受实现选择不开 finding）。
  - UC-CRM-07 UTM 营销活动归因 = **P1**（**new P1-RC-037** UTM copy-on-create 派生缺失——`ErpCrmLeadBizModel.defaultPrepareSave:196-223` 无 campaign.medium→utmMedium/source 复制，grep setUtmMedium/setUtmSource 跨 src/main=0 业务命中，ErpCrmLeadBizModel 不注入 IErpCrmCampaignBiz；**new P1-RC-038** 归因报表完全缺失——`ErpCrmCampaignBizModel.java:11-19` 19 行空 CRUD stub + `ErpCrmReportBizModel.prepareDataset:151-164` switch 仅 2 报表 + glob `**/report/crm/*.xpt.xml` 仅 2 文件；数据模型支撑已就绪属代码逻辑缺口非 ORM 缺口）。
  - UC-CRM-08 事件提醒 Job = **接受**（**复用 P1-MA2-076 resolved R1.24** per-event reminderMinutesBefore 已读 findDueReminders:89-116 + **复用 P1-MA2-086 resolved R1.28** cron leader-lock + TestErpCrmEventReminderJob 强测）。
  - UC-CRM-10 销售预测生成 = **P1**（**new P1-RC-039** Forecast territory tier rollup 缺失——`ForecastAggregator.refreshForecast:50-102` 仅 3 级[个人→团队→公司]，类头注释 `:36` 字面无 territory tier；**sales-forecast.md:222 Deferred**["触发条件：Lead→Territory 映射就绪时"] + **触发条件现已满足**：territory.md §实现注记 1 ErpCrmLead.territoryId propId 41 已添加 → §4 三判据复核重开 P1[与 A1.28 P1-RC-036 同型]；commit/upside/bestCase 公式主路径接受）。
  - UC-CRM-12 销售配额管理 = **接受**（**纠正计划基线 #3 候选缺口假设**——`QuotaRollupCalculator.rollup:44-113` 实际支持 territory 子树聚合 collectSubtreeIds:71-73→:220-227 递归，territory.md §实现注记 4:237-239 字面声明 territory tier 已实现，`testQuotaRollupExplicitValuePriorityAndAggregate:295-329` 实证 region Σ 1000+500=1500；finalize/unfinalize/distribute/pipeline 全实现）。
  - UC-CRM-14 销售序列自动分配与推进 = **接受**（documented 降级 EMAIL_OPENED/REPLIED→eventType 匹配 + 1-active-sequence accepted per sales-sequence.md §实现注记 :229-231；主路径强测）。
  - UC-CRM-15 线索漏斗分析 = **接受**（documented AMIS 可视化 successor per lead-waterfall.md §实现注记 :240；后端聚合 + getFunnelView 强测 TestFunnelAggregationEngine 8 @Test）。
  - **新发现 #4 P2-RC-035**：UC-CRM-14/15 Job bean cron-gating 测试缺失（SequenceOverdue + FunnelAggregation，仅 ForecastRecalc/EventReminder 有 dedicated Job 测试）。
- **关键纠正**：计划基线 #3 候选缺口"UC-CRM-12 Quota territory tier 缺失"经实仓复核 + test 实证**纠正为不成立**（QuotaRollupCalculator.rollup 实际支持 territory 子树聚合，territory.md §实现注记 4 字面声明 territory tier 已实现）。本切片对计划假设的重要修订基于实仓代码 + test 实证，不影响其他切片结论。**新发现 #3'**：UC-CRM-10 ForecastAggregator territory tier rollup 缺失（与计划基线 #3 不同切片不同引擎——Quota 已实现，Forecast 未实现）。
- **产出**：报告 `docs/audits/2026-08-05-1100-rc-ma1-a1-29-crm-f2-marketing-forecast-quota-sequence-funnel.md`（9 段齐全）；`arm-index.md` 新增 4 finding（P1-RC-037/038/039 + P2-RC-035）+ A1.29 报告清单行 + A1.29 RC 交叉引用注记；`requirement-compliance-roadmap.md` A1.29 todo→done。
- **验证**：只读审计无代码变更，build/test 回归风险为零（门控 = 报告 9 段完整性 + §8 checker actual vs baseline + 独立结束审计 pass）。

### 2026-08-05（AUDIT — plan 2026-08-03-1341-2 crm-F1 线索生命周期需求符合性审计）

- **任务**：执行 A1.28 只读审计（`docs/plans/2026-08-03-1341-2-rc-ma1-a1-28-crm-f1-lead-lifecycle.md`），对 UC-CRM-01/02/03/04/09/11 五级追踪。Skill: `multi-dimensional-audit-prompt`。**只读审计——零生产代码变更**。CRM 域**首个 RC 切片**（既有 arm-index 无 RC finding 涉及 crm）。
- **结论**：6 UC 逐矩阵行 + 9 候选缺口分级。整体裁决：**零 P0、5 新 P1、4 新 P2**。
  - UC-CRM-01 线索创建与验证 = **接受 on 主路径 + P2 on lastContactDate**（qualify 状态迁移+首stage+default probability 接受；`doQualify:121-131` 不设 lastContactDate → **new P2-RC-033** watch-only）。
  - UC-CRM-02 线索→商机转化 = **P1**（**new P1-RC-032** 直接升格分支缺失——grep `直接升格|directPromote` 业务命中=0，仅 convertToCustomer 单路径；**new P1-RC-033** convertToCustomer 前置条件弱——`:19-28` 不查 docStatus==QUALIFIED，NEW 状态 LEAD 可被转化）。
  - UC-CRM-03 商机→报价单转化 = **P1**（**new P1-RC-034** convertToQuotation 前置条件弱——`:21-30` 不查 docStatus==QUALIFIED 且不查 stage.isWonStage==true，won-stage 前置静默丢弃；零污染经 testZeroPollutionAssertion 反射强断言接受）。
  - UC-CRM-04 丢单原因记录 = **接受**（lose+lostReason 必填经 requireLostReason:112-117 + 强测）。
  - UC-CRM-09 线索自动评分 = **P1**（**new P1-RC-035** SCHEDULED 触发器完全缺失 + **L2 lead-scoring.md:157 逐字失实声称「SCHEDULED：ErpCrmLeadScoringRecalcJob + scheduler.yaml 已接线」** 实际 4 jobs 无评分 job[owner doc 向实现妥协 §4 根因]；**new P2-RC-031** NOTIFY_OWNER 无派发——determineAction 返回但 LeadScoringEngine 无 notify 调用；**new P2-RC-034** LeadScoreConfigBizModel 19 行 CRUD stub 保存时无 isActive 唯一性校验；评分核心[LOOKUP/FORMULA/BOOLEAN+归一化+auto-qualify]接受）。
  - UC-CRM-11 线索区域自动分配 = **P1**（**new P1-RC-036** ROUND_ROBIN/LOAD_BALANCED 显式降级 MANUAL——`toResult:73-84` 设 degraded=true ownerId 永不设置，§4 三判据复核 territory.md Deferred AI 落地无人工批准痕迹→重开 P1，修复可能触及 ORM[ErpCrmTeamMember] ask-first；**new P2-RC-032** 代理字段——GEOGRAPHY/INDUSTRY/CUSTOMER_SIZE 经 companyName/department/expectedRevenue 非字面字段；territory 引擎 4 conditionType+默认 fallback 接受）。
- **产出**：报告 `docs/audits/2026-08-05-1030-rc-ma1-a1-28-crm-f1-lead-lifecycle.md`（9 段齐全）；`arm-index.md` 新增 9 finding（P1-RC-032~036 + P2-RC-031~034）+ A1.28 报告清单行 + A1.28 RC 交叉引用注记；`requirement-compliance-roadmap.md` A1.28 todo→done。
- **验证**：只读审计无代码变更，build/test 回归风险为零（门控 = 报告 9 段完整性 + §8 checker actual vs baseline + 独立结束审计 pass）。

### 2026-08-05（AUDIT — plan 2026-08-03-1341-1 inventory-F3 盘点/估值/并发/看板需求符合性审计）

- **任务**：执行 A1.27 只读审计（`docs/plans/2026-08-03-1341-1-rc-ma1-a1-27-inventory-f3-stocktake-valuation-concurrency-dashboard.md`），对 UC-INV-07/08/10/11 五级追踪。Skill: `multi-dimensional-audit-prompt`。**只读审计——零生产代码变更**（git status 仅 docs/audits/+docs/plans/+docs/backlog/）。
- **结论**：4 UC 逐矩阵行 + 8 候选缺口分级。整体裁决：**零 P0、0 新 P1、2 新 P2、3 reuse**。
  - UC-INV-07 completeTake stub = **reuse P1-MA2-062**（§4 三判据复核 R1.19 hollow closure 倾向重开，须人工确认 product-scope 是否裁剪盘点自动生成；§7 同根因同控制点不新建编号）。
  - UC-INV-08 并发扣减 = **接受**（乐观锁+UK+重试+负库存语义齐全，A2.11+A2.17 双重证实）。
  - UC-INV-10 存货估值凭证 = **接受含 caveat**（过账链路完整；命名漂移 reuse P2-RC-011+P2-RC-016 watch-only；行级断言缺失 **new P2-RC-029**）。
  - UC-INV-11 库存看板 = **接受**（KPI 实时聚合非硬编码；行级权限 reuse P1-MA2-093 resolved R1.29 + 运行时覆盖存疑点交 MA4；缺料阈值 **new P2-RC-030** watch-only）。
- **产出**：报告 `docs/audits/2026-08-05-0900-rc-ma1-a1-27-...md`（9 段齐全）；`arm-index.md` 新增 P2-RC-029/P2-RC-030 + P1-MA2-062 RC 复核注记 + A1.27 报告清单行；`requirement-compliance-roadmap.md` A1.27 todo→done。
- **验证**：只读审计无代码变更，build/test 回归风险为零（门控 = 报告 9 段完整性 + §8 checker actual vs baseline + 独立结束审计 pass）。基线当日早前已 `mvn clean install -DskipTests` BUILD SUCCESS + `mvn test` 全绿。

### 2026-08-05（EXEC — plan 2026-08-03-1232-5 前端范式文档 Flux 化与漂移回填）

- **任务**：mission-driver 驱动执行 `docs/plans/2026-08-03-1232-5-flux-docs-drift-and-patterns.md` 全 4 阶段（Phase 0-3）。**doc-only 计划**（无代码变更，验证门控为文档审查）。Skill: `document-audit-prompt`（Phase 0 扫描项）。
- **Phase 0（漂移回填 + 一致性扫描）**：4 漂移回填为最终形态——non-standard-views §3.1/§4.1（timeline/calendar flux 原生）、hr org-chart（flux tree）、aps gantt（拖拽已实现 + 右键菜单 flux gantt 无 onContextMenu 残留 successor）、b2b ASN 5→4 值字典（锚点 :10/23/158/160/184-185/259/290/298 全文穷尽，保留「流程条」措辞仅改阶段数）。一致性扫描发现并修复 non-standard-views §5.1/§7 三处 BizModel 行号漂移（crm `:84→:112`、cs `:109-242→:103-174`、prj `:103-163→:108-157`，经实时 grep 核实）。
- **Phase 1（范式文档 Flux 化）**：3 核心 doc（page-structure/non-standard-views/child-table-editor）新增「§0 Flux 渲染权威模型」节（render-mode 切换 + complex/tabs/wizard/group 容器 + flux schema 骨架 + 数据契约 + 事件持久化）；page-structure §3.0 看板三段式范式（data-source + card + chart + crud，复用度 11 页）+ §5 wizard/§8.3/§8.7-§8.12 七 F16 范式 flux 最终形态注记；6 辅助 doc（tree/picker/visible-on/field-formatting/status-color-map/cross-doc）加 flux 控件映射；dashboards.md §实现约定同步。
- **Phase 2（域 ui-patterns + 缺口补齐）**：18 域 ui-patterns 全部加 flux 控件映射注记；notify 缺口闭合（方案 b——模板管理走标准 CRUD，notify/README 新增「页面设计与缺口闭合」节 + flux 映射）；SPC 页面交互设计补齐（quality/spc.md 新增「页面交互设计」节——chart referenceLines UCL/LCL/CL + band + markers + 数据契约 getSpcControlChartData）。
- **Phase 3（路线图 + i18n）**：frontend-ui-roadmap.md「Flux 渲染引擎备选」→「Flux 全量迁移」决策更新（旧决策划除线 + AMIS 退役路径触发条件）；flux-complex-pages.md 状态改「已实施」+ §1.1/§1.2/§2.1/§4.2/§7 多项实施后一致回填（SPC 已解决、complex 槽位已解决、flux 表达式约束登记）；page.yaml i18n Follow-up 登记（§7 #7，触发条件 = P2/P3 页面重写）。
- **验证**：doc-only 无代码变更；文档一致性扫描通过（b2b ASN 4 值 grep 0 残留、行号修复核实）；计划 Plan Status=completed，全 Phase item + Exit Criteria + Closure Gates `[x]`。

### 2026-08-05（EXEC — plan 2026-08-03-1232-4 占位页与未实现项 Flux 落地）

- **任务**：mission-driver 驱动执行 `docs/plans/2026-08-03-1232-4-flux-placeholder-and-unimplemented-pages.md` 全 5 阶段（Phase 0-4）。Skill: `nop-frontend-dev` + `nop-testing`。
- **Phase 0（占位页盘点与裁决）**：grep 确认 16 占位页（7 行 alert）。`ErpInvStockTakeBizModel.completeTake` 核实仅翻转 DONE 未生成盘盈/盘亏移动单。16 页归类：12 落地 + 4 Deferred（ncr-disposal/bank-ledger-line/gl-distribution/asset-repair，实体不存在或设计 Deferred，ORM ask-first 保护区域）。
- **Phase 1（SPC 三件套 + 财务 5 页）**：
  - SPC chart：chart（line + markLine UCL/LCL/CL + 失控点红色高亮）+ select 控制图 + crud 样本明细，数据来自 `getSpcControlChartData`。
  - SPC capability：bar chart 能力等级分布 + crud。
  - SPC sample：line chart 样本均值趋势 + crud。
  - finance 5 页：expense-claim/budget-scenario/budget-control-log/bank-statement/bank-reconciliation，每页 KPI 汇总卡 + crud 列表。2 页 Deferred（bank-ledger-line/gl-distribution）alert 更新为 Deferred 状态。
- **Phase 2（4 未实现设计项）**：
  - disposal-wizard：3 步向导（选资产+处置类型→清理损益预览→ErpAstDisposal__save 提交），菜单已加入 erp-ast.action-auth.xml。
  - stock-take-flow：crud 盘点单列表（DRAFT/IN_PROGRESS/DONE 色块）+ form action 按钮（startTake/completeTake/cancelTake），菜单已加入 erp-inv.action-auth.xml。盘盈/盘亏生成归 Deferred。
  - lead-conversion：3 步向导（选线索→选目标阶段→moveStage 确认），菜单已加入 erp-crm.action-auth.xml。
  - cs 知识库：ErpCsKnowledgeBase CRUD 页已存在（含 categoryId 分类），经既有 CRUD 实现即可用。
- **Phase 3（项目/资产/主数据 4 页）**：project-pnl（损益 KPI + crud）、project-settlement（结算 KPI + crud）、cost-center（成本中心 KPI + crud 含 parentId 层级）、asset-stocktake（盘点 KPI + crud 状态色块）。
- **验证**：`mvn clean install -DskipTests` BUILD SUCCESS；`mvn test` 全绿（含 ErpAllFluxPagesTest FLUX_PAGE_ERROR_COUNT=0）；`npx playwright test tests/e2e/crud/placeholder-pages.smoke.spec.ts` 15 spec 全绿（flux 引擎，7.2s/spec）；compliance checker 无 Java 生产代码变更无漂移。

### 2026-08-05（EXEC — plan 2026-08-03-1232-3 F16 特殊复杂页 flux 原生重写）

- **任务**：mission-driver 驱动执行 `docs/plans/2026-08-03-1232-3-flux-f16-complex-pages-rewrite.md` 全 4 阶段（Phase 0-3）。Skill: `nop-frontend-dev` + `nop-backend-dev` + `nop-testing`。
- **Phase 0（aps 甘特）**：
  - Decision：links 派生=方案 A（同 workOrderId 按 sequence 相邻连边 FS type=0）；拖拽契约=新增 `ErpApsOperationOrder__updateSchedule`（@BizMutation，宿主=实体 BizModel，与 start/complete/cancel 一致；不内联产能校验——产能由排程引擎下次运行暴露，归 successor）。
  - 后端：`IErpApsOperationOrderBiz.findGanttData(machineId,status)`（@BizQuery 聚合，返回 `{tasks,links}`，tasks 映射 text/start=plannedStartDateT/end=plannedEndDateT/progress/status）+ `updateSchedule(opOrderId,start,end)`（@BizMutation 写 plannedStart/EndDateT，`Timestamp.valueOf` 转换）。
  - 前端：`schedule-gantt.flux.yaml` — flux gantt 原生组件（tasks/links/defaultZoom=week/draggable/editable/linkable + onTaskDragEnd→@mutation:updateSchedule）。
- **Phase 1（2 向导）**：
  - Decision：响应式 gating——flux wizard step `visible` 按 period.status/month 控制；linear 模式过滤不可见步骤（annualRollover 仅 12 月可见）。实施期 UX 修正：向导步骤标题应全部可见（进度指示器），仅步骤内 action button 按 status `visible` 门控（移除 step-level visible，保留 button-level）。
  - 前端：`period-close-wizard/main.flux.yaml`（5 步 preCheck/closePeriod/annualRollover/finalize/reverseClose，per-module 状态经 ErpFinAccountingPeriodStatus data-source）+ `visit-wizard/main.flux.yaml`（4 步 info/spare/result/complete，编排 __start/__save/__complete/__cancel）。零后端 delta（复用既有 M4/BizModel mutation）。
- **Phase 2（8 B 族页）**：
  - Decision：BOM 嵌套重建——实施期发现 flux 表达式**仅支持箭头表达式体**（无块体 `{}`、无 while/forEach/if-return），原方案 B（formula 客户端栈算法 IIFE 块体）运行时无法解析，改用方案 A：新增 `IErpMfgBomBiz.findBomTree`（Java Deque 栈算法重建嵌套）。同理薪酬/净需求分组聚合（group-by 需块体 reduce）移至后端：`IErpHrSalaryBiz.findPayrollSummary`、`IErpDrpLineBiz.findNetReqGroups`。
  - 前端 8 flux.yaml：three-way-match（crud×4 + status 差异色）、bom-tree（tree + 后端 findBomTree）、payroll-approval（data-source 聚合 + crud markPaid/voidSalary row-action）、shipment-tracking（timeline + 双 data-source）、net-requirement（data-source + loop + table 嵌套分组）、version-diff（diff-view + 元数据对比表，字段级 diff 数据模型不支持注明）、asn-flow（steps status 驱动 + crud rowClick）、edi-detail（timeline + table + collapse 报文开关）。
- **关键 flux 运行时约束发现（实施期）**：
  1. `??`（nullish）**支持**（`left ?? right` 求值正确），但**属性访问需 `?.`**——data-source 变量首次渲染未解析时为 undefined，非 optional 访问（`poList.items`）抛 "Cannot access member of null or undefined"。全 11 flux.yaml + P2 org-chart 的 `node.`/`$slot.record` 改 `?.`。
  2. flux `grid` schema ≠ AMIS：`columns` 为**列数(number)**，子项用 `items:[{colSpan,body}]`，**非** `columns:[{type:grid-col,span,body}]`。原 AMIS-grid 写法渲染空 grid。全 11 + P2 8 个 flux.yaml 经 PyYAML 批量转换修正。
  3. 箭头函数**仅表达式体**（复杂聚合 reduce/stack 移后端）。
- **E2E**：3 共享视觉 spec（f16-high-risk/f16-complex-pages/f16-p2-complex-pages）+ 2 向导 spec（fin-period-close-wizard/maintenance-visit-wizard）全量重写为 flux `data-slot`/`.nop-*` 契约选择器。`E2E_ENGINE=flux` 下 10 passed / 6 graceful-skip（env-dependent 路由/payroll/向导交互时序，defer to ErpAllFluxPagesTest build 覆盖）/ 0 failed。
- **验证**：`mvn clean install -DskipTests` → BUILD SUCCESS；改动模块 `mvn test` 全绿（aps/mfg/hr/drp-service + app-erp-all，369 tests，0 failures）；`ErpAllFluxPagesTest` FLUX_PAGE_ERROR_COUNT=0。
- **文档**：plan 全 4 阶段 items + exit criteria + closure gates 全 `[x]` + Plan Status: completed；3 个 Decision + BOM 嵌套机制裁决记录；`frontend-ui-roadmap.md` F16 行追加 flux 原生重写 ✅ 标记。
- **连带修复**：P2（1232-2）8 个 flux.yaml 的 `??` + AMIS-grid 运行时缺陷（实施期发现 P2 closure 仅基于 server-side build test，浏览器运行时未验证）。

### 2026-08-05（EXEC — plan 2026-08-03-1232-2 F13 flux 原生重写）

- **任务**：mission-driver 驱动执行 `docs/plans/2026-08-03-1232-2-flux-f13-nonstandard-views-rewrite.md` 全 5 阶段（Phase 0-4）。Skill: `nop-frontend-dev` + `nop-backend-dev` + `nop-testing`。
- **Phase 0（Decision）**：4 个拖拽持久化语义裁决——prj 相邻态受限拖拽（onCardMove 按目标列路由 mutation，非相邻被后端守卫拒绝 + reload 回退）、cs 列→mutation 路由（assign/start/resolve/close/reopen，CANCELLED 终态守卫拒绝）、crm 日历 DATE-only 适配（方案 A 拖拽改 activityDate 按天对齐）、onCardMove payload 字段映射（event.cardId/fromColumnId/toColumnId，经 flux-renderers-scheduling 源码实测确认）。
- **Phase 1（Kanban ×3）**：
  - 后端：新增 3 个 `@BizQuery findBoardData`/`findOpportunityBoardData` 返回扁平图结构（`IErpPrjTaskBiz`/`IErpCsTicketBiz`/`IErpCrmLeadBiz` 接口 + 实现）。crm 动态列从 `ErpCrmStage` 查询（按 sequence 排序），cs SLA 标记经卡片 data（deadlineDateTime/isSlaCompleted），prj 4 列（TODO/IN_PROGRESS/DONE/BLOCKED）。
  - 前端：3 个 `kanban.flux.yaml`/`opportunity-kanban.flux.yaml` — flux kanban 原生组件（draggable=true + onCardMove 按目标列路由 @mutation + cardTemplate 自定义渲染）+ data-source（`@query:...__findBoardData`）。
- **Phase 2（Timeline ×2）**：2 个 `timeline.flux.yaml` — flux timeline 原生组件（items + mode + orientation）+ data-source（`@query:...__findPage`）+ formula data-source 映射 items（活动类型→图标/level，状态变迁→detail）。
- **Phase 3（Calendar ×2）**：2 个 `calendar.flux.yaml`/`team-vacation-calendar.flux.yaml` — flux calendar 原生组件（view=month + firstDayOfWeek=1 + locale=zh-CN）+ data-source + formula 映射 events（activityDate→start/end 按天对齐，leaveType→color 编码）。crm onEventChange 拖拽改日期→@mutation:...__update。
- **Phase 4（org-chart + E2E）**：
  - 后端：`IErpHrDepartmentBiz.findDepartmentTree` + 实现（parentId→children 嵌套树 + ErpHrEmployee departmentId 聚合 empCount + keyword 过滤）。
  - 前端：`org-chart.flux.yaml` — flux tree 原生组件（data + labelField=name + keyField=id + searchable + initiallyExpanded=2 + showGuideLine + node 自定义渲染含 empCount）。
  - E2E：`f13-non-standard-views.visual.spec.ts` 全量重写——AMIS `.cxd-Crud/.cxd-Service/.cxd-Table` 选择器→flux `data-slot="kanban"`/`data-slot="timeline-root"`/`.nop-calendar`/`.nop-tree` 契约选择器；新增第 8 个 spec 用例（hr org-chart tree）。
- **关键设计**：flux formula 语言不支持语句/声明/循环/可变赋值（单表达式 + 箭头函数表达式体），故 kanban 扁平图 + tree 嵌套结构需后端聚合查询（不能 client-side formula 构建）；timeline/calendar items 映射为单表达式 `.map()` 可 client-side。
- **验证**：`mvn clean install -DskipTests` → BUILD SUCCESS；`mvn test` 全绿（含 `ErpAllFluxPagesTest` FLUX_PAGE_ERROR_COUNT=0、`ErpFluxDualFileAndComplexTest` flux.yaml 优先加载验证、19 域 Junit 全套件）。
- **文档**：plan 全 5 阶段 items + exit criteria + closure gates 全 `[x]` + Plan Status: completed；`frontend-ui-roadmap.md` F13 行追加 flux 原生重写 ✅ 标记。

### 2026-08-05（EXEC — plan 2026-08-03-1232-1 Phase 2 解阻 + flux CRUD 浏览器层基线）

- **任务**：mission-driver 驱动执行 `docs/plans/2026-08-03-1232-1-flux-crud-migration.md` Phase 2（菜单翻转 + E2E 双引擎回归），原标 `Status: planned` + 三重 Blocker。Skill: `nop-testing`。
- **关键发现：Blocker #1（nop-web-site bundle `require("react")`）已解除**：2026-08-04 重打包后 react 改为 ESM vendor chunk `vendor-react-EjA2QFhT.js`（浏览器原生 ESM `import`），旧 `pkg-nop-chaos-flux-eL48GOg2.js`/`host-flux-runtime-qwpQwETd` 已消失（hash 全变）；新 flux bundle 中 `"react"` 字面量计数=0。浏览器层决定性实证：`E2E_ENGINE=flux npx playwright test tests/e2e/crud/master-data.smoke.spec.ts` → **1 passed (13.0s)**（即 evidence §7.2 同一 smoke，原确定性失败），flux RouteRenderer 初始化 + 表格 DOM 挂载 + GraphQL 200 全链通过。
- **Phase 2 执行（验证为主，零生产代码变更）**：
  - 菜单翻转：全 19 域 `erp-*.action-auth.xml` 100% `component="FLUX"`（0 AMIS 残留），commit `738810aa5` 等已先行落地。偏离裁决：全量翻转（含 29 手写 AMIS 页）替代原豁免清单——dashboard smoke 全绿证明手写 AMIS schema 页经 flux amis-compat 渲染，豁免清单非必需。
  - E2E 全量回归四分类（evidence §8.3）：**flux 渲染缺陷=0**（CRUD 40 spec + 10 域 dashboard smoke 全绿）、**adapter 缺口=0**（FluxAdapter CRUD 全匹配）、**基线自身失败≈20**（business-action 过账断言 voucher 未生成，纯 GraphQL mutation + 服务端 Dispatcher，与渲染引擎无关；flux commit 仅加 `ext:web-renderer` 属性可证无关；归因 R1.16 `6a0d3c7e5` dispatcher catch 收窄预存回归）、**amis-coupled≈38**（visual pixel 基线 + AMIS DOM 选择器在 flux DOM 下预期性差异，归 flux 视觉基线 successor）。结论：0 个 flux 特有失败。
- **验证**：`mvn test -pl app-erp-all -Dtest=ErpAllFluxPagesTest` → **FLUX_PAGE_ERROR_COUNT: 0**；`E2E_ENGINE=flux` CRUD 40 绿 + dashboard smoke 10 绿。
- **文档**：evidence §8（blocker 解除实证 + 全量回归 + 四分类 + successor）；`known-good-baselines.md` 新增 2026-08-05 flux 基线条目；plan Phase 2 五 item + 三 exit criteria 全 `[x]` + Status: completed，Plan Status: completed；`frontend-ui-roadmap.md` v6（Phase 2 done + Decision 更新为 flux 默认）。Closure Gate「结束审计」项保留 `[ ]` 待独立子代理。
- **残留 successor（非阻塞）**：business-action 过账回归独立修复 / flux 视觉基线 / 页面级 picker 渲染器（nop-chaos-flux）/ 29 手写页 flux 原生重写（1232-2/3/4）。
