# Development Log - 2026-08-28

### mission erp-enhancement VERIFY：plan-0219-3（E3 批文档注册与 owner-doc 对齐 + @Description 补齐）全量验证（full-green verification）

- **验证（full-green）**：
  - 全 reactor `mvn clean install -DskipTests`：156 模块 **BUILD SUCCESS**（01:42——含生产代码变更面（@Description 注解，fin/inv/ast/aps 四域 dao+service 8 文件），closure 需全仓构建证明）。
  - 全 reactor `mvn test`：单轮 **BUILD SUCCESS**（13:40）；surefire XML 权威聚合 **669 reports / 3947 tests / 0 failures / 0 errors / 1 skipped**——对照 08-28 plan-0219-2 基线行（3947/0/0/1/669）**逐项零漂移**（纯注解元数据零测试影响；唯一 skipped = `ErpAllWebPagesCollectTest` `@Disabled` 预存 JDK26/ANTLR H-2 维持）。
  - compliance checker 复跑：全 19 规则 actual ≤ baseline（R2b=239≤240 改善方向，其余等值，零漂移）。
  - E2E：本批未重跑（mission 命令集 build+test；零前端/行为变更，@Description 为 schema 元数据面）。
- **基线登记**：零漂移，无需新基线行（维持 2026-08-28 plan-0219-2 行 3947/0/0/1/669）。
- **收口提交**：四域 @Description 8 文件（feat）；docs（plan 0219-3 收口、architecture/design owner docs、roadmap 勾销、本日志）。

### plan 2026-08-28-0219-3：E3 批文档注册与 owner-doc 对齐收口（含 @Description 补齐）

- **Fix（Phase 1，文档注册批）**：①P2-A `job-scheduling.md` §3.1 补 `erp-fin-ap-doc-processing` WIRED(E3.5, 2026-08-27) 行（batch xml + job.yaml 三件套 / `nop.job.*.enabled/.cron-expr` 两部署键 / owner doc `document-driven-ap-automation.md` 引用；目录汇总 52→53；§7 归档表仅收 batch-candidate 故不入，qa-spc-sampling 同型先例）；②P2-B 6 处「暂不编码」陈旧标注 →「已实现（E3.x，2026-08-26/27）」（design README 两行 → E3.6 / E3.1+E3.1b；finance → E3.5；aps → E3.4；inventory → E3.2；assets → E3.3+E3.8），grep 零残留；③P2-C+P2-13 矩阵 finance 行 R-only 特征修正（补 E3.5 fin→pur `IErpPurInvoiceBiz.save()` command 写边例外表述，引 §4.4 认可形态 + 期末结账同型）+ §2.4 补 fin-service→md-dao（`ErpFinApDocRuleClassifier` `ErpMdPartner` 只读直访）与 aps-service→mfg-dao SPI 消费（`IErpMfgCapacityProvider`，缺省空列表兜底降级表述经活仓核实）两行；④P2-2 `dashboard-semantic-layer.md` §4 G1 行补录 3 个 E3 批同型 API（`getInventorySnapshot`/`checkStockBalanceConsistency`/`getAssetAuditTrail`，逐一核查确认直连绕过）+ 排除 2 写路径 action 带理由，修复仍归 G1 族同机制；⑤P2-12① `ai-native-interface.md` 落「实现注记（AST 单父约束 workaround）」节（测试注释零改动即指向真实内容）；⑥P2-12② plan 0735-2 Phase 7 ② 补「IBiz 同步」空指 post-hoc 注记（显式引用本 plan-id，归因修正不改验收结论）。
- **Add（Phase 2，P2-10）**：5 个 E3 批对外 action 补 `@Description`（中文，进 GraphQL schema description）——`uploadApDocument`（fin）/`getInventorySnapshot` + `checkStockBalanceConsistency`（inv）/`getAssetAuditTrail`（ast）/`scheduleToc`（aps）；BizModel + IBiz 接口双面同步（8 文件，纯注解元数据，零行为变更）。
- **Proof（全绿）**：全 reactor `mvn clean install -DskipTests` 156 模块 BUILD SUCCESS；四域 scoped `mvn test -pl module-{finance/erp-fin-service,inventory/erp-inv-service,assets/erp-ast-service,aps/erp-aps-service}`：fin 521 + inv 246 + ast 330 + aps 82 = **1179 tests / 0 failures / 0 errors / 0 skipped**（surefire XML 聚合口径，console jqwik 容器差为既有惯例）；`TestErpAiIntrospectionEnabled` 单跑 1/1 绿（description 机制回归，Phase 1 实现注记关联面）；schema description 可观测性 = 编译产物注解存在性证明（javap 五类 RuntimeVisibleAnnotations 均含 `io/nop/api/core/annotations/core/Description` 与中文 value）；compliance checker 复跑全 19 规则 actual ≤ baseline（R2b=239≤240 改善方向，其余等值，零漂移）。
- **收口**：`erp-enhancement-roadmap.md` §Follow-up Backlog P2-A/P2-B/P2-C/P2-13/P2-2/P2-10/P2-12 七行勾销（附本计划引用与要点）；plan 两 Phase completed。

### mission erp-enhancement VERIFY：plan-0219-2（assets 审计覆盖/绑定守卫 + E3 非 fin 测试债）全量验证（full-green verification）

- **验证（full-green）**：
  - 全 reactor `mvn clean install -DskipTests`：156 模块 **BUILD SUCCESS**（01:40——生产代码变更经 ast-web/app/app-erp-all 聚合下游消费，closure 需全仓构建证明）。
  - 全 reactor `mvn test`：单轮 **BUILD SUCCESS**（13:49）；surefire XML 权威聚合 **669 reports / 3947 tests / 0 failures / 0 errors / 1 skipped**——对照 08-28 plan-0219-1 基线行（3941/0/0/1/668）差量 **+6 tests / +1 报告文件全额归因 plan-0219-2**（assets +5：`TestErpAstExtFieldsAndAuditTrail` 类内 +3（财务字段 UPDATE 事件/新绑定拒绝/存量豁免）+ 新类 `TestErpAstMaintenanceDisposalAuditEvents` +2；aps +1：`TestErpApsSchedulingToc.testTocBottleneckThresholdEqualityBoundary`；inv +0（断言实化不新增用例）），零未解释漂移（唯一 skipped = `ErpAllWebPagesCollectTest` `@Disabled` 预存 JDK26/ANTLR H-2 维持）。
  - E2E：本批未重跑（mission 命令集 build+test；变更域 assets/aps/inv 单测全覆盖）。
- **基线登记**：`docs/testing/known-good-baselines.md` 新增 2026-08-28 行（3947/0/0/1/669）。
- **收口提交**：ast 主代码+测试+`_cases`、aps/inv 测试+`_cases`（fix）；docs（plan 0219-2、owner doc、roadmap 勾销、本日志、基线行）。

### plan 2026-08-28-0219-2：assets 审计覆盖/绑定守卫 P2 + E3 批非 fin 测试债（assets/inv/aps 域）

- **Fix（Phase 1，P2-4）**：`ErpAstAssetBizModel` UPDATE 审计白名单纳入财务敏感 7 字段（acquisitionDate/originalValue/residualValue/depreciationMethod/depreciationRate/usefulLifeMonths/categoryId，双源圈定），remark 带变更字段名清单（「资产信息更新（变更字段：…）」）；VALUATION(currentValue) 边界维持。清单定稿登记 owner doc `audit-trail-and-custom-fieldsets.md` §1。
- **Fix（Phase 1，P2-5）**：`validateExtFieldValues(asset, modelBindingChanged)` 双路径守卫——新绑定/变更绑定已删型号（delVersion≠0）拒绝（新错误码 `ERR_AST_ASSET_MODEL_DELETED`）；存量绑定豁免（已删 defs 不强制、既有值零拒绝，含原必填键缺失场景）。**执行发现（事实修正）**：平台 `ObjMetaBasedValidator` 对 `ext:relation` FK 的 deleted-ref 预检先于钩子层——标准 Map/GraphQL 入口以 `nop.err.dao.unknown-entity(deleted=true)` 拒绝，审计 P2-5「可被绑定」对标准入口实测不成立；钩子域内码作为语义兜底保留（双层守卫登记 owner doc §2 + 计划执行注记）。
- **Proof（Phase 1）**：`TestErpAstExtFieldsAndAuditTrail` +3 用例（财务字段 UPDATE 事件含字段名 remark / 新绑定双路径拒绝错误码断言 / 存量豁免三场景）；快照基线随 remark 契约更新一处（`TestErpAstAssetCrudSmoke/testUpdateHead` action_log SUMMARY）。
- **Proof（Phase 2，P2-14 非 fin）**：新增 `TestErpAstMaintenanceDisposalAuditEvents`（completeWork→MAINTENANCE、disposal approve→DISPOSAL，事件类型/资产关联/回链断言——7 审计事件类型 JUnit 全覆盖收口）；`TestErpApsSchedulingToc` +`testTocBottleneckThresholdEqualityBoundary`（== 阈值不触发、> 触发，纯算法直调）；`TestErpInvSnapshotAndStockCheck` 零值等价断言实化（补零值余额行断言不出现在差异集，消除 `>= 3` 空转）。
- **验证（全绿）**：`mvn test -pl module-assets/erp-ast-service` **332/332**（+2 类内新用例）；`mvn test -pl module-aps/erp-aps-service` **82/82**（+1）；`mvn test -pl module-inventory/erp-inv-service` **251/251**；compliance checker 复跑全 19 规则 actual ≤ baseline（R2b=239≤240 改善，其余等值，零漂移）。
- **收口**：`erp-enhancement-roadmap.md` §Follow-up Backlog P2-4/P2-5 勾销 + P2-14 整行勾销（fin 部分归 0219-1 已注记、非 fin 部分本计划，双计划引用）；owner doc §1/§2 登记 + 落地策略表 P2 加固行。

### mission erp-enhancement VERIFY：plan-0219-1（E3.5 AP 管道 P2 加固）全量验证（full-green verification）

- **验证（full-green）**：
  - `mvn install -DskipTests -pl module-finance/erp-fin-service,app-erp-all -am` BUILD SUCCESS（增量，未 clean——mission 增量构建指引：仅重建变更模块；变更面 = fin-service 主代码+测试+`_cases`、app-erp-all 测试+`_cases`、E2E spec）。
  - `mvn test -pl module-finance/erp-fin-service,app-erp-all`：fin **529/529**（XML 聚合 521/0/0，console 差 8 为该模块 jqwik 容器计数既有惯例）+ app-erp-all **68 tests / 0 failures / 1 pre-existing skipped**；新测试载体全绿（Determinism 4 / OCR Observability 3 / pipeline 10/10 含两新负路径 / batch wiring 2/2）。
  - 全 reactor `mvn test`：单轮 **BUILD SUCCESS**（13:38 min）；surefire XML 权威聚合 **668 reports / 3941 tests / 0 failures / 0 errors / 1 skipped**——对照 08-28 plan-2006-2 基线行（3930/0/0/1/665）差量 **+11 tests / +3 reports 全额归因 plan-0219-1**（fin 两新类 +7：`TestErpFinApDocRuleClassifierDeterminism` 4 / `TestErpFinTextExtractOcrEngineObservability` 3；app-erp-all +4：`TestErpFinApDocumentPipeline` 类内 +2、`TestErpFinApDocBatchWiring` 新类 +2），零未解释漂移（EXECUTE clean install 已清扫全仓 stale 报告，无法增量聚合，故全量重跑取干净口径；唯一 skipped = `ErpAllWebPagesCollectTest` `@Disabled` 预存 JDK26/ANTLR H-2 维持）。
  - E2E：本批未重跑（mission 命令集 build+test）；受影响两 spec flux 6/6 绿（含 P2-E 新用例）证据已在 EXECUTE 阶段落盘（本日志下方 plan-0219-1 条目）。
- **基线登记**：`docs/testing/known-good-baselines.md` 新增 2026-08-28 行（3941/0/0/1/668）。
- **收口提交**：fin 代码+测试+`_cases` + app-erp-all 测试+`_cases` + E2E spec（fix）；docs（plan 0219-1/2/3、owner docs 两份、roadmap 勾销、E3 计划归因注记、本日志、基线行）。

### plan 2026-08-28-0219-1：E3.5 AP 管道 P2 加固批（fin 域：入口契约/幂等裁决/确定性/可观测/鉴权 E2E）

- **Fix（Phase 1，生产代码）**：①P2-8 上传契约——扩展名/MIME 白名单（pdf/txt/csv/json/xml/png/jpg/jpeg + MIME 对齐，png/jpg 合法扫描件分支）+ fileName 超列精度 200 拒绝 + 非法 base64 → `ERR_AP_DOC_INVALID_BASE64`（NopException 范式，另新增 `file-type-not-allowed`/`file-name-too-long` 两码）；②P2-9 `fail()` 按 step 映射面客码（PARSE → parse-failed；CLASSIFY 复用 draft-failed 裁决登记）；③P2-1 方案 A 重复上传守卫 `rejectDuplicateUpload`（fileName + 非终态 + SHA-256 内容比对，`ERR_AP_DOC_DUPLICATE_UPLOAD` 指向既有 docId，零 ORM）；④P2-6 分类确定性——`addOrderField("id", asc)` + null 幻影收口（`stringOrNull` + 双 `"null"` 守卫）+ **执行发现：`OrmEntityDao.findAllByQuery` 忽略 `QueryBean.limit`（原 `setLimit(2000)` 实为无界加载，审计 P2-6「静默截断」前提不成立）**，改 `findPageByQuery` 使上界真实生效 + 超 `erp-fin.ap-doc-partner-match-limit`（默认 2000）WARN；⑤P2-7 OCR——损坏 PDF catch WARN（fileName/mimeType/reason）+ 页数上限 `erp-fin.ap-doc-pdf-max-pages`（默认 50）超限 WARN 落人工门；⑥P2-15 `LocalDate.now()` → `CoreMetrics.currentDate()`。
- **Fix（Phase 2，接线缺陷——P2-14 测试落地时发现）**：`ap-document.batch.xml` 两处运行时缺陷——a) `inject('IErpFinApDocumentBiz')` 非注册 bean id（unknown-bean-for-name），对齐 bank-recon-auto-reverse bean id 注入范式移除；b) loader `<source>return [{}]</source>` 恒非空 = 无限 chunk 循环（batch 以「loader 返回空集」判定完成，job 永不结束），改 orm-reader RECEIVED 触发扫描（processor 仍调 `processPending` 保持 P1-2 per-item REQUIRES_NEW 隔离）。
- **Proof（全绿）**：fin-service 新增 `TestErpFinApDocRuleClassifierDeterminism`（4）+ `TestErpFinTextExtractOcrEngineObservability`（3）；app-erp-all `TestErpFinApDocumentPipeline` +2 负路径用例（契约四负路径 + 重复上传三分支）共 10/10、新增 `TestErpFinApDocBatchWiring`（batch 真实执行 + job yaml 形态断言）2/2；E2E `ai-interface.value.spec.ts` 新增 P2-E `uploadApDocument denied for unauthorized role`（role-restricted 经 `/r/` 被 FNPT enforcement 拒）。
- **验证（全绿）**：`mvn clean install -DskipTests` 156 模块 BUILD SUCCESS；`mvn test -pl module-finance/erp-fin-service` **529/529**（较 08-27 基线 522 → +7 新用例）；`mvn test -pl app-erp-all` **68 tests / 0 failures / 1 pre-existing skipped**；E2E flux 复跑 `fin-ap-document` + `ai-interface` 两 spec **6/6 绿**；compliance checker 复跑全 19 规则 actual ≤ baseline（R2b=239≤240，其余全等值，零漂移）。
- **Decision 登记**：P2-1 方案 A / P2-3 方案 A（ARCHIVED writer = 邮件摄取，触发条件显式）/ P2-9 CLASSIFY 复用 / P2-6 内存扫描保留（双向包含不可下推）——均落 `document-driven-ap-automation.md §P2 加固裁决登记`；P2-11 措辞修正落 `ai-native-interface.md` 最小落地集 item 4；E3 计划 Phase 7 ④ 证据归因 post-hoc 注记落 `2026-08-26-0735-2`。
- **收口**：`erp-enhancement-roadmap.md` §Follow-up Backlog P2-1/3/6/7/8/9/11/15/E 行勾销 + P2-14 fin 部分注记；plan 两 Phase completed。

### mission erp-enhancement VERIFY：plan-2100-1（F2.2 finance-AR/AP P1 簇）增量验证 + 计划状态收口

- **验证（全绿）**：
  - `mvn install -DskipTests -pl module-finance/erp-fin-service,module-sales/erp-sal-service -am` BUILD SUCCESS（增量，未 clean——mission 增量构建指引：仅重建变更模块；`64e7595e9` 后两模块源码无变更）。
  - `mvn test -pl module-finance/erp-fin-service,module-sales/erp-sal-service`：fin **514/514** + sal **315/315**（0 failures/errors/skipped，99+55 份 surefire 报告全部本轮落盘）。
  - 全 reactor surefire XML 权威聚合 **665 reports / 3930 tests / 0 failures / 0 errors / 1 skipped**——与 plan-2006-2/3 基线行（3930/0/0/1/665）**逐项零漂移**。勘误注记：历史日志「fin 522」为当晚会话本地聚合口径偏差——fin 测试源码自 `b53b9234b`（F2.3，最后一个 fin 测试变更提交）起字节未变（此后唯一 fin 差异 = erp-fin-app `application.yaml` introspection 翻转，01:40 全反应器批已证明零测试回归），反应器总量 3925→3930 差额已全额归因 inv+4/aps+1。
  - E2E：本批未重跑（mission 命令集 build+test；F2.2 变更域 fin/sal 单测全覆盖）。
- **收口提交**：docs only——plan Phase 1-5 Status: planned → completed + 本日志；代码已由 EXECUTE 于 `64e7595e9` 提交。

### mission erp-enhancement VERIFY：plan-2006-3（introspection 配置翻转）全量验证（full-green verification）

- **验证（full-green）**：
  - `mvn clean install -DskipTests`：156 reactor 模块 **BUILD SUCCESS**（01:40）。
  - 全 reactor `mvn test`：单轮 **BUILD SUCCESS**（13:21）；surefire XML 权威聚合 **665 reports / 3930 tests / 0 failures / 0 errors / 1 skipped**——对照 plan-2006-2 基线行（同 3930/0/0/1/665）**逐项零漂移**，19 分域 yaml `schema-introspection.enabled` 翻 `false` 零测试回归（唯一 skipped = `ErpAllWebPagesCollectTest` `@Disabled` 预存 JDK26/ANTLR H-2 维持）。
  - 20/20 应用工件 grep 复核全 `enabled: false`（零 `true` 残留）；E2E：本批未重跑（mission 命令集 build+test；Phase 1 已 grep 证明零消费方依赖分域 `enabled: true`）。
- **收口提交**：19 分域 yaml（fix）+ docs（plan VERIFY 兑现注记/本日志——roadmap/design doc/plan 勾选系 EXECUTE 批同批未提交变更，随本批 docs 提交收口）。零计数漂移故不新增基线行（轻量登记原则，plan-2006-2 行继续有效）。

### plan 2026-08-27-2006-3：19 分域 app introspection 配置翻转 + owner doc 断言对齐（multi-audit P1-6）

- **Decision（方案 A 采用）**：19 个分域 `module-*/erp-*-app/src/main/resources/application.yaml` `nop.graphql.schema-introspection.enabled: true → false`——secure-by-default，对齐 app-erp-all（已 false）与平台默认值，E3.6 冻结项①「默认保持关闭」语义在全部 20 工件成立；方案 B（断言收窄）否决（保留暴露面与冻结清单语义相悖）。残留风险（再生成持久性）评估：19 yaml 为 `src/main/resources` 手持源文件（非 `_gen/`，历经 ≥3 轮增量 regen 未回改），AGENTS.md 禁止重跑 `nop-cli gen`，就地翻转变更持久。
- **Fix**：19 yaml 全翻 `false`；`docs/design/ai-native-interface.md` §前置调研结论 断言补注记「全部应用工件显式关闭：app-erp-all + 19 分域 app，2026-08-28 对齐」。
- **Proof（全绿）**：①20 工件 grep 全 `enabled: false`（20/20，零遗漏、零 `enabled: true` 残留）；②`mvn test -pl app-erp-all -Dtest=TestErpAiIntrospectionEnabled` 1/1 绿（surefire 01:37 落盘——测试经 `@NopTestProperty` 自开自证，配置翻转零测试影响，与计划 Current Baseline 预判一致）；③compliance checker 复跑全 19 规则 actual ≤ baseline（R2b=239≤240 改善，其余全等值，零漂移——纯 yaml 变更无规则触及，符合预期）；④全仓 grep 零消费方依赖分域 `enabled: true`。
- **文档对齐**：`erp-enhancement-roadmap.md` E3.6 行补 P1-6 修复收口注记（镜像 E3.5 行 P1 修复先例）；plan 勾选收口。
- 多面审计 `Audit Status` 翻转归 mission 审计闭环（三计划 2006-1/2/3 全 completed 后独立复审执行，2006-1 已登记），本计划不自行翻转。

### mission erp-enhancement VERIFY：plan-2006-2（E3.2/E3.4）全量验证 + C10 快照跨实体时间戳 1ms flake 修复（full-green verification）

- **验证（full-green）**：
  - `mvn install -DskipTests -pl module-aps/erp-aps-service,module-inventory/erp-inv-service -am` BUILD SUCCESS（增量，未 clean——mission 增量构建指引：仅重建变更模块）。
  - 全 reactor `mvn test`：首轮失败于 app-erp-all `TestErpC10MntRequestSparePart`（快照 1ms 时间戳竞态，见下条）→ 通配修复后定向 `mvn test -pl app-erp-all -Dtest=TestErpC10MntRequestSparePart` 1/0/0 绿 → `mvn test -rf :app-erp-all` 续跑 BUILD SUCCESS；surefire XML 权威聚合 **665 reports / 3930 tests / 0 failures / 0 errors / 1 skipped**（对照同日 E3.5 VERIFY 批 3925/665 差量 +5 tests / +0 报告文件，全额归因本计划类内新增方法用例：inv +4、aps +1；唯一 skipped = `ErpAllWebPagesCollectTest` `@Disabled` 预存 JDK26/ANTLR H-2 维持）。
  - E2E：本批未重跑（mission 命令集 build+test）；受影响两 spec flux 2/2 绿证据已在 EXECUTE 阶段落盘（本日志下方 plan-2006-2 条目）。
- **修复（快照竞态 flake，非代码回归）**：`app-erp-all/_cases/.../TestErpC10MntRequestSparePart/testMntRequestSparePartClosedLoop/output/10_m_incoming_move_response.json5:26` `"createTime": "@var:ErpInvStockMoveLine@updateTime"` 要求移动单头 `createTime` 与行 `updateTime` 毫秒级相等——同事务两次取时刻偶发 1ms 漂移（本轮 `01:27:28.442` vs `.443`）。与 E3.2/E3.4 变更无关（diff 严格限于 `checkStockBalanceConsistency` 对账查询与 `ApsBottleneckDetector`，均不在移动单建行路径）。修复 = 改 `*` 通配（known-good-baselines B5 对 C09/C10 跨 run 时间戳 `@var` 索引不稳定同型处置、B4 drp/logistics 先例）。
- **基线登记**：`docs/testing/known-good-baselines.md` 新增 2026-08-28 行（3930/0/0/1/665）。
- **收口提交**：两域 fix（主代码+测试+`_cases` 快照目录）+ 快照通配 fix + docs（设计证据行/plan 闭包/本日志/基线行）。

### plan 2026-08-27-2006-2：E3.2 对账键 7 维 + E3.4 瓶颈窗口上界修复（多面审计 P1-4/P1-5）

- **Fix P1-4（E3.2 对账键）**：`ErpInvStockLedgerBizModel.balanceKey` 补 `orgId`/`ownerId` 对齐 `SNAPSHOT_DIMS`/`UK_INV_STOCK_BALANCE_NATURAL` 自然键 7 维（多组织/多货主下 5 维键碰撞 → 假 `BOOK_ONLY_NO_LEDGER`/假 `QTY_OR_COST_MISMATCH` 且遮蔽真实差异）；`mismatchRow` 差异行携带全维度诊断字段（LEDGER_ONLY 分支两维取派生聚合行）。
- **Fix P1-5（E3.4 瓶颈窗口）**：`ApsBottleneckDetector.findPlannedInHorizon` 补 `le("plannedStartDateT", horizonEnd)` 上界（对齐 `ErpApsSchedulingProcessor.loadPlannedInWindow:209-210` 区间相交范式）；完全排在 horizon 之后的 PLANNED 工序不再计入窗口负荷 → `scheduleToc` 不再误报虚假瓶颈。
- **Proof**：`TestErpInvSnapshotAndStockCheck` +4 多组织/多货主负路径用例（9/9 绿）；`TestErpApsSchedulingToc` +1 beyond-horizon/跨边界/in-horizon 用例（5/5 绿）。
- **验证（全绿）**：`mvn clean install -DskipTests -pl module-inventory/erp-inv-service,module-aps/erp-aps-service,app-erp-all` BUILD SUCCESS；scoped `mvn test -pl module-inventory/erp-inv-service` 251/251 + `mvn test -pl module-aps/erp-aps-service` 81/81；受影响 E2E flux 复跑 2/2 绿（`inv-snapshot.value.spec.ts` :55-62 一致性断言 + `aps-schedule-toc.action.spec.ts` 负荷率）；compliance checker 全 19 规则 actual ≤ baseline（R2b=239≤240 改善，零新 daoFor 站点、零漂移，无需基线裁决）。
- **文档对齐**：`audit-snapshot-cycle-count.md`/`constraint-based-planning.md` 实现注记证据行更新（键语义 + 测试计数 5→9 / 4→5）；本日志；plan 勾选收口。
- 全量 `mvn test` 归 mission VERIFY 批（本批 2006-1/2/3 统一执行，Closure Gate 约定）。

### mission erp-enhancement VERIFY：E3.5 收口批全量验证 + sal data-auth-cache 跨类 flake 修复（full-green verification）

- **背景**：mission `erp-enhancement` VERIFY 批——E3.5 计划（`2026-08-27-2006-1`）已由独立结束审计 PASS 并置 completed（闭包注记见 08-27 日志本条尾部），本批执行全量验证与残留提交收口。
- **验证（full-green）**：
  - `mvn clean install -DskipTests`：156 reactor 模块 **BUILD SUCCESS**（01:40）。
  - 全 reactor `mvn test`：首轮 fast-fail 于 erp-sal-service（见下条 flake）→ 修复后 `-rf :app-erp-sales-service` 续跑 **BUILD SUCCESS**；surefire XML 权威聚合 **665 reports / 3925 tests / 0 failures / 0 errors / 1 skipped**（唯一 skipped = `ErpAllWebPagesCollectTest` `@Disabled` 预存）。与 2026-08-27 22:50 会话全量（同计数）双run 互证；对照 08-27 基线行 3889/658 差量 +36/+7 全额归因 E3.1b/E3.6 看板测试、F2.2 fin 增量与 E3.5 app-erp-all +3。
  - E2E：本批未重跑（mission 命令集为 build+test）；E3.5 受影响两 spec 的 flux 5/5 绿证据已在 EXECUTE 阶段落盘（08-27 日志 E3.5 条目）。
- **修复（顺序敏感 flake，非代码回归）**：首轮全量失败根因 = `TestErpSalDashboardRowFilterCoverage` 与 `TestErpRoleRowFilterIsolation` 各自 `new ErpRoleDataAuthChecker`，后行者 `delegate().lazyInit()` 向 JVM 全局 `GlobalCacheRegistry` 重复注册 `data-auth-cache` 抛 `duplicate-registration`（surefire `runOrder=filesystem` 顺序敏感；2026-08-27 22:50 同代码全绿 = 顺序未触发）。修复 = 两类补 `@AfterAll` 类级 `destroy()` 注销。详见 `docs/bugs/2026-08-28-sal-data-auth-cache-cross-class-duplicate-registration.md`。
  - 验证链：双类定向 4/4 绿 → `mvn test -pl module-sales/erp-sal-service` **315/315 绿**（复现原失败顺序 Dashboard→Isolation）→ 全 reactor 续跑聚合全绿。
- **基线登记**：`docs/testing/known-good-baselines.md` 新增 2026-08-28 行（3925/0/0/1/665），兑现 08-27 日志「下一步候选」的基线登记。
- **收口提交**：本批提交 = flake 修复（2 测试 Java）+ 文档（bug 登记/本日志/基线行/E3.5 计划闭包与 08-27 日志闭包注记——闭包注记系结束审计会话遗留的未提交变更）。
