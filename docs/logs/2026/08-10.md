# Development Log - 2026-08-10

### P2.2b 角色化渐进（角色账号池 + loginAsRole 真实映射）执行完成（mission permissions-enforcement，账号池与角色登录层）

- **工作项**：P2.2b 角色化渐进——E1.1 五高危域 8 授权角色账号 + 1 通用受限账号种子 + `loginAsRole` 真实角色登录（填充 P2.3 占位）（plan `docs/plans/2026-08-10-0119-1-role-account-pool-and-loginas-role.md`）
- **类型**：implementation-only change（2 auth CSV 种子扩展 + TS 登录 indirection 填充 + 1 Proof spec + owner doc；不改 Java/ORM/契约/action-auth 声明，不翻转任何 enforcement 开关——三开关保持 OFF）—— 不写 BizModel/Processor/xbiz（纯 CSV 种子 + `.ts` 测试 + `.md` 文档，均 allow）
- **产出**（三 Phase 全落地）：
  - **Phase 1 账号集裁决 + CSV 种子扩展**：`nop_auth_user.csv` 追加 9 行（userId 2-10，username `role-<slug>`，PASSWORD/SALT 复用 nop 行 BCrypt 原值）+ `nop_auth_user_role.csv` 追加 9 行绑定（roleId 字面值逐字匹配 `nop_auth_role.csv`）。两项 Decision（账号集 = E1.1 五域 8 角色 + 1 受限；密码 = 复用 nop hash）落地。种子只追加不改 nop 原行
  - **Phase 2 loginAsRole 填充 + 运行时 Proof**：`negative/_helper.ts` 占位（L200-204）替换为真实 `ROLE_ACCOUNTS` 映射（key = roleId 字面值 + username 别名 + 通用别名）+ 防御性会话清空（clearCookies + localStorage try-catch 容错 about:blank SecurityError）+ 委派 `login(page, username, password)` + 未知 key 回退 restricted；新建 `tests/e2e/negative/role-login.smoke.spec.ts`（14 tests：9 账号 `LoginApi__login` GraphQL roleInfos 断言 + 双命名空间 Proof + 密码 "123" 往返 + `loginAsRole` fresh page 登录可演示经 `__Host-nop-token` JWT cookie `preferred_username` 解码 + 未知 key 回退）
  - **Phase 3 owner doc + 日志**：`e2e-runbook.md` loginAsRole 行（占位→真实）+ 新增「角色账号池」节（9 账号清单表 + E1.2 扩展指引 + 双命名空间注记）；`roles-and-permissions.md` §运行基线增 P2.2b 注记；本日志条目
- **关键证据（Proof）**：`role-login.smoke.spec.ts` 14 tests 全绿（flux 引擎）——(1) 9 账号 `LoginApi__login` 认证成功 + `userInfo.roleInfos` 含预期业务 roleId（财务员/管理员/B2B 管理员/B2B 对账员/生产主管/库管员/薪酬审批人/HR 专员/user）；(2) **双命名空间 Proof**：`role-biz-admin` → `roleInfos=[{roleId:"管理员"}]`（业务「管理员」），不含 `admin`/`nop-admin`（不触发 skip-check）；(3) `loginAsRole('财务员')` UI 登录后 `__Host-nop-token` cookie JWT `preferred_username="role-finance"` + 业务查询会话可达；(4) P2.3 demo `action-denied.smoke.spec.ts` 仍绿（零回归——'requester' 回退 restricted，action-auth OFF 下身份不影响业务逻辑拒绝载体）
- **发现（运行时 auth 机制）**：`LoginApi__getLoginUserInfo` GraphQL query 经 `page.request`（cookie 共享）调用返回 `nop.err.auth.jwt.invalid-token`（与业务 query `ErpMdCurrency__findPage` 同 cookie 却不同结果——LoginApi 的 JWT 校验路径与业务实体不同）；改用 `page.context().cookies()` 读 `__Host-nop-token` HTTP-only cookie 解码 JWT `preferred_username` 验证 UI 登录身份（Playwright cookies API 不受 httpOnly 限制）
- **验证**：`mvn clean install -DskipTests` 全 reactor BUILD SUCCESS；`mvn test` 全 reactor BUILD SUCCESS 0 failures/0 errors（CSV+TS 变更 0 Java，full-green verification）；`role-login.smoke.spec.ts` + `action-denied.smoke.spec.ts` 全绿
- **边界裁决**：Non-Goals 守住——不翻转任何 enforcement 开关（归 P2.4/E1.x）/ 不做 E1.1/E1.2 真实负向测试（归 E1.x）/ 不覆盖 E1.2 全量域角色账号（归 E1.2 机械扩展）/ 不做 userId==employee.id 对齐（归 E2.2）/ 不新增角色记录（roleId 词表冻结，仅复用）/ 不改 ORM/Java/action-auth 声明 / 不触碰 ORPHANED auth.ts
- **状态**：roadmap `permissions-enforcement-roadmap.md` P2.2b todo→done；plan `Plan Status: completed`，全部 Phase + 退出标准 + Closure Gates 勾选（除「结束审计由独立子代理」项待独立审计）
- **follow-up（非阻塞 successor）**：P2.4 dry-run 门控（直接后继，消费 role-restricted 统计全 403 影响面）/ E1.1 高危分域翻转（负向主体 = 本计划非 admin 账号）/ E1.2 全量域角色账号扩展（按 `role-<slug>` + 小整数 userId 追加 CSV + ROLE_ACCOUNTS 条目）/ userId==employee.id 对齐（归 E2.2）

### VERIFY permissions-enforcement P2.3 + full-green mvn test 基线修复

- **工作项**：VERIFY mission `permissions-enforcement` plan `2026-08-09-2210-2`（P2.3 负向隔离测试框架）+ 修复阻断 `mvn test` 的 nop-entropy 平台回归
- **P2.3 验证**：plan 已 completed（三 Phase 全 done + Closure Gates 全勾选 + 独立结束审计 approved）。本 VERIFY 仅确认构建：`mvn clean install -DskipTests` BUILD SUCCESS；冒烟 demo `E2E_ENGINE=flux npx playwright test tests/e2e/negative/action-denied.smoke.spec.ts` 1 passed（plan Closure 已记录）
- **mvn test 平台回归修复（nop-entropy）**：
  - **根因 A（JunitBaseTestCase schema 缺失）**：nop-entropy commit `7e06450ff`（08-07，ioc:before/after/depends-on 统一为 resolvedDepends）移除 `ioc:after` → `addNextBean` pull-in 机制，致 `DataBaseSchemaInitializer` 在 `ALL_LAZY` 测试模式下不创建 → `NOP_SYS_SEQUENCE` 表不存在 → `TestErpMdCurrencyRefreshRatesDate`（master-data）+ `TestErpFinVoucherTemplateActiveByDate`（finance）NPE。修复：nop-entropy `orm-defaults.beans.xml` `DataBaseSchemaInitializer` 增 `ioc:force-init="true"`（恢复 7e06450ff 前行为）。日志见 `nop-entropy/ai-dev/logs/2026-08/2026-08-10.md`
  - **根因 B（sales 快照数据漂移）**：nop-entropy SQL 规范化（`f9bc8b238` normalizeTableName/normalizeColumnName）修复了列名匹配，致 `ErpSalReturnProcessor.updateUndeliveredQuantity` 的 `setDeliveredQuantity` UPDATE 此前静默失败现正确生效 → `erp_sal_order_line.DELIVERED_QUANTITY` 由 0（bug 快照）变 10（正确值）。修复：re-record `TestErpSalReturnTrace` + `TestErpSalReturnRefundEndToEnd` 快照（DELIVERED_QUANTITY=10 为正确值，测试逻辑确认 delivery 10 单位）
- **验证**：`mvn clean install -DskipTests` → 156 模块 BUILD SUCCESS；`mvn test` → **全 reactor BUILD SUCCESS，0 failures / 0 errors（full-green verification）**
- **状态**：P2.3 plan 已 completed；本日工作 = VERIFY + 平台回归修复，不改 P2.3 范围内文件（plan 的 `.ts`/`.md` 由 EXECUTE 阶段产出，本阶段仅提交 + 修复阻断项）
