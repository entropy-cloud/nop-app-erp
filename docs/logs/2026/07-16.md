# Development Log - 2026-07-16

### 2026-07-16（执行 E2E PageObject 迁移计划 Phase 0-1 — plan 2026-07-15-2246-1）

- **Phase 0 补充：全局引擎切换机制**
  - 新增 `tests/e2e/pages/engine.ts`：`getEngine()` 工厂函数读 `E2E_ENGINE` 环境变量（默认 `amis`），集中化 AMIS/Flux 引擎选择
  - `fixtures.ts` 扩展 `engine` fixture：所有测试可通过 `{ page, engine }` 解构获取引擎实例，无需在各 `_helper.ts` 中硬编码 `new AmisAdapter()`
  - 用法：`E2E_ENGINE=flux npx playwright test` 全局切换到 Flux 引擎，测试代码零修改

- **Phase 1：CRUD 模块迁移到 PageObject**
  - `crud/_helper.ts` 四个公共函数全部重构：`runCrudListSmoke` → `CrudListPage`+`FormDialog`+`engine`；`assertCrudListValues` → `GraphQLClient.findPage`；`runCrudWriteCycle` → `GraphQLClient` 方法链；`runAmisFormWrite` → `CrudListPage`+`FormDialog`+`GraphQLClient`
  - `master-data.write.amis.spec.ts` 移除已不存在的 `id` 表单字段（AMIS 表单已不再暴露 id 输入框）
  - `AmisAdapter.rows()` 修复：移除 `.first()` 限制 + 放宽行选择器为 `tr, .cxd-Table-row, .cxd-List-item`（对齐原 helper 行为）
  - `CrudListPage.findRowByText()` 改用 Playwright `filter({ hasText })` 替手写循环

- **验证结果**：`tests/e2e/crud/` 全 36 tests 运行：**23 passed / 13 failed**
  - 13 失败全部是预存的 `.cxd-Crud` 15s 渲染超时（finance/inventory/crm/cs/hr/manufacturing/quality/maintenance/logistics/contract/drp/aps/cs-kb-suggestion 的 smoke spec），与迁移前基线一致——Phase 3 根因调查目标
  - 0 新回归：所有 GraphQL 驱动测试（list-value/write/amis-form-write）+ 部分 smoke（master-data/purchase/sales/assets/b2b/projects）全绿
  - AMIS 表单写周期（add→edit→delete 全链经 UI）验证通过

- **Phase 2 待办**：business-actions/dashboards/reports/orchestration/visual 的 `_helper.ts` 迁移到 `GraphQLClient`，消除裸 `page.request.post('/graphql')` 调用

### 2026-07-16（2246-1 Phase 3 根因收口——17 CRUD smoke "渲染超时" 真因为 col-not-prop 生产缺陷）

- **根因（干净 server 实证）**：13+ 域 CRUD smoke 长期失败的 `locator.waitFor Timeout 15000ms`（等 `.cxd-Crud`）不是时序/环境问题，而是 `nop.err.xui.grid.col-not-prop`——view.xml grid 列引用不存在的实体属性 → `PageProvider__getPage`→`loadComponentModel` 抛错 → 页面不渲染 grid → 超时。跨 ≥10 份计划长期误诊为"渲染超时环境问题"。诊断法沉淀为 `docs/lessons/05-nop-e2e-failure-log-first-diagnosis.md`。
- **工具**：新增 `tools/parse-nop-errors.mjs`（Nop server 日志结构化错误解析）+ `tools/check-view-cols.mjs`（view.xml grid 列对照 xmeta 全量静态校验，非失败式）。
- **规模**：`check-view-cols.mjs` 全量枚举 = 源文件 **147 个非法列，跨 13 域**（hr 34/crm 20/finance 19/assets 19/mfg 16/mnt 12/qa 10/projects 5/cs 5/b2b 4/md 1/log 1/aps 1）。来源混合：2256-2 误回退 + 更早遗留非法列。
- **修复非均匀（实证）**：`ErpCrmStage` 简单回退 `stageId→stageName`（真实列）；`ErpCsTicketType` 需逐实体核对（FK 实为 `defaultSlaPolicyId`/关系 `defaultSlaPolicy`，非 `slaPolicyId`）。不能机械批量改。
- **处置**：2246-1 Phase 3 根因项标 `[x]`；生产 view.xml 缺陷超出 2246-1 Non-Goal，开 successor `2026-07-16-0012-1-view-grid-col-not-prop-remediation`（draft，待审计）承接全量修复。2246-1 全套件全绿依赖该 successor 完成。
- **验证**：未改生产代码（仅新增 2 工具 + lesson + 计划/日志文档）。干净 server（自起 PID 73412）实证 cs 失败 / master-data 通过的对照；validateAllPages 失败即抛确认 operationId 为首个 col-not-prop。

### 2026-07-16（执行 successor 0012-1：修复全域 view.xml 非法列 — 18 CRUD smoke 全绿）

- **草案审计**：0012-1 经独立子代理（新会话）草案审计 `accept`（0 Blocker/0 Major/1 Minor 已修订）→ active。
- **Phase 1 修复**：147 个 src 非法列全部修复。分派 4 个独立子代理按域簇并行（hr / crm+cs / finance+assets / mfg+mnt+qa+prj+b2b）+ 主代理修 3 小域（aps/md/logistics）。子类：A 真实列回退（operationId→operationName、stageId→stageName、uomId→uoMId 大写 M）、B 删显示属性→关系名（employeeDisplayName→employee、各 xxxCode→关系）、C FK 名不匹配（slaPolicyId→defaultSlaPolicy、currencyId→salaryCurrency）。每域 `check-view-cols.mjs` 自验 src=0。
- **附加预存缺陷（一并修以达成 validateAllPages 全绿）**：① `ErpB2bPartnerProfile` 重复 `<col id="partnerId">`（预存，2256-2 前即双 partnerName）→ 移除冗余；② `ErpMfgSubcontractOrder` 发料/收货参数 dialog 3 个参数 cell（sourceWarehouseId/receivedQty/destWarehouseId）缺 `custom="true"`→报 cell-not-prop，按 ErpHrRecruitment 范式补上（预存，validateAllPages 此前从未启用故未暴露）。
- **新增诊断测试**：`ErpAllWebPagesCollectTest`（validateAllPages 的非 fail-fast 全量收集版，一次性报告所有 col/cell-not-prop + 重复），保留为页面回归诊断。
- **验证（全绿）**：`node tools/check-view-cols.mjs` src 非法列=0；`ErpAllWebPagesTest`（validateAllPages）exit 0 / 0 errors；`mvn install` 全量 + b2b/mfg-web + app-erp-all 均 BUILD SUCCESS；干净 server（自起 PID 93289，runner jar 含全部修复，curl 200）`npx playwright test tests/e2e/crud/ --grep "CRUD list/form smoke"` → **18 passed (2.3m)**，此前失败的 13 域全绿。
- **意义**：跨 ≥10 份计划长期误诊的"17 CRUD smoke 渲染超时环境问题"彻底根因收口并修复（真因 = view.xml 引用不存在实体属性阻断页面渲染）。
- **状态**：0012-1 Phase 1/2 completed，Closure Gates 除"独立结束审计"外全 `[x]`；Plan Status 仍 active，待独立结束审计。2246-1 Phase 3 smoke 障碍已清除，可推进全套件回归 + 基线落盘。

### 2026-07-16（2246-1 Phase 3 收尾——全套件首次 fresh 全绿 + 基线恢复可信）

- **全套件回归**：干净 server（自起 PID 96497，runner jar 含 0012-1 全部修复，curl 200）`BASE_URL=http://127.0.0.1:8011 SKIP_WEBSERVER=1 npx playwright test --workers=1` → **全套件 405 passed (51.9m)，0 失败**。
- **example spec 2 处测试代码修复**（2246-1 Phase 0 创建的迁移示例 `tests/e2e/examples/crud-smoke.example.spec.ts`，原 18 全挂）：① `page.on('response')` 监听器须在 `crud.navigate()` **之前**挂（否则漏掉导航期 GraphQL 调用→`graphqlResponses=0`，对照 `crud/_helper.ts` 正确顺序）；② 2 个实体选错——`maintenance: ErpMntVisitRequest`（无 pages 目录→超时）→`ErpMntVisit`、`aps: ErpApsConstraint`（无标准 CRUD 列表页/新增按钮）→`ErpApsOperationOrder`（均对齐常规 smoke 已验证实体）。修后 example 18 passed。
- **基线落盘**：`docs/testing/known-good-baselines.md` 新增 2026-07-16 行——全套件 405 passed / 0 失败，**Known Failures = none**，明确标注"17 CRUD smoke 渲染超时=预存环境问题"经根因证伪（真因 col-not-prop 已全修）。这是该基线表自 2026-07-10 起首次 fresh 无已知失败的 E2E 全绿行。
- **runbook 更新**：`docs/testing/e2e-runbook.md` troubleshooting 增第 5 条——列表/页面渲染超时（`locator.waitFor Timeout`）勿假设时序/环境，查 server log `errorCode=`（如 col-not-prop）+ `@_loc` 定位源文件，指向 lesson 05 + `parse-nop-errors.mjs`/`check-view-cols.mjs`。
- **2246-1 状态**：Phase 3 全 `[x]`，Closure Gates 除"独立结束审计"外全 `[x]`；Plan Status 仍 active，待独立结束审计。整条链路（2256-2 → 0012-1 → 2246-1）功能面闭环：跨 ≥10 计划误诊的"17 smoke 超时"彻底根因+修复+全绿验证。
- **验证**：仅改 example spec（测试代码）+ 3 个文档（baselines/runbook/计划/日志）；零生产代码/ORM/契约变更。

### 2026-07-16（独立结束审计关闭 plan 2256-2 — FK 显示名整改计划合规收口）

- **背景**：plan `2026-07-14-2256-2` 标记 completed 但属「自我审计」（违反规则 12），3 项 Closure Gates 未闭合（结束审计/结束证据/日志），Closure Audit Evidence 为占位。本日由独立 general 子代理（新会话 `ses_09508c2b8ffeUGDOuCriH4uDXV`，冷重播无执行者上下文）完成结束审计。
- **裁决**：**PASS WITH NOTES**。冷重播逐项对照实时仓库核实：service 层 `@BizLoader`=0、view.xml `xxxName` 列=0、`tagSet="disp"` 18 源模型、生成 xmeta 280 文件含 `joinRightDisplayProp` + `relation.name` 路径属性（`internal="true"`）、附加修复抽查（setMemo 清零/DATETIME→TIMESTAMP/敏感字段 published）成立、规则 11 五点一致、Anti-Hollow + Deferred 诚实。
- **E2E 门控裁决（用户授权）**：Closure Gates「E2E 快照回归 `[x]`」按本义=后端 autotest 快照（force-save-output 重录，已完成）。Playwright 浏览器层回归是后继 `2026-07-15-2246-1` 的独立范围（Phase 3 未跑），记为残留风险，不阻塞关闭——此裁决覆盖 2246-2 计划「依赖 2246-1 全绿」的 Prereqs。
- **回填**（仅 2256-2 计划文档级）：Plan Status 去「自我审计」措辞→「经独立子代理新会话冷重播审计 PASS WITH NOTES」；3 项门控 `[x]`；Closure Audit Evidence 占位替换为完整证据块；Status Note 追加指针（历史执行记录原貌保留）。
- **副产物**：2246-2（专为 2256-2 结束审计而设的计划）其目标已由本次独立审计达成，状态订正为 superseded。
- **验证**：未改任何生产代码/ORM/契约/测试；2256-2 单文件文档级修订。
