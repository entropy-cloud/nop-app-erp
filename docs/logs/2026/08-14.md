# Development Log - 2026-08-14

### Quality SPC 自动调度链接线落地（RC-R1.26，plan `2026-08-14-2304-2`，第一批纯预授权）

- **工作项**：执行 `docs/plans/2026-08-14-2304-2-rc-mr1-r1-26-qa-spc-evaluate-wiring.md` 四 Phase。落地 finding P1-RC-042（UC-QA-09 AC-4/5 SPC 自动调度链断裂——`spc-sampling.batch.xml` processor 段漏调 `spcRuleEngine.evaluate`，生产调度路径永不触达规则评估 → 失控样本不自动 isOutOfControl=true → NCR/CAPA 自动级联断裂）。**治理**：纯调度接线（batch.xml processor 追加 evaluate 调用 + pom test 依赖 + 测试 + 文档），零 ORM 结构/会计过账/删除变更（roadmap 预授权类目自动执行）。
- **Phase 1（三项 Explore 证据）**：**D1 evaluate 调用位置 = 选项 A**（collectSamples → recalculate → evaluate 同 processor 顺序调用——evaluate:89-92 控制限缺失返回 0 依赖 recalculate 先算限；选项 B 独立批任务多一次加载弃）；**D2 batchChunkCtx.serviceContext null 兜底 = 选项 A**（直接透传无需 helper——`BatchTaskRunner.executeAsync:38` → `newBatchTaskContext()` 无绑定上下文 serviceContext==null 实测复核；`SpcRuleEngine.evaluate` 的 context 仅透传 afterCommit 回调，`createNcrAndAction:93-124` 体零读取 context → null 安全）；**Proof 简单名注入解析不成立**（`GlobalFunctions.inject` → `BeanContainerImpl.getBean(name)` 仅按 bean id 精确匹配 + alias 无简单类名回落；R1.23 先例 `inject('erpCrmLeadScoringRecalcHelper')` 成功系因该 bean **id 本身即简单名** `<bean id="erpCrmLeadScoringRecalcHelper">` 非简单名解析机制）→ **采用 plan 预案改 FQCN bean id**。
- **Phase 2（接线落地）**：`spc-sampling.batch.xml` processor 三段调用链（`inject('app.erp.qa.service.spc.SpcSamplingService').collectSamples(...)` → `...SpcControlLimitCalculator.recalculate(...)` → `...SpcRuleEngine.evaluate(item.id, batchChunkCtx.serviceContext)`，**FQCN bean id 修正**——既有生产简单名 inject 同样会在运行时抛 ERR_IOC_UNKNOWN_BEAN_FOR_NAME（job 默认 enabled=false 从未执行故未暴露，与 P1-RC-042 同根因加深））；`erp-qa-service/pom.xml` 补 nop-batch-dsl test-scope（镜像 R1.23）；`erp-qa-spc-sampling.job.yaml` description 同步 evaluate 步骤 + config-gated NCR 语义。分域 `mvn install -DskipTests -pl module-quality/erp-qa-service -am` BUILD SUCCESS。
- **Phase 3（测试矩阵）**：新增 `TestErpQaSpcSamplingEvaluateBatch` 4 组 batch 任务级测试（`IBatchTaskRunner.execute(taskPath)` 真实执行 loader+processor 全链）全绿——①失控样本（mean=20>UCL=13 规则 1）自动标记 isOutOfControl=true+violatedRules="1"；②NCR(sourceType=SPC)+CAPA afterCommit **经 batch chunk 事务提交实测触发**（`AbstractTransaction.commit` → afterCommit() → onAfterCommit listener 同步执行，快照 CSV 佐证：erp_qa_non_conformance 行 SOURCE_TYPE=SPC/SOURCE_CODE=CHART-BATCH-OOC#1/SEVERITY=HIGH/STATUS=OPEN + erp_qa_action 行 NCR_ID=1/ACTION_TYPE=CAPA/STATUS=PENDING）；③受控样本零 NCR 零 CAPA；④config `erp-qa.spc-auto-ncr-enabled=false` 仅标记不建 NCR；⑤幂等二次执行不重复 NCR。**seed 确定性配方（M1 裁决固化）**：<20 样本（recalculate no-op）+ 显式 cl/ucl/lcl + parameterId 非空 + 不 seed 匹配 parameterId 的 APPROVED 检验行。RECORDING→CHECKING 快照（`nop_batch_task` UUID taskKey 非确定性输出表剔除——checker 仅校验已录制文件）。**验证**：erp-qa-service **172 tests 全绿**（168 基线 + 4 新增，TestErpQaSpcOutOfControl/TestErpQaSpcSampling/TestSpcRuleEnginePure/TestErpQaSpcCapability 等零回归）。
- **Phase 4（文档回填）**：arm-index P1-RC-042 → `done (RC-R1.26)`（含修复摘要）；roadmap RC-R1.26 → done ✅（含落地摘要）；owner doc `spc.md` §关键流程 3 补 batch 接线注记（三段调用链 + FQCN inject 语义 + afterCommit config-gated + <20 样本 seed 配方，不修改需求契约段）；`job-scheduling.md` §3.12 增补 `erp-qa-spc-sampling` 行（SCHEDULED + job.yaml 路径 + config 键 + 证据）+ stale 行 `erp-qa-spc-sample-aggregation`（:206-207 + §7 :343）标注 superseded。watch-only 登记：`spc-capability.batch.xml` 同型简单名 inject（`inject('IErpQaSpcCapabilityBiz')`，bean id=`biz_ErpQaSpcCapability`）latent 缺陷非本行范围（job 默认 disabled）。
- **下一步**：全量验证（全仓 `mvn clean install -DskipTests` + compliance checker 复跑）后独立结束审计；MR1 第一批后续 RC-R1.27+ 由 mission driver 继续。
- **mission-driver 复核（2026-08-15）**：分域 172 tests 全绿 + 全量构建 + checker 零漂移复跑确认（见 plan Closure Gates）；独立结束审计（新会话）后计划 `2026-08-14-2304-2` Plan Status → completed。变更待按 code/docs 拆分提交。

### CRM Forecast 区域 tier rollup 落地（RC-R1.25，plan `2026-08-14-2304-1`，第一批纯预授权）

- **工作项**：执行 `docs/plans/2026-08-14-2304-1-rc-mr1-r1-25-crm-forecast-territory-tier.md` 四 Phase。落地 finding P1-RC-039（UC-CRM-10 L1「团队→区域→公司」4 级层次 vs ForecastAggregator 仅 3 级 rollup——territory tier 整支缺失，A4.2.93 运行时证实 + 触发条件 Lead.territoryId(propId 41) 现存已满足）。**治理**：纯 BizModel 聚合逻辑 + 测试，零 ORM 变更（git diff 仅 erp-crm-service 2 Java 文件 + 1 新测试类 + docs），不触会计过账/删除保护区域（roadmap 预授权类目自动执行）。
- **Phase 1（两项 Decision 裁决）**：**D1 区域行生成范围 = 选项 A（leaf-exact 直接归属行）**——仅对「有直接商机归属」的 territory 节点生成行，行金额=该节点直接商机 Σ（每商机恰计数一次，行间不重叠）；父节点区域总额（含子树）由既有 `accumulatePipeline` 子树 in 查询动态聚合（零 QuotaRollupCalculator 变更，Non-Goal 2 保持）；祖先展开（选项 B）→ pipeline(T1)=A+2B 双计否决；全树占位（选项 C）空行噪音否决。**D2 ownerless 商机归属 = 选项 A（排除）**——territory 收集镜像个人循环 `ownerId != null` 过滤，ownerless 不进入任何 totals，恒等式「公司行 = Σ 区域行 + Σ（无 territory 且有 owner 商机）」保持。
- **Phase 2（实现）**：`ForecastAggregator.refreshForecast` 个人循环内同一遍历收集 `byTerritory`（`Map<Long, List<ErpCrmLead>>`，leaf-exact 不展开祖先）+ 团队/公司之间插入区域 rollup 循环（`buildForecast(period, null, null, territoryId, totals)`，ownerId/teamId null，不 rebuildLines——与团队/公司行一致）；`buildForecast` 增 5 参重载（原 4 参委托 territoryId=null，个人/团队/公司调用点零改动）；类头 javadoc 更新 4 级层次 + leaf-exact 语义 + 子树管道动态视图分层；`ErpCrmForecastBizModel` javadoc 同步。
- **Phase 3（测试矩阵）**：新增 `TestErpCrmForecastTerritoryRollup` 2 组全绿——①`testRefreshForecastTerritoryRollup`：T1 行 = Σ(A)=1000（leaf-exact 不含子节点 B）+ T1-1 行 = Σ(B)=2000 不重叠；无 territory 商机 C 不进区域行但计入公司行；公司行 = 3500 = Σ 区域行(3000) + C(500) 恒等式；区域行字段维度（territoryId 非空 + ownerId/teamId null）；②`testTerritoryPipelineClosureNoDoubleCount`：`ErpCrmQuota__getTerritoryPipeline`（accumulatePipeline 入口）Forecast 段 = commit 1000/upside 2000/bestCase 3000/count 2（子树 in 查询恰命中 T1+T1-1 行一次，非 A+2B 双计——A4.2.93「territory 级管道 Forecast 段恒空」缺口闭合，测试 javadoc 注释 Forecast 段无 periodId 过滤依赖单期间 DB）。**验证**：erp-crm-service **180 tests 全绿**（178 基线 + 2 新增，既有 5 @Test 零回归）+ `mvn install -DskipTests` 构建通过。
- **Phase 4（文档回填）**：arm-index P1-RC-039 → `done (RC-R1.25)`（含修复摘要）；roadmap RC-R1.25 → done ✅（含落地摘要）；owner doc `sales-forecast.md` §实现约定 Deferred 更新为已实现（四级层次 + leaf-exact 直接归属行语义 + 子树总额由 accumulatePipeline 动态聚合分层 + ownerless 口径，不修改需求契约段）。
- **下一步**：全量验证（全仓 `mvn clean install -DskipTests` + compliance checker 复跑）后独立结束审计；MR1 第一批后续 RC-R1.26+ 由 mission driver 继续。
- **mission-driver 复核（2026-08-15 00:30）**：全仓 `mvn clean install -DskipTests` BUILD SUCCESS + `mvn test -pl module-crm/erp-crm-service` **180 tests 0 failures** + `bash docs/audits/nop-compliance-checker.sh` exit 0 **零漂移**（R1d=14/R2a=34/R2b=230/R2c=1393/R2d=34/R3=5/R5=0/R6=2/R10=8/R11=0/R12a=69/R12b=66/R12c=40 全对齐基线）——**full-green verification** 复跑确认。注：全 reactor `mvn test` 有 2 项**预存在**失败（TestAuthSeedLoadingProof NPE + ErpMfgCostRollupLine materialBand），经 git stash 后 clean HEAD 复跑实证与本计划无关，已分别登记 docs/bugs/ 注记。独立结束审计（新会话 ses_ffed3e930ffexkwyx4G1u7YFeD）**PASS**（0 P0/0 P1，2 项 P2 非阻塞[计数笔误已修 + Gate 时序小偏离确认非 hollow]），计划 `2026-08-14-2304-1` Plan Status → completed。变更待按 code/docs 拆分提交。

### CRM UTM 归因族修复落地（RC-R1.24，plan `2026-08-14-1815-3`，第一批纯预授权）

- **工作项**：执行 `docs/plans/2026-08-14-1815-3-rc-mr1-r1-24-crm-utm-attribution-family.md` 四 Phase。落地 2 finding 修复（P1-RC-037 UTM copy-on-create 派生缺失 + P1-RC-038 归因报表完全缺失，同根因[UTM 归因链路]同控制点[campaign 归因数据流]按规则 14 合并）。**治理**：纯 BizModel + 报表 dataset/xpt.xml/page/action-auth，零 ORM 变更（git diff 仅 erp-crm-service Java + 新 xpt.xml + erp-crm-web page/action-auth + `_cases/` 快照 + docs），不触会计过账/删除保护区域（roadmap 预授权类目自动执行）。
- **Phase 1（P1-RC-037 UTM copy-on-create）**：**Decision (A)「未显式传入」判定载体**——entityData 字段 null 即视为未显式传入（GraphQL save 未传字段不 set，entity 字段保持 null；弃 source 字段/提交 map 区分显式 null vs 未传——超出最小修复面）。`ErpCrmLeadBizModel` 注入 `IErpCrmCampaignBiz`（@Inject 非 private）+ `defaultPrepareSave` 新建路径（id==null）+ campaignId 非空时：utmMedium/utmSource 为 null → 经 `campaignBiz.get(id, true, context)` 读 campaign.medium/source 复制（ignoreUnknown=true 防御性跳过）；显式传入不覆盖。**Decision 子项（空值跳过语义）**：实施实测——campaign 不存在时平台 FK 校验（`OrmEntityCopier.copyRefEntity` `loadEntityById` → `nop.err.dao.unknown-entity`）在 defaultPrepareSave **之前**拒绝保存（干净错误码不崩溃），防御性 null 跳过分支保持（理论竞态兜底）；campaign 存在但 medium/source 为 null → 复制跳过不抛。
- **Phase 2（P1-RC-038 归因报表）**：`ErpCrmReportBizModel.prepareDataset` 增 `campaign-attribution` case + `buildCampaignAttributionDataset()`（DB 级 GROUP BY campaignId + COUNT + SUM(expectedRevenue)，经 to-one campaign 解析 campaignName——JOIN 语义下无 campaign 关联 lead 不计入 null 组排除，镜像 funnel 范式）+ `@BizQuery campaignAttributionData`；新 `_vfs/nop/main/report/crm/campaign-attribution.xpt.xml`（campaignName/leadCount/expectedRevenue 三列 + 合计行 + styles 复用）+ `erp/crm/pages/report/campaign-attribution.page.yaml`（FLUX + renderHtml/download 双入口）+ `erp-crm.action-auth.xml` crm-report 子资源（orderNo=10030，app:useCases="UC-CRM-07"）。报表数据源走 ErpCrmReportBizModel 不改 CampaignBizModel（19 行 stub 保持，按 plan 裁决）。
- **Phase 3（测试矩阵）**：新增 `TestErpCrmUtmAttribution` 6 组（RECORDING→CHECKING 快照录制）全绿——037: ①新建+campaignId+未传 utm → 落库复制值 ②显式传 utmMedium → 保留 + utmSource 复制 ③campaign 缺失平台 FK 拒绝 + campaign 字段 null 跳过不抛 ④更新路径（id!=null）不触发复制；038: ①多 campaign 多 lead 聚合正确（count+sum+campaignName 数值断言 + null 组排除）②renderHtml 冒烟渲染成功（含 campaignName）。**验证**：erp-crm-service **178 tests 全绿**（172 基线 + 6 新增）+ `TestErpCrmReportRendering` 9 零回归 + erp-crm-web 构建通过。
- **Phase 4（文档回填）**：arm-index P1-RC-037/P1-RC-038 → `done (RC-R1.24)`；roadmap RC-R1.24 → done ✅（含落地摘要）；owner doc `marketing.md §二` 补 §2.4 实现注记（UTM copy-on-create「未显式传入」判定载体 + 平台 FK 前置拒绝语义 + 归因报表 dataset/xpt/page 接线；不修改需求契约段）。
- **下一步**：全量验证（全仓 `mvn clean install -DskipTests` + compliance checker 复跑）后独立结束审计；MR1 第一批后续 RC-R1.25+（Forecast territory tier 等）由 mission driver 继续。
- **mission-driver 复核（2026-08-14 23:00）**：`mvn clean install -DskipTests -pl module-crm/erp-crm-service,module-crm/erp-crm-web -am` 50/50 模块 BUILD SUCCESS（含 erp-crm-service/web 全新编译）+ `mvn test -pl module-crm/erp-crm-service` **178 tests 0 failures 0 errors**——full-green verification 复跑确认；变更已按 code/docs 拆分提交（feat + docs）。

### CRM 线索评分调度接线落地（RC-R1.23，plan `2026-08-14-1815-2`，第一批纯预授权）

- **工作项**：执行 `docs/plans/2026-08-14-1815-2-rc-mr1-r1-23-crm-lead-scoring-schedule-wiring.md` 四 Phase。落地 P1-RC-035（UC-CRM-09 SCHEDULED 触发器接线 + L2 失实纠正）。**治理**：纯 job 接线（job.yaml/batch.xml）+ config 消费 + helper + 文档修订，零 ORM 结构/会计过账/删除变更（roadmap 预授权类目自动执行）。
- **Phase 1（Explore 三项 Decision/Proof）**：**Decision (A) 接线形态**——确认既有 batch-task 式接线（job.yaml + nopBatchTaskRunner + batch.xml，2026-07-18 迁移标准模式），不新建 Java Job bean；Explore 证据：`inject('IErpCrmLeadScoreBiz')` 经 `biz_ErpCrmLeadScore` bean `ioc:type` 解析（同型 pnl-calc `IErpPrjProjectPnlBiz` 已验证）、`nopBatchTaskRunner` bean 注册于 nop-batch-dsl `batch-dsl.beans.xml:6`、job.yaml 经 `LocalJobConfigLoader` 扫描 `/nop/job/conf` + `@cfg:` 经 `ConfigValueResolver` 解析 + enabled=false 默认不注册。**Decision (A) 失败隔离 F1**——per-item 隔离落地（REQUIRES_NEW helper 镜像 bank-recon `ErpFinBankReconAutoReverseHelper`）：实测 `BatchTaskBuilder.buildChunkProcessor` + `InvokerBatchConsumer`（process/chunk 均整 chunk 单事务，无 per-item 隔离——原 batch.xml `transactionScope="process"` + 裸 biz 调用与 L2「单线索失败隔离」声明不符）。**Proof**——job.yaml 加载链 + inject 解析 + taskPath 命中文件核实（运行时证据由 Phase 3 batch 任务级测试补充）。
- **Phase 2（接线落地 + config 对齐）**：**Decision (A) config 门控键对齐**——job.yaml cronExpr 改消费业务键 `erp-crm.lead-scoring.schedule-cron`（`@cfg:erp-crm.lead-scoring.schedule-cron|0 2 * * *`，弃 `nop.job...cron-expr`），`CONFIG_LEAD_SCORING_SCHEDULE_CRON` 由 dead 转活跃（job.yaml + helper 双消费点）；**实施中发现并修复运行时缺陷**：`batchChunkCtx.serviceContext` 在 nop-batch 执行路径为 null（`BatchTaskRunner.executeAsync` → `newBatchTaskContext()` 无绑定上下文），而 `IErpCrmLeadScoreBiz` 代理调用需非 null ctx（`EvalServiceAction.invoke` → `context.getEvalScope()`）——helper 内空值兜底 `new ServiceContextImpl()`（对齐 R1.4/R1.5 Job bean 自建 ctx 范式），该缺陷同样潜伏于 pnl-calc 等既有 batch job（watch-only 记录）。新增 `ErpCrmLeadScoringRecalcHelper`（REQUIRES_NEW + try/catch WARN 单条失败隔离 + schedule-cron 空值跳过 INFO）+ batch.xml processor 改调 helper + `app-service.beans.xml` 注册 + `ErpCrmConstants` javadoc 更新。
- **Phase 3（测试矩阵）**：新增 `TestErpCrmLeadScoringRecalcJob` 4 组 batch 任务级测试（`IBatchTaskRunner.execute(taskPath)`，nop-batch-dsl test-scope 依赖补入 erp-crm-service pom）全绿——①SCHEDULED 触发后 active 线索生成 ErpCrmLeadScore（triggerEvent=SCHEDULED + totalScore=100 + append-only + 行级快照）；②终态线索（CONVERTED/LOST/CANCELLED）被 loader 排除不评分；③schedule-cron 空值跳过（INFO 日志 + 不评分）；④单条失败线索（ERR_LEAD_NOT_FOUND）WARN 记录不阻断其余线索评分。**验证**：erp-crm-service 172 tests 全绿（168 基线 + 4 新增）+ `TestErpAllJobYamlLoading` 21 通过。
- **Phase 4（文档回填）**：arm-index P1-RC-035 → `done (RC-R1.23)`；roadmap RC-R1.23 → done ✅；owner doc `lead-scoring.md:157` 配置表行失实纠正（batch-task 接线形态 + 键名 + enabled/schedule-cron 门控语义 + REQUIRES_NEW per-item 隔离注记）；`job-scheduling.md` §3.9 行 + 配置键表同步；`compliance-baseline.md` R10 7→8 基线上调注记（per-site 证据：`ErpCrmLeadScoringRecalcHelper` REQUIRES_NEW 1 处，镜像 RC-R1.2 先例，plan Closure Gates 已知失败模式 #1 登记）。
- **下一步**：全量验证（全仓 `mvn clean install -DskipTests` + 全仓 `mvn test` + checker 复跑）后独立结束审计；MR1 第一批后续 RC-R1.24+ 由 mission driver 继续。

# Development Log - 2026-08-14

### CRM 线索转化族修复落地（RC-R1.21 直接升格 + RC-R1.22 转化前置守卫，plan `2026-08-14-1815-1`，第一批纯预授权）

- **工作项**：执行 `docs/plans/2026-08-14-1815-1-rc-mr1-r1-21-22-crm-conversion-family.md` 四 Phase（规则 14 合并：同域 crm 同 owner doc 同结果表面[线索/商机转化契约符合性]）。落地 3 finding 修复（P1-RC-032 直接升格分支缺失 / P1-RC-033 convertToCustomer 不查 QUALIFIED / P1-RC-034 convertToQuotation 不查 QUALIFIED+isWonStage）+ 守卫矩阵测试 + owner doc 注记 + arm-index/roadmap 回填。**治理**：纯 BizModel/Processor/ErrorCode/接口契约方法，零 ORM 变更（`git diff --stat` 仅 erp-crm-service/erp-crm-dao Java + `_cases/` 快照 + docs），不触会计过账/删除保护区域（roadmap 预授权类目自动执行）。
- **Phase 1（P1-RC-032 直接升格）**：`IErpCrmConversionBiz` 增 `convertToOpportunity(leadId, context)` 契约方法（javadoc 描述升格语义+前置）+ **Decision (A) 实现形态**（新增独立 per-mutation Processor `ErpCrmConversionConvertToOpportunityProcessor`，镜像两兄弟类，弃 BizModel 内联——保持 delete-after-extract 范式、下游可 Delta 同名 bean 覆盖）+ **Decision (A) docStatus 语义**（升格后保持 QUALIFIED，L1 UC-CRM-02 未要求 CONVERTED，且保持 QUALIFIED 使后续 convertToQuotation 前置成立；置 CONVERTED 阻断 UC-CRM-03 链路弃）。`ErpCrmConversionProcessor` 增 protected step `promoteToOpportunity`（validateLeadType(LEAD) 先于 validateDocStatus(QUALIFIED) → 原地 setLeadType(OPPORTUNITY) + updateEntity，不建 Partner/新 Lead）+ `validateDocStatus` 共享 step；`ErpCrmLeadBizModel` @BizMutation Facade 委托 + beans.xml 注册；`ErpCrmErrors` 增 `ERR_LEAD_NOT_QUALIFIED`（ARG_LEAD_CODE/ARG_CURRENT_STATUS）。
- **Phase 2（P1-RC-033 + P1-RC-034 前置守卫族）**：**Decision (A) 错误码**（新建 `ERR_LEAD_NOT_QUALIFIED` + `ERR_LEAD_STAGE_NOT_WON` 专属码，弃复用 `ERR_LEAD_ILLEGAL_STATUS_TRANSITION`——守卫是业务前置非状态机迁移，LOST/CANCELLED 拒绝时该码名称误导）。`ErpCrmConversionProcessor` 增 `validateWonStage`（经 `IErpCrmStageBiz` 查 lead.stageId → ErpCrmStage.isWonStage，stageId null 或非 won-stage 抛 `ERR_LEAD_STAGE_NOT_WON`）；ConvertToCustomerProcessor validateLeadType 后接入 validateDocStatus(QUALIFIED)；ConvertToQuotationProcessor validateLeadType 后接入 validateDocStatus + validateWonStage（守卫顺序确定化 docStatus → won-stage → partner，`ERR_OPPORTUNITY_PARTNER_REQUIRED` 可达性保留）。
- **Phase 3（测试矩阵 + 既有冲突测试改造）**：既有 `TestErpCrmLeadConversion` 9 @Test 中 3 个与新守卫冲突的测试显式改造——`testAlreadyConvertedRejected` 种子 NEW→QUALIFIED（保留首次成功+二次 ERR_LEAD_ALREADY_CONVERTED 断言）/ `testFullConversionChain` convertToCustomer 后插入商机 qualify（NEW→QUALIFIED 入漏斗设默认 stage）+ moveStage 至 won-stage（新增 STAGE_WON=1103 isWonStage=true seed）再转报价单 / `testOpportunityWithoutPartnerRejected` 补 won-stage stageId seed（partner 守卫仍可达断言不变）；3 测试 `@EnableSnapshot(saveOutput=true)` 单方法重录后移除注解，6 个无冲突测试零回归。新增 `TestErpCrmConversionGuards` 6 组矩阵全绿[032: QUALIFIED+LEAD 升格成功[leadType=OPPORTUNITY+docStatus=QUALIFIED+原 id 不变+零 Partner/新 Lead]/NEW 拒 ERR_LEAD_NOT_QUALIFIED/已升格重复调 ERR_LEAD_TYPE_MISMATCH；033: NEW convertToCustomer 拒 ERR_LEAD_NOT_QUALIFIED 零创建；034: NEW/LOST OPPORTUNITY 拒 + QUALIFIED 非 won-stage 拒 ERR_LEAD_STAGE_NOT_WON] + RECORDING→CHECKING 快照录制。**验证**：erp-crm-service 168 tests 全绿（162 基线 + 6 新增）。
- **Phase 4（文档回填）**：arm-index P1-RC-032 → `done (RC-R1.21)` + P1-RC-033/034 → `done (RC-R1.22)`；roadmap RC-R1.21/RC-R1.22 → done ✅（含落地摘要）；owner doc 注记——`state-machine.md §Lead` 补转化前置守卫实现注记（直接升格 docStatus 保持 QUALIFIED 语义 + won-stage 判定载体 + 守卫与状态机 latent gap 分层边界声明，不修改需求契约段）+ `README.md §业务规则1/§衔接契约` 补直接升格 + 双前置守卫契约行。
- **下一步**：全量验证（全仓 `mvn test` + `mvn clean install -DskipTests` + compliance checker 零漂移）后独立结束审计；MR1 第一批后续 RC-R1.23（评分调度接线）由 mission driver 继续。
- **mission-driver 复核（2026-08-14 17:32）**：`mvn install -DskipTests -pl module-crm/erp-crm-service -am`（增量，无 clean）BUILD SUCCESS + `mvn test -pl module-crm/erp-crm-service` **168 tests 0 failures** + `bash docs/audits/nop-compliance-checker.sh` exit 0 零漂移（R1d=14/R2a=34/R2b=229/R2c=1392/R2d=34/R3=5/R5=0/R6=2/R10=7/R11=0/R12a=69/R12b=66/R12c=40 actual==baseline）——**full-green verification** 复跑确认；变更已按 code/docs 拆分提交（feat 4ad4e9d33 + docs 5c17c43ab）。

### Contract 域 ErpCtRebateSettlement 返利结算单实体级状态机 Bean 落地（M4.65，plan `2026-08-14-2000-1`，§11.2 M4 plan-first）

- **工作项**：执行 `docs/plans/2026-08-14-2000-1-erpct-rebate-settlement-state-machine-bean.md` 两 Phase。落地 contract 域最后一个状态轴工作项（单实体单轴）——`ErpCtRebateSettlement.status`（dict `erp-ct/settlement-status` 3 值 DRAFT/POSTED/CANCELLED）实体级 Bean（契约 `docs/architecture/entity-state-machine-bean.md`）+ PostSettlementProcessor 接线 + 层 1 矩阵 + 层 2 四方对照 + 层 3 回归。**治理**：postSettlement DRAFT→POSTED 触发 credit-memo AP/AR 负额发票生成（`posted=false`，O-4 架构豁免 `posting-exemptions.md §ErpCtRebateSettlementBizModel`，非 GL 凭证）+ `ErpCtRebateAccrual.isSettled` 回写——会计保护域，按 §11.2 M4 plan-first（硬约束 (i)-(v)，人工门控 2026-08-14 已确认解除，Draft Review Record 门控确认记录齐备）。规则 14 bundling 声明：单轴无需 bundling。
- **Phase 1（Bean + 注册 + 接线 + 层 1 矩阵）**：`ErpCtRebateSettlementStateMachine`（1 实现边 postSettlement(DRAFT→POSTED) + `assertCanPostSettlement` + `postSettlementTargetStatus` + isTerminal[POSTED] + transitions 1 边 + initial[DRAFT]），严格无状态（零 @Inject/DAO/IServiceContext），镜像 M3.18 `ErpCtContractVersionStateMachine` 结构。**Decision (A) CANCELLED = intentional reserved 死状态**（dict 含值 + 全域零 setStatus(CANCELLED) writer + 无 cancel mutation；Bean 不入任一集合，dict 值保留不删对齐 Contract CANCELLED/RebateAgreement EXPIRED+SETTLED 先例；Successor：PM 要求 settlement cancel 工作流时）+ **Decision (B) `posted` 布尔列不对称 = watch-only residual**（Processor 从不 setPosted(true)，credit-memo 发票 posted=false 经 pur/sal 管道后续翻转；Bean 不入轴，owner doc 登记非修正）。`app-service.beans.xml` FQN-id 注册（紧邻 3 既有 SM Bean）。**接线**：PostSettlementProcessor `@Inject ErpCtRebateSettlementStateMachine stateMachine`（非 private，合规 R5），内联 `Objects.equals` DRAFT 守卫（:45）改委托 `assertCanPostSettlement`（try/catch common `ERR_ILLEGAL_STATUS_TRANSITION` 作 cause → 领域 `ERR_CT_SETTLEMENT_ILLEGAL_TRANSITION` 仅传 settlementId + currentStatus，action/expectedStatus 仅存 common 码 cause）；目标态（:90）改调 `postSettlementTargetStatus()`。**credit-memo 生成（AP/AR 负额发票）+ accrual isSettled 回写 + postedAt/postedBy + setCreditMemoBillType/BillCode 全部保留原位**（§11.2 M4 (ii)/(iv)/(v)）。grep 证 Processor 内零 SETTLEMENT_STATUS 常量残留。层 1 矩阵 `TestErpCtRebateSettlementStateMachineMatrix` **7/7 绿**（无重复/冲突边 + DRAFT→POSTED 可达 + 合法/非法来源态 common 码携带 action/currentStatus + transitions 元数据一致 + 初始/终态集合 + **CANCELLED 不在任一集合断言**）。
- **Phase 2（层 2 四方对照 + owner doc 补章节 + 层 3 回归）**：10 维度审查裁决 **Verdict: pass**（0 P0/0 P1）。四方对照（dict 3 值 ↔ owner doc §适用对象四（plan 补章节）↔ Bean 元数据 ↔ 全部 writer：PostSettlementProcessor:94 唯一生产 writer 经 Bean 目标态 + DRAFT CRUD 创建 seed + CANCELLED 零 writer + 框架入口 §9.1 排除）全对齐，无未裁决漂移。owner doc `docs/design/contract/state-machine.md` 补 **§适用对象四：返利结算（RebateSettlement）**（3 值定义 + DRAFT→POSTED 唯一实现边迁移表 + CANCELLED intentional reserved 裁定 + `posted` 不对称 watch-only residual 登记 + postSettlement 副作用边界声明 + §审查提示行），对齐 M3.18/M3.19 补章节先例（§适用对象三 §4 边界声明为前置交叉引用）。层 3 `mvn test -pl module-contract/erp-ct-service -am` **82/82 全绿**（75 既有基线 + 7 层 1 矩阵；`TestErpCtRebateSettlementEnd` SALES DRAFT→POSTED + AR credit-memo + accrual 回写、`TestErpCtContractRebate` PURCHASE DRAFT→POSTED + AP credit-memo + illegal transition guard 零行为回归）。
- **验证基线（全绿）**：`mvn clean install -DskipTests`（全仓库 BUILD SUCCESS）+ `bash docs/audits/nop-compliance-checker.sh`（exit 0，R5=0/R11=0 零漂移）。路线图 M4.65 → done（contract 域状态轴迁移收尾）。
- **Deferred（已裁定带 successor）**：CANCELLED 死状态（PM 要求 settlement cancel 工作流时开独立 plan + dict 值激活）；`posted` 不对称 watch-only residual（归过账侧，仅当 PM 要求独立 posted 翻转语义时重评）；BizModel 死代码清理 `createNegativeApInvoice`/`createNegativeArInvoice`（R6.7 split 遗留 duplicate，contract 域低风险清理批次启动时）；Delta 覆盖实证（M4 保护域归 M5.3）；全局 CRUD 写锁（M0.1 §9 选项 c 排除）。

### 资产域拆分/合并文档双轴 4 实体级状态机 Bean 落地（M4.48–M4.51，plan `2026-08-14-1931-3`，§11.2 M4 plan-first，规则 14 合并）

- **工作项**：执行 `docs/plans/2026-08-14-1931-3-erpast-structural-documents-state-machine-beans.md` 三 Phase（规则 14 合并：同 owner doc `split-merge.md`/同域 erp-ast/同结果表面 = 资产域拆分/合并文档双轴矩阵集中化）。落地 4 个实体级双轴 Bean（契约 `docs/architecture/entity-state-machine-bean.md`）+ 2 facade 接线 + 层 1 矩阵 44 tests + 层 2 四方对照 + 层 3 回归 299 tests。**治理**：Split approve→`AssetSplitPostingDispatcher`（结构性资产拆分过账 + 卡片重组）、Merge approve→`AssetMergePostingDispatcher`（结构性资产合并过账 + 卡片重组）；二者均**仅 post 路径、无 reverse**——reverseApprove 无条件抛 `ERR_AST_SPLIT/MERGE_REVERSE_NOT_SUPPORTED`（不可逆契约，owner doc `split-merge.md` §关键业务规则 5）——均属会计保护域，按 §11.2 M4 plan-first（硬约束 (i)-(v)，人工门控 2026-08-14 已确认解除，Draft Review Record 门控确认记录齐备）。
- **Phase 1（M4.48 + M4.49 Split 双轴，facade 范式镜像计划 2 Disposal）**：`ErpAstSplitApprovalStateMachine`（approveStatus 5 动作 6 边同构 + **reverseApprove(APPROVED→REJECTED) 名义边（nominal edge，运行时不可达）**——Split reverseApprove 是无条件抛错动作（per-mutation `ErpAstSplitReverseApproveProcessor:22-26` require 后直接抛 `ERR_AST_SPLIT_REVERSE_NOT_SUPPORTED`，无 posted 判定、短路在 facade validateTransition 之前），Bean `assertCanReverseApprove` 存在但**不被接线**，javadoc 显式标注不可达；initial={UNSUBMITTED}、terminal={APPROVED} 可逆终态）+ `ErpAstSplitDocumentStateMachine`（**非退化轴 Decision (B)**：approve 是 docStatus 唯一命名 writer——transitions 1 边 approve(DRAFT→ACTIVE)；CANCELLED 经 cancel mutation（`ErpAstSplitCancelProcessor:26`）+ useLogicalDelete；terminal={ACTIVE,CANCELLED}、initial={DRAFT}）。注册 2 Bean（app-service.beans.xml 紧邻既有 SM Bean 块）。**接线（Decision (C)）**：`ErpAstSplitProcessor` 注入 2 Bean（非 private）；validateTransitionFor{Submit,Withdraw,Approve,Reject} 各改调 Approval Bean `assertCanXxx`（try/catch common 码作 cause → cause-chain `ERR_AST_SPLIT_ILLEGAL_STATUS_TRANSITION`，新增 4 参 cause 重载）；`validateTransitionForCancel` 的 **CANCELLED 判定委托 Document Bean `isCancelled()`**（ACTIVE「非已生效」+ posted「非已过账」动态条件保留原位）；`executeApprove` 目标态改调 Bean `*TargetStatus()`。**reverseApprove 不接线**（per-mutation 无条件抛错保持，Bean 边为名义元数据不接运行时）。**比例/金额平衡（PROPORTION_NOT_BALANCED/AMOUNT_NOT_BALANCED）、跨类别（CROSS_CATEGORY_NOT_ALLOWED）、源 IN_SERVICE（SOURCE_NOT_IN_SERVICE）、净值充足（INSUFFICIENT_NET_VALUE）、目标编码唯一（TARGET_ASSET_CODE_DUPLICATE）、已过账（ALREADY_POSTED）全部保留原位**；资产卡片重组 + `AssetSplitPostingDispatcher.tryPost`（仅 post 无 reverse）+ posted 置位保留原位。per-mutation 6 Processor 经 facade 透传自动生效。层 1 矩阵 `TestErpAstSplitApprovalStateMachineMatrix` 13/13（含 reverseApprove 名义边元数据测试）+ `TestErpAstSplitDocumentStateMachineMatrix` 9/9 绿。
- **Phase 2（M4.50 + M4.51 Merge 双轴，镜像 Phase 1）**：`ErpAstMergeApprovalStateMachine`（5 动作 6 边同构 + reverseApprove 名义边）+ `ErpAstMergeDocumentStateMachine`（1 边 approve(DRAFT→ACTIVE) + isCancelled 守卫，同 Split 范式）。注册 2 Bean。接线镜像 Phase 1：`ErpAstMergeProcessor` 注入 2 Bean；validateTransitionFor{Submit,Withdraw,Approve,Reject} 改调 Approval Bean；cancel 守卫 CANCELLED 判定委托 Document Bean `isCancelled()`（ACTIVE/posted 动态条件保留原位）；`executeApprove` 目标态改调 Bean。**reverseApprove 不接线**（per-mutation `ErpAstMergeReverseApproveProcessor:22-26` 无条件抛 `ERR_AST_MERGE_REVERSE_NOT_SUPPORTED` 保持）。**源 IN_SERVICE、跨类别/币种（CROSS_CATEGORY/CURRENCY_NOT_ALLOWED）、无源（NO_SOURCES）、已过账（ALREADY_POSTED）全部保留原位**；资产卡片合并 + `AssetMergePostingDispatcher.tryPost` + posted 置位保留原位。层 1 矩阵 `TestErpAstMergeApprovalStateMachineMatrix` 13/13 + `TestErpAstMergeDocumentStateMachineMatrix` 9/9 绿。
- **Phase 3（层 3 回归 + 二实体一致性）**：`mvn test -pl module-assets/erp-ast-service -am` **BUILD SUCCESS**（erp-ast-service **299/299 tests** 全绿，0 failures/errors；含层 1 矩阵 4 新测试类 44 tests + 既有集成测试 `TestErpAstSplitMerge` 8（approve happy path ×3 + **testApproveThenReverseNotSupported 断言无条件抛 ERR_AST_SPLIT_REVERSE_NOT_SUPPORTED（无 posted 窗口）** + testProportionNotBalancedRejected + testCrossCategoryConfigGatedRejected + testSourceNotInServiceRejected + testMergeCrossCurrencyRejected）等——错误码值/参数、过账时序、不可逆契约、卡片重组时序零行为回归）。二实体一致性：4 Bean 命名（Approval/Document 后缀）/注册（beans.xml 4 FQN bean id）/严格无状态（grep 零 @Inject/DAO/IServiceContext，javadoc 除外）/矩阵形状一致（2 Approval 各 5 动作 6 边含 reverseApprove 名义边；2 Document 各 1 边 + isCancelled 守卫）；facade→Bean 注入（非 private）+ cause-chaining 范式与计划 2 Disposal + Movement 双轴可追溯一致；二 facade 内联 `Objects.equals` 状态判断 grep 证实移除（cancel 守卫 ACTIVE/posted 动态条件按 Decision (C) 保留原位；源 IN_SERVICE 动态守卫保留原位）；reverseApprove per-mutation Processor 无条件抛错保持原位（:24 行实证）。
- **层 2 四方对照**：逐实体逐轴 dict `wf/approve-status` + `erp/doc-status` ↔ `split-merge.md` §关键业务规则 5 不可逆契约 ↔ Bean 元数据 ↔ 全部 writer（2 facade validateTransition 5×2 + executeApprove 目标态 2×2 + per-mutation 6×2 + 创建写 DRAFT/UNSUBMITTED + CRUD 路径 §9.1 排除 + 不可逆守卫 REVERSE_NOT_SUPPORTED 登记）全对齐。无新死状态。**owner doc 补章节**：`assets/state-machine.md` 新增「适用对象四：资产拆分/合并文档双轴（Split / Merge）」（双轴矩阵 + reverseApprove 名义边裁定 + 不可逆契约异常路径 + 审查提示行，对齐 Movement/适用对象三补章节先例）。
- **验证（全绿基线）**：`mvn clean install -DskipTests` 全仓 **BUILD SUCCESS**；`bash docs/audits/nop-compliance-checker.sh` 零漂移 exit 0（R5=0/R11=0）。独立结束审计由独立子代理（新会话）执行。
- **路线图**：`docs/backlog/entity-state-machine-migration-roadmap.md` M4.48–M4.51 标 **done**（规则 1）+ 头部追加 2026-08-14 记录。
- **mission-driver 复核（2026-08-14 17:20）**：`mvn clean install -DskipTests` 全仓 BUILD SUCCESS（1:40 min）+ `mvn test -pl module-assets/erp-ast-service -am` **299 tests 0 failures**（含层 1 矩阵 4 新测试类 44 tests + 既有 `TestErpAstSplitMerge` 8 全绿）+ `bash docs/audits/nop-compliance-checker.sh` exit 0（R5=0/R11=0 零漂移，R12a=69/R12b=66/R12c=40 对齐基线）——**full-green verification** 复跑确认；变更已按 code/docs 拆分提交（feat + docs）。

### 资产域价值调整文档双轴 6 实体级状态机 Bean 落地（M4.42–M4.47，plan `2026-08-14-1931-2`，§11.2 M4 plan-first，规则 14 合并）

- **工作项**：执行 `docs/plans/2026-08-14-1931-2-erpast-value-change-documents-state-machine-beans.md` 四 Phase（规则 14 合并：同 owner doc `assets/state-machine.md`/同域 erp-ast/同结果表面 = 资产域价值调整文档双轴矩阵集中化）。落地 6 个实体级双轴 Bean（契约 `docs/architecture/entity-state-machine-bean.md`）+ 3 facade 接线 + 层 1 矩阵 68 tests + 层 2 四方对照 + 层 3 回归 255 tests。**治理**：ValueAdjustment approve→VALUE_ADJUSTMENT 减值/增值凭证、Disposal approve→DISPOSAL 清理凭证 + Asset 终态（SCRAPPED/SOLD）、Capitalization approve→CAPITALIZATION 入账凭证 + Asset DRAFT→IN_SERVICE，reverseApprove posted=true 窗口红冲上述副作用、posted=false 不对称窗口悬挂经 `DeferredPostingSweepJob` 兜底——均属会计保护域，按 §11.2 M4 plan-first（硬约束 (i)-(v)，人工门控 2026-08-14 已确认解除，Draft Review Record 门控确认记录齐备）。
- **Phase 1（M4.44 + M4.45 Disposal 双轴，facade 范式固化为 assets 版）**：`ErpAstDisposalApprovalStateMachine`（approveStatus 5 动作 6 边：submit UNSUBMITTED/REJECTED→SUBMITTED / approve SUBMITTED→APPROVED / reject SUBMITTED→REJECTED / reverseApprove APPROVED→REJECTED（§16.4）/ withdraw SUBMITTED→UNSUBMITTED；initial={UNSUBMITTED}、terminal={APPROVED} 可逆终态）+ `ErpAstDisposalDocumentStateMachine`（**非退化轴 Decision (B)**：approve 是 docStatus 唯一命名 writer——transitions 1 边 approve(DRAFT→ACTIVE)；CANCELLED 经 useLogicalDelete；terminal={ACTIVE,CANCELLED}、initial={DRAFT}）。注册 2 Bean（app-service.beans.xml 紧邻既有 SM Bean 块）。**接线**：`ErpAstDisposalProcessor` 注入 2 Bean（非 private）；validateTransitionFor{Submit,Withdraw,Approve,Reject,ReverseApprove} 各改调 Approval Bean `assertCanXxx`（try/catch common 码作 cause → cause-chain `illegalTransition`→`ERR_DISPOSAL_ILLEGAL_STATUS_TRANSITION`，新增 4 参 cause 重载）；`validateTransitionForCancel` doc-cancelled 守卫改调 Document Bean `isCancelled()`；`executeApprove:92-93`/`executeReverseApprove:138` 目标态改调 Bean `*TargetStatus()`。per-mutation 5 Processor 经 facade 透传自动生效。**Asset 来源态校验（validateAssetDisposable）、gain/loss 计算、schedule cancel/restore、过账、posted 置位保留原位**；Asset.status side-effect（:86/:128）由计划 1 M4.40 AssetStateMachine 守卫（两计划在该行交汇，接线互不冲突）。层 1 矩阵 `TestErpAstDisposalApprovalStateMachineMatrix` 12/12 + `TestErpAstDisposalDocumentStateMachineMatrix` 9/9 绿。
- **Phase 2（M4.46 + M4.47 Capitalization 双轴，镜像 Phase 1 + 特例边）**：`ErpAstAssetCapitalizationApprovalStateMachine`（5 动作 6 边同构）+ `ErpAstAssetCapitalizationDocumentStateMachine`（2 边：approve(DRAFT→ACTIVE) + **reverseApprove(ACTIVE→CANCELLED) Capitalization 特例边**——`executeReverseApprove:122` 额外写 docStatus=CANCELLED，Disposal/ValueAdjustment 无此写，draft review M2 显式登记不与其他 Document Bean 静默同构；terminal={CANCELLED}、ACTIVE 为可逆中间态）。接线镜像 Phase 1：`ErpAstAssetCapitalizationProcessor` 注入 2 Bean；validateTransitionForXxx 改调 Approval Bean；cancel 守卫改调 Document Bean `isCancelled()`；`executeApprove:85-86` 目标态改调 Bean；`executeReverseApprove:121-122` 目标态改调 Bean（docStatus 经 `reverseApproveTargetStatus()`=CANCELLED）。**折旧计划生成、Asset.status side-effect（→IN_SERVICE/→DRAFT）、过账、posted 置位保留原位**。层 1 矩阵 `TestErpAstAssetCapitalizationApprovalStateMachineMatrix` 12/12 + `TestErpAstAssetCapitalizationDocumentStateMachineMatrix` 11/11 绿。
- **Phase 3（M4.42 + M4.43 ValueAdjustment 双轴，镜像 Phase 1 + 动态守卫 Decision）**：`ErpAstValueAdjustmentApprovalStateMachine`（5 动作 6 边同构）+ `ErpAstValueAdjustmentDocumentStateMachine`（2 边：approve(DRAFT→ACTIVE) + cancel(DRAFT→CANCELLED)——**唯一有独立 cancel mutation 的实体**（`ErpAstValueAdjustmentCancelProcessor:26` 实仓核实）；assertCanCancel 拒绝 ACTIVE「非已生效」/CANCELLED「非已作废」；terminal={ACTIVE,CANCELLED}、initial={DRAFT}）。**Decision（动态守卫不迁移）**：`ERR_ADJUSTMENT_ALREADY_REVERSED`（已红冲）、`ERR_ADJUSTMENT_APPROVAL_REQUIRED`（config-gated 强制审批）、`TYPE_INVALID`/`AMOUNT_INVALID`（类型/金额校验）为 posted/config/业务值动态守卫，保留原位。接线镜像 Phase 1：facade 注入 2 Bean；validateTransition/executeApprove/executeReverseApprove/doAutoApprove 委托 Bean；`validateTransitionForCancel` 固定守卫（ACTIVE/CANCELLED）改调 `assertCanCancel`（按当前态区分 expected「非已生效」/「非已作废」），**posted 守卫「非已过账」保留原位**（posted 不入轴契约 §3）；`validateNotCancelled` 委托 `isCancelled()`。层 1 矩阵 `TestErpAstValueAdjustmentApprovalStateMachineMatrix` 12/12 + `TestErpAstValueAdjustmentDocumentStateMachineMatrix` 12/12 绿。
- **Phase 4（层 3 回归 + 三实体一致性）**：`mvn test -pl module-assets/erp-ast-service -am` **BUILD SUCCESS**（erp-ast-service **255/255 tests** 全绿，0 failures/errors；含层 1 矩阵 6 新测试类 68 tests + 既有集成测试 `TestErpAstCapitalization` 3/`TestErpAstDisposal` 2/`TestErpAstDisposalWorkflowApproval` 3/`TestErpAstValueAdjustment` 6/`TestErpAstPostingReverse` 5/`TestErpAstMovementReverseApprove` 10/`TestErpAstSplitMerge` 8/`TestErpAstInventory` 5/`TestErpAstMaintenance` 15/`TestErpAstCipTransfer` 8/`TestErpAstDepreciation` 9 等——错误码值/参数、过账时序、posted=false 不对称窗口、gain/loss、schedule cancel/restore 零行为回归）。三实体一致性：6 Bean 命名（Approval/Document 后缀）/注册（beans.xml 6 FQN bean id）/严格无状态（grep 零 @Inject/DAO/IServiceContext，javadoc 除外）/矩阵形状一致（3 Approval 各 5 动作 6 边；3 Document：Disposal 1 边 / Capitalization 2 边含特例 / ValueAdjustment 2 边含 cancel）；facade→Bean 注入（非 private）+ cause-chaining 范式与 1950-1 采购审批 + Movement 双轴可追溯一致；三 facade 内联 `Objects.equals` 状态判断 grep 证实全部移除（零残留）。
- **层 2 四方对照**：逐实体逐轴 dict `wf/approve-status` + `erp/doc-status` ↔ owner doc §适用对象三新增节（assets/state-machine.md，三实体双轴矩阵 + Capitalization 特例边 + posted=false 不对称窗口 + 动态守卫登记）↔ Bean 元数据 ↔ 全部 writer（3 facade validateTransition 18 + executeApprove/ReverseApprove/doAutoApprove 8 目标态 + per-mutation 16 守卫透传 + ValueAdjustment cancelProcessor + 创建写 DRAFT/UNSUBMITTED + CRUD 路径 §9.1 排除）全对齐。无新死状态。
- **验证（全绿基线）**：`mvn clean install -DskipTests` 全仓 **BUILD SUCCESS**；`bash docs/audits/nop-compliance-checker.sh` 零漂移 exit 0（R5=0/R11=0，全 19 规则 actual = compliance-baseline BASELINE 块）。独立结束审计（子代理新会话零信任）一轮 FAIL→回填修复后二轮 **PASS**：首轮 MAJOR F1（每日开发日志缺失执行记录——本条目即修复）+ 2 MINOR（M1 库存转固 stock move 为继承性 owner-doc 漂移，`module-assets` 全仓 `IErpInvStockMoveBiz` 零引用，owner doc §适用对象三已改注登记 + Deferred 登记；M2 `ERR_ADJUSTMENT_ALREADY_REVERSED` 全仓零 throw 点死码登记，Phase 3 Decision (A) 处置正确行为无影响）。
- **路线图**：`docs/backlog/entity-state-machine-migration-roadmap.md` M4.42–M4.47 标 **done**（规则 1）+ 头部追加 2026-08-14 记录。
- **mission-driver 复核（2026-08-14 16:52）**：`mvn clean install -DskipTests` 全仓 BUILD SUCCESS + `mvn test -pl module-assets/erp-ast-service -am` **255 tests 0 failures**（含层 1 矩阵 6 新测试类 68 tests 全绿）+ `bash docs/audits/nop-compliance-checker.sh` exit 0（R5=0/R11=0 零漂移）——**full-green verification** 复跑确认；变更已按 code/docs 拆分提交（feat + docs）。

### 资产域核心生命周期/跟踪实体 status 单轴 4 实体实体级状态机 Bean 落地（M4.40 + M4.41 + M4.52 + M4.53，plan `2026-08-14-1931-1`，§11.2 M4 plan-first，规则 14 合并）

- **工作项**：执行 `docs/plans/2026-08-14-1931-1-erpast-core-lifecycle-state-machine-beans.md` 三 Phase（规则 14 合并：同 owner doc `assets/state-machine.md`/同域 erp-ast/同结果表面 = 资产域核心生命周期/跟踪实体 status 轴矩阵集中化）。落地 4 个单实体单轴 Bean（契约 `docs/architecture/entity-state-machine-bean.md`）+ 16 Processor 接线 + 层 1 矩阵 53 tests + 层 2 四方对照 + 层 3 回归 185 tests。**治理**：Asset 资本化→CAPITALIZATION 凭证、处置→DISPOSAL 清理凭证、DepreciationSchedule 折旧→DEPRECIATION 凭证、Inventory 盘盈盘亏过账、Maintenance 维修费用化/资本化凭证 + reverseDepreciation/reversePost 红冲——均属会计保护域，按 §11.2 M4 plan-first（硬约束 (i)-(v)，人工门控 2026-08-14 已确认解除，Draft Review Record 门控确认记录齐备）。
- **Phase 1（M4.40 + M4.41 Asset + DepreciationSchedule Bean）**：`ErpAstAssetStateMachine`（status 单轴**跨实体 writer 轴特殊形态**：Asset 无自有 status mutation，5 处 writer 全为文档 Processor side-effect——capitalization 资本化/disposal 处置/inventory 盘亏触发处置；6 命名动作 7 边：capitalize DRAFT→IN_SERVICE / reverseCapitalize IN_SERVICE→DRAFT（资本化 posted 窗口）/ disposeScrap IN_SERVICE→SCRAPPED / disposeSell IN_SERVICE→SOLD / reverseDisposal SCRAPPED·SOLD→IN_SERVICE（处置 posted 窗口）/ inventoryShortageDisposal IN_SERVICE→SCRAPPED（transitions 无 IDLE 边，运行时守卫保留 {IN_SERVICE,IDLE} 对齐盘点范围过滤）；initial={DRAFT}、terminal={SCRAPPED,SOLD}；**IDLE 死状态登记**（Decision (B)：intentional reserved，owner doc §2/§5 Deferred，dict 值保留不删，对齐 Movement ACTIVE 先例））+ `ErpAstDepreciationScheduleStateMachine`（4 动作 4 边：execute PENDING→EXECUTED / reverse EXECUTED→REVERSED / dispose-cancel PENDING→CANCELLED / restore CANCELLED→PENDING；initial={PENDING}、terminal={EXECUTED（可逆终态）,REVERSED}；CANCELLED 非终态——经 restore 业务恢复路径，对齐 Movement REJECTED 先例）。`app-service.beans.xml` 注册（紧邻 Movement 双轴 L97-100 后）。**接线**：CapitalizationProcessor `createAndActivateAsset` 前置 `assertCanCapitalize`（新卡 null→DRAFT 归一化）+ 目标态 `capitalizeTargetStatus()`；`executeReverseApprove` posted 窗口前置 `assertCanReverseCapitalize` + 目标态；`cancelSchedules` 目标态改 `scheduleBean.cancelTargetStatus()`（仅目标态，plan 裁定）；DisposalProcessor `executeApprove` 前置 `assertCanDispose` + 按 disposalType 选 `disposeSell/disposeScrapTargetStatus()`；`executeReverseApprove` posted 窗口前置 `assertCanReverseDispose` + 目标态；`cancelPendingSchedules`/`restoreCancelledSchedules` 目标态改调 schedule Bean（仅目标态）；ExecuteDepreciationProcessor 目标态委托 `executeTargetStatus()`（**重执行/幂等路径为动态编排逻辑保留原位，未加 assert**——EXECUTED+posted 先红冲再重执行，wasExecuted 检测保留）；ReverseDepreciationProcessor 内联 EXECUTED 守卫改 `assertCanReverse`（try/catch common 码 → cause-chain `ERR_SCHEDULE_ILLEGAL_STATUS_TRANSITION`，参数对外不变）+ 目标态 `reverseTargetStatus()`。§9.2 创建种子排除接线仅 writer 盘点登记：generateDepreciationSchedule 写 PENDING、Recalculate 写 PENDING。层 1 矩阵 `TestErpAstAssetStateMachineMatrix` 13/13 + `TestErpAstDepreciationScheduleStateMachineMatrix` 11/11 绿（IDLE 显式断言不在 transitions + 不可达 + 不在终态/初始态集；DISPOSED 归计划 3 split/merge 范围拒绝）。
- **Phase 2（M4.52 + M4.53 Inventory + Maintenance Bean）**：`ErpAstInventoryStateMachine`（5 命名动作 6 边：create DRAFT→DRAFT 创建种子（方法存在、transitions 不登记边 §9.2）/ submitForCount DRAFT→COUNTING / reconcile COUNTING→RECONCILING / post RECONCILING→POSTED（终态）/ cancel {DRAFT,COUNTING}→CANCELLED（终态）/ reverse POSTED→RECONCILING（**回卷非终态**）；approve/processVariance **不迁移**（守卫 RECONCILING 动态守卫保留原位）；initial={DRAFT}、terminal={POSTED,CANCELLED}）+ `ErpAstMaintenanceStateMachine`（6 命名动作 7 边：create DRAFT→DRAFT / submit DRAFT→SUBMITTED / startWork SUBMITTED→IN_PROGRESS / completeWork IN_PROGRESS→COMPLETED / post COMPLETED→POSTED（终态）/ cancel {DRAFT,SUBMITTED}→CANCELLED（终态）/ reverse POSTED→COMPLETED（回卷非终态）；decideTreatment/approve 不迁移；initial={DRAFT}、terminal={POSTED,CANCELLED}）。**接线**：Inventory facade cancel + 5 per-mutation（CreateInventory 守卫 `assertCanCreate` + 目标态 `createTargetStatus()` / SubmitForCount / Reconcile / Post（守卫 RECONCILING→Bean assertCanPost，错误码由 `ERR_AST_INVENTORY_NOT_RECONCILED` 变更为 `ERR_AST_INVENTORY_ILLEGAL_STATUS_TRANSITION` cause-chain，既有测试仅断言拒绝态）/ Reverse（posted boolean 动态守卫保留原位 + status 守卫 `assertCanReverse`））+ Maintenance facade cancel + 6 per-mutation（CreateMaintenance 目标态 `createTargetStatus()` / Submit / StartWork / CompleteWork / Post / Reverse（reversed/posted 动态守卫保留 + `assertCanReverse`））全 try/catch common 码 → cause-chain 领域码（`ERR_AST_INVENTORY_ILLEGAL_STATUS_TRANSITION`/`ERR_AST_MAINTENANCE_ILLEGAL_STATUS_TRANSITION`），目标态全委托 `*TargetStatus()`。**跨阶段 Asset Bean 接线（两阶段交汇点）**：`ErpAstInventoryProcessor.handleShortageTriggerDisposal:270` 注入 Phase 1 `ErpAstAssetStateMachine`，前置 `assertCanShortageDispose(asset.getStatus())` + 目标态 `shortageDisposeTargetStatus()`（先 Phase 1 落地 Asset Bean，Phase 2 接线注入——按 plan 既定顺序执行）；:251 盘盈建卡写 IN_SERVICE 为 §9.2 创建种子排除接线。层 1 矩阵 `TestErpAstInventoryStateMachineMatrix` 14/14 + `TestErpAstMaintenanceStateMachineMatrix` 15/15 绿（reverse 回卷边显式断言非终态 + cancel 边显式断言终态 + 回卷目标态有出边非死状态）。
- **Phase 3（层 3 回归 + 四实体一致性）**：`mvn test -pl module-assets/erp-ast-service -am` **BUILD SUCCESS**（erp-ast-service **185/185 tests / 24 test classes** 全绿，0 failures/errors；含既有资本化/处置/折旧/盘点/维修集成测试 + reverse 红冲 + Movement 层 3 基线——Processor 写回/过账副作用时序/schedule cancel-restore/stock move/gain-loss 计算/终态守卫零行为回归）。四实体一致性：4 Bean 命名单轴无后缀/同文件注册（紧邻 Movement 双轴）/严格无状态（零 @Inject，纯常量+状态值）/元数据形状统一（assertCanXxx + *TargetStatus + isTerminal + transitions/terminalStatuses/initialStatuses + TransitionDefinition）；16 接线 Processor 内联矩阵判断 grep 证实移除（剩余直写 setStatus 均为 §9.2 创建种子或计划 3 split/merge 范围）；R5=0（@Inject 全非 private）。
- **层 2 四方对照**：Asset 轴 dict `erp-ast/asset-status`（6 值含 DISPOSED）↔ `state-machine.md` §Asset ↔ Bean ↔ 全部 writer（capitalization/disposal/inventory-shortage 5 处 side-effect + CRUD §9.4 排除）全对齐，**IDLE 死状态登记**（transitions 无 IDLE 边 + dict 值保留 + successor），**DISPOSED 归计划 3**（split/merge writer，M4.48-51 范围）；Schedule 轴 dict 4 值 ↔ §折旧计划条目 ↔ Bean ↔ 4 schedule Processor + cancel/restore + 创建写 PENDING 全对齐；Inventory 轴 dict 5 值 ↔ `inventory.md` §一 ↔ Bean ↔ 7 writer（approve/processVariance 不迁移登记）全对齐；Maintenance 轴 dict 6 值 ↔ `maintenance.md` §一 ↔ Bean ↔ 8 writer（decideTreatment/approve 不迁移登记）全对齐。**无 owner-doc 缺口/漂移 finding**（inventory.md/maintenance.md 状态机章节已存在非缺口；owner-doc ↔ 代码矩阵零偏差，无修订）。`posted` 不入轴（§11.2 M4 (iii)）：POSTED 为 status 终态值、`posted` boolean 为过账契约标志两字段独立，reverse 红冲副作用保留在各 Dispatcher 原位。
- **验证（全绿基线）**：`mvn clean install -DskipTests` 全仓 **BUILD SUCCESS**；`bash docs/audits/nop-compliance-checker.sh` 零漂移（全 19 规则 actual = compliance-baseline BASELINE 块逐项相等：R1d=14/R2a=34/R2b=229/R2c=1392/R2d=34/R3=5/R5=0/R6=2/R10=7/R11=0/R12a=69/R12b=66/R12c=40）。独立结束审计由独立子代理（新会话）执行。
- **路线图**：`docs/backlog/entity-state-machine-migration-roadmap.md` M4.40/M4.41/M4.52/M4.53 标 **done**（规则 1）+ 头部追加 2026-08-14 记录。
- **mission-driver 复核（2026-08-14 16:16）**：`mvn clean install -DskipTests -pl module-assets/erp-ast-service -am` BUILD SUCCESS + `mvn test -pl module-assets/erp-ast-service` **187 tests 0 failures**（含层 1 矩阵 4 组 53 tests 全绿）——**full-green verification** 复跑确认；变更已按 code/docs 拆分提交（feat 2b9e0aca3 + docs 6588b0717）。

### 制造域 M4 三实体 5 轴实体级状态机 Bean 落地（M4.35–M4.39，plan `2026-08-14-0930-1`，§11.2 M4 plan-first，规则 14 合并）

- **工作项**：执行 `docs/plans/2026-08-14-0930-1-manufacturing-m4-state-machine-beans.md` 四 Phase（规则 14 合并：同 owner doc `manufacturing/state-machine.md`/同域 erp-mfg/同结果表面 = 制造域三实体状态轴矩阵集中化）。落地 5 轴实体级 Bean（契约 `docs/architecture/entity-state-machine-bean.md`）+ 三路径接线 + 层 1 矩阵 59 tests + 层 2 四方对照 + 层 3 回归。**治理**：WorkOrder approve 触发完工入库 MANUFACTURING_RECEIPT 凭证、领料 MANUFACTURING_ISSUE 凭证 + `IErpInvStockMoveBiz`、委外 SUBCONTRACT_FEE 凭证，reverseApprove/reverseCompletion 经 `MfgSubcontractReversalListener` 逆转——均属会计保护域，按 §11.2 M4 plan-first（硬约束 (i)-(v)，人工门控 2026-08-14 已确认解除，Draft Review Record 门控确认记录齐备）。
- **Phase 1（M4.35 + M4.36 WorkOrder 双轴）**：`ErpMfgWorkOrderDocumentStateMachine`（docStatus 轴 9 动作 14 边：submit DRAFT→SUBMITTED / approve SUBMITTED→NOT_STARTED / checkAvailability NOT_STARTED→STOCK_RESERVED·STOCK_PARTIAL（多目标动态结果，无单值 target，facade 保留 `result.getResultingStatus()` 写入）/ start {STOCK_RESERVED,STOCK_PARTIAL}→IN_PROCESS / stop / resume / close {STOPPED,IN_PROCESS} / reportCompletion / cancel {DRAFT,SUBMITTED,NOT_STARTED}；initial={DRAFT}、terminal={COMPLETED,CLOSED,CANCELLED}）+ `ErpMfgWorkOrderApprovalStateMachine`（approveStatus 轴 5 动作 6 边，reverseApprove→REJECTED 据实保持）。`app-service.beans.xml` 注册（112-131 行段）。**接线**：审批轴经 facade `validateTransitionFor{Submit,Withdraw,Approve,Reject,ReverseApprove}` 改调 Approval Bean `assertCanXxx`（skeleton→facade 路径，Pattern B per-mutation Processor getter submitted/approved/rejected/unsubmittedStatus 委托 Bean `*TargetStatus()`）；操作轴经 `validateTransitionForStart` + 新增 `validateTransitionFor{Stop,Resume,ReportCompletion,CheckAvailability,Cancel}` + `CloseProcessor.validateTransitionForClose` 改调 Document Bean（facade 路径）；`requireStatus` 改调 Bean（DRAFT→assertCanSubmit、SUBMITTED→assertCanApprove 两个 docStatus 侧守卫）；目标态写入（doSubmit/doApprove approveStatus + doStart/doStop/doResume/doClose/reportCompletion/cancel docStatus）委托 Bean `*TargetStatus()`。**动态守卫保留原位**：SoD（`ERR_MFG_APPROVER_IS_CREATOR`）、start 的 STOCK_PARTIAL config-gated `ERR_PARTIAL_KIT_START_FORBIDDEN`、齐套校验、`ProductionVarianceDispatcher`/`ManufacturingIssuePostingDispatcher` 过账、stock move、批次基因链、质检门控、报工超量守卫、docStatus 联动写入（§9.2 选项 c）。层 1 矩阵 25/25 绿。
- **Phase 2（M4.37 + M4.38 SubcontractOrder 双轴）**：`ErpMfgSubcontractOrderDocumentStateMachine`（docStatus 轴 8 动作 11 边：submit {DRAFT,REJECTED}→SUBMITTED（驳回后重提）/ approve / **reject SUBMITTED→REJECTED（Subcontract 独有：doReject 联动写 docStatus=REJECTED，与 WorkOrder 不同，联动写入保留 doReject 原位）** / issueMaterials / receiveFinished / postProcessingFee / reverseCompletion COMPLETED→CANCELLED（posted 判定为动态守卫保留 `validateCanReverse` 原位，Bean 仅编码状态边）/ cancel {DRAFT,SUBMITTED,APPROVED}；initial={DRAFT}、terminal={COMPLETED,CANCELLED}，COMPLETED 为可逆终态）+ `ErpMfgSubcontractOrderApprovalStateMachine`（5 动作 6 边，reverseApprove→REJECTED）。接线同 Phase 1 范式：审批轴 facade 5 guards + per-mutation getter 委托；操作轴 `requireStatus` 改调 Bean（APPROVED→assertCanIssueMaterials、ISSUED→assertCanReceiveFinished、RECEIVED→assertCanPostProcessingFee）+ cancel 直入改 `validateTransitionForCancel`；`doReverseCompletion` 目标态委托 Bean。**豁免路径（§9.2 选项 c）**：`MrpReleaseService.releaseToSubcontractOrder:206-207` spawn（APPROVED/APPROVED，O-4）+ `MfgSubcontractReversalListener.rollbackSubcontractOrder:73` 红冲回写（CANCELLED + posted=false）均不经 Bean。层 1 矩阵 25/25 绿。
- **Phase 3（M4.39 MaterialIssue 单轴）**：`ErpMfgMaterialIssueStateMachine`（docStatus 单轴 4 态，confirm DRAFT→DONE + reverseConfirm DONE→CANCELLED；initial={DRAFT}、terminal={DONE,CANCELLED}，DONE 为可逆终态；实体无 approveStatus，单轴无后缀）。**Decision (B) confirm 两步迁移建模**：单命名动作 confirm 单边 DRAFT→DONE（`confirmTargetStatus()`=DONE），CONFIRMED 为**瞬态中间态**（confirm 动作内部 DRAFT→CONFIRMED→DONE 两步写入，`ConfirmProcessor` 置位为 writer，**非死状态**、非命名动作边界、不入初始/终态集——Bean javadoc + 层 1 测试 (f) 显式登记）。**接线（本地 abstract 路径）**：`AbstractErpMfgMaterialIssueProcessor` 注入 Bean（非 private）+ 新增 `validateTransition` 委托 `assertCanConfirm`；ConfirmProcessor 幂等 DONE 短路保留原位 + 固定守卫改 `validateTransition` + DONE 写入委托 `confirmTargetStatus()`；ReverseConfirmProcessor `validateCanReverse` posted 判定保留原位 + 增 Bean `assertCanReverseConfirm` 状态守卫（映射同码 `ERR_MATERIAL_ISSUE_NOT_POSTED` 保持行为一致）+ 目标态委托 `reverseConfirmTargetStatus()`。misnamed 错误码（work-order）保持既有（路线图 Non-Goal）。层 1 矩阵 9/9 绿。
- **Phase 4（层 3 回归 + 五轴一致性）**：`mvn test -pl module-manufacturing/erp-mfg-service -am` **260/260 全绿**（0 failures/errors；基线 201 + 层 1 矩阵 59；关键层 3 基线 `TestErpMfgWorkOrderStateMachine` 10 + `TestErpMfgWorkOrderEndToEnd` 3 + `TestErpMfgCompletionPosting` 4 + `TestErpMfgProductionVariance` 12 + `TestErpMfgSubcontracting` 6 + `TestErpMfgSubcontractReverse` 4 + `TestErpMfgMaterialIssue` 2 + `TestErpMfgMaterialIssueReversal` 2 全零回归——Processor 写回/审计/SoD/领域错误码+参数/过账 dispatcher/stock move/MfgSubcontractReversalListener 时序不变）。五轴一致性：命名（Document/Approval/无后缀）/同文件注册/严格无状态（零 @Inject）/元数据形状统一/三路径接线（skeleton→facade / facade 直入 / 本地 abstract）可追溯；R5=0（`@Inject` 全非 private）。
- **层 2 四方对照**：逐实体逐轴（WorkOrder docStatus 14 边 + approveStatus 6 边 / SubcontractOrder docStatus 11 边 + approveStatus 6 边 / MaterialIssue 2 边）dict ↔ `manufacturing/state-machine.md` ↔ Bean 元数据 ↔ 全部 writer（10+5 Processor live + checkAvailability/cancel facade 直入 + MrpRelease spawn + ReversalListener 回写 + CONFIRMED 瞬态 writer + CRUD §9.4 选项 c 排除）全对齐，记录于计划 Closure 段。无新死状态、无 owner-doc 漂移。
- **验证（全绿基线）**：`mvn clean install -DskipTests` 全仓 **BUILD SUCCESS**；`bash docs/audits/nop-compliance-checker.sh` 零漂移 exit 0（R5=0/R11=0，R12a=69/R12b=66/R12c=40 全对齐 compliance-baseline BASELINE 块）。独立结束审计由独立子代理（新会话）执行。
- **mission-driver 复核（2026-08-14 15:26）**：`mvn clean install -DskipTests` 全仓 BUILD SUCCESS（4:03 min）+ `mvn test -pl module-manufacturing/erp-mfg-service -am` **260 tests 0 failures** + compliance checker exit 0（R1d=14/R2a=34/R2b=229/R2c=1392/R2d=34/R3=5/R5=0/R6=2/R10=7/R11=0/R12a=69/R12b=66/R12c=40 全对齐基线）——**full-green verification** 复跑确认。
- **路线图**：`docs/backlog/entity-state-machine-migration-roadmap.md` M4.35–M4.39 标 **done**（规则 1）+ 头部追加 2026-08-14 记录。

### 物流域发运单 ErpLogShipment.status 实体级状态机 Bean 落地（M4.57，plan `2026-08-14-0456-3`，§11.2 M4 plan-first）

- **工作项**：执行 `docs/plans/2026-08-14-0456-3-erplog-shipment-state-machine-bean.md` 两 Phase。落地 1 轴实体级 Bean（契约 `docs/architecture/entity-state-machine-bean.md`）+ GatewayDispatcher Facade 接线 + 层 1 矩阵 + 层 2 四方对照 + 层 3 回归。**治理**：DELIVERED 触发 path-1 FREIGHT 运费过账（`IErpFinVoucherBiz.post`，直接调用 `InvPostingExecutor` 范式）+ path-2 config-gated 到岸成本自动创建（`IErpInvLandedCostBiz.generateFreightLandedCost`，默认 OFF）属会计保护域，按 §11.2 M4 plan-first（硬约束 (i)-(v)，人工门控 2026-08-14 已确认解除并转 active）。
- **Phase 1（Bean + 注册 + Facade 接线 + 层 1 矩阵）**：`ErpLogShipmentStateMachine`（严格无状态，5 命名动作 10 边：advise DRAFT→ADVISED / completeShipment ADVISED→DISPATCHED / advanceToInTransit DISPATCHED→IN_TRANSIT / advanceToDelivered {ADVISED,DISPATCHED,IN_TRANSIT}→DELIVERED（**Decision (C) 刻意收紧**——code `:168` 原无来源态守卫，任何非 DELIVERED 含 DRAFT/CANCELLED 可推进到 DELIVERED；Bean 排除 DRAFT/CANCELLED，安全改善非语义破坏）+ cancelShipment 4 源→CANCELLED；initial={DRAFT}、terminal={DELIVERED,CANCELLED}；`transitions()` 10 边元数据）。`app-service.beans.xml` FQN-id 注册（紧邻既有 GatewayDispatcher bean）。**接线（Bean 接入点 = GatewayDispatcher Facade，非 per-mutation Processor——守卫全集中 Facade，6 Processor 单行委托）**：注入 `@Inject ErpLogShipmentStateMachine`（非 private）；`advise`/`completeShipment`/`cancelShipment`/`advanceTracking` 前向守卫全部委托 `assertCanXxx` + 目标态改 `*TargetStatus()` 写回（含 `writeBackSuccess` DISPATCHED 写回）；try/catch common 码 `ERR_ILLEGAL_STATUS_TRANSITION` → cause-chain 领域码 `ERR_LOG_SHIPMENT_ILLEGAL_TRANSITION`（参数对外不变）；**保留原位**（Decision B）：4 幂等短路（advise 已 ADVISED / completeShipment DISPATCHED+ / cancelShipment CANCELLED|DELIVERED / advanceTracking 已 DELIVERED）+ 网关重试/死信/告警 + DISPATCHED/IN_TRANSIT 网关取消 + DELIVERED path-1/path-2 编排 + `freightSettlementStatus` 独立轴。层 1 矩阵 `TestErpLogShipmentStateMachineMatrix` **12/12 绿**（10 边无重复/冲突、6 值全可达、advanceToDelivered 收紧集断言、元数据一致性、终态/初始态、真终态无出边）。
- **Phase 2（层 2 四方对照 + 层 3 回归）**：dict `erp-log/shipment-status`（6 值，orm.xml:39-46）↔ owner-doc §2 迁移表（state-machine.md :22-51）↔ Bean 元数据 ↔ 全部 writer（GatewayDispatcher 5 call-site：advise:81/writeBackSuccess:352/advanceTracking:190/advanceTracking:213/cancelShipment:165 + 创建写 DRAFT + 幂等短路 + CRUD 路径按 §9.2 选项 (c) 排除，xmeta status insertable/updatable 实证）**全对齐**。**DELIVERED drift finding**：advanceTracking DELIVERED 写入原无来源态守卫（IN_TRANSIT 写入有 DISPATCHED 守卫 :178 作对照）→ 分类 `intentional narrowing`（code 比 owner-doc 宽松，Bean 向 owner-doc 收紧方向靠拢；owner-doc 仅 IN_TRANSIT 单源，Bean 保留 ADVISED/DISPATCHED 因 advanceTracking 可从 DISPATCHED 直达 DELIVERED）+ successor（PM 要求 DELIVERED 严格仅从 IN_TRANSIT 时进一步收紧）。层 3 既有 8 个集成测试全绿（`mvn test -pl module-logistics/erp-log-service` **39/39**：矩阵 12 + Gateway 6 + PostingEnd 2 + FreightPosting 4 + Path2LandedCost 3 + CarrierGatewayIntegration 3 + TrackingNoUk 2 + CrudSmoke 5 + LogPostingFaultInjection 1 + CredentialMasking 1——Dispatcher 写回/FREIGHT 过账时序/到岸成本编排/幂等行为/网关重试死信/终态守卫 `ERR_LOG_SHIPMENT_ALREADY_DELIVERED` 零行为回归）。
- **验证（全绿基线）**：`mvn clean install -DskipTests` 全仓 **BUILD SUCCESS**；`bash docs/audits/nop-compliance-checker.sh` 零漂移（R5=0/R11=0，R1d=14/R2a=34/R2b=229/R2c=1392/R2d=34/R3=5/R6=2/R10=7/R12a=69/R12b=66/R12c=40 全对齐 compliance-baseline）。独立结束审计由独立子代理（新会话）执行。
- **路线图**：`docs/backlog/entity-state-machine-migration-roadmap.md` M4.57 标 **done**（规则 1）+ 头部追加 2026-08-14 记录。

# Development Log - 2026-08-14

### HR 域薪酬 ErpHrSalary paymentStatus + approveStatus 双轴实体级状态机 Bean 落地（M4.63 + M4.64，plan `2026-08-14-0456-2`，§11.2 M4 plan-first，规则 14 合并）

- **工作项**：执行 `docs/plans/2026-08-14-0456-2-erphr-salary-state-machine-beans.md` 三 Phase（规则 14 合并：同 owner doc `human-resource/state-machine.md §适用对象四`/同实体 ErpHrSalary/同结果表面，两独立 Bean）。落地 2 轴实体级 Bean（契约 `docs/architecture/entity-state-machine-bean.md`）+ Processor/BizModel/xbiz 接线 + 层 1 矩阵 + 层 2 四方对照 + 层 3 回归。**治理**：markPaid 触发 SALARY_PAYMENT(280) 发放凭证（active）+ approve 触发 Deferred SALARY(270)/290/300 计提凭证（dead-code，config 翻转后触发受保护行为）属会计保护域，按 §11.2 M4 plan-first（硬约束 (i)-(v)，人工门控 2026-08-14 已确认解除并转 active）。
- **Phase 1（M4.63 paymentStatus Bean + 注册 + 接线 + 层 1 矩阵）**：`ErpHrSalaryPaymentStateMachine`（严格无状态，2 命名动作：markPaid PENDING→PAID / voidSalary PENDING→VOID；initial={PENDING}、terminal={PAID,VOID} 纯终态；dict `erp-hr/salary-payment-status` 3 值全活跃无死状态）。`app-service.beans.xml` FQN-id 注册（紧邻既有 4 SM Bean）。接线（镜像 M2.11 范式）：`AbstractErpHrSalaryProcessor` 注入 `@Inject ErpHrSalaryPaymentStateMachine`（非 private）；`ErpHrSalaryMarkPaidProcessor` payment 轴守卫 `assertCanMarkPaid` + 目标态 `markPaidTargetStatus()`（try/catch common 码 → cause-chain `ERR_SALARY_ILLEGAL_STATUS_TRANSITION` + salaryId，approve 轴守卫 Phase 2 迁移）；`ErpHrSalaryGenerateBankFileProcessor` PAID 写入改 Bean 目标态（守卫为查询级 `findPayableSalaries`）；`ErpHrSalaryBizModel.voidSalary` Decision (A) 直接注入 Bean——PAID 终态走领域码 `ERR_SALARY_LOCKED_AFTER_PAID` 保留（§11.4 终态领域异常重叠）、其余非法态 Bean common 码 → 领域码映射，目标态 `voidTargetStatus()`（不提取 Processor，R6.7 successor）。SALARY_PAYMENT 过账 + 银行文件生成 + 审批轴守卫保留原位。层 1 矩阵 `TestErpHrSalaryPaymentStateMachineMatrix` **7/7 绿**。
- **Phase 2（M4.64 approveStatus Bean + 注册 + xbiz 5 动作接线 + 层 1 矩阵）**：`ErpHrSalaryApprovalStateMachine`（严格无状态，5 动作 6 边：submit {UNSUBMITTED,null,REJECTED}→SUBMITTED / approve {SUBMITTED}→APPROVED / reject {SUBMITTED}→REJECTED / reverseApprove {APPROVED}→SUBMITTED / withdrawApproval {SUBMITTED}→UNSUBMITTED + markPaid 交叉守卫 assertCanMarkPaid（一致性，非迁移边，不进 transitions()）；initial={UNSUBMITTED}、terminal={APPROVED} 可逆终态（唯一出边 reverseApprove）；矩阵裁定依据实仓 xbiz 守卫 5 行号 + `TestErpHrPayrollEngine.testIllegalTransitionRejects`）。**机制替代注记（执行期 Decision）**：计划原案「XScript try/catch common→领域映射」在 XLang 引擎不可行——`TryStatement` 语法节点被编译层拒绝（`nop.err.xlang.exec.not-supported-node`，XLang 仅支持 try/finally 且 catch 后隐式重抛）；映射下沉 Java `ErpHrSalaryApprovalGuard` Bean（契约 §7 接线层职责：调 Bean `assertCanXxx`，common 码 → 领域 `ERR_SALARY_ILLEGAL_STATUS_TRANSITION` + salaryId/currentStatus/expectedStatus，cause 保留），XScript 仅 inject Guard + Bean（`*TargetStatus()` 写回），行为契约不变。xbiz 5 动作（submitForApproval/approve/reject/reverseApprove/withdrawApproval）全部委托，非法迁移错误码由平台 `nop.err.wf.approve.invalid-status` 改为领域码（守卫/驳回场景域内错误码约定）；xwf 三级审批链 + wf 启动 + approvedBy/approvedAt + notify 保留原位。`MarkPaidProcessor` approve 轴守卫改 approval Bean `assertCanMarkPaid` 交叉守卫。**dict 漂移裁定 (C)**：`erp-hr/salary-approval-status`（6 值 legacy）未被任何列引用，Bean 按 `wf/approve-status` 4 值编码，legacy 登记 doc drift + successor（PM 要求 6 态审批链时重开，不从 ORM 删除）。层 1 矩阵 `TestErpHrSalaryApprovalStateMachineMatrix` **11/11 绿**；`testIllegalTransitionRejects` 强化断言领域码（xbiz→Guard→Bean 链运行时实证）。
- **Phase 3（层 3 回归 + 双轴一致性复核）**：`mvn test -pl module-hr/erp-hr-service -am` **232/232 全绿**（0 failures/errors；含 `TestErpHrSalaryWorkflowApproval` 3/3 xwf 审批链 + `TestErpHrPayrollEngine` 10/10 PAID 锁/非法迁移/银行文件/过账 + `TestErpHrPayrollSimulation` 12/12 + 全部 6 个 SM 矩阵测试——零行为回归）。双轴一致性：2 Bean 命名（Payment/Approval 后缀）/注册/严格无状态/元数据形状一致；abstract+BizModel→Bean 注入 + cause-chaining 与 M2.11 可追溯一致；xbiz inject 模式运行时验证。
- **层 2 四方对照**：payment 轴 dict `erp-hr/salary-payment-status`（3 值）↔ owner-doc §适用对象四 §4 ↔ Bean 元数据 ↔ 5 writer（PayrollCalculator 初始 + MarkPaid + GenerateBankFile + voidSalary + SimulationConvert 初始 + CRUD 排除）全对齐无死状态；approval 轴 dict `wf/approve-status`（4 值）↔ owner-doc ↔ Bean 元数据 ↔ 8 writer（xbiz 5 动作 + MarkPaid 交叉守卫 + PayrollCalculator 初始 + SimulationConvert 初始 + CRUD 排除）全对齐；`erp-hr/salary-approval-status` 6 值 drift 登记。
- **验证（全绿基线）**：`mvn clean install -DskipTests` 全仓 **BUILD SUCCESS**；`bash docs/audits/nop-compliance-checker.sh` 零漂移（R5=0/R11=0，R1d=14/R2a=34/R2b=229/R2c=1392/R2d=34/R3=5/R6=2/R10=7/R12a=69/R12b=66/R12c=40 全对齐 compliance-baseline）。独立结束审计（子代理新会话零信任）三轮收敛：第一轮 FAIL（文档完成步骤缺失——plan 未 tick/roadmap 未标 done/日志缺失；代码与验证全 PASS）→ 补完文档后第二轮 FAIL（Closure 证据占位符未填，其余全 PASS）→ 第三轮 **PASS**（`ses_00155532effe6WUrdseXy4dgHK`，232 tests 亲跑全绿 + compliance 全对齐 + 工件一致性 10 项检查全 PASS）。
- **路线图**：`docs/backlog/entity-state-machine-migration-roadmap.md` M4.63/M4.64 标 **done**（规则 1）+ 头部追加 2026-08-14 记录。

### 财务域核销单 ErpFinReconciliation.docStatus + 坏账单 ErpFinBadDebt.approveStatus 单轴实体级状态机 Bean 落地（M4.3 + M4.10，plan `2026-08-14-0456-1`，§11.2 M4 plan-first，规则 14 合并）

- **工作项**：执行 `docs/plans/2026-08-14-0456-1-erpfin-reconciliation-baddebt-state-machine-beans.md` 三 Phase（规则 14 合并：同 owner doc `finance/state-machine.md`/同域 erp-fin/同结果表面 = 财务域二实体状态轴矩阵集中化）。落地 2 个单实体单轴 Bean（契约 `docs/architecture/entity-state-machine-bean.md`）+ 7 守卫接线 + 层 1 矩阵 + 层 2 四方对照 + 层 3 回归 + owner doc 补 §对象七/§对象八。**治理**：Reconciliation post 触发 ArApItem 核销联动（settled/openAmount 回写）+ reverse 红冲侧；BadDebt approve 触发 BAD_DEBT_WRITE_OFF/RECOVERY 凭证（config-gated）+ reverseApprove 红冲凭证 + ArApItem 对称回滚——均属会计保护域，按 §11.2 M4 plan-first（硬约束 (i)-(v)，人工门控 2026-08-14 已确认解除并转 active）。
- **Phase 1（M4.3 Reconciliation docStatus Bean + 注册 + 接线 + 层 1 矩阵 + owner doc 章节）**：`ErpFinReconciliationDocumentStateMachine`（严格无状态，2 命名动作：post DRAFT→POSTED / reverse POSTED→REVERSED；initial={DRAFT}、terminal={REVERSED}、POSTED 为中间态；dict `erp-fin/reconciliation-status` 3 值全活跃无死状态）。`app-service.beans.xml` FQN-id 注册（紧邻 Voucher Bean）。接线（镜像 M4.1 Voucher 范式）：`ErpFinReconciliationPostProcessor` 守卫改 Bean `assertCanPost` + 目标态 `postTargetStatus()`；`ErpFinReconciliationReverseProcessor` 守卫 `assertCanReverse` + `reverseTargetStatus()`；BizModel `previewReverse` 守卫同 Bean（一致性）；`AbstractErpFinReconciliationProcessor` + BizModel 的 `statusError()` 重复副本统一 cause-chain（common 码 `ERR_ILLEGAL_STATUS_TRANSITION` 作 cause，领域码 `ERR_RECONCILIATION_STATUS_INVALID` 参数对外不变）。ArApItem 核销联动 + 往来余额刷新 + FX 凭证红冲 + postedBy/postedAt 编排保留原位（§11.2 M4 (ii)/(iv)/(v)）；`posted` 不入轴（本实体无 posted boolean 列，核销状态即 docStatus 表达）。层 1 矩阵 `TestErpFinReconciliationStateMachineMatrix` **7/7 绿**。owner doc 补 **§对象七：核销单状态机**（单轴 + 迁移表 + 过账边界 + 四方对照）。
- **Phase 2（M4.10 BadDebt approveStatus Bean + 注册 + 接线 + 层 1 矩阵 + owner doc 章节）**：`ErpFinBadDebtApprovalStateMachine`（严格无状态，4 命名动作 6 边：submit UNSUBMITTED→SUBMITTED（**无 REJECTED 重提**——facade `validateTransitionForSubmit` require UNSUBMITTED only）/approve {UNSUBMITTED,SUBMITTED}→APPROVED / reject {UNSUBMITTED,SUBMITTED}→REJECTED / reverseApprove APPROVED→REJECTED（§16.4 合规）；initial={UNSUBMITTED}、terminal={APPROVED,REJECTED}，APPROVED 可逆终态 / REJECTED 不可逆终态；ORM 列名 `approvalStatus` 单 p 不改，Bean 命名 Approval 后缀对齐 dict）。`app-service.beans.xml` 注册（紧邻 BudgetScenario Approval Bean）。接线：facade `ErpFinBadDebtProcessor` 注入 Bean（`@Inject` 非 private），`validateTransitionForSubmit/Approve/Reject` + `executeReverseApprove` 守卫改 Bean `assertCan*`（try/catch common 码 → cause-chain `illegalTransition()` 领域码 `ERR_BAD_DEBT_ILLEGAL_APPROVAL_TRANSITION`，参数对外不变）；`doSubmit`/`approveInternal`/`doReject`/`executeReverseApprove` 目标态改 Bean `*TargetStatus()`。**保留原位**：BAD_DEBT_WRITE_OFF/RECOVERY 凭证过账 + ArApItem 变异/对称回滚 + config-gate（`erp-fin.bad-debt-write-off-require-approval`）+ `ErpFinBadDebtReverseApproveProcessor:28` 终态守卫 `ERR_BAD_DEBT_NOT_APPROVED_OR_NOT_POSTED`（含 voucherId 已过账判定，动态业务守卫）+ SoD；WriteOff/Recover auto-approve 旁路（§9.2 选项 c 生成写入）不经 Bean；per-mutation stubs 旁路 common 骨架（Pattern B dead-code）不触碰。层 1 矩阵 `TestErpFinBadDebtStateMachineMatrix` **10/10 绿**。owner doc 补 **§对象八：坏账单状态机**（4 动作 + auto-approve 旁路 + per-mutation stubs 旁路架构事实 + 不可逆 REJECTED 终态 + config-gate 边界）。
- **Phase 3（层 3 回归 + 二轴一致性复核）**：`mvn test -pl module-finance/erp-fin-service -am` **463/463 全绿**（层 1 矩阵 17 + 层 3 回归 446，0 failures/errors；含 `TestErpFinReconciliation` 7/7 + `TestErpFinReconciliationReversePreview` 1/1 + `TestErpFinBadDebt` 7/7 + `TestErpFinBadDebtReversal` 3/3 + `TestErpFinBadDebtProvisionReversal` 5/5 + `TestErpFinAutoReconciliation` 7/7 + `TestErpFinDualSideConsistency` 4/4 + `TestErpFinPartnerBalance` 2/2——Processor 写回/ArApItem 核销联动/过账副作用时序/config-gate 零行为回归）。二轴一致性复核：2 Bean 命名（Document/Approval 后缀）/同文件注册（紧邻 Voucher/BudgetScenario Bean）/严格无状态（零 @Inject）/元数据形状与 M4.1 可追溯一致。
- **验证（全绿基线）**：`mvn clean install -DskipTests` 全仓 **BUILD SUCCESS**；`bash docs/audits/nop-compliance-checker.sh` 零漂移（R5=0/R11=0，R1d=14/R2a=34/R2b=229/R2c=1392/R2d=34/R3=5/R6=2/R10=7/R12a=69/R12b=66/R12c=40 全对齐 compliance-baseline）。
- **路线图**：`docs/backlog/entity-state-machine-migration-roadmap.md` M4.3/M4.10 标 **done**（规则 1）+ 头部追加 2026-08-14 记录。
- **并发会话注记**：执行期检测到另一会话（plan-1351-3 WIP stash）的 stash 操作与本会话 git 操作竞争，致 5 文件工作树出现冲突标记；经核对冲突标记仅为本会话 `git stash pop` 误触他人 stash 所致，已将工作树恢复至冲突前状态（=HEAD），他人 WIP 完整保留于 `stash@{0}`，零数据丢失。

### 财务域员工借款单 ErpFinEmployeeAdvance docStatus + approveStatus 双轴实体级状态机 Bean 落地（M4.6 + M4.7，plan `2026-08-13-1146-3`，§11.2 M4 plan-first）

- **工作项**：执行 `docs/plans/2026-08-13-1146-3-erpfin-employee-advance-state-machine-beans.md` 三 Phase（规则 14 合并：同实体双轴/同 owner doc 义务/同结果表面，两独立 Bean）。落地 2 轴实体级 Bean（契约 `docs/architecture/entity-state-machine-bean.md`）+ 6 守卫接线 + 层 1 矩阵 + 层 2 四方对照 + 层 3 回归 + owner doc 补 §对象六。**治理**：approve 触发业财过账（`EmployeeAdvancePostingDispatcher`→`FinPostingExecutor` EMPLOYEE_ADVANCE 凭证 Dr 1221 其他应收-员工预支/Cr 1002 银行存款 + `ErpFinArApItem` RECEIVABLE，`EmployeeAdvanceAcctDocProvider implements IErpFinAcctDocProvider`）+ reverseApprove/cancel 红冲属会计保护域，按 §11.2 M4 plan-first（硬约束 (i)-(v)，人工门控 2026-08-14 已确认解除并转 active）。本实体为费用报销冲抵的**被冲抵方**（冲抵由 ExpenseClaim 侧 `AdvanceOffsetOrchestrator` 驱动，本 Processor 无 offset 调用）。
- **Phase 1（双轴 Bean + 注册 + 层 1 矩阵）**：`ErpFinEmployeeAdvanceApprovalStateMachine`（严格无状态，5 命名动作：submit {UNSUBMITTED,REJECTED}→SUBMITTED（null 归一 UNSUBMITTED）/withdraw {SUBMITTED}→UNSUBMITTED/approve {SUBMITTED}→APPROVED/reject {SUBMITTED}→REJECTED/reverseApprove {APPROVED}→REJECTED；initial={UNSUBMITTED}、terminal={APPROVED,REJECTED} 可逆终态；形状与 `ErpFinExpenseClaimApprovalStateMachine` 一致但为独立 Bean）+ `ErpFinEmployeeAdvanceDocumentStateMachine`（1 命名动作 cancel {非 CANCELLED}→CANCELLED；initial={DRAFT}、terminal={CANCELLED}；**docStatus 残余值 SUBMITTED/APPROVED/REJECTED 不纳入任一集合**，intentional reserved，同 ExpenseClaim）。`app-service.beans.xml` FQN-id 注册。层 1 矩阵 `TestErpFinEmployeeAdvanceApprovalStateMachineMatrix` **12/12** + `TestErpFinEmployeeAdvanceDocumentStateMachineMatrix` **7/7**（共 19/19 绿）。**Decision（前置）**：(a) docStatus 残余值分类（intentional legacy dict）；(b) `ERR_EMPLOYEE_ADVANCE_NOT_REVERSED_BEFORE_CANCEL`（`ErpFinErrors.java:206`）grep 证实仅定义零 throw——doCancel 自动红冲不抛此码，疑似 dead code，裁定交 Phase 3。
- **Phase 2（Processor 接线 + 层 3 回归，行为保持）**：`ErpFinEmployeeAdvanceProcessor` 注入两 Bean（`@Inject` 非 private），6 个 `validateTransitionFor*` 内联 `Objects.equals` 矩阵守卫全部改 Bean `assertCan*` 委托 + do* 目标态回写（`<action>TargetStatus()`）；common→领域码映射（Approval→`ERR_EMPLOYEE_ADVANCE_ILLEGAL_STATUS_TRANSITION`，Document→`ERR_EMPLOYEE_ADVANCE_ILLEGAL_DOC_STATUS_TRANSITION`，common 作 cause）+ 参数对外不变（「UNSUBMITTED 或 REJECTED」「非已作废」文案保留）；**完整保留** SoD（`doApprove` `SoDGuard` 动态守卫非 Bean 范畴）/tryPost/reverse/markPosted/clearPosted helper/approvedBy/approvedAt 写入/业务规则校验（employee-inactive/partner-missing/amount-invalid）/reverseApprove→REJECTED/cancel 自动红冲 if APPROVED+posted/乐观锁；**cashRepay/reverseCashRepay（BizModel 资金动作，非状态轴）不触碰**。grep 证实无内联矩阵判断残留（仅动态副作用条件）。层 3 全量 **454/454 绿**（含 `TestErpFinEmployeeAdvanceApproval` 7/7 + `TestErpFinEmployeeAdvancePosting` 2/2 凭证+ArApItem RECEIVABLE+红冲 + `TestErpFinEmployeeAdvanceCashRepay` 6/6 + `...CashRepayReversal` 3/3）。
- **Phase 3（层 2 四方对照 + owner doc + 漂移 Decision）**：四方对照 10 维度双轴各一份——6 writer 全集独立 grep 重核（facade do* 5 approve + 1 cancel，per-mutation 桥接排除，BizModel 零状态写入）、reverseApprove→REJECTED 合规（§16.4 非 drift）、docStatus 残余值排除、SoD/过账副作用边界（非 Bean 范畴）、approve→posted=true + ArApItem RECEIVABLE 原序保留、NOT_REVERSED_BEFORE_CANCEL 零 throw 死码确认、冲抵引用关系（被冲抵方无 offset 调用）。漂移裁定：(D1) docStatus 残余值 = `intentional legacy dict`；(D2) M0.2 §3.5 finance M4.6/7「测试：无」与实仓漂移（实有 Approval 7 @Test + Posting 2 + CashRepay 6 + CashRepayReversal 3，建议 reconcile）；(D3) `ERR_EMPLOYEE_ADVANCE_NOT_REVERSED_BEFORE_CANCEL` = dead error code（cancel 自动红冲为 intentional auto-reverse，清理归 successor）；(D4) reverseApprove→REJECTED 已合规；(D5) SoD 动态守卫保留原位。owner doc `docs/design/finance/state-machine.md` 适用对象增列员工借款（第 6 项）+ 新增 **§对象六：员工借款单状态机（双轴）**（approveStatus 5 边 + docStatus 1 边 + 残余值说明 + SoD 声明 + 过账/冲抵边界 + cashRepay 非状态轴声明 + 死码注记）。
- **验证（全绿基线）**：`mvn test -pl module-finance/erp-fin-service -am` **454/454 全绿**（层 1 矩阵 19 + 层 3 回归 435，0 failures/errors）；`mvn clean install -DskipTests` 全仓 **BUILD SUCCESS**（156 模块）；`bash docs/audits/nop-compliance-checker.sh` 零漂移（R5=0/R11=0，R1d=14/R2a=34/R2b=229/R2c=1392/R2d=34/R3=5/R6=2/R10=7/R12a=69/R12b=66/R12c=40 全对齐 compliance-baseline）。独立结束审计（子代理新会话零信任，`ses_001bb790bffeNdNANI9XCQ1KLh`）**APPROVED**（37 关键测试亲跑全绿 + 10 项检查全 PASS；2 项闭包簿记 MINOR 已处理）。
- **路线图**：`docs/backlog/entity-state-machine-migration-roadmap.md` M4.6/M4.7 标 **done**（规则 1）+ 头部追加 2026-08-14 记录。

### 财务域费用报销单 ErpFinExpenseClaim docStatus + approveStatus 双轴实体级状态机 Bean 落地（M4.4 + M4.5，plan `2026-08-13-1146-2`，§11.2 M4 plan-first）

- **工作项**：执行 `docs/plans/2026-08-13-1146-2-erpfin-expense-claim-state-machine-beans.md` 三 Phase（规则 14 合并：同实体双轴/同 owner doc 义务/同验证路径，两独立 Bean）。落地 2 轴实体级 Bean（契约 `docs/architecture/entity-state-machine-bean.md`）+ 6 守卫接线 + 层 1 矩阵 + 层 2 四方对照 + 层 3 回归 + owner doc 补 §对象五。**治理**：approve 触发业财过账（`ExpenseClaimPostingDispatcher`→`FinPostingExecutor` EXPENSE_CLAIM 凭证 + `ErpFinArApItem` PAYABLE + `AdvanceOffsetOrchestrator` 员工借款冲抵）+ reverseApprove/cancel 红冲属会计保护域，按 §11.2 M4 plan-first（硬约束 (i)-(v)，人工门控 2026-08-14 已确认解除并转 active）。
- **Phase 1（双轴 Bean + 注册 + 层 1 矩阵）**：`ErpFinExpenseClaimApprovalStateMachine`（严格无状态，5 命名动作：submit {UNSUBMITTED,REJECTED}→SUBMITTED（null 归一 UNSUBMITTED）/withdraw {SUBMITTED}→UNSUBMITTED/approve {SUBMITTED}→APPROVED/reject {SUBMITTED}→REJECTED/reverseApprove {APPROVED}→REJECTED；initial={UNSUBMITTED}、terminal={APPROVED,REJECTED} 可逆终态）+ `ErpFinExpenseClaimDocumentStateMachine`（1 命名动作 cancel {非 CANCELLED}→CANCELLED；initial={DRAFT}、terminal={CANCELLED}；**docStatus 残余值 SUBMITTED/APPROVED/REJECTED 不纳入任一集合**，intentional reserved）。`app-service.beans.xml` FQN-id 注册。层 1 矩阵 `TestErpFinExpenseClaimApprovalStateMachineMatrix` **12/12** + `TestErpFinExpenseClaimDocumentStateMachineMatrix` **7/12**（共 19/19 绿）。**Decision（前置）**：docStatus 残余值分类——dict 5 值但代码仅写 DRAFT/CANCELLED，workflow 轴 = approveStatus。
- **Phase 2（Processor 接线 + 层 3 回归，行为保持）**：`ErpFinExpenseClaimProcessor` 注入两 Bean（`@Inject` 非 private），6 个 `validateTransitionFor*` 内联 `Objects.equals` 矩阵守卫全部改 Bean `assertCan*` 委托 + do* 目标态回写（`<action>TargetStatus()`）；common→领域码映射（Approval→`ERR_EXPENSE_CLAIM_ILLEGAL_STATUS_TRANSITION`，Document→`ERR_EXPENSE_CLAIM_ILLEGAL_DOC_STATUS_TRANSITION`，common 作 cause）+ 参数对外不变（「UNSUBMITTED 或 REJECTED」「非已作废」文案保留）；**完整保留** SoD（`doApprove` `SoDGuard` 动态守卫非 Bean 范畴）/tryPost/reverse/offset/reverseOffset/posted/approvedBy/approvedAt 写入/业务规则校验/reverseApprove→REJECTED/cancel 红冲 if posted/乐观锁。grep 证实无内联矩阵判断残留（仅动态副作用条件）。层 3 全量 **435/435 绿**（含 `TestErpFinExpenseClaimApproval` 9/9 SoD 自审拒绝 + `TestErpFinExpenseClaimPosting` 3/3 凭证+红冲 + `TestErpFinExpenseOffsetAdvance` 2/2 冲抵+reverse）。
- **Phase 3（层 2 四方对照 + owner doc + 漂移 Decision）**：四方对照 10 维度双轴各一份——6 writer 全集独立 grep 重核（facade do* 5 approve + 1 cancel，per-mutation 桥接排除）、reverseApprove→REJECTED 合规（§16.4 非 drift）、docStatus 残余值排除、SoD/过账/冲抵副作用边界（非 Bean 范畴）、approve→posted=true + ArApItem PAYABLE + offset 原序保留。漂移裁定：(D1) docStatus 残余值 = `intentional legacy dict`（dict 治理归 successor ask-first）；(D2) M0.2 §3.5 finance M4.4/5「测试：无」与实仓漂移（实有 Approval 9 @Test + 4 过账测试，建议 reconcile）；(D3) reverseApprove→REJECTED 已合规；(D4) SoD 动态守卫保留原位。owner doc `docs/design/finance/state-machine.md` 补 **§对象五：费用报销单状态机（双轴）**（approveStatus 5 边 + docStatus 1 边 + 残余值说明 + SoD 声明 + 过账/冲抵边界）。
- **验证（全绿基线）**：`mvn test -pl module-finance/erp-fin-service -am` **435/435 全绿**（层 1 矩阵 19 + 层 3 回归 416，0 failures/errors）；`mvn clean install -DskipTests` 全仓 **BUILD SUCCESS**（156 模块）；`bash docs/audits/nop-compliance-checker.sh` 零漂移（R5=0/R11=0，R1d=14/R2a=34/R2b=229/R2c=1392/R2d=34/R3=5/R6=2/R10=7/R12a=69/R12b=66/R12c=40 全对齐 compliance-baseline）。独立结束审计（子代理新会话零信任）**APPROVED**（33 关键测试亲跑全绿；2 项发现——roadmap M4.4/5 未标 done + 日志缺失——已补齐）。
- **路线图**：`docs/backlog/entity-state-machine-migration-roadmap.md` M4.4/M4.5 标 **done**（规则 1）。

### VERIFY（mission requirement-compliance，plan `2026-08-13-1146-1` 全绿确认 + 2 项预存失败登记）

- **构建验证**：`mvn clean install -DskipTests` 全 reactor **BUILD SUCCESS**（156 模块，1:35）。`mvn test -pl module-finance/erp-fin-service -am` **408/408 全绿**（含新增层 1 矩阵 `TestErpFinNotesReceivableStateMachineMatrix` 15/15 + `TestErpFinNotesPayableStateMachineMatrix` 11/11 + 既有 Receivable/Payable StateMachine + Posting 回归，0 failures/errors）。`bash docs/audits/nop-compliance-checker.sh` 19 规则基线维持（R5=0/R11=0，R10=7 既有漂移）。
- **全 reactor `mvn test` 2 项预存失败（非本计划引入，clean HEAD 复现证据齐备）**：
  1. **mfg `ErpMfgCostRollupLine` view.xml band cells**：`ErpAllFluxPagesTest`/`ErpAllWebPagesTest` 报 `cell-not-prop`（form view `materialBand` 非实体属性）——`452e418d0`（08-11 E4.1）代理视图 4 档位字段引入，E4.1 验证未跑 app-erp-all 页面验证。已登记 `docs/bugs/2026-08-14-0930-mfg-costrollupline-band-cells-cell-not-prop.md` + known-good-baselines Known Failures。
  2. **`TestAuthSeedLoadingProof` NPE**：`OrmTransactionListener.onBeforeCommit` ormTemplate null——nop-entropy `e5ee02b40`（08-11）`nopDefaultTransactionListener.ormTemplate` 改 `ioc:lazy-property`，lazy-prop 阶段晚于 `DataInitInitializer` @PostConstruct 事务提交；.m2 jar 08-13 重建后暴露（08-10/08-11 日志均绿）。外部仓库（ask-first），已登记 `docs/bugs/2026-08-14-0930-authseed-loading-npe-ormtransactionlistener.md` + known-good-baselines Known Failures。
- **排除方法（可复现）**：`git stash` 回退全部未提交变更 → `mvn clean install -DskipTests -pl module-finance/erp-fin-service -am`（确保 .m2 jar 不含本计划变更）→ 两失败测试仍同样失败 → 证明非本计划引入。

### 财务域应收/应付票据 ErpFinNotesReceivable/ErpFinNotesPayable status 实体级状态机 Bean 落地（M4.8 + M4.9，plan `2026-08-13-1146-1`，§11.2 M4 plan-first）

- **工作项**：执行 `docs/plans/2026-08-13-1146-1-erpfin-notes-receivable-payable-state-machine-beans.md` 四 Phase（规则 14 合并：同 finance treasury 票据子系统/同 owner doc 义务/同 status 单轴结果表面）。落地 2 轴实体级 Bean（契约 `docs/architecture/entity-state-machine-bean.md`）+ 11 守卫接线 + 层 1 矩阵 + 层 2 四方对照 + 层 3 回归 + owner doc 补章节。**治理**：receive/discount/endorse/honor/issue 触发业财过账（`NotesPostingDispatcher`→`FinPostingExecutor` 凭证 + `ErpFinArApItem` 辅助账）+ Payable 独有授信占用/释放（`IErpFinCreditFacilityBiz`）属会计保护域，按 §11.2 M4 plan-first（硬约束 (i)-(v)，人工门控 2026-08-14 已确认解除并转 active）。
- **Phase 1（Receivable Bean + 注册 + 层 1 矩阵）**：`ErpFinNotesReceivableStateMachine`（严格无状态，7 命名动作：receive {null,RECEIVED}→RECEIVED（initial 写入 §9.2 c + 幂等）/discount {RECEIVED}/endorse {RECEIVED}/collect {RECEIVED,DISCOUNTED}/honor·dishonor {COLLECTION_PENDING}/writeOff {非终态}；initial={RECEIVED}、terminal={HONORED,DISHONORED,WRITE_OFF}；`transitions()` 7 命名边（每动作一条代表边，多源完整集合在 assertCan*）；**ENDORSED 非终态仅 writeOff 出边**）。`app-service.beans.xml` FQN-id 注册。层 1 矩阵 `TestErpFinNotesReceivableStateMachineMatrix` **15/15 绿**。**Decision（前置）**：collect/dishonor writer 放置不对称 = `intentional legacy layout`（迁移仅改守卫委托，不改 writer 物理位置）。
- **Phase 2（Receivable 接线 + 层 3 回归，行为保持）**：facade 注入 Bean，5 守卫 wrapper（discount/endorse/honorOrDishonor→assertCanHonor/receive/writeOff）改 Bean 委托 + do* 目标态写回（`<action>TargetStatus()`）；receive 守卫**有意收窄**（loose validateNotTerminal→assertCanReceive null/initial，实仓零路径从非 initial 态 receive）；collect/dishonor per-mutation Processor 注入 Bean 直接委托（writer 物理位置不动）；common→`ERR_NOTES_RECEIVABLE_ILLEGAL_STATUS_TRANSITION` 映射（common 作 cause）+ 3 参数对外不变（expectedStatus 文案「RECEIVED 或 DISCOUNTED」「非终态」保留）；**完整保留** tryPostReceivable 过账/reverseReceivable 红冲/FX 贴现派生/markPosted/乐观锁。层 3 全量 **405/405 绿**。
- **Phase 3（Payable Bean + 接线 + 层 1/3）**：`ErpFinNotesPayableStateMachine`（4 命名动作：issue {null,ISSUED}→ISSUED（initial 写入 + 幂等）/honor·dishonor {ISSUED}/writeOff {非终态}；initial={ISSUED}、terminal={HONORED,DISHONORED,WRITE_OFF}；`transitions()` 4 命名边）+ 注册 + facade 4 守卫 wrapper（issue/honor/dishonor/writeOff，validateNotTerminal 移除）改 Bean 委托 + do* 目标态写回；Payable writer 全部在 facade do*（无 per-mutation 不对称）；**完整保留** tryPostPayable 过账/reversePayable 红冲/`reserveCreditIfNeeded`·`releaseOccupiedCredit` 授信占用释放/markPosted/乐观锁。层 1 `TestErpFinNotesPayableStateMachineMatrix` **11/11 绿**；层 3 全量 **416/416 绿**。
- **Phase 4（层 2 四方对照 + owner doc §对象三/四 + 漂移 Decision）**：两轴 × 10 维度四方对照无未裁决漂移——writer 全集 = 11 命名动作写点（Receivable 5 facade do* + collect/dishonor per-mutation；Payable 4 facade do*），grep 重核无第 12 writer；NotesPostingDispatcher 非状态 writer 排除；CRUD 框架入口按契约 §9.4。**Decision**：(a) collect/dishonor writer 不对称 = `intentional legacy layout`；(b) ENDORSED 非终态仅 writeOff = `intentional business behavior`；(c) COLLECTION_PENDING/DISHONORED 不过账 = 过账侧语义（非 Bean 范畴）；(d) M0.2 §3.5 M4.8/M4.9「既有测试：TestErpFinNotes*StateMachine」与实仓一致（无测试名漂移）。owner doc `docs/design/finance/state-machine.md` 适用对象增列票据两项 + 新增 **§对象三：应收票据状态机**（7 态 + 矩阵 + ENDORSED 边界 + 不过账边界 + 实现注记）与 **§对象四：应付票据状态机**（4 态 + 矩阵 + 授信占用/释放边界 + 实现注记），交叉引用 treasury.md（`:197-202` 关键业务规则）+ posting.md 票据业务类型。
- **验证基线（全绿）**：`mvn clean install -DskipTests`（全 reactor BUILD SUCCESS）+ `mvn test -pl module-finance/erp-fin-service`（**416/416**，含 NotesReceivable StateMachine 12 + Posting 6、NotesPayable StateMachine 6 + Posting 2、层 1 矩阵 26）+ `bash docs/audits/nop-compliance-checker.sh`（**R5=0/R11=0** 基线维持；R10=7 为既有漂移非本计划引入，实测 stash 基线同值）。路线图 M4.8 + M4.9 ready → done + 头部追加 2026-08-14 记录。
- **Deferred（已裁定带 successor）**：统一 collect/dishonor writer 物理位置至 facade（代码风格，非行为）；COLLECTION_PENDING/DISHONORED 不过账语义纳入 Bean（归过账侧）；通用 CRUD 写入禁止（M0.1 successor）/ Delta 覆盖实证（M5.3）。

### 库存域 ErpInvTransferOrder/OwnershipTransfer/CostAdjust/LandedCost docStatus 实体级状态机 Bean 落地（M4.31 + M4.32 + M4.33 + M4.34，plan `2026-08-13-0810-3`，§11.2 M4 plan-first）

- **工作项**：执行 `docs/plans/2026-08-13-0810-3-inventory-docstatus-m4-state-machine-bean.md` 三 Phase（规则 14 bundling：同 owner doc `inventory/state-machine.md` + 同 inventory docStatus 结果表面；4 实体矩阵不同故各为独立 Bean + 独立矩阵测试）。落地 4 轴实体级 Bean（契约 `docs/architecture/entity-state-machine-bean.md`）+ 4 实体 Processor 接线 + 层 1 矩阵 + 层 2 四方对照 + 层 3 回归 + owner doc 补 4 章节。**治理**：OwnershipTransfer done / CostAdjust applyCostAdjust / LandedCost approve 触发存货成本过账事件（`*PostingDispatcher`→`IErpFinVoucherBiz.post`）+ 余额/成本层强一致，属财务影响 + 库存强一致保护区，按 §11.2 M4 plan-first（硬约束 (i)-(v)，人工门控 2026-08-14 已确认解除并转 active）；TransferOrder 仅可选跨法人内部往来 GL hook（config-gated + 失败吞掉，较轻保护区如实登记）。
- **Phase 1（4 Bean + 注册 + 层 1 矩阵 + 双轴联动 Decision）**：`ErpInvTransferOrderStateMachine`（单边 confirm {DRAFT}→CONFIRMED；initial={DRAFT}/terminal={CONFIRMED}，无 DONE/CANCELLED writer——物理移动归独立 StockMove 流）/ `ErpInvOwnershipTransferStateMachine`（confirm/done/cancel 3 动作；**独立 dict `erp-inv/ownership-transfer-status` 语义，用 `OWNERSHIP_TRANSFER_STATUS_*` 常量非 `DOC_STATUS_*`**；initial={DRAFT}/terminal={DONE,CANCELLED}）/ `ErpInvCostAdjustStateMachine`（applyCostAdjust {DRAFT,CONFIRMED}→DONE + reverseCostAdjust {DONE}→CONFIRMED；approveStatus 轴不在 Bean）/ `ErpInvLandedCostStateMachine`（approve {DRAFT}→DONE + reverseApprove {DONE}→CANCELLED；`generateFreightLandedCost` 生成路径无边 §9.2）。4 Bean 严格无状态（零 @Inject），`app-service.beans.xml` FQN-id 注册（追加 4 bean）。**Decision（前置，双轴联动）**：CostAdjust/LandedCost docStatus-driving action 同时写 approveStatus——Bean 仅集中 docStatus 边 + assertCan*，approveStatus 写 + gating 保留 Processor（option a，同 StockMove Non-Goal 先例）。层 1 矩阵 `TestErpInvTransferOwnershipCostAdjustLandedCostStateMachines`（4 Bean × 全边 + 可达性 + 元数据一致性 + dict 值可达）**34/34 绿**。
- **Phase 2（4 实体接线 + 层 3 回归，行为保持）**：TransferOrder `validateDraft` 改 Bean 委托 + common→**既有（错误）码 `ERR_INV_STOCK_TAKE_ILLEGAL_TRANSITION`+`ARG_TAKE_ID` 映射**（copy-paste 缺陷行为保持不修正，路线图 Non-Goal）+ intercompany hook 完整保留；OwnershipTransfer `assertStatus`/cancel 改 Bean 委托（common→`ERR_OWNERSHIP_TRANSFER_ILLEGAL_STATUS`）+ **完整保留** `ERR_OWNERSHIP_TRACKING_DISABLED` 动态守卫 + `reclassifyBalances` 数量守恒 + `dispatchIfApplicable`（VMI_CONSUME + config 门，失败保持 DONE+posted=false）；CostAdjust apply/reverse 注入 Bean（守卫置于 posted 门之后——保持既有 `ALREADY_APPLIED`/`NOT_APPLIED` 行为；common→既有 generic `ERR_ILLEGAL_STATUS_TRANSITION` 映射）+ 完整保留成本层更新/红字凭证/审批门/validateNotCancelled；LandedCost approve/reverseApprove 注入 Bean（守卫置于幂等/posted 门之后——保持既有 `ALREADY_APPROVED`/`NOT_POSTED` 行为）+ **完整保留** 悲观锁/分配引擎/子 CostAdjust 联动/双轴写/红冲失败吞掉+告警（G4）。测试 fixture 适配：`TestErpInvLandedCostReverseFailureAlert` 手工装配 Processor 补 `stateMachine` 依赖。层 3 `mvn test -pl module-inventory/erp-inv-service` **208/208 全绿**（含 OwnershipTransfer VMI 重分类+凭证+posted / CostAdjust 8 类 / LandedCost E2E+Reversal+ReverseFailureAlert+ReceiveMutex 并发 / PostingDispatcherFailureHangs 3 实体失败不悬挂 / Standard+WeightedAverageCosting），零行为回归。
- **Phase 3（层 2 四方对照 + owner doc 4 章节 + 漂移/缺陷 Decision）**：4 轴 × 10 维度四方对照（dict↔owner-doc↔Bean↔writer）无未裁决漂移——dict 绑定实测（move-status `:37` ↔ TransferOrder `:608`/CostAdjust `:1233`/LandedCost `:1330`；独立 ownership-transfer-status `:98-103` ↔ OwnershipTransfer `:1014`）；net-0 边缘核实（`tryPost:48-50` 净 0 返回 null → DONE+posted=false，grep 全部既有测试无 DONE 重 apply 路径 → Bean DONE 源态拒绝 = 合理收紧不违反 Non-Goal）；死状态核对无未登记项。**Decision**：(a) **TransferOrder 错误码缺陷 = confirmed live defect**（confirm 守卫抛 StockTake 的 `ERR_INV_STOCK_TAKE_ILLEGAL_TRANSITION`+`ARG_TAKE_ID`，copy-paste bug）——行为保持映射既有（错误）码 + successor Fix 引入 `ERR_INV_TRANSFER_ORDER_ILLEGAL_TRANSITION`；(b) CostAdjust/LandedCost docStatus 无专属 illegal-transition 码 = intentional legacy（Bean common→既有 `ERR_ILLEGAL_STATUS_TRANSITION` 映射）；(c) LandedCost reverse 失败吞掉+告警（G4）= intentional legacy 保留；(d) 跨实体子 CostAdjust 写（LandedCost facade）= 内部编排，Bean 不发明边。owner doc `docs/design/inventory/state-machine.md` 补 4 章节（调拨单/所有权转移单/成本调整单/到岸成本单状态机（独立））+ 适用对象更新。
- **验证基线（全绿）**：`mvn clean install -DskipTests`（全 reactor BUILD SUCCESS）+ `mvn test -pl module-inventory/erp-inv-service`（208/208；关键回归子集 81/81 复跑）+ `bash docs/audits/nop-compliance-checker.sh`（**R5=0/R11=0** 基线维持，19 规则与 §BASELINE 块逐行一致 0 漂移）。路线图 M4.31 + M4.32 + M4.33 + M4.34 ready → done。计划 `Plan Status: active → completed`（独立结束审计 = successor，Closure Audit Evidence 由独立子代理填写）。
- **Deferred（已裁定带 successor）**：TransferOrder 错误码缺陷修正（引入 `ERR_INV_TRANSFER_ORDER_ILLEGAL_TRANSITION`，独立 Fix plan + owner-doc 确认）；TransferOrder DONE/CANCELLED 生命周期 + approveStatus 接入（PM 要求时）；inventory approveStatus 轴（CostAdjust/LandedCost，纳入时另起 plan）；过账编排/posted 契约（watch-only）；通用 CRUD 写入禁止（M0.1 successor）/ Delta 覆盖实证（M5.3）。

### 采购域 ErpPurReceive/Invoice/Payment/Return docStatus 实体级状态机 Bean 落地（M4.13 + M4.15 + M4.17 + M4.19，plan `2026-08-13-0810-1`，§11.2 M4 plan-first）

- **工作项**：执行 `docs/plans/2026-08-13-0810-1-purchase-docstatus-m4-state-machine-bean.md` 三 Phase（规则 14 bundling：同 owner doc `purchase/state-machine.md` + 同 `erp/doc-status` dict + 同 cancel: DRAFT→CANCELLED 契约 + 同结果表面=采购单据 docStatus 生命周期）。落地 4 轴实体级 Bean（契约 `docs/architecture/entity-state-machine-bean.md`）+ 两种 cancel 编排形态接线 + 层 1 矩阵 + 层 2 四方对照 + 层 3 回归。**治理**：cancel approved+posted 逆转存货移动（Receive/Return→`IErpInvStockMoveBiz`）与红字凭证（Invoice/Payment→`PurInvoice/PurPaymentPostingDispatcher.reverse`→`IErpFinVoucherBiz.reverse`），属财务影响保护区，按 §11.2 M4 plan-first（硬约束 (i)-(v)，人工门控 2026-08-14 已确认解除并转 active）。
- **Phase 1（4 Bean + 注册 + 层 1 矩阵）**：`ErpPurReceiveDocumentStateMachine` / `ErpPurInvoiceDocumentStateMachine` / `ErpPurPaymentDocumentStateMachine` / `ErpPurReturnDocumentStateMachine`（均 docStatus 单轴，单边 cancel(DRAFT→CANCELLED)；initial={DRAFT}/terminal={CANCELLED}/transitions=1 边；**ACTIVE 死状态不编码入边**，Decision 记录 = `intentional legacy dead state`，同 Order/Quotation M2）。4 Bean 严格无状态（仅 import 常量/错误码/NopException/java.util），`app-service.beans.xml` FQN-id 注册（追加 4 bean，Document SM 段）。层 1 矩阵 `TestErpPurReceiveInvoicePaymentReturnDocumentStateMachines`（表驱动参数化 4 Bean × 6 维度）**24/24 绿**。
- **Phase 2（两种 cancel 编排形态接线 + Payment 缺口补全 + 层 3 回归，行为保持）**：**形态一（Receive/Return，CancelProcessor 接线，同 Order 先例）**——`ErpPurReceiveCancelProcessor`/`ErpPurReturnCancelProcessor` 注入各自 Bean（非 private，R5），新增 `validateTransitionForCancel` override → `stateMachine.assertCanCancel(from)`（try/catch common 码→域码映射：Receive→泛型 `ERR_ILLEGAL_DOC_STATUS_TRANSITION` / Return→`ERR_RETURN_...`，common 作 cause，**对外错误码+参数不变，不新增/不重命名**）；`cancelledDocStatus()` 改回 `stateMachine.cancelTargetStatus()`；inline `setDocStatus(entity, cancelledDocStatus())`。**形态二（Invoice/Payment，主 Processor 接线）**——`ErpPurInvoiceProcessor`/`ErpPurPaymentProcessor` 注入 `documentStateMachine`（与既有 approval `stateMachine` 并存不冲突），`validateTransitionForCancel` 改 Bean 委托（common→`ERR_INVOICE_...`/`ERR_PAYMENT_...` 映射），`doCancel` 目标态写回 `documentStateMachine.cancelTargetStatus()`（经 CancelProcessor 编排链调用）。**完整保留**：Receive approved→`ensureReversed`（stock move reverse）+ Return approved→`ensureReversed`（posting reverse + stock move reverse）+ Invoice/Payment approved+posted→`postingDispatcher.reverse` + commitment-restore hook（`runCommitmentRestoreOnInvoice/ReturnReverseHook`）+ Payment workflow（`nopFlowId`）+ 乐观锁。**PurReversalListener 4 rollback* 均不触 docStatus**（仅回写 posted=false + APPROVED→REJECTED，grep 证实 0 处 setDocStatus）。**Payment cancel 缺口补全**：`TestErpPurPaymentApproval` 增 `testCancelFromDraft`（对齐 Receive/Invoice/Return 形态）。层 3 `mvn test -pl module-purchase/erp-pur-service` **294/294 全绿**（含 4 实体 cancel + reversal-listener-rollback 2 + commitment-restore 9+7 + workflow-approval 3 + finance-reversal-writeback 2），零行为回归。
- **Phase 3（层 2 四方对照 + 漂移 Decision + owner doc 补注）**：4 轴 × 10 维度四方对照（dict↔owner-doc↔Bean↔writer）无未裁决漂移——DRAFT（初始+创建写入）/ CANCELLED（cancel 唯一生产 writer，经 Bean `cancelTargetStatus()`）/ ACTIVE（**零生产 writer**）；reversal-listener 不触 docStatus；两种编排形态接线正确（Receive/Return 接 CancelProcessor vs Invoice/Payment 接主 Processor）；过账路径（Receive→`IErpInvStockMoveBiz` 无 dispatcher / Invoice→AP_INVOICE / Payment→PAYMENT / Return→PURCHASE_RETURN）完整保留。**Decision**：(a) ACTIVE=`intentional legacy dead state`（dict 值保留不改绑，Bean 不编码入边）；(b) Receive 泛型错误码 `ERR_ILLEGAL_DOC_STATUS_TRANSITION`（无 RECEIVE_ 前缀，msg 硬编码「入库单」）vs Invoice/Payment/Return entity-scoped = `naming drift successor`（保持既有码，同 M3.15 JobCard 先例）；(c) 死 doCancel 辅助 + facade 守卫残留（Receive/Return 主 Processor）= `intentional legacy` 保持原状不双写。owner doc `docs/design/purchase/state-machine.md` §三轴状态分离后补「docStatus 轴 ACTIVE 死状态注记」。
- **验证基线（全绿）**：`mvn clean install -DskipTests`（全 reactor BUILD SUCCESS）+ `mvn test -pl module-purchase/erp-pur-service`（294/294）+ `bash docs/audits/nop-compliance-checker.sh`（**R5=0/R11=0** 基线维持，19 规则与 §BASELINE 块逐行一致 0 漂移）。**独立结束审计 APPROVE**（子代理新会话 `ses_002714aaeffeCq073PlQ5opmst`：A-F 技术核查全 PASS，0 BLOCKER / 0 MAJOR / 3 MINOR 闭包簿记已在本计划 Closure 完成）。路线图 M4.13 + M4.15 + M4.17 + M4.19 ready → done。
- **Deferred（已裁定带 successor）**：Receive 错误码命名对齐泛型→entity-scoped（PM/owner 要求时开 Fix plan）；purchase approveStatus 轴（M4.14/16/18/20 已 done，另计划）；通用 CRUD 写入禁止（M0.1 successor）；Delta 覆盖实证（M4 保护域归 M5.3）。


### M4 大批次人工门控确认（39 工作项 / 13 计划 `draft → active`）

- **事件**：人工/owner 批准全部待 M4 门控的 13 个计划，§11.2 M4 (i) plan-first 人工/owner-doc 门控于 2026-08-14 经人工确认解除。
- **13 计划（全部转 `active`，Draft Review Record 追加 confirmed 记录 + Review Hold 更新）**：`2026-08-13-0810-1`（purchase docStatus M4.13/15/17/19）+ `2026-08-13-0810-3`（inventory docStatus M4.31-34）+ `2026-08-13-1146-1`（notes receivable/payable M4.8/9）+ `2026-08-13-1146-2`（expense-claim 双轴 M4.4/5）+ `2026-08-13-1146-3`（employee-advance 双轴 M4.6/7）+ `2026-08-14-0456-1`（reconciliation+baddebt M4.3/10）+ `2026-08-14-0456-2`（hr-salary 双轴 M4.63/64）+ `2026-08-14-0456-3`（logistics shipment M4.57）+ `2026-08-14-0930-1`（manufacturing M4.35-39）+ `2026-08-14-1931-1`（assets core-lifecycle M4.40/41/52/53）+ `2026-08-14-1931-2`（assets value-change M4.42-47）+ `2026-08-14-1931-3`（assets structural M4.48-51）+ `2026-08-14-2000-1`（contract rebate-settlement M4.65）。
- **门控确认范围**：各计划以行为保持的矩阵集中化方式迁移、过账/红冲/reversal-listener/授信/ArApItem/stock move 等受保护路径完整保留（详见各计划 Review Hold + Draft Review Record confirmed 记录）。
- **roadmap 同步**：`docs/backlog/entity-state-machine-migration-roadmap.md` 39 工作项 Status `todo → ready`（规则 1）+ 头部追加 2026-08-14 人工门控确认批次记录；M4 表内已无 `todo` 项。
- **下一步**：各计划进入实施（Phase 1 Bean 落地），按各计划 Closure Gates 执行。

### Sales 域 ErpSalDelivery/Invoice/Receipt/Return docStatus 实体级状态机 Bean 落地（M4.21 + M4.23 + M4.25 + M4.27，plan `2026-08-13-0810-2`，§11.2 M4 plan-first）

- **工作项**：执行 `docs/plans/2026-08-13-0810-2-sales-docstatus-m4-state-machine-bean.md` 三 Phase（规则 14 bundling：同 owner doc `sales/state-machine.md` + 同 `erp/doc-status` dict + 同 cancel: DRAFT→CANCELLED 契约 + 同结果表面=销售单据 docStatus 生命周期）。落地 4 轴实体级 Bean（契约 `docs/architecture/entity-state-machine-bean.md`）+ 4 CancelProcessor 接线 + 层 1 矩阵 + 层 2 四方对照 + 层 3 回归。**治理**：cancel approved+posted 逆转存货移动（Delivery/Return→`IErpInvStockMoveBiz`）与红字凭证（Invoice/Receipt/Return→`*PostingDispatcher.reverse`），属财务影响保护区，按 §11.2 M4 plan-first（硬约束 (i)-(v)，门控经 owner-doc 证据齐备 + 同域姊妹计划 `2026-08-13-1950-2` approveStatus completed 先例解除）。
- **Phase 1（4 Bean + 注册 + 层 1 矩阵）**：`ErpSalDeliveryDocumentStateMachine` / `ErpSalInvoiceDocumentStateMachine` / `ErpSalReceiptDocumentStateMachine` / `ErpSalReturnDocumentStateMachine`（均 docStatus 单轴，单边 cancel(DRAFT→CANCELLED)；initial={DRAFT}/terminal={CANCELLED}/transitions=1 边；**ACTIVE 死状态不编码入边**）。4 Bean 严格无状态（零 @Inject），`app-service.beans.xml` FQN-id 注册（追加 4 bean，Document SM 段）。层 1 矩阵 `TestErpSalDeliveryInvoiceReceiptReturnDocumentStateMachines`（表驱动参数化 4 Bean × 6 维度）**24/24 绿**。
- **Phase 2（4 CancelProcessor 接线 + 层 3 回归，行为保持）**：4 CancelProcessor 注入各自 Bean（非 private，R5），新增 `validateTransitionForCancel` override → `stateMachine.assertCanCancel(from)`（try/catch common 码→域码映射：Delivery→`ERR_ILLEGAL_DOC_STATUS_TRANSITION` 泛型 / Invoice→`ERR_INVOICE_...` / Receipt→`ERR_RECEIPT_...` / Return→`ERR_RETURN_...`，common 作 cause，**对外错误码+参数不变，不新增/不重命名**）；`cancelledDocStatus()` 改回 `stateMachine.cancelTargetStatus()`。**完整保留**：Delivery/Return approved→`ensureReversed`（stock move reverse）+ Invoice/Receipt approved+posted→`postingDispatcher.reverse` + Invoice commitment-release hook + Receipt workflow/settle（writtenOffStatus 轴不受影响）+ 乐观锁。**SalReversalListener 4 业务类型均不触 docStatus**（仅回写 posted + approveStatus，Delivery SALES_OUTPUT 仅 posted）。层 3 `mvn test -pl module-sales/erp-sal-service` **289/289 全绿**（含 Delivery/Invoice/Receipt/Return cancel + posting/reversal/stock-move/credit-hold + order-to-cash/order-to-delivery + reversal-listener-rollback + posting-failure-hangs），零行为回归。
- **Phase 3（层 2 四方对照 + 漂移 Decision + owner doc 补注）**：4 轴 × 10 维度四方对照（dict↔owner-doc↔Bean↔writer）无未裁决漂移。**Decision**：(a) ACTIVE=`intentional legacy dead state`（dict 存在零生产 writer，仅 Dashboard 读过滤，Bean 不编码入边）；(b) Delivery 泛型错误码 `ERR_ILLEGAL_DOC_STATUS_TRANSITION`（无 DELIVERY_ 前缀）vs Invoice/Receipt/Return entity-scoped = `naming drift successor`（保持既有码，同 purchase Receive 先例）；(c) facade Processor 死 `doCancel`/`validateTransitionForCancel`（CancelProcessor 整体覆写不调用）= `intentional legacy` 保持原状。owner doc `docs/design/sales/state-machine.md` §三轴状态分离后补「docStatus ACTIVE 死状态注记」。
- **验证基线（全绿）**：`mvn clean install -DskipTests`（全 reactor BUILD SUCCESS）+ `mvn test -pl module-sales/erp-sal-service`（289/289）+ `bash docs/audits/nop-compliance-checker.sh`（**R5=0/R11=0** 基线维持；R12c 40 vs 38 为并行计划 finance 变更引入，非本计划）。路线图 M4.21 + M4.23 + M4.25 + M4.27 todo → done。
- **Deferred（已裁定带 successor）**：Delivery 错误码命名对齐泛型→entity-scoped（PM/owner 要求时开 Fix plan）；通用 CRUD 写入禁止（M0.1 successor）；Delta 覆盖实证（M4 保护域归 M5.3）。

### 质量域 M4.58-M4.62 实体级状态机 Bean 落地（plan `2026-08-14-0930-2`，§11.2 M4 plan-first）

- **工作项**：执行 `docs/plans/2026-08-14-0930-2-quality-m4-state-machine-beans.md` 四 Phase。落地 5 轴实体级 Bean（契约 `docs/architecture/entity-state-machine-bean.md`）+ 三路径接线 + 层 1 矩阵 + 层 2 四方对照 + 层 3 回归。
- **5 Bean**（`app.erp.qa.service.statemachine`，严格无状态）：`ErpQaInspectionResultStateMachine`（result 轴 3 动作，recordResult 数据驱动三分支）、`ErpQaInspectionApprovalStateMachine`（approveStatus 让步审批单边）、`ErpQaNonConformanceStateMachine`（status 单轴 6 动作，postNcr/reverseNcr 自环）、`ErpQaRecallStateMachine`（status 操作轴，含 approve/reject 联动边）、`ErpQaRecallApprovalStateMachine`（approveStatus 审批轴 5 动作，reverseApprove=REJECTED）。
- **三路径接线**：abstract（Inspection/NCR/Recall 操作轴）+ orchestrator-facade（Recall 审批轴）+ BizModel-inline（NCR submitReview/cancel、Recall cancel）。固定来源态/目标态判断委托 Bean，动态守卫（CAPA gate、NcrPostingDispatcher、notify gate、NCR auto-create、target locator）保留原位。
- **关键 Decision**：M4.58 docStatus 零-writer 占位轴裁定为 deferred-but-adjudicated（dict-only 排除迁移）；M4.59 approveStatus 仅 concession-approve 单边矩阵（非完整 5 动作）。
- **验证全绿**：`mvn clean install -DskipTests` BUILD SUCCESS + `mvn test -pl module-quality/erp-qa-service -am` **168 tests green**（含 5 矩阵测试 46 tests）+ compliance 全 19 规则 actual = baseline（R5=0、R11=0）。roadmap M4.58-62 → done。

### Finance 域 ErpFinAccountingPeriod.status 实体级状态机 Bean 落地（M4.2，plan `2026-08-13-2045-1`，§11.2 M4 plan-first）

- **工作项**：执行 `docs/plans/2026-08-13-2045-1-erpfin-period-state-machine-bean.md` 三 Phase。落地 1 轴实体级 Bean（契约 `docs/architecture/entity-state-machine-bean.md`）+ 4 per-mutation Processor 接线 + 层 1 矩阵 + 层 2 四方对照 + 层 3 回归。**治理**：会计期间结账（OPEN→CLOSING→CLOSED）/ 最终锁定（CLOSED→CLOSED_FINAL）/ 反结账（CLOSED_FINAL→OPEN，触发期末凭证红冲）属受保护会计行为，按 §11.2 M4 plan-first（硬约束 (i)-(v)，人工门控 2026-08-13 已解除）。消费 M0.1 契约 + M1.3 模板 + M0.2 清单 §3.5 + 同域 Voucher Bean 范式。
- **Phase 1（Bean + 注册 + 层 1 矩阵）**：`ErpFinAccountingPeriodStateMachine`（status 单轴，5 态 4 命名动作：openPeriod NEVER_OPENED→OPEN / close OPEN→CLOSING→CLOSED 两段 / finalize CLOSED→CLOSED_FINAL / reverseClose CLOSED_FINAL→OPEN；initial={NEVER_OPENED}/terminal={CLOSED_FINAL}；**CLOSING 瞬态裁定**=事务内 @BizMutation 回滚语义，Bean 不发明 CLOSING→OPEN 独立边，transitions 编码 close 两段；终态 CLOSED_FINAL 唯一出边=reverseClose 恢复）。严格无状态（零 @Inject），`app-service.beans.xml` FQN-id 注册。层 1 矩阵 `TestErpFinAccountingPeriodStateMachineMatrix` **10/10 绿**。
- **Phase 2（4 Processor 接线 + 层 3 回归，行为保持）**：4 per-mutation Processor（Open/Close/Finalize/ReverseClose）注入 Bean（非 private，R5），`facade.assertPeriodStatus` 删除（grep 0 引用）→ `stateMachine.assertCan<Action>` + `<action>TargetStatus`；facade 新增 `mapIllegalTransition`（common 码→领域 `ERR_PERIOD_ILLEGAL_TRANSITION` + ARG_PERIOD_CODE/CURRENT/EXPECTED，common 作 cause，**对外错误码+参数不变**）。close 两段写经 `closeEnteringTargetStatus`(CLOSING)+`closeTargetStatus`(CLOSED)。**完整保留**：closePeriod 编排（preCheck/AR/AP/INV/AST/GL/closeAnnual 步骤+reclosePeriodCosts）、reverseClose kill-switch（`erp-fin.reverse-close-approval-required`+`ERR_REVERSE_CLOSE_APPROVAL_REQUIRED`）+ 次年门控 + reverseCloseVoucher PL/FX/ANNUAL 红冲 + reverseDepreciation + reopenModules、乐观锁。**generateNextYearPeriods §9.2 初始写入不调 assertCan\***（未改）。层 3 `mvn test -pl module-finance/erp-fin-service` **390/390 全绿**（重点 TestErpFinPeriodStateMachine/PeriodCloseEndToEnd/ReverseClose/PeriodPreCheck/AnnualClose/VoucherPeriodLock），零行为回归。
- **Phase 3（层 2 四方对照 + 漂移 Decision + owner doc 补注）**：5 态四方对照（dict↔owner-doc↔Bean↔writer）无未裁决漂移、无死状态（5 态全可达）。**Decision**：(a) CLOSING 瞬态=`intentional transactional behavior`（事务回滚语义，非 drift）；(b) 模块结账子状态 ar/ap/inv/gl/assetStatus=FIN-3 排除-技术（不同字段不入本轴）；(c) M0.2 §3.1 #6 `TestErpFinPeriodStateMachine` 实仓存在=有效层 3 基线，**无测试名漂移**。owner doc `docs/design/finance/state-machine.md §对象二 §3` 补「CLOSING 瞬态实现注记」+ 反结账恢复 bullet 增 kill-switch 交叉引用 §6 P1-MA3-036。
- **验证基线（全绿）**：`mvn clean install -DskipTests`（全 reactor BUILD SUCCESS）+ `mvn test -pl module-finance/erp-fin-service`（390/390）+ `bash docs/audits/nop-compliance-checker.sh`（**R5=0/R11=0** 基线维持；R12c +2 为既有漂移非本计划引入）。**独立结束审计 PASS**（`ses_0037a262fffe7X0XC0KCBIwk9l`，7 检查全绿，21 具名测试 0 失败，无 blocker）。路线图 M4.2 ready → done。
- **Deferred（已裁定带 successor）**：凭证-期间耦合 `assertPeriodNotLocked`（M4.1 路径，已 done）；模块结账子状态独立状态机化（FIN-3 排除，产品需求时）；反结账完整审批流（owner doc §6 P1-MA3-036 successor，P1-MA2-020 触发）；通用 CRUD 写锁（M0.1 successor）；Delta 覆盖实证（M4 保护域归 M5.3）。

### Finance 域 ErpFinBudgetScenario 双轴（docStatus + approveStatus）实体级状态机 Bean 落地（M4.11 + M4.12，plan `2026-08-13-1950-3`，§11.2 M4 plan-first）

- **工作项**：执行 `docs/plans/2026-08-13-1950-3-erpfin-budget-scenario-state-machine-beans.md` 两 Phase（规则 14 bundling：同一实体 ErpFinBudgetScenario 双轴 + 同一 facade `ErpFinBudgetScenarioProcessor` + 同一 owner doc `budget.md`）。落地 2 轴实体级 Bean + facade validateTransition 接线 + 层 1 矩阵 + 层 2 四方对照 + 层 3 回归。**治理**：approve 生成 BUDGET 影子凭证 + cancel 红冲全部 BUDGET 凭证，属财务影响保护区，按 §11.2 M4 plan-first（硬约束 (i)-(v)，人工门控 2026-08-13 已解除）。消费 M0.1 契约 + M1.3 模板 + M0.2 清单 §3.5 + Voucher Bean 范式。
- **Phase 1（2 Bean + 注册 + facade 接线 + 层 1 矩阵）**：`ErpFinBudgetScenarioDocumentStateMachine`（docStatus 主轴，5 源迁移动作：submit DRAFT/REJECTED→SUBMITTED / approve SUBMITTED→APPROVED / reject SUBMITTED→REJECTED / cancel APPROVED→CANCELLED / carryForward APPROVED→CLOSED；terminal {CANCELLED,CLOSED}；**rollForward = spawn-new-entity metadata-only**，不纳入 assertCan 矩阵，transitions() 标注 `isSpawn=true`）+ `ErpFinBudgetScenarioApprovalStateMachine`（approveStatus 镜像轴，3 动作 + resubmit 边，terminal {APPROVED}）。2 Bean 严格无状态（零 @Inject），`app-service.beans.xml` FQN-id 注册。facade `ErpFinBudgetScenarioProcessor.validateTransition` 按 target 路由到 docStatus Bean `assertCanSubmit/Approve/Reject/Cancel`（try/catch common 码→领域 `ERR_BUDGET_SCENARIO_ILLEGAL_TRANSITION` + ARG_SCENARIO_CODE/CURRENT/EXPECTED，common 作 cause，**对外错误码+参数不变**）。**carryForward/rollForward 绕过 facade validateTransition**（保留各自错误码 `ERR_BUDGET_SCENARIO_NOT_APPROVED`/`ERR_BUDGET_CARRY_FORWARD_RULE_INVALID`），Bean 不接管其守卫。`generateBudgetVoucher`/`reverseBudgetVoucher` 副作用保留原位。层 1 矩阵 `TestErpFinBudgetScenarioStateMachines` **21/21 绿**。
- **Phase 2（层 2 四方对照 + 层 3 回归）**：双轴四方对照（dict↔owner-doc↔Bean↔writer）。docStatus dict 6 值全部有 writer（无死状态）；approveStatus dict 4 值全部有 writer。**三方 drift 登记（successor）**：(1) `budget.md:42`+javadoc 写 REJECTED→DRAFT vs live code REJECTED→SUBMITTED（Bean 据实编码 REJECTED→SUBMITTED）；(2) `budget.md:38` 字段表遗漏 CLOSED；(3) `budget.md:39` 写 `erp-fin/approve-status` vs ORM/Bean 用 `wf/approve-status`；(4) rollForward spawn 未在 javadoc 标注。层 3 `mvn test -pl module-finance/erp-fin-service` **380/380 全绿**（含 TestErpFinBudgetEndToEnd submit→approve→cancel + TestErpFinBudgetCarryForward + TestErpFinBudgetRollForward），零行为回归。
- **验证基线（全绿）**：`mvn clean install -DskipTests`（全 reactor BUILD SUCCESS）+ `mvn test -pl module-finance/erp-fin-service`（380/380）+ `bash docs/audits/nop-compliance-checker.sh`（**R5=0/R11=0** 基线维持）。路线图 M4.11 + M4.12 ready → done。
- **Deferred（已裁定带 successor）**：owner doc/javadoc 4 项 drift 补正（规则 5 Fix 登记）；Delta 覆盖实证（M4 保护域归 M5.3）。

### Finance 域 ErpFinVoucher.docStatus 实体级状态机 Bean 落地（M4.1，plan `2026-08-13-2045-3`，§11.2 M4 plan-first）

- **工作项**：执行 `docs/plans/2026-08-13-2045-3-erpfin-voucher-state-machine-bean.md` 三 Phase。落地 1 轴实体级 Bean（契约 `docs/architecture/entity-state-machine-bean.md`）+ VoucherBizModel 3 方法接线 + 层 1 矩阵 + 层 2 四方对照 + 层 3 回归。**治理**：凭证 DRAFT→POSTED 过账核心 + reverseVoucher 红冲，全域 `IErpFinAcctDocProvider` 聚合入口，为 M4 最核心保护项，按 §11.2 M4 plan-first（硬约束 (i)-(v)，人工门控 2026-08-13 已解除）。消费 M0.1 契约 + M1.3 模板 + M0.2 清单 §3.5 + prj settlement Document Bean 范式。
- **Phase 1（Bean + 注册 + 层 1 矩阵）**：`ErpFinVoucherDocumentStateMachine`（docStatus 单轴，**唯一迁移边** postVoucher DRAFT→POSTED；分类 helper `isPosted`/`isTerminal`；transitions(1)/initial={DRAFT}/terminal={POSTED}；**CANCELLED 死状态不纳入任一集合**；**isReversed 不建模为 docStatus 边**——reverseVoucher 在 POSTED 上置 isReversed=true，POSTED 保留）。严格无状态（零 @Inject），`app-service.beans.xml` FQN-id 注册。层 1 矩阵 `TestErpFinVoucherDocumentStateMachineMatrix` **8/8 绿**（含 CANCELLED 死状态排除 + isPosted 分类 + 终态无出边断言）。
- **Phase 2（接线 + 层 3 回归，行为保持）**：`ErpFinVoucherBizModel` 注入 `ErpFinVoucherDocumentStateMachine`（非 private，R5）。postVoucher 内联 `docStatus==DRAFT` 守卫 → Bean `assertCanPost(from)`（try/catch common 码→领域 `ERR_FIN_VOUCHER_ILLEGAL_TRANSITION` + ARG_VOUCHER_ID/CURRENT_STATUS，common 作 cause，**对外错误码+参数不变**）+ 目标态经 `postVoucherTargetStatus()`；reverseVoucher/previewReverseVoucher 的 `docStatus==POSTED` 守卫 → Bean `isPosted` 分类 helper。**完整保留**：`assertPeriodNotLocked`（凭证-期间耦合动态守卫，属 M4.2）+ postedBy/postedAt + setIsReversed(true) 红冲单边标记 + 业财回链 + 乐观锁。**7 生成路径不接线 Bean**（§9.2 生成写入）。层 3 `mvn test -pl module-finance/erp-fin-service -o` **359/359 全绿**，零行为回归（重点 TestErpFinVoucherPeriodLock/ReversePreview/PostingExceptionWorkbench/PeriodCloseEndToEnd/AnnualClose）。
- **Phase 3（层 2 四方对照 + 漂移 Decision + owner doc 补注）**：独立 grep 重核 ErpFinVoucher.docStatus 生产 writer = **1 命名动作 + 7 生成路径，无第 8 writer**（PostingProcessor:812/CloseVoucherWriter:102/BudgetVoucherGenerator:137/CommitmentVoucherGenerator:149,217/IntercompanyVoucherGenerator:214,305/BudgetScenarioCarryForward:315/ConsolidationElimination:89）；排除 BankStatementImporter/BankReconciliationBuilder（作用于 ErpFinBankStatement/Reconciliation head）。**Decision**：(a) CANCELLED=intentional reserved 死状态（§5.1，零 writer，dict 保留不删）；(b) isReversed=boolean 红冲标记（非状态轴）；(c) 7 生成路径=§9.2 生成写入（如实反映）；(d) reversedVoucherId 双向回链=owner doc 已知简化 successor；(e) M0.2 §3.5「测试：无」与实仓具名层 3 测试漂移，登记建议 reconcile。owner doc `docs/design/finance/state-machine.md §对象一 §7.1` 补生成路径实现注记表 + isReversed 边界声明。
- **验证基线（全绿）**：`mvn clean install -DskipTests`（全 reactor BUILD SUCCESS）+ `mvn test -pl module-finance/erp-fin-service -o`（359/359）+ `bash docs/audits/nop-compliance-checker.sh`（**R5=0/R11=0** 基线维持）。路线图 M4.1 ready → done。
- **Deferred（已裁定带 successor）**：reversedVoucherId 双向回链（报表需求驱动）；DRAFT→CANCELLED 显式作废动作（PM 需求时）；7 生成路径统一经 Bean assertCanPost（归 M0.1 全局 CRUD 写锁）；凭证-期间耦合 assertPeriodNotLocked（M4.2 期间 Bean 落地后核对）；Delta 覆盖实证（M4 保护域归 M5.3）。

### Inventory 域 ErpInvStockMove + ErpInvStockTake 实体级状态机 Bean 落地（M4.29 + M4.30，plan `2026-08-13-2045-2`，§11.2 M4 plan-first）

- **工作项**：执行 `docs/plans/2026-08-13-2045-2-erpinv-stockmove-stocktake-state-machine-beans.md` 三 Phase（规则 14 bundling：同 owner doc `inventory/state-machine.md` + 同 `erp-inv/move-status` dict + 同「DONE 触发存货过账」契约）。落地 2 轴实体级 Bean（契约 `docs/architecture/entity-state-machine-bean.md`）+ StockMove 3 守卫接线 + StockTake 3 动作接线 + 层 1 矩阵 + 层 2 四方对照 + 层 3 回归。**治理**：StockMove DONE 触发 `InvPostingExecutor`→`IErpFinVoucherBiz.post` 存货过账事件 + 库存强一致保护区，按 §11.2 M4 plan-first（硬约束 (i)-(v)，人工门控 2026-08-13 已解除）。消费 M0.1 契约 + M1.3 模板 + M0.2 清单 §1.2/§3.5 + prj Bean 范式。
- **Phase 1（2 Bean + 注册 + 层 1 矩阵）**：`ErpInvStockMoveStateMachine`（docStatus 单轴，4 边：confirm DRAFT→CONFIRMED / complete CONFIRMED→DONE / cancel 双源 {DRAFT,CONFIRMED}→CANCELLED；terminal {DONE,CANCELLED}；**reversal/generateMove 无迁移边**=生成路径 §9.2）+ `ErpInvStockTakeStateMachine`（docStatus 单轴，复用 move-status dict，4 边：startTake/completeTake/cancel 双源；**COUNTING 标签漂移**：owner doc 标「盘点中 (COUNTING)」但 dict 无 COUNTING，目标态实际 CONFIRMED）。2 Bean 严格无状态（零 @Inject），`app-service.beans.xml` FQN-id 注册（追加 2 bean）。层 1 矩阵 `TestErpInvStockMoveAndStockTakeStateMachines` **20/20 绿**。
- **Phase 2（接线 + 层 3 回归，行为保持）**：StockMove facade `ErpInvStockMoveProcessor.doConfirm/doComplete` + `ErpInvStockMoveCancelProcessor.cancel` 注入 `ErpInvStockMoveStateMachine`（非 private，R5），固定来源态守卫改委托 `assertCan<Action>`（try/catch common 码→领域 `ERR_ILLEGAL_STATUS_TRANSITION` + ARG_MOVE_CODE/CURRENT/EXPECTED，common 作 cause，**不新增错误码**），目标态经 `<action>TargetStatus()`。StockTake `ErpInvStockTakeBizModel` 3 动作注入 `ErpInvStockTakeStateMachine`，common→`ERR_INV_STOCK_TAKE_ILLEGAL_TRANSITION` 映射。**完整保留**：validateAvailable + validateBatchExpiry（config-gated）+ applyReservation/releaseReservation + StockMoveBookkeeper 记账 + InvPostingDispatcher 过账派发（**过账失败保持 DONE + `posted=false`**，`testPostingFailureLeavesMoveDonePostedFalse` 回归绿）+ completeTake Deferred（不自动生成差异移动单）。**reverse/generateMove 不接线 Bean**（grep 证实零引用）。层 3 `mvn test -pl module-inventory/erp-inv-service` **174/174 全绿**，零行为回归。
- **Phase 3（层 2 四方对照 + 漂移 Decision + owner doc 补注）**：双轴 × 10 维度四方对照（dict↔owner-doc↔Bean↔writer）无未裁决漂移。**Decision**：(a) StockTake COUNTING 标签漂移 = `doc label drift`（行为一致，保留 CONFIRMED 不改 dict/绑）；(b) reversal 生成新单 + completeTake Deferred = 如实反映（非 implementation drift），Bean 不发明边；(c) M0.2 §3.5 inventory「既有测试：无」与实仓具名层 3 测试轻微漂移，登记建议 reconcile。owner doc `docs/design/inventory/state-machine.md §盘点单状态机（独立）` 补 COUNTING↔CONFIRMED 标签映射注记。
- **验证基线（全绿）**：`mvn clean install -DskipTests`（全 reactor BUILD SUCCESS）+ `mvn test -pl module-inventory/erp-inv-service`（174/174）+ `bash docs/audits/nop-compliance-checker.sh`（exit 0，**R5=0/R11=0** 基线维持）。路线图 M4.29 + M4.30 → done。
- **Deferred（已裁定带 successor）**：inventory 4 缺口轴 TransferOrder/OwnershipTransfer/CostAdjust/LandedCost（M4.31-34，owner-doc 缺口轴须 successor 补章节）；completeTake 自动生成差异移动单（owner doc §盘点 Deferred）；reversal 目标态 DONE→CANCELLED 显式作废（PM 需求时）；Delta 覆盖实证（M4 保护域归 M5.3）。
