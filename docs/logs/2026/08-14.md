# Development Log - 2026-08-14

### Finance 域 ErpFinBudgetScenario 双轴（docStatus + approveStatus）实体级状态机 Bean 落地（M4.11 + M4.12，plan `2026-08-13-1950-3`，§11.2 M4 plan-first）

- **工作项**：执行 `docs/plans/2026-08-13-1950-3-erpfin-budget-scenario-state-machine-beans.md` 两 Phase（规则 14 bundling：同一实体 ErpFinBudgetScenario 双轴 + 同一 facade `ErpFinBudgetScenarioProcessor` + 同一 owner doc `budget.md`）。落地 2 轴实体级 Bean + facade validateTransition 接线 + 层 1 矩阵 + 层 2 四方对照 + 层 3 回归。**治理**：approve 生成 BUDGET 影子凭证 + cancel 红冲全部 BUDGET 凭证，属财务影响保护区，按 §11.2 M4 plan-first（硬约束 (i)-(v)，人工门控 2026-08-13 已解除）。消费 M0.1 契约 + M1.3 模板 + M0.2 清单 §3.5 + Voucher Bean 范式。
- **Phase 1（2 Bean + 注册 + facade 接线 + 层 1 矩阵）**：`ErpFinBudgetScenarioDocumentStateMachine`（docStatus 主轴，5 源迁移动作：submit DRAFT/REJECTED→SUBMITTED / approve SUBMITTED→APPROVED / reject SUBMITTED→REJECTED / cancel APPROVED→CANCELLED / carryForward APPROVED→CLOSED；terminal {CANCELLED,CLOSED}；**rollForward = spawn-new-entity metadata-only**，不纳入 assertCan 矩阵，transitions() 标注 `isSpawn=true`）+ `ErpFinBudgetScenarioApprovalStateMachine`（approveStatus 镜像轴，3 动作 + resubmit 边，terminal {APPROVED}）。2 Bean 严格无状态（零 @Inject），`app-service.beans.xml` FQN-id 注册。facade `ErpFinBudgetScenarioProcessor.validateTransition` 按 target 路由到 docStatus Bean `assertCanSubmit/Approve/Reject/Cancel`（try/catch common 码→领域 `ERR_BUDGET_SCENARIO_ILLEGAL_TRANSITION` + ARG_SCENARIO_CODE/CURRENT/EXPECTED，common 作 cause，**对外错误码+参数不变**）。**carryForward/rollForward 绕过 facade validateTransition**（保留各自错误码 `ERR_BUDGET_SCENARIO_NOT_APPROVED`/`ERR_BUDGET_CARRY_FORWARD_RULE_INVALID`），Bean 不接管其守卫。`generateBudgetVoucher`/`reverseBudgetVoucher` 副作用保留原位。层 1 矩阵 `TestErpFinBudgetScenarioStateMachines` **21/21 绿**。
- **Phase 2（层 2 四方对照 + 层 3 回归）**：双轴四方对照（dict↔owner-doc↔Bean↔writer）。docStatus dict 6 值全部有 writer（无死状态）；approveStatus dict 4 值全部有 writer。**三方 drift 登记（successor）**：(1) `budget.md:42`+javadoc 写 REJECTED→DRAFT vs live code REJECTED→SUBMITTED（Bean 据实编码 REJECTED→SUBMITTED）；(2) `budget.md:38` 字段表遗漏 CLOSED；(3) `budget.md:39` 写 `erp-fin/approve-status` vs ORM/Bean 用 `wf/approve-status`；(4) rollForward spawn 未在 javadoc 标注。层 3 `mvn test -pl module-finance/erp-fin-service` **380/380 全绿**（含 TestErpFinBudgetEndToEnd submit→approve→cancel + TestErpFinBudgetCarryForward + TestErpFinBudgetRollForward），零行为回归。
- **验证基线（全绿）**：`mvn clean install -DskipTests`（全 reactor BUILD SUCCESS）+ `mvn test -pl module-finance/erp-fin-service`（380/380）+ `bash docs/audits/nop-compliance-checker.sh`（**R5=0/R11=0** 基线维持）。路线图 M4.11 + M4.12 ready → done。
- **Deferred（已裁定带 successor）**：owner doc/javadoc 4 项 drift 补正（规则 5 Fix 登记）；Delta 覆盖实证（M4 保护域归 M5.3）。

### Finance 域 ErpFinVoucher.docStatus 实体级状态机 Bean 落地（M4.1，plan `2026-08-13-2045-3`，§11.2 M4 plan-first）

- **工作项**：执行 `docs/plans/2026-08-13-2045-3-erpfin-voucher-state-machine-bean.md` 三 Phase。落地 1 轴实体级 Bean（契约 `docs/architecture/entity-state-machine-bean.md`）+ VoucherBizModel 3 方法接线 + 层 1 矩阵 + 层 2 四方对照 + 层 3 回归。**治理**：凭证 DRAFT→POSTED 过账核心 + reverseVoucher 红冲，全域 `IErpFinAcctDocProvider` 聚合入口，为 M4 最核心保护项，按 §11.2 M4 plan-first（硬约束 (i)-(v)，人工门控 2026-08-13 已解除）。消费 M0.1 契约 + M1.3 模板 + M0.2 清单 §3.5 + prj settlement Document Bean 范式。
- **Phase 1（Bean + 注册 + 层 1 矩阵）**：`ErpFinVoucherDocumentStateMachine`（docStatus 单轴，**唯一迁移边** postVoucher DRAFT→POSTED；分类 helper `isPosted`/`isTerminal`；transitions(1)/initial={DRAFT}/terminal={POSTED}；**CANCELLED 死状态不纳入任一集合**；**isReversed 不建模为 docStatus 边**——reverseVoucher 在 POSTED 上置 isReversed=true，POSTED 保留）。严格无状态（零 @Inject），`app-service.beans.xml` FQN-id 注册。层 1 矩阵 `TestErpFinVoucherDocumentStateMachineMatrix` **8/8 绿**（含 CANCELLED 死状态排除 + isPosted 分类 + 终态无出边断言）。
- **Phase 2（接线 + 层 3 回归，行为保持）**：`ErpFinVoucherBizModel` 注入 `ErpFinVoucherDocumentStateMachine`（非 private，R5）。postVoucher 内联 `docStatus==DRAFT` 守卫 → Bean `assertCanPost(from)`（try/catch common 码→领域 `ERR_FIN_VOUCHER_ILLEGAL_TRANSITION` + ARG_VOUCHER_ID/CURRENT_STATUS，common 作 cause，**对外错误码+参数不变**）+ 目标态经 `postVoucherTargetStatus()`；reverseVoucher/previewReverseVoucher 的 `docStatus==POSTED` 守卫 → Bean `isPosted` 分类 helper。**完整保留**：`assertPeriodNotLocked`（凭证-期间耦合动态守卫，属 M4.2）+ postedBy/postedAt + setIsReversed(true) 红冲单边标记 + 业财回链 + 乐观锁。**7 生成路径不接线 Bean**（§9.2 生成写入）。层 3 `mvn test -pl module-finance/erp-fin-service -o` **359/359 全绿**，零行为回归（重点 TestErpFinVoucherPeriodLock/ReversePreview/PostingExceptionWorkbench/PeriodCloseEndToEnd/AnnualClose）。
- **Phase 3（层 2 四方对照 + 漂移 Decision + owner doc 补注）**：独立 grep 重核 ErpFinVoucher.docStatus 生产 writer = **1 命名动作 + 7 生成路径，无第 8 writer**（PostingProcessor:812/CloseVoucherWriter:102/BudgetVoucherGenerator:137/CommitmentVoucherGenerator:149,217/IntercompanyVoucherGenerator:214,305/BudgetScenarioCarryForward:315/ConsolidationElimination:89）；排除 BankStatementImporter/BankReconciliationBuilder（作用于 ErpFinBankStatement/Reconciliation head）。**Decision**：(a) CANCELLED=intentional reserved 死状态（§5.1，零 writer，dict 保留不删）；(b) isReversed=boolean 红冲标记（非状态轴）；(c) 7 生成路径=§9.2 生成写入（如实反映）；(d) reversedVoucherId 双向回链=owner doc 已知简化 successor；(e) M0.2 §3.5「测试：无」与实仓具名层 3 测试漂移，登记建议 reconcile。owner doc `docs/design/finance/state-machine.md §对象一 §7.1` 补生成路径实现注记表 + isReversed 边界声明。
- **验证基线（全绿）**：`mvn clean install -DskipTests`（全 reactor BUILD SUCCESS）+ `mvn test -pl module-finance/erp-fin-service -o`（359/359）+ `bash docs/audits/nop-compliance-checker.sh`（**R5=0/R11=0** 基线维持）。路线图 M4.1 ready → done。
- **Deferred（已裁定带 successor）**：reversedVoucherId 双向回链（报表需求驱动）；DRAFT→CANCELLED 显式作废动作（PM 需求时）；7 生成路径统一经 Bean assertCanPost（归 M0.1 全局 CRUD 写锁）；凭证-期间耦合 assertPeriodNotLocked（M4.2 期间 Bean 落地后核对）；Delta 覆盖实证（M4 保护域归 M5.3）。

### Inventory 域 ErpInvStockMove + ErpInvStockTake 实体级状态机 Bean 落地（M4.29 + M4.30，plan `2026-08-13-2045-2`，§11.2 M4 plan-first）

- **工作项**：执行 `docs/plans/2026-08-13-2045-2-erpinv-stockmove-stocktake-state-machine-beans.md` 三 Phase（规则 14 bundling：同 owner doc `inventory/state-machine.md` + 同 `erp-inv/move-status` dict + 同「DONE 触发存货过账」契约）。落地 2 轴实体级 Bean（契约 `docs/architecture/entity-state-machine-bean.md`）+ StockMove 3 守卫接线 + StockTake 3 动作接线 + 层 1 矩阵 + 层 2 四方对照 + 层 3 回归。**治理**：StockMove DONE 触发 `InvPostingExecutor`→`IErpFinVoucherBiz.post` 存货过账事件 + 库存强一致保护区，按 §11.2 M4 plan-first（硬约束 (i)-(v)，人工门控 2026-08-13 已解除）。消费 M0.1 契约 + M1.3 模板 + M0.2 清单 §1.2/§3.5 + prj Bean 范式。
- **Phase 1（2 Bean + 注册 + 层 1 矩阵）**：`ErpInvStockMoveStateMachine`（docStatus 单轴，4 边：confirm DRAFT→CONFIRMED / complete CONFIRMED→DONE / cancel 双源 {DRAFT,CONFIRMED}→CANCELLED；terminal {DONE,CANCELLED}；**reversal/generateMove 无迁移边**=生成路径 §9.2）+ `ErpInvStockTakeStateMachine`（docStatus 单轴，复用 move-status dict，4 边：startTake/completeTake/cancel 双源；**COUNTING 标签漂移**：owner doc 标「盘点中 (COUNTING)」但 dict 无 COUNTING，目标态实际 CONFIRMED）。2 Bean 严格无状态（零 @Inject），`app-service.beans.xml` FQN-id 注册（追加 2 bean）。层 1 矩阵 `TestErpInvStockMoveAndStockTakeStateMachines` **20/20 绿**。
- **Phase 2（接线 + 层 3 回归，行为保持）**：StockMove facade `ErpInvStockMoveProcessor.doConfirm/doComplete` + `ErpInvStockMoveCancelProcessor.cancel` 注入 `ErpInvStockMoveStateMachine`（非 private，R5），固定来源态守卫改委托 `assertCan<Action>`（try/catch common 码→领域 `ERR_ILLEGAL_STATUS_TRANSITION` + ARG_MOVE_CODE/CURRENT/EXPECTED，common 作 cause，**不新增错误码**），目标态经 `<action>TargetStatus()`。StockTake `ErpInvStockTakeBizModel` 3 动作注入 `ErpInvStockTakeStateMachine`，common→`ERR_INV_STOCK_TAKE_ILLEGAL_TRANSITION` 映射。**完整保留**：validateAvailable + validateBatchExpiry（config-gated）+ applyReservation/releaseReservation + StockMoveBookkeeper 记账 + InvPostingDispatcher 过账派发（**过账失败保持 DONE + `posted=false`**，`testPostingFailureLeavesMoveDonePostedFalse` 回归绿）+ completeTake Deferred（不自动生成差异移动单）。**reverse/generateMove 不接线 Bean**（grep 证实零引用）。层 3 `mvn test -pl module-inventory/erp-inv-service` **174/174 全绿**，零行为回归。
- **Phase 3（层 2 四方对照 + 漂移 Decision + owner doc 补注）**：双轴 × 10 维度四方对照（dict↔owner-doc↔Bean↔writer）无未裁决漂移。**Decision**：(a) StockTake COUNTING 标签漂移 = `doc label drift`（行为一致，保留 CONFIRMED 不改 dict/绑）；(b) reversal 生成新单 + completeTake Deferred = 如实反映（非 implementation drift），Bean 不发明边；(c) M0.2 §3.5 inventory「既有测试：无」与实仓具名层 3 测试轻微漂移，登记建议 reconcile。owner doc `docs/design/inventory/state-machine.md §盘点单状态机（独立）` 补 COUNTING↔CONFIRMED 标签映射注记。
- **验证基线（全绿）**：`mvn clean install -DskipTests`（全 reactor BUILD SUCCESS）+ `mvn test -pl module-inventory/erp-inv-service`（174/174）+ `bash docs/audits/nop-compliance-checker.sh`（exit 0，**R5=0/R11=0** 基线维持）。路线图 M4.29 + M4.30 → done。
- **Deferred（已裁定带 successor）**：inventory 4 缺口轴 TransferOrder/OwnershipTransfer/CostAdjust/LandedCost（M4.31-34，owner-doc 缺口轴须 successor 补章节）；completeTake 自动生成差异移动单（owner doc §盘点 Deferred）；reversal 目标态 DONE→CANCELLED 显式作废（PM 需求时）；Delta 覆盖实证（M4 保护域归 M5.3）。
