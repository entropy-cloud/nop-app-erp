# Development Log - 2026-08-15

### cs 目录建单必填校验落地（RC-R1.28，plan `2026-08-15-0320-1`，第一批纯预授权）

- **工作项**：执行 `docs/plans/2026-08-15-0320-1-rc-mr1-r1-28-cs-catalog-form-required-validation.md` 四 Phase。落地 finding P1-RC-060（L1 UC-CS-10 异常①「表单必填项缺失 → 禁止提交」——requestFormConfig schema 未驱动校验，`buildTicketData` 盲拷 formData keys）。**治理**：纯 BizModel/Processor 代码逻辑（解析 JSON schema + 必填校验 + 错误码 + 测试），零 ORM 结构/会计过账/删除变更（requestFormConfig 列已存在）。
- **接线点裁决（Decision A）**：`validateRequiredFormFields` 独立 protected step，在 `requireCatalogItem`/`validateCatalogItemUsable` 之后、`buildTicketData` 之前——校验失败在副作用（权益扣减 `applyEntitlementToTicketData` + `ticketBiz.save`）之前拒绝，工单不落库 + 权益不扣减。
- **校验基准裁决（Decision B1 A）**：formData 原始键值（fallback 前）——schema 标必填而客户未提交 → 拒绝，subject/priority 缺省回退是服务端兜底不满足必填语义；`urgency→priority` 按原始键 `urgency` 校验。
- **容错边界裁决（Decision T1）**：requestFormConfig null/空白/非法 JSON/fields 非数组 → 跳过 + LOG.warn（配置数据质量问题不阻断建单，对齐 fulfillmentBiz try/catch WARN 降级范式）；`required` 非布尔 true → 该字段不视为必填；空串/空白值视为缺失。
- **落地**：`ErpCsErrors.java` 新增 `ERR_CATALOG_FORM_REQUIRED_MISSING`（`erp.err.cs.catalog-item.form-required-missing`，参数 catalogItemId + fieldKey + label 若可得，对齐 `catalog-item.*` 错误码族）；`ErpCsServiceCatalogItemCreateFromCatalogProcessor.java` 接线 protected step（`JsonTool.parseBeanFromText` 解析，复用 `CannedResponseRenderer:67` 先例；`StringHelper.isBlank` 判空白）。
- **测试**：`TestErpCsServiceCatalog` 新增 6 组全绿——① 必填缺失拒绝 + 工单零落库 + 权益零扣减（seed 活跃 PAY_PER_TICKET 权益，usedTickets 保持 0 证明未达扣减）② 必填满足放行（含 urgency→priority 映射）③ requestFormConfig null 跳过 ④ 非法 JSON 跳过 ⑤ 空白视为缺失拒绝 ⑥ 非必填字段缺失放行。
- **验证基线（全绿）**：`mvn install -DskipTests -pl module-cs/erp-cs-service` BUILD SUCCESS + `mvn test -pl module-cs/erp-cs-service`（122/122，TestErpCsServiceCatalog 15/15）+ `bash docs/audits/nop-compliance-checker.sh`（actual == baseline 零漂移：R1d=14/R2a=34/R2b=230/R2c=1393/R2d=34/R12 69/66/40）。
- **回填**：arm-index P1-RC-060 → `done (RC-R1.28)` + roadmap RC-R1.28 → done ✅ + owner doc `service-catalog.md §9.4` 补服务端必填校验实现注记（校验时机/基准/错误码/容错语义/数据治理声明/schema key 映射集约束/测试证据）。
- **Deferred（已裁定带 successor）**：生产/种子 requestFormConfig 数据治理（required schema 维护，重开事件 = 门户自助前端上线或目录数据治理批次启动，successor no）+ 前端动态表单渲染（门户自助前端 successor，重开事件 = 门户上线随数据治理批次执行）。
