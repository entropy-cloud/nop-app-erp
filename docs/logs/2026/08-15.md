### finance 银行对账对方账号维度落地（RC-R1.43，plan `2026-08-15-1838-2`，2026-08-12 裁决 A 类 ORM 批量授权）

- **工作项**：执行 `docs/plans/2026-08-15-1838-2-finance-bank-recon-counterparty-dimension.md` 三 Phase（Phase 1 裁决 + Phase 2 ORM/导入接线 + Phase 3 过滤/测试）。落地 finding **P1-RC-004**（L1 UC-FIN-09/14 断言②「自动勾对: 金额 + 反向方向 + valueDate±N天 + **对方账号** 模糊匹配」——`use-cases.md:172,279` 4 维度之一；L3 `BankLedgerQuery.findCandidates:39-84` 候选过滤仅 subjectId+dcDirection+amount+voucherId 窗口，**无对方账号过滤** + `ErpFinBankStatementLine` 无 counterparty 列；运行时影响：同额同日不同对方账号且账面仅 1 候选 → 错误 MATCHED，A4.1.11 评估维持 P1 中低触发率）。**治理**：2026-08-12 ORM 变更批量裁决 A 类（roadmap 头部 :33-34，批准人：用户）授权 `ErpFinBankStatementLine` 加 3 列（对齐 Q3 纯加性类自动执行范围，越界回落 ask-first）+ 独立 plan-audit 义务（本计划 Draft Review Record 记录独立草案审查 2 轮收敛 acceptable）。
- **C1/C2 裁决**（Phase 1 落盘于 plan）：**C1 候选侧对方账号载体 = 选项 A（选定）**——`ErpFinVoucherLine.partnerId → ErpMdPartner.name`（既有辅助核算维度，orm.xml:493 + :512 to-one；否决 B=VoucherLine 加列超 A 类授权越界、C=billRefCode 链路过长；残留风险：partner（ERP 往来单位）≠ 银行流水对方账号字面）；**C2 匹配语义 = 选项 A（选定）**——对账单行 counterpartyName 与候选侧 partner.name **非空精确匹配**（两侧均非空才比较，任一为空放行——保持既有行为兼容零回归；否决 B=LIKE 模糊[跨方言 + 误配面扩大，A4.1.11 评估表明精确匹配已消除核心错误 MATCHED 场景]、C=counterpartyAccount 精确[银行账号侧数据种子缺失]）。
- **落地**：`app-erp-finance.orm.xml` ErpFinBankStatementLine 增 3 可空列（`counterpartyAccount` propId=20 VARCHAR 50 / `counterpartyName` propId=21 VARCHAR 200 / `counterpartyBank` propId=22 VARCHAR 200，无 NOT NULL/无默认/无索引/无 UK）+ `BankStatementLineInput` DTO 3 可选字段 + `BankStatementImporter.importStatement` 行映射 3 字段写入（null 透传）+ `BankLedgerQuery.findCandidates` 签名增 `counterpartyName` 参数（occupied 排除后过滤：`StringHelper.isNotBlank` 时经 ORM to-one 关系 getter `line.getPartner().getName()` 解析候选侧户名比较，**零新增 daoFor 站点 → R2c=1399 不变**）+ `BankStatementMatcher.autoMatch` 传 `line.getCounterpartyName()`（null 兼容零回归）。生成链：`mvn clean install -DskipTests` BUILD SUCCESS + 生成产物核对（Entity `_ErpFinBankStatementLine.java` 3 getter/setter + XMeta propId 20/21/22 + DDL 三方言同步，propId 无重复无 ERR_ORM_MODEL_DUPLICATE_PROP_ID）。
- **测试**：新增 `TestErpFinBankStatementCounterpartyMatch` 4 组全绿（① 同额同日同科目不同对方账号 + 账面多候选 → 过滤后唯一 MATCHED 且 matchedLineId=partner 相符行[对照现状错误 MATCHED] ② 单候选户名不符排除 → UNMATCHED[错误 MATCHED 消除] ③ 对账单无 counterparty null → 过滤空放行多候选 SUSPENSE 保持 ⑤ 多 statement 过滤 + 占用排除联动） + `TestErpFinBankStatementImport` 增 2 组（④ DTO→行字段落库断言 + null 透传）。
- **验证基线（全绿）**：`mvn test -pl module-finance/erp-fin-service` **485/485**（477 基线 + 8 新增，0 failures / 0 errors——含既有 `TestErpFinBankStatementMatch` 7 场景 / `TestErpFinBankReconciliation*` 零回归）+ `mvn clean install -DskipTests` 全量 BUILD SUCCESS + compliance checker **零漂移**（R2c=1399/R10=9 不变——伙伴名经 ORM 关系 getter 惰性加载零新增 daoFor/import 面）。
- **回填**：arm-index P1-RC-004 → `done (RC-R1.43)`（含修复落地摘要 + C1/C2 裁决）+ roadmap RC-R1.43 → done ✅（含落地摘要，消除陈旧「须 ask-first checkbox」字样歧义为 A 类批量授权记录）+ owner doc `bank-reconciliation.md` schema 补注增对方账号维度实现注记（3 列 + 过滤语义 + C1/C2 裁决 + 残留风险；§业务规则 2 契约段未动，Non-Goal 遵守）+ `docs/logs/2026/08-15.md` 本条目。
- **Deferred（已裁定）**：VoucherLine 侧 counterpartyAccount 列（裁决未授权，watch-only residual，successor yes = 未来银行侧账号精确匹配双向需求按 ORM ask-first 流程立项）+ 模糊匹配 LIKE 增强（out-of-scope improvement，successor no——无触发条件不登记）+ 外部文件解析 MT940/CSV/Excel（out-of-scope improvement，DTO 承载已解析数据）+ 前端 AMIS 列展示（Non-Goal，xmeta 由生成链自动同步）。

### finance 过账引擎守卫族落地（RC-R1.41 + RC-R1.42，plan `2026-08-15-1838-1`，2026-08-12 裁决 B 类降级预授权）

- **工作项**：执行 `docs/plans/2026-08-15-1838-1-finance-posting-guards-family.md` 三 Phase（Phase 1 裁决 + Phase 2 GlDistribution Validator + Phase 3 FX 守卫）。落地 finding **P1-RC-001**（L1 UC-FIN-04/15 科目分摊机制完全缺失——FactsValidator 调用循环 :563-565 无实现 Bean 为死代码）+ **P1-RC-002**（L1 UC-FIN-12 断言② 汇率缺失静默回退 rate=1 而非拒绝——`prepareContext:537`+`persistVoucher:817-820`）。**治理**：2026-08-12 批量裁决 B 类（roadmap 头部 :48-49，批准人：用户）将两行从「越界项 ask-first」降级为预授权自动执行——纯代码逻辑 + Bean 静态规则载体，**零 ORM 结构变更**（不物化 `ErpFinGlDistribution`/`ErpFinGlDistributionLine` 实体）+ 独立 plan-audit 义务（本计划 Draft Review Record 已记录独立草案审查 1 轮 acceptable as-is）。
- **D1/D2 裁决**（Phase 1 落盘于 plan）：**D1** 规则载体 = 选项 A Bean 内静态规则表（`ErpFinGlDistributionValidator.rules` setter 注入 `List<ErpFinGlDistributionRule>`[ruleCode + sourceSubjectCode/sourceCostCenterId 源键 + validFrom/validTo + isActive + targets{targetCostCenterId, percent}]，生产默认空表 = 零行为变更，下游 beans.xml property/Delta 同名 bean 覆盖注入）——否决选项 B（ErpFinGlDistribution ORM 实体，超出 B 类授权）+ 选项 A'（VoucherTemplateLine.amountExpression 模板行驱动，无 targetCostCenterId 维度字段不可承载）；**D2** FX 守卫边界 = 选项 A（仅外币 isFunctional=false + rate 缺失 → 拒绝；本位币/currencyId=null/币种不存在保守放行 rate=1），本位币判定经 bizObjectManager 按名解析 `IErpMdCurrencyBiz`（对齐同文件 resolveSubjects 非 BizModel bean 范式 + isFunctional xmeta queryable=true 已核实，**零 daoFor → compliance 零漂移**；`ErpMdAcctSchema.functionalCurrencyId` 作 schema 级细分兜底载体归 successor）。
- **落地**：`ErpFinGlDistributionValidator`（新建，posting/ 包，getOrder()=100）+ `ErpFinGlDistributionRule`/`GlDistributionTarget`（新建 DTO）+ `ErpFinErrors` 2 新码（`ERR_EXCHANGE_RATE_REQUIRED` `erp.err.fin.exchange-rate-required` {currencyCode} / `ERR_GL_DISTRIBUTION_PERCENT_SUM` `erp.err.fin.gl-distribution.percent-sum` {ruleCode}{percentSum}）+ `app-service.beans.xml` GlDistribution Validator bean 注册（经 `ioc:collect-beans by-type=IErpFinFactsValidator` 自动聚合）+ `ErpFinPostingProcessor.prepareContext` 新增 protected step `guardExchangeRate`（外币 rate 缺失拒绝；`persistVoucher` 兜底不动，不做双守卫）。
- **测试**：`TestErpFinGlDistribution` 4 组 + `TestErpFinFxRateGuard` 4 组全绿（直断言范式不录快照）——① 拆行 60/40 守恒 + costCenterId 正确 + 头级借贷不变（引擎级，经 `@NopTestConfig(testBeansFile="/erp/fin/beans/test-gl-distribution.beans.xml")` 注入测试专用规则 Validator，add-test-mock-bean.md 范式）② Σ95 拒绝错误码 + ruleCode/Σ 参数 + 凭证/回链零落库 ③ 无规则透传（3 行 + costCenterId null）④ 规则载体 Bean 静态表加载（BeanContainer 取回断言）+ ⑤ 外币缺失拒绝零落库 ⑥ 显式 6.5 放行行级断言 ⑦ 本位币缺失 rate=1 ⑧ 币种不存在保守放行。
- **验证基线（全绿）**：`mvn test -pl module-finance/erp-fin-service` **479/479**（471 基线 + 8 新增，0 failures / 0 errors）+ `mvn clean install -DskipTests` 全量 BUILD SUCCESS（156 reactor 模块）+ compliance checker **actual == baseline 零漂移**（R1d=14/R2a=34/R2b=230/**R2c=1399**/R2d=34/R3=5/R10=9/R12a=69/R12b=66/R12c=40 精确一致——Validator 零 daoFor + 守卫经 bizObjectManager 零 daoFor，无基线调整）。注：全 reactor `mvn test` 3 项预存在失败不变（TestAuthSeedLoadingProof NPE + mfg ErpMfgCostRollupLine materialBand cell-not-prop 双页面测试，见 known-good-baselines.md:63-64 + docs/bugs/ 双注记，与本次变更无关）。
- **回填**：arm-index P1-RC-001/002 → `done (RC-R1.41/42)`（含修复落地摘要）+ roadmap RC-R1.41/42 → done ✅（含落地摘要 + 消除陈旧「ask-first checkbox」字样歧义，按 plan 超期记录）+ owner doc `posting.md` §FactsValidator（陈旧声明「本工程不强制实现 GL Distribution」→ 实现注记）+ §多币种处理（汇率缺失守卫实现注记 + D2 裁决边界）+ `cost-center.md` §ErpFinGlDistribution 实体未物化裁决注记 + §业务规则 2 实现注记 + `docs/logs/2026/08-15.md` 本条目。
- **Deferred（已裁定）**：GlDistribution 规则实体化（ErpFinGlDistribution 实体 + CRUD 管理界面 + 生效窗口管理，out-of-scope improvement，successor yes = 运行时规则管理需求立项后按 ORM ask-first 流程）+ currencyId=null/币种不存在 FX 守卫保守放行（watch-only residual，当前各域 Provider 均显式传 currencyId，A4.1.2 普查；successor no）。

### master-data priceValidationLevel 默认值收敛落地（RC-R1.40，plan `2026-08-15-1605-3`，第一批纯预授权）

- **工作项**：执行 `docs/plans/2026-08-15-1605-3-rc-mr1-r1-40-md-price-validation-level-default-convergence.md` 三 Phase。落地 finding **P2-RC-057**（A4.2.145：ORM `ErpMdMaterialCategory.priceValidationLevel`（`app-erp-master-data.orm.xml:344`，propId=6）`defaultValue="20"` 孤儿非字典值——dict `erp-md/price-validation` 仅 OFF/WARN/HARD 三值 + 种子输入零显式填充 → "20" 仅经 ORM defaultValue 在插入未显式赋值时物化，违反字典契约；运行时行为正确——`resolvePriceValidationLevel` 非字典值→WARN 兜底）。**治理**：2026-08-08 §7 A5 人工裁决预授权 Q3 纯加性类自动执行——ORM defaultValue 单属性文本收敛 + 增量重生成 + 测试，零表结构/索引/UK/实体/既有数据/行为变更。
- **落地**：`app-erp-master-data.orm.xml:344` `defaultValue="20"`→`defaultValue="WARN"`（仅属性文本修改，其余列定义不动）+ `xmllint --noout` well-formed 通过 + `mvn clean install -DskipTests` 增量重生成 BUILD SUCCESS（**未**重跑 `nop-cli gen`）+ 生成产物核对——XMeta `_ErpMdMaterialCategory.xmeta` defaultValue="WARN" + `_app.orm.xml` defaultValue="WARN" + mysql/oracle/postgresql DDL `default 'WARN'`（defaultValue 令牌同步属预期变化，DDL 结构/索引/UK 零变更）+ 生成侧（`_gen` 注释/i18n）零 "20" 残留；`ErpMdMaterialSkuBizModel` G5 javadoc 收敛注记（`resolvePriceValidationLevel`/`validatePrice` 行为零变更，非字典值→WARN 兜底保留防历史遗留 "20" 值行行为翻转）。
- **测试**：`TestErpMdSkuPriceValidation` 新增 3 组全绿——① `testDefaultPriceValidationLevelConvergedToWarn`（defaultValue 物化断言：不显式赋值创建分类 → 持久化 "WARN"）② `testExplicitPriceValidationLevelUnaffected`（显式 OFF/WARN/HARD 原样持久化不受影响）③ `testNonDictValueFallsBackToWarn`（历史 "20" 非字典值行 resolve 兜底 → WARN 放行带警告，行为不变性证明——既有 4 校验测试零非字典路径覆盖缺口闭合）；4 个 `_cases` output 快照 CSV 重录（defaultValue 物化传播 20→WARN，seed 输入仍零显式填充）。
- **验证基线（全绿）**：`mvn test -pl module-master-data/erp-md-service` **146/146**（143 基线 + 3 新增，0 failures / 0 errors）+ 跨域消费面 `TestErpSalPricingCompliance` 10/10 零回归（sales 种子显式赋值不受影响）+ `mvn clean install -DskipTests` 全量 BUILD SUCCESS + compliance checker 零漂移（R2c=1399/R10=9 不变，零新增 daoFor/import 面）。
- **回填**：arm-index P2-RC-057 → `done (RC-R1.40)`（含修复落地摘要：A5 裁决引用 + defaultValue 收敛 + 行为不变性证明 + 数据卫生 successor）+ roadmap RC-R1.40 → done ✅（含落地摘要）+ owner doc `sku-multi-unit.md` §价格优先级 + §配置项 补收敛实现注记（defaultValue "WARN" + 既有 "20" 值行语义不变 + 数据卫生迁移 successor）+ `docs/logs/2026/08-15.md` 本条目。
- **Deferred（已裁定）**：既有 "20" 值行数据卫生迁移（watch-only residual——运行时行为即 WARN 无行为偏离，defaultValue 收敛只影响新插入；数据 UPDATE 迁移属数据变更须 ask-first，successor yes = 存量数据卫生迁移需求立项后实施）。

### b2b 伙伴上线状态机推进落地（RC-R1.36，plan `2026-08-15-1023-3`，第一批纯预授权）

- **工作项**：执行 `docs/plans/2026-08-15-1023-3-rc-mr1-r1-36-b2b-partner-onboarding-state-machine.md` 五 Phase（Phase 1 裁决 + Phase 2 落地 + Phase 3 监控 job + Phase 4 测试 + Phase 5 回填）。落地 finding **P1-RC-080**（L1 UC-B2B-007「REGISTERED→TESTING[验证通过率≥90%]→CERTIFIED[所有必检项通过]→PRODUCTION[设置 goLiveDate，上线监控 24 小时]→TERMINATED[archivedAt 记录]」——`use-cases.md:220-231`，L3 `ErpB2bPartnerProfileBizModel#activate:22-27` 任意态→PRODUCTION 零状态守卫 + TESTING/CERTIFIED 推进 mutation 零实现 + pass-rate/认证清单门槛零实现 + activate 未设 goLiveDate + deactivate 未设 archivedAt + 24h 监控零实现）。**治理**：纯 BizModel/StateMachine Bean/job 代码逻辑 + 测试，零 ORM 结构/会计过账/删除变更（goLiveDate/archivedAt/status 列已存在）。
- **D1-D4 决策裁决**（Phase 1 落盘于 plan）：**D1** promoteToTesting 前置 = 选项 B 全配置校验（partnerId/protocol/authMethod/transportEndpoint/allowedFormats 非空，Stage 1 后置条件「基本配置完整」字面义务）；promoteToCertified 前置 = 三门槛链（pass-rate≥config 0.9 零行拒绝 + TC-001/TC-004 前缀必过 + 必检清单）；**ERROR 阻断查询本行不实现**（EdiDoc 无 partnerProfileId 锚点，Deferred 同 D4 successor）；**D2** 认证清单 = 选项 A 全必检项通过 + 空清单拒绝（无认证流程证据不可证明「所有必检项通过」）；**D3** 错误码 = 命名族 `ERR_B2B_PARTNER_ILLEGAL_TRANSITION` + 细分门槛码 3 个（profile-incomplete/pass-rate-not-met/certification-not-met，拒绝统一门槛码——诊断粒度丢失）；**D4** 24h 监控 = 选项 A 间接路径锚点（allowedFormats JSON→EdiFormat.code→formatId + org 级 scope 兜底聚合窗口内 ERROR 失败率），job 形态 = R1.4 简单 job bean（否决 batch-task REQUIRES_NEW 的 R10 漂移，R1.34 D6 同型裁决）；选项 C（EdiDoc 加 partnerProfileId 列）触 ORM = ask-first 移出本行登记 successor。
- **落地**：`ErpB2bPartnerProfileStateMachine`（新建，statemachine 包——12 边迁移矩阵[promoteToTesting 1 + promoteToCertified 1 + activate 1 + suspend 4 + deactivate 5] + 终态/初始态分类 + transitions() 元数据，Bean 抛 common 层 `ERR_ILLEGAL_STATUS_TRANSITION` 契约 §2/§7）；`ErpB2bPartnerProfileBizModel` 五 mutation（promoteToTesting/promoteToCertified 新增 + activate/suspend/deactivate 守卫化——Bean 守卫经 `illegalTransition` 映射领域码[镜像 EdiDocBizModel:188-226 范式，common 码作 cause] + activate 设 `goLiveDate=CoreMetrics.currentDate()` + deactivate 设 `archivedAt=CoreMetrics.currentTimestamp()`；门槛校验跨实体聚合经 `IErpB2bTestExchangeBiz`/`IErpB2bCertificationChecklistBiz` 注入零新增 daoFor）；`IErpB2bPartnerProfileBiz` 契约扩展 + javadoc 同步（SUSPENDED→resume 不对称 Deferred 登记）；`ErpB2bErrors` 4 新码 + 参数表注册；`ErpB2bConstants` 6 伙伴状态常量 + `NOTIFY_EVENT_ONBOARDING_MONITOR_ALERT`；`ErpB2bConfigs` 4 新键（test-pass-rate 0.9 / production-monitor-hours 24 / monitor-failure-rate 0.05 / monitor-cron 单键）；beans.xml 注册 StateMachine + job bean；`ErpB2bOnboardingMonitorJob`（新建，`app.erp.b2b.service.job`——execute() 无参 + cron 空值跳过 + PRODUCTION + dateBetween(goLiveDate 窗口) 扫描 + monitorPartner[allowedFormats→formatId 锚点 + dateTimeBetween(createTime) 计数 + 超 5% notify + 逐条失败隔离 + 无 ACTIVE 模板静默跳过]）；`erp-b2b-onboarding-monitor.job.yaml`（app-erp-all 注册：enabled 默认 false + cronExpr `@cfg:erp-b2b.onboarding-monitor-cron|0 0 * * * ?`）；pom 增 notify-dao compile + notify-service test（对齐 finance 先例）；`TestErpAllJobYamlLoading` 23→24。
- **测试**：`TestErpB2bPartnerProfileStateMachineMatrix` 10 组全绿（层 1 纯矩阵：无重复/冲突边 12 边/终态无出边/五动作来源态合法集+非法集/common 码断言）；`TestErpB2bPartnerOnboarding` 13 组全绿——① 守卫矩阵 2（非法迁移拒绝 9 断言[REGISTERED/TESTING 直跳 PRODUCTION 拒绝 + 非法推进 + 终态再操作 + 已暂停再暂停] + 零落库断言 + 合法全生命周期[promoteToTesting→promoteToCertified→activate→suspend→deactivate]）② 门槛 6（配置不完整/通过率 2/3/TC-004 缺失/空清单/必检项未全过/门槛满足放行）③ 字段回写 2（activate goLiveDate=FROZEN_TODAY + deactivate archivedAt 非空 DAO 断言）④ 监控 job 6（超阈值通知落库+recipient 断言/未超阈值 3.3% 零动作+窗口外独立格式防泄漏[ORM 覆写 createTime 致同 org+同格式跨伙伴泄漏——测试独立格式隔离 + 生产近似已注记]/坏 JSON 失败隔离+正常伙伴 subject+body 渲染断言/cron 空值跳过/无模板静默跳过）；XMeta 过滤算子白名单实测 [eq,in,dateBetween,dateTimeBetween] 无 startsWith/ge/le → 前缀匹配改 Java startsWith 内存过滤 + 窗口经 dateBetween/dateTimeBetween 表达。
- **验证基线（全绿）**：`mvn test -pl module-b2b/erp-b2b-service` **80/80**（57 基线 10 类零回归 + 23 新增，0 failures / 0 errors）+ `mvn clean install -DskipTests` 全量 BUILD SUCCESS + compliance checker 零漂移（新方法全经 IBiz 注入零新增 daoFor 站点 + R1.4 job 无 REQUIRES_NEW，R2c=1399/R10=9 不变）+ `TestErpAllJobYamlLoading` 24/24 通过（job.yaml 计数同步）。注：全 reactor `mvn test` 2 项预存在失败不变（TestAuthSeedLoadingProof NPE + mfg ErpMfgCostRollupLine，与 RC-R1.36 无关，见 known-good-baselines.md + docs/bugs/ 双注记）。
- **回填**：arm-index P1-RC-080 → `done (RC-R1.36)`（含修复落地摘要）+ roadmap RC-R1.36 → done ✅（含落地摘要）+ owner doc `partner-onboarding.md` 补「实现注记（RC-R1.36）」段（状态机 Bean/门槛/回写/监控/config 键/测试证据，use-cases L1 未动）+ `docs/logs/2026/08-15.md` 本条目。
- **Deferred（已裁定）**：blocking_level=ERROR 阻断查询（EdiDoc 无 partnerProfileId 锚点，watch-only residual，successor = 伙伴级 EDI 统计需求立项）+ PRODUCTION→TESTING 回退 + SUSPENDED→原阶段 resume（L1 未列，out-of-scope improvement，successor = 生产回退/暂停恢复需求立项，SUSPENDED 滞留不对称已登记）+ EdiDoc 加 partnerProfileId 列（D4 选项 C 触 ORM ask-first，out-of-scope improvement，successor = 伙伴级 EDI 统计需求立项）+ 监控窗口内重复告警去重（需「已通知」标记列 = ORM ask-first，watch-only residual，已知行为已注记）+ 真实 EDI 测试执行（watch-only residual，既有 EdiDoc 链路承载）+ 凭证轮换/证书到期自动停用（P2-RC-068 范围）+ 前端上线向导 AMIS 接线（watch-only residual，后端 mutation 能力面已提供）。

### contract 到期自动化 job 重开族落地（RC-R1.35，plan `2026-08-15-1023-2`，第一批纯预授权）

- **工作项**：执行 `docs/plans/2026-08-15-1023-2-rc-mr1-r1-35-ct-expiry-job-reopen-family.md` 四 Phase（Phase 1 裁决 + Phase 2 落地 + Phase 3 测试 + Phase 4 回填）。落地 finding **P1-MA2-071 reuse 重开**（表 E 重开族 Q9 维持强制：UC-CT-05 A/B/C/E「nop-job 扫描 erp_ct_contract 条件 `status=ACTIVE AND endDate BETWEEN now() AND now()+30d` / 30/15/7 分级通知 / 续期按配置 auto-create-renewal-draft 自动创建续期草稿[parentContractId 关联] / endDate 到达自动 EXPIRED / 异常：endDate 到达仍有未完成的开票计划 → 先完成开票再 EXPIRED」——`use-cases.md:91-107`，L3 全域零 ErpCt*Job + 零 job.yaml + `ErpCtContractBizModel#expire` 仅手工单点 + 零 auto-create-renewal-draft config + parentContractId 零业务使用 + 零 30/15/7 通知）。**治理**：调度接线 + config + notify + BizModel 批量方法（纯代码逻辑 + 测试），零 ORM 结构/会计过账/删除变更（parentContractId 复用既有列；expire 复用既有状态机 Bean 守卫）。
- **D1-D5 决策裁决**（Phase 1 落盘于 plan）：**D1** 30/15/7 窗口 config 化 = 三个独立键（`erp-ct.contract-expiry-warning-days-30/15/7` 默认 30/15/7，对齐 hr 单窗口 config 先例，每档独立可调）；**D2** 7 天升级「经办人上级」 = nop-auth 平台上级链（实仓核查修正：md 员工无 superiorId、md 组织无 managerId → 字面 IErpMdEmployeeBiz 链不可落地；nop-auth.orm.xml 实证 `NopAuthUser.managerId` 直接上级 → 兜底 `NopAuthUser.deptId → NopAuthDept.managerId` 部门负责人，与 R1.4 superiorId→department.managerId 语义同构 + 零新增依赖；双 null LOG.warn 跳过）；**D3** 未完成开票异常路径 = 选项 A（expire 前 isInvoiced=false AND planDate≤today 的 InvoicePlan 逐条 triggerInvoice 复用既有 Processor，触发失败逐条隔离不影响 expire）；**D4** 续期草稿 = 到期时创建（expire 前，config-gated 默认 false，合同头复制 code "-RN" 后缀 + parentContractId 关联 + DRAFT + startDate=原endDate+1，幂等守卫防重复）；**D5** cron 键 = 单键模式（job.yaml cronExpr 与 bean 空值跳过共用 `erp-ct.contract-expiry-cron`，`@cfg:key|default` 空值回退默认实证 ConfigValueResolver）。
- **落地**：`ErpCtContractExpiryJob`（新建，`app.erp.ct.service.job`——execute() 无参 + cron 空值跳过 + runExpiryWarnings[scanExpiringContracts 30 天窗口 + 逐合同按剩余天数分档 30/15/7 + notifyWarning/notifyEscalation 逐条失败隔离 + resolveEscalationUserId 上级链] + runExpirations[expireOverdueContracts]）；`erp-ct-contract-expiry.job.yaml`（app-erp-all 注册：enabled 默认 false + cronExpr `@cfg:erp-ct.contract-expiry-cron|0 0 1 * * ?` + invoker erpCtContractExpiryJob.execute）；`IErpCtContractBiz`/`ErpCtContractBizModel` 增 `scanExpiringContracts`（@BizQuery，dateBetween 窗口）+ `expireOverdueContracts`（@BizMutation + @SingleSession，dateBetween(epoch, today-1) 表达 endDate<today——XMeta 白名单无 lt，对齐 TriggerDuePlansProcessor 注记；逐合同 triggerDueInvoicesBeforeExpire → createRenewalDraftIfEnabled → assertCanExpire → EXPIRED，逐条失败隔离）；`ErpCtConfigs` 5 新键（contract-expiry-cron/warning-days-30/15/7/auto-create-renewal-draft）；`ErpCtConstants` 3 通知事件（contract-expiry-warning-30/15/escalation-7）；beans.xml 注册 job bean；`TestErpAllJobYamlLoading` 22→23。
- **测试**：`TestErpCtContractExpiryJob` 14 组全绿——① 三级通知 5（30/15/7 分档 + payload 关联断言 + 窗口边界[15 天→15 档/16 天→30 档] + 部门负责人兜底 + 双 null 跳过 + 无模板静默跳过）② 批量 expire 1（过期→EXPIRED/未过期零动作）③ D3 异常路径 2（到期未开票计划先 triggerInvoice[AP 发票草稿落库 + isInvoiced 回写 + EXPIRED] + 单计划触发失败隔离[行物理删除 → 计划保持未开票 + 合同仍 EXPIRED]）④ 续期草稿 3（config on 创建[parentContractId+DRAFT+-RN code+日期推进] / config off 零创建 / 幂等守卫不重复建）⑤ cron 空值跳过 1 ⑥ 单条失败隔离 1（续期草稿 code UK 冲突 → 该合同 WARN 跳过不 EXPIRED + 其它合同正常推进）⑦ GraphQL RPC 冒烟 1（scanExpiringContracts/expireOverdueContracts 经 IGraphQLEngine 可路由）。
- **验证基线（全绿）**：`mvn test -pl module-contract/erp-ct-service` **146/146**（132 基线 + 14 新增，0 failures / 0 errors）+ `mvn clean install -DskipTests` 全量 BUILD SUCCESS + compliance checker 零漂移（新方法全经 IBiz 注入零新增 daoFor 站点——scan/expire/触发/草稿查询均经 findList/IBiz，R2c=1399 不变）+ `TestErpAllJobYamlLoading` 23/23 通过（job.yaml 计数同步）。注：全 reactor `mvn test` 2 项预存在失败不变（TestAuthSeedLoadingProof NPE + mfg ErpMfgCostRollupLine，与 RC-R1.35 无关，见 known-good-baselines.md + docs/bugs/ 双注记）。
- **回填**：arm-index P1-MA2-071 → `done (RC-R1.35)`（重开族 Q9 强制实现完成，A/B/C/E 四级全维度收敛注记）+ roadmap RC-R1.35 → done ✅（含落地摘要）+ owner doc `state-machine.md` §2 ACTIVE→EXPIRED 自动化 Deferred 注记 → 已实现注记 + §4 续期草稿 Deferred 注记 → 已实现注记 + §7 到期提醒 Deferred 注记 → 已实现注记 + §9 场景 A step 7/8 收敛（use-cases L1 未动）+ `docs/logs/2026/08-15.md` 本条目。
- **Deferred（已裁定）**：邮件/短信真实通道载体（watch-only residual，站内通知已派发，载体属 notify 子系统 successor）+ 续期审批流联动（续期草稿经审批引擎完整流，out-of-scope improvement，属 RC-R1.34 引擎范围扩展）+ 前端到期提醒视图/续期操作 AMIS 接线（watch-only residual，后端能力面已提供）。

### contract 终止与审批族落地（RC-R1.34，plan `2026-08-15-1023-1`，第一批纯预授权）

- **工作项**：执行 `docs/plans/2026-08-15-1023-1-rc-mr1-r1-34-ct-terminate-approval-family.md` 六 Phase（Phase 1 裁决 + Phase 2-4 落地 + Phase 5 测试 + Phase 6 回填）。落地 finding P1-RC-076（L1 UC-CT-06 A/C/D「提交法务审批 / 法务审批通过后，系统执行终止操作[合同→TERMINATED + 当前版本 isCurrent=false 归档 + 截停所有未执行 InvoicePlan + 生成 TODO 善后结算] / 法务驳回 → 合同保持原状态」——`use-cases.md:121-128/132`，L3 terminate 单向翻转零门控/截停/TODO/归档）+ P1-RC-077（L1 UC-CT-07「按 totalAmount 匹配节点 / 生成 Record 首 PENDING 其余 WAITING / 逐节点审批 / 全通过可 ACTIVE / 驳回超限 3 次锁定 / 超时 72h 升级」——`use-cases.md:141-143`，L3 ApprovalMatrix/Record CRUD 桩零工作流逻辑）。**治理**：纯 BizModel/Engine/job 代码逻辑 + 测试，零 ORM 结构/会计过账/删除变更（InvoicePlan 截停经 useLogicalDelete 逻辑删除；rejectCount 派生计数；D2 经 IBiz 注入零 daoFor）。
- **D1-D7 决策裁决**（Phase 1 落盘于 plan）：**D1** terminate 法务门控 = 选项 B（独立 approveTermination/rejectTermination + ApprovalRecord 复用[approvalMatrixId=null 判别]）——法务门控为 §6 强制义务不受 approval-enabled config 门控（A 方案引擎复用的 config-gate 破裂 + 引擎感知合同终局动作耦合）；法务角色 config `erp-ct.terminate-approver-role`（默认「合同审批人」）；**D2** 角色→用户解析 = 平台 nop-auth roleName 双过滤（NopAuthRole→NopAuthUserRole→userId 确定序最小，IBiz 注入零 daoFor，approverId 无命中留空手工指定）；**D3** 驳回超限 = 派生计数（contractId+approvalOrder REJECTED 记录数 vs max-retries=3，零 ORM）；**D4** InvoicePlan 截停 = 逻辑删除（useLogicalDelete delVersion 标记，隐式失效+显式标记双保险）；**D5** 善后 TODO = `ct.terminate-winddown` 通知承载（经办人 createdBy，无 ACTIVE 模板静默跳过）；**D6** 72h 升级 job = R1.4 简单 job bean 范式（低基数扫描 + 逐条失败隔离，否决 batch-task REQUIRES_NEW 的 R10 漂移）；**D7** 驳回重提 = 纳入范围（resubmit 追加新 ApprovalRecord 行[REJECTED 历史保留 + 新 PENDING 行]，派生计数递增使锁定可达——L1 step 6 字面义务）。
- **落地**：`ErpCtApprovalWorkflowEngine`（新建，`app.erp.ct.service.approval`——matchByAmount/generateRecords/链状态推导/D2 解析）；`ErpCtApprovalRecordBizModel` approve/reject/resubmit @BizMutation（逐节点推进 + 审批人守卫 + D3 锁定 + D7 追加行 + 通知）；`ErpCtContractBizModel` terminate 两段化 + approveTermination/rejectTermination（TERMINATED + 版本归档 + InvoicePlan 截停 + 善后通知）+ submit 后置 config-gated 生成接线；`ErpCtContractActivateProcessor` activate 前置链完整性校验（config-gated ERR_CT_APPROVAL_NOT_COMPLETE）；`ErpCtApprovalTimeoutEscalationJob` + `erp-ct-approval-timeout.job.yaml`（R1.4 双键门控 + 升级通知上一节点/经办人）；`ErpCtErrors` 7 新码；`ErpCtConstants` 审批状态 5 常量 + 6 通知事件；`ErpCtConfigs` 7 键登记（approval-enabled/max-retries/timeout-hours/urgent-threshold/amendment-reapproval-threshold/timeout-cron/terminate-approver-role）；`IErpCtApprovalRecordBiz`/`IErpCtContractBiz` 契约同步；beans.xml 引擎 + job 注册。
- **测试**：新增 3 类 22 组全绿——`TestErpCtApprovalWorkflow` 11（config-gated 双路径/金额匹配生成/逐节点推进/activate 联动+拒绝/驳回通知/守卫/D7 驳回→重提×3→锁定闭环追加行断言）+ `TestErpCtTerminateGate` 6（发起/重复拒绝/副作用全落地[TERMINATED+版本归档+截停+善后通知]/驳回保持/守卫/未过法务不执行）+ `TestErpCtApprovalTimeoutJob` 5（超时升级通知/未超时零动作/cron 跳过/失败隔离/config 绑定）；既有调整——TestErpCtContractTerminate/Posting/Rebate 两段化 + Rebase 快照重录 + E2E `ct-contract-lifecycle` 两段化（loginAsRole「合同审批人」）+ `TestErpAllJobYamlLoading` 21→22；`_cases/` 自动录制。
- **验证基线（全绿）**：`mvn test -pl module-contract/erp-ct-service` **132/132**（110 基线 + 22 新增，0 failures / 0 errors）+ `mvn clean install -DskipTests` 全量 BUILD SUCCESS + compliance checker actual == baseline 零漂移（R2c=1399 不变——引擎/记录/合同侧全经 IBiz 注入；附修 R1.33 遗留 javadoc 注释命中致 R1b 基线漂移 1 处，注释改写恢复 0）。注：全 reactor `mvn test` 仍有 2 项**预存在**失败（与 RC-R1.34 无关，见 known-good-baselines.md:63-64 + docs/bugs/ 双注记）：`TestAuthSeedLoadingProof` NPE（nop-entropy `e5ee02b40` lazy-property 回归，外部仓库 ask-first）+ mfg `ErpMfgCostRollupLine` materialBand cell-not-prop（E4.1 `452e418d0` 引入，页面/实体字段对齐待独立计划裁决）。`TestErpAllJobYamlLoading` 22/22 通过（job.yaml 计数同步）。
- **回填**：arm-index P1-RC-076/077 → `done (RC-R1.34)`（含修复落地摘要）+ roadmap RC-R1.34 → done ✅（含落地摘要）+ owner doc `approval-workflow.md` 实现注记段（引擎/触发入口/逐节点/activate 联动/D2/D3/D7/72h job/通知事件/config 键/Deferred）+ `state-machine.md` §3 terminate 两段化实现注记 + `docs/logs/2026/08-15.md` 本条目。
- **Deferred（已裁定）**：审批撤回（NEGOTIATION→DRAFT）与转交（L1 未列，out-of-scope improvement，successor = PM 审批操作完备性立项）+ terminate 申请独立实体载体（D1 备选，审计追溯增强，successor = 终止申请审计追溯需求立项）+ amendment-reapproval-threshold 小额免审快捷通道（watch-only，amend→submit 全链重审结构性达成）+ 前端审批操作 AMIS 接线（watch-only residual，后端 mutation 能力面已提供）。

### contract 创建校验与版本族落地（RC-R1.32，plan `2026-08-15-0456-2`，第一批纯预授权）

- **工作项**：执行 `docs/plans/2026-08-15-0456-2-rc-mr1-r1-32-ct-create-validation-version-family.md` 四 Phase。落地 finding P1-RC-072（L1 UC-CT-01 A/B「系统校验 总金额 = ∑行金额；startDate < endDate / 提交审批 → NEGOTIATION（创建 v1 版本，isCurrent=true）」——`use-cases.md:13/15`，L3 `defaultPrepareSave` 零金额/日期校验 + 无 submit mutation + 测试手工 seed 版本证实 v1 非自动）+ P1-RC-073（L1 UC-CT-02 A/C「复制原合同所有行到变更单 / 变更单被驳回 → 原合同保持 ACTIVE 不变」——`use-cases.md:33/43`，L3 amend 仅版本翻转零行复制 + 零驳回恢复）。**治理**：纯 BizModel/Processor 代码逻辑 + 测试，零 ORM 结构/会计过账/删除变更。
- **D1-D6 决策裁决**（Phase 1 落盘于 plan）：**D1** 创建校验语义 = 有行时 totalAmount 必填且 == Σ行金额（scale 4 HALF_UP 容差），create 用 in-memory 实体图（`entity.getLines()`，框架 `copyToEntity` 先于 `defaultPrepareSave` 已 attach 嵌套行）/ submit 用 `IErpCtContractLineBiz.findList` DAO 口径（对齐 R1.8 totalHours 先例），无行时 totalAmount 可空（既有测试零调整），日期单侧缺失容忍，更新路径不校验（submit 为权威门卫）；**D2** v1 初始状态 = DRAFT（初始态写入，activate 前须 finalizeVersion）；**D3** amend 行复制 = 选项 A 行保留（同合同模型 ⇒ 复制语义 = 行保留，零复制代码）；**D5** rejectAmend 恢复目标 = SIGNED 最大 versionNo（无 SIGNED 回落 FINALIZED 最大者，恢复时推导跨请求可行）；**D6** activate 对非 FINALIZED current 静默跳过 watch-only 登记（activate 前须显式 finalizeVersion 契约）；**D4** 预算特批按 roadmap 范围裁剪（Deferred But Adjudicated，触发条件已登记）。
- **落地**：`ErpCtContractBizModel`——`defaultPrepareSave` 增 `validateCreateFields`（跨字段校验）+ `submit` @BizMutation（守卫 Bean → `validateContractFields` → `ensureVersionOnSubmit` 零版本建 v1 / 已有版本保留 current → NEGOTIATION）+ `rejectAmend` @BizMutation（守卫 Bean → `restoreCurrentVersion` D5 选项 B → ACTIVE）+ helpers（直写形态对齐 suspend/resume/terminate/expire 五先例）；`ErpCtContractStateMachine` 7 边 → 9 边（`assertCanSubmitForNegotiation`/`submitTargetStatus` + `assertCanRejectAmend`/`rejectAmendTargetStatus`，transitions() 元数据同步）；`ErpCtErrors` 新增 `ERR_CT_AMOUNT_MISMATCH`/`ERR_CT_DATE_RANGE_INVALID`；`IErpCtContractBiz` 契约增 submit/rejectAmend；`ErpCtContractAmendProcessor.amend` 补 D3 行保留语义注释（零逻辑变更）。
- **测试**：`TestErpCtContractCreateValidate` 16 组全绿——① 创建校验 5（缺失容忍放行/嵌套行匹配放行/嵌套行不匹配拒绝/有行 totalAmount 缺失拒绝/日期倒置拒绝）② submit 4（v1 自动创建+NEGOTIATION/非 DRAFT 拒绝/已有版本保留 DRAFT current 不动[MAJOR-1]/DAO 行口径金额不一致拒绝）③ 全链双路径 2（submit→finalizeVersion(v1)→activate 首提 + amend→submit→finalizeVersion(v2)→activate[MAJOR-3]）④ rejectAmend 4（SIGNED 最大恢复/非 DRAFT 拒绝/finalize-then-reject 边界[iteration-4]/重复周期二次恢复[MAJOR-2]）⑤ amend 行保留 1；`TestErpCtContractStateMachineMatrix` 7→9 边 + DRAFT 可达性断言反转 + switch 扩展。
- **验证基线（全绿）**：`mvn test -pl module-contract/erp-ct-service` **98/98**（82 基线 + 16 新增，TestErpCtContractPosting/Terminate/CrudSmoke/ESignature/Rebate 零回归）+ 全量构建 + compliance checker 零漂移（零新增 daoFor/import 面——行查询经 `IErpCtContractLineBiz` 注入）。
- **回填**：arm-index P1-RC-072/073 → `done (RC-R1.32)`（含修复落地摘要，显式记录「同合同模型 ⇒ 复制语义 = 行保留」防 compliance 漂移）+ roadmap RC-R1.32 → done ✅（含落地摘要）+ owner doc `state-machine.md` §2 漂移注记更新为已实现（submit 边 + v1 创建语义）+ §适用对象二 版本族裁定注记（D2/D3/D5/D6）+ §审查提示。
- **Deferred（已裁定）**：P1-RC-072 ③ 预算校验 hook + 特批流程（D4 范围门裁剪，watch-only residual，successor yes = 预算特批业务流立项 / finance budget 跨域契约行启动 / roadmap 范围修订）+ 前端 submit/rejectAmend AMIS 按钮接线（watch-only residual，successor no）+ amend 遗留 DRAFT 版本行去留（optimization candidate，successor no）。

### contract 计费族落地（RC-R1.33，plan `2026-08-15-0456-3`，第一批纯预授权）

- **工作项**：执行 `docs/plans/2026-08-15-0456-3-rc-mr1-r1-33-ct-billing-family.md` 四 Phase（Phase 1 裁决 + Phase 2-3 落地 + Phase 4 测试 + Phase 5 回填）。落地 finding P1-RC-074（L1 UC-CT-03 A/C「系统按合同行的 invoiceTerm 批量生成 InvoicePlan / 已开票的 InvoicePlan 不允许修改金额」——`use-cases.md:54-58/63`，L3 无按 term 生成编排方法 + 无 defaultPrepareUpdate isInvoiced 守卫）+ P1-RC-075（L1 UC-CT-04 B/C/D「周期结束时汇总 ConsumptionLine 总量与合同行预估总量对比 / 超量部分生成额外计费 InvoicePlan / 系统生成 AP/AR Invoice 草稿给财务 / 消耗量超过预估 120% → 触发超量审批通知」——`use-cases.md:79-85`，L3 ConsumptionLineBizModel CRUD 桩零业务命中）。**治理**：纯 BizModel/Processor 代码逻辑 + 测试，零 ORM 结构/会计过账/删除变更（发票草稿复用既有 triggerInvoice 的 O-4 豁免范式）。
- **D2-D6 决策裁决**（Phase 1 落盘于 plan Decision Record）：**D2** 生成入口 = 入参化契约（contractId + items[{contractLineId, invoiceTerm, planDate, amount}]）——L1「合同行已配置 invoiceTerm」与 ORM 结构错配（Plan.invoiceTerm mandatory、Line 无该列）的入参化解释，零 ORM 变更 + 支持多 term 混合；**D3** planDate 入参显式（不引入 config 推导键）+ 服务端幂等查重（contractLineId+invoiceTerm+planDate → `ERR_CT_INVOICE_PLAN_DUPLICATE`）；**D4** isInvoiced 守卫字段集 = {amount, planDate, invoiceTerm}（remark 放行），ORM 脏值追踪取更新前状态防同请求解锁绕过（对齐 R1.9 publish 守卫范式），`ERR_CT_INVOICE_PLAN_INVOICED_IMMUTABLE`；**D5** notify event `ct.consumption-over-120-percent`（`ErpCtConstants.NOTIFY_EVENT_CONSUMPTION_OVER_120`）+ context 键集 {contractId, contractCode, contractLineId, lineDescription, estimatedQuantity, totalConsumedQuantity, overRatio} + 无 ACTIVE 模板静默跳过（R1.4 范式）；**D6** 超量金额 = overQuantity × line.unitPrice（scale 4 HALF_UP）+ 超量草稿行量/额不一致声明（header 是计费基准，行级仅供明细展示）；行级聚合入口（合同级循环归 successor）。
- **落地**：`ErpCtInvoicePlanBizModel`——`generateInvoicePlansByTerm` @BizMutation（委托 `ErpCtInvoicePlanGenerateByTermProcessor`：合同 ACTIVE 守卫[SUSPENDED 专属 + 通用非 ACTIVE] → 行归属校验 `ERR_CT_INVOICE_PLAN_LINE_NOT_IN_CONTRACT` → 幂等查重 → isInvoiced=false 落库）+ `defaultPrepareUpdate` 增 isInvoiced 守卫；`ErpCtConsumptionLineBizModel`——`periodSummarize` @BizMutation（委托 `ErpCtConsumptionPeriodSummarizeProcessor`：期间 Σ ConsumptionLine.quantity 对比 line.quantity → 超量复用生成面能力建额外 InvoicePlan + 复用 triggerInvoice Processor 生成 AP/AR 发票草稿[同事务 flush+evict 重载处理 NEW 态] → Σ>预估×1.2 经 `IErpSysNotificationBiz.notify` 派发 → 返回 `ErpCtConsumptionPeriodSummarizeResult`）；`ErpCtErrors` 新增 5 错误码（invoiced-immutable/duplicate/line-not-in-contract/consumption-line-not-found/date-range-invalid）；`ErpCtConstants.NOTIFY_EVENT_CONSUMPTION_OVER_120`；IBiz 契约增 generateInvoicePlansByTerm/periodSummarize；DTO `ErpCtInvoicePlanGenerateItem`/`ErpCtConsumptionPeriodSummarizeResult`（erp-ct-dao `app.erp.ct.biz`，对齐 ConvertToOrderRequest/BatchOperationResult 先例）；beans.xml 注册 2 Processor；pom 增 notify-dao（compile）+ notify-service（test，对齐 cs/purchase 先例）。
- **测试**：`TestErpCtBillingFamily` 12 组全绿——① 批量生成 4（2 行×2 term 生成 4 plan + 非 ACTIVE 拒绝零落库 + 幂等拒绝 + 行归属拒绝）② isInvoiced 锁 3（已开票改金额拒绝 + 金额不变 + 未开票可改 + 改 remark 放行）③ 汇总正确 1（Σ 计算 + line.quantity 对比 + 未超量零动作）④ 超量生成 1（Σ>预估 → 额外 plan + AP 发票草稿落库 + overAmount 断言）⑤ 120% 通知 2（seed USER_LIST 模板 → notify 落库 + recipient 断言 + 无模板静默跳过）⑥ 行不存在拒绝 1；`_cases/` 无快照（R1.32 直断言范式，拒绝路径不录快照）。
- **验证基线（全绿）**：`mvn test -pl module-contract/erp-ct-service` **110/110**（98 基线 + 12 新增，Posting/Rebate/ESignature/CreateValidate 等零回归）+ 全量构建通过 + compliance checker **R2c 1394→1399 基线上调登记**（新增 2 per-mutation Processor 5 处 daoProvider.daoFor 同型站点 per-site 证据落 `compliance-baseline.md`，镜像 R1.29/R1.18/19/R6.9 先例；其余 18 规则 actual = baseline 零漂移）。
- **回填**：arm-index P1-RC-074/075 → `done (RC-R1.33)`（含修复落地摘要 + D2-D6 裁决引用）+ roadmap RC-R1.33 → done ✅（含落地摘要）+ owner doc `volume-discount.md` 新增「消耗计费与开票计划生成」实现注记段（生成入口契约 + isInvoiced 锁 + periodSummarize 语义 + 120% 通知事件 + Line 无 invoiceTerm 列结构注记；use-cases L1 未动）+ `docs/logs/2026/08-15.md` 本条目。
- **Deferred（已裁定）**：消耗计费定时调度 job（周期触发归部署/调度 successor，重开事件 = 周期自动化业务流立项）+ 合同级 periodSummarize 聚合循环（D6 行级入口的合同级扩展，successor no）+ 前端批量生成/消耗录入 AMIS 接线（watch-only residual，successor no）+ 生成入口入参化 vs 未来 Line 加 invoiceTerm 列的 ORM 演进（触 ORM ask-first，successor yes = 行级 term 持久化需求立项）。

# Development Log - 2026-08-15

### maintenance 设备 IDLE 恢复分支落地（RC-R1.39，plan `2026-08-15-1605-2`，第一批纯预授权）

- **工作项**：执行 `docs/plans/2026-08-15-1605-2-rc-mr1-r1-39-mnt-equipment-idle-restore-branch.md` 四 Phase（Phase 1 裁决 + Phase 2 落地 + Phase 3 测试 + Phase 4 回填）。落地 finding **P2-RC-061**（§7 A4 纯逻辑修复预授权：UC-MAIN-03 C-IDLE 分支——L1 `use-cases.md:57/63`「COMPLETED(完成, 设备→RUNNING/IDLE)」双分支，L3 `EquipmentStatusLinker.restoreToRunning:38-43` 恒恢复 RUNNING 无 IDLE 分支 + javadoc AI 自标简化非人工批准）。**治理**：纯 support 组件代码逻辑 + 测试，零 ORM 结构/会计过账/删除变更（`changeEquipmentStatus` 签名/守卫/config 门控不变）。
- **D1-D3 决策裁决**（Phase 1 落盘于 plan）：**D1** 前态载体 = 选项 A linker 内部 transient 缓存 `priorStatusCache`（`ConcurrentHashMap<Long,String>` 包级可见 + `MAX_CACHE_ENTRIES=1024` 超限清空 fail-safe）——`linkToUnderMaintenance` 前置 `equipmentBiz.get` 读当前状态**仅 IDLE 缓存**（覆盖写，非 IDLE 移除悬挂残留）/ `linkToDown` **不缓存**（停机恢复恒 RUNNING = §4.3 字面语义零变化）/ `restoreToRunning` 消费（**先 remove 再恢复**防并发重复消费，命中 IDLE→IDLE 未命中→RUNNING）；选项 B（调用方显式传前态）否决破坏 linker 自包含封装；选项 C（ORM 快照列）被 A4 裁决排除登记 successor；**D2** 恢复目标规则 = 前态==IDLE（仅 visit 路径捕获）→ IDLE，其余（RUNNING 来源/停机路径/缓存缺失）→ RUNNING；**D3** 测试语义 = IDLE 断言反转 + 更名 + 三新测试。
- **落地**：`EquipmentStatusLinker` 新增 `priorStatusCache` + `MAX_CACHE_ENTRIES` + 三个 protected helper（`capturePriorStatus`[IDLE put 覆盖写 / 非 IDLE remove] / `consumePriorStatus`[先 remove 再恢复] / `guardCacheSize`[超限 clear fail-safe]）；`linkToDown` 零缓存；类头 javadoc 去除「IDLE→RUNNING 已知简化偏差」失实声明改双分支说明 + 残余风险①-⑤注记（重启/多实例回退 RUNNING + 非事务缓存写残留方向保守 + 悬挂条目消费或覆盖清除 + 超限清空 + @Version 乐观锁兜底）。
- **测试**：`TestErpMntVisitRequestStateMachine`——既有 `testVisitCompleteFromIdleEquipmentRestoresRunning`（A4.2.148 原断言 RUNNING）断言反转 + 更名 `testVisitCompleteFromIdleEquipmentRestoresIdle`（实现前 RED 实现后 GREEN Proof）+ 新增 `testVisitCancelFromIdleEquipmentRestoresIdle`（cancel 路径同语义）/`testVisitCompleteFromDownEquipmentRestoresRunning`（DOWN 非缓存态回退 RUNNING 证实捕获面收敛）/`testRestoreWithoutPriorLinkFallsBackToRunning`（缓存缺失直接 restore 回退 RUNNING 不回退异常，模拟重启语义，经 `@Inject EquipmentStatusLinker` + `CTX=new ServiceContextImpl()` 直接调用）；停机路径回归重跑 `TestErpMntDowntimeAndE2E#testDowntimeRecordSetsDownAndCompleteRestores` + `#testDowntimeTotalMinutesReflectsDuration` 断言零变更（DOWN→RUNNING 行为零变化）。
- **验证基线（全绿）**：`mvn test -pl module-maintenance/erp-mnt-service` **108/108**（105 基线 + 3 新增，0 failures / 0 errors）+ `mvn clean install -DskipTests` 全量 BUILD SUCCESS + compliance checker 零漂移（R2c=1399/R10=9 不变——零新增 daoFor/import 面，EquipmentStatusLinker 既有 `IErpMntEquipmentBiz` 注入）。
- **回填**：arm-index P2-RC-061 → `done (RC-R1.39)`（含修复落地摘要：D1 载体裁决 + 双分支实现 + 残余风险注记）+ roadmap RC-R1.39 → done ✅（含落地摘要）+ owner doc `equipment-integration.md` §3.3 补双分支实现注记（transient 缓存载体 + 残余风险 + ORM 快照列 successor）+ `docs/logs/2026/08-15.md` 本条目。
- **Deferred（已裁定）**：transient 缓存载体残余风险（重启/多实例丢失回退 RUNNING = 现状已接受行为 + 非事务缓存写残留方向保守，watch-only residual，successor = 持久化前态快照列 `ErpMntEquipment.preMaintenanceStatus` 需求立项后 ask-first）+ 「取决于排产」跨域排产协调判定（out-of-scope，属 P1-RC-068/RC-R1.76）+ ORM 快照列备选（A4 裁决排除，successor 同前）+ 停机路径前态感知（IDLE 来源停机恢复恒 RUNNING = §4.3 字面语义，watch-only，与 ORM 快照列 successor 同源）。

### logistics 调度接线族落地（RC-R1.37 + RC-R1.38，plan `2026-08-15-1605-1`，第一批纯预授权）

- **工作项**：执行 `docs/plans/2026-08-15-1605-1-rc-mr1-r1-37-38-logistics-job-wiring-family.md` 五 Phase（Phase 1 裁决 + Phase 2 R1.37 落地 + Phase 3 R1.38 接线 + Phase 4 测试 + Phase 5 回填）。落地 finding **P1-RC-084**（L1 UC-LOG-01「超过 24 小时未确认的 DRAFT 发运单触发升级通知」——`use-cases.md:15`，L3 零 escalation 业务命中 + 零 config key + 零 scheduler/job + 零 notify 调用）+ **P1-RC-085**（L1 UC-LOG-03「定时轮询间隔可配置（默认 4 小时）」——`use-cases.md:39`，L3 `erp-log.tracking-poll-cron` dead config 零生产消费 + scanForPolling 仅 manual @BizMutation，A4.2.175 运行时证实 DISPATCHED 滞留）。**治理**：调度接线 + config + notify（job bean + job.yaml + `ErpLogConfigs` 新键 + `IErpSysNotificationBiz` 派发 + seed 模板），**零 ORM 结构/会计过账/数据删除变更**（P1-RC-083 重复发运 DB UK 与 P1-RC-086 容量预约实体为独立越界行 RC-R1.83 / RC-R1.84，不在本行）。
- **D1-D4 决策裁决**（Phase 1 落盘于 plan）：**D1** tracking-poll job 形态 = 选项 A 简单 job bean（否决 batch-task REQUIRES_NEW：scanForPolling 是全量语义单入口，batch 化需重构 per-shipment 轮询方法超范围 + R10 基线漂移；外部网关调用超时风险与 manual 入口既有已接受行为一致 watch-only）；roadmap 行「nop-batch job.yaml 注册」按意图解释 = job.yaml 注册 + 批量调 scanForPolling；**D2** draft-escalation 接收人 = USER_LIST `${submitterUserId}`（shipment.createdBy 发货员——物流域角色权限边界未设计 roles-and-permissions.md:195，ROLE 路由无落地点；「通知物流主管」登记 successor）；**D3** 通知模板 seed = 选项 A 新增 seed 行（`log.draft-escalation` + USER_LIST，ID 7201 三方言 SQL，P1-RC-084 验收含「触发升级通知」seed 使运行时可达可断言）；**D4** 超时基准 = updateTime（对齐 R1.4/R1.34 超时先例）+ `dateTimeBetween(updateTime, epoch, cutoff)` 窗口（XMeta 白名单无 lt）+ SCAN_LIMIT=200。
- **落地**：`ErpLogConfigs` 增 `erp-log.draft-escalation-hours`（默认 24）/+ `draft-escalation-cron`（默认 `0 30 1 * * ?`）+ CONFIG_TRACKING_POLLING_CRON javadoc 双消费点注记（dead config 转活跃）；`ErpLogConstants.NOTIFY_EVENT_DRAFT_ESCALATION`=`log.draft-escalation`；`ErpLogDraftEscalationJob`（新建，`app.erp.log.service.job`——execute() 无参 + cron 空值跳过 + runDraftEscalation[ormTemplate.runInSession + IErpLogShipmentBiz.findList DRAFT + dateTimeBetween(updateTime) + limit 200 + 逐单 context{shipmentId, shipmentCode, submitterUserId=createdBy, elapsedHours} 经 notificationBiz.notify 派发 + try/catch WARN 逐条失败隔离]）+ `ErpLogTrackingPollJob`（新建——cron 空值跳过 + ormTemplate.runInSession 包裹调 `IErpLogShipmentBiz.scanForPolling` 一次 + LOG.info 条数 + 顶层 try/catch）；`erp-log-draft-escalation.job.yaml` + `erp-log-tracking-poll.job.yaml`（app-erp-all 注册：enabled 默认 false + cronExpr `@cfg:<业务键>|默认` + invoker bean/execute + jobGroup erp-log）；app-service.beans.xml 注册 2 job bean；seed 模板行三方言 SQL（ID 7201）；`TestErpAllJobYamlLoading` 24→26 两处硬编码同步。
- **测试**：`TestErpLogDraftEscalationJob` 6 组全绿（① 超阈值通知落库 + recipient==createdBy + status=SENT ② 未超阈值零动作 + 状态不变 ③ cron 空值跳过 ④ 单条失败隔离[ThrowingDraftEscalationJob 子类覆写 escalateShipment 抛异常——异常单不阻断其余两条正常派发] ⑤ 无 ACTIVE 模板静默跳过 ⑥ config 键绑定断言）+ `TestErpLogTrackingPollJob` 3 组全绿（① 调度路径双运单 DISPATCHED→IN_TRANSIT→DELIVERED + onDelivered 运费过账联动[SETTLED + FREIGHT 凭证回链 + actualDeliveryDate] ② cron 空值跳过 ③ 单条 onDelivered 失败隔离[预置 SETTLED 运单触发 ERR_LOG_SHIPMENT_ALREADY_DELIVERED 不中断正常单 SETTLED+凭证]）；erp-log-service **48 tests 全绿**（39 基线 + 9 新增，含 TestErpLogShipmentStateMachineMatrix 12 零回归）。
- **验证基线（全绿）**：`mvn test -pl module-logistics/erp-log-service` 48/48 + `mvn test -pl app-erp-all -Dtest=TestErpAllJobYamlLoading` 26/26 + 全量 `mvn clean install -DskipTests` BUILD SUCCESS + compliance checker 零漂移（job bean 全经 IBiz 注入零新增 daoFor 站点 + R1.4 job 无 REQUIRES_NEW，R2c=1399/R10=9 不变）。注：全 reactor `mvn test` 3 项**预存在**失败不变（`TestAuthSeedLoadingProof` NPE[nop-entropy e5ee02b40 lazy-property，ask-first] + `ErpAllWebPagesTest`/`ErpAllFluxPagesTest` cell-not-prop[mfg E4.1 452e418d0 materialBand]，见 known-good-baselines.md + docs/bugs/ 双注记，与 RC-R1.37/38 无关）。
- **回填**：arm-index P1-RC-084 → `done (RC-R1.37)` + P1-RC-085 → `done (RC-R1.38)`（含修复落地摘要 + A4.2.175 运行时确认留存）+ roadmap RC-R1.37 / RC-R1.38 → done ✅（含落地摘要）+ owner doc `state-machine.md` §8 补 DRAFT 24h 升级实现注记（config 键/job/notify 事件/接收人裁决/主管路由 successor）+ `carrier-integration.md` §5.4 补轮询接线实现注记（cron 键转活跃 + job 接线 + max-days 未消费登记）+ `docs/logs/2026/08-15.md` 本条目。
- **Deferred（已裁定）**：重复升级通知（DRAFT 持续滞留每日重复派发——「已通知」标记列触 ORM ask-first，watch-only residual，successor yes）+ 主管级升级路由（D2 接收人——物流角色基础设施落地后切换 ROLE 模板 + 上级链解析，watch-only residual，successor yes）+ 外部网关调用长事务风险（D1 选项 A——scanForPolling 内外部 trackShipment 调用与 manual 入口既有已接受行为一致 + job 低频率 + gateway 超时/重试既有兜底，watch-only residual，successor no）。
### maintenance 额外故障另开新请求编排落地（RC-R1.31，plan `2026-08-15-0456-1`，第一批纯预授权）

- **工作项**：执行 `docs/plans/2026-08-15-0456-1-rc-mr1-r1-31-mnt-report-additional-fault.md` 四 Phase。落地 finding P1-RC-069（L1 UC-MAIN-07 C「另开新维护请求(OPEN)处理额外故障」——`use-cases.md:129`，L3 无「维护访问 IN_PROGRESS → 另开新维护请求(OPEN)」编排方法 grep 零命中）。**治理**：纯 BizModel/Processor 代码逻辑 + 测试，零 ORM 结构/会计过账/删除变更（visit remark/result 载体 + request 建单字段均已存在）。
- **E1 建单载体与 code 唯一性（终裁：IBiz.save data map + 毫秒时间戳后缀）**：载体经 `IErpMntRequestBiz.save(Map, context)`（镜像 `generateResponsiveVisit:33-42` 范式）；code `"REQ-VST-{visitId}-{yyyyMMddHHmmssSSS}"`——固定 code 二次上报必撞 `UK_MNT_REQUEST_CODE`（visit 保持 IN_PROGRESS 可合法多次上报），秒级后缀同秒双报有碰撞风险，毫秒级唯一性充分（长度 45 ≤ 50）；冻结时钟核查 = `ThreadLocalFrozenClock.currentTimeMillis` 委托系统真实时钟仅冻日期，快照安全核查 = autotest 框架对新建实体 prop 以 `@var:` 掩码（`TestErpMntVisitCrudSmoke` 先例实证）；requestedBy = context userId 数值解析失败回退 visit.assignedTo。
- **E2 守卫错误码（复用 `ERR_INVALID_VISIT_STATUS_TRANSITION`）**：经 `illegalVisitTransition(visit, from, IN_PROGRESS)` 复用既有码（参数 visitCode/currentStatus/expectedStatus 可追溯），备选新增码拒绝（错误码膨胀 + 与既有守卫码重复，对齐 R1.30 E1 复用先例）。
- **E3 remark 追加（追加语义 (b)）**：visit.remark += `"[额外故障] " + description`，精度 1000 守卫（既有内容优先，截断新增）；`remark` 入参（`@Optional`，GraphQL 层可省略）归新 request.remark 不混入 visit。
- **E4 result/工时范围声明**：不写 visit.result（complete 语义执行结果 dict）+ 不写 totalMinutes（complete 按 startTime→endTime 计算，IN_PROGRESS 期写入会被覆盖），工时记录义务归 complete 流程。
- **落地**：`ErpMntVisitReportAdditionalFaultProcessor`（新建，扩展 `AbstractErpMntVisitProcessor` + 注入 `IErpMntRequestBiz`，protected step `doAppendVisitRemark`/`doCreateRequest`）+ `IErpMntVisitBiz` 契约（priority/remark `@Optional`——GraphQL 参数省略放行，`nop.err.graphql.field-null-arg`/`field-empty-arg` 实测校准）+ `ErpMntVisitBizModel` @BizMutation 委托 + `app-service.beans.xml` 注册；错误码零新增（E2 复用）。
- **测试**：`TestErpMntVisitReportAdditionalFault` 6 组全绿——① IN_PROGRESS 成功建单（OPEN/equipmentId/description/priority 缺省 NORMAL/requestedBy 回退 201/code 前缀 + 长度 ≤50 断言 + visit 保持 IN_PROGRESS + result/totalMinutes null）② DRAFT/SCHEDULED/COMPLETED/CANCELLED 参数化拒绝 + `ERR_INVALID_VISIT_STATUS_TRANSITION` + 消息含 currentStatus + 零落库 ③ remark 追加保留既有记录 ④ visit 不存在 `ERR_VISIT_NOT_FOUND` ⑤ 重复上报两 request 落库 code 唯一（E1 方案 A 实证）⑥ 新 request 接 accept→startRepair 闭环 + priority/remark 入参断言；`_cases/` 快照目录 6 个新方法自动生成。
- **验证基线（全绿）**：`mvn test -pl module-maintenance/erp-mnt-service` **105/105**（99 基线 + 6 新增）+ `mvn clean install -DskipTests` 全量 BUILD SUCCESS + compliance checker 零漂移（`@Inject IErpMntRequestBiz` 类型注入零 daoFor 面）。
- **回填**：arm-index P1-RC-069 → `done (RC-R1.31)`（含修复落地摘要）+ roadmap RC-R1.31 → done ✅（含落地摘要）+ owner doc `state-machine.md §4` 补 reportAdditionalFault 实现注记（守卫/建单语义/测试证据/P1-RC-067 successor 边界声明）。
- **Deferred（已裁定）**：P1-RC-067 visit→request 结构化联动（requestId 列 + visit COMPLETED → request COMPLETED 自动联动，触 ORM ask-first，successor yes = RC-R1.75 启动时）+ 前端 AMIS reportAdditionalFault 一键按钮接线（watch-only residual，A4.2.154 半自动流程可达现状保持，successor no）+ 同毫秒双报残余窗口（E1 文档化残余，successor no）。

### maintenance 排程冲突人员维度落地（RC-R1.30，plan `2026-08-15-0320-3`，第一批纯预授权）

- **工作项**：执行 `docs/plans/2026-08-15-0320-3-rc-mr1-r1-30-mnt-schedule-conflict-personnel-dimension.md` 四 Phase。落地 finding P1-RC-066（L1 UC-MAIN-09 B-人员维度——`use-cases.md:157`「校验: 设备/人员 同时段是否已有排程」，L3 `checkScheduleConflict` 仅设备维度无 assignedTo 维度）。**治理**：纯 BizModel/Processor 代码逻辑 + 测试，零 ORM 结构/会计过账/删除变更（assignedTo 列已存在）。
- **E1 裁决（选项 A）**：复用 `ERR_VISIT_SCHEDULE_CONFLICT`（L1 单一冲突语义 + 消费者 grep 仅 define 点/抛点/测试断言零前端 i18n 依赖 → 无区分维度需求）；共享义务落地 = 消息模板双维度化「设备 {equipmentId}/执行人 {assignedTo} …」+ define 参数表注册既有 ARG_ASSIGNED_TO + 设备维度查询先于人员维度（`testVisitScheduleConflict` 双维度命中时设备先抛，断言保持）。
- **落地**：`ErpMntVisitScheduleProcessor.checkScheduleConflict` 双维度化——`checkEquipmentDimensionConflict`（:63-76）+ `checkPersonnelDimensionConflict`（:78-94，assignedTo null 守卫，eq/in 过滤 + 排除自身循环）独立查询独立判定，统一抛点 `throwScheduleConflict`（:96-102 四参数齐备：visitCode/equipmentId/assignedTo/conflictVisitCode）；`ErpMntErrors.java:45-47` 模板双维度改写。
- **测试**：`TestErpMntVisitRequestStateMachine` 新增 5 组全绿——① 同人不同设备同日 → 拒绝（含 `resp.getMsg().contains(被冲突 code)` 追溯断言）② 同设备不同人 → 设备维度拒绝 ③ 同人不同日 → 放行 ④ CANCELLED 同伴 → 放行 ⑤ DRAFT 同伴 → 首排程放行/首 SCHEDULED 后再排程拒绝（动态计数证据）；seedVisit 扩展 5/6 参重载；快照录制于 `_cases/` 5 个新方法目录；既有 `testVisitScheduleConflict` 零改动保持。
- **验证基线（全绿）**：`mvn test -pl module-maintenance/erp-mnt-service` **99/99**（TestErpMntVisitRequestStateMachine 15/15）+ 全量构建 + compliance checker 零漂移（纯查询逻辑无新 daoFor/import 面）。
- **回填**：arm-index P1-RC-066 → `done (RC-R1.30)`（含修复摘要）+ roadmap RC-R1.30 → done ✅（含落地摘要）+ owner doc `state-machine.md §4` 补双维度实现注记（查询语义/错误码/测试证据/P2-RC-060 边界声明）。
- **Deferred（已裁定）**：P2-RC-060 warn/config 冲突模式（独立 P2 watch-only，本行仅补人员维度，`erp-mnt.schedule-conflict-mode` config key 不在本行）+ 人员同日多 visit 软排程/优先级策略（optimization candidate，均 successor no）。

### master-data supplier 价格解析 SPI 生产实现落地（RC-R1.29，plan `2026-08-15-0320-2`，第一批纯预授权）

- **工作项**：执行 `docs/plans/2026-08-15-0320-2-rc-mr1-r1-29-md-supplier-price-resolver-spi.md` 四 Phase。落地 finding P1-RC-063（L1 UC-MD-03 ④「价格表（供应商专属）」层——`IErpMdSupplierPriceResolver` 无生产实现致采购价格表层 no-op 恒落默认档，`ErpMdMaterialSkuBizModel.supplierPriceResolver:62-64` @Inject @Nullable 注入 null → `resolvePrice:151` 分支永假）。**治理**：纯 SPI 实现 + beans.xml 注册 + 测试，零 ORM 结构/会计过账/删除变更（`ErpPurSupplierPriceList` 表已存在，orm.xml:384-427）。
- **决策裁决**：**P1** priority 命中裁决 = orm.xml:399 权威约束「数字小优先」+ 同档 unitPrice 低者（采购保守语义）；**U1** 单位匹配 = `uoMId == sku.uoMId` 精确匹配（多单位 SKU 按单位取价，null 宽放到仅 materialId）；**U2** 货币维度 = 不参与匹配（SPI 签名无 currencyId，登记 Deferred But Adjudicated 边界残余）；**P2** 跨域消费面 = grep census `module-purchase` main 对 `resolvePrice`/`IErpMdMaterialSkuBiz` 零命中 → 选项 B（采购取价接线 successor，与 A1.15-A1.17 协同行对接）。
- **落地**：`ErpPurSupplierPriceResolver.java`（`module-purchase/erp-pur-service/.../service/support/`，对齐 ErpSalCustomerPriceResolver 包路径）——`resolveSupplierPrice(sku, partnerId)`：防御 null → `daoProvider.daoFor(ErpPurSupplierPriceList.class).findAllByQuery`（filter supplierId/materialId/isActive + U1 uoMId 条件）→ 效期窗口内存裁决（null 开放边界）→ P1 裁决取最优（priority 小优先 + 同档 unitPrice 低者）→ 返回 unitPrice / 无命中 null；`module-purchase app-service.beans.xml` 注册（bean id = FQCN + `daoProvider` `<ioc:inject type="io.nop.dao.api.IDaoProvider"/>`，无 `ioc:type`——`BeanFinder.findByType` 按 isAssignableFrom 类型注入，镜像 `ErpSalCustomerPriceResolver` :173-181 先例；根元素补 `xmlns:ioc` 命名空间）。
- **测试**：`TestErpPurSupplierPriceResolver` 14 组全绿——12 SPI 单测（命中/supplier 不符/material 不符/inactive/效期外/priority 小优先/同档低者/效期边界/开放边界/末日命中/防御 null/uoMId 精确匹配含 null 宽放）+ 2 GraphQL 集成（seed material+sku+价格表行 → `ErpMdMaterialSku__resolvePrice` 经真实 beans.xml 注册返回价格表层价 7.77 非默认档 10.0000，运行时断言注入非 null + 无命中回退默认档 10.0000）。
- **验证基线（全绿）**：`mvn test -pl module-purchase/erp-pur-service` **308/308**（294 基线 + 14 新增）+ `mvn test -pl module-master-data/erp-md-service` **143/143** 零回归 + `mvn clean install -DskipTests` 全量 BUILD SUCCESS + `bash docs/audits/nop-compliance-checker.sh`（**R2c 1393→1394 基线上调登记**——SPI 实现类 1 处 daoFor per-site 证据落 `compliance-baseline.md`，镜像 R1.2/R1.23/R1.27 先例 + project-context 已知失败模式 #1）。
- **回填**：arm-index P1-RC-063 → `done (RC-R1.29)`（含修复落地摘要）+ roadmap RC-R1.29 → done ✅（含落地摘要）+ owner doc `sku-multi-unit.md §价格优先级` 补 supplier 层实现注记（SPI 生产实现 + 注册 + 查询语义 + U1/U2 裁决 + 测试证据 + P2 消费面）+ purchase README 跨域协作表补价格表层取价行（价格表层已就绪 + 采购取价接线 successor）。
- **Deferred（已裁定带 successor）**：purchase 侧 resolvePrice 取价消费面（P2 选项 B 零消费，重开事件 = 采购取价接线行启动，successor yes）+ 货币维度精确匹配（U2，SPI 签名扩展跨域契约变更，successor no）+ 价格表阶梯价/批量价联动（optimization candidate，successor no）。

### cs 目录建单必填校验落地（RC-R1.28，plan `2026-08-15-0320-1`，第一批纯预授权）

- **工作项**：执行 `docs/plans/2026-08-15-0320-1-rc-mr1-r1-28-cs-catalog-form-required-validation.md` 四 Phase。落地 finding P1-RC-060（L1 UC-CS-10 异常①「表单必填项缺失 → 禁止提交」——requestFormConfig schema 未驱动校验，`buildTicketData` 盲拷 formData keys）。**治理**：纯 BizModel/Processor 代码逻辑（解析 JSON schema + 必填校验 + 错误码 + 测试），零 ORM 结构/会计过账/删除变更（requestFormConfig 列已存在）。
- **接线点裁决（Decision A）**：`validateRequiredFormFields` 独立 protected step，在 `requireCatalogItem`/`validateCatalogItemUsable` 之后、`buildTicketData` 之前——校验失败在副作用（权益扣减 `applyEntitlementToTicketData` + `ticketBiz.save`）之前拒绝，工单不落库 + 权益不扣减。
- **校验基准裁决（Decision B1 A）**：formData 原始键值（fallback 前）——schema 标必填而客户未提交 → 拒绝，subject/priority 缺省回退是服务端兜底不满足必填语义；`urgency→priority` 按原始键 `urgency` 校验。
- **容错边界裁决（Decision T1）**：requestFormConfig null/空白/非法 JSON/fields 非数组 → 跳过 + LOG.warn（配置数据质量问题不阻断建单，对齐 fulfillmentBiz try/catch WARN 降级范式）；`required` 非布尔 true → 该字段不视为必填；空串/空白值视为缺失。
- **落地**：`ErpCsErrors.java` 新增 `ERR_CATALOG_FORM_REQUIRED_MISSING`（`erp.err.cs.catalog-item.form-required-missing`，参数 catalogItemId + fieldKey + label 若可得，对齐 `catalog-item.*` 错误码族）；`ErpCsServiceCatalogItemCreateFromCatalogProcessor.java` 接线 protected step（`JsonTool.parseBeanFromText` 解析，复用 `CannedResponseRenderer:67` 先例；`StringHelper.isBlank` 判空白）。
- **测试**：`TestErpCsServiceCatalog` 新增 6 组全绿——① 必填缺失拒绝 + 工单零落库 + 权益零扣减（seed 活跃 PAY_PER_TICKET 权益，usedTickets 保持 0 证明未达扣减）② 必填满足放行（含 urgency→priority 映射）③ requestFormConfig null 跳过 ④ 非法 JSON 跳过 ⑤ 空白视为缺失拒绝 ⑥ 非必填字段缺失放行。
- **验证基线（全绿）**：`mvn install -DskipTests -pl module-cs/erp-cs-service` BUILD SUCCESS + `mvn test -pl module-cs/erp-cs-service`（122/122，TestErpCsServiceCatalog 15/15）+ `bash docs/audits/nop-compliance-checker.sh`（actual == baseline 零漂移：R1d=14/R2a=34/R2b=230/R2c=1393/R2d=34/R12 69/66/40）。
- **回填**：arm-index P1-RC-060 → `done (RC-R1.28)` + roadmap RC-R1.28 → done ✅ + owner doc `service-catalog.md §9.4` 补服务端必填校验实现注记（校验时机/基准/错误码/容错语义/数据治理声明/schema key 映射集约束/测试证据）。
- **Deferred（已裁定带 successor）**：生产/种子 requestFormConfig 数据治理（required schema 维护，重开事件 = 门户自助前端上线或目录数据治理批次启动，successor no）+ 前端动态表单渲染（门户自助前端 successor，重开事件 = 门户上线随数据治理批次执行）。
