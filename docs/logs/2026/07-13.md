# Development Log - 2026-07-13

### 2026-07-13（制造委外引擎：委外单生命周期 + MRP 释放 + GL 过账 — plan 2026-07-13-0455-1）

- 执行 `docs/plans/2026-07-13-0455-1-manufacturing-subcontracting-engine.md`，全 5 Phase 完成。源：`docs/audits/2026-07-12-1504-competitive-levers-implementation-audit.md` 裁决 M-1（委外 successor）。
- **Phase 1（字典 / 过账类型 / COA 契约）**：`erp-mfg/subcontract-status` 字典 3→8 态（DRAFT/SUBMITTED/APPROVED/ISSUED/RECEIVED/COMPLETED/CANCELLED/REJECTED）；`ErpFinBusinessType` 新增 SUBCONTRACT_ISSUE(502)/RECEIPT(503)/FEE(504) + `erp-fin/business-type` 字典项；COA 补 1408 委外物资科目。
- **Phase 2（BizModel + Processor）**：`ErpMfgSubcontractOrderProcessor`（protected step 编排，镜像 WorkOrderProcessor）+ `ErpMfgSubcontractOrderBizModel`（thin Facade）+ `ErpMfgSubcontractOrder.xbiz`（审批轴委托 Processor）+ IErpMfgSubcontractOrderBiz 接口；三段动作 issueMaterials(APPROVED→ISSUED)/receiveFinished(ISSUED→RECEIVED)/postProcessingFee(RECEIVED→COMPLETED)；ErrorCode 守门 6 个。
- **Phase 3（GL 过账）**：`SubcontractPostingDispatcher`（镜像 ManufacturingIssuePostingDispatcher）+ 3 个 AcctDocProvider（Issue/Receipt/Fee，科目分解 Dr/Cr 对齐设计 subcontracting.md）；beans 注册 app-service.beans.xml。
- **Phase 4（MRP 释放）**：`MrpReleaseService.releaseSubcontractRequest` config-gated（`erp-mfg.subcontract-release-enabled` 默认 false）；生成 APPROVED 委外单 + 幂等门控。
- **Phase 5（测试 + owner-doc）**：`TestErpMfgSubcontracting` 3 测试（全链生命周期 + 凭证科目分解断言 + 非法迁移守卫 + MRP 释放幂等）；`subcontracting.md`/`state-machine.md` 实现偏离补注 + successor 触发条件。
- **验证（full-green verification）**：`mvn clean install -DskipTests` 154 模块 BUILD SUCCESS；mission verify 期间 `mvn test` 全 reactor BUILD SUCCESS（含新增 `TestErpMfgSubcontracting` 3/3）；`mvn test -pl module-manufacturing/erp-mfg-service` 106 测试 0 失败；`mvn test -pl module-finance/erp-fin-service` 185 测试 0 失败。
- Plan Status: active → completed；全 Phase [x] + Status completed。结束审计由独立子代理执行（见计划 Closure Audit Evidence）。

### 2026-07-13（View.xml 表单布局全面优化：ID 隐藏 + 分组折叠 + 查询表单 — plan 2026-07-12-1500-1）

- 执行 `docs/plans/2026-07-12-1500-1-view-form-layout-overhaul.md`，全 3 Phase 完成。源：`docs/analysis/view-form-layout-gap-analysis.md` §4-6。
- **Phase 0（codegen 模板修复）**：
  - `nop-entropy/nop-frontend-support/nop-web/.../view-gen.xlib:GenForm` 跳过条件扩展：`if(prop.name == 'id' && prop.tagSet?.contains('seq-default')) continue;`
  - `nop-entropy/nop-kernel/nop-codegen/.../web-gen.xlib:GenLayout` 同步增加 seq-default id 过滤（实际 codegen 路径，gap analysis 原误认为 GenForm 为根因）
  - 修复前置 nop-entropy bug：3 个 orm-web codegen 模板（`view.xml.xgen` / `main.page.yaml.xgen` / `picker.page.yaml.xgen`）中 `<c:when>` / `<c:otherwise>` 应为 `<when>` / `<otherwise>`（`ChooseTagCompiler` 仅匹配无前缀标签名，`web-gen.xlib:40-51` 已用正确范式）
  - `mvn clean install -DskipTests` 触发全域 codegen 增量重生成，5+ 域抽样 `_gen/_*.view.xml` form layout 无 `id[ID]` 行确认
- **Phase 1（核心 5 域 22 实体）**：purchase 6（Order/Receive/Invoice/Payment/Return/Requisition）+ sales 6（Order/Delivery/Invoice/Receipt/Return/Quotation）+ inventory 3（StockMove/StockBalance/TransferOrder）+ finance 3（Voucher/VoucherLine/Reconciliation）+ master-data 4（Material/Partner/Subject/Organization）。每实体 view/edit 表单 3-5 分组（baseInfo 展开 + amount 展开 + approval^折叠 + audit^折叠），query 表单填充 4-7 查询字段 + filterOp（code like / date date-between），大表单（≥20 字段）设 `size="lg"`。
- **Phase 2（扩展域 17 实体）**：manufacturing 3（WorkOrder 43 字段 5 组/Bom/JobCard）+ assets 2（Asset/DepreciationSchedule）+ crm 2（Lead 40 字段 6 组/Activity）+ hr 1（Employee 38 字段 6 组）+ projects 2（Project/ProjectSettlement）+ logistics 1（Shipment 40 字段 6 组）+ quality 1（Inspection）+ maintenance 1（Visit）+ cs 1（Ticket，保留 `__kbSuggestion` custom cell，移除 `id[ID]`）+ aps 1（Schedule）+ b2b 1（EdiDoc）+ contract 1（Contract）。
- **验证（全绿）**：`mvn clean install -DskipTests` 154 模块 BUILD SUCCESS；`mvn test` 全 reactor BUILD SUCCESS；39 实体 view.xml `xmllint --noout` 全 well-formed。
- nop-entropy codegen 变更日志记录于 `nop-entropy/ai-dev/logs/2026/07-13.md`（按 AGENTS.md 规则 8 双日志要求）。
- Plan Status: active → completed；全 Phase [x] + Status completed；Closure Gates 全 [x]。


### 2026-07-13（Processor private → protected 全面开放，Delta 纯文件定制就绪 — plan 2026-07-12-1400-1）

- 执行 `docs/plans/2026-07-12-1400-1-processor-private-to-protected-for-delta-customization.md`，Phase 1+2 全部完成。源：`docs/analysis/productization-readiness-analysis.md` §6.1 + §7.2。
- **Phase 1（Finance + Projects，9 文件 / 35 方法 + 1 内部类型）**：`ErpFinPostingProcessor`(5)、`ErpFinAccountingPeriodProcessor`(10 + `TbAgg`)、`ErpFinBudgetScenarioProcessor`(3)、`ErpFinEmployeeAdvanceProcessor`(3)、`ErpFinExpenseClaimProcessor`(2)、`ErpFinNotesReceivableProcessor`(1)、`ErpFinNotesPayableProcessor`(1)、`ErpFinBadDebtProcessor`(1)、`ErpPrjProjectSettlementProcessor`(9) 全部 `private` → `protected`（static 方法 → `protected static`）。
- **Phase 2（Assets + Inventory + Purchase + Sales + CRM，15 文件 / 22 方法 + 1 内部类型）**：9 Assets Processor(`AssetCapitalization`/`Merge`/`Inventory`/`Split`/`Cip`/`ValueAdjustment`/`DepreciationSchedule`/`Disposal`/`Maintenance`)、3 Inventory(`StockMove`/`OwnershipTransfer`/`LandedCost`)、`ErpPurReceiveProcessor`、`ErpSalDeliveryProcessor`、`ErpCrmConversionProcessor`(`Setter` 内部接口)。全量 `rg` 扫描确认 24 Processor 文件零 `private` 方法残留。
- **Non-Goal 保留**：`ErpFinPostingProcessor.PostingRun` 的 `private` 构造器（有意工厂方法约束 `forPost()`/`forReverse()`，非 Delta 阻塞）。
- **修复性质**：纯可见性拓宽（`private` → `protected`），零行为/契约/ORM 变更。`ErpPrjProjectSettlementProcessor.nz` 实例方法保持非 static（计划 note 误标 "去掉 static"，原代码本就无 static）。
- **验证（全绿）**：`mvn clean install -DskipTests` 154 模块 BUILD SUCCESS；`mvn test` finance+projects+assets+inventory+purchase+sales+crm 代表域全绿无回归。
- 文档对齐：`docs/analysis/productization-readiness-analysis.md` §6.1 标注"已修复（2026-07-13）"状态横幅。Plan Status: active → completed；Phase 1+2 全部 [x] + Status completed。结束审计待独立子代理执行。

### 2026-07-13（日期参数报表下载/渲染生产修复 — plan 2026-07-12-1321-3）

- 执行 `docs/plans/2026-07-12-1321-3-report-date-param-download-fix.md`，Phase 1+2 全部完成。解除 0413-1 Deferred「日期参数报表空/默认日期下载真实可用时」。
- **根因**：12 日期参数报表经 AMIS `input-date` 将日期序列化为裸 Unix 时间戳（秒）/未填字段序列化为空串 `""` → 后端 6 报表 BizModel 的 `asDate` strict-parse `LocalDate.parse(v.toString())` 抛 `DateTimeParseException` → 下载/渲染失效。此前仅测试层 `page.route` 归一化 workaround 绕过。
- **Phase 1（后端宽容解析）**：6 报表 BizModel（fin/mfg/ast/mnt/prj/qa）的 `asDate`（空串→null / 10·13 位时间戳→`LocalDate.ofInstant(Instant.ofEpoch{Second,Milli}, ZoneOffset.UTC)` / ISO→`LocalDate.parse`）+ `asLong`（空串→null）重写。每文件加 `import java.time.Instant` + `ZoneOffset`。
- **Phase 2 scope correction（执行期发现）**：移除的 `page.route` workaround 归一化**所有** `""→null`（非仅日期键），故暴露 2 个**非日期参数报表** BizModel 同类空串漏洞：`ErpInvReportBizModel.asLong`（`Long.valueOf("")`→NumberFormatException）+ `ErpMdReportBizModel.asString`（返回 `""` 致 `loadMaterials("")`/`buildPartnerListDataset("")` 加 `=""` 过滤匹配空集）。两处与 B1 同源，按 Phase 1 范式同步修复（`s.trim().isEmpty()→null`）。Non-Goal 边界修正：空串解析宽容属后端解析修复（非聚合逻辑变更）。
- **Phase 2（前端 + 测试 workaround 移除）**：12 报表 page.yaml 的 `input-date` 追加 `valueFormat: "YYYY-MM-DD"`（23 匹配）；移除 `tests/e2e/reports/_helper.ts` + `tests/e2e/visual/_helper.ts` 的 `page.route` 日期归一化段。
- **关键决策**：`valueFormat` 对 `${NOW()}` 程序化默认值的格式化行为不可靠（AMIS 版本相关），但 Phase 1 后端宽容解析已覆盖裸时间戳路径，故 `ar-ap-aging` 的 `value:` 不改——后端兜底为残留风险唯一防线，已验证全绿。
- **验证（全绿）**：`mvn clean install -DskipTests` 154 模块 BUILD SUCCESS；`npx playwright test reports.download.spec.ts reports.amis-download.spec.ts visual/reports.visual.spec.ts` **82 passed**（12 日期参数 + 12 非日期参数报表真实 AMIS 序列化，0 failures，9.9m）。
- 文档对齐：`e2e-runbook.md`「下载功能」已知限制段标 ~~已解除~~ + 两处 helper 编排 prose（:417/:466）移除 `page.route` 描述；`backlog/README.md` 增 1321-3 行 ✅ done。
- 独立结束审计（ses_0a879ddbeffeBVbKQieu7axbF0）VERDICT: PASS（8/8）。非阻塞观察：crm/hr BizModel 保留旧 `asLong` strict 模式（范围外，潜在风险归 Follow-up）。Plan Status: active → completed。

### 2026-07-13（Finance 凭证行精确数值断言 + 自动核销浏览器层 E2E — plan 2026-07-12-1321-2）

- 执行 `docs/plans/2026-07-12-1321-2-finance-voucher-numeric-auto-recon-e2e.md`，Phase 1+2 全部完成。解除 0413-2 两项 Deferred（坏账核销/计提凭证行精确数值断言 + runAutoReconciliation 三策略浏览器层 E2E）。
- **Phase 1（凭证行数值断言）**：`fin-bank-recon` 叠加 NORMAL Dr 1002=100/Cr 2240OTHER=100 + REVERSAL 同向取负（Dr 1002=-100/Cr 2240OTHER=-100）；`fin-bad-debt` 叠加 writeOff Dr 1231=100/Cr 1122=100 + recovery Dr 1122=100/Cr 1231=100 + provision RESERVE Dr 6701/Cr 1231（金额动态计算=requiredProvision−allowanceBalance，单 schema 确定性）。红冲凭证行同向取负范式（buildReversalDraft dcDirection 不变金额取负）经 p2p-reverse.spec.ts/quality-ncr-reverse 既有验证确认。
- **Phase 2（自动核销三策略 E2E）**：新建 `fin-auto-recon.action.spec.ts`（FIFO/BY_AMOUNT/BY_RATIO 各 1 用例，自包含 partner+OPEN AR_INVOICE+RECEIPT 对→runAutoReconciliation→核销单自动 create+post+双方辅助账 SETTLED）；`playwright.config.ts` webServer JVM args 追加 `-Derp-fin.auto-reconcile=true`。
- **后端 flush 修复**（`ErpFinReconciliationBizModel.runAutoReconciliation` :191-192）：`create(...)` 后 `post(...)` 前缺 `orm().flushSession()`——`create` 的 `saveEntity` 仅入队 INSERT（Nop ORM 无 auto-flush-before-query），`post` 的 `loadLines` SQL SELECT 看不到未 flush 的行→`lines.isEmpty()`→`ERR_RECONCILIATION_NOT_FOUND`。镜像既有 `flushBeforeBalance()` 范式（:349-351，post 内 settler→partnerBalanceUpdater 间同一 flush 模式）补一行。单元测试通过因 `JunitAutoTestCase` harness 层 session 管理不同于 Quarkus 生产运行时（identity map 一致但 query 路径不同）。计划 Non-Goals 声称零生产代码变更，实际因 session 可见性 gap 需一行 flush 修复（非业务逻辑变更）。
- **验证（全绿）**：`mvn clean install -DskipTests` 154 模块 BUILD SUCCESS；Phase 1 specs 6/6 passed + Phase 2 spec 3/3 passed + business-actions 全套件 57/57 passed（无回归）+ finance 看板/ar-ap-aging 报表 4/4 passed（基线无漂移）。`PLAYWRIGHT_PORT=8011`（application.yaml `quarkus.http.port=8011` 非 8080）。
- 套件计数 257→260（+3 auto-recon 用例，+1 spec 文件）。Plan Status: active → completed；Phase 1+2 全部 [x] + Status completed。

### 2026-07-13（Purchase autotest 快照日期漂移修复 — mission 验证阻塞解除）

- 执行 mission 'erp' verify+commit 时 `mvn test` 在 `app-erp-purchase-service` 失败（8 测试类 17 errors）。经根因分析（nop-debugging 技能 Phase 1）确认为 **pre-existing 日期翻转 flakiness**，与本计划 finance `flushSession()` 改动无关：purchase 域 8 个 `JunitAutoTestCase` 快照测试用 `CoreMetrics.currentDate()` 播种数据（看板/退货/预算），快照 CSV（`_cases/.../output/tables/*.csv`）于 2026-07-12 录制含绝对日期；今日翻到 2026-07-13 后所有 currentDate()-派生日期漂移 +1 天，CHECKING 模式逐行比对失败。框架的 `TestClock`（`useTestClock`）不冻结日期（仍用 `System.currentTimeMillis()`），故无 config 级时钟冻结。其他 17 域快照均用硬编码日期（LocalDate.of），不受影响——日期漂移**仅限 purchase**。
- **修复**：对 30 个受影响 output CSV 中 currentDate()-派生的日期格（BUSINESS_DATE/DUE_DATE/DELIVERY_DATE/VOUCHER_DATE 等，共 41 格）改为 nop-match 通配符 `*`（镜像既有快照对 DEL_VERSION/CREATE_TIME/UPDATE_TIME 的 `*` 惯例——这些快照本就对非确定字段用 `*`，只是漏掉了日期）。硬编码日期（LocalDate.of 2026-07-01/02/05/15/31 等）保持字面值。方法：`nop.autotest.force-save-output=true` 重录制识别漂移格 → 单元格级 old≠new 且均为日期 → 原 CSV 仅这些格置 `*`（丢弃重录制引入的 DEL_VERSION `*`→`0` / 行尾噪声）。**durable**：日期无关，不再随日历漂移。
- **验证（full-green）**：`mvn clean install -DskipTests` 154 模块 BUILD SUCCESS；`mvn test` 全 reactor **2890 tests, 0 failures, 0 errors, BUILD SUCCESS**（此前 purchase 17 errors 已清零，其余 17 域 + master-data/notify/inventory 等全绿）。finance service 单测（含 `TestErpFinAutoReconciliation` 验证 flushSession 修复）全绿。
- 此为 mission verify 期间发现的 pre-existing 阻塞，机械低风险测试数据修复（零生产代码/契约/ORM 变更），按 AGENTS.md 低风险编辑跳过正式计划，证据记入本日志 + 提交消息。
