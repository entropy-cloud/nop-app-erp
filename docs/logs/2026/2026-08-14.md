# Development Log - 2026-08-14

### 2026-08-14

- 维护域 M4.54-56 实体级状态机 Bean 落地（plan `docs/plans/2026-08-14-0930-3-maintenance-m4-state-machine-beans.md` done）
- 新增 3 Bean：`ErpMntVisitStateMachine`（status 6 边）、`ErpMntSparePartUsageDocumentStateMachine`（docStatus 2 边）、`ErpMntSparePartUsageApprovalStateMachine`（approveStatus 1 边最小矩阵），镜像 M3.17 `ErpMntRequestStateMachine` 范式
- 接线：`AbstractErpMntVisitProcessor` / `AbstractErpMntSparePartUsageProcessor` 注入 Bean + cause-chaining；4 Visit + 2 SparePartUsage Processor 目标态改调 Bean `*TargetStatus()`
- SparePartUsage `validateNotConfirmed` silent-guard gap 按 Deferred 保留既有行为（行为保持优先，修复须 ask-first）；`validateCanReverse` docStatus 守卫委托 Bean `assertCanReverseConfirm`（cause-chain 到既有 `ERR_SPARE_PART_USAGE_NOT_POSTED` 码，错误码/参数不变）
- 测试：层 1 矩阵 23 case 全绿；层 3 既有 94 case 零回归；`mvn clean install -DskipTests` 156 模块 BUILD SUCCESS；合规 R5=0/R11=0
- roadmap M4.54-56 → done

- 反审核共享骨架 §16.4 合规化修复（plan `docs/plans/2026-08-14-2000-2-reverse-approve-skeleton-section16-4-compliance.md` done）
- Work Item `reverseApprove-§16.4-compliance-fix` = 5+ 状态机迁移计划（0945-1/1950-1/1950-2/0930-1/0930-2）共有 Deferred successor 关闭
- 修复 `module-common-service/.../AbstractReverseApproveProcessor.java`：(a) `doReverseApprove:39` 改调 `rejectedStatus()`（原 `submittedStatus()` 违反 §16.4）；(b) 新增 default `rejectedStatus() { return "REJECTED"; }`（方案 B，对齐全 25 子类 `wf/approve-status` 标准 dict 全局 REJECTED 值）；(c) javadoc 更新目标态为 REJECTED
- 零行为回归：独立子代理零信任盘点全 25 子类（Category A 4 覆写 doReverseApprove→Bean REJECTED + Category B 19 覆写公共 reverseApprove + Category C 2 抛不可逆异常），无一依赖骨架默认 body；缺陷运行时惰性。`submittedStatus()` 成 dead code 保留 abstract 以最小化跨域 churn
- 验证：`mvn clean install -DskipTests` 156 模块 BUILD SUCCESS + 跨域 5 service（purchase/sales/assets/finance/quality）`mvn test` 全绿（0 failures/0 errors）+ compliance R5=0/R11=0 零漂移
