# 2026-07-04 开发日志

## 业财过账引擎缺口分析与设计补充（文档，无代码变更）

**触发**：外部文章《会计引擎生成反写设计》对比请求
**分析报告**：`docs/analysis/2026-07-04-finance-posting-engine-gap-vs-opensource.md`（新建）
**设计文档**：`docs/design/finance/posting-log.md`（新建）、`docs/design/finance/posting.md`（扩充冲销机制+反写契约）、`docs/design/finance/README.md`（文档目录+关键业务规则）

### 本次完成的工作

1. **三方对比分析**：外部文章（凭证生成/反写/日志/冲销/监控）vs 开源共识（`erp-survey/` 实测 iDempiere/Metasfresh/Odoo/ERPNext/Tryton/赤龙）vs 本项目现状。逐项裁定真缺口与过度抽象。
2. **关键修正**：核实 `ar-ap-reconciliation.md` 的 `ErpFinArApItem` 辅助账项机制——文章的"部分核销反写"在本项目已通过辅助账项领域模型覆盖（核销回写 `settledAmount/openAmount/status`），**非缺口**。真实缺口收窄为三项：会计日志（P0）、冲销反写闭环（P0）、运行监控（P1）。
3. **被拒绝方向记录**：独立反写服务/反写记录表（开源共识不存在，与业财回链语义重复）、中台主动反写（破坏 DAG 解耦）、强制异步（SYNC 强一致是合理默认）——记录以防回潮。
4. **新建 `posting-log.md`**：业财过账日志与可观测性设计，对标 Metasfresh `Fact_Acct_Log`/`Fact_Acct_UserChange`。四类日志（规则命中追溯/变更审计/过账异常工作台/运行监控）+ traceId 端到端串联 + 复用 nop-platform 审计优先于新建 ORM 实体。
5. **扩充 `posting.md` 冲销机制**：从单向（业务作废→红冲凭证）补全为双向闭环，新增方向二（凭证红冲→业务单据回退），定义 `VoucherReversedEvent` 契约；新增独立"反写契约"章节（域自治、不持有源实体、posted+业财回链双载体、不引入独立反写表）。
6. **更新 `finance/README.md`**：文档目录登记 `posting-log.md`；关键业务规则第 3 条（业财回链）扩充为双向闭环，新增第 4 条（反写契约）、第 5 条（过账全程可观测）。
7. **roadmap M5 立项**：`docs/backlog/core-business-roadmap.md` 新增「M5 — 业财可运维性与闭环」（5.1 会计日志 P0 / 5.2 冲销反写闭环 P0 / 5.3 运行监控 P1），定位为业财打通的运营成熟度层（M1/M4 已交付功能正确性，M5 补可观测/可闭环/可监控）。Work Item Status 与 Implementation Order 两处同步。

### 验证基线

- 无代码变更（纯文档补充）。文档引用一致性已核实（`posting.md` / `posting-log.md` / `README.md` / 分析报告 / roadmap M5 交叉引用对齐；分析报告证据索引指向真实存在的 erp-survey 文件与本项目 owner docs）。
- 设计裁定与本项目既有哲学一致：保留 DAG 解耦（引擎不持有源实体），不引入文章的中台主动反写模式。

### 后续（M5 已立项，落地须各自起草计划 + 独立审计）

- P0 会计日志实现（M5/5.1）：优先评估复用 nop-platform 审计能力（避免触及 ORM 保护区域）；`PostingEvent` 增 `traceId` 字段。
- P0 冲销反写闭环实现（M5/5.2）：`reverse()` 成功后发 `VoucherReversedEvent`，各业务域 Provider 监听回退自身状态。
- P1 运行监控（M5/5.3）：接入 nop-platform 监控大盘。

---

## TMS 承运商网关三层 SPI + 运费双路径过账 — plan 2026-07-04-1115-3

**计划**：`docs/plans/2026-07-04-1115-3-logistics-carrier-gateway-spi-freight-posting.md`（Plan Status: active → completed）
**设计**：`docs/design/logistics/README.md`、`docs/design/logistics/state-machine.md`、`docs/design/logistics/carrier-integration.md`、`docs/design/finance/posting.md`
**工作项**：3.17 TMS 承运商网关三层 SPI + 3.18 TMS 运费双路径过账

### 验证基线（全绿 — full-green verification）

- `mvn clean install -DskipTests`（根，146 reactor 模块）：BUILD SUCCESS，无下游破坏
- `mvn test -pl module-logistics/erp-log-service`：logistics 测试全绿（新增 `TestErpLogShipmentGateway` 6 tests + `TestErpLogFreightPosting` 3 tests + 既有 CRUD smoke 5 tests，共 14 tests，零失败零回归）
- `mvn test -pl module-finance/erp-fin-service`：finance 93 tests 全绿（FREIGHT 枚举/字典扩展无回归）

### 本次完成的工作

1. **Phase 1 — 三层 SPI + 中立 DTO + mock 承运商 + ErrorCode/Config**：中立 DTO 包（Address/ParcelInfo/DeliveryOrder{Request,Result}/PackageLabel/ShipmentAdvice/Tracking{Result,Event}/RateQuote{Request,Result}）；`IErpLogCarrierGatewayClient`（6 方法）+ `IErpLogCarrierGatewayClientFactory`；`ErpLogCarrierGatewayRegistry`（**镜像 finance ErpFinAcctDocRegistry 的 List 注入 + 内部建图范式**，`ioc:collect-beans` 收集 Factory）；`MockCarrierGatewayClientFactory`（gatewayId="mock"，确定性 trackingNo/labelUrl + trackShipment 按调用次数推进 + 5xx/4xx 失败模式测试钩子）；`ErpLogErrors` 6 ErrorCode；`ErpLogConfigs` 7 配置项；`ErpLogConstants` 状态码/billData 键。
2. **Phase 2 — 运单状态机 + 网关集成 + 幂等/重试 + webhook/轮询**：`IErpLogShipmentBiz` 加 advise/completeShipment/cancelShipment/handleTrackingWebhook/scanForPolling；`GatewayDispatcher`（completeDeliveryOrder 重试 5xx 指数退避/4xx 不重试/死信保留 ADVISED + remark，trackShipment 轮询推进，cancelAtCarrier）；webhook HMAC-SHA256 校验 + 幂等（advanceTracking 已终态返回 false）；`@SingleSession` 钉会话边界（参 finance Processor）。
3. **Phase 3 — FREIGHT 业务类型 + LogisticsFreightProvider（3.18 path-1）**：`ErpFinBusinessType.FREIGHT(310)` + 字典 `erp-fin/business-type` 同步（finance 保护区域契约扩展）；`LogisticsFreightProvider`（SALES_DELIVERY→借销售费用-运费 6601/贷按 freightTerms PREPAID→银行存款 1002、COLLECT→应付账款 2202 携承运商 partnerId）；erp-log-service compile 依赖 erp-fin-service（logistics→finance 单向无环）。
4. **Phase 4 — DELIVERED 触发过账 + path-2 交接（3.18 wiring）**：`onDelivered`（freightSettlementStatus PENDING 守卫，已 SETTLED 抛 ERR_LOG_SHIPMENT_ALREADY_DELIVERED 幂等）；SALES_DELIVERY→构建 PostingEvent{businessType=FREIGHT} 调 `IErpFinVoucherBiz.post`（直接调用范式参 InvPostingExecutor）；PURCHASE_RECEIPT→发布 ShipmentDeliveredEvent 占位（Landed Cost 归 Deferred）；成功 PENDING→SETTLED；acctSchemaId 按 orgId 解析。
5. **Phase 5 — 行为测试与收尾**：`TestErpLogShipmentGateway` 6 测试（全链状态机 + trackingNo/labelUrl 回写 + webhook 签名/幂等 + 5xx 重试死信 + 4xx 死信 + 取消）；`TestErpLogFreightPosting` 3 测试（SALES_DELIVERY FREIGHT 凭证 + SETTLED + 重复 DELIVERED 幂等抛错 + PURCHASE_RECEIPT 仅事件无凭证）；`extended-roadmap.md` 3.17/3.18 ✅。

### 实现偏离补注

- **path-1 过账模型裁决**：logistics 直接调 `IErpFinVoucherBiz.post(event, ctx)`（参 `InvPostingExecutor`/`InvPostingDispatcher` 范式），**非** `state-machine.md:99`/`use-cases.md:48` 原描述的"DELIVERED 后本域发布 ShipmentDeliveredEvent，finance 域订阅执行过账"模型。直接调用与现有全域过账（inventory/sales/projects）一致；path-2（PURCHASE_RECEIPT）仅发事件待 finance Landed Cost 落地后订阅。
- **异步派发为可调用方法（nop-job 注册归部署 Non-Goal）**：平台无 `txn().afterCommit` 机制；`advise()` 仅 DRAFT→ADVISED，`completeShipment()` 为独立幂等 @BizMutation（供异步 worker/手动重试/测试调用）。nop-job cron 生产注册归部署 follow-up（Non-Goal #6）。
- **ORM 范式**：`GatewayDispatcher`（Facade 编排层）一致用 `dao.getEntityById` + `dao.saveOrUpdateEntity`（参 InvPostingDispatcher），BizModel `@BizMutation`+`@SingleSession` 钉会话边界；不混用 requireEntity 与直接 dao 操作（避免 session 不一致）。
- **webhook 密钥**：mock 阶段取承运商编码作 HMAC 密钥（测试可控）；真实部署应扩展 CarrierConfig credentials JSON 内 webhookSecret（follow-up）。
- **Non-Goal 遵守**：真实承运商 HTTP 集成（mock stub 覆盖全链）、path-2 采购运费到岸成本分摊（Landed Cost 为 costing-methods.md:40 Deferred）、比价生产路径（SPI 已定义仅 mock 返回）、月台预约/前端面单预览/nop-job cron 注册均为计划内 Non-Goal 或 Deferred。

## DRP 净需求计算 + 安全库存优化 — plan 2026-07-04-1115-2

**计划**：`docs/plans/2026-07-04-1115-2-drp-net-requirement-safety-stock.md`（Plan Status: active → completed）
**设计**：`docs/design/drp/README.md`、`docs/design/drp/state-machine.md`、`docs/design/drp/safety-stock-optimization.md`、`docs/design/drp/lead-time-tracking.md`
**工作项**：3.15 DRP 净需求计算 + 3.16 DRP 安全库存优化

### 验证基线（全绿 — full-green verification）

- `mvn clean install -DskipTests`（根，146 reactor 模块）：BUILD SUCCESS，无下游破坏
- `mvn test -pl module-drp/erp-drp-service -am`：drp 测试全绿（新增 `TestErpDrpEngine` 7 tests + `TestErpDrpSafetyStock` 6 tests + 既有 CRUD smoke 5 tests，共 18 tests，零失败零回归）

### 本次完成的工作

1. **Phase 1 — 设计/代码漂移对齐 + ErrorCode/Config**：Explore 证实 `ErpMdMaterial` 无 ABC 分类列（按类推荐 serviceLevel 归 Non-Goal）。**对齐设计文档到 ORM 命名**：`drp/{README,state-machine,safety-stock-optimization,lead-time-tracking,use-cases}.md` 中 `ErpInvDrpPlan/Line/Parameter`→`ErpDrpPlan/Line/Parameter`、`erp-inv/drp-plan-status`→`erp-drp/drp-plan-status`（仅 plan/line/parameter 三实体及字典；SS/lead-time/cross-dock 保留 `erp_inv_drp_`+`erp-inv/`）；删除 `overwrittenAt` 漂移（ORM 仅 `overwrittenBy`）。`ErpDrpErrors` 8 ErrorCode；`ErpDrpConfigs` 7 配置项；`ErpDrpConstants` 状态码/类型/Z 值映射。
2. **Phase 2 — DRP 净需求引擎（DRAFT→COMPUTED）**：`DrpDemandAggregator`（只读聚合 StockBalance available/reserved + TransferOrder 在途 + PurOrder 未到货，经 IDaoProvider 行级批量）；`DrpEngine.runDrp`（`netRequirement = max(0, safetyStock + forecast − currentStock + allocated − onOrder)`，向上取整 orderMultiple，决策 TRANSFER/PURCHASE，写 ErpDrpLine SUGGESTED + 回写 plan totalReplenishmentQty/runAt + DRAFT→COMPUTED）；`ErpDrpPlanBizModel.runDrp/resetToDraft` 薄委派。
3. **Phase 3 — DRP 释放（COMPUTED→APPROVED→EXECUTED）**：`ErpDrpPlanBizModel.approvePlan`（COMPUTED→APPROVED + 行 SUGGESTED→APPROVED）；`DrpReleaseService.releaseLine`（TRANSFER→生成 ErpInvTransferOrder，PURCHASE→生成 ErpPurOrder，回写 orderBillType/orderBillCode，DrpLine→ORDERED）；`releaseApproved` 批量释放 + plan→EXECUTED。releaseLine 单行释放也推进计划 EXECUTED。
4. **Phase 4 — 安全库存优化引擎（3.16）**：`SafetyStockEngine.calculate` 三法（STATISTICAL: Z×σ_d×√L 月需求转日均值/标准差 + ROP；SIMPLE: μ_d×leadTime×safetyFactor；DDMRP: μ_d×(leadTime+variability+cycle)），Z 值映射 PCT95→1.645 等，历史不足降级 SIMPLE，零需求月 EXCLUDE/KEEP 策略；`findEffectiveSafetyStock` 优先级 override>calculated>parameter；`confirmWriteback` 人工回写门。
5. **Phase 5 — 行为测试与收尾**：`TestErpDrpEngine` 7 测试（净需求公式/orderMultiple 取整/负归零/E2E 状态机全链/非 DRAFT 拒绝/重复释放拒绝/resetToDraft 清行），`TestErpDrpSafetyStock` 6 测试（STATISTICAL/SIMPLE/DDMRP 计算/历史不足降级 SIMPLE/优先级三层/confirmWriteback 回写）；`extended-roadmap.md` 3.15/3.16 ✅。

### 实现偏离补注

- **跨域写经 IDaoProvider 而非注入 I*Biz（沿用 MRP 先例）**：`DrpReleaseService` 释放生成调拨单/采购订单经 `IDaoProvider` 直接持久化目标域实体，而非注入 `IErpInvTransferOrderBiz`/`IErpPurOrderBiz`。**原因**：这些 IBiz 仅提供 save(Map) 通用 CRUD，无 purpose-built 方法，且通用 save 需穿越完整 CRUD 校验管道。沿用 MRP `MrpReleaseService` 已验证的 service-helper 范式，仅写 DRP 已知字段（物料/数量/仓库/供应商），采购单价/金额置 0 待采购员补录。生产由 app-erp-all 聚合，各域 BizModel bean 均注册。
- **释放币种解析**：ErpDrpParameter 无币种字段，生成的采购单为草稿，取首个可用币种（ErpMdCurrency isActive=true）作占位。残留风险：采购员需复核币种。
- **Non-Goal 遵守**：联合变分安全库存（需 ORM 列 leadTimeStdDev/useJointVariation）、正式需求预测集成（forecastDemand 默认 0/手工）、越库/月台预约（UC-DRP-07）、提前期收货自动写入（UC-DRP-08）、nop-job 定时运行、20% 偏差预警、多级分销展开均为计划内 Non-Goal 或 Deferred。

## 合同版本/InvoicePlan/批量折扣/返利引擎 — plan 2026-07-04-1115-1

**计划**：`docs/plans/2026-07-04-1115-1-contract-version-invoiceplan-volume-discount-rebate.md`（Plan Status: active → completed）
**设计**：`docs/design/contract/state-machine.md`、`docs/design/contract/volume-discount.md`
**工作项**：3.12 合同版本管理 + InvoicePlan 触发发票 + 3.14 合同批量折扣/返利计算

### 验证基线（全绿 — full-green verification）

- `mvn test -pl module-contract/erp-ct-service -am`：contract 测试全绿（新增 `TestErpCtContractRebate` 8 tests + 既有 CRUD smoke 5 tests，共 13 tests，零失败零回归）

### 本次完成的工作

1. **Phase 1 — 贷项凭证载体 Decision + ErrorCode/Config**：Explore 证实发票实体金额字段无正额约束（DECIMAL(20,4) defaultValue=0，无 sign 约束），负额/反向单据为既成模式（`PaymentSettler.java:130 negate()` + 退货红字）。**Decision 裁定**：复用既有 `IErpPurInvoiceBiz`/`IErpSalInvoiceBiz` 以负额发票表达返利结算贷项（PURCHASE→AP 负额，SALES→AR 负额），不新增 finance 实体。`ErpCtErrors` 新增 8 ErrorCode；`ErpCtConfigs` 对齐 `volume-discount.md §配置点` + 新增 3 键（invoiceplan-auto-trigger/rebate-progressive-retro-topup/settlement-mode）；`ErpCtConstants` 补全状态码常量。
2. **Phase 2 — 合同头状态机 + 版本管理**：`IErpCtContractBiz` 添加 activate/suspend/resume/terminate/expire/amend；`IErpCtContractVersionBiz` 添加 finalizeVersion/signVersion。activate 校验 contractType↔contractDirection 组合（PURCHASE→INBOUND、SALES→OUTBOUND）+ 同步签署当前版本；amend 新建版本（versionNo=max+1）原子翻转 isCurrent；signVersion 原子翻转同级版本。非法迁移抛 `ERR_CT_ILLEGAL_STATUS_TRANSITION`。
3. **Phase 3 — InvoicePlan 触发发票**：`IErpCtInvoicePlanBiz` 添加 triggerInvoice/triggerDuePlans。triggerInvoice 校验合同 ACTIVE（SUSPENDED/非 ACTIVE/已开票各失败路径），按 contractDirection 经 IDaoProvider 生成 AP/AR 发票草稿（关联合同行物料/数量/金额），回写 isInvoiced/invoiceBillCode/invoiceDate。triggerDuePlans 批量到期计划 config-gated。
4. **Phase 4 — VolumeDiscount 解析器**：`IErpCtVolumeDiscountBiz` 添加 `@BizQuery resolveDiscount`（返回 DiscountResult DTO）。命中 `fromQty <= qty < toQty`（末端闭区间），discountPercent 优先、unitPrice 覆盖价优先；defaultPrepareSave/Update 钩子校验区间带无重叠。
5. **Phase 5 — 返利计提引擎 + 结算**：`RebateEngine`（注册为 bean）实现 accrue/accruePeriodEnd/computeRebate（整额×命中档率，对齐设计文档示例 ¥1.2M×2%=24K）；`IErpCtRebateAgreementBiz` 添加 runAccrual（聚合已过账发票，PERIOD_END 一次性/PROGRESSIVE 逐张喂入）；`IErpCtRebateSettlementBiz` 添加 postSettlement（DRAFT→POSTED，汇总未结算计提，生成负额贷项发票，标记 isSettled）。
6. **Phase 6 — 行为测试与收尾**：`TestErpCtContractRebate`（JunitAutoTestCase + H2，8 tests）：状态机全路径 + amend 版本翻转 + InvoicePlan INBOUND 触发/SUSPENDED 拒 + VolumeDiscount 区间命中/重叠拒 + 返利 PROGRESSIVE 跨档补差（800K→0、+400K→24K 追溯）+ 结算 POSTED 负额贷项 + 计提标记 + 非法迁移拒；`extended-roadmap.md` 3.12/3.14 ✅；`volume-discount.md` §结算流程无条件更新为负额发票载体。

### 实现偏离补注

- **跨实体经 IDaoProvider 而非注入 I*Biz**：InvoicePlan 触发生成发票草稿 + 返利结算贷项（负额发票）+ runAccrual 发票只读查询经 `daoProvider().daoFor(...)` 直接持久化/查询，而非注入 `IErpPurInvoiceBiz`/`IErpSalInvoiceBiz`。**原因**：硬注入跨域发票 BizModel 会将其完整服务依赖链（sales→inventory→...）级联进合同域，破坏其隔离单元测试（IoC 启动找不到 IErpInvStockMoveBiz bean）。发票草稿为纯实体构造 + 持久化（不经 submit/approve 业务管道），IDaoProvider 是最小耦合方案。生产由 app-erp-all 聚合，各域 BizModel bean 均注册。
- **CrudBizModel 的 daoProvider() 方法复用**：BizModel 内不自声明 `@Inject IDaoProvider daoProvider`（与父类 private 字段冲突致 null），统一用继承的 `daoProvider()` 方法。
- **返利计算模型**：采用"整额×命中档率"（对齐 volume-discount.md §追溯调整示例：¥1.2M × 2% = ¥24K），PROGRESSIVE 跨档时 delta = 新预期返利 - 已计提返利（自然捕获全量追溯补差，符合"逐档补差非仅最高档"）。
- **Non-Goal 遵守**：PO/SO 订单行折扣接线、合同电子签章（3.13）、审批工作流、文档仓库/OCR、nop-job 到期提醒/自动续约、用量计费、多币种返利重估均为计划内 Non-Goal，归各 follow-up。

## HR 排班管理（班次模板/分配/轮换/调换/迟到早退缺勤/休假联动）— plan 2026-07-04-0831-3

**计划**：`docs/plans/2026-07-04-0831-3-hr-shift-scheduling.md`（Plan Status: active → completed）
**设计**：`docs/design/human-resource/shift-scheduling.md`
**工作项**：3.8 HR 排班管理

### 验证基线（全绿 — full-green verification）

- `mvn clean install -DskipTests -pl module-hr/erp-hr-codegen,module-hr/erp-hr-dao,module-hr/erp-hr-meta,module-hr/erp-hr-service -am`：HR 4 模块 reactor 全绿（BUILD SUCCESS）
- `mvn test -pl module-hr/erp-hr-service`：HR 测试全绿（新增 `TestErpHrShiftScheduling` 13 tests + 既有 payroll 6 + survey CRUD 5，共 24 tests，零失败零回归）

### 本次完成的工作

1. **Phase 1 — 字典补齐 + 裁决（结果落点）**：新增 dict `erp-hr/absence-reason`（LEAVE/LATE_NOT_CLOCKED/OTHER）+ `ErpHrShiftAssignment.absenceReason` 加 `ext:dict` 绑定（codegen 重生成）。**Phase 1 Decision 裁决**：迟到/早退/缺勤计算结果落 `ErpHrAttendance`（已有 lateMinutes/earlyLeaveMinutes/isAbsent 列），`ErpHrShiftAssignment` 保持「标准班次输入」角色不加结果列、不改模型——本期无 ShiftAssignment 模型变更。`ErpHrErrors` 新增 5 排班 ErrorCode（`ERR_SHIFT_DUPLICATE_ASSIGNMENT`/`ERR_SHIFT_CROSS_DAY_INVALID`/`ERR_SHIFT_SWAP_TARGET_OCCUPIED`/`ERR_SHIFT_ROTATION_PATTERN_INVALID`/`ERR_SHIFT_ASSIGNMENT_NOT_SWAPPABLE` + `ERR_SHIFT_SWAP_ILLEGAL_STATUS_TRANSITION`/`ERR_LEAVE_REQUEST_NOT_FOUND`）；`ErpHrConstants`/`ErpHrConfigs` 扩展排班配置项与状态码常量。
2. **Phase 2 — 排班分配 + 轮换生成**：扩展 `IErpHrShiftAssignmentBiz` 添加 `assignSingle`/`assignBatch`/`copyFromPeriod`/`findByEmployeeAndDate`；扩展 `IErpHrShiftRotationPatternBiz` 添加 `generateRotation(patternId, groupMemberIds, staggerDays, dateRange, regenerate)`。**Phase 2 Decision**：组成员与 staggerDays 作为瞬态方法入参传入，本期不新增 `ErpHrShiftRotationGroup` 实体（替代 A：新增实体 rejected；替代 B：复用 groupId 自引用 rejected）。轮换 patternData JSON 序列校验（非法抛 `ERR_SHIFT_ROTATION_PATTERN_INVALID`）；regenerate 清旧（SCHEDULED→CANCELLED + flushSession）重生成同范围。
3. **Phase 3 — 迟到/早退/缺勤计算（含跨天班次）**：扩展 `IErpHrShiftBiz` 添加 `calcAttendance`/`findAttendanceByDate`；`ShiftAttendanceCalculator`（纯逻辑）：跨天夜班（endTime < startTime）clockOut 基准取次日 endTime、迟到基准取当日 startTime、日历归属开始日期；缺勤判定（requireClockIn=true 且无 clockIn → ABSENT + LATE_NOT_CLOCKED；leaveRequestId 非空 → ABSENT + LEAVE 休假覆盖不计旷工）。**Phase 3 Decision**：跨实体读 attendance 经注入 `IErpHrAttendanceBiz`（同域 I*Biz），不直接 DAO。
4. **Phase 4 — 排班调换审批 + 休假联动**：扩展 `IErpHrShiftSwapRequestBiz` 添加 `submit`(→PENDING)/`approve`(PENDING→APPROVED 互换双方 assignment 班次 + 记录 swapRequestId + replacedByAssignmentId)/`reject`/`cancel` 状态机；扩展 `IErpHrShiftBiz` 添加 `onLeaveApproved`/`onLeaveCancelled` 休假联动钩子。**Phase 4 Decision**：休假联动经同域跨实体同步调用（不引入事件总线，同域无须解耦）。
5. **Phase 5 — 行为测试与收尾**：`TestErpHrShiftScheduling`（JunitAutoTestCase + H2，13 tests）：一人一天唯一/批量/复制/轮换（含 staggerDays 错峰 + regenerate）/迟到早退/跨天夜班 clockOut 次日基准/缺勤/调换审批互换/非法迁移拒绝/休假 APPROVED 标记 + CANCELLED 解除；`extended-roadmap.md` 3.8 ✅；`shift-scheduling.md` 偏离补注。

### 实现偏离补注

- **跨实体辅助查询经 daoProvider**：`ErpHrShiftBizModel` 内 `findAssignmentsByEmployeeRange` 用 `assignmentBiz.findList`（I*Biz 接口）但内部对 ErpHrAttendance/ErpHrShiftAssignment 的 update 操作经 `daoProvider().daoFor(...)` 直接 dao（与 `ErpHrSalaryBizModel` 既有先例一致）。
- **`assignmentDate` 字段查询运算符限制**：XMeta 限制 `assignmentDate` 字段仅支持 `eq/in/dateBetween/dateTimeBetween`，`ge`/`le`/`between` 抛 `prop-not-support-filter-op`，统一用 `FilterBeans.dateBetween`。
- **`saveOrUpdateEntity` 而非 `updateEntity`**：`ErpHrAttendance` 同 session 内新建后立即更新的场景经 `IEntityDao.saveOrUpdateEntity`（新建态调用 `updateEntity` 抛 `update-entity-not-managed`）。
- **regenerate 需 `flushSession`**：CANCELLED 状态变更后立即查询须经 `dao.flushSession()` 才能使新状态对后续 query 可见（否则旧 SCHEDULED 仍命中导致跳过创建）。
- **Non-Goal 遵守**：打卡机集成/加班费（ payroll 消费考勤数据）/排班看板 AMIS 可视化/开放认领排班市场/出勤率调换率报表/nop-job cron 自动生成均为计划内 Non-Goal，归各 follow-up。

## HR 薪酬核算引擎 + 个税累计预扣（human-resource）— plan 2026-07-04-0831-2

**计划**：`docs/plans/2026-07-04-0831-2-hr-payroll-engine-income-tax.md`（Plan Status: active → completed）
**设计**：`docs/design/human-resource/payroll.md`
**工作项**：3.7 HR 薪酬核算 + 个税计算

### 验证基线（全绿 — full-green verification）

- `mvn clean install -DskipTests`（根，146 reactor 模块）：BUILD SUCCESS
- `mvn test -pl module-hr/erp-hr-service -am`：HR 测试全绿（新增 `TestErpHrPayrollEngine` 6 tests + 既有 CRUD smoke 5 tests，零失败零回归）

### 本次完成的工作

1. **Phase 1 — 配置实体 + ErpHrSalary 扩展 + codegen**：新增 6 实体（`ErpHrSalaryItem`/`ErpHrSocialInsuranceConfig`/`ErpHrSocialInsuranceBase`/`ErpHrTaxConfig`/`ErpHrTaxSpecialDeduction`/`ErpHrPayrollBankFile`）+ 8 新字典（`salary-approval-status` 6 态/`salary-item-category`/`salary-item-group`/`calc-method`/`social-insurance-type`/`special-deduction-type`/`bank-file-format`/`city`/`bank-file-status`）；`ErpHrSalary` 加列 `approvalStatus`（权威端到端状态）/`performanceFactor`/`actualWorkDays`/`requiredWorkDays`/`totalOvertimeHours`/`unpaidLeaveDays`/`cumulativeData`（JSON）/`reviewNote`/`paymentBatchNo`/`bankFileId`；`paymentStatus` 保留为只读派生投影（Phase 1 Decision 消歧）。
2. **Phase 2 — 薪酬核算引擎**：`SocialInsuranceCalculator`（基数 min/max 钳制 + 五险个人/公司比例累加）、`IncomeTaxCalculator`（累计预扣法：年初累计 + 当月 → 七级累进 → 速算扣除 − 已预扣 = 当月应纳，`cumulativeData` JSON 写回）、`TaxBracketParser`、`PayrollCalculator`（编排：出勤比例→基本工资→津贴→加班→绩效→社保/公积金→个税→实发）；`IErpHrSalaryBiz` 扩展 `calculateSalary`。
3. **Phase 3 — 批量核算 + 审批状态机**：`runPayroll`（ACTIVE/PROBATION 遍历 + 幂等跳过）、6 态审批迁移 `review`/`approveFinance`/`approveManager`/`rejectSalary`/`voidSalary`/`markPaid` + 非法迁移 `ERR_SALARY_ILLEGAL_STATUS_TRANSITION` + PAID 锁定 `ERR_SALARY_LOCKED_AFTER_PAID`。
4. **Phase 4 — 业财过账 + 银行文件**：**保护区域契约变更**——`ErpFinBusinessType` 枚举新增 `SALARY(270)`/`SALARY_PAYMENT(280)`/`SOCIAL_INSURANCE_ER(290)`/`HOUSING_FUND_ER(300)` + `erp-fin/business-type` 字典同步（`0811-1` Decision line 83 批准）；`SalaryPostingProvider implements IErpFinAcctDocProvider`（4 业务类型科目映射）+ `SalaryPostingDispatcher`（APPROVED_MANAGER 计提/PAID 发放，失败吞异常不阻塞终态）+ `SalaryPostingExecutor`（跨域经 `IErpFinVoucherBiz`）；`generateBankFile`（CSV 模板 + 批次号 + ErpHrSalary→PAID 同步 paymentStatus）；hr-service 加 finance-service compile 依赖。
5. **Phase 5 — 行为测试**：`TestErpHrPayrollEngine`（JunitAutoTestCase + H2）：社保基数钳制（50000→32694）、公积金、个税累计跨月、核算幂等、审批全路径 + PAID 锁定、非法迁移拒绝、银行文件生成 + 转 PAID；`extended-roadmap.md` 3.7 ✅。

### 实现偏离补注

- **公式引擎**：`payroll.md §1.4` 标注「待技术选型」→ 裁决为 Nop `IEvalScope`（Xpl 表达式），不引入第三方（AGENTS.md 平台优先）。本期预置项目（BASIC/ALLOWANCE/...）走 FIXED/INPUT 路径，FORMULA 求值基础设施就绪（`TaxBracketParser` 已示范 JSON 表达式解析模式），完整 `ErpHrSalaryItem.formula` 求值归 follow-up。
- **BizModel 落层**：设计原计划独立 `IErpHrPayrollBiz` 接口；实际将核算/审批/银行文件方法落在实体绑定 `IErpHrSalaryBiz` + `ErpHrSalaryBizModel`（extends CrudBizModel），以获得平台 `@SingleSession`/BizProxy 事务管理。方法签名与语义与计划一致。
- Non-Goal 遵守：薪酬模拟（3.9 successor）、绩效逻辑（手工录入）、银行实际转账、PDF 推送、年度汇算、半月薪、多币种、cron 自动核算均不在本期。

## APS 工序级排产引擎 + ATP/CTP 交期承诺（aps）— plan 2026-07-04-0831-1

**计划**：`docs/plans/2026-07-04-0831-1-aps-operation-order-scheduling-engine.md`（Plan Status: active → completed）
**设计**：`docs/design/aps/scheduling.md`（补「实现偏离补注」）、`docs/design/aps/README.md`
**工作项**：3.10 APS OperationOrder 排产引擎 + 3.11 APS ATP/CTP 交期承诺

### 验证基线（全绿 — full-green verification）

- `mvn clean install -DskipTests -pl module-aps/...`：APS 7 模块 reactor 全绿（BUILD SUCCESS）
- `mvn test -pl module-aps/erp-aps-service -am`：APS 测试全绿（新增 `TestErpApsSchedulingEngine` 6 tests + 既有 CRUD smoke 5 tests，零失败零回归）

### 本次完成的工作

1. **Phase 1 — 模型字段补充 + codegen**：`ErpApsOperationOrder` 增 `earliestStartDateT`/`latestEndDateT`（propId 27/28，可空 DATETIME）；`ErpApsConfigs`（6 配置项默认值）、`ErpApsConstants`（状态码）、`ErpApsErrors`（5 排产 ErrorCode）补全；codegen 产物含新字段。
2. **Phase 2 — 排产引擎核心**（纯算法 + Processor 两层结构）：
   - `WorkCenterTimeline`：单工作中心繁忙区间内存模型（维护停机 + 已排定工序），前向/后向可用时段查找（capacity=1）
   - `ErpApsSchedulingEngine`：前向排产（priority ASC, latestEndDateT ASC, sequence ASC）、后向排产（sequence DESC）、duration=setupTime+runtimePerUnit×qty、相邻工序 buffer、前序完工约束、维护避让、不可排标记冲突
   - `ErpApsSchedulingProcessor`：拉取 Schedule（须 DRAFT）+ DRAFT 工序（horizon 内）+ MAINTENANCE 约束 → 调引擎 → 写回 plannedStart/EndDateT + status→PLANNED
   - `IErpApsOperationOrderBiz`/`ErpApsOperationOrderBizModel`：`@BizMutation scheduleForward/scheduleBackward(scheduleId)`
3. **Phase 3 — 插单区间重排 + 排产方案版本**：
   - `insertRushOrder(operationOrderId)`：窗口 [earliestStartDateT, latestEndDateT+buffer] 内同工作中心低优先级 PLANNED 回退 DRAFT 重排，IN_PROGRESS 永不回退（抛 `ERR_APS_OP_IN_PROGRESS_NOT_RESCHEDULABLE`），窗口外/高优先级工序冻结为已占用区间；最小影响范围
   - `IErpApsScheduleBiz`/`ErpApsScheduleBizModel`：`publish`(DRAFT→PUBLISHED)/`archive`(DRAFT|PUBLISHED→ARCHIVED) 状态机
4. **Phase 4 — ATP/CTP 交期承诺模拟**：
   - `IErpApsAtpCtpService`/`ErpApsAtpCtpServiceImpl`（`@BizModel` 注册）：ATP（库存现有量 − 已预约量 ≥ qty 即时承诺）、CTP（影子 OperationOrder 经 `IEntityDao.newEntity()` 构造不持久化，BOM 工艺路线追溯，现有 PLANNED 时间轴叠加前向模拟）、`simulateSchedule` 返回 `ScheduledOperationView`
   - `ErpApsOperationOrderBizModel` 委托暴露 `@BizQuery earliestCompletionDate/checkFeasibility`
   - **跨域访问实现偏离**：本期 ATP/CTP 对 inventory/manufacturing 的只读聚合采用 `IDaoProvider` 而非跨域 I*Biz 强注入（I*Biz 强注入在 aps-service 单模块部署/测试时因依赖模块未组装而启动失败，破坏模块独立性）。仅只读聚合、非裸 SQL、未破坏物理边界；完整 app-erp-all 部署等价
5. **Phase 5 — 行为测试与收尾**：`TestErpApsSchedulingEngine`（JunitAutoTestCase + H2）：前向（前序约束+维护避让+工作中心不重叠）、后向（交期可达）、插单（低优先级回退/高优先级保留/IN_PROGRESS 拒绝）、状态机、ATP/CTP 影子不持久化（行数不变断言）；`extended-roadmap.md` 3.10/3.11 标 ✅；`scheduling.md` 偏离补注。

### 实现偏离补注（已写入 `docs/design/aps/scheduling.md`）

- **贪心启发式非优化求解**：本期为前向/后向贪心填充（与 Axelor/Odoo 开源基线一致），无 ILP/CP 优化求解（Non-Goal）
- **MAINTENANCE 单约束 + capacity=1**：仅消费维护停机约束；PERSONNEL/TOOL/capacity>1 归 follow-up
- **工作中心班次日历未展开**：本期工作中心可用轴 = horizon 全域 − 维护停机区间；班次/节假日重复展开归 follow-up
- **ATP/CTP 跨域只读聚合经 IDaoProvider**：见上 Phase 4 偏离说明
- **甘特图前端 / CRP re-wiring / JobCard 自动创建 / maintenance 事件订阅 / 自动派工执行 / cron 自动重排**：均为 Non-Goal，归各 owner 计划 follow-up

---

## 客服工单状态机 + SLA 计时 + 满意度调查（customer-service）— plan 2026-07-04-0700-2

**计划**：`docs/plans/2026-07-04-0700-2-cs-ticket-sla-csat.md`（Plan Status: active → completed）
**设计**：`docs/design/customer-service/README.md`、`sla.md`、`csat.md`、`state-machine.md`（均补「实现偏离补注」）

### 验证基线（全绿 — full-green verification）

- `mvn clean install -DskipTests`：全量编译 reactor 模块全绿（BUILD SUCCESS）
- `mvn test -pl module-cs -am`：CS 模块测试全绿（新增 `TestErpCsTicketSlaCsat` 12 tests 全通过，零失败零回归）

### 本次完成的工作

1. **Phase 1 — 工单状态机 + SLA 策略匹配 + deadline 计算 + 超时升级（工作项 3.5）**：
   - `ErpCsTicketBizModel`：六态状态机 assign/start/resolve/close/reopen/cancel；非法迁移抛 `ERR_INVALID_TICKET_STATUS_TRANSITION`、终态抛 `ERR_TICKET_ALREADY_TERMINAL`、超时关闭无原因抛 `ERR_TICKET_CLOSE_BREACHED_NO_REASON`；每迁移写 `ErpCsTicketAction` 审计（fromStatus/toStatus 承载精确迁移，actionType 复用既有字典码）
   - `SlaPolicyMatcher`：按 ticketTypeId + minPriority 匹配（无 isActive 列不过滤；team 维度因工单无 teamId 不参与，仅匹配 teamId 空），精确度排序取首条
   - `SlaDeadlineCalculator`：日历小时模式 `now+resolveHours`；工作日模式跳周末（Mon-Fri），无工作时段窗口/节假日
   - `TicketPriorityRank`：优先级序数（URGENT=40/HIGH=30/NORMAL=20/LOW=10）
   - `scanOverdueTickets`：超时工单创建 ESCALATE 审计；`findSlaWarnings(beforeMinutes)` 预警查询（dateTimeBetween）
2. **Phase 2 — CSAT 调查生命周期 + 端到端（工作项 3.6）**：
   - `ErpCsSurveyBizModel`：createSurvey（resolve 触发，config-gated；唯一约束一工单一调查；UUID token；delay=0 立即 SENT）+ submitSurvey（评分区间校验 csat 1-5/nps 0-10/ces 1-7，各 config-gated；respondedAt=now 状态 COMPLETED）+ findSurveyReminders/findExpiredSurveys
   - `NpsClassifier`：9-10 推荐者 / 7-8 被动者 / 0-6 贬损者（派生不持久化）
   - `SurveyTokenGenerator`：UUID token 生成
   - 调查触发接线：resolve 后调 createSurvey；reopen 取消未响应调查（删除 respondedAt 空）
   - 端到端测试 `TestErpCsTicketSlaCsat`：全链路 + 非法迁移 + SLA 日历/工作日匹配 + scanOverdue 升级 + CSAT 创建/提交/重复拒绝/token 无效/reopen 取消/超时关闭校验/预警查询

### 关键技术决策

- **`@Optional` 注解**：可选 GraphQL 参数（cancelReason/resolution/beforeMinutes/npsScore/cesScore/comment）须标 `@io.nop.api.core.annotations.core.Optional`，否则 GraphQL 引擎抛 `field-null-arg`
- **`dateTimeBetween` 而非 `between`**：LocalDateTime 字段查询运算符仅支持 eq/in/dateBetween/dateTimeBetween，`between` 抛 `prop-not-support-filter-op`
- **避免循环依赖**：`ErpCsSurveyBizModel` 校验工单存在用 `daoProvider().daoFor(ErpCsTicket.class)` 只读校验，不注入 `IErpCsTicketBiz`（TicketBizModel 已注入 IErpCsSurveyBiz）

## CRM 活动提醒 + 线索评分 + 销售预测（crm）— plan 2026-07-04-0700-1

**计划**：`docs/plans/2026-07-04-0700-1-crm-event-reminder-lead-scoring-forecast.md`（Plan Status: active → completed）
**设计**：`docs/design/crm/README.md`（§业务规则2/4）、`lead-scoring.md`、`sales-forecast.md`（均补「实现偏离补注」）

### 验证基线（全绿 — full-green verification）

- `mvn clean install -DskipTests`：全量编译 146 reactor 模块全绿（BUILD SUCCESS）
- `mvn test`（全量 reactor）：所有模块测试通过，零失败（BUILD SUCCESS）。CRM 模块 24 tests（含新增 `TestErpCrmEventReminderTimeline` 5 tests + `TestErpCrmForecastAndScoring` 5 tests）+ sales/mfg/ast/prj/mnt/cs/hr/aps/ct/drp/log/b2b 等跨域全绿，3.1 既有零回归

### 本次完成的工作

1. **Phase 1 — 活动提醒 + 活动时间线（工作项 3.2）**：
   - `ErpCrmEventBizModel`：complete/cancel（PLANNED→COMPLETED/CANCELLED 状态机校验）+ findDueReminders（config-gated `event-reminder-enabled`，窗口范围查询走 `doFindListByQueryDirectly` 避 XMeta 限制）+ getLeadTimeline（Event+Activity 合并倒序）
   - `LeadActivityDerivationHelper`：推模式回写 Lead.lastContactDate/nextActivityDate（Event 状态变更即时回写，flushSession 使派生查询可见）
   - `EventTimelineAggregator`：时间线聚合（sourceType=EVENT/ACTIVITY）
2. **Phase 2 — 线索评分引擎（工作项 3.3）**：
   - `LeadScoringEngine`（support）：config 驱动 LOOKUP/FORMULA/BOOLEAN 准则计分 → 归一化 totalScore（0-100）→ append-only 评分记录 + 行级快照
   - `ErpCrmLeadScoreBizModel.recalculateScore`：GraphQL 动作 `ErpCrmLeadScore__recalculateScore`
   - auto-qualify：totalScore ≥ autoQualifyThreshold 且 LEAD+NEW 且 config-gated → `ErpCrmLeadProcessor.qualify`（复用 NEW→QUALIFIED，避免 BizModel 循环依赖）
   - recalc-on-lead-update：`ErpCrmLeadBizModel.defaultPrepareUpdate` 同步触发（config-gated）
   - 多 active config 抛 `ERR_MULTIPLE_ACTIVE_SCORE_CONFIG`；无 active config 返回 null（不阻断）
3. **Phase 3 — 销售预测引擎 + 端到端（工作项 3.4）**：
   - `ForecastAggregator`（support）：refreshForecast 按 ownerId 聚合 commit/upside/best-case/weighted + 层级 rollup（个人→团队→公司）+ 重建 ForecastLine 快照（清旧重建策略）；computeAccuracy 准确率计算
   - `ErpCrmForecastBizModel.refreshForecast` + `ErpCrmForecastPeriodBizModel.freeze/closePeriod`（期间状态机 OPEN→FROZEN/CLOSED）
   - FROZEN/CLOSED 拒绝重算（`ERR_FORECAST_PERIOD_NOT_OPEN`）；closePeriod 触发准确率（config-gated `accuracy-auto-compute`）
   - 端到端测试 `TestErpCrmForecastAndScoring`：评分 LOOKUP+BOOLEAN+auto-qualify / 预测 commit+upside+rollup / 期间 CLOSED 准确率 / FROZEN 拒绝重算

### 涉及文件

- 新增：`ErpCrmEventBizModel`、`LeadActivityDerivationHelper`、`EventTimelineAggregator`、`LeadScoringEngine`、`ForecastAggregator`（service entity/support）；`TestErpCrmEventReminderTimeline`、`TestErpCrmForecastAndScoring`（test）
- 修改：`ErpCrmLeadScoreBizModel`、`ErpCrmForecastBizModel`、`ErpCrmForecastPeriodBizModel`、`ErpCrmLeadBizModel`（service entity）；`IErpCrmEventBiz`、`IErpCrmLeadScoreBiz`、`IErpCrmForecastBiz`、`IErpCrmForecastPeriodBiz`（dao biz）；`ErpCrmErrors`、`ErpCrmConstants`（service）；`app-service.beans.xml`（bean 注册）
- 文档：`docs/design/crm/README.md`（配置点 + 偏离补注）、`lead-scoring.md`、`sales-forecast.md`（偏离补注）

---

## CRM 线索→商机→报价单转化（crm）— plan 2026-07-04-0549-2

**计划**：`docs/plans/2026-07-04-0549-2-crm-lead-opportunity-quotation-conversion.md`（Plan Status: completed）
**设计**：`docs/design/crm/README.md`（已补「实现偏离补注」）

### 验证基线（全绿）

- `mvn clean install -DskipTests`：BUILD SUCCESS（146 reactor 模块 / 19 顶层）
- `mvn test -pl module-crm/erp-crm-service`：14 tests pass（5 既有零回归 + 9 新增 `TestErpCrmLeadConversion`）
- `mvn test -pl module-crm/erp-crm-service -am`：含 sales 69 tests pass（跨域 I*Biz 调用零回归）

### 本次完成的工作

1. **Phase 1 — Lead docStatus 状态机 + 漏斗阶段流转 + 查重**：
   - `ErpCrmLeadBizModel`(Facade) + `ErpCrmLeadProcessor`(protected step)：qualify(NEW→QUALIFIED+默认 stageId) / lose(NEW/QUALIFIED→LOST, lostReasonId 必填) / cancel(→CANCELLED) + moveStage(允许回退+convLog 全量留痕+probability 为空取阶段 defaultProbability)
   - `LeadDuplicateChecker`：companyName/contactEmail/contactPhone 命中既有非终态线索；`erp-crm.auto-convert-duplicate-lead=false`（默认）仅提示不阻断
   - `ErpCrmErrors`/`ErpCrmConstants`

2. **Phase 2 — 转化闭环（convertToCustomer / convertToQuotation）**：
   - `IErpCrmConversionBiz` 契约 + `ErpCrmConversionProcessor`：LEAD→经 `IErpMdPartnerBiz.save` 建客户 + 新建 OPPORTUNITY + 原 lead 弱指针(CRM_LEAD)+CONVERTED；OPPORTUNITY→经 `IErpSalQuotationBiz.save` 建报价单 + 弱指针(SALES_QUOTATION)+CONVERTED
   - 幂等 `ERR_LEAD_ALREADY_CONVERTED`；`ERR_OPPORTUNITY_PARTNER_REQUIRED`；`ERR_LEAD_TYPE_MISMATCH`
   - 核心零污染：sales/master-data 实体零字段新增（crm-dao 新增 sal-dao compile 依赖仅为引用 ErpSalQuotation 返回类型）

3. **Phase 3 — 端到端测试**：`TestErpCrmLeadConversion`（9 用例：全链路 + 5 异常路径 + 查重 + 零污染反射断言）

### 实现偏离（已记入 crm/README.md）

- 转化动作（convertToCustomer/convertToQuotation）实现于 `ErpCrmLeadBizModel`（@BizModel("ErpCrmLead")）而非独立的 ErpCrmConversion BizModel：非实体 BizModel 不会被 GraphQL 自动注册为业务对象（unknown-biz-obj-name）。`IErpCrmConversionBiz` 契约接口保留为衔接 seam，由 ErpCrmLeadBizModel 实现。计划 Phase 2 目标已允许此替代（"ErpCrmConversionBizModel（或 ErpCrmLeadBizModel 转化动作）"）。
- `IErpCrmLeadBiz.lose` 的 lostReasonId/lostReasonDesc 标注 `@Optional`：Nop GraphQL 对 `@Name` 参数默认非空校验（field-null-arg），须显式 @Optional 才能让业务校验在缺失时抛 `ERR_LOST_REASON_REQUIRED`。

### 新增/修改的关键文件

- 后端：`IErpCrmLeadBiz.java`、`IErpCrmConversionBiz.java`、`ErpCrmLeadBizModel.java`、`ErpCrmLeadProcessor.java`、`ErpCrmConversionProcessor.java`、`LeadDuplicateChecker.java`、`ErpCrmErrors.java`、`ErpCrmConstants.java`、`app-service.beans.xml`
- 跨域依赖：`erp-crm-dao/pom.xml`（+sal-dao）、`erp-crm-service/pom.xml`（+sal/md/inv/qua/fin service test deps）
- 测试：`TestErpCrmLeadConversion.java`

---

## VMI 所有权转移（inventory）— plan 2026-07-04-0549-1

**计划**：`docs/plans/2026-07-04-0549-1-inventory-vmi-ownership-transfer.md`（Plan Status: completed）
**设计**：`docs/design/inventory/consignment.md`（已补「实现偏离补注」）

### 验证基线（全绿）

- `mvn clean install -DskipTests`：BUILD SUCCESS（146 reactor 模块 / 19 顶层）
- `mvn test -pl module-inventory/erp-inv-service`：49 tests pass（45 既有零回归 + 4 新增 `TestErpInvOwnershipTransfer`）
- `mvn test -pl module-finance/erp-fin-service`：93 tests pass（generator + business-type 改动零回归）

### 本次完成的工作

1. **Phase 1 — owner 维度建模**：
   - `ErpInvStockBalance`/`ErpInvStockLedger` 加 `ownerId`(BIGINT→ErpMdPartner) + `ownershipType`(dict `erp-inv/ownership-type`，默认 OWNED)
   - 新字典 `erp-inv/ownership-type`（OWNED/VMI_SUPPLIER/CONSIGNMENT_OUT/CUSTOMER_PROVIDED）
   - `StockMoveBookkeeper` 余额键 config-gated：`erp-inv.ownership-tracking-enabled=false`（默认关）时 ownerId 一律 null、不入键，与基线逐字节一致

2. **Phase 2 — 所有权转移单状态机 + 同库位调账**：
   - 新实体 `ErpInvOwnershipTransfer`/`Line` + 字典 `erp-inv/ownership-transfer-type`/`status`
   - `ErpInvOwnershipTransferBizModel`(Facade) + `ErpInvOwnershipTransferProcessor`(protected step)：DRAFT→CONFIRMED→DONE/CANCELLED
   - DONE 同库位重分类（改 ownershipType/ownerId，数量守恒，不移动）；不变量校验 sourceLocId==destLocId + tracking-enabled 门控 + transferType/ownershipType 一致性

3. **Phase 3 — OWNERSHIP_TRANSFER 业财过账 + AP 联动**：
   - `ErpFinBusinessType.OWNERSHIP_TRANSFER(260)` + 同步 `erp-fin/business-type` 字典
   - `InvOwnershipTransferProvider`（借 1401 存货 / 贷 2202 应付-供应商）
   - `OwnershipTransferPostingDispatcher`（config 门控 `vmi-auto-generate-ap`，非 VMI_CONSUME 不生成应付）
   - `ErpFinArApItemGenerator.resolveProfile` 新增 OWNERSHIP_TRANSFER → DIRECTION_PAYABLE + SOURCE_BILL_OWNERSHIP_TRANSFER（复用既有核销链）

### 实现偏离（已记入 consignment.md）

- `ErpInvOwnershipTransfer.transferType` 列 VARCHAR(20)→(30)：字典值 `OWNERSHIP_TO_CUSTOMER`（21 字符）超长
- VMI_SUPPLIER 余额本期经直接余额种子建立（VMI 收货流属 Non-Goal successor）

### 新增/修改的关键文件

- 模型：`module-inventory/model/app-erp-inventory.orm.xml`、`module-finance/model/app-erp-finance.orm.xml`
- 后端：`ErpInvOwnershipTransferProcessor.java`、`ErpInvOwnershipTransferBizModel.java`、`IErpInvOwnershipTransferBiz.java`、`InvOwnershipTransferProvider.java`、`OwnershipTransferPostingDispatcher.java`、`StockMoveBookkeeper.java`、`ErpInvConstants.java`、`ErpInvErrors.java`、`ErpFinBusinessType.java`、`ErpFinConstants.java`、`ErpFinArApItemGenerator.java`、`app-service.beans.xml`
- 测试：`TestErpInvOwnershipTransfer.java`（4 用例：VMI_CONSUME happy / tracking-disabled 拒绝 / loc-mismatch 拒绝 / 非 VMI_CONSUME 无应付）

## 全域字典 int→string 重构完成

**计划**：`docs/plans/2026-07-03-2108-1-dict-int-to-string-refactor.md`（Plan Status: completed）

### 验证基线（全绿）

- `mvn clean install -DskipTests`：BUILD SUCCESS（146 reactor 模块）
- `mvn test`：BUILD SUCCESS（全域全量测试通过）
- grep 门控：`valueType="int"` 命中 0；`ext:dict=.*INTEGER` 命中 0；`set[A-Za-z]*(Status|Type|Direction)\([0-9]+\)` 命中 0；`(!=|==)\s*Erp[A-Za-z]*Constants` 命中 0

### 本次完成的工作

1. **Phase 4 测试代码迁移**（主要工作量）：
   - inventory (6 files)、purchase (19 files)、sales (16 files)、assets (5 files) 测试中 `setStatus(10)`/`setPartnerType(10)`/`setDirection(10)` 等数值字面量 → 对应 String code（如 `"ACTIVE"`/`"CUSTOMER"`/`"DEBIT"`）
   - `baseReq(Integer moveType)` 等 helper 签名 `Integer`→`String`
   - `findBillLink(String, int businessType)` → `String businessType`；调用 `ErpFinBusinessType.XXX.getCode()` → `.name()`
   - D4 比较语义修正：`TestErpSalDeliveryStockMove` 中 `residual.getDocStatus() != ErpInvConstants.DOC_STATUS_DONE` → `!ErpInvConstants.DOC_STATUS_DONE.equals(residual.getDocStatus())`
   - `TestErpMdSupplierApprovalStateMachine` 中 `(Number)resp.getData().get("status")).intValue()` → 直接 String 比较

2. **CRUD 快照输出重录制**：17 个 `*CrudSmoke` 测试经 `forceSaveOutput=true` 重跑重录制，dict 字段值从 int (10/20) 同步为 String code（`"INCOMING"`/`"DRAFT"` 等）；delete 用例 CSV 的 `DEL_VERSION` 列改为通配符 `*`（规避逻辑删除时间戳非确定性）。

3. **Phase 1 误伤回退**（执行中发现的前期遗留）：
   - 多个非 dict 的 DECIMAL 域字段（budget/hours/costRate/weight/volume/dimension/downtimeMinutes 等）在前期 Phase 1 执行中被误改为 VARCHAR，逐域回退为 DECIMAL
   - `measuredValue` 字段因需支持文本实测值（"OK"/"PASS"）保留 VARCHAR
   - 受影响业务代码同步修正：quality（InspectionResultEvaluator / TemplateLineSpec / InspectionLineResultInput）、projects（ProjectCostAggregator / CostRateResolver / TimesheetBizModel / BudgetChecker / TimesheetPostingDispatcher）、maintenance（VisitBizModel / DowntimeEntryBizModel）、assets（CapitalizationAcctDocProvider：`readInt` → `readCode`）

4. **ErpFinCashForecastBizModel 比较语义修正**：`le("status", ...)` 过滤器对 String 列做字典序比较无意义，改为 `in("status", ...)` 枚举有效状态码。

### 文档对齐

- `docs/architecture/system-baseline.md` D1 条目从 "Deferred+前向指导" 改为 "已落地"，记录 string dict + Objects.equals 比较约束。
