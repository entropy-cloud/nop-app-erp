# Development Log - 2026-08-25

### OA-02 修复：closePeriod 同事务 FX 凭证写侧 flush → PERIOD_CLOSE 必含 FX 腿（plan `docs/plans/2026-08-25-0330-2-fix-closeperiod-fx-flush-profit-loss.md`，三 Phase done，会计保护区域双独立子代理批准）

- **Phase 1 缺陷登记 + flush 边界修复**：`docs/bugs/2026-08-25-closeperiod-fx-flush-profit-loss-missing-leg.md` 登记 + `docs/audits/arm-index.md` P1 汇总区补「开放式审计（OA）」回链段（OA-02 反向回填通道）。flush 落点 Decision 选 **③写侧自防御**：`CloseVoucherWriter.writeVoucher` 末尾 `((IOrmEntityDao<?>) voucherDao).getOrmTemplate().flushSession()`（+11 行含注释）——决定性证据 = 试算平衡快照对同事务 FX **与 PL 两凭证全部不可见**（C13 旧快照实证无 6603/4103 腿），①closeGlModule 内 flush/②聚合入口 flush 均无法让试算平衡看到 PL 凭证（PL 凭证由聚合服务自身在聚合后写入），仅写侧单点全覆盖（PL 聚合/试算平衡/年度结转聚合/未来新增同事务凭证源）；AST 折旧走 `IErpFinVoucherBiz.post` REQUIRES_NEW 天然可见、INV 成本重算不产 GL 凭证且自带 flush；closeAnnual 同型不可见经③顺带覆盖（年度结账端到端验证仍归 Deferred successor）。6 调用方逐一评估（双批准复核）：均事务内单凭证流，flush≠commit 回滚语义不变。**Protected-Area Approval Record**：Approver A（`ses_fca4d82ddffe4sQvjZuu04cnLI`）+ Approver B（`ses_fca4d594dffeBVtpHWcCYZQQuJ`）fresh session 双 **APPROVE**，记录落计划文件。Proof：`mvn test -pl module-finance/erp-fin-service` **497/0/0 全绿**——伴随语义对齐：`TestErpFinAnnualClose.testBankFxRevaluationForeignAccount` 原「银行 FX 不触发 P&L 结转」前提为缺陷行为产物（FX 收益腿现在正确触发结转）→ 补 seed 4103 + 6603 归零断言；7 类 15 处快照差量全数归因（flush 拆分延迟写 → 乐观锁 VERSION 递增 + 序列 ID 位移 + 试算平衡 PL/FX 腿新增 + 年度结账次年期间 ID 23→24）RECORDING→CHECKING 往返重录。
- **Phase 2 C13 断言与快照翻转**：`PL_TOTAL` 1130→**1280**；4103 双腿方向化锚定（新增 `findLineBySubjectAndDirection`：Cr 1130 收入侧 + Dr 150 费用侧）+ 新增 Cr 6603 150 FX 结转腿断言 + `subjectNetAmount` 6603 结账后归零断言（对齐 fin-service `testProfitLossClosingIncludesFxGainLoss` 语义）；反结账/重结账幂等断言经常量自动 1280 口径（红字 -1280/重结 1280）。快照重录：output tables 语义差量 = PL 凭证 4 腿 1280 ×3（原凭证/红字/重结）+ 试算平衡两批次各 +2 行（6603 Dr150/Cr150 归零、4103 Dr150/Cr1130 → 净贷 **980** = 1130−150）+ 1122 含 FX Cr150 + 5001 含结转 Dr1130 归零；响应 json5/input tables 零语义差量（重录字面量覆盖 HEAD `*` 通配纪律 → 还原 HEAD）；CHECKING 往返绿。Proof：`mvn test -pl app-erp-all` **54/0/0/1 全绿**（其余 21 用例零漂移）。
- **Phase 3 owner-doc 裁决与勘误**：`period-close.md` §期末结账步骤新增「同事务期末凭证可见性裁决（2026-08-25）」——flush 边界为实现契约（CloseVoucherWriter 写侧统一 flushSession）+ PERIOD_CLOSE 必含 FX 腿 + 汇兑损益结账后净额归零派生不变量 + bug 回链；「不含 FX」用例禁令随裁决落盘解除。`integration-testing.md` C13 勘误(2) 改写「已修复」：1280 四腿分解 + 裁决回链 + 两套件语义统一。
- **跨模块差量（sales）**：`TestErpSalMultiCurrencyReconFx.testMultiCurrencyReconciliationFxGain` 快照 `erp_fin_reconciliation` VERSION 1→2（核销 FX 走 `AbstractErpFinReconciliationProcessor` → CloseVoucherWriter 写侧 flush 同因拆分）——业务字段零漂移，重录归因后绿。
- **验证（全绿）**：`mvn clean install -DskipTests` 156 模块 BUILD SUCCESS（01:41）；`mvn test -pl module-finance/erp-fin-service` 497/0/0；`mvn test -pl app-erp-all` 54/0/0/1；全 reactor `mvn test` BUILD SUCCESS（13:02，surefire XML 干净口径 **3834/0/0/1/642——对照 V.1 基线逐项零漂移**）；`bash docs/audits/nop-compliance-checker.sh` 全规则 actual==baseline 零漂移。基线行落 `docs/testing/known-good-baselines.md` 2026-08-25 行。

### OA-01 修复：notify 模板族 + CS 编号规则聚合进 app `_init-data` + 快照重录 + owner-doc 真相修正（plan `docs/plans/2026-08-25-0330-1-aggregate-notify-cs-seeds-into-app-init-data.md`，三 Phase done）

- **Phase 1 种子聚合落盘**：新增 `app-erp-all/src/main/resources/_vfs/_init-data/erp_sys_notification_template.csv`（27 行模板族 7101-7106/7111-7114/7121-7124/7131-7134/7201-7209，转制自 `module-notify/deploy/sql/{mysql,oracle,postgresql}/_seed_erp-notify.sql`）+ `nop_sys_code_rule.csv`（1 行 `cs-ticket-code` TK 编号规则，转制自 `module-cs/deploy/sql/{三方言}/_seed_erp-cs.sql`）。资产口径 94 → **96 CSV + 1 SQL**。
  - Decision（载体选型）：选 CSV 而非追加 `.sql`——与既有 96 CSV 同构、参与 `TestErpSeedDataIntegrity` 引用完整性门禁、`DataInitInitializer` 拓扑序自动加载；`.sql` 绕过门禁与拓扑序仅作回退（未触发）。CSV 列头对齐既有约定：省略审计列（CREATED_BY/CREATE_TIME/UPDATED_BY/UPDATE_TIME）与 DEL_VERSION/VERSION（`erp_cs_ticket.csv`/`nop_auth_role.csv` 先例；既有 94 CSV grep 零审计列命中）；`nop_sys_code_rule` 含 DEL_FLAG+REMARK（`nop_auth_role.csv` 平台表先例）；JSON 字段 RFC4180 引号转义（`"{""userIds"":...}"`，module-notify `_cases` 同款先例）。**三方言一致性核对**：notify/cs 两族 mysql/oracle/postgresql 三方言 diff **零差异**（连时序函数差异都无——`CURRENT_TIMESTAMP` 字面同构），CSV 转制无方言歧义。
  - Proof：`mvn test -pl app-erp-all -Dtest=TestErpSeedDataIntegrity,TestAuthSeedLoadingProof` 全绿（2/2 + 3/3）；TestErpSeedDataIntegrity fresh-DB 留存 H2（db/erp-integrity.mv.db）实测 `ERP_SYS_NOTIFICATION_TEMPLATE` **COUNT=27** + `NOP_SYS_CODE_RULE` 含 `cs-ticket-code-rule`（CODE_PATTERN=`TK{@year}{@month}{@csTicketMonthSeq:4}`）。
- **Phase 2 受影响面评估 + 快照重录**：
  - **四档清单（逐用例触发模板映射，重录后 output/tables/erp_sys_notification.csv 实证）**：
    - 确定受影响（已重录）：**C01**（`wf.pur-payment.task-assigned` ×2 → 用户 2=财务员 ROLE 解析；7111 result 被 end-listener 幂等守卫早退、7131 cc 因 ROLE 财务经理无种子角色行——双不触发；default 序列 ID 漂移 +2）；**C04**（7202 `cs.ticket-created`→提单人 autotest-ref + 7204 `cs.knowledge-suggest-create`→处理人 cs-agent-01；+2）；**C16**（7202→autotest-ref + 7204→it-c16-agent + 既有自包含 7109；**7101 SLA 预警实证未触发**——resolve 达标路径不进 overdue 升级分支，草期「不得默认在列」预判成立）；**C17**（7114 `wf.hr-salary.result`→提单人 autotest-ref + 既有自包含 7109；7124/7134 因 ROLE「HR专员/财务主管/部门负责人」无种子匹配（种子角色名「HR 专员」带空格）+ 无 user_role 映射不触发；+1）。
    - 零落库（全绿实证，快照零影响）：**C03**（receipt xwf 三通知全零落库——7122/7132 ROLE 经理·销售经理无匹配；7112 result 被幂等守卫早退（approve RPC 先置 APPROVED，与 C17 直驱 invokeStep 的时序差异））；**C07**（7106 差异 +2 < 阈值默认 100）；**C05/C19**（7201 派发方为 cron 门控 Job）；**C10**（7208/7209 用例零 downtime 触达）。
    - 明确不触发：`wf.ast-disposal.*`（7113/7123/7133，it-cases 零 ErpAstDisposal 触达）。
    - 激活但无 it 用例消费方：7101（升级链/scan job）、7102（过账异常 Recorder，it 过账全成功）、7103（信用超限 Checker，额度内）、7104/7205/7207（job/履行链）、7203（assign-no-match，C04/C16 均显式指定处理人/候选池空留 NEW）、7206（履行失败路径）——全绿 + grep 归档。
  - **重录执行**：C01/C04/C16/C17 四用例类级临时 `snapshotTest = SnapshotTest.RECORDING` → 运行（snapshot-finished 预期异常）→ 还原注解 → CHECKING 往返全绿。**录制噪音治理**：input/tables 时间戳刷新/新增文件全部还原或清除（mechanism (c) tableInit=false 下惰性）；C16 墙钟字段（response 的 startDateTime/endDateTime/duration/respondedAt/sentAt + `erp_cs_ticket.csv` DURATION 列）与 C17（createTime/updateTime/sentAt）录制器输出字面量/@var: 别名 → 手工恢复 B4-B10 `*` 通配纪律；C01 nop_wf_step_instance actor 列 `*` → 确定性字面量（finance/starter，CHECKING 往返实证稳定）保留。
  - **断言污染复核**：实证零污染——新通知接收人（autotest-ref/用户 2/cs-agent-01/it-c16-agent）与 C16/C17 自包含断言接收人（it-c16-user/it-c17-user）完全隔离，`notificationsOf`/`countUnread(userId=...)` 参数化；54/0/0/1 全绿无需改任何断言。
  - **面 1 评估**：零影响——`nop.orm.init-database-data=true` 全仓测试代码仅 app-erp-all 声明（域模块零命中，平台默认 false）；域级快照走 input/tables tableInit 自备种子（module-cs `_cases` 自备 `nop_sys_code_rule.csv` 先例）。
  - **E2E 评估**：零影响——唯一触达通知端点的 `notify-inbox.action.spec.ts` 自包含（900000000+ 段模板 id + 独立 eventType + 接收人 nop）全相对断言（`toBeGreaterThanOrEqual`/`toContain`/`beforeN-1`）；`cs-ticket.action.spec.ts` 显式提供 code 不依赖 TK 规则。
- **Phase 3 owner-doc 修正**：`integration-testing.md` §C16 勘误(4) 划线改写——根因从「未实现动作面」修正为「模块 deploy 种子未聚合进 app 装配（2026-08-25 已聚合修复，plan 本计划）」+ 聚合后落库行为（7202/7204）与 7101 仍仅 overdue 路径的表述；C17 勘误(3) 补注 7114 已落库（markPaid 结论不变）。`seed-data.md` 新增「模块 deploy 种子 ↔ 聚合 `_init-data` 种子同步义务（强制）」节（规则 + 登记表（notify/cs 已聚合 + 全仓无其他 `_seed_*`）+ 联动义务）；计数对齐：seed-data.md（头部/快照重录义务/95-vs-94 归一）+ integration-testing.md（B2 基线行 + 8 处「全量 94 seed」族）+ roadmap（资产行/既有资产/风险②/M0.1/裁决3/V.2 注记）+ e2e-runbook（装载行）全部 → **96 CSV + 1 SQL**；变体 grep（94 CSV/94 csv/94 seed/95 CSV/94 张）在 docs/design|architecture|testing|backlog 归零；`docs/backlog/README.md:126`「92→94」E2E spec 计数未触碰。
- **验证（全绿）**：`mvn test -pl app-erp-all` **54/0/0/1 BUILD SUCCESS**（快照重录后两轮一致，含 TestErpSeedDataIntegrity 2/2）；`bash docs/audits/nop-compliance-checker.sh` 18 规则计数与 `compliance-baseline.md §BASELINE` 机器可读块**逐项相等零漂移**（零生产代码变更，符合预期）；`mvn clean install -DskipTests` 全 reactor **BUILD SUCCESS**（01:47）。
- **提交说明义务登记（seed-data.md 义务规则 3，供提交时引用）**：seed 变更 = 新增 `erp_sys_notification_template.csv`（27 行）+ `nop_sys_code_rule.csv`（1 行）→ 面 2 重录 `TestErpC01P2pGoldenPath`/`TestErpC04SalReturnWithCs`/`TestErpC16CsSlaNotification`/`TestErpC17HrSalaryPayment` 四用例快照（通知行 + 序列 ID 漂移）；面 1 零影响（域模块不消费 `_init-data`）；E2E 零影响（全相对断言自包含，零 spec 修改）。

### integration-test V.2 收尾对齐 + mission 收口（plan `docs/plans/2026-08-25-0232-1-v2-closure-alignment-docs-registration.md`，三 Phase done）

- **Phase 1**：`docs/architecture/seed-data.md` 增「快照重录义务（seed 变更联动，强制）」节（置于「导入策略」后）——双面资产盘点（面 1 各域 `module-<domain>/erp-*-service/_cases/`（19 域实仓复核）+ 面 2 `app-erp-all/_cases/io/nop/app/all/it/` 22 用例 + 试点共 23 类）+ 义务规则（`_vfs/_init-data/` 94 CSV + `zz-sequence-advance.sql` 变更 → 变更方双面同步重录 + 提交说明登记重录范围 + E2E 数值断言联动评估）+ 交叉引用（roadmap 横切关注点 1/3、e2e-runbook fresh-DB 纪律双向互指）。
- **Phase 2**：e2e-runbook「集成测试」段刷新至 V.1 口径——基线小节 = 23 集成类三层全比对全绿 + `mvn test -pl app-erp-all` **54/0/0/1（套件墙钟 104s）** + 权威计数源 known-good-baselines 2026-08-25 V.1 行（不内联复制）；运行方式注释 `29/0/0/1` → `54/0/0/1` + 补用例类范本 `-Dtest=TestErpC01P2pGoldenPath`；fresh-DB 纪律补 seed-data.md「快照重录义务」回指（双向互指）；三小节核对零陈旧计数。`docs/index.md` 补集成测试双路由（`docs/design/integration-testing.md` + e2e-runbook「集成测试」节；roadmap 覆盖面审查 MINOR 采纳项，实仓 grep 零命中为真实缺口）。
- **Phase 3**：roadmap V.2 → done + 头部「最后更新」mission 收口注记（M0.x + B1-B10 + V.1 + V.2 全 done）；README P8 → ✅ done（完成口径行内登记：22 用例 19 域 ≥2 + V.1 全绿基线 + V.2 收尾登记）；本日志条目。
- **验证**（纯文档计划：Closure Gates 以文本一致性核查 + grep 复核替代构建/测试门控——零代码/零快照/零 seed 变更，V.1 基线口径冻结）：e2e-runbook 集成测试节 `29/0/0/1` 零残留 ✓；index.md `integration-testing` 命中 ✓；双面路径实仓存在 ✓；五处文本（roadmap/README/e2e-runbook/seed-data/日志）计数与结论一致（54/0/0/1、23 类、双面路径口径）✓。验证命令状态 n/a（无可执行资产变更）。
- **VERIFY 独立复跑**（mission-driver 2026-08-24-223318，docs-only 工作集，零代码/零快照/零 seed 变更）：`mvn clean install -DskipTests` 全 reactor **BUILD SUCCESS**（156 模块，02:21，模块数零漂移）；全量 `mvn test` **BUILD SUCCESS**（17:39）——surefire XML 权威计数 **3834 tests / 0 failures / 0 errors / 1 skipped / 642 报告文件**，与 known-good-baselines 2026-08-25 V.1 基线行**完全一致零漂移**；唯一 skipped = `ErpAllWebPagesCollectTest` `@Disabled` 预存（JDK26/ANTLR H-2）维持；E2E 未重跑（零可执行资产变更，V.1 构造性满足裁定维持）。
- **mission 收口**：integration-test mission 全工作项（M0.1-M0.3 + B1-B10 + V.1 + V.2）done。

### G-001 修复收尾：flux CrudQueryFormConfig.mode 语义统一 + workaround 移除 + 全链路发布验证（plan `2026-08-24-2115-1-fix-flux-crud-queryform-mode-semantics.md`，全部 Phase done）

- **上游修复确认**（nop-chaos-flux commit `ce5e53145`）：`CrudQueryFormConfig.mode` 类型扩展为 `'manual'|'auto'|'normal'|'horizontal'|'vertical'|'inline'` 并集；`data-schema-validation.ts` 新增导出 `resolveFormMode(layout, mode)`（`mode` 为 label 位置值时优先，`vertical` 归一为 `normal`，legacy `manual/auto` 回退 `layout` 派生），接入 queryForm region 编译。
- **workaround 移除确认**（nop-entropy commit `836a4c4cf5`）：`flux-web/grid_crud.xpl` queryForm 仅设 `mode`（不再同时设 `layout+mode`），注释引用 G-001。
- **验证状态：全绿** — ① flux `pnpm --filter @nop-chaos/flux-renderers-data typecheck` 通过 + `test` **120 文件/882 tests 全绿**（新增 `crud-query-form-mode-resolution.test.ts` 11 case，覆盖 plan 全部 6 类）；② `mvn install -pl nop-frontend-support/nop-web -DskipTests` BUILD SUCCESS；③ `scripts/rebuild-flux-chain.sh` 全链路 5 步完成，runner jar 重建（02:15）；④ 浏览器视觉验证 7 PNG 全生成，视觉模型确认 `01-crud-voucher`/`02-crud-purchase` queryForm label 全部同行左置、无 label-above-input（08-24 遗留 IoC 循环依赖阻塞在本轮 clean rebuild 后不复现）；⑤ `mvn test -pl app-erp-all` **54/0/0/1** 与 B10 基线零漂移。
- **入档**：gotcha `docs/architecture/flux-integration-gotchas.md` G-001 状态翻为「已修复」（workaround 移除 + 修复证据 + 验证结论）；plan Closure Audit Record 已回填、Plan Status → completed。
- **VERIFY 独立复跑**（02:28，docs+PNG 工作集，零代码变更）：`mvn install -DskipTests -pl app-erp-all` BUILD SUCCESS（增量，无 clean）+ `mvn test -pl app-erp-all` **54/0/0/1 BUILD SUCCESS**，与 B10/V.1 基线零漂移。

### integration-test V.1 全量回归与全绿基线登记（plan `2026-08-24-1943-1-v1-full-regression-green-baseline.md`，full-green verification）

- **Phase 1 边界锚定**：覆盖矩阵 22 用例 ↔ 实仓 `app-erp-all/src/test/java/io/nop/app/all/it/` 类 1:1 核验（19/19 域 ≥2，Σ=88，roadmap 规则 5）；E2E 零回归边界采**构造性满足裁定**——计划来源枚举 M0.x+B1-B10 共 27 提交逐提交 `git show --name-only` 全列审计，全部仅触及测试 Java/`_cases`/docs/missions/app-erp-all pom（零生产零 seed），预先裁决例外 `750577323`/`63781668c` 复核确认；锚点 = 执行时点最新全量行（2026-08-25 delVersion 行 = B10 口径 3834/0/0/1/642）；`lsof` 8011/8080 无 live server + effective-pom 复证 `forkCount=1`/`parallel=none` 串行化生效。
- **Phase 2 模块级门**：`mvn test -pl app-erp-all` = **54/0/0/1 BUILD SUCCESS**（23 集成类全执行含 22 用例 CHECKING 三层全比对；与锚点逐项一致零差量）；套件墙钟 **104s**——远低于设计文档 §4 模型 8-26 分钟（模型高估 = 启动假设 ~12s/类，实测摊薄 ~3-4s/类）。
- **Phase 3 全量回归**：`mvn clean install -DskipTests` = **156 模块 BUILD SUCCESS**（01:47，模块数零漂移）；全 reactor `mvn test` = **BUILD SUCCESS**（13:58）——surefire XML 权威计数 **3834 tests / 0 failures / 0 errors / 1 skipped / 642 报告文件**，与锚点逐项零漂移（零新增失败/零新增 tests，差量归因义务空集）；证据拷贝 `_tmp/v1-surefire-evidence/`（642 XML，clean 前拷贝）。唯一 skipped = `ErpAllWebPagesCollectTest` `@Disabled` 预存 JDK26/ANTLR H-2 维持。
- **Phase 4 文档对齐**：known-good-baselines 增 2026-08-25 V.1 基线行；roadmap V.1 → done（含 details 口径漂移注记：2026-08-15 行文预存失败列举已清零，锚点 = 执行时点最新全量行口径）；设计文档 §4 增耗时实测注记。验证状态：**全绿**（install 156 + app-erp-all 54/0/0/1 + 全量 3834/0/0/1/642）。
- **mission-driver 2026-08-24-223318 VERIFY 步骤独立复核**：`mvn clean install -DskipTests` 全 reactor **BUILD SUCCESS**（01:40）；全量 `mvn test` **BUILD SUCCESS**（14:37）——surefire XML 权威计数 **642 报告文件 / 3834 tests / 0 failures / 0 errors / 1 skipped**，与本计划 Phase 3 及 known-good-baselines 2026-08-25 V.1 基线行**完全一致零漂移**；唯一 skipped = `ErpAllWebPagesCollectTest` `@Disabled` 预存（JDK26/ANTLR H-2）维持。

### delVersion 双仓重录 VERIFY 复核（plan `2026-08-24-0900-1-revert-delversion-var-masking-two-repo-rerecord`，full-green verification）

- **mission-driver 2026-08-24-223318 VERIFY 步骤独立复核**：`mvn clean install -DskipTests` 全 reactor **BUILD SUCCESS**（156 模块，01:42）；全量 `mvn test` **BUILD SUCCESS**（13:45）——surefire XML 权威计数 **642 报告文件 / 3834 tests / 0 failures / 0 errors / 1 skipped**，与 known-good-baselines 2026-08-25 行及 B10 基线**完全一致零漂移**；唯一 skipped = `ErpAllWebPagesCollectTest` `@Disabled` 预存（JDK26/ANTLR H-2）维持。
- 清零门控复核：`rg -rl "@var:[A-Za-z]+@delVersion" module-* app-erp-all --glob '*.csv' | wc -l` = **0**。
- 本轮 VERIFY 同时覆盖工作树内并行的视觉框架修复流（fin/hr/mnt 页面改动，`ErpAllFluxPagesTest` 页面级基线保持全绿）与 flux picker delta 构建产物追赶（nop-vfs-index/reflect-config），随本批一并提交。
