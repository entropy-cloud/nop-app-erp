# 客服工单域页面设计要点

> 本文档定义客服工单域关键业务页面的结构布局、交互模式与导航流程。
> 字段定义以 `module-cs/model/app-erp-cs.orm.xml` 为准，业务语义与状态机见 `state-machine.md`、`sla.md`。
> 调研引用格式 `[源项目#要点]`，详见 `docs/analysis/erp-survey/`。

## 设计原则

1. **SLA 状态可视化**：每个工单关联的 SLA 状态通过颜色指示灯（绿/黄/红）在列表和详情页清晰展示。
2. **状态驱动 UI**：操作按钮字段由工单状态联合控制，不依赖角色硬编码。
3. **活动日志完整可追溯**：工单详情页纵向展示所有操作日志（分派/备注/升级/解决），按时间倒序。
4. **知识库即用即查**：创建/编辑工单时实时推荐知识库文章，降低重复解答成本。
5. **看板式工单管理**：按状态分组的工单看板，支持快速分派和状态变更。

## 页面清单

| 页面 | 类型 | 主要用户 | 复杂度 | 对应实体 |
|------|------|----------|--------|----------|
| 工单列表 | 列表页 + 筛选面板 | 客服/主管 | ★★☆ | ErpCsTicket |
| 工单详情 | 仪表板式详情 | 客服/主管 | ★★★ | ErpCsTicket + ErpCsTicketAction |
| 工单看板 | 看板视图 | 客服主管 | ★★★ | ErpCsTicket |
| 客服仪表板 | 卡片式概览 | 客服经理 | ★★☆ | 聚合数据 |
| 知识库管理 | 列表+编辑 | 知识库管理员 | ★★☆ | ErpCsKnowledgeBase |
| SLA 策略配置 | 表单 | 管理员 | ★★☆ | ErpCsSlaPolicy |
| 工单类型配置 | 表单 | 管理员 | ★☆☆ | ErpCsTicketType |

## 通用页面模式

### 列表页结构

```
┌────────────────────────────────────────────────────┐
│ [新建工单]  [批量操作▾]                              │
│ ┌─ 筛选区 ──────────────────────────────────────┐ │
│ │ 工单状态: [☐NEW ☐ASSIGNED ☐IN_PROGRESS ...]   │ │
│ │ 优先级: [●全部 ●URGENT ●HIGH ●NORMAL ●LOW]    │ │
│ │ 工单类型: [下拉]  处理人: [选择]                 │ │
│ │ 客户: [选择]  创建日期: [从] ~ [至]              │ │
│ │ 关键字: [____]  [查询] [重置] [保存筛选方案▾]    │ │
│ └───────────────────────────────────────────────┘ │
├────────────────────────────────────────────────────┤
│ ┌────┬──────┬────────┬──────┬──────┬──────┬──────┐ │
│ │ #  │ 单号 │ 主题   │ 客户  │ 优先级│状态 │ SLA  │ │
│ │ 1  │TK-001│登录报错│某公司│ 🔴URG│ 处理中│ 🟡2h│ │
│ │ 2  │TK-002│打印机  │ 某公司│ 🟡NOR│ 已分配│ 🟢8h│ │
│ │ 3  │TK-003│网络故障│ 某公司│ 🔴URG│ 未分派│ 🔴-1h│ │
│ │    │           [查看] [操作▾]                   │ │
│ └────┴──────┴────────┴──────┴──────┴──────┴──────┘ │
│ 分页: [<] 1 2 3 ... [>]                            │
└────────────────────────────────────────────────────┘
```

**要点**：
- SLA 列显示剩余时间（绿=充裕，黄=即将超时，红=已超时）
- 优先级列用彩色标签：URGENT=红色、HIGH=橙色、NORMAL=蓝色、LOW=灰色
- 状态列用标签展示，颜色区分：NEW(灰)、ASSIGNED(蓝)、IN_PROGRESS(黄)、RESOLVED(绿)、CLOSED(灰)
- 操作按钮根据状态动态渲染：NEW 显示[分派][取消]，ASSIGNED 显示[开始处理][重新分派]，IN_PROGRESS 显示[解决][升级]
- 支持"我的工单"快捷筛选
- 参考 🟢 Axelor Helpdesk Ticket 列表

### 详情页通用结构

```
┌──────────────────────────────────────────────────────────┐
│ 工单: TK-2026-001  状态: ● 处理中   SLA: 🟡 剩余 2h     │
│ ──────────────────────────────────────────────────────── │
│ 工具栏: [分配] [开始处理] [标记解决] [取消] [打印]       │
│         [创建知识库文章] [操作历史]                        │
├──────────────────────────────────────────────────────────┤
│ ┌─ 工单信息 ────────────┬─ SLA 信息 ────────────────┐  │
│ │ 主题: ERP 登录报错 500 │ SLA 策略: 故障-urgent-4h  │  │
│ │ 客户: 某某有限公司     │ 创建时间: 06-30 09:00     │  │
│ │ 联系人: 张三(138xxx)  │ 截止时间: 06-30 13:00     │  │
│ │ 工单类型: 故障         │ 已用时长: 2h              │  │
│ │ 优先级: ● URGENT      │ SLA 状态: 🟡 即将超时      │  │
│ │ 处理人: 李四           │ 暂停总时长: 0             │  │
│ │ 团队: 技术支持组       │ 升级级别: L0（未升级）    │  │
│ │ 来源: 门户            │                           │  │
│ │ 关联单: SO-2026-001   │                           │  │
│ └───────────────────────┴─────────────────────────────┘  │
│ ──────────────────────────────────────────────────────── │
│ SLA 时间轴                                                 │
│ [创建 09:00] ── [分配 09:05] ── [开始 09:10] ── [现在]  │
│                        ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓░░░░░░░░         │
│                        已用 2h             剩余 2h        │
│ ──────────────────────────────────────────────────────── │
│ 解决方法 (处理人填写，RESOLVED 后可见)                      │
│ [解决方法描述: ____]                                      │
│ ──────────────────────────────────────────────────────── │
│ 活动日志 (操作时间轴)                                      │
│ 06-30 10:00  备注  李四: "已定位 bug，正在修复中"         │
│ 06-30 09:10  状态  李四: ASSIGNED → IN_PROGRESS          │
│ 06-30 09:05  分派  系统: → 李四（技术支持组）            │
│ 06-30 09:00  状态  系统: NEW → ASSIGNED（自动分派）       │
│ 06-30 09:00  创建  张三(客户): 新建工单                   │
│ [添加备注] [上传附件]                                      │
│ ──────────────────────────────────────────────────────── │
│ 关联文档                                                    │
│ [TK-2026-001_截图.png] [TK-2026-001_日志.txt]              │
└──────────────────────────────────────────────────────────┘
```

**要点**：
- SLA 信息区根据剩余时间改变背景色：>50%=绿色，<50%=黄色，超时=红色
- SLA 时间轴纯展示，不可交互
- 活动日志按时间倒序，显示操作人 + 时间 + 操作内容
- 每个状态变更显示前后状态（from → to）
- 解决方法区在 IN_PROGRESS 时显示为输入框，RESOLVED 后只读
- 关联单据（relatedBillType/relatedBillCode）显示为可点击链接跳转

---

## 各页面设计要点

### 工单看板

**页面入口**：售后服务 → 工单看板

```
┌──────────────────────────────────────────────────────────┐
│ [新建工单]  筛选: [我的团队] [全部]  排序: [优先级]      │
├──────┬────────┬────────┬────────┬────────┬──────────────┤
│ 待分派│ 处理中 │ 等待客户│ 已解决 │ 已关闭 │              │
│ (NEW)│ (IN_PR)│(PENDING)│(RESOLV)│(CLOSED)│              │
│      │        │        │        │        │              │
│ [卡片]│ [卡片] │ [卡片] │ [卡片] │ [卡片] │              │
│ TK001│ TK003  │ TK005  │ TK007  │ TK009  │              │
│ 🔴URG│ 🟡HIGH │ 🟢NORM  │ 🟢LOW  │        │              │
│ 张三  │ 李四   │ 赵六   │ 钱八   │        │              │
│ 02:00 │ 剩余3h │ 暂停2d │ 待确认 │        │              │
│      │        │        │        │        │              │
│ [卡片]│ [卡片] │        │        │        │              │
│ TK002│ TK004  │        │        │        │              │
│ 🟡HIGH│ 🔴URG  │        │        │        │              │
│ 未分配│ 王五   │        │        │        │              │
│ 已4h  │ 🔴-1h  │        │        │        │              │
└──────┴────────┴────────┴────────┴────────┴──────────────┘
```

**要点**：
- 列从工单状态字典动态读取，按业务流程排序
- 每个卡片显示：工单编号、主题（截断）、优先级标签、处理人、SLA 状态
- SLA 超时的卡片左上角显示 🔴 标记
- 支持拖拽变更状态（如从 NEW 拖到 ASSIGNED → 弹出分配处理人对话框）
- 待分派列 NEW 工单闪烁高亮提示
- 点击卡片 → 弹出工单预览浮层（不跳转详情页）
- 参考 🟢 Axelor Helpdesk Dashboard.xml

### 客服仪表板

**页面入口**：售后服务 → 仪表板

```
┌──────────────────────────────────────────────────────────┐
│ 客服仪表板                                                │
├──────────────┬──────────────┬──────────────┬──────────────┤
│ 待分派工单    │ 处理中工单    │ 今日已解决    │ SLA 达标率   │
│ 3            │ 12           │ 8            │ 92%          │
│ ⚠️ 1 已超 1h │ 🔴 2 超时    │ 🟢           │ 📉 较上月-3% │
├──────────────┴──────────────┴──────────────┴──────────────┤
│ 待处理工单按优先级分布 (柱状图)                              │
│ ┌────────────────────────────────────────────────────┐   │
│ │ URGENT: ████ 4     HIGH: ██████ 6                  │   │
│ │ NORMAL: ██████████ 10   LOW: ████ 4                │   │
│ └────────────────────────────────────────────────────┘   │
├──────────────────────────────────────────────────────────┤
│ SLA 达标率趋势 (折线图, 近 30 天)                          │
│ ┌────────────────────────────────────────────────────┐   │
│ │ 98% ████████████                                   │   │
│ │ 95% ████████████████                               │   │
│ │ 92% ████████████████████                           │   │
│ │ 90% ██████████████████████████                     │   │
│ │   06-01  06-07  06-14  06-21  06-28               │   │
│ └────────────────────────────────────────────────────┘   │
├──────────────────────────────────────────────────────────┤
│ 最近超时工单列表                                           │
│ ┌────┬──────┬────────┬──────┬────────┬────────┬──────┐  │
│ │ 单号│ 客户 │ 主题   │ 处理人│ 超时时长│ 状态   │ 升级 │  │
│ │TK003│ 某公司│ 网络   │ 李四  │ 2h     │ IN_PR │ L1   │  │
│ │TK005│ 某公司│ 打印机 │ 赵六  │ 5h     │ RESOL │ —    │  │
│ └────┴──────┴────────┴──────┴────────┴────────┴──────┘  │
└──────────────────────────────────────────────────────────┘
```

**要点**：
- 顶部四个 KPI 卡片：待分派/处理中/已解决/SLA 达标率
- KPI 卡片显示变化趋势（↑↓ 📈📉）
- 待分派工单显示红色警告（超 1h 未分派）
- 图表使用 AMIS 图表组件（柱状图 + 折线图）
- 最近超时工单列表可点击跳转详情页
- 参考 🟢 Axelor Helpdesk Dashboard.xml

### 知识库管理

**页面入口**：售后服务 → 知识库

**列表页**：

```
┌────────────────────────────────────────────────────┐
│ [新建文章]  [管理分类]  [导入]  [导出]               │
│ 搜索: [____]  分类: [下拉]  状态: [●已发布 ○草稿]   │
├────────────────────────────────────────────────────┤
│ ┌────┬────────────┬────────┬──────┬──────┬──────┐ │
│ │ #  │ 标题       │ 分类    │ 状态  │ 阅读量│ 操作│ │
│ │ 1  │ 如何重置密码│ 常见问题│ 🟢发布│ 1,230 │ ✎   │ │
│ │ 2  │ 打印机设置  │ 设备指南│ 🔴草稿│ —     │ ✎   │ │
│ │ 3  │ 报错500处理 │ 故障排除│ 🟢发布│ 860   │ ✎   │ │
│ └────┴────────────┴────────┴──────┴──────┴──────┘ │
└────────────────────────────────────────────────────┘
```

**编辑页**：

```
┌────────────────────────────────────────────────────┐
│ 标题: [____]                                        │
│ 分类: [下拉选择]  标签: [标签输入]  发布: [☑]       │
├────────────────────────────────────────────────────┤
│ 正文 (富文本编辑器)                                  │
│ ┌────────────────────────────────────────────────┐ │
│ │ B / I / U │ 标题 │ 列表 │ 代码 │ 插入图片      │ │
│ │ ────────────────────────────────────────────── │ │
│ │ [编辑区域]                                      │ │
│ └────────────────────────────────────────────────┘ │
├────────────────────────────────────────────────────┤
│ 关联工单: [TK-001] [TK-003] (文章关联的工单追溯)    │
│ 创建信息: 张三 | 06-20  最后更新: 李四 | 06-28      │
└────────────────────────────────────────────────────┘
```

**要点**：
- 文章标题搜索支持全文检索（title + content）
- 分类树形结构（支持多级分类，如"常见问题/登录/密码重置"）
- 标签自由输入，逗号分隔，支持标签筛选
- 富文本编辑器支持代码高亮、表格、图片上传
- 文章可关联工单（工单解决后建议创建知识库条目）
- 阅读量自动累加

### 知识库嵌入工单创建

```
客户/客服输入工单主题时：
        │
        ├─► 实时搜索知识库（title LIKE '%主题关键词%'）
        │
        ├─► 在工单表单下方显示 Top 5 推荐文章
        │
        └─► 客服/客户点击文章可参考解决方案：
                ├─ 采纳 → 工单直接 RESOLVED（自动填充解决方法引用文章）
                └─ 忽略 → 继续填写工单
```

### SLA 策略配置

**页面入口**：系统设置 → 售后服务 → SLA 策略

```
┌────────────────────────────────────────────────────┐
│ [新建策略]                                          │
├────────────────────────────────────────────────────┤
│ ┌────┬────────┬──────┬──────┬──────┬──────┬────┐ │
│ │ 策略名 │ 工单类型│ 优先级│ 团队  │ 时限 │ 工作 │  │
│ │       │        │ 最低 │      │      │ 日   │  │
│ │故障-紧急│ 故障   │URGENT│ 技术组│ 4h   │ ☑   │ │
│ │故障-高 │ 故障   │HIGH  │ 技术组│ 8h   │ ☑   │ │
│ │服务请求│ 服务请求│ NORMAL│ 客服组│ 24h  │ ☐   │ │
│ │咨询-低 │ 咨询   │ LOW  │ —    │ 72h  │ ☐   │ │
│ └────┴────────┴──────┴──────┴──────┴──────┴────┘ │
└────────────────────────────────────────────────────┘
```

## 跨页面导航流

```
工单列表 → [新建] → 工单详情（编辑模式）
    ↓                      ↓
[快速分派]             [活动日志] → 操作时间线
    ↓                      ↓
工单看板 → [拖拽变更状态] → SLA 时间轴
    ↓                      ↓
[超时升级]             [创建知识库文章] → 知识库编辑
    ↓
客服仪表板 ← [聚合数据] → SLA 月报
```

## 调研参考汇总

| 设计点 | 参考来源 | 应用方式 |
|--------|----------|----------|
| 工单列表 + SLA 状态指示灯 | 🟢 Axelor Ticket.xml | 绿/黄/红三色指示 SLA 状态 |
| 工单详情页活动日志 | 🟢 Axelor Ticket.xml | 纵向时间线展示操作记录 |
| 客服仪表板（KPI + 趋势） | 🟢 Axelor Helpdesk Dashboard.xml | 四卡片 + 柱状图 + 折线图 |
| 知识库分类 + 全文搜索 | 🟢 Axelor KnowledgeBase | 树形分类 + 全文检索 |
| 工单看板按状态分组 | 🟢 Axelor Helpdesk Dashboard.xml | 拖拽变更工单状态 |
| SLA 时间轴可视化 | 🟡 业界通用模式 | 水平时间轴 + 已用/剩余时长 |
| 知识库嵌入工单创建 | 🟢 Axelor Ticket.knowledgeBase建议 | 输入主题时实时搜索推荐 |
| SLA 策略配置 | 🟢 Axelor Sla.xml | 按类型/优先级/团队匹配 |
| 待分派工单超时高亮 | ⚪ 本项目设计意图 | 超过指定时间闪烁提示 |

## 树形 CRUD 范式（ServiceCatalogItem）

客服域含 1 个自引用树形实体：`ErpCsServiceCatalogItem`（服务目录树）。遵循 `docs/design/tree-entity-patterns.md` 跨域范式。

### 列集表

| 实体 | tree-list grid 列集 | 域专用业务约束 |
|------|---------------------|----------------|
| `ErpCsServiceCatalogItem` | `code` `name` `parentId` `ticketTypeId` `slaPolicyId` `fulfillmentProcessId` `isActive` `sequence` | 服务目录树，叶子节点关联工单类型/SLA 策略/履行流程；F12 工单详情目录树 successor 依赖 |

### 配置要点

1. **tree-list grid**：克隆 `<grid id="list">` + 追加 `<selection>children @TreeChildren(max:5)</selection>`
2. **crud main grid="tree-list"**：`<table loadDataOnce="true" sortable="false" pager="none">` + URL `@query:ErpCsServiceCatalogItem__findList/{@listSelection}?filter_parentId=__null`
3. **add-child simple page**：`<simple name="add-child" form="add"><data><parentId>$id</parentId></data></simple>` + rowActions 追加 `row-add-child-button`（用于「在某目录项下新建子项」）
4. **parentId tree-select 控件**：edit/add 表单 `<cell id="parentId">` 升级为 `<tree-select>`，URL 加 `filter_id__ne=$id` 排除自身防循环引用
5. **picker 升级**：picker.page.yaml table 改 tree 配置（`loadDataOnce + pager=none + filter_parentId=__null`）

### ErpCsServiceCatalogItem 域专用约束（F12 工单详情目录树 successor）

服务目录树叶子节点保留 `ticketTypeId`（工单类型）+ `slaPolicyId`（SLA 策略）+ `fulfillmentProcessId`（履行流程）业务字段。F12 工单详情 tabs 容器 successor 依赖此 tree CRUD 范式（用户在工单录入时通过 tree picker 选择服务目录叶子节点，自动带出关联的工单类型/SLA/履行流程）。

详细配置模板与反模式自检见 `docs/design/tree-entity-patterns.md`。
