<?xml version="1.0" encoding="UTF-8" ?>
<view x:extends="_gen/_ErpCtSignatureRequest.view.xml" x:schema="/nop/schema/xui/xview.xdef"
      xmlns:x="/nop/schema/xdsl.xdef">

    <grids>
                <grid id="list">
            <cols x:override="bounded-merge">
                <col id="id" mandatory="true" ui:number="true" sortable="true"/>
                <col id="orgId" label="业务组织"/>
                <col id="contractVersionId" mandatory="true" ui:number="true" sortable="true"/>
                <col id="provider" mandatory="true" sortable="true"/>
                <col id="providerRequestId" label="提供商请求"/>
                <col id="status" mandatory="true" sortable="true">
                    <gen-control>
                        <c:script><![CDATA[
                            const valueProp = dispMeta?.id || 'status';
                            const labelProp = propMeta?.['graphql:labelProp'] || (valueProp + '_label');
                            const successVals = ['COMPLETED','APPROVED','RECEIVED','DELIVERED','PAID','SETTLED','HONORED','RETRIED','EXECUTED','COMPUTED','CONFIRMED','RUNNING','MATERIAL_TRANSFERRED','ACTIVE','CLOSED','DONE','SUCCESS'];
                            const dangerVals = ['REJECTED','CANCELLED','FAILED','DISHONORED','OVERDUE','DOWN','TERMINATED','WRITE_OFF'];
                            const warningVals = ['ON_HOLD','SUSPENDED','EXPIRED','RETRYING'];
                            const primaryVals = ['SUBMITTED','IN_PROGRESS','PARTIAL','PARTIALLY_TRANSFERRED','WORK_IN_PROGRESS','PROCESSING','NEGOTIATION','OPEN','UNDER_MAINTENANCE','COLLECTION_PENDING','DISCOUNTED','ENDORSED','ISSUED'];
                            function quote(v){return "'" + v + "'";}
                            function inList(vals){return "[" + vals.map(quote).join(",") + "].indexOf(" + valueProp + ") >= 0";}
                            const expr = inList(successVals) + " ? 'success' : "
                                + inList(dangerVals) + " ? 'danger' : "
                                + inList(warningVals) + " ? 'warning' : "
                                + inList(primaryVals) + " ? 'primary' : 'default'";
                            return {
                                type: "tpl",
                                tpl: '<span class="label label-${' + expr + "}"
                                    + ">"
                                    + "${" + labelProp + "}</span>"
                            };
                        ]]></c:script>
                    </gen-control>
                </col>
                <col id="signers" sortable="true"/>
                <col id="signingDeadline" sortable="true"/>
                <col id="completedAt" sortable="true"/>
                <col id="certificateUrl" sortable="true"/>
                <col id="evidenceNo" sortable="true"/>
                <col id="attachmentFileId" sortable="true"/>
                <col id="errorMsg" sortable="true"/>
                <col id="remark" sortable="true"/>
                <col id="delVersion" mandatory="true" ui:number="true" sortable="true"/>
                <col id="createdBy" mandatory="true" sortable="true"/>
                <col id="createTime" mandatory="true" sortable="true"/>
                <col id="updatedBy" mandatory="true" sortable="true"/>
                <col id="updateTime" mandatory="true" sortable="true"/>
            </cols>
        </grid>
        <grid id="pick-list"/>
    </grids>

    <forms>
        <form id="view"/>
        <form id="edit"/>
        <form id="add"/>
    </forms>

    <pages>
        <crud name="main">
            <rowActions x:override="bounded-merge">
                <action id="row-view-button"/>
                <action id="row-update-button" actionType="drawer"/>
                <action id="row-query-status-button" label="查询状态" level="primary" icon="fa fa-search">
                    <api url="@mutation:ErpCtSignatureRequest__queryAndUpdateStatus?requestId=$id"/>
                    <confirmText>确认从签署提供商查询并更新状态？</confirmText>
                    <messages>
                        <success>签署状态已更新</success>
                    </messages>
                </action>
                <action id="row-reject-button" label="拒绝" level="warning" icon="fa fa-times">
                    <api url="@mutation:ErpCtSignatureRequest__rejectSignature?requestId=$id"/>
                    <confirmText>确认拒绝此签署请求？</confirmText>
                    <messages>
                        <success>签署请求已拒绝</success>
                    </messages>
                </action>
                <action id="row-cancel-button" label="取消" level="danger" icon="fa fa-ban">
                    <api url="@mutation:ErpCtSignatureRequest__cancelSignatureRequest?requestId=$id"/>
                    <confirmText>确认取消此签署请求？</confirmText>
                    <messages>
                        <success>签署请求已取消</success>
                    </messages>
                </action>
                <action id="row-delete-button"/>
            </rowActions>
        </crud>
        <picker name="picker"/>
    </pages>
</view>