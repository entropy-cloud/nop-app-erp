# 维护概览 dashboard（plan 2026-08-29-1913-2 Phase 1）。
# flux 原生重写：data-source → KPI 卡 + OEE + chart + 2 crud。
# 数据源：ErpMntDashboard__getDashboardKpi / __getDashboardOeeKpi / __getEquipmentStatusDistribution
#       / __findEquipmentDowntimeAlert / __findMaintenanceOverdueAlert。
type: page
title: "维护概览"
body:
  - type: form
    name: filterForm
    title: ""
    mode: inline
    wrapWithPanel: false
    className: m-3
    body:
      - type: input-date
        name: startDate
        label: "开始日期"
        format: YYYY-MM-DD
        placeholder: "留空=本月1日"
        clearable: true
      - type: input-date
        name: endDate
        label: "结束日期"
        format: YYYY-MM-DD
        placeholder: "留空=今天"
        clearable: true
      - type: button
        label: "刷新"
        icon: refresh-cw
        onClick:
          action: refreshSource
          targetId: kpiData
          then:
            action: refreshSource
            targetId: oeeData
          then:
            action: refreshSource
            targetId: statusData
          then:
            action: refreshSource
            targetId: downtimeData
          then:
            action: refreshSource
            targetId: overdueData

  - type: data-source
    name: kpiData
    action: ajax
    args:
      url: '@query:ErpMntDashboard__getDashboardKpi'
      method: post
      data:
        startDate: "${filterForm?.startDate || null}"
        endDate: "${filterForm?.endDate || null}"
      selection: equipmentTotal runningCount openRequestCount periodVisitCount

  - type: grid
    className: m-3
    gap: sm
    columns: 4
    items:
      - colSpan: 1
        body:
          - type: card
            className: border rounded p-3
            body:
              - type: text
                tag: div
                className: text-muted
                text: "设备总数"
              - type: text
                tag: span
                className: h3 font-bold
                text: "${kpiData?.equipmentTotal ?? 0}"
      - colSpan: 1
        body:
          - type: card
            className: border rounded p-3
            body:
              - type: text
                tag: div
                className: text-muted
                text: "运行中设备"
              - type: text
                tag: span
                className: h3 text-success
                text: "${kpiData?.runningCount ?? 0}"
      - colSpan: 1
        body:
          - type: card
            className: border rounded p-3
            body:
              - type: text
                tag: div
                className: text-muted
                text: "待处理维护请求"
              - type: text
                tag: span
                className: h3 text-warning
                text: "${kpiData?.openRequestCount ?? 0}"
      - colSpan: 1
        body:
          - type: card
            className: border rounded p-3
            body:
              - type: text
                tag: div
                className: text-muted
                text: "本期维护访问数"
              - type: text
                tag: span
                className: h3 text-primary
                text: "${kpiData?.periodVisitCount ?? 0}"

  - type: data-source
    name: oeeData
    action: ajax
    args:
      url: '@query:ErpMntDashboard__getDashboardOeeKpi'
      method: post
      data:
        startDate: "${filterForm?.startDate || null}"
        endDate: "${filterForm?.endDate || null}"
      selection: oeeDisplay availabilityDisplay performanceDisplay qualityDisplay computedCount equipmentTotal

  - type: wrapper
    className: m-3 border rounded p-3
    body:
      - type: text
        tag: div
        className: text-muted
        text: "设备 OEE（可用率×性能×质量）"
      - type: text
        tag: span
        className: h3 text-danger
        text: "${oeeData?.oeeDisplay ?? '—'}"
      - type: text
        tag: div
        className: text-muted text-sm
        text: "可用率 ${oeeData?.availabilityDisplay ?? '—'} · 性能 ${oeeData?.performanceDisplay ?? '—'} · 质量 ${oeeData?.qualityDisplay ?? '—'} · 样本 ${oeeData?.computedCount ?? 0}/${oeeData?.equipmentTotal ?? 0} 台"

  - type: data-source
    name: statusData
    action: ajax
    args:
      url: '@query:ErpMntDashboard__getEquipmentStatusDistribution'
      selection: status count

  - type: chart
    chartType: pie
    title: "设备状态分布"
    source: ${statusData ?? []}
    height: 300
    series:
      - { name: "设备数", dataRegionKey: count, nameField: status }

  - type: data-source
    name: downtimeData
    action: ajax
    args:
      url: '@query:ErpMntDashboard__findEquipmentDowntimeAlert'
      selection: equipmentCode equipmentName status

  - type: crud
    className: m-3
    title: "设备停机预警（状态=DOWN 且停机未恢复）"
    source: ${downtimeData ?? []}
    syncLocation: false
    headerToolbar: []
    footerToolbar: []
    columns:
      - name: equipmentCode
        label: "设备编码"
      - name: equipmentName
        label: "设备名称"
      - name: status
        label: "状态"

  - type: data-source
    name: overdueData
    action: ajax
    args:
      url: '@query:ErpMntDashboard__findMaintenanceOverdueAlert'
      selection: scheduleCode scheduleName equipmentId nextDueDate overdueDays

  - type: crud
    className: m-3
    title: "维护逾期预警（阈值经 erp-dash.mnt-maintenance-overdue-days 配置）"
    source: ${overdueData ?? []}
    syncLocation: false
    headerToolbar: []
    footerToolbar: []
    columns:
      - name: scheduleCode
        label: "计划编码"
      - name: scheduleName
        label: "计划名称"
      - name: equipmentId
        label: "设备ID"
      - name: nextDueDate
        label: "下次到期日"
      - name: overdueDays
        label: "逾期(天)"