<?xml version="1.0" encoding="UTF-8" ?>
<view x:extends="_gen/_ErpCrmEvent.view.xml" x:schema="/nop/schema/xui/xview.xdef" xmlns:x="/nop/schema/xdsl.xdef">

    <grids>
                <grid id="list">
            <cols x:override="bounded-merge">
                <col id="id" mandatory="true" ui:number="true" sortable="true"/>
                <col id="code" mandatory="true" sortable="true"/>
                <col id="orgId" label="业务组织"/>
                <col id="eventType" sortable="true"/>
                <col id="eventCategory" label="活动类别"/>
                <col id="subject" sortable="true"/>
                <col id="relatedLead" label="关联线索"/>
                <col id="partnerId" label="客户"/>
                <col id="contactId" label="联系人"/>
                <col id="parentEvent" label="父活动"/>
                <col id="startDateTime" sortable="true" align="center">
                    <gen-control>
                        <c:script><![CDATA[
                            return { type: 'datetime', format: 'YYYY-MM-DD HH:mm:ss' };
                        ]]></c:script>
                    </gen-control>
                </col>
                <col id="endDateTime" sortable="true" align="center">
                    <gen-control>
                        <c:script><![CDATA[
                            return { type: 'datetime', format: 'YYYY-MM-DD HH:mm:ss' };
                        ]]></c:script>
                    </gen-control>
                </col>
                <col id="status" sortable="true">
                    <gen-control>
                        <c:script><![CDATA[
                            const valueProp = dispMeta?.id || 'status';
                            const labelProp = propMeta?.['graphql:labelProp'] || (valueProp + '_label');
                            const successVals = ['COMPLETED','APPROVED','RECEIVED','DELIVERED','PAID','SETTLED','HONORED','RETRIED','EXECUTED','COMPUTED','CONFIRMED','RUNNING','MATERIAL_TRANSFERRED','ACTIVE','CLOSED','DONE','SUCCESS'];
                            const dangerVals = ['REJECTED','CANCELLED','FAILED','DISHONORED','OVERDUE','DOWN','TERMINATED','WRITE_OFF'];
                            const warningVals = ['ON_HOLD','SUSPENDED','EXPIRED','RETRYING'];
                            const primaryVals = ['SUBMITTED','IN_PROGRESS','PARTIAL','PARTIALLY_TRANSFERRED','WORK_IN_PROGRESS','PROCESSING','NEGOTIATION','OPEN','UNDER_MAINTENANCE','COLLECTION_PENDING','DISCOUNTED','ENDORSED','ISSUED'];
                            function quote(v){return "'" + v + "'";}
                            function inList(vals){return "[" + vals.map(quote).join(",") + "].indexOf(" + valueProp + ") >= 0";}
                            const expr = inList(successVals) + " ? 'success' : "
                                + inList(dangerVals) + " ? 'danger' : "
                                + inList(warningVals) + " ? 'warning' : "
                                + inList(primaryVals) + " ? 'primary' : 'default'";
                            return {
                                type: "tpl",
                                tpl: '<span class="label label-${' + expr + "}"
                                    + ">"
                                    + "${" + labelProp + "}</span>"
                            };
                        ]]></c:script>
                    </gen-control>
                </col>
                <col id="priority" sortable="true"/>
            </cols>
        </grid>
        <grid id="pick-list"/>
    </grids>

    <forms>
        <form id="view" size="lg">
            <layout x:override="replace">
=========>baseInfo[基本信息]======
 code[编码] orgId[业务组织]
 eventType[事件类型] eventCategoryId[事件分类]
 subject[主题] description[描述]
=========>schedule[时间安排]======
 startDateTime[开始时间] endDateTime[结束时间]
 duration[时长(分钟)]
=========>association[业务关联]======
 relatedLeadId[关联线索] relatedBillType[关联类型]
 relatedBillCode[关联单号] partnerId[往来单位]
 contactId[联系人] ownerId[负责人]
=========>status[状态]======
 status[状态] priority[优先级]
=========>recurrence[重复]======
 isRecurrent[是否重复] parentEventId[父事件]
 reminderMinutesBefore[提前提醒(分钟)]
========^audit[审计信息]=========
 remark[备注]
 createdBy[创建人] createTime[创建时间]
 updatedBy[修改人] updateTime[修改时间]
            </layout>
        </form>
        <form id="edit" size="lg">
            <layout x:override="replace">
=========>baseInfo[基本信息]======
 code[编码] orgId[业务组织]
 eventType[事件类型] eventCategoryId[事件分类]
 subject[主题] description[描述]
=========>schedule[时间安排]======
 startDateTime[开始时间] endDateTime[结束时间]
 duration[时长(分钟)]
=========>association[业务关联]======
 relatedLeadId[关联线索] relatedBillType[关联类型]
 relatedBillCode[关联单号] partnerId[往来单位]
 contactId[联系人] ownerId[负责人]
=========>status[状态]======
 status[状态] priority[优先级]
=========>recurrence[重复]======
 isRecurrent[是否重复] parentEventId[父事件]
 reminderMinutesBefore[提前提醒(分钟)]
========^audit[审计信息]=========
 remark[备注](2)
 createdBy[创建人] createTime[创建时间]
 updatedBy[修改人] updateTime[修改时间]
            </layout>
        </form>
        <form id="add" x:prototype="edit"/>
        <form id="query" editMode="query" title="查询条件">
            <layout>
 code[编码] subject[主题]
 orgId[业务组织] eventType[事件类型]
 status[状态] startDateTime[开始时间]
            </layout>
            <cells>
                <cell id="code" filterOp="like"/>
                <cell id="subject" filterOp="like"/>
                <cell id="orgId" filterOp="eq"/>
                <cell id="eventType" filterOp="eq"/>
                <cell id="status" filterOp="eq"/>
                <cell id="startDateTime" filterOp="date-between"/>
            </cells>
        </form>
    </forms>

    <pages>
        <crud name="main">
            <rowActions x:override="bounded-merge">
                <action id="row-view-button"/>
                <action id="row-update-button" actionType="drawer"/>
                <action id="row-complete-button" label="完成" level="success" icon="fa fa-check">
                    <api url="@mutation:ErpCrmEvent__complete?eventId=$id"/>
                    <confirmText>确认完成此活动？</confirmText>
                    <messages>
                        <success>活动已完成</success>
                    </messages>
                    <visibleOn>${status == 'PLANNED'}</visibleOn>
                </action>
                <action id="row-cancel-button" label="取消" level="danger" icon="fa fa-ban">
                    <api url="@mutation:ErpCrmEvent__cancel?eventId=$id"/>
                    <confirmText>确认取消此活动？</confirmText>
                    <messages>
                        <success>活动已取消</success>
                    </messages>
                    <visibleOn>${status == 'PLANNED'}</visibleOn>
                </action>
                <action id="row-delete-button"/>
            </rowActions>
        </crud>
        <picker name="picker"/>
    </pages>
</view>