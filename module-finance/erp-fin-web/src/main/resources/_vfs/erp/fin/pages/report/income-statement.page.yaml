# 利润表（plan 2026-08-29-1913-2 Phase 1）。
# flux 原生重写：data-source → form → html。
type: page
title: "利润表"
body:
  - type: form
    name: filterForm
    title: ""
    mode: inline
    className: m-3
    body:
      - type: input-number
        name: periodId
        label: "会计期间ID"
        placeholder: "输入期间ID"
        value: 1
      - type: button
        label: "渲染报表"
        onClick:
          action: refreshSource
          targetId: reportHtmlData
      - type: button
        label: "下载 XLSX"
        actionType: download
        api:
          url: /p/ErpFinReport__download
          method: post
          responseType: blob
          data:
            reportName: "income-statement"
            renderType: "xlsx"
            data:
              periodId: "${filterForm?.periodId}"
      - type: button
        label: "下载 PDF"
        actionType: download
        api:
          url: /p/ErpFinReport__download
          method: post
          responseType: blob
          data:
            reportName: "income-statement"
            renderType: "pdf"
            data:
              periodId: "${filterForm?.periodId}"

  - type: data-source
    name: reportHtmlData
    action: ajax
    args:
      url: '@query:ErpFinReport__renderHtml'
      method: post
      data:
        reportName: "income-statement"
        data:
          periodId: "${filterForm?.periodId}"

  - type: html
    className: m-3
    html: "${reportHtmlData ?? ''}"