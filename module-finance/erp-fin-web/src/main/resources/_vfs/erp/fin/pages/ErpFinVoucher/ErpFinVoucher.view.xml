<?xml version="1.0" encoding="UTF-8" ?>
<view xmlns:i18n-en="i18n-en" x:extends="_gen/_ErpFinVoucher.view.xml" x:schema="/nop/schema/xui/xview.xdef" xmlns:x="/nop/schema/xdsl.xdef">
    <controlLib>/erp/xlib/control.xlib</controlLib>

    <grids>
        <grid id="list">
            <cols x:override="bounded-merge">
                <col id="id" mandatory="true" ui:number="true" sortable="true"/>
                <col id="code" mandatory="true" sortable="true"/>
                <col id="voucherType" mandatory="true" sortable="true"/>
                <col id="voucherDate" mandatory="true" sortable="true" align="center"></col>
                <col id="orgId" label="业务组织" i18n-en:label="Business Org"/>
                <col id="isReversed" sortable="true"/>
                <col id="totalDebit" ui:number="true" sortable="true" align="right" domain="amount"></col>
                <col id="totalCredit" ui:number="true" sortable="true" align="right" domain="amount"></col>
                <col id="balanceStatus" label="平衡状态" i18n-en:label="Balance Status" sortable="true" custom="true">
                    <gen-control>
                        <c:script><![CDATA[
                            return { cell: { type: 'text', text: '<span class="label label-${totalDebit == totalCredit ? "success" : "danger"}">' +
                                    '${totalDebit == totalCredit ? "✓ 平衡" : "✗ 不平衡"}</span>' } };
                        ]]></c:script>
                    </gen-control>
                </col>
                <col id="docStatus" mandatory="true" sortable="true">
                    <gen-control>
                        <c:script><![CDATA[
                            const valueProp = dispMeta?.id || 'docStatus';
                            const labelProp = propMeta?.['graphql:labelProp'] || (valueProp + '_label');
                            return {
                                cell: { type: "text", text: '<span class="label label-${' + valueProp
                                    + " == 'ACTIVE' ? 'primary' : 'default'}\" "
                                    + "${" + valueProp
                                    + " == 'CANCELLED' ? \"style='text-decoration:line-through'\" : ''}>"
                                    + "${" + labelProp + "}</span>" }
                            };
                        ]]></c:script>
                    </gen-control>
                </col>
            </cols>
        </grid>
        <grid id="pick-list"/>
    </grids>

    <forms>
        <form id="view" layoutControl="tabs">
            <layout x:override="replace">
=========>baseInfo[基本信息]======
 code[凭证号] voucherType[凭证字]
 voucherDate[凭证日期] voucherNo[凭证编号]
 orgId[核算组织] acctSchemaId[账套]
 periodId[会计期间] docStatus[凭证状态]
==========>posting[过账信息]=========
 @totalDebit[借方合计] @totalCredit[贷方合计]
 postingType[过账类型] isReversed[是否红字冲销凭证]
 reversalOfVoucherId[原冲销凭证ID]
=========>lines[分录行]======
 lines[分录行](2)
==========^audit[审计信息]=========
 remark[备注]
 createdBy[创建人] createTime[创建时间]
 updatedBy[修改人] updateTime[修改时间]
            </layout>
            <cells>
                <cell id="lines">
                    <view path="/erp/fin/pages/ErpFinVoucherLine/ErpFinVoucherLine.view.xml" grid="sub-grid-view"/>
                </cell>
            </cells>
        </form>
        <form id="edit" layoutControl="tabs">
            <layout x:override="replace">
=========>baseInfo[基本信息]======
 code[凭证号] voucherType[凭证字]
 voucherDate[凭证日期] voucherNo[凭证编号]
 orgId[核算组织] acctSchemaId[账套]
 periodId[会计期间] docStatus[凭证状态]
=========>posting[过账信息]=========
 @totalDebit[借方合计] @totalCredit[贷方合计]
 balanceBadge[平衡校验]
=========>lines[分录行]======
 quickTemplate[快捷模板](2)
 lines[分录行](2)
 remark[备注](2)
            </layout>
            <cells>
                <!--
                  借贷平衡实时警示 badge（F16 P1，plan §Phase 1，Phase 0 Explore (a) 降级方案落地）。
                  per Explore (a) 裁决：行级 onEvent→头合计聚合受 xview schema 约束（行 scope 无法干净写头 totalDebit/totalCredit），
                  故采用 autoBalance 按钮扩展（既有，用户点击刷新头合计）+ 本 badge（实时可见性）。
                  visibleOn：仅当存在分录行或合计非 0 时显示，避免新建空凭证时噪声。
                -->
                <cell id="balanceBadge" custom="true" notSubmit="true">
                    <gen-control>
                        <c:script><![CDATA[
                            return {
                                type: 'text',
                                    className: 'mb-0',
                                    text: '<span class="label label-${' + 'totalDebit == totalCredit ? "success" : "danger' + '}"><i class="fa ${' + 'totalDebit == totalCredit ? "fa-check-circle" : "fa-exclamation-triangle' + '}"></i> ${' + 'totalDebit == totalCredit ? "借贷平衡" : "借贷不平衡（需点击下方刷新合计按钮）"' + '}</span>'
                            };
                        ]]></c:script>
                    </gen-control>
                </cell>
                <!--
                  快捷模板 toolbar 按钮（F16 P1，plan §Phase 1）。
                  per Phase 0 Explore (a)+(b)：链接到凭证模板配置页（previewTemplate 按钮在该页调用
                  ErpFinVoucherTemplate__renderTemplate @BizMutation 完成预览）。采用 link 而非内嵌 dialog，
                  因 xview <c:script> 为 XLang（${} 会被编译期插值），复杂 AMIS dialog 的 ${} 运行期表达式
                  需经 page.yaml（YAML）承载——voucher 录入页的快捷入口降级为导航链接，模板预览在配置页落地。
                -->
                <cell id="quickTemplate" custom="true" notSubmit="true">
                    <gen-control>
                        <c:script><![CDATA[
                            return {
                                type: 'button',
                                label: '快捷模板',
                                level: 'primary',
                                icon: 'fa fa-magic',
                                actionType: 'link',
                                link: '/ErpFinVoucherTemplate-main',
                                tooltip: '跳转到凭证模板配置页，使用「预览测试生成凭证」按钮（调用 renderTemplate 表达式引擎）预览分录行'
                            };
                        ]]></c:script>
                    </gen-control>
                </cell>
                <!--
                  借贷自动平衡按钮（ui-patterns §1 line 67）+ 头余额合计刷新：
                  点击计算 totalDebit - totalCredit 差额，填入最后一行（借<贷补借方，反之补贷方）。
                  同时刷新头 totalDebit/totalCredit 实时聚合（行 SUM）。
                  
                  实时聚合 onEvent 在 P0/P1 plan 中已 DEFERRED（P1 plan `2026-07-20-0629-1` §Deferred「头聚合 onEvent
                  在 P0 实际未实施」），本计划对齐既有行为：head totals 由本按钮 + 保存后服务端写入。
                  actionType=custom + script 经 doAction(setValue) 写回 lines + totalDebit + totalCredit。
                -->
                
                <!--
                  lines 子表 cell：引用 ErpFinVoucherLine sub-grid-edit（17 列布局）。
                  xview schema 限制 <view> 仅允许 <data> 子节点，onEvent 头聚合已移到 autoBalance 按钮触发。
                -->
                <cell id="lines">
                    <view path="/erp/fin/pages/ErpFinVoucherLine/ErpFinVoucherLine.view.xml" grid="sub-grid-edit"/>
                </cell>
            </cells>
        </form>
        <form id="add"/>
        <form id="asideFilter" editMode="query" submitOnChange="true">
            <layout>
 voucherType[凭证字] docStatus[凭证状态]
 voucherNo[凭证编号] periodId[会计期间]
 postingType[过账类型] acctSchemaId[账套]
            </layout>
            <cells>
                <cell id="voucherType" filterOp="eq"/>
                <cell id="docStatus" filterOp="eq"/>
                <cell id="voucherNo" filterOp="like"/>
                <cell id="periodId" filterOp="eq"/>
                <cell id="postingType" filterOp="eq"/>
                <cell id="acctSchemaId" filterOp="eq"/>
            </cells>
        </form>
        <form id="query" editMode="query" title="查询条件" i18n-en:title="Query Condition">
            <layout>
 code[凭证号] voucherDate[凭证日期]
            </layout>
            <cells>
                <cell id="code" filterOp="like"/>
                <cell id="voucherDate" filterOp="date-between"/>
            </cells>
        </form>
        <!-- 红字冲销预览（row-reverse-button 的 dialog 载体；plan 2026-08-30-2238-1 flux 原生化重写） -->
        <form id="reversePreview" title="红字冲销预览" i18n-en:title="Red Reverse Preview" size="sm">
            <layout>
previewBody[预览]
</layout>
            <cells>
                <cell id="previewBody" custom="true" notSubmit="true">
                    <gen-control>
                        <c:script><![CDATA[
                            return {
                                type: 'container', direction: 'column', gap: 'sm',
                                body: [
                                    {
                                        type: 'data-source', name: 'previewData', action: 'ajax',
                                        args: { url: '@query:ErpFinVoucher__previewReverseVoucher', method: 'post', data: { voucherId: '${id}' } }
                                    },
                                    {
                                        type: 'text',
                                        text: "凭证号：<b>${previewData.voucherCode}</b>（${previewData.voucherType}）<br/>凭证日期：${previewData.voucherDate} ｜ 分录行数：${previewData.lineCount}<br/>原借方合计：${previewData.totalDebit} ｜ 原贷方合计：${previewData.totalCredit}<br/>红字预估借方：<span style='color:#d9534f'>${previewData.reversedDebit}</span> ｜ 红字预估贷方：<span style='color:#d9534f'>${previewData.reversedCredit}</span><br/>确认后原凭证将被标记为「已红冲」，不可恢复。"
                                    },
                                    {
                                        type: 'button', label: '确认红冲', level: 'danger',
                                        onClick: {
                                            action: 'ajax',
                                            args: { url: '@mutation:ErpFinVoucher__reverseVoucher?voucherId=${id}', method: 'post' },
                                            messages: { success: '凭证已红冲' },
                                            then: [ { action: 'closeSurface' }, { action: 'refreshNearest' } ]
                                        }
                                    }
                                ]
                            };
                        ]]></c:script>
                    </gen-control>
                </cell>
            </cells>
        </form>
    </forms>

    <pages>
        <crud name="main">
            <rowActions x:override="bounded-merge">
                <action id="row-view-button"/>
                <action id="row-update-button" actionType="drawer"/>
                <action id="row-post-button" label="过账" i18n-en:label="Post" level="success" icon="fa fa-check-circle"
                        tooltip="借贷不平衡时无法过账（列表「平衡状态」列显示差额）">
                    <api url="@mutation:ErpFinVoucher__postVoucher?voucherId=$id"/>
                    <confirmText>确认过账此凭证？过账后不可编辑。</confirmText>
                    <messages>
                        <success>凭证已过账</success>
                    </messages>
                    <visibleOn>${docStatus == 'DRAFT'}</visibleOn>
                    <disabledOn>${(totalDebit || 0) != (totalCredit || 0)}</disabledOn>
                </action>
                <action id="row-reverse-button" label="红冲" i18n-en:label="Red Reverse" level="danger" icon="fa fa-undo" actionType="dialog">
                    <visibleOn>${docStatus == 'POSTED' &amp;&amp; !isReversed}</visibleOn>
                    <!-- F7 §3 冲销预览（plan 2026-07-23-1145-2 Phase 2，2026-08-31 flux 原生化重写）：
                         preview-then-confirm 范式：dialog 指向 reversePreview 页（data-source 调 previewReverseVoucher REST），
                         确认按钮调 __reverseVoucher。原 amis onEvent ajax+outputVar+dialog 链在 flux 下不可编译。 -->
                    <dialog page="reversePreview" size="sm"/>
                </action>
                <action id="row-view-source-bills-button" label="源单据" i18n-en:label="Source Bills" level="info" icon="fa fa-reply"
                        tooltip="反查生成本凭证的源业务单据，并提供跳转（经 ErpFinVoucherBillR voucherId 反查）"
                        actionType="drawer">
                    <drawer page="/erp/fin/pages/ErpFinVoucherBillR/bills-by-voucher.page.yaml" size="lg">
                        <data>
                            <voucherId>${id}</voucherId>
                        </data>
                    </drawer>
                </action>
                <action id="row-delete-button"/>
            </rowActions>
        </crud>
        <!-- 红字冲销预览页（row-reverse-button 的 dialog page= 指向此页） -->
        <simple name="reversePreview" form="reversePreview"/>

        <picker name="picker"/>
    </pages>
</view>