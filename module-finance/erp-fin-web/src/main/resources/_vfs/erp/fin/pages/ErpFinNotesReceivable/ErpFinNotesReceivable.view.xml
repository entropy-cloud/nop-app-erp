<?xml version="1.0" encoding="UTF-8" ?>
<view x:extends="_gen/_ErpFinNotesReceivable.view.xml" x:schema="/nop/schema/xui/xview.xdef"
      xmlns:x="/nop/schema/xdsl.xdef">

    <grids>
        <grid id="list">
            <cols x:override="bounded-merge">
                <col id="id" mandatory="true" ui:number="true" sortable="true"/>
                <col id="code" mandatory="true" sortable="true"/>
                <col id="orgId" label="业务组织"/>
                <col id="notesType" mandatory="true" sortable="true"/>
                <col id="notesNo" sortable="true"/>
                <col id="drawerName" sortable="true"/>
                <col id="payeeName" sortable="true"/>
                <col id="issueDate" sortable="true"/>
                <col id="dueDate" sortable="true"/>
                <col id="currencyId" label="币种"/>
                <col id="amountSource" ui:number="true" sortable="true"/>
                <col id="amountFunctional" ui:number="true" sortable="true"/>
                <col id="partnerId" label="出票客户"/>
                <col id="status" sortable="true">
                    <gen-control>
                        <c:script><![CDATA[
                            const valueProp = dispMeta?.id || 'status';
                            const labelProp = propMeta?.['graphql:labelProp'] || (valueProp + '_label');
                            return {
                                type: "tpl",
                                tpl: '<span class="label label-${' + valueProp
                                    + " == 'HONORED' ? 'success' : " + valueProp
                                    + " == 'DISHONORED' ? 'danger' : (" + valueProp
                                    + " == 'RECEIVED' || " + valueProp + " == 'DISCOUNTED' || " + valueProp
                                    + " == 'ENDORSED' || " + valueProp + " == 'COLLECTION_PENDING' ? 'primary' : 'default')}\">"
                                    + "${" + labelProp + "}</span>"
                            };
                        ]]></c:script>
                    </gen-control>
                </col>
            </cols>
        </grid>
        <grid id="pick-list"/>
    </grids>

    <forms>
        <form id="view"/>
        <form id="edit"/>
        <form id="add"/>
    </forms>

    <pages>
        <crud name="main"/>
        <picker name="picker"/>
    </pages>
</view>