type: page
title: "财务概览"
body:
  - type: form
    name: filterForm
    title: ""
    mode: inline
    wrapWithPanel: false
    className: m-3 erp-dashboard-filter
    body:
      - type: input-number
        name: periodId
        label: "会计期间ID"
        placeholder: "留空=全部期间"
        clearable: true
        size: sm
        # 限制期间ID输入框宽度，避免刷新按钮被挤到右侧超出可视区
        style:
          maxWidth: 240px
      - type: button
        label: "刷新"
        icon: "refresh-cw"
        level: primary
        size: sm
        actionType: reload
        target: "kpiService,trendChart,cashFlowCrud"
        style:
          marginLeft: 8px

  - type: service
    name: kpiService
    api:
      url: /graphql
      method: post
      dataType: raw
      data:
        query: "query(${'$'}periodId:String){ ErpFinDashboard__getDashboardKpi(periodId:${'$'}periodId) }"
        variables:
          periodId: "${periodId || null}"
      adaptor: |
        const d = payload.data || {};
        const gql = d.data ? d.data : d;
        const kpi = gql.ErpFinDashboard__getDashboardKpi || {};
        return { status: 0, msg: '', data: kpi };
    body:
      - type: grid
        className: m-3
        gap: 'sm'
        columns:
          - type: wrapper
            className: "bg-white border rounded shadow-sm p-3 erp-kpi-card"
            body:
              - type: tpl
                className: text-muted
                tpl: "<div>本期收入</div>"
              - type: tpl
                tpl: "<div class='h3 m-t-xs m-b-0 text-success'>¥${revenue | round:2}</div>"
          - type: wrapper
            className: "bg-white border rounded shadow-sm p-3 erp-kpi-card"
            body:
              - type: tpl
                className: text-muted
                tpl: "<div>本期支出</div>"
              - type: tpl
                tpl: "<div class='h3 m-t-xs m-b-0 text-danger'>¥${expense | round:2}</div>"
          - type: wrapper
            className: "bg-white border rounded shadow-sm p-3 erp-kpi-card"
            body:
              - type: tpl
                className: text-muted
                tpl: "<div>本期净利润</div>"
              - type: tpl
                tpl: "<div class='h3 m-t-xs m-b-0 font-bold'>¥${netProfit | round:2}</div>"
          - type: wrapper
            className: "bg-white border rounded shadow-sm p-3 erp-kpi-card"
            body:
              - type: tpl
                className: text-muted
                tpl: "<div>银行存款余额</div>"
              - type: tpl
                tpl: "<div class='h3 m-t-xs m-b-0'>¥${bankBalance | round:2}</div>"
          - type: wrapper
            className: "bg-white border rounded shadow-sm p-3 erp-kpi-card"
            body:
              - type: tpl
                className: text-muted
                tpl: "<div>应收余额</div>"
              - type: tpl
                tpl: "<div class='h3 m-t-xs m-b-0 text-primary'>¥${arBalance | round:2}</div>"
          - type: wrapper
            className: "bg-white border rounded shadow-sm p-3 erp-kpi-card"
            body:
              - type: tpl
                className: text-muted
                tpl: "<div>应付余额</div>"
              - type: tpl
                tpl: "<div class='h3 m-t-xs m-b-0 text-warning'>¥${apBalance | round:2}</div>"

  - type: chart
    name: trendChart
    className: m-3 bg-white border rounded shadow-sm p-3 erp-chart-card
    api:
      url: /graphql
      method: post
      dataType: raw
      data:
        query: "query(${'$'}months:Int){ ErpFinDashboard__getDashboardTrend(months:${'$'}months) }"
        variables:
          months: 12
      adaptor: |
        const d = payload.data || {};
        const gql = d.data ? d.data : d;
        const rows = gql.ErpFinDashboard__getDashboardTrend || [];
        return {
          status: 0,
          msg: '',
          data: {
            title: { text: '收支与利润趋势（近12月）' },
            tooltip: { trigger: 'axis' },
            legend: { data: ['收入', '支出', '净利润'] },
            xAxis: { type: 'category', data: rows.map(function(r){return r.month;}) },
            yAxis: { type: 'value' },
            series: [
              { name: '收入', type: 'line', data: rows.map(function(r){return r.revenue;}) },
              { name: '支出', type: 'line', data: rows.map(function(r){return r.expense;}) },
              { name: '净利润', type: 'line', data: rows.map(function(r){return r.netProfit;}) }
            ]
          }
        };

  - type: crud
    name: cashFlowCrud
    className: m-3 erp-alert-card"
    title: "现金流预警"
    syncLocation: false
    headerToolbar: []
    footerToolbar: []
    api:
      url: /graphql
      method: post
      dataType: raw
      data:
        query: "{ ErpFinDashboard__findCashFlowAlert }"
      adaptor: |
        const d = payload.data || {};
        const gql = d.data ? d.data : d;
        const rows = gql.ErpFinDashboard__findCashFlowAlert || [];
        return { status: 0, msg: '', data: { items: rows, count: rows.length } };
    columns:
      - name: alertType
        label: "预警类型"
      - name: bankBalance
        label: "银行余额"
      - name: threshold
        label: "阈值"
      - name: shortfall
        label: "缺口"
