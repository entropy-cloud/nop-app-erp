<?xml version="1.0" encoding="UTF-8" ?>
<view x:extends="_gen/_ErpInvBatch.view.xml" x:schema="/nop/schema/xui/xview.xdef" xmlns:x="/nop/schema/xdsl.xdef">

    <grids>
        <grid id="list"/>
        <grid id="pick-list"/>
    </grids>

    <forms>
        <form id="view"/>
        <form id="edit" x:abstract="true"/>
        <form id="add" x:abstract="true"/>
        <form id="asideFilter" editMode="query" submitOnChange="true">
            <layout>
 batchNo[批号] materialId[物料]
 warehouseId[仓库] status[状态]
            </layout>
            <cells>
                <cell id="batchNo" filterOp="like"/>
                <cell id="materialId" filterOp="eq"/>
                <cell id="warehouseId" filterOp="eq"/>
                <cell id="status" filterOp="eq"/>
            </cells>
        </form>
        <form id="query" editMode="query" title="查询条件">
            <layout>
 batchNo[批号]
            </layout>
            <cells>
                <cell id="batchNo" filterOp="like"/>
            </cells>
        </form>
    </forms>

    <pages>
        <crud name="main">
            <listActions x:override="bounded-merge"/>
            <rowActions x:override="bounded-merge">
                <action id="row-view-button" level="primary" label="@i18n:common.view">
                    <dialog page="view"/>
                </action>
            </rowActions>
        </crud>
        <picker name="picker"/>
    </pages>
</view>
