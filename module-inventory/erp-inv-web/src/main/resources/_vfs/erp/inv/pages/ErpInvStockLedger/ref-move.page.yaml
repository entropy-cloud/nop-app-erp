# fixedProps用于指定关联属性，表示这是一个关联子表的crud页面。在本页面中moveId字段的值固定为传入的值。
# 父页面（ErpInvStockMove 列表行）通过 row-action drawer 打开本页，data.moveId 注入到子表上下文。
x:gen-extends: |
  <web:GenPage view="ErpInvStockLedger.view.xml" page="main" fixedProps="moveId" xpl:lib="/nop/web/xlib/web.xlib" />
